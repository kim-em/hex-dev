# 20260602T164800Z 881e8686 — Issue #2567

## Accomplished

- Claimed directive #2567 (HO-4 / BHKS Theorem 5.2 / `factorFast`
  termination) and confirmed the full leaf theorem still needs the remaining
  D1 prerequisite chain (#6345, #6346).  During rebase, #6344 had landed on
  `origin/main`, and the wrappers were adjusted to the updated Recovery
  surface.
- Added `one_le_factorFastPrecisionCap` in
  `HexBerlekampZassenhausMathlib/BHKSBound.lean`, deriving positivity of the
  public fast precision cap from the packaged BHKS threshold slack.
- Added two HO-4 Recovery capstone wrappers in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalIndicatorsAtPrecisionForCoeffBound_internalCapPositiveAndPrimeLowerBound`
  and
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound_internalCapPositiveAndPrimeLowerBound`.
- These wrappers compose the existing `_internalPrimeLowerBound` capstones with
  `one_le_factorFastPrecisionCap`, removing both caller-side easy obligations:
  `1 ≤ Hex.factorFastPrecisionCap f` and
  `2 ≤ (factorFastCapLiftData f primeData).p`.

## Current frontier

The public `factorFast_terminates : ∀ f : Hex.ZPoly, Hex.factorFast f ≠ none`
theorem still needs the remaining genuine D1 mathematical inputs, especially
per-class Mignotte product congruences (#6345) and final assembly (#6346).
This session narrows the final assembly call surface without changing
algorithmic behavior or theorem statements outside additive wrappers.

## Next step

After #6345 lands, use the new
`_internalCapPositiveAndPrimeLowerBound` canonical-support wrapper in #6346's
final composition so the call site only has to provide cap separation, support
partition shape, expected true factors, and per-class product congruences.

## Blockers

- The full #2567 directive cannot close until #6345 and #6346 land.
- Existing dependency warnings and `sorry` declarations remain outside this
  session's touched files.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound` — green.
- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`
  in touched files — no matches.
