# Progress - 2026-06-16 22:36:28 UTC (session 8398c118, work #7622)

## Accomplished

Claimed and implemented #7622. The slow exhaustive nonzero branch of
`Hex.exhaustiveCoreFactorsWithBound` now lifts at
`Hex.ZPoly.exhaustiveLiftBound core B`, i.e.
`max B (defaultFactorCoeffBound (toMonic core).monic)`, while preserving the
public `B` argument and leaving the `B = 0` branch unchanged.

Threaded the helper-bound lift through the proof-facing surfaces that reason
about factors emitted by `exhaustiveCoreFactorsWithBound`:

- retargeted the executable membership lemmas in
  `HexBerlekampZassenhausMathlib/Basic.lean` to the helper-bound lift;
- split the executable consumer bound from the Hensel lift bound in the core
  coverage and irreducibility wrappers;
- retargeted the outer slow exhaustive Hensel/descent surfaces in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` to helper-bound lift
  data and used the #7621 precision bridge.

Verification:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token scan over touched Lean files: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`

## Current frontier

The executable slow exhaustive branch and the directly connected Mathlib proof
surfaces are aligned on the helper-bound lift. Existing imported warning output
still includes pre-existing `sorry` warnings in older Mathlib bridge files.

## Next step

Downstream issues #7623 and #7638 can now consume the helper-bound slow
exhaustive surface to prove default-bound raw irreducibility and retarget the
remaining slow-exhaustive proofs.

## Blockers

None for this session. The worktree still has a pre-existing unrelated
`.claude/CLAUDE.md` modification; it was not touched or staged.
