# PNT+ direct rational logarithm acceptance

## Accomplished

- Audited the 29 actual pinned `IEANTN/LogTables.lean` sites that remained
  after the natural-log and negative-exponential slices and grouped them by
  provider shape.
- Implemented one bounded Mathlib-free provider for the largest coherent
  non-Li₂ family: fifteen exact rational inputs supporting seventeen source
  declarations, with authenticated dyadic reductions, atanh parameters,
  endpoints, and term counts.
- Added the reusable ordinary-kernel range-reduction/atanh proof, all seventeen
  source-shaped wrappers, and a generic `ProofFrontend` theorem for the tight
  large-shift `32e12` row.
- Added all-row runtime guards, source/reduction/term/cross-row/window/false-cut
  mutations, mathematical falsity and guarded axiom reports.
- Updated both interval SPECs and exactly the seventeen corresponding inventory
  records; source regeneration reports twelve distinct LogTables sites pending.

## Current frontier

The direct rational-logarithm slice is locally complete on top of
`559572fe5e8a8e4dfd7fd377b67a8a7fc9b5d55b`. Focused modules, both complete
experiment libraries, pinned-source inventory verification, structural checks,
trust checks, and freshness are green.

## Next step

Preserve the local commit until the small-prime edge lands, then rebase once
onto the supplied exact main and run the publication review/CI cycle.

## Blockers

None for this slice. The twelve pending LogTables sites require distinct
small/large exponential, nested-log, or pi providers. Li₂ and other admitted
LeanCert-owned public bounds remain deliberately excluded.
