# issue-8542: merge-required lattice-certification conformance case (irreducible SD6)

Session type: feature (issue #8542), branch `issue-8542`.

## Accomplished

- Added `adv/swinnerton_dyer_sd6` (the committed bench SD₆ from
  `bench/HexBench/LatticeSpike.lean`, degree 64, minimal polynomial of
  √2+√3+√5+√7+√11+√13, FLINT-confirmed irreducible over ℤ) to
  `cases_lattice` in
  `conformance/HexBerlekampZassenhaus/EmitFixtures.lean`, emitted
  through the existing sentinel-only `emitHybridTracedCase` from #8538.
  This is the arm-3 (irreducibility-certification) twin of the arm-2
  (split) `adv/swinnerton_dyer_sd5_pair` case; both lattice answer
  paths are now merge-required through the public dispatcher.
- Pinned `modFactorPrime = 19` / `modFactorDegrees = [2]*32`,
  independently verified with python-flint: 19 is the first prime ≥ 3
  with a squarefree SD₆ image (3, 5, 7, 11, 13, 17 all fail; the
  selector is first-suitable and SD₆ is monic), and SD₆ mod 19 has
  exactly 32 degree-2 factors.
- Regenerated `bz.jsonl` (51 → 52 cases; first 204 lines
  byte-identical) and `bz-trace-baseline.json` (diff is exactly the one
  new entry: `tier = "lattice"`, `declined = true`,
  `subsetCandidates = 206368`).
- Rewrote the "Lattice-tier merge requirement" docstring sections to
  cover both lattice arms, with Codex-suggested precision fixes
  (admissible-prime qualifier; per-case half-size-frontier wording).

## Verification

- `bz_flint.py --check`: 102 checked / 0 failures (was 101/0).
- `bz_trace_gate.py`: 52 traces / 0 failures (was 51/0).
- Second emission run byte-for-byte identical to the first.
- Emitted trace: `tier = "lattice"`, `prime = 19`, `r = 32`,
  `subsetCandidates = 206368`, `declined = true`; factor result is the
  single irreducible SD₆ factor, multiplicity 1, scalar 1.
- Emission wallclock 30.9 s locally (was ~23 s; SD₆ costs ~8 s).

## Budget check (issue's load-bearing item)

Main-branch CI data: the "Conformance oracles + BZ gates" step took
~30 s before sd5_pair landed and ~66–69 s after, so sd5_pair's two
emissions cost ~18 s each on CI vs 16 s local — a ≈1.15× CI/local
ratio, not the feared 2–4×. Extrapolating, SD₆ puts the timed second
emission at ~32–40 s against the `timeout 180` cap, so the timeout was
left unchanged. The issue requires recording the fresh measurement from
this PR's own CI run before merge; that number goes in a PR comment
once CI completes.

## Current frontier

Done; PR next. Confirm the PR CI's `timeout 180 lake exe
hexbz_emit_fixtures` step duration and record it.

## Next step

None beyond the CI-timing confirmation. The pinned trace values are the
regression net for future dispatcher/lattice changes.

## Blockers

None.

## Second opinion (Codex)

No correctness blockers. Addressed both low-severity docstring
findings ("modulo every prime" softened to admissible squarefree-image
primes; the ΣC(31,≤15) sentence now distinguishes the sd5_pair
16-block search from sd6's exhaust-all-subsets certification).
Medium process finding accepted: keep `timeout 180` unchanged but
record the PR CI's measured emission duration before merge.
