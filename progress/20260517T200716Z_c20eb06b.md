# HexHensel Phase 6: polish Quadratic.lean public surface

Session UUID prefix: `c20eb06b`. Closes issue #4801.

## Accomplished

First Phase 6 polish slice for `HexHensel/Quadratic.lean` — public
surface only. The file is 2296 lines; lines 35-1778 are private
helpers, out of scope per the issue.

Docstrings tightened:

- `QuadraticLiftResult` struct (line 13): expanded the one-liner to
  spell out the role of each of the four fields (`g` monic updated
  factor, `h` complementary factor, `s`/`t` updated Bezout witnesses)
  and the modulus they satisfy.
- `QuadraticLiftResult.factorError` (line 30, was line 26):
  rewritten to name the role of the residue (driver of the factor
  update achieving `g' * h' ≡ f (mod m^2)`) rather than repeat the
  formula.
- `QuadraticLiftResult.bezoutError` (line 36, was line 30): same —
  named as the residue measured against the new factors that drives
  the witness correction.
- `quadraticHenselStep` (line 1788, was line 1779): extended the
  one-liner with the input list (`f`, `g`, `h`, `s`, `t`), the three
  preconditions consumed by spec theorems (`g` monic, factor
  congruence, Bezout congruence), and the resulting `m^2`
  conjuncts.
- `quadraticHenselStep_factor_congr_mod_base` (line 2222): fixed the
  LaTeX-style `m^2 \ m` notation in the existing multi-line
  docstring. The renderer does not handle backslash in inline code
  cleanly; replaced with "`m^2` beyond what is already determined
  modulo `m`".

Per-deliverable status:

1. Lines 1-33 docstrings: tightened struct, `factorError`,
   `bezoutError`; left `reduceModSquare` abstract per the issue's
   default recommendation.
2. Lines 1779-2295 docstrings: extended `quadraticHenselStep`; fixed
   `_factor_congr_mod_base` LaTeX; left `_factor_spec`,
   `_bezout_spec`, `_spec`, `_monic` unchanged — the existing
   one-liners are adequate.
3. `@[simp]` audit: no public declaration in this file qualifies.
   The four `QuadraticLiftResult` struct projections are
   auto-generated. `reduceModSquare`, `factorError`, `bezoutError`
   are definition-shape `let`-bindings, not rewrites.
   `quadraticHenselStep` is a non-rewrite. The five spec theorems
   carry preconditions (`hm`, `hprod`, `hbez`, `hmonic`) and so are
   not unconditional canonical-form rewrites.
4. Private helper audit (lines 35-1778): every private declaration
   is named after the algebraic operation, congruence shape, or
   step it implements (`addModSquare`, `mulModSquare`,
   `divModMonicModSquare`, `divModMonicModSquare_zero_mod_base`,
   `quadraticHenselStep_bezout_correction_algebra`,
   `quadraticHenselStep_factor_first_order_exact`, etc.). No name
   was found that fails to telegraph what it proves. The SPEC
   non-obvious-private-helper rule is satisfied without changes.
5. No definition, theorem statement, argument order, namespace,
   visibility, or proof changes.
6. No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry`.

## Verification

- `lake build HexHensel.Quadratic` — pass.
- `lake build HexHensel` — pass.
- `lake build` — pass (244 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `rg "sorry|axiom|native_decide|TODO|FIXME" HexHensel/Quadratic.lean`
  diff vs `origin/main` — no new entries.

## Current frontier

`HexHensel/Quadratic.lean` public surface is documented. The private
body (lines 35-1778) remains a candidate for a dedicated Phase 6
slice if a future audit surfaces gaps, but the current naming pattern
is sufficient under the SPEC rule.

## Next step

Other untouched Phase 6 surfaces of HexHensel:

- `HexHensel/Linear.lean` surfaces 2-4 (issue #4800 covers surface 2;
  surfaces 3-4 not yet decomposed).
- `HexHensel/QuadraticMultifactor.lean` — not yet polished.

## Blockers

None.
