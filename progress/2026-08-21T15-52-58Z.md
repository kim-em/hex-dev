# Supported source closure and tactic frontier

## Accomplished

- Made version-zero frontend facts source-driven: selected source rows retain
  caller intervals and computed rows are seeded with `whole`.
- Proved that exact source containment discharges every initial fact, and added
  source-facing lower, upper, conjunction, and singleton closure lemmas.
- Converted the shared arithmetic frontend canary into fully discharged
  ordinary theorems with no caller-supplied per-node model or initial-fact
  premise.
- Clarified that recursive depth bounds nesting but not construction or a full
  traversal of an already-built branching term.

## Current frontier

- The historical forward Meta client is being audited against the current
  supported Frontend/Rule/Proof APIs and the new source-driven closure.

## Next step

- Reconcile the smallest sound `interval`, `interval?`, and nonmutating
  `interval_bound` vertical, then run the complete affected build and static
  gates.

## Blockers

- None in the source-driven model bridge. Generic search-to-recipe and split
  replay remain separate unsupported work and must not be implied by this
  forward vertical.
