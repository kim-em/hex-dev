# 2026-05-11 00:54 UTC — Phase 2 re-review of HexBerlekampZassenhaus (9f1c6e17)

## Accomplished

- Claimed and executed issue #3154 (Phase 2 re-review after the post-#2637
  `Factorization` SPEC reshape).
- Ran the issue's verification suite against `HexBerlekampZassenhaus`:
  `lake build`, conformance build, `hexbz_bench list`,
  `scripts/check_dag.py`, `scripts/status.py`, and the banned-marker /
  scaffold-language greps. All pass.
- Confirmed the three remaining `sorry`s in `Basic.lean`
  (`bhksLatticeBasis_independent`, `isIrreducible_iff`,
  `factor_product_of_bound`) are all theorem-level Group B / Group C
  obligations covered by the Mathlib bridge.
- Identified one Phase 2 wrong-shape finding: the public `factor`
  combinator (`Basic.lean:2589`) is defined as
  `factorWithBound f (defaultFactorCoeffBound f)` instead of the SPEC's
  `(factorFast f).getD (factorSlow f)`. Same combinator shape, but the
  fast-path precision is Mignotte rather than `factorFastPrecisionCap f
  = max bhksBound Mignotte`. PR #3143 introduced the divergence for
  bench tractability; human-oversight #2637 pins the SPEC equation, so
  the divergence is blocking Phase 2 entry.
- Filed follow-up #3210 ("HexBZ: align public factor combinator with
  SPEC equation"), describing both available remedies (restore the SPEC
  equation or update the SPEC under #2637's owner).
- Refreshed `status/hex-berlekamp-zassenhaus.scaffolding-reviewed` to
  record the review against the current SPEC, the finding, and the
  follow-up.
- Did NOT bump `libraries.yml.HexBerlekampZassenhaus.done_through`;
  it stays at 1 pending #3210.

## Current frontier

- HexBerlekampZassenhaus stays at Phase 1 (`done_through: 1`).
- One blocking Phase 2 gap is filed (#3210); everything else in the
  audit targets is clean.

## Next step

- A worker claims #3210 and either edits `factor` to match the SPEC
  equation or coordinates a SPEC amendment under #2637. After that
  resolves, a follow-up Phase 2 review session can bump
  `done_through` from 1 to 2.

## Blockers

- None for this session.
