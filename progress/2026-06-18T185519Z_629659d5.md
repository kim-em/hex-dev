# 2026-06-18T185519Z — feature session (629659d5)

## Accomplished

- **#7975 (fast raw irreducibility substrate) — skipped with premise-check
  diagnosis.** Investigated before coding. The requested unconditional
  `factorFastRaw_irreducible_of_some` cannot be built from existing
  infrastructure: the conditional endpoints (`rawIrreducible_of_cut`,
  `rawIrreducible_of_forwardInputs`) carry two *soundness* hypotheses —
  `CutProjectionHypotheses` (the BHKS `W ⊆ L'` forward inclusion) and the
  support-partition count equality — that have **no producer** from the
  executable success `factorFastFactorsWithBound … = some`. The cut
  field's own docstring (Lattice.lean:1810) flags it as future "B4/B5
  work". Recovery.lean's `factorFast_terminates` family runs the opposite
  direction (consumes these to prove non-failure). Executable success only
  certifies `product == f`, which does not imply per-factor irreducibility.
  This is the same gap as open directive #2564. Posted a detailed comment
  and `coordination skip`ped for planner re-triage. The slow disjuncts are
  unconditional only because trial division (`exhaustiveIntegerTrial`) is
  self-certifying; the fast/LLL path has no such self-certification yet.

- **#7963 (HexGF2 Phase 6 grind annotation pass) — completed.** Added
  `@[simp, grind =]` to 25 equation-shaped public characterising lemmas in
  `HexGF2/Basic.lean` (coeff_*, words_*, degree?/degree_*, ofUInt64_*,
  ofWords_empty, isZero_iff_eq_zero/isZero_zero/isZero_monomial_eq_false,
  and the GF(2) add identities add_self/zero_add/add_zero/
  add_add_cancel_left/right, plus coeff_shiftLeft/coeff_mulXk).

## Decisions

- Did NOT blanket-annotate all 38 `@[simp]` lemmas. Excluded: derived
  coeff convenience lemmas subsumed by the core rewrites
  (coeff_add_self_false, coeff_add_zero_{left,right}_bool); niche literal
  cases (words_ofWords_single_zero/pair_zero, words_ofWords_single_nonzero
  — the last being a conditional rewrite); internal Array plumbing
  (normalizeWords_get?_getD, coeffWords_normalizeWords, xorWords_size); the
  `≠` fact monomial_ne_zero (not an equation); the implication
  degree_eq_of_degree?_eq_some; and two isZero iffs sharing the
  `p.isZero = true` LHS with the canonical isZero_iff_eq_zero (kept only
  the canonical one to avoid same-LHS rewrite conflict).
- **Payoff justification:** no in-file proof shortens (Basic.lean has zero
  `grind` call sites; its proofs are already `rfl`/`simp` one-liners). The
  benefit is downstream `grind` availability — `HexGF2/Multiply.lean`,
  `Field.lean`, `Euclid.lean`, `Clmul.lean`, and `Conformance.lean` all
  have existing `grind` sites that can now use the packed-`GF2Poly`
  representation rewrites. Full `lake build HexGF2` stayed green (52 jobs);
  `HexGF2.Basic` builds in 15s, no slowdown, no new linter warnings.

## Current frontier

- HexGF2 Phase 6 grind annotation pass landed; scope kept to Basic.lean.

## Next step

- A planner should re-triage #7975 (now `replan`): the real prerequisite is
  the B4/B5 cut-soundness lemma (BHKS norm bound + Gram-Schmidt cut)
  producing `CutProjectionHypotheses` + partition-count equality from the
  executable success, or its replacement under directive #2564. This should
  be its own issue, not a discharge pass over the existing endpoints.

## Blockers

- None for #7963. #7975 is blocked on unproven recombination soundness
  (B4/B5 / #2564), not on this session.
