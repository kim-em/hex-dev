# Session 20260511T000439Z_c2d1d0d9 — feat #3166

## Accomplished

- Migrated the loop-local proof-only Yun predicates and supporting list /
  repeated-tail lemmas onto the level-indexed `yunFactorsWithLevel` helper
  from #3172.  Added (all `private`):
  - `yunFactorsWithLevel_reverse_append`
  - `yunFactorsWithLevel_repeated_eq_nil`
  - `yunFactorsWithLevel_repeated_dvd_repeated_of_acc`
  - `yunFactorsWithLevel_repeated_dvd_repeated`
  - `yunFactorsCurrentTailCoprimeWithLevel`
  - `yunFactorsPairwiseReadyWithLevel`
  - `yunFactorsPairwiseReadyWithLevel_step`
  - `yunFactorsPairwiseReadyWithLevel_succ_of_current_tail`
  - `yunFactorsPairwiseInvariantWithLevel`
  - `yunFactorsPairwiseInvariantWithLevel_of_derivative_split`
  - `yunFactorsPairwiseInvariantWithLevel_step`
  - `yunFactorsWithLevel_factors_squareFree_of_steps`
  - `yunFactorsWithLevel_factors_squareFree_of_derivative_split`
  - `yunFactorsWithLevel_pairwise_coprime_nil_of_ready`
  - `yunFactorsWithLevel_pairwise_coprime_nil_of_invariant`
  - `yunFactorsWithLevel_pairwise_coprime_nil`
- The new theorems carry the factor multiplicity as `base * level`, stepping
  to `base * (level + 1)` on each recursive call, matching the executable
  shape of `yunFactorsWithLevel` from #3172.
- Existing `yunFactors`/`yunFactorsPairwise*` definitions and the public
  `yunFactors c w multiplicity fuel` / `yunFactorsContribution c w multiplicity fuel`
  entry points are untouched; existing consumers
  (`squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant`,
  `yunFactors_factors_squareFree_of_derivative_split`, etc.) still build.
- `lake build HexPolyFp.SquareFree` and `lake build HexPolyFp` are clean
  except for the two pre-existing `sorry`s at lines 2777 and 4543 (was 4282
  before insertion).  `python3 scripts/check_dag.py` passes.

## Current frontier

Downstream callers wiring the level form into the `pthRoot`/Frobenius outer
loop (`squareFreeAuxRev`) still consume the original `yunFactors` predicates;
the level twins are in place to support migrating those consumers in a
successor.

## Next step

A follow-up could (a) replace the inner Yun call site in `squareFreeAuxRev`
with `yunFactorsWithLevel` at `base := multiplicity` and rewire the existing
pairwise-coprime infrastructure, or (b) consume the level twins from the
characteristic-2 / p-power proofs that #3122 needs.  Both are explicitly
out of scope for this issue.

## Blockers

None.
