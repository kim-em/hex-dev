# Session 0c8e14fd — work #4039

## Accomplished

- Claimed #3994, found it stale because #4027 had been closed as a partial/replan and replaced by open #4039, then skipped #3994 with that reason.
- Claimed #4039 and added the current-column lower-triangle capture layer in `HexGramSchmidt/Int.lean`:
  - `getArrayEntry_foldl_setArrayEntry_row_notMem`
  - `getArrayEntry_foldl_setArrayEntry_col_mem`
  - `getArrayEntry_writeScaledColumn_below`
  - `getArrayEntry_scaledCoeffArrayLoop_capture_current_col`
  - `scaledCoeffArrayLoop_lower_matches_current_step`
- Created successor issue #4071 for the remaining full recursive lower-triangle sync.
- Verified `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested marker grep.

## Current frontier

#4039 now has the current-column capture theorem and matrix-state wrapper needed for the `j = state.step` case. The full arbitrary-column loop relation still needs a separate induction that combines this capture case with the existing processed-column preservation lemma and the early-singular zero-tail behavior.

## Next step

Land this partial PR for #4039, then handle #4071 to prove `scaledCoeffArrayLoop_lower_matches` (or equivalent) from the new capture wrapper plus `getArrayEntry_scaledCoeffArrayLoop_preserve_col_before_step`.

## Blockers

None for the landed partial. Completing #4039 outright is larger than this session because the remaining theorem is the full recursive lower-triangle sync, not just the missing current-column capture helper.
