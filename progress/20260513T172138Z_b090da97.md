# Session b090da97 — issue #3491

## Accomplished

- Claimed #3491 (HexMatrix: add Mathlib-free local adjugate identities for
  determinant minors). Issue #3252 was the oldest unclaimed but had just
  been claimed by another agent; fell back to the next oldest.
- Added a Mathlib-free `adjugate` matrix definition to
  `HexMatrix/Determinant.lean`, plus the entrywise `mul_adjugate_apply`
  identity `(M * adjugate M)[i][j] = if i = j then det M else 0`.
- Built the proof on three new helpers: `setRow`, `cofactor_setRow_self`
  (cofactor along the replaced row is unchanged), and
  `foldl_alien_cofactor_eq_zero` (alien cofactor sum vanishes by reducing
  to `det_eq_zero_of_row_eq` on the replaced-row matrix).
- Exposed Desnanot-Jacobi-shaped corner simplifications:
  `adjugate_zero_zero`, `adjugate_last_last`, plus the general
  `adjugate_eq_cofactorSign_mul_deleteRowCol`. Column-`0` and
  column-`last` views (`mul_adjugate_apply_zero/_last`) are direct
  specializations of `mul_adjugate_apply`.
- Verified with `lake build HexMatrix.Determinant`, `lake build HexMatrix`,
  `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, and
  the banned-marker scan (only the pre-existing Bareiss `sorry` remains).

## Current frontier

The local adjugate surface is now available inside `HexMatrix`. Parent
issue #3484 (Desnanot-Jacobi assembly) can use these to mirror the
`auxAdjugateM` proof shape from
`HexMatrixMathlib/Determinant/DesnanotJacobi.lean` without importing
Mathlib's adjugate facts.

## Next step

Open the PR closing #3491 and let CI/auto-merge integrate. The
Desnanot-Jacobi assembly itself remains the next step for #3484.

## Blockers

- None for #3491.
- Pre-existing `.claude/CLAUDE.md` worktree modification was left
  untouched and is not part of this PR.
