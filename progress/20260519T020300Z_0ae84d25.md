# 20260519T020300Z — planner cycle

Session type: `/plan` planner. `POD_QUEUE_DEFICIT=2` at entry
(1 unclaimed → target ≥ 3).

## Accomplished

- Set auto-merge on the four open HexPolyFp coeff_*_ring migration
  PRs (#5198, #5200, #5202, #5203) — all `MERGEABLE` with no failed
  checks; base-branch policy blocks immediate merge because CI is
  still IN_PROGRESS, so `--auto` will land them on green.
- Skipped manual merge of PR #2656 (`spec: add hex-real-roots and
  hex-sturm (status: planned)`): owner-authored speculative SPEC
  PR, not agent work product. Leaving for the owner.
- No `replan`-labelled candidates and no broken-PR fix issues to
  file (no PRs reported by `coordination orient` under "PRs
  needing attention").
- Verified open `agent-plan` issue bodies for overlap. The BHKS
  step-3 / step-4 follow-ups (`bhks_bad_vector_norm_sq_gt_cutRadiusSq4_…`
  and `factorFast_terminates`) are blocked on #5201 and #5204 and
  therefore not eligible under the "no transitive blocking" rule.
- Audited the A2 abstract-bound propagation chain on the monic
  `_some_factor_associated_` surface: the matching `_of_bound`
  siblings on both the monic wrapper and its `_primitive_pos_lc_core`
  / `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  consumers are already landed. No fresh propagation issue to file
  on this surface.
- Audited remaining call sites of the explicit-zero-law `coeff_add
  _ _ _ X` / `coeff_sub _ _ _ X` form across the codebase. Found
  three not-yet-migrated files:
  - `HexBerlekamp/RabinSoundness.lean` (34 sites, all ZMod64 p, the
    typeclass-instance prerequisites are already in place).
  - `HexHensel/Linear.lean` (8 sites, but coefficients are `Int`
    and `AddZeroLaw Int` / `SubZeroLaw Int` typeclass instances do
    not yet exist — blocked on instance-side work, not filed this
    cycle).
  - `HexGfqRing/Operations.lean` (2 sites, ZMod64 p — left for a
    future cycle, too small in isolation).

Two new atomic agent-plan issues filed (deficit = 2):

1. **#5205 — `feat: add HexGfqField toQuotient_div and
   toQuotient_zpow simp wrappers`**: identified as the planner
   follow-up in `progress/20260519T011620Z_6b55bdcd.md`; small
   unblocked feature, ≤ 50 LoC, completes the `toQuotient_*` family
   so that downstream `repr_div` / `repr_zpow` wrappers become
   possible.
2. **#5206 — `review: migrate HexBerlekamp/RabinSoundness.lean to
   coeff_*_ring simp wrappers`**: sibling of #5180–#5183 for a new
   file; 34 sites, mechanical, all over ZMod64 p, plan flags worker
   decomposition if the diff exceeds the inventoried ceiling.

`coordination set-target` not invoked — active development, full
queue refill is the cycle goal.

## Current frontier

Unclaimed queue is now 3 (#5204, #5205, #5206), matching
`POD_MIN_QUEUE=3`. The HexPolyFp coeff_*_ring migration cluster is
draining (four auto-merges pending), and the next natural review
extensions are `HexBerlekamp/RabinSoundness.lean` (#5206 this cycle)
followed by `HexGfqRing/Operations.lean` and eventually
`HexHensel/Linear.lean` once `AddZeroLaw Int` / `SubZeroLaw Int`
instances are filed.

## Next step

Next planner cycle (after the four auto-merges complete and the
HexPolyFp cluster fully drains): consider filing
- a feature issue for `AddZeroLaw Int` / `SubZeroLaw Int` typeclass
  instances (precondition for migrating `HexHensel/Linear.lean`),
- the tiny `HexGfqRing/Operations.lean` migration (review, 2 sites),
- the `repr_div` / `repr_zpow` simp wrappers (review, blocked on
  #5205).

## Blockers

None for this planner cycle. PR #2656 left for owner decision.
