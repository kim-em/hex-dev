# 21836cb9 - work #5991

## Accomplished

- Claimed #5991 and audited the named helper surface in `HexGF2/Field.lean`.
- Moved the Boolean coefficient-list construction helpers under `GF2Poly.Internal`.
- Moved quotient divided-difference/root-count helpers and proof-facing quotient product/power helpers under `GF2nPoly.Internal`, while keeping quotient-field facts such as `elements`, `mem_elements`, cardinality, Frobenius fixed-point, and irreducibility-facing theorems public.
- Updated stale `HexGF2/Irreducibility.lean` documentation to point to `HexGF2/RabinSoundness.lean` and its completed irreducibility theorems.
- Verified with `lake build HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and a touched-file diff grep for forbidden `sorry`/`axiom`/`native_decide` additions.

## Current frontier

#5991 is ready for PR. The public quotient-field surface is narrower, but the executable behavior and theorem statements consumed by Rabin soundness remain intact.

## Next step

Open the PR for #5991 and let CI/auto-merge handle integration.

## Blockers

- A pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and is not part of this work.
