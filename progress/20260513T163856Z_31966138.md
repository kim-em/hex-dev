# Issue #3762 — RREF rank bridge

## Accomplished

- Claimed #3762 and selected it after human-oversight items were replan-gated
  or dependency-blocked.
- Replaced the `HexMatrixMathlib/RankSpanNullspace.lean` `rank_eq` proof hole
  with an `IsRREF`-scoped theorem proved via computed nullspace basis
  independence and rank-nullity.
- Made the existing `Hex.Matrix.IsRREF.nullspace_get_free` and
  `nullspace_get_free_ne` coordinate lemmas public so the Mathlib bridge does
  not reach through private RREF internals.
- Verified `lake build HexMatrix.RREF`, `lake build
  HexMatrixMathlib.RankSpanNullspace`, `lake build HexMatrixMathlib`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the no-new-placeholder
  grep for the touched files.

## Current frontier

The RREF rank bridge is discharged on the correct RREF surface. The broader
echelon-form statement was too weak because `IsEchelonForm` does not require
nonzero pivots.

## Next step

Create the PR for #3762 and let CI validate the narrowed theorem surface
against downstream imports.

## Blockers

- None.
