# 20260603T011549Z work #6416

## Accomplished

- Claimed issue #6416 and added two additive constructors in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  - `BHKS.FactorFastCapSeparationInputs.ofBridgeDataWithComparison`, packaging
    the actual-cap `BadVectorBridgeData`, cut hypotheses, named analytic
    comparison, cap dominance, prime-choice equation, and stored normalized-core
    precision equation into a closed `FactorFastCapSeparationInputs` record.
  - `BHKS.FactorFastCapSeparationInputs.ofBridgeData`, which starts from the
    raw strict Hadamard/l2norm inequality and uses
    `FactorFastCapLiftAnalyticComparison.ofBridgeData`, deriving the required
    prime positivity from `choose_eq`.

## Current frontier

The cap-side package now has a constructor surface that matches the existing
consumer wrappers, including
`FactorFastCapSeparationInputs.latticeIdentification` and
`factorFast_ne_none_of_factorFastCapSeparationInputsAndCanonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`.
The constructor deliberately threads the normalized-core precision equation as
an input because `factorFastCapLiftData f primeData` is built from
`factorFastPrecisionCap f`, while the consumer field is stated with
`factorFastPrecisionCap (normalizeForFactor f).squareFreeCore`.

## Next step

Construct the remaining mathematical producers that feed this package for the
canonical true supports: the actual `BadVectorBridgeData`, cut hypotheses,
cap-dominance proof, normalized-core precision equation, and strict analytic
comparison for the nontrivial fast branch.

## Blockers

No local build blocker.  A future producer still needs a theorem or branch fact
connecting the actual stored cap-lift precision to the normalized-core
precision equation required by the packaged consumers.

`/reflect` was unavailable in this non-interactive session; `coordination
reflect` is not a supported helper command.

## Verification

- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/Recovery.lean HexBerlekampZassenhausMathlib/BadVector.lean HexBerlekampZassenhausMathlib/TerminationBound.lean`
  returned no matches.
