# 20260517T104313Z — work session 8c2b6b87 (#4638/#4643 replan, #4649)

## Accomplished

Claimed #4638 and decomposed it into ordered successor issues #4643, #4644,
#4646, #4647, and #4648, then marked #4638 `replan` with the required
`Decomposed into ...` breadcrumb. Claimed #4643 next and found the requested
primitive-pos-lc recovery theorem shape is under-specified from the current
scaled modular recovery hypothesis; left a concrete coefficient-level blocker
comment and skipped #4643 for replan.

Claimed and completed #4649. Moved the row-add determinant helper cluster out
of `HexGramSchmidt/Int.lean` and into `HexGramSchmidtMathlib/Int.lean`,
preserving names and namespace `Hex.GramSchmidt.Int`. The relocated cluster
includes `scaledCoeffMatrix_rowAdd_pivot_det`,
`leadingGramMatrixInt_rowAdd_outside`, `leadingGramMatrixInt_rowAdd_inside`,
and their bridge-local row-add/dot support. Removed the now bridge-only
`rowAdd_get_rect` helper from the Mathlib-free file; kept Mathlib-free
`dot_comm_int` because row-swap helpers still use it.

Verification for #4649:

- `lake build HexGramSchmidt.Int` passed
- `lake build HexGramSchmidtMathlib.Int` passed
- `lake build HexGramSchmidt HexGramSchmidtMathlib` passed
- `python3 scripts/check_dag.py` passed
- `git diff --check` passed
- `rg -n 'scaledCoeffMatrix_rowAdd_pivot_det|leadingGramMatrixInt_rowAdd_(inside|outside)' HexGramSchmidt/Int.lean` returned no hits
- `rg -n '^import HexGramSchmidtMathlib|Matrix\.bareiss_eq_det|HexMatrixMathlib\.bareiss_eq_det' HexGramSchmidt HexLLL` returned no hits
- Touched-file `sorry`/forbidden-token inventory matches `origin/main`

## Current frontier

#4649 is ready for PR. The #4638 chain needs planner attention before its
successor issues proceed, because the first generated sub-issue #4643 is
blocked by the scaled-vs-unscaled recovery mismatch.

## Next step

Publish the #4649 relocation PR. For the #4638 line, replan around either a
scaled executable candidate for primitive non-monic cores or stronger
hypotheses relating the unscaled lifted product to the represented integer
factor.

## Blockers

No blockers for #4649. #4643 is blocked on a design correction to the primitive
non-monic recombination recovery statement.
