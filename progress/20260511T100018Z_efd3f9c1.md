# 2026-05-11 10:00 UTC — work/feature session (efd3f9c1)

## Accomplished

- Claimed #3252, verified it was stale, and skipped it for replan:
  #3278 had been closed only because it was decomposed into #3315/#3316,
  and the public McCoy annihilator theorem required by #3252 is not on
  `main`.
- Claimed #3327 to repair PR #3309's Linux bench gate.
- Diagnosed the failed HexLLL Isabelle bench cases:
  - PR #3309's merge commit passed `lake exe hexlll_bench verify` locally
    with all 32 benchmarks ok.
  - The GitHub rerun showed `scripts/oracle/setup_lll_isabelle.sh` hit a
    Zenodo HTTP 504 before HexLLL verify.
  - The CI line `export HEX_LLL_ISABELLE_SVP="$(...)"` masked the setup
    failure, so the benchmark child processes failed later with opaque
    `child exited with code 1` errors.
- Patched `scripts/oracle/setup_lll_isabelle.sh` to use the stable
  `/records/2636367/...` Zenodo URL and curl retries.
- Patched `.github/workflows/ci.yml` to split assignment from `export`, so
  a setup-script failure fails the bench step immediately.
- Posted corrected diagnostic comments on #3327 and PR #3309.

## Current frontier

- The oracle setup script now survives a transient 504 in a fresh local
  cache and builds `svp_verified`.
- Local `lake exe hexlll_bench verify` passes all 32 benchmarks.

## Next step

- Open the PR for #3327 and let CI confirm the patched workflow restores
  the Linux build gate for PR #3309 and future runs.

## Blockers

- None for this repair PR. The existing unrelated `.claude/CLAUDE.md`
  modification remains untouched in the worktree.
