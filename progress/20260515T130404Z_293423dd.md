# Issue #4231

## Accomplished

- Claimed issue #4231.
- Added
  `HexBerlekampZassenhausMathlib.exhaustiveCoreFactorsWithBound_mem_of_recombinationSearchMod_first_success`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
- The theorem composes the existing first-success witness for
  `recombinationSearchMod` with the public
  `exhaustiveCoreFactorsWithBound` membership bridge, so downstream coverage
  assembly can avoid separately unpacking the fixed-lift search result.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - affected-file search for `sorry`, `axiom`, `native_decide`, `TODO`, and
    `FIXME`

## Current frontier

- The public-wrapper step from a first successful split to membership in
  `exhaustiveCoreFactorsWithBound` is now packaged.
- The main #4231 recursive/rest correspondence and final
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  theorem remain open.
- Existing repository warnings remain unchanged, including pre-existing
  `sorry` warnings in BZ/Mathlib dependency files.

## Next step

- Prove the recursive invariant for `recombinationSearchModAux` after a first
  split is consumed, then use this membership bridge to assemble the public
  coverage theorem.

## Blockers

- None for the partial substrate landed in this session.
