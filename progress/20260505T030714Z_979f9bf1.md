# Work session: HexBerlekamp distinctDegreeFactor product preservation

## Accomplished

- Claimed #2225, found scope too large for one session because the
  bucket-invariant theorem requires a Frobenius-power successor lemma
  (`powModMonic (frobeniusXPowMod f hmonic k) f hmonic p =
  frobeniusXPowMod f hmonic (k + 1)`) that does not yet exist in
  `HexPolyFp/Frobenius.lean`. Decomposed into:
  - #2232 — product preservation (this session's claim).
  - #2233 — Frobenius successor step lemma in `HexPolyFp/Frobenius.lean`.
  - #2234 — bucket invariants, depends on #2233.
  Skipped #2225 with a `Decomposed into ...` breadcrumb.
- Claimed #2232 and proved `prod_distinctDegreeFactor` in
  `HexBerlekamp/DistinctDegree.lean` by induction on the loop fuel.
- Added private helpers:
  - `factorProduct_append`,
  - `degreeBucketProduct_append_some`,
  - `degreeBucketProduct_appendBucket?_some` /
    `degreeBucketProduct_appendBucket?_none`,
  - `mul_div_eq_of_dvd`,
  - `candidate_mul_div_residual`,
  - `distinctDegreeLoop_product_invariant`.
- Added `[ZMod64.PrimeModulus p]` to `prod_distinctDegreeFactor`'s
  instance signature; the gcd/division surfaces require it via the
  existing `instDivModLawsZMod64Fp` and `GcdLaws` instances.
- Verified:
  - `lake build HexBerlekamp.DistinctDegree`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `HexBerlekamp/DistinctDegree.lean` only sorry remaining is the
    `distinctDegreeFactor_bucket_invariants` theorem covered by #2234.

## Current frontier

The product half of the original #2225 is closed by this PR. The
bucket-invariant half waits on:

1. #2233 (Frobenius successor lemma), then
2. #2234 (bucket invariants using the lemma from #2233).

## Next step

Whoever picks up #2233 unblocks #2234, after which both surfaces in
`HexBerlekamp.DistinctDegree.lean` are sorry-free.

## Blockers

- None for #2232.
- #2234 is explicitly blocked on #2233.
