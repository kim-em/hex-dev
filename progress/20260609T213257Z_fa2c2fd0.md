# 20260609T213257Z (#6715)

## Accomplished

Claimed #6715 and proved the `HexPolyZ/Mignotte.lean` lower-square bound for
the executable Newton square-root iterator.

Added a private `sqrtGap n x := x - n / x` proof substrate showing that, while
the current iterate remains above the square-root bound, one Newton step halves
this gap. From initial state `x = n`, `n.log2 + 1` steps force a contradiction
against `n < 2 ^ (n.log2 + 1)` unless the square bound has already been reached.

Exposed:

```lean
theorem Hex.ZPoly.floorSqrt_sq_le (n : Nat) :
    Hex.ZPoly.floorSqrt n * Hex.ZPoly.floorSqrt n ≤ n
```

The executable definitions are unchanged.

## Current frontier

The requested `floorSqrt_sq_le` theorem is closed. The new proof is stronger
than the previous near-envelope route: it proves the phase-one `n.log2 + 1`
fuel budget already reaches the lower-square bound.

## Next step

Use `Hex.ZPoly.floorSqrt_sq_le` in #6630 to prove the
`coeffL2NormBound^2 ≤ 2 * coeffNormSq` bridge and then thread the BHKS
`h_coeff` discharge.

## Blockers

None for this issue.

Verification performed:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Second-opinion review found no correctness issue; it noted the new phase-one
  proof dominates the older near-envelope route, which can be retired in a
  later cleanup if desired.
