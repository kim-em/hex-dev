## Accomplished

Claimed #2988 and added the private degree-forcing support in
`HexPolyFp/SquareFree.lean`:

- `ne_zero_of_isZero_false`
- `mul_ne_zero_of_ne_zero`
- `powLinear_ne_zero` / `pow_ne_zero`
- `powLinear_degree?_getD` / `pow_degree?_getD`
- `dvd_one_of_all_powers_dvd_nonzero`

The final helper proves that if every local `pow d n` divides a nonzero
polynomial `g`, then `d ∣ 1`, using the existing finite-field product-degree
theorem and the same nonzero constant divisor finish as
`dvd_one_of_mul_right_dvd_right`.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

#2988 is complete. The marker scan still reports the four pre-existing
`SquareFree.lean` sorries at the shifted line numbers; this session did not add
or remove those sorries.

## Next step

Create the PR for #2988. Downstream #2990 can use this helper together with the
derivative power-induction work from #2987 once both land.

## Blockers

None for #2988. The worktree had an unrelated pre-existing modification to
`.claude/CLAUDE.md`; it was left untouched and uncommitted.
