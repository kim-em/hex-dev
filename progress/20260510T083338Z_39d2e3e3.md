# 20260510T083338Z - Issue #2958

## Accomplished

Claimed #2958 and added the Mathlib-facing BHKS projected-lattice interface in
`HexBerlekampZassenhausMathlib/Lattice.lean`.

The new module packages:

- executable `Hex.BhksProjectedRows` as integer and rational Mathlib row-span
  matrices;
- the integer projected row span `L'`;
- the rational row space consumed by the RREF equivalence-class stage;
- true-factor support indicator vectors and the indicator lattice `W`;
- a cut-projection hypothesis record and a proved `W <= L'` theorem from those
  generator hypotheses.

The root `HexBerlekampZassenhausMathlib.lean` now imports the new module.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.Lattice`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|sorry' HexBerlekampZassenhausMathlib/Lattice.lean HexBerlekampZassenhausMathlib.lean`

The grep reported no new `axiom`, `native_decide`, or `sorry` in the files
touched for this issue.

## Current frontier

The executable projected rows now have stable proof-facing `L'` and `W`
interfaces.  The next HO-4 step can instantiate the cut-projection hypothesis
from the BHKS norm bound and Gram-Schmidt cut soundness, then feed the same
projected-row data into bad-vector and equivalence-class arguments.

## Next step

Continue with the blocked successor #2959 once this PR lands, or prove the
upstream B4/B5 facts needed to populate `CutProjectionHypotheses`.

## Blockers

No blockers remain for #2958.
