# Work: true-factor CLD lattice vector surface (#7593)

## Accomplished

- Claimed #7593 as a `feature` one-shot worker issue.
- Added a proof-facing true-factor row-coefficient vector in
  `HexBerlekampZassenhausMathlib/Lattice.lean`.
- Added `trueFactorCLDVector` as the selected top-row combination in a
  `BhksLatticeBasis`.
- Proved `trueFactorCLDVector_memLattice`, showing the vector is in the
  integer row lattice by construction.
- Added `TrueFactorCLDNormBound` and `TrueFactorCLDVectorData` to make the
  remaining canonical-basis obligations explicit:
  projected indicator block, executable CLD coefficient block, and full
  cut-radius norm bound.
- Narrowed `Lattice.lean` from the aggregate `HexBerlekampZassenhaus` import to
  `HexBerlekampZassenhaus.Basic`, avoiding the unrelated `CrossCheck` dependency
  for this module.

## Current frontier

This is a partial surface, not the full #7593 closure. The structural
row-lattice membership is proved, but the canonical-basis coordinate
identities and full CLD-tail norm bound still need producer proofs.

## Next step

Prove a canonical producer for `TrueFactorCLDVectorData` at
`Hex.bhksLatticeBasis f p k liftedFactors`, using the block form of
`bhksLatticeEntry`, then connect the norm field to the existing B2/B3/B4
coefficient-bound surfaces.

## Blockers

The full requested build
`lake build HexBerlekampZassenhausMathlib.Lattice HexBerlekampZassenhausMathlib.BadVector HexBerlekampZassenhausMathlib.Recovery`
built `Lattice` and `BadVector`, then spent several minutes compiling the
unrelated aggregate dependency `HexBerlekampZassenhaus/CrossCheck.lean` through
the downstream `Recovery` path; that broad verification was stopped. Narrow
verification of `HexBerlekampZassenhausMathlib.Lattice` passed.
