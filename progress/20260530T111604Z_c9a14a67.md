**Accomplished**

- Wired the verified-Isabelle/AFP BZ comparator as a gating bench
  oracle for `HexBerlekampZassenhaus.Bench`. Resolves HO-5a (#5772),
  the dispatched-worker successor to closed PR #5777.
- Reused verbatim from the closed branch `agent/9cd9666a`:
  - `scripts/oracle/bz-isabelle/Hex_BZ_Export.thy` —
    Isabelle wrapper theory `Hex_BZ_Export` importing AFP
    `Berlekamp_Zassenhaus.Factorization_External_Interface` and
    invoking `export_code factor_int_poly in Haskell`.
  - `scripts/oracle/bz-isabelle/Main.hs` — persistent-subprocess
    driver: one line-delimited JSON request `{"coeffs":[...]}` per
    factor call, reply
    `{"ok":true,"result":{"scalar":c,"factors":[...]}}`.
  - `scripts/oracle/bz-isabelle/ROOT` — Hex_BZ_Export session
    definition (depends on the AFP `Berlekamp_Zassenhaus` session).
  - `HexBerlekampZassenhaus/Bench.lean` — three new bench
    registrations (`runIsabelleFactorChecksum`,
    `runIsabelleFactorBaselineChecksum`,
    `runFactorIsabelleDomainChecksum`) plus a Lean-side
    canonical-factor-multiset cross-check guard that fires on
    `(x-1)(x-2)`, `Φ₅`, and `(x²-2)(x²-3)` before the first timed
    request.
- Added what was missing from PR #5777 — the CI plumbing that made
  PR #5777 fail bench-verify:
  - `scripts/oracle/setup_bz_isabelle.sh` — adapted to a dated AFP
    URL pin (`afp-2026-05-29.tar.gz`, SHA
    `51f0eea952b391b9053dd3eb30ba1736e1e8618ff005bf197c3b76a2b8a3c5c7`)
    and to relying on `isabelle` being on `PATH` (CI does the install
    before invoking the script).
  - `.github/workflows/ci.yml` — restores a single combined cache
    entry covering `${{ runner.tool_cache }}/isabelle/Isabelle2025-2`,
    `~/.isabelle/Isabelle2025-2/heaps`, and `.cache/oracles/bz-isabelle`
    keyed by `hashFiles('scripts/oracle/setup_bz_isabelle.sh',
    'scripts/oracle/bz-isabelle/**')`; downloads + extracts
    Isabelle2025-2 from the upstream tarball on cold cache only and
    appends `bin` to `GITHUB_PATH`; exports
    `HEX_BZ_ISABELLE="$(scripts/oracle/setup_bz_isabelle.sh)"` before
    `check_bench_verify_budget.sh` (sibling of the existing
    `HEX_LLL_ISABELLE_SVP` line).
- Verified locally end-to-end:
  - `lake build hexbz_bench` passes (warnings from pre-existing
    `sorry`s in `HexBerlekampZassenhaus.Basic`,
    `HexGramSchmidt.Int`, `HexLLL.Basic` are unrelated).
  - `setup_bz_isabelle.sh` produces a working binary.
  - `hexbz_bench verify` on all three new targets succeeds.
  - `hexbz_bench run runIsabelleFactorChecksum` records ~129 ms
    median on `(x²-2)(x²-3)`.
  - `check_bench_verify_budget.sh hexbz_bench` reports 4s total
    against a 360s hard cap.
  - `check_dag.py`, `check_phase4.py`, `check_benches_mathlib_free.py`
    all clean.

**Current frontier**

- The PR completes acceptance-matrix items 1–7 (CI Isabelle install,
  AFP heap cache, wrapper theory exporting Haskell, GHC-compiled
  driver, JSON wire format, correctness gate before timing,
  per-call subprocess startup baseline, persistent-subprocess driver
  reuse, CI workflow updated).
- Downstream HO-5 work (#5771): HO-5b (perf-report rewrite consuming
  this comparator), HO-5c (extend bench schedules), HO-5d (remove
  `fallbackPrimeChoiceData`). All blocked on this PR landing.

**Next step**

- Land this PR. The next worker on HO-5b/c/d can consume
  `Hex.BerlekampZassenhausBench.runIsabelle*Checksum` directly.

**Blockers**

- None.
