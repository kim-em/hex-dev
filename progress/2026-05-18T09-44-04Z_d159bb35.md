# 2026-05-18T09:44Z — feature session (d159bb35)

## Accomplished

Filed abstract-bound (`_of_bound`) sibling of
`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
in `HexBerlekampZassenhausMathlib/IntReductionMod.lean` (issue #4971).

* New theorem `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`
  replaces the original's `hprecision : 2 * defaultFactorCoeffBound squareFreeCore < d.p ^ d.k`
  with the four-parameter abstract-bound block
  `(B' : Nat)` / `hcore_lc_le` / `hvalid` / `hprecision : 2 * B' < d.p ^ d.k`.
  Body mirrors the original verbatim; the single `hprecision` consumer
  (the forward call to
  `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`)
  is rewired to its `_of_bound` sibling.
* Original `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
  rewritten as a thin wrapper instantiating
  `B' := Hex.ZPoly.defaultFactorCoeffBound (Hex.normalizeForFactor f).squareFreeCore`,
  discharging `hcore_lc_le` via `defaultFactorCoeffBound_valid` + `leadingCoeff_eq_coeff_last`,
  and `hvalid` via the universal `defaultFactorCoeffBound_valid`. Signature
  unchanged; the two in-tree consumers (`reassemblyExpansionComplete_exhaustive_of_ne_zero`
  and its `_of_primitive_pos_lc_core` variant) continue to type-check via the
  rewired thin wrapper without further edits.

## Current frontier

Build of `HexBerlekampZassenhausMathlib.IntReductionMod` shows the same
18-error pile in `Basic.lean` as `origin/main` (pre-existing whnf timeouts
and unknown-constant errors at lines 4848–16027). No new errors or warnings.
`python3 scripts/check_dag.py` exits 0. `git diff --check` clean. No new
`sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` in the diff.

## Next step

Pairing-partner / outer-B work for the two `reassemblyExpansionComplete_exhaustive_*`
consumers (lines 3644 monic and 3804 `_of_primitive_pos_lc_core`) can now
be lifted by routing through this new `_of_bound` sibling. Combined with
#4970's aux `_of_bound` (blocked on #4947), this unblocks the umbrella's
own `_of_bound` sibling.

## Blockers

None.
