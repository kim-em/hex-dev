# HexConway Conway-table oracle

## Accomplished

- Claimed human-oversight issue #1997.
- Expanded the committed `HexConway` Lübeck table slice to `p in {2, 3, 5, 7, 11, 13}` and `n in {1..6}`.
- Added `HexConway/EmitFixtures.lean`, a committed fixture sample, a Lübeck cache JSON file, a cache-regeneration script, and `scripts/oracle/conway_luebeck.py`.
- Wired a `oracle-conway` CI job that checks Lean emission against the committed fixture and compares against both the committed Lübeck cache and the installed `conway-polynomials` package.
- Verified `lake build HexConway`, fresh fixture diff, the committed-cache oracle, the optional-package oracle in a temporary venv, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

`HexConway` now has the requested small committed Conway-table slice plus an external table-source oracle path. The optional Python package leg is explicitly required in CI, while local runs can skip it when the package is absent.

## Next step

Review CI results for the new `oracle-conway` job after the PR opens.

## Blockers

- `HexConway/Basic.lean` still carries the pre-existing proof-level `sorry` for `luebeckConwayPolynomial_2_1_irreducible`.
- The broadened imported table uses axiomatised imported-entry irreducibility and positive-degree witnesses; the oracle checks table data wiring, not formal irreducibility proofs.
- The worktree still has an unrelated pre-existing `.claude/CLAUDE.md` modification that this session did not touch.
