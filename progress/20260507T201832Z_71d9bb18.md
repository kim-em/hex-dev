# 2026-05-07 20:18 UTC — gramDet_sizeReduce wrapper (71d9bb18)

## Accomplished

Claimed issue #2544 and replaced the `gramDet_sizeReduce` proof in
`HexGramSchmidt/Update.lean` with the intended thin wrapper over
`GramSchmidt.Int.gramDet_rowAdd_earlier`, unfolding `sizeReduce` to expose
`Matrix.rowAdd b j k (-r)`.

Verification passed:

- `lake build HexGramSchmidt.Update`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Update.lean HexGramSchmidt/Int.lean`

## Current frontier

`gramDet_sizeReduce` no longer contributes a theorem-level `sorry`. The
remaining `sorry`s in `HexGramSchmidt/Update.lean` are adjacent size-reduction,
scaled-coefficient, and adjacent-swap formula surfaces outside this issue.

## Next step

Continue with the next Gram-Schmidt update issue after the rational row-add and
integer wrapper surfaces land.

## Blockers

No blocker for this slice. The pre-existing `.claude/CLAUDE.md` worktree
modification remains untouched and uncommitted.
