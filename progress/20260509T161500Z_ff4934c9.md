# 20260509T161500Z - Issue #2778 partial

## Accomplished

Claimed #2778 after the human-oversight item had already been claimed by
another agent. Added the next determinant-transpose support slice in
`HexMatrix/Determinant.lean`:

- `list_nodup_map_on`, a local injectivity-on-list helper for mapped nodup
  proofs.
- `inversePermutationVector`, a total wrapper around the existing inverse
  construction for permutation vectors.
- membership and involution facts for `inversePermutationVector` on
  `permutationVectors n`.
- `inversePermutationVector_map_permutationVectors_perm`, proving that inverse
  maps the full permutation-vector list to a permutation of itself.
- `permutationVectors_inverseVector_sum`, packaging that list permutation as
  the fold-level reindexing lemma needed by the eventual transpose proof.

Committed as `b217998 feat: add inverse permutation list reindexing`.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

## Current frontier

The product-side substitution and now the fold-level inverse-list reindexing
are available. The determinant transpose theorem still needs the sign-side
parity result:

```lean
detSign (inversePermutationValues perm hnodup) = detSign perm
```

Once that is proved, the final `det_transpose` fold proof should be able to
combine the existing product lemma with `permutationVectors_inverseVector_sum`.

The pre-existing unstaged `.claude/CLAUDE.md` modification remains untouched.

## Next step

Prove inversion parity/sign preservation for inverse permutation vectors, then
assemble `detTerm` and `det` reindexing into the public `det_transpose`
theorem.

## Blockers

No environmental blockers. The remaining blocker is mathematical: a checked
parity argument that the inverse permutation has the same inversion parity as
the original permutation.
