# HexMatrix old-boundary insertion helpers

## Accomplished

- Claimed #2149 and added private old-boundary `insertAt` normalization
  lemmas in `HexMatrix/Determinant.lean`.
- Proved that the old-boundary insertion permutation is the existing final
  insertion permutation with the old-boundary and final rows transposed.
- Added a private determinant-term helper reducing the old-boundary insertion
  term to the existing final-insertion term on a row-swapped matrix, including
  the sign change.
- Verified `lake build HexMatrix.Determinant`, `lake build HexMatrix`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-placeholder scan of
  `HexMatrix/Determinant.lean`.

## Current frontier

#2149 is complete at the intended helper layer. The follow-up old-boundary
off-diagonal term rewrite can now use the private row-index and row-swap
comparison lemmas instead of reopening dependent `Fin` normalization.

## Next step

Claim the blocked follow-up issue #2150 once coordination unblocks it, and use
`detTerm_insertAt_boundary_eq_neg_rowSwap_insertAt_last` to rewrite the
successor old-boundary final-column term through the existing final-insertion
bordered-minor APIs.

## Blockers

- `lake build HexMatrix` still reports pre-existing `sorry` warnings in
  `HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`.
- The worktree had a pre-existing `.claude/CLAUDE.md` modification before this
  session; it was left untouched and unstaged.
