# Progress - 2026-06-14T19:46:01Z (session 0d8b8b35, work)

## Accomplished

Issue #7328: added one-sentence Phase 6 docstrings to the six undocumented
array pivot-search declarations in `HexMatrix/Bareiss.lean`:

- `findPivotArrayAux`
- `findPivotArray?`
- `findPivotArrayAux_matrixToRows`
- `findPivotArray?_matrixToRows`
- `findPivotArrayAux_matches`
- `findPivotArray?_matches`

The change is doc-only: no signatures, statements, proofs, declaration order,
imports, attributes, or simp/grind sets were touched.

Also claimed and released stale issues #7326 and #7327 after verifying their
requested docstrings were already present on `origin/main`.

Verification:

- `lake build HexMatrix.Bareiss`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Bareiss.lean`: `12 0`.
- Added-line forbidden-token scan over `HexMatrix/Bareiss.lean`: no matches.

## Current frontier

The Bareiss pivot-search doc batch is complete. The next Bareiss array-update
cluster beginning at `stepArray` remains out of scope for #7328.

## Next step

Open a PR for #7328 after committing the Bareiss docstrings and this progress
entry.

## Blockers

None.
