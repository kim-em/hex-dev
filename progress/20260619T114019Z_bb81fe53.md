# BHKS fast-core hsize producer (#8056)

## Accomplished

Claimed #8056 after the higher-priority directive #2564 proved dependency-blocked.
Added the fixed-lift `BHKS.size_eq_indicators_of_candidates` wrapper and the
fast-core `factorFastCoreWithBound_some_size_eq_indicators` wrapper in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`. Refactored the two
forward-input irreducibility wrappers to reuse the new fixed-lift size producer
instead of rebuilding the `hsize` proof inline.

## Current frontier

The #8056 size-equality plumbing is complete and scoped to the existing
forward-cut fast branch. The worktree still has a pre-existing off-limits
`.claude/CLAUDE.md` modification that this session did not touch or stage.

## Next step

Proceed to the neighboring partition-count packaging issue (#8057), then the
fast raw-branch assembler (#8058) once its dependencies are ready.

## Blockers

None for #8056.
