# 20260609T223728Z (#6711)

## Accomplished

Claimed #6711 and added a normalized derivative-active initial-split completion
wrapper in `HexPolyFp/SquareFree.lean`:

- `yunFactorsNormalizedLevelCompletes_of_derivative_active_initial_split`

The theorem packages the existing normalized size-bound completion substrate for
the executable initial split `(c, g) = (f / gcd f f', gcd f f')`, consuming
`YunDerivativeActiveNormalizedStateProvider hp` directly. This gives downstream
scalar-aware Yun callers an initial-split entry point without requiring the raw
`YunDerivativeActiveLevelStateProvider hp`.

## Current frontier

The weighted-product proof surface still has raw-provider consumers such as
`squareFreeAuxRevContribution_derivative_active_pow_obligation` and
`squareFree_weightedProduct`. The new wrapper supplies the normalized completion
entry point needed for follow-up threading through scalar-sync/product-sync
lemmas, but it does not itself refactor those downstream consumers.

## Next step

Use `yunFactorsNormalizedLevelCompletes_of_derivative_active_initial_split`
together with the existing scalar-sync lemmas to replace raw
`yunFactorsLevelCompletes` assumptions in the derivative-active weighted-product
path.

## Blockers

None for this substrate step.

Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`
