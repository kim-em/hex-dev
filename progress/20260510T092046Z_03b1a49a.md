# Review HexBZ adversarial fixture coverage after pinned JSON lands (#2948)

## Accomplished

Reviewed the post-#2966 adversarial fixture stream against the HO-2 directive
(#2565) and the `hex-berlekamp-zassenhaus.md` SPEC bar.

Verified:

- `lake build HexBerlekampZassenhaus.Conformance HexBerlekampZassenhaus.EmitFixtures`
  succeeds.
- `lake exe hexbz_emit_fixtures > /tmp/hexbz-bz.jsonl` re-emits the committed
  fixture stream byte-for-byte (50 records).
- `python3 scripts/oracle/bz_flint.py /tmp/hexbz-bz.jsonl` and
  `python3 scripts/oracle/bz_flint.py --check` both pass: 25 cases checked,
  0 failures.
- `python3 scripts/check_dag.py` clean; `git diff --check` clean.
- `bz.jsonl` carries pinned modular metadata for both `adv/x4_plus_1`
  (`modFactorPrime: 5`, `modFactorDegrees: [2,2]`) and `adv/quad_sqrt2_sqrt3`
  (`modFactorPrime: 23`, `modFactorDegrees: [1,1,1,1]`).
- `adv/quad_sqrt2_sqrt3` has a `factor` result `[1, [[[-3,0,1],1],[[-2,0,1],1]]]`
  accepted by python-flint (matches `(X²−3)(X²−2)`).
- `Conformance.lean` Lean-side `#guard modularFactorDegreesAt? …` checks exist
  for all four originally-listed adversarial polynomials (`x4Plus1`,
  `quadSqrt2Sqrt3`, `swinnertonDyerSD3`, `phi15`).

Coverage verdict against the SPEC quote ("Core-tier conformance must include
at least one input where true integer factors require a non-trivial subset
product of lifted mod-p factors, and at least one input that splits heavily
(≥ 4 distinct mod-p factors) over a small admissible prime"):

- `adv/x4_plus_1`: irreducible quartic recombined from both lifted mod-5
  quadratics → satisfies "non-trivial subset product".
- `adv/quad_sqrt2_sqrt3`: each integer quadratic factor formed from a strict
  2-element subset of 4 mod-23 linears → satisfies *both* SPEC requirements
  simultaneously.

SD3 / Φ₁₅ are not literally required by the SPEC; they were directive
aspirational coverage and are explicitly deferred to scientific-budget bench
per the owner comment in `HexBerlekampZassenhaus/Bench.lean` line 442–449.
No follow-up conformance issue created.

## Current frontier

HO-2 SPEC minimum is met by the post-#2966 fixture stream. The verdict
comment is the deliverable for #2565.

## Next step

Post the verdict comment on #2565 and create the PR closing #2948.

## Blockers

None.
