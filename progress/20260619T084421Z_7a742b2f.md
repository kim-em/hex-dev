# HexBerlekampZassenhaus bounded wrapper API (#6246)

## Accomplished

Exposed the bounded proof-facing wrappers in
`HexBerlekampZassenhaus/Basic.lean` without changing factorization dispatch:

- made `factorSlowTrialWithBound` public and added
  `factorSlowTrialWithBound_eq_factorizationOfFactors`;
- added public combined slow raw/wrapper API,
  `factorSlowFactorsWithBound` and `factorSlowWithBound`, with
  `factorSlowWithBound_eq_factorizationOfFactors`;
- made `factorFastWithBound` public and added
  `factorFastWithBound_eq_map_factorizationOfFactors`;
- made the existing fast wrapper map lemmas public:
  `factorFastWithBound_eq_some_of_factors_some` and
  `factorFastWithBound_ne_none_of_factors_ne_none`;
- added
  `factorWithBound_eq_factorFastWithBound_getD_factorSlowWithBound`
  so downstream proofs can use the SPEC-shaped bounded combinator view.

## Current frontier

`lake build HexBerlekampZassenhaus` completed successfully after the final
edit. `python3 scripts/check_dag.py`, `git diff --check`, and the added-line
forbidden-token scan over `HexBerlekampZassenhaus/Basic.lean` all passed.

The worktree still contains a pre-existing off-limits `.claude/CLAUDE.md`
modification; it was not touched or staged for this issue.

## Next step

Open the PR for #6246. Downstream `HexBerlekampZassenhausMathlib` proof
assembly can now refer to the bounded fast and slow wrapper definitions
directly.

## Blockers

None.
