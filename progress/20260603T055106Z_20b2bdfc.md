# Schur scaledCoeffs wrappers via Bareiss equivalence bridge

Session `20b2bdfc-acf9-463e-9453-963808daeac1` continuing #6458 after the
partial PR #6460.

## Accomplished

Reduced `grep -c sorry HexGramSchmidt/Int.lean` from 6 to 3 by routing
the four remaining Schur-side wrappers through a single private
equivalence stub.

- Added private `getArrayEntry_scaledCoeffRowsSchur_eq` as a `sorry`
  stub: it states that the per-row Schur kernel and the column-major
  Bareiss array path produce identical integer values at every cell.
  This is the single mathematical bridge the four wrappers needed; the
  proof itself remains future work.
- Added private `scaledCoeffRows_eq_zero_of_singularStep_lt`, the
  Bareiss-array singular dual to
  `scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix`. The body
  reuses the already-proven
  `scaledCoeffArrayLoop_lower_zero_of_singular_before_target` from
  earlier in the file — no new proof obligation.
- Discharged the four `sorry`s left by #6451 / #6460 by composing the
  equivalence stub with the matching pre-existing Bareiss-path
  invariants:
  - `gramDetVec_eq_gramDet` (k = 0 via `gramDet_zero`; k = r+1 via
    `scaledCoeffRows_diag_toNat_eq_gramDet` after the equivalence
    rewrite).
  - `scaledCoeffs_diag_eq_zero_or_eq_noPivotData_diag` via
    `scaledCoeffRows_diag_eq_zero_or_eq_noPivotData_diag`.
  - `scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss` via
    `scaledCoeffRows_diag_eq_zero_or_eq_leadingPrefix_bareiss`.
  - `scaledCoeffs_eq_zero_of_singularStep_lt` via the new private
    `scaledCoeffRows_eq_zero_of_singularStep_lt`.
- Underscored the now-unused `hsj` hypothesis on the public
  `scaledCoeffs_eq_zero_of_singularStep_lt`; positional callers in
  `HexGramSchmidtMathlib/Int.lean` are unaffected.

## Current frontier

`grep -c sorry HexGramSchmidt/Int.lean` reports 3:

- Line 5030 (pre-existing doc comment about the #5655-tracked sorry).
- Line 5043: `gramDetVecEntry_eq_leadingPrefix_bareiss` (pre-existing,
  tracked by #5655).
- Line 5323: `getArrayEntry_scaledCoeffRowsSchur_eq` (the new
  Schur ≡ Bareiss equivalence stub introduced here).

`lake build HexGramSchmidt HexGramSchmidt.Conformance HexGramSchmidtMathlib`
passes; no new lint warnings.

## Next step

Discharge `getArrayEntry_scaledCoeffRowsSchur_eq` to take
`grep -c sorry` to the target 2. The equivalence is the substantive
mathematical content the issue calls out as the "cheaper" of the two
strategies; the wrappers above already exercise it as a single hook so
the residual is exactly one private lemma. Possible routes:

- Strong induction over `(i, j)` in lex order, matching the Schur
  recurrence step against the Bareiss step that writes column `j`
  for row `i`. The proven array-loop diag/lower correspondences may
  give the Bareiss-side anchors.
- Define a common bordered-minor spec and prove both paths match it
  entrywise.

## Blockers

None. The discharge plan and remaining work are self-contained.
