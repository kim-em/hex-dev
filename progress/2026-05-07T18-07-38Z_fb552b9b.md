# 2026-05-07 18:07 UTC — executable CLD surface audit (fb552b9b)

## Accomplished

Claimed review issue #2586 and completed the executable-surface audit for the
HO-1 Berlekamp-Zassenhaus CLD rewrite. Posted findings to #2564 with exact
declaration names and file paths for the reusable prime/lift, Hensel,
polynomial arithmetic, Mignotte, LLL, RREF, exhaustive recombination, and
normalisation/reassembly surfaces.

The comment also identifies the missing local executable helpers for HO-1:
`factorSlow`, `factorFast`, `bhksBound`, CLD coefficients, centred residue and
two-sided cut helpers, BHKS lattice construction, LLL cut/projection, RREF
equivalence-class extraction, and indicator candidate reconstruction.

## Current frontier

The executable side does not need separate prerequisite infrastructure before
HO-1. The missing work is local implementation in
`HexBerlekampZassenhaus/Basic.lean`; Mathlib bridge dependency mapping remains
separate issue #2587.

## Next step

Create the PR for #2586 so the audit result is attached to the claimed review
work item. The HO-1 implementation worker can start by promoting the exhaustive
path to `factorSlow`, then add `bhksBound` and the CLD helper layer.

## Blockers

No blocker for #2586. The worktree still contains the pre-existing
`.claude/CLAUDE.md` modification, left untouched.
