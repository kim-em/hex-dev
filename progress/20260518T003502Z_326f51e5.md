# Session 326f51e5 — planner refill: scaled Mathlib-bridge membership wrapper

## Accomplished

Planner cycle with `POD_QUEUE_DEFICIT=1` (`POD_MIN_QUEUE=3`,
`POD_QUEUE_DEPTH=2`).  Filed one atomic feature issue rounding out the
scaled `exhaustiveCoreFactorsWithBound` prerequisite chain from
#4639 / #4626:

* **#4875** — BZ Mathlib-free: scaled membership wrapper for
  `exhaustiveCoreFactorsWithBound` (Mathlib-bridge).  Adds
  `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
  in `HexBerlekampZassenhausMathlib/Basic.lean` mirroring the unscaled
  wrapper at line 8699.  Depends on #4866 (provides
  `recombineScaledExhaustive_eq_of_scaledRecombinationSearchMod_some`)
  and #4870 (swaps `exhaustiveCoreFactorsWithBound`'s body to the
  scaled flavour).  Purely additive; the existing unscaled wrappers
  at lines 8699 and 8733 are explicitly left for the #4870 worker to
  repair or remove.  #4866's "Out of scope" section names this as
  the fourth prerequisite.

Two ready-to-merge PRs (#4871, #4872) were skipped at Step 2 because
the `build` and `conformance` checks were still in flight; branch
protection refused the merge.  The next planner cycle picks them up.
No PRs needed `replan`-flow fix issues (all currently failing checks
are `IN_PROGRESS`, not `FAILURE`).

## Current frontier

The scaled `exhaustiveCoreFactorsWithBound` chain from #4639 / #4626 is
now planner-side complete in four atomic pieces — all four prerequisite
issues exist:

1. #4866 (executable wrapper + invariants) — PR #4872 in flight.
2. #4869 (product lemma) — blocked on #4866.
3. #4870 (call-site swap) — blocked on #4866 + #4869.
4. #4875 (scaled membership wrapper, this session) — blocked on
   #4866 + #4870.

The successor capstone — a primitive + positive-leading-core variant of
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
(line 12761 of `HexBerlekampZassenhausMathlib/Basic.lean`) — is the
natural next planner item once #4875 lands.  The scaled coverage
substrate `scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
(line 12653) already exists, so the capstone is a short compose.
Listed in #4875's "Out of scope" for the next planner cycle to refile
when the queue deficit demands it.

## Next step

Planner exits.  Dispatcher will pick up the new issue once #4866 and
#4870 unblock it (currently sequential: PR #4872 must merge → #4869 +
#4870 unblock → both must merge → #4875 unblocks).

## Blockers

None for this session.  #4875 carries automatic `blocked` labels from
its `depends-on:` declarations; the dispatcher's unblock loop will
clear them when the prerequisites merge.
