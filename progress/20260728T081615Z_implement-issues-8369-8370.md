# Implement issues #8369 and #8370

## Accomplished

- Rewrote both GitHub issue bodies as self-contained implementation handoffs.
  In particular, #8369 is now conditional on successful prime selection and
  states the full-vector BHKS reconstruction formula; #8370 names the actual
  selector, admissibility, dispatch, and cleanup surfaces.
- Posted the kernel-checked counterexample to the old unconditional #8369
  premise: for `f = X^3 - hotPathPrimorial * X`,
  `toMonicPrimeData? f = none` and consequently `factorLattice f = none`.
- Implemented #8370's 94-prime bounded selector proof surface, good-prime and
  discriminant/primorial bridges, strict-primorial success theorem,
  separable/normalized-core specializations, hybrid product theorems, and the
  modular/lattice `Factorization` dispatch theorems.
- Deleted the obsolete selector-walk cluster from `ChoosePrimeData.lean`.
- Corrected `latticePrecisionCap` so it dominates the BHKS bound for both the
  square-free core and the actual monic transform used to construct the
  lattice. Added the corresponding dominance and strict-power theorems.
- Added the permanent executable guards for the no-hot-prime cubic
  counterexample.
- Added the lattice-span foundations needed by #8369: true-support span,
  indicator membership, forward containment `W ≤ L'`, reverse/equality
  wrappers, retained-prefix norm bounds, and the generic LLL row/basis norm
  estimate.
- Restored and compiled the sound resultant/CLD infrastructure in
  `HexBerlekampZassenhausMathlib/Resultant.lean`, including the Hadamard
  resultant bound, common-factor/resultant lemmas,
  `pow_dvd_resultant_of_map_dvd`, and the CLD syzygy.
- Updated the SPEC and manual to distinguish the coefficient-bound schedule
  from the Hensel exponent, record the selector limitation, and state the
  corrected full-vector reconstruction route.
- Previously completed targeted builds for the selector/admissibility,
  hybrid dispatch, resultant, lattice, entry-point, and manual modules, and
  one full `lake build`. `git diff --check` is clean and the diff introduces
  no `sorry`, `axiom`, or `native_decide`.

## Current frontier

- The remaining work is the corrected #8369 totality proof.
- Two generic block-matrix identities were just added after
  `bhksLatticeBasis_blockForm` in
  `HexBerlekampZassenhausMathlib/Lattice.lean`:
  `vecMul_first_of_blockForm` and `vecMul_tail_of_blockForm`. They have not
  yet reached the edited module in a completed build.
- A targeted `lake build HexBerlekampZassenhausMathlib.Lattice` unexpectedly
  recompiles `HexBerlekampZassenhaus/FactorEntryPoints.lean`; that Lean
  process reached roughly 54% of system RAM and 100% of one CPU. It was
  interrupted to avoid another session/OOM crash.
- The Codex thread itself has accumulated roughly 1.97 million tokens, mostly
  through repeated Lean build output. A fresh agent should use this file and
  the rewritten issues instead of inheriting the entire conversation.

## Next step

1. Compile `HexBerlekampZassenhausMathlib/Lattice.lean` with output redirected
   to a temporary log, and fix the two new vector identities before proceeding.
2. Define the polynomial reconstructed from the *entire* lattice vector:
   for coefficient `j`,
   `p^ell * tail_j + sum_i e_i * centeredResiduePow p ell
   (centeredResiduePow p a ((cldQuotientMod f lifted_i p a).coeff j))`.
3. Use `cldQuotientMod_coeff_decomp_of_lt`, `ell ≤ a`, and the two block
   identities to show this is congruent modulo `p^a` to the corresponding
   coefficient of the full POL sum. Package it as a degree-`< n` polynomial.
4. Combine that congruence with `pow_dvd_resultant_of_map_dvd`, the nonzero
   resultant proof, and the Hadamard bound to obtain the BHKS bad-vector
   contradiction.
5. Formalize the partition adjustment from BHKS Lemma 3.2: add/subtract true
   support indicators so every true support meets the exponent support, while
   one chosen support contains a zero exponent and no support is all zero.
6. Apply the contradiction to every retained reduced row, obtaining
   `L' ≤ W`, hence `L' = W`.
7. Prove the reverse RREF-signature implication from `L' = W`, then connect
   the exact support partition to `bhksRecover?_eq_some_of_checks` and expose
   the conditional raw and `Factorization` totality theorems named in #8369.
8. Run the full build plus the relevant conformance and benchmark gates, then
   audit every checklist item in both issues.

## Blockers

- No mathematical blocker is known. The old unconditional theorem was false,
  but the rewritten conditional contract is sound.
- Operational risk: this worktree's module dependency graph can trigger a
  very memory-heavy rebuild of `FactorEntryPoints.lean`, and the current Codex
  session is unusually large. Continue in a fresh session and keep verbose
  build logs out of the conversation.
