# Progress: #6888 — decouple scaled recovery from representation predicate

## Accomplished

- Claimed #6888 after #2564 correctly failed claim due open dependencies.
- Refactored the stale scaled-product recovery lemmas in
  `HexBerlekampZassenhausMathlib/Basic.lean` so they take the explicit
  old-shape scaled congruence
  `reduceModPow (scaledLiftedFactorProduct core d S) = reduceModPow factor`
  instead of consuming `RepresentsIntegerFactorAtLift`.
- Propagated that explicit congruence through the monic-core and scaled
  recombination recovery wrappers in the affected region.

## Current frontier

The target build compiles past the edited recovery lemmas and reaches the
known #6804 scaled/dilate recursive-search prefix mismatch, now reported at
`HexBerlekampZassenhausMathlib/Basic.lean:16475`.

## Next step

Continue with the dependent predicate-swap issues: callers that still truly
need the old scaled congruence should pass it explicitly, while the public
representation predicate can move to the recovered monic-coordinate carrier.

## Blockers

- `lake build HexBerlekampZassenhausMathlib.Basic` still fails at the
  pre-existing #6804 mismatch between `DensePoly.scale ... centeredLiftPoly`
  and `ZPoly.dilate ... centeredLiftPoly` inside
  `scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`.
- `second-opinion` was attempted for final review, but the helper produced no
  output after several minutes and was terminated.
