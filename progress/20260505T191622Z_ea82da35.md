# 2026-05-05 19:16 UTC — work session (ea82da35)

## Accomplished

Claimed #2244 and #2245 first because they were the oldest oversight items.
Both were stale on current HEAD: the requested Conway and GFqField axioms had
already been replaced by theorem bodies. Commented the current state and
released both as stale/blocked because `lake build HexConway`,
`lake build HexConway.Basic`, `lake build HexGfqField`, and
`lake exe hexgfqfield_bench list` all abort while building the umbrella
`HexBerlekamp` target with `dyld: missing symbol called`.

Claimed #2323, assessed it as too large for one session, and decomposed it
into #2385, #2386, and #2387.

Claimed #2385 and exposed the read-only no-pivot Bareiss proof API in
`HexMatrix/Bareiss.lean`: `BareissState`, `exactDiv`, `stepMatrix`, the
bordered-minor update lemma, no-pivot loop branch/access lemmas, `finish`,
and `noPivotInitialState`. `bareissNoPivotData` now uses
`noPivotInitialState`; executable behavior is unchanged.

## Current frontier

#2385 is ready for PR. The remaining Bareiss determinant work is split into
#2386 for the Mathlib-side one-step bordered-minor recurrence and #2387 for
the recursive `noPivotLoop` invariant/no-singular-branch theorem.

## Next step

After #2385 lands, work #2386, then #2387, then return to #2324 and #2315.
Separately, the current `HexBerlekamp` umbrella target has a `dyld` missing
symbol failure that blocks broad builds depending on it.

## Blockers

`HexBerlekamp` umbrella target build aborts with `dyld: missing symbol called`
and Lean exit code 134. This blocked full verification for #2244 and #2245,
but not the `HexMatrix` checks for #2385.
