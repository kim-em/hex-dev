# 2026-05-14 10:45 UTC — work session 3e562a46 (issues #3987, #4005)

## Accomplished

- Claimed #3987, checked its prerequisites, and released it as stale because
  #3997 was closed through replan/decomposition while branch successors
  #4005-#4007 remain open.
- Claimed #4005 and implemented the core branch-shape prerequisite in
  `HexBerlekampZassenhaus/Basic.lean`:
  `factorWithBound_entry_mem_small_mod_singleton_raw`.
- The new theorem pins recorded `factorWithBound` entries in the fast-path
  small-mod singleton branch to the exact `reassemblePolynomialFactors`
  singleton square-free-core raw array, after the
  `collectFactorMultiplicities` normalization layer.
- Decomposed the remaining #4005 mathematical work into #4018 and #4019 and
  left the required breadcrumb comment on #4005.

## Current frontier

#4005 is only partially complete. The executable branch shape is available,
but the finite-field singleton Berlekamp soundness theorem and the Mathlib
reduction/Gauss lift are still missing.

## Next step

Land the partial PR for #4005, then work #4018 before #4019. After #4019,
the small-mod singleton branch should be available for the per-branch
irreducibility assembly.

## Blockers

- #4019 depends on #4018.

## Verification

- `lake build HexBerlekampZassenhaus.Basic` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `rg -n '^\\s*sorry\\b|^axiom\\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`
  reports only the pre-existing `isIrreducible_iff` placeholder, now at line
  4529.
