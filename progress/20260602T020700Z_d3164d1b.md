# d3164d1b - work #6076

## Accomplished

- Claimed and executed feature issue #6076 (double-row replacement
  determinant transport for the ordered four-row `nDet` Plucker kernel
  in the HexMatrix Mathlib bridge layer).
- Added `OrderedFourShift.cycleBehind` — the inverse of `cycleAhead`,
  defined as `(cycleAhead a m hm).symm`. Same sign `(-1) ^ m`. Four
  val-lemmas: `cycleBehind_val_below`, `_above`, `_base` (i.val = a
  → val = a + m), `_in` (a < i.val ≤ a + m → val = i.val - 1).
- Proved `matrixEquiv_double_setRow_eq_submatrix_nMatrix`: under
  `matrixEquiv`, `setRow (setRow (nMatrix B p1 q) r2 B[p1]) r3 B[q]`
  equals `(matrixEquiv (nMatrix B p2 p3 h23)).submatrix σ id` for
  `σ = cycleAhead p1.val (p2.val - p1.val - 1) * cycleBehind (p3.val - 1) (q.val - p3.val - 1)`.
  Seven-case proof on `i.val`: below p1, at r2, at r3, p1 ≤ i.val < p2 - 1,
  p2 ≤ i.val < p3 - 1, p3 ≤ i.val < q - 1, i.val ≥ q - 1.
- Proved `det_double_setRow_eq_pow_mul_nDet`: combining the matrixEquiv
  identification with `Matrix.det_permute`, `Equiv.Perm.sign_mul`, and
  the cycle-sign lemmas gives
  `det (setRow (setRow M r2 B[p1]) r3 B[q]) =
   (-1) ^ (p2.val - p1.val - 1 + (q.val - p3.val - 1)) * nDet B p2 p3 h23`.
- Exposed `ordered_four_signed_Plucker_p1_side`: a rewrite-ready
  specialization of `ordered_four_det_mul_det_setRow_setRow_eq_cofactorRowPairing_mul_sub`
  where the LHS double-setRow det and the two p1-side cofactor pairings
  are pushed to signed `nDet` form. The q-side `cofactorRowPairing`
  terms remain symbolic — they become signed `nDet` once #6091 lands
  the q-row pow_mul_nDet wrappers (matching #6091's stated scope).
- Verified with `lake build HexMatrixMathlib` (clean),
  `git diff --check`, `python3 scripts/check_dag.py`, and a
  forbidden-token grep over the diff.

## Current frontier

#6076 is complete and ready for PR. The bridge layer now has:
- All four `cofactorRowPairing M r? B[p1]` ↔ signed nDet B p_? q
  transports (from #6074),
- The q-row substrate `nMatrix_ordered_four_p1_p?_row_q`
  (from #6075/#6087),
- The double-row det → signed nDet B p2 p3 transport (this PR),
- A `Plucker_p1_side` assembly that wires (LHS = sign × nDet B p2 p3)
  through to two of four RHS cofactor pairings.

The remaining gap on the way to #6012 is #6091: push
`cofactorRowPairing M r2 B[q]` and `cofactorRowPairing M r3 B[q]` to
signed `nDet B p1 p_t` form. Once that lands, a wrapper analogous to
`ordered_four_signed_Plucker_p1_side` can close the full four-term
Plucker identity for #6012.

## Next step

Create the PR for #6076. After #6091 lands, write the full four-term
signed Plucker wrapper (small new theorem, no new substrate needed).

## Blockers

None.
