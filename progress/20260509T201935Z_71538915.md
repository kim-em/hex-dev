# 20260509T201935Z - Issue #2866

## Accomplished

Read the latest progress note, listed unclaimed issues, and selected #2854 as
the next critical HexMatrix feature item. Determined #2854 was too broad for a
single stable proof step, decomposed it into #2866 and #2867, left the required
`Decomposed into #2866, #2867` breadcrumb on #2854, and released #2854 for
replan.

Claimed #2866 and added private finite-sum column replacement infrastructure to
`HexMatrix/Determinant.lean`: `det_colReplace_zero` and
`det_colReplace_sum_list`. The list lemma expands a replaced determinant column
whose entries are finite folded sums into the corresponding finite folded sum
of scalar-multiplied replaced-column determinants, using the existing
`det_colReplace_add` and `det_colReplace_smul` APIs.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

## Current frontier

The single-column finite-sum expansion needed by #2854 is now available as a
private helper. #2867 can consume it to prove the all-column ordered tuple
expansion over `columnTupleVectors`.

## Next step

Claim #2867 after #2866 lands and lift `det_colReplace_sum_list` across all
determinant columns to prove the generic ordered tuple expansion theorem.

## Blockers

None for #2866. The worktree still has a pre-existing unrelated dirty
`.claude/CLAUDE.md` change that this session did not touch.
