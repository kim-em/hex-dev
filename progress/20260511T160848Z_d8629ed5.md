# HexConway supported-entry wrappers

## Accomplished

- Claimed issue #3477.
- Added `supportedEntry_2_2`, `supportedEntry_2_3`, `supportedEntry_2_4`,
  `supportedEntry_2_5`, and `supportedEntry_2_6` in `HexConway/Basic.lean`.
- Reused the existing proof that `2` is prime and the existing
  `luebeckConwayPolynomial?_hit_2_n` lookup lemmas.
- Verified:
  - `lake build HexConway.Basic`
  - `lake build HexConway`
  - `python3 scripts/status.py HexConway`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexConway/Basic.lean HexConway.lean`

## Current frontier

- The HexConway API now exposes committed `p = 2`, degree `1` through `6`
  Tier 1 supported entries.
- The worktree still has a pre-existing `.claude/CLAUDE.md` modification that
  this session did not touch.

## Next step

- Open a PR for issue #3477.

## Blockers

- None.
