## Accomplished

Claimed #4433 and added the recursive constructor for the quadratic
multifactor lift invariant in `HexHensel/QuadraticMultifactor.lean`.

The implementation adds:

- `Hex.ZPoly.QuadraticMultifactorCoprimeSplits`, a Mathlib-free recursive
  split-coprimality predicate over the original `FpPoly` factors.
- `Hex.ZPoly.quadraticMultifactorLiftInvariant_of_factorsModP`, which builds
  `QuadraticMultifactorLiftInvariant` from monic factor data, mod-`p` product
  congruence, and the split-coprimality predicate.

Verification run:

- `lake build HexHensel.QuadraticMultifactor`
- `lake build HexHensel`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n 'axiom |native_decide|TODO|FIXME|sorry' HexHensel/QuadraticMultifactor.lean`

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` was introduced.

## Current frontier

#4433 is complete. The next issue in this Hensel chain is #4434, which should
wrap the new factors-mod-`p` invariant constructor for `choosePrimeData`.

The constructor intentionally requires `factors ≠ []`; the empty-list case of
`QuadraticMultifactorLiftInvariant` asks for congruence modulo `p^k`, which is
not derivable from only the supplied mod-`p` boundary product congruence.

## Next step

Work #4434 and choose the exact public `choosePrimeData` boundary surface that
feeds `QuadraticMultifactorCoprimeSplits`.

## Blockers

None.
