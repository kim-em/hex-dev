# Schmeisser zero-control substrate

Session: `57287621` (work, issue #5664)

## Accomplished

Claimed #5664 after #5667 raced and was claimed by another agent. Added a
derivative-free Schmeisser zero-control substrate in
`HexPolyZMathlib/SchurSzego.lean`.

The new API names exterior root counts over `Polynomial.roots` multisets,
closed-disk root hypotheses, radius-wise domination from a threshold, the
Schmeisser composition zero-control conclusion, and the degree-indexed
Grace-Walsh-Szego/de Bruijn-Springer source predicate. It also packages these
into root-count, `rootsRadiusProduct`, and coefficient-form wrappers without
introducing new analytic assumptions into derivative adapters.

## Current frontier

The hard analytic theorem itself remains to be proved as an inhabitant of
`graceWalshSzegoZeroControlAtDegree n`. The surrounding algebraic and multiset
handoff now has a concrete no-sorry theorem surface for later specialization to
Schmeisser composition root-count domination.

## Next step

Use `graceWalshSzegoZeroControlAtDegree` as the source target for the next
Schmeisser issue, then specialize it to the existing
`rootsRadiusProduct_le_of_graceWalshSzegoZeroControlAtDegree` wrappers and the
derivative-kernel adapters in `HexPolyZMathlib/RobinsonForm.lean`.

## Blockers

None for this substrate layer.

Verification completed:

- `lake build HexPolyZMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexPolyZMathlib/SchurSzego.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

Unrelated worktree state:

- `.claude/CLAUDE.md` was already modified and was left untouched.
