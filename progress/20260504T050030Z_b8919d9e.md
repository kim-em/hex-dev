# 2026-05-04 05:00Z — feature session, issue #1986

## Accomplished

- Closed #1986 (Oracle infrastructure bootstrap).  Lands the worked
  python-flint cross-check for `hex-poly` plus the shared
  infrastructure every per-library oracle (#1987-#2004, #2023) will
  reuse.  Specifically:
  - `scripts/oracle/__init__.py` and `scripts/oracle/common.py`:
    JSONL fixture/result schema, JSON failure-record schema,
    `read_fixtures` / `split_fixtures_results` / `write_failure` /
    `assert_equal` helpers (stdlib only).
  - `Hex/Conformance/Emit.lean` (new top-level `lean_lib Hex`):
    `emitPolyFixture` / `emitMatrixFixture` / `emitLatticeFixture` /
    `emitPrimeFixture` / `emitResult`, plus `polyValue` and
    `divModValue` helpers for shaping result-record payloads.  Hand-
    rolled JSON serializer keeps `Hex` dependency-free.
  - `HexPoly/EmitFixtures.lean` + `lean_exe hexpoly_emit_fixtures`:
    bootstrap fixture set covering `mul` (4 cases) and `divmod` (2
    exact-division cases) over `Hex.DensePoly Int`.  `gcd` is
    deferred — `Hex.DensePoly Int.gcd` truncates and so does not
    match `fmpz_poly.gcd`'s primitive associate; per-library issue
    #1987 will re-emit those cases over `Rat`.
  - `scripts/oracle/poly_flint.py`: reads the JSONL stream, re-runs
    each `mul` / `divmod` through `flint.fmpz_poly`, writes failure
    records on mismatch, exits non-zero.  `--check` runs against the
    committed sample at `conformance-fixtures/HexPoly/poly.jsonl`
    so the driver is testable without a Lean rebuild.
  - `.github/workflows/conformance.yml`: new `oracle-pyflint` job
    `pip install`s python-flint, builds the emit binary, diffs the
    fresh JSONL against the committed sample (drift sentinel), pipes
    the emission into the oracle, and uploads any failure records as
    an artifact.
  - `SPEC/testing.md` § Infrastructure contract: filled in the
    deferred placeholder with the artefact layout actually built and
    a step-by-step procedure for adding a new oracle.
  - `scripts/conformance_targets.py` regex tightened to require
    end-of-line so `import Hex.Conformance.Emit` no longer trips the
    orphan-import warning.
  - `scripts/libgraph.py` `KNOWN_EXCEPTIONS` adds `Hex` so the
    infrastructure library is treated like `HexManual` (no
    `libraries.yml` phase entry).
  - `.gitignore` ignores `conformance-failures/*.json`; the directory
    sentinel `README.md` stays tracked.
- Verified end-to-end: `lake build` green; `lake exe
  hexpoly_emit_fixtures` produces 18 JSONL records that match the
  committed sample; `python3 scripts/oracle/poly_flint.py --check`
  passes with python-flint 0.8.0; deliberately corrupting one
  result line trips the oracle and writes a JSON failure record.

## Current frontier

Per-library oracle wiring (#1987 HexPoly mul/gcd/divmod, then the
sibling library issues) becomes mostly mechanical replication of
the worked example.  The pattern is:

1. Add `Hex<X>/EmitFixtures.lean` defining a `main` that calls the
   shared `Hex.Conformance.Emit` helpers.
2. Register `lean_exe hex<x>_emit_fixtures`.
3. Commit a sample at `conformance-fixtures/Hex<X>/<topic>.jsonl`.
4. Add `scripts/oracle/<lib>_<oracle>.py` mirroring `poly_flint.py`.
5. Extend `oracle-pyflint` (or add a sibling job) in
   `conformance.yml`.

`gcd` cross-checks specifically need rational-coefficient fixtures —
the bootstrap intentionally avoids them.

## Next step

Pick up #1987 (HexPoly python-flint oracle for mul/gcd/divMod
cross-checks).  Now that this PR closes #1986, the planner's
unblock pass will clear `blocked` from #1987 (and the rest of the
oracle cluster) on the next loop.

## Blockers

None.
