# 2026-05-20T15:25Z — work session e78ea818

## Accomplished

Claimed #5633 first, found it stale, and skipped it: the public
`rootsRadiusProduct` theorem is now tracked by #5666 and remains blocked on
#5665.

Claimed #5674 and advanced the monicised-core Hensel correspondence surface in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- Added `monicisedCoreLiftData_liftedFactor_monic`,
  `monicisedCoreLiftData_liftedFactor_injective`, and
  `monicisedCoreLiftData_liftedFactor_natDegree_pos` wrappers over the existing
  `henselLiftData` invariant lemmas on
  `(Hex.MonicisedCoreData.ofCore core).monicCore`.
- Updated stale `factorsModPBerlekampForm` destructuring after the executable
  API stopped storing an explicit field instance; local proofs now reconstruct
  it from `hprime`.
- Replaced a fragile hand proof of nonzero monic modular images with
  `Hex.monicModularImage_ne_zero_of_ne_zero`.
- Migrated the exhaustive-branch irreducibility bridge signatures from the old
  precision-as-`B` correspondence shape to the raw-bound
  `Hex.monicisedCoreLiftData` shape.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Basic` succeeds.  The file still has
the existing localized analytic `sorry`s; this session did not add new `sorry`,
`axiom`, `native_decide`, `TODO`, or `FIXME`.

## Next step

Continue #5674/#5675 by wiring the new monicised invariant wrappers into the
next transport constructor that supplies the modular-product congruence for
`(Hex.MonicisedCoreData.ofCore core).monicCore`.

## Blockers

No external blocker.  The remaining hard part is the monicised-core modular
transport theorem, which is already separated into the successor issue chain.
