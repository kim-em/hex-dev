# 20260509T164519Z - Issue #2809

## Accomplished

Claimed human-oversight issue #2809 and repaired the five inconclusive HexLLL
Phase-4 scientific benchmark registrations:

- narrowed `runSizeReduceChecksum` to the stable `128..160` schedule;
- raised measurement signal for `runGramSchmidtCoeffChecksum`;
- raised measurement signal for `runOfBasisHarshCubicChecksum`;
- changed the random-bounded and harsh-cubic public first-short-vector models
  to match the committed near-orthogonal fixture paths rather than the
  worst-case swap-count model.

Refreshed the HexLLL headline scientific artefact and report:

- `reports/bench-results/hex-lll-885431e.json`
- `reports/hex-lll-performance.md`

Verification completed:

- `lake exe hexlll_bench run` on the five affected registrations, exported to
  `/tmp/hex-lll-affected-final.json`: all five verdicts consistent.
- Full scientific report run exported to
  `reports/bench-results/hex-lll-885431e.json`: all parametric verdicts
  consistent; fixed hashes matched.
- `lake exe hexlll_bench verify Hex.LLLBench.runSizeReduceChecksum
  Hex.LLLBench.runGramSchmidtCoeffChecksum
  Hex.LLLBench.runFirstShortVectorHarshCubicChecksum
  Hex.LLLBench.runOfBasisHarshCubicChecksum
  Hex.LLLBench.runFirstShortVectorRandomBoundedChecksum`
- `lake exe hexlll_bench list`
- `python3 scripts/check_phase4.py`
- `lake build`

Also ran full `lake exe hexlll_bench verify`; all non-fpylll registrations
passed, and the only failures were the three known `fpylll` comparator
registrations because this host still lacks the Python `fpylll` package.

## Current frontier

#2809's scientific benchmark failures are repaired in the local branch. The
report now keeps only #2810 in HexLLL Concerns, since the missing informational
fpLLL ratios are still host/tooling-dependent.

The worktree still has the pre-existing unstaged `.claude/CLAUDE.md`
modification, which was not touched.

## Next step

Commit `HexLLL/Bench.lean`, `reports/hex-lll-performance.md`,
`reports/bench-results/hex-lll-885431e.json`, and this progress file; then
create the PR for #2809.

## Blockers

Full unfiltered `lake exe hexlll_bench verify` remains blocked by #2810 until
the benchmark host has `fpylll` installed or those informational comparator
registrations are otherwise made scheduled-only.
