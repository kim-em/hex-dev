# 2026-05-05 19:07 UTC — work session (915eeb5d)

## Accomplished

Claimed #2332 and discharged `HexMatrixMathlib/Determinant.lean::det_eq`.
The proof now converts the Hex determinant fold over permutation vectors into
a `Finset` sum over `Equiv.Perm (Fin n)`, reuses the existing
`PermutationVector.equivs` and `detSign_eq_permSign` bridges, converts
`List.finRange` products to `Finset` products, and reindexes Mathlib's
`Matrix.det_apply'` expansion by inverse permutations.

## Current frontier

The determinant bridge theorem is no longer a `sorry`. Downstream Matrix
Bareiss issues can now depend on the public `det_eq` theorem instead of a
placeholder.

## Next step

Pick up #2323 to build the no-pivot Bareiss loop invariant, then #2324 and
#2315 can compose it into the no-pivot and row-pivoting Bareiss determinant
proofs.

## Blockers

None for this session. `lake build HexMatrixMathlib` still reports pre-existing
`sorry` warnings in `HexMatrix.RREF`, `HexMatrix.Bareiss`,
`HexMatrixMathlib.Basic`, and `HexMatrixMathlib.RankSpanNullspace`.
