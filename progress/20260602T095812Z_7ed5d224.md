# 2026-06-02 09:58 UTC — feature session (HexGF2 Phase 6 CLMUL automation)

Session: `7ed5d224`. Issue: #6299.

## Accomplished

- Annotated `clmul_xor_left` and `clmul_xor_right` in `HexGF2/Clmul.lean`
  with `@[grind =]`, exposing equation-style XOR-linearity to `grind`
  without changing the trusted runtime contract.
- Added a proof-mode block to `HexGF2/Conformance.lean` exercising the
  Phase 6 deliverables:
  - packed `GF2Poly` addition cancellation (`p + p = 0`,
    `p + (p + q) = q`, `(p + q) + q = p`) via `simp` using the existing
    `@[simp]` lemmas `add_self`, `add_add_cancel_left`,
    `add_add_cancel_right`;
  - CLMUL zero (`clmul 0 x = (0, 0)`, `clmul x 0 = (0, 0)`) via `simp`
    using the existing `@[simp]` `clmul_zero_left` / `clmul_zero_right`;
  - CLMUL XOR-linearity on both sides via `grind`, exercising the new
    `@[grind =]` annotations.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Conformance`,
  and `lake build HexGF2`, plus `python3 scripts/check_dag.py` and
  `git diff --check`.

## Current frontier

`HexGF2` Phase 6 proof polishing on the CLMUL automation surface is
closed for the deliverables in #6299. Executable definitions, the
extern wrapper, packed multiplication internals, and the existing
value-level fixtures are unchanged.

## Next step

Other Phase 6 libraries still have unclaimed automation smoke checks
(e.g. #6305 HexArith extended-GCD). #6300 (HexGramSchmidt prevPivot
lemma) was claimed by a concurrent agent and lost the race here.

## Blockers

None.
