# HO-5d-2b — HBZ Mathlib bridge wrappers off total choosePrimeData

Issue: #5854.
Session UUID prefix: 274301d2.

## Accomplished

Migrated all 11 bridge wrappers in `HexBerlekampZassenhausMathlib/Basic.lean`
to take an explicit `primeData : Hex.PrimeChoiceData` parameter paired with
`hchoose : Hex.choosePrimeData? core = some primeData`, replacing direct
references to `Hex.choosePrimeData (Hex.normalizeForFactor f).squareFreeCore`
in the bridge statements.

Wrappers migrated:

- `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_count`
- `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
- `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
- `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
- `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
- `existsUnique_modPFactorSubset_of_choosePrimeData`
- `modPSubsetPartitionHypotheses_of_choosePrimeData`
- `henselSubsetLiftHypotheses_of_choosePrimeData_henselLiftData_descent`
- `henselSubsetLiftHypotheses_of_choosePrimeData_henselLiftData`
- `henselSubsetCorrespondenceHypotheses_of_choosePrimeData_success`
- `henselSubsetCorrespondenceHypotheses_of_choosePrimeData_success_descent`

Threaded `hchoose` through the IntReductionMod cascade that consumes
`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`:

- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`

The top-level `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
public surface now takes an `hchoose` parameter alongside the existing
`hselected` premise.

## Current frontier

`lake build HexBerlekampZassenhausMathlib` fails on three pre-existing
errors at `HexBerlekampZassenhausMathlib/Basic.lean:5099,5994,6280` —
all three are `Hex.isGoodPrime_squareFreeModP` call sites whose local
proof expects `Hex.DensePoly.gcd ... = 1` but the upstream API has
returned the weaker `Hex.squareFreeModP` (i.e. `gcdIsUnit gcd = true`)
since PR #5779. The baseline build (before my changes) reports the
identical three errors. My migration does not introduce any new
errors; verified by comparing baseline build output to post-change
build output (same five error lines).

## Next step

A separate PR is needed to migrate those three call sites off the
`gcd = 1` form to the post-#5779 `gcdIsUnit gcd = true` form, after
which `lake build HexBerlekampZassenhausMathlib` would succeed and
this PR's verification step would pass.

## Blockers

Pre-existing build failure described above blocks the issue's
`lake build HexBerlekampZassenhausMathlib succeeds` verification
criterion; otherwise the migration is complete per the issue's first
verification criterion (no bridge wrapper statement mentions
`Hex.choosePrimeData` directly).
