# Issue #6217: BHKS CLD coefficient-bound replan

## Accomplished

- Claimed #6217 after confirming the only unclaimed directive was already
  claimed and the older directives were blocked.
- Chose the SPEC-preserving resolution for the false
  `abs_cldCoeffs_le_bhksCoeffBound` target: repair the executable cut path so
  `cldCoeffs` centers the ambient `p^a` representative before the lower `Psi`
  cut.
- Created successor issue #6220 for the executable CLD cut correction and
  restored Lemma 5.1 column-bound bridge.
- Created successor issue #6221 for the `BHKS.auxiliaryPolynomial` shape repair,
  blocked on #6220.
- Added `reports/bhks-cld-coeff-bound-replan.md` and updated the two BHKS D1
  frontier reports so downstream work points at #6220/#6221 instead of the
  disproved #5224 path.

## Current frontier

- #6217 is resolved as a planning/replan artifact.
- #6220 is the next executable/proof step for the CLD column-bound path.
- #6221 follows #6220 and should repair the bad-vector auxiliary-polynomial
  shape before #5512/#5204 consume it.

## Next step

- Open a PR closing #6217 with the replan report and successor-issue links.

## Blockers

- None for #6217.
- Verification passed: `git diff --check`, report meta-commentary grep,
  `python3 scripts/check_dag.py`, and full `lake build` (244 jobs).
