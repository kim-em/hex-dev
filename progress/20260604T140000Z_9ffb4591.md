# 2026-06-04T14:00:00Z — Add unconditional scaledCoeffs_lower_eq_det_scaledCoeffMatrix (partial #6547)

Session UUID prefix: 9ffb4591
Issue: #6547

## Accomplished

- Added public Mathlib-side unconditional theorem
  `scaledCoeffs_lower_eq_det_scaledCoeffMatrix` to
  `HexGramSchmidtMathlib/Int.lean`, just after the bridge wrapper
  `scaledCoeffs_lower_eq_zero_of_singularStep_lt`.
- Statement: for `j.val < i.val`, the Rat-cast of the executable integer
  scaled Gram-Schmidt coefficient equals the Rat-cast of the Mathlib
  Leibniz determinant of the Cramer minor `scaledCoeffMatrix b i j hji`.
- Proof case-splits on whether the no-pivot Bareiss pass over
  `gramMatrix b` reaches column `j.val` without a singular step:
  non-singular delegates to the existing
  `scaledCoeffs_lower_eq_det_scaledCoeffMatrix_of_no_singular` (Stage B,
  #6519); singular composes `scaledCoeffs_lower_eq_zero_of_singularStep_lt`
  (Stage C' #6546 bridge wrapper) with
  `scaledCoeffMatrix_det_eq_zero_of_singularStep_lt` (existing helper at
  line 1569).
- `lake build` clean across all 244 targets.
- Sorry counts unchanged: `HexGramSchmidt/Int.lean` 3 (pre-existing at
  6136 prose, 6149 Bareiss-leading-prefix #5655 sorry, 6463 residual);
  `HexGramSchmidtMathlib/Int.lean` 0.

## Current frontier

This is deliverable 1 of #6547's four-part work plan. Deliverable 2
(decircularising three bridge theorems via the new theorem) and
deliverable 3 (closing the residual `getArrayEntry_scaledCoeffRowsSchur_eq`
sorry at HexGramSchmidt/Int.lean line 6463) and deliverable 4 (full
consumer migration to bridge) remain.

## Next step

The residual sorry closure (deliverable 3) is the gating piece — until it
lands, the new theorem (1) and the eventual reproofs of the three
bridge theorems (`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`,
`scaledCoeffRows_lower_eq_coeffs`, `scaledCoeffs_eq`) all remain
transitively tainted by the sorry through
`scaledCoeffs_lower_eq_det_scaledCoeffMatrix_of_no_singular` →
`scaledCoeffs_eq_scaledCoeffMatrix_det` →
`scaledCoeffs_eq_scaledCoeffMatrix_bareiss` → the Mathlib-free
`scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix` and
`scaledCoeffs_eq_zero_of_singularStep_lt` helpers.

Closing the residual sorry requires:

1. Adding `(hquot : StepWitness b) (hi : i < n)` to Mathlib-free
   `getArrayEntry_scaledCoeffRowsSchur_eq`. Because the proof needs to
   call sub-D (line 8002, currently 7895 after move-prep deletions)
   and C' (line 8147, currently 8040), the theorem must be relocated
   to after C' to satisfy Lean's forward-reference constraints.
2. Writing helper lemmas for the residual case:
   - Strict-lower non-singular Bareiss-side bridge (extractable from
     `scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix` at line
     7331's intermediate `h_state_level`).
   - Diagonal non-singular Bareiss-side bridge (uses
     `scaledCoeffArrayLoop_diag_matches` plus
     `noPivotLoop_diag_of_le_step` for stability across fuel `j` →
     fuel `n`).
   - Diagonal singular Bareiss-side bridge (via `scaledCoeffArrayLoop_diag_matches`
     singular branch with `s ≤ i` sub-case).
3. Propagating `(hquot : StepWitness b)` through the consumer chain
   in HexGramSchmidt/Int.lean: `gramDetVec_eq_gramDet` (line 6465),
   `scaledCoeffs_diag_toNat` (6488), `scaledCoeffs_diag_eq_zero_or_eq_*`
   helpers, `scaledCoeffs_diag_of_nonneg`,
   `scaledCoeffs_eq_zero_of_singularStep_lt`,
   `scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix`.
4. Moving `ofBasis_valid` from `HexLLL/Basic.lean:432` to
   `HexLLLMathlib/Basic.lean` (so it can discharge `hquot` via
   `StepWitness.ofGram b`).
5. Updating bridge consumers (`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`
   at line 1681, `scaledCoeffs_eq_scaledCoeffMatrix_bareiss` at 1729,
   and `scaledCoeffs_diag` at 2426) to pass `StepWitness.ofGram b`.

After (1)-(5), the new theorem (1) and the three bridge theorems
automatically become untainted, and the reproofs in deliverable 2 can
be applied without further taint chasing.

## Blockers

None — this is incremental partial progress. The directive permits
splitting "decircularise + new theorem" from "migrate consumers" per
the issue body's permission paragraph. The remaining structural work
(steps 1-5 above) is well-scoped for a successor session.
