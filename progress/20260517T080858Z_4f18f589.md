# 20260517T080858Z — work session 4f18f589 (#4594 decomposed)

## Accomplished

**#4594 (HO-1 substrate, quadratic arms `hcomplete` discharger)** —
decomposed into three sub-issues and skipped for planner triage.

Scoping confirmed that the structural fact
`(normalizeForFactor f).repeatedPart = ∏ q^{e_q}` (ranging over
`coreFactors`) **cannot** be proven Mathlib-free against the current
`Hex.ZPoly` machinery: the obligation reduces to
`Associated (radical (toPolynomial primitivePart)) (toPolynomial squareFreeCore)`
over `Polynomial ℤ`, which in turn requires the char-0 identity
`radical p = p / gcd(p, p')`. `Hex.ZPoly` has no `radical`,
`normalizedFactors`, or `divRadical ∣ derivative` lemma — and the
existing `Squarefree (toPolynomial squareFreeCore)` bridge
(`IntReductionMod.normalizeForFactor_squareFreeCore_toPolynomial_squarefree`,
`HexBerlekampZassenhausMathlib/IntReductionMod.lean:872`) alone does
not imply that `squareFreeCore` captures all distinct prime factors of
`primitivePart`. This mirrors the rerouting decision made for the
small-mod singleton sibling (#4597, supersedes #4593).

Created sub-issues:

* **#4602** — Mathlib structural radical-relation. Mathlib-side fact
  in `HexBerlekampZassenhausMathlib/IntReductionMod.lean` deriving the
  `repeatedPart = ∏ q^{e_q}` decomposition via `radical` /
  `divRadical_dvd_derivative` (`Mathlib/RingTheory/Polynomial/Radical.lean:35`)
  over `Polynomial ℚ` plus Gauss-style primitive descent. Heavy lift.
* **#4603** — Mathlib-free expansion helper. Pure
  `consumeExactPower` / `expandRepeatedPartFactorArray` induction
  showing the residual reduces to `1` given the `∏ q^{e_q}`
  decomposition plus pairwise non-association. No Mathlib dependence.
* **#4604** — Final discharger assembly. Combines #4602 + #4603 plus
  two small plumbing lemmas (polyProduct identity for
  `quadraticIntegerRootFactors?`, pairwise non-association of splitter
  outputs) into the public
  `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`. Depends on
  both #4602 and #4603 via `coordination add-dep` (label `blocked`).

Breadcrumb comment on #4594 documents the decomposition; `coordination
skip` released the claim and marked it `replan` for the next planner
to close or narrow.

## Current frontier

#4594 is sitting in the planner queue with the decomposition
breadcrumb. The next planner can close it once #4602/#4603/#4604 land,
or narrow it to whatever residual scope it sees fit.

Of the four arm dischargers requested by parent umbrella #4590:

* Constant arm (#4585): claimed and in flight by another agent.
* Small-mod singleton arm (#4597): unclaimed; full plan in place
  including the Mathlib-side structural fact and the expansion helper.
* Quadratic arms (#4594): decomposed here into #4602/#4603/#4604.
* Exhaustive arm (#4561): blocked on #4567 (factorsModP_monic).

## Next step

A worker can pick up #4603 immediately (pure Mathlib-free; independent
of #4602). #4602 is a self-contained Mathlib-side proof effort — best
tackled by someone comfortable with Mathlib's `radical` / `Squarefree`
/ Gauss-descent machinery. #4604 is blocked until #4602 and #4603
both land.

Note that #4603 (the expansion helper) overlaps in flavour with
#4597's deliverable 2 (`expandRepeatedPartFactorArray_pow_singleton`,
the singleton-`q` specialisation): if #4597 lands first, #4603 can
generalise its proof; if #4603 lands first, #4597's deliverable 2
becomes a one-line specialisation.

## Blockers

None for this decomposition. The three new sub-issues stand on
existing infrastructure (the heavy Mathlib structural fact has a clear
proof outline; the expansion helper has all needed Mathlib-free
machinery already exposed).
