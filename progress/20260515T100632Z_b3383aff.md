# Issue #4205

## Accomplished

- Claimed feature issue #4205 (BZ certificate soundness: prove UFD
  subset-degree lemma for modular factors).
- In `HexBerlekampZassenhausMathlib/UFDPartition.lean`, added two new
  theorems:
  - `normalizedFactors_le_map_normalize_of_dvd_prod_irreducibles`: the
    abstract UFD subset-factor lemma. In any
    `[CommMonoidWithZero] [NormalizationMonoid] [UniqueFactorizationMonoid]`
    `α`, a non-zero divisor of a product of irreducibles has its
    `normalizedFactors` bounded above by `qs.map normalize`.
  - `natDegree_eq_sum_subset_of_dvd_prod_irreducibles`: the polynomial
    subset-degree packaging needed by the BZ certificate degree-obstruction
    consumer. Over `[Field K]` `[DecidableEq K]`, a non-zero divisor `g` of
    a product of irreducible polynomials `qs` has `g.natDegree` equal to a
    sub-multiset sum of `qs.map natDegree`.
- Added the two minimal extra imports needed for the polynomial form
  (`Mathlib.Algebra.Polynomial.BigOperators` for `natDegree_multiset_prod`
  and `Mathlib.Algebra.Polynomial.FieldDivision` for `degree_normalize`).
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.UFDPartition`
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME` — none.

## Current frontier

- Bridge C from #4173 (UFD subset-degree lemma for modular factors) is
  available.
- The polynomial form takes `Polynomial K` over a `Field` and produces the
  subset-sum decomposition over `Multiset Nat`, which is what the
  `hasSubsetDegree`-style obstruction in
  `HexBerlekampZassenhaus/Basic.lean` ultimately needs to contradict.

## Next step

- Use `natDegree_eq_sum_subset_of_dvd_prod_irreducibles` in #4173 to
  derive the contradiction with `primeData.hasSubsetDegree
  obs.targetDegree = false` for the proper-factor modular image, once the
  remaining bridges (#4203 / PR #4207, #4204, #4206) are in place.

## Blockers

- None for issue #4205.
