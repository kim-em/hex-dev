# 2026-05-07 22:59 UTC - Bareiss logical-source row-swap transport (e03fd82b)

## Accomplished

Claimed issue #2553 and added the row-swap transport lemmas needed by the
row-pivoted Bareiss invariant in `HexMatrixMathlib/Determinant/Bareiss.lean`:

- `leadingPrefix_rowSwap_eq_of_le`: swapping source rows `kFin` and `pivot`
  with both indices `≥ k` leaves the size-`k` leading prefix unchanged.
- `rowSwap_getElem_of_ne` / `rowSwap_getElem_swap_eq`: helper row-equality
  surfaces over `Hex.Matrix.rowSwap_getElem`.
- `borderedMinor_rowSwap_source_row`: the bordered-minor at level `k` of
  `rowSwap M kFin pivot` equals the bordered-minor of `M` with the border
  row index permuted by the swap (`if i = pivot then kFin else if i = kFin
  then pivot else i`), under `kFin.val = k` and `k + 1 ≤ pivot.val`.
- `bareissPivotInvariant_regular_swap`: transports `BareissNoPivotInvariant
  source state` to `BareissNoPivotInvariant (rowSwap source k pivot)
  {state with matrix := rowSwap state.matrix k pivot, rowSwaps := +1}` in
  the pivot-search `some pivot` branch. The `prevPivot_eq` field uses the
  leading-prefix invariance helper; `trailing_eq` uses the bordered-minor
  transport plus `hinv.trailing_eq` at the swap-permuted index.

Verification:

- `lake build HexMatrixMathlib.Determinant.Bareiss`
- `lake build HexMatrixMathlib.Determinant`
- `lake build` (full project build clean)
- `python3 scripts/check_dag.py`
- `git diff --check`
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`.

## Current frontier

The row-pivoted Bareiss proof now has the source row-swap transport piece
plus the existing no-swap step (`bareissPivotInvariant_regular_no_swap`)
and pivot-search-failure surfaces. The remaining work is composing these
into the recursive `pivotLoop` invariant (#2554).

## Next step

Wire the swap transport + no-swap step into the row-pivoted Bareiss
recursive invariant in #2554.

## Blockers

None for this slice. The pre-existing pod-managed `.claude/CLAUDE.md`
worktree modification remains untouched.
