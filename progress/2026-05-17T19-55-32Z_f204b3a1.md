# 2026-05-17T19:55Z — non-monic exactQuotient bridge wrappers (issue #4774)

## Accomplished

- Added `Hex.exactQuotient?_eq_some_of_pos_lc_pos_degree_mul_eq` in
  `HexBerlekampZassenhaus/Basic.lean`. Non-monic converse to
  `exactQuotient?_product`: from `0 < lc candidate`, `0 < candidate.degree?.getD 0`,
  and `quotient * candidate = target`, concludes
  `exactQuotient? target candidate = some quotient`. Discharges
  `shouldRecordPolynomialFactor` from positive degree (`0`, `C 1`, `C (-1)`
  all have `degree?.getD 0 = 0`), then composes
  `divMod_eq_of_pos_lc_pos_degree_mul_eq` (from #4773) with the existing
  `exactQuotient?_eq_some_of_divMod_eq_of_shouldRecord` packaging lemma.
- Added `exactQuotient?_scaledRecombinationCandidate_eq_some_of_eq_factor_of_primitive_pos_lc`
  in `HexBerlekampZassenhausMathlib/Basic.lean`. Non-monic scaled-candidate
  bridge mirroring the existing `…_eq_some_of_eq_factor` (monic) variant,
  consuming the new substrate wrapper instead of the monic-only one. Same
  proof skeleton: extract the cofactor from `factor ∣ target`, route the
  multiplication through the wrapper.

## Current frontier

Both wrappers are landed and feed the #4647 primitive recursive
recombination coverage assembly. Build clean across
`HexBerlekampZassenhaus`, `HexBerlekampZassenhausMathlib`, and
`scripts/check_dag.py`. No new `sorry`/`axiom`/`native_decide`/`TODO`/
`FIXME` entries.

## Next step

- #4647 (HO-1 reassembly) is now unblocked on the non-monic
  exactQuotient bridge side.

## Blockers

None.
