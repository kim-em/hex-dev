## Accomplished

- Claimed human-oversight issue #2715 for the `HexGfq` Phase-4 audit reset follow-up.
- Re-evaluated `SPEC/Libraries/hex-gfq.md` and `HexGfq/Bench.lean` against the post-#2687 requirements in `PLAN/Phase4.md`, `SPEC/benchmarking.md`, and `SPEC/profiling.md`.
- Confirmed `reports/hex-gfq-performance.md` is absent and `libraries.yml` has no `HexGfq.phase4.input_families` metadata, so the required headline report/profile evidence package is not present.
- Confirmed the existing `hexgfq_bench` registrations still smoke-check: `lake exe hexgfq_bench list` reports 5 benchmarks and `lake exe hexgfq_bench verify` passes all 5.
- Rolled `HexGfq.done_through` from 4 to 3 in `libraries.yml`.
- Filed re-promotion follow-up #2745 for the missing input-family metadata, scientific verdicts, profile evidence, headline report, and Phase-4 re-promotion work.

## Current frontier

- `HexGfq` no longer claims Phase 4 without the new evidence package.
- The current bench module is still useful for the committed generic/packed convenience-wrapper surface, but Phase-4 re-promotion needs structured input families, scientific runs, profile evidence, and a headline report with empty Concerns.

## Next step

- Work #2745 to define the `HexGfq` Phase-4 input families, gather traceable benchmark/profile evidence, produce `reports/hex-gfq-performance.md`, and re-promote `HexGfq` only if every exit criterion is satisfied.

## Blockers

- None for the rollback. Re-promotion depends on completing the evidence package in #2745.
