# 2026-05-10 18:06 UTC — feature session #3035

## Accomplished

- Claimed issue #3035 (HexBZ Mathlib forward-verification bridge) and reset
  the agent worktree onto `origin/main`.
- Lifted `private` on `bhksIndicatorOneCount`, `bhksIndicatorAllOnes`,
  `bhksDegenerateIndicatorPartition`, `bhksIndicatorCandidates?`, and the
  `bhksLatticeBasis_independent` proof in `HexBerlekampZassenhaus/Basic.lean`
  so the new bridge can name them. Added doc comments while exposing them;
  executable definitions are unchanged.
- Created `HexBerlekampZassenhausMathlib/Recovery.lean` with the SPEC
  Group D forward-verification bridge:
  * Helpers `latticeBasisOfLiftData`, `HasPositiveDimension`,
    `projectedRowsOfLiftData`, `equivalenceClassIndicatorsOfLiftData` extract
    the executable `Hex.bhksRecover?` building blocks at any
    `(f, d : LiftData)`.
  * `EquivalenceClassRecoveryHypotheses` and the matching theorems
    `bhksEquivalenceClassIndicators_eq_of_recovery` /
    `bhksEquivalenceClassIndicators_projectedRows_eq_of_recovery` package
    BHKS Lemma 3.3 / B7 as the bridge from `L' = W` to the executable
    indicator output (deliverable 1).
  * `RecoveryHypotheses` packages the four guards inside `Hex.bhksRecover?`
    (rows positivity, non-degenerate partition, per-indicator
    reconstruction, exact product), and `bhksRecover_eq_some_of_recovery`
    proves the executable returns `some <expected factors>` under those
    guards. The proof unfolds `Hex.bhksRecover?` and reduces each branch
    via `dif_pos` / `simp only`.
  * `ForwardRecoveryInputs` is the SPEC-input layer: it explicitly carries
    `lattice_eq_indicators` (the conclusion shape of #3034's cap separation
    theorem), `mignotte_precision`
    (`p^k > 2 · defaultFactorCoeffBound f`), the abstract B7 `indicators_match`,
    and the abstract A2 `candidates_eq` and `product_eq` obligations.
    `ForwardRecoveryInputs.toRecoveryHypotheses` promotes the SPEC bundle to
    `RecoveryHypotheses`, and `bhksRecover_eq_some_of_forwardInputs` is the
    headline theorem (deliverable 2).
- Wired `Recovery` into `HexBerlekampZassenhausMathlib.lean`.
- Verified `lake build HexBerlekampZassenhausMathlib`,
  `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, and
  `git diff --check`. No new `axiom`, `native_decide`, `TODO`, `FIXME`, or
  `sorry` introduced; the only sorries on the touched lines are pre-existing
  in `bhksLatticeBasis_independent` and elsewhere.

## Current frontier

The bridge is intentionally abstract over the residual SPEC obligations:

- B7 (`indicators_match`): the executable equivalence-class indicators
  match a target list under `L' = W`. Discharging this depends on
  executable `Matrix.rref` correctness (HexMatrix Phase 5) plus a finite
  case analysis of the BHKS Lemma 3.3 signature partition.
- A2 (`candidates_eq`): the centred-residue lift recovers integer
  coefficients exactly under `p^k > 2 · defaultFactorCoeffBound f`. This
  is the Mignotte / Hensel reconstruction obligation; it consumes the
  Mignotte bound already in `HexPolyZMathlib/Mignotte.lean`.
- `product_eq` and `nondegenerate`: structural consequences once B7 + A2
  land.

## Next step

Open a sub-issue (or task in PLAN/) tracking the discharge of `B7`
(`indicators_match`) once HexMatrix Phase 5 RREF correctness lands, and a
companion sub-issue for A2 (`candidates_eq`) using the Mignotte coefficient
bound from `HexPolyZMathlib/Mignotte.lean`. A subsequent issue can then
chain `bhksRecover_eq_some_of_forwardInputs` into the outer
precision-doubling loop (`factorFastCoreWithBound`) toward the final
`factorFast_terminates` theorem (#2567).

## Blockers

None for this session — the issue's deliverables were narrowly scoped to a
single precision/recovery call, and #3034 (cap separation) has already
landed. Future bridge work is tracked above.
