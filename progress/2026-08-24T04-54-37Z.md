# Runtime-backed public interval tactic

## Accomplished

- Replaced the tactic's manually assembled arithmetic actions and independent
  replay path with the typed runtime registry, branch, controller, retained
  lineage, and sealed `RuntimeEmit` result path.
- Closed the emitted target against the caller's exact source expressions and
  proofs, including a kernel-checked recursive evaluation bridge from the
  reified term back to the original Lean expression.
- Made runtime limits and chronology accounting match controller execution,
  and made an unsettled controller frontier fail closed.
- Updated the precision-starvation conformance canary to require that
  fail-closed controller boundary.
- Added an exact two-action/one-short chronology canary for the public
  controller integration.
- Removed the superseded arithmetic-result projections and manual operation
  membership helpers.
- Reconciled both interval SPECs and the conformance overview with the typed
  RuntimeRule → controller → terminal → RuntimeEmit architecture.
- Built `HexIntervalMathlib.Tactic` and
  `HexIntervalMathlib.TacticConformance` successfully.

## Current frontier

The public tactic implementation is complete on top of the typed emitter and
expression-bridge stack. It still needs to be restacked onto the squash merges
of those dependency PRs, reviewed as an exact diff, and opened as its own PR.

## Next step

Restack after the RuntimeRule and RuntimeEmit layers merge, rerun the full
targeted build and trust-surface checks, then request the independent review
and open the public-tactic PR.

## Blockers

None.
