# Session 20260604T044721Z_b550fcf9

Issue: #6508.

## Accomplished

Replaced the bounded post-prefix `choosePrimeData?` extension in
`HexBerlekampZassenhaus/Basic.lean` with an input-fuelled odd-candidate walk.
The fixed `smallPrimeCandidates` prefix still uses the existing scored fold and
therefore preserves small-prime tie-breaking; only the fallback after that
prefix now walks candidates via `Hex.Nat.isPrimeTrial` and stops at the first
good prime.

Added walk-specific proof helpers so the existing public certification lemmas
still compile:

- `choosePrimeData?_prime`
- `choosePrimeData?_fModP_eq`
- `choosePrimeData?_isGoodPrime`
- `choosePrimeData?_factorsModP_berlekamp_form`

Retained `finitePrimeSearchNoneQuadratic` as a regression fixture and changed
its documentation from "current finite-search failure witness" to "old bounded
search regression." The new guard confirms `choosePrimeData?` now selects a
prime past the former closed candidate set.

Updated the conformance cascade wording to refer to the post-prefix walk instead
of the removed `extendedSmallPrimeCandidates`, and updated the Recovery D1
comment so it no longer says the current executable finite search makes the
unconditional theorem false.

Verification completed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- diff grep for new `axiom`, `native_decide`, `TODO`, `FIXME`, `sorry`

## Current frontier

The executable prime-search candidate set is no longer the old closed fixed
list. It is still `Option`-typed, with structurally recursive fuel derived from
the input polynomial, matching the D2 bridge-theorem setup.

A stronger low-bound guard
`(factorFastFactorsWithBound finitePrimeSearchNoneQuadratic 2).isSome` was
tested and failed under first-good-prime semantics: the first good prime past
the former closed set does not force the bounded fast recombination branch to
succeed at `B = 2`. The committed regression therefore checks the prime-search
failure mode directly, not a low-bound fast-path success claim.

## Next step

The follow-up D2 issue can now prove
`choosePrimeData?_isSome_of_primitive_squareFree` against the input-fuelled walk
instead of a fixed finite candidate list.

If the project still wants an executable no-fallback fast-path guard for the
old-product quadratic, a separate issue should decide whether prime selection
may prefer a later singleton modular factor profile over the first good prime.
That policy change interacts with existing conformance runtime.

## Blockers

None for the prime-search unbounded-walk prerequisite. The low-bound fast-path
fixture expectation described above remains a separate policy/testing question.
