# Accomplished

- Completed the release-readiness implementation for issue #9115 across the
  polynomial factorization chain, including the stable Lean 4.32.2 toolchain,
  corrected `Hex.ZPoly` namespace, removal of compatibility surfaces,
  mathematical module organization, curated public umbrellas, proof splits,
  localized heartbeat budgets, documentation, README quickstarts, and release
  publisher hardening.
- Refactored the Berlekamp-Zassenhaus and CLD proof chain around one
  direct-coordinate Hensel/recombination design with an exact fallback. The
  direct lattice adequacy, recovery, support, factorization, soundness, and
  totality modules all build from a clean tree.
- Refreshed the Hex factorization performance record at clean source revision
  `75291ae2` on Lean 4.32.2 and regenerated the comparison reports and figures.
  The 392-row run solved 375 rows within ten seconds and retained unchanged
  external records where the host, corpus, CPU placement, protocol, and inputs
  were unchanged.
- Hardened publication validation and exact pin rewriting, including
  Reservoir TOML dependencies, skeleton build roots, failed-run baseline
  persistence, and workflow publication of an advanced baseline.
- Received an independent Claude Opus release review and addressed its
  substantive findings, including live skeleton requirements, exact external
  pins, publisher failure behavior, executable naming, prose defects, axiom
  guards, and performance-report consistency.
- Passed the final local release verification:
  - clean `lake build`: 9,718 jobs;
  - every CI executable and every conformance fixture emitter built;
  - publisher unit tests: 9/9;
  - release manifest: 35 split repositories plus one aggregate;
  - dependency DAG and release trust-surface checks;
  - 408 published Lean files with no `axiom`, `sorry`, or `native_decide`;
  - byte-identical 392-case factorization corpus;
  - 24-target bench verification: 175 seconds under the 360-second cap;
  - interval policy-frontier canary;
  - all required external oracles, including FLINT, PARI, NTL, and the
    Isabelle comparison infrastructure;
  - BZ trace gate: 54 traces, zero failures;
  - final BZ emitter catastrophic-timeout check.

# Current frontier

The release candidate is committed and locally green on
`release-polyfactor-9115`. The next operation is to push it, open the PR that
closes #9115, and follow its exact GitHub Actions jobs through merge.

# Next step

After the PR merges, push the three prepared unmanaged skeleton corrections
for `hex-lll`, `hex-berlekamp-zassenhaus`, and
`hex-berlekamp-zassenhaus-mathlib`, reconcile their
`release-sync-baseline` entries, run a complete live publication dry-run, run
the real sync, and verify every published repository from a fresh clone in
dependency order.

# Blockers

None.
