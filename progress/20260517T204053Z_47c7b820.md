# 2026-05-17 20:40 UTC — exhaustive-arm expansion-precondition packaging (#4806)

Session: `agent/47c7b820` (feature worker on #4806, precursor to #4627).

## Accomplished

Landed `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, just after the sibling
umbrella `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` (#4561).

The new theorem packages the three currently provable expansion-wrapper
preconditions consumed by
`Hex.expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition`
(in `HexBerlekampZassenhaus/Basic.lean`) — `shouldRecord`, `normalizeFactorSign
q = q`, and `Hex.ZPoly.Irreducible q` — over every entry of the default-precision
exhaustive core-factors array at the same outer-bound shape used by the sibling
umbrella. The substrate derivation duplicates the umbrella's `hp_prime` / `hform`
/ `hgood` / lifted-factor `monic` / `natDegree_pos` / `inj` / `hcore_record`
chain verbatim so the two theorems consume the same evidence bundle.

## Substrate gap recorded

The expansion wrapper also requires `Hex.DensePoly.Monic q` and
`0 < q.degree?.getD 0` on every emitted factor. Neither is exposed by the current
wrapper stack: every emitted factor is structurally
`normalizeFactorSign (primitivePart (centeredLiftPoly (polyProduct selected) modulus))`
for some `selected ∈ Hex.subsetSplits d.liftedFactors.toList`, and both
preconditions then follow from `recombinationCandidate_monic` /
`natDegree_toPolynomial_recombinationCandidate_eq_sum` (already in
`HexBerlekampZassenhausMathlib/Basic.lean`) plus a "`selected = liftedSubsetSelectedList
d S` for some `LiftedFactorSubset d`" bridge through
`subsetSplits_mem_exists_mask`. That bridge has not been built; the docstring on
the new theorem describes the substrate shape and defers the two missing
conjuncts to a follow-up sub-issue, mirroring the gap-shim precedent of the
sibling umbrella (#4561 Gap 1 / Gap 2 / Gap 3).

## Current frontier

#4627 (the consumer of this packaging) now has the irreducibility / shouldRecord
/ normalizeFactorSign half of the discharger; the monicness and degree-positivity
gap remains the remaining substrate work before #4627 can land.

## Next step

A successor `feature` issue should build the bridge from
`recombinationSearchModAux` candidates to `recombinationCandidate d (S : LiftedFactorSubset d)`
and use it to lift `recombinationCandidate_monic` /
`natDegree_toPolynomial_recombinationCandidate_eq_sum` into
`recombineExhaustive_monic` / `recombineExhaustive_natDegree_pos`, then extend
the new theorem in this PR (or supersede it) with the remaining two conjuncts.

## Blockers

None for this slice. #4627 stays narrowed pending the monicness/degree-positivity
substrate.

## Verification

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` clean.
- `lake build HexBerlekampZassenhausMathlib` clean.
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` clean.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
  HexBerlekampZassenhausMathlib/Basic.lean HexBerlekampZassenhaus/Basic.lean`
  introduces no new `sorry|axiom|native_decide|TODO|FIXME`.
