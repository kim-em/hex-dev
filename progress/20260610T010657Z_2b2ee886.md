# 20260610T010657Z (#6684)

## Accomplished

Claimed #6684 and removed the arbitrary Hensel subset correspondence fallback
from `HexBerlekampZassenhausMathlib/Basic.lean`.

- Deleted `henselSubsetCorrespondence_analytic_obligation` and the two public
  fallback constructors that depended on it:
  `henselSubsetCorrespondenceHypotheses_of_choosePrimeData` and
  `henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData`.
- Narrowed the older lifted-partition and slow-path substrate wrappers so they
  consume explicit `HenselSubsetCorrespondenceHypotheses` evidence instead of
  manufacturing it from arbitrary `PrimeChoiceData`.
- Updated successful-descent wrappers and docstrings to route through the
  explicit descent/forward-transport correspondence path.
- Confirmed no remaining references to the deleted analytic helper or removed
  fallback constructor names.

Verification performed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Basic.lean`

## Current frontier

The Hensel-correspondence half no longer exposes a universal theorem over
arbitrary `PrimeChoiceData`. Remaining partition-field analytic content is
still represented by `InitialLiftedFactorSubsetPartitionEvidence`, as tracked
separately by the lifted-partition work.

## Next step

Review and merge the PR for #6684. Downstream callers should use the
successful-descent constructors or pass explicit correspondence evidence into
the narrowed substrate wrappers.

## Blockers

No technical blocker. `/reflect` is not available in this non-interactive
environment, so it was skipped per the worker-flow fallback.
