# 2026-05-05 10:05 UTC — feature session (5492221a)

## Accomplished

Claimed #2316 and deleted the obsolete final-column / off-diagonal
residual scaffolding from `HexMatrix/Determinant.lean`. The chain was
left over from the abandoned Mathlib-free reproof of the
Desnanot-Jacobi identity (recovery plan #2308); its consumer story is
now satisfied by the Mathlib bridge `bareissNoPivot_eq_det` (#2314).

326 lines removed from `HexMatrix/Determinant.lean` (3649 → 3323).

## Audit summary

Verified via repo-wide grep that none of the deleted symbols had
consumers outside the chain itself:

- `detFinalColumnOffDiagonal`, `detFinalColumnOffDiagonalSum`
- `detFinalColumnOffDiagonal_succ_split_last`,
  `detFinalColumnOffDiagonalSum_succ_flatMap`
- `det_finalColumn_diagonal_sum`, `det_finalColumn_expansion`
- `detFinalColumnOffDiagonalSum_eq_det_sub_diagonal`
- `det_borderedMinor_finalColumn_expansion`,
  `det_borderedMinor_succ_finalColumn_expansion`
- `detFinalColumnOffDiagonalSum_borderedMinor_eq`,
  `detFinalColumnOffDiagonalSum_borderedMinor_succ_eq`
- `detBorderedMinorOffDiagonalProductResidual`,
  `det_borderedMinor_desnanot_difference_eq_offDiagonalProductResidual`,
  `det_borderedMinor_desnanot_of_offDiagonalProductResidual_eq_zero`,
  `detBorderedMinorOffDiagonalProductResidual_eq_zero_of_succ_offDiagonal_identity`
- helpers `foldl_det_sum_map_start`, `foldl_det_sum_finRange_succ_last`
  (only consumed by the deleted theorems)

Kept (still live):

- `permutationVectors_duplicateRow_sum` /
  `permutationVectors_duplicateCol_sum` chain — still needed by
  `det_eq_zero_of_row_eq` / `det_eq_zero_of_col_eq` (out of scope).
- `foldl_det_sum_filter_split_start`, `foldl_det_sum_filter_split`,
  `foldl_det_sum_map` — feed the duplicate-row/col chain.

## Verification

- `lake build HexMatrix.Determinant` — clean
- `lake build HexMatrix` — clean (pre-existing sorries in `RREF.lean`
  and `Bareiss.lean` remain; unchanged)
- `lake build` (full) — clean
- `python3 scripts/check_dag.py` — exit 0
- `git diff --check` — clean
- No new `axiom`, `native_decide`, `TODO`, or `FIXME`

## Current frontier

`HexMatrix/Determinant.lean` is materially smaller. The remaining
Bareiss-correctness consumer is `bareiss_eq_det` in
`HexMatrix/Bareiss.lean` (still has its own `sorry`, discharged
separately by #2315 / `HexMatrixMathlib`).

## Next step

Continue Rabin / Berlekamp scaffolding work on the queue: #2299
(backward Rabin divisibility theorem), #2335 (quotient Frobenius
fixed-point), #2342 (GF2nPoly.X powers ↔ Rabin Frobenius remainders).

## Blockers

None.
