# Progress 2026-05-15 06:57 UTC

## Accomplished

Closed #4166 — added the singular dual of
`scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix`.

- New private state-level helper
  `scaledCoeffArrayLoop_lower_zero_of_singular_before_target` in
  `HexGramSchmidt/Int.lean`. Inducts on fuel, using
  `scaledCoeffArrayLoop_lower_singular_after_step` at the singular step
  and `scaledCoeffArrayLoop_regular_branch` plus
  `Matrix.noPivotLoop_regular_branch` to advance through non-singular
  steps. Preserves the strict `state.step < c → coeffs[r][c] = 0`
  invariant across regular branches via
  `getArrayEntry_writeScaledColumn_of_col_ne`.
- New public theorem `scaledCoeffs_eq_zero_of_singularStep_lt` packages
  the state-level result against the initial state
  `(step := 0, matrix := gramRows b, coeffs := zeroRows n, prevPivot := 1)`
  and `Matrix.noPivotInitialState (Matrix.gramMatrix b)`. Statement is in
  the public `GramSchmidt.entry (scaledCoeffs b) i j = 0` form.

No sorrys, axioms, native_decide, TODO, FIXME, or new Mathlib facts
introduced. `scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss` and
`scaledCoeffs_eq_scaledCoeffMatrix_det` were not used.

Builds verified:
- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt HexGramSchmidtMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

The parent #4163 still needs the singular branch of the Cramer/Bareiss
case-split proof for `scaledCoeffs_eq_scaledCoeffMatrix_bareiss`. The
new theorem provides the executable-side zero claim needed for that
branch.

## Next step

Compose `scaledCoeffs_eq_zero_of_singularStep_lt` with the Mathlib-side
vanishing of `Matrix.bareiss (scaledCoeffMatrix b i j hji)` in the
singular branch of the parent assembly issue (#4167).

## Blockers

None.
