# 2026-06-04 15:21Z — closing residual sorry at HexGramSchmidt/Int.lean:6463 (#6551)

## Accomplished

Closed the residual sorry in `getArrayEntry_scaledCoeffRowsSchur_eq` (the
`j ≤ i, j ≠ 0` case) by composing Sub-D
(`getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing`) and Sub-C′
(`getArrayEntry_scaledCoeffRowsSchur_eq_zero_of_singularStep_lt`) for the
in-bounds branch and routing the `n ≤ i` out-of-bounds branch through both
arrays having `size = n`.

Threaded `(hquot : StepWitness b)` through the now-moved block:
`getArrayEntry_scaledCoeffRowsSchur_eq`, `gramDetVec_eq_gramDet`,
`scaledCoeffs_diag_toNat`, `scaledCoeffs_diag_eq_zero_or_eq_noPivotData_diag`,
`scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss`,
`scaledCoeffs_diag_of_nonneg`, `scaledCoeffs_eq_zero_of_singularStep_lt`,
`scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix`. Added a small
`scaledCoeffRowsSchur_size = n` helper next to `schurRowLoop_size`.

Reordered HexGramSchmidtMathlib/Int.lean: moved the augmented-Gram
infrastructure (`augmentedGram` def, upper-block/trailing-col/trailing-row
lemmas, `noPivotLoop_augmentedGram_invariant`,
`bareissGramCanonicalCoeff_eq_borderedMinor_aug`,
`StepWitness.ofGram`, etc.) and its two prerequisites
(`matrix_diag_at_fin_eq`, `noPivotLoop_extends_singularStep`) up before the
five consumer theorems (`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`,
`scaledCoeffs_eq_scaledCoeffMatrix_bareiss`, `scaledCoeffRows_lower_eq_coeffs`,
`scaledCoeffs_eq`, `scaledCoeffs_diag`) so each consumer can discharge via
`StepWitness.ofGram b` locally instead of demanding the hypothesis from its
callers.

Migrated `LLLState.ofBasis_valid` from `HexLLL/Basic.lean` (Mathlib-free)
to `HexLLLMathlib/Basic.lean` (Mathlib side), since `gramDetVec_eq_gramDet`
now consumes a `StepWitness b` and the canonical instance lives on the
Mathlib side.

## Acceptance criteria

- `grep -c sorry HexGramSchmidt/Int.lean` = 2 (docstring + `#5655`-tracked sorry at line 6158); residual at the previous line 6463 is gone. ✓
- `grep -c sorry HexGramSchmidtMathlib/Int.lean` = 0. ✓
- `lake build` clean across the whole project (244 jobs). ✓
- No new `sorry`, `axiom`, or `native_decide`. ✓
- `getArrayEntry_scaledCoeffRowsSchur_eq` retains its `(i j : Nat)` signature,
  gains `(hquot : StepWitness b)`. ✓

## Current frontier

Sub-A and Sub-B of #6549 have landed; deliverable 1 of #6549 (reproving the
three Mathlib-side bridge theorems via the unconditional
`scaledCoeffs_lower_eq_det_scaledCoeffMatrix`) is still open and tracked as
a third sibling sub-issue per the directive.

## Next step

File the third sibling sub-issue of #6549 (style-only reroute of
`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`,
`scaledCoeffRows_lower_eq_coeffs`, `scaledCoeffs_eq` through the new
unconditional `scaledCoeffs_lower_eq_det_scaledCoeffMatrix`).

## Blockers

None.
