# 20260602T151444Z 14532541 — Directive #2567 (HO-4)

## Accomplished

- Followed directive priority: claimed standing `directive` issue #2567 (HO-4:
  formalise BHKS Theorem 5.2 / `factorFast_terminates`) on a fresh worktree
  branch.
- Added unconditional lemma `one_le_factorFastPrecisionCap` to
  `HexBerlekampZassenhausMathlib/BHKSBound.lean`, proving
  `1 ≤ Hex.factorFastPrecisionCap f` for every `f` via the existing
  `one_le_bhksBound` + `bhksBound_le_factorFastPrecisionCap` chain.
- Added two sibling capstone wrappers in
  `HexBerlekampZassenhausMathlib/Recovery.lean` that internalize the
  `hB_pos : 1 ≤ Hex.factorFastPrecisionCap f` argument:
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalIndicatorsAtPrecisionForCoeffBound_internalCapPositive`
  and
  `factorFast_ne_none_of_mignottePrecisionCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound_internalCapPositive`.
  Each delegates to its non-internalized counterpart, substituting
  `one_le_factorFastPrecisionCap f` for the dropped hypothesis.

## Current frontier

The public unconditional theorem
`factorFast_terminates : ∀ f : Hex.ZPoly, Hex.factorFast f ≠ none` remains
unproved.  The remaining semantic recovery hypotheses (cut hypotheses,
bridge data, resultant bound, non-degeneracy, candidate reconstruction,
product equality, canonical-supports/expected-true-factor package, per-class
modular product congruence) are still passed explicitly through the new
wrappers.

## Next step

Either:

- Continue narrowing the final assembly surface by internalizing further
  unconditional side conditions (`hp`, `hk`, `hf_ne_zero` once the
  corresponding nonzero-input wrapper is in place), or
- Wait for in-flight subissues #6344 (canonical true-support recovery
  inputs, currently in PR #6358) and #6345 (per-class Mignotte product
  congruences) to land, then assemble the canonical-supports finalist.

## Blockers

- None for this partial contribution.
- Full #2567 still requires unconditional production of the remaining
  semantic recovery hypotheses, which is the residual scope of the
  open subissues.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound` — green.
- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `lake build` — green (244 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` in the
  modified files.
