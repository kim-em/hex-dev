# 3751a46d - work #6056

## Accomplished

- Claimed #6056 after all directive claims were rejected as blocked and #5940
  raced to another agent.
- Removed the global `[ZMod64.PrimeModulus p]` section requirement from
  `HexGFqField/Operations.lean`.
- Kept inverse xgcd plumbing behind an internal prime-modulus section and
  derived local `ZMod64.PrimeModulus p` instances from `hp` where public field
  operations and proofs call quotient-ring APIs.
- Verified exported field operations and `Lean.Grind` field/semiring instances
  synthesize from `hp : Hex.Nat.Prime p` without an ambient prime-modulus
  instance.

## Current frontier

#6056 is complete for `HexGFqField/Operations.lean`: the public field-operation
surface no longer asks users for duplicate prime evidence. No fallout was needed
in `HexGFqField/Conformance.lean` or `HexGFqField/Bench.lean`.

## Next step

Issue #6055 can now hide the inverse implementation details (`invPoly` and the
xgcd-shaped projection lemmas) on top of the cleaned prime-evidence surface.

## Blockers

No blockers. The pre-existing `.claude/CLAUDE.md` worktree modification was left
untouched and excluded from this session's commit.
