# Issue #3757 decomposition (B7 indicator-signatures bridge)

## Accomplished

- Claimed issue #3757 (HexBZ Mathlib: prove B7 indicator signatures
  from true-factor lattice).
- Oriented on the executable BHKS pipeline (`bhksColumnSignature`,
  `bhksInsertSignatureClass`, `bhksClassIndicator`,
  `bhksEquivalenceClassIndicators`) and the Mathlib-side surface
  (`projectedRowSpanInt`, `projectedRowSpaceRat`,
  `trueFactorIndicatorLattice`, `indicatorVector`, and the
  `EquivalenceClassRecoveryHypotheses` structure).
- Reviewed #3756's merged row-span bridge
  (`HexMatrixMathlib.spanContains_iff_mem_span`) and confirmed it is
  the right tool for the rref/row-span side of B7.
- Concluded the issue is too large for one session: three independent
  proof obligations (integer→rational scalar extension, RREF
  column-signature ↔ row-span agreement, partition-atom assembly +
  derived constructor), each multi-hundred lines.
- Created three follow-on sub-issues with crisp scopes:
  - #3789: rational row-span transport from integer `L' = W`.
  - #3791: RREF column-signature ↔ row-span agreement bridge.
  - #3792: final assembly — `expectedIndicatorArrayOfSupports`,
    BZ-specific `bhksEquivalenceClassIndicators` bridge, and
    `EquivalenceClassRecoveryHypotheses.ofIndicatorLattice`
    constructor.
    Marked `depends-on: #3789` and `depends-on: #3791` so the planner
    can sequence them.
- Left a `Decomposed into #3789, #3791, #3792` breadcrumb on #3757 and
  skipped it (`replan` label) for planner triage.

## Current frontier

The B7 decomposition is on the queue. #3789 and #3791 are independent
and can be worked on immediately; #3792 is blocked until both land.

## Next step

A planner cycle should triage #3757's `Decomposed into` breadcrumb
(either close it as fully covered by the sub-issues, or narrow it to
any residual scope). Then workers can pick up #3789 and #3791 in
parallel.

## Blockers

- None for this session. #3792 is blocked-on the other two sub-issues
  by design.
