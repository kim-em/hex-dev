# Progress — 2026-06-18 (session baeeb517, /work → /feature #8022)

## Accomplished

Completed #8022 (HexArith UInt64 Wide Phase 6 grind coverage). Promoted the
four `mulFull` projection/view lemmas in `HexArith/UInt64/Wide.lean` from
`@[simp]` to `@[simp, grind =]`:

- `mulFull_fst_eq_mulHi` — `(mulFull a b).1 = mulHi a b`
- `mulFull_snd_eq_mul` — `(mulFull a b).2 = a * b`
- `toNat_mulFull_fst` — `(mulFull a b).1.toNat = a.toNat * b.toNat / word`
- `toNat_mulFull_snd` — `(mulFull a b).2.toNat = a.toNat * b.toNat % word`

All four are literal `lhs = rhs` equations characterizing the executable
wide-multiply API. The orientation is projection/Nat-view → simpler executable
or arithmetic term, so no rewrite loop. All four were accepted by `grind =`
(no rejections, no loop warnings); none had to be left `@[simp]`-only.

## Verification

- `lake build HexArith` → 61 jobs, `Build completed successfully`, no
  `error`/`warning`/loop/rejection in the log.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `python3 scripts/check_dag.py` → exit 0.
- Scope confined to `HexArith/UInt64/Wide.lean`; C extern, lakefile, and
  bench/conformance files untouched.

## Current frontier

`HexArith/UInt64/Wide.lean` Phase 6 grind coverage complete.

## Next step

Sibling Phase 6 slices remain unclaimed: #8023 (HexMatrix RREF), #8024
(HexHensel QuadraticMultifactor).

## Blockers

None.

Note: worktree carries a pre-existing unrelated `.claude/skills/...`
modification from before this session; not touched or staged.
