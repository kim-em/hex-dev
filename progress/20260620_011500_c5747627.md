# Phase 6 grind coverage for HexHensel/Basic.lean modP_reduceModPow (#8139)

## Accomplished

- Promoted `modP_reduceModPow` (`HexHensel/Basic.lean:232`) from `@[simp]`
  to `@[simp, grind =]`. Confirmed literal `lhs = rhs`
  (`modP p (reduceModPow f p (k + 1)) = modP p f`); its sibling
  `modP_reduceModPow_of_pos` already carries the same tag, so this
  reconciles the pair.
- Left `linearHenselStep_g` (`HexHensel/Linear.lean:131`) and
  `linearHenselStep_h` (`:143`) `@[simp]`-only — both RHSs are
  `let`-wrapped, which `grind =` rejects. Added a one-line comment at
  each recording the deliberate exclusion so the next sweeper does not
  re-flag them.

## Current frontier

- `lake build HexHensel` green (112 jobs).
- `lake build HexHenselMathlib` (CI-gated bridge) green (1538 jobs). The
  three `unused variable` warnings in `HexHenselMathlib/Correctness.lean`
  are pre-existing and unrelated (that file was not touched).
- `python3 scripts/check_dag.py` exit 0.
- Annotation/comment-only: no proof bodies, signatures, imports, or
  `*Mathlib` files touched. No `axiom`/`sorry`/`native_decide` on added
  lines.

## Next step

None for this issue. Other HexHensel files are out of scope for this
sweep entry.

## Blockers

None.
