# Session 6f6c3f09 — HexMatrix Phase 6 colAdd source-column readback wrappers

Claimed and completed issue #5478.

## Accomplished

- Added two wrappers to `HexMatrix/RowEchelon.lean` mirroring the
  existing `rowAdd_getElem_src_of_ne` / `row_rowAdd_src_of_ne`
  pair:
  - `colAdd_getElem_src_of_ne`: entry-level readback, source-column
    entries are unchanged when `src ≠ dst`.
  - `col_colAdd_src_of_ne` (`@[simp]`): column-level corollary,
    discharged by `col_colAdd_of_ne M src c hsrcdst`.
- Note: the issue body suggested `col_colAdd_of_ne M dst c hsrcdst`;
  the correct call mirroring the row analogue is `M src c hsrcdst`
  (matches `row_rowAdd_src_of_ne` at line 207).
- Verified `lake build HexMatrix.RowEchelon`, `lake build HexMatrix`,
  `lake build`, `scripts/check_dag.py`, and `git diff --check`.
  No `sorry`/`axiom`/`native_decide` added.

## Current frontier

Issue #5478 ready to publish.

## Next step

Push branch and open PR closing #5478.

## Blockers

None.
