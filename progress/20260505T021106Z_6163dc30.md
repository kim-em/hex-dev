## Accomplished

- Claimed #2193.
- Added fixed Tier 1 Conway irreducibility benchmark registrations for
  `C(2, 1)` and `C(13, 6)` in `HexConway/Bench.lean`.
- Updated the `HexConway.Bench` module docstring and adjacent benchmark
  rationale to keep lookup, Tier 1 irreducibility verification, Tier 2
  Conway compatibility, and Tier 3 search separate.
- Added source-table guards tying the fixed irreducibility benchmark inputs
  back to `Conway.luebeckConwayPolynomial?`.
- Verified:
  - `lake build HexConway.Bench`
  - `lake exe hexconway_bench list`
  - `lake exe hexconway_bench verify`
  - `lake exe hexconway_bench run Hex.ConwayBench.runTier1Irreducibility_2_1Checksum`
  - `lake exe hexconway_bench run Hex.ConwayBench.runTier1Irreducibility_13_6Checksum`
  - `python3 scripts/check_phase4.py`
  - `python3 scripts/status.py HexConway`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexConway/Bench.lean`

## Current frontier

#2193 is implemented. `HexConway.Bench` now lists four registrations: the
existing Tier 1 lookup benchmark, the existing `SupportedEntry` fixed benchmark,
and the two new Tier 1 irreducibility fixed benchmarks.

## Next step

Continue later HexConway Phase 4 slices, especially broader Tier 1
irreducibility coverage or separate Tier 2 Conway-compatibility benchmarks when
that phase slice is planned.

## Blockers

- No blocker for #2193.
- `python3 scripts/check_phase4.py --all` still reports pre-existing
  cost-model comment gaps in other benchmark files outside this issue's scope.
