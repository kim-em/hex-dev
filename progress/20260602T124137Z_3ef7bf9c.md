# 20260602T124137Z 3ef7bf9c — Issue #6337

## Accomplished

- Claimed directive issue #6337.
- Reworked `HexGramSchmidt/Int.lean` so `stepScaledRows` updates each
  trailing row through `Array.modify`, extracting the row from the current
  outer array before clearing the pivot column.
- Cached the pivot row once and changed the inner update to read trailing
  entries from the row being mutated, so the generated C no longer increments
  the extracted source row before the first row write.
- Added local `foldl` bookkeeping lemmas for `Array.modify` and for dependent
  same-slot row writes, preserving the existing `stepScaledRows` correctness
  API.
- Added two-rung benchmark evidence in
  `reports/bench-results/hex-lll-3ef7bf9c-step-scaled-rows-two-rung.json`.
- Regenerated both HexLLL comparator SVGs; the regenerated files were
  byte-identical to the committed versions.

## Current frontier

`stepScaledRows` now has the intended generated-C shape:
`lean_array_fget` of the row, placeholder outer `lean_array_fset`, direct
`lean_array_set` on the extracted row, then final outer `lean_array_fset`.
There is no `lean_inc` of the extracted row before the pivot-column clear.

Benchmark medians on `carica` from the two-rung run:

- `Hex.LLLBench.runFirstShortVectorHarshCubicNormSq55`: `663.871 ms`.
- `Hex.LLLBench.runFirstShortVectorRandomBoundedNormSq120`: `1.751 s`.

## Next step

Open the PR for #6337 with the IR finding, benchmark medians, and verification
commands. If broader performance evidence is desired, run a full consolidated
family sweep on an otherwise idle benchmarking host and refresh the comparator
plot input exports.

## Blockers

- None for the code change.
- The local two-rung benchmark ran while another worktree had a concurrent
  `hexlll_bench` process on the same host, so the medians should be treated as
  PR evidence rather than final report-quality comparator data.

## Verification

- `lake build HexGramSchmidt.Int` — passed.
- `lake build` — passed.
- `lake exe hexgramschmidt_bench verify` — all 10 benchmarks passed.
- `lake exe hexlll_bench verify` — all 73 benchmarks passed.
- HexGramSchmidt fresh fixture diff plus `python3 scripts/oracle/gs_flint.py`
  — checked 14 cases, 0 failures.
- HexLLL fresh fixture diff plus `python3 scripts/oracle/lll_fpylll.py`
  — checked 9 cases, 0 failures.
- IR check in `.lake/build/ir/HexGramSchmidt/Int.c` confirmed the source-row
  increment before the inner row write is gone.
