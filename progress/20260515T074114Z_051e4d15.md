# 2026-05-15 07:41 UTC — work session, issues #3945 and #4064

## Accomplished

- Claimed #3945 first because it was the highest-priority visible directive.
  Verified the rollback had already landed (`HexGfqRing.done_through = 3`)
  and that the remaining Phase 4 re-validation is currently blocked by
  `scripts/status.py HexGfqRing` reporting `HexPolyFp.done_through >= 4`.
  Marked #3945 `replan` with that stale/blocker explanation.
- Claimed #4064 and added
  `Hex.Berlekamp.exists_kernelWitnessSplit?_some_of_witnessProduct_dvd_of_pos_degree`
  in `HexBerlekamp/RabinSoundness.lean`.
- The new theorem composes the existing square-free distribution result
  `exists_nontrivial_gcd_of_witnessProduct_dvd_of_pos_degree` with the
  executable search reflection
  `kernelWitnessSplit?_some_of_nontrivial_splitFactorAt`, keeping the
  witness-product divisibility hypothesis explicit.
- Commit: `d5f84fe feat: compose Berlekamp witness split proof`.

## Current frontier

- #4064 now has the requested executable composition lemma from
  witness-product divisibility plus non-constancy to concrete
  `kernelWitnessSplit?` success.
- The upstream derivation of the witness-product divisibility hypothesis from
  fixed-space/kernel hypotheses remains out of scope for #4064.

## Next step

- Open the PR for #4064.
- A later Berlekamp worker can use the new theorem after #4160 supplies the
  prime-field product substitution needed to derive the `hdvd` hypothesis.

## Blockers

- #3945 cannot complete its remaining re-validation/re-bump until
  `HexPolyFp.done_through >= 4`; current status reports `HexPolyFp` at
  `done_through: 3`.
- No blockers for #4064.
