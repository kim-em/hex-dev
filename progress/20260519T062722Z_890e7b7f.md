# Issue #5288 GFq zpow wrappers

## Accomplished

Added `GFq.repr_zpow_ofNat` and `GFq.repr_zpow_negSucc` in
`HexGfq/Basic.lean`, next to the existing `GFq.repr_pow` wrapper. Both wrappers
specialize the existing generic `GFqField` integer-power representative lemmas
for Conway-backed `GFq`, with `rfl` proofs and `@[simp]` annotations.

Verified:

- `lake build HexGfq.Basic`
- `lake build HexGfq`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- wrapper-name `rg` check found both declarations
- scoped forbidden-token check found no added `sorry`, `axiom`, `native_decide`,
  `TODO`, or `FIXME` in `HexGfq/Basic.lean`

## Current frontier

Issue #5288 is complete and ready for PR publication. The worktree still has a
pre-existing unrelated modification in `.claude/CLAUDE.md`; it was left
untouched and is not part of this change.

## Next step

Create the PR for #5288 from `agent/890e7b7f`.

## Blockers

None.
