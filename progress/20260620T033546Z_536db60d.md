# 2026-06-20T03:35Z — review (HexLLL Phase 6 exit-criteria audit, #8166)

## Accomplished

Audited `HexLLL` against the four Phase 6 exit criteria gating the
`done_through 5 → 6` bump. All four pass; bumped `libraries.yml`
`HexLLL.done_through` to 6. One low-risk inline docstring added.

**Audit findings (evidence-backed):**

1. **Mathlib linter clean — PASS.** `lake build HexLLL` green, 399 jobs,
   `Build completed successfully`, zero `error`/`warning`/`sorry` lines
   (`/tmp/hexlll-build2.log`). `HexLLL` is `mathlib: false`, so Mathlib's
   env-linters do not apply; the core Lean frontend linters run during
   build and surface nothing. No `set_option linter … false` / `nolint`
   suppressions exist, so the clean build is genuine, not masked.

2. **Docstring coverage — PASS.** The importable surface
   (`Basic.lean` + `Conformance.lean`) has **zero** public
   `def`/`structure`/`class`/`inductive`/`instance` missing a `/-- -/`
   docstring. The only public decls without docstrings are
   convention-exempt: the `main` CLI entry points in `EmitFixtures.lean`
   (`:172`) and `ProviderProbe.lean` (`:6`, `:30`), and the bench-harness
   plumbing in `Bench.lean` (121 `runXxx : Unit → IO Int` thunks,
   `Hashable` instances, hash helpers) — none of which is importable API.
   Six private helpers in `Basic.lean` lack docstrings; five are routine
   plumbing (tally bumps `bumpChecker`/`bumpSteered`/`bump`, `intNat?`,
   `slice`) and are rule-exempt. The sixth, the non-obvious invariant
   lemma `sizeReduce_foldl_memLattice_iff` (`:1920`), got an inline
   docstring in this PR.

3. **No dead code — PASS.** Every public declaration is referenced.
   `Ival.ofInt`/`ofRat`/`divPos` resolve to `Hex.Ival.*` and are consumed
   in `HexLLLMathlib/Interval.lean` (via `open Hex`); `IntervalGS.sizeOK`/
   `lovaszOK`, `LLLCore.teleBound`/`basisNormSq_zero`, `swapStep_d_eq`,
   `potential`, and `sameLatticeCert_sound` are all consumed in-file,
   in `Conformance.lean`, or downstream in the `HexLLLMathlib`
   verification layer. Two public theorems are currently unreferenced —
   `swapStep_ν_eq` (`:1504`) and `potential_eq_gramDetProduct` (`:1539`).
   Both are documented forward-looking characterization theorems (the
   ν-field sibling of the consumed `swapStep_d_eq`, and the `potential`
   correctness spec named in its own docstring as feeding later
   termination proofs). They are intentional verified-spec API, not orphan
   dead code, and deliverable #2 only authorizes removing dead *private*
   helpers, so they are left in place. No dead private helpers exist.

4. **Native path + Lean-checked evidence — PASS.** No `native_decide`,
   no runtime oracle, zero `sorry`/`axiom`/`admit`. The three
   `unsafe def …Impl` declarations (`withRecordCheckerOutcomeImpl`,
   `withRecordSteeredOutcomeImpl`, `withRecordOutcomeImpl`) are
   `@[implemented_by]` telemetry whose verified semantics is the identity
   continuation `k`; the compiled side-effect records outcome
   classifications and does not change the pure path. `HexLLL` exposes no
   irreducibility or field-construction claim, so criterion 6 is N/A.

The grind-automation slice was already complete before this audit (the
issue notes zero un-promoted `@[simp]` equation-lemma candidates).

## Verification

- `lake build HexLLL` → green (399 jobs), clean log after the docstring
  edit.
- `python3 scripts/check_dag.py` → exit 0.
- `python3 scripts/status.py HexLLL` → `Phase 7`, `done_through: 6`
  (off Phase 6 ready).

## Current frontier

`HexLLL` is now Phase 6 complete (`done_through: 6`). The only source
change is one private-lemma docstring; the gating change is the
`libraries.yml` bump.

## Next step

None for HexLLL. Sibling Phase 6 exit-criteria audits remain in the queue
(`HexGramSchmidt` #8173, `HexPolyZ` #8174). A follow-up could optionally
wire `swapStep_ν_eq` / `potential_eq_gramDetProduct` into the
`HexLLLMathlib` verification layer if those characterizations become
needed there.

## Blockers

None.

Note: the worktree carries a pre-existing unrelated modification to
`.claude/skills/agent-worker-flow/SKILL.md` from before this session; not
touched or staged.
