# 2026-05-20T11:39Z — work session b73dcee0

## Accomplished

Claimed #5668 first, found it stale, and released it for replan because its
prerequisite #5667 was closed by partial-triage and the actual missing
Gram column-zero/no-replacement invariant is tracked under #5677/#5683.

Claimed #5683 and landed a compiling first slice in `HexGramSchmidt/Int.lean`:

- Added the private data-bearing `BareissGramRowInvariant` structure for the
  no-pivot Bareiss Gram row-vector representation.
- Proved `bareissGramRowInvariant_initial` for
  `Matrix.noPivotInitialState (Matrix.gramMatrix b)`, using the input rows
  themselves as witnesses and packaging each row as a one-hot integer row
  combination.
- Created follow-up #5686 for the remaining no-pivot-loop preservation proof.

## Current frontier

#5683 is only partially complete.  The invariant substrate and initial state
compile, but the regular-branch row-vector update and no-pivot-loop
preservation theorem remain open.

## Next step

Pick up #5686: prove the one-step preservation lemma for
`BareissGramRowInvariant`, then package it over `Matrix.noPivotLoop` so #5677
can derive the Gram singular pivot column-zero suffix.

## Blockers

No technical blocker for the partial slice.  `lake build HexGramSchmidt.Int`
passes with the two pre-existing `sorry` warnings and existing linter notes.
