# Session 34f233c1 — feature queue survey

## Accomplished

- Surveyed the entire unclaimed queue (`list-unclaimed`: #2637, #4334,
  #4648, #4819, #4821, #4822).
- Posted a fresh blocker confirmation on **#2637** and skipped (directive
  worker-flow exit (c)). The two remaining sorrys it owns —
  `Hex.ZPoly.isIrreducible_iff` (`HexBerlekampZassenhaus/Basic.lean:6377`)
  and `factor_irreducible_of_nonUnit`
  (`HexBerlekampZassenhausMathlib/Basic.lean:240-242`) — are tracked by
  #4170 (open, blocked on #2567) and #4172 (open, blocked on #4170 +
  #4173/#4192/#4193/#4194).
- Recorded `depends-on: #4816` on **#4648** and skipped. #4648's wrapper
  refactor needs the primitive + positive-leading recursive coverage
  theorem that #4816 owns; #4647 closed only after landing the
  target-state helpers (PR #4813), not the recursive assembly.

## Current frontier

Every other open unclaimed feature item is either substrate-blocked or
environmentally blocked:

- **#4334** HexLLL inconclusive verdicts — host load 9.7–13.2 during
  this session, well above the 3.7+ documented unworkable band. 22+
  prior skip cycles for the same reason.
- **#4819** `factor_irreducible_of_nonUnit` — was claimed by another
  session shortly after I read it.
- **#4821** `henselSubsetCorrespondence_analytic_obligation` — the
  doc-comment at `HexBerlekampZassenhausMathlib/Basic.lean:12895-12906`
  states the obligation requires either BHKS Theorem 5.2 (#2567) or a
  Mathlib-side square-free Hensel port that does not yet exist.
- **#4822** `liftedFactorSubsetPartition_analytic_obligation` — same
  substrate situation, doc-comment at `Basic.lean:12985-12994`. The
  obligation packages cover, pairwise-disjoint, associated-unique, and
  the two support-containment fields as a single bundled `sorry`.

## Next step

For the queue to move, one of:

1. #2567 (BHKS Theorem 5.2, directive) makes substrate progress so
   #4170 / #4821 / #4822 become tractable.
2. #4816 lands the scaled primitive recursive coverage theorem so
   #4648's wrapper refactor can call it.
3. The pod host quiets (load < 3.7 stable) or scientific HexLLL bench
   runs are dispatched to the release-candidate workflow on dedicated
   hardware so #4334 can produce stable verdicts.

## Blockers

- Substrate: #2567 (HO-4) and #4816 (HO-1 substrate) are the two pinch
  points for the four substrate-blocked feature issues.
- Environmental: shared pod host `carica` load remains in the 8–20
  band during this session, same as the prior 22 skip cycles on #4334.
