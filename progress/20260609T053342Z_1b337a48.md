# 20260609T053342Z (#6616)

## Accomplished

- Claimed #6616 (D5c: re-prove `lll` headline theorems for the
  dispatched `lll` via `certCheck_sound`).
- Discovered all three deliverables are already in place, shipped by
  D5b (#6633, merged commit `4ee60490`):
  - `lll_isLLLReduced` (`HexLLLMathlib/Independent.lean:2407-2418`)
    proves `isLLLReduced (lll b δ ...) δ (11/20)` by case split on
    `LLLProvider.dispatch`. Native branch routes through
    `lllNative_isLLLReduced` + `isLLLReduced.mono_η`; certified branch
    routes through `dispatch_some_property`.
  - `lll_memLattice_iff` (`HexLLLMathlib/Independent.lean:2421-2431`)
    proves the same-lattice property by the same case split.
  - `lll_short_vector` (`HexLLLMathlib/Independent.lean:2450-2470`)
    consumes the two above plus `lll_independent` and
    `short_vector_bound_of_size_bound`, no dispatch awareness needed.
  - `dispatch_some_property`
    (`HexLLLMathlib/Independent.lean:2396-2401`) is the single chokepoint
    for the certified branch: composes `dispatch_some_certCheck`
    (Mathlib-free Bool extraction) with `HexLLLMathlib.certCheck_sound`
    (`HexLLLMathlib/Basic.lean:777-788`). Checker internals are not
    referenced anywhere in the three headline theorems above.
- PR #6633's description explicitly notes that `lll_short_vector` "and
  `lll_first_row_norm_sq_le_unconditional` consume the headline theorems
  abstractly and need no edits" — D5b's scope swallowed D5c's
  deliverables.
- Verified via `lake build HexLLLMathlib` (green at `4ee60490`) and
  `grep -nE '\bsorry\b|\baxiom\b'` over the three relevant files (no
  matches).
- Posted a comment on #6616 with the exact code listings for the three
  theorems and the soundness chain, then closed the issue as
  completed.

## Current frontier

D5c is resolved-as-already-done. No new code landed; no PR opened.
The dispatched `lll`'s public headline theorems are unconditional and
their certified-branch correctness composes through `certCheck_sound`
alone, as the SPEC requires.

## Next step

Nothing in the D-series of `hex-lll` follows on directly from this
issue. The next slice for HexLLL is D6 (Mathlib Euclidean restatement),
D7 (conformance), or D8 (bench) per the directive's own "Out of scope"
list.

## Blockers

None.
