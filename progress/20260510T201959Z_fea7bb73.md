# 20260510T201959Z — feature session fea7bb73

## Accomplished

Claimed issue #3123 and completed the Mathlib-side row-pivoted Bareiss
determinant bridge.

- Split the generic determinant bridge into
  `HexMatrixMathlib/Determinant/Core.lean`.
- Made `HexMatrixMathlib/Determinant.lean` a small re-export/aggregation
  module that exposes `bareissDet_eq_det` through the Mathlib-side Bareiss
  bridge instead of `Hex.Matrix.bareiss_eq_det`.
- Added row-pivoted final-step packaging lemmas in
  `HexMatrixMathlib/Determinant/Bareiss.lean`.
- Proved `bareissData_eq_mathlib_det` and `bareiss_eq_mathlib_det` from the
  existing pivot invariant, singular-branch determinant-zero theorem, and
  no-pivot final-corner determinant bridge.

Verification passed:

- `lake build HexMatrixMathlib.Determinant.Bareiss`
- `lake build HexMatrixMathlib.Determinant`
- `lake build HexMatrixMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrixMathlib/Determinant/Bareiss.lean HexMatrixMathlib/Determinant.lean HexMatrixMathlib/Determinant/Core.lean`

## Current frontier

`HexMatrix/Bareiss.lean` still contains its out-of-scope Mathlib-free
`bareiss_eq_det` proof hole. The Mathlib bridge no longer depends on that
hole.

## Next step

Let CI validate the module split and determinant bridge PR, then continue with
remaining downstream Bareiss or determinant consumers.

## Blockers

None.
