# 2026-05-06 01:11 UTC — feature session (a29fa66a)

## Accomplished

Claimed and implemented #2324 in
`HexMatrixMathlib/Determinant/Bareiss.lean`, with two supporting helpers
in `HexMatrix/Bareiss.lean`.

New theorems in `HexMatrixMathlib/Determinant/Bareiss.lean`:

- `bareissNoPivot_eq_det` (capstone): under `NonzeroBareissPivots M`,
  `Hex.Matrix.bareissNoPivot M = Matrix.det (matrixEquiv M)`.
- Private helpers used by the capstone:
  - `noPivotLoop_step_succ_le`: the no-pivot loop preserves
    `state.step + 1 ≤ n`.
  - `noPivotLoop_step_succ_ge`: under `NonzeroBareissPivots`, with enough
    fuel, the loop reaches `state.step + 1 ≥ n`.
  - `leadingPrefix_self`: `leadingPrefix M n h = M` (any `h : n ≤ n`).
  - `trailing_corner_entry_eq_det`: when the recurrence step equals `k`,
    the `(k, k)` entry of the working matrix equals `Hex.Matrix.det M`.
    Stated with `state` as an explicit free variable to enable `subst`
    over the structure projection.

New helpers in `HexMatrix/Bareiss.lean`:

- `BareissData.det_succ_eq`: for non-singular Bareiss elimination of a
  positive-size matrix, `data.det = data.sign * (last diagonal entry)`.
- `BareissData.det_zero_eq`: for non-singular Bareiss elimination of an
  empty matrix, `data.det = data.sign`.

These were necessary because `BareissData.lastDiag?` is a `private def`
in `HexMatrix/Bareiss.lean`, so external code cannot unfold it directly.

The capstone splits on `n` into the empty case (both sides reduce to
`1` via `BareissData.det_zero_eq` + `Matrix.det_isEmpty`) and the
non-empty `n = k + 1` case. The `n = k + 1` case uses
`bareissNoPivotInvariant_holds` from #2387 to obtain the bordered-minor
invariant on the final state, combines `noPivotLoop_step_succ_le` and
`noPivotLoop_step_succ_ge` (with `omega`) to derive
`final.step = k`, applies `trailing_corner_entry_eq_det` to get the
`(k, k)` entry, then bridges via `BareissData.det_succ_eq`,
`BareissData.sign = 1` (since `noPivotLoop` never swaps rows, by
`noPivotLoop_rowSwaps`), and `det_eq` from #2321.

Verified: `lake build HexMatrixMathlib.Determinant.Bareiss`,
`lake build` (full), `python3 scripts/check_dag.py`, `git diff --check`.
No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
`sorry`.

## Current frontier

The Bareiss capstone for the no-pivot recurrence is now in place:
`HexMatrixMathlib.bareissNoPivot_eq_det`. The remaining work in the
Bareiss chain is:

- The public row-pivoting wrapper `bareissDet_eq_det` (#2315), which
  needs a singular-case companion `bareissNoPivot_eq_zero_of_...` (left
  as out-of-scope here per the issue body).
- The existing `sorry` in `HexMatrix/Bareiss.lean:507` for
  `bareiss_eq_det`, which depends on the row-pivoting wrapper.

## Next step

#2315 (public row-pivoting wrapper). Needs the singular-case companion
plus row-swap bookkeeping, so likely larger than a single session.

## Blockers

None for the work landed.
