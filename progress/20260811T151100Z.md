# Accomplished

- Reconciled the proof-schema table slice with the current interval frontend
  on `main`.
- Preserved the current live quotation gate and candidate-search state
  isolation while retaining schema lookup by exact replay address.
- Preserved the SPEC's chronology warning: a final-history lookup is quotation
  data, not proof that an input was available at the required earlier event.

# Current frontier

The reconciled slice adds package-supplied proof-schema handles and rejects
missing, duplicate, or wrongly selected replay addresses. It remains a
selection layer; the later generic frontend owns proof-term construction.

# Next step

Run the focused reconciled build and repository checks, push the exact head,
and obtain a fresh review while the branch-execution stack continues.

# Blockers

None.
