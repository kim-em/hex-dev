# 2026-05-18 07:15 UTC — #4935 scaledRecombinationSearchModAux abstract-bound sibling

## Accomplished

- Claimed and executed #4935: added the abstract-bound sibling
  `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean`, parameterised by
  `B' : Nat`, `hcore_lc_le`, the universal divisor coefficient bound
  `hvalid`, and `hprecision : 2 * B' < d.p ^ d.k` (Option A).
- Threaded the abstract bound through all five `_of_bound` substrate
  call sites in the fuel induction:
  - `not_represents_empty_..._of_primitive_pos_lc_core_of_bound`
    (empty-`J` step, per-factor `hvalid g hg_dvd_core`),
  - `representsIntegerFactorAtLift_primitive_of_bound`
    (per-factor `hvalid f_cov hf_cov_dvd_core`, plus `hcore_lc_le`),
  - `scaledRecombinationCandidate_eq_factor_of_recovery_of_bound`
    (per-factor `hvalid f_cov hf_cov_dvd_core`),
  - `natDegree_toPolynomial_eq_sum_of_represents_..._of_bound`
    (per-factor `hvalid f_cov hf_cov_dvd_core`, plus `hcore_lc_le`),
  - `liftedFactorSubsetPartition_prefix_none_..._scaled_of_bound`
    (universal `hvalid` + `hcore_lc_le` + `B'` + `hprecision` unchanged).
- Rewired the original `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  at line 14305 (post-insertion) as a thin wrapper that instantiates
  `B' := defaultFactorCoeffBound core`, discharges `hcore_lc_le` via
  `defaultFactorCoeffBound_valid core hcore_ne core (dvd_refl core)`
  paired with `leadingCoeff_eq_coeff_last`, and discharges the universal
  `hvalid` via `defaultFactorCoeffBound_valid core hcore_ne`. The
  wrapper introduces the implicit-universal `target`/`J`/`localFactors`/
  `fuel` block before passing them through (needed because `exact` on
  a `∀`-suffixed term otherwise eagerly instantiates the universal
  binders).
- Downstream public wrapper
  `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core`
  (currently at ~line 14478) still type-checks unchanged through the
  rewired thin original.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` shows the **identical**
  set of 18 pre-existing errors as `origin/main` (whnf heartbeat timeouts
  at 4848 / 5129 / 5455 / 5542 / 5589 / 5973 / 6121 / 6293 / 6274 /
  15342 / 15453, `cases`-nested failure at 5851, kernel-unknown errors
  at 6608 / 6672 / 15660 / 15706, and the existing sorries at
  15132 / 15227). The post-edit lines for the four post-change errors
  shifted by exactly +100, matching the size of the inserted theorem.
- No new errors anywhere in lines 14000–14999.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` exits 0.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean |
  grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` returns no
  matches.

## Current frontier

The `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
abstract-bound sibling is now landed. The natural next targets are:

- The monic recursive-coverage analogue
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  at line 13734 — separate issue, blocked on the monic prefix-none
  `_of_bound` (#4931 territory).
- Once the monic companion lands, the public `_factor_associated_*`
  wrappers at lines 14478 (`_of_primitive_pos_lc_core`) and the monic
  variant can themselves be lifted, then the propagation continues up
  into the exhaustive-coverage layer in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

## Next step

Branch ready to push and PR-create via `coordination create-pr 4935`.

## Blockers

None.
