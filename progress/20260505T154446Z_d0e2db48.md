# 2026-05-05 15:44 UTC — work session (d0e2db48)

## Accomplished

Claimed #2360 and discharged the `gf2nPolyIrreducible` proof in
`HexGF2/Bench.lean` by adding a reusable
`GF2Poly.gf2nPoly_modulus_irreducible` theorem in
`HexGF2/CommonIrreducibility.lean`.

Added public quotient-witness remainder lemmas in `HexGF2/Euclid.lean`:
`mod_eq_self_of_reduced` and `mod_eq_of_eq_add_mul_right`. The GHASH
degree-128 Rabin certificate uses precomputed pow-chain entries plus
quotient witnesses for each squaring step, so checking avoids the
intractable 128-step long-division reduction.

Verification run:

* `lake build HexGF2.CommonIrreducibility`
* `lake build HexGF2.Bench`
* `lake exe hexgf2_bench list`
* `lake exe hexgf2_bench verify`
* `python3 scripts/status.py HexGF2`
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg "sorry" HexGF2/Bench.lean` returns no hits.
* `rg -n "axiom|native_decide|TODO|FIXME" HexGF2/CommonIrreducibility.lean HexGF2/Bench.lean HexGF2/Euclid.lean` returns no hits.

## Current frontier

The GHASH benchmark fixture now has a project-side irreducibility witness.
`HexGF2/RabinSoundness.lean` still has its pre-existing Rabin-theory
`sorry` leaves, and unrelated `HexPolyFp` files still report existing
`sorry` warnings during the bench executable builds.

## Next step

Continue with the remaining `HexGF2` Rabin soundness leaves or the
follow-up quotient/Frobenius bridge issues, depending on queue priority.

## Blockers

None for this session.
