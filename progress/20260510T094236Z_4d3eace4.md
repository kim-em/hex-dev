## Accomplished

Reviewed the HexPolyFp square-free quotient proof path (issue #2971). Confirmed
the previously-requested `d * g ∣ derivative (c * g)` route is unsalvageable:
the `F_5` counterexample with `d = X`, `c = X^2`, `g = X * (X + 1)` extends to
the full derivative-product setting, so no narrow extra hypothesis rescues
that statement.

Identified a corrected proof strategy for `derivativeSplit_quotient_common_dvd_derivative_one`:
induction on a polynomial-power chain. Given `d ∣ c` and `d ∣ derivative c`, the
existing `derivativeSplit_common_dvd_quotient_derivative_dvd_gcd` already gives
`d ∣ g`. Iterate this to `pow d n ∣ g` for every `n`, splitting `derivative f =
derivative c * g + c * derivative g` and case-splitting on `n mod p` for the
`(n : ZMod64 p) * c * pow d (n-1) * derivative d * h_n` term (the `n ≡ 0 (mod p)`
case vanishes; the `n ≢ 0` case reduces via `d ∣ derivative d * q` from
expanding `derivative c = derivative d * q + d * derivative q`). The arbitrary
`n` then forces `d.size ≤ 1` by `degree?_mul_eq_add_degree?` plus the
`gcd`-nonzero helper from #2962, and the size-one finish reuses the
`scale_one_poly` + `dvd_scale_self_of_ne_zero` pattern from
`dvd_one_of_mul_right_dvd_right`.

Posted the strategy as a comment on #2964 and replaced the stale body of #2964
with a self-contained brief that names the four narrow helper lemmas the worker
will need (`pow_succ`, `derivative_pow`, the inductive `pow d n ∣ g` step, and
the final assembly). Did not file separate sub-issues; the helpers are tightly
coupled and best landed together with the final theorem in one PR. The
"decompose at execution time" path (Step 4b of `agent-worker-flow`) remains
available if the worker finds the assembly larger than a single session.

Comment: https://github.com/kim-em/hex/issues/2964#issuecomment-4414974439

## Current frontier

#2964 is now ready for a worker as soon as #2971 closes (it carries
`depends-on: #2971`, which the next `coordination check-blocked` cycle will
clear). The downstream consumer #2950 has not been touched in this session;
its body still correctly cites #2964 and continues to wait.

## Next step

A planner / worker can pick up #2964 directly. No fresh planning is needed for
this branch of the Yun proof chain.

## Blockers

None for this review issue. The worker who claims #2964 will need to land all
four helper lemmas plus the main theorem in one PR.
