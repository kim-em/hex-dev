# PNT+ nested-logarithm final-base acceptance

## Accomplished

- Replayed the nested-log fixture onto exact `main` commit
  `22d293dd6f2e6a29ba49961ea40496cd7194fce5`, preserving the subsequently
  merged sine and isolated branch-tree experiment/conformance registrations.
- Added the exact proof-only benchmark-freshness exemption for the final
  `lakefile.lean` blob and documented why the zero-touching planner mutation
  removes the original point fact.
- Added a symmetric inner-schema negative which rejects replay when the exact
  `6.58` source fact is missing.
- Rebuilt the focused runtime, Mathlib semantics, and conformance modules plus
  the complete interval experiment and conformance targets.
- Reverified pinned PNT inventory source identity, inventory tests, benchmark
  freshness, release manifest and trust surface, dependency DAG, target
  registration, headers, line limits, and whitespace.
- Applied fresh Opus review polish locally: the package now proves the same
  two-stage route at shifted node positions, replay rejects a wrong output
  node, the inventory names the exact Taylor provider, and the SPEC discloses
  the zero-touching mutation's deliberate source removal.

## Current frontier

The branch is ready for upstream review.  Inventory record 404 now has a
two-event generic replay from the exact `329/50` point through a positive inner
log enclosure to the outer lower bound `0.633415`.

## Next step

Update the existing non-draft PR, run a genuinely fresh exact-head Opus review,
and monitor the matching CI run without merging it.

## Blockers

None for the source-pinned finite fixture.  General arbitrary-rational log
evaluation still requires a Mathlib-free numerical provider and a reusable
rational interval fact domain.
