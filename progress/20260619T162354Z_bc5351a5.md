# HexGramSchmidtMathlib Phase 6 grind sweep (#8074)

## Accomplished

Diagnosed #8067 as unsound in its requested raw-fast theorem shape and released
it for replan with a concrete issue comment: the full fast `some` branch can
emit unrecorded unit raw factors (for example a nonzero constant core), so
`∀ raw, Hex.ZPoly.Irreducible raw` is too strong for the current `h_raw`
contract.

Claimed #8074 and promoted the five public equation-shaped
`HexGramSchmidtMathlib/Basic.lean` row transport simp lemmas to
`@[simp, grind =]`: `rowToEuclidean_apply`, `rowToEuclidean_zero`,
`rowToEuclidean_add`, `rowToEuclidean_sub`, and `rowToEuclidean_smul`.
All five were re-read as literal `lhs = rhs` equations before tagging.

Verification:

- `lake build HexGramSchmidtMathlib.Basic` green; `HexGramSchmidtMathlib.Basic`
  built in 3.6s.
- `lake build HexGramSchmidtMathlib` green; sibling files replayed existing
  unused-variable warnings in `Int.lean` and `Update.lean`.
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` exit 0.
- Added-line forbidden-token grep over `HexGramSchmidtMathlib/Basic.lean`
  found no new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

Annotation-only change in `HexGramSchmidtMathlib/Basic.lean`; no proof bodies,
statements, imports, or sibling files changed. The pre-existing local
`.claude/CLAUDE.md` modification remains untouched and outside this commit.

## Next step

Open the #8074 PR from this branch.

## Blockers

None for #8074. #8067 needs replanning around a recordable-raw or narrower
fast-core-success theorem shape.
