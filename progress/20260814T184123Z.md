# PR #9225 local preparation

## Accomplished

- Replayed only the generic bottom-up branch-proof feature onto exact merged
  #9224 main `22d293dd6f2e6a29ba49961ea40496cd7194fce5`.
- Preserved current refutation, PNT-log, Machin, endpoint-sine, and retained
  runtime-tree content while moving the ReLU client into its own conformance
  module.
- Updated the extracted ReLU client to the sealed session-owned branch-start
  API and restored the exact parent, child, scope, chronology, rule-key,
  version, and installed-fact guards from the merged ReLU vertical.
- Kept both ordinary ReLU theorems honestly named and documented as
  distinct-assumption plumbing for an unconditional result, with guarded axiom
  reports.
- Audited the generic fold's shape checks, traversal bounds, source/run and
  child-input binding, callback trust boundary, and final kernel type check.
- Refreshed the exact lakefile freshness exemption for registering only the
  Mathlib-free branch-proof module and its ReLU conformance client.

## Current frontier

- The local feature candidate builds its focused modules. It is intentionally
  unpushed while #9260 prepares the final upstream base.

## Next step

- Rebase once onto the final post-#9260 main, rerun focused/static checks, then
  push and obtain fresh exact-head review and CI.

## Blockers

- None.
