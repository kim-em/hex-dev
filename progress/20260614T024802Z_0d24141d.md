# Progress: #7005 — monicGcd scalar-leak def-swap (PARTIAL)

Feature session on the residual of #6983: route every Yun/squareFree gcd
through the monic-normalized `monicGcd` (substrate from PR #7003) to kill the
F_p (p>2) scalar leak in `squareFreeDecomposition`. Branch based on the #7003
head `agent/b544b291` (substrate not yet merged to main).

This is the atomic def-swap. It is **not green**: 27 errors remain, all in the
genuinely-hard scalar-sync / provider / interface-unification clusters. The
mechanical bulk (87 → 27 errors) is done and committed in logical increments.

## Accomplished

1. **6 executable def sites** swapped to `monicGcd` (deliverable #1, complete):
   `yunFactorsWithLevel`, `yunFactors`, `yunFactorsContributionWithLevel`,
   `yunFactorsContribution` (`let y := monicGcd c w`),
   `squareFreeAuxRevContribution`, `squareFreeAuxRev` (`let g := monicGcd f df`).

2. **Loop-mirror migration** (deliverable #3, mostly complete):
   - `DensePoly.gcd c w` → `monicGcd c w` in all recursion mirrors (line ≥3000),
     preserving the 9 `normalizeMonic (DensePoly.gcd c w)` forms (those *are*
     `monicGcd` by definition; must not be double-wrapped).
   - 136 `DensePoly.gcd f (DensePoly.derivative f)` squareFreeAux mirrors → monicGcd.
   - 2 constant-form mirrors `DensePoly.gcd (C k) w` → monicGcd.
   - Reconstruction/dvd lemma applications swapped to monicGcd analogues
     (`div_monicGcd_mul_reconstruct`, `monicGcd_dvd_left/right`) with `hp`
     obtained locally via `ZMod64.PrimeModulus.prime (p := p)` (no signature
     ripple — the key that made the migration tractable).
   - Added bridge `dvd_monicGcd` (`d ∣ c → d ∣ w → d ∣ monicGcd c w`) for the
     `apply DensePoly.dvd_gcd` sites whose goal became `_ ∣ monicGcd …`.
   - Wrapped `isZero = false` mirror proofs in `monicGcd_isZero_false_of_gcd_nonzero`.

3. **Keystone for scalar-sync** (deliverable #2, foundation only):
   - `normalizeMonic_C_mul_snd` — monic normalization absorbs a nonzero constant
     scalar: `(normalizeMonic (C v * h)).2 = (normalizeMonic h).2` for `v ≠ 0`.
   - `monicGcd_scalar_invariant` —
     `monicGcd (C u_c·c) (C u_w·w) = monicGcd c w` for `u_c,u_w ≠ 0`.
     This is the load-bearing lemma: with monic normalization the unit the raw
     gcd would leak is absorbed, so scaled/unscaled recursion variables coincide
     *exactly* (no tracked scalar `v` on the gcd). Both compile.

4. **Over-reach reverts**: the bulk pass wrongly converted proof bodies of three
   lemmas whose *statements* are raw (`normalizeMonic (DensePoly.gcd c w)`):
   `normalizeMonic_gcd_monic_of_right_nonzero`,
   `yunStep_gcd_normalized_one_of_common_dvd_one`, `gcd_C_ne_zero_dvd_one`.
   Reverted their proofs to raw gcd divisibility.

No `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` added. `git diff --check`
clean.

## Current frontier — 27 remaining errors (all genuine rework)

Run `lake build HexPolyFp.SquareFree` to reproduce. Clusters:

- **Scalar-sync rebuild (11 errors)** — `yunScalarStep` (4884),
  `yunFactorsContributionWithLevel_scalar_step_bridge` (4932),
  `…_scalar_sync` (7740), `…_scalar_sync_of_derivative` (7924).
  These proofs still define `yScaled := DensePoly.gcd (C u_c·c)(C u_w·w)` (raw),
  but the executable now computes `monicGcd (C u_c·c)(C u_w·w)`. Recipe: set
  `yScaled := monicGcd (C u_c·c)(C u_w·w)` and rewrite it to `monicGcd c w = y`
  via `monicGcd_scalar_invariant`. The tracked gcd scalar `v` then disappears:
  the tail `ih` is invoked with `u_c := 1` (not `v`) since `yScaled = y` exactly;
  only the residual second argument keeps its `u_w` scalar. `step_bridge`/
  `branch_sync` collapse accordingly (the raw-gcd `= C v * monicGcd` relations
  are no longer needed).

- **normalizeMonic↔monicGcd interface unification (≈6 errors)** —
  `yunStep_tail_common_dvd_one_of_gcd_normalized_one` (7050),
  `yunStep_gcd_normalized_one_of_constant_common_dvd_one` (7193),
  `…_size_bound_derivative_active` (8323),
  `yunFactorsPairwiseReachable_common_dvd_one_derivativeSplit` (5861).
  The `(normalizeMonic (DensePoly.gcd c w)).2 = 1` / `.size` statements now
  mismatch the monicGcd-producing proofs around them. Cleanest fix: restate this
  whole family in terms of `monicGcd c w` (= `(normalizeMonic (gcd c w)).2` by
  `monicGcd_def`) and update the small consumer set in lockstep. Add helpers
  `monicGcd_eq_one_of_dvd_one` and `dvd_one_of_monicGcd_eq_one` mirroring the
  existing `normalizeMonic_*` versions.

- **Provider re-grounding (deliverable #4, not started — ≈8 errors)** —
  `squareFreeAuxRevContribution_derivative_active_pow_obligation` (8848),
  `squareFreeAuxRev_factor_dvd_input` (9526),
  `squareFreeAuxRev_pairwise_coprime_nil_core_of_residual` (9880).
  With monic intermediates `squareFreeContributionReachable` of every recursion
  variable is provable (`monic ⇒ size=1 → =1`), so the false
  `YunDerivativeActiveRawStateProvider` / `hrawState` / `residualInvariant`
  hypotheses should be re-grounded as lemmas and stripped from the chain
  `…_pow_obligation → …_correct_pow_of_nonzero → …_correct →
  squareFreeAux_weightedProduct_nonzero → squareFree_weightedProduct`.
  Use `monic_div_monicGcd_left/right_of_monic` (already in the substrate).

## Next step

1. Rebuild the scalar-sync family using `monicGcd_scalar_invariant` (above).
2. Unify the normalizeMonic-one/size family on `monicGcd`.
3. Re-ground the providers and strip `hrawState`.
4. Add the F_5 `#guard` (deliverable #5): `weightedProduct d.factors = f` for
   `(x+1)^5 (x+2)^5 (x+3)` over F_5 (coeffs low→high
   `[1,2,0,0,0,4,3,0,0,0,3,1]`), guarding the regression.
5. Verify: `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
   `python3 scripts/check_dag.py`.

## Blockers

- Atomic change: no green intermediate, so this lands `--partial` (red, will not
  auto-merge) for planner re-scoping of the residual.
- Branch is based on unmerged PR #7003 (`agent/b544b291`); rebase onto main once
  #7003 lands.
