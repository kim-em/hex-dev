# 93a536d0 — repair PR #4917 merge conflict (issue #5099)

## Accomplished

- Claimed issue #5099 and repaired PR #4917 by merging current
  `origin/main` into the remote PR head `agent/8a4738a1`.
- Resolved the `HexBerlekampZassenhausMathlib/Basic.lean` conflict by
  keeping main's newly landed primitive degree-cover wrapper and preserving
  #4902's two per-normalized-factor `_of_bound` siblings plus their thin
  default-bound wrappers.
- Left the intended #4902 theorem surface unchanged:
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate_of_bound`
  and
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate_of_primitive_pos_lc_core_of_bound`
  remain present, with the existing unsuffixed wrappers delegating through
  the abstract-bound siblings.

## Current frontier

PR #4917 is repaired locally as a merge commit on top of `origin/main`.
The only source conflict was in the unscaled per-normalized-factor bridge
cluster. No downstream #4918/#4919 theorem work was added.

## Next step

Push the merge repair back to `agent/8a4738a1` and confirm GitHub reports
PR #4917 as mergeable.

## Blockers

`lake build HexBerlekampZassenhausMathlib.Basic` preserves the known
`Basic.lean` baseline: it fails in the existing heartbeat-timeout and
kernel unknown-constant clusters around lines 4885, 5133, 5441, 5528,
5575, 5812, 5925, 6070, 6215/6234, 6549/6613, 15353, 15464, and
15671/15717, with no errors in the repaired theorem region.
