# Progress - session b16196e1 (work → feature)

## Accomplished

Two issues handled this session.

**#7578 (cut projection soundness) — skipped for replan, premise unsound.**
Before writing any proof, sanity-checked deliverable 1 (per "directives are
hypotheses"). The prescribed cut-soundness lemma ("surviving rows of
`bhksWithinGramSchmidtCut` integer-span every short lattice vector of `L`",
justified by "rows failing the cut are too long to participate") is false:
the cut keeps row `i` iff `‖b*_i‖² ≤ R = cutRadiusSq4/4`, but the survivor
set need not be a GS-norm prefix under LLL, so a trailing long row can still
participate in a short lattice vector. Concrete LLL-reduced (δ=3/4)
counterexample `b₀=(4,0)`, `b₁=(2,3)` (GS norms 16, 9; Lovász `9 ≥ 8`): with
`R=15`, row 0 is cut, row 1 survives, and `v=b₀−b₁=(2,−3)` has `‖v‖²=13 ≤ R`
but `(2,−3) ∉ ℤ·(2,3)`. An adversarial verification agent confirmed the
counterexample and the key distinction: "too long to participate" proves only
the leading-coefficient bound (true), not survivor-span membership (false) —
and deliverable 2 needs the false half. Independently, deliverable 2's
factorization→lattice bridge is absent (trueSupports is abstract; no lemma
that indicators are projections of genuine short lattice vectors; no
CLD-vanishing-for-true-factors lemma; LLL preservation proven only for
`steeredReduce`, not `shortVectorsUnchecked`). Posted a full diagnostic
comment with the corrected decomposition and `coordination skip`'d for replan.

**#7579 (HexGF2 Phase 6 Euclid polish) — landed.**
Added the two gcd zero-identity normal forms downstream callers otherwise have
to unfold `xgcd`/`xgcdAux` for: `gcd_zero_right : gcd p 0 = p` and
`gcd_zero_left : gcd 0 q = q`, both `@[simp]` (mirroring `Nat.gcd_zero_*`).
The rest of `HexGF2/Euclid.lean` was already at Phase 6 quality (docstrings,
simp/grind annotations on the division/gcd/inverse surface), so no further
changes were warranted; left the public long-form theorem names and the
`grind` regression `example` as-is.

Verification: `lake build HexGF2` (full library, incl. Field/RabinSoundness),
`python3 scripts/check_dag.py` (exit 0), `git diff --check`, added-line
forbidden-token scan (no sorry/axiom/native_decide/TODO/FIXME).

## Current frontier

`HexGF2/Euclid.lean` gcd surface now has zero-identity simp normal forms.

## Next step

#7578 awaits a planner replan with the corrected target-specific lemma shape
(structural survivor-span membership for `(indicator_S | 0)`, plus the
predecessor bridge lemmas listed in the issue comment).

## Blockers

None for #7579. #7578 is blocked on a corrected plan (premise defect).
