## Accomplished

- Claimed #6413 after checking the unclaimed queue and selecting it as the
  active prerequisite for the SquareFree normalized-provider chain.
- Added two private bridge lemmas in `HexPolyFp/SquareFree.lean`:
  - `yunFactorsContributionWithLevel_normalized_tail_reconstruct`
  - `yunFactorsContributionWithLevel_normalized_tail_product_bridge`
- The new lemmas make the normalized repeated-tail scalar explicit when
  transporting the scaled Yun contribution product, so the proof surface does
  not assume executable `DensePoly.gcd` output is monic.

## Current frontier

This is useful substrate for #6413 but does not close the full issue. The
remaining hard part is still a genuine synchronization theorem for the raw
derivative-active Yun split of `DensePoly.C u * f` against the normalized split
for `f`, strong enough to identify both the contribution product and residual
through executable gcd/division scalar ambiguity.

## Next step

Prove or decompose the missing executable-gcd scalar-multiple facts:

- derivative and initial split behavior for `DensePoly.C u * f` with `u ≠ 0`;
- how `DensePoly.gcd (DensePoly.C u * f) (derivative (DensePoly.C u * f))`
  relates to a scalar multiple of the normalized/raw gcd of `f`;
- quotient compatibility for the Yun `c = f / g` and tail `w / g` steps.

Then use those facts to relate the full `yunFactorsContributionWithLevel` state
under scalar normalization.

## Blockers

No build blocker for this slice. The full #6413 deliverable remains blocked on
the executable gcd/division scalar-synchronization lemmas, which were not present
in the current codebase.

## Verification

- `lake build HexPolyFp.SquareFree` passed.
- `lake build HexPolyFp` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean` found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
