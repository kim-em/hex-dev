# 20260516T221716Z — feature session: #4529 Berlekamp factor Nodup

## Accomplished

Landed the structural claim `Hex.Berlekamp.berlekampFactor_factors_nodup` for
square-free monic inputs as two commits on `agent/4d285970`:

1. `refactor: tighten isNontrivialSplitFactor to require strict size decrease`
   — replaced the `g ≠ f` conjunct in `isNontrivialSplitFactor` with the
   strict `g.size < f.size`, eliminating the non-identity unit-multiple
   `g = u · f` case the parent issue flagged as the obstruction to a
   clean `Nodup` invariant. Updated
   `kernelWitnessSplit?_some_of_nontrivial_splitFactorAt` and the call site
   `exists_kernelWitnessSplit?_some_of_witnessProduct_dvd_of_pos_degree`,
   which now derives strict size from the existing `hnonconst` hypothesis
   via the `(f / gcd).size = 1 ⟹ f ∣ gcd` chain in `HexPolyFp.Basic`.

2. `feat: prove Hex.Berlekamp.berlekampFactor_factors_nodup (#4529)` —
   split into an abstract loop invariant
   `berlekampFactor_factors_nodup_of_no_squared` in
   `HexBerlekamp/Factor.lean` and a `gcd f f' = 1`-instantiated wrapper
   `berlekampFactor_factors_nodup` in `HexBerlekamp/RabinSoundness.lean`.
   The wrapper discharges the abstract hypothesis via
   `isUnitPolynomial_of_squareFree_of_squared_dvd`.

## Current frontier

- `HexBerlekamp/Factor.lean` now exports
  `kernelWitnessSplit_size_lt`, `kernelWitnessSplit_factor_pos_degree`,
  and `berlekampFactor_factors_nodup_of_no_squared` as public structural
  facts about the executable Berlekamp loop.
- The squareFree-instantiated theorem matches the signature stated in
  the parent issue body.

## Next step

Sub-issue B (the Mathlib-bridge `berlekampFactor_factors_nodup` in
`HexBerlekampMathlib/Basic.lean` and the `choosePrimeData`-shaped wrapper
`factorsModP_nodup_of_factorsModPBerlekampForm` in
`HexBerlekampZassenhausMathlib/Basic.lean`) can now use this theorem to
discharge the `hfactorsModP_nodup` parameter on
`henselLiftData_liftedFactor_injective_of_choosePrimeData`.

## Blockers

None.
