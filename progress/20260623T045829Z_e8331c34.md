# M1 Hensel lift invariant + core per-factor congruence — #8307

Session type: feature (one-shot `/once`). UTC 2026-06-23T04:58.

## Accomplished

Landed the deliverable of #8307 (prereq for the #8290 core forward cut): the
`monicTarget` Hensel lift invariant and, from it, the per-factor core-coordinate
congruence `coreLiftData_liftedFactor_hensel_semantics`. Purely additive
(+341 lines across `Basic.lean` and `LiftBridge.lean`), no `sorry`/`axiom`/
`native_decide`. `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
green (3785 jobs).

New lemmas in `HexBerlekampZassenhausMathlib/Basic.lean` (M1 section):

- `modP_scale_intCast` — `modP p (scale c f) = scale ↑c (modP p f)` for integer
  `c`, proved coefficientwise through `toZMod` into the genuine field `ZMod p`.
- `monicModularImage_modP_eq_modP_monicTarget` — the crux bridge:
  `monicModularImage (modP p core) = modP p (monicTarget core p k)` under a good
  prime and `gcd(ℓf, p^k) = 1`. The scalar identification `(lc (modP p core))⁻¹ =
  ↑ℓf⁻¹` is done in `ZMod p` via `eq_inv_of_mul_eq_one_*` plus the unit-residue
  certificate `leadingCoeffInverse_mul_emod` (reduced mod p from mod p^k).
- `factorsModP_polyProduct_congr_monicImage_of_factorsModPBerlekampForm` — the
  form-only product congruence (the `_of_primitive_pos_lc_core` body with its
  unused primitivity / lc / good-prime premises dropped, so it applies to
  non-monic `core` with no spurious `Primitive core` obligation).
- `factorsModP_polyProduct_congr_monicTarget` — `∏ factorsModP ≡ monicTarget
  (mod p)`, combining the form-only congruence with the bridge.

Main theorem in `HexBerlekampZassenhausMathlib/LiftBridge.lean`:

- `scale_mul_right_zpoly` — `scale c (a*b) = a * scale c b` (transport to
  `Polynomial ℤ`).
- `coreLiftData_liftedFactor_hensel_semantics` — the M1 analogue of
  `toMonicLiftData_liftedFactor_hensel_semantics`. Rebuilds the
  `QuadraticMultifactorLiftInvariant` for the `monicTarget` from `core`'s own
  `choosePrimeData?` witness (boundary facts transfer because `monicTarget ≡
  ℓf⁻¹·core (mod p)`), takes the singleton subset/complement congruence, and
  scales by `ℓf` back into `core`'s coordinate via
  `leadingCoeff_scale_monicTarget_congr_core`. The complement `h` is
  `scale ℓf (∏ rest)`.

## Current frontier

`coreLiftData_liftedFactor_hensel_semantics` exists, is green, and produces the
exact `hfac` shape (`∀ i, ∃ h, Monic gᵢ ∧ 0 < deg gᵢ ∧ congr core (gᵢ * h)
(p^a)`) that #8290's core forward cut consumes. The selection witness is
`choosePrimeData? core = some primeData`, matching the #8304 swap.

## Next step

#8290 (the core forward cut producer) can now route `recoveredColumnBound`,
`supportShortVectorData_of_recoveredLift`, and
`cutProjectionHypotheses_of_recoveredLift` through this `hfac`.

## Blockers

None.
