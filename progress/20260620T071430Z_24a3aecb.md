# 2026-06-20T07:14Z — feature session (#8204)

## Accomplished

- **HexHensel Phase 7 reference chapter.** Authored
  `HexManual/Chapters/HexHensel.lean`, a Verso chapter for the
  executable Hensel-lifting layer following the `HexPoly`/`HexPolyZ`
  model: introduction situating Hensel lifting between `HexPolyFp`/
  `HexPolyZ` and the factorization stack; coefficientwise reduction
  (`reduceModPow`, `reduceModSquare`, `congr_reduceModPow`); single-step
  corrections (`LinearLiftResult`, `linearHenselStep`,
  `QuadraticLiftResult`, `quadraticHenselStep`) with the linear
  (`p^k → p^(k+1)`) vs. quadratic (`m → m²`) trade-off; the iterative
  and multifactor lifts (`henselLift`, `multifactorLift`,
  `multifactorLiftQuadratic`); key correctness theorems
  (`henselLift_spec`, `multifactorLift_spec`,
  `multifactorLiftQuadratic_spec`, `QuadraticLiftLoopInvariant`,
  `quadraticLiftLoopInvariant_step`, `multifactorLiftQuadratic_each_monic`);
  and a cross-references section.
- **Worked example.** A `#guard` block over the cleanly-evaluable
  coefficient-reduction surface (`reduceModPow` mod 7², `reduceModSquare`
  mod 5²). The lift functions transitively call the `@[extern]`
  `HexArith.Int.extGcd`, which the elaborator's interpreter cannot run,
  so per the issue's allowance those are documented by
  signature/docstring rather than live `#eval`. Values validated against
  the fast library before the slow `HexManual` build.
- Wired the chapter into `HexManual.lean` (import + `{include}`) and
  bumped `libraries.yml[HexHensel].done_through: 6 → 7`.

## Verification

- `lake build HexManual` green; `HexManual.Chapters.HexHensel` built with
  no errors (all `{name}`/`{docstring}` cross-references resolve).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` lists `HexHensel` under "Fully done".
- No `axiom`/`sorry`/`native_decide` introduced; no Mathlib dep added to
  a computational library.

## Current frontier

HexHensel is fully done (Phase 7). No tutorials are anchored to it.

## Next step

Other `done_through ≥ 6` libraries still need Phase 7 chapters
(`HexHenselMathlib` is at Phase 6).

## Blockers (noted, not mine to fix)

- Directive #2564 (van Hoeij CLD headline theorem) is **correctly
  unclaimable**: it is blocked by open dep #4818, which is itself blocked
  on #2564 (circular by design). I verified the block is *not* stale: the
  remaining two sorries (`factor_irreducible_of_nonUnit`,
  `Hex.ZPoly.isIrreducible_iff`) reduce to the fast-path BHKS/CLD
  raw-factor irreducibility, which has no unconditional proof and carries
  the documented "hsep-at-success" soundness trap (#7950/#7951). The easy
  fast branches (constant, singleton, quadratic) and the structural
  assembly (`factor_entries_irreducible` taking `h_raw`) are proved; the
  general CLD core success branch is the genuine remaining math. #4818's
  body already documents this accurately, so no new comment was warranted.
