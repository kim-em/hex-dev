# 2026-06-05 00:00Z — HexLLL Step 1 GS norm telescope (#6558)

## Accomplished

Exposed `LLLCore.basisNormSq` and added the Mathlib-free Step 1 proof chain in
`HexLLL/Basic.lean`: adjacent Lovasz/size-reduction norm bound, reciprocal
adjacent bound, and the telescoped first-vector bound.

Verified:

- `lake build HexLLL.Basic`
- `lake build HexLLL`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Lean-only added-line scan for `sorry`, `axiom`, `native_decide`, `TODO`,
  and `FIXME`

## Current frontier

Issue #6558 deliverables are implemented. The downstream short-vector assembly
step remains separate.

## Next step

Open the PR for #6558 with the new `LLLCore.stepBound`, `LLLCore.stepAlpha`,
and `LLLCore.teleBound` lemmas available to downstream code.

## Blockers

No implementation blockers. `/reflect` is not available as an executable in
this non-interactive shell (`/reflect` returned `no such file or directory`).
