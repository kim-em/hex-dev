# Planner: HexHensel Multifactor Phase 6 polish

Session: `ab8197f4` (plan)

## Accomplished

- Oriented: `coordination orient` reported three blocked directives
  (#2637, #2567, #2564), queue depth 2 with `POD_QUEUE_DEFICIT=1`, and
  no broken PRs needing repair. The single open PR #2656 was draft and
  not merge-eligible.
- Reviewed the last five progress files; identified that #5599 already
  owns HexHensel/Basic.lean Phase 6 polish, leaving sibling modules in
  `HexHensel/` open for separate slices.
- Surveyed `HexHensel/Multifactor.lean` (312 lines) and found a clean,
  atomic polyProduct-cluster polish target:
  - `Hex.ZPoly.one_mul_zpoly` is the basic left-identity normaliser for
    `ZPoly` multiplication and is currently un-annotated, despite being
    invoked by name from multiple `rw` sites in
    `HexBerlekampZassenhaus/Basic.lean`.
  - `polyProduct_singleton_append` and `polyProduct_cons_toArray` are
    head-prepend specialisations that may admit `@[simp]` once checked
    against the existing `polyProduct_append` / `polyProduct_singleton`
    simp lemmas for loop-freedom.
  - `list_foldl_mul_eq_mul_foldl_one` is an associativity helper that
    should remain un-annotated but may benefit from a docstring tighten.
- Posted issue #5612 (`feature`) covering exactly that cluster, with
  Deliverables / Verification / Out-of-scope explicitly disjoint from the
  in-flight #5599 slice on `HexHensel/Basic.lean`.

## Current frontier

Queue depth has been refilled to the configured minimum
(`POD_MIN_QUEUE=3`): two pre-existing unclaimed feature issues (#5602,
#5603) plus the newly posted #5612. Four other feature issues are
claimed (#5598, #5599, #5601, #5602 was unclaimed; #5602/#5603 in
HexPolyZMathlib pipeline). All three open directives remain blocked on
their dependency chains; the replan loop owns those.

## Next step

A future planner cycle should refill from any new gap. Candidate Phase 6
targets that have not had recent polish:

- `HexHensel/Multifactor.lean` (this issue's target — taken).
- `HexHensel/Linear.lean` (1761 lines, no Phase 6 slice scheduled).
- `HexHensel/Quadratic.lean` (2306 lines, no Phase 6 slice scheduled).
- The `Hex.Conway.conwayPoly_*` cluster at the bottom of
  `HexConway/Basic.lean` (`conwayPoly_nonconstant`,
  `conwayPoly_irreducible`, `conwayPoly_monic`) — flagged as a
  follow-up by the previous HexConway Phase 6 progress.
- `HexGfq/Basic.lean` GF2q section (no recent polish since #5329).

## Blockers

None. `coordination set-target` and `coordination set-min-queue` not
called — operator-owned, and the project is in active development, not
winding down.
