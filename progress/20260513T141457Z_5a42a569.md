# Session 5a42a569 — HexBZ conformance: align adversarial oracle fixture stream (#3528)

## Accomplished

- Skipped #3626 (HexPolyZMathlib Phase 4 benchmark harness) with a
  `coordination skip` documenting the conflict with
  `SPEC/benchmarking.md` §"Mathlib-free benches" (landed in #3684) and
  with HO-33 (#3689, merged 2026-05-12), which explicitly deleted the
  analogous bridge `Bench.lean` files and their lakefile / CI entries.
- Left informational comments on the sibling stale issues #3629
  (HexHenselMathlib) and #3634 (HexGramSchmidtMathlib) pointing at the
  same SPEC / HO-33 conflict so the next planner can triage them
  together.
- Claimed #3528 and verified its central assumption: regenerating
  `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl` from
  `lake exe hexbz_emit_fixtures` produces a byte-identical file
  (already committed as such by #3501 / #3600). No fixture-stream
  diff was required.
- Updated `HexBerlekampZassenhaus/EmitFixtures.lean`:
  - Module docstring now lists the four `adv/*` HO-2 (#2565) cases
    alongside the existing shape catalogue, with a per-case role
    breakdown citing
    `SPEC/Libraries/hex-berlekamp-zassenhaus.md` §"Conformance fixtures".
  - Renamed the "Pinned-prime modular split smoke case" section to
    "HO-2 adversarial cases with pinned modular split metadata" with
    a docstring that ties each emitted case to its conformance
    counterpart and oracle role.
  - Each `adv/*` case carries an inline comment naming its case-id,
    modular split, and HO-2 role (subset-product vs heavy-split).
  - The degree-list in the catalogue gained `8` (it was missing the
    Swinnerton-Dyer and Φ₁₅ degree band).
- Updated `HexBerlekampZassenhaus/Conformance.lean`:
  - The "Adversarial modular split smoke cases" section header now
    tags HO-2 / #2565 and quotes the SPEC requirement.
  - The docstring lists the four `adv/*` case-id stems so the
    correspondence between the Lean polynomial names and the JSONL
    case-ids is explicit.
- Updated `scripts/oracle/bz_flint.py`:
  - Module docstring adds an "HO-2 adversarial coverage (#2565)"
    paragraph naming the four `adv/*` cases, their pinned modular
    splits, and the role of the `modFactorPrime` /
    `modFactorDegrees` cross-check.

## Verification

- `lake build HexBerlekampZassenhaus.EmitFixtures` —
  built successfully.
- `lake build HexBerlekampZassenhaus.Conformance` —
  built successfully.
- `lake build HexBerlekampZassenhaus.CrossCheck` —
  built successfully.
- `lake exe hexbz_emit_fixtures > /tmp/hexbz-bz.jsonl &&
  diff conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl
  /tmp/hexbz-bz.jsonl` — exit 0 (no fixture-stream changes).
- `python3 scripts/oracle/bz_flint.py --check` —
  `checked 27 case(s), 0 failure(s)`.
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' …` — clean on the
  edited files.

## Current frontier

Issue #3528 deliverables 1 (per-case HO-2 source comments), 2 (fixture
stream regeneration), and 3 (HO-2 coverage language in oracle and
conformance docstrings) are discharged on this branch. The oracle code
itself was not adjusted because the regenerated stream matched
byte-for-byte; the issue's "only adjust the oracle code if the
regenerated metadata reveals a genuine driver gap" condition did not
fire.

## Next step

- Land this PR.
- Planner triage on #3626 / #3629 / #3634 to reconcile the
  Hex*Mathlib Phase 4 model with SPEC + HO-33 (either narrow the
  issues to phase4.input_families bookkeeping only, or close them
  with a forward link).

## Blockers

None.
