# Session e0c44ac6 — bounded BZ wrapper products (#3871)

## Accomplished

- Claimed #3871 after #3860 was claimed concurrently.
- Added product lemmas for integer-root splitting, quadratic shortcut output,
  slow bounded wrapper arrays, and successful fast bounded wrapper arrays in
  `HexBerlekampZassenhaus/Basic.lean`.
- Reused the landed core product lemmas and exact normalized reassembly theorem.
- Adjusted the private raw slow/fast constant-core branches to keep the
  constant square-free core in the raw array, so the raw array product contract
  is exact before the existing factorization filter.

## Current frontier

The raw bounded slow wrapper and every successful fast wrapper branch now have
private polynomial-product witnesses. The public `factor_product_of_bound`
skeleton remains out of scope and still has its pre-existing `sorry`.

## Next step

Use these raw wrapper lemmas in the later canonicalization/product issue that
bridges `factorizationOfFactors` and `Factorization.product`.

## Blockers

None. The worktree still has the pre-existing `.claude/CLAUDE.md` modification
from pod bootstrap; it was not staged.
