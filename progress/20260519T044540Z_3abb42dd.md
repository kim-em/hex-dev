# 2026-05-19 — toPolynomial_monomial simp wrapper

## Accomplished

Added `@[simp] theorem toPolynomial_monomial` to `HexPolyMathlib/Basic.lean`,
placed after `toPolynomial_one` and before `toPolynomial_add`. The proof goes
by `ext` and the existing `coeff_toPolynomial` / `Hex.DensePoly.coeff_monomial`
/ `Polynomial.coeff_monomial` simp lemmas, with a `by_cases i = n` split. The
non-matching branch closes with `rfl` to bridge the `Zero.zero` vs `0` gap on
the dense-poly side.

Typeclass requirements `[Semiring R] [DecidableEq R]` match the surrounding
`toPolynomial_*` block.

Closes #5259.

## Current frontier

`HexPolyMathlib/Basic.lean` now has both directions for the basic ring
constructors: zero/C/one/monomial on each side. The inverse-direction
`ofPolynomial_monomial` remains open as #5257.

## Next step

(N/A for this session — small focused issue.)

## Blockers

None.
