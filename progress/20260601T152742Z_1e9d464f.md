# 1e9d464f - work #6054

## Accomplished

- Claimed #6054 after #6056 raced to another agent.
- Added private accumulator invariants showing Yun-emitted square-free factors
  have positive multiplicity through `yunFactorsWithLevel` and
  `squareFreeAuxRev`.
- Exposed closed public decomposition-shaped wrappers:
  - `squareFreeDecomposition_factors_squareFree`
  - `squareFreeDecomposition_multiplicity_pos`
- Verified `lake build HexPolyFp`, the Mahler/Schur counterexample oracle,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token
  diff grep over `HexPolyFp/SquareFree.lean`.

## Current frontier

#6054 is partially complete. The square-free and positive-multiplicity facts
are now closed wrappers over the actual `squareFreeDecomposition` output, but
the weighted-product and pairwise-coprime public theorems still expose
provider-parametric proof plumbing.

## Next step

Derive the derivative-active state and residual providers privately for the
normalized decomposition call path, then expose closed
`squareFreeDecomposition_weightedProduct` and
`squareFreeDecomposition_pairwise_coprime` wrappers and move the provider-shaped
theorems behind an internal boundary.

## Blockers

No technical blocker for the landed subset. Full closure needs a larger proof
assembly around the existing `yunFactorsDerivativeActiveReachable` and
`squareFreeAuxRevResidualSatisfied` machinery.
