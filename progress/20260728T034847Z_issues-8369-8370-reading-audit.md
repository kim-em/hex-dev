# Accomplished

- Audited issues #8369 and #8370 against the current lattice, prime-selection,
  Mathlib bridge, and dispatcher implementations.
- Read the BHKS rational-factorization proof and compared its notation,
  theorem numbering, termination condition, and LLL cut argument with the
  implementation and SPEC.
- Identified preparatory proof gaps: no explicit true-support submodule/reverse
  containment interface, no general retained-row LLL norm bound, and no proved
  analytic soundness theorem for `bhksBound`.
- Identified smaller integration facts for #8370: the live selector is a fixed
  two-list fold, its hot-path list has 94 odd primes rather than 95, Mathlib's
  resultant/discriminant API is already available, and the executable
  `HexResultantMathlib` correspondence still contains unfinished proofs.
- Checked the final dispatcher and noted that a no-trial theorem must discharge
  its product-equality guard as well as prove lattice non-failure.

# Current frontier

No implementation was changed.  The literature-to-code map is complete enough
to order the work: first fix the exact BHKS theorem/version/constants and design
the missing proof-facing lattice interfaces; #8370's selector and discriminant
lemmas can proceed largely independently.

# Next step

Typecheck proposed theorem signatures at the transformed-monic boundary, prove
the fixed-candidate selector existence lemma, and state an exact `W = L'`
termination target using a new true-support span.  Before proving the numeric
cap, derive `bhksBound` from one pinned version of the BHKS rational theorem and
verify every constant and inequality.

# Blockers

- The current SPEC cites theorem/equation numbering that does not match either
  the pinned 2008 paper or arXiv v1, and its bound discussion does not yet
  justify the executable `bhksBound` formula.
- The reverse lattice inclusion needs a general LLL row-norm lemma not currently
  exposed by the Mathlib bridge.
- Depending on the public theorem shape chosen for #8370, using the executable
  resultant layer would also require completing its correspondence proofs;
  using Mathlib `Polynomial.resultant`/`Polynomial.discr` avoids that dependency.
