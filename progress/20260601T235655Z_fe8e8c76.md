# Work session #6163 / #6165

## Accomplished

- Claimed #6163 and inspected `HexPolyFp/SquareFree.lean`, `HexPolyFp/Basic.lean`, and `HexPoly/Euclid.lean` around the Yun derivative-active reachable state, `normalizeMonic`, and executable `DensePoly.gcd`.
- Determined that the issue's requested raw executable gcd monicity lemma is not valid for the current implementation: `DensePoly.gcd` returns the Euclidean last nonzero remainder without normalizing it.
- Marked #6163 for replan with a concrete counterexample shape: over `F_5`, `gcd (x^2 + 1) (x + 1)` reaches constant remainder `2`, so the raw gcd is not monic even though both inputs are monic.
- Claimed #6165 and added the review-only report `reports/hex-arith-wide-word-api-phase6-review.md`.
- Reviewed `HexArith/UInt64/Wide.lean`, `HexArith/ffi/wide_arith.c`, Barrett/Montgomery consumers, `PLAN/Phase6.md`, and `SPEC/Libraries/hex-arith.md`.
- Filed follow-up feature issues #6167, #6169, and #6168 for the concrete Phase 6 gaps found in the report.

## Current frontier

- The derivative-active raw monic invariant planned in #6155 cannot be proved against the current raw `DensePoly.gcd` transition as stated.
- A viable successor needs to either normalize the gcd used by the Yun transition or retarget the downstream invariant to normalized states.
- The UInt64 wide-word API itself is close to Phase 6 quality, but the Montgomery consumers still need `mulFull` alignment and a reconstruction-automation audit before treating this slice as polished.

## Next step

- Replan #6163 into an implementation/API decision: normalize the Yun gcd transition first, or add normalized-gcd state lemmas and adjust #6155 accordingly.
- Execute #6167, #6169, and #6168 as narrow implementation follow-ups from the wide-word review.

## Blockers

- False assumption in #6163: raw `DensePoly.gcd` is not canonical monic for monic inputs.
- None for #6165. `git diff --check` and `python3 scripts/check_dag.py` passed.
