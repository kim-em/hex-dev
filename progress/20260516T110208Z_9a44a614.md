# 2026-05-16T11:02Z — review #4490 (final Lean.Grind instance audit)

## Accomplished

Audited the three final `Lean.Grind.{Semiring, Ring, CommRing}` instance
constructions at `HexGfqRing/Operations.lean` lines ~1048–1116. Every field
already pulls from the intended public characterising lemma (`repr_*` laws
via `ext`, `pow_eq_linearPow` for `pow_succ`, `natCast_succ`,
`nsmul_eq_natCast_mul`, `sub_eq_add_neg`, `neg_zsmul_eq`, `intCast_neg_eq`,
`repr_mul_comm`). Two narrow polish changes landed:

- Semiring `pow_zero` field now uses `pow_eq_linearPow` consistently with
  `pow_succ`, replacing a three-line `change … pow.go …; unfold pow.go; rfl`
  block that exposed the implementation auxiliary. The new proof is
  `change pow a 0 = 1; rw [pow_eq_linearPow]; rfl` and reduces against
  `linearPow x 0 = 1` directly.
- CommRing `mul_comm` field drops `simpa using repr_mul_comm a b` for
  `exact repr_mul_comm a b`; after `apply ext` the goal is literally the
  lemma statement.

Remaining `rfl` fields (`ofNat_eq_natCast`, `zsmul_natCast_eq_nsmul`,
`intCast_ofNat`) sit on stable definitional boundaries: each one's two sides
both reduce to `natCast f hf n` (for the OfNat/NatCast and IntCast `.ofNat`
cases) or `nsmul n a` (for the zsmul `.ofNat` case) via the operation
definitions immediately above the instance block, so no characterising lemma
is needed and the `rfl` is the right API boundary. `simp`-based proofs of
`zero_mul` / `mul_zero` / `neg_add_cancel` are clean as-is — each one's
`simp` set is the minimal step to bridge `repr (0 : PolyQuotient _) = 0` via
`repr_zero` / `reduceMod_zero`.

## Current frontier

- `lake build HexGfqRing.Operations HexGfqRing HexGfqField HexGfq`: green.
- `HexGfqRing/Operations.lean`: 0 `sorry`, 0 `axiom`, 0 `native_decide`,
  0 TODO/FIXME.
- `python3 scripts/check_dag.py`: clean.
- `python3 scripts/status.py HexGfqRing`: Phase 6, `done_through: 5`
  (unchanged — review only).
- `git diff --check`: clean.

## Next step

Issue #4489 (`pow` / `linearPow` exponentiation cluster review) is the
remaining unclaimed Phase 6 review for `HexGfqRing/Operations.lean`. Once
that and the nsmul / intCast / zsmul review (#4482) land, the planner can
audit whether Phase 6 has any further Operations.lean clusters to cover or
whether `done_through` can be promoted to 6.

## Blockers

None.
