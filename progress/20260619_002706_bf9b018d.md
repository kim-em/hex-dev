# HexArith Phase 6 grind automation coverage for Barrett/Context.lean (#8032)

## Accomplished

Promoted the 7 equation-shaped public `@[simp]` lemmas in
`HexArith/Barrett/Context.lean` to `@[simp, grind =]`, matching the
ongoing Phase 6 grind-coverage sweep:

`mk_pinv`, `mk_pinv_toNat`, `mulMod_eq_mul_of_mul_lt`,
`mulMod_zero_left`, `mulMod_zero_right`, `mulMod_one_left`,
`mulMod_one_right`.

All confirmed literal `lhs = rhs`. The proof-valued projection pair
`mk_p_gt` / `mk_p_lt` is left `@[simp]`-only: their RHS is a stored
proof (`hp` / `hlt`), proof-irrelevant and useless as a `grind`
rewrite target, per the issue guidance.

Grind-loop check: every promoted RHS is `0`, `a`, `a * b`, or a
`pinv` value — none reintroduces `mulMod` or an `mk`-projection, so no
cycle. The one/zero identities both terminate at `a`/`0`, so they
compose with the existing `mulMod_comm` (`mulMod a b → mulMod b a`)
grind rule without looping.

## Current frontier

`lake build HexArith` green; `HexArith.Barrett.Context` 2.6s — no
grind-loop slowdown. `scripts/check_dag.py` exit 0. `git diff --check`
clean; no banned tokens. Annotation-only; no proof bodies changed, no
new imports, no `*Mathlib` file touched.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps at session
start: #8029 (HexGF2/Euclid.lean), #8030 (HexGFqField/Operations.lean
repr cluster).

## Blockers

None.
