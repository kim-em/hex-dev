# Progress: #6959 — Yun repeated-tail reachability (diagnosed unsound, skipped)

Feature session. Claimed #6959 ("HexPolyFp: prove Yun repeated-tail
reachability for square-free product"). After investigation, the premise is
unsound at a level deeper than the issue anticipates; reported with
machine-checked evidence and skipped for replan. No code landed.

## Accomplished

- Confirmed the part #6959 actually targets is true: the raw repeated tail
  `(yunFactorsContributionWithLevel (f/g) g 1 1 fuel).2` is **never a
  non-trivial constant** for monic inputs (kernel-evaluated `#guard` over a
  battery of `F_5` polynomials). So the #6883 diagnosis was right and this
  sub-fact is provable without the raw provider.
- Found the actual soundness gap: the public theorem `squareFree_weightedProduct`
  (and `squareFreeAuxRevContribution_correct`) has a **genuinely false
  conclusion** over `F_p` for `p > 2`. Counterexample (machine-checked):
  `f = (x+1)^5 (x+2)^5 (x+3)` over `F_5`, monic. `squareFreeAuxRevContribution
  f 1 (f.size+1)` and the public `C unit * weightedProduct factors` both
  evaluate to `3·f ≠ f`. The multiplicity-1 factor comes out `3x+4 = 3(x+3)`,
  non-monic.
- Root cause: in the contribution loop `y = gcd(c, w)` can return a
  non-trivial **constant** (the coprime case returns `2` over `F_5`, exactly
  the documented behaviour at `normalizeMonic_gcd_monic_of_right_nonzero`).
  Then `z = c/y` and `w/y` each scale by `1/y`, so `c·w → c·w/y = f/2`. The
  freed scalar is never accumulated — `unit` only captures the top-level
  `normalizeMonic` scalar. Even the raw pow invariant
  `contribution.1 * pow contribution.2 1 = pow f 1` evaluates to `4·f`, not
  `f`. Over `F_2` (only unit `1`) the leak is invisible, which is why the
  existing `F_2` `#guard` passes and the gap went unnoticed.
- Consequence: removing `YunDerivativeActiveRawStateProvider` (the #6959 /
  parent #6872 goal) is impossible while the conclusion is false; the provider
  is the load-bearing false hypothesis currently masking it. Posted full
  evidence on #6959 and `coordination skip`ped it (replan).

## Current frontier

#6959 marked `replan` with a detailed comment. The corrected scope is an
**upstream algorithm/statement fix**, not provider removal: either normalize
emitted factors/residuals to monic inside the Yun loop and accumulate freed
scalars into `unit`, or restate the public theorem with the correct
accumulated scalar.

## Next step

A planner should re-scope #6872's chain around the scalar-leak fix. A cheap
permanent guard: add a `p > 2` (e.g. `F_5`) `#guard` on
`C unit * weightedProduct = f` to `HexPolyFp/SquareFree.lean` so the leak is
caught by CI rather than only by manual probing.

## Blockers

None for the diagnosis. The provider-removal task itself is blocked on the
upstream scalar-correctness fix.
