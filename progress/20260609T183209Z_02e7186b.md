# 20260609T183209Z (#6431)

## Accomplished

Claimed #6414 first, found its prerequisite premise stale, and released it
with a concrete counterexample. The existing bridge cannot simply drop the raw
recursive equality while keeping only the normalized recursive equality: a
nonzero non-one constant tail with zero fuel makes the normalized equality
trivial while the scalar bridge conclusion is false. Added #6431 as a
dependency of #6414 and marked #6414 blocked/replan.

Claimed #6431 and added normalized Yun-level progress substrate in
`HexPolyFp/SquareFree.lean`:

- `yunFactorsNormalizedLevelCompletes`, a normalized termination predicate that
  recognizes scalar-unit raw states through `(normalizeMonic c).2`.
- `yunLevel_measure_lt_of_nonconstant_step`, a raw step measure lemma that
  uses `1 < c.size` directly instead of requiring raw
  `squareFreeContributionReachable c`.
- `yunFactorsNormalizedLevelCompletes_of_size_bound_derivative_active`, deriving
  normalized completion from `YunDerivativeActiveNormalizedStateProvider` over
  reachable derivative-active states.

## Current frontier

#6431 has a coherent normalized progress theorem, but the old raw
`yunFactorsLevelCompletes` consumers have not yet been refactored to use this
normalized predicate plus the scalar synchronization wrappers. This is partial
progress toward the scalar-unit completion bridge needed by #6414.

## Next step

Use `yunFactorsNormalizedLevelCompletes_of_size_bound_derivative_active` with
the existing scalar synchronization lemmas around
`yunFactorsContributionWithLevel_scalar_sync_of_derivative_active_reachable` to
replace the raw-provider call path in derivative-active weighted-product
correctness.

## Blockers

None for this partial substrate. Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`
