# HexPolyZ: Rat divMod_spec from array reconstruction (#1960)

## Accomplished

- Claimed issue #1960 after all human-oversight directives were unclaimable due to `replan`.
- Added private `rat_divMod_spec_core` in `HexPolyZ/Basic.lean`.
- Proved the nonzero-divisor branch from `DensePoly.divModArray_reconstruction` with Rat leading-coefficient cancellation.
- Handled division by the zero polynomial via `divMod_remainder_eq_self_of_size_zero_core` and zero-polynomial algebra.
- Filled `ratDivModLaws.divMod_spec` from `rat_divMod_spec_core`, removing the targeted `sorry`.
- Verified `lake build HexPolyZ.Basic`, `lake build HexPolyZ`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

`ratDivModLaws.divMod_spec` is now proved. The remaining Rat division/modulo fields in `ratDivModLaws` are still placeholders, and #1947 can use this reconstruction result for canonical remainder congruence.

## Next step

- Proceed with #1947: prove Rat canonical remainder congruence and fill `ratDivModLaws.mod_eq_mod_of_congr`.

## Blockers

None for this slice.
