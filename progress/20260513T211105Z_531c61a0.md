## Accomplished

- Claimed #3822 and completed the derivative-active square-free contribution pow obligation in `HexPolyFp/SquareFree.lean`.
- Moved the private derivative-active contribution correctness block below the helper cluster it consumes.
- Added the recursive IH parameter to `squareFreeAuxRevContribution_derivative_active_pow_obligation` and used the level-form Yun product invariant, residual unpacking, `pthRoot` tail validity, and `pthRoot_pow_mul_prime_of_derivative_zero` to close the nontrivial residual branch.
- Strengthened the local square-free weighted-product state payload to include reachability for the Yun residual component needed by the tail-validity helper.
- Verified:
  `lake build HexPolyFp.SquareFree`,
  `lake build HexPolyFp`,
  `python3 scripts/check_dag.py`,
  `git diff --check`, and
  `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`.

## Current frontier

The tracked #3782/#3822 `squareFreeAuxRevContribution_derivative_active_pow_obligation` hole is closed, and `HexPolyFp/SquareFree.lean` has no `sorry` or banned markers.

## Next step

Continue with downstream consumers of `squareFree_weightedProduct`; any caller supplying the derivative-active state payload now needs to provide reachability for both the current component and residual component.

## Blockers

None.
