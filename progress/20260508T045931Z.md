## Accomplished

- Claimed issue #2642.
- Added `GramSchmidt.Int.scaledCoeffMatrix_rowAdd_pivot_det` in
  `HexGramSchmidt/Int.lean`.
- The theorem proves the determinant-level row-add pivot bridge for the
  Cramer matrix computing the scaled coefficient: `rowAdd b j k c` changes
  the pivot Cramer determinant by adding `c` times the leading Gram
  determinant.
- Verified `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt.Update`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

- The requested determinant bridge exists and builds.
- `HexGramSchmidt/Update.lean` still has its pre-existing `sorry` for
  `scaledCoeffs_sizeReduce_pivot`; this PR does not wire the bridge into that
  theorem.

## Next step

- A follow-up can use `scaledCoeffMatrix_rowAdd_pivot_det` plus the existing
  Cramer/Bareiss-to-`scaledCoeffs` bridges to discharge
  `scaledCoeffs_sizeReduce_pivot`.

## Blockers

- None for this issue's determinant bridge.
