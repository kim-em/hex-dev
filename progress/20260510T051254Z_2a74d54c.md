# 20260510T051254Z - Issue #2943

## Accomplished

Started by reading the latest progress note and inspecting the available
queue. `coordination orient` exposed blocked human-oversight directives and
no ordinary unclaimed work; after `coordination check-blocked`, #2928 became
claimable.

Claimed #2928, checked the dependency from #2927, and found the issue was too
large as written: it needed both a missing finite-field polynomial cancellation
helper and the derivative-split common-divisor assembly. Decomposed #2928 into
#2943 and #2944, added the required `Decomposed into #2943, #2944` breadcrumb,
and skipped #2928 for planner triage.

Claimed #2943 and added
`dvd_one_of_mul_right_dvd_right` in `HexPolyFp/SquareFree.lean`. The helper
proves that if nonzero `g` satisfies `d * g ∣ g`, then `d ∣ 1`, by comparing
polynomial product degrees to force `d` to be a nonzero constant and then
reusing `dvd_scale_self_of_ne_zero`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean HexPolyFp/Basic.lean`

The final grep still reports the pre-existing `sorry`s in
`HexPolyFp/SquareFree.lean`; no new banned marker or proof hole was added.

## Current frontier

#2943 supplies the cancellation step needed by #2944. The next assembly issue
can use it with `yunStep_common_dvd_derivative_current` to show that a common
divisor in the derivative-active base split divides `1`.

The worktree also contains a pre-existing modification to `.claude/CLAUDE.md`
that this session did not touch.

## Next step

Create the PR for #2943, then let #2944 unblock after #2943 lands.

## Blockers

None for #2943.
