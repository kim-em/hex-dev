# Work session: HexConway odd-prime degree-6 Tier 1 benchmarks

## Accomplished

- Claimed #2216 and extended `HexConway/Bench.lean` with fixed Tier 1 irreducibility benchmark inputs and registrations for committed Luebeck entries `C(3, 6)`, `C(5, 6)`, and `C(11, 6)`.
- Added local `FpPoly` definitions, monic proofs, and source-table `#guard`s tying the new benchmark inputs to `Conway.luebeckConwayPolynomial?`.
- Updated the benchmark module docstring and fixed-registration rationale to list the added entries as Tier 1 irreducibility coverage only.
- Verified:
  - `lake build HexConway.Bench`
  - `lake exe hexconway_bench list`
  - `lake exe hexconway_bench verify`
  - `lake exe hexconway_bench run Hex.ConwayBench.runTier1Irreducibility_3_6Checksum`
  - `lake exe hexconway_bench run Hex.ConwayBench.runTier1Irreducibility_5_6Checksum`
  - `lake exe hexconway_bench run Hex.ConwayBench.runTier1Irreducibility_11_6Checksum`
  - `python3 scripts/check_phase4.py`
  - `python3 scripts/status.py HexConway`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - targeted scan of `HexConway/Bench.lean` for forbidden placeholders

## Current frontier

`HexConway` remains at Phase 4 readiness with broader fixed Tier 1 irreducibility benchmark coverage for the committed degree-6 odd-prime entries.

## Next step

Continue Phase 4 coverage for remaining advertised `HexConway` surfaces if further benchmark issues are dispatched; do not fold Tier 2 compatibility or Tier 3 search into this Tier 1 slice.

## Blockers

- No blocker for #2216.
- Existing upstream `sorry` warnings remain in imported modules; this session did not add new `sorry`s.
