# 20260602T161957Z 859c3882 — PR #6364 repair

## Accomplished

- Claimed repair for PR #6364 (`feat: internalize cap-positivity in HO-4 capstones`), which was conflict-marked against `main`.
- Merged `origin/main` into the PR branch and resolved the conflict in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Preserved the PR's cap-positive wrappers while keeping the newer bridge-data and internal-prime-lower-bound wrappers from `main`.
- Updated the cleanly merged canonical-indicator cap-positive wrapper to use the current
  `FactorFastCapLiftAnalyticComparison` bridge API.

## Current frontier

PR #6364 is locally repaired and verified. The branch still needs the merge
commit pushed and the PR marked salvaged.

## Next step

Stage the resolved merge and this progress note, commit, push with
`--force-with-lease`, and run `coordination mark-pr-salvaged 6364`.

## Blockers

- None for this repair.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
