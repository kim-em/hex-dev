# 20260519T011620Z — HexGfqField inverse repr wrapper

## Accomplished

Claimed issue #5193 after checking the unclaimed queue. Skipped #5180 for this
session because its issue body says it remains blocked on #5184, whose PR is
still open.

Added `GFqField.repr_inv_of_ne_zero` in `HexGfqField/Operations.lean`, adjacent
to the existing `repr_*` simp wrappers. The theorem composes the existing
`toQuotient_inv_of_ne_zero` wrapper with the `repr` definition and carries the
same nonzero hypothesis shape as the quotient theorem.

Verification completed:

- `lake build HexGfqField.Operations`
- `lake build HexGfqField`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexGfqField/Operations.lean`

The Lean builds replayed one pre-existing linter warning at
`HexGfqField/Operations.lean:686` about using `simp` instead of `simpa`; this
session did not introduce it.

## Current frontier

`HexGfqField` now has the requested conditional inverse representative simp
wrapper. The remaining `repr_div` and `repr_zpow` polish remains out of scope
until matching `toQuotient_div` and `toQuotient_zpow` wrappers exist.

## Next step

Open the PR for #5193. A follow-up planner issue can add the quotient-level
division and integer-power wrappers before adding the corresponding `repr_*`
surface.

## Blockers

None for #5193.
