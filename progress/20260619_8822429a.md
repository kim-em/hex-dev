# HexGF2 Field.lean GF2nPoly Phase 6 grind coverage (#8040)

## Accomplished

Promoted the 16 equation-shaped public `@[simp]` lemmas in the
`GF2nPoly` section of `HexGF2/Field.lean` to `@[simp, grind =]`,
continuing the Phase 6 grind-automation sweep:

`.val`-projection equations: `mul_val`, `one_val`, `add_val`,
`neg_val`, `sub_val`, `zero_val`.

Inversion / identity / absorbing equations: `inv_zero`, `zero_add`,
`add_zero`, `add_self`, `mul_zero`, `zero_mul`, `one_mul`, `mul_one`.

Reduce equations: `reducePoly_zero`, `reducePoly_monomial_zero`.

All 16 re-read at the point of promotion and confirmed literal
`lhs = rhs` forms. No exclusions: every issue-listed candidate was a
clean equation. The `GF2n` section, the `frobeniusIter` cluster, and
all `↔`/`<`/membership lemmas were left untouched per the deliverables.

## Current frontier

`lake build HexGF2` green — `HexGF2.Field` elaborates in 1.7s with no
grind-loop slowdown (the `.val` projections push through the
constructor and the identity lemmas collapse a binary op to one side,
so all rules strictly reduce structure and terminate). Downstream
consumers all green: `HexGF2.EmitFixtures`, and cross-library
`HexGFq` (incl. `HexGFq.CrossCheck`) and `HexGF2Bench`.
`scripts/check_dag.py` exit 0. Annotation-only: no proof bodies
changed, no new imports, no `*Mathlib` file touched, no `axiom` /
`sorry` / `native_decide`.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps at session
start: #8029 (HexGF2/Euclid.lean), #8035 (HexArith/Nat/Prime.lean).

## Blockers

None.
