# Accomplished

- Proved a quantitative root-product converse that turns a uniformly small
  remote-root tail into the actual executable `Hex.nkWitness` inequalities.
- Proved the implemented `Hex.separationDepth` makes that tail small for a
  separable polynomial whenever the designated root lies in the central half
  of the square.
- Connected the resulting witness to both possible successful branches of the
  actual `.nk` component certifier, including a corollary that the returned
  certificate still covers the designated root.
- Obtained an Opus second opinion with no blockers; clarified the remaining
  leaf-to-enclosing-depth gap in the SPEC in response to its review.
- Passed focused and umbrella builds, the copyright/DAG/file-size/phase checks,
  and a full `lake build` without new warnings.

# Current frontier

The fixed-depth NK certification bridge is complete. Its component-level
theorem honestly assumes the doubled enclosing square itself has reached
`separationDepth`.

# Next step

Address any actionable Opus review findings, publish and merge the PR for
#8832, then return to the corrected component-width/gluing invariant needed to
transfer leaf depth to enclosing-square depth and assemble driver completeness.

# Blockers

The original item-30 directive cannot yet supply enclosing-square depth from
leaf depth: the proof still needs glue maximality/coverage and a quantitative
bound on survivor-component extent. This is documented on #8830 and #8755;
the present fixed-depth theorem does not assume that missing result.
