# PNT+ Table 10 row-25 coordinate shard

## Accomplished

- Audited pinned PNT+ revision `21998bb6196b56789f72a52656a781a75e134eb0`
  and identified the exact row-25 tuple, five `table_10_row25_k*_margin`
  declarations, and source comment proving the historical bare `k = 5`
  endpoint false.
- Added one bounded Mathlib-free five-coordinate row package authenticating
  source rows, coefficients, exponential windows, listed cells, corrected
  targets, and coordinate-specific failure diagnostics.
- Proved the shared endpoint majorants and row convexity in the ordinary
  kernel, using one `k = 1` proof and one parameterized `k = m + 2` proof for
  columns 2 through 5.
- Added generic package/schema/chronology/frontend replay for all five cells,
  a source-shaped row-25/k=5 theorem, a decoded coordinate-205 false mutation,
  wrong-column rejection, and guarded axiom reports.
- Recorded exact partial-shard provenance while leaving the Table 10 family
  pending; updated both SPECs, registrations, and the exact proof-only Lake
  freshness exemption.
- Passed the focused runtime/proof/conformance build, retained Table 12/FKS2
  relevant builds, PNT inventory check and exact-source verification, 26
  inventory unit tests, static/DAG/Phase-4/trust/freshness/Mathlib-free gates,
  banned-token scan, and `git diff --check`.

## Current frontier

- The coordinate-pinned row-25 shard is complete locally. It covers five
  numeric majorant premises after a localized PNT+ rewrite, not the surrounding
  `B_8_exact` reductions or the rest of Table 10.
- A broader experiment-library build attempted to populate an incomplete
  shared Mathlib cache and exhausted disk on unrelated Mathlib modules. The
  bounded relevant target set was rerun successfully after removing only those
  newly generated, regenerable cache artifacts.

## Next step

- Preserve this local commit until the higher-priority BKLNW ladder and
  exponential-interface migration lands, then rebase the shard onto current
  main and run the normal publication review/CI cycle if selected.

## Blockers

- None in the Table 10 implementation. The shared workspace cannot currently
  sustain a from-source all-Mathlib rebuild, but all required focused and
  relevant PNT targets are green.
