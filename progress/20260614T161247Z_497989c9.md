# 20260614T161247Z_497989c9 — doc session (issue #7266)

## Accomplished

Phase 6 docstring batch for `HexMatrix/Determinant.lean`: added
one-sentence backtick-led `/-- … -/` docstrings to all 13 listed
undocumented private declarations in the `finTranspose` involution and
`transposePermutationValues` cluster (lines ~2534–2643):
`finTranspose` (def), `finTranspose_left`, `finTranspose_right`,
`finTranspose_of_ne`, `finTranspose_involutive`,
`finTranspose_injective`, `finTranspose_ne_iff`,
`finRange_map_finTranspose_perm`; `transposePermutationValues` (def),
`rowSwap_get`, `rowSwap_get_finTranspose`,
`transposePermutationValues_get`,
`transposePermutationValues_insertAt_last_boundary`.

None of the listed declarations already carried a docstring; nothing
skipped.

## Current frontier

Doc-only diff, 22 insertions / 0 deletions, no signature/proof/`simp`
changes. `lake build HexMatrix.Determinant` green, `scripts/check_dag.py`
exits 0, `git diff --check` clean, no banned tokens or em-dashes on
added lines.

## Next step

Later batches for `HexMatrix/Determinant.lean`: the
`transposePermutationValues_toList_*`/`_take`/`_middle`/`_drop`
decomposition lemmas (~2740+), the `swapPermutationValues` cluster
(~2997+), and the `inversePermutationValues` cluster (~3063+).

## Blockers

None.
