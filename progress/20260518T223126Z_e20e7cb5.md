## Accomplished

- Claimed feature issue #5132 for HexGF2 Phase 6 coefficient-addition simp wrappers.
- Added public docstringed `@[simp]` coefficient lemmas in `HexGF2/Basic.lean`:
  `coeff_add_bne`, `coeff_add_self_false`, and `coeff_add_zero_left_bool`.
- Rewired local coefficient-addition proof sites in `HexGF2/Basic.lean` to use
  `coeff_add_bne` instead of rewriting the internal `coeff_add_eq_bne` theorem
  directly.

## Current frontier

The requested GF2 coefficient-addition API polish is complete. Verification run:

- `lake build HexGF2.Basic`
- `lake build HexGF2`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git grep -n "coeff_add_bne\\|coeff_add_self_false\\|coeff_add_zero_left_bool" HexGF2/Basic.lean`
- `git diff -- HexGF2/Basic.lean | rg -n "sorry|axiom|native_decide|TODO|FIXME"` found no matches.

Second opinion found no correctness bug. It noted that
`coeff_add_self_false` and `coeff_add_zero_left_bool` are mostly named public
convenience lemmas because existing structural simp lemmas can already reduce
those forms, but the issue explicitly requested those named simp wrappers.

`reflect` was not available on `PATH`.

## Next step

Publish the PR closing #5132.

## Blockers

None.
