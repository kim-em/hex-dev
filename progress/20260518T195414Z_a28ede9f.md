# 2026-05-18 19:54 UTC — forty-fourth Phase 4/5 checkpoint (a28ede9f)

Claimed summarize issue #5078 and recorded this checkpoint covering
the merged-PR wave since #5018 closed at `2026-05-18T15:01:29Z`.
Window upper bound is the issue's creation timestamp
`2026-05-18T19:45:51Z`, plus a single post-creation PR per the
issue body's verification note.

```
gh pr list --state merged --limit 40 \
    --json mergedAt,number,title \
    --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
  | awk '$1 > "2026-05-18T15:01:29Z"' \
  | awk '$1 <= "2026-05-18T19:45:51Z"' \
  | wc -l
```

= **27 new PRs merged** in the issue's nominal window, matching
the headline estimate exactly. One additional PR — #5076 — merged
at `2026-05-18T19:48:01Z`, ~2 min after issue creation; it is the
PASS audit on the `zpoly_dvd_trans` closure that this summary
calls out, so it is included below for completeness (28 PRs total).

## `coordination orient` snapshot

```
=== DIRECTIVES (highest priority) ===
#2637 HexBerlekampZassenhaus: Factorization record API + sign/multiplicity bugs
#2567 HO-4: Formalise BHKS Theorem 5.2 (factorFast termination, leaf theorem)
#2564 HO-1: BZ recombination — rollback and rewrite to van Hoeij CLD

=== Unclaimed work items ===
#4334 HexLLL Phase 4: investigate inconclusive scientific benchmark verdicts

=== Claimed work items ===
#5078 Summarize forty-fourth Phase 4/5 checkpoint           (this session)
#5077 BZMathlib: extract fpPoly_dvd_trans helper            (next sibling
                                                             cluster)

=== Issues with open PRs ===
#4902 BZMathlib: abstract-bound siblings of
      exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate
      (monic + primitive unscaled)                          (PR #4917, ~17 h)

=== Blocked issues (full chain) ===
#4951 [#4929] liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core
              (unscaled primitive)
#4934 [#4931] recombinationSearchModAux_some_and_covers_* (monic)
#4931 [#4927] liftedFactorSubsetPartition_prefix_none (monic)
#4929 [#4925] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_*
#4927 [#4924] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd
#4925 [#4919] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_*
#4924 [#4918] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd
#4919 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset_*
#4918 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset (monic)
#4880 [#2564] BZ Mathlib-free: drop hcore_monic Gap 1 from umbrella
#4832 [#4821, #4830, #4831] BZMathlib: lifted partition recombination support
#4831 [#4821, #4830] BZMathlib: lifted partition disjointness + assoc uniq
#4830 [#4821, #4821] BZMathlib: initial lifted partition cover field
#4825 [#4680, #2567] BZ Mathlib-free: ZPoly.Irreducible per factor.factors entry
#4821 [#4680] BZMathlib: discharge executable Hensel subset analytic obligation
#4819 [#4821] BZMathlib: prove factor_irreducible_of_nonUnit
#4818 [#4825] BZ: prove executable ZPoly isIrreducible_iff
#4680 [#2567] HO-1 substrate: compose ModP and Hensel lift constructors
#4172 [#4170] HO-1 residual: executable irreducibility checker theorem
#4170 [#2567] HO-1 residual: executable factor irreducibility bridge

=== Open pull requests ===
#4917 [agent/8a4738a1] exists_representingSubset_of_mem_normalizedFactors_*
                       (repair-claimed) [CONFLICTS]
#2656 [spec/real-roots-and-sturm] spec: hex-real-roots and hex-sturm (planned)

=== PRs needing attention ===
#4917 (still in CONFLICTS, repair-claimed)
```

Compared to the previous summarize window: the unclaimed queue
collapsed almost entirely. Only #4334 (host-load-gated HexLLL
Phase-4 scientific re-run) remains — the cascade kept worker
throughput in step with planner output. #5077 entered the queue
on the same planner cycle that filed this summary (drafted in
the #5069 audit comment's out-of-scope observation) and is
already claimed.

`libraries.yml` is unchanged this window:
`HexBerlekampZassenhausMathlib` remains at `done_through: 2` —
the Mathlib-bridge layer's Phase-3 surface. No SPEC files were
touched; this summary is journal-style commentary on the merge
wave, not a roadmap update.

## Headline — `zpoly_dvd_trans` cluster closure

The window's headline arc is the closure of the
`Hex.ZPoly`-level divisor-transitivity cluster in
`HexBerlekampZassenhausMathlib/Basic.lean`. The arc spans four
PRs and a single audit DEVIATION/follow-up cycle:

1. **#5036** — extract `private theorem zpoly_dvd_trans` at
   `Basic.lean:2069` (current line after #5066's hoist) and
   rewire the first three inline destructure sites.
2. **#5045** — extend to the six remaining inline sites flagged
   by issue #5044 (the planner queue for this rewire).
3. **#5062** posts the audit comment on #5048 (post-#5045 audit).
   Invariant 1 verdict: **DEVIATION** — 26 inline `rcases` /
   `refine` / `rw` transitivity sites missed by the original
   queue, plus 3 calc-form sites that could not be rewired below
   the helper's then-position. Invariants 2 and 3: PASS.
4. **#5066** — hoists `zpoly_dvd_trans` to its minimal exposure
   point above the first calc-form caller
   (`henselSubsetCorrespondenceRest_transport_of_disjoint` at
   `Basic.lean:2088`) and rewires the three calc-form sites.
5. **#5070** — rewires the 26 rcases-form sites.
6. **#5076** (post-window) posts the closure audit on #5069.
   All three invariants verbatim PASS: 1 def + 38 use sites in
   `Basic.lean` (`3 (#5036) + 6 (#5045) + 3 (#5066) + 26 (#5070)
   = 38` exactly), docstring intact, zero `IntReductionMod.lean`
   or `Recovery.lean` cross-file occurrences.

This is the audit pattern's **first end-to-end success**: file →
audit DEVIATION → file follow-ups → re-audit PASS. The
three-invariant template introduced by #5031 demonstrably
caught a 29-site coverage gap and routed it through to the
planner queue before the cluster was declared closed.

The Monic-route cluster hoists (#5042, #5049) expose
`zpoly_size_pos_of_monic` to the
`monic_primitive_sign_normalized_of_monic` neighbourhood for
the first time, opening that area for further consolidation
downstream. The forward + reverse `isZero = false` plumbing
cleanup wrapped up across both `Basic.lean` and
`IntReductionMod.lean` during this window.

## Per-sub-thread bullets

### Sub-thread 1 — `zpoly_dvd_trans` cluster closure (4 PRs)

* **#5036** — extract `zpoly_dvd_trans` + rewire 3 inline
  divisor-transitivity sites (`Basic.lean:7945 / 8018 / 8229`
  in current numbering).
* **#5045** — rewire 6 remaining inline sites (`Basic.lean:8308
  / 9421 / 9589 / 9703 / 9771 / 9871` in current numbering;
  closes #5044).
* **#5066** — hoist `zpoly_dvd_trans` above
  `henselSubsetCorrespondenceRest_transport_of_disjoint` and
  rewire 3 calc-form sites (`Basic.lean:2104 / 2114 / 2277`).
* **#5070** — rewire 26 inline `rcases` / `refine` / `rw` sites
  in `Basic.lean` between lines 9948 and 13733.

Audit pair **#5062 / #5076** (closing #5048 / #5069
respectively) bound this arc:

* #5062 (closing #5048) — post-#5045 audit. Verdict:
  `Invariant 1: DEVIATION (26 inline + 3 calc sites missed)`,
  `Invariant 2: PASS`, `Invariant 3: PASS`. The DEVIATION list
  routed to follow-up issues #5064 / #5065, executed as #5070 /
  #5066.
* #5076 (closing #5069) — post-#5064/#5066 closure audit.
  Verdict: `PASS x3`. 39 occurrences in `Basic.lean` (1 def +
  38 uses) exactly matching the projected count. Zero
  cross-file occurrences in `IntReductionMod.lean` or
  `Recovery.lean`.

### Sub-thread 2 — `isZero = false` plumbing cleanup (5 PRs)

Both directions of the `Hex.DensePoly.isZero = false` plumbing
boilerplate cleared this window.

**Forward direction** (`(isZero_eq_false_iff _).mp`):

* **#5059** — 6 inline `isZero` / `size` plumbing sites.
* **#5060** — 6 more inline sites.

**Reverse direction** (`(isZero_eq_false_iff _).mpr ∘
size_pos_of_ne_zero _`, or `Bool.eq_false_iff.mpr`):

* **#5071** — `IntReductionMod.lean:1445` reverse-direction
  block.
* **#5074** — 4 inline `cases hz : <poly>.isZero with` blocks in
  `Basic.lean`.
* **#5075** — `IntReductionMod.lean:1500` `by_cases hderiv`
  block.

After #5075, the only `isZero = false := by` shape outstanding
in the reverse direction was the `hderiv_false` derivative-case
site at `IntReductionMod.lean:1492-1497` — which #5075 itself
closes. Both files are now clean of inline `isZero` /
`size_pos` boilerplate at the BZMathlib layer.

### Sub-thread 3 — Monic-route cluster consolidation (8 PRs + 2 audits)

Continued extension of the Monic-route projection family
clustered at `Basic.lean:6707-6749`.

**Helper extraction / sibling additions:**

* **#5026** — extract `zpoly_normalize_factor_sign_of_monic` +
  rewire 2 inline destructure sites.
* **#5058** — extract `zpoly_size_pos_of_pos_lc` (sibling of
  `_of_monic`) + rewire 2 inline pos_lc-source size_pos sites
  at `Basic.lean:8214 / 8425`.

**Hoists exposing existing helpers to new neighbourhoods:**

* **#5042** — hoist `zpoly_size_pos_of_monic` above
  `monicModularImage_modP_eq_of_monic`, collapsing a 9-line
  `by_contra` proof at `Basic.lean:5338`.
* **#5049** — hoist `zpoly_size_pos_of_monic` above
  `monic_primitive_sign_normalized_of_monic`, collapsing a
  13-line `hcs_pos` inline proof at `Basic.lean:4479`.

**Argument-form harmonisation:**

* **#5025** — harmonise 7 `zpoly_ne_zero_of_monic`
  `IntReductionMod` call sites to the post-#5001 implicit-`f`
  form.
* **#5028** — harmonise 2 `zpoly_ne_zero_of_pos_lc`
  `IntReductionMod` call sites to the post-#5028 idiom.

**Downstream rewires using existing cluster helpers:**

* **#5022** — rewire inline `.2.1` arg-position projection at
  `Basic.lean:5597` to `zpoly_primitive_of_monic` (atomic
  readability gain filed as #5017 by #5013's out-of-scope
  follow-up).
* **#5029** — rewire 3 inline triple-destructure sites at
  `Basic.lean:10083 / 10193 / 13595` onto Monic-route cluster
  helpers.

**Feasibility audits and post-extraction audits:**

* **#5032** (closing #5031) — post-#4991 helper-extraction
  cascade audit. Invariants 1-3 PASS.
* **#5043** (closing #5038) — feasibility study for hoisting
  Monic-route helpers above
  `monic_primitive_sign_normalized_of_monic`. Result: feasible;
  executed as #5049.
* **#5054** (closing #5050) — feasibility study for
  `monic_primitive_sign_normalized_of_monic` cluster inversion.
  Result: PASS feasibility verdict; awaits planner action.
* **#5063** (closing #5052) — post-#5058 audit of
  `zpoly_size_pos_of_pos_lc`. Invariants 1-3 PASS.

The Monic-route cluster's stable footprint at window end:
4 `_of_monic` siblings (`size_pos` / `ne_zero` / `lc_pos` /
`primitive`) + 2 `_of_pos_lc` siblings (`ne_zero` / `size_pos`)
+ 1 `zpoly_normalize_factor_sign_of_monic` + the helper
bundle's `monic_primitive_sign_normalized_of_monic` rewire
surface.

## Other window activity

**Sibling helper extraction outside the three sub-threads:**

* **#5037** — extract
  `normalizeForFactor_squareFreeCore_mul_repeatedPart_primitive_of_ne_zero`
  helper + rewire 3 `IntReductionMod` sites. Sibling of the
  pre-window
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`
  cluster.

**Documentation / warning suppression:**

* **#5020** — suppress 5 unused `hcore_ne` warnings by
  underscore-prefixing at `_of_bound` `Basic.lean` signatures.
* **#5033** — add docstrings to 3 undocumented Monic-route
  cluster helpers (`zpoly_size_pos_of_monic`,
  `zpoly_lc_pos_of_monic`, `zpoly_primitive_of_monic`).
* **#5051** — reframe
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`
  docstring and drop a stale `(#4611)` PR-metadata leak in
  helper-3's `IntReductionMod` docstring.

**Progress entry housekeeping:**

* **#5061** — progress entry chore for #5053 (read-only audit
  session log of `zpoly_size_pos_of_monic`).

## Audit pattern observation

The three-invariant template introduced by #5031 (call-site
coverage / framing / cross-file consistency) is now the
project's standard audit shape. Of the 6 audit issues that
closed in this window (#5032 / #5043 / #5054 / #5062 / #5063 /
#5069), **5 PASS verbatim and 1 (#5048 → closed by #5062)
reports DEVIATION on Invariant 1**. The DEVIATION drove two
file follow-ups (#5070, #5066) that flipped the cluster to PASS
on re-audit (#5069 → closed by #5076 in the post-window minute).

The audit pattern's value proposition in this window:

* **Catches coverage gaps the original rewire queue missed.**
  The #5048 audit surfaced 26 + 3 = 29 missed sites — 76% of the
  final 38-site cluster — that the original #5036 + #5044
  queues did not visit.
* **Routes follow-up work mechanically.** DEVIATION verdicts
  generate planner-queue work without requiring a human-readable
  bridging step.
* **Pre-empts cluster-extension downstream by surfacing sibling
  clusters.** The #5069 audit's out-of-scope observation
  identified an `Hex.FpPoly p`-level divisor-transitivity
  cluster (#5077) that the ZPoly-level helper cannot cover
  because `Hex.FpPoly p` uses a different bespoke `Dvd`
  instance (`instDvdOfAddOfMul`).

## In-flight at issue creation time

* **PR #4917** (closing #4902) is still `repair-claimed` in
  `CONFLICTS` state. Blocks the monic + primitive_pos_lc_core
  `_of_bound` substrate siblings (#4918, #4919, #4924, #4925,
  #4927, #4929, #4931, #4934, #4951). Re-derive the dependent
  chain at execution time with `coordination orient` /
  `gh issue list --label blocked`.
* **#5069 audit PASS comment posted; closed by #5076** in the
  ~2 minutes between this issue's creation and the worker
  picking it up. The audit's out-of-scope observation drafted
  the `fpPoly_dvd_trans` analogue cluster, picked up as
  **#5077** in this planner cycle.

## HO-1 critical-path commentary

The BZ Mathlib-free chain (capstone #4170, sub-capstone #4825)
remains blocked on #4680 (analytic obligation composition) and
#2567 (directive). This window's helper-extraction /
plumbing-cleanup contributes infrastructure consolidation in
`HexBerlekampZassenhausMathlib/Basic.lean` (Mathlib-bridge
layer) — it does not change the directive-blocked status of
#2564 / #2567 / #2637 nor unblock anything on the executable
side.

The cascade continues to provide useful planner-queue work
without competing with directive-level threads. From the
critical-path perspective, this is "filler" work — necessary
but not sufficient infrastructure cleanup. The remaining
directive-level threads cannot be advanced by Mathlib-bridge
consolidation alone.

## #4880 umbrella drop posture

Helpers 1 (#4945) and 5 (#4953) substrate remain closed;
helpers 2 / 3 / 4 remain architecturally infeasible per the
#4940 audit. No window activity altered this posture. The
Monic-route cluster's continued extension sits at the
Mathlib-bridge layer, not at the umbrella's Gap-1 / Gap-3
surface.

## Honest framing — limitations and gaps

This summary documents progress; honest framing requires
calling out what the progress is and isn't:

* **Mathlib-bridge layer only.** Every PR in this window
  touches `HexBerlekampZassenhausMathlib/Basic.lean` or
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`. The
  Mathlib-free executable layer (under `HexBerlekampZassenhaus/`)
  saw zero PRs. This is appropriate for a planner cycle that
  is `blocked: [#2567]` on most critical-path issues, but it is
  not progress toward the BZ Mathlib-free chain's exit
  criteria.
* **Roadmap state unchanged.**
  `HexBerlekampZassenhausMathlib.done_through` remains at `2`.
  No other library advanced phase counters this window.
  Phase-4 / Phase-5 surfaces for the other libraries are
  unaffected.
* **Cascade is self-extending.** The pattern of "extract
  helper → rewire sites → audit → identify sibling cluster"
  generates its own follow-up work. This is fine for keeping
  workers fed during a directive-blocked period but should not
  be confused with progress toward the public capstone
  surfaces.
* **The `zpoly_dvd_trans` closure does not unblock anything.**
  The closed cluster is internal cleanup in a single file;
  no public surface or downstream theorem statement changed.
  The 38 rewired sites are smaller / more readable individually
  but compose identically to the pre-window inline forms.
* **PR #4917 has been blocking 9 abstract-bound siblings for
  ~17 hours.** This is the longest-standing blocker on the
  planner queue this window. The repair-claimed status means
  it is owned by a repair agent, not a fresh worker session.

## Verification

* PR list matches the issue body's expected count: **27 PRs in
  the nominal window** (issue creation timestamp upper bound),
  plus **#5076** post-window per the verification note.
* `zpoly_dvd_trans` cluster closure arc identified as headline,
  with the full audit DEVIATION → re-audit PASS cycle
  documented (#5048 → #5070 + #5066 → #5069 → #5076).
* Five PASS audits referenced (#5032, #5043, #5054, #5063,
  #5069) plus the one DEVIATION/follow-up cycle (#5048).
* No source-tree `.lean` files edited. No `SPEC/`, `PLAN/`,
  `libraries.yml`, or `PLAN.md` changes. No `.claude/CLAUDE.md`
  edits.
* `fpPoly_dvd_trans` follow-up referenced by its issue number
  (#5077); no further speculation about future work.

## Accomplished

* Read issue #5078, the previous summarize entry
  (`progress/20260518T145024Z_444df9fe.md`), the project
  specification (`SPEC/SPEC.md`, `PLAN.md`, `libraries.yml`),
  and the previous session's audit progress entry
  (`progress/20260518T193859Z_eeafaae8.md`).
* Verified the merge-window PR count against the issue body's
  headline estimate via the gh query template; collected the
  one post-creation PR (#5076).
* Cross-checked audit verdicts for #5032 / #5043 / #5054 /
  #5048 / #5062 / #5063 / #5069. The #5048 audit's DEVIATION
  verdict and the subsequent #5069 PASS re-audit are the arc's
  closure points.
* Wrote this summary as the session's progress entry.

## Current frontier

* `zpoly_dvd_trans` (ZPoly-level) closed; `fpPoly_dvd_trans`
  (FpPoly-level) is #5077 in the planner queue (claimed at
  summary time).
* Monic-route cluster's `monic_primitive_sign_normalized_of_monic`
  body opened for further consolidation by the #5049 hoist.
* Forward + reverse `isZero = false` plumbing fully cleared at
  the BZMathlib layer.

## Next step

Open the PR closing #5078 with this progress entry. No
follow-up issues filed by this session — the `fpPoly_dvd_trans`
sibling cluster (#5077) was filed by the planner cycle that
created #5078.

## Blockers

None for this session. Project-level blockers (directives
#2564 / #2567 / #2637) are unchanged.
