# Session 25d686a2 — feature work on #5204

## Accomplished

Triaged and **skipped** issue #5204 (BHKS D1 step 2:
`auxiliaryPolynomial_l2norm_sq_le`). Posted a detailed comment on the
issue and routed it back to `replan` via `coordination skip`.

The skip is in line with the issue body's own scope guidance ("If the
worker discovers that an internal step (e.g. the columnwise
`|cldCoeffs| ≤ bhksCoeffBound` bound) demands a substantial preliminary
file outside `BadVector.lean`, stop and use `coordination skip`…").

## Current frontier

The three issue deliverables compose around a coefficient-wise bound
`|cldCoeffs[i][j]| ≤ bhksCoeffBound input j`
(`BHKS.abs_cldCoeffs_le_bhksCoeffBound` in deliverable 3). The bound is
the BHKS Lemma 5.1 statement that the exact-rational CLD quotient
`Φ(g) = f · g'/g` has integer coefficients bounded by
`C(n−1, j) · n · ‖f‖₂`, under the true-factor hypothesis `g | f`. The
structural `psiCut` bound is `p^{k−b}/2 + 1/2`, unbounded in `liftData.k`,
so the desired bound is not provable from arithmetic on `psiCut` alone.

`SPEC/Libraries/hex-berlekamp-zassenhaus.md` line 396 spells out the
proof pathway (Mahler measure, Landau, binomial coefficient bound) and
notes "Read BHKS Lemma 5.1's proof in §5 before attempting." That is
clearly a separate file rather than a tight in-`BadVector.lean` lemma.

## Next step

A planner should decompose into:

1. `BHKS.abs_cldCoeffs_le_bhksCoeffBound` — its own ~200-line module
   building on `HexPolyZMathlib/Mignotte.lean`. Needs the true-factor
   hypothesis (`liftedFactors.getD i 0 | f` mod `p^k`) surfaced into
   the lemma's signature; the issue's universally quantified form is
   false otherwise.
2. The reduced `auxiliaryPolynomial_l2norm_sq_le` — Cauchy–Schwarz via
   `Finset.sum_mul_sq_le_sq_mul_sq` over deliverable (1). ~100 lines in
   `BadVector.lean`.

`reports/bhks-bad-vector-theorem-shape.md §3 step 2` should also be
updated: its claim that the proof goes through "using the existing
`bhksCoeffBound` columnwise bound" assumes a column bound that does not
yet exist in the codebase.

## Blockers

None for this session — the skip routes cleanly. Downstream, the BHKS
Theorem 5.2 cap-separation chain remains blocked on the missing
`abs_cldCoeffs_le_bhksCoeffBound` lemma.
