# 2026-05-11 01:31 UTC — work session (23940a36)

## Accomplished

- Triaged the two unclaimed `feature` issues:
  - **#3153** (scaled square-free weighted product reconstruction):
    skipped — its declared dependencies #3196 and #3197 are
    technically closed, but both closed as
    *partial-progress superseded* with the actual unconditional
    invariants tracked in still-open issues #3208 (termination
    + unconditional scaled Yun product invariant) and #3209
    (composition with unscaled witness, depending on #3200/#3199).
    Bridge PR #3204 also still open. The "routine combination"
    described in the issue body is not yet possible.
  - **#3122** (pthRoot tail cross-coprimality): too large for one
    session as stated. Decomposed into:
    - **#3217** — add the recursive residual derivative-zero
      predicate plus the structural divisibility helper
      "every output factor of `squareFreeAuxRev g m fuel []`
      divides `g`".
    - **#3218** — restate the tail-cross theorem with the new
      hypotheses, prove it, and cascade the residual provider
      through the consumer chain.
- Claimed and landed **#3217** in `HexPolyFp/SquareFree.lean`:
  - Added `squareFreeAuxRevResidualSatisfied : FpPoly p → Nat → Nat → Prop`
    capturing the residual derivative-zero invariant at every
    recursive depth of `squareFreeAuxRev` (`pthRoot` branches and
    Yun-loop branches alike).
  - Proved `squareFreeAuxRev_factor_dvd_input`: under the recursive
    invariant, every output factor divides the input. Induction on
    `fuel`, case-splitting on `g.isZero`, derivative-zero, and
    `isOne loop.2`. Uses `pthRoot_dvd_self_of_derivative_zero`,
    `yunFactorsWithLevel_factor_dvd_current`,
    `yunFactorsWithLevel_repeated_dvd_repeated`,
    `gcd_isZero_false_of_right_isZero_false`, and a small inline
    nonzero-from-divisibility argument.
  - Proved the coprime-transfer corollary
    `squareFreeAuxRev_factors_coprime_of_input_coprime` as a
    one-liner composing the divisibility helper with the input
    coprimality hypothesis.
- Verified:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
    shows only the two pre-existing proof holes (lines 2960 and
    4632 — `_pow_obligation` and the unchanged tail-cross sorry).
    No new banned markers introduced.

## Current frontier

- The new helper + predicate stand alone as reusable infrastructure
  for #3218 and ultimately #2917. They capture the recursive
  residual derivative-zero invariant that the tail-cross
  cross-coprimality argument needs to thread through the
  `squareFreeAuxRev` recursion.

## Next step

- A successor agent picks up **#3218**: restate
  `yunFactorsWithLevel_squareFreeAuxRev_tail_cross_coprime` with
  hypotheses `hreachable : yunFactorsPairwiseReachable c w fuel`
  and `hresidual : (...) ∧ squareFreeAuxRevResidualSatisfied
  (pthRoot loop.2) (base * level * p) fuel`, prove it using
  `yunFactorsWithLevel_factors_coprime_repeated_of_reachable`,
  `pthRoot_dvd_self_of_derivative_zero`, and the new
  `squareFreeAuxRev_factors_coprime_of_input_coprime`. Then
  cascade the residual provider through `_core_of_yun_invariant`,
  `_core`, and the rest of the pairwise-coprime consumer chain.

## Blockers

- None for this slice; the deep math (the residual provider for
  derivative-active inputs) is tracked in #3199 / #3200 / #3209
  → #2917 and is out of scope for #3217.
