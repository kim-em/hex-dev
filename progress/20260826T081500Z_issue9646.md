# Issue #9646 progress

- Added a build-only kernel replay module for the complete advertised
  `hex-int-factor` replay boundary.
- Covered valid and corrupt factorization and order certificates with
  `decide +kernel`.
- Covered every kernel-facing divisor consumer on bounded canonical checked
  factorizations, while leaving divisor sorting outside the promised boundary;
  consolidated the existing large-subject kernel examples into this probe.
- Exercised table and Pocklington primality replay, the minimal-order rejection
  clause, and both Carmichael reducers.
- Registered the probe in Lake, the DAG/build-only allowlists, and the existing
  single Ubuntu CI job without adding a job or workflow.
