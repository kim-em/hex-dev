# Session: close GF2n.toGeneric RingEquiv obligations (#7936)

## Accomplished
- Discharged the four `sorry`s in the `GF2n` namespace of
  `HexGF2Mathlib/Field.lean` (`ofGeneric_toGeneric`, `toGeneric_ofGeneric`,
  `toGeneric_add`, `toGeneric_mul`), so `GF2n.equiv : GF2n ≃+* GenericFiniteField`
  is sorry-free.
- Added private bridge helpers (all in the Mathlib layer, no executable-layer
  edits):
  - `degree_toFpPoly` / `degree_ofFpPoly`: degree preserved across the
    `GF2Poly ↔ FpPoly 2` bridge.
  - `ofUInt64_canonicalWordLT_packedReduceWord`: the single-word reduction reads
    back the residue mod the modulus (re-derives the masking-identity privately,
    so `Hex.GF2n.reduce` need not be unfolded by name — `(reduce w).val` is defeq
    to the fully-public `canonicalWordLT … (packedReduceWord …)` term).
  - `toFpPoly_mod` / `toFpPoly_mod_modulus`: `toFpPoly` commutes with the
    polynomial remainder (`div_mul_add_mod` + `reduceMod_add_mul_self_right` +
    degree bridge).
  - `ofUInt64_reduce_val` / `ofUInt64_reduceWide_val` / `ofUInt64_add_val` /
    `ofUInt64_mul_val`: word-level XOR/clmul ops read back as poly +/* mod the
    modulus.
  - `ofUInt64_toWords_getD_of_reduced` (low-word round-trip for reduced polys,
    replica of the private `ofUInt64_lowWord_eq_of_degree_lt_64`),
    `isZero_or_degree_lt_ofUInt64`, `eq_of_val_eq`, `reduceMod_modulusFpPoly_*`.
  - Added a `ZMod64.PrimeModulus 2` instance in the `GF2n` namespace (needed by
    the `GFqRing.reduceMod` algebraic lemmas).

## Current frontier
- `HexGF2Mathlib` builds green; `HexGFqMathlib` (downstream) builds green;
  `scripts/check_dag.py` exit 0. No new sorry/axiom/native_decide on added lines.

## Next step
- The parallel `GF2nPoly` namespace in the same file still has its four `≃+*`
  sorries (#7937) — out of scope here. The helpers added here are GF2n-specific;
  GF2nPoly already has its own `reducePoly_val_eq_mod`/`mod` machinery in the
  executable layer to mirror.

## Blockers
- None.
