# Session db8f3bd2 — work #4006 / #4041

## Accomplished

- Checked the unclaimed queue and attempted the leading human-oversight item
  #3945; `coordination claim` rejected it because it needs replan.
- Claimed #4006, assessed it as too broad for one session, and decomposed it
  into #4041, #4042, #4043, and #4044 with a parent breadcrumb before skipping
  #4006 for replan triage.
- Claimed #4041 and exposed the exhaustive slow-branch API in
  `HexBerlekampZassenhaus/Basic.lean`:
  `exhaustiveCoreFactorsWithBound`,
  `exhaustiveSlowRawFactorsWithBound`,
  `factorWithBoundUsesExhaustiveBranch`,
  `factorWithBound_entry_mem_exhaustive_branch_raw`, and
  `exhaustiveSlowRawFactorsWithBound_mem_normalization_or_core`.
- Made the existing exhaustive core product, normalization, and recordability
  facts public.
- Verified with `lake build HexBerlekampZassenhaus`,
  `lake build HexBerlekampZassenhausMathlib.Basic`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

#4041 is implemented locally and ready for PR creation. The next Group A
successors are blocked on this public API surface.

## Next step

Open the PR for #4041 and let #4042 proceed once #4041 lands.

## Blockers

None.
