# Session 6877358c — Harmonise HO-4 leaf capstones cap-separation/Mignotte surface

Session: `6877358c-c4c7-4bdf-b74e-42dad94eb9d9`
Branch: `agent/6877358c`

## Accomplished

Claimed and executed issue #4383 (harmonise HO-4 leaf capstones
cap-separation/Mignotte surface in
`HexBerlekampZassenhausMathlib/Recovery.lean`). Both deliverables
landed.

**Deliverable 1 — parameter-block alignment.** Reordered the
cap-separation `ForwardRecoveryInputs` producer chain
(`ofCapSeparation`, `ofCapSeparationCanonicalIndicators`,
`ofCapSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`) and its
two consumers (`bhksRecover_eq_some_of_capSeparation` and
`factorFast_ne_none_of_capSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`)
so that `trueSupports` immediately follows `rows_pos`, matching the
existing Mignotte-side layout. The cap-separation-only block
`(localFactorIndex, localFactorDegree, H, hcap_le, C, hC_nonneg, hC,
hcap)` is now contiguous instead of split by `(hB_pos, hnormalized, hp,
hk)`. Verified there are no external callers (only progress notes
mention the capstones), so the reorder is purely local.

**Deliverable 3 — shared lift-shape abbreviation.** Introduced a
file-local `private abbrev factorFastCapLiftData f primeData` for the
nested `Hex.henselLiftData (Hex.normalizeForFactor f).squareFreeCore
(Hex.precisionForCoeffBound (Hex.factorFastPrecisionCap f) primeData.p)
primeData` expression that the two HO-4 leaf capstones repeated ~8 and
~10 times. Rewrote both capstone signatures to use the abbreviation.
`abbrev` is reducible so the change is transparent at the kernel
level. Net effect on the capstones: cap-separation went from 96 lines
to 60, Mignotte went from 114 lines to 65; total file shrunk by 73
lines.

Each deliverable landed as its own conventional-prefix commit:

- `1a33cfd refactor: align cap-separation chain parameter order with Mignotte`
- `9b56f6f refactor: factor shared lift-shape expression in HO-4 leaf capstones`

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Recovery` and
`lake build HexBerlekampZassenhausMathlib` both pass.
`python3 scripts/check_dag.py` exits 0. `git diff --check` is clean.
No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
`sorry`.

## Next step

PR ready for the issue close. The harmonisation issue's two
deliverables are both complete.

## Blockers

None.
