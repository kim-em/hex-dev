# 2026-05-16 16:44 UTC — thirty-fifth Phase 4/5 checkpoint (a204e4ee)

Claimed summarize issue #4512 and recorded this checkpoint covering
the merged PR wave since #4369 closed at 2026-05-16T02:49:57Z.

## Accomplished

Read the 62 PRs merged after #4369 closed, the last 20 progress
entries (covering the 2026-05-16T03Z–16Z window), the current
`coordination orient` frontier, and the still-open `agent-plan` issue
bodies for the HexBZ Mathlib coverage stack, HO-43 / HO-44, and the
HO-1 executable irreducibility tail.

The dominant change since the previous checkpoint is that the HexBZ
Mathlib recombination-candidate-divisibility chain landed end to end:
matched-prefix mask helpers, monic capstones, recombinationCandidate /
representedFactor natDegree accounting, candidate divisibility
(#4430), cover-at-min containment, the mask-level enumeration-order
helper, the matched-state wrapper, and finally the
prefix-none-under-partition assembler that unblocks #4301. HO-43
Round 2 is also substantively closed via the 1850-line
determinant/Bareiss relocation, leaving HO-44 as a single-file orphan
deletion. The HexGfqRing Phase 6 review surface on
`HexGfqRing/Operations.lean` is now fully covered.

- **HexBZ Mathlib recombination-candidate divisibility chain — full
  closure of the #4430 capstone and unblock of #4301**:
  - Mask / subset side: #4385 (matched lifted-factor masks → subsets),
    #4388 (matched `subsetSplitsWithFirst` prefix characterization),
    #4382 (partition structure from successful candidate quotient),
    #4505 (mask-level `subsetSplits_prefix_exists_bit_diff_aux`,
    partial discharge of #4504), #4511 (top-level matched-state
    wrapper `liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches`,
    closing #4508), and #4516 (this cycle's terminal
    `liftedFactorSubsetPartition_prefix_none` assembler, closing
    #4367). Reviews #4509 and #4517 audited #4505 and #4511; #4519 is
    the trim follow-up PR for #4517.
  - Monic capstones: #4386 (`multifactorLiftQuadratic` output monic),
    #4390 (`henselLiftData_liftedFactor_monic` umbrella), #4399
    (integer-factor monic capstone), #4421 (Fp lift monic helper),
    #4466 (composition of the umbrella from `choosePrimeData`
    boundary facts). #4463 added the monic + squarefree
    `toPolynomial recombinationCandidate` helpers.
  - natDegree accounting: #4444 (recombinationCandidate natDegree as
    sum of selected lifted-factor natDegrees) and #4450
    (represented-factor natDegree, same shape).
  - Quadratic Hensel lift bridge: #4411 / #4435 / #4442 / #4449
    constructed `QuadraticMultifactorLiftInvariant` and wrapped it
    around `choosePrimeData`. #4431 added the `liftedFactorProduct`
    splits and `RepresentsIntegerFactorAtLift` mul helpers.
  - Divisibility capstone proper: #4396 (divisor-of-recombination
    support contained in selected subset), #4471 (normalize-factor
    bridge for reverse coverage), #4473 (finite represented-subset
    degree union), #4486 (reverse-coverage existence), #4494
    (candidate support packaging), #4497 (the #4430 main capstone
    `representedFactor_dvd_recombinationCandidate_of_subset`), #4498
    (cover-at-min containment from `recombinationCandidate ∣
    target`). The capstone divisibility review #4502 audited #4497;
    the cover-at-min review #4510 audited #4498.

- **HO-43 Round 2 substantively closed**: #4501 (private-helper
  promotion in `HexGramSchmidt/Int.lean`) followed by #4503
  (1850-line determinant/Bareiss relocation from
  `HexGramSchmidt/Int.lean` to `HexGramSchmidtMathlib/Int.lean`).
  #4391 refreshed the relocation inventory. #4407 / #4470 / #4480 /
  #4491 are the Round 3 / Round 4 prep PRs (row-add GramSchmidt
  bridge cluster, proof-free LLL executable entrypoints, triangular
  independence helpers, determinant-backed independence cluster).
  #4428 added the triangular `gramDet` positivity helper and #4438
  moved it to the bridge. Directive #3899 is now wholly mechanical
  to close once the remaining `replan` triage runs.

- **HO-44 narrowed to a single-file orphan**: #4448 audited the
  blocker; #4454 replaced the `bareiss_eq_det` bridge calls in
  `HexGramSchmidtMathlib`. Combined with #4503's Round 2 closure,
  `Hex.Matrix.bareiss_eq_det` in `HexMatrix/Bareiss.lean:1342` is the
  only remaining HO-44 deliverable.

- **HexGfqRing Phase 6 review surface fully covered**: ten review PRs
  finished `HexGfqRing/Operations.lean` — #4476 (quotient API doc),
  #4477 (Frobenius power doc), #4481 (ofPoly / reduceMod simp normal
  form), #4483 (axiom helpers polish), #4484 (natCast cluster), #4492
  (nsmul / intCast / zsmul cluster), #4493 (final Lean.Grind
  instances), #4495 (pow / linearPow), #4506 (linearNSmul / nsmul.go
  binary-decomposition bridge), and #4507 (capstone divisibility
  cross-review). Open PR #4518 (for #4515) lands the corresponding
  `done_through: 5 → 6` bump.

- **HexBZ HO-4 forward-recovery polish**: #4379 (monic cofactor from
  bounded congruence), #4384 (cross-reference Mignotte from
  cap-separation capstone), #4387 (HO-4 leaf-capstone surface
  harmonisation). #4402 / #4403 polished the DensePoly core API and
  UInt64 wide-word component API. #4404 cross-referenced consumers
  in the monic cluster.

- **HexBZ Phase 1 reconcile**: #4488 reconciled the Factorization
  scaffold status; #4415 added the non-monic `exactQuotient`
  wrapper.

- **Other Phase 6 review polish**: #4414 (HexPoly executable
  division/GCD), #4418 (HexArith Nat helper layer), #4420 (HexPoly
  Operations), #4427 (HexHensel `Array.polyProduct` consolidation),
  #4455 (HexPoly unary coefficient simp surface).

## Current frontier

Open PRs:

- #4519 (review polish of #4511's helper, closing #4517).
- #4518 (`HexGfqRing.done_through: 5 → 6` bump, closing #4515).
- #2656: unrelated SPEC draft for hex-real-roots / hex-sturm.

Unclaimed `agent-plan` work (after this summarize claim):

- #4301: HexBZ recursive rest-search coverage. Now newly unblocked
  by #4516 (the `liftedFactorSubsetPartition_prefix_none` assembler
  this cycle). The body's two flagged prerequisites — the
  prefix-none-under-partition lemma and `Monic f_min` from
  `cover_at_min` — are both satisfied: the assembler discharges
  prefix-none directly and `henselLiftData_liftedFactor_monic`
  (#4390 / #4466) plus `monic_primitive_sign_normalized_of_monic`
  give the monic witness inline. Four successive worker sessions
  have skipped or decomposed this issue citing scope; the recursive
  induction across the LiftedFactorSubsetPartition /
  HenselSubsetCorrespondenceRest / matches API is large but no
  longer requires further prerequisite work.
- #4334: HexLLL Phase 4 inconclusive scientific benchmark verdicts.
  Repeatedly skipped for host-contention reasons since
  2026-05-15; per `SPEC/benchmarking.md` re-dispatch waits on
  quiet/dedicated hardware.

Claimed work:

- #4512 (this summarize task).

Blocked chains to keep visible:

- BZ Mathlib coverage critical path: #4301 (now unblocked) → #4274 →
  #4006 → #4008 → #3987 → #3969 → #3970. The whole stack rests on
  #4301 — that single recursive coverage proof is the gate for the
  remaining six BZ coverage / Factorization-capstone issues.
- HexGramSchmidt scaledCoeffRows diagonal determinant chain: #3977 →
  #4053 → #3899. Round 2 closure does not unblock these; they sit
  on different bridge lemmas.
- HO-1 executable irreducibility tail: #4172 → #4170 → #4006.
- HO-18 / HexLLL Phase 4 report: directive #3662 still gated on
  #4334.
- Open directives: #2564 (HO-1 rewrite), #2567 (HO-4 BHKS 5.2),
  #2637 (Factorization record API), #3662 (HO-18 perf report), #3899
  (HO-43 — substantively complete after Round 2; remaining `replan`
  triage will close it), #3900 (HO-44 — narrowed to the single
  `HexMatrix/Bareiss.lean:1342` orphan deletion).

## Status survey

`python3 scripts/status.py` reports broad ready capacity across
Phase 4–6 libraries. Most active executable targets this cycle:

- **HexBerlekampZassenhausMathlib (Phase 3 wait)** — the
  recombination-candidate-divisibility chain landed end to end, and
  the prefix-none assembler unblocks #4301. The library still
  reports `waiting on HexBerlekampZassenhaus.done_through ≥ 3`.
- **HexGfqRing (Phase 6)** — Operations review surface fully
  covered; the `done_through: 5 → 6` bump is in flight as #4518.
- **HexGramSchmidt / HexGramSchmidtMathlib (Phase 5 / Phase 4)** —
  HO-43 Round 2 closure shipped (#4501 + #4503); the gramDet
  triangular positivity helper moved into the bridge (#4438).
- **HexLLL (Phase 4)** — no measurement progress this cycle; #4334
  remains environmentally gated.

The 1160-line `HexGfqRing/Operations.lean` file-split follow-up
flagged in `progress/20260516T153130Z_33ea7f9d.md` after the Phase 6
review wave is the next natural housekeeping target there but is not
yet planned.

## Next step

1. Let #4518 (HexGfqRing Phase 6 bump) and #4519 (#4517 review
   polish) settle. After #4518 lands the status report will show
   HexGfqRing at `done_through: 6`.
2. Dispatch / claim #4301 now that the
   `liftedFactorSubsetPartition_prefix_none` prerequisite is in
   tree. This is the single critical-path unblock for the BZ Mathlib
   coverage stack #4274 → #4006 → #4008 → #3987 → #3969 → #3970.
3. Trigger the `replan` triage on directive #3899 (HO-43) to close
   it after Round 2, and on #3900 (HO-44) to dispatch the orphan
   `bareiss_eq_det` deletion in `HexMatrix/Bareiss.lean:1342`.
4. Re-dispatch #4334 (HexLLL Phase 4 verdicts) only when quiet or
   dedicated hardware is available per `SPEC/benchmarking.md`.

## Blockers

No blocker for this checkpoint.
