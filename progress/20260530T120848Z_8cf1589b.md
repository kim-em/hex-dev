**Accomplished**

- Claimed #5790 and regenerated the HexHensel Phase-4 profile cases under
  `scripts/profile/run_profile.sh` timed-region filtering.
- Updated `reports/hex-hensel-performance.md` §Profile with fresh
  `bridge-operations`, `linear-hensel`, `quadratic-hensel`, and
  `multifactor-lifting` profile data, including retained-sample diagnostics.
- Verified that all dominant inclusive costs still map to registered
  `HexHensel/Bench.lean` targets; no audit-found follow-up was needed.
- Verified:
  - `lake build hexhensel_bench`
  - `git diff --check`
  - `rg -n "(axiom|native_decide|sorry|TODO|FIXME)" reports/hex-hensel-performance.md`
    returned no matches

**Current frontier**

- HexHensel's headline report now reflects bench-thread timed-region
  profiling instead of whole-child-process samply profiles.

**Next step**

- Open the PR for #5790 with the report update and this progress file.

**Blockers**

- None.
