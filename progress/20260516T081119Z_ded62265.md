# Session ded62265 — HexBZ represented-factor natDegree (#4440)

Session: `ded62265-01d0-4186-9864-caec32c69658`
Issue: `#4440`

## Accomplished

Claimed `#4440` and implemented the represented integer-factor degree identity
in `HexBerlekampZassenhausMathlib/Basic.lean`:

- Added `recombinationCandidate_eq_factor_of_recovery_of_monic_core`, a
  record-filter-free core form of the existing recovery equality. The original
  `recombinationCandidate_eq_factor_of_recovery` theorem remains as a wrapper
  with its previous signature for compatibility.
- Added `natDegree_toPolynomial_eq_sum_of_represents`, proving that a factor
  represented by a lifted-factor subset has Mathlib `natDegree` equal to the
  sum of the selected lifted-factor `natDegree`s.

The proof identifies the represented factor with its recombination candidate
via centered-lift recovery, derives `2 ≤ d.p ^ d.k` using the existing
nonzero-factor contradiction pattern, and then reuses
`natDegree_toPolynomial_recombinationCandidate_eq_sum`.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic` — success; only the two
  pre-existing `sorry` warnings in this file remain.
- `lake build HexBerlekampZassenhausMathlib` — success.
- `python3 scripts/check_dag.py` — success.
- `git diff --check` — clean.
- `rg -n '^\s*axiom\b|\bnative_decide\b' HexBerlekampZassenhausMathlib/Basic.lean`
  — no matches.
- Forbidden-token scan for `TODO`/`FIXME` found no matches; `sorry` matches are
  the two pre-existing declarations at lines 162 and 241.

## Current frontier

`#4440` is complete. The represented-factor side of the degree-counting
argument is now available.

## Next step

`#4441` can use the candidate-side identity from `#4439` plus this
represented-factor identity to prove the `T = ⊔ S_g` reverse coverage and
assemble the main candidate divisibility theorem residual from `#4430`.

## Blockers

None.
