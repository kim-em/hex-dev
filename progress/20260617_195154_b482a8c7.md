# Progress — 2026-06-17 (session b482a8c7, /work)

## Accomplished

### #7819 (HexPolyZMathlib proof-only Phase 4 completion) — landed

Premise-checked and confirmed the directive: `HexPolyZMathlib` exposes no
separable executable computational surface, so Phase 4 ("performance &
benchmarking") is a bookkeeping completion attributing executable
performance to the Mathlib-free sibling `HexPolyZ` (`done_through: 5`).

Premise evidence:
- All four files (`Basic.lean`, `Mignotte.lean`, `RobinsonForm.lean`,
  `SchurSzego.lean`) define proof-side objects over Mathlib noncomputable
  types: `Polynomial ℤ`, `ℂ[X]`, `Multiset ℂ`, `ℝ`, `ℂ`. No `#eval`, no
  `native_decide`, no `deriving DecidableEq/Repr`.
- `Basic.lean` bridge maps (`toPolynomial`, `ofPolynomial`, `equiv`) target
  noncomputable `Polynomial ℤ`; not a runtime-evaluated kernel.
- `lakefile.lean` / `SPEC/benchmarking.md` forbid `Hex*Mathlib` bench
  targets. Matches precedents #5397, #7808, #7809 exactly.

Single-line bump: `libraries.yml` `HexPolyZMathlib.done_through` `3 → 4`.
No other library entry touched.

## Verification

- `python3 scripts/status.py HexPolyZMathlib` → `done_through: 4`, advanced
  to Phase 5, `ready: yes`.
- `python3 scripts/status.py` → exit 0, no new blocked regressions.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --stat` → only `libraries.yml` (1 line).

## Current frontier

`HexPolyZMathlib` now at Phase 5 (implementation work loop). The sibling
proof-only bridge #7820 (HexLLLMathlib) is the identical remaining unclaimed
Phase-4 bookkeeping completion.

## Next step

Claim #7820 (HexLLLMathlib) for the same single-line `done_through 3 → 4`
bump once premise-checked.

## Blockers

None.
