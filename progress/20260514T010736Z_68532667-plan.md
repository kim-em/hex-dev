# Planner session 68532667

## Accomplished

Reviewed queue state (POD_QUEUE_DEPTH=2, POD_QUEUE_DEFICIT=1) and
posted one new feature issue:

- #3890 — HexGramSchmidt: prove pivot adjacent-swap scaledCoeff
  invariance. Single-deliverable, independent of #3886, isolates the
  one remaining `scaledCoeffs_adjacentSwap_*` sorry that does not
  reference `dk' = adjacentSwapGramDetQuotient` and can be proved from
  `coeffs_adjacentSwap_pivot` (line 376) using the
  `scaledCoeffs_eq` + `intCast_rat_injective_local` template already
  proven in `scaledCoeffs_adjacentSwap_lower_prev` /
  `scaledCoeffs_adjacentSwap_lower_curr`.

## Observations not actioned

- #3878 was triage-closed with a planner comment flagging two
  remaining options for #3879 (re-host into `HexMatrixMathlib/`, or
  narrow to Int). #3879 is currently `blocked` on the closed #3878
  and is no longer actionable as-stated; per
  `SPEC/Libraries/hex-matrix.md:131-144` the Desnanot-Jacobi proof
  lives in HexMatrixMathlib. This is a re-planning question, not a
  new-issue question, so left to `/replan` or a follow-up planner.
- The four `above_prev`/`above_curr` adjacent-swap sorries in
  `HexGramSchmidt/Update.lean` (lines 875, 881, 893, 899) have an
  apparent definition mismatch between the formula in the theorem
  statements and the integer numerator definitions
  (`adjacentSwapScaledCoeffAbovePrevNumerator`,
  `adjacentSwapScaledCoeffAboveCurrNumerator`). Did not file an issue
  for those four sorries: the discrepancy needs investigation before
  the deliverable can be specified, and #3886 (the `dk'` formula)
  must land first anyway.

## Current frontier

Unclaimed queue: #3890 (this issue), #3886, #3889 — three feature
items at min-queue.

## Next step

Next planner: investigate the numerator-vs-formula discrepancy in
`adjacentSwapScaledCoeffAbove{Prev,Curr}Numerator` after #3886 lands,
then file the appropriate `above_*` issues. Separately, replan #3879
to either re-host to `HexMatrixMathlib/Determinant/` or narrow to
`Int` per the planner triage on #3878.

## Blockers

None for this session.
