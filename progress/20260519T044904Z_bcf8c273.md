# Session bcf8c273 — HexGfq GF2q repr neg/sub wrappers

## Accomplished

Closed issue #5260. Added `@[simp]` wrappers in `HexGfq/Basic.lean`:

- `Hex.GF2q.repr_neg`, proving `repr (-x) = repr x` by definitional equality
  through `GF2n.neg`.
- `Hex.GF2q.repr_sub`, proving that subtraction representatives normalize to
  the reduced XOR of the two packed representatives through `GF2n.sub`.

Verified:

- `lake build HexGfq.Basic`
- `lake build HexGfq`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check -- HexGfq/Basic.lean`
- `rg -n '^@\[simp\] theorem repr_(neg|sub)\b' HexGfq/Basic.lean`
- `git diff -- HexGfq/Basic.lean | rg -n -e '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

No new Lean `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` tokens.
The project-wide Lean banned-token count remains `0`.

## Current frontier

`GF2q` now exposes representative simp lemmas for zero, one, negation, and
subtraction. Addition and multiplication wrappers are being handled separately
by issue #5258.

## Next step

Continue Phase 6 API polishing for `HexGfq`, especially any remaining
representative wrappers once the adjacent add/mul work lands.

## Blockers

None. There is an unrelated pre-existing `.claude/CLAUDE.md` worktree
modification; it was not touched or included.
