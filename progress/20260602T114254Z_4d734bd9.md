# Progress 20260602T114254Z (session 4d734bd9)

## Accomplished

Claimed issue #6282 and rewired the private initial no-pivot Gram invariant path
in `HexGramSchmidt/Int.lean`. The initial specialization now uses a
`BareissGramInitialRegularStepQuotientProvider` and derives each reachable
regular row-entry relation through
`bareissGramInitialRegularStep_entry_eq_dot`, instead of requiring callers to
provide `hentry_regular`.

Updated the private singular-step consumers
`leadingPrefix_gram_zero_pivot_column_zero_of_singular_step` and
`leadingPrefix_gram_bareiss_toNat_eq_zero_of_noPivot_singular` to take quotient
provenance, preserving the existing narrow private API boundary for the
remaining #5655 work.

Verification completed:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over `HexGramSchmidt/Int.lean`

All passed. The Lean builds still report the pre-existing warnings in
`HexGramSchmidt.Basic` and `HexGramSchmidt.Int`, including the existing `sorry`.

## Current frontier

The initial no-pivot Gram row invariant no longer asks callers for the full
row-entry theorem. Downstream work still needs to propagate the quotient
provenance through `gramDetVecEntry_eq_leadingPrefix_bareiss` and its public
consumers.

## Next step

Issue #6269/#5655 can build on the quotient-provider boundary to expose a clean
no-pivot Gram row consumer and then discharge the leading-prefix determinant
bridge.

## Blockers

None for this session.
