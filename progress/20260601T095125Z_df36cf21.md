# 2026-06-01T09:51Z — IntReductionMod choosePrimeData cleanup (#5868)

## Accomplished

- Read the latest progress entry and followed the worker queue.
- Claimed directive #2564 first, audited the current BZ implementation
  surface, and released it as a stale parent: the executable
  `factorSlow` / `factorFast` / `factorWithBound` / `factor` API now
  exists, while the remaining headline-proof gaps are already covered by
  existing blocked decomposition issues.
- Claimed #5868 and migrated all six
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` proof-body sites
  away from `unfold Hex.choosePrimeData`.
- Each site now uses
  `Hex.choosePrimeData_eq_of_choosePrimeData?_some hchoose` to obtain the
  total-form equality from the existing `choosePrimeData?` witness.

## Current frontier

- `rg -n "unfold Hex\\.choosePrimeData" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  returns no hits.
- `git diff --check` passes.
- The diff introduces no new `sorry`, `axiom`, `native_decide`, `TODO`, or
  `FIXME`.

## Next step

- Re-run `lake build HexBerlekampZassenhausMathlib` after #5878 lands.

## Blockers

- `lake build HexBerlekampZassenhausMathlib` currently fails before this
  target in `HexBerlekampZassenhausMathlib/Basic.lean:6314` with the
  known strict-gcd / `Hex.squareFreeModP` mismatch tracked by #5878.
