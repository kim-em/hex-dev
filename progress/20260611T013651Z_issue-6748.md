# Progress: #6748 — squareFreeCore primitive + squareFreeRat feeders

## Accomplished

Landed two Mathlib-free feeders for the slow-trial `h_raw` discharge in
`HexBerlekampZassenhaus/Basic.lean`, alongside the existing
`squareFreeCore_*_of_ne_zero` family near `:15711`:

1. `squareFreeCore_primitive_of_ne_zero (f : ZPoly) (hf : f ≠ 0) :
   ZPoly.Primitive (normalizeForFactor f).squareFreeCore` — extracts the
   left factor of the `squareFreeCore * repeatedPart` primitivity invariant
   supplied by
   `ZPoly.primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`
   applied to `(extractXPower (primitivePart f)).core` (nonzero by
   `extractXPower_core_ne_zero_of_ne_zero`).

2. `squareFreeCore_squareFreeRat_of_ne_zero (f : ZPoly) (hf : f ≠ 0) :
   Hex.ZPoly.SquareFreeRat (normalizeForFactor f).squareFreeCore` —
   forwards the recorded core's non-zeroness (from
   `squareFreeCore_ne_zero_of_ne_zero`) into
   `ZPoly.primitiveSquareFreeDecomposition_squareFreeCore`.

A local `private theorem ZPoly_primitive_left_of_mul` discharges
`Primitive (p * q) → Primitive p` via the integer-content arithmetic
`content (p*q) = content p * content q` with non-negative integer
content forcing `content p = 1`. Mirrors the existing private helper of
the same shape in `HexBerlekampZassenhaus/SmallModSingleton.lean:173`;
kept local rather than moving to `HexPolyZ/Basic.lean` to minimise
blast radius for this issue.

`lake build HexBerlekampZassenhaus` succeeds (full package, 497 jobs).
`lake build HexBerlekampZassenhausMathlib.IntReductionMod` succeeds
(downstream Mathlib-side consumer of the parent #6719 wrappers).
`python3 scripts/check_dag.py` passes. No `sorry` / `axiom` /
`native_decide` / `TODO` / `FIXME` on added lines.

## Current frontier

With these two feeders plus the existing
`squareFreeCore_ne_zero_of_ne_zero` and
`squareFreeCore_leadingCoeff_pos_of_ne_zero`, all four core-shape
hypotheses of `exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
(`:13443`) and `quadraticIntegerRootFactors?_factor_irreducible_of_primitive`
(`:14060`) are now dischargeable from `f ≠ 0` alone when specialised to
`(normalizeForFactor f).squareFreeCore`. The fifth hypothesis (`hbound`)
flows from `defaultFactorCoeffBound_valid` (Mathlib-side) — unchanged.

## Next step

Unblocks #6719 (slow-trial `h_raw` discharge). The next worker on that
issue (or its decomposed successors per the residual sub-issues called
out in `20260611T000331Z_issue-6719-partial.md`) can now apply the four
feeders without first having to prove the missing two.

## Blockers

None.
