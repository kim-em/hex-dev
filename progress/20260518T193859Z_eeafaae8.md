# Audit session — #5069 (post-#5064/#5066 `zpoly_dvd_trans` cluster closure)

## Accomplished

Posted the audit comment on
[#5069](https://github.com/kim-em/hex/issues/5069#issuecomment-4481345752)
verifying the three invariants for the
`HexBerlekampZassenhausMathlib/Basic.lean` `zpoly_dvd_trans`
cluster after #5064 (rcases-form) and #5066 (calc-form hoist
+ rewires) landed.

- **Invariant 1 (call-site coverage):** PASS. 39 occurrences in
  `Basic.lean` (1 `private theorem` definition at line 2069 +
  38 rewired uses across lines 2104 through 13733). The
  expected `3 (#5036) + 6 (#5045) + 3 (#5066) + 26 (#5064) =
  38` use count matches exactly. The six remaining
  `refine ⟨_ * _, ?_⟩` matches plus the two `mul_assoc_poly
  (S := Int)` matches outside the helper body are classified
  as helper-internal, non-transitivity (`a ∣ b ∧ c ∣ d →
  a*c ∣ b*d` product; `a ∣ b → a ∣ y*b` `dvd_mul_left` shape),
  or out-of-scope-layer (`Hex.FpPoly`-level transitivity,
  which `zpoly_dvd_trans` does not cover).
- **Invariant 2 (framing/docstring/hoist):** PASS. Docstring
  introduced by #5066 (`dc2ba756`) is intact (contract-first
  sentence + Semigroup-instance semantic note). The issue
  body's "bespoke `Dvd` instance" paraphrase is wording-only;
  the in-tree note says "Mathlib `Semigroup` instance" — same
  root cause. `private theorem`, no PR refs / metadata /
  provenance hints. Hoist position `Basic.lean:2069` is the
  minimal exposure point for the first calc-form caller
  (`henselSubsetCorrespondenceRest_transport_of_disjoint` at
  line 2088). The 7-line shift from the issue's projected
  `:2076` is the line-collapse from #5064 above the helper.
- **Invariant 3 (cross-file consistency):** PASS. 39 in
  `Basic.lean`, 0 in `IntReductionMod.lean`, 0 in
  `Recovery.lean`. Exactly one `private theorem zpoly_dvd_trans`
  declaration across the BZMathlib tree.

Out-of-scope observation drafted in the audit comment for the
next planner cycle: there is an analogous `Hex.FpPoly`-level
divisor-transitivity cluster with at least three confirmed
sites (`Basic.lean:4985-4989`, `Basic.lean:5726`,
`Basic.lean:5803`) plus two calc-form candidates
(`Basic.lean:1347, 1383`) that uses a different bespoke `Dvd`
instance (`instDvdOfAddOfMul`) and would benefit from a
parallel `fpPoly_dvd_trans` helper. The full draft issue body
is in the audit comment, ready to lift.

## Current frontier

The `zpoly_dvd_trans` cluster (ZPoly-level) is closed. No further
extraction or rewire work anticipated for this helper. Issue
#5069 is ready to close on PR merge.

## Next step

Next planner cycle could pick up the `fpPoly_dvd_trans` analogue
cluster (drafted in the audit comment). Otherwise, no follow-up.

## Blockers

None.
