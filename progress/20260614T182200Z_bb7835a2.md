# Progress — 2026-06-14 (session bb7835a2, feature)

## Accomplished

Issue #7315: added one-sentence Phase 6 docstrings to the five
undocumented private helper theorems in `HexPolyFp/SquareFree.lean`
(lines ~5738–5787):

- `powLinear_ne_zero` — `powLinear d n ≠ 0` for nonzero base, by induction.
- `pow_ne_zero` — the same nonzero fact for `pow`, via `pow_eq_powLinear`.
- `powLinear_degree?_getD` — `deg (powLinear d n) = n * deg d`.
- `pow_degree?_getD` — the same linear-degree identity for `pow`.
- `dvd_one_of_all_powers_dvd_nonzero` — every power dividing a nonzero
  `g` forces `d ∣ 1` (degree-growth contradiction).

Doc-only change: no signatures, statements, proofs, simp/grind
annotations, declaration order, or imports touched.

## Verification

- `lake build HexPolyFp.SquareFree`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexPolyFp/SquareFree.lean`: `5 0` (additions only).

## Current frontier

This five-lemma power/degree/divisibility cluster is now documented.
Other Phase 6 doc batches (#7311 HexPolyZ rational-scaling, #7314
HexConway Lübeck-table hits) remain unclaimed.

## Next step

None for this issue.

## Blockers

None.
