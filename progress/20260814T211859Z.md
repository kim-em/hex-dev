# Huge-argument cosine sign acceptance probe

## Accomplished

- Added a Mathlib-free fixed-data package graph for `Real.cos (10^10)` with an
  authenticated quotient `3183098861`, a strict rational residual window, and
  separate constant, reduction, local-cosine, negation, and periodicity steps.
- Added Mathlib semantic replay using the provider-agnostic constant-enclosure
  claim reused from the sine experiment, discharged here by Mathlib's
  20-decimal pi bounds.
- Added a generic `TargetRun`/chronology/`ProofFrontend` conformance theorem
  `0 < Real.cos (10^10)`, an axiom-report guard, exact trace assertions, and
  structurally valid wrong-quotient and wrong-residual replay mutations.
- Documented the fixed sign canary and the remaining 80-bit enclosure,
  computed-reduction, provider-selection, and 1,000-bit constant gaps in both
  interval SPECs.
- Built the focused modules and the complete `HexIntervalExperiment`,
  `HexIntervalMathlibExperiment`, and `HexConformance` targets successfully;
  the Mathlib-free bench check, trust-surface audit, diff check, and local
  static scans are clean.

## Current frontier

The fixed huge-argument sign fact is end-to-end and ordinary kernel evidence.
The large quotient and residual payload fields are replay-critical, while the
orchestration remains the generic package/session/proof frontend.

## Next step

Replace the fixed residual table with a computed range-reduction certificate,
connect a precision-parametric constant provider and local cosine enclosure,
and strengthen the result to the roadmap's exact dyadic 80-bit enclosure.

## Blockers

None for the fixed sign canary. The stronger enclosure needs the explicitly
documented computed reducer and precision-parametric provider/local evaluator,
not an emergency proof bridge.
