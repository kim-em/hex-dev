# NumberField fixed-presentation semantics

## Accomplished

- Proved that checked integer irreducibility transfers to `ℚ`, hence gives
  separability, and identified each canonical algebraic number's normalized
  defining polynomial with its rational minimal polynomial.
- Proved the monic rational defining polynomial's degree and irreducibility,
  constructed the inverse reduced-coordinate map, and established the
  `QAdjoin`/`AdjoinRoot` bijection.
- Proved selected-root evaluation injective without an inverse/field cycle.
- Proved both defensive extended-GCD branches in executable inversion
  unreachable, then proved inversion and division correspondence.
- Installed an opt-in field structure that retains the executable arithmetic
  and existing rational scalar action, and bundled the `AdjoinRoot` ring
  equivalence and complex ring embedding.
- Moved natural and integer powers into the Mathlib-free executable layer and
  added definitional regressions pinning the scoped field to every executable
  operation and the existing rational scalar action.
- Updated the NumberField manual to distinguish the completed fixed-field
  foundation from later semantic stages.
- Built `HexNumberFieldMathlib.AdjoinRoot`, `HexNumberFieldMathlib`,
  `HexManual`, and the full repository successfully. The touched modules close
  six existing `sorry`s and add none.
- Audited the headline declarations with `#print axioms`; each depends only on
  `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`.

## Current frontier

The coherent fixed-presentation milestone passed independent mathematical and
source review, is rebased onto current `main`, and has completed post-review
validation. It is ready for its single publication PR.

## Next step

Publish and merge this single milestone PR before starting the next NumberField
semantic slice.

## Blockers

None.
