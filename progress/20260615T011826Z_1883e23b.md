# Progress — 2026-06-15 (session 1883e23b, feature)

## Accomplished

Issue #7408: added one-sentence Phase 6 docstrings to the ten
undocumented private helper lemmas in
`HexArith/Montgomery/InvNat.lean`:

- `two_pow_dvd_uint64_word`, `mod_uint64_word_mod_two_pow`,
  `int_newton_refine_dvd`, `nat_newton_refine_dvd`,
  `int_dvd_of_nat_dvd`, `nat_mod_eq_one_of_int_dvd_sub_one`,
  `int_dvd_sub_one_of_nat_mod_eq_one`,
  `nat_mod_eq_pred_of_int_dvd_add_one`, `odd_square_mod_eight`,
  `montPosInvStep_mod_refine`.

Doc-only change: no signatures, statements, simp/grind attributes,
declaration order, or proofs touched. Each docstring states what the
lemma proves and its role in the 2-adic Newton-refinement /
wrapping-arithmetic chain behind the Montgomery inverse, matching the
existing one-line style on the documented neighbours.

## Verification

- `lake build HexArith.Montgomery.InvNat`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexArith/Montgomery/InvNat.lean`: `16 0`
  (additions only).

## Current frontier

InvNat.lean docstring cluster complete. The remaining undocumented
`HexArith/Montgomery` helpers (RedcNat and siblings) are separate
Phase 6 batches.

## Next step

None for this issue. Other unclaimed Phase 6 doc batches remain.

## Blockers

None.
