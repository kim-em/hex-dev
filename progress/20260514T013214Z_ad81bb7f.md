# Progress - feature #3209

## Accomplished

Added the scaled-loop residual derivative-zero composition theorem
`yunFactorsContributionWithLevel_residual_derivative_zero_of_derivative_split`
in `HexPolyFp/SquareFree.lean`, immediately after the unscaled provider
`yunFactorsResidualDerivativeZero_of_derivative_split` (line ~5289).

The new theorem composes:

- `yunFactorsContributionResidualDerivativeZero_of_derivative_split`
  (the unscaled witness from #3846) to discharge the
  `yunFactorsContributionResidualDerivativeZero c g level fuel`
  hypothesis on `(c, g) = (f / gcd f f', gcd f f')`, and
- `yunFactorsContributionWithLevel_residual_derivative_zero_of_unscaled`
  (the residual scaled↔unscaled bridge from #3204) to transport the
  derivative-zero fact onto `yunFactorsContributionWithLevel c g base
  level fuel`.

Signature takes `base level fuel : Nat` so callers can specify the
exact scaling exponent layout they need; the typical caller uses
`base = multiplicity, level = 1` to match the
`squareFreeAuxRevContribution_derivative_active_pow_obligation`
contribution shape, but the lemma is general enough to cover other
scaling layouts.

The `hstate` derivative-active-state thread-through matches the
upstream `_of_derivative_split` signature, so consumers like
`squareFreeAuxRevContribution_derivative_active_pow_obligation` (which
already supplies an equivalent four-tuple state) need only project to
the three-tuple shape this lemma expects.

Verification:
- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  returns no matches.

## Current frontier

The new composition theorem is available but currently unused — the
existing `_pow_obligation` still derives `htail_derivative` from
`squareFreeAuxRevResidualSatisfied`. That refactor was out of scope
for this issue; the parent #3153 follow-up (or a separate planner
issue) can swap callers over and eventually drop the `_hresidual`
invariant once all downstream consumers route through the structural
provider.

## Next step

Rewire `squareFreeAuxRevContribution_derivative_active_pow_obligation`
to consume `yunFactorsContributionWithLevel_residual_derivative_zero_of_derivative_split`
directly, removing the `squareFreeAuxRevResidualSatisfied` ↔
`_hresidual` dependency on the active-branch path.

## Blockers

None.
