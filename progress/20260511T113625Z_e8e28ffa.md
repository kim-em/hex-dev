# 2026-05-11 11:36 UTC — work/review session (e8e28ffa)

## Accomplished

- Claimed #3351, found the predecessor #3350 helper was too weak for the requested polynomial instantiation, and requeued #3351 for replan with the concrete dependency-ordering blocker.
- Lost the claim race for #3377, then claimed #3378.
- Reviewed `HexGF2/Irreducibility.lean` against the Phase 6 API-polish goal.
- Added narrow `[simp]` characterisation lemmas for:
  - `(rabinWitnesses f).all Prod.snd`
  - `IrreducibilityCertificate.powWitness?`
  - `IrreducibilityCertificate.bezoutWitness?`

## Current frontier

- The Rabin certificate checker API now exposes the main witness-list and certificate-accessor normal forms without requiring downstream callers to unfold list/array plumbing.
- The review did not find a need to change executable checker behavior.
- `HexGF2/RabinSoundness.lean` already contains the larger bridge from checker success to `GF2Poly.Irreducible`, so no new follow-up was needed for that surface.

## Next step

- Open the PR for #3378 and note in the PR body that the review added only proof-facing simp lemmas.

## Blockers

- #3351 needs replan or a stronger abstract McCoy grid helper: the landed helper only supplies larger-`p` diagonal dependencies, while the polynomial lower-`q` cells also need smaller-`p`/higher-`q` facts.
- The pre-existing `.claude/CLAUDE.md` worktree modification remains untouched.
