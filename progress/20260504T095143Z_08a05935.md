# Repair PR #2035

Session: `08a05935-ec0f-42bd-a6b0-c95c29b7ee5a`

## Accomplished

Repaired CI failure on PR #2035 (HexPoly python-flint oracle for
mul/gcd/divMod cross-checks). The failing job was
`conformance (HexGfqMathlib)`; root cause was a pre-existing main
breakage inherited from #1849 (`fix: require prime modulus for Fp
division laws`).

`#1849` added `[ZMod64.PrimeModulus p]` to the `Lean.Grind.{Semiring,
Ring, CommRing}` instances on `Hex.GFqField.FiniteField` in
`HexGfqField/Operations.lean`, but `HexGfqMathlib/Basic.lean` did not
mirror that requirement in its `FiniteField`/`GFq` namespace `variable`
declarations. The Mathlib `field` instance, `Fintype` instance, and
`equivGaloisField` could no longer synthesize the prerequisite
`Lean.Grind.*` instances.

Fix:
- Added `[Hex.ZMod64.PrimeModulus p]` to both `FiniteField` and `GFq`
  namespace `variable` blocks.
- Added `omit` clauses on `fintype_card` (both namespaces) so the
  unused-section-variable lint stays quiet on the cardinality theorems
  that go through `finEquiv` rather than the `field` instance.
- Added a local `[Hex.ZMod64.PrimeModulus 2]` instance to
  `HexGfqMathlib/Conformance.lean` so the `(2, 1)` examples can resolve
  the new constraint, mirroring the pattern used in
  `HexGfq/CrossCheck.lean`.

`lake build` is green end-to-end.

## Current frontier

PR #2035 ready for re-run; auto-merge will pick it up once CI reports
green.

## Next step

None for this session — repair is complete, exiting after `mark-pr-salvaged`.

## Blockers

None.
