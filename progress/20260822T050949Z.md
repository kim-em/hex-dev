# Characteristic-polynomial completion audit

## Accomplished

- Audited `SPEC/Libraries/hex-char-poly.md` requirement by requirement against
  the core, Mathlib bridge, conformance, benchmark, release, and CI artifacts.
- Removed the Berkowitz column's runtime `getD` fallback in favor of a proved
  in-bounds access, and renamed the exact moment count so the implementation
  reflects its structural, fuel-free totality.
- Added `lake exe hexcharpoly_bench growth`, which reports wallclock and peak
  intermediate bit size for every dimension and entry-bit rung while checking
  the instrumented recurrence against the public `charPoly` result.
- Rebuilt the full 9,831-job graph and rechecked all 17 fixtures with FLINT,
  all 13 benchmark registrations with FLINT and PARI, exact fixture emission,
  the PARI flag-3 coefficient protocol, release metadata, trust surface,
  dependency boundaries, conformance registration, and repository policies.

## Current frontier

The characteristic-polynomial specification is fully implemented and the
completion audit has no open requirement. The branch is ready for review.

## Next step

Open the upstream PR; after merge, run the released-repository sync in dry-run
mode before publishing the two managed mirrors.

## Blockers

None.
