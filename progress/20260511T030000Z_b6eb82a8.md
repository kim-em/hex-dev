# 2026-05-11 03:00 UTC — feature session (b6eb82a8)

## Accomplished

- Inspected #3122 (HexPolyFp pthRoot tail cross-coprimality); confirmed
  that the decomposition into #3217 + #3218 from session 23940a36 is
  in place but the parent's `replan` label still let `coordination
  list-unclaimed` surface it. Skipped #3122 again with a pointer to
  the existing decomposition so the planner can narrow or close it.
- Claimed #3216 and discharged the
  `natDegree_toPolynomial_le_degree_getD_of_dvd` sorry in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  - Added a reusable generic bridge fact in
    `HexPolyMathlib/Basic.lean`:
    `natDegree_toPolynomial (p : Hex.DensePoly R) :
      (toPolynomial p).natDegree = p.degree?.getD 0`
    Proof case-splits on `p.size = 0` (zero polynomial: both sides are
    `0`) versus positive size, using `coeff_eq_zero_of_size_le` and
    `coeff_last_ne_zero_of_pos_size` to upgrade
    `Polynomial.natDegree_le_iff_coeff_eq_zero` /
    `Polynomial.le_natDegree_of_ne_zero` into a `le_antisymm`.
  - Added a `toPolynomial_dvd` helper that transfers executable
    divisibility through the existing ring structure: from `p ∣ q`,
    produce `toPolynomial p ∣ toPolynomial q` via `toPolynomial_mul`.
  - Used both in `HexBerlekampZassenhausMathlib/Basic.lean`: lift
    `f ≠ 0` to `toPolynomial f ≠ 0` through `equiv.injective`, transfer
    `g ∣ f`, apply `Polynomial.natDegree_le_of_dvd`, then rewrite the
    Mathlib `natDegree` on the RHS back to the executable `degree?`
    via the new bridge fact.
- Imported `Mathlib.Algebra.Polynomial.Degree.Lemmas` to make
  `natDegree_le_iff_coeff_eq_zero` available.

## Verification

- `lake build HexPolyMathlib.Basic` — clean.
- `lake build HexBerlekampZassenhausMathlib.Basic` — clean; only the
  out-of-scope sorries from the issue's exclusion list remain.
- `lake build HexPolyZMathlib HexPolyMathlib HexBerlekampZassenhausMathlib`
  — clean.
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry'
  HexBerlekampZassenhausMathlib/Basic.lean` — the target sorry is
  removed; remaining sorries match the issue's out-of-scope list.

## Current frontier

- The Mathlib-side Mignotte chain (`defaultFactorCoeffBound_valid`)
  still depends on the unrelated `l2norm_toPolynomial_le_coeffL2NormBound`
  sorry (covered by #3215) and several other sorries tracked in the
  same file.

## Next step

- A separate session can pick up #3215 to discharge the L2 norm
  comparison and unlock the Mignotte capstone.

## Blockers

- None for this slice.
