# 2026-05-16T17:17Z — planner: dispatch first Phase 7 chapter issue

## Accomplished

Surveyed orient / status / open `agent-plan` issue bodies and the
last five progress files. `POD_QUEUE_DEFICIT=1` with the existing
unclaimed work covering two distinct frontiers (#4521 in-flight as
PR #4522, #4334 environmentally gated, #4301 four-times skipped).
PR #4522 (#4521's worker output) is mergeable but blocked by branch
protection until CI finishes. No broken / conflicting PRs.

`scripts/status.py` now reports `HexGfqRing -> Phase 7 (user-facing
documentation)` — the first library in the repo to clear Phase 6
following the `done_through: 5 -> 6` bump landed in #4520. No Phase
7 chapter exists in tree: `HexManual.lean` is a single-line
placeholder, `HexManual/` holds only legacy markdown checkpoints,
and `lakefile.lean` already wires Verso `v4.30.0-rc2` and the
`lean_lib HexManual` aggregator.

Dispatched the bootstrap Phase 7 issue:

- #4523 `HexGfqRing Phase 7: write HexManual chapter and bump
  done_through to 7` — three deliverables: write
  `HexManual/Chapters/HexGfqRing.lean` per the chapter outline in
  `PLAN/Phase7.md:48-58`, wire it into `HexManual.lean`, and bump
  `libraries.yml HexGfqRing.done_through: 6 -> 7`. Body pins the
  scope to HexGfqRing alone (HexGfqRing has no anchored tutorial in
  the `PLAN/Phase7.md:62-76` table) and explicitly leaves Verso
  convention choices (signature-pull macros, code-example
  embedding patterns) to the worker — those conventions will be
  reused by every subsequent chapter.

## Current frontier

Open `agent-plan` queue after this dispatch:

- #4523 (this cycle) — Phase 7 HexGfqRing chapter.
- #4334 — HexLLL Phase 4 verdicts (environmentally gated).
- #4301 — BZ Mathlib recursive coverage (skipped 4×; both flagged
  prerequisites now in tree once #4522 merges).
- Blocked chain rooted at #4301: #4274 → #4006 → #4008 → #3987 →
  #3969 → #3970, plus #4172 → #4170 → #4006 and the HexGramSchmidt
  scaledCoeffRows diagonal-determinant chain #3977 → #4053 → #3899.

Open PRs: #4522 (#4521 nodup helper, CI in progress), #2656
(human-authored SPEC draft, draft state — leave alone).

## Next step

Next planner cycle should reassess after #4522 lands (#4521's
`LiftedFactorListMatches.nodup_of_injOn` helper). With both flagged
#4301 prerequisites in tree (#4516 prefix-none assembler + #4521
Nodup helper), the recursive coverage proof may now be claimable
directly; a fifth skip would be the trigger for further
planner-led decomposition (the speculative `cover_at_min` monic
discharge helper I considered this cycle but did not file).

Also worth tracking: HexGfqRing being the only Phase 7-ready
library means no parallel Phase 7 dispatch is possible until more
libraries reach `done_through ≥ 6` — see the active Phase 6
"Ready" list (HexArith, HexPoly, HexModArith, HexGF2, HexPolyFp)
for the next batch of candidates, each of which still needs its
own Phase 6 exit audit.

## Blockers

None for this planner session. #4523 is the only new issue created
this cycle; the queue deficit of 1 is now filled.
