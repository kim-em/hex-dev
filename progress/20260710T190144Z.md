**Accomplished**

Reviewed the flat row-major `Hex.Matrix` migration core in `HexMatrix/Basic.lean`,
`HexMatrix/Elementary.lean`, and `HexMatrix/SPEC/hex-matrix.md` for public
statement drift, in-place mutation claims, SPEC accuracy, and flat-index edge
cases. Also checked representative downstream hot-path uses in Bareiss, row
reduction, and LLL native code.

**Current frontier**

The value-level API mostly preserves the opaque row/entry semantics, but the
review found documentation and statement-surface concerns around
representation-specific lemmas and over-strong in-place claims for row/column
mutation helpers.

**Next step**

If acting on the review, restore row-observation-facing public lemma statements
where possible and either implement the column analogues through the flat
per-entry engines or soften their in-place documentation/SPEC claims.

**Blockers**

None for this review. I did not re-run the whole Lean build, per request.
