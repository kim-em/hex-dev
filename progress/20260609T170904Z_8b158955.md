# 20260609T170904Z (#6661)

## Accomplished

Claimed and completed #6661 in `HexPolyFp/SquareFree.lean`.

Added two private normalized-tail scalar synchronization wrappers after
`yunFactorsContributionWithLevel_scalar_sync_of_derivative_active_reachable`:

- `yunFactorsContributionWithLevel_scaled_normalized_tail_reconstruct`
- `yunFactorsContributionWithLevel_scaled_normalized_tail_product_sync`

The first composes recursive scalar sync with normalized-tail reconstruction to
show the normalized scaled residual reconstructs to a nonzero scalar multiple of
the raw residual. The second composes the same scalar sync with the normalized
tail product bridge and `pow_C_mul_sync`, exposing the weighted-product
synchronization with an explicit nonzero scalar factor.

Before #6661, checked the highest-priority directive #2564; `coordination
claim` reported it blocked, so I did not work it. Claimed #4831, found its
associated/unit transport premise stale, commented concrete evidence on the
issue, and released it for replanning. Claimed #6656, found #6692 explicitly
states #6656 must remain blocked until the sharper phase-one handoff lands,
added `depends-on: #6692`, and released it for replanning.

## Current frontier

#6661's wrapper layer is in place. The next HexPolyFp normalized-Yun work can use
the private scaled normalized-tail reconstruction and product-sync wrappers
instead of reopening the recursive scalar synchronization proof.

## Next step

Open the PR for #6661. Downstream #6431/#6394 can then consume the new wrappers
when assembling the normalized Yun scalar-unit completion invariant.

## Blockers

None for #6661.

Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
