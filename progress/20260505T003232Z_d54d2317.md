# HexPoly weighted derivative convolution helper

## Accomplished

- Claimed feature issue #2136.
- Added `DensePoly.rat_mulCoeffSum_derivative_product_rule` in `HexPoly/Euclid.lean`, backed by finite-fold weighted diagonal helpers over `Rat`.
- Reused the existing diagonal `mulCoeffSum` machinery rather than duplicating schoolbook multiplication folds in `HexPolyZ`.
- Verified the touched HexPoly file has no forbidden placeholders.

## Current frontier

The standalone weighted derivative convolution identity is available for downstream `HexPolyZ` work. The final `HexPolyZ.Basic` square-free branch still has its pre-existing theorem-level `sorry`.

## Next step

Use `DensePoly.rat_mulCoeffSum_derivative_product_rule` to finish the Rat derivative product rule and then the quotient-by-gcd square-free chain.

## Blockers

- `lake build HexPolyZ.Basic` still reports the existing out-of-scope `sorry` at `HexPolyZ/Basic.lean:1735`.
