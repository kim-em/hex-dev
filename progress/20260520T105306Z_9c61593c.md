# 2026-05-20T10:53Z — feature session 9c61593c

## Accomplished

Closed #5660 (HexGramSchmidt Phase 5: package row-combination prefix-span
truncation) by adding to `HexGramSchmidt/Int.lean` near
`normSq_latticeVec_ge_min_basis_normSq`:

- `prefixCoeffsCast c k : Vector Rat (k.val + 1)` and the last-entry
  lemma `prefixCoeffsCast_last`.
- A local `castIntMatrix` alias mirroring the `private` definition in
  `HexGramSchmidt/Basic.lean` so the new theorems can name the term that
  `basis_span` uses in its statement; the two definitions are defeq.
- `foldl_int_dot_cast`: integer cast distributes over a one-step `dot`
  foldl.
- `rowCombination_int_getElem` and
  `rowCombination_prefix_castIntMatrix_getElem`: entry-level expansions
  of the integer row combination and the cast-prefix row combination.
- `cast_rowCombination_eq_prefix_rowCombination`: under the zero-tail
  hypothesis on `c`, the cast row combination equals the row combination
  of the first `k.val + 1` cast input rows with `prefixCoeffsCast c k`.
  Combines `foldl_int_dot_cast` with the truncation helper
  `foldl_finRange_eq_prefix_of_zero_above` from #5669.
- `prefixSpan_castIntMatrix_of_rowCombination`: the prefix-span witness
  on the cast input rows, packaged via `Exists.intro`.
- `prefixSpan_basis_of_rowCombination`: transport of the cast-input
  prefix-span through `basis_span` to a prefix-span on `basis b`.

## Current frontier

`normSq_latticeVec_ge_min_basis_normSq` is still a `sorry`; the
prefix-span witness produced here is the substrate that #5657 needs to
identify the highest GS coordinate, which in turn feeds the lower-bound
proof in #5649.

## Next step

Pick up #5657 next: with the prefix-span witness now available, the
top-coordinate identification can use `coeffs_upper`/`coeffs_diag`
against the prefix-span input rows.

## Blockers

None for this issue. `lake build HexGramSchmidt` is clean (the two
`sorry` warnings are in pre-existing declarations, and the `simpa`
linter note is on pre-existing code).
