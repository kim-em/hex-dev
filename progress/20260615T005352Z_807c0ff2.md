# Review session — forward monic correspondent (#7384)

## Accomplished

Reviewed the forward monic correspondent infrastructure in
`HexBerlekampZassenhausMathlib/Basic.lean` (lines ~20032–20357, from
PR #7373): `exists_monicCorrespondent_of_dvd` plus supporting lemmas
`monicDilate`, `monicDilate_monic`, `monicDilate_coeff`,
`dilate_monicDilate`, `dilate_injective`, `scale_injective`,
`scale_scale`, `scale_mul_scale`, `degree?_getD_of_ne_zero`.

**Verdict: clean — no defects, no code change.** Findings posted as a
comment on #7384 and the issue closed.

Checked all three deliverables:
1. Corrected premise sound: `hlc : 0 < leadingCoeff core` forces the
   recovery scalar `lc^a/lf > 0`, so `primitivePart (dilate lc g) =
   factor` exactly (not `-factor`). Verified the `core=-X^2+1`,
   `factor=X-1` counterexample fails without `hlc`. `hfdeg` rules out
   zero/constant factors and feeds `dilate_monicDilate`. `0 < lf`
   recovered correctly from `normalizeFactorSign` (def at
   `HexBerlekampZassenhaus/Basic.lean:1618`).
2. No hidden weakening: canonical witness `g = monicDilate lc factor`;
   both divisibility branches present (constant-cofactor `b=0` →
   `g = (toMonic core).monic`; `b>0` → `g*h = monic`).
3. Hygiene: no sorry/axiom/native_decide in window; injectivity side
   conditions discharged from real hypotheses.

Minor (not a defect): `hdeg` is logically redundant — derivable from
`hfdeg` + the proof's own `hab : a+b=d`. Harmless; downstream supplies it.

## Current frontier

PR #7373 infrastructure verified sound. It is the `hrepP`-feeding
prerequisite for the in-flight bijection assembly (#7364, #7381).

## Next step

None for this review. Downstream bijection work (#7364, #7381) can rely
on the contract; in the squareFreeCore regime `0 < leadingCoeff core`
and `1 ≤ degree factor` are both available, so the landed signature is
not awkward.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib`: `...Mathlib.Basic` built,
  0 errors; no warnings in the correspondent window (20032–20357).
  Pre-existing sorries confined to `HexBerlekampMathlib/Basic.lean` and
  `HexBerlekampZassenhausMathlib/FactorSoundness.lean`.
- `python3 scripts/check_dag.py`: exit 0.
