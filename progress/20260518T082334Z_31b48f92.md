# Session 31b48f92 — feature: BZMathlib `_of_bound` for `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence` (#4938)

## Accomplished

Claimed #4938 and implemented the abstract-bound (`_of_bound`)
propagation through the public-facing coverage theorem
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
in `HexBerlekampZassenhausMathlib/Basic.lean`. The PR adds one new
`_of_bound` theorem and rewires the existing original as a thin wrapper:

* `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence_of_bound`
  — forwards to
  `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core_of_bound`
  (the #4936 substrate) and threads `B'`, `hcore_lc_le`, `hvalid`,
  `hprecision` through verbatim. All other plumbing (`hmatches` /
  `hfuel` derivations, the `hsearchMod` unfold step, the final
  `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
  application) is precision-agnostic and carries over verbatim.
* Original rewired to instantiate
  `B' := Hex.ZPoly.defaultFactorCoeffBound core`, discharging
  `hcore_lc_le` via `defaultFactorCoeffBound_valid` +
  `leadingCoeff_eq_coeff_last`, and `hvalid` via the universal
  `defaultFactorCoeffBound_valid core hcore_ne`.

The original keeps its signature exactly; its sole in-tree consumer
(`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`)
continues to type-check via the rewired thin wrapper.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — 16 errors, all
  pre-existing (whnf timeouts at 4848/5129/5455/5542/5589/5973/6121/6293/6274,
  cases-failed at 5851, kernel-unknowns at 6608/6672, plus the second
  region at 15586/15697/15904/15950 shifted +81 lines from the prior
  baseline at 15505/15616/15823/15869; the +81 line shift matches the
  net `+107/-26` diff against `origin/main`).
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing error
  set, no new errors.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no
  matches.

## Current frontier

The next `_of_bound` consumer one layer up is
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
(#4944). It is unblocked once this PR lands.

## Next step

Land the PR; the `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_*_of_bound`
sibling is the natural next target for the A2 abstract-bound cascade.

## Blockers

None for this work item.
