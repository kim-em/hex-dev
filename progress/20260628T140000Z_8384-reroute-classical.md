# #8384 — re-route to classical-tier irreducibility + delete off-path scaffolding

## Accomplished
- **Premise re-check overturned the approved plan.** The plan (and a Codex
  review) framed the headline `factor_irreducible_of_nonUnit`
  (`FactorSoundness.lean`) as nearly done via `factor_irreducible_of_nonUnit_of_fastCore`
  → the fast-core keystone `factorFastCore_irreducible_of_liftedTrueSupport`
  (one residual `hsize` bridge). Verified in-tree + by `#eval` that this is
  **off the public path**: `Hex.factor = factorHybrid` (`Basic.lean:9921`); its
  factors come from `factorHybridFactors` (classical / lattice / trial tiers),
  not from `factorWithBound`/`factorFastCore`. No `factorHybrid = factorWithBound`
  bridge exists. Empirically every input (monic, non-monic, reducible,
  irreducible, up to deg-8 cyclotomic) is factored by the **classical tier**
  (`tier=classical`); the fast BHKS core declines at the production bound
  (`B = defaultFactorCoeffBound < cldCoeffFloor`).
- **Deleted the off-path apparatus** (commit `b4cdfbc`): the 15
  `HexBerlekampZassenhausMathlib` BHKS lattice-recovery / fast-core-irreducibility
  modules (28,240 lines; reachable from the headline only via the deleted
  FactorSoundness reduction; no live or external importers) + the two off-path
  FactorSoundness lemmas. `lake build HexBerlekampZassenhausMathlib` green;
  `check_dag.py` passes; the single remaining `sorry` is the target. All
  recoverable from git history.

## Current frontier
The real obligation: **irreducibility of `classicalCoreFactorsWithBound`'s
output** (`HexBerlekampZassenhaus/Basic.lean`), i.e. minimality of
`scaledRecombinationSmart`. The candidate it forms is
`normalizeFactorSign (primitivePart (dilate coreLc (centeredLiftPoly (∏ subset) modulus)))`
— the **dilate** coordinate, the one where the Hensel-subset correspondence is
proven (the scale-coordinate pain was specific to the now-deleted lattice/M1
path). The needed dilate-coordinate infrastructure **survived** in `Basic.lean`:
`RecoveredAtLift` (2348), `RepresentsIntegerFactorAtLift` (2370),
`liftedRecoveryCandidate` (2528), `liftedFactorProduct` (2221), and the
subset-containment/minimality lemma `toMonicLiftData_subset_of_dvd_liftedRecoveryCandidate`
(21730). The #8401 reconstruction (`scaledRecombinationSmart*_product`, product =
target) is the other half already in hand.

## Next step
1. State the target lemma `classicalCoreFactorsWithBound … = some cf → ∀ g ∈ cf,
   Irreducible g`, and align `scaledRecombinationSmartCandLoop`'s candidate with
   `liftedRecoveryCandidate` (the centeredLift / primitivePart / normalizeFactorSign
   wrapping vs the `RecoveredAtLift` form).
2. Decompose `factor_irreducible_of_nonUnit` over `factorHybridFactors`' three
   branches: trial (sound by construction), lattice (rare; handle or sorry),
   classical (the target).
3. Prove classical minimality: smallest dividing subset ⇒ irreducible, via the
   size-ordered search + the dilate-coordinate subset-containment lemma (a proper
   integer factor would correspond to a smaller dividing subset, contradiction).

## Blockers
None procedural. The classical minimality proof is genuine multi-session BHKS/
Zassenhaus soundness, but in the tractable (dilate) coordinate with surviving
infrastructure — not the blocked scale-coordinate path the deleted apparatus
fought.
