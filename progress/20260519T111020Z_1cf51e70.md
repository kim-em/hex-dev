# 2026-05-19T11:10Z — feature session, issue #5431

## Accomplished

- Added `composeModMonic_eq_mod` to `HexPolyFp/ModCompose.lean`,
  giving downstream callers the `%`-style characterisation
  `composeModMonic f g modulus hmonic = DensePoly.compose f g % modulus`.
- Added `composeModMonic_mod_eq_self`, stating that
  `composeModMonic ... % modulus = composeModMonic ...` so callers
  no longer have to reprove that the executable result is already
  reduced.
- Both lemmas are short corollaries of the existing
  `composeModMonic_eq_modByMonic_compose`,
  `DensePoly.modByMonic_eq_mod`, and `DensePoly.mod_mod`; no new
  imports.

## Current frontier

- `lake build HexPolyFp.ModCompose`, `lake build HexPolyFp`,
  `lake build`, `python3 scripts/check_dag.py`, `git diff --check`,
  and the forbidden-token grep all pass.

## Next step

- Open PR for #5431 and exit.

## Blockers

None.
