# Progress 20260602T103425Z (session 4880d3c3)

## Accomplished

Closed issue #6301 — proved the quotient-backed regular Gram row-entry
algebra lemma in `HexGramSchmidt/Int.lean`.

- Added a small private helper `dot_rowCombination_mul_right_int` that
  factors a scalar out of `Matrix.dot (Matrix.rowCombination b _) _` when
  the coefficient vector is `Vector.ofFn (fun a => f a * s)`. Reuses
  the existing `rowCombination_bareiss_coeff_update` and
  `dot_bareiss_row_update_left` helpers with the trivial `y := 0`
  decomposition.
- Added the private theorem `bareissGramRegularStep_entry_eq_dot`,
  which consumes a `BareissGramRegularStepQuotient` and
  `state.prevPivot ≠ 0`, plus an `h_processed` side premise giving
  zero entries for already-cleared columns strictly before
  `state.step`, and proves the row-entry relation for one regular
  Gram-row Bareiss step:
  `(stepMatrix state.matrix state.step state.matrix[step][step]
     state.prevPivot)[i][j] =
     Matrix.dot (rowCombination b (bareissGramRowInvariantStepCoeff …))
       (b.row j)`.
  The proof case-splits `j.val` against `state.step`, covering the
  trailing-block (`exactDiv` of the Bareiss numerator), pivot-column
  (both sides zero), and already-cleared-column (both sides zero)
  regimes.

Per the issue, the theorem is kept `private`, not wired into
`bareissGramRowInvariant_noPivotLoop_initial`, and does not modify any
executable definitions.

## Current frontier

Verification:
- `lake build HexGramSchmidt.Int` — clean.
- `lake build HexGramSchmidt` — clean.
- `python3 scripts/check_dag.py` — pass.
- `git diff --check` — no whitespace issues.
- No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` tokens.

## Next step

Final assembly issue (the next downstream step after #6301) will wire
`bareissGramRegularStep_entry_eq_dot` into
`bareissGramRowInvariant_noPivotLoop_initial`'s `hentry_regular`
argument, threading the `prevPivot ≠ 0` source from #6300
(`noPivotLoop_initial_gram_prevPivot_ne_zero_of_regular_prefix`) and
the column-zero source from `noPivotLoop_matrix_processed_col_eq_zero`.

## Blockers

None.
