# 2026-05-11 11:54 UTC — work session (3be0f205)

## Accomplished

- Reviewed the unclaimed queue and first claimed #3185, then skipped it for replan because the advertised closed prerequisite did not leave a reusable upper-triangular positive-diagonal `Matrix.independent` criterion in the current tree.
- Claimed #3346, then skipped it for replan because the closed #3351 prerequisite did not leave the polynomial-specific last-`q`-coefficient McCoy helper in `HexPoly/Euclid.lean`.
- Claimed #3369, then skipped it for replan after scope assessment: the requested Yun state provider needs additional nonzero and `squareFreeContributionReachable` preservation lemmas before the wrapper theorem can be threaded.
- Claimed #3385 and reviewed `HexBerlekampZassenhaus/Conformance.lean`, `HexBerlekampZassenhaus/EmitFixtures.lean`, `scripts/oracle/bz_flint.py`, the committed BZ JSONL fixtures, and Phase 3/testing guidance.
- Made the BZ conformance contract explicitly name signed scalar and `(factor, multiplicity)` bucket checks, and documented that the signed-scalar edge fixture group emits actual public `factor` results while python-flint supplies the independent expectation.
- Verified:
  - `lake build HexBerlekampZassenhaus.Conformance`
  - `lake exe hexbz_emit_fixtures > /tmp/hexbz_bz.jsonl && diff -u conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl /tmp/hexbz_bz.jsonl`
  - `python3 scripts/oracle/bz_flint.py --check`
  - `python3 scripts/status.py HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Conformance.lean HexBerlekampZassenhaus/EmitFixtures.lean`

## Current frontier

- The signed-scalar/multiplicity edge-case parity surface is documented in both the Lean conformance module and the fixture emitter.
- The committed BZ fixture output is unchanged; the existing oracle checks 27 cases with zero failures.

## Next step

- Create the PR for #3385 with only the conformance/emitter documentation changes and this progress note.

## Blockers

- `.claude/CLAUDE.md` was already dirty and unrelated/off-limits; it was not touched for this issue.
