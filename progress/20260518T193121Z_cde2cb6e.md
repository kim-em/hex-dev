# Session cde2cb6e — #5073 implementation

## Accomplished

- Claimed and executed issue #5073: rewired the
  `IntReductionMod.lean:1500-1505` `by_cases`-derived
  `isZero = false` block onto `Bool.eq_false_iff.mpr hderiv`.
- Replaced the 6-line `cases h : (...).isZero | false => rfl |
  true => exact absurd h hderiv` plumbing with the direct
  one-liner. `hderiv_false` binding name and statement type
  preserved verbatim; downstream call to
  `psd_repeatedPart_eq_ratPolyPrimitivePart_gcd_of_derivative_not_isZero`
  at line ~1513 typechecks unchanged.
- Verified: `lake build HexBerlekampZassenhausMathlib` preserves
  the 16-error Basic.lean baseline; 0 errors in
  IntReductionMod.lean. `scripts/check_dag.py` exits 0.
  `git diff --check` clean. No new sorry / axiom /
  native_decide / TODO / FIXME tokens introduced.
- Greppable invariants:
  - `isZero = false := by` in IntReductionMod.lean: 2 → 1
    (line 1500 site closed; line 1446 site remains — target of
    open PR #5071 for #5068).
  - `exact absurd .* hderiv` in IntReductionMod.lean: 1 → 0.
- Diff stat: 1 file changed, +2 / −5, net −3 lines.

## Current frontier

After #5073 (this) and #5068 / PR #5071 both land, the BZMathlib
library has zero inline `by_cases`-style `<poly>.isZero = false`
derivations in IntReductionMod.lean. All derivations route
through `Hex.DensePoly.isZero_eq_false_iff`,
`Hex.ZPoly.size_pos_of_ne_zero`, or `Bool.eq_false_iff`.

## Next step

Land the sibling 4-site `Basic.lean` `0 < size → isZero = false`
cluster (separate planning issue mentioned in #5073's context
section) to close out the BZMathlib reverse-direction isZero
cleanup across the whole library.

## Blockers

None.
