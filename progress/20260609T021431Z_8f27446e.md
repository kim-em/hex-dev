# Same-lattice certificate bridge (#6612)

## Accomplished

Claimed directive #6612 and added the certified same-lattice bridge in
`HexLLL/Basic.lean`:

- `Matrix.rowCombination_mul` transports row-combination witnesses through an
  explicit integer row transform.
- `Matrix.memLattice_of_mul_eq` proves one-sided lattice membership transport
  from `U * B = B'`.
- `Matrix.memLattice_iff_of_mul_eq` composes transforms in both directions to
  prove `∀ v, B.memLattice v ↔ B'.memLattice v`.
- `Matrix.sameLatticeCert` checks `(U * B == B') && (V * B' == B)`.
- `Matrix.sameLatticeCert_sound` turns an accepted boolean certificate into
  same-lattice equivalence.

Verification run:

- `lake build HexLLL`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line grep for `sorry`, `axiom`, and `native_decide`

All passed. Build warnings observed were pre-existing linter warnings outside
the touched file.

## Current frontier

The D3 lattice bridge is complete and available from `HexLLL/Basic.lean`.

## Next step

Use `Matrix.sameLatticeCert_sound` inside the future external reducer
certificate checker that also verifies reducedness and independence.

## Blockers

None.
