# 2026-06-08T10:45Z — feature session 413716e6

Worked issue #6582 (sub-issue of #6581).

## Accomplished

Added the three named entry points and product theorems in
`HexBerlekampZassenhaus/Basic.lean`:

- `factorSlowTrialFactorsWithBound (f B) : Array ZPoly` — mirrors
  `factorSlowFactorsWithBound`'s constant/quadratic-root short-circuits and
  dispatches to `exhaustiveIntegerTrialCoreFactorsWithBound` on the residual
  exhaustive branch instead of the modular recombination one.
- `private factorSlowTrialWithBound (f B) : Factorization` — wraps the raw
  factor array through `factorizationOfFactors`.
- `factorSlowTrial (f) : Factorization` — instantiates the wrapper at
  `ZPoly.defaultFactorCoeffBound`.
- `factorSlowTrialFactorsWithBound_polyProduct`,
  `factorSlowTrialWithBound_product`, `factorSlowTrial_product` —
  product invariants. Three branch-specific helpers
  (constant / quadratic / trial) mirror the layered structure of the
  existing `factorSlowWithBound_product_of_*_branch` family.
- `#guard` fixtures: `factorSlowTrialFactorsWithBound … 4 =
  #[exhaustiveNonMonicQuadraticGuard]` matches the existing slow-path
  guard at line 6659, and a second `#guard` checks that `factorSlowTrial`
  agrees with `factorSlow` on the same fixture.

## Current frontier

`HexBerlekampZassenhaus/Basic.lean` builds green (full target ~104s).
`lake exe hexbz_bench list` completes in ~3s wallclock on warm cache.
No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry`.

## Next step

Parent #6581 covers the actual three-tier rewiring of `factor` itself
(deliverable 3) and the `factorSlow → factorSlowModular` rename
(deliverable 1). Those are separate sub-issues; this PR only adds the
named entry points so they can be referenced by name.

## Blockers

None.
