# Propagator admission hardening

## Accomplished

- Derived instantiation substitution and generation depth from the selected
  engine action rather than registry-supplied trigger labels.
- Made structural instance identity ignore untrusted family labels and made
  zero-effect proposals return duplicate without advancing the program or
  consuming an instance slot.
- Recorded equality identifiers for every instantiation output, including
  reused links, while appending only genuinely new equality edges.
- Preserved valid candidate facts when the advisory-suggestion store is full;
  a bounded prefix is retained and dropped suggestions are counted.
- Made admission transparent for ordinary-kernel reasoning and documented the
  exposed experimental engine versus the required abstract production API.
- Added conformance canaries for generation laundering, authoritative
  substitutions, reused equality provenance, no-op admission, suggestion
  overflow, unbounded diagnostic codes, and mismatched reply serials.
- Added the scheduler spike to the existing CI executable build set.
- Built `HexInterval.PropagatorConformance`, the scheduler executable, and the
  complete `HexIntervalExperiment` / `HexConformance` aggregate successfully.

## Current frontier

The generic propagator PR has passed local builds and source/trust lints after
the admission fixes. A fresh independent pre-merge review and a new GitHub CI
run remain to be checked.

## Next step

Commit and push the reviewed hardening, address any substantiated final review
findings, then merge the generic framework before rebasing the policy layer.

## Blockers

None.
