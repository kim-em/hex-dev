# Session d3de2dc5: double-row nMatrix setRow transport (#6144)

## Accomplished

- Added a private `rowMoveUp_setRow_of_gt` helper in
  `HexMatrix/Determinant.lean`: when the `setRow` target sits strictly
  above the `rowMoveUp` move interval (`src + k < j.val`), the
  destination row is untouched by `rowMoveUp` on both sides, so the
  operations commute. Case-split on whether the test row is at `j`,
  below the interval, at the move source, above the interval, or
  strictly inside.
- Added the private double-row transport
  `det_setRow_setRow_nMatrix_r2_r0_r3_r1_eq_pow_mul_nDet_r2_r3`. For
  ordered rows `r0 < r1 < r2 < r3` this expresses
  `det (setRow (setRow (nMatrix B r0 r1 h01) s2 B[r0]) s3 B[r1])`
  as `(-1)^((r2 - r0 - 2) + (r3 - r1 - 2)) * nDet B r2 r3 h23`.
- The proof composes two applications of the existing
  `rowMoveUp_setRow_nMatrix_replace_first_eq_nMatrix`: first sliding
  `B[r0]` up to position `r0.val` (using the new commute helper to lift
  the outer `setRow s3 B[r1]` past the inner `rowMoveUp`), then sliding
  `B[r1]` up to position `r1.val - 1` of the intermediate
  `nMatrix B r1 r2 h12`. Two `det_rowMoveUp` peels and one
  `Lean.Grind.Semiring.pow_add` combine the sign factors; `grind`
  collapses the residual `(-1)^a * (-1)^a = 1` algebra via the existing
  `neg_one_pow_mul_self`.

## Current frontier

- `lake build HexMatrix` passes; `python3 scripts/check_dag.py` is
  silent; `git diff --check` is clean; the diff contains no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Next step

- Create PR closing #6144. After merge, #6105 (canonical raw `nDet`
  four-row Plucker assembly) becomes ready to consume both this and the
  one-row transports from #6143.

## Blockers

- None.
