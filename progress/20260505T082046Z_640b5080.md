# Work session: execute human-oversight directive #2308 (Bareiss/Desnanot-Jacobi library drift recovery)

## Accomplished

Process-only execution of #2308. No code changes in this PR.

**Step 1 — Closed nine superseded Bareiss-tower issues** with a uniform
breadcrumb pointing back to #2308 and forward to the new `HexMatrixMathlib`
replacement issues:
#1873, #1875, #1880, #1890, #1893, #1901, #2097, #2114, #2118.

**Step 2 — Filed three replacement issues against `HexMatrixMathlib`**, each
with a populated `Library placement` field per #2306:
- #2313 — inline mathlib4 PR #37716 (Desnanot-Jacobi identity) with
  attribution into `HexMatrixMathlib/Determinant/DesnanotJacobi.lean`.
- #2314 — prove `bareissNoPivot_eq_det` via the inlined identity.
  `depends-on: #2313`.
- #2315 — prove `bareissDet_eq_det` (public API with row pivoting).
  `depends-on: #2314`.

**Step 3 — Audited `HexMatrix/Determinant.lean` (3570 lines) and filed two
migration sub-issues**, one per logical move:
- #2316 — delete obsolete final-column / off-diagonal residual scaffolding
  (concrete deletion-candidate list with line ranges; consumer-grep audit
  required per candidate). `depends-on: #2314`.
- #2317 — audit surviving `borderedMinor` / `leadingPrefix` det theorems and
  move adjugate-style ones to `HexMatrixMathlib`. `depends-on: #2314, #2316`.

`HexMatrix/Basic.lean` `borderedMinor` / `leadingPrefix` *data definitions*
and entry simp lemmas (~L166-276) are flagged in #2317 as definitional and
expected to stay in `HexMatrix`.

**Step 4 — Cross-cluster placement audit.** Verified each of #2129, #2206,
#2228, #2248, #2286 already carries the relevant `depends-on:` line on its
infrastructure issue (#2206/#2213 for #2129, #2213 for #2206, #2227 for
#2228, #2247 for #2248, #2285 for #2286). Added a `## Library placement`
section to each of the five issue bodies with the mandatory four-question
answers per #2306.

**Verification gates run.** `python3 scripts/check_dag.py` clean. No code
changes, so `lake build` was not needed; all build/verification work for
this directive is deferred to the migration sub-issues #2316/#2317 and the
new replacement issues #2313/#2314/#2315.

## Current frontier

The recovery program is now decomposed into a clean dependency graph:

```
#2313 (DJ inline) → #2314 (bareissNoPivot_eq_det)
                  → #2315 (bareissDet_eq_det) [depends on #2314]
                  → #2316 (delete scaffolding) [depends on #2314]
                  → #2317 (move borderedMinor theorems) [depends on #2314, #2316]
```

The end state of #2316 is what satisfies the verification criterion
"`HexMatrix/Determinant.lean` line count drops". This PR does not move that
needle — it sets up the work.

## Next step

A planner should triage:
1. The three sibling open issues #2293, #2296, #2298 (and their predecessor
   #2250) — they target the same `prefixRowDeletionMinor` /
   `prefix-row final-column` chain that #2316 will delete. They were not in
   the nine-issue close list of #2308 and should not be closed unilaterally,
   but they likely need to be re-scoped to `HexMatrixMathlib` or absorbed by
   #2316/#2317.
2. The order of work: #2313 unblocks #2314/#2315/#2316/#2317, so a worker
   should pick it up first.

## Blockers

None for this issue — it is process work and is complete.

The migration sub-issues filed under it are blocked on their own
predecessors (the `blocked` label is set automatically by `coordination plan`
when `depends-on` references an open issue), which is the intended state.
