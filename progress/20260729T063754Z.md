# Generic sine proof lift

## Accomplished

- Restacked the non-polynomial sine vertical directly onto the generic
  composed-extension checker.
- Added the fact domain's node-local `holdsPrefix` proof.
- Changed the transparent checker result from an extended-program entailment
  to the caller's base-program entailment.
- Deleted the temporary manual extension/assumption/result transport theorem.
- Made the concrete real theorem depend only on the evidence returned by
  `SemanticReplay.check`; the package-owned `sinNegExtends` theorem now occurs
  only inside the instance schema which transparent replay dispatches.
- Rebuilt `HexInterval.SinOddConformance`; the conformance manifest,
  copyright, line-count, trust-surface, banned-token, and diff checks are
  clean.

## Current frontier

The generic checker now closes the entire path from the quoted extended trace
to `-1 ≤ Real.sin (-x) ∧ Real.sin (-x) ≤ 0` over the original program.

## Next step

Review this local stacked commit, then let the root session coordinate the
lower-stack rebase and push order.

## Blockers

None.
