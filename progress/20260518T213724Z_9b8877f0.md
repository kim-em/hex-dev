# Promote FpPoly leading coefficient nonzero helper

Issue: https://github.com/kim-em/hex/issues/5106

## Accomplished

- Promoted the private `leadingCoeff_ne_zero_of_isZero_false` helper in
  `HexPolyFp/SquareFree.lean` to public theorem
  `fpPoly_leadingCoeff_ne_zero_of_isZero_false` with a contract-first docstring.
- Rewired the four direct local uses in `HexPolyFp/SquareFree.lean` to call the
  promoted theorem.
- Verified the grep invariant reports the public theorem and no old private
  declaration.

## Current frontier

The #5106 proof/API polish patch is complete and ready for PR. The only
unrelated dirty worktree entry observed during the session is
`.claude/CLAUDE.md`, which was not touched.

## Next step

Open a PR closing #5106.

## Blockers

None. `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
`python3 scripts/check_dag.py`, and `git diff --check` passed.
