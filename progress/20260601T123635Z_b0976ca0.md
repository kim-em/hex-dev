# b0976ca0 - feature #5936 partial

## Accomplished

- Read the latest progress note, worker-flow skill, queue state, and issue
  bodies for #5936 and #5952.
- Claimed #5936 as the older HexMatrix dependency unblocker.
- Added a public Mathlib-free `det_mul` theorem in `HexMatrix/Determinant.lean`,
  extracting the existing Cauchy-Binet determinant-multiplication argument that
  was previously embedded in the Desnanot-Jacobi auxiliary proof.
- Verified `lake build HexMatrix`, `git diff --check`, and the added-line grep
  for `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
- Decomposed the remaining #5936 proof into #5953 (one-row replacement cofactor
  Jacobi bridge) and #5954 (final two-row Laplace assembly), with #5954 blocked
  on #5953, and left the required `Decomposed into #5953, #5954` breadcrumb on
  #5936.

## Current frontier

#5936 is partially advanced but not complete. The reusable determinant
multiplication substrate now exists; the remaining hard proof is the
columnwise Jacobi identity
`det M * cofactor (setRow M r u) s c = ...`, after which the requested
two-row determinant identity should be a focused fold/Laplace assembly.

## Next step

Work #5953, proving the one-row replacement cofactor bridge in
`HexMatrix/Determinant.lean`. Then #5954 can assemble the #5936 theorem and
unblock #5929.

## Blockers

None for the partial substrate PR. A pre-existing `.claude/CLAUDE.md` worktree
modification was left untouched.
