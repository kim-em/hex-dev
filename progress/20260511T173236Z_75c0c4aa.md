# Work session 75c0c4aa — issue #3520

## Accomplished

- Claimed issue #3520 after human-oversight issues were either blocked or marked for replan.
- Added `ExecutableBadVectorWitness.ProjectedBadVectorSetupBridge` in `HexBerlekampZassenhausMathlib/BadVector.lean`.
- Added a callback constructor from the packaged bridge to the exact `bad_setup_of_projected_not_indicator` shape expected by cap separation.
- Added `BHKS.ExecutableCapSeparationHypotheses.ofProjectedBadVectorSetupBridge` and `projectedRowSpan_eq_trueFactorIndicatorLattice_of_cap_bridge` in `HexBerlekampZassenhausMathlib/TerminationBound.lean`.
- Verified `lake build HexBerlekampZassenhausMathlib.BadVector HexBerlekampZassenhausMathlib.TerminationBound`, `lake build HexBerlekampZassenhausMathlib`, `python3 scripts/check_dag.py`, `git diff --check`, and the targeted banned-marker scan.

## Current frontier

- The cap-separation theorem can now be instantiated from a packaged per-projected-vector bad-vector bridge instead of requiring callers to assemble the callback manually.
- The deeper failed-recovery-to-bad-vector construction remains an explicit upstream obligation.

## Next step

- Use the packaged bridge from the eventual failed-recovery construction that supplies the canonical auxiliary polynomial, local-factor degree positivity, rational coprimality, and resultant divisibility for each projected vector in `L' \ W`.

## Blockers

- None for this issue.
