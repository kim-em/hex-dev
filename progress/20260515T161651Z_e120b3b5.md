# 2026-05-15 16:16 UTC - thirty-second Phase 4/5 checkpoint (e120b3b5)

## Accomplished

Claimed summarize issue #4262 and recorded this checkpoint. Before that, #4227
was claimed from the unclaimed queue but immediately released as stale: its
body still points at closed #4226, while the required fixed-space polynomial
iff now lives in the decomposed #4258 -> #4260 -> #4261 chain. The pre-existing
pod-managed `.claude/CLAUDE.md` modification was left untouched. No source
code, SPEC, top-level PLAN, or AGENTS file was changed.

Since summarize issue #4152 closed at 2026-05-15T06:08:32Z, the merge wave has
kept the same three high-traffic fronts moving: Berlekamp-Zassenhaus executable
and Mathlib coverage, HexBerlekamp Rabin/no-kernel-split algebra, and
HexGramSchmidt/HexLLL boundary and bench cleanup. HexPolyFp Phase 4 also moved
from profiling/report concerns to benchmark-verdict cleanup, which matters
because it gates the HO-45 / HexGfqRing Phase 4 re-validation directive.

### HexBerlekampZassenhaus executable and Mathlib bridges

- The exhaustive and fast-core coverage chain gained more public bridges:
  lifted subset recombination search (#4208), UFD subset-degree and uniqueness
  packaging (#4209, #4210, #4213), recombination product flattening (#4197),
  fallback exclusion (#4215), public first-success/exhaustive membership
  substrate (#4224, #4234), fast-core branch irreducibility (#4232), and
  first-success exhaustive membership (#4234).
- Small-mod singleton work advanced through integer mod-p transport (#4207),
  factor-product bridge material (#4220, #4225), prime-choice provenance and
  monic modular image exposure (#4239), and the composition without an explicit
  `hirr_fModP` premise (#4252). A follow-up still needs to derive the monic
  modular image square-free premise automatically; PR #4259 is open for that
  chain.
- The recursive coverage frontier is #4236. It is claimed and should assemble
  recursive `recombinationSearchModAux` coverage into the public
  `exhaustiveCoreFactorsWithBound` coverage theorem. Downstream BZ Mathlib
  issues #4006/#4008/#3987 remain blocked behind that coverage path.

### HexBerlekamp Rabin and no-kernel-split chain

- The Rabin soundness path landed both directions of the direct/reverse Rabin
  irreducibility bridge (#4237, #4241), plus the fixed-space equation wrapper
  for Berlekamp matrices (#4248).
- The witness-product and compose stack now includes singleton
  irreducibility substrate (#4219), witness product identity (#4214),
  `compose_sub_X` / witness substitution into `xPowSubX 1` (#4201), and the
  compose-form Frobenius identity
  `compose w (FpPoly.linearPow FpPoly.X p) = FpPoly.linearPow w p` (#4229).
- The final matrix bridge has been decomposed into executable column entries
  (#4258, PR #4263 open), the coefficient-vector matrix action identity
  (#4260, blocked on #4258), and the fixed-space polynomial iff (#4261,
  blocked on #4260). #4227 should wait for #4261 before retrying the final
  no-kernel-split irreducibility composition.

### HexPolyFp Phase 4 and HO-45

- HexPolyFp profiling/report attribution landed (#4251): the Phase 4 report
  now names leaf-cost categories, inclusive-cost rankings, profile commands,
  row metadata, and benchmark-target attribution for the relevant input
  families.
- HexPolyFp benchmark verdict cleanup landed (#4256): the noisy/inconclusive
  Frobenius, modular composition, and square-free schedules were retuned, the
  scientific JSONL was regenerated, and the report now records conclusive
  verdict evidence without cap-truncation advisories.
- `scripts/status.py` still reports `HexGfqRing -> Phase 4` blocked on
  `HexPolyFp.done_through >= 4`, so HO-45 is not done merely because the
  report work landed. The next HexPolyFp/HexGfqRing phase-state step should
  confirm the final Phase 4 report state and only then re-bump the dependent
  marker needed by #3945.

### HexGramSchmidt, HexLLL, and HO-43

- The Mathlib-boundary cleanup gained more bridge relocation and packaging:
  the Cramer/Bareiss bridge for scaled Gram-Schmidt coefficients (#4145),
  adjacent-swap pivot bridge relocation (#4190), Cramer/Bareiss case-split
  bridge assembly (#4191), and related HexLLL / oracle protocol cleanup
  (#4186).
- HO-43 is still open as a directive. The key unresolved shape remains
  top-down relocation: consumers must be moved before producers, and the
  size-reduce residual (#3916 / #4157 / #4153 chain) remains the blocker for
  the later `HexGramSchmidt/Int.lean` relocation rounds.
- HO-44 remains gated on finishing HO-43 Round 3; do not remove the orphan
  `bareiss_eq_det` until the `HexGramSchmidt` direct/transitive users are gone.

## Current frontier

`coordination orient` currently shows open directives #3945, #3900, #3899,
#3662, #2637, #2567, #2566, #2565, and #2564; claimed #4236; open PRs #4263,
#4259, #4255, #4189, and #2656; and blocked chains including #4260/#4261,
#4236 -> #4006 -> #4008 -> #3987, and HO-43 residuals.

The visible priority frontier is:

- HO-45 / HexPolyFp Phase 4 gating: #4251 and #4256 landed, but
  `HexGfqRing` still waits on `HexPolyFp.done_through >= 4`; #3945 remains the
  directive to close once the Phase 4 state is revalidated and rolled forward.
- HexBerlekamp matrix bridge: #4258 is in PR, #4260 waits on it, #4261 waits on
  #4260, and #4227 has been marked replan because it should depend on #4261,
  not the superseded #4226 body.
- BZ Mathlib exhaustive coverage: #4236 is claimed and is the immediate
  recursive-coverage blocker for #4006 and the downstream per-branch
  irreducibility chain.
- HO-43 relocation: the top-down relocation directive remains active; Round 2
  / Round 3 residuals are blocked on the size-reduce consumer refactor rather
  than a simple producer move.
- HO-1 / BZ Factorization record: the signed-scalar and multiplicity API work
  in #2637 remains directive-level residual work, separate from the current
  Mathlib coverage chain.

## Quality metrics

- `python3 scripts/status.py` reports ready dispatch targets for Phase 6
  HexArith/HexPoly/HexModArith/HexGF2/HexHensel/HexConway/HexPolyMathlib,
  Phase 5 HexMatrix/HexGramSchmidt/HexPolyZ/HexGfqField/HexGfq/
  HexMatrixMathlib, Phase 4 HexLLL/HexPolyFp/HexModArithMathlib/
  HexGramSchmidtMathlib/HexHenselMathlib, and Phase 1
  HexBerlekampZassenhaus.
- Blocked phase targets remain HexGfqRing, HexBerlekamp, HexPolyZMathlib,
  HexLLLMathlib, HexBerlekampMathlib, HexGF2Mathlib, HexGfqMathlib, and
  HexBerlekampZassenhausMathlib.
- This summary introduces no Lean declarations and does not change the
  repository's existing `sorry`, `axiom`, or `native_decide` profile.

## Next step

Pick #4260 once PR #4263 lands if prioritizing the Berlekamp no-kernel-split
chain, or continue #4236/#4006 if prioritizing the BZ Mathlib exhaustive
coverage chain. For directive work, HO-45 should first settle the
HexPolyFp Phase 4 marker / HexGfqRing re-validation state; HO-43 should not
retry the final Int relocation before the size-reduce residual is unblocked.

## Blockers

No blocker for this summary. Project-level blockers are the explicit dependency
chains above plus the still-open directives.
