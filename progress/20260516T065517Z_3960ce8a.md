# Session 3960ce8a — HexBZ Mathlib candidate divisibility helpers (#4423)

Session: `3960ce8a-8918-4753-8d3e-e5309e966b42`
Issue: `#4423` (partial; decomposed remainder to `#4430`)

## Accomplished

Landed the algebraic helpers underlying #4423 in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `toPolynomial_liftedFactorProduct` — bridges the foldl product
  `liftedFactorProduct d S` to `∏ i ∈ S, toPolynomial (liftedFactor d i)`
  in `Polynomial ℤ` via `Finset.prod_map_toList`.
- `liftedFactorProduct_eq_mul_sdiff_of_subset` — multiplicative splitting
  along `T = S ⊔ (T \ S)` when `S ⊆ T`. Proved by transport to
  `Polynomial ℤ` and `Finset.prod_sdiff`.
- `liftedFactorProduct_union_of_disjoint` — splitting along an explicit
  disjoint union `S ∪ U`.
- `congr_of_reduceModPow_eq` (private) — reverse direction of
  `reduceModPow_eq_of_congr` via transitivity through
  `Hex.ZPoly.congr_reduceModPow`.
- `representsIntegerFactorAtLift_mul_of_monic_core` — under monic core,
  disjoint `S ∪ T` represents `f * g` whenever `S` represents `f` and `T`
  represents `g`. Combines `liftedFactorProduct_union_of_disjoint` with
  `Hex.ZPoly.congr_mul`.

These four public theorems give the multiplicative-product API needed to
package "T is a union of representing subsets" facts into a single
`RepresentsIntegerFactorAtLift` statement, ready for the multi-factor
recovery argument in the remaining piece of #4423.

## Current frontier

The original #4423 deliverable shape (`factor ∣ recombinationCandidate d T`
from `S ⊆ T` alone) is not provable as stated. The conclusion requires the
structural fact `T = ⊔_g S_g` over the irreducible factors `g` of
`recombinationCandidate d T`, which in turn requires the partition's
`exists_subset` plus `exactQuotient?` information to set up the
degree-counting argument. The minimum-viable working version takes
`hpartition`, `hTJ`, `hquot`, plus monicness of the lifted factors as
additional hypotheses.

Spec for the remaining theorem and its proof outline (UFD case split +
degree-counting contradiction) are filed as `#4430` and depend on this
partial PR via the helpers landed here.

## Next step

Issue `#4430` — implement the main divisibility theorem
`representedFactor_dvd_recombinationCandidate_of_subset` under partition +
exactQuotient hypotheses, using the helpers landed here plus a
degree-counting argument for `T = ⊔_g S_g`.

## Blockers

None for the helpers. `#4430` (the residual main theorem) is filed with
`depends-on: #4423` so the planner can pick it up after this partial PR
merges.
