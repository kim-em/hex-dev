# Session 9d227a16 - issue #5428

## Accomplished

- Claimed issue #5428 after confirming all open directive issues were blocked.
- Added `bhksIndicatorSelectedFactorsArray` in `HexBerlekampZassenhaus/Basic.lean`
  as the named executable fold for selected lifted factors.
- Added two public selector-success theorems:
  - `bhksIndicatorSelectedFactors_eq_some_selectedArray_of_getD`
  - `bhksIndicatorSelectedFactors_eq_some_of_getD`
- Verified the Mathlib-free target with `lake build HexBerlekampZassenhaus`.
- Verified `python3 scripts/check_dag.py`, `git diff --check`, and the added-code
  grep for `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
- Confirmed quality metrics match the issue baseline: `sorry` 85, `axiom` 9,
  `native_decide` 1.

## Current frontier

The selector API is packaged for downstream BHKS recovery proofs without
changing executable recovery behavior.

## Next step

Use the new selector-success theorem from the bridge-side recovery constructors
or follow-up issue #5429.

## Blockers

- `lake build HexBerlekampZassenhausMathlib.Recovery` currently fails before
  reaching `Recovery.lean`, in `HexBerlekampZassenhausMathlib.Basic`, with
  existing timeout and unknown-constant errors.
- `.claude/CLAUDE.md` was dirty at session start and was left unstaged.
