# 2026-05-19T00:04Z — feature session a39874e0

## Accomplished

Resolved issue #5173: added the two missing `@[simp]` representative
wrappers `repr_neg` and `repr_sub` in `HexGfqField/Operations.lean`,
placed between `repr_mul` and `repr_pow` as the issue specified.
Both proofs are `rfl`, matching the existing `repr_add` / `repr_mul`
shape. This brings the `repr_*` cluster to API parity with the
already-complete `toQuotient_*` cluster on the additive-group
fragment (`zero / one / add / mul / neg / sub / pow`).

## Current frontier

`HexGfqField/Operations.lean` `repr_*` row now covers
`zero / one / add / mul / neg / sub / pow`. Remaining asymmetric
corners against the `toQuotient_*` row are `nsmul`, `intCast`,
`zsmul` — none of which were in scope for this issue.

## Next step

PR up; awaiting CI + auto-merge. No follow-up needed for this issue.

## Blockers

None.
