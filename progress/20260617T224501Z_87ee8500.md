# Progress — 2026-06-17 (session 87ee8500, /work)

## Accomplished

Sanity-checked and released two BHKS-path issues for replan:

- #7847: the requested aggregate-tail route is not currently compatible with
  the existing `trueFactorCLDVector` norm consumers. The one-cut aggregate tail
  is not the same object as the row-combination vector whose lattice membership
  and tight norm are consumed downstream. Posted a corrected blocker comment and
  marked the issue `replan`.
- #6672: capstone remains blocked on the unconditional fast-BHKS raw
  irreducibility substrate; marked `replan` with a pointer to the #7847
  boundary problem.

Completed #7838 in `HexGFq`:

- Added committed `Conway.PackedGF2Entry` instances for degrees `n = 2..6`.
- Used direct Conway lower words: `0x3` for `n = 2,3,4`, `0x5` for `n = 5`,
  and `0x1B` for `n = 6`.
- Backed the new small-degree packed irreducibility witnesses with executable
  `GF2Poly` Rabin certificates; reused the existing degree-4 common
  irreducibility theorem.
- Extended `HexGFq/Conformance.lean` with guards for all new committed entries
  and a nontrivial `GF2q 4` bridge covering `modulus`, `ofWord`, `repr`,
  `reprFpPoly`, and `toGFq`.

## Current frontier

`GF2q n` is now available for the committed binary Conway entries `n = 1..6`.
The generic ergonomic `GFq` constructor layer from #7839 is still unclaimed and
can build on this packed-entry coverage.

## Next step

Open a PR for #7838. After that, #7839 is the natural next HexGFq Phase 6 item.
The BHKS aggregate-tail path needs replanning around the object boundary between
aggregate support tails and the row-combination lattice vector.

## Blockers

No blockers for #7838.

The worktree still has a pre-existing unrelated modification to
`.claude/CLAUDE.md`; it was not touched or staged.

Verification for #7838:

- `lake build HexGFq.Basic`
- `lake build HexGFq.Conformance`
- `lake build HexGFq`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over `HexGFq/Basic.lean` and
  `HexGFq/Conformance.lean`
