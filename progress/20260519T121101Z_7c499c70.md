# Session 7c499c70 - issue #5454

## Accomplished

- Claimed feature issue #5454.
- Added `Polynomial.schurReflectedAtRootForm`, the one-root Schur reflection
  form that keeps all other root factors unchanged.
- Added
  `Polynomial.mahlerMeasure_derivative_le_schurReflectedAtRootForm_derivative_of_monotoneOn`
  in `HexPolyZMathlib/RobinsonForm.lean`.
- The new theorem records a non-circular formal route for the
  de Bruijn-Springer/Boyd derivative comparison: prove the one-factor Schur
  path monotonicity for the root-deletion context, then reflect exterior roots
  one at a time toward the existing Robinson-form derivative equality.
- Verified:
  - `lake build HexPolyZMathlib.RobinsonForm`
  - `lake build HexPolyZMathlib`
  - `python3 scripts/oracle/mahler_schur_counterexample.py`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -- HexPolyZMathlib/RobinsonForm.lean | rg -n '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true`

## Current frontier

The actual analytic monotonicity theorem for
`derivativeMahlerAlongLinearFactor` along `schurRootPath` remains unproved.
The new theorem isolates the exact per-root hypothesis needed to use it inside
the polynomial root factorization without assuming the downstream Mahler
derivative bound.

## Next step

Prove the Schur-path monotonicity hypothesis for
`C p.leadingCoeff * ((p.roots.erase α).map fun β => X - C β).prod` when
`1 < ‖α‖`, or build an induction that applies the new reflection step to every
exterior root once that monotonicity ingredient is available.

## Blockers

- The source analytic monotonicity/majorization theorem is still absent from
  Mathlib and the local project.
