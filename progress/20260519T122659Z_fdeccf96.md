## Accomplished

- Claimed issue `#5438` after the directive queue was blocked, the Conway
  issues raced to other agents, and `#4334` was skipped for non-quiet
  benchmark hardware.
- Added
  `factorFast_ne_none_of_mignottePrecisionCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The new wrapper composes the existing Mignotte capstone with:
  - `ForwardRecoveryInputs.canonicalSupportIndicators_nondegenerate`
  - `ForwardRecoveryInputs.bhksIndicatorSelectedFactors_expectedIndicatorArrayOfSupports`
  - `ForwardRecoveryInputs.selectedFactorArraysOfSupports`
- Kept executable recovery definitions unchanged.
- Verified hygiene:
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -- HexBerlekampZassenhausMathlib/Recovery.lean | rg -n '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` produced no matches.
- Quality metrics remained unchanged: `sorry` 85, `axiom` 9,
  `native_decide` 1.

## Current frontier

- The wrapper now lets HO-4 call sites supply canonical support partition
  shape facts instead of raw `nondegenerate`, `selectedFactors`, and
  `hselected` arguments.
- The caller still must supply the mathematical obligations: B7 lattice
  equality, expected true-factor package, per-class Mignotte product
  congruence, factorFast normalization/cap hypotheses, and support-class
  nonempty/bounds facts.

## Next step

- Use the new canonical-support capstone at the next HO-4 call site that has
  the lattice equality and expected true-factor package available.

## Blockers

- `lake build HexBerlekampZassenhausMathlib.Recovery` did not reach
  `Recovery.lean`; it failed first while rebuilding
  `HexBerlekampZassenhausMathlib.Basic` with pre-existing timeout and unknown
  constant errors.
- `lake env lean HexBerlekampZassenhausMathlib/Recovery.lean` is blocked
  because that failed rebuild left
  `.lake/build/lib/lean/HexBerlekampZassenhausMathlib/Basic.olean` missing.
- The worktree has a pre-existing local `.claude/CLAUDE.md` modification; this
  session did not touch it.
