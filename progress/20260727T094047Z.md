# Interval structural scaling vertical

## Accomplished

- Added transparent, Mathlib-free centered-checker workload families which
  independently vary dead program nodes, adjacent fact references, far fact
  references, and result-irrelevant source facts.
- Gave every committed workload an exact closed-form lookup budget and an
  independently recomputed five-component lookup count; conformance checks
  cover exact acceptance, one-step-short rejection, dimensions, and maximum
  ladder-point component counts.
- Added compiled construction/checker probes, fresh whole-module replay and
  forced-kernel-WHNF probes, and an adaptive sweep harness with provenance,
  artifact hashes, paired baselines, RSS, stable checksums, and exact formula
  validation.
- Extended the existing single CI job and Lake targets without introducing a
  new workflow or Mathlib dependency.
- Passed focused builds, DAG and line-count checks, the Mathlib-free bench
  audit, Python compilation, whitespace checks, and a complete 105-case smoke
  sweep with all 21 WHNF points.
- Corrected the fact-cap control to keep the selected result in range while a
  trailing fact crosses the cap, calibrated every timing mode independently,
  balanced sample order across each run, normalized WHNF setup across workload
  families, and added the missing 2,000-node dimension guard after an
  independent benchmark-integrity audit.
- Made round-limit calibration return the last repeat count actually measured,
  rather than an untried next proposal.

## Current frontier

The implementation is ready to be committed and replayed onto the
squash-merged centered-product main commit. The smoke sweep is diagnostic only;
the clean five-sample evidence run must happen after that rebase.

## Next step

Commit the implementation, rebase its single new commit onto `origin/main`, run
the full repository build, then collect and interpret the clean scale evidence
before updating the interval SPEC and report.

## Blockers

None.
