# 2026-05-21T03:00Z — partial HexPolyZMathlib Grace-Walsh-Szego source theorem

## Accomplished

Claimed #5701 after #5709 was already claimed by another agent. Confirmed that
`HexPolyZMathlib/SchurSzego.lean` already has the explicit
`graceWalshSzegoZeroControlAtDegree` source-hypothesis surface plus the
root-count and `rootsRadiusProduct` wrappers, and that Mathlib in this checkout
does not expose a ready-made Grace-Walsh-Szego/de Bruijn-Springer composition
theorem to apply directly.

Added the no-sorry degree-zero base case
`Polynomial.graceWalshSzegoZeroControlAtDegree_zero`. The proof reduces the
Schmeisser composition to a constant polynomial and uses `roots_C`.

Verified:

- `lake build HexPolyZMathlib.SchurSzego`
- `lake build HexPolyZMathlib`
- `python3 scripts/oracle/mahler_schur_counterexample.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- targeted banned-token scan on the `HexPolyZMathlib/SchurSzego.lean` diff

## Current frontier

#5701 remains open at the hard analytic all-degree theorem:
`Polynomial.graceWalshSzegoZeroControlAtDegree_proven (n)`. The local API
surface is ready to consume it, but the de Bruijn-Springer / Grace-Walsh-Szego
source proof itself is still missing beyond the degree-zero case.

## Next step

Prove the next nontrivial source-theorem slice, likely degree one or a
circular-domain source theorem strong enough to imply the closed-radius
`Polynomial.roots` multiset count statement.

## Blockers

No technical blocker for the committed base case. The remaining all-degree
theorem is substantially larger than a local wrapper change and requires the
actual analytic composition-polynomial formalization.
