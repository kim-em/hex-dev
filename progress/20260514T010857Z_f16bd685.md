# Session f16bd685 — decompose #3873 into slow/fast product lemmas plus assembly

## Accomplished

- Claimed #3873 ("HexBZ core: assemble factor_product_of_bound from bounded product bridges").
- Orienting on `HexBerlekampZassenhaus/Basic.lean`, found that the bridges added
  by PR #3884 still require per-factor canonicality hypotheses
  (`normalizeFactorSign factor = factor` and
  `shouldRecordPolynomialFactor factor = true`) for every emitted factor of
  both `factorSlowFactorsWithBound` and `factorFastFactorsWithBound`. The
  planner's framing ("an assembly proof once the dependencies are in place")
  understated the residual work: per-branch canonicality across X-power,
  repeatedPart, singleton squareFreeCore, quadratic root factors, exhaustive
  recombination output, and BHKS recovery output. The constant-core branches
  where `squareFreeCore = 1` additionally require the more flexible
  `factorizationOfFactors_product_of_filtered_product` bridge instead of the
  canonicality-based one.
- Decomposed into three follow-up issues:
  - #3889 `HexBZ core: prove factorSlowWithBound_product unconditionally`
  - #3891 `HexBZ core: prove factorFastWithBound_product_of_some`
  - #3892 `HexBZ core: assemble factor_product_of_bound from slow and fast
    product lemmas` (depends on #3889 and #3891)
- Left the `Decomposed into #3889, #3891, #3892` breadcrumb on #3873 and
  `coordination skip`ped it for planner triage.

## Current frontier

The slow-path branch facts (#3889) and fast-path branch facts (#3891) are
independent and can be worked in parallel. Once both merge, #3892 is a
five-line case split using the new lemmas plus the bridges already in place.

## Next step

Workers should claim #3889 and #3891 independently. #3892 will unblock once
both merge.

## Blockers

None for this session. The pre-existing `.claude/CLAUDE.md` modification
from pod bootstrap remained unstaged.
