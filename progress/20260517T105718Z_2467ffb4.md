# 20260517T105718Z — feature session 2467ffb4 (#4652)

Session UUID prefix: 2467ffb4. Issue: #4652 — HO-1 substrate (#4638 sub):
scaled primitive recombination recovery.

Also skipped #4334 once for the 29th time: carica load 12.89/13.47/13.67 on 24
CPUs is well above the documented `<2` quiet-host precondition; no probe
attempted.

## Accomplished

Added the corrected primitive non-monic recovery substrate to
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `scaledRecombinationCandidate core d S` — centred lift of
  `scaledLiftedFactorProduct core d S` followed by `primitivePart` and
  `normalizeFactorSign`. The defining product diverges from
  `recombinationCandidate` exactly when `lc(core) ≠ 1`, which is the regime
  the substrate is intended for.
- `scaledRecombinationCandidate_eq_factor_of_recovery` — recovery theorem
  taking `core ≠ 0`, `factor ∣ core`, `content factor = 1`,
  `normalizeFactorSign factor = factor`, the Hensel representation, and
  Mignotte precision; concludes `scaledRecombinationCandidate core d S =
  factor`. Proved directly from
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery` and
  `primitivePart_eq_self_of_primitive`. No `Monic core`, no
  `scaledLiftedFactorProduct = liftedFactorProduct` collapse.
- `scaledRecombinationCandidate_eq_factor_of_henselSubsetCorrespondence` —
  Hensel-correspondence wrapper mirroring the existing monic-core wrapper,
  so #4648 can consume it at the public boundary.

The existing monic recovery path
(`recombinationCandidate_eq_factor_of_recovery_of_monic_core`,
`recombinationCandidate_eq_factor_of_recovery`,
`recombinationCandidate_eq_factor_of_henselSubsetCorrespondence`) is
untouched.

`lake build HexBerlekampZassenhausMathlib.Basic` and
`lake build HexBerlekampZassenhausMathlib` both pass. `python3
scripts/check_dag.py` passes. `git diff --check` is clean. `rg
"sorry|axiom|native_decide|TODO|FIXME"` count is unchanged (10 matches in
both `origin/main` and the working copy) — all pre-existing.

## Current frontier

The downstream sub-chain (#4644 → #4646 → #4647 → #4648) is unblocked at
#4644, which uses the new `scaledRecombinationCandidate_eq_factor_of_recovery`
indirectly via a primitive-factor packaging step (`representsIntegerFactorAtLift_primitive`).

The new helper deliberately does *not* identify `recombinationCandidate` (the
unscaled executable surface) with `factor` for primitive non-monic cores —
the original false claim documented on #4643. Downstream consumers that need
the executable surface will either thread the scaled candidate or add an
explicit `scaledRecombinationCandidate = recombinationCandidate ∨ associated`
bridge; this is left to #4648 since the precise shape depends on what the
public wrapper exposes.

## Next step

Planner picks up #4644 once `check-blocked` unblocks it. Worker landing
#4644 calls `scaledRecombinationCandidate_eq_factor_of_recovery` from the new
substrate when needed for the represented-factor primitive package.

## Blockers

None for the substrate. The downstream chain is gated on the planner-merge
cycle, not on technical obstacles.
