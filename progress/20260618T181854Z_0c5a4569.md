# Progress — 2026-06-18 (session 0c5a4569, /work → /feature #7961)

## Accomplished

`@[grind =]` automation pass over `HexGFq/Basic.lean` (#7961).

Converted 50 equation-shaped public characterising lemmas from `@[simp]`
to `@[simp, grind =]`, matching the existing convention in the sibling
`HexGFqField/Basic.lean`. These are the `rfl`-style `repr_*` / `ofPoly_*`
/ `modulus_*` / `toGFq_*` rewrites across the `GFq`, `GFqC`, and `GF2q`
namespaces — exactly the inline `@[simp] theorem` forms (51 of them).

Two principled exclusions:

- `inv_zero` (`(0 : GFq p n h)⁻¹ = 0`): `@[grind =]` is rejected here —
  the LHS gives grind no instantiable pattern (`n`, `h` appear only in
  the type, not the term), so it cannot recover the theorem parameters.
  Left as `@[simp]`-only.
- The three Prop-shaped `@[simp]` facts (`modulus_nonconstant`,
  `degree_pos`, `degree_lt_word`) are not equations, so out of the
  issue's "equation-shaped" scope. Left untouched.

## Justification

Per the issue's premise note (and the same fallback #7955/#7961 permit):
there are 0 `by grind` sites inside `HexGFq/Basic.lean`, so no in-file
proof shortens. The payoff is **downstream `grind` availability** —
these equational rewrites of the canonical-representative API are now
visible to `grind` calls in consuming layers and the `*Mathlib` bridges,
consistent with how `HexGFqField/Basic.lean` already exposes its
`repr_*` lemmas.

## Verification

- `lake build HexGFq` → `Build completed successfully (524 jobs)`.
  `HexGFq.Basic` builds in 3.2s; no grind-loop, no slowdown, no new
  warnings on the file.
- The two `simpa` linter warnings in the build log are in
  `HexGFqRing/Operations.lean` (untouched, pre-existing).
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `python3 scripts/check_dag.py` → exit 0.
- No `libraries.yml` bump; no `*Mathlib` bridge touched; no new imports.

## Current frontier

`HexGFq/Basic.lean` now exposes its representative-API rewrites to
`grind`. Sibling Phase-6 grind passes remain: `HexGF2` (#7963).

## Next step

#7963 (HexGF2 grind automation) is the directly analogous remaining pass.

## Blockers

None.
