## Accomplished

Claimed and completed #3007, the RREF echelon span solver reconstruction helper.

In `HexMatrix/RREF.lean`, factored the local pivot-coordinate vector from
`IsEchelonForm.spanCoeffs` into the private helper
`IsEchelonForm.echelonCoeffs`.

Added private RREF reconstruction lemmas:

- `pivot_column_entry`, showing each RREF pivot column is the corresponding
  basis column, using `pivot_one`, `above_pivot_zero`, `below_pivot_zero`, and
  `zero_row`.
- `rowCombination_pivotCoeff`, reading a row-combination coefficient back from
  the pivot coordinate.
- `rowCombination_eq_of_coeffs_eq_on_rank`, showing row combinations agree when
  coefficients agree on nonzero echelon rows.
- `rowCombination_echelonCoeffs_of_rowCombination`, proving the pivot-coordinate
  recipe reconstructs any vector already in the row span of `D.echelon`.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`
  reported only the pre-existing sorries at lines 116, 225, 600, and 605.

## Current frontier

The RREF-local reconstruction ingredient for #3004 is now available. The helper
is intentionally private and not yet wired into the public span membership
wrapper.

## Next step

Pick up #3008 to connect this helper with the existing row-span transport and
finish the public `spanContains`/`spanCoeffs` completeness path.

## Blockers

None.
