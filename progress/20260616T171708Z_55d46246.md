# Work: true-factor CLD lattice vector surface review (#7626)

## Accomplished

- Claimed #7626 as a `review` worker issue after the unclaimed directive #2564
  was reported blocked by `coordination claim`.
- Reviewed `HexBerlekampZassenhausMathlib/Lattice.lean` against the executable
  BHKS basis block layout in `HexBerlekampZassenhaus/Basic.lean`.
- Confirmed that `trueFactorRowCoeffs` selects exactly the top BHKS rows, that
  `TrueFactorCLDVectorData.project_eq` and `.coeff_eq` use the correct
  projected and coefficient-column offsets, and that `trueFactorCLDVector` has
  structural row-lattice membership via `Hex.Matrix.memLattice`.
- Posted the review verdict on #7626:
  https://github.com/kim-em/hex/issues/7626#issuecomment-4721399070
- Verified `lake build HexBerlekampZassenhausMathlib.Lattice`.

## Current frontier

The reviewed surface is sound for downstream use. #7616 still owns producing a
canonical `TrueFactorCLDVectorData` certificate for the executable
`bhksLatticeBasis`, and #7620 should consume that certificate rather than
assuming the coordinate identities without it.

## Next step

Downstream workers can proceed with #7616/#7620: prove the canonical producer,
then use its projected indicator and CLD-coordinate fields in the cut-projection
assembly.

## Blockers

None for this review. The build emitted existing dependency warnings in
`HexGramSchmidtMathlib` and `HexLLLMathlib`, but no target failure.
