# 20260602T150135Z 5d88320c — Issue #6354

## Accomplished

- Claimed feature issue #6354.
- Added successful-branch HO-1 constructors in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  `liftedFactorSubsetPartition_of_choosePrimeData_success_descent` and
  `slowPathHenselSubstrate_of_choosePrimeData_success_descent`.
- Documented the older `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`
  route as the fallback surface and pointed explicit descent/forward-transport
  callers at the successful-branch constructor.

## Current frontier

The monic `choosePrimeData? = some ...` slow-path substrate can now package its
`corr` field and the partition's embedded correspondence through the
non-circular descent/forward-transport path when callers have those primitive
lifted-side hypotheses. The parametric fallback and the partition-cover
analytic helper remain in place for callers that cannot yet provide the
successful-branch package.

## Next step

Successor work can either instantiate the new successful substrate from the
existing concrete Hensel-lift invariant surfaces, or add the corresponding
non-monic `toMonicPrimeData?` sibling once a matching descent/forward package
is available for that branch.

## Blockers

- None for this successful-branch packaging layer.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — passed.
- `lake build HexBerlekampZassenhausMathlib` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Diff-only forbidden-token scan for
  `HexBerlekampZassenhausMathlib/Basic.lean` — no matches.
