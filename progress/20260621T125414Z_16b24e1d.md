# Fast-BHKS multi-factor core producer (steps 2-3 of #8282) — diagnosed a missing prerequisite

Session type: one-shot `/once` on #8282. UTC 2026-06-21T12:54.

## Accomplished

Landed the separation-free fast-core multi-factor irreducibility producer at the
loop **success** precision `k'`, sorry-free, and isolated the genuine residual
blocking steps 2-3.

`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`:
`factorFastCore_irreducible_of_liftedTrueSupport`. From a successful
`factorFastCoreWithBound core B primeData k fuel = some coreFactors` (plus the
core facts `core ≠ 0`, positive leading coeff / degree, `Primitive`, `Squarefree`,
`coeff 0 ≠ 0`, `2 ≤ degree`, `toMonicPrimeData? core = some primeData`) it
concludes `∀ factor ∈ coreFactors.toList, Hex.ZPoly.Irreducible factor`,
discharging **every** BHKS leaf of the keystone endpoint
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift` (`:807`)
from core facts:

- success precision `k'` + floor `cldCoeffFloor core ≤ k'` from
  `factorFastCoreWithBound_some_indicatorCandidates`;
- `hbound` (monic Mignotte) from `BHKS.defaultFactorCoeffBound_toMonic_le_cldCoeffFloor`
  (★) + `precisionForCoeffBound_spec`;
- `hcore_bound` from `two_mul_defaultFactorCoeffBound_core_lt_pow_of_cldCoeffFloor_le` (★★);
- `lift` family from the keystone `recoveredLiftOfLiftedTrueSupport`;
- `hf_lc`/`hfactor_monic`/`hp`/`hk`/`hsep`/`hthr`/`hfac` from the keystone field
  projections, `toMonicLiftData_two_le_p`/`_one_lt_modulus`,
  `two_mul_bhksCoeffBound_lt_pow_of_cldCoeffFloor_le`,
  `bhksCoeffCutThreshold_le_of_cldCoeffFloor_le`, and
  `toMonicLiftData_liftedFactor_hensel_semantics`;
- `hbasis` from `Hex.bhksLatticeBasis_independent`;
- `hpartition` from `factorFastCoreWithBound_some_partition_eq_normalizedFactors_card` (`:480`).

Only **one** hypothesis remains: `hsize`, the equality
`coreFactors.size = (monic-basis equivalence-class indicator count)`.

`lake build HexBerlekampZassenhausMathlib` green (3779 jobs). No new
`sorry`/`axiom`/`native_decide`; route stays separation-free.

## Current frontier — the premise gap blocking the headline `sorry`

The issue's step-2 recipe lists `hsize ← factorFastCoreWithBound_some_size_eq_indicators`,
but that producer gives the size over the **core** basis
`bhksLatticeBasis core …` (first argument `core`), while the separation-free
keystone `recoveredLiftOfLiftedTrueSupport` recovers over the **monic** basis
`bhksLatticeBasis (toMonic core).monic …`. Feeding `:807` with the keystone lift
family pins its `L`, hence its `hsize`, to the monic basis. So the obligation is

    coreFactors.size = (bhksEquivalenceClassIndicators (monic-basis projected rows)).size

while the executable only delivers `coreFactors.size = (core-basis indicators).size`.

`bhksEquivalenceClassIndicators` size is genuinely basis-dependent:
`bhksEquivalenceClassIndicators_size_eq` reduces it to the rref column-signature
partition length of the projected rows, and `cldCoeffs f` (and `bhksCutThresholds f`)
depend on `f`. There is **no** core ↔ monic indicator-size bridge in the tree
(searched: no `cldCoeffs_toMonic`, no `bhksLatticeBasis … = bhksLatticeBasis (toMonic …)`,
no projected-rows / indicator-size equality). This is a real missing prerequisite,
the un-monic-recovery analogue of how (★★)/#8284 had to land before steps 2-3.

`factorFastCore_irreducible_of_monicRecoveredLift` (`:854`, the existing scaffold)
also takes its monic-basis `hsize` as a hypothesis, confirming the gap is not an
artifact of the keystone route.

## Next step

File/land the bridge prerequisite, then close steps 2-3:

- **Bridge:** `(bhksEquivalenceClassIndicators (bhksProjectedRows (bhksLatticeBasis (toMonic core).monic …) _)).size = (bhksEquivalenceClassIndicators (bhksProjectedRows (bhksLatticeBasis core …) _)).size` at `d = toMonicLiftData core k' primeData`. True (both count the irreducible-factor classes) but needs CLD monic-covariance of the projected rref column signatures. With it, `hsize` follows from `factorFastCoreWithBound_some_size_eq_indicators` and `factorFastCore_irreducible_of_liftedTrueSupport` closes unconditionally.
- **Step 2 (raw):** wrap the core producer into a `factorFastFactorsWithBound f B = some rawFactors` multi-factor producer (mirror `rawIrreducible_of_cut`'s raw↔core reassembly), discharging the branch-selection hypotheses, and relate the fast branch's `choosePrimeData?` primeData to `toMonicPrimeData?`.
- **Step 3 (dispatch):** assemble `h_raw` of `factor_entries_irreducible` (Basic.lean:443). The other arms are already unconditional: constant `factorFastFactorsWithBound_raw_irreducible_of_constant`, small-mod-singleton `…_of_smallModSingleton`, quadratic `…_of_quadratic`, and the two slow disjuncts `slowModularRaw_irreducible_of_fast_none` / `factorSlowTrialFactorsWithBound_factor_irreducible_of_fast_none`. Then `factor_irreducible_of_nonUnit f := factor_entries_irreducible f h_raw` (FactorSoundness.lean:18-20), adding `import …PartitionRefinement` to `FactorSoundness`.

## Blockers

The headline `sorry` (`factor_irreducible_of_nonUnit`, FactorSoundness.lean:20)
cannot close without the core ↔ monic indicator-size bridge above; it is not in
the issue's leaf set. No `sorry`/`axiom` introduced. Partial PR lands the core
producer modulo `hsize`.
