## Accomplished

Review session for #4983 — audit of the four landed `_of_bound` siblings
at the IntReductionMod cap layer.

* Reviewed canonical `_of_bound` exemplars in
  `HexBerlekampZassenhausMathlib/Basic.lean` at lines 1834
  (`centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision_of_bound`),
  1889
  (`existsUnique_recoveringLiftedFactorSubset_of_henselSubsetCorrespondence_of_bound`),
  and 15417
  (`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`).
* Audited four entries in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  against three consistency invariants (docstring framing, parameter
  ordering, wrapper rewiring):
  1. `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
     (sibling `:3114`, wrapper `:3360`)
  2. `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`
     (sibling `:3449`, wrapper `:3665`)
  3. `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
     (sibling `:3809`, wrapper `:3969`)
  4. `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`
     (sibling `:4032`, wrapper `:4213`)
  The fifth entry listed in the issue body, #4976's public umbrella
  `_of_bound` sibling, is still open and not yet in tree, so it is
  excluded from this audit.
* Posted audit comment at https://github.com/kim-em/hex/issues/4983#issuecomment-4477011411.
* Filed follow-up feature #4984 to land the textual docstring
  normalisation (four-entry `sibling/companion` → `variant` framing fix,
  plus three-wrapper `Thin wrapper over ..._of_bound` addendum to match
  the canonical Basic.lean idiom at `:1856` / `:1922`).

## Audit findings

* **Deliverable 2 (parameter ordering): clean.** All four entries place
  the abstract block `(B' : Nat) (hcore_lc_le ...) (hvalid ...)
  (hprecision : 2 * B' < d.p ^ d.k)` at the syntactic position of the
  original `hprecision` binder, with correct intra-block order.
* **Deliverable 3 (wrapper rewiring): clean modulo a minor stylistic
  drift.** All four wrappers instantiate `B' := defaultFactorCoeffBound
  squareFreeCore`, discharge `hvalid` via `defaultFactorCoeffBound_valid
  core hcore_ne` directly, and forward `hprecision` verbatim. The
  `hcore_lc_le` discharge uses the canonical named `hcore_dvd_self`
  pattern in wrappers 2/3/4; wrapper 1 (`_aux` at `:3417-3427`) uses
  `(dvd_refl _)` inline. Substantively equivalent, covered by the
  "allowable variation" clause.
* **Deliverable 1 (docstring framing): two consistent deviations
  across the cascade.** Opening phrase: all four entries use
  `Abstract-bound sibling of` / `Abstract-bound _of_bound companion to`
  instead of the canonical `Abstract-bound variant of`. Wrapper
  addendum: only wrapper 2 carries the `Thin wrapper over
  ..._of_bound` addendum that the canonical Basic.lean wrappers (e.g.
  `:1856-1864`, `:1922-1927`) all carry; wrappers 1, 3, 4 lack it.
  Both deviations are pure textual normalisation, deferred to #4984.

## Current frontier

Cap-layer audit at IntReductionMod complete. The next layer
(monic outer `_of_bound` siblings in Basic.lean, e.g.
`recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition_of_bound`
at #4934) can begin its mechanical extension once #4984 lands the
textual cleanup.

## Next step

* #4984 — textual docstring normalisation pass (≤ 40 lines, all in
  `IntReductionMod.lean`, no proof bodies touched).
* Once #4976 lands the public umbrella `_of_bound` sibling, a small
  delta audit on that one entry against the same three invariants.

## Blockers

None for this session's deliverable. Wider cascade work has the
directive-blocked items per #4880, but those are out of scope for
this audit.
