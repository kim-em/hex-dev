# 20260504T100747Z — feature session f70f96f5

Issue: #2070 — HexPolyZMathlib: write Phase 3 Conformance.lean and bump
done_through to 3.

## Accomplished

- Added `HexPolyZMathlib/Conformance.lean` mirroring the established
  `HexPolyMathlib/Conformance.lean` pattern: `noncomputable section`,
  private `Hex.ZPoly` and `Polynomial ℤ` fixtures, `example := named_lemma`
  proofs against the simp-bridge surface (`toPolynomial`,
  `ofPolynomial`, `equiv`, `equiv.symm`, `coeff_toPolynomial`,
  `toPolynomial_zero`, `toPolynomial_C`, `toPolynomial_add`,
  `toPolynomial_mul`, `toPolynomial_ofPolynomial`,
  `ofPolynomial_toPolynomial`, `equiv_apply`, `equiv_symm_apply`).
- Module docstring declares `Oracle: none`, `Mode: always`, plus the
  Covered-operations / properties / edge-cases lists per
  `SPEC/testing.md` § per-library module contract.
- ≥3 cases per advertised operation (typical / edge / adversarial),
  with typical sizes near the upper end of the `core` profile band
  (degree 9 for the typical `ZPoly` / `Polynomial ℤ` round-trip
  fixtures; degree 8 for the multiplication factor pair).
- Wired the new module via `import HexPolyZMathlib.Conformance` in
  `HexPolyZMathlib.lean`.
- Bumped `libraries.yml` `HexPolyZMathlib.done_through` from `2` to `3`.

## Verification

- `lake build HexPolyZMathlib` green; `HexPolyZMathlib.Conformance`
  elaborates in 2.4s.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexPolyZMathlib/Conformance.lean`
  empty.

## Current frontier

PR posted; no follow-up expected from this session.

## Next step

Mignotte-bound oracle / numerical cross-check for the real-valued
`mignotte_bound` would be a separate Phase 3 follow-up if needed.
`HexPolyZ` (non-Mathlib) Phase 3 conformance is its own track.

## Blockers

None.
