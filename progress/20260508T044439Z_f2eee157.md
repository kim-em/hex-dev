## Accomplished

- Claimed #2554 and added the row-pivoted Bareiss invariant in
  `HexMatrixMathlib/Determinant/Bareiss.lean`.
- Packaged the invariant existentially over a logical source matrix, with the
  row-swap sign relation back to the original source and the existing
  `BareissNoPivotInvariant` for the current state.
- Proved initial, regular no-swap, regular row-swap, and recursive
  `pivotLoop` preservation for paths whose final `singularStep = none`.
- Verified `lake build HexMatrixMathlib.Determinant.Bareiss`,
  `lake build HexMatrixMathlib.Determinant`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token scan of the edited Bareiss file.

## Current frontier

- The row-pivoted regular-loop invariant is available for downstream Bareiss
  determinant packaging.
- The top-level executable theorem `Hex.Matrix.bareiss_eq_det` still has its
  pre-existing `sorry` in `HexMatrix/Bareiss.lean`; this session did not touch
  that executable file.

## Next step

- Use `pivotLoop_invariant_of_singularStep_eq_none` to prove the final public
  row-pivoted Bareiss determinant capstone, including final-state determinant
  extraction and singular-case packaging.

## Blockers

- None for the invariant slice.
