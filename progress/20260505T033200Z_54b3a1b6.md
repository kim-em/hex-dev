# Work session: HexPolyFp.Frobenius successor step lemma

## Accomplished

- Claimed #2233 (label: `feature`).
- In `HexPolyFp/Frobenius.lean`, proved
  `frobeniusXPowMod_succ` and the supporting machinery:
  - `powLinear` (private linear-fold polynomial power) plus
    `powLinear_succ`, `powLinear_succ_left`, `powLinear_add`,
    `powLinear_double`, `powLinear_double_add_one`,
    `powLinear_powLinear_mul`.
  - `mod_powLinear_mod_eq` — reducing the base modulo `f` does not
    change `powLinear` modulo `f`.
  - `powModMonicAux_zero` and `powModMonicAux_succ_eq` — definitional
    unfolding lemmas for the well-founded `powModMonicAux`.
  - `modByMonic_eq_mod_swap` — bridge between `FpPoly.modByMonic` and
    `% f`.
  - `powModMonicAux_mod_eq` — loop invariant
    `(powModMonicAux f hmonic k base acc) % f = (acc * powLinear base k) % f`,
    proved by strong induction on the fuel.
  - `powModMonicAux_pos_self_mod` and `powModMonic_pos_self_mod` —
    for positive fuel/exponent, the auxiliary loop's output is
    already reduced mod `f`.
  - `powModMonic_mod_eq` — the characterization
    `(powModMonic base f hmonic n) % f = (powLinear base n) % f`.
- Final theorem combines reducedness on both sides with the bridge
  lemma to derive `frobeniusXPowMod f hmonic (k + 1) =`
  `powModMonic (frobeniusXPowMod f hmonic k) f hmonic p`.
- Verified:
  - `lake build HexPolyFp.Frobenius`
  - `lake build HexPolyFp`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No `axiom`, `native_decide`, `sorry`, `TODO`, or `FIXME` in
    the touched file.

## Current frontier

`HexPolyFp/Frobenius.lean` now exposes `frobeniusXPowMod_succ`, the
external step lemma the bucket-invariant proof in
`HexBerlekamp.DistinctDegree` needed.

## Next step

Sibling issue (`distinctDegreeFactor_bucket_invariants`) can now consume
`frobeniusXPowMod_succ` to discharge the loop invariant
`currentFrob = frobeniusXPowMod f hmonic d` across iterations.

## Blockers

None.
