## Accomplished

Claimed and completed #3022.

Added `HexBerlekampZassenhausMathlib/TerminationBound.lean`, a proof-facing
BHKS termination support module that packages:

- real-valued dominance of `Hex.bhksBound f` from
  `Hex.factorFastPrecisionCap f <= a`;
- real-valued dominance of `Hex.ZPoly.defaultFactorCoeffBound f` from the same
  cap hypothesis;
- paper-threshold dominance at any precision above `factorFastPrecisionCap`;
- wrappers around
  `ExecutableBadVectorWitness.no_bhks_bad_setup_of_l2norm_upper_lt_divisor`
  under the remaining explicit `hlt` bad-vector contradiction hypothesis.

Imported the new module from `HexBerlekampZassenhausMathlib.lean`.

Verified:

- `lake build HexBerlekampZassenhausMathlib.TerminationBound`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' \
  HexBerlekampZassenhausMathlib/TerminationBound.lean \
  HexBerlekampZassenhausMathlib.lean`
  returns no matches

## Current frontier

The BHKS Mathlib bridge now has a small cap-wrapper layer for downstream
HO-4 termination work. The remaining work is to connect failed fast recovery
runs to the explicit `hlt`/bad-vector setup hypotheses.

## Next step

Use the new `TerminationBound` interface in the next BHKS separation or
`factorFast_terminates` support issue, so those proofs do not restate the
`factorFastPrecisionCap` max algebra.

## Blockers

None.
