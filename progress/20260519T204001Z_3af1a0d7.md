# 2026-05-19T20:40 UTC — HexPolyFp composeModMonic API polish

## Accomplished

- Claimed and completed #5539.
- Audited `HexPolyFp/ModCompose.lean`'s public `composeModMonic` surface and
  immediate theorem cluster.
- Marked `composeModMonic_mod_eq_self` as a `[simp]` theorem so quotient-ring
  normal forms discharge automatically when reducing a modular composition
  result again via `%`.
- Added `[simp]` theorem `modByMonic_composeModMonic_eq_self`, the matching
  `modByMonic` spelling for callers that state reduction through the executable
  monic-reduction wrapper.
- Kept the Horner implementation and existing correctness theorem shapes
  unchanged.

## Current frontier

- `composeModMonic` now exposes both `%` and `modByMonic` reduced-result normal
  forms as automation-facing lemmas.
- This session did not attempt to certify all of `HexPolyFp` Phase 6 or bump
  `libraries.yml`; the issue scope was the focused modular-composition API
  polish item.

## Next step

- Continue Phase 6 API-quality passes on other `HexPolyFp` surfaces, or run a
  broader HexPolyFp linter/docstring/dead-declaration audit before considering a
  Phase 6 completion bump.

## Blockers

- None. Verified with `lake build HexPolyFp.ModCompose`, `lake build HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a prohibited-marker
  grep on the affected diff.
