# 20260609T165611Z (#6695)

## Accomplished

Claimed #6695 and extracted the two top-level Yun scalar synchronization
unit-mismatch cases in `HexPolyFp/SquareFree.lean` into private helpers:

- `yunContribution_scalarSync_raw_one`
- `yunContribution_scalarSync_scaled_one`

The recursive
`yunFactorsContributionWithLevel_scalar_sync_of_derivative_active_reachable`
now dispatches the raw-one and scaled-one branches through those helpers
instead of carrying their constant-state scalar proofs inline. Executable
definitions were unchanged.

## Current frontier

The noisy top-level mismatch branches requested by #6695 are packaged for
reuse by the successor full recursive scalar-sync issue. The remaining
both-false recursive branch is unchanged.

## Next step

The successor issue can use the extracted mismatch helpers and focus on the
both-true/both-false branch structure for the final recursive scalar sync
assembly.

## Blockers

None. Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`
