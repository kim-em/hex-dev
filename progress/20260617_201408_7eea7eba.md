# Progress — 2026-06-17 (session 7eea7eba, /work → /feature)

## Accomplished

### #7823 (HexGFqMathlib proof-only Phase 4 completion) — landed

Premise-checked then executed the proof-only Phase-4 bookkeeping bump for
`HexGFqMathlib` (`libraries.yml` `done_through` `3 → 4`).

**Premise-check (deliverable 1):** confirmed `HexGFqMathlib` exposes no
separable executable computational kernel beyond proof-side bridge
definitions. `Basic.lean` and `GF2q.lean` are entirely
correspondence/spec plumbing over Mathlib types: the `Field` instance
(`Field.ofMinimalAxioms` over the `Lean.Grind.Field` laws), `finEquiv` /
`fintype` / `fintype_card` (Fintype transport via `TypeEquiv`),
`reducedRepEquiv` / `reducedRepFinEquiv`, `equivGaloisField` /
`equivGFq` / `genericEquivGFq` (`RingEquiv` to Mathlib's `GaloisField`).
The two *computable* (non-`noncomputable`) defs flagged by the directive,
`coeffIndex` (`Basic.lean:24`) and `ofIndexBelowDegree` (`Basic.lean:31`),
are thin base-`p` index/representative helpers used only inside
`reducedRepFinEquiv` to construct the `noncomputable finEquiv` for the
`Fintype` instance — not a runtime arithmetic path. Their
constructor/representative-projection performance is already attributed by
`HexGFq`'s `phase4.input_families` `generic-constructor-projection` and
`packed-constructor-projection` (`libraries.yml:295`, `done_through: 5`).
`lakefile.lean:133` declares only `lean_lib HexGFqMathlib` (no bench exe);
the executable kernel and its bench live in `HexGFq` (`HexGFq.Bench`,
`lakefile.lean:217`), per the `lakefile.lean:203-205` "No bench exes for
`Hex*Mathlib` libraries" comment. Premise holds — same shape as merged
precedents #5397, #7808, #7809 and sibling #7821.

**Deliverable 2:** single-line bump, no other library entry touched.

## Verification

- `python3 scripts/status.py HexGFqMathlib` → `done_through: 4`, advanced
  to Phase 5, `ready: yes`.
- `python3 scripts/check_dag.py` → exit 0.
- `lake build HexGFqMathlib` → `Build completed successfully (2380 jobs)`.
  All `sorry` warnings are pre-existing proof-phase obligations (Basic
  lines 37/44/50/57/99/178, GF2q lines 28/32/38/46/78); no new errors,
  no new `sorry`/`axiom`.
- `git diff` is the single `done_through` line in `libraries.yml` (plus
  this progress file).

## Current frontier

Remaining proof-only Phase-completion bumps: #7827
(HexGramSchmidtMathlib Phase 5), #7829 (HexModArithMathlib Phase 5),
#7819 (HexPolyZMathlib), #7820 (HexLLLMathlib).

## Next step

Claim the next proof-only bump (#7827 / #7829 / #7819 / #7820), or the
HO-1 capstone #6672 (note: its body declares a still-open fast-BHKS
substrate blocker — premise-check before executing).

## Blockers

None for this item.
