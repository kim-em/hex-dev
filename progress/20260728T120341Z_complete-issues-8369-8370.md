# Complete issues #8369 and #8370

## Accomplished

- Completed the sorry-free BHKS termination chain from the full adjusted
  lattice vector through the auxiliary polynomial/resultant contradiction,
  reverse span containment, exact true-support span equality, executable
  recovery, and cap-schedule success.
- Added the exact public #8369 capstones
  `factorLatticeFactorsWithBound_ne_none_of_toMonicPrimeData` and
  `factorLattice_ne_none_of_toMonicPrimeData`.
- Completed #8370's branch-tagged hybrid composition with selected-prime,
  normalized-core good-prime, and strict-primorial wrappers. The public
  classical and lattice product theorems discharge both defensive product
  guards, so all three syntactic routes to the trial backstop are excluded
  under the stated hypotheses.
- Updated the BZ SPEC and factor-tactics manual to describe the proved
  executable `bhksBound`, the full-vector reconstruction, conditional lattice
  totality, the 94-prime selector, and conditional modular dispatch.
- Corrected the bounded-selector canary from `X^3 - P*X` to `X^3 - P`.
  Both reduce to `X^3` at every hot-path prime, but the former normalizes to a
  quadratic and makes the integer-root shortcut enumerate divisors of the
  94-prime primorial before reaching the selector. The corrected cubic keeps
  both `toMonicPrimeData? = none` and `factorLattice = none` kernel-checkable;
  the #8369 and #8370 issue bodies were updated to match.
- Updated the conformance expectations for the new proved `bhksBound` values.
- Rebased onto current `origin/main`, adopted the mainline
  `liftedFactorSubsetPartition_of_toMonicModP` rename, and refreshed the
  `X^4 + 1` fast-setup benchmark hash for the larger proved lattice cap.
- Split the two public entry-point lifts into the dependency-ordered
  `LatticeTotality.lean` module, keeping `LatticeTier.lean` below the CI
  3,000-line cap; `scripts/check_file_line_counts.py` is green.
- Audited #8639 against the current tree and GitHub state: the proved
  `FpPoly.mulPacked`/native `__uint128_t` kernel is present and intentionally
  dormant after its neutral end-to-end measurement; #8639 is closed, and its
  matrix remainder #8664 is also closed.
- Verified these gates again after the rebase: `lake build`,
  `lake build HexBerlekampZassenhaus.Conformance`,
  `lake exe hexbz_bench list`, and `lake exe hexbz_bench verify` (all 16
  benchmarks). Fresh BZ fixture emission is byte-identical to the committed
  208-line JSONL baseline. `git diff --check` is clean, and the diff introduces
  no `sorry`, `axiom`, or `native_decide`. `#print axioms` on both totality
  capstones and both dispatch wrappers reports only `propext`,
  `Classical.choice`, and `Quot.sound`.

## Current frontier

- The implementation and local verification for #8369 and #8370 are complete.
- The worktree is ready to commit and publish as the `no-fallback` pull request.

## Next step

- Review and merge the `no-fallback` pull request; let GitHub CI run the
  required external-oracle leg.

## Blockers

- `python-flint` is not installed in this local environment, so the BZ FLINT
  oracle reported its supported local skip. Fixture regeneration and every
  Lean/conformance/benchmark gate available locally passed.
