# Expand `SPEC/future-work.md`

## Accomplished

- Rewrote `SPEC/future-work.md` from 5 items to 35, covering sparse
  matrices, multivariate polynomials, sparse univariate polynomials,
  primality, integer factorization, cylindrical algebraic decomposition,
  multivariate factorization, and rational-expression tactics, plus the
  supporting items those turned out to need (characteristic and minimal
  polynomials, polynomial matrices and invariant factors, modular
  techniques, generic finite fields, Gröbner bases, symbolic summation,
  p-adic types, certificate serialization).
- Added two reading conventions at the top: library names refer to the
  intended graph rather than the built tree, and a statement of what a
  positive certificate does and does not establish.
- Cross-checked every claim about the existing tree against
  `libraries.yml` and the sources, after a Codex review found several
  sections leaning on planned libraries as if they were built.

## Current frontier

- `libraries.yml` is consistent with the tree; `scripts/check_dag.py`
  passes. An earlier apparent gap (the number-field tower libraries
  missing) was an artefact of a stale worktree base, closed by
  caf0126c on main.

## Next step

- Individual items here are ready to be promoted to `SPEC/Libraries/`
  drafts when the project wants them; each names its prerequisites.

## Blockers

- None.
