# 2026-05-11T18-45-24 · review · 8e96ae32

## Accomplished

Issue #3562 (review BHKSBound cap API): added one consumer-facing
bundling theorem to `HexBerlekampZassenhausMathlib/BHKSBound.lean`:

- `factorFastPrecisionCap_real_dominates_bhksPaperThreshold` —
  from `Hex.factorFastPrecisionCap f ≤ a` and the project
  `0 ≤ C ≤ 2` constant convention, returns the three real-valued
  dominance facts HO-4 leaf proofs need bundled in one ∧:
  - `bhksPaperThresholdReal f C ≤ (a : ℝ)`
  - `(Hex.bhksBound f : ℝ) ≤ (a : ℝ)`
  - `(Hex.ZPoly.defaultFactorCoeffBound f : ℝ) ≤ (a : ℝ)`

No executable arithmetic changed. No new axiom/`sorry`. The cap API
was otherwise already sufficient for downstream HO-4 work — naming,
hypothesis order, and the existing real-valued dominance lemmas were
already in good shape.

## Current frontier

BHKSBound.lean cap API review complete.

## Next step

Issue #2567 (BHKS Theorem 5.2 leaf) downstream work can now use the
new bundle theorem at the cap step.

## Blockers

None.
