**Accomplished**

- Claimed #5773 as a one-shot priority work item.
- Built the HO-5a verified-Isabelle BZ comparator with
  `scripts/oracle/setup_bz_isabelle.sh`.
- Ran the current BZ scientific bench suite plus the fixed
  Lean/Isabelle/baseline comparator targets into
  `reports/bench-results/hex-berlekamp-zassenhaus-4e2c8b3.json`.
- Rewrote the head of `reports/hex-berlekamp-zassenhaus-performance.md`
  so the external comparator evidence is the headline section and the
  old internal complexity verdicts are informational appendix material.
- Updated the Concerns section to remove the stale "no comparator
  metadata" concern and replace it with the measured fixed-rung
  comparator limitations.
- Verified:
  - `git diff --check -- reports/hex-berlekamp-zassenhaus-performance.md reports/bench-results/hex-berlekamp-zassenhaus-4e2c8b3.json`
  - proof-marker grep over the report and JSON artefact
  - `lake exe hexbz_bench verify`
  - `lake build`

**Current frontier**

- The report now records real `hex/isabelle` comparator data from the
  registered HO-5a comparator targets. The canonical fixed comparator
  rung meets the `<= 1x` goal after subtracting the baseline overhead.
- The current bench executable still exposes only a fixed canonical
  comparator rung, not per-family scaling ladders for every scientific
  BZ target. The PR should therefore be partial, with the remaining work
  routed back through replan.

**Next step**

- Add per-family Isabelle comparator registrations, or otherwise extend
  the HO-5a comparator surface, so each scientific BZ target can report
  a largest-eligible-rung ratio verdict and trend.

**Blockers**

- Current infrastructure does not provide the issue's requested
  per-target comparator ladders. `runFactorIsabelleDomainChecksum` also
  measured at `32 ns` and triggered lean-bench's compile-fold warning,
  so the fixed Lean/Isabelle ratio is wiring evidence rather than a
  full scientific timing signal.
