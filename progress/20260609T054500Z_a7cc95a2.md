# 20260609T054500Z (#6617)

## Accomplished

- Claimed #6617 (D6: HexLLLMathlib Euclidean headline for the
  dispatched `lll`).
- Discovered both deliverables are already in place in
  `HexLLLMathlib/Independent.lean`, shipped by the combination of
  #6624 (η-parameterization of `isLLLReduced` and the
  `reduced_first_row_norm_sq_le_of_mem_latticeSubmodule` workhorse)
  and #6633 (case-split-on-dispatch wiring of `lll_isLLLReduced`,
  `lll_independent`, `lll_memLattice_iff`,
  `lll_mem_latticeSubmodule_iff`):
  - `lll_first_row_norm_sq_le_unconditional`
    (`HexLLLMathlib/Independent.lean:2537-2562`) — dispatched `lll`
    Euclidean headline at `α = 1/(δ − 121/400)`, holding regardless
    of `LLLProvider.dispatch` path.
  - `lllNative_first_row_norm_sq_le_unconditional`
    (`HexLLLMathlib/Independent.lean:2509-2531`) — classical native
    Euclidean headline at `α = 1/(δ − 1/4)`.
- Both consume the η-generalized conditional Euclidean workhorse
  `reduced_first_row_norm_sq_le_of_mem_latticeSubmodule`
  (`HexLLLMathlib/Basic.lean:351-376`), instantiated at η = 11/20
  (dispatched `lll`) and η = 1/2 (`lllNative`). The certified
  branch's correctness routes through `dispatch_some_property` →
  `HexLLLMathlib.certCheck_sound` only; checker internals are not
  referenced by the Euclidean headlines.
- Verified via `lake build HexLLLMathlib` (green) and
  `grep -nE '\bsorry\b|\baxiom\b|\bnative_decide\b' HexLLLMathlib/`
  (no matches).
- Posted a comment on #6617 with the exact code listings and the
  resolution rationale, closing the issue as completed.

The directive's "Library placement" suggestion of
`HexLLLMathlib/Basic.lean` is not satisfied literally: the two
unconditional theorems live in `HexLLLMathlib/Independent.lean`
because the dispatch-aware ingredients
(`lll_isLLLReduced`, `lll_memLattice_iff`, etc.) live there and
depend on the `HexGramSchmidtMathlib.Update` surface. The
conditional workhorse `reduced_first_row_norm_sq_le_of_mem_latticeSubmodule`
itself does live in `Basic.lean`, matching the SPEC's name placement.

## Current frontier

D6 is resolved-as-already-done. No new code landed. The Mathlib-side
Euclidean restatement of the LLL short-vector bound is unconditional
on the dispatched `lll` at the public `η = 11/20` constant and on
`lllNative` at the classical `η = 1/2` constant.

## Next step

The remaining HexLLL D-series items per the directive's own "Out of
scope" list are D7 (conformance) and D8 (bench). Neither is unblocked
or queued by this resolution beyond the SPEC making both Euclidean
headlines available as named hypotheses.

## Blockers

None.
