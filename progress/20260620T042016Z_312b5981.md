# HexGramSchmidt dead-code removal — done_through 5→6 (#8182)

## Accomplished

Removed the `HexGramSchmidt` dead-code cluster gating the Phase 6
`done_through 5 → 6` bump, driven by an iterative unreferenced-private
rescan fixpoint (not by guessing the cascade).

**Removals (43 decls, 1159 lines).**
- `Basic.lean`: 9 listed private decls (81 lines).
- `Int.lean`: 14 listed private + 3 listed public theorems, then a
  rescan-driven cascade of 16 more newly-orphaned private helpers
  across three further passes (1078 lines). The cascade ran deeper
  than the issue's ~497-line estimate because removing the two
  `det_*` bordered-minor theorems orphaned the `coeffBM` multilinearity
  sub-cluster (`coeffBM_entry_lt/last`, `colReplace_borderedMinor_…`,
  `foldl_acc_congr_int`, `foldl_finRange_indicator_eval_int`,
  `borderedMinor_last_col_eq`), which in turn orphaned their own local
  helpers. `Matrix.borderedMinor` itself (load-bearing, 100+ uses) and
  `coeffBM` (still referenced) were left intact.

**Directive correction — one listed decl kept.** The issue listed
`bareissGramCanonicalCoeff_zero` (Int.lean) among the 4
"confirm-then-remove" public theorems, but that lemma carries
`@[simp, grind =]`. The issue's *own* deadness criterion excludes
attributed decls precisely because automation references them by
attribute, not by name — a once-occurring name does not make a
registered simp/grind lemma dead, and removing it could silently weaken
downstream `HexGramSchmidtMathlib`/`HexLLL` automation this build does
not cover. Kept it; removed the other 3 attribute-free public theorems
as specified. No `@[simp]`/`@[grind]`-attributed declaration was removed
(verified: `git diff | grep '^-.*@\['` is empty).

## Verification

- `lake build HexGramSchmidt` green after every removal pass (3 build
  passes, all exit 0).
- Unreferenced-private rescan returns empty (fixpoint reached after
  pass 3).
- All 43 removed names have 0 live references in `.lean` outside
  `progress/`.
- No `axiom`/`sorry`/`native_decide` introduced (pure deletion); no
  proof of a surviving decl touched.
- `libraries.yml` `HexGramSchmidt.done_through: 5 → 6`.
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` now reports `HexGramSchmidt -> Phase 7`
  (off Phase 6 ready).

## Current frontier

Phase 6 closeout complete for `HexGramSchmidt`. The bordered-minor
multilinearity machinery (`coeffBM` and its surviving lemmas) remains —
it is still referenced by live proofs, so it is not dead.

## Next step

None for this issue. `bareissGramCanonicalCoeff_zero` (the kept
attributed lemma) could be re-evaluated for genuine simp/grind use in a
future automation-audit pass that builds the full downstream, but it is
not dead by the project's deadness definition.

## Blockers

None.
