# Issue #6210: HexHensel Phase 6 Bezout pair at target precision

## Accomplished

- Claimed #6210 and read `HexHensel/QuadraticMultifactor.lean` to confirm
  the loop invariant carries the Bezout half through every doubling step.
- Added public theorem `henselLiftQuadratic_bezout_spec` alongside
  `henselLiftQuadratic_spec` in `HexHensel/QuadraticMultifactor.lean`.
  Same argument order, hypothesis bundling, and docstring shape as the
  existing product-congruence companion. Proof mirrors
  `henselLiftQuadratic_spec` four-step pattern: run
  `iterateQuadraticHensel_invariant`, project Bezout via
  `QuadraticLiftLoopInvariant.bezout_congr`, descend the loop modulus to
  `p^k` with `congr_of_pow_le` and `le_two_pow_quadraticDoublingSteps`,
  then push through the final `reduceModPow` on the four witnesses with
  two `congr_mul_reduceModPow_pair` calls combined by `ZPoly.congr_add`.

## Current frontier

- #6210 deliverables complete.
- Recommendations 1 and 3 from
  `reports/hex-hensel-lift-api-phase6-review.md` (linear multifactor
  invariant constructors; simp/grind annotation audit on invariant
  projections) remain unfiled and explicitly out of scope here.

## Next step

- Push branch and open PR closing #6210 via `coordination create-pr`.

## Blockers

- None.
- Verification passed: `lake build HexHensel.QuadraticMultifactor`,
  `lake build HexHensel`, full `lake build` (244 targets),
  `python3 scripts/check_dag.py`, `git diff --check`, forbidden-token
  diff grep over the touched Lean file (no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`), and
  `git grep -n 'henselLiftQuadratic_bezout_spec' HexHensel/` finds the
  new declaration.
