# 2026-05-13T14:44:59Z — feature session 0b152b72

## Accomplished

Claimed #3629 (HexHenselMathlib bench harness), then skipped after
confirming it was stale: SPEC/benchmarking.md §"Mathlib-free benches"
(landed in #3684, 2026-05-12) and HO-33 (#3685, PR #3689) explicitly
forbid `Hex*Mathlib/Bench.lean` files and `lean_exe *mathlib*_bench`
entries; the structural lint `scripts/ci/check_benches_mathlib_free.sh`
would reject the issue's deliverables.

Claimed #3503 next, skipped: depends on closed-unmerged PR #3504.

Claimed #3553 (HexGramSchmidt adjacent-swap residual + suffix). After
orienting on `HexGramSchmidt/Basic.lean` and the prior #3552 (#3567)
landing, decomposed the issue:

- Sub-issue #3733: rational swapped-current residual formula
  (Rat-layer wrapper).
- Sub-issue #3734: rational adjacent-swap suffix preservation.

Reason for decomposition: the residual formula's Rat-layer wrapper
requires division-heavy algebraic manipulation in this Mathlib-free
file (no `field_simp` / `linear_combination` available), and the
suffix-preservation theorem needs a prefix-span equality argument
that is structurally distinct from the residual formula work. Each
piece is a coherent stand-alone follow-up; the private foundation
landed here unblocks both.

Landed coherent subset on `agent/0b152b72`:

- New private helpers in `HexGramSchmidt/Basic.lean`:
  - `rowSwap_toList_get!_right` (swap[k] = b[km1])
  - `rowSwap_toList_get!_of_gt` (swap[t] = b[t] for t > k)
  - `basisMatrix_rowSwap_adjacent_curr`: the division-free private
    form of the new k-th basis row after an adjacent swap, namely
    `prev - projectionCoeff (b.row km1) swappedPrev • swappedPrev`,
    where `swappedPrev = curr + projectionCoeff (b.row k) prev • prev`.

The proof decomposes `(basisRows swapped).take k` as
`(basisRows b).take km1 ++ [swappedPrev]` (using the existing
`basisRows_take_eq_of_prefix` and `basisMatrix_rowSwap_adjacent_prev`),
applies `reduceAgainstBasis_append` to peel off `swappedPrev`, and
uses linearity (`reduceAgainstBasis_add_left` /
`reduceAgainstBasis_smul_left`) to split the subtractProjection into
the `b.row km1` reduction (gives `prev` via the basisRows recurrence)
and the `swappedPrev` reduction (gives `swappedPrev` itself since
both `curr` and `prev` are orthogonal to the basis prefix
`(basisRows b).take km1`).

`lake build HexGramSchmidt` succeeds; no new `axiom`/`native_decide`/
`TODO`/`FIXME`/`sorry` introduced. The two pre-existing `sorry`s in
`HexGramSchmidt/Update.lean` are unchanged.

## Current frontier

`HexGramSchmidt/Basic.lean` now exposes a clean private statement
about the new k-th basis row of an adjacent row swap. The two
follow-up sub-issues (#3733, #3734) consume this and complete the
remaining residual + suffix work.

## Next step

A planner cycle should triage #3553 against the breadcrumb comment
linking #3733 and #3734, then a worker session can claim #3733 once
the partial PR lands.

## Blockers

None.
