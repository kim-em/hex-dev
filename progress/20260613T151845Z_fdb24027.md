# Progress: #6846 — rational coprimality forward wrapper

## Accomplished

Claimed #6829 first, diagnosed its premise as stale after #6837, commented the
missing raw executable-tail bridge shape on the issue, and released it for
replan.

Claimed #6846 and added
`coprime_input_aux_over_rat_of_forward` in
`HexBerlekampZassenhausMathlib/BadVector.lean`. The theorem exposes the BHKS
Lemma 3.2 rational coprimality clause as a named top-level wrapper around the
closed `ForwardCorrespondence.coprime` contrapositive. Rewired
`isBhksBadVectorSetup_of_forward` through the named theorem.

## Current frontier

The #6846 deliverable is closed at the API level: the forward correspondence
now has a named coprimality projection in the same namespace layer as the other
bad-vector setup accessors.

## Next step

Create the PR for #6846. The broader B8 packaging issue #6779 still needs the
resultant clause path before it can assemble `ProjectedBadVectorSetupBridge` /
`BadVectorBridgeData`.

## Blockers

`lake build HexBerlekampZassenhausMathlib` still fails in the pre-existing
`HexBerlekampZassenhausMathlib/Basic.lean` scale-to-dilate seam at line 16353:
the prefix-none hypothesis still has the old
`DensePoly.scale ... centeredLiftPoly` candidate shape while the callee expects
the new `ZPoly.dilate ... centeredLiftPoly` candidate shape. This is unrelated
to the `BadVector.lean` wrapper.
