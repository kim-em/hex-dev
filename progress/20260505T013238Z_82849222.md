# Work session: HexMatrix prefix-row insertAt helper split

## Accomplished

- Claimed #2131, assessed it as too broad for a single proof task, and decomposed it into #2178 and #2181.
- Added #2181 as an explicit dependency of downstream #2118.
- Claimed #2178 and added private `insertAt`/list access helpers in `HexMatrix/Determinant.lean` for arbitrary earlier-prefix insertion:
  - inserted-row lookup,
  - before-insertion prefix lookup,
  - shifted-row lookup after insertion,
  - final-row lookup after prefix insertion.
- Verified the helper layer with `lake build HexMatrix.Determinant`, `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-placeholder scan of `HexMatrix/Determinant.lean`.

## Current frontier

#2178 has the core arbitrary-prefix `insertAt` helper layer needed by the later term theorem. The full bordered-minor product/sign rewrite remains in #2181.

## Next step

Use the new prefix insertion helpers to prove the #2181 earlier-prefix `detTerm` rewrite, including the sign relation and current-`k` bordered-minor product reindexing.

## Blockers

No technical blocker in the helper slice. The repository still has a pre-existing uncommitted `.claude/CLAUDE.md` pod-managed diff that this session did not modify or include.
