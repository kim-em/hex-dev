## Accomplished

Claimed #7623 and added the default-bound slow modular raw-factor irreducibility
producer in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

New proof surface:
- `reassemblyComplete_of_slowSubstrate_bound`
- `reassemblyComplete_of_slowSubstrate`
- `slowModularRaw_irreducible_of_fast_none`

The theorem has the capstone-facing shape requested by #7623: from
`factorFastFactorsWithBound f (defaultFactorCoeffBound f) = none` and
`factorSlowModularFactorsWithBound f (defaultFactorCoeffBound f) = some rawFactors`,
it proves every `raw ∈ rawFactors.toList` is `Hex.ZPoly.Irreducible`.

The exhaustive branch routes through `slowPathHenselSubstrate_of_toMonicPrimeData`,
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`,
and the new substrate-based reassembly-completeness helper. The quadratic branch
uses the existing quadratic reassembly certificate and executable irreducibility
lemma. The constant slow branch is excluded by the fast-path `none` degree lemma.

Verification:
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over `IntReductionMod.lean`

Quality counts unchanged: `sorry` 68, `axiom` 11, `native_decide` 2.

## Current frontier

#7623's requested slow modular raw-factor theorem is implemented and verified.
The result is ready for the #6672 raw-disjunct assembly work to consume.

## Next step

Wire `slowModularRaw_irreducible_of_fast_none` into the middle disjunct of
`factor_entries_irreducible`'s `h_raw` producer in a follow-up capstone issue.

## Blockers

None for this issue. The worktree still contains a pre-existing unrelated
`.claude/CLAUDE.md` modification; it was not touched or staged.
