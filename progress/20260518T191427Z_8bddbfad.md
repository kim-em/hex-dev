# 20260518T191427Z — feature session 8bddbfad (#5068)

## Accomplished

Rewired the reverse-direction `(primitivePart core).isZero = false`
derivation at `HexBerlekampZassenhausMathlib/IntReductionMod.lean:1445`
onto the composition
`(Hex.DensePoly.isZero_eq_false_iff _).mpr ∘ Hex.ZPoly.size_pos_of_ne_zero _`,
collapsing the 11-line `rcases`/`ext_coeff` block to a 3-line term-mode
proof (per #5068 deliverable 1).

Final shape:

```lean
have hpp_isZero_false : (Hex.ZPoly.primitivePart core).isZero = false :=
  (Hex.DensePoly.isZero_eq_false_iff _).mpr
    (Hex.ZPoly.size_pos_of_ne_zero _ (by rw [hpp]; exact hcore_ne))
```

The inline `by rw [hpp]; exact hcore_ne` form typechecked without
needing the documented fallback (no `at *` or pulled-out `have`).

## Verification

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`: baseline
  preserved (16 errors all in `Basic.lean`, none in `IntReductionMod.lean`).
- `lake build HexBerlekampZassenhausMathlib`: same 16-error baseline.
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- Diff introduces no `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`.
- Greppable invariants:
  - `isZero = false := by` in `IntReductionMod.lean`: 2 → 1 (only the
    `IntReductionMod.lean:1510` derivative site survives).
  - `Hex.ZPoly.size_pos_of_ne_zero` in `HexBerlekampZassenhausMathlib/`:
    1 → 2 (baseline `Basic.lean:143` plus the new `IntReductionMod.lean`
    use).
  - `apply Hex.DensePoly.ext_coeff` in `IntReductionMod.lean`: 9 → 8.
- `git diff --stat` on the source file: `+3 / −11`, net −8 lines.

## Current frontier

#5068 deliverables 1–3 satisfied. Binding name `hpp_isZero_false`
preserved; downstream uses at lines 1457 (`psd_repeatedPart_eq_one_…`
call), 1467 (`by_cases hderiv`), 1495 (`hP_isUnit`), and elsewhere
typecheck against the same baseline.

## Next step

Open PR closing #5068.

## Blockers

None.
