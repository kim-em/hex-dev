# Discharge dvd_gcd and xgcd_bezout for ZMod64 GcdLaws (#2242)

## Accomplished

- Replaced two `sorry`s in `instGcdLawsZMod64Fp` (`HexPolyFp/Basic.lean`)
  with applications of the existing `_of_divModLaws` core lemmas:
  - `dvd_gcd` via `DensePoly.dvd_gcd_of_divModLaws`
  - `xgcd_bezout` via `DensePoly.xgcd_bezout_of_divModLaws`
- Added `[PrimeModulus p]` to the GcdLaws instance binders so the
  `instDivModLawsZMod64Fp` instance can be supplied as the
  `DivModLaws` argument. Used explicit `@`-application threading
  `(instDivModLawsZMod64Fp p)` because the explicit `(p : Nat)`
  binder on the underlying instance prevents ordinary class
  synthesis at this site.
- Named the GcdLaws instance `instGcdLawsZMod64Fp` for symmetry with
  `instDivModLawsZMod64Fp`.
- `lake build` clean; `python3 scripts/check_dag.py` clean.
- HexPolyFp/Basic.lean sorry count: 7 → 5.

## Current frontier

- `gcd_dvd_left` and `gcd_dvd_right` fields of `instGcdLawsZMod64Fp`
  remain `sorry` — those need the `hsmall` helper and are tracked
  by the sibling issue (#2241).

## Next step

The next worker on this corner picks up #2241 (the `hsmall` helper
plus the two remaining GcdLaws fields).

## Blockers

None.
