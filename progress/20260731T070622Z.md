# HexBZ direct-coordinate factorization CI

## Accomplished

- Diagnosed the clean CI failure from the job log rather than the collapsed PR
  check state.
- Updated the Release 4 lattice guard to the typed `FactorTier` and
  `classicalDecline` trace fields.
- Added the direct-coordinate executable modules to the `import all` closure
  used by `FactorPolyTests`, so kernel reduction sees their bodies across
  module boundaries.
- Rebuilt `HexReleaseTests` and `HexReleaseExamples`; all 9,230 jobs pass,
  including the previously failing `FactorPolyTests` and `Release4`.
- Confirmed that the former `PublicSurface.lean` and generic `Data.lean` have
  been replaced by files named for the mathematical objects they contain.

## Current frontier

The two clean-build failures are fixed locally. The correction needs to be
pushed to PR #9114 and exercised by GitHub Actions.

## Next step

Push the fix, inspect the new Actions job directly, and let the already armed
auto-merge complete once CI is green.

## Blockers

None.
