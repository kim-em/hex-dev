## Accomplished

- Claimed human-oversight issue #2704 for the HexModArith Phase-4 audit reset follow-up.
- Rechecked `PLAN/Phase4.md`, `SPEC/benchmarking.md`, `SPEC/profiling.md`, `SPEC/Libraries/hex-mod-arith.md`, and `HexModArith/Bench.lean`.
- Confirmed `hexmodarith_bench` currently registers eleven linear benchmark targets for the `ZMod64` API surface and Barrett/Montgomery hot-loop wrappers.
- Rolled `HexModArith.done_through` from 4 to 3 because the required headline report and profile evidence do not exist.
- Added `HexModArith.phase4.input_families` metadata for `word-residue-core`, `barrett-hot-loop`, and `montgomery-hot-loop`.
- Filed follow-on issue #2722 to regenerate the scientific benchmark/profile evidence package and re-promote only if the Phase-4 report has complete traceability and empty Concerns.
- Linked the #2704 result from umbrella audit #2690.

## Current frontier

- `HexModArith` no longer claims Phase 4 without the post-#2687 evidence package.
- The existing bench executable remains wired and smoke-checks successfully.

## Next step

- Work #2722 to run scientific `hexmodarith_bench` cases, decide any required Barrett/Montgomery compare coverage, collect per-family profiles, add `reports/hex-mod-arith-performance.md`, and restore `done_through: 4` only if all Phase-4 exit criteria pass.

## Blockers

- None for the rollback. Re-promotion depends on new benchmark/profile artefacts.
