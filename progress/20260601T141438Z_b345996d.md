# b345996d - work #5979

## Accomplished

- Claimed #5979 after all directive candidates were blocked by dependencies.
- Refactored `factorFastFactorsWithBound` so both non-constant `B = 1` and
  `B > 1` non-quadratic paths match on
  `choosePrimeData? normalized.squareFreeCore`.
- Made the no-prime-data branch return `none` instead of invoking
  `factorFastCoreWithBound` with `choosePrimeData` fallback data.
- Updated the core-success, branch-shape, entry-shape, and product proofs to
  simplify through explicit `choosePrimeData? = some primeData` witnesses.
- Verified with `lake build HexBerlekampZassenhaus`,
  `rg -n "choosePrimeData\\?_isSome" HexBerlekampZassenhaus/Basic.lean`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token
  diff grep from #5979.

## Current frontier

#5979 is implemented in `HexBerlekampZassenhaus/Basic.lean`. The fast path now
requires real selected prime data before attempting the BHKS core.

## Next step

Open the PR for #5979. Follow-on #5980 can propagate the witness-shaped
dispatch through product and entry proofs.

## Blockers

No blocker for #5979. The pre-existing `.claude/CLAUDE.md` worktree
modification was left untouched and excluded from this session's commit.
