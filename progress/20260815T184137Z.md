# PNT+ Table 10 exact-supremum completion

## Accomplished

- Preserved the exact local Table 10 stack through
  `0425b9cdd2d258860b170e21966ad84a72c6d604` and audited pinned PNT+
  `BKLNW_table10_dispatch.lean` at revision
  `21998bb6196b56789f72a52656a781a75e134eb0`.
- Established that `bklnw_table_10_verification` is a finite 287-row by
  five-column dispatcher over 1,435 ordinary margin theorems. The migrated 87
  target calls occur in 56 declarations, of which 54 are dispatcher margin
  theorems and two are stronger bare helpers; the other 1,381 margin theorems
  and the dispatcher itself contain no LeanCert numerical premise.
- Added `PntTable10Exact.exactBound`, whose expansion theorem is the exact
  two-term `iSup` body obtained by unfolding PNT+ `B_8_exact` and `B`.
- Proved reusable ordinary-kernel full-interval and pointwise reductions from
  the checked coefficient majorants to that exact supremum.
- Added source-membership adapters for row 25, convex rows 60--85, pointwise
  rows 90/95, row 13800.7464, and the coupled row 43 / `19 * log 10`
  transition. Each retains the source tuple, finite column, chronology, and
  three PNT+-owned coefficient hypotheses while consuming the existing
  authenticated row provider.
- Added a representative source-shaped exact theorem and guarded axiom
  reports. Existing conformance retains the bounded planning, no-draft/no-retry
  mutation rejection, chronology, and `ProofFrontend` evidence.
- Reclassified all 125 Table 10 calls, the two now-removable direct LeanCert
  imports, the Table 10 generated family, and the aggregate 128-call BKLNW
  family as accepted after localized rewrite. Exact-source verification
  against the pinned checkout passes.
- Updated both SPECs and file organization, registered the proof-only module
  and conformance guard, and added the exact Lake freshness exemption.
- Passed focused Table 10 module and conformance builds, inventory unit/check/
  source verification, copyright, line-count, DAG, Phase-4, trust-surface,
  release-manifest, Mathlib-free bench, freshness, diff-hygiene, and banned-
  mechanism checks.

## Current frontier

The exact pinned Table 10 interface is complete after a localized PNT+
rewrite: replace the 125 tactic calls, unfold `B_8_exact` through
`exactBound_eq_sourceSum`, invoke the matching adapter, and retain the
unchanged ordinary row proofs and generated dispatcher.

## Next step

Keep the completed stack local until publication sequencing is provided. A
later landing should replay it onto the requested exact main, preserve the
current FKS/public registrations, refresh the exact Lake exemption, and rerun
fresh review and CI.

## Blockers

None. Arbitrary caller-supplied Table 10 generation and production-scale
performance remain future work, but neither is required by the exact pinned
PNT+ interface.
