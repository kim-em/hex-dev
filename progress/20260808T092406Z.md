# Unblock semantic replay CI

## Accomplished

- Merged the now-landed arbitrary-function policy/session foundation from
  `main` into the semantic-replay branch.
- Added an exact-blob exemption for registering the proof-only semantic-replay
  experiment and conformance targets in `lakefile.lean`; the measured
  factorization service graph is unchanged.
- Rebuilt semantic replay together with the policy/function and policy/session
  conformance targets, and reran the factor-source freshness and dependency-DAG
  checks successfully.

## Current frontier

The semantic-replay PR is ready to be retargeted to `main` and rerun in CI. It
provides function packages with their own certificate decoders and soundness
lemmas rather than putting a fixed arithmetic language in the engine.

## Next step

Push this integration commit, retarget PR #9072 to `main`, and review its exact
CI result while continuing the chronology/proof-emission layer above it.

## Blockers

None.
