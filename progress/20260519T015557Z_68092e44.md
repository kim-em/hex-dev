# 20260519T015557Z — HexLLL Phase 4 scientific timing: host-contention skip

Session type: `/work` → `/feature` worker on issue #4334.

## Accomplished

Claimed issue #4334 (HexLLL Phase 4 inconclusive verdict
investigation), probed host carica, confirmed it is still unsuitable
for scientific timing, and skipped the issue back to `replan` with
measurements documented on the issue.

Measurements at probe time:

- Load averages 20.86 / 16.68 / 17.03 on 24-core box (vs 18.95 / 14.84
  / 14.72 at the immediately previous skip ~50 minutes earlier — load
  has risen, not fallen).
- Concurrent lean builds running in worktree `8af6586c`
  (`HexGramSchmidt/Conformance.lean`, `HexPolyFp/SquareFree.lean`).
- Three Python processes pegged at ~99% CPU (unrelated personal
  scripts), plus VS Code helpers at ~97–100%.
- `lake exe hexlll_bench list` smoke: 92.04s wall / 266.60s user, ratio
  ≈ 2.90 — essentially identical to the immediately previous skip's
  91.87 / 262.01 (ratio ≈ 2.85).

This is the same host-contention pattern documented in the issue body
and in every recent skip comment. Scientific verdict promotion needs
a user/wall ratio near 1, not near 3.

## Current frontier

Issue #4334 is back in the queue with `replan` set. The body already
documents the redispatch condition (quiet/dedicated hardware per
`SPEC/benchmarking.md` §"Scientific timing runs"). No code change
required from this session; nothing landed.

## Next step

Either:

1. Wait for carica to quiet down (no concurrent lean builds, low
   Python load), then redispatch. The current heavy contender is
   another in-process worktree at `8af6586c`; this will pass on its
   own.
2. Run the release-candidate benchmarking workflow on dedicated
   hardware as the SPEC suggests, which side-steps host carica
   entirely.

## Blockers

- Host contention (transient, not a code blocker).
