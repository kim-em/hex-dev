## Accomplished

Claimed #3067 and discharged the small-prime modular support holes in
`HexBerlekampZassenhaus/Basic.lean`:

- proved the `Lean.Grind.Field (ZMod64 p)` instance from a `Nat.Prime p`
  witness, including zero inverse, inverse nonzero, inverse involution, and
  integer-power laws;
- proved `monicModularImage_monic` for nonzero modular images using the local
  `ZMod64.PrimeModulus` wrapper and existing leading-coefficient scaling
  lemmas;
- added `HexArith.Int.extGcd_zero_left_s_ofNat`, a small proof-only lemma
  exposing the canonical Bezout coefficient used to prove `ZMod64.inv 0 = 0`.

Verified:

- `lake build HexArith.ExtGcd`
- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean`
  reports only pre-existing unrelated proof holes.

## Current frontier

The modular Berlekamp branch now has proof-backed finite-field and monic-input
preconditions for the small-prime candidate surface. Adjacent prime-selection
certificate holes remain separate and are tracked by #3066.

## Next step

Continue the HexBZ core proof pipeline by proving the `choosePrimeScore?` and
`isGoodPrime` certificate theorems in #3066, or proceed to the downstream
factorization product invariants once the prime-selection proof layer lands.

## Blockers

None.
