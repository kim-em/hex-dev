## Accomplished

Claimed #2924 and proved the reachable Yun common-divisor provider in
`HexPolyFp/SquareFree.lean`.

Added a square-free propagation lemma for factors, proved that every
`yunFactorsPairwiseReachable` state has a square-free current component, and
used that to assemble `yunFactorsPairwiseReachable_common_dvd_one` for the
step constructor surface requested by #2924.

Wired the concrete provider into `squareFreeAuxRev_pairwise_coprime_nil_core`,
removing that proof hole and threading the required `Hex.Nat.Prime p` witness
through the local pairwise-coprime helper chain.

Verified:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  reports only pre-existing unrelated `sorry` markers.

## Current frontier

The reachable common-divisor provider needed by #2918's pairwise-ready
assembly is now explicit. The file still has unrelated proof holes in the
weighted-product, square-free tail-cross, and step-squarefree clusters.

## Next step

Continue the HexPolyFp square-free chain from the remaining blocked successor
issues, especially #2920 / #2916 once #2924 lands.

## Blockers

None for #2924.
