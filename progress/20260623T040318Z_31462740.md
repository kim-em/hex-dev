# Core extractor family — #8305

Session type: feature (one-shot `/once`). UTC 2026-06-23T04:03.

## Accomplished

Authored the Core-coordinate (van Hoeij `M1`) extractor family in
`HexBerlekampZassenhaus/Basic.lean`, mirroring the existing M2
`bhksIndicatorCandidate?_*` / `bhksIndicatorCandidates?_*` /
`bhksRecoverClassified_success_*` lemmas for the Core classifier
(`bhksIndicatorCandidateCore?` / `bhksIndicatorCandidatesCore?` /
`bhksRecoverClassifiedCore`). Purely additive (+635 lines, only
`Basic.lean`); no executable call site touched, so the build stays green.
`lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib` green
(498 + 3784 jobs). No `sorry`/`axiom`/`native_decide` in the diff.

New lemmas, alongside the Core defs (after `coreRecover?`):

- Per-candidate: `bhksIndicatorCandidateCore?_{normalizeFactorSign,
  shouldRecord, dvd, leadingCoeff_nonneg, primitive, positive_degree}`.
  Same proof shape as the M2 family; the only divergence is `_primitive`,
  where the Core candidate wraps `ZPoly.primitivePart` directly (no
  `normalizeCandidateFactor`), so primitivity comes from
  `primitivePart_eq_zero_of_content_eq_zero` + `primitivePart_primitive`
  on the centred lift's content rather than `normalizeCandidateFactor_primitive`.
- Fold helpers: `bhksIndicatorCandidatesCoreStep_fold_{none,
  all_of_candidate, size_eq}`.
- Plural: `bhksIndicatorCandidatesCore?_{all_of_candidate,
  normalizeFactorSign, shouldRecord, dvd, primitive, leadingCoeff_nonneg,
  positive_degree, size_eq}` (the `size_eq` needed by #8290's size wrapper).
- Classifier success: `bhksRecoverClassifiedCore_success_{product,
  all_of_candidates, normalizeFactorSign, shouldRecord, dvd,
  indicatorCandidates}`.

## Current frontier

The Core extractor family now exists and is green, dead-but-proven until
#8304 routes `factorFastCoreWithBound` through `bhksRecoverClassifiedCore`.
No M2 lemma, the `factorFastCoreWithBound` call site, the unfold lemma, or
`cldCoeffFloor` was touched.

## Next step

#8304 (the M1 fast-path swap) can now consume these extractors; #8290 can
consume `bhksIndicatorCandidatesCore?_size_eq`. The not-yet-mirrored M2
constructor/inverse lemmas (`_eq_some_of_forall_candidate`, `_eq_some_of_getD`,
`_getD_candidate`, and the dilatedCenteredLift identity, which has no Core
analogue since the Core path has no `dilate`) can be added when a consumer
needs them.

## Blockers

None.
