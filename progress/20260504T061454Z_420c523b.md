# 2026-05-04 06:14 UTC — feature session for #2017

## Accomplished

Bumped `HexPolyZ/Conformance.lean` typical/adversarial fixtures from
degree 2-5 to degree 10, exercising the upper end of the SPEC `core`
profile's polynomial-degree band (SPEC/testing.md § "Profile sizes",
ranges 8-12).

- `zpoly*` — `3 - 2x^2 + 7x^10` and `5x - 7x^3 + 11x^10` (with extra
  trailing zeros so normalization is still under test).
- `congr*` (mod 5 / mod 12) — 11-coefficient pairs with each
  coordinate difference divisible by the modulus.
- `coprime*` Bezout (mod 5 / mod 3) — `1 + x^10`, `2 + x^10` for the
  typical case (witness `(-1)·F + (1)·G = 1` lifts as integers); the
  sparse adversarial uses `3 + x^2 + x^10` with a constant unit `G = 2`.
- `content*` / `primitivePart*` — bumped to degree 10 with chosen
  coefficient gcds (`1`, `6`, `7`).
- `squareFree*` — `(x - 1)^10`, `2·(x - 1)^10`, `x^10`, `x^10 - 1`.
  The repeated-part check now compares against `(x - 1)^9`.
- `ratPolyPrimitivePart` — bumped to `(1/2)·(1 - x^10)`.
- `Mignotte` helpers — `binom 12 5`, `floorSqrt 10^6`, `floorSqrt
  (10^8 - 1)`, plus `coeffNormSq` / `coeffL2NormBound` /
  `mignotteCoeffBound` on degree-10 polynomials whose Euclidean norms
  are perfect squares (`169 = 13^2`, `676 = 26^2`).

The typical/edge/adversarial structure is preserved throughout — only
sizes were scaled, no cases were dropped.

## Verification

- `lake build HexPolyZ.Conformance` green.
- Fresh elaboration of the bumped file: ~1.1 s wall (well under the
  2 s budget called out by #2017).

## Current frontier / Next step

Issue closed by PR. Sibling SPEC-profile bumps (#2012, #2013, #2014,
#2016) remain unclaimed for other workers.

## Blockers

None.
