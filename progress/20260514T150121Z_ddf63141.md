# Session ddf63141 — #4034

## Accomplished

- Claimed #4034 after the higher-priority human-oversight items reported
  `CLAIM FAILED`.
- Exposed public `Matrix.nullspaceBasisMatrix`, `Matrix.nullspace_sound`,
  and `Matrix.nullspace_complete` wrappers around the existing RREF nullspace
  proofs.
- Added Berlekamp fixed-space kernel predicates and proved:
  - `fixedSpaceKernelVectors_sound`
  - `fixedSpaceKernel_sound`
  - `fixedSpaceKernelVectors_complete`
  - `fixedSpaceKernelPolynomial_coeffVector_complete`
- Verified `lake build HexBerlekamp.Basic`, `lake build HexBerlekamp`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

The executable nullspace basis now has a Mathlib-free soundness/completeness
surface at the coefficient-vector level. Downstream Berlekamp completeness work
can use these statements to move between fixed-space matrix nullspace
membership and coefficient-vector spans.

## Next step

Use the new coefficient-vector span theorem in the #4037 composition once the
CRT witness and witness-split successor issues have landed.

## Blockers

None for this issue.
