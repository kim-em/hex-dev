## Accomplished

- Claimed #2627 and wired `factorFastCoreWithBound` to build `LiftData` at
  each precision step, call `bhksRecover?`, return `some` on verified BHKS
  recovery, and otherwise continue the bounded doubling schedule until the cap.
- Added focused guards for the low-bound miss, the sufficient-bound BHKS
  success path, the bounded fast wrapper, and `factorWithBound` fallback product
  preservation.
- Updated the nearby `factorFast` comment to describe the current BHKS capped
  fast path.
- Verified:
  - `lake build HexBerlekampZassenhaus`
  - `lake build HexBerlekampZassenhausMathlib`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

- `HexBerlekampZassenhaus/Basic.lean` now has the executable BHKS recovery
  loop expected by #2627.
- The worktree still has a pre-existing unstaged `.claude/CLAUDE.md` change;
  this session did not touch or stage it.

## Next step

- Push the branch and open the PR for #2627.

## Blockers

- None.
