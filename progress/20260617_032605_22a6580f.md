# Progress - 2026-06-17 (session 22a6580f, one-shot #7668)

## Accomplished

Claimed #7668 as a one-shot feature worker. Added the executable
first-success loop determinism skeleton:

- `Hex.factorFastCoreWithBound_eq_some_of_recovery_on_schedule_of_no_prior_recovery`
  in `HexBerlekampZassenhaus/Basic.lean`.
- `HexBerlekampZassenhausMathlib.factorFastCoreWithBound_eq_expected_of_forwardInputs_on_schedule_of_no_prior_recovery`
  in `HexBerlekampZassenhausMathlib/PartitionRefinement.lean`.
- `HexBerlekampZassenhausMathlib.factorFastCoreWithBound_eq_expected_of_forwardInputs_at_cap_of_no_prior_recovery`,
  the canonical `initialHenselPrecision` / `quadraticDoublingSteps + 2`
  cap specialization.

These prove that cap recovery from a `ForwardRecoveryInputs` package is the
actual fast-core loop output once all other scheduled recovery calls are known
to return `none`.

Verification:

- `lake build HexBerlekampZassenhaus` succeeded.
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement` succeeded.
- `git diff --check` clean.
- Added-line forbidden-token scan over touched Lean files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The mathematical BHKS precision-soundness theorem remains open: prove that a
scheduled recovery success implies the Mignotte/cap precision, or directly
prove the no-prior-recovery premise for every scheduled `k ≠ B` below the cap.
Without that theorem, #7668's unconditional cap-output identity is not closed.

## Next step

Work #7671: prove no scheduled `bhksRecover?` success below Mignotte/cap, then
apply the new cap wrapper to obtain the unconditional loop output equality.
In particular, verify that a coarse strict sub-product cannot pass the final
`Array.polyProduct candidates == core` check at an intermediate precision.

## Blockers

None for the landed skeleton. The remaining theorem is the substantive
precision-soundness argument and was split to #7671.
