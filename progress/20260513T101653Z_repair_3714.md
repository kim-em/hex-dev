# Repair session: PR #3714

## Accomplished

- Claimed PR #3714 (`bench: wire HexBerlekamp FLINT comparators`) via
  `coordination claim-pr-repair`.
- Diagnosed the failed `build` CI job: the new
  `runFlintRabinTestChecksum*` / `runFlintDistinctDegreeChecksum*`
  benches in `HexBerlekamp/Bench.lean` route through
  `scripts/oracle/flint_bench_driver.py`, which imports `flint`. The
  build job's pip install step only installed `cysignals fpylll`, so
  21 of 46 benches failed verification with
  `ModuleNotFoundError("No module named 'flint'")`. The conformance
  job already installs `python-flint` for its oracle suite; the build
  job was just out of sync.
- Added `python-flint` to the `Install Python bench comparator
  dependencies` step in `.github/workflows/ci.yml`.
- Verified locally: `lake build hexberlekamp_bench` and
  `lake exe hexberlekamp_bench verify` — all 46 benchmark(s) passed.

## Current frontier

PR #3714 is locally verified after the CI install fix and ready to
push.

## Next step

Commit, push with `--force-with-lease`, and mark PR #3714 salvaged.

## Blockers

None.
