## Accomplished

- Claimed issue #2728 and restored the HexPolyZ Phase-4 evidence package.
- Added `HexPolyZ.phase4.input_families` metadata to `libraries.yml` and re-promoted `HexPolyZ.done_through` from 3 to 4.
- Ran the full `hexpolyz_bench` scientific sweep and committed the export artefact at `reports/bench-results/hex-poly-z-33b7f720dcce.json`.
- Added `reports/hex-poly-z-performance.md` with bench targets, verdicts, comparator accounting, representative profile coverage, and an empty Concerns section.
- Verified with `python3 scripts/check_phase4.py`, `python3 scripts/check_dag.py`, `lake exe hexpolyz_bench list && lake exe hexpolyz_bench verify`, `git diff --check`, and `lake build HexPolyZ`.

## Current frontier

- HexPolyZ now satisfies the stricter Phase-4 structural checks and is back at `done_through: 4`.
- The worktree still has the unrelated pre-existing `.claude/CLAUDE.md` modification that this session did not touch.

## Next step

- Continue with the remaining Phase-4 audit-reset follow-ups for sibling libraries, especially the unclaimed restored-evidence issues that follow the same report pattern.

## Blockers

- None for #2728.
