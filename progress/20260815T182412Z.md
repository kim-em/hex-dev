# Table 10 supporting `a₂` provider

## Accomplished

- Audited all 38 pinned PNT+ supporting declarations. They are exactly rows
  21--24 and 26--59 and uniformly instantiate `a2_mid_le`; rows 20 and 25 use
  a separate existing certified-bound shape.
- Added a Mathlib-free source-indexed package authenticating each `(b, K)`,
  the exact rational target, the 20-decimal `log 2` window, exponential-table
  scale, and complete natural-number head-plus-tail inequality.
- Added one ordinary-kernel theorem covering both branches of PNT+'s copied
  `Inputs.default.a₂` maximum, including the independent
  `2^(floor (b/log 2)+1)` branch. The proof does not consume any migrated
  Table 10 target-coordinate theorem.
- Added generic semantic replay, a source-membership wrapper for all 38 rows,
  and a representative row-21 closure through package chronology and
  `ProofFrontend`.
- Added all-row positive controls; draft-free source, argument, floor, log
  endpoint/scale, base-scale, endpoint, and cross-row replay mutations; an
  ordinary false-endpoint refutation; and guarded axiom reports.
- Updated both interval SPECs, the pinned inventory evidence, Lake
  registrations, and the exact proof-only runtime exemption.
- Built the focused runtime, semantics, and conformance modules. Verified the
  pinned inventory against PNT+ revision
  `21998bb6196b56789f72a52656a781a75e134eb0`; copyright, line-count, DAG,
  phase-4, trust-surface, release-manifest, Mathlib-free bench, freshness, and
  inventory tests all pass.

## Current frontier

All 87 target-numeric calls and all 38 supporting `a₂` calls in the pinned
Table 10 source family now have bounded authenticated providers after
localized PNT+ rewrites. The generated family remains honestly pending.

## Next step

Preserve this local stack until the FKS publication sequence is clear, then
rebase it without broad conflict resolution and obtain an independent review
and exact CI before publication.

## Blockers

The surrounding PNT+ `B_8_exact` dispatch has not yet been migrated. Arbitrary
Table 10 rows and production-scale performance also remain outside this fixed
source acceptance provider. There is no bridge blocker for the completed
38-row supporting layer.
