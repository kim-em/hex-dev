# Slow-arm #2564 decoupling: same mis-architecture pattern as the fast arm

## Accomplished

Investigated whether the slow-modular arm genuinely depends on directive #2564
(van Hoeij CLD restructure / now the headline-correctness umbrella). Verdict:
**no** — the #2564 edges were speculative cycle edges, now stripped.

Evidence:
- The slow-modular irreducibility path (`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`,
  IntReductionMod.lean:4897 → `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`,
  Basic.lean:17747) references the CLD lattice / bad-vector / resultant machinery
  **zero times** (exhaustive grep both directions). It is already built on the
  **forward UFD count equality** (`le_antisymm` of coverage `count_ge` + `count_le`
  → `..._of_count`) + the **Hensel subset correspondence** — the same architecture
  the fast arm was just re-pointed at (#7461). The slow arm is the original home.
- `InitialLiftedFactorSubsetPartitionEvidence` (Basic.lean:18277) is purely
  Hensel/modular (cover/disjoint/unique + recovery fields); no CLD reference.
- The #2564 edges on #6774 and #7362 were explicitly **conditional/uncertain** in
  the issue text ("whether they also require the #2564 structure rewrite", "depends
  on #2564 IF the structure still requires the refuted fixed-subset contract").
- #2564 is the headline-correctness **umbrella** (Deliverables = the headline
  theorem, subsuming the prior directive), so `leaf → #2564` is a cycle
  (`#2564 ← #6672 ← #6774/#7362 → #2564`) — identical to the fast-arm #6779→#2564
  cycle already stripped.
- The "refuted fixed-subset contract" the issues worried about has **no live
  presence in source** (no `fixedSubset`/`refuted` identifier anywhere; predecessor
  issues #7360/#7364/#7433 closed as superseded). No deep underlying refutation
  remains in the slow path (unlike the fast arm's dead resultant).

Actions:
- Stripped `depends-on: #2564` from #6774 and #7362 (via REST; GraphQL rate-limited)
  with explanatory comments.
- Verified the full 12-node capstone path (both arms) is now cycle-clean of #2564.

## Current frontier

Both arms decoupled from the #2564 umbrella and cycle-clean:
- Fast: #7461 (dispatched) → #6771 → capstone #6672.
- Slow: #7453 → #7455 → #7361, plus #7362 (now ready) → #6774 → capstone #6672.
  All Hensel/modular producers; #7447 landed.

## Next step

Dispatch the freed slow leaves #7453 and #7362 (pod once needs GraphQL, which was
rate-limited; resumes on reset). Then both arms grind toward capstone #6672 →
FactorSoundness.lean:18 → #2564.

## Blockers

None hard. GitHub GraphQL rate limit was transiently exhausted (many gh calls +
watcher); REST used for edits. pod once dispatch resumes on GraphQL reset.
