# 2026-05-06 21:58 UTC — work session (7668cf2a)

## Accomplished

Claimed #2439, triaged it as too broad for one proof session, and decomposed it
into:

- #2521 for structured row-pivoted `pivotLoop` branch API.
- #2522 for the regular row-pivoted determinant invariant.
- #2523 for the pivot-search-failure determinant-zero lemma.

Marked #2439 for replan with the required `Decomposed into #2521, #2522, #2523`
breadcrumb, then claimed #2521.

For #2521, updated `HexMatrix/Bareiss.lean` by exposing structured pivot search
and adding public row-pivoted branch equations:

- `pivotLoop_zero_fuel`
- `pivotLoop_done`
- `pivotLoop_regular_branch_no_swap`
- `pivotLoop_singular_branch_no_pivot`
- `pivotLoop_regular_branch_swap`

Verification completed:

- `lake build HexMatrix.Bareiss`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Bareiss.lean`

The only `sorry` hit in `HexMatrix/Bareiss.lean` is the pre-existing
`bareiss_eq_det` capstone.

## Current frontier

#2521 is implemented and ready for PR creation. The downstream issues #2522 and
#2523 are blocked on #2521.

## Next step

Create the PR for #2521.

## Blockers

`lake build HexMatrixMathlib.Determinant.Bareiss` was started but stopped after
several minutes because it was compiling a large Mathlib dependency set rather
than reaching the local target. The worktree still has a pre-existing unrelated
`.claude/CLAUDE.md` modification.
