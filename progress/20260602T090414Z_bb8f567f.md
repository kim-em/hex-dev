# 20260602T090414Z bb8f567f — Issue #6263

## Accomplished

Closed sub-issue 4 of 4 of the BHKS D1 bridge construction (#5512):

- **`HexBerlekampZassenhausMathlib/BadVector.lean`**: Added
  `ExecutableBadVectorWitness.localFactorDegree_pos_of_bridge_data` as the
  third `_of_bridge_data` accessor, matching the pattern of
  `coprime_input_aux_over_rat_of_bridge_data` and
  `resultant_divisible_by_p_pow_of_bridge_data` already in the file.
- **`HexBerlekampZassenhausMathlib/Recovery.lean`**: Added
  `bridgeOfLiftData`, the `ProjectedBadVectorSetupBridge` constructor
  specialised to the `badVectorWitnessOfLiftData` witness, plus
  `capSeparationOfBridgeData`, the single consumer wrapper composing the
  bridge with `ExecutableCapSeparationHypotheses.ofProjectedBadVectorSetupBridge`.
  The cut, `hp`, and `hlt` side conditions remain explicit (#5204 / #5216
  territory).

## Deviation from spec

Issue #6263 placed `bridgeOfLiftData` in `BadVector.lean`. Since
`badVectorWitnessOfLiftData` lives in `Recovery.lean` and the import chain
runs BadVector ← TerminationBound ← Recovery, placing `bridgeOfLiftData` in
BadVector.lean would create a circular import. The function naturally
belongs alongside `badVectorWitnessOfLiftData`, so it went in Recovery.lean
together with the consumer wrapper.

## Current frontier

The four-field bridge is fully assembled. Consumers of
`badVectorWitnessOfLiftData` can now produce
`ExecutableCapSeparationHypotheses` directly from a `BadVectorBridgeData`
plus the analytic side conditions.

## Next step

`#5204` (auxiliary polynomial l2/Cauchy-Schwarz bound) and `#5216`
(cut-radius separation theorem) discharge the remaining `hp` / `hlt` /
`hcut` parameters. Then `#5237` assembles `factorFast_terminates`.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVector` — green
- `lake build HexBerlekampZassenhausMathlib.Recovery` — green
- `lake build HexBerlekampZassenhausMathlib` — green
- `python3 scripts/check_dag.py` — exit 0
- `git diff --check` — clean
- No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`.
