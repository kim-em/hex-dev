# HexBZ direct-coordinate factorization evidence

## Accomplished

- Recorded the final 392-row Hex sweep at source revision `081b7a67`: 375
  solved, 17 unsolved, 338.141 microsecond median, 6.995 millisecond p90, and
  8.130 second slowest solved row.
- Rebuilt the current comparison report and figures from that Hex record plus
  the unchanged same-host FLINT, PARI, NTL, Isabelle BZ, and Isabelle LLL
  records.
- Removed superseded diagnostic Hex routes from default current-state plots
  while retaining explicit plotting support for named historical records.
- Regenerated the conformance fixture after the direct engine changed the
  unspecified order of the two `adv/swinnerton_dyer_sd5_pair` factors.
- Verified exact fixture reproduction, 106 FLINT oracle cases, and all 54 BZ
  trace-gate cases.
- Opened PR #9114 and obtained a fresh independent review of the complete diff.
  The review found no mathematical soundness, completeness, dispatcher
  totality, or direct-CLD coordinate defect.
- Fixed the review's certain CI failure by removing four deleted executable
  targets, added lifted-factor width to the deterministic trace baseline, and
  restored explicit classical-tier negative-control guards.
- Replaced the generic `Data.lean` with `CoreProblem.lean`; prime plans, lift
  plans, search outcomes, and dispatch traces now live with the corresponding
  mathematical or algorithmic objects.
- Corrected stale module and coordinate documentation and disclosed the
  current p90, SD5, and `xpow120_minus1` regressions plus the benchmark
  worktree-provenance marker.

## Current frontier

The review blockers and actionable cleanup are fixed. Focused executable,
Mathlib-bridge, and conformance builds are green. The public proof modules use
mathematical object names; no `PublicSurface.lean`, generic `Data.lean`, or
corresponding import remains.

## Next step

Commit and push the review fixes, run the complete local validation set and PR
CI, address any failures, and merge.

## Blockers

None.
