# 2026-06-04T10:40:57Z — Sub-B of #6533: bareiss-step divisibility on canonical StepWitness

Session UUID prefix: 86f25f22
Issue: #6536

## Accomplished

- Added private theorem `noPivotLoop_initial_gram_bareiss_step_dvd` in
  `HexGramSchmidt/Int.lean`, immediately after
  `bareissGramRowInvariant_noPivotLoop_initial_canonical`.
- Statement: at every reachable regular step of the no-pivot Bareiss
  pass over `Matrix.gramMatrix b`, the matrix-level Bareiss numerator
  `state.matrix[k][k] * state.matrix[i][j] - state.matrix[i][k] * state.matrix[k][j]`
  is divisible by `state.prevPivot`, for any column `j`.
- Witness: dot of `Matrix.rowCombination b (Vector.ofFn hq.q)` against
  `b.row j`, where `hq` comes from the canonical-`StepWitness` quotient
  package at the regular step.
- Proof closes the obligation as the directive prescribes:
  `entry_eq_dot` to rewrite both `[i][j]` and `[k][j]` to dots,
  `dot_bareiss_row_update_left` (reversed) and
  `rowCombination_bareiss_coeff_update` (reversed) to fuse into a single
  row combination, `coeff_num_eq_mul` to convert the inner vector into
  `q a * state.prevPivot`, then `dot_rowCombination_mul_right_int` to
  factor out `state.prevPivot`.
- `lake build` clean across all targets; sorry count in `Int.lean`
  unchanged at 3.

## Current frontier

Sub-B done. The parent (#6533) and any further sub-issues that build on
this matrix-level divisibility are unblocked for the next worker.

## Next step

A follow-up consumer can use this lemma to discharge integer
`exactDiv`-style obligations for the matrix-level Bareiss step on the
initial Gram trajectory (the next layer above the coefficient quotient
package).

## Blockers

None.

## Notes

- `have hinv := …` makes the local hypothesis opaque; switching to
  `let hinv := …` was needed so the implicit `hinv` argument of
  `bareissGramRowInvariant_noPivotLoop_initial_canonical` unifies with
  the `hinv` argument of `hquot fuel hinv h_canon …`.
- `ring` is unavailable in this Mathlib-free file; used
  `Int.mul_comm _ _` to flip the final factor.
