# 20260603T050712Z work #6440

## Accomplished

- Claimed directive issue #6440 and implemented a new row-major
  `scaledCoeffSchurRows` executable path for
  `Hex.GramSchmidt.Int.scaledCoeffRows`.
- The new path precomputes the Gram rows once, reads determinant values from
  the existing no-pivot Gram determinant vector, fills each coefficient row
  left-to-right, and computes strict lower-triangle entries through the
  Schur-complement sigma recurrence.
- Routed public `scaledCoeffRows` through the Schur-row path while leaving the
  older column-major Bareiss helpers in place as proof infrastructure.
- Replaced the old-loop-specific proof bodies at the public diagonal/lower
  compatibility boundary with explicit `sorry` placeholders; no `axiom` or
  `native_decide` was introduced.

## Current frontier

`HexGramSchmidt.Conformance`, `HexLLL.Conformance`, and full `lake build` pass
with the new executable path, but the fresh LLL comparator benchmark exports
are blocked.  Even a smoke run of
`Hex.LLLBench.runFirstShortVectorHarshCubicNormSq15` stayed runtime-bound after
the executable rebuild and had to be terminated.  The initial random-bounded
full export also had to be terminated after it was still at the low rungs well
past the expected time.

## Next step

Profile the Schur-row executable path before regenerating reports.  Likely
follow-ups are to reduce array access/update overhead in the sigma loop and to
prove or specialize the recurrence enough that benchmark registration and fixed
hash checks do not spend minutes in the new pure-Lean path.

## Blockers

- Benchmark deliverables are not honest yet: no
  `hex-lll-random-bounded-schur.json`,
  `hex-lll-harsh-cubic-extended-schur.json`, regenerated SVGs, or refreshed
  `reports/hex-lll-performance.md` numbers were committed because the smoke
  benchmark did not complete in a reasonable time.
- The proof surface is partial: six old Bareiss-loop compatibility statements
  now contain `sorry` and need proper Schur-recurrence proofs before this can
  become a complete proof-clean rewrite.

## Verification

- `lake build HexGramSchmidt.Conformance HexLLL.Conformance` passed.
- `lake build` passed.
- `git diff --check` passed.
- Added-line forbidden-token check found no `axiom` or `native_decide`; added
  `sorry` placeholders are intentional and grep-able for the partial proof
  frontier.
