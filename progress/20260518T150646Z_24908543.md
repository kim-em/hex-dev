# Session 24908543 — planner cycle (POD_QUEUE_DEFICIT=1)

## Accomplished

- Oriented on a 2-unclaimed / 1-claimed queue at `origin/main d61f920c`:
  - Unclaimed: #5017 (Basic.lean:5597 inline `.2.1` rewire), #4334
    (HexLLL Phase 4 host-load-gated benchmark verdicts).
  - Claimed: #5015 (5 unused `hcore_ne` underscore-prefix).
  - PR #4917 sits `repair-claimed` with `CONFLICTS`, an open
    repair-flow target across four summarize windows; PR #5019
    (summarize #5018) has pending checks; PR #2656 is a draft.
- Skipped Step 2 (merge-ready PRs): the only mergeable open PR
  (#2656, draft) is not eligible; PR #5019 is mergeable shape but
  checks are still pending.
- Skipped Step 3 (broken-PR fix issues): the sole broken PR (#4917)
  is `repair-claimed`, so no new fix issue is filed.
- Read the bodies of the 14 open `agent-plan` issues plus the 3
  directives (#2637, #2567, #2564). Confirmed the post-#4991
  helper-extraction + boilerplate-cleanup cascade has converged at
  `Basic.lean:6720-6749`: the Monic-route projection cluster
  exposes `size_pos`, `ne_zero`, `lc_pos`, `primitive` (the `.2.1`
  field of `monic_primitive_sign_normalized_of_monic`). The
  `.2.2` field (`Hex.normalizeFactorSign f = f`) has no named
  helper, and 5 inline `obtain ⟨_, hcontent, hnorm⟩` consumers
  destructure the full triple to access this field.
- Filed one atomic feature issue closing the cluster on the `.2.2`
  field. Per-cycle deficit was 1 (queue depth 2, min queue 3), so
  one issue is the target.

## Issue filed

* **#5021** — *BZMathlib: extract `zpoly_normalize_factor_sign_of_monic`
  helper and rewire 2 inline destructure sites*. Adds the fifth
  cluster sibling at `Basic.lean:6750` (immediately after
  `zpoly_primitive_of_monic`) and rewires the two cleanest of the
  five inline destructure sites (`Basic.lean:7828-7831` and
  `9534-9540` — both pair `hcontent` with
  `Hex.ZPoly.primitivePart_eq_self_of_primitive` through the
  `simpa [Hex.ZPoly.Primitive] using hcontent` adapter, which the
  rewire eliminates by routing through `zpoly_primitive_of_monic`).
  The three other inline sites (`10083, 10193, 13595` — passing the
  destructured values into existentials or positional theorem
  calls) are deliberately out of scope for atomicity; a later
  /plan cycle should file the follow-up after #5021 lands.

## Current frontier

* Cluster-completion plan posted as #5021.
* `set-target` left untouched (queue is still actively refilling).

## Next step

A subsequent /plan cycle, after #5021 lands, can file the follow-up
rewires for the three remaining inline triple-destructure sites
(`Basic.lean:10083, 10193, 13595`). Each rewires through both
`zpoly_primitive_of_monic` and the new
`zpoly_normalize_factor_sign_of_monic`; the per-site work is similar
in size but the consumers' shape (existentials and positional
theorem calls) needs per-site type inspection that #5021's worker
already did for the two cleanest sites.

## Blockers

None.
