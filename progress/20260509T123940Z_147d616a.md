# 20260509T123940Z — DDF bucket Frobenius invariants (#2787)

## Accomplished

Claimed feature issue #2787 and replaced the `sorry` in
`HexBerlekamp/DistinctDegree.lean` theorem
`distinctDegreeFactor_bucket_invariants` with a proof.

The proof adds private DDF-local support lemmas showing that unit
polynomials divide arbitrary targets, that multiplying a divisor by a unit
preserves divisibility, and that the same-degree power loop emits only
factors dividing its active Frobenius difference when the current residual
divides the original square-free input.  The outer DDF loop now carries the
residual-divides-input and Frobenius-state invariants through bucket
appends, combining the existing positive-degree proof with the new
Frobenius-divisibility proof.

Verified:

- `lake build HexBerlekamp`
- `lake build HexBerlekampMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekamp/DistinctDegree.lean`

## Current frontier

The issue deliverable is complete.  `HexBerlekamp/DistinctDegree.lean` has
no remaining `sorry` or banned markers.

The worktree still contains a pre-existing unstaged `.claude/CLAUDE.md`
change from before this session; it was not touched or staged.

## Next step

Push the branch and open the PR closing #2787.

## Blockers

None.
