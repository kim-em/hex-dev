# 20260603T232429Z work #6503 (skipped)

## Accomplished

Diagnosed and skipped issue #6503 (umbrella close-out for #6458) per the
directive-handling rule. Posted full chain analysis as
[issue comment](https://github.com/kim-em/hex/issues/6503#issuecomment-4617525440).

## Diagnosis

The directive's proposed proof for the residual sorry at
`HexGramSchmidt/Int.lean:6028` (`getArrayEntry_scaledCoeffRowsSchur_eq`)
is circular:

* The suggested composition uses `scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix`
  (line 7199, added by #6502) to convert the Schur-side LHS to a
  `noPivotLoop` matrix entry.
* But `scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix`'s proof body
  uses `getArrayEntry_scaledCoeffRowsSchur_eq` itself (i.e., the sorry)
  as a rewrite step.
* Every bridge formula connecting `scaledCoeffs` to `bareiss(scaledCoeffMatrix)`
  (`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`, `scaledCoeffs_eq`)
  routes through the same sorry transitively.

Closing the sorry requires a Schur-side characterization
(`getArrayEntry scaledCoeffRowsSchur i j = bareiss(scaledCoeffMatrix b i j hji)`
for `j ≤ i`) that does not exist in the bridge layer. The σ-chain helpers
`schurSigma_foldl_eq_noPivotCorrection_zero` (line 7067) and
`schurSigma_noPivotCorrection_succ` (line 7167) are written as base/succ
building blocks but the synthesis into the full characterization (which
is a Sylvester-style cofactor identity) is not in place. That synthesis
is new math, not pure composition through existing bridge facts —
violating the directive's "no new math" premise.

The docstring at `HexGramSchmidt/Int.lean:6011` itself acknowledges this:
the Schur side needs an analogous characterization built directly from
the `schurScaledCoeffEntry`/`schurSigma` recurrence.

## Current frontier

Issue #6503 is back in the queue with `replan` label. Suggested re-spec
options noted in the comment:

1. Spec the Schur-side characterization as its own directive (using
   the existing σ-correction helpers); then #6503's deliverables
   become genuine composition.
2. Split #6503 into a refactor-only PR (rename + redesign + provider
   construction + `ofBasis_valid` move) and a follow-up gated on (1)
   for the migration + sorry closure.

## Next step

Replanner picks up #6503 and applies one of the re-spec options.

## Blockers

None for this session. The work item is a directive blocked by a
premise issue, not by infrastructure I could fix in-session.

## Verification

- Diagnosed claim verified by `grep` of
  `scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix` body
  (`HexGramSchmidt/Int.lean:7199–7210`).
- No source files modified; no commits made.
- Issue #6503 skipped via `coordination skip` (claim released,
  `replan` label applied).
