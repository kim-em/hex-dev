# Issue #6188: public prime coprime helper

## Accomplished

- Claimed #6188 after confirming directive issues were blocked and #6155 had been claimed by another session.
- Promoted `Hex.Nat.Prime.coprime_of_not_dvd` from private to public in `HexArith/Nat/Prime.lean` with a one-line docstring.
- Replaced the duplicated gcd case split in `HexModArith/Prime.lean` with a call to the public helper, using symmetry for the `Nat.Coprime aval p` orientation required by `ZMod64.inv_mul_eq_one`.
- Committed the code change as `af6bdbb1` (`feat: expose prime coprime helper`).

## Current frontier

- #6188 deliverables are complete.
- The worktree still has an unrelated unstaged `.claude/CLAUDE.md` modification that this session did not touch.

## Next step

- Publish the PR for #6188.
- Remaining unclaimed Phase 6 cleanup includes #6187 and #6190; #6155 is claimed by another session.

## Blockers

- None.
- Verification passed: `lake build HexArith.Nat.Prime`, `lake build HexArith`, `lake build HexModArith.Prime`, `lake build HexModArith`, full `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, and corrected forbidden-token diff grep over touched Lean files.
