# Progress: BZMathlib abstract-bound `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`

Issue: #4934. Branch: `agent/cf8429c2`.

## Accomplished

- Added `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean` as the monic abstract-bound
  sibling of `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`.
  Takes `(B' : Nat)`, the universal core-divisor coefficient bound
  `hvalid : ∀ g : Hex.ZPoly, g ∣ core → ∀ i, (g.coeff i).natAbs ≤ B'`, and
  `hprecision : 2 * B' < d.p ^ d.k` in place of the core-shape
  `defaultFactorCoeffBound core` precision constraint (Option A: the
  universal `g ∣ core` form, since `f_cov`, `g`, and `quotient` are bound
  locally inside the proof body).
- At each of the five `_of_bound` substrate call sites, the per-factor
  `hvalid` is specialised to the local divisor:
  - `not_represents_empty_of_irreducible_dvd_core_of_bound` (empty-`J`
    step) receives `hvalid g hg_dvd_core`.
  - `representsIntegerFactorAtLift_monic_of_bound`,
    `recombinationCandidate_eq_factor_of_recovery_of_monic_core_of_bound`,
    and `natDegree_toPolynomial_eq_sum_of_represents_of_bound`
    (cover-at-min recovery and natDegree positivity) each receive
    `hvalid f_cov hf_cov_dvd_core`.
  - `liftedFactorSubsetPartition_prefix_none_of_bound` (prefix-none
    discharge, #4931) receives the universal `hvalid` unchanged.
- In the recursive IH call (step 8 of the original), `hvalid` and `B'`
  are captured by closure — no transport needed, because `core` is
  fixed throughout the induction.
- Rewired the original
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  as a thin wrapper that instantiates
  `B' := Hex.ZPoly.defaultFactorCoeffBound core` and discharges the
  universal `hvalid` via `defaultFactorCoeffBound_valid core hcore_ne`.
  The wrapper preserves the existing signature exactly; the downstream
  consumer at line ~14780
  (`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`)
  continues to type-check unchanged.
- Updated the existing docstring on the (now-wrapper) original to
  note its thin-wrapper status.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — no new errors at
  the new `_of_bound` (line 14063) or the rewired wrapper (line 14389).
  Pre-existing environment-specific failures unchanged: whnf timeouts
  at 4927/5161/5469/5556/5603/5840/5939/6084/6228/6247 in front half;
  `Dependent elimination failed` at 5840; kernel unknown-constant
  errors at 6562/6626; and the back-half whnf timeouts and constant
  errors shifted by the +69 line insertion (now 16090/16201 whnf,
  16389/16435 kernel). Per the issue body these are environment-
  specific and not introduced by this change; CI is the authority.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean |
   grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` — no
  matches.

## Current frontier

The monic-side A2 abstract-bound propagation has now climbed past the
recursive-coverage auxiliary
`recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`.
The next natural targets are:
- the public wrapper
  `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
  (line ~14780), lifted to an `_of_bound` sibling extracting per-factor
  coverage at a supplied `factor` (mirror of the already-landed scaled
  flavour `_of_bound` at line ~14828);
- the scaled companion auxiliary already lives at line 14389 in
  `_of_bound` form (landed in a prior PR).

## Next step

Continue the A2 abstract-bound propagation cycle into the
`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_bound`
public-wrapper layer, then into the exhaustive-coverage layer in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

## Blockers

None for this issue. The broader Basic.lean module remains red on
the host due to pre-existing whnf timeouts and unknown
kernel-constant errors; CI is the authority on full typecheck.
