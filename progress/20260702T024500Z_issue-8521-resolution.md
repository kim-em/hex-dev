# 2026-07-02 — #8521 resolution: PR evaluation, merge, hprec discharge

Interactive session (Kim + Claude), branch `issue-8417`.

## Accomplished

- Evaluated the two competing resolutions of #8521: PR #8522 (codex) and
  PR #8523. Both make the identical executable fix
  (`factorFastPrecisionCap` keyed on `bhksBound core`); #8523 strictly
  dominates (machine-checked `#guard` counterexample, SPEC update, proper
  rename of the false-premised lemma, call-site-shaped
  `…_of_choosePrimeData` discharge variant, fuller validation).
  Independently verified the counterexample family `(xᵃ−1)(xᵃ⁺¹−1)`:
  crossover at exactly `a = 18`, ratio ≈ 4.53 at the witness.
- Runtime evaluation with compiled A/B probes (baseline `origin/main` vs
  fixed): conformance 100/0, trace 50/0, fixtures byte-identical,
  `hexbz_bench verify` 18/18, `runFactorChecksum` deg 16–24 at parity.
  Direct `factorLattice` probes: counterexample family 92 vs 89 ms
  (identical output), `(x−1)²(x⁴+1)` (cap-shrink direction) identical
  output, SD3 1940 vs 1931 ms. SD4/SD5/SD6 caps bit-identical pre/post
  (core = f), so the SD tail cannot regress.
- Merged #8523 (squash; closes #8521). Closed #8522 as superseded.
- Merged post-merge `origin/main` into `issue-8417` and discharged the
  `hprec` sorry at the lattice call site with
  `Hex.two_mul_bhksBound_squareFreeCore_lt_pow_cap_of_choosePrimeData f
  primeData hchoose` — verified green locally on an identical tree
  (eval branch build: 3756 jobs, 3 sorry declarations remaining in
  `LatticeTier.lean`).

## Current frontier

`issue-8417` branch (PR #8517) is now a 3-sorry frontier, all genuine
deep content: `latticeArm2_fastCore_count` (CLD count-equality),
`latticeArm3_bhksSingleAllOnes_irreducible` (`hclasses` adequacy heart),
and the `reassemblyExpansionComplete` side condition in
`factorLatticeFactorsWithBound_factor_irreducible`.

## Next step

Follow-up issues filed this session: `cldCoeffFloor core ≤
factorFastPrecisionCap f` dominance (needed by #8417 completeness), and
lattice-tier trace/conformance/bench coverage (the corpus never leaves
the classical tier, so cap changes are currently test-invisible).
SD4 `factorLattice` wall-clock observation (>12 min CPU both versions,
vs 1.9 s for SD3) recorded on #8417.

## Blockers

None.
