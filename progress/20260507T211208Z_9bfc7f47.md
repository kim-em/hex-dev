# 2026-05-07 21:12 UTC - BHKS LLL cut projection (9bfc7f47)

## Accomplished

Claimed issue #2608 and added the BHKS LLL cut/projection slice in
`HexBerlekampZassenhaus/Basic.lean`:

- `BhksProjectedRows` for the compact projected `L'` row surface.
- `bhksCutRadiusSq4` computing the all-integer `4 * B'^2 = 4r + n*r^2` cut
  radius.
- `bhksProjectedRows`, which runs `lll.shortVectors` on `BhksLatticeBasis`,
  computes Gram-Schmidt squared lengths from `GramSchmidt.Int.gramDetVec` as
  `d_{i+1}/d_i`, filters by the BHKS cut, and projects surviving rows to the
  first `r` indicator coordinates.
- Guards for the cut-radius arithmetic and projection width/contents.

Verification passed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The BHKS fast-path now has executable CLD lattice construction plus the
follow-on LLL cut/projection surface that produces `L'`. Public factorization
behavior remains unchanged.

## Next step

Add the RREF/equivalence-class recovery stage over `BhksProjectedRows`, then
wire candidate reconstruction and exact-division verification in later slices.

## Blockers

No blockers for this slice. The pre-existing pod-managed `.claude/CLAUDE.md`
worktree modification remains untouched.
