# 2026-05-05 22:40 UTC — work session (459da6b6)

## Accomplished

Claimed #2399 after both open human-oversight directives were rejected by
`coordination claim` as `needs replan`.

Added the Mathlib-side Bareiss Desnanot reindexing scaffold in
`HexMatrixMathlib/Determinant.lean`:

- `bareissDesnanotIndex`, the permutation ordering a `(k+2) × (k+2)`
  bordered minor as `[k, 0, ..., k-1, k+1]`.
- simp lemmas for its first and last positions.
- `det_borderedMinor_bareissDesnanotIndex`, showing same-axis reindexing
  preserves the Hex bordered-minor determinant.
- `desnanot_jacobi_borderedMinor_reindex`, specializing the inlined
  Mathlib Desnanot-Jacobi identity to the reindexed next bordered minor.

## Current frontier

The Desnanot identity is now available at the correct reindexed Mathlib
matrix. The remaining work is to rewrite its four deleted-row/column
subdeterminants back to the corresponding Hex bordered minors, including the
single-axis permutation signs for the off-diagonal minors, then compose with
`bareissExactDiv_borderedMinor_of_mul_eq`.

## Next step

Prove focused determinant rewrite lemmas for the Desnanot minors of
`bareissDesnanotIndex`:

- interior minor equals the previous leading/bordered pivot determinant;
- `M11` equals the current `i,j` bordered minor;
- `Mkk` equals the `k,k` bordered pivot minor;
- `M1k` and `Mk1` equal the `i,k` and `k,j` bordered minors up to matching
  row/column permutation signs whose product cancels in the Desnanot formula.

Then package the rewritten identity in the exact `hdesnanot` shape expected by
`bareissExactDiv_borderedMinor_of_mul_eq`.

## Blockers

No technical blocker. Verification passed with only pre-existing warnings:

- `lake build HexMatrixMathlib.Determinant`
- `lake build HexMatrixMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
