# 96273a12 — HexArith UInt64 wide-word API review (issue #5102)

## Accomplished

- Claimed review issue #5102 after confirming there were no unclaimed
  directive issues in the label-filtered queue.
- Audited `HexArith/UInt64/Wide.lean` against Phase 6 API-polishing
  criteria: public documentation, theorem naming, simp coverage, and
  characterising lemmas.
- Checked downstream usage in `HexArith/Barrett/Reduce.lean`,
  `HexArith/Montgomery/Redc.lean`, and
  `HexArith/Montgomery/Context.lean`.
- Posted the review comment on #5102 with PASS/DEVIATION findings and a
  follow-up issue draft for the concrete Montgomery `mulFull` usage gap.

## Current frontier

The wide-word API itself looks documented and usable. Downstream proofs use
`UInt64.toNat_mulHi`, `UInt64.toNat_addCarry`, `UInt64.mulHi_mulLo`, and
`UInt64.mulFull_eq_mulHi_mul` rather than unfolding the executable wide-word
definitions.

The audit found one executable-path deviation from the Layer 1 contract:
`MontCtx.toMont` and `MontCtx.mulMont` still split products with separate
`UInt64.mulHi` and wrapped multiplication instead of `UInt64.mulFull`.

## Next step

Create and dispatch a follow-up feature issue to rewrite the two Montgomery
product-splitting definitions through `UInt64.mulFull` while preserving the
existing public theorem statements.

## Blockers

None. Review verification passed:

- `rg -n "sorry|TODO|FIXME|axiom|native_decide" HexArith` returned no matches.
- `python3 scripts/status.py HexArith` reports Phase 6 ready with
  `done_through: 5`.
- `lake build HexArith` completed successfully.
- `git diff --check` exited 0.
