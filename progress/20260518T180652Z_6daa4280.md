# Session 6daa4280 — review #5050 (+ skip #4334)

## Accomplished

Posted feasibility audit on #5050 covering inversion of the
`monic_primitive_sign_normalized_of_monic` cluster (deferred from #5040's
verdict-(c)). Audit at
https://github.com/kim-em/hex/issues/5050#issuecomment-4480483238.

Four sections per the deliverables template:
- §1 substrate availability check for each of the 4 cluster helpers
  (`zpoly_size_pos_of_monic`, `zpoly_lc_pos_of_monic`,
  `zpoly_primitive_of_monic`, `zpoly_normalize_factor_sign_of_monic`).
  Conclusion: substrate (`leadingCoeff_eq_coeff_last`, `content_dvd_coeff`,
  `Hex.ZPoly.content`, `Hex.normalizeFactorSign`) all available at the
  pre-umbrella location (~line 4486) — no new lemma needed.
- §2 collapsed umbrella body sketch (3-line anonymous constructor).
- §3 net diff estimate: ~−8 lines on `Basic.lean`, dominated by umbrella
  body shrink from ~32 to ~9 lines, partially offset by helper bodies
  growing from 2-line destructures to 13-line / 6-line first-class proofs.
- §4 verdict (a): single feature issue. Justification: zero new substrate,
  zero callsite churn (umbrella has only the two corollary callers — every
  downstream consumer already uses the corollaries), helpers are tightly
  coupled so splitting per (b) gains nothing. Full follow-up issue body
  inline-drafted per the canonical Current state / Deliverables / Library
  placement / Context / Verification template, ready for the next planner
  cycle to post via `coordination plan`.

Also skipped #4334 (HexLLL Phase 4 inconclusive verdicts) at session
start: host carica loadavg 14.09 / 12.42 / 12.90 on 24 cores at claim,
warm `lake exe hexlll_bench list` probe 21.68s wallclock (~657×
inflation vs ~33ms baseline). Same documented host-contention regime as
the recent skip chain (8fff8530, cfd4cc30, 90a573bd, 07a1f4ed, fb04cae6).
Replan triage will pick up the issue when carica is idle or on dedicated
hardware.

## Current frontier

`scripts/check_dag.py` exit 0; `git diff --check` clean. The only
`git diff origin/main` content under tracked files is a pre-existing
modification to `.claude/CLAUDE.md` (present in the session's initial
git status, off-limits to agents per
`.claude/CLAUDE.md`#Off-limits Files) — left untouched.

No code changes; this is a read-only review issue. PR-as-deliverable
does not apply.

## Next step

Next planner cycle picks up #5050's inline-drafted follow-up body and
creates the inversion feature issue with `depends-on: #5050`. Worker
that claims the follow-up hoists `zpoly_primitive_of_monic` and
`zpoly_normalize_factor_sign_of_monic` above the umbrella, replaces
their destructure bodies with the content-1 / normalizeFactorSign
case proofs lifted from the umbrella, and collapses the umbrella body
to the 3-line anonymous-constructor term sketched in §2.

#4334 stays in replan for the next eligible host-idle cycle.

## Blockers

None for #5050 (audit complete). #4334 blocked on carica idle / dedicated
hardware availability per SPEC/benchmarking.md "Scientific timing runs".
