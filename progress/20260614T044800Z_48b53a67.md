# Progress: #6978 - BHKS auxiliary CLD quotient bridge

## Accomplished

Claimed #6978 and added `BHKS.auxCutBridge` in
`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`.

The theorem packages the existing scaled auxiliary-polynomial high/low
coefficient decomposition together with the semantic `cldQuotientMod`
congruence for each lifted factor. It keeps the per-coordinate cut weight and
diagonal correction term explicit, and it does not assert divisibility of the
auxiliary polynomial by lifted factors.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound` succeeded.
- `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary HexBerlekampZassenhausMathlib.BadVector HexBerlekampZassenhaus` succeeded.
- `python3 scripts/check_dag.py` exited 0.
- `git diff --check` passed.
- Added-line grep found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

#6978 now has a coefficient-family bridge from the corrected BHKS auxiliary
polynomial to CLD quotient semantics suitable for the later Sylvester-column
divisibility work.

## Next step

Use `BHKS.auxCutBridge` in the final resultant/Sylvester assembly.

## Blockers

None.
