# 20260602T153055Z 7bd98fb2 — Issue #2567

## Accomplished

- One-shot dispatch on directive #2567 (HO-4, BHKS Theorem 5.2 /
  `factorFast_terminates`).
- Added a prime-lower-bound-internalised variant of the top
  cap-separation-plus-bridge-data-plus-canonical-supports HO-4 capstone in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound_internalPrimeLowerBound`.
- The wrapper drops the caller-side
  `hp : 2 ≤ (factorFastCapLiftData f primeData).p` obligation by deriving
  the lower bound from `hchoose` via `Hex.choosePrimeData?_prime` and
  `Nat.Prime.two_le`, using definitional equality of
  `(factorFastCapLiftData f primeData).p` and `primeData.p` through
  `henselLiftData_p`.

## Current frontier

The remaining genuine mathematical inputs to `factorFast_terminates`
still come from the open D1 prerequisites #6344 (canonical true-support
recovery, has-pr/PR #6358) and #6345 (per-class Mignotte product
congruences, blocked on #6344). The final-assembly composition lives in
#6346 and is blocked on #6345.

This PR narrows the caller obligation surface on the prime side of the
top canonical-support capstone so that the eventual final theorem call
site needs one fewer trivially-derivable `2 ≤ p` discharge step. A
companion `1 ≤ factorFastPrecisionCap f` internalisation is in flight on
PR #6364 for a different (canonical-indicators) capstone; the two
internalisations target different parameters and capstones so they
compose at the final assembly site rather than overlap.

## Next step

Wait on #6344 → #6345 → #6346. Once #6344 lands canonical supports and
the `ExpectedTrueFactors` package, and #6345 lands the per-class modular
product congruences, #6346 can compose them through the existing
canonical-support wrappers (now with one less prime-lower-bound side
condition to discharge) to produce the public `factorFast_terminates`
theorem.

## Blockers

- Final `factorFast_terminates` cannot land until #6344 (claimed,
  PR #6358 active) and #6345 (blocked on #6344) close. The decomposition
  is by design; this session contributes an incremental wrapper rather
  than stepping on the active sub-issue work.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
