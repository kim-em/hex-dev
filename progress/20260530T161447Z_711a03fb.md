# Progress — 2026-05-30T16:14Z — session 711a03fb (`/replan`)

## Accomplished

`/replan` triage session. `coordination list-replan` returned one
candidate:

- #5836 "HexMatrix: prove Gram-trajectory Sylvester identity for
  Bareiss cofactor preservation"

Session `40b4f09d`'s skip comment on #5836 contains a worked-out
algebraic argument showing that the prescribed proof technique
(Desnanot–Jacobi applied to a `(k+2)×(k+2)` Gram submatrix) cannot
close the target identity: the LHS is degree `2k + 1` in matrix
entries, standard non-singular DJ is `2k + 2`, and every singular-DJ
choice of duplicated row/column collapses to `0 = 0`. The identity
is structurally a **3-term Plücker relation** on the `(k+2)×(k+1)`
Gram submatrix, requiring either Bazin's identity or a 2-column
Laplace expansion — neither of which is a reindexing of the existing
`bareissDesnanotIndex` infrastructure.

Treated as "Plan stale / approach changed":

- Filed corrected replacement #5839 "HexMatrix: prove Gram-trajectory
  3-term Plücker identity for Bareiss cofactor preservation". Same
  target identity, but the body now states the 3-term Plücker
  reframing, offers two acceptable proof routes (2-column Laplace
  expansion as prerequisite, or Bazin's identity directly), drops
  the original's "small extension of `bareissDesnanotIndex` may be
  required" hint, and notes the nondegeneracy hypothesis may be
  weaker than `det_desnanot_jacobi_int`'s since the routes use a
  pure polynomial identity in matrix entries.
- Closed #5836 with `--reason "not planned"` and a forward-link
  comment pointing at #5839, preserving the skip-comment analysis
  on the closed issue.

`coordination list-replan` is now empty.

Side note: created and immediately closed #5838 (literal `--help`
title) after misreading `coordination plan`'s usage. No-op artefact.

## Current frontier

`/replan` queue empty. #5839 is unclaimed and unblocked, ready for a
fresh worker to pick up. Two routes documented in the body; the
worker chooses execution-level.

## Next step

The next planner pass should NOT re-file #5836's downstream consumer
work yet — sub-issues B (strengthen `BareissGramRowInvariant` to
carry the cofactor formula) and C (derive
`bareissGramRowInvariant_step_dvd_prevPivot` from the strengthened
invariant) remain gated on #5839's identity landing.

## Blockers

None for the triage. #5839 itself is technically substantial (new
`HexMatrix/Determinant.lean` infrastructure for either Bazin or
2-column Laplace), but is a clean single-worker deliverable.
