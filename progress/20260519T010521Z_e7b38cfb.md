# 20260519T010521Z — BHKS D1: bhksBound arithmetic component bounds

Session type: `/feature` worker on issue #5185.

## Accomplished

Added five reusable `Nat`-valued lemmas to
`HexBerlekampZassenhausMathlib/BHKSBound.lean`, inserted immediately
after `bhksThresholdNatBound_le_bhksBound` and before the real-valued
section:

- `one_le_bhksFourPowFactor` — `1 ≤ 4 ^ (n*n)`, since the base is positive.
- `one_le_bhksCoeffNormFactor` — `1 ≤ (sumSquared f + 1) ^ n`.
- `one_le_bhksThresholdNatBound` — `1 ≤ bhksThresholdNatBound f` (the
  `+ 1` slack does the work).
- `one_le_bhksBound` — `1 ≤ Hex.bhksBound f`.
- `bhksBound_dominates_spec_components` — bundled theorem:
  `bhksDegreeFactor f * bhksFourPowFactor f * bhksCoeffNormFactor f *
  bhksLog2Factor f ≤ Hex.bhksBound f`.

The bundled theorem packages the four SPEC Group-D component factors
(`n`, `4^(n*n)`, `(sumSquared + 1)^n`, `(log2 (sumSquared + 1))^n`) on
the LHS with `Hex.bhksBound f` on the RHS. The `+ 1` slack in `bhksBound`
absorbs the entire product, so no hypotheses are required.

No alteration to `bhksBound`, `factorFastPrecisionCap`, or any
algorithmic code. No new imports, no new file.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound` — fails at the
  same pre-existing `whnf` heartbeat timeouts in
  `HexBerlekampZassenhausMathlib/Basic.lean` (lines 4927, 5161, 5469,
  5556, 5603, 5840, 5939, 6084, 6228, 6247) and the same cascaded
  kernel "unknown constant" errors at lines 16320 and 16366. The
  baseline build (without my changes, via `git stash`) produces a
  byte-identical error set: `diff /tmp/errs-baseline.txt
  /tmp/errs-with-changes.txt` is empty. These pre-existing
  environment-specific failures are documented in
  `progress/20260518T034405Z_d1e63d44.md` and explicitly acknowledged
  in the issue body.
- Proof tactics verified independently against a standalone stub
  Lean file mirroring the component-factor structure: all five
  lemmas elaborated cleanly under the toolchain `leanprover/lean4:v4.30.0-rc2`.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git diff -- HexBerlekampZassenhausMathlib/BHKSBound.lean | rg -nE
  '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'` — empty.

## Current frontier

`HexBerlekampZassenhausMathlib/BHKSBound.lean` now exposes both the
component `_eq` lemmas (pre-existing) and the component positivity /
bundled-dominance lemmas (this PR). Later BHKS Group-D termination
work can chain through `bhksBound_dominates_spec_components` without
re-unfolding `Hex.bhksBound`.

## Next step

A natural follow-up is the BHKS Group-D step 2 lattice/resultant
work (issues #5186, #2567) — the arithmetic substrate is now in
place for the integer side of the BHKS Theorem 5.2 / factorFast
termination proof.

## Blockers

None for this issue. The pre-existing `whnf` heartbeat timeouts in
`HexBerlekampZassenhausMathlib/Basic.lean` are tracked separately and
do not block downstream additions whose verification can be checked
by baseline comparison.
