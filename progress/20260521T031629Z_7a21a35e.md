# 2026-05-21T03:16Z — work issue #5675

## Accomplished

Claimed #5675 after the visible directive issues were rejected by
`coordination claim` as blocked. Updated
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` so the outer-bound lifted
partition wrapper uses `Hex.monicisedCoreLiftData`, and added a
`MonicisedCoreTransportPackage` surface packaging the normalized square-free
core facts, monicised-core degree/nonzero facts, correspondence/partition
constructors, scaled recovery, and scaled exhaustive-membership bridge.

While making the target compile, aligned stale destructuring of
`choosePrimeData?_factorsModP_berlekamp_form` with its current two-field shape,
raised heartbeat caps on the affected heavy wrappers, replaced a brittle
singleton length proof, and transported existing lifted-factor facts through
the monicised lift in the residual monic-core wrappers.

Verified:

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- filtered diff check for new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`

## Current frontier

#5675's package is in place. The immediate downstream consumer #5636 can now
replace the remaining explicit `hcore_monic` residual in the exhaustive
branch with the monicised-core transport package.

## Next step

Pick up #5636 to drop the `hcore_monic` premise from
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` and its
private auxiliary, using the new package rather than rebuilding the transport
facts locally.

## Blockers

None for this issue.
