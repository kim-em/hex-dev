# 2026-05-05 11:37 UTC — feature session (922b6be1)

## Accomplished

Claimed #2350 (sub-issue of human-oversight #2245) and discharged 14
of 16 per-modulus `FpPoly.Irreducible` `sorry` placeholders in
[`HexGfqField/EmitFixtures.lean`](HexGfqField/EmitFixtures.lean) via
literal `Berlekamp.IrreducibilityCertificate`s checked by
`Berlekamp.checkIrreducibilityCertificateLinear` and routed through
`Berlekamp.rabinTest_imp_irreducible`.

Per-modulus pow chains (`X^(p^k) mod m` for `k = 0..n`) and Bezout
witnesses (one per maximal proper divisor of `n`) were precomputed
externally with sympy's `gf_*` operations and pasted into the file as
record literals.

Discharged moduli (14):
- `m_p2_n2`, `m_p2_n3`, `m_p2_n4`, `m_p2_n6`
- `m_p3_n2`, `m_p3_n3`, `m_p3_n4`, `m_p3_n6`
- `m_p5_n2`, `m_p5_n3`, `m_p5_n4`
- `m_p7_n2`, `m_p7_n3`, `m_p7_n4`

Pending (2):
- `m_p5_n6` (Conway poly for `GF(15625)`) — `5^6 = 15625` linear
  iterations per pow-chain entry exceeds practical kernel-reduction
  budget.
- `m_p7_n6` (Conway poly for `GF(7^6)`) — `7^6 = 117649` iterations.

Filed follow-up #2358 (depends on #2350) requesting an incremental
`checkPowChainLinearIncremental` variant whose chain validation is
`O(p · n)` instead of `O(sum p^k)`. The cert data for both pending
moduli has already been computed and is captured in the issue body /
this branch's git history.

Other changes:
- Added `import HexBerlekamp.RabinSoundness` to
  `HexGfqField/EmitFixtures.lean`.
- Added per-prime `polyP2/3/5/7 : Array Nat → FpPoly _` helpers,
  matching the `polyFive` style from `HexGfqField/Conformance.lean`.
- Added `maxProperDiv_2/3/4/6` precomputed lemmas. `simp` does not
  reliably reduce `Berlekamp.maximalProperDivisors n` for `n ≥ 3`
  (the underlying filter expression survives), so the cert checks
  unfold against these explicit equalities instead.
- Updated the file's leading docstring to describe the Rabin
  certificate evidence chain.

## Verification

- `lake env lean HexGfqField/EmitFixtures.lean` (1:16 wall): only
  expected sorry warnings on lines 689 and 826 (the two pending
  `m_p5_n6_irr` / `m_p7_n6_irr` placeholders).
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.

The full `lake build HexGfqField` still fails at the pre-existing
`HexBerlekamp` umbrella `dyld: missing symbol called` precompile
failure (orthogonal to this work and noted in earlier progress
entries).

## Current frontier

Unblocked: 2 of 16 cases still on `sorry`, both n=6 with large
primes, both blocked on a smarter pow-chain checker (#2358).

## Next step

Land the `checkPowChainLinearIncremental` variant from #2358, then
discharge the remaining `m_p5_n6_irr` and `m_p7_n6_irr` using the
already-computed cert data.

## Blockers

None for this session. The `HexBerlekamp` umbrella precompile
failure is pre-existing and orthogonal.
