# 2026-06-01T10:07Z - BZ Option API product contracts (#2564 partial)

## Accomplished

- Claimed directive issue #2564 and worked a bounded no-fallback API slice.
- Added `factorWithBound?_product_of_some` and `factor?_product_of_some` in
  `HexBerlekampZassenhaus/Basic.lean`, exposing product preservation for the
  explicit `Option`-returning factoring APIs when they return `some`.
- Verified `lake build HexBerlekampZassenhaus.CrossCheck`.

## Current frontier

- This is a partial HO-1 contribution. It does not remove the remaining total
  fallback call sites and does not prove the five-clause Mathlib headline
  correctness theorem.
- Attempted degree-72 kernel guards for the split-family no-prime path were too
  expensive for `CrossCheck`; they were not kept. The existing cheap collision
  guard remains the in-Lean coverage, while degree-72 stays in emitted/oracle
  space.

## Next step

- Continue replacing or proving away `choosePrimeDataWithFallback` consumers in
  the production factoring path, or thread the new `factor?` product contract
  into Mathlib-side callers that can propagate explicit no-prime failure.

## Blockers

- Full discharge of #2564 remains larger than this session: it still requires
  the van Hoeij CLD headline correctness theorem and closure of the remaining
  `HexBerlekampZassenhausMathlib/` obligations.
