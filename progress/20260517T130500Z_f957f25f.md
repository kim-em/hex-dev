# 20260517T130500Z — feature session f957f25f (#4626 decomposition)

Session UUID prefix: f957f25f. Issue: #4626 — HO-1 substrate: relax
exhaustive wrapper away from monic squareFreeCore.

## Accomplished

Scope assessment of #4626 found the work too large for one session and
decomposed it into three self-contained sub-issues with a clean
dependency chain.

The relaxation in `HexBerlekampZassenhausMathlib/Basic.lean` requires
generalising ~29 distinct theorems consuming `hcore_monic` across ~78
reference sites. The deep machinery genuinely uses `Monic core` for
two essential algebraic identities:

- The scaling collapse `scaledLiftedFactorProduct core d S =
  liftedFactorProduct d S` (line 3617-3622), which follows from
  `Hex.DensePoly.leadingCoeff core = 1`. For primitive non-monic
  `core`, `scaledLiftedFactorProduct = scale (lc core) liftedFactorProduct`
  and the equality fails.
- The conclusion `Monic factor` in `representsIntegerFactorAtLift_monic`
  (line 5993). For primitive non-monic `core`, divisors of `core` are
  primitive (by Gauss) but not generally monic — their leading
  coefficients divide `lc(core)`.

These changes propagate up through `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
(line 8143), specifically the recursive cofactor argument (lines
8294-8310) which currently derives `quot_monic` from `f_cov_monic` +
`target_monic` via `Polynomial.Monic.of_mul_monic_right`. The
corresponding primitive-product identity needs the right Gauss-flavoured
discharge.

### Sub-issues created

- **#4638** (substrate refactor, unblocked): generalise the
  recombination chain — six helper lemmas plus the
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  top-level theorem — from `Monic core` to
  `Primitive core ∧ 0 < leadingCoeff core`. Retains the existing
  `_of_monic_core` variants as thin specialisations so existing
  monic-core callers don't break. The body flags that this sub-issue
  is itself large; the worker should assess whether to further
  decompose per-helper before starting.

- **#4639** (wrapper relaxation, blocked on #4638): relax the three
  top-level wrappers in `HexBerlekampZassenhausMathlib/Basic.lean`
  (`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`,
  `_factor_zpolyIrreducible_of_henselSubsetCorrespondence`,
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`)
  to consume the relaxed substrate.

- **#4640** (umbrella update, blocked on #4639): drop the explicit
  `hcore_monic` Gap 1 premise from
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
  (`HexBerlekampZassenhausMathlib/IntReductionMod.lean` line 1500),
  using `squareFreeCore_leadingCoeff_pos_of_ne_zero`,
  `normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive`, and
  `zpoly_primitive_of_toPolynomial_isPrimitive` to discharge primitive
  + positive-leading internally from `hf_ne : f ≠ 0`.

The parent #4626 was skipped via `coordination skip` with the
`Decomposed into #4638, #4639, #4640` breadcrumb, routing to the next
planner's replan-triage cycle.

## Current frontier

The substrate refactor (#4638) is the critical path. It involves
algebraic generalisation rather than mechanical premise threading —
the proof shape for `recombinationCandidate = factor` under
non-monic primitive core has to route through the
`primitivePart`/`normalizeFactorSign` collapse rather than the
direct scaling identity. The worker landing #4638 should investigate
whether `recombinationCandidate d S` (which uses unscaled
`liftedFactorProduct`) actually equals the primitive integer factor
`factor` of non-monic `core`, since
`scaledLiftedFactorProduct ≡ factor mod p^k` and
`liftedFactorProduct = unscale(lc(core), scaledLiftedFactorProduct)`
disagrees with `factor` when `c | factor` fails (which is the typical
case for primitive `factor` and `c > 1`). If the executable's
recombination is only correct for monic cores, the relaxation strategy
must change (e.g., add an internal monicisation pass at the wrapper
boundary rather than relaxing the substrate).

## Next step

Worker claims #4638 and starts the substrate refactor. After it lands,
the planner picks up #4639 (auto-unblocked by `check-blocked`), and
then #4640 after #4639 lands.

## Blockers

None for #4638. The downstream #4639 and #4640 are blocked on their
predecessors, which is the intended dependency chain.
