# 2026-05-21T04:32Z — work issue #5689

## Accomplished

Claimed #5689 after #5636 was already claimed by another agent. Added the
successful-branch Hensel subset correspondence surface in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `henselSubsetCorrespondenceHypotheses_of_choosePrimeData_success`, composing
  `modPSubsetPartitionHypotheses_of_choosePrimeData` with an explicit
  `HenselSubsetLiftHypotheses` package under
  `Hex.choosePrimeData? core = some (Hex.choosePrimeData core)`;
- `henselSubsetCorrespondenceHypotheses_of_choosePrimeData_success_descent`,
  the consumer-facing wrapper that first builds the non-circular lift package
  from `HenselLiftDescentHypotheses` plus forward transport.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexBerlekampZassenhausMathlib/Basic.lean | rg -n -e
  '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returns no matches

## Current frontier

#5689's successful-branch constructor is in place. Downstream #4821/#5214 can
now consume a correspondence package in the `choosePrimeData? = some ...`
branch without using the older unconditional analytic fallback constructor.

## Next step

Use the new successful-branch correspondence wrapper in the HO-1 consumers that
already carry the `choosePrimeData?` success equation and the lifted-side
descent/forward-transport facts.

## Blockers

None for this issue. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification, which this session left untouched.
