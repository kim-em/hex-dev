# Session cb64e4a9 - HexArith wide-word Phase 6 review (#4398)

Session: `cb64e4a9-a942-4e98-a6de-c836bc9499fb`
Issue: `#4398`

## Accomplished

Reviewed `HexArith/UInt64/Wide.lean` and `HexArith/ffi/wide_arith.c`
against the HexArith Phase 6 quality bar and the SPEC extern contract.

Landed a small Phase 6 API polish in `HexArith/UInt64/Wide.lean`:

- added low-word component lemmas for `addCarry` and `subBorrow`, tagged
  `[simp]` with conditional-free Nat normal forms;
- added explicit outgoing carry/borrow characterisation lemmas without
  global `[simp]` annotations, because those Bool projections are too
  aggressive for existing Montgomery REDC proofs.

No C extern changes were needed. The existing `wide_arith.c` bodies match
the SPEC contract: `mulHi`/`mulFull` use one `__uint128_t` product, and
carry/borrow use compiler overflow intrinsics.

Second-opinion review flagged the original `subBorrow` projection as a
poor conditional `[simp]` shape; the final version uses the uniform
modulo form.

Verification:

- `lake build HexArith.UInt64.Wide` - clean
- `lake build HexArith` - clean
- `python3 scripts/check_dag.py` - clean
- `git diff --check` - clean
- `rg -n '^\\s*axiom\\b|\\bnative_decide\\b|TODO|FIXME|\\bsorry\\b' HexArith/UInt64/Wide.lean HexArith/ffi/wide_arith.c` - no matches

## Current frontier

`HexArith/UInt64/Wide.lean` now exposes component-level characterising
lemmas so Barrett and Montgomery callers do not need to unfold
extern-backed definitions to reason about returned pair fields.

`libraries.yml` was intentionally left unchanged; this was a narrow
Phase 6 review/polish issue, not a complete HexArith Phase 6 exit audit.

## Next step

Continue HexArith Phase 6 review on the next narrow API surface, especially
Barrett and Montgomery user-facing theorem APIs.

## Blockers

None for this issue.

Pre-existing worktree modification in `.claude/CLAUDE.md` was left
untouched and is not part of this session's changes.
