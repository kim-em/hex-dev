# issue-8531: merge-required lattice-tier conformance case

Session type: feature (issue #8531), branch `issue-8531`.

## Accomplished

- Added `adv/swinnerton_dyer_sd5_pair` (`SD₅(x)·SD₅(x+1)`, degree 64,
  content 1) to `conformance/HexBerlekampZassenhaus/EmitFixtures.lean`
  with pinned coefficients, pinned `modFactorPrime = 29` /
  `modFactorDegrees = [2]*32` (FLINT-verified: 32 degree-2 factors,
  squarefree, and 29 is exactly the prime `toMonicPrimeData?` picks).
- New `emitHybridTracedCase`: a single `factorHybridTraced` run supplies
  the `factor` result, a derived `classicalFactor` (null on decline),
  and the **hybrid** trace — so the committed trace pins
  `tier = "lattice"`, `declined = true`, `subsetCandidates = 206368`
  (the post-#8537 `levelAwareSubsetBudget 32` boundary, per the issue
  comment superseding the body's 262144).
- Regenerated `bz.jsonl` (50 → 51 cases; first 200 lines byte-identical
  to the old fixture) and `bz-trace-baseline.json` (diff is exactly the
  one new entry).

## Verification

- `bz_flint.py --check`: 101 checked / 0 failures (issue target: 101/0).
- `bz_trace_gate.py`: 51 traces / 0 failures (issue target: 51/0).
- Second emission run byte-for-byte identical to the committed fixture.
- Factors emitted are exactly SD₅(x) and SD₅(x+1) (checked against the
  committed scheduled-corpus SD₅ and an independently recomputed SD₅).
- Emission wallclock 23 s locally (M-series) vs the CI `timeout 180`
  cap; the new case costs ~16 s of that.
- No new `sorry`/`axiom`/`#guard`; build clean, no warnings.

## Current frontier

Done; PR next.

## Next step

None for this issue. When #8417's dispatcher work lands, the pinned
subsetCandidates/tier values are the regression net.

## Blockers

None.

## Second opinion (Codex)

No fatal findings. Addressed: made `emitHybridTracedCase` sentinel-only
(errors unless classical declined and lattice answered, so the helper
cannot mis-derive `classicalFactor` on the corpus-never
certification-miss path), and reworded the docstrings so exact
`prime`/`r`/`subsetCandidates` pinning is attributed to the
committed-fixture byte-diff (the trace gate pins tier/decline and
upper-bounds the count). Noted but out of scope: the first emission in
`run_oracles.sh` is untimed (pre-existing for all oracles; job default
timeout applies), and `modFactorDegrees` does not assert modular
squarefreeness (indirectly covered: the pinned `prime = 29` in the
trace requires the prime selector's squarefree check to pass there).
