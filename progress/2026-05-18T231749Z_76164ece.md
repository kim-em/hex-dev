# 2026-05-18T23:17Z — feature session 76164ece

## Accomplished

Implemented #5160: added four `Hex.Nat.Prime` accessor wrappers to
`HexArith/Nat/Prime.lean` inside `namespace Prime`:

- `one_lt : 1 < p` (proof: `hp.two_le`, definitionally `2 ≤ p`)
- `pos : 0 < p` (proof: `Nat.lt_of_lt_of_le (by decide) hp.two_le`)
- `ne_zero : p ≠ 0` (proof: `Nat.ne_of_gt hp.pos`)
- `ne_one : p ≠ 1` (proof: `Nat.ne_of_gt hp.one_lt`)

Each carries a one-line docstring matching the file's existing
`two_le` / `dvd_mul` style. No `@[simp]` attribute — these are
plain accessors. Placed immediately after `two_le` (line 80) and
before `coprime_of_not_dvd`.

Verified: `lake build HexArith.Nat.Prime` (17 jobs), `lake build
HexArith` (57 jobs), full `lake build` (244 jobs) all green;
`scripts/check_dag.py` clean; `git diff --check` clean; no
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in the diff.

## Current frontier

Issue #5160 deliverables landed and verified. Existing callers in
`HexBerlekampZassenhausMathlib`, `HexPolyFp`, and `HexBerlekamp`
that re-derive these facts via `have := hp.two_le; omega` can now
migrate to the new accessors in a follow-up pass; per the issue
that rewiring is explicitly out of scope here.

## Next step

Open PR closing #5160.

## Blockers

None.
