# Issue #6234: fpylll comparator startup amortization

## Accomplished

- Tried to claim directive issues #2567 and #2564 first; both claims failed because the coordination script reported them blocked.
- Claimed #6223, verified it is stale against current `main` because the normalized SquareFree provider substrate is still in open PR #6232 for #6227, and released it for replan with that dependency noted.
- Claimed #6234 and verified the current `lean-bench` pin already supports fixed-target inner repeats through `minTotalSeconds`.
- Updated the HexLLL fpylll and Isabelle process-call fixed registrations in `HexLLL/Bench.lean` to set `minTotalSeconds := 1.0`, so fixed children amortize one-time comparator startup inside each measured child.
- Regenerated both fpylll fixed-ladder exports:
  - `reports/bench-results/hex-lll-fpylll-0c2d9a9e2d0a.json`
  - `reports/bench-results/hex-lll-fpylll-harsh-cubic-4a69408a680d.json`
- Regenerated both comparator plots:
  - `reports/figures/hex-lll-comparator-random-bounded.svg`
  - `reports/figures/hex-lll-comparator-harsh-cubic.svg`
- Updated `reports/hex-lll-performance.md` with the new fpylll medians, hashes, ratios, and methodology text.

## Current frontier

- The regenerated fpylll medians are monotone and no longer sit on the CPython startup floor:
  - random-bounded: `1.837 ms` at `n = 30` through `271.702 ms` at `n = 180`;
  - harsh-cubic: `423.473 us` at `n = 15` through `7.356 ms` at `n = 55`.
- The committed Isabelle data was not regenerated in this session; the Isabelle registrations now have the same fixed-child amortization floor for future runs.
- The worktree still contains the unrelated pre-existing `.claude/CLAUDE.md` modification; this session did not stage or modify it.

## Next step

- Push the branch and open the PR for #6234.

## Blockers

- None for #6234.
- Verification passed: `lake build HexLLL.Bench`, `python3 scripts/check_dag.py`, monotonicity and inner-repeat checks over both regenerated fpylll JSON exports, `scripts/plots/hex-lll-comparator.py --family random-bounded`, `scripts/plots/hex-lll-comparator.py --family harsh-cubic`, `rsvg-convert` rendering of both SVG plots, `git diff --check`, forbidden-token diff grep for `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` in touched Lean changes, and full `lake build`.
