# Work session: HexLLL sizeReduceColumn determinant field

## Accomplished

- Inspected unclaimed work and claimed #2129 first, then skipped it as stale because the final HexPolyZ square-free-core assembly is blocked on open #2206, which depends on claimed #2213.
- Claimed #2212.
- Discharged the `LLLState.sizeReduceColumn` `d_eq` proof field in `HexLLL/Basic.lean` using `GramSchmidt.Int.gramDet_sizeReduce` and the previous state's `d_eq`.
- Verified:
  - `lake build HexLLL.Basic`
  - `lake build HexLLL`
  - `python3 scripts/status.py HexLLL`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - touched diff scan for `axiom`, `native_decide`, `TODO`, `FIXME`, and new `sorry`

## Current frontier

#2212 is only partially complete. The `d_eq` proof field is done, but the `ν_eq` proof field remains. A direct attempt showed that the proof needs more than the existing pivot/lower `GramSchmidt.Int.sizeReduce` update lemmas: it also needs unchanged-entry facts for coefficients or scaled coefficients away from the targeted row/cells.

## Next step

Replan the remaining `ν_eq` proof around explicit Gram-Schmidt size-reduction unchanged-entry lemmas, then assemble the row/cell case split for `LLLState.sizeReduceColumn`.

## Blockers

- No build blocker for the partial change.
- The remaining #2212 proof is blocked on missing local update API for unaffected Gram-Schmidt coefficient or scaled-coefficient entries under `GramSchmidt.Int.sizeReduce`.
