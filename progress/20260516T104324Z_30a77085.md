# 2026-05-16T10:43Z — review session 30a77085

## Accomplished

Claimed #4482 and reviewed the nsmul / intCast / zsmul cluster in
`HexGfqRing/Operations.lean` against the HexGfqRing Phase 6 expectations
and the `SPEC/Libraries/hex-gfq-ring.md` "Contents" clause that forbids
the textbook `n+1 ↦ pred + 1` recursion for `nsmul n x` / `natCast n`.

Added docstrings to every previously undocumented public declaration in
the cluster (lines ~720–950): `nsmul_zero`, `nsmul_succ`,
`repr_nsmul_zero`, `repr_nsmul_succ`, `natCast_succ`,
`nsmul_eq_natCast_mul`, `intCast_ofNat`, `intCast_negSucc`,
`repr_intCast_ofNat`, `repr_intCast_negSucc`, `zsmul_ofNat`,
`zsmul_negSucc`, `repr_zsmul_ofNat`, `repr_zsmul_negSucc`,
`neg_zero_eq`, `neg_neg_eq`, `neg_zsmul_eq`, `intCast_neg_eq`.

The `nsmul_zero` / `nsmul_succ` docstrings explicitly call out that the
public lemma states the *unfolded* recurrence shape consumed by
`Lean.Grind.Semiring`, while the underlying `nsmul.go` implementation
takes the binary-decomposition path through `linearNSmul_double` /
`linearNSmul_double_add_one`. This is the SPEC's split between observed
behaviour and implementation strategy, and the previous undocumented
form risked a future reader misreading `nsmul_succ` as evidence of the
forbidden textbook recursion. Each other docstring names the specific
`Lean.Grind.{Semiring,Ring}` axiom field it witnesses so the
instance-construction block at lines ~1022–1090 has a clear forward
trail of citations.

Audited the rest of the cluster and confirmed no further changes are
needed:

- `@[simp]` annotations are consistent and intentional. The
  `nsmul_*` / `repr_nsmul_*` / `intCast_*` / `repr_intCast_*` /
  `zsmul_*` / `repr_zsmul_*` lemmas are uniformly `@[simp]`; the
  `natCast_succ`, `nsmul_eq_natCast_mul`, `neg_zero_eq`, `neg_neg_eq`,
  `neg_zsmul_eq`, `intCast_neg_eq` lemmas are uniformly non-`@[simp]`.
  None of the non-simp lemmas is a missed `@[simp]` candidate — the
  four negation lemmas are `Ring` axiom witnesses already reachable
  through the typeclass simp set, and `natCast_succ` would loop with
  the `OfNat` simp set.
- Naming is consistent with the rest of the file — the `repr_<op>`
  prefix is uniform across `repr_nsmul_*`, `repr_intCast_*`,
  `repr_zsmul_*`.
- Proof shapes are direct. The heavier `neg_neg_eq` calc is the
  textbook additive-group cancellation `-(-x) = -(-x) + (-x + x) =
  (-(-x) + -x) + x = 0 + x = x`; shortening it would require an
  upstream `reduceMod_neg_reduceMod` in `HexGfqRing/Basic.lean`,
  which is out of scope for this review issue.
- The private support helpers `zmod64_zero_add`, `zmod64_zero_add_zero`,
  `fpPoly_C_add`, `const_add` are genuinely private and not shadowing
  any public lemma. There is no public `FpPoly.C_add` (or
  `ZMod64.zero_add` outside the `Lean.Grind.Semiring (ZMod64 p)`
  instance proof at `HexModArith/Ring.lean:278`), so the helpers
  cannot be eliminated without adding new lemmas to those files,
  which is out of scope for this review.

## Current frontier

- `lake build HexGfqRing.Operations` succeeds.
- `lake build HexGfqRing HexGfqField HexGfq` succeeds.
- `python3 scripts/check_dag.py` succeeds.
- `python3 scripts/status.py HexGfqRing` reports Phase 6,
  `done_through: 5`.
- `git diff --check` succeeds.
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b'
  HexGfqRing/Operations.lean` returns no hits.
- Pre-existing warnings outside this diff:
  `HexGfqField/Operations.lean:671` has an unnecessary `simpa`
  warning, and `HexMatrix/Bareiss.lean:1342` uses `sorry`.

## Next step

Continue the remaining focused Phase 6 reviews for the other
`HexGfqRing/Operations.lean` sub-surfaces: the `repr_*` ring-axiom
cluster (#4479, unclaimed), the private `linearNSmul` / `nsmul.go` /
`linearPow` / `pow.go` binary-decomposition helpers, and the final
`Lean.Grind.{Semiring,Ring,CommRing}` instance constructions
(lines 1022–1090). The latter two still need their own review issues.

Separately, `HexGfqRing/Operations.lean` is now 1154 lines, past the
1000-line splitting threshold from the project review guidance. A
follow-up issue should plan the split — the `nsmul.go` /
`pow.go` binary-decomposition helpers are a natural candidate for a
new `HexGfqRing/BinaryDecomposition.lean` (or similar) file once the
remaining Phase 6 review surfaces in `Operations.lean` are done.

## Blockers

None for #4482.
