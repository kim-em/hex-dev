# HexMatrix Phase 7 reference chapter (#8258)

Session type: feature (via /work). UTC 2026-06-20T11:46.

## Accomplished

Authored the Phase 7 reference chapter for `HexMatrix` and bumped it to
`done_through: 7` ("Fully done").

- New `HexManual/Chapters/HexMatrix.lean` Verso chapter, modelled on the
  computational siblings (`HexLLL`, `HexGF2`): intro, dense matrix type
  and operations, the Leibniz determinant + row/column laws, the Bareiss
  fraction-free integer determinant, row-echelon / RREF transforms with
  span + nullspace readers, a worked determinant example, and
  cross-references.
- Wired into `HexManual.lean` (import + `{include 0 ...}`).
- `libraries.yml[HexMatrix].done_through: 6 → 7`.

## Key facts discovered

- The issue's "no `@[extern]`" premise is slightly off: `bareiss` routes
  through `Hex.Matrix.exactDiv`, which *is* `@[extern]`. Per
  `PLAN/Phase7.md`, a standalone `lake env lean` throwaway run errors with
  "Could not find native implementation" on the `bareiss` `#guard`s while
  the Leibniz `det` `#guard`s evaluate green. That is a pass, not a
  failure — the real `HexManual` build precompiles, so all guards
  evaluate there (confirmed: full build green).
- The worked example pins `det A = 3` (literal, validated by the green
  standalone `det` guards) and `bareiss A = det A` (the same
  agreement shape `Conformance.lean` already pins and builds), so the
  Bareiss guard is of a proven-safe form, not a hardcoded literal.
- Computational/proof boundary: `HexMatrix` proves only internal
  structural Bareiss facts; the `bareiss = det` correspondence lives in
  the not-yet-charactered `HexMatrixMathlib`. Named in prose as
  forthcoming (no `{ref}` to a non-existent tag), per the #8231 pattern.

## Verification

- `lake build HexManual.Chapters.HexMatrix` green;
  `lake build HexManual` green (2586 jobs).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` lists `HexMatrix` under "Fully done".
- No `axiom` / `sorry` / `native_decide`. Diff touches only
  `HexManual/Chapters/HexMatrix.lean` (new), `HexManual.lean`,
  `libraries.yml`.

## Current frontier / Next step

#8258 complete. `HexMatrixMathlib` is still Phase 5 (`done_through: 5`);
once it reaches Phase 7 its chapter should resolve the forthcoming
cross-reference promised here (a `hex-matrix-mathlib` tag + `{ref}`).

## Blockers

None.
