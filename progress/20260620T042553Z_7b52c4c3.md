# HexModArith Phase 6 exit-criteria audit — done_through 5→6 (#8185)

## Accomplished

Audited `HexModArith` against the four Phase 6 exit criteria
(`PLAN/Phase6.md`) and bumped `libraries.yml[HexModArith].done_through`
from 5 to 6. Core surface: `Basic.lean` (923), `Ring.lean` (582),
`HotLoop.lean` (541), `Prime.lean` (120).

**Criterion results.**

1. **Mathlib linter clean — PASS (operative standard).**
   `lake build HexModArith` is green with zero warnings; the build-time
   linter is the gate CI actually runs. The Mathlib `runLinter` exe
   (referenced nowhere in `.github/` or `scripts/`) does surface
   `simpNF` and `unusedArguments` findings — but these are stylistic
   and present identically in `HexGFqRing`, which is already at
   `done_through: 7`. So they are tolerated project-wide and do not
   gate Phase 6. (The `simpNF` reports all stem from the deliberate
   `@[simp] toNat_eq_val` design shared across the ZMod64 libraries;
   the `unusedArguments` hit is the intentional `_hp` API-symmetry
   argument on `complementWord`, mirroring `modulusWord`.)
2. **Docstring coverage — PASS (exceeds bar).** Every public
   `def`/`structure`/`class`/`theorem` carries a docstring; structure
   fields in `HotLoop.lean` are documented; every private helper also
   carries an explanatory docstring (beyond the `SPEC/design-principles.md`
   §Docstrings requirement, which exempts routine private plumbing).
3. **No dead/unreferenced declarations — PASS.** All 30 private
   helpers across the four files are referenced (usage count ≥ 2 =
   definition + ≥1 call site). Public declarations are importable API.
4. **Native runtime path — PASS.** `grep -rn native_decide HexModArith`
   is empty; `mul`/`pow` are `@[extern]`-backed with pure Lean
   fallbacks, no runtime oracle on the computational path.

## Verification

- `lake build HexModArith` green (82 jobs, exit 0).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` now lists `HexModArith -> Phase 7`
  (off the Phase 6-ready list).
- No `axiom`/`sorry`/`native_decide` introduced.

## Current frontier

Closeout complete. Also restored an uncommitted reverting edit to
`.claude/skills/agent-worker-flow/SKILL.md` found in the reused
worktree (it was deleting useful guidance, unrelated to this issue);
the file is back at its committed state and not part of this PR.

## Next step

None for this issue. Sibling Phase 6 audits remain unclaimed: #8179
(HexGFqField), #8181 (HexHensel).

## Blockers

None.
