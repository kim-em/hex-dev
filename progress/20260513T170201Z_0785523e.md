# Issue #3760 partial Cramer solve bridge

## Accomplished

- Claimed issue #3760 for the HexGramSchmidt Cramer solve-column bridge.
- Added `scaledCoeffMatrix_replacementColumn_solve` in `HexGramSchmidt/Int.lean`.
- The new theorem proves that the chosen original-row projection coordinates
  solve the cast leading Gram matrix system with the prefix-projection RHS.
- Ran:
  - `lake build HexGramSchmidt.Int`
  - `lake build HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Int.lean`

## Current frontier

The remaining issue #3760 gap is the final RHS rewrite from dotting prefix
rows with `basisPrefixProjection` to the replacement Gram column
`Matrix.dot (b.row p) (b.row i)` for `p ≤ j`.

## Next step

Prove a prefix-projection dot identity showing that, for `p ≤ j < i`,
`Matrix.dot (castIntRow b p) (basisPrefixProjection b i j ...)` equals the
rational cast of `Matrix.dot (b.row p) (b.row i)`, then use it to strengthen
`scaledCoeffMatrix_replacementColumn_solve` to the exact downstream Cramer
RHS shape.

## Blockers

- None.
