# Issue #3218 — pthRoot tail cross-coprimality with residual hypotheses

## Accomplished

- Added `dvd_one_of_normalizeMonic_eq_one` (HexPolyFp/SquareFree.lean):
  the reverse direction of `normalizeMonic_eq_one_of_dvd_one`, used to
  bridge the project's `squareFreeFactorCoprimeRel` to the `∣ 1` form.
- Added `squareFreeAuxRevResidualSatisfied_one` providing the trivial
  residual invariant on the unit polynomial. Used to discharge the
  `isOne loop.2 = true` branch of the tail-cross theorem.
- Restated `yunFactorsWithLevel_squareFreeAuxRev_tail_cross_coprime`
  with explicit `hp`, `hreachable`, and `hresidual` hypotheses; replaced
  the `sorry` with a structured proof that combines:
  - `yunFactorsWithLevel_factors_coprime_repeated_of_reachable` for
    `a` vs `loop.2` coprimality;
  - `squareFreeAuxRev_factor_dvd_input` (via the residual invariant) to
    show every output factor `b.factor ∣ pthRoot loop.2`;
  - `pthRoot_dvd_self_of_derivative_zero` to lift to `b.factor ∣ loop.2`,
    with case handling for `loop.2 = 0` and `loop.2 = 1` branches;
  - `DensePoly.dvd_gcd` plus the new reverse normalize-monic lemma to
    conclude `(normalizeMonic (gcd a.factor b.factor)).2 = 1`.
- Cascaded the residual provider parameter through
  `squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant`,
  `_nil_core`, `_core`, `_of_acc`, `_nil`, and finally the public
  `squareFree_pairwise_coprime`. Inside `_of_yun_invariant`, the
  generic `squareFreeAuxRevResidualSatisfied f m (fuel+1)` is unfolded
  through the `f.isZero = false`/`(deriv f).isZero = false` branch
  to produce exactly the shape `tail_cross_coprime` expects.

## Current frontier

- The public `squareFree_pairwise_coprime` now takes a `residualInvariant`
  hypothesis. Discharging it is the residual scope of #2917 (the final
  pairwise-coprime assembly).
- The pre-existing `sorry` at line 2960
  (`squareFreeAuxRevContribution_derivative_active_pow_obligation`) is
  out of scope for #3218 — tracked by #3153.

## Next step

- #2917 can now discharge the `residualInvariant` hypothesis to close
  the public `squareFree_pairwise_coprime` theorem.

## Blockers

- None for this issue. Building cleanly: `HexPolyFp.SquareFree`,
  `HexPolyFp`, `HexBerlekamp`.

## Verification

- `lake build HexPolyFp.SquareFree` ✓
- `lake build HexPolyFp HexBerlekamp` ✓
- `python3 scripts/check_dag.py` ✓
- Sorry count: net -1 in `HexPolyFp/SquareFree.lean` (2 on `main` → 1
  here). Tail-cross cluster `tail_cross_coprime` → `_pairwise_coprime_nil`
  is sorry-free, with no new `axiom`/`native_decide`/`TODO`/`FIXME`.
