# Session 1928fea3 — planner cycle (BZMathlib naming-harmonisation followups)

## Accomplished

Planner session. Queue state at orient: `POD_MIN_QUEUE=3`, `POD_QUEUE_DEPTH=1`,
`POD_QUEUE_DEFICIT=2`. No claimed work, no PRs ready to merge (PRs #5012 and
#5013 still in CI; #2656 draft; #4917 in repair). All 3 open directives
(#2637, #2567, #2564) are unclaimed but require infrastructure that earlier
planner cycles have already decomposed into the blocked `agent-plan` queue;
no new directive-supporting issues to add this cycle.

Filed 2 atomic feature issues to fill the deficit, both pure cleanup of the
`HexBerlekampZassenhausMathlib/Basic.lean` style debt flagged in the recent
`defaultFactorCoeffBound_leadingCoeff_natAbs_le` rollout (PR #4991 and the
#4995..#5011 follow-up cascade). Both are unblocked, contained to one file,
and independent of the in-flight #5010 / #5007 helper-extraction PRs:

* **#5014** — Harmonise `hcore_lc_bound` → `hcore_lc_le` parameter naming on
  7 `_of_bound` siblings (44 textual replacements in `Basic.lean`; pure
  rename). The 10 sibling theorems already use the post-#4991 `hcore_lc_le`
  convention; this completes the harmonisation flagged in PR #5006's
  progress note (commit ab303d05).
* **#5015** — Suppress 5 `unused variable hcore_ne` warnings at the
  `_of_bound` thin-wrapper signatures (`Basic.lean:4344, 8263, 8513, 8672,
  10866`) by renaming to `_hcore_ne`, matching the existing 8-site
  underscore-prefix convention at lines 1290, 3904, 3937, 3965, 3997,
  4029, 4062, 4161. Closes the warning crop flagged in PR #5003's progress
  note (commit f8ea0dd3).

The dispatcher emits two "potentially overlapping open issues" warnings for
both posts; these are false positives (substring match on "BZMathlib" /
"_of_bound" tokens). Neither new issue touches the proof obligations of the
abstract-bound sibling backlog (the open issues actually named), and the
scope sections explicitly list those backlogs as out-of-scope.

## Current frontier

Queue should now sit at depth ≈ 3 (the previously-counted #4334 plus #5014
and #5015), restoring `POD_MIN_QUEUE`. PRs #5012 and #5013 will close
their parent issues (#5007 / #5010) on merge. PR #4917 remains the only
broken PR; a repair agent is already on it (per `repair-claimed`).

## Next step

* Next planner cycle: monitor for #5012 / #5013 landings, then queue the
  per-rewire-site cleanup followups (drop now-unused `have`s at the 6
  `hcore_primitive` rewire sites — same pattern as #4998 / #4999 / #5003
  after the `hcore_lc_le` rewires landed).
* Two known additional cleanup candidates noted but not filed this cycle
  (worker can decompose if appetite allows; otherwise a later planner):
  - Rewire the inline `(monic_primitive_sign_normalized_of_monic
    hcore_monic).2.1` at `Basic.lean:5597` to `zpoly_primitive_of_monic
    hcore_monic` once #5010 lands — requires the helper to be in scope at
    that site, which is currently a forward-reference (helper at ~6745,
    site at 5597). Either move the helper cluster earlier in the file or
    leave the inline form.
  - Flatten the 2 explicit-typed `have hcore_pos : 0 < … := …` blocks at
    `IntReductionMod.lean:2976` and `3079` to the 1-line form already in
    use at `IntReductionMod.lean:2480`. Borderline atomic (~6-line
    savings); deferred.

## Blockers

None. Planner exits normally.
