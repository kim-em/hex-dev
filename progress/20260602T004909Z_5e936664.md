# Issue 6177: fpylll random-bounded comparator ladder

## Accomplished

- Added fpylll fixed benchmark targets for random-bounded rungs
  `n = 45, 60, 75, 90, 120, 150, 180` in `HexLLL/Bench.lean`, using the
  existing cached input refs.
- Registered those seven targets with the fixed benchmark harness alongside the
  existing `n = 30` target.
- Ran the fpylll random-bounded ladder through all eight rungs and committed
  the export at `reports/bench-results/hex-lll-fpylll-0c2d9a9e2d0a.json`.
- Updated `scripts/plots/hex-lll-comparator.py` to read the new fpylll export
  and draw the fpylll series as a connected curve.
- Regenerated `reports/figures/hex-lll-comparator.svg` and visually verified a
  rendered preview: Lean, Isabelle, and fpylll each show eight connected points
  over `n = 30..180`.
- Updated `reports/hex-lll-performance.md` with the new fpylll artifact,
  medians, and full random-bounded ratio trend.

## Current frontier

- Issue #6177 deliverables are complete.
- The new fpylll random-bounded timings show fpylll slower than Lean at
  `n = 30, 45`, roughly tied at `n = 60`, and faster from `n = 75` upward.

## Next step

- Open the PR for #6177 and let CI validate the committed benchmark/report
  changes.

## Blockers

- None. `scripts/plots/hex-lll-comparator.py --help`, the eight-rung fpylll
  export check, SVG visual inspection, `git diff --check`, and `lake build`
  all passed. Existing local `.claude/CLAUDE.md` changes were not touched.
  `/reflect` was unavailable in this shell (`no such file or directory`).
