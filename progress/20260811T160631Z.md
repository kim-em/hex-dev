# Accomplished

- Reconciled the arbitrary fact-event proof fold with the current chronology
  walker, schema table, and direct-emission tactic.
- Preserved the exact `(node, version)` evidence table, ordered dependency
  lookup, and independently carried program version for rule and transport
  events.
- Kept negative checks for future dependencies, stale program versions,
  malformed payloads, and missing or wrongly selected proof schemas.
- Built the proof emitter and sine proof/tactic conformance targets
  successfully.

# Current frontier

Fact events now use a function-independent proof fold. The canary still seeds
its initial evidence and names one fixed program; later layers derive those
pieces from arbitrary reified caller input and support multiple instantiations.

# Next step

Push the reconciled exact head for independent review, then reconcile the
generic seed and program-reconstruction layers.

# Blockers

None.
