# Session 2f45f282 — Review HO-4 leaf capstones cap-separation vs Mignotte

Session: `2f45f282-2c4b-4fb2-b16b-aca2b3596917`
Branch: `agent/2f45f282`

## Accomplished

Claimed and executed issue #4381 (review of the two HO-4 leaf capstones
`BHKS.factorFast_ne_none_of_capSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`
and
`BHKS.factorFast_ne_none_of_mignottePrecisionCanonicalIndicatorsExpectedFactorsAtPrecisionForCoeffBound`
in `HexBerlekampZassenhausMathlib/Recovery.lean`).

Findings on the four audit questions:

1. **Parameter-block alignment**: one structural drift —
   `trueSupports` appears at parameter position 4 in the Mignotte
   capstone but at position 6 in cap-separation, with
   `(localFactorIndex, localFactorDegree, H)` inserted between
   `rows_pos` and `trueSupports`. This makes the cap-separation-only
   block non-contiguous. Filed as part of follow-up #4383.
2. **Doc-comment cross-reference**: defect. Cap-separation doc-comment
   lacked a forward reference to the Mignotte parallel. Local fix
   landed in this PR (added a "Mignotte-side parallel entry point" line
   pointing at the parallel capstone).
3. **Shared lift-shape preamble**: repetition real (~8× / ~10×). No
   existing notation; per issue instructions, no new notation invented
   here. Filed as part of follow-up #4383.
4. **Hypothesis-coverage parity**: aligned modulo the documented branch
   difference. `hf_ne_zero` in Mignotte but not cap-separation is
   explained by the producer chains (Mignotte threads `hf_ne_zero`
   through coefficient-bound machinery; cap-separation's `product_eq`
   discharges it implicitly via `ofIndicatorCandidateFacts`).

Filed follow-up #4383 covering the parameter-block reorder and the
shared lift-shape preamble factoring.

Posted the review summary as a comment on #4381 with explicit findings
and links.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Recovery` passes.
`python3 scripts/check_dag.py` exits 0. `git diff --check` clean. No new
`axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`.

## Next step

Follow-up #4383 (harmonise HO-4 leaf capstones cap-separation/Mignotte
surface) is ready for a planner/feature worker to claim. It is
independent of the core BZ Mathlib chain (#4301, #4372).

## Blockers

None.
