# SPEC placement for Schmeisser/de Bruijn-Springer source theorem

Session: `116e56f2` (work, issue #5646)

## Accomplished

Skipped malformed issue #5645 because it had an empty body and no executable
work-type label.

Claimed #5646 and clarified `SPEC/Libraries/hex-poly-z-mathlib.md` so the
Schmeisser/de Bruijn-Springer composition-polynomial theorem surface is
allocated to `hex-poly-z-mathlib`.

The SPEC now names the relevant `Polynomial ℂ` API surface: Schmeisser
composition plumbing, derivative kernel lemmas, `rootsRadiusProduct`, the
public derivative-adapter-free product theorem, coefficient-form wrappers, and
local derivative adapters in `HexPolyZMathlib/RobinsonForm.lean`.

## Current frontier

The placement question is resolved in SPEC text. Implementation issues for the
hard source theorem and dependent wrappers can now cite
`SPEC/Libraries/hex-poly-z-mathlib.md` directly.

## Next step

Re-dispatch the closed/blocked Schmeisser source-theorem implementation work as
narrow `HexPolyZMathlib` proof issues, starting with the
`rootsRadiusProduct_le_of_schmeisserComposition` source theorem or its
radius-wise root-count substrate.

## Blockers

None for the SPEC placement slice.

Verification completed:

- `git diff --check`
- `rg -n "research completed|progress note|meta-commentary|TODO|FIXME" SPEC/Libraries/hex-poly-z-mathlib.md`
  returned no matches

Unrelated worktree state:

- `.claude/CLAUDE.md` was already modified in the worktree and was left
  untouched.
