# 20260602T115203Z 41a15da2 — Issue #6322

## Accomplished

- Claimed and completed issue #6322.
- Tagged the public `HexMatrix` row-span and nullspace wrapper facts in
  `HexMatrix/RREF.lean` with conservative `grind` attributes:
  `spanCoeffs_sound`, `spanContains_iff`, `nullspaceBasisMatrix_col`,
  `nullspace_sound`, and `nullspace_complete`.
- Extended `HexMatrix/Conformance.lean` with a proof-mode cluster for the
  RREF/span/nullspace wrapper surface.
- Kept executable definitions and theorem statements unchanged.

## Current frontier

HexMatrix now exposes the committed RREF row-span/nullspace wrapper API through
automation annotations. The conformance examples cover direct wrapper use,
`simp` normalization for `spanContains`, and `grind` closure for nullspace
soundness and the nullspace matrix/vector column bridge.

One practical detail: generic row-span goals of the shape
`spanCoeffs M v = some c -> rowCombination M c = v` cause current `grind` to
instantiate the wrapper lemma but then stall in vector-equality contradiction
closing. The conformance examples therefore use the public wrapper lemmas
directly for those two row-span facts.

## Next step

Continue with the remaining unclaimed Phase 6 polishing issues, likely
HexModArith #6323 or HexGF2 #6326.

## Blockers

None for this issue.

## Verification

- `lake build HexMatrix.RREF` — green.
- `lake build HexMatrix.Conformance` — green.
- `lake build HexMatrix` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token diff grep over `HexMatrix/RREF.lean` and
  `HexMatrix/Conformance.lean` found no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
- Repository-wide grep counts remained `sorry_count=84`,
  `axiom_decl_count=0`, `native_decide_count=2`.
