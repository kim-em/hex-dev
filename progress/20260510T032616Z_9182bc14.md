# 20260510T032616Z - Issue #2932

## Accomplished

Claimed #2932 and replaced the false raw `toPolynomial_gcd` equality in
`HexPolyMathlib/Euclid.lean` with the proved theorem
`toPolynomial_gcd_associated`.

Added local divisibility transport helpers across the
`DensePoly`/`Polynomial` equivalence, then used the executable
`Hex.DensePoly.GcdLaws` universal property and Mathlib's
`EuclideanDomain.gcd` universal property to prove association.  Updated the
xgcd gcd-component and equivalence-facing gcd bridge names/comments to make
the raw-associated surface explicit.

Updated `HexPolyMathlib/Conformance.lean` so conformance examples reference
`toPolynomial_gcd_associated`, `toPolynomial_xgcd_gcd_associated`, and
`equiv_gcd_associated` instead of the false equality names.

Verification completed:

- `lake build HexPolyMathlib.Euclid`
- `lake build HexPolyMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyMathlib/Euclid.lean HexPolyMathlib/Conformance.lean`

The final grep reports the three pre-existing xgcd proof holes in
`HexPolyMathlib/Euclid.lean`; the raw gcd proof hole is gone.

## Current frontier

The corrected gcd bridge is available as an associated theorem under
`[Hex.DensePoly.GcdLaws R]`.  The remaining `HexPolyMathlib/Euclid.lean`
holes are the xgcd coefficient and Bezout transport theorems, which still
target Mathlib's normalized gcd/coefficient API.

## Next step

Use the associated gcd bridge in the downstream xgcd correspondence work, or
first provide/import concrete `GcdLaws` instances where consumers need the
bridge without an explicit law assumption.

## Blockers

None for this issue.  The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md` that this session did not touch.
