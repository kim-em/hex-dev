# 20260609T141019Z (#6660)

## Accomplished

Claimed #6660 and audited the existing Yun scalar infrastructure in
`HexPolyFp/SquareFree.lean`.

Landed a coherent partial substrate for the recursive scalar-sync proof:

- `div_C_ne_zero_eq_C_inv_mul`: exact division by a nonzero constant polynomial
  is multiplication by the inverse constant.
- `yunFactorsContributionWithLevel_constant_scalar_sync`: strengthens the
  existing constant-state theorem so a nonzero constant `c = DensePoly.C k`
  leaves the residual as an explicit nonzero scalar multiple of the original
  `w`, not just an arbitrary `w0`.

This stronger constant-state result is the piece needed for the top-level
TF/FT branches where `isOne c` and `isOne (DensePoly.C u_c * c)` disagree.

Created successor issue #6680 for the remaining recursive theorem. The audit
found that #6660's body under-specified a second mismatch point in the quotient
branch: after `yunFactorsContributionWithLevel_scalar_step_bridge`, the proof
also needs to compare `isOne z` with `isOne (DensePoly.C (u_c * v⁻¹) * z)`.

## Current frontier

The compiled partial substrate is in `HexPolyFp/SquareFree.lean`; the recursive
scalar synchronization theorem itself remains open and is now scoped in #6680.

## Next step

Solve #6680 by first proving a one-step contribution helper for the four
`isOne z` / scaled-`z` cases, then assemble the recursive theorem by induction
using the existing asymmetric step bridge and the new constant-state sync.

## Blockers

None for the partial substrate. Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`: no matches
