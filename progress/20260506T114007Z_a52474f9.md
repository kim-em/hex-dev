# 2026-05-06 11:40 UTC — work session (a52474f9)

## Accomplished

Claimed #2461 and confirmed the rollback state was already present:
`libraries.yml` had `HexBerlekamp.done_through: 3`.

Strengthened the HexBerlekamp DDF oracle surface in
`scripts/oracle/berlekamp_flint.py` by documenting that strict bucket
comparison applies to repeated-factor inputs and rejecting duplicate
Lean degree buckets before dictionary normalisation can hide them.

Updated `HexBerlekamp/EmitFixtures.lean` documentation to match the
strict repeated-factor DDF oracle contract.

Re-entered Phase 4 for HexBerlekamp by setting
`HexBerlekamp.done_through: 4`.

Verification run:

- `python3 scripts/oracle/berlekamp_flint.py --check`
- `lake build HexBerlekamp.EmitFixtures`
- `python3 scripts/check_dag.py`
- targeted mutated-fixture regression: `p5/rep/sqLin` with
  `buckets = []` and `residual = f` is rejected
- `git diff --check`
- `rg -n '^axiom |native_decide|TODO|FIXME' scripts/oracle/berlekamp_flint.py HexBerlekamp/EmitFixtures.lean libraries.yml`
- `lake exe hexberlekamp_emit_fixtures > /tmp/hexberlekamp-fresh.jsonl`
- `lake build`
- `diff -u conformance-fixtures/HexBerlekamp/berlekamp.jsonl /tmp/hexberlekamp-fresh.jsonl`
- `python3 scripts/oracle/berlekamp_flint.py /tmp/hexberlekamp-fresh.jsonl`

## Current frontier

#2461 is ready to publish as the conformance-restoration PR. The
implementation path on current `origin/main` already ran strict DDF
bucket comparison for repeated-factor inputs; this session added an
extra duplicate-degree guard and restored the library phase state.

## Next step

Create the PR for #2461.

## Blockers

No blocker for #2461. The worktree still has the pre-existing unrelated
`.claude/CLAUDE.md` modification, which this session did not touch.
