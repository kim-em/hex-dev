# Issue 8751: HexRoots performance ratchet

## Accomplished

- Added outward-rounded bounded-significand coefficient balls, fully soft and
  exact-seeded Taylor enclosures, Graeffe root-squaring, shared all-count
  candidate scans, and soft `T₀` filtering with exact fallback.
- Threaded the new combined witnesses through the Taylor-shift cache landed in
  PR #8927, and added the Pellet-bearing all-atoms local finisher with the
  original NK early-emission path preserved.
- Proved coefficient enclosure, Graeffe root/multiplicity transport, soft
  Pellet and root-free soundness, certifier preservation, and local-finisher
  coverage/readiness/disjointness through driver completeness.
- Remeasured the degree 10/20/50/100 regression rows on the final core and
  tightened the SPEC ceilings to 0.3 s, 7 s, 105 s, and 21 min respectively.
  Degree 100 now completes in 630.421 s; the slower degree-50 call is 9.24×
  faster than the preceding checkpoint.
- Restored the six curated plus fifty seeded degree-20 oracle fixture tier;
  the 112-line JSONL stream contains a successful response for every request.
- Passed the complete `lake build`, the focused soundness/completeness builds,
  both modified executable builds, all 14 fixed benchmark digest checks,
  `git diff --check`, and JSON parsing of the committed fixture.
- Replayed the committed stream with python-flint 0.9.0: all 56 cases passed.
- Reran all 13 canonical fixed benchmarks on the final tree. Every digest
  agrees; the resulting export updates the report's current medians and
  re-anchors the NK-only cap at 20 seconds around its 9.809-second median.
- Completed the required independent Opus review. It found no correctness or
  soundness blocker. In response, the final core prechecks coarse-atom
  disjointness before local finishing, reuses cached shifts for seeded balls,
  and tries the shallow seeded witness before the fully soft ladder. The
  report now discloses the current fixed-driver regression against the older
  pre-#8839 export and correctly attributes degree-20 emission to the local
  finisher rather than the size-gated soft kernel.
- Updated the corner-gluing conformance guard to distinguish the old exact
  level-zero Pellet check from the broader public Graeffe witness, and passed
  an explicit `lake build HexRoots.Conformance`.
- Passed the post-review complete build, focused executable build, all fixed
  digest checks, Phase-4 script, published trust-surface scan, conformance
  target check, Mathlib-free bench lint, and `git diff --check`.

## Current frontier

The independent review is addressed, final measurements are recorded, and
the branch is rebased on current `origin/main`. The complete 9,501-target
build and every focused local check are green. GitHub PR/CI remains.

## Next step

Open, green, and squash-merge the PR. Record the GitHub-hosted oracle-step
wallclock before merge.

## Blockers

None.
