# 2026-06-03T08:45:56Z — session 7188dec1 (one-shot D2 #6468)

## Accomplished

- Read directive #6468 ("Schur ≡ Bareiss equivalence: non-singular σ
  invariant against borderedMinor") and oriented in `HexGramSchmidt/Int.lean`
  around the Bareiss-side analog `scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix`
  (line 6633) and the D1-merged frame lemmas.
- Mapped the σ-chain ≡ Bareiss-bordered-minor identity onto the
  underlying Bareiss step recurrence:
  `σ_p = d_{p+1} · gram[j][i] − τ_{p+1}` with
  `τ_q = (noPivotLoop q (noPivotInitialState (gramMatrix b))).matrix[i][⟨j⟩]`.
  Established that the proof factors through (i) a nested strong
  induction characterizing `scaledCoeffRowsSchur[a][b]` against the
  noPivotLoop state, plus (ii) trailing-block symmetry from
  `noPivotLoop_matrix_symm_preserve` to swap `rows[j][l]` against
  `M_l[l][j]`, plus (iii) `exactDiv` divisibility tracking.
- Confirmed the underlying Desnanot-Jacobi identity is **not** a
  free-standing lemma in HexMatrix.Bareiss — it lives only as the
  conditional `bareissExactDiv_borderedMinor_of_mul_eq` /
  `stepMatrix_borderedMinor_update` hypotheses, so the σ-side proof
  cannot just cite it.
- Decomposed: created sub-issue #6475 isolating the σ-chain ↔ Bareiss
  invariant lemma (the technical heart) as a private helper. Posted
  the decomposition rationale on #6468 and `coordination skip`ped
  with the `Decomposed into #6475` breadcrumb.

## Current frontier

- Sub-issue #6475 is the next worker's target. Once it lands, the
  directive's exact-form theorem
  `scaledCoeffRowsSchur_lower_eq_borderedMinor` is the routine
  composition with `scaledCoeffMatrix_eq_borderedMinor` +
  `noPivotLoop_full_eq_borderedMinor_at_trailing` + the symmetry
  swap.
- D2 (#6468) is left for the next planner to either close (if #6475's
  scope fully covers it) or narrow to the residual composition (more
  likely).

## Next step

- Successor claims #6475 to prove the σ-chain ≡ Bareiss invariant.
- After that lands, a fresh claim on #6468 (narrowed by planner)
  produces the directive's named lemma in ~100–200 lines.

## Blockers

- None. The decomposition uses only existing pre-merged infrastructure;
  no new toolchain or dependency changes required.
