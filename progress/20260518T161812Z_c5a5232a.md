# Review session — post-#4991 cascade audit (issue #5031)

## Accomplished

Executed issue #5031 read-only audit on the post-#4991 helper-extraction
cascade. Branch baseline: `b1c7ccce` (origin/main HEAD after #5028
merge). PR #5029 (closes #5024) and issue #5030 still open at audit
time; audit reports the resulting interim state.

Posted single audit comment on issue #5031
(https://github.com/kim-em/hex/issues/5031#issuecomment-4479617158)
covering all three invariants with verbatim grep commands and
expected-vs-observed counts.

## Findings

* **Invariant 1 (call-site coverage):** 7 of 8 helpers pass.
  One deviation — `Basic.lean:5338` in
  `monicModularImage_modP_eq_of_monic` derives `0 < core.size`
  via a 9-line by_contra/cases proof despite having
  `hcore_monic : Hex.DensePoly.Monic core` in scope.
  `zpoly_size_pos_of_monic` would discharge it in one application
  but is `private` and defined at `Basic.lean:6720`, *after* the
  call site — fix requires hoisting the helper. Audit comment
  drafts a follow-up issue title and body for the next planner
  to file.
* **Invariant 2 (docstring framing):** Three missing docstrings
  (`zpoly_ne_zero_of_monic`, `zpoly_lc_pos_of_monic`,
  `zpoly_ne_zero_of_pos_lc`) match exactly the scope of pending
  issue #5030 — not a fresh deviation. Framing on the four
  documented Monic siblings is internally consistent; the two
  non-Monic helpers (`defaultFactorCoeffBound_leadingCoeff_natAbs_le`,
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`) use
  multi-line descriptive docstrings, which is the correct shape
  for "composition / packaging" helpers (not single-implication
  projections). No PR-number / session metadata in any docstring.
* **Invariant 3 (cross-file consistency):** Pass — each of the
  eight helpers is defined exactly once across both
  `Basic.lean` and `IntReductionMod.lean`.
* Three inline `monic_primitive_sign_normalized_of_monic`
  triple-destructure sites remain at
  `Basic.lean:10090-10091, 10200-10201, 13602-13603` — pending
  PR #5029. Not a fresh deviation.

## Current frontier

Audit complete. No code changes (review is read-only — confirmed
clean `git diff --check` and `git diff origin/main` empty before
posting the comment). `python3 scripts/check_dag.py` exit 0.

## Next step

Close issue #5031 (review work item complete via comment
deliverable). Next planner cycle will pick up the drafted
follow-up issue (the 5338 hoist + rewire) per the audit-comment
breadcrumb.

## Blockers

None.
