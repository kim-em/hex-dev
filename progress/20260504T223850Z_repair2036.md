# PR #2036 repair: HexPolyZ oracle workflow merge

## Accomplished

- Claimed PR #2036 for repair.
- Merged `origin/main` into `agent/ca715d7e` and resolved the
  `.github/workflows/conformance.yml` conflict by keeping both the
  HexPolyZ and HexGramSchmidt python-flint oracle fixture blocks.
- Verified locally with `lake build`, `python3 scripts/check_dag.py`,
  `git diff --check HEAD`, `python3 scripts/conformance_targets.py --check`,
  a PR-diff banned-marker scan, and the HexPolyZ fixture/oracle path:
  `lake exe hexpolyz_emit_fixtures`, fixture `diff`, and
  `python3 scripts/oracle/polyz_flint.py`.

## Current frontier

PR #2036 is locally merge-conflict-free after the merge commit. The
only remaining work for this session is to push the repaired branch
and mark the PR salvaged.

## Next step

- Push `agent/ca715d7e` with `--force-with-lease`.
- Run `coordination mark-pr-salvaged 2036`.

## Blockers

None.
