# HexHensel Phase 5 completion audit and bump (#3449)

## Accomplished

- Re-audited HexHensel against Phase 5 exit criteria from `PLAN/Phase5.md`.
- Confirmed zero banned markers (`sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`) across `HexHensel/` and `HexHensel.lean`.
- Confirmed `HexHensel.lean` imports `HexHensel.Conformance` and
  `HexHensel.CrossCheck`, keeping the Phase 3 conformance surface on
  the library build path.
- `lake build HexHensel` is green (156 jobs). The single in-tree
  `sorry` warning lives in `HexPolyFp.SquareFree`, a dependency;
  Phase 5 coupling is local, so deps' proofs are not HexHensel's
  responsibility.
- Bumped `libraries.yml` `HexHensel.done_through` from `4` to `5`.
- `python3 scripts/status.py HexHensel` now reports
  `HexHensel -> Phase 6 (proof polishing)`.
- `python3 scripts/check_dag.py` exits clean; `git diff --check` is
  clean.

## Current frontier

HexHensel has cleared Phase 5. The downstream
`HexBerlekampZassenhaus` pipeline continues to depend on its declared
API. `HexHenselMathlib` is independently at Phase 4 (performance &
benchmarking) and unaffected by this bump.

## Next step

The next worker for HexHensel would pick up Phase 6 (proof polishing)
per `PLAN/Phase6.md`. Sibling Phase 5 audit issues `#3450` (HexConway)
and `#3451` (HexGfqRing) remain unclaimed.

## Blockers

None.
