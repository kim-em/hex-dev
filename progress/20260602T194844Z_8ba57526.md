# 20260602T194844Z work #6385

## Accomplished

- Claimed #6385 and completed the canonical selector-product bridge in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Added the closed member-list equality
  `ForwardRecoveryInputs.selectedFactorsOfMembers_polyProduct`, proving that
  the executable BHKS selected-factor array for a support-class member list has
  the same product as the proof-side `liftedFactorSubsetOfMembers` product.
- Added `selectedFactorArraysOfSupports_polyProduct`, the indexed support
  partition wrapper that feeds directly into
  `productCongruencesOfCanonicalSupportMemberRepresentations`.

## Current frontier

The selector-product equality for canonical support classes is closed.  The
remaining BHKS D1 work is outside this issue: downstream callers can now use the
indexed equality with existing `RepresentsIntegerFactorAtLift` certificates to
discharge the canonical `hproduct` family.

## Next step

Use `selectedFactorArraysOfSupports_polyProduct` in the next recovery assembly
slice that packages the canonical expected-factor representations for
`factorFast_ne_none_of_mignottePrecisionCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound`.

## Blockers

No blocker for this issue.  The full library still reports pre-existing
warnings in unrelated modules, including existing `sorry` warnings, but no new
forbidden tokens were added in the touched Lean diff.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
