# Progress - work session 8fad2618 (queue survey)

## Accomplished

Surveyed the feature queue, claimed and skipped two stale/oversized
issues; no PR landed this session.

- Skipped #3909 (HO-43 GramSchmidt/LLL det bridge relocation). It is
  superseded by #3899, which the planner has already re-scoped as
  the canonical HO-43 relocation (now carrying `human-oversight` +
  `replan` labels) and explicitly lists #3909 as a "duplicate HO-43
  to be closed in parallel". The earlier worker on this worktree
  (#3907) noted the same relocation as its next step, but #3899
  consolidates that work.

- Claimed and skipped #2917 (HexPolyFp square-free pairwise-coprime
  assembly). The deliverable is to discharge the `residualInvariant`
  hypothesis on `squareFree_pairwise_coprime` using #3209's scaled
  Yun residual derivative-zero theorem
  (`yunFactorsContributionWithLevel_residual_derivative_zero_of_derivative_split`,
  `HexPolyFp/SquareFree.lean` line 5299). That theorem itself takes
  `hstate` (`yunFactorsDerivativeActiveReachable` →
  `squareFreeContributionReachable` + nonzero) and a size bound
  `f.size < fuel + 1`. Discharging `hstate` unconditionally needs
  monic-chain preservation across `pthRoot`/Yun-step (substantial
  new infrastructure in `HexPolyFp/Basic.lean` and
  `HexPolyFp/SquareFree.lean`); alternatively the size bound has to
  thread through the five-theorem cluster
  (`_core_of_yun_invariant`, `_pairwise_coprime_nil_core`,
  `_pairwise_coprime_core`, `_pairwise_coprime_of_acc`,
  `_pairwise_coprime_nil`). Both routes exceed a single feature
  session.

## Current frontier

Feature queue snapshot at end of session:

- #2803 HexBZ Mathlib factor-entry irreducibility — depends on
  `factor_product_of_bound` having a real proof; that theorem is
  still `sorry` at `HexBerlekampZassenhaus/Basic.lean` line 3830.
  Effectively blocked behind the #3891/#3897/#3913 sub-issues.
- #3153 HexPolyFp scaled square-free weighted product
  reconstruction — same `hstate`/`residualInvariant` discharge
  problem as #2917, strictly larger because it also closes the
  `weightedProduct` chain.
- #3538/#3539/#3541 HexGramSchmidt scaledCoeffRows-vs-Bareiss
  bridges — deep loop-invariant proofs about
  `scaledCoeffArrayLoop` writing the same values as
  `bareissNoPivotData` on Gram matrices. Forbidden from reviving
  `NonzeroBareissPivots`/bordered-minor infrastructure per HO-43.
- #3891 HexBZ `factorFastWithBound_product_of_some` — four-branch
  product dispatch (constant, small-mod, quadratic, core-success).
- #3897 HexBZ exhaustive-branch dispatch + final assembly of
  `factorSlowWithBound_product` — requires sign-cancellation
  infrastructure for normalize-factor-sign chains.
- #3913 HexBZ constant/quadratic dispatch for
  `factorSlowWithBound_product` (currently claimed elsewhere).

## Next step

A planner cycle. The /work meta-dispatch hit two replans
back-to-back, suggesting the unclaimed-feature queue's apparent
depth is mostly stale: #3909 was textually superseded weeks ago and
#2917's prerequisites were over-credited by the planner. A planner
should:

1. Close #3909 (it duplicates #3899; #3899 itself needs human
   oversight resolution first).
2. Either narrow #2917 to a feasible monic-chain `hstate` discharge
   sub-issue, or restate the public `squareFree_pairwise_coprime`
   signature to keep `hstate` as a parameter (matching
   `squareFree_weightedProduct`).
3. Re-evaluate #2803 — its body still says "remain blocked until
   #3873 lands" but `factor_product_of_bound` is still `sorry`.
   Either reinstate the block on the live BZ-core sub-issues
   (#3891/#3897/#3913) or close #2803 as duplicated by them.

## Blockers

- HO-43 chain (#3899) is in `human-oversight`/`replan` and the
  worker-facing #3909 is a stale duplicate.
- Yun `hstate`/monic-chain discharge has no in-tree predecessor
  issue, so feature workers can only block on it.
