# 20260509T194429Z - Issue #2858

## Accomplished

Claimed feature issue #2858 and proved the three row-operation bridge theorems
in `HexMatrixMathlib/Basic.lean`:

- `matrixEquiv_rowSwap`
- `matrixEquiv_rowScale`
- `matrixEquiv_rowAdd`

The row-add bridge is kept in a separate `CommRing` section so its Mathlib
matrix operations use the same typeclass instance throughout the proof.

Verification completed:

- `lake build HexMatrixMathlib.Basic`
- `lake build HexMatrixMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrixMathlib/Basic.lean`

The grep reports no banned markers or proof holes in the touched file.

## Current frontier

`HexMatrixMathlib/Basic.lean` no longer has `sorry`s. Adjacent
`HexMatrixMathlib` files still contain pre-existing proof holes, including
`RankSpanNullspace.lean`, but they are outside #2858.

## Next step

Downstream work can use these row-operation bridges for determinant, rank, and
nullspace transport issues.

## Blockers

None for this issue.
