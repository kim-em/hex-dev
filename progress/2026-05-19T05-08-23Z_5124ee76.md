# GFq unary/sub representative wrappers — issue #5250

Session: 5124ee76-f662-4fc6-95f4-77638be8ae64, type work/feature worker.

## Accomplished

- Listed unclaimed work and selected #5250 as the narrow unblocked Phase 6 item.
- Claimed #5250.
- Added `GFq.repr_neg` and `GFq.repr_sub` in `HexGfq/Basic.lean`, immediately
  after the existing `GFq.repr_add` and `GFq.repr_mul` wrappers.
- Verified:
  - `lake build HexGfq.Basic`
  - `lake build HexGfq`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git grep -nE '^@\[simp\] theorem repr_(neg|sub)' HexGfq/Basic.lean`
  - Scoped forbidden-token diff check for `HexGfq/Basic.lean`
- Opened PR #5272 for #5250.

## Current frontier

The generic Conway-specialized `GFq` convenience layer now exposes unary and
subtractive representative simp wrappers matching the underlying
`HexGfqField` API.

## Next step

Monitor PR #5272 through CI and auto-merge.

## Blockers

None for #5250. The worktree still contains a pre-existing unrelated
modification to `.claude/CLAUDE.md`; this session did not touch or stage it.
