# 20260601T142817Z — fd9aa0f9: #5998 one-row cofactor replacement bridge

## Accomplished

Checked the unclaimed queue and claimed #5912 first because it was the oldest
feature and unblocked a long HexMatrix chain. Verified it was stale: #5917 had
been closed for replan and the arbitrary basis-vector Plucker theorem needed by
#5912 was not present in `HexMatrix/Determinant.lean`. Marked #5912 for replan
with that concrete stale-dependency reason.

Claimed #5998 and implemented the bridge-layer one-row replacement cofactor
identity in `HexMatrixMathlib/Determinant/Core.lean`:
`det_mul_cofactor_setRow_eq_cofactorRowPairing_mul_sub`. The proof stays out of
`HexMatrix/Determinant.lean`, uses Mathlib adjugate identities through
`matrixEquiv`, and rewrites back to the Hex APIs `setRow`, `cofactor`, and
`cofactorRowPairing`.

## Current frontier

#5998 is complete and ready for PR. The new theorem supplies the reusable
one-row replacement bridge needed by the downstream two-row adjugate replacement
identity (#5999).

## Next step

After this lands, continue with #5999 by consuming
`det_mul_cofactor_setRow_eq_cofactorRowPairing_mul_sub` instead of rebuilding
the adjugate argument.

## Blockers

None for #5998. #5912 remains stale until the arbitrary basis-vector Plucker
transport issue is replanned and completed.

## Verification

* `lake build HexMatrixMathlib` — green.
* `git diff --check` — green.
* `git diff -- HexMatrixMathlib/Determinant/Core.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true` — no matches.
