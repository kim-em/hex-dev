# Non-polynomial arbitrary-propagator vertical

## Accomplished

- Added a shape-triggered package which recognizes `sin (-x)` by opaque
  operation keys, instantiates `sin x` and `-(sin x)`, and admits a
  package-owned equality edge.
- Added generic sine and negation forward propagators with nonempty
  node-index/method-tag certificate bodies.
- Defined structural real semantics over arbitrary programs and
  action/node-generic fact and equality schemas rather than a literal
  two-program model table.
- Replayed the quoted trace through instance, equality, sine, negation, meet,
  transport, and final implication checking.
- Added rejection tests for both a wrong sine-identity schema and a wrong
  `Real.sin_neg` method tag.
- Proved an inhabited base-program model and the concrete theorem that
  `0 ≤ x ≤ 2` implies `-1 ≤ Real.sin (-x) ≤ 0`.
- Added the conformance target and recorded the implemented acceptance case
  and generic base-lift obligation in the SPEC.
- Built `HexInterval.SinOddConformance`; the conformance manifest, copyright
  check, line-count check, trust-surface check, banned-token scan, and
  `git diff --check` are clean.

## Current frontier

The vertical is green on the pre-lift semantic checker. Its package-owned
extension theorem is checked during instance replay, but the local base theorem
still uses that theorem through an explicitly temporary manual lift.

## Next step

Restack on the generic composed-extension/`holdsPrefix` checker change and
replace the isolated temporary lift with the base-program entailment returned
directly by `SemanticReplay.check`.

## Blockers

None. The generic lift change is already available on the parent stack.
