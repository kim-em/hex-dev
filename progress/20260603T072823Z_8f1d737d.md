# 20260603T072823Z work #2567

## Accomplished

Added `factorFast_terminates_ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
in `HexBerlekampZassenhausMathlib/Recovery.lean`: a final-step wrapper that
threads the pointwise auxiliary vector/correction bounds plus the
`bhksPaperThresholdReal`-shape estimate (along with the bridge data, projected
vector, CLD column bounds, executable precision/prime-choice equations, and the
canonical-recovery tail package) directly to `Hex.factorFast f ≠ none`.

The proof is a pure composition: it instantiates
`FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
on the cap-separation side and feeds the resulting record into
`factorFast_terminates` together with the recovery tail inputs.

## Current frontier

The final assembly chain now has a single top-level wrapper that consumes the
deepest available BHKS Theorem 5.2 inputs (pointwise auxiliary bounds + paper
threshold) without exposing the intermediate `FactorFastCapSeparationInputs`
record to callers. Remaining HO-4 work is unchanged: bound the canonical
`coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree` product
by `bhksPaperThresholdReal squareFreeCore C` for the actual cap-lift bridge
(the genuine BHKS §5 inequality content), and assemble the pointwise vector
and correction-weight estimates from BHKS Lemma 3.2.

## Next step

Either:

1. Specialise the new wrapper to a fixed `vectorSquareBound` /
   `correctionWeightedBound` that pulls explicit bounds out of the bridge data
   (e.g., a `BadVectorBridgeData` accessor returning concrete reals), so the
   pointwise hypotheses become derived rather than caller-supplied.
2. Prove the `hpaper` paper-threshold-shape inequality for the cap-lift
   `coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree`
   product against `bhksPaperThresholdReal squareFreeCore C` — this is the
   genuine BHKS §5 Theorem 5.2 inequality content and the largest remaining
   analytic gap.

## Blockers

No new technical blocker. The remaining open prerequisites (#6344, #6345) are
the same canonical-support recovery tail providers cited by earlier sessions.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus` passed.
- Added-line forbidden-token grep over the touched file found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
