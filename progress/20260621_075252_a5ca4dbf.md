# 2026-06-21 07:52 UTC — one-shot work session (a5ca4dbf)

Session type: `/once` work dispatch on issue #8064
(`perf: van Hoeij recombination core scales steeply at high degree`).

## Accomplished

- Verified that #8064's core deliverable was already delivered by merged
  PR #8271 (`ba97dd7`): linear-time `@[csimp]` `chooseFast` removed the
  `r·2^(n-1)` `Hex.Nat.choose` blowup in the CLD lattice build, taking
  degree-24 split-family `factorFast` from 30.5 s → ~2.75 s (11×).
- Re-measured the current tree with a per-stage attribution harness
  (temporary `attrib_prof` exe, since reverted) splitting `choosePrimeData?`
  out of full `factorFast` on the split inputs:

  | degree | factorFast | choosePrimeData? | prime-selection share |
  |--------|-----------|------------------|----------------------|
  | 16 | 118 ms | 109 ms | 92% |
  | 20 | 372 ms | 352 ms | 95% |
  | 24 | 933 ms | 897 ms | 96% |

  The recombination core this issue targets (CLD build + `hex-lll` LLL +
  Hensel precision schedule + reconstruction) is now ~4% of `factorFast`
  at degree 24 (~36 ms). The attribution has inverted relative to the
  issue's filing premise.
- Closed #8064 as completed, with the evidence table and pointers to the
  follow-up issues that own the now-dominant prime-selection cost:
  #8269 (Berlekamp EDF gcd-sweep guard) and #8063 (`DensePoly.divMod`
  over F_p substrate).

## Current frontier

The van Hoeij recombination core is no longer the high-degree bottleneck.
`choosePrimeData?` (prime selection → `primeChoiceDataScore` →
`berlekampFactorsModP`) dominates at ≥92% across degrees 16/20/24.

## Next step

None for the recombination core — optimizing a 4% non-bottleneck stage is
premature. High-degree `factorFast` wins now live in the prime-selection
subsystem, tracked by #8269 and #8063.

## Blockers

None. No code change was warranted for this issue; the work was already
merged and the residual is out of scope and separately tracked.
