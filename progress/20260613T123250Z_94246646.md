# Progress: #6827 — scalar-aware normalized Yun product invariant

## Accomplished

Added a private derivative-active product invariant in
`HexPolyFp/SquareFree.lean`:

- `yunFactorsContributionWithLevel_normalized_pow_invariant`

The theorem packages the existing raw contribution identity together with the
normalized-tail scalar form
`pow (DensePoly.C (normalizeMonic contribution.2).1) * pow (normalizeMonic contribution.2).2`,
so the residual scalar introduced by normalization is explicit. The
derivative-active contribution proof now routes through this theorem instead of
assembling the raw product directly at the use site.

## Current frontier

The SquareFree derivative-active proof still ultimately consumes the raw
identity conjunct for the existing exact `squareFreeAuxRevContribution` result,
but the normalized-tail invariant is now exposed at the local contribution
boundary for the follow-on provider refactor.

## Next step

Use the new invariant in #6828 to replace raw weighted-product provider plumbing
with normalized completion/provider inputs.

## Blockers

None for this issue.
