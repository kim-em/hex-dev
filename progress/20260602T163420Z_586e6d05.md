# 20260602T163420Z 586e6d05 — Directive #2567 (HO-4)

## Accomplished

- Claimed directive #2567 and followed the current partial-completion path for
  HO-4 / BHKS D1.
- Added
  `no_bhks_bad_setup_of_factorFastCapLiftAnalyticComparison` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The theorem specializes the generic
  `ExecutableBadVectorWitness.no_bhks_bad_setup_at_factorFastPrecisionCap`
  wrapper to the actual `factorFastCapLiftData` witness and consumes the named
  `FactorFastCapLiftAnalyticComparison` package directly, so downstream
  actual-cap callers no longer need to project its raw Hadamard/l2norm field
  before eliminating a bad-vector setup.

## Current frontier

The public theorem
`factorFast_terminates : ∀ f : Hex.ZPoly, Hex.factorFast f ≠ none` is still
not fully assembled. This session narrows the actual-cap bad-vector comparison
surface; it does not construct canonical true supports, per-class Mignotte
product congruences, or the final recovery package.

## Next step

Continue the D1 chain through the existing narrow residual issues: #6344
canonical true-support recovery inputs, #6345 per-class Mignotte product
congruences, #6346 final assembly, and #6361 for the remaining direct producer
of `FactorFastCapLiftAnalyticComparison` from the BHKS norm/cap arithmetic.

## Blockers

- Full #2567 remains blocked on the semantic recovery prerequisites currently
  split across #6344/#6345/#6346, with #6361 covering a remaining analytic
  producer surface.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
