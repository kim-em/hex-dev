# Session 8119ec36 — Yun residual derivative-zero provider (#3770)

## Accomplished

- Claimed #3770 after human-oversight items were blocked, stale, or already
  marked for replan.
- Added a private recursive provider
  `yunFactorsContributionResidualComplete_of_derivative_zero_common` in
  `HexPolyFp/SquareFree.lean`, threading derivative-zero of the repeated part
  and the current common-divisor condition through Yun tail steps.
- Added derivative-zero wrappers for the contribution residual and the
  derivative-active split, including
  `yunFactorsResidualDerivativeZero_of_derivative_active_coprime`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested marker
  scan. The scan reports only the pre-existing tracked
  `squareFreeAuxRevContribution_derivative_active_pow_obligation` `sorry`.

## Current frontier

The residual derivative-zero provider can now be derived from the derivative
split coprimeness witness without passing an opaque terminal-residual witness.
Downstream #3239 can use the new derivative-active coprime wrapper when
assembling the reachable Yun residual provider path.

## Next step

Wire this provider into the derivative-active square-free assembly chain, or
continue with #3821 to expose the terminal repeated residual reachability and
nonzero facts needed by the recursive square-free IH.

## Blockers

None for this slice. The existing #3782 `sorry` in
`squareFreeAuxRevContribution_derivative_active_pow_obligation` remains out of
scope for #3770.
