# 2026-05-13T14:05:25Z — feature session 690ed94e

## Accomplished

Closed issue #3523 (HexBZ Mathlib: build ForwardRecoveryInputs from cap
separation). Added two declarations to
`HexBerlekampZassenhausMathlib/Recovery.lean`:

- `BHKS.ForwardRecoveryInputs.ofCapSeparation` — fixed-lift constructor that
  builds `ForwardRecoveryInputs f d` from cap-level inputs (`rows_pos`,
  bad-vector witness params, `trueSupports`, cap dominance
  `Hex.factorFastPrecisionCap f ≤ d.k`, `ExecutableCapSeparationHypotheses`,
  Mignotte side condition, and the B7/A2 candidate facts already accepted by
  `ofIndicatorCandidateFacts`). The `lattice_eq_indicators` field is
  supplied internally by
  `BHKS.projectedRowsOfLiftData_eq_trueFactorIndicatorLattice_of_cap`; the
  rest delegates to `ofIndicatorCandidateFacts`.
- `BHKS.bhksRecover_eq_some_of_capSeparation` — small corollary composing
  `ofCapSeparation` with `bhksRecover_eq_some_of_forwardInputs` at a fixed
  `LiftData`.

## Current frontier

- `HexBerlekampZassenhausMathlib/Recovery.lean` now exposes a one-shot
  cap-separation entry point that hides the manual field-by-field wiring
  between cap separation and `ForwardRecoveryInputs`.
- Bad-vector packaging and the scheduled-recovery bridge surfaces remain
  owned by the closed predecessor issues (#3520, #3512); this PR did not
  re-prove cap separation or repackage resultant obligations.

## Next step

Downstream work on BHKS Theorem 5.2 (issue #2567) can now construct
`ForwardRecoveryInputs` through `ofCapSeparation`, leaving only the
abstract B7/A2 obligations (`indicators_match`, per-indicator candidate
facts) to discharge.

## Blockers

None.
