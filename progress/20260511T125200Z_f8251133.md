# 2026-05-11 12:52 UTC — PR #3402 repair

## Accomplished

Claimed PR #3402 for repair and diagnosed the failed ubuntu `build` job.
The Lean build and conformance checks had passed; the failure was in the
bench smoke gate when `scripts/oracle/setup_lll_isabelle.sh` hit repeated
Zenodo HTTP 504 responses while fetching the verified Isabelle LLL archive.

Extended that script's `curl` retry window while keeping the same source URL
and SHA-256 verification. The direct setup command then observed one 504,
retried successfully, built `svp_verified`, and the LLL bench smoke check
passed.

Verified:

- `bash -n scripts/oracle/setup_lll_isabelle.sh`
- `git diff --check`
- `python3 scripts/check_dag.py`
- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt.Update`
- `scripts/oracle/setup_lll_isabelle.sh`
- `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)" lake exe hexlll_bench list`
- `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)" lake exe hexlll_bench verify`
- `lake build`

## Current frontier

PR #3402 remains the same Gram determinant bridge feature, with an added CI
resilience fix for the Isabelle LLL bench helper download.

## Next step

Push the repaired PR branch and mark PR #3402 salvaged.

## Blockers

None.
