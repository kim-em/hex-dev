# 2026-06-05 01:16Z — route factorSlow through integer trial division (#6542)

## Accomplished

Closed issue #6542 by rewiring the slow-path raw-factor definitions in
`HexBerlekampZassenhaus/Basic.lean` away from
`exhaustiveCoreFactorsWithBound (... primeData)` and onto the
prime-independent integer trial-division core
`exhaustiveIntegerTrialCoreFactorsWithBound` landed in #6541.

Definition changes:

- `exhaustiveSlowRawFactorsWithBound` now reassembles around
  `exhaustiveIntegerTrialCoreFactorsWithBound (normalizeForFactor f).squareFreeCore B`.
- `factorSlowFactorsWithBound`'s non-quadratic fallback branch uses the
  same integer trial core. The constant-core and quadratic-root
  short-circuits are untouched.
- `exhaustiveSlowRawFactorsWithBound?` and `factorSlowFactorsWithBound?`
  become total `some` wrappers; their characterisation theorems
  (`_eq_some_of_isSome`, `_eq_some_iff_safe_branch`) are replaced by a
  single `_eq_some : ... = some ...` rfl statement.

Branch / product / entry-shape lemma updates:

- `factorSlowFactorsWithBound_branch` third disjunct now names the
  integer trial core array.
- Witness-form variant `factorSlowFactorsWithBound_branch_of_choosePrimeData?_some`
  removed (its `hchoose` premise is no longer meaningful for the slow
  path; the plain `_branch` lemma is the canonical statement).
- `factorSlowFactorsWithBound_polyProduct` rewired via
  `exhaustiveIntegerTrialCoreFactorsWithBound_polyProduct` (companion
  invariant from #6541).
- `factorSlowWithBound_product_of_exhaustive_branch` rewired via the
  companion `_normalizeFactorSign` and `_shouldRecord` invariants,
  discharged from `squareFreeCore_leadingCoeff_pos_of_ne_zero`.
- `exhaustiveSlowRawFactorsWithBound_mem_normalization_or_core` updated
  to expose the integer trial core array on the right disjunct.
- `factorWithBound_entry_mem_exhaustive_branch_xPower_or_core_of_reassemblyComplete`
  and its `factor`-default specialisation drop their
  `(primeData, hchoose)` premises and expose the integer trial core
  array on the right disjunct.

`choosePrimeData`, `choosePrimeData?`, `exhaustiveCoreFactorsWithBound`,
and the BHKS modular recombination machinery remain in place for
fast-path and BHKS consumers per the issue scope.

Verification:

- `lake build HexBerlekampZassenhaus.Basic` green.
- `lake build HexBerlekampZassenhaus` green.
- `rg -n "factorSlowFactorsWithBound|exhaustiveSlowRawFactorsWithBound" HexBerlekampZassenhaus/Basic.lean | rg choosePrimeData`
  empty: slow definitions no longer call `choosePrimeData`.
- No new forbidden tokens (`axiom`, `native_decide`, `TODO`, `FIXME`,
  `sorry`).

## Current frontier

Slow-path raw definitions and Basic.lean branch / product / entry-shape
lemmas are migrated. The Mathlib-side caller
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
in `HexBerlekampZassenhausMathlib/IntReductionMod.lean` still threads the
old modular-core irreducibility argument (Hensel lifting on
`exhaustiveCoreFactorsWithBound (... primeData)`) and now fails to
type-check against the updated branch-shape lemma signature. This is
expected: the integer-trial-division slow path needs a fundamentally
different irreducibility argument (exact integer divisor enumeration up
to the Mignotte bound), and the parent issue #6530 sequences that as a
follow-up.

## Next step

A successor planner / worker should retool
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
(and its umbrella `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`)
to discharge irreducibility from the integer-trial enumeration rather
than the Hensel modular subset-correspondence chain. The new argument
will use the `exhaustiveIntegerTrialCoreFactorsWithBound` companion
invariants (`_polyProduct`, `_normalizeFactorSign`, `_shouldRecord`)
plus a "no nontrivial integer divisor exists up to Mignotte" closure
lemma.

## Blockers

None for this issue. Mathlib-side follow-up tracked by parent #6530.
