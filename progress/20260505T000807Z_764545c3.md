# HexPoly executable gcd-order laws

UTC: 2026-05-05T00:08:07Z. Session type: work/feature.

## Accomplished

- Claimed feature issue #2108.
- Added generic `DensePoly.xgcdAux` divisibility infrastructure in `HexPoly/Euclid.lean`.
- Proved reusable wrappers:
  - `DensePoly.gcd_dvd_left_of_divModLaws`
  - `DensePoly.gcd_dvd_right_of_divModLaws`
  - `DensePoly.dvd_gcd_of_divModLaws`
- Added the Rat constant-divisor remainder helper in `HexPolyZ/Basic.lean`.
- Replaced the first three `ratGcdLaws` placeholders with proofs using the new generic surface.
- Rebasing onto current `main` brought in the #2107 Bezout helper; after conflict resolution, `ratGcdLaws.xgcd_bezout` is also filled from `DensePoly.xgcd_bezout_of_divModLaws`.
- Reduced touched-file `sorry` count in `HexPolyZ/Basic.lean` from five to one; the remaining proof is the out-of-scope square-free theorem.

## Current frontier

The executable Rat gcd law package is complete. Downstream work can now use `DensePoly.GcdLaws Rat` for the quotient-by-gcd square-free path.

## Next step

Use the completed `DensePoly.GcdLaws Rat` instance to advance #1930/#2109.

## Blockers

- `HexPolyZ/Basic.lean` still has one pre-existing `sorry` outside this issue's scope.
- The worktree contains an unrelated `.claude/CLAUDE.md` modification that this session did not touch or commit.
