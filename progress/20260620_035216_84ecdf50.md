# HexPolyZ Phase 6 exit-criteria audit (#8174)

Session type: review. Audited `HexPolyZ` (`done_through: 5`) against the
four Phase 6 exit criteria and performed the `5 → 6` bump after clearing
dead code.

## Accomplished

**Criterion 1 — Mathlib linter: PASS.** No `#lint` framework exists in the
Mathlib-free layer, so the check reduces to the built-in Lean linters that
fire during elaboration. Forced re-elaboration of `Basic.lean` and
`Mignotte.lean` (`lake env lean <file>`) emits zero warnings, before and
after the edits.

**Criterion 2 — docstrings: PASS.** Every public `def`/`structure`/
`class`/`inductive`/`abbrev` and every public `theorem`/`lemma` in both
core files carries a docstring (Basic: 0 missing; Mignotte: 0 missing).
Prior polish (#7059, #7072) had already closed this; the audit confirms it
and the dead-code deletion left it at 100%.

**Criterion 3 — dead declarations: FIXED then PASS.** A reachability
analysis (roots = public/instance/`simp`/`grind`-tagged decls plus anything
referenced from other files; edges = name references inside decl bodies)
found a cohesive **17-declaration** orphaned branch, all `private` and
unreachable from the public API:

- `Mignotte.lean` (15 decls, 306 lines): the superseded `near_or_sq_le` /
  very-far-contract Newton-square-root convergence formulation —
  `sqrtStep_far_contracts`, `sqrtStep_far_lt_self`, `sqrtNearEnvelope`,
  `sqrtNearEnvelope_of_not_very_far`, `sqrtAux_near_or_sq_le`,
  `sqrtAux_preserves_sq_or_near`, `sqrtStep_very_far_contracts`,
  `sqrtStep_very_far_lt_self`, `sq_pow_mul`, `sqrtAux_contract_of_not_done`,
  `log2_succ_pow_bound`, `phase_one_power_contradiction`,
  `sqrtAux_phase_one_sharp_core`, `sqrtAux_phase_one_near_or_sq_le`,
  `sqrtAux_full_fuel_near_or_sq_le`. The live soundness path uses the
  parallel `_sq_le` lemmas (`sqrtAux_phase_one_sq_le` →
  `sqrtAux_full_fuel_sq_le` → `floorSqrt_sq_le`, plus `le_ceilSqrt_sq`);
  the `near_or_sq_le` branch had no remaining consumer and no open issue
  planning one.
- `Basic.lean` (2 decls, 48 lines): `rat_size_le_one_of_mul_dvd_self`
  (#2183, May) and `rat_quotient_derivative_gcd_dvd_repeated` (#2153, May),
  aged unreferenced helpers. Their callees (`rat_common_divisor_...`) retain
  live consumers, so no further cascade.

Removed all 17; `lake build HexPolyZ` stays green (the compiler is the
ground truth that they were dead), a re-scan reports zero remaining dead
decls, and the dependents `HexHensel` and the merge-gating bridge
`HexPolyZMathlib` build green.

**Criterion 4 — native path: PASS.** No `native_decide`, no
`implemented_by`/`extern`/`opaque` runtime oracle anywhere in the library.

**Verdict: all four criteria pass after the inline dead-code removal.**
Bumped `libraries.yml[HexPolyZ].done_through` 5 → 6. `check_dag.py` exit 0;
`status.py` now reports HexPolyZ at Phase 7. No `axiom`/`sorry`/
`native_decide` introduced; the Lean diff is pure deletion (0 insertions,
354 deletions).

## Current frontier

HexPolyZ is Phase 6 complete (`done_through: 6`), eligible for Phase 7
(user-facing documentation).

## Next step

None for this issue. A Phase 7 doc pass (HexManual reference chapter) is the
natural successor, matching the sibling `*Mathlib` Phase 7 closeouts.

## Blockers

None.
