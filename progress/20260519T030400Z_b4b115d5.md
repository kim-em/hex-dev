# /work — claimed and skipped #4334 (HexLLL Phase 4 inconclusive verdicts)

Session: b4b115d5-... (meta /work dispatch).

## Accomplished

- `coordination orient`: 3 directive issues all `blocked`; only #4334
  (feature) and #5221 (`agent-plan`, empty body, title "--help") were
  unclaimed. #5221 has no worker label, so #4334 was the only candidate.
- Claimed #4334; probed host load:
  - `uptime`: load averages 11.72 12.16 12.41 on 24-core box (~49% on
    1-min average).
  - Top CPU consumers: Code Helper (Plugin) at 119.9%, Code Helper
    73713/16976/14211/11532 Python procs all at ~99%, replayd at 89.2%,
    plus a 53.8% rsync.
  - `lake exe hexlll_bench list` smoke: 89.16s wall / 256.78s user,
    user/wall ratio ≈ 2.88. Matches the documented 2.85-2.90 contention
    range that every prior skip on #4334 recorded.
- Skipped #4334 with the canonical host-contention reason and a pointer
  to this progress file. Re-applies the issue's standing redispatch
  precondition: quiet/dedicated hardware, or the release-candidate
  benchmarking workflow per `SPEC/benchmarking.md` §"Scientific timing
  runs".

## Current frontier

#4334 remains the sole open verdict-promotion blocker for HexLLL
Phase 4. The unclaimed worker queue is otherwise empty (3 directives all
`blocked`, #5221 not a worker label). The realistic forward path is the
release-candidate benchmarking workflow on dedicated hardware, not
another opportunistic carica probe — the host has now skipped at least
11 consecutive worker attempts with user/wall ratios consistently in the
2.8-2.9 range.

## Next step

If carica drops to load averages well below 5 with a user/wall ratio
near 1 on the list smoke, the next /work session can attempt the
scientific run. Otherwise the dedicated scheduled benchmark workflow is
the right vehicle.

## Blockers

Host carica sustained contention (load averages 12-23 across all
documented skips on this issue) inflates the per-spawn floor enough that
every parametric registration on the issue produces zero verdict-eligible
rows.
