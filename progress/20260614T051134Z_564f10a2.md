# Progress: #7036 — migrate monic partition consumers to lifted candidate equality

## Accomplished

Claimed #7036 (monic consumer slice of #7030, depends on the now-merged #7035).
Established the baseline: `HexBerlekampZassenhausMathlib/Basic.lean` is hard-red
on `main` with 11 `Application type mismatch` errors, all passing
`RepresentsIntegerFactorAtLift` where the old scaled-product modular recovery
lemmas expect a `reduceModPow` congruence. Four of those belong to the monic
slice (#7036); the other seven belong to the sibling primitive/scaled slice
(#7037, which `depends-on` #7036).

Fixed the four #7036 sites soundly:

- Added `LiftedFactorSubsetPartition.recombinationCandidate_eq` (deliverable 1):
  composes the #7035 partition field `liftedRecoveryCandidate_eq` with
  `liftedRecoveryCandidate.eq_recombinationCandidate_of_lc_one` to derive the
  executable equality `recombinationCandidate d S = f` from sound hypotheses.
- Re-signatured `natDegree_toPolynomial_eq_sum_of_represents` to be partition-
  based (drops the unsound scaled-recovery derivation), and removed the dead,
  unsound `_of_bound` twin.
- Removed `representsIntegerFactorAtLift_monic{,_of_bound}`. These are not
  soundly provable in isolation: `RecoveredAtLift.monicFactor` carries no monic
  constraint, so `factor = primitivePart(monicFactor)` need not be monic. In the
  monic search the sound route is `recombinationCandidate_monic` applied to the
  partition equality (`f_cov = recombinationCandidate d S_cov`, which is monic on
  a monic core).
- Rewired the monic recursive search proof
  (`recombinationSearchModAux_some_and_covers_..._of_bound`): cover-at-min
  recovery now uses the new wrapper, monicness comes from
  `recombinationCandidate_monic`, and the natDegree positivity reads off
  `natDegree_toPolynomial_recombinationCandidate_eq_sum` through `hrec_eq`.
  exact-quotient and should-record consume the sound `hrec_eq` transitively.
- Updated the one other caller of the natDegree lemma
  (`exists_mem_representedSubset_of_degree_cover_of_bound`) to the partition
  signature; its now-unused abstract-bound hypotheses are underscore-prefixed.

## Current frontier

The four #7036 errors are gone; the file's only remaining errors are exactly the
seven #7037 sites, line-shifted by the net deletion to 9827, 10319, 14266,
14388, 14611, 16375, 16475 (declarations: `representsIntegerFactorAtLift_primitive_of_bound`,
`natDegree_..._of_primitive_pos_lc_core_of_bound`, the two `not_represents_empty_*`
lemmas, `representedFactor_dvd_recombinationCandidate_of_subset`, and the scaled
search proof). No new errors were introduced by this change.

Verification: `check_dag.py` exits 0; `git diff --check` clean; no
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` and no em-dashes on added lines;
sorry count unchanged (1 pre-existing, the C1 `isIrreducible_iff` placeholder).

## Next step

Land this PR. #7037 (primitive/scaled slice) `depends-on` #7036 and will turn
`HexBerlekampZassenhausMathlib.Basic` fully green; it can reuse
`LiftedFactorSubsetPartition.recombinationCandidate_eq` and the
`recombinationCandidate_monic` pattern. The two stale prose comments referencing
the removed `representsIntegerFactorAtLift_monic` live inside #7037's
declarations and should be updated there.

## Blockers

None for #7036. The whole-file `lake build HexBerlekampZassenhausMathlib.Basic`
cannot be green until #7037 also lands; this is the expected mid-migration state
for the (CI-unbuilt) Mathlib layer.
