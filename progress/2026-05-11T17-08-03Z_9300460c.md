**Accomplished**

- Claimed issue #3389.
- Added a private `DensePoly Int` bridge theorem in `HexPoly/Euclid.lean`
  instantiating the finite coefficient-array McCoy annihilator for actual
  polynomial coefficients.
- Used `p.size` as the finite left bound, converting polynomial product
  coefficient divisibility to finite convolution divisibility and discharging
  out-of-bound left coefficients with `coeff_eq_zero_of_size_le`.
- Verified:
  - `lake build HexPoly.Euclid`
  - `lake build HexPoly`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPoly/Euclid.lean`

**Current frontier**

- The private theorem requested by #3389 is proved without new executable
  definitions or banned markers.
- The follow-on public McCoy wrapper remains out of scope for this issue.

**Next step**

- Use this bridge theorem when assembling the public scalar annihilator wrapper
  in the dependent HexPoly McCoy issue.

**Blockers**

- None.
