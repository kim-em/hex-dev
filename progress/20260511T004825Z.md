## Accomplished

- Reviewed the landed `FormalFrontier/pod` stack on
  `feat/coordination-port-and-burners` in `/Users/kim/projects/lean/pod`.
- Compared planner-lock and claim race logic in `pod/coordination.py` against
  the pre-port bash script from `origin/main`.
- Traced ETag, GraphQL, REST fallback, back-pressure, and TUI background fetch
  paths in `pod/github.py` and `pod/cli.py`.
- Ran the targeted test set through the repo venv:
  `tests/test_github_client.py`, `tests/test_coordination.py`,
  `tests/test_burners.py`, and `tests/test_orphan_label_helpers.py`.

## Current frontier

- Ready to report review findings. Main issues are GraphQL POSTs not being
  ETag-cached despite comments claiming they are, and race-sensitive claim
  checks succeeding when comment re-reads fail or are delayed by back-pressure.

## Next step

- If applying fixes, start with `pod/github.py` GraphQL cache-key semantics and
  `pod/coordination.py` fail-closed race re-read behavior, then add focused
  regression tests.

## Blockers

- None. The system `pytest` and plain `python3` path are unusable for this repo,
  but `.venv/bin/python -m pytest ...` works.
