# 20260603T022810Z PR #6428 repair

## Accomplished

- Claimed PR #6428 for repair and checked out `agent/cf2213bb`.
- Merged `origin/main` into the PR branch.
- Resolved the single content conflict in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Preserved the PR's `FactorFastCapSeparationInputs.ofBridgeDataWithComparison`
  and raw `ofBridgeData` package constructors while retaining `main`'s
  corrected auxiliary-l2 actual-cap producer.

## Current frontier

The branch is locally merge-resolved against `origin/main` and the relevant
Lean recovery target compiles.  The staged merge also carries the already
present PR changes and progress files from the branch.

## Next step

Commit the merge repair, push the PR branch with `--force-with-lease`, and mark
PR #6428 salvaged.

## Blockers

- None for this repair.

## Verification

- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Staged Lean diff contains no added `sorry`, `axiom`, `native_decide`,
  `TODO`, or `FIXME`.
