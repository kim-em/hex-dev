# Capstone premise corrected: the swap needs re-pointing, not a hard irreducibility proof

## Key finding (verified against source)
Reading `HexBerlekampZassenhausMathlib/{FactorSoundness,Basic,IntReductionMod}.lean`
overturned the earlier capstone framing:

- `factor_irreducible_of_nonUnit` (`FactorSoundness.lean:18`) is an **accepted shipped
  `sorry`** on `main`. `factorWithBound_entries_irreducible` (`Basic.lean:421`) is
  **conditional** on an `h_raw` hypothesis (raw factors irreducible), whose producers
  are partly landed / partly blocked on the #7479-class unscaled-support gap.
- So `Hex.factor` has **no unconditional entries-irreducible guarantee**. The swap to
  the hybrid does **not** regress one, and does **not** require a new classical-tier
  irreducibility proof. Keeping the headline a `sorry` over the hybrid is no regression.

This means the capstone is the *opposite* of what was feared: not a blocked
math proof, but a large **mechanical re-pointing** of ~34 Mathlib-free + ~75
Mathlib-layer `factor`-level theorems (CI-gated dual layer, one green PR, no
intermediate green state per the `hex-lean-mathlib-boundary` skill).

## What re-points how
- **Unconditional** `factor`-level contracts — the real work: `factor_product` via
  `factorHybrid_product` (#8398, done); `factor_entry_*` / `factor_pairwise_first` /
  `factor_scalar` / Mathlib `factor_headline_*` / `FactorSoundness` via a one-time
  `factorHybrid f = factorizationOfFactors f (factorHybridFactors f)` bridge (every
  hybrid tier assembles via `factorizationOfFactors`, like `factorWithBound`).
- **Conditional** `factor_*_branch_entry_irreducible_of_choosePrimeData` cluster
  (IntReductionMod, ~7) — about `factorWithBound`'s *specific* dispatch branches;
  leave attached to `factorWithBound` (now proof-facing). Headline stays `sorry`.

## Banked this session
- #8397 (hybrid dispatch), #8398 (self-certifying + product), #8401 (smart
  recombination structural + reconstruction proof). DEV.md + issues current.

## Next step
The swap PR (step 3 in DEV.md): voluminous but not hard. Do `def factor := factorHybrid`,
add the `factorHybridFactors` bridge, re-point the unconditional `factor_*` in both
layers, keep the headline `sorry`, build the WHOLE `HexBerlekampZassenhausMathlib`
green, update Conformance.lean + baselines. Best as a focused dedicated session given
the dual-layer CI gate and ~100-theorem surface.

## Blockers
None mathematical. The remaining work is scale (theorem re-pointing across a CI-gated
layer), not a blocked proof — a meaningful de-risking of the capstone.
