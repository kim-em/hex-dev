# 2026-05-18T10:25Z — #4974: abstract-bound siblings of `reassemblyExpansionComplete_exhaustive_of_ne_zero` (monic + primitive_pos_lc_core)

## Accomplished

Claimed and executed issue #4974: added two `_of_bound` siblings in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` and rewired the two
existing originals as thin wrappers.

Specifically:

* Inserted `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
  immediately before the existing monic original. Its signature replaces
  `hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound (Hex.normalizeForFactor f).squareFreeCore < d.p ^ d.k`
  with the abstract-bound block
  `(B' : Nat) (hcore_lc_le ...) (hvalid ...) (hprecision : 2 * B' < d.p ^ d.k)`,
  inserted at the same syntactic position as the original `hprecision`.
  The proof body mirrors the original verbatim, with the single forward
  call to
  `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
  replaced by its `_of_bound` sibling (the #4971 product), threading
  `B'`, `hcore_lc_le`, `hvalid`, `hprecision`.
* Rewired the existing monic original
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` as a thin wrapper
  instantiating
  `B' := Hex.ZPoly.defaultFactorCoeffBound (Hex.normalizeForFactor f).squareFreeCore`.
  Discharges `hcore_lc_le` via `defaultFactorCoeffBound_valid` paired
  with `leadingCoeff_eq_coeff_last` (matching the existing #4971 /
  #4944 / #4938 boilerplate at adjacent layers), and `hvalid` via the
  universal `defaultFactorCoeffBound_valid` directly.
* Inserted
  `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`
  immediately before the existing primitive variant. Same abstract-bound
  shape; proof body identical except the single forward call to the
  expansion-preconditions substrate becomes its `_of_bound` sibling.
* Rewired the existing primitive original
  `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
  as a thin wrapper with the same `B'` instantiation and `hcore_lc_le`
  / `hvalid` discharge as the monic wrapper.

Both originals' signatures are preserved exactly. The sole in-tree
consumer of the monic original — the umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` at the
bottom of `IntReductionMod.lean` — continues to type-check via the
rewired thin wrapper without further edits.

Verification:

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — same 18
  errors / 39 warnings as `origin/main` baseline (all in
  `HexBerlekampZassenhausMathlib/Basic.lean`; pre-existing whnf
  timeouts and kernel-unknown errors unchanged).
* `lake build HexBerlekampZassenhausMathlib` — 18 errors / 40 warnings,
  matching origin/main (the 40th warning is the pre-existing
  `unused variable hne` in `HexGramSchmidtMathlib/Int.lean`).
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
   | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` — empty.

Diff: 298 insertions / 265 deletions, single file.

## Current frontier

The A2 abstract-bound (`_of_bound`) propagation now covers both
`reassemblyExpansionComplete_exhaustive_*` consumers of the #4971
expansion-preconditions substrate. Combined with #4970 (the umbrella
aux `_of_bound`), the public umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
(`IntReductionMod.lean`) is now one cascade step from its own
`_of_bound` sibling: its body's two consumers of `hprecision` (the
inner `reassemblyExpansionComplete_exhaustive_of_ne_zero` call and the
forwarded `hprecision` argument to the aux) both have `_of_bound`
siblings in place.

## Next step

* File the umbrella's own `_of_bound` sibling once #4970 lands. Its body
  has exactly two consumers of `hprecision`: the inner
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` call (lifted here)
  and the forwarded `hprecision` argument to the aux (lifted by #4970).
* The `_of_primitive_pos_lc_core` umbrella variant remains unwired into
  any in-tree consumer; it becomes a drop-in once the #4880
  directive-blocked refactor of helpers 2/3/4 lands and the umbrella's
  monic-to-primitive variant is filed.

## Blockers

None.
