## Accomplished

- Claimed feature issue #4925 after confirming directive issues were blocked by open dependencies.
- Added
  `exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
- Rewired the existing
  `exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_of_primitive_pos_lc_core`
  theorem as a `defaultFactorCoeffBound core` wrapper.
- The wrapper discharges the abstract `hvalid` hypothesis by converting
  normalized-factor membership into
  `g ∣ recombinationCandidate d T ∣ target ∣ core`, then applying
  `defaultFactorCoeffBound_valid`.

## Current frontier

- #4925 deliverables are implemented in the primitive positive-leading-core
  cover-extraction layer.
- The downstream
  `coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_primitive_pos_lc_core`
  call site remains on the original signature.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic` reached past the edited
  declarations, then failed in existing large-file health areas with known
  heartbeat timeouts and dependent unknown constants before/after this region.
  No diagnostic pointed at the new declarations.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean | grep -E '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true`
  produced no output.
- Grep confirmed the new `_of_bound` declaration and the wrapper call site are
  present.

## Next step

- Publish the PR for #4925. Follow-on issue #4929 can then lift the
  `coverAtMin_*_of_primitive_pos_lc_core` layer to the abstract-bound shape.

## Blockers

- Full `lake build HexBerlekampZassenhausMathlib.Basic` remains blocked by
  pre-existing heartbeat/large-file failures documented in prior progress.
