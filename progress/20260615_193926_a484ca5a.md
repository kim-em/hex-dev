# Progress - 2026-06-15 (session a484ca5a, feature)

## Accomplished

Issue #7515: added the to-monic reverse-descent carrier surface in
`HexBerlekampZassenhausMathlib/Basic.lean`.

- Added `MonicDescent`, a data-bearing witness tying a lifted representation of
  the original factor to a mod-`p` subset representing the `RecoveredAtLift`
  monic correspondent.
- Added packing/projection helpers for original lifted representation,
  dilation equality, and monic divisibility.
- Added `MonicDescentHypotheses` as a `Prop`-shaped descent package mirroring
  the ordinary descent antecedents (`Irreducible` and `factor ∣ core`) while
  avoiding the false original-factor mod-`p` descent claim.
- Kept `HenselLiftDescentHypotheses` unchanged.

Second-opinion review flagged that the first draft made the hypotheses package
too strong by dropping the sibling antecedents and returning data directly; the
landed version incorporates that feedback.

## Current frontier

The proof-facing carrier is available for the to-monic slow-path constructors.
The large wrappers have not been rewired in this issue, by design.

## Next step

Use `MonicDescentHypotheses` in the follow-up to rebuild the
`toMonicPrimeData` correspondence without asserting
`RepresentsIntegerFactorModP primeData factor S` for the original non-monic
factor.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Basic.lean`
