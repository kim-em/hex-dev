# 20260603T052051Z work #6458

## Accomplished

- Claimed directive issue #6458.
- Added Schur row/column loop invariants proving that
  `scaledCoeffRowsSchur` leaves every above-diagonal entry at zero.
- Discharged `scaledCoeffs_upper` for the public Schur-backed
  `scaledCoeffs` API using that triangular invariant.
- Discharged the public `scaledCoeffs_diag_toNat` wrapper by reducing it
  to the packaged Schur diagonal in `gramDetVec` plus the remaining core
  `gramDetVec_eq_gramDet` theorem.

## Current frontier

- `HexGramSchmidt/Int.lean` now has 5 `grep -c sorry` hits: 4 actual
  theorem placeholders plus the existing explanatory comment line near
  `gramDetVecEntry_eq_leadingPrefix_bareiss`.
- The remaining actual placeholders are:
  `gramDetVecEntry_eq_leadingPrefix_bareiss`,
  `gramDetVec_eq_gramDet`,
  `scaledCoeffs_diag_eq_zero_or_eq_noPivotData_diag`,
  `scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss`, and
  `scaledCoeffs_eq_zero_of_singularStep_lt`.

## Next step

- Finish the core Schur/Bareiss bridge. The public diagonal wrappers now
  make clear that `scaledCoeffs_diag_toNat` only needs
  `gramDetVec_eq_gramDet`; the signed diagonal and singular lower-entry
  facts still need either Schur-vs-Bareiss entry equivalence or direct
  Schur singular-zero derivations.
- For the old Bareiss leading-prefix theorem, the existing singular
  helper needs a concrete
  `BareissGramInitialRegularStepQuotientProvider` package before it can
  be applied without `sorry`.

## Blockers

- No external blocker. The remaining work is proof work around exact
  division quotient provenance / Schur-Bareiss equivalence, larger than
  this partial checkpoint.

## Verification

- `lake build HexGramSchmidt.Int` passed after the changes.
- `/reflect` was requested by the worker flow, but no slash-command tool
  or `reflect` executable is available in this non-interactive session.
