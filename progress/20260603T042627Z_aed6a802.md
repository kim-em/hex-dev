# 20260603T042627Z work #6440

## Accomplished

- Claimed directive issue #6440 for the one-shot session.
- Added `Hex.GramSchmidt.Int.scaledCoeffRowsSchur`, a per-row
  Schur-complement scaled-coefficient kernel, and routed public
  `data`/`scaledCoeffs` through it.
- Kept the old Bareiss row helpers in place because existing invariant and
  Mathlib bridge proofs still depend on them.
- Rebuilt the public scaled-coefficient packaging lemmas as explicit proof
  obligations with `sorry` rather than weakening statements or adding axioms.
- Verified emitted Gram-Schmidt fixtures against the FLINT oracle:
  `gs_flint.py: checked 14 case(s), 0 failure(s)`.
- Re-ran comparator benchmarks and committed Schur exports:
  `reports/bench-results/hex-lll-random-bounded-schur.json`,
  `reports/bench-results/hex-lll-harsh-cubic-extended-schur.json`, and the
  supporting Lean/Isabelle harsh export.
- Regenerated both HexLLL comparator SVGs and updated the plot defaults.
- Updated `reports/hex-lll-performance.md` with the new ratios. Harsh-cubic
  `n = 65` is now Lean `621.32 ms` vs Isabelle `950.08 ms`, ratio `0.6540`.
- Advanced `libraries.yml` `HexLLL.done_through` from `3` to `4`.

## Current frontier

The executable Schur recurrence is in the public path and passes fixture
oracle checks plus `lake build`. The benchmark target is achieved, but the
proof port is incomplete: nine new `sorry` placeholders mark the diagonal,
upper-zero, singular-zero, and Mathlib Cramer/Bareiss bridge obligations for
the Schur-backed public `scaledCoeffs`.

## Next step

Port the old Bareiss-array invariant proofs to the Schur row recurrence, or
prove an equivalence between `scaledCoeffRowsSchur` and the existing
Bareiss/determinant characterization, then remove the new `sorry`
placeholders.

## Blockers

- Full completion of #6440 is blocked on the Schur proof port. The PR should be
  treated as partial until those proof obligations are discharged.
- `/reflect` is not available as a callable tool in this Codex session.
