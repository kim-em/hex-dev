# Progress: #6819 — lifted partition consumer migration triage

Feature session for #6819.

## Accomplished

- Confirmed the corrected carrier swap is present: `RepresentsIntegerFactorAtLift`
  is `Nonempty (RecoveredAtLift core d factor S)` in
  `HexBerlekampZassenhausMathlib/Basic.lean`.
- Repaired the compatibility theorem
  `representsIntegerFactorAtLift_mul_of_monic_core` so it no longer unfolds the
  public predicate as the old scaled-product congruence. It now unwraps the two
  recovered witnesses and constructs the product recovered witness directly.
- Kept `RecoveredAtLift.mul` as the arbitrary-core recovered-carrier
  multiplication helper and changed it from `theorem` to `def`, since the target
  is data rather than a proposition.
- Cleaned stale documentation on `InitialLiftedFactorSubsetPartitionEvidence`
  that still described `RepresentsIntegerFactorAtLift` as signed scaled-product
  equality.

Verified:
- `lake build HexBerlekampZassenhaus` succeeds.
- `python3 scripts/check_dag.py` succeeds.
- `git diff --check` succeeds.
- Added lines in `HexBerlekampZassenhausMathlib/Basic.lean` introduce no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

`lake build HexBerlekampZassenhausMathlib.Basic` remains red. The local errors
from this session's compatibility wrapper are gone; the first remaining error is
now `Basic.lean:2793`, where
`existsUnique_recoveringLiftedFactorSubset_of_henselSubsetCorrespondence_of_bound`
still passes `hS : RepresentsIntegerFactorAtLift core d factor S` to
`centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision_of_bound`, whose
argument is the old scaled-product congruence:

```lean
(scaledLiftedFactorProduct core d S).reduceModPow d.p d.k =
  factor.reduceModPow d.p d.k
```

Later errors are the same recovered-carrier migration surfaces already covered
by the decomposed follow-up issues: the explicit Hensel forward transport around
`Basic.lean:9148`, direct recovery callers around `9486`/`9497`/`9596`,
empty-subset callers around `14343`/`14465`, and recursive scaled-search callers
around `16082`/`16464`/`16564`.

## Current frontier

#6819 is not a single residual consumer patch. The package fields themselves now
reference the corrected public predicate, but several direct exact-recovery and
recursive-search callers still need to stop treating the predicate as the old
scaled congruence.

## Next step

Work the first exact-recovery package failure at `Basic.lean:2793`, preferably
under the already-created direct exact-recovery migration issues rather than
expanding #6819 further. The likely shape is to restate that recovering-subset
API around `liftedRecoveryCandidate` / `RecoveredAtLift.candidate_eq_of_bound`,
or to make its old scaled-product conclusion require an explicit old congruence
hypothesis instead of `RepresentsIntegerFactorAtLift`.

## Blockers

No external blocker. The remaining scope is proof/API migration work, not a
missing dependency.
