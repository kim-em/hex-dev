# Issue 8625 D2: certification re-key, phase 1 complete (full build green)

## Accomplished

- Dependency-cone survey (delegated agent, recorded in the #8625 thread
  plan): 30 witness-keyed theorems, all re-keyable from a semantic bundle,
  ZERO selection-walk dependencies, exactly one literal-Berlekamp-output
  proof to rewrite (SearchAssembly's product lemma).
- `HexBerlekampZassenhausMathlib/ModPFactorization.lean`: the semantic
  bundle (prime, isGoodPrime, fModP_eq, monic/ne_nil/nodup/coprime/
  irreducible/natDegree_pos factor facts, and the monicModularImage-form
  product congruence — general, no side conditions), producers
  `modPFactorization_of_choosePrimeData{,_of_monic}` and
  `modPFactorization_of_toMonicPrimeData`, derived
  `ModPFactorization.product_congr_target` (monic collapse).
- Re-keyed onto the bundle, full library building green:
  - SearchAssembly: the literal-output product lemma RE-PROVED from the
    bundle via modP_eq_of_congr + modP/liftToZ roundtrip (the one genuine
    re-proof); exists_factor_of_modPIndex, existsUnique_modPFactorSubset,
    modPSubsetPartitionHypotheses, core_ne_zero, modPFactor
    injective/monic, modPFactor_index_cover; the alternate
    henselSubsetLift surface threads primitivity+lc-pos (derived from
    monic at its one caller) instead of demanding a bundle.
  - ModPFactor: divisibility lemma re-keyed to atomic prime/good facts.
  - Descent: squarefree monicModPImage lemma re-keyed to atomic facts;
    modPFactorSubset_disjoint re-keyed to the bundle.
  - Transport:1594 and the IntReductionMod outer wrapper construct
    bundles at their call sites (still witness-keyed themselves).
- Also this session (earlier): sub-floor peel product-soundness stack
  (boundedExactQuotient? restructured with outermost re-multiply guard;
  subFloorPeelSize/subFloorPeel?/subFloorScan/reliftLadder product
  lemmas); fast-path condition fixed to node-monic; found
  `irreducible_toPolynomial_dilate_iff` (MonicCorrespondent) — dilation
  irreducibility transport already exists for the piece producer.

## Current frontier

Phase 2 of the re-key: ToMonicUniqueness (81, 182, 300, 511, 667, 717,
881 + 833/922 siblings), SubsetCoprimality (492, 556, 636),
ForwardHenselTransport (693), MonicCorrespondent (970, 1343),
Transport (1594), IntReductionMod roots (48, 740) — replace their
`hselected` hypotheses with `hval : ModPFactorization (toMonic core).monic
primeData` so the piece path can enter. All are pass-through or
extractor-swap sites per the survey; the bundle now supplies everything
they extract.

Phase 3 (the remaining real math): the piece producer
`modPFactorization_of_piecePrimeData` — dilation transport of the bundle
along `FpPoly.monicDilate` for `piecePrimeData?` outputs, using the exact
identity `M(gh) = D_{lc h}(M g) * D_{lc g}(M h)` and the peel product
witnesses; then the ladder spec + per-remainder irreducibility (task 4)
and the wiring + fixtures (task 5).

## Blockers

None.
