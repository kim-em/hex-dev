# Issue #4193

## Accomplished

- Claimed feature issue #4193 after directive candidates were rejected as blocked
  or needing replan.
- Added `UFDPartition.normalizedFactors_list_prod_eq_of_irreducible`, a generic
  helper identifying normalized factors of a list product of irreducibles with
  the multiset of normalized list entries.
- Added
  `UFDPartition.scalar_eq_and_normalizedFactors_eq_of_monic_irreducible_product_eq`
  for scalar-prefixed products of flattened monic irreducible integer
  polynomial factors.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.UFDPartition`
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The Mathlib/UFD layer now has a reusable flattened-factor uniqueness helper
  returning scalar equality plus normalized-factor multiset equality.
- The helper assumes nonzero scalar-prefixed monic factors; downstream
  `Hex.Factorization` uniqueness still needs to provide those monic/irreducible
  hypotheses from the executable factorization bridge.
- Existing repository warnings remain unchanged, including pre-existing `sorry`
  warnings in unrelated modules and existing BZ Mathlib placeholders.

## Next step

- Use the new UFD helper in the corrected `Hex.Factorization` uniqueness theorem
  once the factor entries are transported to monic irreducible flattened
  `Polynomial ℤ` factors.

## Blockers

- None for issue #4193.
