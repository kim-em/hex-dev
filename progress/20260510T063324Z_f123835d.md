# 20260510T063324Z - Issue #2934

## Accomplished

Claimed #2934 after confirming PR #2931 had merged and #2939 was closed.
Added a smoke-safe Swinnerton-Dyer `SD_3` benchmark registration in
`HexBerlekampZassenhaus/Bench.lean`:

- `runAdvSwinnertonDyerSD3ModularSplitChecksum` records the pinned `p = 71`
  modular split profile where SD3 has eight local linear factors.
- Adjacent comments explain why full `factor` / `factorFast` SD3 registrations
  remain out of the smoke gate for now: they exceed the current per-call budget.
- Added stable checksum helpers for optional modular factor-degree profiles.

Verification completed:

- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_phase4.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

Opened PR #2951. The first GitHub `build` check failed at
`scripts/check_phase4.py` because the commit message lacked the required
cost-model derivation for the new registration; amended the commit message and
force-pushed. After the update, `build-macos` passed and the Ubuntu `build` /
`conformance` checks were still pending in the GitHub queue.

## Current frontier

The SD3 adversarial shape is now visible to `hexbz_bench list` and covered by
`verify` through its modular split profile. Full SD3 `factor` / `factorFast`
timing remains scientific-only future work until the implementation or harness
budget can support it without breaking smoke verification.

## Next step

Let the pending GitHub Ubuntu `build` and `conformance` checks for PR #2951
finish.

## Blockers

None for this issue.
