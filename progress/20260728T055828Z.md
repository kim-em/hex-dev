# Semantic replay restack

## Accomplished

- Rebased the package-owned semantic replay protocol onto
  `agent/interval-package-replay` at `b70fad3a`.
- Preserved the hardened dropping-draft, payload-session, and replay-format
  APIs as the parent layer; the semantic protocol remains one child commit.
- Rebuilt `HexInterval.Experiment.SemanticReplay`,
  `HexInterval.SemanticReplayConformance`, and `HexIntervalExperiment`.
- Rechecked the semantic files for banned `native_decide`, `axiom`, and
  `sorry` uses.

## Current frontier

The generic proof-side protocol and two-function exact-dispatch canary compile
against the current package replay stack without adaptation.

## Next step

Implement the centered Mathlib soundness vertical described in
`progress/20260728T055554Z.md`.

## Blockers

None for the protocol layer.
