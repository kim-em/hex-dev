# Chebyshev/Legendre M1 review hardening

## Accomplished

- Addressed the independent review's high-priority coverage finding by pinning
  accepted M1 routes for Chebyshev U18 and Legendre P28 and exercising checked
  M1-certificate rejection followed by the exact M2 fallback.
- Made M1 routing explicit in `FactorTrace` and its JSON/baseline gate.
- Bounded every factorization passed to the exponential two-prime
  subset-degree check, kept one-factor inputs on M2, and delayed
  original-coordinate transport until the M1 branch is actually taken.
- Added a sound original-coordinate candidate-degree prefilter and a Mathlib
  proof that it cannot reject an exact divisor.
- Passed the full 9,650-target build, all 16 benchmark checks, the regenerated
  54-trace gate, and 106 FLINT oracle comparisons.

## Current frontier

The reviewed code and conformance changes are green. They alter the measured
implementation and trace schema, so the final Hex-only 392-row sweep and report
artifact still need to be refreshed; the already-current PARI, NTL, FLINT, and
Isabelle measurements do not.

## Next step

Commit the review hardening, run the pinned single-core Hex sweep, refresh the
performance report and figures from that artifact, then push for final review
and CI.

## Blockers

None.
