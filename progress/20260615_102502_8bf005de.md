# Progress — 2026-06-15 (session 8bf005de, one-shot /once #7362)

## Accomplished

Premise check on #7362 (producer for
`InitialLiftedFactorSubsetPartitionEvidence core (toMonicLiftData core B primeData)`).
Outcome: partially unblocked, but four of the seven fields have **no
non-circular producer**. Decomposed rather than bashing with sorries.

- Confirmed #7455 (existential transport) and #7469 (sign bridge +
  mod-p disjointness) are merged; #7469 closes the exact
  `normalizeFactorSign f = f` gap the previous claimant reported.
- Classified the 7 fields:
  - Assemblable now: `liftedRecoveryCandidate_eq` (via
    `RecoveredAtLift.candidate_eq_of_monic_dvd` + #7469
    `normalizeFactorSign_eq_of_representsAtLift`), `cover` (via #7455
    transport + `exists_factor_of_modPIndex`), `unique_up_to_associated`
    (via `existsUnique_recoveringLiftedFactorSubset_*` + Associated-invariance).
  - Genuine analytic leaves: `pairwise_disjoint`,
    `support_subset_of_dvd_recombinationCandidate`,
    `support_subset_of_dvd_liftedRecoveryCandidate`,
    `support_subset_of_dvd_scaledRecombinationCandidate`.
- Source-verified circularity: every candidate→subset lemma
  (`representingSubset_subset_of_dvd_*` ~5227, `coverAtMin_*` ~12763/~16002,
  `mem_T_iff_*` ~12103) takes a `LiftedFactorSubsetPartition` and delegates to
  its `support_subset_*` field. All references to
  `InitialLiftedFactorSubsetPartitionEvidence` (Basic.lean + IntReductionMod.lean)
  are binders/projections — no producer exists.
- Established `toMonicLiftData core B primeData =
  henselLiftData (toMonic core).monic …` (HexBerlekampZassenhaus/Basic.lean:6811),
  so the coprimality infra (`henselLiftData_liftedSubset_complement_isCoprime_mod_p`
  ~8703, `liftedFactorProduct_eq_mul_sdiff_of_subset` ~8779, `RecoveredAtLift.congr`
  ~2294) supplies the ingredients for the missing core — feasible, not unsound.

## Actions taken

- Created **#7474** (lift-stage subset-uniqueness from coprime lifted-factor
  products) with precise target shapes + proof route.
- `coordination add-dep 7362 7474` (7362 now `blocked`).
- Posted full diagnosis comment on #7362.
- `coordination skip 7362` → `replan`.

## Current frontier

#7474 is the residual analytic core. Once it lands, #7362 becomes a clean
7-field assembly (3 fields already mapped to existing lemmas above; 4 from
#7474; `liftedRecoveryCandidate_eq` from #7469).

## Next step

A worker claims #7474: prove the four non-circular lemmas, then #7362
assembles. No code landed this session (premise-check + decomposition only).

## Blockers

#7362 blocked on #7474 (open).
