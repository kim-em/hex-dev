# Progress: #6772 decomposition / #6817 recovered candidate surface

## Accomplished

- Checked the directive queue: #2564 is open but blocked, so fresh feature work was valid.
- Claimed #6772, confirmed it spans a definition change, monic transport rewrite, and broad consumer migration across `HexBerlekampZassenhausMathlib/Basic.lean`.
- Decomposed #6772 into:
  - #6817: additive recovered lifted-factor candidate surface.
  - #6818: replace `RepresentsIntegerFactorAtLift` with the recovered-candidate representation.
  - #6819: migrate lifted partition/substrate consumers.
- Claimed #6817 and added `liftedRecoveryCandidate`, matching the slow-path dilation recovery shape:
  `normalizeFactorSign (primitivePart (dilate (leadingCoeff core) (centeredLiftPoly (liftedFactorProduct d S) (d.p ^ d.k))))`.
- Proved monic-collapse lemmas:
  - `liftedRecoveryCandidate.eq_productCandidate_of_lc_one`
  - `liftedRecoveryCandidate.eq_recombinationCandidate_of_lc_one`

## Current frontier

#6817 is implemented as an additive surface only. The old
`RepresentsIntegerFactorAtLift` predicate is intentionally unchanged here so
the replacement can happen in #6818 against a stable candidate definition.

Verification:

- `lake build HexBerlekampZassenhaus` succeeds.
- `git diff --check` succeeds.
- Added lines contain no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
- `lake build HexBerlekampZassenhausMathlib` still fails at the pre-existing
  scale-vs-dilate recovery-chain site in `HexBerlekampZassenhausMathlib/Basic.lean`
  around line 16031, after the new definitions elaborate. This is the known
  #6804/#6772 recovery-chain blocker, not introduced by this additive patch.

## Next step

Publish #6817 as a scoped PR. Then #6818 should redefine
`RepresentsIntegerFactorAtLift` around `liftedRecoveryCandidate` and rework the
monic transport/recovery lemmas.

## Blockers

Full `HexBerlekampZassenhausMathlib` verification is blocked by the existing
post-executable-fix mismatch: downstream proofs still feed scale-shaped prefix
evidence to the now-dilation executable recombination lemma.
