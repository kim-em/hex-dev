# 2026-05-11 15:20 UTC — work #2674

## Accomplished

- Claimed issue #2674 after all human-oversight directives were blocked or marked for replan.
- Replaced the `sorry` in `HexGramSchmidt/Update.lean` for `scaledCoeffs_sizeReduce_pivot`.
- Used `scaledCoeffMatrix_rowAdd_pivot_det` at `c = -r`, `scaledCoeffs_eq_scaledCoeffMatrix_det`, and `leadingGramMatrixInt_det_eq_gramDet_int`.
- Fast-forwarded the branch to current `origin/main`.
- Verified:
  - `lake build HexGramSchmidt.Update`
  - `lake build HexGramSchmidt`
  - `lake build HexLLL`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Update.lean HexGramSchmidt/Int.lean`

## Current frontier

- The size-reduction pivot theorem is proved and no longer contributes a `sorry` warning.
- The marker scan still reports pre-existing `sorry`s later in `HexGramSchmidt/Update.lean` and in `HexGramSchmidt/Int.lean`; none were introduced by this session.

## Next step

- Continue with the remaining adjacent-swap and Gram-Schmidt update theorem holes tracked by separate issues.

## Blockers

- None for #2674.
