# Accomplished

- Restacked package-owned replay formats over the repaired matcher payload
  session.
- Preserved the real `sin (-x)` recognizer, dynamic contractor, package scope
  veto, exact matcher batch, and replay-format ownership through the merge.
- Adapted the matcher conformance driver to the sealed `Invocation.plan`
  boundary provided by this layer.
- Clarified that direct-engine callers can validate start-time scope tables,
  while the current checked session starts empty and exercises dynamically
  proposed scopes.
- Built the full 31-job interval, matcher, scope, policy, registry, dyadic,
  payload, and replay target set. DAG, diff-hygiene, and banned-proof scans
  pass.

# Current frontier

The repaired matcher, checked payload session, dynamic-scope authority, and
handler-owned replay formats now compose. Semantic theorem decoding remains
the next proof boundary.

# Next step

Restack the proof-producing policy session and semantic replay layers over
this exact head, then retarget and merge the stack from the bottom after each
reviewed CI head is green.

# Blockers

None.
