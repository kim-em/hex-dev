## Accomplished

Claimed #4475 and reviewed `HexPolyFp/Frobenius.lean`'s Frobenius power-mod
surface against the Phase 6 API polish bar.

Added missing docstrings for the public bridge theorems connecting the
kernel-reducible linear evaluators to the production Frobenius routines, and for
`frobeniusXPowMod_zero`.

Added the public theorem `FpPoly.frobeniusXMod_eq_frobeniusXPowMod_one`, then
replaced the downstream private duplicate in `HexBerlekamp/DistinctDegree.lean`.
The simp audit found that the linear-to-production bridge lemmas should remain
explicit rather than `@[simp]`: global simp rewrites break conformance proofs
that intentionally unfold the linear evaluator to kernel-reduced closed terms.

Committed the source changes as `81e89b43 doc: polish Frobenius power API`.

## Current frontier

- `lake build HexPolyFp.Frobenius` succeeds.
- `lake build HexPolyFp` succeeds.
- `lake build HexBerlekamp` succeeds.
- `python3 scripts/check_dag.py` succeeds.
- `python3 scripts/status.py HexPolyFp` reports Phase 6, `done_through: 5`,
  `ready: yes`.
- `git diff --check` succeeds.
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b' HexPolyFp/Frobenius.lean`
  returns no hits.

## Next step

Open the PR for #4475. Remaining HexPolyFp Phase 6 polish should continue in
separate narrow review issues for the other files listed out of scope in #4475.

## Blockers

None for #4475. The worktree still has a pre-existing unrelated modification to
`.claude/CLAUDE.md`; this session did not touch or commit it.
