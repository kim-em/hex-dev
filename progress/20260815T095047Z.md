# BKLNW smaller power ladder

## Accomplished

- Verified the ten remaining pinned PNT+ declarations
  `cert_pow29_upper`, `cert_pow37_upper`, `cert_pow44_upper`,
  `cert_pow51_upper`, `cert_pow58_upper`, `cert_pow63_upper`,
  `cert_pow145_upper`, `cert_pow217_upper`, `cert_pow289_upper`, and
  `cert_pow361_upper` at PNT+ commit
  `21998bb6196b56789f72a52656a781a75e134eb0`.
- Added one parameterized Mathlib-free ladder certificate and runtime package.
  It authenticates the exact source limit/endpoint pair, the `4..20` explicit
  band, the `k ≥ 21` tail and cardinality, the eight-decimal table scale, and
  the complete natural-number rational inequality.
- Proved all 18 package-owned base bounds from the checked 20-digit `log 2`
  series enclosure and an exponential Taylor remainder; proved one reusable
  finite-sum theorem and exact source-shaped wrappers for all ten declarations.
- Added generic semantic replay with an explicit certificate/model equality
  guard, successful package runs and replay for all ten rows, decoded mutation
  rejection, cross-row rejection, a mathematically false endpoint mutation,
  guarded axiom reports, and source-pinned inventory/SPEC evidence.
- Declared the bounded runtime cost honestly as `18M` and raised the payload
  atom bound only to the selected source numerator; this was load-bearing for
  the `M = 289` row.
- Built `HexInterval`, `HexIntervalMathlib`, and
  `HexIntervalMathlib.PntBKLNWPowConformance`; verified the source inventory,
  its 26 unit tests, the published trust surface, factor-sweep freshness, ban
  grep, and `git diff --check`.

## Current frontier

The complete eleven-member PNT+ `pow*_upper` family has package-owned
ordinary-kernel replacements when combined with the existing `pow433` canary.
The imported `LeanCert.CertifiedBounds.BKLNW` interface remains classified
pending because its twenty exponential lower/upper declarations are not part
of this slice.

## Next step

Rebase this local feature onto the eventual landing base after current public
arithmetic work, preserving the merged public-power documentation and guards,
then refresh any exact Lake freshness exemption only if the final rebase
changes `lakefile.lean`.

## Blockers

None in the implemented power-ladder premise. No remote branch or PR was
created.
