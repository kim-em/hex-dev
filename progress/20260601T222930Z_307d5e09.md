# 307d5e09 - work #6021

## Accomplished

- Claimed feature issue #6021.
- Added the final bridge-layer universal Plucker theorem
  `HexMatrixMathlib.det_plucker_three_term` in
  `HexMatrixMathlib/Determinant/Core.lean`.
- Reintroduced the three equal-row basis-vector cases locally in the bridge
  layer and combined them with the existing arbitrary distinct-row theorem
  `det_plucker_three_term_basisVec_of_ne`.
- Added private bridge-local fold helpers needed to fold the basis-vector
  coefficient identity back to an arbitrary vector.
- Verified with `lake build HexMatrixMathlib`, `git diff --check`,
  `python3 scripts/check_dag.py`, and a forbidden-token grep over the touched
  Lean diff.

## Current frontier

#6021 is complete. The Mathlib bridge now exposes the universal three-term
Plucker identity for an arbitrary row vector and three ordered basis rows.

## Next step

Create the PR for #6021.

## Blockers

None.
