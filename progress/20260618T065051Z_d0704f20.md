# Progress — 2026-06-18 (session d0704f20, /work → /feature #7881)

## Accomplished

Claimed #7881 and added the packed encoder-side bit bridge in
`HexGF2Mathlib/Basic.lean`.

- Added private arithmetic helpers for `Nat.testBit` under high aligned addends
  and low aligned prefixes.
- Proved `wordsToNatAux_testBit_getD`, showing that the bit at global index
  `64 * (i + wordIdx) + bitIdx` agrees with the corresponding stored `UInt64`
  word bit when `bitIdx < 64`.
- Exposed public theorem `toNat_testBit_eq_coeff`, stating
  `(toNat p).testBit j = p.coeff j` for packed `GF2Poly`.
- Left the existing packed↔`FpPoly 2` ring-equivalence sorries and Phase 5b
  finite-index sorries untouched.

## Current frontier

#7881 is complete. The new `toNat_testBit_eq_coeff` theorem is available for
the blocked Phase 5b finite-index bound and inverse issue.

## Next step

Proceed to #7882 / Phase 5b: prove the `toNat` degree bound and
`ofNatBelowDegree` round-trip theorems using the decoder helpers and the new
encoder bit equivalence.

## Blockers

None for #7881.

Verification:

- `lake build HexGF2Mathlib.Basic` succeeded, with only pre-existing requested
  sorries.
- `lake build HexGF2Mathlib` succeeded, with pre-existing sorries/linter
  warnings outside this issue.
- `python3 scripts/status.py HexGF2Mathlib` reports Phase 5, ready.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` clean.
- Added-line forbidden-token scan over `HexGF2Mathlib/Basic.lean` found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

Note: the worktree still has a pre-existing unrelated `.claude/CLAUDE.md`
modification; it was not touched or staged.
