# Progress: HexMatrix Bareiss data projection docs (#6290)

## Accomplished

Clarified `HexMatrix/Bareiss.lean`'s module comment so the Mathlib-free layer
is described as exposing executable Bareiss data and state for later bridge
proofs, not as exposing a theorem equating Bareiss with the generic Leibniz
determinant.

Added field-level documentation for `BareissData` and `BareissState`,
covering the terminal matrix encoding, row-swap parity sign convention,
`some k` singular-step meaning, previous pivot, and current step.

Added `BareissData.det_eq_zero_of_singularStep` as a small public projection
lemma characterizing the singular-step determinant encoding.

Verification:

- `lake build HexMatrix.Bareiss`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over `HexMatrix/Bareiss.lean` found no added
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The focused Bareiss module comment and public data/projection documentation
follow-up from the Phase 6 review is complete.

## Next step

Continue the remaining Phase 6 follow-up issues from the determinant/Bareiss
API review, especially determinant proof-infrastructure visibility
classification and any residual conformance/API polishing issues not already
covered by open PRs.

## Blockers

None for this session. The worktree still contains an unrelated pre-existing
local modification to `.claude/CLAUDE.md`, which was not touched or staged.
