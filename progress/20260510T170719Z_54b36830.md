## Accomplished

Claimed issue #3059 and added public matrix-level identity and associativity
lemmas in `HexMatrix/Basic.lean`:

- `Matrix.one_mul`
- `Matrix.mul_one`
- `Matrix.mul_assoc`

The proofs reuse the existing fold helpers and `mul_assoc_vec`; no executable
matrix definitions were changed.

Verified:

- `lake build HexMatrix.Basic`
- `lake build HexMatrix.RowEchelon`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Basic.lean`
  produced no matches.

## Current frontier

Issue #3059 is implemented and verified locally. Downstream blocked work can
use the new public matrix multiplication laws for row-operation preservation.

## Next step

Commit the `HexMatrix/Basic.lean` and progress-note changes, then create the PR
for issue #3059.

## Blockers

None.
