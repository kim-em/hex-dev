# Session 9001e025 — work, issue #4244

## Accomplished

- Claimed #4244 and stacked this branch on open dependency PR #4243
  (`Matrix.sub_identity_mulVec`) because #4243 had not merged yet.
- Added the Berlekamp-local fixed-space bridge in `HexBerlekamp/Basic.lean`:
  `isFixedSpaceKernelVector_iff_berlekampMatrix_mulVec_eq`.
- Aligned `fixedSpaceMatrix` and the fixed-space kernel APIs with the
  `Lean.Grind.Ring`/`Lean.Grind.Field` operation surface used by
  `HexMatrix.nullspace`, so the #4243 `Q - I` helper applies without a
  `ZMod64` instance mismatch.
- Verified:
  - `lake build HexBerlekamp.Basic`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - no new `axiom`, `native_decide`, `TODO`, `FIXME`, or diff-level `sorry`
    in the touched Lean file.

## Current frontier

- Deliverable 1 is partially discharged: the executable kernel predicate now
  has a compiled iff to the Berlekamp fixed-space equation on the same
  field-derived matrix-vector surface as the nullspace API.
- The remaining #4244 work is still open: column/action infrastructure for
  `berlekampMatrix`, the coefficient-vector multiplication identity, and the
  final `IsFixedSpaceKernelPolynomial ↔ f ∣ linearPow w p - w` bridge.

## Next step

- Continue #4244 from the new wrapper by proving the closed-form column/action
  lemmas for `berlekampMatrix`, factoring through
  `FpPoly.compose_w_linearPow_X`.

## Blockers

- #4243 was still open when this work began, so this branch is stacked on its
  `Matrix.sub_identity_mulVec` commit. If #4243 changes before merge, rebase
  this partial branch onto the merged helper.
