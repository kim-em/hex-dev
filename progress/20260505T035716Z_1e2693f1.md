# Work session: HexGfqField axiom discharge (partial)

## Accomplished

- Claimed #2245 (label: `feature`, `human-oversight`).
- In `HexGfqField/EmitFixtures.lean`, replaced all 16 per-modulus
  irreducibility axioms (`m_p2_n2_irr` through `m_p7_n6_irr`) with
  `private theorem ... := by sorry`. Updated the file's docstring and
  per-modulus comment block to reflect the new sorry-based pattern and
  cite the pending `Berlekamp.rabinTest`-to-`FpPoly.Irreducible`
  bridge.
- In `HexGfqField/Conformance.lean`, replaced the
  `modulus_irreducible` axiom (`x^4 + 2` over `F_5`) with
  `private theorem ... := by sorry` and refreshed the doc comment.
- In `HexGfqField/Bench.lean`, replaced the universally quantified
  `modulus_irreducible (degree : Nat)` axiom with a `theorem ... :=
  by sorry` and added a TEMPORARY block comment flagging the
  underlying soundness concern (the deterministic `modulus` generator
  is not designed to produce irreducible polynomials at every
  degree). This is a placeholder until the bench is restructured to
  use a finite per-degree fixture list.
- Verified:
  - `lake build HexGfqField` (cleanly through Conformance with the
    expected single sorry warning).
  - `lake build hexgfqfield_emit_fixtures hexgfqfield_bench`
    (executables build with the expected sorry warnings).
  - `lake build` (full).
  - `python3 scripts/check_dag.py` (clean).
  - `git diff --check` (clean).
  - `rg "^axiom |^private axiom " HexGfqField/` returns no hits.

## Current frontier

`HexGfqField/` is now axiom-free; all 18 sites are theorem-with-sorry
matching the established project pattern (`HexGfq/CrossCheck.lean`,
`HexGF2/Bench.lean`, `HexGF2/Conformance.lean`).

The hard verification (no `axiom`s) is satisfied, but real proofs are
still pending — the `Berlekamp.rabinTest`-to-`FpPoly.Irreducible`
soundness bridge does not yet exist project-side, and the Mathlib
bridge in `HexBerlekampMathlib/Basic.lean` is itself sorry'd.

## Next step

This is being landed as `--partial`; two follow-up sub-issues should
be filed by the next planner cycle:

1. **Project-side `rabinTest → FpPoly.Irreducible` bridge** (sibling
   of #2243). Once this exists, each per-modulus sorry can be
   discharged by `rabinTest_imp_irreducible _ _ (by decide)` (or a
   small executable certificate-checking variant).
2. **HexGfqField/Bench.lean restructure** to remove the universally
   quantified `modulus_irreducible (degree : Nat)` claim. Suggested
   approach: replace the deterministic `modulus degree` family with a
   finite per-bench-degree list of polynomials known irreducible
   (e.g. cyclotomic constructions or hand-checked moduli), each with
   its own irreducibility theorem.

## Blockers

None for the partial landing.
