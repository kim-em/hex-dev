# 20260517T200200Z — work session 7c56bf68 (#4334 skip, 26th attempt)

## Accomplished

**#4334 (HexLLL Phase 4 inconclusive scientific verdicts)** — claimed and
skipped with replan. The only unclaimed issue (selected via `/work` then
matched to `/feature`).

Repeats the documented measurement-environment blocker on `carica`:

- Load avg 20.94 / 16.03 / 13.96 on 24 CPUs at 2026-05-17T19:58Z, falling
  to 13.20 / 15.15 / 14.00 by 20:01Z — 1-min still ≈55–87 % of capacity.
- Sibling Lean worker at 458.3 % CPU compiling
  `HexBerlekampZassenhaus/Basic.lean` from worktree `eb8a0284`.
- A second Lean worker at 98.5 % CPU in a different project
  (Etingof-RepresentationTheory Chapter6/InfiniteTypeConstructions.lean).
- Standard system daemons holding usual shares: `replayd` 88.3 %,
  Pylance 99.8 %, `coreaudiod` 57.0 %, a `find` at 54.3 %, VS Code
  renderer 42.6 %, `mediaremoted` 31.4 %.

Smoke verifier `lake exe hexlll_bench verify` still passes — all 52 fixed
benchmarks ok. No scientific probe attempted, matching the standing
guidance in the issue body and the 25 prior skip cycles: a probe under
this load would reproduce the documented ~1.2–1.3 s per-spawn floor with
zero verdict-eligible rows, providing no new information.

## Current frontier

`HexLLL.done_through` remains at 3 pending scientific verdicts for the
five parametric registrations listed in the issue body. The
infrastructure obstacle is unchanged: 26 consecutive worker attempts
have run into the same concurrent-Lean-worker contention pattern, with
the host typically holding 4–8× the spawn-floor-acceptable load.

## Next step

Wait for either (a) `carica` to be genuinely idle (no sibling Lean
workers, load < 2), or (b) the release-candidate benchmarking workflow
on dedicated hardware per `SPEC/benchmarking.md` §"Scientific timing
runs". Continued worker re-dispatch under current conditions will keep
producing identical skip cycles until one of those preconditions holds.

## Blockers

Measurement environment — unchanged across 26 attempts.
