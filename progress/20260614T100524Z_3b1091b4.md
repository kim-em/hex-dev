# Progress — 2026-06-14 (feature session, #7122)

## Accomplished

**#7122 (transport factorFast schedule recovery henselLiftData → toMonicLiftData)
— completed.** The premise-check caveat (skill flags the `toMonicLiftData`
modP→lift transport as one of the "big three with no producer") did **not**
bite: no bridge lemma between the two `LiftData` defs is needed, because the
whole recovery chain — `ForwardRecoveryInputs f d` (Recovery.lean:606) and
`bhksRecover_eq_some_of_forwardInputs` (Recovery.lean:2078) — is **generic in
`d : Hex.LiftData`**. The fix is to *state* the Mathlib-side cap/schedule
theorems over the lift data the executable `factorFast` genuinely uses
(`toMonicLiftData`, per the already-migrated executable consumer
`factorFast_ne_none_of_core_recovery_on_schedule`, Basic.lean:7708/7722),
rather than the stale `henselLiftData`.

Concretely: `Hex.henselLiftData` appears concretely in Recovery.lean at exactly
5 sites (3 in `_on_schedule`, 1 in `_at_cap` hinputs, 1 in the
`factorFastCapLiftData` abbrev); all 871 other references route through the
`factorFastCapLiftData` abbrev, which is never unfolded, and the
`ofCapSeparation*` builders take `hliftedFactor_monic` as a hypothesis rather
than deriving it from the lift-data def. So the swap is contained:

1. 5-site token swap `Hex.henselLiftData` → `Hex.ZPoly.toMonicLiftData`
   (same `(core, B, primeData)` arg shape).
2. 3 stale docstrings updated (lift shape is now the monic-core lift).
3. `set_option maxHeartbeats 1000000 in` on the 3 cap structures
   (`CanonicalRecoveryInputs`, `CanonicalRecoveryTailInputs`,
   `FactorFastCapSeparationInputs`): `toMonicLiftData` unfolds to
   `henselLiftData (toMonic core).monic …`, a larger term whose
   `projectedRowsOfLiftData … .factorCount` field-type elaboration tips past
   the default 200k budget. 400k was insufficient, 1M suffices; consumers of
   the structures elaborate fine at the default budget.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery`: my target errors
  (2131/2167/2180) and their cascade are gone. The only remaining Recovery.lean
  errors are the **#7121 scale→dilate group (1139/1235/1844)**, explicitly
  scoped out by this issue ("may remain until that lands").
- `python3 scripts/check_dag.py` exits 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`/em-dash on added lines.
- `factorFastCapLiftData` is `private`; no external callers of the migrated
  theorems exist (only TerminationBound.lean doc comments mention henselLiftData).

## Current frontier

Recovery.lean's schedule/cap recovery path is now faithfully stated over
`toMonicLiftData`. Remaining Recovery.lean red is entirely #7121's scale→dilate
remodel.

## Next step

#7121 (scale→dilate, errors 1139/1235/1844) is the sibling group that keeps
Recovery.lean from full green. Independent of this work.

## Blockers

None.
