# 2026-06-02T00:53Z — feature session 6cdb4d0b

Claimed and executed #6189 (HexArith Phase 6: expose `Prime.dvd_mul`
as iff to match Mathlib shape).

## Accomplished

- Restated `Hex.Nat.Prime.dvd_mul` in `HexArith/Nat/Prime.lean` from
  forward-only to iff form: `p ∣ a * b ↔ p ∣ a ∨ p ∣ b`. Reverse
  direction proved via `Nat.dvd_trans` against
  `Nat.dvd_mul_right` / `Nat.dvd_mul_left`.
- Updated the two `rcases` call sites to use `(... ).mp`:
  - `HexArith/Nat/Prime.lean:169` (internal use in
    `choose_prime_dvd_from_mul_identity`).
  - `HexModArith/Prime.lean:46` (in
    `eq_zero_or_eq_zero_of_mul_eq_zero`).
- Updated `SPEC/Libraries/hex-arith.md` "Euclid's lemma" signature
  block to the iff form.

## Current frontier

`lake build HexArith.Nat.Prime`, `lake build HexArith`,
`lake build HexModArith.Prime`, `lake build HexModArith` all clean.
`python3 scripts/check_dag.py` exits 0. Diff contains no forbidden
tokens.

## Next step

Push branch, open PR.

## Blockers

None.
