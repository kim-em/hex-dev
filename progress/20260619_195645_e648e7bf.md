# 2026-06-19 19:56 UTC — feature session (e648e7bf)

Session type: `/work` → `/feature` (issue #8092).

## Accomplished

Phase 6 grind-automation sweep for `HexGF2Mathlib` (#8092). Promoted every
equation-shaped public `@[simp]` lemma in `HexGF2Mathlib/Basic.lean` (9) and
`HexGF2Mathlib/Field.lean` (10) to `@[simp, grind =]`:

- `Basic.lean`: `toFpPoly_zero`, `ofFpPoly_zero`, `toFpPoly_one`,
  `ofFpPoly_toFpPoly`, `toFpPoly_ofFpPoly`, `toFpPoly_add`, `toFpPoly_mul`,
  `equiv_apply`, `equiv_symm_apply`.
- `Field.lean`: `ofGeneric_toGeneric`, `toGeneric_ofGeneric`, `toGeneric_add`,
  `toGeneric_mul` (both the `GF2n` and `GF2nPoly` sections — names repeat), plus
  `reducedPackedRepIndex_ofIndex`, `reducedPackedRepOfIndex_index`.

Every promoted lemma is a literal `lhs = rhs` with a plain def-headed LHS
(`toFpPoly`/`ofFpPoly`/`toGeneric`/`ofGeneric`/`reducedPackedRep*`) or a CoeFun
application (`equiv p`, `RingEquiv.symm equiv p`) — all accepted as `grind =`
patterns with no `invalid pattern` rejection or whnf PANIC. No `@[simp]`-only
lemmas remain in either file: every public `@[simp]` here was equation-shaped,
so there are no exclusions to note.

Annotation-only: 19 attribute lines changed, no proof body / import / other
file touched. The build warnings at `Basic.lean:210/276/355/369/379` are
pre-existing (proof-body linter hints), untouched by this diff.

## Verification

- `lake build HexGF2Mathlib` — green, no elaboration blowup.
- `lake build HexGFqMathlib` (downstream consumer) — green, 0 errors,
  `HexGFqMathlib.Basic` 8.5s / `GF2q` 2.3s (no grind-loop slowdown).
- `scripts/check_dag.py` — exit 0.
- No new `axiom`/`sorry`/`native_decide`.

## Current frontier

#8092 is complete. The other claimable Phase 6 grind issues #8090
(HexPolyZMathlib RobinsonForm/Mignotte) and #8094 (HexGFqMathlib/Basic) remain
for other workers; #8090 was already claimed when this session ran.

## Directive #2564 note (investigated, not actioned)

Spent the early part of the session on the highest-priority directive #2564
(BHKS van Hoeij CLD rewrite headline theorem). Findings, for the record:

- #2564 is **unclaimable**: it is in a circular `depends-on` deadlock with
  #4818 (each lists the other), and `coordination claim 2564` fails "blocked by
  dependencies". There is no `remove-dep` command; blocking is computed from
  `depends-on:` lines in the body.
- The whole headline theorem (`factor_headline`, FactorSoundness.lean) is
  already proved **modulo one sorry**: `factor_irreducible_of_nonUnit`
  (FactorSoundness.lean:20) and the capstone `Hex.ZPoly.isIrreducible_iff`
  (Basic.lean:235), which consumes it.
- The remaining gap is genuine, not assembly: the slow paths have unconditional
  raw-irreducibility producers (`slowModularRaw_irreducible_of_fast_none`,
  `factorSlowTrialFactorsWithBound_factor_irreducible_of_fast_none`), but the
  **fast path** only has irreducibility *under* undischarged
  `ForwardRecoveryInputs`/`CutProjectionHypotheses`/recovered-lift certificates.
  No producer turns `factorFast = some` into those certificates.
- That producer is actively tracked: #8068 ("close default factor irreducibility
  from raw branch proofs", blocked on #6779) needs it; #6779 is demoted off the
  critical path; and the executable finish #8062 (interactive-Claude branch
  `fix/factorfast-bpoly-precision-guard`) must land first because it changes
  `factorFastFactorsWithBound`'s precision schedule and guard. So the directive
  is correctly blocked and is not a single-session task.

## Next step

Pick up #8094 (HexGFqMathlib/Basic Phase 6 grind) or another unclaimed feature.

## Blockers

None for #8092. Directive #2564 blocked as documented above.
