# Progress 20260602T111823Z (session f9d0cbc6)

## Accomplished

Claimed issue #6316 and added proof-only normalized-tail bridge lemmas in
`HexPolyFp/SquareFree.lean`.

- Added `squareFreeAuxRevContribution_pthRoot_normalized_tail_bridge`, relating
  the raw derivative-zero recursive contribution on `pthRoot tail` to the
  normalized-tail recursive contribution with the required leading-unit factor.
- Added `derivative_active_raw_tail_weighted_product_bridge_via_normalized`,
  composing that raw-to-normalized bridge with the existing normalized weighted
  product bridge so the later #6286 refactor has a direct product-site lemma.
- Left all executable definitions unchanged.

## Current frontier

Verification completed:

- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over `HexPolyFp/SquareFree.lean`

No added `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` tokens.

## Next step

The downstream #6286 consumer refactor can replace the raw derivative-active
recursive IH at the weighted-product site with the normalized recursive IH,
using the new private composed bridge.

## Blockers

None.
