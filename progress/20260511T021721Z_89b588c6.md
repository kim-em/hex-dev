# 2026-05-11 02:17 UTC — feature session (89b588c6)

## Accomplished

- Claimed #3212 and added the two remaining BHKS wrapper-lifting
  theorems in `HexBerlekampZassenhaus/Basic.lean` (Mathlib-free).
  These close the wrapper-plumbing chain
  `factorFastFactorsWithBound = some → factorFastWithBound = some →
  factorFast = some` so the proof-layer recovery assembly tracked for
  #2567 can chain through in one cap-level step.
- Inserted four theorems right after the `factorFast` definition and
  its `#guard` checks:
  - private `factorFastWithBound_eq_some_of_factors_some` — by
    `unfold factorFastWithBound; rw [h]; rfl` (the wrapper is just
    `(factorFastFactorsWithBound f B).map (factorizationOfFactors f)`).
  - private `factorFastWithBound_ne_none_of_factors_ne_none` —
    `match hex : factorFastFactorsWithBound f B`, dispatching the
    `some` branch through the equality theorem above and using
    `Option.some_ne_none`, and rejecting the `none` branch via the
    incoming hypothesis.
  - public `factorFast_eq_some_of_factorFastWithBound_cap_eq_some`
    and public `factorFast_ne_none_of_factorFastWithBound_cap_ne_none`
    — both immediate from `factorFast f := factorFastWithBound f
    (factorFastPrecisionCap f)`, proved by `:= h` term-mode.

## Verification

- `lake build HexBerlekampZassenhaus` — clean; only the pre-existing
  sorries (lines 2031, 2694, 2708 → now 2033, 2696, 2711) remain.
- `lake build` — clean (Build completed successfully, 15 jobs).
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry'
  HexBerlekampZassenhaus/Basic.lean` shows only the three pre-existing
  sorries on lines 2033, 2696, 2711.

## Current frontier

- `factorFastWithBound` remains `private`; that's intentional — the
  next consumer of these wrapper theorems is the cap-level
  termination assembly inside `HexBerlekampZassenhaus/Basic.lean`
  for #2567 / Mignotte side (#3201), not an external module.
- The chain
  `recovery_at_cap → factorFastCoreWithBound = some →
   factorFastFactorsWithBound = some → factorFastWithBound = some →
   factorFast ≠ none`
  is now fully wired except for the recovery-at-cap hypothesis,
  which is tracked outside this issue.

## Next step

- A separate session can pick up the cap-level recovery hypothesis
  proof (the `factorFastCoreWithBound = some` premise) tracked by
  the #2567 chain (Mignotte side via merged #3201, recovery side via
  parent chain). Once available, that session can chain these four
  wrappers in one term to discharge `factorFast f ≠ none`.

## Blockers

- None for this slice.
