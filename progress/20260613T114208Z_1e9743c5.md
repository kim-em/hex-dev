# Progress: #6791 — BHKS true-indicator norm bound

## Accomplished

Claimed #6791 and added the first reusable BHKS true-indicator norm API in
`HexBerlekampZassenhausMathlib/Lattice.lean`.

- Added `indicatorVector_sq_apply_le_one`.
- Added `indicatorVector_sq_sum_le_factorCount`, bounding the squared real
  norm of a `0/1` support indicator by the ambient lifted-factor count.
- Added `indicatorVector_sq_sum_le_bhksCutRadiusSq4`, showing the executable
  BHKS cut-radius expression dominates that indicator norm for a
  `Hex.BhksLatticeBasis`.
- Added `indicatorVector_sq_sum_le_projectedRows_cutRadiusSq4`, stated against
  the `cutRadiusSq4` field stored in the executable `bhksProjectedRows` output.

## Current frontier

This closes a small norm-bound slice for the later Gram-Schmidt cut-survival
constructor. It does not yet construct a full retained-row `CutSurvival`
certificate; that still needs the row-survival interface to identify the
post-cut projected row for each true-support indicator.

## Next step

Use the new projected-rows cut-radius lemma in the cut-survival assembly that
turns a true-factor lattice vector and Gram-Schmidt cut soundness into
`CutSurvival`, then `CutProjectionHypotheses`.

## Blockers

`lake build HexBerlekampZassenhausMathlib.BHKSBound` still fails in
`HexBerlekampZassenhausMathlib/Basic.lean` at the known slow-recombination
dilation recovery mismatch: the proof chain supplies the old scale-shaped
`hprefix` while the executable lemma now expects the `ZPoly.dilate` candidate.
That blocker is unrelated to this Lattice-only slice and matches the prior
progress note's #6804 frontier.
