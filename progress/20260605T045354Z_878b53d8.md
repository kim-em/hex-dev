# 2026-06-05 04:53Z — swapStep preservation lemmas (#6565)

Session UUID `878b53d8`. Issue #6565 (depends on #6571, now closed).

## Accomplished

- Added `swapStep_valid`, `swapStep_independent`, and `swapStep_b_eq` to
  `HexLLLMathlib/Independent.lean`.
- `swapStep_valid` proves both `Valid` fields:
  - `d_eq` via `Vector.getElem_set_self`/`_ne` and
    `gramDet_adjacentSwap_pivot` / `_of_ne`.
  - `ν_eq` by case-splitting the lower-triangular index `(i, j)` into the
    four regions (`i < km1`, `i = km1`, `i = kFin`, `k < i`) and applying
    the matching `scaledCoeffs_adjacentSwap_*` lemma in each branch.
- `swapStep_independent` derives positivity of the pivot determinant from
  `adjacentSwap_gramDetNumerator_dvd` and the positivity of the numerator
  (factors `gramDet b (k+1)`, `gramDet b km1`, `B^2`); off-pivot indices
  reuse `gramDet_adjacentSwap_of_ne`.
- Helper machinery added: `setPrefix`, two `Vector.modify` get lemmas, a
  `foldl_setSource_get_eq` characterising the row-prefix swap, and
  `foldl_modify_rows_get` characterising the outer "for each row above
  pivot, run the closure" pattern.

## Verification

- `lake build HexLLLMathlib` succeeds.
- No `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` introduced.

## Current frontier

Issue #6565 closes with this PR. With swap preservation and existing
size-reduction preservation in hand, the fuel-bounded LLL loop has all
state-preservation invariants needed for fuel sufficiency (#6567).

## Next step

Use `swapStep_valid` / `swapStep_independent` in `lllLoop` analysis (the
fuel sufficiency proof, #6567).

## Blockers

None.
