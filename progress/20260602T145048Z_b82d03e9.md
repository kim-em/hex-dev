# 20260602T145048Z b82d03e9 — Issue #2567

## Accomplished

- One-shot dispatch on directive #2567 (HO-4, BHKS Theorem 5.2 / `factorFast_terminates`).
- Added bridge-data form of the Mignotte canonical-supports capstone in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound`.
- The wrapper consumes the cap-lift bad-vector bridge package plus
  `FactorFastCapLiftAnalyticComparison` from the previous landed PR (#6349)
  and assembles the `lattice_eq_indicators` hypothesis internally via
  `projectedRowSpan_eq_trueFactorIndicatorLattice_of_factorFastCapLift_bridge`,
  rather than asking the caller to instantiate the projected-row-span
  equality directly.

## Current frontier

The remaining mathematical inputs to the final theorem are the open D1
prerequisites #6344 (canonical true-support recovery inputs, claimed) and
#6345 (per-class Mignotte product congruences, blocked on #6344). The final
assembly issue #6346 is blocked on #6345.

This PR narrows the cap-separation side of the assembly one more step so
that the eventual final theorem call site needs one fewer manual lattice
identification step; the genuinely mathematical recovery facts still come
from #6344/#6345.

## Next step

Wait on #6344 → #6345 → #6346. Once #6344 lands its canonical supports
and `ExpectedTrueFactors` package, and #6345 lands the per-class
modular product congruences, #6346 can compose them through the new
bridge-data wrapper to produce the public `factorFast_terminates` theorem.

## Blockers

- Final `factorFast_terminates` cannot land until #6344 (claimed by
  another agent) and #6345 (blocked on #6344) close. The decomposition
  is by design; this session contributes an incremental wrapper rather
  than re-decomposing or stepping on the active sub-issue work.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
