# Session 2da9b600 — multifactorLiftQuadratic monicness (#4374)

## Accomplished

- Promoted `Hex.ZPoly.reduceModPow_monic_of_monic` in `HexHensel/Linear.lean`
  from `private` to public.
- Moved the cofactor monic API from `HexBerlekampZassenhausMathlib/Basic.lean`
  to `HexHensel/QuadraticMultifactor.lean` so it is available at the Hensel
  layer:
  - `int_eq_of_congr_of_bounds` (private)
  - `int_eq_zero_of_mod_zero_of_bounds` (private)
  - `Hex.ZPoly.monic_of_congr_mul_monic_monic`
  - `Hex.ZPoly.monic_reduceModPow_of_congr_mul_monic_monic`
- Added in `HexHensel/QuadraticMultifactor.lean`:
  - `Hex.ZPoly.henselLiftQuadratic_g_monic` — the lifted monic factor stays
    monic. Loop invariant gives `Monic looped.g`;
    `reduceModPow_monic_of_monic` preserves it across the final reduction.
  - `Hex.ZPoly.henselLiftQuadratic_h_monic` — the cofactor is monic when
    `f` is monic. Uses `henselLiftQuadratic_spec` plus
    `monic_reduceModPow_of_congr_mul_monic_monic`; the `lifted.h ≠ 0`
    obligation comes from a top-coefficient contradiction (else
    `p^k ∣ 1`, contradicting `p^k > 1`).
  - `Hex.ZPoly.multifactorLiftQuadratic_each_monic` — every entry of the
    multifactor lift output is monic, via a list-level forall-membership
    helper inducted over `factors`.

## Verification

- `lake build HexHensel.QuadraticMultifactor` — passes.
- `lake build HexHensel.Linear` — passes.
- `lake build HexBerlekampZassenhausMathlib` — passes (pre-existing `sorry`
  warnings at `HexBerlekampZassenhausMathlib/Basic.lean:160` and `:239`
  unchanged).
- `lake build HexBerlekampZassenhaus` — passes.
- `python3 scripts/check_dag.py` — passes.
- `git diff --check` — clean.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
  `sorry` in affected files.

## Notes

The cofactor monic API moved down to the Hensel layer because the new
`henselLiftQuadratic_h_monic` consumer lives in
`HexHensel/QuadraticMultifactor.lean`, and `HexHensel` cannot import
`HexBerlekampZassenhausMathlib`. No external callers referenced the
relocated names, so the move is a strict relocation. The names changed
from `HexBerlekampZassenhausMathlib.monic_*` to `Hex.ZPoly.monic_*`,
which is the natural namespace for `Hex.ZPoly`-typed statements.

## Next step

The umbrella wrapper `henselLiftData_liftedFactor_monic` (issue #4376,
blocked on this issue) now has the underlying monicness in hand and can
proceed. The integer-factor monic capstone (issue #4377) is gated on
#4376.
