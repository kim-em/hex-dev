# 20260609T153328Z (#6682)

## Accomplished

Claimed #6682 and added the non-circular `toMonicPrimeData?` successful-descent
surface in `HexBerlekampZassenhausMathlib/Basic.lean`.

New constructors:
- `henselSubsetLiftHypotheses_of_toMonicPrimeData_henselLiftData_descent`
- `henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData_success_descent`
- `slowPathHenselSubstrate_of_toMonicChoosePrimeData_success_descent`

The correspondence constructor composes through
`henselSubsetCorrespondence_of_modPSubsetPartition` and the explicit
descent/forward-transport Hensel path.  It does not use
`henselSubsetCorrespondence_analytic_obligation`; the original-core mod-`p`
partition is carried as an explicit hypothesis because `toMonicPrimeData?`
selects prime data for `(Hex.ZPoly.toMonic core).monic`, not by itself a
partition theorem for arbitrary divisors of the original `core`.

The slow-path substrate sibling sources its `corr` field from the new
non-circular to-monic correspondence.  Its `partition` field intentionally
continues to use `liftedFactorSubsetPartition_of_toMonicPrimeData`, matching
#6682's stated allowance until the lifted-partition successor work replaces
that fallback.

## Current frontier

#6682 deliverables are complete.  The remaining broader HO-1 substrate work is
the separate lifted-partition replacement path, not this correspondence
constructor.

## Next step

Downstream callers that have `Hex.ZPoly.toMonicPrimeData? core = some
primeData`, an original-core `ModPSubsetPartitionHypotheses`, lifted-side
descent, and forward Hensel transport can now build the to-monic Hensel
correspondence without routing through the analytic fallback.

## Blockers

None.  Verification performed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Basic.lean`:
  no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
