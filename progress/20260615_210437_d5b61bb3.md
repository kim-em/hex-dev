# Progress - 2026-06-15 (session d5b61bb3, work/feature)

## Accomplished

- Claimed #7441 long enough to mark it `replan`: its body was stale after the newer BHKS chain split (#7519 -> #7520 -> #7521), so workers should not build against the old #7506 capstone consumer.
- Claimed #7516 and rewired the `toMonicPrimeData?` successful-descent surfaces in `HexBerlekampZassenhausMathlib/Basic.lean`.
- Removed the obsolete to-monic `HenselSubsetLiftHypotheses` constructor that forced original-factor mod-`p` reverse descent.
- Rebuilt `henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData_success_descent` to consume `MonicDescentHypotheses`, direct lifted-existence evidence, and the non-circular `toMonicLiftData_unique_subset` lemma.
- Updated `slowPathHenselSubstrate_of_toMonicChoosePrimeData_success_descent` to use that sound to-monic correspondence and to take the to-monic precision bound explicitly.

## Current frontier

The to-monic successful-descent wrappers no longer claim that `primeData` selected for `(toMonic core).monic` represents the original non-monic factor modulo `p`. They now expose the remaining forward/existence obligation directly and use the monic-correspondent descent carrier for the successful-lift package.

## Next step

Successor issue #7517 can wire slow-path callers to the updated to-monic API by supplying the lifted-existence evidence and the explicit `(toMonic core).monic` precision bound.

## Blockers

None for #7516. Existing unrelated local modification `.claude/CLAUDE.md` was present before this session and was not touched.
