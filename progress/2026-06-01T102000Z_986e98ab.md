# 2026-06-01T10:20Z — work session

## Accomplished

- Claimed directive #2564.
- Added `HexBerlekampZassenhausMathlib.factor_headline_contract_core`, a bundled
  Mathlib-side contract for the default `Hex.factor` surface covering product
  preservation, Mathlib irreducibility of recorded factors, positive
  multiplicities, syntactic no-duplicate polynomial keys, and the signed-content
  scalar convention.
- Left the pre-existing `.claude/CLAUDE.md` worktree modification untouched.

## Current frontier

- The new theorem deliberately packages only clauses already available from
  existing APIs. The remaining HO-1 headline strengthening is to add primitive
  factor entries and replace syntactic key distinctness with non-association.
- `HexBerlekampZassenhausMathlib/Basic.lean` still has existing localised
  `sorry`s, including the capstone irreducibility theorem this contract depends
  on.

## Next step

- Prove or expose the primitive-entry and non-association clauses so the bundled
  contract can be strengthened to the full five-clause HO-1 headline theorem.

## Blockers

- `lake build HexBerlekampZassenhausMathlib` currently fails downstream in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` due to existing API drift
  around `squareFreeModP`, `liftedFactorSubsetPartition_of_choosePrimeData`, and
  `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`. The direct edited
  target `lake build HexBerlekampZassenhausMathlib.Basic` succeeds.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib` (fails in downstream
  `IntReductionMod.lean` as noted above)
- `git diff --check`
- `git diff -- HexBerlekampZassenhausMathlib/Basic.lean | rg '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
