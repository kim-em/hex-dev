# Progress — 2026-06-15 (session b642598b, feature)

## Accomplished

Issue #7386 (BHKS §5 auxiliary domination): landed the two C-independent
analytic building blocks as a **partial** PR, and decomposed the remaining
multi-lemma chain into well-scoped sub-issues.

New lemmas in `HexBerlekampZassenhausMathlib/BHKSBound.lean` (63 lines, all
additions, no deletions):

- `one_lt_l2norm_toPolynomial_of_two_le_support`: an integer polynomial with
  `2 ≤ support.card` has `1 < l2norm (toPolynomial ·)`. Strengthens the
  existing `one_le_l2norm_toPolynomial_of_ne_zero` from `≤` to strict. Pure
  real analysis (each support coord contributes `≥ 1`, two terms give
  `‖·‖₂² ≥ 2`, `Real.sqrt_lt_sqrt`).
- `bhksPaperAuxiliaryFactorReal_pos`: `n·(2C)^(n²)·(log‖f‖₂)^n > 0` given
  `0 < C`, `1 ≤ bhksDegree f`, `1 < ‖f‖₂` (via `Real.log_pos` + `pow_pos`).

Both take their reachability inputs as hypotheses, so the proofs are
self-contained and don't touch the `normalizeForFactor` machinery.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound`: green (EXIT 0).
- Whole-library default build: 0 errors; only pre-existing sorries in
  unrelated modules (HexBerlekampMathlib, HexHenselMathlib).
- `python3 scripts/check_dag.py`: exits 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.

## Decomposition

Parent #7386 → partial PR + `Decomposed into #7439, #7440, #7441`:

- #7439: reachable core has ≥2 nonzero coeffs ⟹ `1 < ‖core‖₂` (the
  reachability half of sub-piece 1; the analytic half landed here).
- #7440: feasibility + quantitative `correctedRHS` bound (cldColumnNormBound,
  precision schedule). **Feasibility analysis goes first** — the
  `p^(2(k−cut))` scaling vs `(log‖core‖₂)^n` magnitude is the residual risk
  the prior session (3050128d) flagged and could not resolve.
- #7441: the domination capstone + `h_aux` discharge at `C := 2`; blocked on
  #7439 and #7440.

## Current frontier

The two clean analytic lemmas are the only purely-real-analysis pieces; the
rest of BHKS §5 needs either the reachability machinery (Basic.lean
`normalizeForFactor`/`extractXPower`/`squareFreeCore`) or the quantitative
certificate magnitude analysis, both deferred to the sub-issues.

## Next step

A worker should claim #7440 and do the feasibility/magnitude analysis before
anyone attempts #7441 — if the constant is mis-stated the domination is
unprovable as written and #7386 needs a corrected shape.

## Blockers

None for this session. #7441 is correctly blocked on #7439/#7440.
