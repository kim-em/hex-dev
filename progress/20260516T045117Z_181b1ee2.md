# Session 181b1ee2 - Integer-factor monic capstone (#4377)

Session: `181b1ee2-d160-4452-8ae0-081db9cb4151`
Issue: `#4377`

## Accomplished

Landed `representsIntegerFactorAtLift_monic` in
`HexBerlekampZassenhausMathlib/Basic.lean` (after
`henselLiftData_liftedFactor_monic`, step 2 of #4363's monic capstone
chain).

The proof chains:

- `liftedFactorProduct_monic` (new): foldl induction on `Finset.toList`
  using `zpoly_monic_mul` from `Hex.ZPoly.leadingCoeff_mul_of_nonzero`.
- `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`:
  under `Monic core`, the scale factor is `1`, so the recovery identifies
  `factor` with `centeredLiftPoly (liftedFactorProduct d S) (d.p^d.k)`.
- `monic_centeredLiftPoly_of_monic` (new): for `2 ≤ m`, the centred-lift
  of a monic `ZPoly` is monic — leading coefficient `1` survives
  `centeredModNat 1 m = 1` (split on `m = 2` vs `3 ≤ m`), and the size
  is preserved (size-`ge` from non-zero centred top coefficient,
  size-`le` from `size_ofCoeffs_le`).

The `2 ≤ d.p^d.k` precondition is discharged inside the capstone from
`factor ≠ 0` (forced by `factor ∣ core` and `core ≠ 0` via
`DensePoly.zero_mul`): if `d.p^d.k = 1`, every centred coefficient
reduces to `0` and `factor` collapses to `0`, contradiction.

Three small private helpers added (`zpoly_size_pos_of_monic`,
`zpoly_ne_zero_of_monic`, `zpoly_monic_one`, `zpoly_monic_mul`) plus the
public `liftedFactorProduct_monic` and `monic_centeredLiftPoly_of_monic`
lemmas that downstream callers may reuse.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — clean.
- `lake build HexBerlekampZassenhausMathlib` — clean (3461 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
  `sorry` introduced; pre-existing sorries at L160 / L239 are
  untouched.

## Current frontier

`#4377` deliverables complete. The capstone is now invocable by the
downstream chain `#4301` / `#4274` once the recursive-coverage assembler
discharges its `Monic (liftedFactor d i)` premise (the natural source is
the `henselLiftData_liftedFactor_monic` umbrella, gated on the
companion bridge tracked by `#4393`).

## Next step

A consumer that wants to drive the chain end-to-end will still need
the companion theorem from `#4393` to convert
`choosePrimeData`-boundary inputs into the
`QuadraticMultifactorLiftInvariant` premise of the umbrella. Once both
land, the public coverage statements in `#4301` / `#4274` can thread
`Monic factor` cleanly.

## Blockers

`/reflect` skill was unavailable in this pod session; skipped per
`agent-worker-flow` guidance.

None on the capstone itself.
