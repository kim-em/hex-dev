# 20260603T213819Z work #2567

## Accomplished

Added a named target for the BHKS Theorem 5.2 auxiliary-side product in
`HexBerlekampZassenhausMathlib/BHKSBound.lean`:

* `bhksPaperAuxiliaryFactorReal f C := bhksPaperDegreeFactorReal f *
  bhksPaperConstantFactorReal f C * bhksPaperLogFactorReal f` —
  packages the three paper factors `n · (2C)^(n²) · (log ‖f‖₂)^n`
  that the auxiliary-power sub-bound
  `auxiliaryBound^input.natDegree ≤ ...` must dominate.
* `bhksPaperThresholdReal_eq_coeffNorm_mul_auxiliary` — exposes the
  factored form `bhksPaperThresholdReal f C = bhksPaperCoeffNormFactorReal
  f * bhksPaperAuxiliaryFactorReal f C`.
* `bhksPaperAuxiliaryFactorReal_nonneg` — non-negativity under the
  project `0 ≤ C` convention.
* `bhksPaperAuxiliaryFactorReal_le_natCast` — integer-side dominance
  `bhksPaperAuxiliaryFactorReal f C ≤ (bhksDegreeFactor f *
  bhksFourPowFactor f * bhksLog2Factor f : Nat)` under the project
  `0 ≤ C ≤ 2` convention, threading `bhksPaperDegreeFactorReal_eq_natCast`,
  `bhksPaperConstantFactorReal_le_fourPowFactor`, and
  `bhksPaperLogFactorReal_le_log2Factor`.
* `bhksPaperThresholdReal_ge_of_factored_bounds'` — restatement of
  `bhksPaperThresholdReal_ge_of_factored_bounds` against the named
  auxiliary target so callers can hit
  `auxPow ≤ bhksPaperAuxiliaryFactorReal f C` directly instead of
  unfolding the three-way product.

## Current frontier

The factored-paper-threshold chain in
`HexBerlekampZassenhausMathlib/Recovery.lean` still consumes the
three-way product `bhksPaperDegreeFactorReal f *
bhksPaperConstantFactorReal f C * bhksPaperLogFactorReal f` literally
in its `h_aux` hypothesis (8 occurrences across the chain and its four
wrapper variants).  The new
`bhksPaperThresholdReal_ge_of_factored_bounds'` is a drop-in shim that
lets downstream callers hit the named target without changing the
existing chain signatures.

PR https://github.com/kim-em/hex/pull/6492 (open, branch
`agent/7bade7d4`) is advancing the orthogonal coefficient-power
sub-bound on `bhksPaperCoeffNormFactorReal f` directly — no overlap
with this change.

## Next step

Two independent options:

1. **Adopt the named auxiliary target** in the eight Recovery.lean
   occurrences of the three-way product (rewrite-rule style
   refactor; preserves all existing wrapper signatures via
   `bhksPaperAuxiliaryFactorReal`-eq-product).
2. **Auxiliary-power sub-bound** —
   `‖H_v‖₂^input.natDegree ≤ bhksPaperAuxiliaryFactorReal core C`,
   the genuine BHKS Lemma 3.2 content (resultant + Hadamard chain
   on the bad-vector polynomial).

## Blockers

None.  The new lemmas are pure compositions over the existing
non-negativity, monotonicity, and integer-dominance pieces in
`BHKSBound.lean`.

## Verification

- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  passed (3779 jobs, full library green).
- Added-line forbidden-token grep
  (`sorry|axiom|native_decide|TODO|FIXME`) returns nothing.
- Diff: 95 added lines, zero deletions (purely additive).
