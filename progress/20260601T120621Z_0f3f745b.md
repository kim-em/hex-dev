# 0f3f745b — work #5926

## Accomplished

- Claimed #5926 after checking the directive and unclaimed queues.
- Updated `HexBerlekampZassenhausMathlib/IntReductionMod.lean` so the
  lifted-partition and Hensel-subset outer-bound call sites pass the explicit
  `primeData` and `hchoose : Hex.choosePrimeData? ... = some primeData`
  witnesses instead of substituting back through total `Hex.choosePrimeData`.
- Reworked the exhaustive-arm proof bodies to use the explicit `primeData`
  for prime/modulus bookkeeping, exhaustive-core factors, and branch-shape
  calls.
- Made the local `MonicReductionCorrespondence` package witness-parametric
  and derived early lifted-factor facts directly from the monicized Hensel lift
  APIs, avoiding a forward reference to the later convenience extractor.

## Current frontier

The #5926 call-site work is in place. `lake build
HexBerlekampZassenhausMathlib.IntReductionMod` now gets past those call sites,
but the target still fails at the small-mod singleton square-free bridge:

```lean
hsquareFree : Hex.squareFreeModP core primeData.p
```

is supplied where `gcd_monicModularImage_derivative_eq_one` currently expects
the raw gcd-equals-one shape. This is the separately claimed companion issue
#5925, so this session did not broaden into that scope.

## Next step

Land #5925, then rerun:

```bash
lake build HexBerlekampZassenhausMathlib.IntReductionMod
lake build HexBerlekampZassenhausMathlib.Basic
```

If #5925 changes the same local small-mod singleton area only, the #5926
changes should remain focused on the lifted-partition/Hensel-subset witness
threading.

## Blockers

- #5925 is still required for a clean `HexBerlekampZassenhausMathlib.IntReductionMod`
  build. Verification completed here:
  `git diff --check` passed, and the added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` returned no matches.
