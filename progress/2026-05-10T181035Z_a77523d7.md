# Progress — agent/a77523d7

Issue: #3052 *HexMatrix.RREF: specify pivot search and column elimination*

## Accomplished

Added behavioral specs for `findPivotAux`, `findPivot?`, and the column-side
facts about `eliminateColumn` in `HexMatrix/RREF.lean`:

- `findPivotAux_some_ge`, `findPivotAux_some_nonzero`, `findPivotAux_some_above`
- `findPivotAux_none`
- `findPivot?_some_ge`, `findPivot?_some_nonzero`, `findPivot?_some_above`
- `findPivot?_none`
- private `rowAdd_get_dst`, `rowAdd_get_other` entry-level helpers
- `eliminateColumn_step_pivotRow_entry`, `eliminateColumn_step_other_entry`,
  `eliminateColumn_step_zero_at_x` (single-step facts)
- `eliminateColumn_foldl_pivotRow`, `eliminateColumn_foldl_outside`
  (fold-level facts)
- `eliminateColumn_pivotRow`: pivot row unchanged
- `eliminateColumn_zero`: non-pivot rows zeroed in pivot column under
  `M[pivotRow][col] = 1`

Build clean: `lake build HexMatrix.RREF` and `lake build HexMatrix` succeed
with only the two pre-existing `sorry`s (`rref_isRREF`,
`nullspace_complete`).

## Current frontier

Deliverable 2's third clause — same-operation transform preservation for
`eliminateColumn` — is not in this PR. It needs the `rowAdd_mul`
multiplication lemma, which is unlanded. The original issue body cited "using
#3051 lemmas" but #3051 was decomposed into #3059 (closed), #3060 (open,
unclaimed), #3061 (open, blocked); the multiplication lemmas live in #3060,
which has not yet landed. `coordination check-blocked` cleared the blocked
label on #3052 when #3051 closed via decomposition, but the actual proof
infrastructure was not produced.

## Next step

Spawned #3080 *HexMatrix.RREF: prove eliminateColumn transform preservation*
with `depends-on: #3060`. Once #3060 closes (delivering `rowAdd_mul` etc.),
#3080 can land the transform-preservation lemma using the new infrastructure.

Partial PR for #3052 created via `coordination create-pr 3052 --partial`
(decomposition breadcrumb left on #3052 pointing to #3080).

## Blockers

None for this session. Forward progress on transform preservation depends on
#3060.
