# 20260603T011754Z work #6415

## Accomplished

- Claimed issue #6415 and investigated the `choosePrimeData? = none` branch
  blocking the proposed unconditional `factorFast_terminates` theorem.
- Added a concrete executable witness in
  `HexBerlekampZassenhaus/Basic.lean`:
  `finitePrimeSearchNoneQuadratic = P * X^2 + X + 1`, where `P` is the product
  of every odd prime searched by `choosePrimeData?`.
- Documented why this witness reaches the normalized square-free fast path,
  bypasses `quadraticIntegerRootFactors?`, and makes every searched modulus
  inadmissible because the leading coefficient vanishes.
- Created successor issue #6427 to narrow the final theorem target to a
  prime-search-safe conditional statement instead of the false unconditional
  `factorFast_terminates` statement.

## Current frontier

The finite-search failure branch is reachable for the current executable
algorithm, so the unconditional theorem target from #6398 is false unless
prime selection is changed.  The final assembly should now target a conditional
statement carrying an explicit
`choosePrimeData? (normalizeForFactor f).squareFreeCore = some primeData`
witness, alongside the remaining BHKS producer packages.

## Next step

Work issue #6427: update the final D1 theorem target and assembly-facing
comments/lemmas so they state the strongest correct conditional termination
result for the current executable `factorFast`.

## Blockers

- No code blocker for this slice.
- `/reflect` was unavailable in this non-interactive shell (`reflect` not found;
  `coordination reflect` is not a supported coordination command).

## Verification

- `lake build HexBerlekampZassenhaus` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added diff contains no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
