# 2026-05-09T22:45:34Z - Issue #2876

## Accomplished

Claimed feature issue #2876 after confirming dependency #2875 is closed. Added
proof-facing coefficient bridge lemmas:

- `Hex.GramSchmidt.Rat.coeffs_lower_projection`
- `Hex.GramSchmidt.Rat.coeffs_lower_projection_comm`
- `Hex.GramSchmidtMathlib.rat_coeffs_lower_projection_real`

These expose the strictly lower `Rat.coeffs` entries as projection
coefficients and connect them to Mathlib's real inner-product projection
coefficient for converted rows.

Verification completed:

- `lake build HexGramSchmidt.Basic`
- `lake build HexGramSchmidtMathlib.Basic`
- `lake build HexGramSchmidtMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean HexGramSchmidtMathlib/Basic.lean`

The `rg` check reports only pre-existing `sorry`s in these files; this session
did not add `sorry`, `axiom`, or `native_decide`.

## Current frontier

The coefficient normalization part of the Mathlib bridge is now available as a
public theorem. The remaining work is the finite-sum/fold bridge that rewrites
`GramSchmidt.prefixCombination` over `List.finRange i` into the `Finset.Iio`
sum in `InnerProductSpace.gramSchmidt_def''`, followed by the final induction
for `rat_basis_row_eq_gramSchmidt`.

## Next step

Prove a `rowToEuclidean_prefixCombination_eq_Iio_sum`-style helper using
`rat_coeffs_lower_projection_real` and the induction hypothesis for earlier
rows, then use `GramSchmidt.Rat.basis_decomposition` and
`InnerProductSpace.gramSchmidt_def''` to close `rat_basis_row_eq_gramSchmidt`.

## Blockers

None. This is a partial PR because the remaining `Iio` sum bridge is a separate
substantial proof step.
