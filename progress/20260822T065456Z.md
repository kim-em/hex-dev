# Typed runtime controller review repair

## Accomplished

- Replaced the controller's fixed linear-looking complexity claim with the
  exact four Search authentications per selection, quadratic offer duplicate
  scans, two full tree checks plus the standalone cost pass, and a per-step
  bound whose retained-transition term grows with the lineage.
- Made `maxChoices` count select/dismiss decisions while permitting the policy
  to stop after exactly the cap, and added both exact-cap success and over-cap
  refusal canaries.
- Documented the all-or-nothing `runWithin` error boundary, scheduling-only
  offer predicates, matcher epoch and action-limit meanings, and dismissal
  reappearance/incompleteness behavior across the module, umbrella, and SPEC.
- Added an early retained-current-node serial alignment check, an opaque
  `Controller.State.mk` seal canary, and conformance coverage showing a handler
  veto is absent and unselectable through Search while direct structurally
  authenticated runtime advancement remains authorized.
- Built `HexInterval.RuntimeController`, `HexInterval.RuntimeConformance`, and
  the `HexInterval` umbrella; ran the DAG unit test, DAG check, trust-surface
  check, forbidden-token scan, and diff checks successfully.

## Current frontier

- The reviewed sealed typed runtime-controller layer is repaired and green in
  the local worktree, ready for the parent to restack or review.

## Next step

- Parent coordination can consume the amended local commit after checking the
  reported head; no push or PR was created here.

## Blockers

- None.
