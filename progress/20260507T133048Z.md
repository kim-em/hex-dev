# 2026-05-07 13:30 UTC — PR repair #2563

## Accomplished

Claimed repair PR #2563 and diagnosed its failing `conformance` check.
The PR was mergeable; CI failed because `HexGfq.Conformance` still expected
`Conway.luebeckConwayPolynomial? 2 2` and `3 1` to be unsupported, while the
current committed Conway table includes those entries.

Updated the `HexGfq` conformance edge-case guards to use unsupported table
boundaries that still hold: `(2, 7)` and `(17, 1)`.

Verification completed:

- `lake build HexGfq.Conformance`
- `lake build`
- `python3 scripts/check_dag.py`
- `python3 scripts/status.py`
- `git diff --check`

## Current frontier

PR #2563 has a targeted conformance repair ready to push back to
`spec/bz-cld-recombination`.

## Next step

Commit the repair, push it to the PR branch, and mark PR #2563 salvaged.

## Blockers

The worktree still contains an unrelated pre-existing `.claude/CLAUDE.md`
modification that this repair session did not touch.
