# HexHensel Phase 6 exit-criteria audit (#8181)

Session type: review. Audited the four Phase 6 exit criteria for
`HexHensel` (beyond the already-complete grind-automation slice) and
bumped `done_through 5 → 6`.

## Accomplished

Audited the core surface (`Basic.lean`, `Linear.lean`,
`Multifactor.lean`, `Quadratic.lean`, `QuadraticMultifactor.lean`,
~5900 lines). Verdict: **all four criteria pass after inline fixes →
bumped to 6.**

### Criterion 1 — Mathlib linter clean: PASS
Forced a fresh recompile of all five core modules (deleted their
oleans; deps stay cached) and captured the build: zero Lean linter
warnings (unusedVariables/unusedSectionVars/etc.). `HexHensel` is
`mathlib: false`, so the available linter surface is the Lean built-in
linters, which are clean.

### Criterion 2 — docstring coverage: PASS
- Public `def`/`structure`/`class`/`inductive`/`abbrev`: **0 missing**.
- Importable (non-`private`) theorems: **0 missing**.
- Non-obvious private helpers: added docstrings to the 6 load-bearing
  invariant lemmas the audit flagged — `add_low_degree_degree?_eq`,
  `add_low_degree_monic`, `reduceModPow_degree?_eq_of_monic`,
  `henselLiftLoop_invariant` (Linear.lean);
  `iterateQuadraticHensel_invariant`,
  `multifactorLiftQuadraticList_each_monic` (QuadraticMultifactor.lean).
- Residual (de-scoped per the issue's "do not docstring every private
  helper in the large Linear/Quadratic files"): ~12 non-obvious private
  `quadraticHenselStep_*` Bezout/correction lemmas in `Quadratic.lean`
  still lack docstrings. Not a Phase 6 blocker; a bounded follow-up
  could document the Bezout-correction block (Quadratic.lean ~986–2221).

### Criterion 3 — no dead/unreferenced declarations: PASS
Cross-repo reference audit of every public decl in the five files.
Removed 4 genuinely-dead declarations (sole occurrence = definition,
not `@[simp]`/`@[grind]`, not instances):
- `FpPoly.congr_reduceModPow_liftToZ` (Basic.lean) — redundant
  specialization of `congr_reduceModPow`.
- `linearHenselStep_h_degree?_eq` (Linear.lean) — the `h`-degree
  variant; only the `g`-variant is needed (g is the tracked monic
  factor), and that one is used internally.
- `QuadraticLiftResult.bezoutError` (Quadratic.lean) — unused `def`;
  the step inlines `s*g + t*h - 1`. (Companion `factorError` stays — 15
  uses.)
- `quadraticDoublingSteps_pos_of_one_lt` (QuadraticMultifactor.lean) —
  convenience lemma with no caller.

Kept (NOT dead):
- `polyProduct_pair`, `polyProduct_replicate_zero_toArray` — both
  `@[simp, grind =]` automation lemmas.
- `henselLiftQuadratic_bezout_spec`, `henselLift_monic_of_base` —
  documented public contracts whose paired siblings
  (`henselLiftQuadratic_spec`, `henselLift_congr_of_base`) are consumed
  by `HexHenselMathlib/Correctness.lean`. API ahead of consumer (the
  bezout-pair spec is the natural companion the headline BZ correctness
  theorem in #2564 will need); classified as public API, not dead.

### Criterion 4 — native computational path: PASS
No `native_decide` anywhere in the library; no `@[extern]`, `opaque`,
`@[implemented_by]`, or runtime oracle on the five core modules.

## Verification
- `lake build HexHensel` green (112 jobs), zero warnings.
- `lake build HexHenselMathlib` green (1538 jobs) — downstream consumer
  intact after the removals.
- `python3 scripts/check_dag.py` exit 0; `scripts/status.py` now lists
  `HexHensel -> Phase 7`.
- No `axiom`/`sorry`/`native_decide` introduced.

## Current frontier
HexHensel is Phase 6 complete (`done_through: 6`), eligible for Phase 7.

## Next step
Optional bounded follow-up: docstring the private
`quadraticHenselStep_*` Bezout/correction block in `Quadratic.lean`.

## Blockers
None.
