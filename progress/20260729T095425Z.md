# Structural matcher reference cursor

## Accomplished

- Added an engine-owned reference cursor over append-only node, equality, and
  propagator-application identifiers.
- Froze one structural ceiling per epoch, so admitting a match cannot move the
  remainder of the scan; renewal exposes only the later network suffix.
- Made creation generations engine-derived, visit accounting cumulative, and
  one-short resource failure leave the cursor unchanged.
- Added conformance checks for exact ordering, multi-batch continuation,
  deterministic renewal, arena shrink, and the absence of a package-provided
  completion flag.
- Used the matcher stream to recognize `sin (-x)`, construct `sin x` and
  `-(sin x)`, add the oddness equality and a scoped contractor, and pass that
  request through the existing atomic admission path.
- Checked exact rollback at one-short node, equality, application, and queue
  limits, then rebuilt the matcher and conformance targets successfully.

## Current frontier

The transparent linear cursor is now a conformance oracle for later indexed
or compiled matchers. It is not yet ordinary scheduler or policy work, and its
structural inputs are not yet copied into replay events.

## Next step

Merge the latest dynamic-scope generation fix into this stack, run the complete
affected build, open the stacked PR, and then integrate cursor epochs and
structural inputs into actions, policy keys, and replay.

## Blockers

None.
