# Session progress — fix CI saturation end-to-end

## Accomplished

### Lean-bench upstream — root-cause fix
- https://github.com/kim-em/lean-bench/pull/62 — `feat: invoke verify
  runners in-process`. Replaced per-(spec, param) child-process spawn
  in `verifyOne` / `verifyFixedOne` with direct in-process call via
  `findRuntimeEntry` / `findFixedRuntimeEntry`. Dropped panic + hang
  isolation (explicitly accepted and documented in the module
  docstring — do NOT fake with `IO.asTask + timer`). Merged at
  `8e6cd42`.

### Hex CI rewiring
- #3692 — bumped `lake-manifest.json` to the merged lean-bench rev,
  plus added bench-exe pre-build to the `Build hex` step so the
  budget script measures verify time, not compile time. Merged at
  `c75bd74`.
- #3694 — dropped `pull_request` from `conformance.yml`. Conformance
  still runs on push-to-`main` and `workflow_dispatch`. Removes the
  recurring runner-pool saturation that conformance was causing on
  every PR. Merged at `6870dc8`.
- #3695 — one-line fix: `bhksProductMismatchRecoverLift` was missing
  the `p_pos := by decide` field that PR #3476 added to `LiftData`
  on three sibling sites. Main had been broken since that merge;
  the saturated bench-verify CI masked it until the fast-pipeline
  drain surfaced it on every retried PR. Merged at `d113841`.
- #3696 — HO-35 ratchet: `BENCH_VERIFY_WARN_ONLY=1` off,
  `BENCH_VERIFY_HARD_CAP_SECONDS=300` (SPEC long-term target).
  Justified by two clean ~50 s data points after #3692. Merged at
  `e36b589`.

### Branch protection
- `main` required-status-checks now `[build, build-macos]` only
  (was `[build, build-macos, conformance]`). Done via API patch.

### Issue tracking
- Closed HO-36 (#3688) as obsolete; original "scientific tuning
  leaking into smoke" diagnosis was wrong, real cause was
  per-spawn overhead fixed by lean-bench PR.
- Opened HO-37 (#3693) — `speed up conformance workflow (24-min
  Lean compile dominates)`. Strawman approaches: `.lake/build`
  cache, trimmed target set, smoke-vs-nightly split, dedicated
  hardware. Non-urgent now that conformance is non-gating.

### Backlog reboot
- 52 stale PRs rebooted via empty `ci: re-run on the new fast
  pipeline (HO-37)` commits. Initial close+reopen via `gh pr` did
  not retrigger CI (GitHub deduplication); empty-commit push did.
- All conformance runs cancelled (in_progress + queued) to free
  the 20-runner pool for gating `CI` jobs. Backlog drained at
  19:04, ~57 min after the conformance cancel.
- Of the 52 rebooted PRs, only 1 went green on retry. Most failed
  with the same `p_pos` error that masked the main bug — fixing
  #3695 should turn most of those greenable on next test.

### Pod feature work
- FormalFrontier/pod#64 — `feat: add 'pod once' for one-shot
  priority dispatch`. New CLI `pod once <ISSUE> [--type TYPE]` for
  out-of-band priority work: bypasses dispatch queue + quota cap,
  spawns outside the `target` pool, exits after one iteration.
  Codex flagged 6 review issues (HIGH × 2, MEDIUM × 3, LOW × 1);
  all six addressed in a follow-up commit:
  - Failure-path `continue`s now mark `state.finishing` for
    one-shot mode via `_abort_one_shot_iteration` (5 sites).
  - Dedicated `/once` prompt template (Claude + Codex) that
    explicitly `coordination claim <N>`s without queue listing.
  - Runtime claim-mismatch guard in the JSONL monitor terminates
    the agent if `claimed_issue != target_issue`.
  - `cmd_once` preflight: refuses non-OPEN issues, already-claimed
    issues, unknown `--type`, bad gh JSON; rejects all before fork.
  - `_is_regular_agent(a)` helper applied to auto-spawn,
    `cmd_add`, `cmd_finish`, `cmd_kill` so one-shots don't count
    against `target` accountants.
  - Return-to-human handler exempts one-shot agents.
  Tests rewritten: 17 once-mode tests, 374/374 suite-wide.

## Current frontier

- New main CI run `25727452051` in-progress to verify the 300 s
  hard cap doesn't fire spuriously (expected: ~50 s actual vs
  300 s cap, plenty of headroom).
- pod #64 awaiting Kim review.
- 52-PR backlog drained but mostly red — needs `pod` dispatch with
  the `repair` worker type to triage the stale branches.

## Next step

- Verify new main CI completes green with the hard cap enforced.
- Wait for Kim's authorisation to resume `pod target N`. With the
  fast pipeline (~13 min build, ~50 s bench verify) and repair-
  worker preference, the 52-PR backlog should drain in a couple of
  hours of dispatch.
- Consider re-rebooting a sample of the 52 failed PRs once main is
  stable — many likely fail only because they were tested against
  the `p_pos`-broken main.

## Blockers

- None for the CI fix itself; the user paused dispatch and asked
  for confirmation that CI is fixed before resuming. Reporting via
  this progress file + the in-session messages.
