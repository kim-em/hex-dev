# Work: slow exhaustive lift-bound switch (#7622)

## Accomplished

Claimed #7622 after closing stale #7616 as already covered by merged PR #7632
with residual #7631. Changed the executable nonzero branch of
`Hex.exhaustiveCoreFactorsWithBound` to lift with
`Hex.ZPoly.exhaustiveLiftBound core B`, preserving the `B = 0` branch and the
observable guard shape.

Updated the nearby executable guard and the local executable proof surfaces in
`HexBerlekampZassenhaus/Basic.lean`:
- normalize/sign/record/primitive wrapper proofs now case-split on the helper
  lift;
- the product proof now uses the helper lift consistently through the
  recombination search branch.

Retargeted the first Mathlib-facing membership and exhaustive-wrapper surfaces
in `HexBerlekampZassenhausMathlib/Basic.lean` so direct consumers of
`Hex.exhaustiveCoreFactorsWithBound` use the helper-bound lift. Raised two
existing local heartbeat caps around heavy slow-exhaustive branch wrappers after
the retarget.

Verification completed:
- `lake build HexBerlekampZassenhaus.Basic` passed.
- `lake build HexBerlekampZassenhausMathlib.Basic` passed, with only pre-existing
  warnings.
- `git diff --check` passed.
- Added-line forbidden-token scan over touched Lean files found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

`HexBerlekampZassenhausMathlib.IntReductionMod` is the remaining frontier. It
now fails at old-bound slow-exhaustive call sites because their
`HenselLiftDescentHypotheses` / `HenselSubsetCorrespondenceHypotheses` inputs
still package `toMonicLiftData core B primeData`, while the executable wrapper
now consumes `toMonicLiftData core (exhaustiveLiftBound core B) primeData`.

## Next step

Retarget the slow-exhaustive proof inputs in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` to the helper-bound lift:
the `hdescent`, `hlifted_of_modP`, `hinitial`, local lifted-factor facts, and
precision hypotheses should all use
`Hex.ZPoly.exhaustiveLiftBound core B`. The bridge lemmas
`exhaustiveLiftBound_precision` and `exhaustiveLiftBound_monic_precision` are
already present at the top of `IntReductionMod.lean`.

## Blockers

No premise blocker found. This is a scope/time split: the executable change and
`Mathlib.Basic` retarget are complete, but `IntReductionMod` still needs the
remaining helper-bound descent retarget before #7622 is complete.
