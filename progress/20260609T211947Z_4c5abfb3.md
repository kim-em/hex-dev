# 20260609T211947Z (#6696)

## Accomplished

Claimed #6715 first because it was the direct continuation of the latest
HexPolyZ floor-sqrt work. Audited the remaining proof obligation, confirmed the
current endpoint lemmas do not imply the target by arithmetic alone, posted a
corrected issue comment with concrete failed near-envelope log-descent shapes,
and released #6715 for replanning.

Claimed #6696 and added the private raw recursive scalar synchronization theorem
in `HexPolyFp/SquareFree.lean`:

```lean
private theorem yunFactorsContributionWithLevel_scalar_sync
```

The proof handles the zero-gcd branch with the existing div-zero/scaled-gcd
substrate and reuses the scalar step bridge plus branch sync in the nonzero-gcd
recursive branch.

## Current frontier

#6696's requested private theorem is present and compiles. The existing
derivative-active wrapper remains in place; it can now be simplified to delegate
to the raw theorem in a future cleanup, but that is not needed for the current
deliverable.

## Next step

Use `yunFactorsContributionWithLevel_scalar_sync` in downstream scalar-aware Yun
completion work (#6711 / normalized completion consumers) where reachability is
not the right API boundary.

## Blockers

None for #6696.

Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
