## Accomplished

- Claimed feature issue #4334 after confirming no unclaimed directive was available.
- Confirmed #4927 is already covered by open PR #5151 (`Closes #4927`), so did not duplicate that work.
- Re-read the Phase 4 benchmarking requirements and HexLLL performance context.
- Verified the HexLLL bench harness wiring:
  - `lake exe hexlll_bench list` succeeded.
  - `lake exe hexlll_bench verify` passed all 52 registered benchmarks.
- Confirmed `python3 scripts/status.py HexLLL` still reports HexLLL ready for Phase 4 with `done_through: 3`.
- Ran a bounded scientific probe for `Hex.LLLBench.runSizeReduceChecksum` to `/tmp/hex-lll-4334-sizereduce-probe.json`.
- Released #4334 as replan/blocked-by-environment after the probe confirmed zero verdict-eligible rows.
- Claimed feature issue #5154.
- Added `linearPow_one`, `linearPow_X`, and `linearPow_C` in `HexPolyFp/Basic.lean`.
- Added one-line docstrings for `linearPow_succ`, `linearPow_succ_left`, and `linearPow_add`.

## Current frontier

- The probe confirmed the current host is unsuitable for Phase 4 scientific verdict evidence:
  - host load was high during the session (`10.13/16.04/16.40`, then `22.05/17.03/16.61`, later `11.27/14.97/15.87`);
  - harness self-measured per-spawn floor was `1.325 s`;
  - `runSizeReduceChecksum` produced zero verdict-eligible rows, with the first row killed at the configured cap.
- #5154 deliverables are implemented in `HexPolyFp/Basic.lean`.
- Removed the untracked `.cache/` oracle cache generated by HexLLL verification; the only unrelated dirty worktree entry is the pre-existing `.claude/CLAUDE.md` modification.

Verification for #5154:

- `lake build HexPolyFp.Basic` passed.
- `lake build HexPolyFp` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `git diff -- HexPolyFp/Basic.lean | grep -E '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true` produced no output.
- `grep -n 'theorem linearPow_one\\|theorem linearPow_X\\|theorem linearPow_C' HexPolyFp/Basic.lean` confirmed the three new declarations.

## Next step

- Re-run #4334 only on quiet/dedicated hardware, or when `carica` is idle enough for a stable low spawn floor.
- Once a quiet run is available, run all five affected parametric HexLLL registrations into a clean `reports/bench-results/` artifact and update `reports/hex-lll-performance.md` / `libraries.yml` only if the Phase 4 gates are actually met.
- Publish the PR for #5154.

## Blockers

- Current host contention prevents usable scientific benchmark verdicts for #4334.
