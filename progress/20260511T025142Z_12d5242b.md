# 2026-05-11 02:51 UTC — feature session (12d5242b)

## Accomplished

- Claimed and discharged #3215 (the
  `l2norm_toPolynomial_le_coeffL2NormBound` sorry in
  `HexBerlekampZassenhausMathlib/Basic.lean`).
- Added the Mathlib-side correctness chain to
  `HexPolyZMathlib/Mignotte.lean`:
  - `sqrtStep_ge_natSqrt`: each Newton-style integer sqrt step lands at
    least at `Nat.sqrt n`, by case-splitting on whether `x ≥ 2 * s` and
    handing the residual `(2s - x) * x ≤ s²` inequality to `nlinarith`
    via `sq_nonneg ((s : Int) - x)`.
  - `sqrtAux_ge_natSqrt`: induction on the fuel, threading `1 ≤ x` and
    `Nat.sqrt n ≤ x` through both branches of the iteration.
  - `natSqrt_le_floorSqrt`, `lt_floorSqrt_succ_sq`, and
    `le_ceilSqrt_sq` follow as plumbing lemmas, the last of which
    splits on the perfect-square branch of `ceilSqrt`.
  - `coeffNormSq_le_coeffL2NormBound_sq`: definitional consequence of
    `le_ceilSqrt_sq`.
  - `l2norm_toPolynomial_sq_le_coeffNormSq`: the existing private proof
    from `BHKSBound.lean`, relocated to the Mignotte bridge module so it
    can be consumed by both downstream sites without duplication.
  - `l2norm_toPolynomial_le_coeffL2NormBound`: composes the squared
    bound with `Real.sqrt_le_sqrt` and `Real.sqrt_sq` to land the bridge
    fact at the level of `Real.sqrt`.
- Promoted `Hex.ZPoly.sqrtStep` and `Hex.ZPoly.sqrtAux` from `private`
  to `def` in `HexPolyZ/Mignotte.lean` so the Mathlib-side proofs can
  unfold them by `show`. The executable surface (`floorSqrt`,
  `ceilSqrt`, `coeffNormSq`, `coeffL2NormBound`,
  `defaultFactorCoeffBound`, `mignotteCoeffBound`) is unchanged.
- Routed `HexBerlekampZassenhausMathlib.l2norm_toPolynomial_le_coeffL2NormBound`
  through the new public theorem and removed the duplicate
  `range_foldl_add_eq_finset_sum_nat` and
  `l2norm_toPolynomial_sq_le_coeffNormSq` from
  `HexBerlekampZassenhausMathlib/BHKSBound.lean`; its `+1`-slack helper
  now calls the relocated public theorem.

## Verification

- `lake build` — clean.
- `lake build HexBerlekampZassenhausMathlib.Basic` — clean; only the
  out-of-scope sorries from #3215's exclusion list remain
  (`defaultFactorCoeffBound_valid`, `irreducibleByFactorization_iff`,
  `factor_product`, `factor_irreducible_of_nonUnit`, `factor_unique`,
  `checkIrreducibleCert_sound`).
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry'
  HexBerlekampZassenhausMathlib/Basic.lean` — the target sorry is
  gone; remaining sorries match the issue's out-of-scope list.

## Current frontier

- The Mathlib-side Mignotte capstone
  (`defaultFactorCoeffBound_valid`) now has its L2-norm dependency
  discharged; the residual obligations are `f_poly ≠ 0` /
  `g_poly ∣ f_poly` lifts plus the final composition of
  `mignotte_bound` with `mignotteCoeffBound_le_defaultFactorCoeffBound`.

## Next step

- A separate session can pick up the
  `defaultFactorCoeffBound_valid` capstone now that
  `l2norm_toPolynomial_le_coeffL2NormBound` and the
  `natDegree_toPolynomial_le_degree_getD_of_dvd` predecessor (from
  session b6eb82a8) are both available.

## Blockers

- None for this slice.
