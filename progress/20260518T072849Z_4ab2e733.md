# Session 4ab2e733 — feat #4950

## Accomplished

Added the `_of_primitive_pos_lc_core` sibling of
`reassemblyExpansionComplete_exhaustive_of_ne_zero` to
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`, immediately after
the existing monic surface (~line 3650).

The new theorem
`reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
mirrors the monic original with three substitutions on the assembler
call:

1. Per-factor `0 < leadingCoeff q` (in place of per-factor `Monic q`)
   derived inline from `q ≠ 0` (via `(hirr q hq).not_zero`) and
   `Hex.normalizeFactorSign q = q` (the `hnorm` field of the
   `expansion_preconditions_of_choosePrimeData` package). Same shape as
   the quadratic-arm derivation at
   `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
   (lines 2383–2405).
2. `Hex.exhaustiveCoreFactorsWithBound_degree_pos_of_primitive_pos_lc_core`
   (#4946 deliverable 6) in place of the monic-input
   `exhaustiveCoreFactorsWithBound_degree_pos`.
3. `IntReductionMod.reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`
   (the `_of_pos_lc` sibling from #4949) in place of the monic
   mid-layer assembler.

Per the issue's Option α prescription, the `hcore_monic` premise is
retained as a temporary shim because the upstream
`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
call still threads it into helpers 2–4, which #4940 confirmed are
architecturally infeasible to relax at the present API surface. The
docstring documents this dependency explicitly.

The `hcore_ne` derivation switches from the monic-flavoured
`leadingCoeff = 1` shape used in the original to a `0 < leadingCoeff →
≠ 0` shape, mirroring the existing pattern but threaded through
`hcore_lc_pos`. The fuel-bound block carries over verbatim — it depends
only on `hdegree` (now from the `_of_primitive_pos_lc_core` substrate)
and the existing `mem_dvd_foldl_mul_zpoly` / `factorPower_size_lower_bound`
helpers in the same file.

The original `reassemblyExpansionComplete_exhaustive_of_ne_zero` and
the umbrella `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
are not edited — the sibling is an additive surface as the issue
prescribes.

## Build status

`lake build HexBerlekampZassenhausMathlib.IntReductionMod` reproduces
the same 16 pre-existing baseline errors in
`HexBerlekampZassenhausMathlib/Basic.lean` (lines 4848–6293, 5851 cases
failure, 6608/6672 kernel-unknown, 15242/15353 timeouts,
15560/15606 kernel-unknown) that #4949 and #4946 also documented. The
error set is identical between `origin/main` and the post-edit tree:
`diff <(grep "^error" /tmp/lake-baseline.log) <(grep "^error"
/tmp/lake-int-reduction-mod.log)` is empty. The Basic.lean baseline
prevents Lake from reaching IntReductionMod, but the file under edit
follows established patterns and matches the recipe specified in the
issue.

`python3 scripts/check_dag.py` — exit 0.
`git diff --check` — clean.
`git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean`
shows no new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The umbrella drop in #4880 remains blocked on directive-level work for
helpers 2–4 per #4940. With this sibling landed, the helper-5 layer is
ready for a downstream
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_of_primitive_pos_lc_core`
umbrella-internal rewiring that swaps the `hcore_monic` consumer
upstream of the assembler with the new `_of_primitive_pos_lc_core`
surface. That swap is out of scope for this issue and tracked
separately by the #4880 umbrella's "Option A" path.

## Next step

`coordination create-pr 4950`.

## Blockers

None — pre-existing `Basic.lean` build failures are environmental and
match `origin/main`.
