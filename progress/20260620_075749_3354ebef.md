# HexMatrix Phase 6 dead-code removal (#8218)

## Accomplished

Removed the 21 provably-dead `private` theorems identified in the
HexMatrix Phase 6 audit punch-list (#8195, item 2):

- `HexMatrix/Bareiss.lean`: `getEntry_swapRowsArray_matches`
- `HexMatrix/RowEchelon.lean`: `dotProduct_smul_ofFn_right`
- `HexMatrix/Determinant.lean` (19 theorems): `crossInversionCount_append_left`,
  `crossInversionCount_singleton_left`, `detTerm_congr_matrix`,
  `insertAt_prefix_get_self`, `insertAt_prefix_get_before`,
  `insertAt_prefix_get_last`, `finList_length_le_card`, `finTranspose_ne_iff`,
  `transposePermutationValues_insertAt_last_boundary`,
  `swapPermutationValues_map_permutationVectors_perm`,
  `detSign_transposePermutationValues_involutive`, `columnChoiceMatrix_none`,
  `det_columnChoiceMatrix_expand_column`, `columnChoiceMatrix_some`,
  `columnSumMatrixWithPrefix_nil`, `columnSumMatrixWithPrefix_eq_columnTupleMatrix`,
  `det_columnSumMatrixWithPrefix_expand`,
  `det_plucker_three_term_basisVec_of_gt_p3_of_nDet`,
  `twoColDet_plucker_basisVec_of_lt_p1`

481 lines deleted (460 theorem lines + collapsed separator blanks).

Deadness re-confirmed two ways before deletion: a whole-tree
`grep -rn` showed each name had exactly one occurrence (its own
definition); `columnChoiceMatrix_none`'s extra hits were substring
matches against the live `colReplace_columnChoiceMatrix_none_self`,
which was preserved. After deletion `lake build HexMatrix` is green
(389 jobs) — the rebuild is the proof of deadness — and a follow-up
`grep` confirms zero remaining references.

## Current frontier

`HexMatrix` `done_through` stays at 5 (not bumped, per the issue). Two
Phase 6 exit-criteria gaps remain from the #8195 audit: simpNF
normal-form cleanup and public-theorem docstrings.

## Next step

Follow-up issues for the remaining HexMatrix Phase 6 gaps (simpNF,
public docstrings); the bump to `done_through: 6` belongs to whichever
closes the last gap.

## Blockers

None.
