# Orthogonal GS norm lower-bound helper

Session: `63232bc0` (work, issue #5648)

## Accomplished

Claimed #5648 after releasing stale #5642 for replan. Added the
Mathlib-free orthogonal row-combination lower-bound surface in
`HexGramSchmidt/Basic.lean`.

The new helper chain proves that a row combination of pairwise orthogonal
rational rows has squared norm equal to the sum of coefficient squares times
row squared norms, then exposes
`rowCombination_normSq_ge_of_orthogonal_coeff_sq_ge_one` for the final lattice
lower-bound proof. Also added `one_le_intCast_mul_self_of_ne_zero`, packaging
the nonzero integer coefficient square lower bound over `Rat`.

## Current frontier

Issue #5644 still needs the row-combination decomposition from #5647 and then
can combine that decomposition with the orthogonal lower-bound lemma from this
session.

The stale duplicate core placeholder from #5642 remains a replan item: the
Bareiss-facing theorem is already proved in `HexGramSchmidtMathlib/Int.lean`,
while the duplicate private core theorem crosses the bridge boundary in its
singular branch.

## Next step

Prove the GS-coordinate decomposition for integer lattice row combinations
(#5647), then use the highest-nonzero coefficient and the new orthogonal
lower-bound lemma to discharge `normSq_latticeVec_ge_min_basis_normSq`.

## Blockers

None for #5648.

Verification completed:

- `lake build HexGramSchmidt.Basic`
- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexGramSchmidt/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

Unrelated worktree state:

- `.claude/CLAUDE.md` was already modified and was left untouched.
