# 20260602T183233Z repair #6369

## Accomplished

- Claimed PR #6369 for repair.
- Checked out `agent/783403b3` and reproduced the `main` merge conflict in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Resolved the conflict by keeping both independently added actual-cap helper
  APIs:
  `FactorFastCapLiftAnalyticComparison.ofBridgeData` and
  `no_bhks_bad_setup_of_factorFastCapLiftAnalyticComparison`.
- Merged `origin/main` into the PR branch.

## Current frontier

PR #6369 is locally salvaged and ready to push after this progress file is
included in the repair commit.

## Next step

Push `agent/783403b3` with `--force-with-lease` and mark PR #6369 salvaged.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
