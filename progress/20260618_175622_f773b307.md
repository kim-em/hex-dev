# Progress — 2026-06-18 (session f773b307, /work → /feature #7967)

## Accomplished

Completed #7967: Phase 6 `@[grind]` automation coverage for
`HexHensel/Basic.lean`.

Added `@[simp, grind =]` (upgrading from `@[simp]`) to 11 of the 12
`@[simp]` lemmas — the equation-shaped characterising/normal-form
rewrites:

- Coefficient characterisations: `coeff_modP`, `coeff_reduceModPow`,
  `coeff_liftToZ`.
- Zero/one normal forms: `modP_zero`, `reduceModPow_zero`,
  `reduceModPow_one_of_nontrivial`, `liftToZ_zero`, `modP_one`.
- Reduction/round-trip rewrites: `modP_reduceModPow_of_pos`,
  `reduceModPow_reduceModPow`, `modP_liftToZ`.

**Excluded:** `modP_reduceModPow` (the literal-`k+1` form,
`modP p (reduceModPow f p (k+1)) = modP p f`) keeps `@[simp]` only. As
a `grind` rewrite it is subsumed by its general sibling
`modP_reduceModPow_of_pos` (`0 < k`), which `grind` can apply to the
`k+1` term by discharging `0 < k+1`. Adding both as `grind` rules is
redundant, so per the "do not blanket-annotate all 12" deliverable it
is left out. The literal-pattern version is retained as `@[simp]`
because `simp` benefits from the direct syntactic match.

## Justification (deliverable #2)

No in-file proof was shortened: the existing `Basic.lean` proofs are
complete and lean on delicate `change`/`rfl` steps over `ZMod64`
internals; rewriting them to use the now-available `grind` would risk
fragility for no gain, and `grind` is not used as a closing tactic in
this file today. The justification is **downstream `grind`
availability**, confirmed by inspection:

- `HexBerlekampZassenhaus/Basic.lean` and
  `HexBerlekampZassenhaus/SmallModSingleton.lean` use `grind` and
  reference these `modP` / `reduceModPow` / `liftToZ` conversions.
- The `HexHensel/` lifting drivers (`Linear`, `Quadratic`,
  `Multifactor`, `QuadraticMultifactor`) consume the same defs.

Making the coefficient-level rewrites visible to those `grind` calls
is the payoff; no contrived in-file change was forced.

## Verification

- `lake build HexHensel` → 112 jobs, `Build completed successfully`,
  no `error`/`warning`. `HexHensel.Basic` built in 475ms (no slowdown,
  no `grind`-loop); all downstream `HexHensel.*` subfiles green.
- `git diff --check` clean; no added-line
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
- `python3 scripts/check_dag.py` → exit 0.
- Scope respected: only `HexHensel/Basic.lean` touched; `libraries.yml`
  `done_through` unchanged; no `*Mathlib` bridge touched; no new
  imports.

## Current frontier

`HexHensel/Basic.lean` grind coverage done. Sibling Phase 6 passes
(`HexGFq` #7961, `HexGF2` #7963) remain unclaimed.

## Next step

Pick up another unclaimed Phase 6 grind-coverage issue, or progress the
HO-1 directive tree (#2564) via its in-flight sub-issues.

## Blockers

None for this issue.

Note: worktree carries a pre-existing unrelated
`.claude/skills/agent-worker-flow/SKILL.md` modification from before
this session; not touched or staged.
