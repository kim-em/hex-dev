# 2026-05-21T06:07Z — work issue #5677

## Accomplished

Started by reading the latest progress entry and the worker flow. Claimed #5702,
then skipped it as stale/blocked because #5701 is closed but the promised
all-degree `Polynomial.graceWalshSzegoZeroControlAtDegree_proven` (or equivalent)
is not present in `HexPolyZMathlib/SchurSzego.lean`; only the degree-zero source
case exists.

Claimed #5677 after #5729 raced to another worker and host load made #4334's
scientific benchmark rerun unsuitable for this session. Added a narrow
Mathlib-free pivot-search packaging slice in `HexGramSchmidt/Int.lean`:

- `noPivotLoop_initial_gram_step_eq_of_prefix_none`, aligning an initial
  no-pivot Gram prefix state with column `s` when `singularStep = none`;
- `noPivotLoop_initial_gram_findPivot?_eq_none_of_column_zero`, turning a proved
  suffix zero column into the executable `Matrix.findPivot? = none` fact.

Verified:

- `lake build HexGramSchmidt.Int`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexGramSchmidt/Int.lean HexMatrix/Bareiss.lean | rg -n -e
  '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returns no matches

## Current frontier

#5677 is only partially complete. The remaining core proof is the singular
Gram pivot column-zero suffix: use the row-vector invariant, zero pivot,
integer self-orthogonality, and trailing-block symmetry to prove the
`h_column_zero` premise now packaged into `findPivot? = none`.

The file still has a pre-existing `sorry` warning at
`HexGramSchmidt/Int.lean:3451`; this session did not add or touch it.

## Next step

Prove the suffix column-zero theorem for
`Matrix.noPivotLoop s (Matrix.noPivotInitialState (Matrix.gramMatrix b))`, then
apply `noPivotLoop_initial_gram_findPivot?_eq_none_of_column_zero` to discharge
the executable no-replacement-pivot form needed by #5668.

## Blockers

No technical blocker for the pivot-search packaging slice. The full #5677 proof
still needs the closed regular-step row-invariant entry obligation or a direct
way to instantiate the existing parameterized invariant surface.
