# 20260602T093459Z 7c9492d1 — Issue #6285

## Accomplished

Added a private normalized-tail reconstruction bridge package in
`HexPolyFp/SquareFree.lean`:

- `pow_normalized_pthRoot_reconstruct_of_derivative_zero` lifts
  `pthRoot_normalizeMonic_reconstruct_of_derivative_zero` through arbitrary
  multiplicity powers.
- `derivative_active_normalized_tail_weighted_product_bridge` packages the
  derivative-active weighted-product algebra: a normalized recursive tail result
  reconstructs the raw tail power after carrying the leading scalar factor.

No executable square-free definitions or public theorem statements changed.

## Current frontier

The scalar/normalization algebra needed at the derivative-active repeated-tail
boundary is now available privately. The current residual invariant still
supplies the raw `pthRoot` residual at the active proof site; a successor can use
this bridge when refactoring that residual provider to normalized tails.

## Next step

Refactor `squareFreeAuxRevResidualSatisfied` or its derivative-active consumer
so the recursive IH is supplied by the normalized-tail state facts from
`yunFactorsContributionWithLevel_normalized_pthRoot_tail_valid`, then replace
the raw-tail IH call in
`squareFreeAuxRevContribution_derivative_active_pow_obligation`.

## Blockers

None for the bridge lemmas. Full proof-site rewiring still needs a normalized
residual-satisfaction payload; this issue added the algebraic bridge without
changing the residual invariant shape.

## Verification

- `lake build HexPolyFp.SquareFree` — green
- `lake build HexPolyFp` — green
- `python3 scripts/check_dag.py` — exit 0
- `git diff --check` — clean
- Forbidden-token diff grep over `HexPolyFp/SquareFree.lean` found no added
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
