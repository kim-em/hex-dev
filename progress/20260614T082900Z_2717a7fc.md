# Progress: #7099 HexArith/ExtGcd.lean Phase 6 docstrings

## Accomplished

Claimed and completed #7099. Added `/-- … -/` docstrings to the five public
`@[simp]` Bezout-identity theorems in `HexArith/ExtGcd.lean` that the Phase 6
pass had skipped:

- `extGcd_bezout_proj` (Nat) — cofactor identity against the routine's own
  returned gcd component, inputs coerced to `Int`.
- `extGcd_bezout_gcd` (Nat) — same identity with the right-hand side resolved
  to `Nat.gcd a b`.
- `pureIntExtGcd_bezout_proj` (Int) — projection form over `Int`, no coercion.
- `extGcd_bezout_proj` (UInt64) — projection form, words coerced via
  `Int.ofNat ·.toNat`.
- `extGcd_bezout_gcd` (UInt64) — gcd resolved to `Nat.gcd a.toNat b.toNat`.

Docstrings only — no statement/proof/attribute/import/namespace edits.
Followed neighbour style (`extGcd_bezout`, `pureIntExtGcd_bezout`,
`extGcd_spec`) and honoured the vocabulary ban (no "bridge", no em-dash).

## Verification

- `lake build HexArith.ExtGcd` — exit 0, clean, no new warnings.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --numstat` — 35 insertions, 0 deletions; `git diff --check` clean.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`/em-dash on added lines.

## Current frontier

#7099 ready to PR. After this lands, `HexArith` docstring coverage is
complete (does not by itself license a `done_through: 6` bump).

## Next step

Push branch, open PR closing #7099.

## Blockers

None. Pre-existing uncommitted `.claude/` edits in the worktree were left
untouched (off-limits / out of scope).
