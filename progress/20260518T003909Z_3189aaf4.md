# 2026-05-18T00:39:09Z — session 3189aaf4 (feature, #4874)

## Accomplished

- Issue #4874: rerouted `HexGramSchmidtMathlib.Int.scaledCoeffRows_lower_eq_coeffs`
  through the sorry-free public bridge `scaledCoeffs_eq_scaledCoeffMatrix_bareiss`
  and deleted the private sorry-bearing helper
  `scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss` at the top of the file.
- Relocated three theorems
  (`scaledCoeffMatrix_det_eq_zero_of_singularStep_lt`,
  `scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`,
  `scaledCoeffs_eq_scaledCoeffMatrix_bareiss`) from after the
  adjacent-swap section to immediately before
  `scaledCoeffRows_lower_eq_coeffs`, so the new proof can use the
  case-split bridge without a forward reference. The adjacent-swap
  pivot product proof (which consumes `scaledCoeffs_eq` at line ~1900
  of the original file) is unaffected because `scaledCoeffs_eq`
  remains defined before it.
- Added a docstring to `scaledCoeffs_eq` describing the reroute and
  updated the docstring on `scaledCoeffs_eq_scaledCoeffMatrix_bareiss`
  to mention the new private consumer.

Diff: `HexGramSchmidtMathlib/Int.lean` only, +107 −112, removing exactly
one `sorry`.

Verification:
- `lake build HexGramSchmidtMathlib.Int` — clean
- `lake build HexGramSchmidtMathlib HexGramSchmidt` — clean
- `python3 scripts/check_dag.py` — passes
- `git diff --check` — passes
- `rg "scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss"` over Lean
  sources — no matches in `HexGramSchmidt*/` (only historical progress
  notes still mention the deleted symbol)
- `rg "sorry|axiom|native_decide|TODO|FIXME" HexGramSchmidtMathlib/Int.lean`
  — only two docstring mentions of "sorry-free", no actual constructs

## Current frontier

`HexGramSchmidtMathlib/Int.lean` is now sorry-free. The three remaining
sorries in the Mathlib-free `HexGramSchmidt/Int.lean` (lines 2930, 3021,
3115) are explicitly out of scope per the issue.

## Next step

Open a follow-up issue (if not already planned) to discharge the three
Mathlib-free sorries in `HexGramSchmidt/Int.lean`. Independent of this
PR.

## Blockers

None.
