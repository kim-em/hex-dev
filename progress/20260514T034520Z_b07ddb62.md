# 2026-05-14 03:45 UTC — work session b07ddb62 (issue #3941)

## Accomplished

Attempted #3935 first, but released it as stale because its stated
constant/small-mod fast dispatch prerequisites are still only present in open
PR #3938, not on `main`.

Claimed #3941 and fixed the concrete executable factor-output regression for
square inputs whose `repeatedPart` is exactly the non-unit `squareFreeCore`.
`reassemblePolynomialFactors` now duplicates the already-split core factors in
that case instead of recording the raw repeated square-free core. The product
proofs and slow quadratic canonicality proofs were updated for the new
duplicate-core branch.

Added a guard for `(X^2 - 1)^2` showing the public `factor` output records
only `X + 1` and `X - 1`, each with multiplicity `2`.

Verification run:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhausMathlib/Basic.lean`
- `rg -n '\bsorry\b' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhausMathlib/Basic.lean`

## Current frontier

#3941 is partially complete. The executable square-case bug is fixed, but the
general Mathlib-free invariant that every recorded non-unit factor emitted by
`factorWithBound` / `factor` is irreducible is still not in place.

The bridge theorem `factor_irreducible_of_nonUnit` remains behind the existing
product and irreducibility placeholders in `HexBerlekampZassenhaus/Basic.lean`
and `HexBerlekampZassenhausMathlib/Basic.lean`.

## Next step

Continue #3941 by generalising the repeated-part refinement beyond the exact
`repeatedPart = squareFreeCore` case, then expose a theorem over
`factorWithBound` entries suitable for the Mathlib bridge.

## Blockers

No technical blocker for the executable square-case fix. Full #3941 completion
still depends on proving or exposing the broader irreducible-entry invariant;
this PR should be treated as partial progress.
