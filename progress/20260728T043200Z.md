# NumberField/Resultant PR-stack consolidation

## Accomplished

- Paused the next NumberFieldMathlib milestone in the recoverable stash
  `paused-numberfield-minpoly-20260728` so no new feature work overlaps the PR
  cleanup.
- Audited the 45-PR Resultant/NumberField chain and selected the top branch,
  `NumberFieldTowerField` (PR #9012), as the single aggregate merge vehicle.
- Merged current `origin/main` into the aggregate branch, resolving the six
  textual conflicts as unions of the RCF/interval/release work on `main` and
  the Resultant/NumberField targets in this stack.
- Repaired the semantic Roots integration exposed by the full build: the
  positive-degree proof now handles the newer soft-Pellet witness path through
  explicit out-of-range witness lemmas.
- Updated the NumberField roots benchmark hash for the newer deterministic
  isolation representative; the conformance guards still verify one root of
  multiplicity two.
- Validated the aggregate with `lake build` (9551 jobs), the combined
  conformance and emit-fixture targets, the bench Mathlib-free lint and its 27
  unit tests, the dependency-DAG check, and the Resultant, NumberField, and
  NumberFieldTower benchmark verification suites. The NumberField suite was
  repeated after updating its deterministic hash.

## Current frontier

- The aggregate branch is green locally and ready to replace the serial PR
  chain. PR #9012 is temporarily closed so the consolidation push does not
  start redundant CI before it is retargeted to `main`.

## Next step

- Commit and push the consolidation, retarget and reopen PR #9012 against
  `main`, and monitor its single CI run through merge.
- After #9012 lands, close any still-open intermediate PRs with an explicit
  note that their commits and contents were subsumed by the aggregate merge.
- Resume the stashed NumberFieldMathlib work only after no PR in this task's
  stack remains open.

## Blockers

- The only remaining gate is the aggregate PR's GitHub CI and merge checks.
