# Accomplished

- Audited the executable `nkWitness` definitions against the amended SPEC,
  including the running-power `z₂` fold and strict radius inequalities.
- Proved the exact residual, derivative-defect, and radial derivative bounds
  in sup coordinates, with the specified `k` factor and no `k(k - 1)` loss.
- Applied Newton--Kantorovich to obtain a unique root in the closed dyadic
  square, then proved strict interior membership and derivative nonvanishing.
- Extracted the generic Newton-map `image_bound` estimate and reused it in the
  contraction theorem and the strict-interior proof.
- Added the open-square geometry API, the combined `NKData.sound` contract,
  and the `HexRootsMathlib.NKWitness` umbrella import.
- Ran focused and full repository builds and the source policy checks.
- Obtained an Opus review finding no substantive soundness issue; removed its
  one genuinely unused helper observation while retaining the separate
  upstream copyright notice and the project-standard `@[expose]` convention.
- Opened and claimed #8791 for the next component-invariant slice.

# Current frontier

Execution-plan item 10 (#8790) is implemented and review-clean. The branch
still needs its final rebase onto the squash-merged polynomial-specialization
PR, commit, publication, exact CI inspection, and merge.

# Next step

Rebase the item-10 commit onto current `origin/main`, open and merge its PR,
then begin #8791 by formalizing only those component invariants preserved by
the executable constructors and proving doubled-square region containment.

# Blockers

None.
