# Progress — 2026-06-15 (session 047b4fe4, feature)

## Accomplished

Issue #7376: added one-sentence Phase 6 docstrings to the 12
`luebeckConwayPolynomialOfCoeffs_{p}_{n}_monic` private wrappers for
p ∈ {5, 7}, n ∈ {1..6}, in `HexConway/Basic.lean` (lines ~2869–2999).

Each docstring matches the established style landed for the
`_irreducible` wrappers (commit 3c714ea3), adapted from "irreducible
`FpPoly` … irreducibility proof" to "monic `DensePoly` … monicity
proof":

> The coefficient-list constructor for the committed `C(p, n)` Conway
> entry yields a monic `DensePoly`, via the `luebeckConwayPolynomial?`
> table hit and the literal's monicity proof.

Doc-only change: no signatures, statements, proof bodies, declaration
order, simp sets, or fixtures touched.

## Verification

- `lake build HexConway.Basic`: green (476/476).
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexConway/Basic.lean`: `36 0` (additions only).
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.

## Current frontier

The p ∈ {11, 13} monic block (lines ~3001–3122) is the remaining batch
in the `_monic` documentation stream. The p ∈ {2, 3} monic wrappers
(#7372) and p ∈ {5,7,11,13} irreducible wrappers (#7370/#7371) are
covered by concurrent PRs not yet merged into this base.

## Next step

Document the p ∈ {11, 13} monic wrappers (separate issue/batch).

## Blockers

None.
