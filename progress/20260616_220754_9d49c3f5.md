# Progress - 2026-06-16 22:07:54 UTC (session 9d49c3f5, work #7649)

## Accomplished

Claimed #7649 and added the proof-facing true-factor lift interface in
`HexBerlekampZassenhausMathlib/Lattice.lean`.

- Added `BHKS.supportProduct`, selecting lifted factors in lattice factor order.
- Added `BHKS.TrueFactorLift`, tying an abstract `BhksLatticeBasis` and support
  to concrete `Hex.bhksLatticeBasis f p a liftedFactors` data, a genuine factor
  of `f`, and the selected support product.
- Added `TrueFactorLift.blockForm`, `liftedFactors_eq`, `cutThresholds_eq`, and
  `cldRows_eq`, so downstream proofs can recover block form and the executable
  `Hex.cldCoeffs`/`Hex.psiCut` origin of `cldRows`.
- Added `BHKS.TightColumnBound` and `BHKS.tightNormBound_of_lift`, restating the
  tight CLD column-bound reducer behind the true-factor lift interface.

Created downstream blocked issues for the analytic work deliberately left out
of #7649:
- #7650: product CLD identity for true-factor supports.
- #7651: tight `psiCut` aggregation bound, depending on #7650.

Committed the interface as `387679da feat: add true-factor lift interface`.

## Current frontier

The API no longer asks workers to prove the tight CLD column estimate from
arbitrary `LiftedFactorSupport` and raw `BhksBlockForm`. The remaining proof
frontier is constructing `TightColumnBound L S D` from actual lift data.

## Next step

Land the #7649 PR, then work #7650 followed by #7651 to construct the tight
column-bound certificate from the true-factor lift package.

## Blockers

None for #7649. The worktree still contains a pre-existing unrelated
`.claude/CLAUDE.md` modification; it was not touched, staged, or committed.
