# Session 85bffad1 — feature #6851

## Accomplished
- Closed #6851: added `exists_witnesses_of_map_dvd_zmod` to
  `HexBerlekampZassenhausMathlib/Resultant.lean`. From a `ZMod n`
  divisibility `q.map ∣ f.map`, it returns honest integer polynomial
  witnesses `a, r` with `f = q * a + C n * r`.
- Proof route per the issue sketch: `Polynomial.map_surjective` +
  `ZMod.intCast_surjective` lift the modular quotient `b` to an integer
  `a`; `f - q*a` reduces to 0 mod n, so each coefficient is divisible by
  n via `ZMod.intCast_zmod_eq_zero_iff_dvd`; `Polynomial.C_dvd_iff_dvd_coeff`
  yields `r`.
- Dropped the suggested `q.Monic` and `0 < n` hypotheses — neither is
  used by the proof; the lemma is strictly more general without them and
  still matches the `f = q * a + C n * r` shape #6848/#6849 consume.
- Added `import Mathlib.Data.ZMod.Basic`.
- `lake build HexBerlekampZassenhausMathlib.Resultant` green; forbidden-token
  scan (sorry/axiom/native_decide/TODO/FIXME) clean on added lines;
  `check_dag.py` clean; executable `HexBerlekampZassenhaus` lib green.

## Current frontier
- The lemma is self-contained and independent of the #6847/#6848
  determinant/Sylvester chain, as the issue noted.

## Pre-existing regression discovered (NOT mine, out of scope)
- `lake build HexBerlekampZassenhausMathlib` (whole library) fails on
  `HexBerlekampZassenhausMathlib/Basic.lean:16087` — an Application type
  mismatch on `hprefix` inside
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`.
- Basic.lean is byte-identical to origin/main and does not import
  Resultant.lean, so this is unrelated to my change.
- Root cause hypothesis: a recent executable-layer refactor
  (#6802 "recombine non-monic cores via toMonic inverse substitution",
  #6813) changed the shape of `scaledRecombinationSearchModAux` /
  `shouldRecordPolynomialFactor` recording branch; the Mathlib proof in
  Basic.lean still expects the old shape.
- CI's "Build hex" step only builds the Mathlib-free `*_bench` targets and
  `hexlll_provider_probe`; it never builds `HexBerlekampZassenhausMathlib`,
  so this hard error is invisible to merge-gating CI. It blocks the whole
  HO-1 dependent chain locally. Worth a planner issue / repair.

## Next step
- A planner should file a fix issue for the Basic.lean:16087 regression
  (executable-shape drift vs the Mathlib recombination-coverage proof).

## Blockers
- None for #6851.
