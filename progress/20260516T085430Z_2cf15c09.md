# 2026-05-16T08:54Z — feature session 2cf15c09

Closes #4400.

## Accomplished

Added the composed convenience wrapper
`HexBerlekampZassenhausMathlib.henselLiftData_liftedFactor_monic_of_choosePrimeData`
in `HexBerlekampZassenhausMathlib/Basic.lean`, immediately after the existing
abstract-invariant version `henselLiftData_liftedFactor_monic`. The wrapper
combines #4434's upstream
`Hex.ZPoly.QuadraticMultifactorLiftInvariant_of_choosePrimeData` with the
existing abstract-invariant umbrella so a Mathlib-bridge consumer can discharge
per-output monicness of `Hex.henselLiftData` directly from the
`choosePrimeData` boundary facts (per-factor monicness, mod-`p` product
congruence, sequential split coprimality, nonempty witness) without
reconstructing the internal `QuadraticMultifactorLiftInvariant` themselves.

The proof is a thin composition: invoke the upstream wrapper to obtain the
abstract invariant, then feed it into `henselLiftData_liftedFactor_monic`. The
doc-comment cross-references both `henselLiftData_liftedFactor_monic` (the
abstract-invariant version) and the upstream
`Hex.ZPoly.QuadraticMultifactorLiftInvariant_of_choosePrimeData`.

`hp_prime` is typed as `Hex.Nat.Prime primeData.p`, matching what the upstream
wrapper expects (inside the `Hex.ZPoly` namespace `Nat.Prime` resolves to
`Hex.Nat.Prime`).

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` and
  `lake build HexBerlekampZassenhausMathlib` both succeed; no new `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME` from this diff.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.

## Next step

Public coverage callers (the eventual #4274 entry point and any direct
`HenselSubsetCorrespondence` consumer) can now use the new wrapper as a
single-call boundary-facts-to-liftedFactor-monic step.

## Blockers

None.
