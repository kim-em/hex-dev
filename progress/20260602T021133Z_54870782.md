# HexMatrix q-before raw nDet Plucker specialization (issue #6094)

## Accomplished

- Skipped #6125 for replan: its requested bridge
  `yunFactorsDerivativeActiveReachable_stateProvider` (normalized hstate →
  raw hstate) is provably impossible. `squareFreeContributionReachable f`
  is defined as `f.size = 1 → f = 1`, but `(normalizeMonic c).2` is
  always monic for nonzero `c`, so the normalized predicate is vacuous
  and contains no information about whether raw `c` equals `1` when
  `c.size = 1` (counterexample `c = DensePoly.C 2 : FpPoly 5`). The
  deliverable assumes a raw monic invariant that #6155's closing comment
  explicitly retracts. The skip comment lays out three concrete replan
  directions.
- Claimed #6094 and added `det_plucker_three_term_nDet_of_lt_p1` in
  `HexMatrix/Determinant.lean`, the q-before specialization of the
  canonical four-row `nDet_plucker_four_row_canonical` (PR #6202, merged
  during this session). The proof is a single application with
  `(r0, r1, r2, r3) := (q, p1, p2, p3)` and matching strict-order
  hypotheses.

## Current frontier

- Sibling q-between issue #6044 awaits the same canonical theorem via
  a different row permutation, still blocked further upstream.
- #6101 (twoColDet wrapper of the same q-before identity) is now
  actionable from the same `nDet_plucker_four_row_canonical` substrate
  and is left unclaimed for the next worker.

## Next step

- Open PR closing #6094.
- A follow-up session can claim #6101.

## Blockers

- None for #6094. `lake build HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token diff scan over the touched
  Lean file all pass.
