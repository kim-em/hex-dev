# 20260509T162535Z - Issue #2801

## Accomplished

Claimed #2679 first, verified it was stale because #2678 had been closed by
decomposition into #2778/#2779 and the matrix-level Gram determinant
nonnegativity theorem was not on `origin/main`, then released it for replan.

Claimed #2801 and replaced the failed high-degree HexGfqField calibration path
with a CI-feasible degree-8 ladder:

- added certificate-checked `m_p7_n8 := x^8 + x + 3` to
  `HexGfqField/Bench.lean`;
- extended `bundleForN` and all eight benchmark registrations to
  `paramSchedule := .custom #[2, 3, 4, 6, 8]`;
- ran the scientific sweep and committed
  `reports/bench-results/hex-gfq-field-n8-schedule.json`;
- updated `reports/hex-gfq-field-performance.md` with the new verdicts and
  kept `HexGfqField.done_through` at `3`.

Verification completed:

- `lake build HexGfqField.Bench`
- `lake exe hexgfqfield_bench list && lake exe hexgfqfield_bench verify`
- scientific `lake exe hexgfqfield_bench run ... --export-file reports/bench-results/hex-gfq-field-n8-schedule.json`
- `python3 scripts/check_dag.py`
- `python3 scripts/check_phase4.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGfqField/Bench.lean`

## Current frontier

The degree-8 schedule is buildable and resolves two verdicts:
`runNegSubChecksum` and `runFrobChecksum` are now consistent with declared
complexity. Six registrations remain inconclusive:
`runOfPolyReprChecksum`, `runAddChecksum`, `runMulChecksum`,
`runPowChecksum`, `runInvDivChecksum`, and `runZPowChecksum`.

## Next step

Plan a follow-on calibration route that widens scientific evidence without
adding degree-12/16 certificate checking to normal `HexGfqField/Bench.lean`
elaboration. The promising directions are a faster certificate-check path or a
split fixture/verification target outside the merge-gating build.

## Blockers

PR #2790 showed direct degree-12/16 certificate checks in the normal benchmark
module are CI-risky. The worktree still has the pre-existing unstaged
`.claude/CLAUDE.md` modification, which was not touched.
