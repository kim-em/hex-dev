# 20260602T150640Z a9c187cc — issue #6353

## Accomplished

- Claimed issue #6353.
- Added
  `BHKS.FactorFastCapLiftAnalyticComparison.ofCapSeparationHypotheses`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Added the actual-cap projected-row-span wrapper
  `projectedRowSpan_eq_trueFactorIndicatorLattice_of_factorFastCapLift_capSeparation`.
- Reworked
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalIndicatorsAtPrecisionForCoeffBound`
  to consume the named `FactorFastCapLiftAnalyticComparison` package instead
  of restating the raw Hadamard/l2norm inequality.

## Current frontier

The actual-cap recovery bridge now has a non-raw comparison call shape for
callers that already possess cap-separation hypotheses. The deeper analytic
leaf proving cap separation itself from projected-vector norm bounds remains
outside this small API packaging change.

## Next step

Continue BHKS D1 assembly by wiring downstream true-support and candidate
congruence producers through the new comparison/cap-separation wrappers.

## Blockers

- None for this session.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — passed.
- `lake build HexBerlekampZassenhausMathlib.TerminationBound` — passed.
- `lake build HexBerlekampZassenhausMathlib` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` for `sorry` / `axiom` /
  `native_decide` / `TODO` / `FIXME` — no matches.
