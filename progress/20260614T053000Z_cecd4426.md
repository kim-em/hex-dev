# Progress: #6956 — review PR #6951 recovered predicate multiplicative closure

Review session. Reviewed PR #6951 (`BZ recovery: repair multiplicative
closure for the recovered lifted predicate`, closes #6890) for soundness of
the recovered-coordinate multiplicative closure.

## Accomplished

Posted an approving review comment on PR #6951. Findings:

- `RecoveredAtLift.mul` is sound and needs no monicity / no scaled-product
  equality. `congr` uses `liftedFactorProduct_union_of_disjoint` (correctly
  oriented) + `congr_mul`; `dilate_eq` uses `dilate_mul` (dilation is a ring
  hom for any fixed scalar) + new Gauss lemma `primitivePart_mul` +
  `hf/hg.dilate_eq`. Both factors dilate by the same fixed `leadingCoeff
  core`; the old scaled-product predicate is not reintroduced as a hidden
  assumption. Addresses the #6956 focus directly.
- New Gauss lemma `DensePoly.primitivePart_mul` (+ `scale_left_cancel`,
  `primitivePart_scale_of_primitive`, `ZPoly` wrapper) reuses the existing
  `content_mul` decomposition and handles zero-content branches. PR is purely
  additive (0 deletions).

Verified locally:
- `lake build HexPoly.Euclid` — clean.
- `lake build HexPolyZ.Basic` — clean (the two `unnecessarySimpa` warnings at
  712/2898 are pre-existing, not on the new line-750 wrapper).
- `lake build HexBerlekampZassenhaus` — clean (497 jobs; CI-gated layer green).
- `python3 scripts/check_dag.py` — exit 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
- CI: `build`, `build-macos`, `conformance` all pass.

## Key finding (non-blocking, recorded in the review)

The PR progress file claims `HexBZMathlib.Basic` "now builds clean / the
`Basic.lean:16475` hard-red is no longer present." This is inaccurate:
`lake build HexBerlekampZassenhausMathlib.Basic` still fails, at the
`scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none` application
(`Basic.lean:16503` on the PR branch). I confirmed this is **pre-existing and
independent of the PR** by restoring `Basic.lean` to `origin/main` and
rebuilding — the identical error reproduces (same lemma, same downstream
`hcore_lc_le`/`hirr` warnings, only line numbers shifted). Because Lean
elaborates top-to-bottom and the only error is downstream of the addition at
line 8717, `RecoveredAtLift.mul` itself compiles. Per the
`hex-lean-mathlib-boundary` skill the Mathlib layer is not CI-built, so this
is not a merge gate.

## Current frontier

PR #6951 reviewed and approved (comment only — cannot self-approve on the
same account). All CI checks green; mergeable.

## Next step

The pre-existing `scaledRecombinationSearchModAux` failure in
`HexBerlekampZassenhausMathlib/Basic.lean` (~16503) is the active red seam on
main's Mathlib layer; a future repair/feature issue should target it. Not in
scope for #6956.

## Blockers

None.
