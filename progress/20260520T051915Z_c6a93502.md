# HexBerlekampZassenhaus scalar contract wrappers

Session: `c6a93502` (work, issue #5619)

## Accomplished

Claimed #5616 first, confirmed it duplicated the existing missing-source-theorem
replan coverage in #5614 and #5610, closed accidentally-created duplicate #5620,
and released #5616 back to replan with the duplicate/stale rationale.

Claimed #5619 and added public scalar-contract lemmas in
`HexBerlekampZassenhaus/Basic.lean`:

- raw `factorizationOfFactors` scalar characterization without exposing
  `signedContentScalar`;
- bounded `factorWithBound` scalar characterization;
- default `factor` scalar characterization;
- explicit zero, positive-leading-coefficient, negative-leading-coefficient,
  and scalar-zero-iff-input-zero wrappers.

## Current frontier

Directive #2637 now has a public API surface for the signed content scalar
contract on the raw, bounded, and default factorization entry points.

## Next step

The remaining directive residuals are the smaller factor-entry default wrappers
tracked by #5617 and the audit tracked by #5618.

## Blockers

None for #5619.

Verification completed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhaus.Conformance`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexBerlekampZassenhaus/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

Quality metrics after the slice:

- `sorry` occurrences in Lean files: 84
- `axiom` declarations in Lean files: 0
- `native_decide` occurrences in Lean files: 1
