## Accomplished

Claimed #3011 and added the private `nullspace_echelon_sound` helper in
`HexMatrix/RREF.lean`.

The proof adds private fold helpers for finite sums with one or two nonzero
terms, then proves the RREF nullspace row calculation by splitting each column
through the pivot/free partition. Pivot-column terms vanish except for the
active pivot row, free-column terms vanish except for the active free column,
and those two surviving contributions cancel.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`
  reports only the pre-existing `sorry`s at lines 116, 222, 652, and 657.

## Current frontier

`D.echelon * E.nullspace.get k = 0` is now available privately. The public
`nullspace_sound` transport from the original matrix `M` remains the next
theorem-level `sorry` in this area.

## Next step

Use `nullspace_echelon_sound` plus the row-transform inverse facts to prove
`nullspace_sound`, then continue toward `nullspace_complete`.

## Blockers

None.
