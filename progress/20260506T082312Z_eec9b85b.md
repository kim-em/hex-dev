# 2026-05-06 08:23 UTC — work session (eec9b85b)

## Accomplished

Claimed human-oversight issue #2460 and replaced the
HexBerlekampZassenhaus python-flint oracle's permissive re-factoring
comparison with a direct primitive-factor multiset comparison.

- `scripts/oracle/bz_flint.py` now factors the original input once and
  compares Lean's reported nonconstant components directly against FLINT's
  irreducible factor slots after primitive/content normalisation.
- `libraries.yml` rolls `HexBerlekampZassenhaus.done_through` from `3` back
  to `2`.
- `HexBerlekampZassenhaus/EmitFixtures.lean` and
  `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl` are narrowed to
  Phase-2-stable cases that satisfy the stricter oracle.

Verification run:

- `scripts/run-oracles-local.sh bz_flint --check`
- negative smoke fixture for unfactored `x^4 - 1` rejected by
  `scripts/oracle/bz_flint.py`
- fixture freshness diff via `lake exe hexbz_emit_fixtures`
- `lake build`
- `python3 scripts/check_dag.py`
- `python3 scripts/conformance_targets.py --check`
- `.venv-oracles/bin/python -m py_compile scripts/oracle/bz_flint.py`
- `git diff --check`
- `rg -n '^axiom |native_decide' HexBerlekampZassenhaus scripts/oracle/bz_flint.py conformance-fixtures/HexBerlekampZassenhaus libraries.yml`

## Current frontier

#2460 is implemented as the rollback + strict-oracle PR. The stricter
oracle exposes the previously frozen partial BZ outputs, so those failing
fixture cases are no longer advertised as Phase-3-covered while the library
is rolled back to Phase 2.

## Next step

Publish the PR for #2460.

## Blockers

No blocker for this issue. The pre-existing `.claude/CLAUDE.md` worktree
change was not touched or included.
