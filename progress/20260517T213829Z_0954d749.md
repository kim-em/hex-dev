# Session 20260517T213829Z_0954d749

## Accomplished

- Implemented `Hex.quadraticIntegerRootFactors?_pairwise_not_associated` in
  `HexBerlekampZassenhaus/Basic.lean` (HO-1 substrate #4785, supersedes the
  square-free-corrected signature for #4779).
- Added two private helpers feeding the assembly:
  - `linearFactorForRoot_not_associated_of_ne`: distinct integer roots produce
    non-`ZPoly`-associated monic linears (the `C (-1)` unit branch flips the
    leading coefficient; the `C 1` branch reduces to comparing `coeff 0`).
  - `linearFactor_dvd_listFoldl_of_mem`: if `r ∈ rs`, the polynomial product
    of `rs.map linearFactorForRoot` is divisible by `linearFactorForRoot r`
    (induction on `rs` using `list_foldl_mul_eq_mul_foldl_one` to lift the
    inductive divisor past a single left multiplication).
- Main theorem casework: `core.degree?.getD 0 = 2` gates entry; the
  `split.2 = 1` branch reduces to the linear-vs-linear pairwise non-
  association (via `List.pairwise_map` + `hrs_nodup.imp`); the
  `split.2.degree?.getD 0 ≤ 1` branch additionally needs the cross-pair
  non-association, which decomposes the `Associated` unit factor `u`:
  - `u = C 1`: extracts `(linearFactorForRoot r)^2 ∣ core` via
    `linearFactor_dvd_listFoldl_of_mem`, refuted by
    `linearFactor_squared_not_dvd_of_squareFreeRat` (#4791).
  - `u = C (-1)`: contradicts the residual's positive leading coefficient
    (inherited from `hcore_lc_pos` through the splitter's monic-product
    invariant `splitIntegerRootFactorsAux_polyProduct_leadingCoeff_pos`).

## Verification

- `lake build HexBerlekampZassenhaus.Basic`: clean.
- `lake build HexBerlekampZassenhaus`: clean.
- `python3 scripts/check_dag.py`: clean (exit 0).
- `grep "sorry|axiom|native_decide" HexBerlekampZassenhaus/Basic.lean`: only
  the pre-existing `isIrreducible_iff` sorry at line 6375 (target of #4818);
  no new entries from this session.

## Current frontier

The new theorem is now available to the `reassemblyExpansionComplete`
discharger (residual scope of #4747) and the sibling assembly issue #4604
(deliverable 2). Both downstream consumers are blocked on other substrates
(#4778 for #4747; #4747 itself for #4604) so this assembly does not unblock
them immediately, but it removes a substrate dependency.

## Next step

- Land the PR via `coordination create-pr 4785`.
- A follow-up planner cycle is expected to either close #4779 (predecessor
  with the proven-false signature) and #4604 if their bodies become stale
  in light of the corrected signature, or narrow them.

## Blockers

None.
