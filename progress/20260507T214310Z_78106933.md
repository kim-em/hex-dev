# 2026-05-07 21:43 UTC - HO-2 adversarial fixture audit (78106933)

## Accomplished

Attempted to claim the oldest human-oversight issues, but #2565, #2566,
and #2567 were blocked at claim time. Claimed #2526 next, found it stale
because prerequisite #2606 is still open with `has-pr`, closed the duplicate
decomposition issue #2616 created during triage, and released #2526 for
replanning with the dependency reason.

Claimed #2613 and completed the HO-2 fixture audit. Posted findings on #2565
with coefficient lists, python-flint factor signatures, intended case ids,
and schema/oracle coverage notes for:

- `adv/x4_plus_1`
- `adv/quad_sqrt2_sqrt3`
- `adv/swinnerton_dyer_sd3`
- `adv/phi15`

## Current frontier

The existing Berlekamp-Zassenhaus JSONL fixture/oracle schema can validate
final integer factor signatures, but it cannot encode or check pinned modular
prime expectations. That means several HO-2 fixtures would not necessarily
exercise the intended heavy-splitting or recombination path if added as plain
`factor` fixtures.

## Next step

Before the blocked HO-2 fixture PR is claimed, add a narrow conformance-only
way to encode pinned-prime modular factor expectations, either in JSONL/oracle
metadata or as a Lean conformance helper/API for explicit-prime modular
factor-degree checks.

## Blockers

No blocker for #2613 remains. The pre-existing pod-managed
`.claude/CLAUDE.md` worktree modification remains untouched.
