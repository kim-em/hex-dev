# Progress: #6810 — normalized SquareFree tail bridge

## Accomplished

Added `squareFreeAuxRevContribution_pthRoot_normalized_tail_bridge_of_nontrivial`
in `HexPolyFp/SquareFree.lean`. The new helper removes the old direct raw
correctness equality premise from the bridge surface: raw-root contribution
correctness is now derived from `squareFreeAuxRevContribution_correct_pow_of_nonzero`
using the raw root's reachability, nonzero, fuel, residual, and provider
payloads, then composed with the existing normalized-tail reconstruction
bridge.

Verified:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`

## Current frontier

The derivative-active call site still constructs the raw-root correctness
equality through its local recursive IH. The new bridge gives the follow-up
raw-IH-removal step a named theorem to route that obligation through validity
and residual/provider facts instead of passing an equality directly.

## Next step

Use the new bridge in the #6811 derivative-active refactor: replace the local
raw recursive correctness call with the bridge plus the existing raw validity,
normalized-tail correctness, and residual/provider payloads.

## Blockers

None for this slice.
