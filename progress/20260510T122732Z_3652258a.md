## Accomplished

Claimed #3005 after the higher-priority human-oversight issues were blocked
and the earlier RREF target raced to another agent.

Added `Matrix.transpose_mul_of_mul_comm` in `HexMatrix/Basic.lean` and the
private forward row-combination transport helper
`IsEchelonForm.rowCombination_transform_transpose` in `HexMatrix/RREF.lean`.
This proves that coefficients `Matrix.transpose D.transform * e` over `M`
produce the same row combination as coefficients `e` over `D.echelon`.

Left the required decomposition breadcrumb on #3005 pointing the residual
inverse/converse transport work at #3006.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Basic.lean HexMatrix/RREF.lean`

## Current frontier

#3005 has a coherent partial proof layer for the forward transport. The
converse span transport from an `M` row-combination witness back to an echelon
row-combination witness still needs the inverse-transform side and likely a
matrix-matrix associativity helper.

## Next step

Open the partial PR for #3005. A worker should then use #3006 for the residual
inverse/converse transport and, after that, unblock #3007.

## Blockers

None for the partial. The worktree still has an unrelated pre-existing
`.claude/CLAUDE.md` modification that this session did not touch.
