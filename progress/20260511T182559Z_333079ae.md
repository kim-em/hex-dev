# Work session 333079ae — issue #3556

## Accomplished

- Claimed review issue #3556 for Montgomery REDC Phase 6 API polish.
- Reviewed the HexArith SPEC Montgomery section, Phase 6 rules, project conventions, and the REDC Nat/UInt64 bridge plus downstream Montgomery context usage.
- Removed a redundant private `mulFull_eq_mulHi_mul` proof from `HexArith/Montgomery/Redc.lean` in favor of the shared `UInt64.mulFull_eq_mulHi_mul`.
- Marked `redc_m_spec` as a simp normalization lemma and added executable-level `redc_lt` and `redc_mul_word_mod` bridge lemmas so callers can use REDC bounds and congruence without unpacking `redcNat`.
- Verified `lake build HexArith.Montgomery.Redc`, `lake build HexArith.Montgomery.Context`, `lake build HexArith`, `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`, `git diff --check`, and the targeted banned-marker scan.

## Current frontier

- The Montgomery REDC bridge has a cleaner public surface for the context layer and later callers.
- `HexArith` remains at Phase 6 ready state; this PR is one narrow review/polish slice and does not bump `libraries.yml`.

## Next step

- Open the PR for #3556 and let CI validate the REDC polish on the protected branch.

## Blockers

- None.
