# Progress — 2026-06-15 (session ca293b12, feature)

## Accomplished

Issue #7448: proved `(normalizeForFactor f).squareFreeCore.coeff 0 ≠ 0` for
`f ≠ 0` in `HexBerlekampZassenhaus/Basic.lean` (new public theorem
`squareFreeCore_coeff_zero_ne_zero`, placed in the `squareFreeCore` cluster).

Supporting private helpers added in the same file (Mathlib-free executable
layer, so no `ring`/`by_contra`):

- `foldl_step_const_inner_pos`, `foldl_step_const_pos_i`,
  `foldl_step_const_inner`, `foldl_outer_const_pos`, `mulCoeffSum_const`,
  `coeff_mul_const`: the constant term of a `ZPoly` product is the product of
  the constant terms. No existing lemma computed `(p * q).coeff 0`; this unfolds
  the schoolbook `mulCoeffSum` fold and shows only the `(0,0)` index pair
  contributes to `i + j = 0`.
- `splitInitialZeros_tail_getD_ne_zero`: the zero-stripped tail is empty or has
  nonzero head.
- `extractXPower_core_coeff_zero_ne_zero`: the `X`-power-free core of a nonzero
  primitive part has nonzero constant term.

The headline proof reads the constant term of the rational reassembly
`toRatPoly core = scale unit (toRatPoly (sfc * rp))`
(`primitiveSquareFreeDecomposition_reassembles_xfree_over_rat`): a nonzero LHS
constant term forces `sfc.coeff 0 ≠ 0` via `coeff_mul_const`.

## Verification

- `lake build HexBerlekampZassenhaus.Basic`: green (491 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
- Diff is additions only (`161 0`).

## Current frontier

#7448 complete. It is deliverable 1 of #7439 (BHKS §5 reachable-core support
lower bound); the remaining #7439 deliverables (≥2 nonzero coefficients ⟹
`1 < ‖core‖₂`) are separate and still claimed by another session.

## Next step

None for this issue.

## Blockers

None.
