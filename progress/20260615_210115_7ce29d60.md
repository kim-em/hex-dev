# Progress — 2026-06-15 (session 7ce29d60, feature)

## Accomplished

Issue #7519: resolved the BHKS §5 joint-bound small-degree side condition by
adding two reusable lemmas to `HexBerlekampZassenhausMathlib/BHKSBound.lean`
(after `one_lt_l2norm_squareFreeCore_of_toMonic_degree`):

- `two_le_natDegree_liftedRecoveryCandidate_univ`: the full recovered candidate
  over **all** lifted local factors (`liftedRecoveryCandidate core d Finset.univ`)
  has `natDegree ≥ 2` whenever there are ≥2 lifted factors, each monic of
  positive transported degree. Proved outright via the existing degree-sum
  decomposition `natDegree_toPolynomial_liftedRecoveryCandidate_eq_sum` — the
  candidate's degree is the sum over `Finset.univ` of per-factor degrees, a sum
  of ≥2 strictly positive terms. This is the genuine combinatorial content; no
  hand-waved hypothesis.
- `two_le_bhksDegree_of_liftedRecoveryCandidate_univ`: derives
  `2 ≤ bhksDegree core` from the above plus the degree-preserving full recovery
  `hrecover` (recombination of all lifted factors reconstructs the core's
  degree). This is the side-condition the joint `bhksPaperThresholdReal_chain_*`
  path consumes in place of the unsound `HasPositiveDimension` premise.

### Premise check (directives-are-hypotheses)

The parent boundary caveat (#7506) is correct: `HasPositiveDimension` does NOT
force `2 ≤ n`, and the joint domination genuinely fails at `n = 1` (core `x+1`).
Resolution (a) from the caveat is sound and reachable: the recombination/cap-lift
surface is only entered with `2 ≤ primeData.factorsModP.size`
(`HexBerlekampZassenhaus/Basic.lean:7373`), mirrored by
`d.liftedFactors.size = primeData.factorsModP.size`. The full lifted-factor
product reconstructs the core, so its degree is the sum of ≥2 positive
local-factor degrees — a linear core has a single factor and never reaches
recombination. All building blocks already exist:
`natDegree_toPolynomial_liftedRecoveryCandidate_eq_sum` (Basic.lean:10576),
per-factor positivity
(`toMonicLiftData_liftedFactor_natDegree_pos_of_monicPrimeData`), monicity
(`toMonicLiftData_liftedFactor_monic`), full-product congruence
(`henselLiftData_liftedFactorProduct_univ_congr_core`).

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound`: green (4.5s, 3729 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
- `git diff --numstat`: `78 0` (additions only).
- Unused-variable linter warnings in the log are all pre-existing in
  `Basic.lean`, not the new declarations.

## Current frontier

The side-condition lemma is established and threaded as a small named lemma,
ready for the blocked joint inequality (#7520) and surface-wiring (#7521).

## Next step

#7520 proves the joint auxiliary-domination inequality consuming
`2 ≤ bhksDegree core`; #7521 discharges the threaded `hrecover`/`hsize`/`hpos`
facts from the executable cap-lift surface (precision-gated full recovery for
`hrecover`; recombination guard for `hsize`; the `toMonicLiftData_*` lemmas for
`hpos`/monicity).

## Blockers

None.
