# Phase 6 polish: HexHensel/Linear.lean definition cluster (issue #4796)

## Accomplished

Added two private-helper docstrings and tightened one public-struct
docstring in surface 1 of `HexHensel/Linear.lean` (lines 1-167).

- `ZPoly.scale_coeffwiseDiv_sub_of_congr` (was line 37): caller-facing
  docstring explaining that the congruence `g ≡ f (mod m)` forces `m` to
  exactly divide each coefficient of `f - g`, so `coeffwiseDiv` followed
  by `DensePoly.scale m` round-trips; consumed by the correction-proof
  chain in `linearHenselStep`.
- `linearHenselStep_correction_identity` (was line 145): docstring
  naming the algebraic identity it discharges and the role it plays
  (turning a Euclidean division plus a Bezout identity into the
  rearrangement that makes one Hensel correction step exact mod `p`).
- `LinearLiftResult`: tightened the struct docstring from the bare
  "Result of one linear Hensel lift step" to make explicit that the
  struct packages the lifted first factor `g` and the lifted
  complementary factor `h`, and that callers can pattern-match on
  `.g` / `.h` directly or rewrite via the unfolding simp lemmas
  `linearHenselStep_g` / `linearHenselStep_h`.

The two short, mechanical private helpers `list_getD_map_range` (line
12) and `congr_mul_reduceModPow_pair` (was line 135) follow immediately
from standard `List.getD` / `ZPoly.congr_mul` shape and are left
without docstrings per the issue's "obvious" carve-out.

`@[simp]` audit: existing tags (`coeff_coeffwiseDiv`,
`coeff_liftScaledIncrement`, `linearHenselStep_g`, `linearHenselStep_h`)
are all unconditional canonical-form rewrites and remain. No new
candidates: `congr_liftScaledIncrement_zero` is a `ZPoly.congr`
proposition rather than a rewrite, and no other unconditional
canonical-form equality appears in surface 1.

No definitions, theorem statements, argument orders, namespace
placements, or proof bodies changed.

## Verification

- `lake build HexHensel.Linear` — clean (101/101).
- `lake build HexHensel` — clean (108/108).
- `lake build` — clean (244/244).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `grep -nE 'sorry|axiom|native_decide|TODO|FIXME' HexHensel/Linear.lean`
  — no matches.

## Current frontier

Surface 1 of `HexHensel/Linear.lean` (lines 1-167) is now Phase-6-clean
for docstring coverage and simp discipline.

## Next step

Three further Phase 6 polish slices over `HexHensel/Linear.lean` remain,
each a separate planner-issue per the parent plan:

- `modP` / `liftToZ` congruence cluster (lines 169-572).
- Correction-proof chain (lines 575-1117) — mostly private; the planner
  should decide whether a dedicated polish issue is warranted.
- Driver + monic-preservation + final spec (lines 1119-1661) — likely
  requires further decomposition.

## Blockers

None.
