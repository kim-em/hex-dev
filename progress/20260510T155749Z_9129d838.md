## Accomplished

Claimed #2950 and added
`yunFactorsPairwiseReachable_common_dvd_one_derivativeSplit` in
`HexPolyFp/SquareFree.lean`.

The new private theorem packages the derivative-split Yun base case: for
`g = gcd f f'`, `c = f / g`, `y = gcd c g`, and `z = c / y`, any common
divisor of `z` and `y` divides `1`. The proof reconstructs `c = z * y`,
uses the existing Yun step derivative bridge to get divisibility of
`derivative c`, then applies the square-free quotient common-divisor theorem.

Verified:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  reports only the pre-existing `sorry`s in unrelated obligations.

## Current frontier

The derivative-split base common-divisor theorem requested by #2950 is proved.
Downstream pairwise-coprime work can now consume this initial Yun split fact.

## Next step

Use the new derivative-split common-divisor theorem in the broader
Yun pairwise invariant chain, or continue with the remaining independent
SquareFree proof obligations already marked by existing `sorry`s.

## Blockers

None.
