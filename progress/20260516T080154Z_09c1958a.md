## Accomplished

Claimed #4446 and completed the HO-44 Bareiss orphan blocker audit.

Posted the current blocker table on #3900:

- `HexMatrix/Bareiss.lean` still contains the orphan
  `Matrix.bareiss_eq_det` definition owned by #3900.
- `HexGramSchmidt/Int.lean` still has illegal Mathlib-free consumers in the
  scaled-coefficient/determinant chain and the determinant-positive
  independence cluster.
- `HexGramSchmidtMathlib/Int.lean` and `HexGramSchmidtMathlib/Update.lean`
  are legal bridge locations, but still call the orphan directly.
- No `HexLLL/` or `HexLLLMathlib/` `.lean` references are returned by the
  audit command.

Created follow-up #4447 for the previously unowned bridge-layer cleanup:
replace the direct `Matrix.bareiss_eq_det` calls in `HexGramSchmidtMathlib`
with the `HexMatrixMathlib` Bareiss correctness surface before #3900 deletes
the orphan theorem.

Verification run:

- `rg -n 'Hex\.Matrix\.bareiss_eq_det|Matrix\.bareiss_eq_det|bareiss_eq_det' HexMatrix HexMatrixMathlib HexGramSchmidt HexGramSchmidtMathlib HexLLL HexLLLMathlib -g '*.lean'`
- `python3 scripts/check_dag.py`

## Current frontier

#3900 remains blocked. Existing owners now cover all live blockers:

- #4445 / #3899 for the Mathlib-free GramSchmidt independence boundary.
- #3899 / #4053 / #3977 for the scaled-coefficient and Gram-determinant
  bridge chain.
- #4447 for direct bridge-layer orphan calls in `HexGramSchmidtMathlib`.

## Next step

Work #4447 or #4445 before attempting #3900 deletion.

## Blockers

None for #4446 itself. The worktree had a pre-existing `.claude/CLAUDE.md`
modification; this session left it untouched.
