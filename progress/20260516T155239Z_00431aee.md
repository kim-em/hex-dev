# Progress: #4508 — full

## Accomplished

- Landed the top-level wrapper
  `liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches` in
  `HexBerlekampZassenhausMathlib/Basic.lean`, immediately after
  `liftedSubsetSplit_prefix_mem_of_matches`.
- Added a private helper `LiftedFactorListMatches.exists_tail_indices`
  exposing the canonical mask decomposition of a matched-state at its
  head index. The helper packages `localFactors = head :: ys.map
  (liftedFactor d)` together with `ys.Nodup`, `J.min' hne ∉ ys`, and the
  per-`S` canonical mask equations
  `liftedSubsetSelectedList d S = head :: (tail.zip mask_S).filterMap ...`
  and `liftedSubsetSelectedList d (J \ S) = (tail.zip mask_S).filterMap ...`,
  in one go. The wrapper invokes the helper twice, once for the boundary
  `S` and once for the recovered `T`.
- Threaded the helper output through the proof: `subsetSplitsWithFirst
  (head :: tail) = (subsetSplits tail).map f` plus `List.map_eq_append_iff`
  on `hsplits` gives a `subsetSplits tail` decomposition; the canonical
  form of `S` identifies the boundary inner split as the `mask_S`
  filterMap; the canonical form of `T` paired with `hsplit_eq` from
  `liftedSubsetSplit_prefix_mem_of_matches` places the `mask_T`-canonical
  inner split inside `preIdx`. The mask-level helper
  `subsetSplits_prefix_exists_bit_diff_aux` (#4505) then yields a position
  `i' < tail.length` with `mask_T[i'] = false` and `mask_S[i'] = true`,
  which translates back to `ys[i'] ∈ J ∩ S ∧ ys[i'] ∉ T`.
- All cases compile; no `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` introduced. The two pre-existing `sorry` warnings at lines
  ≈160 and ≈239 are upstream.

## Current frontier

`liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches` is now available
as the prefix-side discharge for #4367 (assemble prefix-none lemma for
partitioned recursive recombination). The consumer in #4367 must supply
`localFactors.Nodup` from a Hensel-coprimality fact (e.g. distinctness
of Hensel-lifted factors at squarefree mod-`p` inputs), as flagged in
the deviation from the original signature in #4504.

## Next step

#4367 can now wire this lemma in. No follow-up needed for #4508.

## Blockers

None.

Commit: `baf0befe` on branch `agent/00431aee`.
