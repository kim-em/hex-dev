# 2026-05-18 07:46 UTC — forty-first Phase 4/5 checkpoint (ad48e498)

Claimed summarize issue #4958 and recorded this checkpoint covering
the merged-PR wave since #4909 closed at `2026-05-18T03:18:34Z`.
Window pinned at the upper end by the issue's creation timestamp
`2026-05-18T07:35:51Z` per its verification block. PR count:
```
gh pr list --state merged --limit 30 \
    --json mergedAt,number,title \
    --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
  | awk '$1 > "2026-05-18T03:18:34Z"' \
  | awk '$1 <= "2026-05-18T07:35:51Z"' \
  | wc -l
```
= **13 new PRs merged**, matching the issue body's headline estimate
exactly. (PR #4957 — umbrella-internal `_of_primitive_pos_lc_core`
rewiring of `reassemblyExpansionComplete_exhaustive_of_ne_zero`,
closing #4950 — merged at `2026-05-18T07:38:29Z`, just after issue
creation, and lands in the next summary per the issue's upper-bound
pin.)

## `coordination orient` snapshot

```
=== DIRECTIVES (highest priority) ===
#2637 HexBerlekampZassenhaus: Factorization record API + sign/multiplicity bugs
#2567 HO-4: Formalise BHKS Theorem 5.2 (factorFast termination, leaf theorem)
#2564 HO-1: BZ recombination — rollback and rewrite to van Hoeij CLD

=== Unclaimed work items ===
#4334 HexLLL Phase 4: investigate inconclusive scientific benchmark verdicts
#4880 BZ Mathlib-free: drop hcore_monic Gap 1 from umbrella
      (directive-level-blocked per #4940; not actionable as a feature)

=== Claimed work items ===
#4958 Summarize forty-first Phase 4/5 checkpoint                (this session)
#4956 BZMathlib: _of_pos_lc sibling of
      reassemblyExpansionComplete_singleton_of_irreducible
#4936 BZMathlib: abstract-bound siblings of scaled
      _some_factor_associated_ public wrappers
      (primitive + primitive_pos_lc_core)

=== Issues with open PRs ===
#4955 (PR #4959) BZ Mathlib-free: _of_pos_lc sibling of
      expandRepeatedPartFactorArray_pow_singleton (singleton-arm leaf)
#4902 (PR #4917) BZMathlib: abstract-bound siblings of
      exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate
      (repair-claimed, CONFLICTS)

=== Blocked issues (truncated; full chain in orient) ===
#4951 [#4929] liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core
              (unscaled primitive)
#4947 [#4944] factor_exhaustive_branch_entry_core_zpolyIrreducible_*
              (public bridge)
#4944 [#4938] exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_*
#4938 [#4936] exhaustiveCoreFactorsWithBound_coverage_*
#4934 [#4931] recombinationSearchModAux_some_and_covers_* (monic)
#4931 [#4927] liftedFactorSubsetPartition_prefix_none (monic)
#4929 [#4925] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_*
#4927 [#4924] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd
#4925 [#4919] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_*
#4924 [#4918] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd
#4919 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset_*
#4918 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset (monic)
#4832 [#4821, #4830, #4831] BZMathlib: lifted partition recombination support
#4831 [#4821, #4830] BZMathlib: lifted partition disjointness + assoc uniq
#4830 [#4821, #4821] BZMathlib: initial lifted partition cover field
#4825 [#4680, #2567] BZ Mathlib-free: ZPoly.Irreducible per factor.factors entry
#4821 [#4680] BZMathlib: discharge executable Hensel subset analytic obligation
#4819 [#4821] BZMathlib: prove factor_irreducible_of_nonUnit
#4818 [#4825] BZ: prove executable ZPoly isIrreducible_iff
#4680 [#2567] HO-1 substrate: compose ModP and Hensel lift constructors
#4172 [#4170] HO-1 residual: executable irreducibility checker theorem
#4170 [#2567] HO-1 residual: executable factor irreducibility bridge

=== Open pull requests ===
#4959 [agent/766ddc8f] singleton-arm leaf substrate
#4917 [agent/8a4738a1] exists_representingSubset_of_mem_normalizedFactors_*
                       (repair-claimed) [CONFLICTS]
#2656 [spec/real-roots-and-sturm] spec: hex-real-roots and hex-sturm (planned)
```

## Per-theme PR groupings (13 PRs)

### BZMathlib A2 `_of_bound` propagation — scaled primitive side (9 PRs)

The A2 abstract-bound (`_of_bound`) cascade reached the scaled-side
recursive-coverage layer this window. Each sibling lifts a lemma whose
original constrained precision via the core-shape
`defaultFactorCoeffBound core` to an abstract bound `B' : Nat` with
`hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'` and
`2 * B' < d.p ^ d.k`. The originals are rewired as thin instantiations
on the abstract interface. This window extends the chain through the
`scaledRecombinationCandidate` / `representingSubset` /
`liftedFactorSubsetPartition_prefix_none` / `scaledRecombinationSearchModAux`
surface — the recursive-coverage layer of the scaled-primitive side.

* **#4915** — `exists_mem_representedSubset_of_degree_cover` abstract-bound
  siblings (monic + primitive unscaled).
* **#4920** — `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum`
  abstract-bound sibling (utility lemma for the scaled side).
* **#4922** — `exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core`
  abstract-bound sibling (closes #4907).
* **#4928** — `exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
  abstract-bound sibling (closes #4921).
* **#4933** — `mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
  abstract-bound sibling (closes #4923).
* **#4937** — `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
  abstract-bound sibling (closes #4926).
* **#4941** — `coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
  abstract-bound sibling (closes #4930). Cover-at-min layer.
* **#4948** — `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core_scaled`
  abstract-bound sibling (closes #4932).
* **#4954** — `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  abstract-bound sibling (closes #4935). Recursive-coverage capstone for
  the scaled-primitive-non-monic side of this window.

### #4880 umbrella drop substrate (4 PRs)

Substrate landings supporting the strategic drop of the
`hcore_monic` Gap 1 from the
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
umbrella. After this window, helpers 1 and 5 of the umbrella have
their `_of_primitive_pos_lc_core` substrate fully closed; helpers
2, 3, 4 are architecturally infeasible at the present API surface per
the #4940 audit.

* **#4914** — relax three exhaustive-coverage wrappers in
  `HexBerlekampZassenhaus/Basic.lean` to primitive + pos-leading core
  (closes #4879). Executable-side prerequisite that #4880's umbrella
  drop sits on.
* **#4945** — `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm_of_primitive_pos_lc_core`
  (closes #4939). Helper-1 substrate sibling.
* **#4952** — `reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`
  (closes #4949). Mid-layer `_of_pos_lc` substrate consumed by the
  helper-5 path.
* **#4953** — `exhaustiveCoreFactorsWithBound_degree_pos_of_primitive_pos_lc_core`
  (closes #4946). Helper-5 substrate, weakened-conclusion sibling
  threading per-output `0 < leadingCoeff` in place of `Monic`.

(PR #4957, umbrella-internal rewiring of
`reassemblyExpansionComplete_exhaustive_of_ne_zero` itself via a
`_of_primitive_pos_lc_core` sibling, merged 3 minutes after issue
creation and lands in the next summary.)

## Status snapshot

* **`libraries.yml`**: no commits in window
  (`git log --since="2026-05-18T03:18:34Z" --until="2026-05-18T07:35:51Z" -- libraries.yml`
  empty). The next `done_through` bump is still gated on the HO-1
  critical-path chain.
* **In-flight PRs at orient time** (`2026-05-18T07:46:18Z`):
  * **PR #4959** (singleton-arm leaf substrate
    `expandRepeatedPartFactorArray_pow_singleton` for #4955) — open,
    branch `agent/766ddc8f`.
  * **PR #4917** (abstract-bound siblings of
    `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
    for #4902) — repair-claimed with merge conflicts, branch
    `agent/8a4738a1`. Has been in repair since the previous window.
  * (PR #4957, in flight at *issue creation* time per the issue body,
    has since merged — see note above.)
* **Directive closures in window**: none. #2637, #2567, #2564 all open
  as expected.
* **Substrate investigation closures**: #4940 (helper-2 audit,
  replan-labelled) closed with the architectural-infeasibility finding
  for helpers 2/3/4; #4942 (helper-5 substrate investigation) closed
  with #4946 decomposition.

## HO-1 critical-path commentary (capstone #4170 / sub-capstone #4825)

The BZ Mathlib-free chain remains gated on #4680 (Hensel subset
analytic obligation composition) and ultimately on directive #2567
(BHKS Theorem 5.2). Within this window the substrate
`_of_bound` cascade advanced significantly: the scaled-primitive side
now has its recursive-coverage layer (`scaledRecombinationSearchModAux`,
`liftedFactorSubsetPartition_prefix_none_*`, `coverAtMin_*`,
`mem_T_iff_*`, `exists_representingSubset_*`) fully `_of_bound`-lifted.

The role this plays on the critical path: lowering the precision
threshold required by the
`recombinationSearchModAux_some_factor_associated_*` /
`exhaustiveCoreFactorsWithBound_coverage_*` chain, which is the
primary downstream consumer once the A2 / Mignotte recovery wiring is
complete. The next two `_of_bound` consumers in flight are the
abstract-bound siblings of the scaled public wrappers (#4936,
currently claimed) and `exhaustiveCoreFactorsWithBound_coverage`
(#4938, blocked on #4936). Beyond those, the unscaled-monic side of
the recursive-coverage layer (#4918 / #4919 / #4924 / #4925 / #4927 /
#4929 / #4931 / #4934 / #4951) is filed and queued behind #4902 /
#4917.

## #4880 umbrella drop posture

After this window, helper 1 (`factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`)
and helper 5 (`reassemblyExpansionComplete_exhaustive_of_ne_zero`)
substrate are closed:

* Helper 1 substrate: #4945 ✓
* Helper 5 substrate: #4953 ✓ (weakened-conclusion sibling) plus
  #4952 (`_of_pos_lc` mid-layer) plus #4957 (umbrella-internal
  rewiring; merged just outside this window)

Helpers 2, 3, 4 are **architecturally infeasible** at the present API
surface per #4940's audit. The underlying
`multifactorLiftQuadratic_each_monic`
(`HexHensel/QuadraticMultifactor.lean:1196`) has a per-output
monicness conclusion that is false for non-monic primitive core: for
`n` mod-`p` factors and a non-monic primitive core, the multifactor
recursion's final residual carries `lc(core)` as its leading
coefficient. Helpers 3 and 4 route through helper 2's per-output
monicness internally and inherit the same blocker.

The umbrella drop therefore remains **directive-level blocked**.
Closing the residual gap requires either:

* **Option A**: relax `hd_liftedFactor_monic` along the consumer chain
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_*` →
  `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_*` →
  `_coverage_*` →
  `recombinationSearchModAux_some_factor_associated_*` →
  `scaledRecombinationSearchModAux_some_and_covers_*` to "all-but-one
  monic; the residual carries `lc(core)`". ~10–15 substrate sub-issues.
* **Option B**: modify `exhaustiveCoreFactorsWithBound`
  (`HexBerlekampZassenhaus/Basic.lean:5495`) or introduce a
  `henselLiftData_scaled` variant that monicises by scaling before
  invoking `multifactorLiftQuadratic`, with cascading proof updates.

Both options are directive-scope. Per #4940's recommendation, the
next planner cycles should **not** file substrate sub-issues for
helpers 2/3/4. The umbrella's residual Gap 1 is documented in the
docstring at `HexBerlekampZassenhausMathlib/IntReductionMod.lean:3589`
as architecturally infeasible at the present API surface.

## Carryover for the next summarize

* **PR #4957** (umbrella-internal `_of_primitive_pos_lc_core` rewiring
  of `reassemblyExpansionComplete_exhaustive_of_ne_zero` for #4950) —
  merged at `2026-05-18T07:38:29Z`, three minutes after this issue's
  creation; lands in the next summary.
* **PR #4959** (singleton-arm leaf substrate for #4955) — open at
  orient time.
* **PR #4917** (abstract-bound siblings of
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
  for #4902) — in repair, CONFLICTS. Has been in this state across
  multiple summarize windows; if the repair agent abandons it, the
  underlying issue #4902 returns to the queue.
* **Claimed but no PR yet**: #4956 (singleton-arm
  `reassemblyExpansionComplete_singleton_of_irreducible` `_of_pos_lc`
  sibling), #4936 (scaled `_some_factor_associated_` public wrappers
  abstract-bound siblings).
* **Blocked chain** (transitive on #2567 / #4680):
  #4818 / #4819 / #4821 / #4825 / #4830 / #4831 / #4832 / #4170 /
  #4172. Plus the monic-side recursive-coverage `_of_bound` chain
  (#4918 / #4919 / #4924 / #4925 / #4927 / #4929 / #4931 / #4934 /
  #4951) queued behind the public bridge (#4944 / #4947 / #4938).
* **#4880 umbrella drop**: directive-level blocked on Option A /
  Option B. Workers should not claim this issue (per its own body).
* **HexLLL Phase 4 inconclusive bench (#4334)**: still parked on
  host-contention measurement skips; awaits a quiet/dedicated-hardware
  run before the registered models can be re-evaluated. No movement
  this window.
