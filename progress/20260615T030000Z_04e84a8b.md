# Progress — 2026-06-15 (session 04e84a8b, feature)

## Accomplished

Issue #7317: added one-sentence Phase 6 docstrings to the nine
undocumented private declarations of the array-backed row-swap
correctness cluster in `HexMatrix/Bareiss.lean` (lines ~474–705):

- `array_getElem!_set!_same`, `array_getElem!_set!_ne`,
  `array_getElem!_setIfInBounds_same`, `array_getElem!_setIfInBounds_ne`,
  `swapRowsArray`, `rowSwap_get`, `getEntry_swapRowsArray_matrixToRows`,
  `rowsToMatrix_swapRowsArray_matrixToRows`,
  `getEntry_swapRowsArray_matches`.

Doc-only change: no signatures, statements, simp/grind annotations,
declaration order, or proofs touched. Each docstring states what the
declaration computes or proves and its role in showing the executable
`swapRowsArray` matches the abstract `rowSwap`.

## Verification

- `lake build HexMatrix.Bareiss`: green (379 jobs).
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Bareiss.lean`: `18 0` (additions only).
- No banned vocabulary or em-dashes on added lines.

## Current frontier

This cluster is complete. The neighbouring Phase 6 doc batches for
`HexMatrix/Bareiss.lean` past `findPivotArrayAux` (line ~706) and the
declarations before line 474 remain separate batches and were not
touched.

## Next step

None for this issue. Other unclaimed Phase 6 doc batches (#7319 HexPoly
Euclid Bezout, #7321 HexGramSchmidt prefixSpan) remain in the queue.

## Blockers

None.
