# Accomplished

- Rebased the representative PNT+ Table 12 fixture onto exact `main`
  `591a3315bc6a61cdb118d399640fa4a3d933c62a`, retaining the merged nested-tree
  conformance registration while adding the Table 12 modules.
- Corrected the fixed fact domain so every pair of row-25 upper cuts intersects
  to the tighter cut. Added a universal semantic intersection theorem, an
  injective exposed tag check for installed facts, and positive/negative replay
  canaries for a distinct-cut intersection.
- Replaced the misleading general coordinate decoder with the bounded
  `paperCellForCode?` recognizer. The radix documentation now states the exact
  `column < 8` injectivity condition and explicitly excludes logarithmic rows.
- Quoted the pinned `C_bk_S` source definition and exact row-25 nine-tuple in
  both SPECs. The source correspondence now explicitly derives
  `C + 1 = 0.86 + 1 = 1.86`, records `c = 0.88` and
  `RS_prime.c₀ = 1.03883`, and explains that `M` is source identity rather than
  an input to `C_bk_S`.
- Extended the fixed certificate with `M = 32e12`, added executable identity
  guards, and added a kernel theorem checking every scientific-decimal tuple
  field and coefficient against the encoded constants.
- Corrected all DAG-sharing claims: the row anchor output is a token, while
  actual reuse lives in the single action/payload and the proof-side theorem
  that computes one shared row sum before five projections.
- Passed the focused conformance build, full `lake build` (9,757 jobs), exact
  PNT inventory tests/check/source verification, trust-surface check,
  copyright/line-count/DAG/release/manual/phase/conformance lints, benchmark
  import checks and unit suites, banned-token scan, and `git diff --check`.

# Current frontier

- The repaired representative fixture remains exactly 5 of 130 cells. Its
  runtime intersection, source correspondence, coordinate diagnostic, and
  sharing claims now match their mathematical and implementation semantics.
- The exact `lakefile.lean` transition is registered under blob
  `ff882ce5c2495994c54ad410511788cf614a33eb`; factor freshness can be run after
  the repaired tree is committed because the checker intentionally reads
  `HEAD` blobs.

# Next step

- Commit the repair, confirm factor freshness at that exact commit, push the
  feature branch, open the non-draft upstream PR, obtain a genuinely fresh
  exact-edge Opus review, and monitor the exact-head CI run without merging.
- Complete the remaining 115 ordinary and 10 logarithmic cells in a later
  generated/chunked family fixture before changing the pending inventory
  classification.

# Blockers

- No mathematical, API, replay, or trust-boundary blocker remains for the
  representative batch.
- Full 130-cell acceptance remains deliberately out of scope for this PR and
  is still recorded as 125 pending cells.
