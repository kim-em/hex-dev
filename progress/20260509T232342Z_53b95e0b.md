# 20260509T232342Z - PR #2896 repair

## Accomplished

Claimed repair PR #2896 and salvaged the failed Phase 4 CI gate for the
HexBerlekampZassenhaus benchmark smoke harness.

- made the adjacent comments for the three `setup_benchmark` registrations in
  `HexBerlekampZassenhaus/Bench.lean` explicitly state their cost-model
  derivations;
- amended the PR commit message with the corresponding derivation text so
  `scripts/check_phase4.py` can validate the changed registrations from git
  history.

Verification completed:

- `python3 scripts/check_phase4.py`
- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

The Lean builds still report pre-existing imported-module `sorry` warnings; the
new benchmark file remains free of `sorry`, `axiom`, `native_decide`, `TODO`,
and `FIXME`.

## Current frontier

PR #2896 is locally repaired and ready for force-push. The branch remains a
smoke harness only; full HO-3 benchmark coverage still waits for HO-2
adversarial fixtures.

## Next step

Push the amended PR branch and mark PR #2896 salvaged.

## Blockers

None.
