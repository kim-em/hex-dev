# Issue #4216 (partial)

## Accomplished

- Claimed #4216 from the unclaimed queue.
- Reviewed the in-flight partial PR #4233 (session `5910eff8`, CI failed
  only on a transient `Bench verify` 503). Cherry-picked its commit
  `fc326b3` ("expose prime choice provenance") onto `agent/bfa8b2a9` so
  the new work supersedes #4233 cleanly.
- Removed the cherry-picked session's progress entry (kept only the
  current session's entry).
- In `HexBerlekampZassenhaus/Basic.lean`:
  - Exposed `monicModularImage` and `monicModularImage_monic` (no longer
    `private`) so the Mathlib bridge can connect to the executable monic
    image used by prime selection.
  - Added `berlekampFactorsModP_eq_of_isZero_false` — the defining
    equation for the candidate-level factor array on a nonzero modular
    image, packaged as a usable lemma.
  - Added the `isGoodPrime` fold-provenance chain
    (`primeChoiceDataScore_isGoodPrime`,
    `betterPrimeChoiceDataScore_isGoodPrime`,
    `choosePrimeDataScoreStep_isGoodPrime`,
    `choosePrimeDataScore_fold_isGoodPrime`).
  - Exposed `choosePrimeData?_isGoodPrime` so callers can recover the
    good-prime predicate from a successful `choosePrimeData?` result.
- Verified:
  - `lake build HexBerlekampZassenhaus`
  - `lake build HexBerlekampZassenhausMathlib`
  - `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search shows no new `sorry`, `axiom`, `native_decide`,
    `TODO`, or `FIXME` in affected files.

## Current frontier

Deliverable 1 of #4216 has three of four facts in place:

- selected prime primality — `choosePrimeData_prime` (cherry-picked).
- `primeData.fModP = Hex.ZPoly.modP primeData.p core` —
  `choosePrimeData_fModP_eq_modP` (cherry-picked).
- good-prime square-free + leading-coefficient — derivable from
  `choosePrimeData?_isGoodPrime` chained through
  `isGoodPrime_squareFreeModP` / `isGoodPrime_leadingCoeffAdmissible`.

The remaining piece — selected `primeData.factorsModP` is the
`Berlekamp.berlekampFactor` output for the monic modular image — is
not packaged yet. Deliverables 2 and 3 (singleton-branch composition in
`IntReductionMod.lean` using the no-split Berlekamp bridge) depend on it
and on a `DensePoly.scale` ↔ Mathlib `Polynomial.C * _` transfer that
lets monic-image irreducibility cross to `fModP` irreducibility.

Tracked in successor issue (filed at end of session).

## Next step

- Land the factorsModP equality (existential form, naming a witness
  `Lean.Grind.Field (ZMod64 data.p)` plus `(ZPoly.modP data.p f).isZero
  = false`) by mirroring the existing `_prime` / `_fModP_eq` fold
  provenance.
- Then add the Mathlib bridge `toMathlibPolynomial_C` /
  `toMathlibPolynomial_scale` and use Mathlib `IsUnit_C` /
  `Associated.irreducible_iff` to transfer `Berlekamp.berlekampFactor`
  output irreducibility from the monic image to `fModP`.
- Compose the final `IntReductionMod` singleton theorem using
  `irreducible_of_berlekampFactor_factors_length_le_one`.

## Blockers

- None for the partial scope landed here.
- The remaining factorsModP / IntReductionMod work needs a
  `(ZPoly.modP p f).isZero = false` lemma derived from
  `isGoodPrime f p = true`. The connecting fact is
  `leadingCoeffModP f p ≠ 0` → the leading position of `modP` survives
  the trailing-zero trimming in `FpPoly.ofCoeffs`. The proof should
  live near the existing `isGoodPrime_*` lemmas.
