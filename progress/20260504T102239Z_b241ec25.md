# Repair PR #2036 (HexPolyZ python-flint oracle)

## Accomplished
- Claimed PR #2036 via `coordination claim-pr-repair`.
- Merged `origin/main` into `agent/ca715d7e`. Resolved conflict in
  `.github/workflows/conformance.yml` by keeping both the HexPolyZ and
  HexGF2 oracle blocks side-by-side (HexPolyZ first, HexGF2 second,
  preserving the structure from each side of the conflict).
- Verified with `lake build` (clean) and a fresh `hexpolyz_emit_fixtures`
  run that diffs cleanly against the committed fixture; the
  `polyz_flint.py` oracle reports `checked 20 case(s), 0 failure(s)`.

## Current frontier
- PR #2036 ready to push and mark salvaged.

## Next step
- Push branch with `--force-with-lease`, then
  `coordination mark-pr-salvaged 2036`.

## Blockers
- None.
