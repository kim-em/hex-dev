# 20260609T202423Z (#6656)

## Accomplished

Claimed #6656 and audited the current `HexPolyZ/Mignotte.lean` square-root
substrate. The phase-one theorem `sqrtAux_phase_one_near_or_sq_le`, append
lemma `sqrtAux_append`, and preservation lemma `sqrtAux_preserves_sq_or_near`
are present and compile.

Added the private full-fuel composition lemma:

```lean
private theorem sqrtAux_full_fuel_near_or_sq_le
    (n : Nat) :
    let y := sqrtAux n (2 * n.log2 + 1) n
    y * y ≤ n ∨ sqrtNearEnvelope n y
```

This proves that the exact executable fuel budget reaches either the desired
lower-square bound or the near-root envelope after composing phase one with the
remaining fuel.

Created successor issue #6715 for the remaining phase-two convergence proof and
left the required `Decomposed into #6715` breadcrumb on #6656.

## Current frontier

The remaining obligation is to eliminate the final `sqrtNearEnvelope` case.
Current infrastructure gives
`y * y ≤ n ∨ y * y < 16 * n` for
`y = sqrtAux n (2 * n.log2 + 1) n`, but there is no existing lemma that turns
the residual near-envelope case into `y * y ≤ n`.

## Next step

Prove the substantive phase-two near-root convergence theorem requested in
#6715, then use it to add:

```lean
private theorem sqrtAux_terminates_via_stop_from_n (n : Nat)
    (hn : 0 < n) :
    (sqrtAux n (2 * n.log2 + 1) n) *
      (sqrtAux n (2 * n.log2 + 1) n) ≤ n
```

and expose public `Hex.ZPoly.floorSqrt_sq_le`.

## Blockers

None external. The blocker is proof-local: the phase-two convergence argument
still needs a real near-envelope elimination lemma, not just preservation of
nearness.

Verification performed:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
