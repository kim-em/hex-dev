# Progress — 2026-06-15 (session cbd0ea3a, feature)

## Accomplished

Issue #7389: added one-sentence Phase 6 docstrings to the seven
undocumented private helper lemmas around `inversePermutationValues`
in `HexMatrix/Determinant.lean` (lines ~3097–3256):

- `fin_mem_of_full_nodup`, `fin_idxOf_lt_of_full_nodup`,
  `inversePermutationValues_get_value`,
  `inversePermutationValues_get_index`,
  `inversePermutationValues_nodup`,
  `inversePermutationValues_insertAt_last_castSucc`,
  `inversionCount_inversePermutationValues_insertAt_last_castSucc`.

Each docstring leads with the backticked name and states what the
lemma asserts plus its role in the inverse-permutation sign-tracking
machinery behind the determinant expansion. `inversePermutationValues`
itself was left as-is (already documented). Doc-only: no signatures,
statements, proofs, `private` modifiers, declaration order, simp sets,
or imports touched.

## Verification

- `lake build HexMatrix.Determinant`: green (376 jobs).
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Determinant.lean`: `23 0`
  (additions only).
- Each of the seven names is immediately preceded by `/-- ... -/`.
- Added lines grep clean for `sorry`/`axiom`/`native_decide`/`TODO`/
  `FIXME` and banned vocabulary.

## Current frontier

Doc cluster complete. The separate permutation-vector *insertion*
cluster (~1866–1925, #7388, claimed by another session) and the
`HexGramSchmidt/Int.lean` integer self-dot cluster (#7390) remain
independent Phase 6 batches.

## Next step

None for this issue.

## Blockers

None.
