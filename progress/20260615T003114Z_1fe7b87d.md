# Progress — 2026-06-15 (session 1fe7b87d, feature)

## Accomplished

Claimed #7375 (establish a positive C-side premise for the BHKS cap-lift
auxiliary domination). After a premise check, **skipped it for replan**
with an evidence-backed diagnosis (issue comment 4703593391): the
directive's premise — "a `0 < C`-threaded proof is expected to close
this" — is unsound, and the gap is C-*independent*.

Key findings:

- **Option (1) impossible.** `C` has no executable provenance; it is a
  proof-layer analytic parameter, threaded abstractly with only
  `0 ≤ C`/`C ≤ 2` through every cap-lift surface. No `C := <const>`
  anywhere.
- **Option (2) insufficient.** The domination LHS `√(correctedRHS)^d`
  is a fixed positive constant independent of `C` (`correctedRHS` is
  built from `cldColumnNormBound core p`, which never takes `C` —
  `BadVector.lean:337`; the landed certificate bound
  `auxiliaryPolynomial_l2norm_le_sqrt_of_bridge_data`, `BadVector.lean:1565`,
  is C-independent). The RHS `n·(2C)^(n²)·L^n → 0` as `C → 0⁺`, so the
  domination is *false* for small positive `C`. No proof from
  `0 ≤ C ≤ 2` (even with `0 < C`) can exist. Confirmed: no lower bound
  on `bhksPaperConstantFactorReal _ C` exists in the tree, only
  `_nonneg` and the upper bounds `_le_fourPowFactor`/`_le_natCast`.
- Second, stronger counter-model: `bhksPaperLogFactorReal core =
  (log‖core‖₂)^n = 0` when `‖core‖₂ = 1` (e.g. `core = x`, reachable as
  `squareFreeCore (x²)`), collapsing the RHS to 0 for *every* `C ≥ 0`.
- The genuine missing content is the **C-independent** BHKS §5
  certificate→threshold quantitative bound at fixed `C := 2`:
  `√(correctedRHS)^d ≤ bhksPaperAuxiliaryFactorReal core 2`, relating
  `cldColumnNormBound` + the precision schedule to the paper threshold.
  This is a multi-lemma analytic development, not hypothesis threading.

This refines the two prior skip reports (comments 4703345284,
4703347848), which attributed the gap to "a quantitative constraint on
C." The constraint is not on C: fixing C to any positive constant does
not help, because the gap is entirely on the certificate side.

## Verification

No code changed — diagnosis only. Build untouched. No PR.

## Current frontier

#7375 is back in the `replan` queue with a precise, C-independent
re-scoping recommendation. A planner should reframe it as the BHKS §5
certificate→threshold bound at fixed `C := 2` (and likely decompose it).

## Next step

For the replanner: replace the free `0 ≤ C ≤ 2` parameter on the
cap-lift surfaces with `C := 2`, and scope the residual
`√(correctedRHS)^d ≤ bhksPaperAuxiliaryFactorReal core 2` as an analytic
sub-development (bounds on `cldColumnNormBound`, the precision schedule
`k`/`p^(2(k-cut))`, and `‖core‖₂`).

## Blockers

None for this session — the issue itself is blocked on the corrected
C-independent decomposition, now documented for replan.
