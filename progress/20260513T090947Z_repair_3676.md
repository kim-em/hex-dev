# Repair session: PR #3676

## Accomplished

- Claimed PR #3676 (`HO-16: HexLLL: rewrap Isabelle comparator in persistent subprocess protocol`) for repair.
- Diagnosed the failed Linux `build` check as isolated to `hexlll_bench verify`: several Isabelle-backed fixed benchmarks saw `svp_verified` close stdout or hit a broken pipe.
- Hardened the Isabelle persistent-driver patch to stream `lines` from stdin via `getContents`, avoiding the explicit `isEOF` probe in the request loop.
- Hardened the Lean request path so EOF before a reply line is treated like a protocol failure: the cached child is dropped, a fresh driver is spawned, and the request is retried once.
- Made `scripts/oracle/setup_lll_isabelle.sh` reset the extracted source tree before applying changed patches, so cached oracle sources cannot accumulate stale patch state.
- Verified locally:
  - `scripts/oracle/setup_lll_isabelle.sh`
  - `lake build`
  - `HEX_LLL_ISABELLE_SVP="$PWD/.cache/oracles/lll-isabelle/src/experiments/svp_verified" lake exe hexlll_bench verify`
  - `python3 scripts/check_dag.py`

## Current frontier

PR #3676 has a targeted repair ready to push.

## Next step

Push `agent/9b55e5d6` with `--force-with-lease` and mark PR #3676 salvaged.

## Blockers

None.
