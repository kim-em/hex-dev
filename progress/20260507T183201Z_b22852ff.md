# 2026-05-07 18:32 UTC — HO-1 BHKS bound slice (b22852ff)

## Accomplished

Claimed human-oversight issue #2564 after confirming the rollback requested
there had already landed on `main` via #2582. Added the SPEC-required
`bhksBound : ZPoly -> Nat` executable helper to
`HexBerlekampZassenhaus/Basic.lean` using the pinned all-integer BHKS
precision cap formula, and added conformance guards for zero, linear, and
quadratic inputs.

Verification passed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

HO-1 remains a large implementation/proof rewrite. The new bound helper is now
available for the future `factorFast` precision-doubling loop, but the current
production recombination path still uses the pre-HO additive LLL lattice and
the public API still lacks the final `factorSlow` / `factorFast` / fallback
combinator architecture.

## Next step

Continue HO-1 by replacing the current additive recombination internals with
BHKS CLD helpers: centred two-sided cuts, CLD coefficient computation, and the
all-coefficients lattice basis shape.

## Blockers

The full zero-sorry HO-1 bridge proof set is too large for this slice. The
pre-existing pod-managed `.claude/CLAUDE.md` worktree modification remains
untouched.
