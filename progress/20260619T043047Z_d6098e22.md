# HexArith Nat Prime grind automation coverage (#8035)

## Accomplished

Promoted the four equation-shaped public `@[simp]` lemmas in
`HexArith/Nat/Prime.lean` to `@[simp, grind =]`:

`choose_zero_right`, `choose_zero_succ`, `choose_succ_succ`, `choose_self`.

All four were re-read before promotion and are literal `lhs = rhs` forms.
`choose_succ_succ` remains an oriented Pascal unfold that strictly decreases
the arguments on the right-hand side, with terminal zero/self rewrites, so no
rewrite cycle was introduced.

## Current frontier

`lake build HexArith` completed successfully. `HexArith.Nat.Prime` rebuilt in
1.7s, with no grind-loop slowdown. `python3 scripts/check_dag.py` exited 0.
`git diff --check` was clean, and the added-line grep found no `sorry`,
`axiom`, `native_decide`, `TODO`, or `FIXME`.

## Next step

Open the PR for #8035.

## Blockers

None.
