# 2026-05-14 07:29 UTC — feature session (c0cf5a65) on #3892

## Accomplished

- Claimed #3892 and replaced the `sorry` in `factor_product_of_bound`
  with the planned assembly: `cases` on `factorFastWithBound f B`, fall
  back to `factorSlowWithBound_product` in the `none` arm and to
  `factorFastWithBound_product_of_some` in the `some` arm.
- Relocated `factor_product_of_bound` from line 4147 down past
  `factorSlowWithBound_product` (line 6014) and
  `factorFastWithBound_product_of_some` (line 6448) so both helpers are
  in scope. The new home is immediately after the fast wrapper, before
  the `checkIrreducibleCert_*` block.
- `lake build HexBerlekampZassenhaus.Basic`,
  `lake build HexBerlekampZassenhaus`, and
  `lake build HexBerlekampZassenhausMathlib.Basic` all succeed.
  `python3 scripts/check_dag.py` and `git diff --check` are clean.
- Only remaining `sorry` in `HexBerlekampZassenhaus/Basic.lean` is the
  pre-existing `isIrreducible_iff` placeholder at line 4135, tracked by
  #3969.

## Current frontier

- `factor_product_of_bound` is now complete. Downstream issue #3970
  (BZ Mathlib capstones) is no longer blocked on this leg; it remains
  blocked on #3953 and #3969.

## Next step

- Open the PR for #3892.

## Blockers

- The `hB` hypothesis is unused in the assembled proof — neither
  residual wrapper depends on the bound, so the linter emits an
  `unused variable` warning. The issue body explicitly forbids
  changing the statement, so I left it as-is.
