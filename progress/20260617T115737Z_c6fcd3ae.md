# Progress - 2026-06-17 (session c6fcd3ae, issue #7737)

## Accomplished

Claimed #7737 and repaired the linear Hensel Mathlib bridge prerequisites.
Strengthened `hensel_correct` and `hensel_degree` in
`HexHenselMathlib/Correctness.lean` with the real executable prerequisites:
`[Hex.ZMod64.PrimeModulus p]`, `hp : 1 < p`, and
`hgdeg : 0 < g.degree?.getD 0`.

Replaced the two target `sorry`s:
- `hensel_correct` now consumes `Hex.ZPoly.henselLift_congr_of_base` and
  transfers the executable congruence through
  `zpoly_congr_toPolynomial_map_eq`.
- `hensel_degree` now consumes a new executable bridge
  `Hex.ZPoly.henselLift_degree?_of_base` and transfers executable `degree?`
  preservation through `HexPolyMathlib.natDegree_toPolynomial`.

Added the executable theorem `henselLift_degree?_of_base` in
`HexHensel/Linear.lean`, with a private loop lemma proving the leading factor's
`degree?` is preserved by the linear Hensel loop under the same invariants used
by `henselLift_spec`.

Verification:
- `lake build HexHenselMathlib.Correctness` succeeded.
- `lake build HexHenselMathlib` succeeded.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` succeeded.
- Added-line forbidden-token scan over touched Lean files found no
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

The targeted top-level bridge `sorry`s are gone. `Correctness.lean` still has
the pre-existing later `sorry` in the multifactor section, outside #7737's
scope, and Lean still reports existing unused-variable warnings on
`hensel_extends`.

## Next step

Review or repair the remaining unrelated `Correctness.lean` multifactor `sorry`
through its own planned issue.

## Blockers

None for #7737. The worktree still contains a pre-existing unrelated
`.claude/CLAUDE.md` modification that was not touched or staged.
