# HexLLL firstShortVector identity benchmark (#2069)

## Accomplished

- Claimed feature issue #2069 after confirming dependency #2068 was closed.
- Added a deterministic `(n + 3) x (n + 3)` identity-basis fixture for `lll.firstShortVector` in `HexLLL/Bench.lean`.
- Registered `runFirstShortVectorIdentityChecksum` with a quadratic model, a fixed hot loop for signal-floor stability, and a verdict-eligible schedule.
- Verified `lake exe hexlll_bench list`, `lake exe hexlll_bench verify`, `lake exe hexlll_bench run Hex.LLLBench.runFirstShortVectorIdentityChecksum`, `lake build HexLLL`, `python3 scripts/check_dag.py`, `git diff --check`, and the banned-marker scan on `HexLLL/Bench.lean`.

## Current frontier

HexLLL now benchmarks the top-level `lll.firstShortVector` traversal on the degenerate BZ-style identity recombination basis. The direct run reports the declared quadratic model as consistent.

## Next step

Open a PR closing #2069 with the benchmark registration and this progress note.

## Blockers

None.
