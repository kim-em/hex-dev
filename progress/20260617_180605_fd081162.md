# Progress — 2026-06-17 18:06 UTC (session fd081162, /work → /review)

## Accomplished

Picked #7797 (review label) via `/work` judgment: the other two unclaimed items
(#6672 capstone, #7793 coverage producer) are both blocked on a missing
unconditional fast-BHKS substrate already diagnosed in prior sessions, whereas
#7797 is a self-contained read-only audit independent of the #2564/#6672 chains.

Completed the soundness audit of the FpPoly→Mathlib / Rabin transport surface in
`HexBerlekampMathlib/Basic.lean` and posted the verdict as a review comment:
https://github.com/kim-em/hex/issues/7797#issuecomment-4733774250

Findings (all three deliverables checked):
- **Direction soundness — sound.** `rabinTest_true_irreducible` (`:1053`) uses
  only the forward transport (`:673`), never the reverse; positive-degree comes
  from the executable test's own `0 < basisSize` check, so no vacuous-constant
  escape; `hmonic` is load-bearing.
- **Transport faithfulness — sound.** `fpPolyEquiv` (`:148`) is a genuine `≃+*`
  with all four obligations proved (no sorry); the essential `0 < natDegree`
  hypothesis on `irreducible_toMathlibPolynomial_of_fpPolyIrreducible` (`:448`)
  blocks the nonzero-constant-to-unit case; ZMod-p prime instance threaded via
  `primeModulus_of_fact`, not assumed.
- **gcd primitives — sound, and improved.** Associated/normalize used in the
  sound direction; #7778 *removed* a previously-sorried false exact-equality
  (`toMathlibPolynomial_gcd`, F₅ counterexample) and migrated consumers onto the
  associated primitives without weakening any to vacuity.

One scoped flag (known, outside the Rabin chain): `irreducible_of_mem_berlekampFactor`
(`:948`) ships a documented `sorry` (`:960`); its two consumers (`:966`, `:1017`)
have no local sorry but transitively depend on it. This is the Berlekamp-factor
leg, distinct from the audited Rabin transport surface (which is sorry-free).

## Verification

- `lake build HexBerlekampMathlib` → `Build completed successfully (2333 jobs)`,
  0 `error:`, only the one documented `sorry` warning at `:948`.
- No code changes (read-only audit). No PR.

## Current frontier

Audit closed; the Rabin/FpPoly/gcd transport surface is sound and `sorry`-free.

## Next step

The remaining Berlekamp-factor `sorry` at `:960` (every emitted Berlekamp factor
is `FpPoly.Irreducible`) is the natural follow-up; it gates the two
transitively-sorried consumers. Not in scope for this review issue.

## Blockers

None for this issue. The other two unclaimed items (#6672, #7793) remain blocked
on the missing fast-BHKS unconditional substrate.
