# 2026-05-10 23:08 UTC — work session (dc6ee410)

## Accomplished

- Claimed #3155 after the previous attempt PR #3170 was abandoned as
  unsalvageable on top of merged #3159.
- Added `yunFactorsContributionResidualComplete_of_derivativeZero` to
  `HexPolyFp/SquareFree.lean`, proving the reverse direction of the
  existing forward bridge `yunFactorsContributionResidualDerivativeZero_of_complete`.
  The proof mirrors the forward bridge's structure: structural induction
  on fuel with `by_cases` on `isOne c`. Together with the forward bridge
  this establishes `complete ↔ derivativeZero` for the recursive
  completion predicate added in #3159.
- Added `yunFactorsContributionResidualComplete_of_derivative_active`,
  the derivative-active provider deliverable: takes the recursion-tip
  `yunFactorsContributionResidualDerivativeZero c g multiplicity fuel`
  as the completion hypothesis and returns the structural
  `yunFactorsContributionResidualComplete c g multiplicity fuel`. The
  hypothesis has the precise shape that excludes the `fuel = 0`
  counterexample by carrying the derivative-zero claim for whatever
  fuel value the caller supplies.
- Added `yunFactorsResidualDerivativeZero_of_derivative_active` which
  chains the new provider through
  `yunFactorsResidualDerivativeZero_of_derivative_split_complete` to
  expose the concrete `yunFactorsResidualDerivativeZero` fact under the
  same hypothesis (deliverable 2).
- Verified:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
    shows no new banned markers (the two pre-existing sorries remain
    at lines 2695 and 4200 in the product reconstruction and tail-cross
    clusters).

## Current frontier

- The `complete ↔ derivativeZero` equivalence is now fully proved, and
  the derivative-active provider carries the completion fact through
  to the concrete `yunFactorsResidualDerivativeZero` shape downstream
  code needs.
- The remaining open work is the mathematical claim itself: showing
  that, for the derivative-active branch starting state, the eventual
  Yun loop residual either equals 1 or has zero derivative (Frobenius
  identity applied to the p-power part of the factorization). This is
  separate from the bridge work and was out of scope for this issue.

## Next step

- Use the new `yunFactorsResidualDerivativeZero_of_derivative_active`
  in the tail-cross proof for #3122, supplying `hresidual` from the
  caller (the deep math fact will be proved separately).

## Blockers

- None for this slice.
