# Progress: #6816 — assemble modPFactor_index_cover

## Accomplished

Landed the modP cover theorem requested by #6796 in
`HexBerlekampZassenhausMathlib/Basic.lean`, after the two sub-lemmas
(#6814 `mem_modPSubset_of_dvd`, #6815 `exists_factor_of_modPIndex`) and
#6796's subset-partition surface all merged.

- `modPFactor_index_cover core primeData hchoose i`: for each selected
  modular factor index `i`, produces an irreducible integer divisor
  `g ∣ core`, a representing subset `S`, `i ∈ S`, and
  `RepresentsIntegerFactorModP primeData g S`. Matches the issue's
  requested signature exactly (no `hcore_ne` parameter — derived
  internally). Assembled from `exists_factor_of_modPIndex` (recover `g`
  whose monic modP image the indexed factor divides),
  `exists_modPFactorSubset_of_modPSubsetPartition` (a representing subset
  for `g` via `modPSubsetPartitionHypotheses_of_choosePrimeData`), and
  `mem_modPSubset_of_dvd` (the divisibility forces `i ∈ S`).

- Three reusable `choosePrimeData`-shaped helpers, factored out of the
  facts previously built inline inside
  `existsUnique_modPFactorSubset_of_choosePrimeData_of_some`:
  - `core_ne_zero_of_choosePrimeData` (isGoodPrime keeps modP nonzero);
  - `toMathlibPolynomial_modPFactor_injective_of_choosePrimeData`
    (Nodup + `toMathlibPolynomial` injective);
  - `toMathlibPolynomial_modPFactor_monic_of_choosePrimeData`
    (each `factorsModP` entry monic, preserved by `toMathlibPolynomial`).
  These discharge the `hf_inj` / `hmonic` premises of
  `mem_modPSubset_of_dvd` from `choosePrimeData?` alone.

## Verification

- New declarations elaborate with **no errors and no warnings**. Lean
  reports every per-declaration error and continued past the pre-existing
  #6804 seam (the `unused variable hirr` warning at line 17725 proves
  elaboration reached declarations after the seam and before mine); the
  build log's only error is at 16087, far before my additions at ~17974.
- `python3 scripts/check_dag.py` exits 0.
- Forbidden-token grep over added lines: no `sorry` / `axiom` /
  `native_decide` / `TODO` / `FIXME`.

## Blockers

- `lake build HexBerlekampZassenhausMathlib.Basic` (and `.Mathlib`) does
  **not** produce a green build on current `main`: there is a pre-existing
  type-mismatch error at `Basic.lean:16087` in
  `scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none` — the
  consuming lemma still expects the `Hex.DensePoly.scale` candidate form
  while #6821 switched the candidate to `Hex.ZPoly.dilate`. This is the
  open `scale`→`dilate` migration owned by #6804, unrelated to and
  untouched by this change. Merge-gating CI does not compile
  `HexBerlekampZassenhausMathlib`, so the module is tolerated red on main
  and additive correct lemmas (e.g. #6831, #6825, and now #6816) continue
  to land. The module will build green once #6804 closes the seam.

## Next step

#6804 closes the `scale`→`dilate` seam, after which the whole module
builds. Downstream cover-side consumers (#6773, #6786) assemble
`LiftedFactorSubsetPartition` from this modP cover.
