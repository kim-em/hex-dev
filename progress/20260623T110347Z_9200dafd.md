# #8317 core-coordinate cap-separation tower — surface→glue tiers landed (one-shot `/once`)

Session type: feature (one-shot, `pod once 8317`). UTC 2026-06-23T11:03.

## Accomplished

Landed an additive, green, `sorry`/`axiom`/`native_decide`-free
**core-coordinate (M1) analogue of the `factorFastCapLiftData` cap-separation
assembly tower** in `Recovery.lean` (new section before `end BHKS`, 21
declarations). This is the surface→residue→glue portion of #8317's requested
`#8290`-style split.

Key realisation that made this mechanical: the existing `toMonicLiftData` tower
(`Recovery.lean:2901-3756`) is **generic over the lift data** — every proof
consumes the `cut` and `BadVectorBridgeData` *as hypotheses* and calls
lift-data-agnostic underlying lemmas (`capSeparationOfBridgeData`,
`projectedRowsOfLiftData_eq_trueFactorIndicatorLattice_of_cap`,
`ExecutableBadVectorWitness.*`). Moreover `(coreLiftData …).p`/`.k` are
**definitionally equal** to `(toMonicLiftData …).p`/`.k` (both come from
`precisionForCoeffBound` + `primeData`, not the monic target), and the witness
`.inputPolynomial` is `toPolynomial core` regardless of lift. So the port is a
verbatim duplication with `factorFastCapLift → factorFastCoreCapLift` and the
abbrev body `toMonicLiftData → coreLiftData`; the analytic estimates transfer
unchanged, as the issue predicted.

New declarations (all `HexBerlekampZassenhausMathlib.BHKS`):

- `factorFastCoreCapLiftData` (`private abbrev`) — `coreLiftData
  (normalizeForFactor f).squareFreeCore (factorFastPrecisionCap f) primeData`.
- `badVectorWitnessOfFactorFastCoreCapLiftData`.
- surface L2 tier: `factorFastCoreCapLift_input_l2norm_sq_le_coeffNormSq_add_one`,
  `_inputPolynomial_l2norm_le_coeffL2NormBound`,
  `_l2norm_product_lt_divisor_of_auxiliary_bound`.
- bridge-data extractors: `_auxiliary_eq_of_bridge_data`,
  `_trueFactor_liftedFactor_coeff_dvd_of_bridge_data`,
  `_selected_trueFactor_liftedFactor_coeff_dvd_of_bridge_data`,
  `_precision_separation_of_bridge_data`, `_localFactorDegree_pos_of_bridge_data`,
  `_coprime_input_aux_over_rat_of_bridge_data`,
  `_resultant_divisible_by_p_pow_of_bridge_data`,
  `_auxiliaryPolynomial_l2norm_sq_le_of_bridge_data`.
- `FactorFastCoreCapLiftAnalyticComparison` (structure) +
  `.ofCapSeparationHypotheses`, `.ofBridgeData`, `.ofL2normBounds`,
  `.ofBridgeDataCorrectedAuxiliaryL2normSq`.
- `no_bhks_bad_setup_of_factorFastCoreCapLiftAnalyticComparison`.
- **`capSeparationOfBridgeDataAtFactorFastCoreCapLift`** (`def`) — the headline
  producer: builds `ExecutableCapSeparationHypotheses
  (badVectorWitnessOfFactorFastCoreCapLiftData …) trueSupports` from the cut,
  bridge, prime positivity, and analytic comparison. This is exactly the
  `ExecutableCapSeparationHypotheses (badVectorWitnessOfLiftData f (coreLiftData …) …)`
  discharge the deliverable asks for.
- `projectedRowSpan_eq_trueFactorIndicatorLattice_of_factorFastCoreCapLift_bridge`.

Verification: `lake build HexBerlekampZassenhausMathlib` green (3784 jobs, 0
errors); the only `sorry` warning is the pre-existing
`factor_irreducible_of_nonUnit` (`FactorSoundness.lean:18`). `#print axioms` on
`capSeparationOfBridgeDataAtFactorFastCoreCapLift` and the `projectedRowSpan`
theorem = `[propext, Classical.choice, Quot.sound]` (clean cone). Additions are
Mathlib-layer-only; executable/conformance/bench untouched.

## Current frontier

The cap-separation hypotheses are now produced over `coreLiftData`. The
remaining **assembly** tier of #8317:

1. **Feed `ForwardRecoveryInputsCore.ofCapSeparation` (#8314).** That constructor
   wants `hcap : ExecutableCapSeparationHypotheses (badVectorWitnessOfLiftData f d
   rows_pos …) trueSupports` plus the B7/A2 fields (`expectedIndicators`,
   `indicators_match`, `nondegenerate`, `expectedFactors`, `hsize`, `hcandidate`,
   `product_eq`). My `capSeparationOfBridgeDataAtFactorFastCoreCapLift` supplies
   `hcap` (its witness unfolds to `badVectorWitnessOfLiftData core (coreLiftData
   core cap primeData) …`). Wiring detail to watch: `ofCapSeparation`'s `f` is the
   polynomial being factored, but the cap tower's witness input is `core =
   (normalizeForFactor f).squareFreeCore`, so the assembly instantiates
   `ForwardRecoveryInputsCore` at `core`, not the outer `f`; the B7/A2 fields must
   be produced for that `core`.
2. **Port the `factorFast_terminates_ofBridgeDataAndFactoredPaperThreshold(')`
   capstone** (`Recovery.lean:9904/10015`) to the core tower — it has many more
   hypotheses than the glue tier but is still generic assembly over the same
   underlying lemmas.

## Next step

Land the assembly tier as the follow-up sub-PR: a
`forwardRecoveryInputsCore_of_factorFastCoreCapLift_*` producer composing
`capSeparationOfBridgeDataAtFactorFastCoreCapLift` with
`ForwardRecoveryInputsCore.ofCapSeparation`, then the core `factorFast_terminates`
capstone. After that, #8304 re-points the ~36 M2 termination theorems
mechanically.

## Blockers

None for the landed brick. The assembly tier's only non-mechanical wrinkle is the
`f`-vs-`core` instantiation and producing the B7/A2 indicator/candidate fields
for `core` (these have landed producers elsewhere — `bhksIndicatorCandidatesCore?`
success, non-degeneracy — to be wired, not proved fresh).
