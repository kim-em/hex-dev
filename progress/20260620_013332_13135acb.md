# HexGFqMathlib Phase 7 — HexManual reference chapter (#8132)

## Accomplished

Wrote `HexManual/Chapters/HexGFqMathlib.lean`, the Phase 7 reference
chapter for the `HexGFqMathlib` bridge library, following the established
Verso pattern (`HexGFqRing.lean`, `HexModArithMathlib.lean`):
`#doc (Manual)`, `%%%tag%%%` blocks, `{docstring ...}` / `{name}` /
`{ref ...}` programmatic embeds pulled from source. Sections: Introduction
(what the Mathlib bridge gives — `Fintype`/cardinality/`Field` transport
and `GaloisField` identification), Encoders (`FpPoly.coeffIndex`,
`ofIndexBelowDegree`), Reduced representatives (`FiniteField.ReducedRep`,
`reducedRepEquiv`, `reducedRepFinEquiv`), Principal results
(`FiniteField.fintype_card`, `GFq.fintype_card_eq_pow`,
`card_eq_galoisField_card`, headline `equivGaloisField`), the packed
binary bridge (`Hex.GF2q.modulusFpPoly_eq_conway`, `equivGFq`), and
Cross-references (`{ref "hex-gfq-ring"}` to the `HexGFqRing` chapter).

Added `import` + `{include 0 ...}` for the chapter to `HexManual.lean`,
and bumped `libraries.yml[HexGFqMathlib].done_through` from 6 to 7.

## Current frontier

`lake build HexManual` green (2545 jobs); every embedded `{docstring}` /
`{name}` / `{ref}` resolves, so all referenced declarations exist and the
embedded code typechecks. The only linter warnings are pre-existing in
`HexGF2Mathlib/Basic.lean` (untouched). `scripts/check_dag.py` exit 0
after the bump. No `axiom`/`sorry`/`native_decide`; no computational
`hex-*` library gained a Mathlib dependency (all Mathlib use stays inside
`HexManual`).

## Next step

None for this issue. Sibling unclaimed Phase 6 grind sweeps remain:
#8141 (HexModArith/Basic.lean) and #8144 (HexGFq/Basic.lean inv_zero).

## Blockers

None.
