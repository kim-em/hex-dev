# Accomplished

- Updated the monorepo toolchain and its Verso/Mathlib dependencies to Lean
  `v4.33.0-rc1`.
- Removed the HexBasic list shims that are now provided by Lean core and made
  the dependent-array transports exposed by the new compiler explicit across
  the affected computational and Mathlib bridge libraries.
- Built the complete monorepo successfully: all 9,716 `lake build` jobs pass.
- Confirmed that the compatibility diff introduces no `axiom`, `sorry`, or
  `native_decide` declaration and passes `git diff --check`.

# Current frontier

- The Lean 4.33 compatibility branch is ready for its Hex-dev pull request and
  standalone-release validation.
- SOS's Mathlib-free staging branch is prepared locally but still pins the old
  HexMvPoly release until the compatible Hex mirrors are published.

# Next step

- Land the Hex compatibility pull request, run the release-sync dry run and
  publish the affected mirrors, then pin SOS to the resulting immutable
  HexMvPoly revision and validate/tag the staging branch.

# Blockers

- None.
