# 2026-08-04 — review of issue #9157 / PR #9172

## Accomplished

Reviewed the root-extraction branch, its reconstruction proof, every changed
public `berlekampFactor` theorem, the Rabin linear-factor consolidation, the
re-added `KernelWitness` helpers, downstream Mathlib consumers, budget
arithmetic, and runtime allocation behavior. Confirmed the existing public
theorem signatures were preserved and found no factorization soundness gap.

Ran targeted builds:

- `lake build HexBerlekamp HexBerlekampMathlib HexBerlekampZassenhausMathlib`
  completed successfully (9179 jobs).
- `lake build HexConformance` completed successfully (9322 jobs).

During the review, concurrent uncommitted workspace changes replaced the
`ZMod64.values p` filter with a tail-recursive scan and corrected the new
unconditional monic/irreducible claim. I reviewed those changes too and reran
the combined downstream/conformance build successfully (9409 jobs). Remaining
findings are the lack of a conformance guard that actually takes the new
branch, inaccurate older result docs, an over-public unchecked
`rootFactorsOf`/proof-helper surface, and partial rather than complete helper
consolidation.

## Current frontier

Review is complete. This session changed only this progress file; the three
source modifications visible in the worktree arrived concurrently and were
preserved.

## Next step

Commit the concurrent streaming-scan/doc fixes after adding a prime-29
fast-branch conformance guard and addressing the remaining API/doc cleanup.

## Blockers

None.
