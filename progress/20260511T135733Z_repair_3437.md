# 2026-05-11 13:57 UTC — repair session (PR #3437)

## Accomplished

- Claimed PR #3437 (`review: polish HexGF2 CLMUL API`) for repair after
  both the build and conformance jobs failed.
- Diagnosed: the failure was in `HexBerlekampZassenhaus/Basic.lean`,
  unrelated to the PR's HexGF2 CLMUL polish. PR #3413
  (commit `62e9f9d`) introduced the named step helper
  `bhksIndicatorCandidatesStep` but left
  `bhksIndicatorCandidates?_foldl_eq_some_append` stated in terms of the
  inline lambda, so `bhksIndicatorCandidates?_eq_some_of_getD` could no
  longer `rw [hfold]`. The bug is on `main` and affects every PR that
  contains the bad commit but not the fix.
- PR #3431 has the canonical fix (`9a527ad`). Cherry-picked just the
  `HexBerlekampZassenhaus/Basic.lean` change onto this branch (dropped
  the foreign progress file).
- Verified locally:
  - `lake build HexBerlekampZassenhaus.Basic` — clean (only pre-existing
    `sorry` warnings).
  - `lake build HexGF2.Clmul HexGF2` — clean.
  - `python3 scripts/check_dag.py` — clean.
  - `git diff --check` — clean.

## Current frontier

- Branch carries the original HexGF2 CLMUL polish commit plus the
  surgical BHKS fix needed to clear CI. Once PR #3431 lands, the
  cherry-picked patch becomes redundant; a rebase will drop it
  cleanly (identical patch).

## Next step

- Push `--force-with-lease`, call `coordination mark-pr-salvaged 3437`,
  let auto-merge handle the rest.

## Blockers

- None for the repair.
