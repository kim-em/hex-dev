# Session 27457258 — work #4094

## Accomplished

- Claimed #4094 and focused it on the small-mod singleton irreducibility bridge.
- Added `IntReductionMod.Hex_ZPoly_Irreducible_of_primeChoice_fModP`, exposing
  the existing mod-`p` Gauss transfer on the executable `PrimeChoiceData.fModP`
  surface.
- Added `IntReductionMod.squareFreeCore_irreducible_of_small_mod_singleton`,
  packaging the selected small-mod singleton core irreducibility theorem with an
  explicit modular irreducibility hypothesis that #4076 can later discharge.
- Verified `lake build HexBerlekampZassenhausMathlib.IntReductionMod`,
  `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`,
  `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the affected-file grep for `axiom`,
  `native_decide`, `TODO`, `FIXME`, and `sorry`.

## Current frontier

The branch theorem still takes `PrimeChoiceData.fModP` Mathlib irreducibility
as an explicit hypothesis. This is intentional until the final Berlekamp
singleton theorem from #4076 or its successors lands.

## Next step

After #4076 lands, specialize the explicit `hirr_fModP` hypothesis using the
Berlekamp singleton/no-kernel-split irreducibility theorem for the selected
modular factorization.

## Blockers

- No external blocker found for this bridge layer.
