# Progress 20260614T095103Z (session 717800fb)

Feature session on #7044 (deliverable 3: complete the fast-path Mathlib
soundness model migration from the additive `scale` coordinate to the
monic-transform `dilate` coordinate).

## Accomplished

- Landed the issue's named key artifact:
  `dilatedCenteredLift_of_representsIntegerFactorAtLift` in
  `HexBerlekampZassenhausMathlib/Basic.lean`. It derives the migrated
  dilated-centered-lift reconstruction equality
  `dilate (leadingCoeff core) (centeredLiftPoly (polyProduct selected) (p^k))
   = expectedFactor` from the new-model `RepresentsIntegerFactorAtLift`
  carrier. Verified: `lake build HexBerlekampZassenhausMathlib.Basic` green
  (0 errors), no `sorry`/`axiom`/`native_decide` in the addition.
- Confirmed the deliverable-3 premise is **sound on the monic-gated chain**:
  the fast-path recovery producer requires `hcore_monic`, so `leadingCoeff
  core = 1`, the dilation collapses to the identity, and the conversion from
  the `RecoveredAtLift` carrier (which only yields a `primitivePart`-wrapped
  `dilate_eq`) to the raw dilate shape goes through via
  `RecoveredAtLift.candidate_eq_of_monic_dvd` plus monic primitivity collapse.

## Current frontier

Recovery.lean is **not** green. The remaining work + one independent blocker
are scoped in the issue comment. In brief:

1. The scale→dilate shape migration must propagate from the producer
   (`bhksIndicatorCandidate?_representsIntegerFactorAtLift`, which I rebuilt as
   a `RecoveredAtLift` witness in a WIP patch) up through the
   `productCongruence*` family, the two `product_congr` structure fields
   (`CanonicalRecoveryTailInputs`, `CanonicalRecoveryInputs`), and ~6 chain
   theorems to the single break at
   `ofMignottePrecisionCanonicalIndicatorsExpectedFactorsAtPrecisionForCoeffBound`.
   The WIP producer + conversion edits are saved at
   `/tmp/recovery_wip_7044.patch` (not committed; Recovery reverted to clean).
2. **Independent pre-existing blocker:**
   `factorFast_ne_none_of_forwardInputs_on_schedule` (Recovery.lean) times out
   at `whnf`/`isDefEq` even at `maxHeartbeats 1000000` (5x default). Present in
   the baseline build before any of my edits — latent breakage unmasked now
   that Basic.lean is green. Needs its own heavy-defeq investigation (factor
   the `bhksRecover_eq_some_of_forwardInputs` / `factorFast_ne_none_of_core_
   recovery_on_schedule` step into a thin lemma), independent of the dilate
   migration. Until it is fixed, Recovery.lean cannot reach green regardless of
   the shape migration.

## Next step

Follow-up issue (or re-scoped #7044) should: (a) fix the
`factorFast_ne_none_of_forwardInputs_on_schedule` timeout first; (b) then apply
the WIP producer + `productCongruence*`→dilate family + structure-field
migration, consuming `dilatedCenteredLift_of_representsIntegerFactorAtLift`;
(c) then green up PartitionRefinement.lean (downstream of Recovery, unexamined).

## Blockers

The independent timeout (item 2 above) blocks a green Recovery.lean this
session; it is out of scope for the scale→dilate soundness deliverable.
