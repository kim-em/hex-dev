# 20260509T164108Z - Issues #2778 and #2818

## Accomplished

Claimed #2778, confirmed that #2808 had already landed inverse-permutation
membership/involution and product reindexing infrastructure, and decomposed the
remaining determinant-transpose work into:

- #2820 for inverse-permutation determinant sign preservation.
- #2821 for the final `det_transpose` fold assembly, dependent on #2820.

Released #2778 with the required `Decomposed into #2820, #2821` breadcrumb.

Claimed #2818 and produced `reports/bareiss-dependency-review.md`. The report
audits the current `Matrix.bareiss_eq_det` consumers, records that
Mathlib-free `HexGramSchmidt.Int` still needs a Bareiss-to-Leibniz surface, and
recommends superseding #2670 with ownership clarification plus a core proof
issue rather than reopening another removal attempt.

Verification completed:

- `git diff --check`
- `python3 scripts/check_dag.py`
- `rg -n "TODO|FIXME|native_decide|axiom" reports/bareiss-dependency-review.md HexMatrix/Bareiss.lean HexGramSchmidt/Int.lean HexMatrixMathlib/Determinant.lean HexMatrixMathlib/Determinant/Bareiss.lean`

## Current frontier

The Bareiss dependency split is now documented in a committed report. The core
technical blocker remains unresolved: either `Hex.Matrix.bareiss_eq_det` must
be proved in the Mathlib-free core, or the Gram-Schmidt determinant bridge
surface must be redesigned so it no longer needs that theorem.

## Next step

Create the PR for #2818. A planner should dispatch follow-ups for Bareiss proof
ownership and the Mathlib-free `bareiss_eq_det` proof if it accepts the report's
recommendation.

## Blockers

None for the review deliverable. Full removal of the orphan theorem-level
`sorry` remains blocked by the design decision recorded in the report.
