# Accomplished

- Added a Mathlib companion for the opaque centered operation
  `x ↦ 1/4 - (x - 1/2)^2` over the exact dyadic interval domain.
- Proved the package-owned forward theorem `[0,1] ↦ [0,1/4]` and exposed it
  through the generic packed fact-schema boundary.
- Ran a live session containing only the centered package, checked its exact
  retained action, fact event, payload entry, completion state, and installed
  interval, then replayed the quote into an ordinary real theorem.
- Added a negative replay case in which the required `[0,1]` premise is
  replaced by the whole line; the package schema rejects it.
- Added a compile-checked axiom report for the end theorem and documented the
  resulting arbitrary-function vertical in the interval SPEC.
- Passed focused builds, copyright, line-count, dependency, trust-surface,
  Phase 4, conformance-registration, diff, and factor-freshness checks.

# Current frontier

An arbitrary function now owns both its executable contractor and its exact
real-semantic replay theorem without adding a function case to the engine or
proof frontend. The current canary is deliberately one forward step.

# Next step

Obtain an independent exact-head review, then extend the exact interval domain
with outward-rounded arithmetic contractors and use the same package boundary
for a second function or a backward centered rule.

# Blockers

None.
