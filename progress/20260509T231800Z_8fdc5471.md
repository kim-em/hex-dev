# 20260509T231800Z - Issue #2892

## Accomplished

Claimed feature issue #2892 and added a minimal
`HexBerlekampZassenhaus/Bench.lean` smoke harness for the public BZ
factorization API:

- deterministic low-degree split input family for smoke verification;
- stable checksums for `ZPoly`, `Factorization`, and `Option Factorization`;
- `LeanBench` registrations for `factor`, `factorFast`, and `factorSlow`;
- `lakefile.lean` executable entry `hexbz_bench`.

Verification completed:

- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

The new benchmark file has no `sorry`, `axiom`, `native_decide`, `TODO`, or
`FIXME`. Existing imported modules still emit their pre-existing proof-hole
warnings during builds.

## Current frontier

HO-3 still needs the full Phase 4 benchmark matrix after HO-2 supplies the
adversarial fixture corpus. This PR only wires smoke-sized harness plumbing.

## Next step

Complete HO-2 adversarial BZ fixtures, then extend this harness into the full
degree, height, precision, modular-factor-count, and adversarial benchmark
suite required by #2566.

## Blockers

Full HO-3 completion remains blocked on HO-2 / #2565.
