# 20260602T113942Z 31b7eeae — Issue #6314

## Accomplished

- Claimed and completed issue #6314.
- Added automation coverage for the canonical `HexGFq` modulus-selection
  facts in `HexGFq/Basic.lean`:
  - `GFq.modulus_nonconstant` as `@[simp]`;
  - `GFq.modulus_irreducible` as `@[grind =>]`, with an explicit
    unfolded Conway-polynomial grind pattern;
  - `GFq.modulus_prime` with an explicit projection grind pattern, since
    the theorem conclusion has no usable application pattern for a bare
    `@[grind]` attribute;
  - `GF2q.degree_pos` and `GF2q.degree_lt_word` as `@[simp]`;
  - `GF2q.modulus_irreducible` as `@[grind =>]`.
- Added proof-mode conformance examples in `HexGFq/Conformance.lean`
  demonstrating the new `simp` and `grind` surface on the committed
  `(p, n) = (2, 1)` entry, plus an algebraic representative simp
  composition check.

## Current frontier

`HexGFq` now exposes automation for its canonical modulus facts alongside
the existing representative rewrite surface. The only non-attribute
detail is `GFq.modulus_prime`: Lean's grind attribute cannot infer a
pattern from `Hex.Nat.Prime p`, so the usable theorem registration is an
explicit `grind_pattern` on `h.prime`.

## Next step

Sibling Phase 6 issue #6319 for `HexPoly` Euclidean law projection
automation remains unclaimed. Older issue #6286 still depends on #6316
landing before its consumer refactor is sound to attempt.

## Blockers

None.

## Verification

- `lake build HexGFq.Basic` — green, with pre-existing replayed
  unnecessary-`simpa` lints in `HexGFqRing/Operations.lean`.
- `lake build HexGFq.Conformance` — green, with the same pre-existing
  replayed lints.
- `lake build HexGFq` — green; `HexGFq.CrossCheck` rebuilt during the
  first aggregate run and completed successfully.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token diff grep over `HexGFq/Basic.lean` and
  `HexGFq/Conformance.lean` found no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
