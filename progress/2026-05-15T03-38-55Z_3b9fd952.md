# 2026-05-15T03:38:55Z — feature session 3b9fd952

## Accomplished

Closed parent issue #3994 (HexGramSchmidt: assemble scaledCoeffArrayLoop
Cramer no-pivot invariant from step correspondence).

Added matrix-level helpers in `HexGramSchmidt/Int.lean` (near
`noPivotLoop_full_eq_borderedMinor_at_trailing`):

- `noPivotLoop_step_le_add`: the `step` field advances by at most
  `fuel`.
- `noPivotLoop_matrix_symm_preserve`: trailing-block symmetry of the
  state matrix is preserved by each Bareiss update step.
- `noPivotLoop_borderedMinor_swap_at_trailing`: for symmetric `M`, the
  trailing entries of `noPivotLoop` on the two bordered minors that
  differ only by swapping the border row/column agree.

Added GramSchmidt-specific bridges (after `dot_comm_int`):

- `gramMatrix_symm`: integer Gram matrix entries are symmetric under
  index swap.
- `scaledCoeffMatrix_eq_borderedMinor`: the Cramer determinant matrix
  for `(i, j)` is `borderedMinor (gramMatrix b) j _ ⟨j, _⟩ i`.
- `noPivotLoop_scaledCoeffMatrix_eq_borderedMinor_at_trailing`:
  the trailing no-pivot Bareiss value on `scaledCoeffMatrix b i j hji`
  equals the value on `borderedMinor (gramMatrix b) j _ i j` (border
  row/column swapped).
- `zeroRows_size` and `zeroRows_row_size`: initial coefficient buffer
  size lemmas to apply state-level invariants at the top-level.
- `scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix`: when the
  no-pivot Bareiss pass on `gramMatrix b` is non-singular through
  column `j`, the executable scaled-coefficient array entry below
  the diagonal at `(i, j)` equals the trailing entry of the no-pivot
  Bareiss-style loop on `scaledCoeffMatrix b i j hji`.

## Current frontier

The non-singular composite is exposed. The full assembly to
`scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss` (still `sorry`)
needs the singular-branch composite plus a bridge from the
fraction-free trailing value to `Matrix.bareiss` (or to `Matrix.det`).
Per the parent issue, neither was in scope; these remain for follow-up
issues.

## Next step

Successor issue: combine the non-singular composite added here with
the singular branch of `scaledCoeffArrayLoop_lower_matches` to give
the full state-level composite (handling both branches), and bridge
the no-pivot trailing value to `Matrix.bareiss` via row-pivot
agreement on symmetric positive-semidefinite Gram matrices.

## Blockers

None encountered.
