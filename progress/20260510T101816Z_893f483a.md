# HexGramSchmidt prefix row-span helper decomposition (#2974/#2981)

## Accomplished

- Read the latest progress note and followed `agent-worker-flow`.
- Claimed #2974, oriented in `HexGramSchmidt/Basic.lean`, and found that the
  final `basis_span` proof needed smaller row-combination plumbing first.
- Decomposed #2974 into #2981, #2982, and #2983, left the required
  `Decomposed into #2981, #2982, #2983` breadcrumb, and marked #2974 for
  replan.
- Claimed #2981 and added private prefix row-span algebra in
  `HexGramSchmidt/Basic.lean`:
  - `extendStrictPrefixCoeff`
  - `rowCombination_prefixRows_extendStrictPrefixCoeff`
- Verified:
  - `lake build HexGramSchmidt.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean`

## Current frontier

#2981 now provides the zero-extension embedding from strict-prefix row
combinations into inclusive prefix row combinations. The two original
`basis_span` sorries remain in `HexGramSchmidt/Basic.lean` for #2982/#2983.

## Next step

Create the PR for #2981. After it lands, #2982 can use the new helper plus the
existing reconstruction lemmas to prove `Hex.GramSchmidt.Rat.basis_span`.

## Blockers

None.
