# issue-8545: cross-system factorization benchmark suite

Session type: feature (issue #8545), branch `issue-8545`.

Six deliverables; this session lands the foundation (1 + 2) that unblocks the rest.

## Accomplished

- **Deliverable 1 (corpus).** `scripts/bench/gen_factor_corpus.py` +
  `bench/corpus/hexbz-factor-corpus.jsonl` (205 instances, degrees 3..1030
  across 10 families). Deterministic: regenerates byte-identically; `--check`
  diffs against the committed file. Swinnerton-Dyer built by exact integer
  `A^2 - p B^2` recurrence (no floats); classical families via sympy;
  Hoeij-Zimmermann coefficients vendored under `scripts/bench/vendor/hoeij/`
  and parsed from Maple format (needs no network at sweep time). Mix-doctrine
  `combined` flag caps each family at 15, spread across its degree range.
  Cross-checked: all 198 `expectedFactorDegrees` agree with python-flint.
- **Deliverable 2 (hex service + library change).**
  `bench/HexBench/FactorService.lean` (`lean_exe hexbz_factor_service`,
  `--entry factor|factorLattice|factorFast|factorClassicalNoDecline`) speaks the
  exact Isabelle line protocol. Verified round-trips against FLINT on sample
  instances; declines reply `{"ok":true,"result":null}`. Library change is
  additive and does NOT touch the CI-gated production path: new
  `scaledRecombinationFull` / `classicalCoreFactorsToCompletion` /
  `factorClassicalNoDecline` in `HexBerlekampZassenhaus/Basic.lean` reuse the
  proven recombination loops with the #8530 level-aware tightening removed.
  `scaledRecombinationSmart` is untouched (it is unfolded by a CI-gated Mathlib
  proof at `IntReductionMod.lean:7102`). Whole library + service build green.

## Current frontier

Foundation committed. Drivers (3), orchestrator (4), records (5), plots+report
(6) next.

## Two-Isabelle update (issue edited mid-session)

The issue now requires TWO verified Isabelle comparators: `isabelle-bz`
(existing `setup_bz_isabelle.sh`, unchanged) and `isabelle-lll` (new, AFP
`LLL_Factorization`). The latter is gated behind a build spike and may be
dropped if it fails; it does not block 1-2 or the other drivers. Note the name
collision: `scripts/oracle/setup_lll_isabelle.sh` already exists for LLL lattice
reduction (SVP), so the factorization driver is named distinctly.

## Next step

FLINT/PARI/NTL drivers, isabelle-lll scaffolding, orchestrator (6 systems + 4
hex entries), cactus plots, report, SPEC addendum. Run a real local
hex+FLINT(+NTL) sweep to produce a committed record + figures.

## Blockers

Full multi-system sweep + AFP `LLL_Factorization` build spike are carica tasks
(heavy AFP build; cypari2 not installed locally).
