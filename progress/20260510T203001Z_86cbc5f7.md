# 2026-05-10 20:30 UTC — work session (86cbc5f7)

## Accomplished

- Claimed #3117 and rechecked the corrected derivative-active square-free
  product invariant request against the current executable semantics.
- Added a focused characteristic-2 guard in `HexPolyFp/SquareFree.lean` for
  the shifted multiplicity case: `f = (X + 1)^6` over `F_2` currently
  decomposes to a weighted product whose coefficient list is not the input's
  coefficient list.
- Verified the partial guard with:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

- The requested no-executable-change proof target cannot close as stated:
  the public weighted-product theorem is false for the current executable
  multiplicity semantics, not merely missing a stronger private invariant.
- `HexPolyFp/SquareFree.lean` still has the pre-existing product-chain `sorry`
  and the separate pairwise-coprime cluster `sorry`.

## Next step

- Replan #3117 around one of two real choices: fix executable Yun
  multiplicity propagation after derivative-zero descent, or weaken/replace
  the public product theorem so it states the product actually emitted by the
  current algorithm.

## Blockers

- #3117 forbids changing executable square-free, gcd, `pthRoot`, and
  `weightedProduct` definitions while also requiring the public product theorem
  to build; the new guard shows those requirements are inconsistent.
