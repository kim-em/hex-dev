# 2026-06-18T23:14:57Z — feature session (#8008)

## Accomplished

Phase 6 grind automation coverage for `HexGF2/Basic.lean`. Promoted the six
clean equation-shaped public `@[simp]` lemmas to `@[simp, grind =]`:

- `words_ofWords_single_zero`
- `words_ofWords_pair_zero`
- `xorWords_size`
- `coeff_add_self_false`
- `coeff_add_zero_left_bool`
- `coeff_add_zero_right_bool`

Each is a literal `lhs = rhs`. The two iff-shaped lemmas
(`isZero_eq_true_iff_words_eq_empty`, `isZero_eq_false_iff_words_ne_empty`)
were deliberately left `@[simp]`-only, as the issue directs — `grind =`
applies only to equations, not biconditionals. No additional clean
equation lemma in the file fit the pattern beyond the listed six.

## Current frontier

`lake build HexGF2` → `Build completed successfully (52 jobs)`;
`HexGF2.Basic` rebuilt in 16s with no grind-loop slowdown.
`python3 scripts/check_dag.py` → exit 0. `git diff --check` clean; no
banned tokens in added lines. Scope confined to `HexGF2/Basic.lean`.

## Next step

Sibling Phase 6 issues remain unclaimed: #8010 (HexGFqRing Basic.lean),
#8012 (HexGF2 Multiply.lean).

## Blockers

None.
