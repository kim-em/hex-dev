# HexGF2 Phase 6: simp attributes on GF2nPoly val cluster

## Accomplished

- Claimed issue #5247.
- Added `@[simp]` to four operation-val wrappers in `HexGF2/Field.lean`:
  `mul_val`, `one_val`, `add_val`, `neg_val`. Statements and proofs unchanged.
- Verified `lake build HexGF2.Field`, `lake build` (244 jobs clean),
  `python3 scripts/check_dag.py`, and `git diff --check`. No simp regressions
  triggered in `HexGF2/`, `HexGfqField/`, `HexGfqRing/`, or `HexGfq/`.

## Current frontier

- The simp normal form for `.val` on packed `GF2nPoly` arithmetic now pushes
  through `*`, `1`, `+`, `-` (unary) automatically; `sub_val` remains a
  non-simp wrapper by design (char-2 collapse).

## Next step

- Open the PR for issue #5247.

## Blockers

- None.
