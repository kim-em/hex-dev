## Accomplished

Claimed #2920 and assembled the named reachable Yun pairwise invariant
provider in `HexPolyFp/SquareFree.lean`.

Added a private theorem
`yunFactorsPairwiseInvariant_of_derivative_split_reachable` that wraps
`yunFactorsPairwiseInvariant_of_derivative_split_common_dvd_one` by
supplying the `hcommon` argument from
`yunFactorsPairwiseReachable_common_dvd_one` (landed in #3069). The
boolean shape `(DensePoly.derivative f).isZero ≠ true` matches the
existing `_of_derivative_split_common_dvd_one` consumer.

Replaced the inline assembly in
`squareFreeAuxRev_pairwise_coprime_nil_core` with a call to the new
named theorem, removing the inline `hcommon` lambda.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- pre-existing unrelated `sorry` count in `HexPolyFp/SquareFree.lean`
  unchanged (3 holes, none in the assembled provider chain).

## Current frontier

The reachable provider is now named and reusable for any future
derivative-split call site.

## Next step

Future work continuing the Yun cluster (e.g. `pthRoot` tail
cross-coprimality at `yunFactors_squareFreeAuxRev_tail_cross_coprime`)
remains out of scope for this issue.

## Blockers

None.
