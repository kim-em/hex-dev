# 2026-05-21T06:15Z — work issue #5737

## Accomplished

Claimed #5737 and added the Barrett hot-loop smart constructor in
`HexModArith/HotLoop.lean`:

- `Hex.BarrettCtx.ofModulus`, building the wrapper from the indexed modulus
  and `1 < p`, `p < 2 ^ 32` side conditions;
- `[simp]` characterisation lemmas for the stored word modulus, stored
  modulus equality, and underlying Barrett reciprocal.

Replaced the manual Barrett wrapper records in `HexModArith/Conformance.lean`
with `Hex.BarrettCtx.ofModulus`. Left the Montgomery records untouched because
they are tracked separately by #5738.

Verified:

- `lake build HexModArith.HotLoop`
- `lake build HexModArith.Conformance`
- `lake build HexModArith`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexModArith/HotLoop.lean HexModArith/Conformance.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returns no matches

## Current frontier

#5737 is complete. Barrett hot-loop callers can now construct
`Hex.BarrettCtx p` without filling representation fields or directly naming
the lower-level `_root_.BarrettCtx.mk`.

## Next step

Handle #5738 with the analogous Montgomery smart constructor, or use the new
Barrett constructor in additional internal examples if desired.

## Blockers

None. The worktree still contains an unrelated pre-existing `.claude/CLAUDE.md`
modification, which this session left untouched.
