# Issue #6227: HexPolyFp normalized derivative-active provider substrate

## Accomplished

- Claimed #6222 after #6220 was claimed by another agent, read the SquareFree
  consumer chain, and found that replacing the false raw state provider is not
  a mechanical signature change: both weighted-product recursion and pairwise
  residual recursion rely on raw `squareFreeContributionReachable` facts.
- Decomposed #6222 into #6227, #6229, and #6230, posted the required
  `Decomposed into #6227, #6229, #6230` breadcrumb, and released #6222 with
  `coordination skip` for replan triage.
- Claimed #6227 and implemented the normalized-provider substrate in
  `HexPolyFp/SquareFree.lean`:
  - moved `normalizeMonic_squareFreeContributionReachable` and
    `normalizeMonic_squareFreeContributionPayload` before the derivative-active
    consumer block;
  - added `YunDerivativeActiveNormalizedStateProvider`;
  - moved `yunFactorsDerivativeActiveReachable_normalized_stateProvider` to the
    first point where `yunFactorsDerivativeActiveReachable_nonzero` is
    available;
  - added
    `pthRoot_normalized_valid_of_derivative_zero_nontrivial_of_monic` so later
    consumers can collapse normalized p-th-root validity back to raw `pthRoot`
    only under an explicit monicity hypothesis.

## Current frontier

- #6227 deliverables are complete.
- #6229 is the next dependency: refactor the weighted-product consumer chain
  to consume `YunDerivativeActiveNormalizedStateProvider`.
- #6230 remains blocked on #6229 and covers the pairwise/residual-provider
  chain.

## Next step

- Commit this change and open a PR closing #6227 via `coordination create-pr`.

## Blockers

- None.
- Verification passed: baseline `lake build HexPolyFp` before edits, then
  post-edit `lake build HexPolyFp`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-token diff grep over
  `HexPolyFp/SquareFree.lean` for added `sorry`, `axiom`, `native_decide`,
  `TODO`, or `FIXME`.
- `.claude/CLAUDE.md` was dirty before this session's code edits and was not
  touched or staged.
