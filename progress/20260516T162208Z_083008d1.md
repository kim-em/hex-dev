# Prefix-none assembler under LiftedFactorSubsetPartition (#4367)

## Accomplished

Landed `liftedFactorSubsetPartition_prefix_none` in
`HexBerlekampZassenhausMathlib/Basic.lean` after
`representedFactor_dvd_recombinationCandidate_of_subset`. This is the
prefix-none discharge for `hprefix` in
`recombinationSearchModAux_first_success_witness_of_liftedSubset_candidate_eq_factor_of_matches`:
every executable split enumerated before the canonical
`(liftedSubsetSelectedList d S, liftedSubsetSelectedList d (J \ S))`
boundary returns `none` under one step of `recombinationSearchModAux`,
provided `S` is the representing subset of an irreducible integer
factor at `J.min'`.

Proof structure:

1. `liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches` (the #4508
   wrapper now at `HexBerlekampZassenhausMathlib/Basic.lean:3306`)
   recovers `T ⊆ J` with `J.min' ∈ T`, the canonical split form for
   `split`, and a witness `i ∈ J ∩ S \ T`.
2. The inline `candidate'` is definitionally
   `recombinationCandidate d T`, exposed via `show`.
3. Case on `Hex.shouldRecordPolynomialFactor`:
   - `false`: the if-branch is `none` immediately.
   - `true`: case on `Hex.exactQuotient?`; the `some quotient'` branch
     is contradicted by
     `coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd`
     (the #4395 / PR #4498 capstone at L5475), which produces a
     representing subset `S_cov ⊆ T` with `J.min' ∈ S_cov`. The
     partition's `pairwise_disjoint` field (via the shared `J.min'`)
     forces `Associated factor f_cov`, then `unique_up_to_associated`
     collapses `S = S_cov`. Combined with `S_cov ⊆ T`, this contradicts
     `i ∈ S ∧ i ∉ T`.

The `hlocal_nodup : localFactors.Nodup` precondition is threaded
straight from the wrapper. The consumer in #4301 must supply this from
a Hensel-coprimality fact against the partition; a self-contained
`liftedFactor d`-injectivity-on-J-filter helper is left as a separable
sub-task.

## Current frontier

The prefix-none discharge is in tree. The recursive coverage proof for
`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
(#4301) can now consume both:

- `recombinationSearchModAux_first_success_witness_of_liftedSubset_candidate_eq_factor_of_matches`
  (the first-success witness at L4462)
- `liftedFactorSubsetPartition_prefix_none` (this PR)

to close the recursive step, modulo the `localFactors.Nodup` derivation
and the `recombinationCandidate d S = factor` equation discharge.

## Next step

For whoever takes #4301: the missing pieces are (a) a Nodup helper on
the matched `localFactors` at the recursive state, and (b) the
candidate-equals-factor equation that bridges
`recombinationCandidate d (cover_at_min subset) = factor`. Both are
self-contained sub-tasks if they prove hard.

## Blockers

None.
