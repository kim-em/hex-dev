# 2026-05-11T19:48 UTC — feature session 9f2e89f2

## Accomplished

Filled `gramDet_eq_prod_normSq_core` (issue #3575).

- In `HexMatrix/Determinant.lean`: added two helpers used by the new proof:
  - `Matrix.colReplace_self`: replacing a column by itself is the identity.
  - `Matrix.det_colReplace_add_otherCols`: adding a finite linear
    combination of other columns to one column preserves `det`.
- In `HexGramSchmidt/Int.lean`: added a tower of private helpers and the
  `_core` proof, all on top of partial PR #3576's triangular determinant
  prerequisites:
  - cast-bridge primitives: `foldl_intCast_add_aux`,
    `foldl_intCast_mul_aux`, `detSign_intCast`, `castIntDetMatrix`,
    `detProduct_intCast`, `detTerm_intCast`, `det_intCast`.
  - rational dot algebra: `dot_add_right_rat`, `dot_smul_right_rat`,
    `dot_zero_right_rat`, `dot_comm_rat`, `foldl_sum_start_rat`,
    `foldl_sum_congr_simple`, `foldl_mul_congr_simple`.
  - `dot_prefixCombination_right_rat` and its
    `dot_prefixCombination_right_eq_zero_of_dot_zero` corollary.
  - auxiliary matrix `auxMatrix b k hk` (the M_final from the issue plan),
    with `auxMatrix_zero_above` (lower triangular), `auxMatrix_diag`
    (`Vector.normSq` on the diagonal), and
    `auxMatrix_det_eq_prod_normSq` (via `det_lowerTriangular_eq_foldl_diag`).
  - interpolating `progressMatrix b k hk s`, with
    `progressMatrix_succ_eq_colReplace` (the entrywise col-op identity),
    `progressMatrix_succ_det` (det invariance per step via
    `det_colReplace_add_otherCols`), and `progressMatrix_det_invariant`.
  - cast bridge: `dot_castIntRow_eq_cast_dot`,
    `progressMatrix_zero_eq_castIntDetMatrix`,
    `gramDet_rat_eq_progressMatrix_zero_det`.
  - the new `gramDet_eq_prod_normSq_core` chains all of the above; the
    public `gramDet_eq_prod_normSq` now calls it.

The independence hypothesis `hli` is not used in the chain (both sides of
the equality are computed purely from `b`); it is suppressed with
`_hli` and a docstring note. Downstream callers (`basis_normSq`,
`gramDet_pos`) still take it.

## Verification

- `lake build HexGramSchmidt.Int` — clean (only pre-existing `sorry`s
  in `scaledCoeff` / `basis_normSq_core` / `normSq_latticeVec_ge_min_basis_normSq`
  and `HexMatrix.Bareiss`).
- `lake build HexGramSchmidt` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Int.lean` — empty.
- `git diff --check` — clean.

## Current frontier

`gramDet_eq_prod_normSq` is now `sorry`-free. The basis is laid for
`basis_normSq_core` (line 560) which can derive `Vector.normSq ((basis b).row k) =
(gramDet b (k+1)) / (gramDet b k)` directly from the product chain proven
here, but that is a separate issue.

## Next step

CI build of this PR and merge after #3576 (the prerequisite triangular
determinant + `gramDet_pos` PR) lands. Then a follow-up issue to fill
`basis_normSq_core` using the new chain.

## Blockers

None.
