# Work issue #3496 — HexMatrix deletion minor/cofactor API

## Accomplished

- Claimed parent issue #3490, assessed it as too large for one session, and
  decomposed it into #3496 and #3497.
- Claimed #3496.
- Added `skipIndex`, `deleteRowCol`, `cofactorSign`, and `cofactor` to
  `HexMatrix/Determinant.lean`, with focused entry/sign simplification lemmas.
- Verified `lake build HexMatrix.Determinant`, `lake build HexMatrix`,
  `python3 scripts/check_dag.py`, and `git diff --check`.
- Ran the banned-marker scan; it still reports only the pre-existing
  `HexMatrix/Bareiss.lean` proof hole.

## Current frontier

- The Mathlib-free determinant file now has the deletion minor and signed
  cofactor API needed by the Laplace expansion proof.
- Follow-up issue #3497 is blocked on #3496 and owns the row/column expansion
  theorem work.

## Next step

- Open the PR for #3496.

## Blockers

- None.
