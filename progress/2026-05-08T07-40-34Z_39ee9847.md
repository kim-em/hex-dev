## Accomplished

- Claimed issue #2673.
- Added `GramSchmidt.Int.scaledCoeffs_eq_scaledCoeffMatrix_det`, exposing the below-diagonal executable scaled coefficient as the corresponding Cramer determinant.
- Added `GramSchmidt.Int.leadingGramMatrixInt_det_eq_gramDet_int_of_nonneg`, isolating the remaining `gramDet` cast bridge to the nonnegativity of leading Gram determinants.
- Created follow-up issue #2676 for the unconditional leading Gram determinant nonnegativity/sign bridge and left a decomposition breadcrumb on #2673.
- Verified `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt.Update`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

- The Cramer determinant wrapper is available and builds.
- The unconditional theorem
  `leadingGramMatrixInt_det_eq_gramDet_int` is still blocked on a proof that every leading integer Gram determinant is nonnegative.

## Next step

- Prove `leadingGramMatrixInt_det_nonneg` in #2676, then derive the unconditional cast bridge and use it in the dependent size-reduction pivot issue.

## Blockers

- None for this partial slice.
