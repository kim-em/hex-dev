# 20260517T090335Z — replan session c5fb187c

## Accomplished

Triaged two `replan` issues to clear the queue.

**#4610 (HO-1 substrate, #4602 sub: Mathlib structural divisibility):**
worker-decomposed into #4617 and #4618 with explicit no-residual claim.
The decomposition comment states: "The full #4610 deliverable signature
is reproduced verbatim in #4618; together the two sub-issues cover
#4610's scope with no residual." Verified both sub-issues are open
(#4617: standalone char-0 `gcd(p, p') ~ divRadical p` helper; #4618:
Hex-specific bridging + Gauss descent + UFD pow conclusion, with
`depends-on: #4617`). Closed #4610 with forward-link comment per the
worker-decomposed-full-coverage disposition.

**#4561 (HO-1 substrate, exhaustive-branch arm umbrella):** narrowed
body. The four `factorsModP_*_of_factorsModPBerlekampForm` boundary
dischargers (#4567, #4570, #4573, #4574) that the original substrate
cluster #4563 was decomposed into have all landed since the issue was
last skipped. Worker session `283b7244` then attempted the assembly and
surfaced two genuine new substrate gaps:

* **Gap 1:** `hcore_monic` is not derivable from `f ≠ 0`. The original
  derivation list pointed at a non-existent
  `normalizeForFactor_squareFreeCore_monic`. `(normalizeForFactor
  f).squareFreeCore` has `0 < leadingCoeff` (via
  `Hex.squareFreeCore_leadingCoeff_pos_of_ne_zero`,
  `HexBerlekampZassenhaus/Basic.lean:8833`) but is not monic in
  general — counterexample `f = 2X + 1` gives leading coefficient 2.
  Resolving requires a wrapper relaxation refactor
  (`Monic` → `Primitive + 0 < leadingCoeff` in
  `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`,
  `Basic.lean:8573`, and its consumers), which is non-trivial substrate
  work.

* **Gap 2:** the umbrella's "irreducible for every recorded entry"
  conclusion needs `hcomplete : reassemblyExpansionComplete` to rule
  out the `repeatedPart` prefix entry (which is not irreducible in
  general — for `f = (X+1)^3`, `repeatedPart = (X+1)^2`). The sibling
  fast-path arm umbrellas (constant #4565, singleton #4564, quadratic
  #4571) take `hcomplete` as an explicit premise and discharge it via
  per-arm `reassemblyExpansionComplete_*_of_ne_zero` substrate. The
  exhaustive arm lacks an analogous discharger (no
  `reassemblyExpansionComplete_exhaustive_of_ne_zero`); building one
  is non-trivial because the exhaustive arm's `coreFactors` depends on
  the algorithm's recombination output rather than a fixed array
  shape.

Narrowed the issue body to land the umbrella with both gaps as
explicit premises (worker's option (a)), mirroring the sibling
fast-path arm umbrellas. Recorded Gap 1 and Gap 2 as out-of-scope
follow-ups in the issue body for the next planner to triage. Removed
the `replan` label so the issue returns to the unclaimed pool.

Did not create new `agent-plan` issues — per replan doctrine, fresh
planning is the next planner's job.

## Current frontier

`coordination list-replan` is empty.

## Next step

Pod will release the planner lock. The next planner can either:

1. Triage Gap 1 / Gap 2 from #4561's body and decide whether to (a)
   build the wrapper relaxation refactor (Gap 1), (b) build the
   exhaustive-arm reassembly-expansion-complete discharger (Gap 2), or
   (c) leave the two explicit premises as permanent shims.
2. Continue HO-1 capstone assembly with the now-claimable narrowed
   #4561 plus the landed sibling fast-path arm umbrellas (#4564,
   #4565, #4571).

## Blockers

None.
