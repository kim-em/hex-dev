# 20260519T001318Z — HexGF2 Phase 6 neg_val / sub_val wrappers

Session type: `/feature` worker on issue #5178.

## Accomplished

Added two `_val` wrappers to `HexGF2/Field.lean` in the `GF2nPoly`
namespace, immediately after `add_val` and before `zero_val`:

- `neg_val (a) : (-a).val = a.val` — proof is `rfl`, since `Neg`
  is wired directly to `neg` and `neg a := a` (characteristic 2).
- `sub_val (a b) : (a - b).val = (a.val + b.val) % f` — proof
  reduces `sub` to `add` and delegates to `add_val`.

Both are bare `theorem` (no `@[simp]`), consistent with the existing
`add_val` / `mul_val` / `one_val` shape (only the `rfl`-true
`zero_val` is `@[simp]`).

`lake build HexGF2.Field`, `lake build HexGF2`, and `lake build` all
pass. `python3 scripts/check_dag.py` exits 0. `git diff --check`
clean, no new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`.
No call sites elsewhere changed.

## Current frontier

`GF2nPoly` now exposes the full additive `_val` corner (zero, one,
add, neg, sub, mul). Downstream proofs that currently unfold
`Neg.neg` / `Sub.sub` by hand can switch to the canonical-form
rewrites.

## Next step

A natural Phase 6 follow-up is a sweep of `HexGF2/*` (and any
downstream callers) to migrate hand-unfoldings of negation/subtraction
on `GF2nPoly` to the new wrappers — analogous to the `HexGfqField`
migration that followed PR #5162.

## Blockers

None.
