# 20260510T191546Z — feature session 98166a8b

## Accomplished

Claimed #3098 and corrected the individual-factor square-free theorem chain in
`HexPolyFp/SquareFree.lean` to use normalized executable gcds:
`(normalizeMonic (DensePoly.gcd factor (DensePoly.derivative factor))).2 = 1`.

Proved `yunFactorsStepsSquareFree_of_derivative_split` by deriving the step
predicate from the existing reachable Yun state invariant. The proof now uses
the current-state square-free divisor criterion, passes through
`squarefree_factor_of_squarefree`, and normalizes the raw executable gcd via
`normalizeMonic_eq_one_of_dvd_one`.

Added a focused `#guard` for the existing `polyFive #[1, 1, 1]` counterexample
fixture, checking that every emitted factor has normalized derivative gcd `[1]`.
Updated `SPEC/Libraries/hex-poly-fp.md` with the minimal clarification that
the raw gcd square-free contract is stated after monic normalization.

Verification run:
- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `lake build HexBerlekamp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

`HexPolyFp/SquareFree.lean` still has the pre-existing unrelated proof holes at
the current lines 2675 and 3802.

## Next step

Open the PR for #3098 and let CI validate the normalized-gcd theorem statement
against downstream users.

## Blockers

None.
