# Session c004a87a — work #4099

## Accomplished

- Read the latest progress entry and oriented against `PLAN.md`,
  `PLAN/Conventions.md`, and the relevant SPEC context.
- Claimed #4096, found it was blocked because its prerequisite scalar
  `FpPoly.eval_add` / `eval_sub` layer is still only on open PR #4097, and
  released it for replan with that blocker.
- Claimed #4071, diagnosed the lower-column sync as two separable proof
  obligations, decomposed it into #4099 and #4100, and released #4071 for
  replan.
- Claimed #4099 and added
  `scaledCoeffArrayLoop_lower_matches_start_column`, packaging the existing
  current-column capture theorem in the pre-step `Matrix.noPivotLoop 0` form
  needed by the target-column recursion.
- Verified `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested grep
  for `axiom` / `native_decide` / `TODO` / `FIXME` / `sorry`.

## Current frontier

#4099 now has the base target-column theorem for the case where the target
column is exactly the starting step. The full non-singular recursive theorem
still needs to advance from the starting step to an arbitrary later target
column and identify the captured entry with the corresponding pre-step
`Matrix.noPivotLoop (j - step)` matrix.

## Next step

Continue #4099 by proving the induction over the distance from
`state_matrix.step` to the target column `j`, using the new start-column
theorem at the capture point and
`getArrayEntry_scaledCoeffArrayLoop_preserve_col_before_step` for the
remaining fuel.

## Blockers

- #4100 remains blocked on #4099.
- #4096 should wait for PR #4097 or be replanned to depend explicitly on it.
