# PNT+ BKLNW exponential interface

## Accomplished

- Audited the exact pinned `LeanCert.CertifiedBounds.BKLNW` use in
  `BKLNW_a2_bounds.lean` and corrected the stale count: eleven lower/upper
  pairs, hence 22 declarations, at arguments 20, 25, 30, 35, 40, 43, 100,
  150, 200, 250, and 300.
- Added one Mathlib-free source-indexed certificate schema. It authenticates
  the argument, logarithmic floor, explicit-band/tail split, twelve-decimal
  base scale, both source endpoints, and the complete lower/upper natural
  rational inequalities.
- Proved all 61 base enclosures from an exponential Taylor remainder, the
  logarithmic floors from the package-owned 20-digit log-2 series, exact and
  tail finite-sum bounds, and one generic ordinary-kernel certificate theorem.
- Added exact source-shaped wrappers for all 22 declarations without importing
  LeanCert or using `native_decide`.
- Routed a representative row through generic package planning, chronology,
  replay, and `ProofFrontend`; all eleven rows run and replay through the same
  parameterized schema, while cross-row reuse rejects.
- Added decoded mutations for source index, argument, floor, split, tail start,
  tail cardinality, scale, and each endpoint, plus independent false lower and
  upper endpoint theorems and guarded axiom reports.
- Reclassified the source-pinned imported BKLNW role as accepted after a
  localized PNT+ rewrite while explicitly declining namespace/drop-in parity,
  corrected the SPEC count, documented fixed-provider/resource limitations,
  and refreshed the exact proof-only Lake exemption.
- Built both experiment libraries and the complete conformance target (9,468
  jobs), and passed source-pinned inventory verification, 26 inventory unit
  tests, static checks, trust-surface and Mathlib-free-bench checks,
  factor-sweep freshness, ban grep, and `git diff --check`.

## Current frontier

The full theorem role actually used from pinned
`LeanCert.CertifiedBounds.BKLNW` now has package-owned ordinary-kernel Hex
replacements: eleven power upper bounds and 22 exponential lower/upper bounds.
The exponential provider remains a fixed eleven-row acceptance package.

## Next step

Preserve this local stack until the public power/powWithin landing sequence is
complete, then rebase the ladder plus exponential feature onto the exact new
main and recompute the Lake freshness exemption before review and CI.

## Blockers

None for the source-pinned interface. Arbitrary arguments and requested base
precision, cached or balanced folds, a bit-complexity resource model, and the
separate BKLNW tactic/Table 10 workloads remain future work.
