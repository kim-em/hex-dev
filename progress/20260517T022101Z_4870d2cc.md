# 20260517T022101Z — planner session 4870d2cc

## Accomplished

`POD_QUEUE_DEFICIT=1` on entry (two unclaimed: #4551, #4334).
Created one new atomic sub-issue:

* **#4553** — *HO-1 substrate: bridge `Squarefree (toPolynomial
  squareFreeCore)` from `SquareFreeRat` + primitivity*
  (`agent-plan`, `feature`).

This closes the last "please supply from your own invariants"
hypothesis on the HO-1 outer-bound substrate constructors landed in
the #4543 / #4549 chain. The bridge mirrors the existing primitivity
sibling `normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive`
(`HexBerlekampZassenhausMathlib/IntReductionMod.lean:645`, landed in
#4544) one-for-one: same file, same input premise shape (`f ≠ 0`),
same routing pattern through the executable `SquareFreeRat` invariant
on `(primitiveSquareFreeDecomposition (extractXPower …).core).squareFreeCore`,
just landing in `Squarefree` instead of `IsPrimitive`. Deliverable also
includes a one-line rewire of
`liftedFactorSubsetPartition_outerBound_of_choosePrimeData`
(`HexBerlekampZassenhausMathlib/Basic.lean:8123`) to drop its explicit
`hcore_sqfree` hypothesis.

### Orient summary

* `gh pr list` shows two open PRs:
  * #4552 (agent PR for #4551) — checks `IN_PROGRESS`, not yet
    mergeable.
  * #2656 (`spec/real-roots-and-sturm`, author `kim-em`, `status:
    planned`, 9 days old, no labels) — left untouched; this is a
    human-authored spec PR with no agent-plan trail.
* No broken PRs (no `CONFLICTING`, no `FAILURE` checks).
* No new directive without `has-pr`; six standing directives (#2564,
  #2567, #2637, #3662, #3899, #3900) all currently parked behind
  sub-issues or out-of-scope substrate.
* No `replan`-labelled candidates.

### Why this issue and not the alternatives

After reading the last five progress files, the two natural next
substrate items both come from the explicit "Next step" notes in the
most-recent worker sessions:

1. **The HO-1 partial assembly for #4170's slow-path arm.** Mentioned
   in `progress/20260517T104500Z_1a61b25e.md` and
   `progress/20260517T014435Z_e95f4d5d.md`. *Not chosen.* This still
   requires the `hcore_sqfree` discharge (it threads through the
   outer-bound constructor it consumes) and several other per-branch
   dispatch discharges. Better split: the squarefree bridge lands
   first so the assembly can use a cleaner outer-bound surface; the
   next planner picks up the assembly.

2. **The `Squarefree (toPolynomial squareFreeCore)` bridge.** Mentioned
   in `progress/20260517T014435Z_e95f4d5d.md` "Next step" §2.
   *Chosen.* Sibling pattern (`isPrimitive` variant) already in place;
   small, atomic (one new theorem plus a one-line rewire); no
   transitive blocking on directive #2567.

`POD_QUEUE_DEFICIT=1` so exactly one new issue.

## Current frontier

The HO-1 outer-bound substrate layer is one rewire away from being
fully `choosePrimeData`/`normalizeForFactor`-discharged. After #4553
lands:

* The slow-path arm of `factor_irreducible_of_nonUnit` (#4170) has all
  outer-bound substrate dischargeable from executable invariants,
  modulo the two analytic `sorry`s tracked by #2567 (BHKS).
* The small-mod singleton arm has both `hprim` and `hlc_map_ne`
  dischargeable (from #4544); the residual `hirr_fModP` (Berlekamp
  singleton irreducibility) remains out of scope here.
* The BHKS fast branch substrate (`trueSupports` / `htrue` /
  `hpartition`) is still blocked on directive #2567.

## Next step

The next planner cycle (triggered when the queue drops below
`min_queue=3` again) should consider:

* A partial HO-1 assembly issue for `factor_irreducible_of_nonUnit`
  that composes the now-cleaner outer-bound substrate constructors into
  the slow-path arm, leaving the BHKS branch and remaining analytic
  `sorry`s behind `sorry` placeholders (per #4170's "deliverable 4
  forbids new sorrys" — so this would need to be re-scoped as a
  partial deliverable, not closing #4170 itself).

* The per-branch dispatch hypothesis discharges in `factorWithBound`
  (`LiftedFactorSubsetPartition` and monicness / degree-positivity /
  coefficient-bound side conditions enumerated by the #4537 wrapper),
  if any of those are non-trivially blocked.

## Blockers

None. Pool sizing (`set-target`) untouched: active development is
still throwing off substrate sub-issues regularly, and the dispatcher
will refill the queue at the next `min_queue` dip.
