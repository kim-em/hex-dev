# Session 20260604T234939Z (78d85c63) — #6541 standalone integer trial-division core

## Accomplished

Closed #6541 by adding `exhaustiveIntegerTrialCoreFactorsWithBound`,
the standalone executable for `factorSlow`'s exhaustive arm.

- Definition in `HexBerlekampZassenhaus/Basic.lean`: peels integer-root
  linear factors via `splitIntegerRootFactorsAux`, then enumerates
  candidate polynomials of degrees `1..deg(residual)/2` with
  coefficients in `[-B, B]` and divides each via `exactQuotient?`.
- Helpers: `boundedIntegerList`, `boundedCoefficientVectors`,
  `trialDivisionCandidatesOfDegree`, `trialDivisionCandidatesUpTo`,
  `trialDivisionPeelAux`. Candidate generation filters to
  positive-leading-coefficient recordable polynomials at construction
  time so the peel emits only normalized recordable factors by
  membership.
- Local executable invariants proven:
  - `exhaustiveIntegerTrialCoreFactorsWithBound_polyProduct`
    (unconditional)
  - `exhaustiveIntegerTrialCoreFactorsWithBound_normalizeFactorSign`
    (requires `0 < leadingCoeff core`)
  - `exhaustiveIntegerTrialCoreFactorsWithBound_shouldRecord`
    (requires `0 < leadingCoeff core`)
- `factorSlowFactorsWithBound` call sites unchanged, per the issue scope.

## Current frontier

Helper and its three local invariants land. The downstream rewiring
of `factorSlowFactorsWithBound` (the original parent #6530) is the
next step.

## Next step

Open a planner directive to wire `factorSlowFactorsWithBound`'s
square-free-core fallback through
`exhaustiveIntegerTrialCoreFactorsWithBound`, replacing the
`choosePrimeData` / `exhaustiveCoreFactorsWithBound` route. The
`factorizationOfFactors` product proof can be discharged via the
new `_polyProduct` theorem; the per-factor recorded/normalized
invariants are now available too.

## Blockers

None.
