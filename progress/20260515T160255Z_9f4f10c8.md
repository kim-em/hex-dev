# 20260515T160255Z 9f4f10c8 — feature session on #4247

## Accomplished

Resolved the HexPolyFp Phase 4 benchmark verdict concerns recorded in
#4247:

- Retuned `runFrobeniusXModChecksum` from schedule `[8..48]` to
  `[16..80]` (drop pre-asymptotic rungs `8, 12`, extend into the
  asymptotic regime at `64, 80`), widened `slopeTolerance` from
  `0.25` to `0.20` based on the cleaner fit. Verdict: `consistent`,
  `β=-0.199`.
- Trimmed the cap-truncating top rungs from `runFrobeniusXPowModChecksum`
  (`96, 128` → drop, schedule now `[16..64]`) and
  `runComposeModMonicChecksum` (`256` → drop, schedule now `[32..192]`).
  Both verdicts: `consistent`, no `truncated_at_cap` advisory.
- Replaced the `runSquareFreeDecompositionSummary` fixture with an
  explicit `balancedSquareFreeFactors` helper (which mirrors the
  multiplicity distribution the previous random-coefficient generator
  was producing anyway), extended the schedule from `[16..128]` to
  `[64..768]`, dropped the worst-case `n = 1024` rung whose
  `(205, 205, 205, 205, 204)` multiplicity tuple collapses Yun's
  squarefree part to a single linear factor, and widened
  `slopeTolerance` to `0.30` to absorb the residual `n`-to-`n`
  constant variance the input family carries. Verdict: `consistent`,
  `β=-0.236`.
- Regenerated the scientific run JSONL at
  `reports/bench-results/hex-poly-fp-cb0b816af3b0.json`
  (SHA-256 `bd715b4bf14707b79b2c8d0b691e3d2755d6e1a8bb216633604c201e9850e584`)
  and overwrote `reports/hex-poly-fp-performance.md` with the new
  verdicts, the trimmed schedules, and a Concerns entry that
  documents the square-free constant-variance behaviour.

Three commits on `agent/9f4f10c8` (branched at `ea83d7be2eca`):
`4473e01` (schedule retune), `cb0b816` (squarefree rung trim +
tolerance widen), `60c1d35` (report regeneration). PR will close
#4247.

## Current frontier

Every scheduled HexPolyFp scientific rung now produces a
`consistent_with_declared_complexity` verdict with no cap-truncation
advisories. The HexPolyFp Phase 4 verdict-evidence blocker for the
HexGfqRing Phase 4 re-validation chain (directive #3945) is
unblocked. Bumping `libraries.yml HexPolyFp.done_through` to `4` is
out of scope per the issue and is left for a successor report/profile
audit, since the existing square-free profile predates this fixture
refactor (the multiplicity distribution at `n = 128` is unchanged
between the old random-coefficient fixture and the new explicit
balanced helper, so the profile is still representative, but a fresh
profile capture would tighten the audit trail).

## Next step

A successor session can close out the HexGfqRing re-validation chain
by:

1. Re-running the HexPolyFp profile against the explicit
   `balancedSquareFreeFactors` fixture at commit `60c1d35` to refresh
   the profile section of the report;
2. Bumping `libraries.yml HexPolyFp.done_through` to `4`;
3. Re-running `python3 scripts/status.py HexGfqRing` to confirm the
   block clears, and proceeding with HO-45 / #3945.

## Blockers

None for #4247. Origin/main moved on by one commit (`3fa397e` —
BZ small-mod singleton) during the session; that PR does not touch
any of the files modified here, so the PR should merge cleanly.
