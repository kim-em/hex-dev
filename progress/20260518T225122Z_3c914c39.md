## Accomplished

- Claimed feature issue #5146 after confirming directive issues were blocked and the oldest benchmark issue was unsuitable on the current loaded host.
- Added docstringed dense-constructor API lemmas in `HexPoly/Dense.lean`:
  `size_C_zero`, `size_C_of_ne_zero`, `isZero_C_eq_true_iff`,
  `monomial_zero`, `size_monomial_of_ne_zero`,
  `isZero_monomial_eq_true_iff`, and
  `isZero_monomial_eq_false_of_ne_zero`.
- Kept the change scoped to proof/API polish; no constructor or runtime behavior changed.

## Current frontier

Issue #5146 deliverables are complete.

Verification run:

- `lake build HexPoly.Dense`
- `lake build HexPoly`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git grep -n "size_C_zero\\|size_C_of_ne_zero\\|isZero_C\\|size_monomial\\|isZero_monomial\\|monomial_zero" HexPoly/Dense.lean`
- `git diff -- HexPoly/Dense.lean | rg -n "\\bsorry\\b|\\baxiom\\b|native_decide|TODO|FIXME"` returned no matches.

## Next step

Publish the PR closing #5146.

## Blockers

None for #5146. The worktree still has a pre-existing `.claude/CLAUDE.md` modification that this session did not touch.
