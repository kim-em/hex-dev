# 2026-06-04 — Decompose #6533 (Mathlib-free non-singular Schur ≡ Bareiss workhorse)

Session: `4870970b`, issue
[#6533](https://github.com/kim-em/hex/issues/6533) (parent),
sub-issues
[#6535](https://github.com/kim-em/hex/issues/6535),
[#6536](https://github.com/kim-em/hex/issues/6536),
[#6537](https://github.com/kim-em/hex/issues/6537),
[#6538](https://github.com/kim-em/hex/issues/6538).

## Accomplished

Decomposed #6533 into a 4-piece dependency chain after diagnosing a
canonicity-exposure infrastructure gap that the directive's proof
sketch implicitly relied on.

- **#6535 (Sub-A)**: expose canonicity of
  `bareissGramRowInvariant_noPivotLoop_initial`. The existing aux
  threads `IsCanonicalAt` through its recursion but does not return
  it. Recommended refactor: bundle canonicity into the aux's return
  type via `PSigma`, then `_initial.1` / `_initial_canonical.2`.
- **#6536 (Sub-B)**: `noPivotLoop_initial_gram_bareiss_step_dvd` —
  matrix-level Bareiss step divisibility consuming the canonicity
  helper to apply `StepWitness.Cell.coeff_num_eq_mul`.
- **#6537 (Sub-C)**: `schurSigma_foldl_eq_noPivotCorrection` — the
  σ-fold ≡ noPivot correspondence via internal induction on `q`,
  consuming the divisibility helper at the successor step.
- **#6538 (Sub-D)**: the main theorem
  `getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing`,
  closing the workhorse target via outer strong induction on `q`.

Dependency chain: #6535 → #6536 → #6537 → #6538. Each sub-issue's
deliverable signature and proof shape are documented in its body
(referencing existing helpers in `HexGramSchmidt/Int.lean` by line
number).

Spent the bulk of the session attempting Sub-A directly. The aux at
[HexGramSchmidt/Int.lean:4099](../HexGramSchmidt/Int.lean#L4099) was
refactored to return a Σ' bundle of `(invariant, canonicity)`. The
zero, done, and regular branches went through; the singular branch
needs a `bareissGramCanonicalCoeff_eq_of_singular` stability lemma
that propagates `bareissGramCanonicalCoeff_succ_singular` through
`fuel` iterations of a singular fixedpoint. The proof requires
deriving `(noPivotLoop (elapsed + j) init).matrix[.step][.step] = 0`
from the same fact at `elapsed`, going through the state-stability
lemma based on `noPivotLoop_id_at_singular_fixedpoint` (Bareiss.lean
line 1201). This step gets stuck on dependent-typing issues around
the matrix-index proof: `rw` on Fin- or Nat-indexed matrix entries
hits "motive is not type correct" errors when the index value is
substituted but the index bound proof depends on the substituted
value. Tactics tried: `rcases` on the matrix equation (fails because
LHS is not a free variable), `rw [idxEq, h_matrix]` after let-binding
the Fin indices, term-level `h_step ▸ h_matrix ▸ hp` (`▸` cannot
compute the motive), explicit Fin-form intermediate. Reverted the
in-progress refactor on revert; the working tree is clean against
`origin/main`.

Verified:

- `lake build HexGramSchmidt.Int` clean after revert.
- `grep -c sorry HexGramSchmidt/Int.lean` = 3 (unchanged).
- Parent #6533 skipped with breadcrumb; sub-issues #6535–#6538
  created with linear `depends-on` chain and `blocked` labels on the
  downstream three (auto-applied by `coordination plan`).

## Current frontier

Sub-A (#6535) is the next unblocked piece. The recommended approach
captured in its body: refactor the aux's return type to a Σ' bundle,
fold canonicity tracking into each branch case, and handle the
singular branch's stability via a separately-proved
`noPivotLoop_state_eq_of_singular` state-stability lemma that takes
state equalities (rather than indexed-entry equalities) to avoid the
dependent-typing obstacle in matrix-index rewriting. Working entirely
in Fin-indexed form for the diag-zero conversion and supplying the
Nat-indexed callee form by direct term equality
(rather than `rw`) is also worth trying — the sub-issue body flags
the obstacle so the next worker is not surprised.

## Next step

A successor session claims #6535, lands the canonicity exposure,
then the rest of the chain unblocks one at a time.

## Blockers

None on the chain itself. The dependent-typing obstacle on
matrix-index rewriting is the *technical* hurdle inside #6535 but
is not a project-level blocker — multiple workable approaches are
sketched in the sub-issue body.

## Commits

None (working tree reverted to `origin/main` after the in-progress
refactor stalled). Progress entry covers the decomposition and the
captured proof-sketch artifacts in the sub-issue bodies.
