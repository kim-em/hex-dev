# Progress — 2026-06-18 (session 3675f04d, /work → /feature #7978)

## Accomplished

Completed #7978: discharged the `hfloor` open hypothesis
`cldCoeffFloor (normalizeForFactor f).squareFreeCore ≤ factorFastPrecisionCap f`
threaded through the `factorFast ≠ none` determinism chain in `Recovery.lean`.

Premise sanity-check first (per CLAUDE.md "Directives are hypotheses"): pure-Nat
`#eval` over ~20 non-monic / non-squarefree / large-leading-coeff `f` confirmed
the inequality holds with many orders of magnitude of slack, and that
`coeffNormSq core ≤ coeffNormSq f` empirically (normalization + squarefree-core
reduce the norm). Premise sound → executed.

### New executable lemmas (`HexBerlekampZassenhaus/Basic.lean`)

Public faces of the private `transformedCore` machinery, needed across the
Mathlib boundary:
- `toMonic_monic_coeff_of_leadingCoeff_ne_one` — coefficient law for the monic
  transform in the genuine-transform branch.
- `toMonic_monic_degree_getD` — monic transform preserves recorded degree
  (unconditional).
- `toMonic_monic_size_of_pos_degree` — stored size = `deg + 1` for positive
  degree.

### New Mathlib-layer lemmas (`HexBerlekampZassenhausMathlib/BHKSBound.lean`)

Explicit floor bound (commit b062e263):
- `toMonic_monic_coeff_natAbs_le`: each monic-transform coefficient has natAbs
  `≤ D^n` (`D = defaultFactorCoeffBound f`, `n = deg core`), routing the
  `a_i·c^(n-1-i)` blow-up through the existing Mignotte divisor bound
  `defaultFactorCoeffBound_valid` + `core ∣ f`.
- `coeffNormSq_toMonic_monic_le`, `coeffL2NormBound_toMonic_monic_le`,
  `bhksCoeffBound_toMonic_monic_le`, `cldCoeffFloor_le_explicit`
  (`cldCoeffFloor core ≤ 2·(2^(n-1)·n·((n+1)·D^n))`).
- Nat helpers: `choose_le_two_pow`, `hex_choose_eq`, `hex_choose_le_two_pow`,
  `ceilSqrt_le_of_le_sq`, `foldl_add_le`, `foldl_max_le`.

Absorption capstone (commit 3e17b984):
- `defaultFactorCoeffBound_le`: `D ≤ 2^(N-1)·L` via `choose k j ≤ 2^(k-1)`
  (`two_mul_choose_succ_le` → `choose_le_two_pow_pred` → `binom_le_two_pow_pred`)
  and a `foldl_preserve_le` invariant over the nested `defaultFactorCoeffBound`
  fold.
- `key_pow_ineq`: `2^(Nn)·n(n+1) ≤ N·2^(2N²)` for `1 ≤ n ≤ N`, the uniform
  inequality (`N+1 ≤ 2^N ≤ 2^(N²)`) that the `2^(N-1)` tightening makes hold for
  all N (no small-N special case).
- `one_le_log2_succ`, `cldCoeffFloor_eq_zero_of_degree_zero` (degree-0 core,
  incl. `f = 0`, gives floor 0).
- `cldCoeffFloor_le_factorFastPrecisionCap` — the deliverable, assembling the
  explicit bound, `defaultFactorCoeffBound_le`, and `bhksBound`'s `4^(N²)` slack.

The 30+ `hfloor` consumers in `Recovery.lean` are hypothesis-parametric by
design; the new theorem is the value callers feed, so no consumer rewiring was
needed.

## Verification

- `lake build HexBerlekampZassenhausMathlib` → 3790 jobs, success, no new
  warnings in `BHKSBound.lean`.
- `lake build HexBerlekampZassenhaus` → 497 jobs, success.
- `python3 scripts/check_dag.py` → exit 0.
- Added-line forbidden-token grep (sorry/axiom/native_decide/TODO/FIXME) → none.
- Pre-existing `sorry` at `HexBerlekampZassenhausMathlib/Basic.lean:233`
  (the #6672 `factor_irreducible_of_nonUnit` capstone) is untouched.

## Current frontier

`cldCoeffFloor_le_factorFastPrecisionCap` is available for the determinism /
existence chain. The fast-BHKS irreducibility endpoint (#7975) and the #6672
capstone remain the BZ frontier; `hfloor` was on the existence side, now closed.

## Next step

Downstream existence/completeness wrappers can now drop their `hfloor`
assumptions by feeding `cldCoeffFloor_le_factorFastPrecisionCap`.

## Blockers

None for this issue.

Note: worktree carries a pre-existing unrelated `.claude/skills/...SKILL.md`
modification from before this session; not touched or staged.
