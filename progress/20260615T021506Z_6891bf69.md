# Progress — 2026-06-15 (session 6891bf69, feature)

## Accomplished

Issue #7437: added one-to-three-sentence Phase 6 docstrings to the seven
undocumented private monic-degree characterisation theorems in
`HexHensel/Linear.lean` (lines ~1372–1459):

- `coeff_last_eq_leadingCoeff`, `monic_size_pos`,
  `degree?_eq_some_of_coeff_eq_one_and_high_coeff_zero`,
  `monic_of_coeff_eq_one_and_high_coeff_zero`,
  `degree?_eq_some_size_sub_one_of_monic`,
  `degree?_getD_eq_size_sub_one_of_monic`,
  `coeff_last_eq_one_of_monic`.

Doc-only: no signatures, statements, proofs, attributes, declaration
order, or `private` modifiers touched. Each docstring states what the
lemma proves and its role in the monic-degree bookkeeping behind
`linearHenselStep`. Wording varied per declaration.

## Verification

- `lake build HexHensel.Linear`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexHensel/Linear.lean`: `21 0` (additions only).
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in added lines.

## Current frontier

The cluster is documented. `size_le_of_degree?_getD_lt` (1461 onward,
the low-degree-addition / `reduceModPow` theme) is a separate Phase 6
batch and was left untouched.

## Next step

None for this issue. #7438 (HexGramSchmidt Int.lean rowSwap cluster)
remains unclaimed in the feature queue.

## Blockers

None.
