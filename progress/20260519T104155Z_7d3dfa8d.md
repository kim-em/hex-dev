## Accomplished

- Claimed #5417.
- Added `MontCtx.fromMont_mulMont_toMont_zero_left` and
  `MontCtx.fromMont_mulMont_toMont_zero_right` in
  `HexArith/Montgomery/Context.lean`.
- Proved both wrappers from the public Montgomery multiplication equality
  theorem and the context modulus positivity fact, without unfolding the
  Montgomery reduction internals.
- Verified with:
  - `lake build HexArith.Montgomery.Context`
  - `lake build HexArith`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n 'fromMont_mulMont_toMont_zero_(left|right)' HexArith/Montgomery/Context.lean`
  - `git diff -- HexArith/Montgomery/Context.lean | rg -n '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

#5417 is complete. The Montgomery `UInt64` context now has named simp wrappers
for multiplication by zero on both sides after `toMont`/`fromMont` conversion.

## Next step

Review and merge the PR for #5417, then continue with the remaining unclaimed
analytic work if appropriate.

## Blockers

None.
