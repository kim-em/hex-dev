# Interval structural scaling evidence

## Accomplished

- Collected the clean five-sample structural scale record from committed source
  after all 105 independently calibrated cases converged; every selected repeat
  count was measured and every source hash remained stable.
- Confirmed 525 compiled samples, 21 forced-kernel points, exact separated
  lookup counters, balanced sample order, stable checksums, and import-matched
  replay axiom sets of `[propext, Quot.sound]`.
- Wrote `reports/hex-interval-scale.md` with compiled, kernel, artifact,
  resource-accounting, and inference-limit findings.
- Updated the interval SPEC to keep the list checker as a transparent reference,
  require an endpoint-erased structural skeleton, and specify the immediate
  Mathlib-free Core-`Rat` planning/canonical-table replay vertical.
- Re-ran repository line-count, dependency-DAG, whitespace, Python, and
  Mathlib-free benchmark checks; the full 9,468-job build and focused replay
  builds are green.

## Current frontier

The implementation, raw evidence, report, and SPEC revision are ready for a
draft pull request and independent Opus review. The rational vertical remains
an experiment: its canonical boundary is fixed, while cancellation strategy,
table layout, wire format, projection witness, and replay chunk size remain
open.

## Next step

Commit the evidence and SPEC, push the branch, open the draft pull request, and
ask a fresh Opus reviewer to inspect soundness, measurement integrity, and the
resulting design decisions. Begin the endpoint-erased/Core-`Rat` vertical in a
separate worktree while review and CI run.

## Blockers

None.
