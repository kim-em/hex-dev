**Accomplished**

- Claimed and resolved directive #5775 (HO-5d `fallbackPrimeChoiceData`
  removal): closed it in favour of the existing decomposition
  #5816/#5817/#5819 after three prior workers had skipped it leaving
  the directive stuck behind a circular `depends-on: #5775` on its own
  sub-issue. Posted a forward-link comment summarising which sub-issue
  covers which resolution prong.
- Edited #5816's body to remove `depends-on: #5775`; ran
  `coordination check-blocked` which cleared the orphan `blocked`
  label, leaving #5816 claimable for fresh workers.
- Claimed #5816 and orientation-audited the executable refactor scope
  in `HexBerlekampZassenhaus/Basic.lean` (~13.2k lines, 189
  `choosePrimeData` references in this file alone, ~294 more in the
  Mathlib bridge). Confirmed the production callers reducible to five
  defs (`ZPoly.toMonicPrimeData`, `exhaustiveMonicCoreGuardPrimeData`,
  `exhaustiveSlowRawFactorsWithBound`, the exhaustive arm of
  `factorSlowFactorsWithBound`, and both arms of
  `factorFastFactorsWithBound`).
- Landed the additive partial step recommended by the scope audit:
  added `factorWithBound? : ZPoly → Nat → Option Factorization` and
  `factor? : ZPoly → Option Factorization` as new public Option-
  propagating boundaries that return `some` exactly on the executable
  branches the silent fallback does not consume (constant-core,
  quadratic-integer-root short-circuit, `choosePrimeData? = some`).
  Returns `none` precisely on the cascade that the `p = 3` fallback
  would otherwise mask.
- Recorded the predicate characterisation of the `some` branch as
  `factorWithBound?_eq_some_iff_safe_branch` (proved by case-splitting
  the three guards) and specialised it to the default Mignotte bound
  as `factor?_eq_some_iff_safe_branch`. No new `sorry`, no new
  `axiom`; pre-existing sorry at the same logical position (line 7073
  → 7140 post-insertion).
- `lake build HexBerlekampZassenhaus` clean (143 jobs, only the
  pre-existing single sorry warning).

**Current frontier**

- `fallbackPrimeChoiceData` is still present and the total
  `choosePrimeData` is still called by the production-side functions
  enumerated above; #5816's full deliverable is NOT discharged. This
  PR is the strictly-additive Option-propagating boundary only.
- The next narrower slices of #5816 (now visible in the issue
  comments): migrate executable guards / conformance off the totals
  (#5816-b); remove `fallbackPrimeChoiceData` once #5817 + #5819 land
  (#5816-c, gated on either the unreachability proof or the
  full-public-API `Option` propagation).

**Next step**

- Either follow-up planner narrows #5816 around the two slices above,
  or a worker can claim #5816 again and pick up #5816-b directly
  (migrating `exhaustiveMonicCoreGuardPrimeData` / the `#guard` line
  on `exhaustiveNonMonicQuadraticGuard` to the `?` variants is a
  natural starting point, since those are pure executable guards with
  no proof-bridge coupling).

**Blockers**

- None for this PR. Full #5816 discharge remains gated on either
  #5819 (extended prime search → unreachability proof) or a public-
  API change to `Option Factorization` (cascading through Mathlib
  bridges, conformance, benches).
