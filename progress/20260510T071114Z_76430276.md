# 20260510T071114Z - Issue #2949

## Accomplished

Claimed #2949 and added a proved local helper in `HexPolyFp/SquareFree.lean`:
`derivativeSplit_common_dvd_quotient_derivative_dvd_gcd`.  The helper captures
the valid part of the derivative-gcd quotient argument: if `d` divides
`c = f / gcd f f'` and `d` divides `c'`, then `d` divides `gcd f f'`.

Created successor issue #2955 for the remaining square-factor exclusion needed
to conclude `d ∣ 1`, and left the required `Decomposed into #2955` breadcrumb
on #2949.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep still reports the four pre-existing `sorry`s in
`HexPolyFp/SquareFree.lean`; no new `sorry`, `axiom`, or `native_decide` was
introduced.

## Current frontier

#2949 is partially completed by the common-divisor-to-gcd helper.  The residual
proof obligation is #2955: prove the local square-factor/multiplicity surrogate
that turns `d ∣ c`, `d ∣ c'`, and `d ∣ gcd f f'` into `d ∣ 1`.

## Next step

Land the partial PR for #2949, then work #2955 before #2950 consumes the full
derivative-split common-divisor base theorem.

## Blockers

None for the partial helper.  The full #2949 theorem did not fit as a direct
single-session proof because the current Mathlib-free `FpPoly` API has no
square-factor or multiplicity lemma for the quotient by `gcd f f'`.
