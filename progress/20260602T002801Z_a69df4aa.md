# BHKS CLD column bound bridge (issue #6162)

## Accomplished

- Claimed #6162 as the highest-impact unblocked feature item after confirming
  directive issues were blocked.
- Added `HexBerlekampZassenhausMathlib/CLDColumnBound.lean` with:
  - `BHKS.phi`, the exact integer-polynomial CLD column
    `(f * g.derivative).divByMonic g`;
  - `BHKS.phi_eq_factor_mul_derivative`, identifying `phi f g` with
    `h * g.derivative` under a monic factorisation `f = g * h`;
  - `BHKS.abs_phi_coeff_le`, a coefficient bound from the replacement
    analytic obligation `M(Phi) <= n * ||f||_2`, explicitly avoiding the
    invalid unconditional derivative Mahler estimate.
- Imported the new module from `HexBerlekampZassenhausMathlib.lean`.

## Current frontier

- The exact downstream BHKS coefficient inequality is now factored into a
  formal algebraic/coefficients bridge plus the remaining analytic
  `Phi`-Mahler bound hypothesis.
- The full proof of that analytic `Phi`-Mahler estimate remains the next
  mathematical frontier before #5224 can consume the bound without an extra
  hypothesis.

## Next step

- Prove the `hphi_mahler` hypothesis for BHKS true factors using a valid
  root-sum or Mahler argument, then specialize `BHKS.abs_phi_coeff_le` without
  exposing that hypothesis to executable consumers.

## Blockers

None for this scoped bridge. Verification completed:
`lake build HexBerlekampZassenhausMathlib.CLDColumnBound`,
`lake build HexBerlekampZassenhausMathlib`, `lake build`,
`python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token
diff check all pass. The first cold target build hit transient Mathlib
`.olean` write failures, then the rerun succeeded.
