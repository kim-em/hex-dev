# Session 8af54147 — HexBZ constant branch for factorSlowWithBound_product (#3913 partial)

## Accomplished

- Claimed #3913 after confirming PR #3911 had merged and no human-oversight
  item was available.
- Added `ZPoly.primitiveSquareFreeDecomposition_squareFreeCore_eq_one_of_degree_zero`,
  plus local support lemmas, so the BZ layer can prove a nonzero normalized
  degree-0 square-free core is `1` without unfolding private internals.
- In `HexBerlekampZassenhaus/Basic.lean`, proved:
  - `squareFreeCore_eq_one_of_constant_of_ne_zero`;
  - prefix-factor normalized/recorded helpers for the polynomial normalization
    prefix;
  - filtered-product preservation when a normalized/recorded prefix is followed
    by a trailing `1`;
  - `factorSlowWithBound_product_of_constant_branch`.
- Verified:
  - `lake build HexPolyZ.Basic`;
  - `lake build HexBerlekampZassenhaus.Basic`;
  - `lake build HexBerlekampZassenhaus`;
  - `python3 scripts/check_dag.py`;
  - `git diff --check`;
  - `rg -n '\bsorry\b|^axiom\b|native_decide' HexBerlekampZassenhaus/Basic.lean HexPolyZ/Basic.lean`
    reports only the two pre-existing BZ sorries.

## Current frontier

#3913 is partially complete. The constant branch dispatch layer is done and
builds. The quadratic branch remains open:

- `factorSlowWithBound_product_of_quadratic_branch` still needs canonicality /
  recordability for every factor emitted by `quadraticIntegerRootFactors?`.
- Linear factors can use the existing `linearFactorForRoot` helpers.
- The residual factor pushed by `splitIntegerRootFactorsAux` still needs a
  focused positive-leading / non-unit / recordability argument from the
  `split.2.degree?.getD 0 ≤ 1` branch and the product relation.

## Next step

Continue #3913 from the quadratic branch by proving a helper for
`quadraticIntegerRootFactors?` outputs:

```lean
∀ factor ∈ coreFactors.toList,
  normalizeFactorSign factor = factor ∧
  shouldRecordPolynomialFactor factor = true
```

Then dispatch with `factorSlowWithBound_product_of_all_recorded_normalized`.

## Blockers

- None for the constant branch.
- The residual pushed by `quadraticIntegerRootFactors?` is the remaining proof
  bottleneck for the quadratic branch.
