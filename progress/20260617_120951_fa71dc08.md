# Progress - 2026-06-17 (session fa71dc08, review #7742)

## Accomplished

Claimed and executed review #7742 (audit BZ good-prime unit-gcd fallback
boundary, supporting directive #2564). **Verdict: PASS**, no source changes.
Posted file:line evidence comment covering all three deliverables and closed
the issue.

- **Unit-gcd semantics.** `gcdIsUnit g := g.size == 1` (`Basic.lean:65`)
  accepts exactly normalized nonzero constants, because `DensePoly` carries
  its no-trailing-zeros invariant as a structure field
  (`DensePolyNormalized`, `HexPoly/Dense.lean:13-20`) — every reachable value
  is normalized, so size 1 ⟺ nonzero constant, size 0 = zero (rejected),
  size ≥ 2 = nonconstant (rejected). Correctly accepts non-monic unit
  representatives that the old literal-`1` check rejected.
- **Good-prime propagation.** `squareFreeModP`, `isGoodPrime`, `choosePrime`
  (via `scoreCandidate`/`choosePrimeScore?`), and `choosePrimeData?` all gate
  on the same `gcdIsUnit`/`isGoodPrime` predicate; no stale literal-`1` check
  survives. Selected `PrimeChoiceData` carries `choosePrimeData?_isGoodPrime`
  downstream.
- **Fallback discipline.** Refuted `fallbackPrimeChoiceData` silent-`p=3` path
  is gone. `factorWithBound` chain (`Basic.lean:8075`) terminates in
  `factorSlowTrial` (integer trial division, no prime data) —
  `choosePrimeData?`-independent. Residual total `choosePrimeData`
  (`.get!`, `Basic.lean:2349`) has zero production call sites (only comments +
  rewrite lemma), panics rather than silently defaulting; admissible under
  design-principles §8.

Verification: `lake build HexBerlekampZassenhaus.Conformance` — clean
(492/492), all good-prime guards pass.

## Current frontier

Tactical good-prime boundary is sound. The architectural directive #2564
(BHKS van Hoeij CLD rewrite + headline correctness theorem) remains the
open umbrella; this audit confirms the forward-compatible tactical fix is
intact.

## Next step

None for this review. Directive #2564 work continues via its decomposed
feature issues (FactorSoundness capstones, branch irreducibility).

## Blockers

None. Worktree carries pre-existing unrelated `.claude/*` modifications from
before this session; not touched or staged.
