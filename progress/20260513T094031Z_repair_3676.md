# Repair session: PR #3676 (round two)

## Accomplished

- Re-diagnosed PR #3676 after the first repair pass left CI red.
- Root cause: `patch` writes `patching file 'haskell_sources/Main_Verified.hs'`
  to stdout, which gets captured by
  `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)"` in
  the workflow whenever the oracle cache is fresh. The env var then
  contains a newline followed by the binary path, so
  `IO.Process.spawn` on it fails immediately and the bench reports
  "svp_verified closed stdout before replying" / "child exited with
  code 1" for every Isabelle target.
- Locally reproduced the CI failure by clearing the oracle cache and
  re-running `lake exe hexlll_bench verify`: 8 of 32 fixed Isabelle
  benchmarks failed the same way as CI.
- Fixed `scripts/oracle/setup_lll_isabelle.sh` so the per-patch
  application redirects to stderr (`patch ... >&2`). The script's only
  remaining stdout is the final `printf '%s\n' "${binary}"`.
- Re-ran with a fresh cache: all 32 benchmarks pass. `check_dag.py`
  and `check_phase4.py` also clean.

## Current frontier

PR #3676 has the fix ready to push.

## Next step

Push `agent/9b55e5d6` with `--force-with-lease` and mark PR #3676
salvaged.

## Blockers

None.
