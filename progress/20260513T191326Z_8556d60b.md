# Session 8556d60b - issue #3804

## Accomplished

- Triaged #3479 (no-pivot Bareiss invariant): its `depends-on: #3492`
  wording was stale (#3492 was decomposed into #3800/#3801/#3802 and
  the Mathlib-free Desnanot-Jacobi bordered-minor surface it needs
  lives in #3802, still open). Added `depends-on: #3802` to #3479 and
  skipped it with that explanation.
- Claimed #3804 and added the Mathlib-free Bareiss Desnanot
  source-entry helpers and structural submatrix equalities in
  `HexMatrix/Determinant.lean`:
  - `bareissCyclicShift`, `bareissCyclicShift_of_val_zero`,
    `bareissCyclicShift_of_pos` — the row/column rearrangement
    induced by `bareissDesnanotIndex k` on
    `(Fin.last (k + 1)).succAbove` sub-positions.
  - Four source-entry helpers
    `borderedMinor_at_{succ_succ, castSucc_castSucc, succ_castSucc,
    castSucc_succ}` describing, for each combination, the source
    entry selected by `bareissDesnanotIndex k`-reindexing a `(k+2)`
    bordered minor.
  - Three private value-form bordered-minor entry lemmas
    (`borderedMinor_entry_val_k_lt`,
    `borderedMinor_entry_val_k_val_k`,
    `borderedMinor_entry_lt_val_k`) to sidestep dependent-index
    rewriting on `r = Fin.last k` / `c = Fin.last k`.
  - Five structural submatrix equalities corresponding to `M11`,
    `M_kk`, `M_1k`, `M_k1`, and the interior leading prefix.
- Verified with:
  - `lake build` (full project)
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexMatrix HexMatrix.lean`
    confirms the only `sorry` is the pre-existing
    `Hex.Matrix.bareiss_eq_det` placeholder (out of scope per the
    issue).

## Current frontier

`HexMatrix/Determinant.lean` now has the full entrywise reindexing
surface needed by the future Mathlib-free Desnanot-Jacobi
bordered-minor assembly (#3802). The Mathlib-side proof in
`HexMatrixMathlib/Determinant/Core.lean` retains its own
`source_row_of_*` and `M*_eq_matrixEquiv_borderedMinor_*` helpers
since they are stated via `matrixEquiv`; the new Mathlib-free helpers
are stated directly against `Hex.Matrix.borderedMinor` and
`Hex.Matrix.leadingPrefix`.

## Next step

Downstream issue #3802 can compose `desnanot_jacobi` (or a
Mathlib-free counterpart from #3800) with these structural
equalities to produce the Mathlib-free
`desnanot_jacobi_borderedMinor`, which then unblocks
`HexMatrix/Bareiss.lean`'s no-pivot Bareiss invariant (#3479).

## Blockers

- None for this issue.
