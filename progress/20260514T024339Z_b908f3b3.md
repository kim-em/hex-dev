# 2026-05-14 02:43 UTC — work session b908f3b3 (issue #3890)

## Accomplished

Claimed #3890 and completed the pivot adjacent-swap scaled-coefficient proof:

- Added `GramSchmidt.Int.scaledCoeffMatrix_rowSwap_adjacent_pivot_transpose`,
  showing the pivot Cramer matrix after an adjacent row swap is the transpose
  of the original pivot Cramer matrix.
- Replaced the `sorry` in `scaledCoeffs_adjacentSwap_pivot` by reducing both
  sides through `scaledCoeffs_eq_scaledCoeffMatrix_det` and `Matrix.det_transpose`.

Verification run:

- `lake build HexGramSchmidt.Update`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '\bsorry\b' HexGramSchmidt/Update.lean`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Update.lean HexGramSchmidt/Int.lean`

## Current frontier

The pivot adjacent-swap scaled-coefficient invariance is now proved. The
remaining adjacent-swap integer update holes in `HexGramSchmidt/Update.lean`
are the four above-row formulas and divisibility side conditions that #3890
explicitly left out of scope.

## Next step

Dispatch or claim a follow-up issue for the above-row adjacent-swap scaled
coefficient formulas:

- `scaledCoeffs_adjacentSwap_above_prev`
- `adjacentSwap_scaledCoeffAbovePrevNumerator_dvd`
- `scaledCoeffs_adjacentSwap_above_curr`
- `adjacentSwap_scaledCoeffAboveCurrNumerator_dvd`

## Blockers

None for #3890.
