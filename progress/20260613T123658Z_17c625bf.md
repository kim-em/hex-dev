# Progress: #6454 — factorFast_terminates from ForwardRecoveryInputs (blocked on upstream Basic breakage)

## Accomplished

Wrote the deliverable wrapper
`factorFast_terminates_of_forwardRecoveryInputs` in
`HexBerlekampZassenhausMathlib/Recovery.lean` (committed `1de4e4c5`, pushed
to `agent/17c625bf`). It collapses the two `factorFast_terminates` consumer
packages into a single `ForwardRecoveryInputs`-keyed surface:

- recovery-tail package via
  `CanonicalRecoveryTailInputs.ofForwardRecoveryInputsCanonicalRepresentations`
  (the existing recovery-side collapse, line 4076);
- cap-separation package via
  `FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
  (line 5129);
- both fed to `factorFast_terminates` at `h.rows_pos`.

It mirrors the existing
`factorFast_terminates_ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
(line 7184) exactly, substituting `rows_pos → h.rows_pos` and replacing the
trailing `recoveryInputs` parameter with the forward-recovery surface
(`h`, `hindicators`, the support-shape facts, monicness, `hp_two_lt`).

Premise check passed: the three sibling producers (#6448 ExpectedTrueFactors,
#6450 RepresentsIntegerFactorAtLift, #6452 cap paper-threshold) all landed
and are closed; the target wrapper genuinely did not exist before this.

## Blockers

**Cannot compile-verify.** `HexBerlekampZassenhausMathlib/Basic.lean:15990`
does not build on `main` (clean source, genuine type mismatch). The
executable `scaledRecombinationCandidate` / `candidate'` migrated to
`ZPoly.dilate coreLc (centeredLiftPoly …)` (HexBerlekampZassenhaus/Basic.lean
:10823, landed by the #6808/#6813 dilate remodel), but the Mathlib-side
consumer still constructs `hprefix` / `hcandidate_def` in the old
`centeredLiftPoly (DensePoly.scale coreLc …polyProduct)` form. The Mathlib
migration that fixes this (#6801) was CLOSED with `replan`; its successors
(#6804 "migrate the scaledRecombinationCandidate recovery and coverage chain
to the dilation model", then #6805) are blocked behind #6817/#6818/#6819.

`Recovery.lean` is downstream of `Basic.lean`
(Recovery → TerminationBound → BHKSBound → Basic), so the whole
`HexBerlekampZassenhausMathlib` library is unbuildable until that migration
lands. The merge-gating CI builds only the Mathlib-free bench/conformance
targets, so it does not catch this — main is silently red for the entire
Mathlib bridge layer, which blocks all headline-theorem-critical-path work.

Did **not** attempt to fix Basic.lean:15990: it is multi-lemma dilate-model
migration work, owned by the claimed/blocked #6804 tree, out of #6454's scope.

## Current frontier / Next step

Wrapper is written and manually type-checked against exact signatures but
unverified. Marked #6454 blocked on #6804 and released the claim. Once the
dilate migration lands (Basic builds), a successor cherry-picks `1de4e4c5`
(or re-applies from `agent/17c625bf`), runs `lake build
HexBerlekampZassenhausMathlib`, and closes #6454.

## Queue assessment (for the next worker / planner)

After releasing #6454 I assessed the rest of the unclaimed queue and found
no cleanly-actionable item for a fresh worker:

- All `HexBerlekampZassenhausMathlib` items (#4818, #6454, #6773, #6779,
  #6786, and the B-builder tree) are unbuildable until the dilate migration
  lands — the Basic.lean:15990 breakage above is library-wide. Several of
  these also still have open real dependencies (#6779→#6777, #6773→#6804,
  #6786→#6773/#6804) despite stale labels showing them as unclaimed.
- The two HexPolyFp items (#6376, #6380) sit in `HexPolyFp/SquareFree.lean`,
  the same file as the active PR #6826 (#6811), which is reshaping the
  derivative-active raw-tail / weighted-product bridge those issues build on.
  Substrate in flux + direct file-conflict risk — better to wait for #6826
  to merge.

So I stopped rather than start conflict-prone or unverifiable work. The
single highest-leverage project action right now is landing the dilate
migration (#6817→#6818→#6819→#6804→#6805): it unblocks the entire Mathlib
bridge layer (headline-theorem critical path) and removes the silent-red
state from main.
