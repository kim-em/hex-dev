# HexPolyMathlib Phase 7 reference chapter (#8211)

## Accomplished

Authored `HexManual/Chapters/HexPolyMathlib.lean`, the Phase 7 reference
chapter for the Mathlib-layer bridge `HexPolyMathlib`, modelled on the
sibling Mathlib-layer chapters `HexModArithMathlib.lean` and
`HexGFqMathlib.lean` (not the computational `HexPoly.lean`):

- Introduction framing `HexPolyMathlib` as the proof-transfer bridge
  between executable `Hex.DensePoly` and Mathlib `Polynomial`, noting the
  whole surface is `noncomputable`.
- Transfer maps: `{docstring}` for `toPolynomial`/`ofPolynomial` plus the
  coefficient laws `coeff_toPolynomial`/`coeff_ofPolynomial`.
- Round-trip laws `toPolynomial_ofPolynomial`/`ofPolynomial_toPolynomial`.
- Ring-homomorphism transport: constants/monomials (`toPolynomial_zero`,
  `_one`, `_C`, `_monomial`), the arithmetic laws (`_add`, `_neg`,
  `_sub`, `_mul`, `_derivative`), and the `ofPolynomial_*` counterparts.
- Two worked `example`s that *type-check* (not `#eval`) a transported
  multiplication and a round-trip via `by simp`, per the issue's
  noncomputable-surface guidance.
- Degree/leadingCoeff (`natDegree_toPolynomial`,
  `leadingCoeff_toPolynomial`), divisibility transport (`toPolynomial_dvd`,
  `ofPolynomial_dvd`, `toPolynomial_dvd_iff`).
- The ring equivalence `equiv` with `equiv_apply`/`equiv_symm_apply`.
- Euclidean transport from `Euclid.lean`: `toPolynomial_gcd_associated`,
  `toPolynomial_xgcd_gcd_associated`, `toPolynomial_xgcd_bezout_raw`/
  `_associated`, and the `equiv_*` forms, with prose explaining the
  `Associated`-not-equality normalization gap.
- Cross-reference `{ref "hex-poly"}` (resolves).

Wired the chapter into `HexManual.lean` (import + `{include 0 ...}`) and
bumped `libraries.yml[HexPolyMathlib].done_through: 6 → 7`.

## Verification

- `lake build HexManual` green (2575 jobs); the chapter built clean with
  no warnings. Verso errors on any unresolved `{name}`/`{docstring}`/
  `{ref}`, so a green build confirms every directive resolves.
- Caught and fixed one 60-column overflow on a comment line inside the
  `lean` example block (rebuilt clean).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` moves `HexPolyMathlib` to "Fully done".
- No `axiom`/`sorry`/`native_decide` introduced.

## Current frontier

`HexPolyMathlib` Phase 7 complete. Remaining unclaimed feature issues at
session end: #8231 (HexGFq Phase 7 chapter) and #8234 (HexHenselMathlib
Phase 6 audit, a review item).

## Next step

A successor can pick up #8231 (HexGFq chapter) or #8234.

## Blockers

None.
