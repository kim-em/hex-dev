# Work issue #3472 — HexGfq degree-32 packed witness partial

## Accomplished

- Checked human-oversight priority first; #2564 was blocked and could not be
  claimed.
- Claimed #3503, verified that its body still depended on open PR #3504, and
  skipped it as blocked/stale.
- Claimed #3472 and proved the N32 packed `GF2Poly` irreducibility witness in
  `HexGfq/CrossCheck.lean` using a local Rabin certificate.
- Filed follow-up #3531 for the remaining N32 generic `FpPoly 2`
  irreducibility proof and linked it from #3472.
- Verified `lake build HexGfq.CrossCheck`, `lake build HexGfq`,
  `python3 scripts/status.py HexGfq`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the requested banned-marker scan.

## Current frontier

- N32 packed irreducibility is discharged.
- N32 generic irreducibility remains as the original `sorry`; the direct
  literal generic certificate evaluated true but its proof was too slow with a
  straightforward `simp`/constructor approach.

## Next step

- Land this as a partial PR for #3472, then address #3531 with a checker
  helper or quotient-witness proof for the generic pow chain.

## Blockers

- None for the packed-side partial PR.
