# Progress: HexPolyFp normalized SquareFree tail validity (#6284)

## Accomplished

Claimed #6267, assessed it as too broad for one proof session, and decomposed
it into #6284, #6285, and #6286 with #6285 depending on #6284 and #6286
depending on #6285. Marked #6267 for replan with the required breadcrumb.

Claimed #6284 and added private normalized-tail substrate in
`HexPolyFp/SquareFree.lean`:

- raw nonzero preservation for `yunFactorsContributionWithLevel` tails along
  derivative-active reachability;
- normalized contribution-tail reachability/nonzero from
  `YunDerivativeActiveNormalizedStateProvider`;
- normalized `pthRoot` tail validity guarded by normalized nontriviality;
- a `yunFactorsWithLevel` normalized `pthRoot` tail fuel-bound projection.

Verification:

- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over `HexPolyFp/SquareFree.lean`

## Current frontier

#6284 is complete as a private helper slice. The recursive IH in
`squareFreeAuxRevContribution_derivative_active_pow_obligation` still uses the
raw tail; that is intentionally left to #6285 and #6286.

## Next step

Open a PR for #6284. After it lands, #6285 should prove the scalar and
normalization reconstruction bridge needed at the recursive IH boundary.

## Blockers

None for #6284. The worktree still contains the unrelated pre-existing local
modification to `.claude/CLAUDE.md`, which was not touched or staged.
