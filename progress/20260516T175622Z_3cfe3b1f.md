# 2026-05-16T17:56Z — feature session 3cfe3b1f (#4301)

## Accomplished

- Proved the recursive coverage capstone for
  `Hex.recombinationSearchModAux` (#4301) in
  `HexBerlekampZassenhausMathlib/Basic.lean`.
- Two new declarations near the end of the file:
  - `private theorem recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`:
    universal-quantifier auxiliary doing the fuel-based induction; under a
    `LiftedFactorSubsetPartition core d J target` rest-state and the
    standard core/lifted-factor monicness hypotheses, returns
    `∃ result, recombinationSearchModAux ... = some result ∧
    ∀ irreducible factor ∣ target, ∃ emitted ∈ result, Associated emitted factor`.
  - `theorem recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`:
    thin specialisation matching the deliverable signature from the issue
    body (single `factor` hypothesis).
- The universal-quantifier formulation lets a single induction simultaneously
  supply the recursive-rest success witness and the per-factor associated
  emitted; the deliverable then specialises by passing the issue's `factor`
  hypotheses to the universal claim.
- Proof structure (induction on `fuel`):
  - `fuel = 0`: vacuous from `J.card < 0`.
  - `target = 1` branch: executable returns `some []` directly; universal
    claim vacuous because irreducibles do not divide units.
  - `target ≠ 1` branch:
    1. Derive `J.Nonempty` by extracting an irreducible divisor of `target`
       via `WfDvdMonoid.exists_irreducible_factor` and contradicting the
       empty representing subset via
       `not_represents_empty_of_irreducible_dvd_core`.
    2. `LiftedFactorSubsetPartition.cover_at_min` gives
       `(f_cov, S_cov)` with `J.min' ∈ S_cov`.
    3. `representsIntegerFactorAtLift_monic` lifts `f_cov` to monic, then
       `recombinationCandidate_eq_factor_of_recovery_of_monic_core` gives
       `recombinationCandidate d S_cov = f_cov`.
    4. `exactQuotient?_recombinationCandidate_eq_some_of_eq_factor` produces
       `quotient` with `quotient * f_cov = target`.
    5. `Monic.of_mul_monic_right` transports monicness back to `quotient`.
    6. Partition transport via `liftedFactorSubsetPartition_transport`;
       matches transport via `LiftedFactorListMatches.sdiff_of_subset`.
    7. Cardinality: `(J \ S_cov).card < J.card` since `J.min' ∈ S_cov ⊆ J`.
    8. Apply IH on `(quotient, J \ S_cov)`.
    9. Decompose the canonical split membership via `List.append_of_mem`.
    10. Discharge `hprefix` via `liftedFactorSubsetPartition_prefix_none`
        with `localFactors.Nodup` from `LiftedFactorListMatches.nodup_of_injOn`.
    11. `recombinationSearchModAux_eq_some_of_step_of_prefix_none` packages
        the executable step output as `recombinationCandidate d S_cov :: restFactors`.
    12. Universal coverage: case-split on `Associated factor f_cov`. The
        associated case uses `f_cov` itself as the emitted witness; the
        non-associated case derives `factor ∣ quotient` via
        `UniqueFactorizationMonoid.irreducible_iff_prime` and
        `Prime.dvd_or_dvd` (with the `factor ∣ f_cov` branch killed by
        `Irreducible.associated_of_dvd` plus the not-associated hypothesis),
        then applies the IH coverage.
- Threads two new hypotheses beyond the issue's listed signature:
  - `hd_modulus : 2 ≤ d.p ^ d.k` — required by the prefix-none assembler.
  - `hd_liftedFactor_inj : Function.Injective (liftedFactor d)` — discharges
    the `Nodup` precondition of the prefix-none assembler via the
    `Set.InjOn`-restricted form of `LiftedFactorListMatches.nodup_of_injOn`.
- Dropped redundant `htarget_ne_zero` and `htarget_primitive` hypotheses
  from the issue's draft signature: both are derivable from the threaded
  `htarget_monic`.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓ (built transitively)
- `python3 scripts/check_dag.py` ✓ (silent pass)
- `git diff --check` ✓
- Sorry inventory in
  `HexBerlekampZassenhausMathlib/Basic.lean` unchanged at 2
  (pre-existing `irreducibleByFactorization_iff` L162 and
  `factor_irreducible_of_nonUnit` L241); no new `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
- Two commits: `43e527de` (WIP scaffold + target=1 branch),
  followed by the inductive step + simplification round on the same branch.

## Next step

- The natural follow-up to this issue is the public-wrapper composition
  that lifts this recursive lemma to `exhaustiveCoreFactorsWithBound`,
  flagged as out-of-scope in the issue body. That follow-up needs the
  initial-state constructor for `LiftedFactorSubsetPartition` from
  `HenselSubsetCorrespondenceHypotheses` plus a square-free reduction
  hypothesis (also out-of-scope here).
- The `hd_liftedFactor_inj` hypothesis introduced by this proof is
  expected at the call site to come from a Hensel-coprimality fact about
  the lifted local factors (distinct lifted factors are pairwise coprime
  and monic, hence unequal as polynomials); supplying that witness from
  `henselLiftData` invariants is a separable sub-task.

## Blockers

- None.
