# Progress — 2026-05-07T22:48 UTC — agent/ca801968

## Accomplished

Closed `prod_berlekampFactor` (issue #2621) in `HexBerlekamp/Factor.lean`,
removing the last theorem-level `sorry` in that file. The proof builds a
loop invariant that `factorProduct` is preserved by every successful
split:

- `splitWithWitnesses?_product_spec`: any successful witness split
  satisfies `r.factor * r.cofactor = f` (lifts
  `kernelWitnessSplit_product_spec` to the witness-list driver).
- `factorProduct_cons_eq`: `factorProduct (x :: xs) = x * factorProduct xs`,
  built from the foldl helpers below.
- `foldl_mul_left_factor`: `xs.foldl (·*·) (z * a) = z * xs.foldl (·*·) a`
  by induction on `xs` using `mul_assoc_poly`.
- `foldl_mul_eq_mul_foldl`: `xs.foldl (·*·) z = z * xs.foldl (·*·) 1` via
  `mul_one_right_poly` and the previous helper.
- `one_mul_poly`: `(1 : FpPoly p) * a = a` from `mul_comm_poly` plus
  `mul_one_right_poly`.
- `factorProduct_splitFirstFactor?_eq`: replacing the first splittable
  factor preserves `factorProduct`, using associativity to fold
  `r.factor * (r.cofactor * factorProduct rest)` to
  `(r.factor * r.cofactor) * factorProduct rest = fac * factorProduct rest`.
- `factorProduct_berlekampFactorLoop_eq`: the fuelled split-loop
  preserves `factorProduct` for any input list.

Final step: `(berlekampFactor f hmonic).product`
= `factorProduct (berlekampFactorLoop … [f])`
= `factorProduct [f]` (loop preservation)
= `f * factorProduct []` (cons-eval)
= `f * 1` = `f`.

Added `[ZMod64.PrimeModulus p]` to the final theorem signature; the plan
explicitly permits this since the helpers (and `kernelWitnessSplit_product_spec`)
need it. No other typeclass changes; no edits outside
`HexBerlekamp/Factor.lean`.

## Notes / friction

`rw` repeatedly failed to match `(z * a) * b` against
`List.foldl (fun acc factor => acc * factor) (z * a * b) bs`-style goals,
even though the patterns are textually identical and instances should
unify. Worked around by using `congr 1` to split into the underlying
poly-mul equation, then closing it directly with the algebraic lemma as
a term. The same `congr 1` pattern was used to bridge `z = z * 1` for
`foldl_mul_eq_mul_foldl`.

## Current frontier

`HexBerlekamp/Factor.lean` has zero theorem-level sorries. The two
remaining sorries in `HexBerlekamp/DistinctDegree.lean` (lines 163, 174)
are out of scope for this issue and remain.

## Next step

Per the plan's "Out of scope" list, `HexBerlekamp/DistinctDegree.lean`
sorries are the natural next Phase 5 implementation-loop targets for
the `HexBerlekamp` library.

## Blockers

None.
