# 20260509T211803Z - Issue #2875

## Accomplished

Claimed #2875 after #2867 was concurrently claimed by another agent. Added the
Mathlib bridge theorem in `HexGramSchmidtMathlib/Basic.lean`:

- `rowToEuclidean_inner`

The proof includes a private normalization helper connecting the executable
`List.finRange.foldl` rational dot product to a real `List.sum`, then rewrites
Mathlib's `PiLp.inner_apply` finite sum over the same `Fin` range.

Verification completed:

- `lake build HexGramSchmidtMathlib.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidtMathlib/Basic.lean`

## Current frontier

The row-to-Euclidean inner/dot bridge is available for the Mathlib
Gram-Schmidt proof surface. The pre-existing `rat_basis_row_eq_gramSchmidt`
proof remains the next major theorem in this file.

## Next step

Use `rowToEuclidean_inner` to rewrite projection coefficients while proving the
rowwise rational Gram-Schmidt correspondence with Mathlib's `gramSchmidt`.

## Blockers

No blocker for #2875. The worktree still has an unrelated pre-existing
`.claude/CLAUDE.md` modification that this session did not touch.
