## Accomplished

Claimed PR #3043 for repair after PR #3042 was already repair-claimed.

Diagnosed the failed `build` and `conformance` checks as the same upstream
`HexMatrix.RREF` duplicate private declaration error:
`Hex.Matrix.IsRREF.foldl_add_eq_acc_ring`. The determinant helper introduced
by PR #3043 itself built successfully in CI, and current `origin/main` already
contained the RREF repair from #3040.

Merged `origin/main` into `agent/1875a405` to bring in that fix without
changing the PR's determinant helper.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexMatrix/Determinant.lean HexMatrix/RREF.lean`
  reports no matches.
- `lake exe hexmatrix_bench list`
- `lake exe hexmatrix_bench verify`
- `lake build $(python3 scripts/conformance_targets.py --space-separated) hexmatrix_emit_fixtures`

## Current frontier

PR #3043 is locally repaired and verified. The only code change beyond the
original PR content is the merge of current `origin/main`, which removes the
duplicate RREF helper that blocked CI.

## Next step

Push `agent/1875a405` with `--force-with-lease` and mark PR #3043 salvaged.

## Blockers

None.
