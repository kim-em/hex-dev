# Progress — session 839e6600 (/work → diagnose #6672, /feature #7955)

## Accomplished

### Diagnosed and skipped #6672 (HO-1 capstone, directive #2564 critical path)
Claimed the unblocked capstone `factor_irreducible_of_nonUnit`
(its only `depends-on: #7894` is closed). Premise-checked: the assembly
route `factor_entries_irreducible f h_raw` (`Basic.lean:427`) still needs
the **unconditional fast substrate** `factorFastRaw_irreducible_of_some`,
which does not exist (`grep` empty). The two slow disjuncts have
unconditional substrates (`slowModularRaw_irreducible_of_fast_none`
`IntReductionMod.lean:6243`, `factorSlowTrialFactorsWithBound_factor_irreducible_of_fast_none`
`:6440`); the fast disjunct's routes (`PartitionRefinement.lean:589/660/613`)
all still take `CutProjectionHypotheses`/`TrueFactorLift`/`hsep` as free
binders. Root blocker is the hsep-at-success-precision trap (verified in
commit 5d6e9fae / #7950): the fast-core loop accepts a product-verified
precision below the CLD floor, where `hsep` is literally false. The
concrete unblocker is the executable CLD-adequacy gate #7951 (in progress)
plus a follow-on Mathlib soundness remodel. Posted full evidence as an
issue comment and `coordination skip 6672` for replan. No code, no sorry.

### Completed #7955 (HexPolyFp Phase 6 grind automation)
Annotated all 29 equation-shaped `@[simp]` lemmas in
`HexPolyFp/Basic.lean` (eval_*, coeff_*, ring laws, linearPow_*) as
`@[simp, grind =]`, following the Mathlib-free sibling convention
(HexMatrix/HexModArith). `FpPoly p` has no `Grind.CommRing` instance, so
grind otherwise lacks these characterising rewrites. Demonstrated payoff:
`linearPow_one` shortened from a three-rewrite chain to `by grind`.

## Verification
- `lake build HexPolyFp` → 98 jobs, success, no warnings/errors (downstream
  files SquareFree/Conformance/QuotientFrobenius/Compose included — no
  grind loop or regression).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  on added lines. sorry/axiom count unchanged at 0.
- Diff scoped to `HexPolyFp/Basic.lean` only (30/30).

## Current frontier
#7955 done. The directive #2564 capstone (#6672) is sequenced behind the
executable acceptance gate #7951.

## Next step
After #7951 lands, file/sequence the Mathlib `factorFastRaw_irreducible_of_some`
substrate, then re-open #6672 assembly. Sibling grind pass #7957 (HexLLL)
remains unclaimed.

## Blockers
#6672 blocked on #7951 + post-gate Mathlib substrate (documented on the issue).

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; left untouched and unstaged.
