# 2026-05-09 (UTC) — issue #2736 (HexHensel Phase-4 evidence)

## Accomplished

- Added `HexHensel.phase4.input_families` to `libraries.yml` with four
  families (`bridge-operations`, `linear-hensel`, `quadratic-hensel`,
  `multifactor-lifting`) covering the nine `Hex.HenselBench.*` registrations.
  `python3 scripts/check_dag.py` and `python3 scripts/check_phase4.py`
  both pass.
- Ran `lake exe hexhensel_bench list && lake exe hexhensel_bench verify`
  at commit `60dbf8026826` on `carica`; both succeeded for all nine
  registered benchmarks.
- Ran the scientific `lake exe hexhensel_bench run` over all nine
  parametric registrations and exported
  `reports/bench-results/hex-hensel-60dbf8026826.json`. Eight of nine
  verdicts came back **consistent with declared complexity** at
  `|β| ≤ 0.101`. The ninth, `Hex.HenselBench.runHenselLiftChecksum`,
  came back **inconclusive** with `β=+0.158`; the encoded `(n, k)`
  ladder ran the `param=128_064` rung into the per-call wallclock
  cap and the harness skipped `param=192_064`.
- Recorded developer-local samply profiles via the `--profiler "samply
  record --save-only --unstable-presymbolicate"` form of
  `hexhensel_bench profile` for all four `phase4.input_families`
  entries: `bridge-operations` via `runReduceModPowChecksum` at
  `n = 131072`, `linear-hensel` via `runLinearHenselStepChecksum` at
  `n = 512`, `quadratic-hensel` via `runQuadraticHenselStepChecksum`
  at `n = 512`, and `multifactor-lifting` via
  `runMultifactorLiftQuadraticChecksum` at `param = 192_064`.
- Symbolicated the resulting Firefox Profiler JSONs against samply's
  `.syms.json` sidecar with the developer-local helper, classified
  leaf costs across {own, GMP, allocation, Lean runtime, kernel,
  other}, and produced inclusive-cost rankings restricted to
  `Hex.HenselBench.*`/`Hex.ZPoly.*`/`Hex.DensePoly.*`/`Hex.ZMod64.*`
  symbols.
- Wrote `reports/hex-hensel-performance.md` with the five mandated
  subsections: Bench targets, Verdicts, Comparator ratios (none
  declared in SPEC), Profile (one case per input family with
  leaf-cost categorisation, inclusive-cost ranking, and
  dominant-cost narrative attributing every dominant inclusive cost
  to a registered bench target), Concerns (one entry: the
  inconclusive `runHenselLiftChecksum` verdict).
- Filed follow-up issue #2791 for the cost-model investigation of
  `runHenselLiftChecksum` (either refine `liftLinearComplexity` to
  account for bigint operand growth on `Z/p^k`, or tighten
  `Hex.ZPoly.henselLift` to perform per-step coefficient reduction
  the way the multifactor wrapper does).

## Current frontier

- `HexHensel` parks at `done_through: 3`. The Phase-4 evidence
  package (input-family metadata, scientific verdicts for eight of
  nine registrations, profile coverage for all four input families,
  attribution review) is in place; the only remaining blocker is the
  inconclusive `runHenselLiftChecksum` verdict tracked in #2791.

## Next step

- Land #2791. After that closes, the follow-up worker bumps
  `HexHensel.done_through` from 3 to 4 in the same change that
  removes the §Concerns entry from
  `reports/hex-hensel-performance.md`.
- Other audit-rollback follow-ups remain open in the queue
  (`HexBerlekamp` #2739, `HexConway` #2741); they follow the same
  workflow but operate on different bench surfaces.

## Blockers

- `runHenselLiftChecksum` verdict (tracked in #2791) prevents
  promoting `HexHensel.done_through` to 4.
