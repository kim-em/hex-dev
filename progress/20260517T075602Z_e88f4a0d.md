# 20260517T075602Z — review session e88f4a0d (#4595)

## Accomplished

Closed #4595: HO-1 review of PR #4582 (fast-path quadratic arm umbrella
`factor_quadratic_branch_entry_irreducible_of_quadraticRoots`).

### Deliverable verification

All seven deliverables verified by inspection on merged commit `0ffd01cd`:

1. **Branch-shape lemma** (`factorWithBound_entry_mem_quadratic_branch_raw`,
   `HexBerlekampZassenhaus/Basic.lean:5668`): identifies each recorded
   entry as the sign-normalisation of a raw factor in the quadratic-branch
   reassembly. Proof unfolds `factorFastFactorsWithBound` and dispatches
   via `if_neg hdeg`, `if_neg (B ≠ 0)`, `if_neg (B ≠ 1)` (both `B ≠ 0` and
   `B ≠ 1` discharged from `hB_gt_one : 1 < B` via `omega`), `hquad`. No
   silent weakening of dispatch hypotheses. Fast/slow-path asymmetry
   (`hB_gt_one` vs `hfast_none`) is the same one flagged in the #4584
   sibling review — correct and unavoidable.

2. **Size-two primitive helper** (`ZPoly.irreducible_of_size_two_primitive`,
   `Basic.lean:5957`, `private`): correctly uses primitivity (`content
   f = 1`) rather than monicness. Constant factors `c` are bounded via
   `c.natAbs ∣ content f = 1`, forcing `c ∈ {±1}` via `Nat.le_of_dvd`. No
   silent appeal to leading-coefficient sign positivity — the bound is
   from divisibility alone, with the sign case-split via
   `Int.natAbs_eq`.

3. **Residual irreducibility** (`quadraticIntegerRootFactors?_residual_irreducible`,
   `Basic.lean:7826`, `private`): casework on residual size correct.
   * Size-1: derives `split.2.coeff 0 = 1` from primitivity + positive
     leading coefficient on `core` + content = 1; the resulting
     `split.2 = C 1 = 1` contradicts `hres_one`.
   * Size-2: inlines the size-two-primitive argument applied to `core`'s
     primitivity (via the `core = C(c) * (v * polyProduct split.1)`
     decomposition in `const_factor_to_unit` local helper, line 7989).
     The inline-via-core approach is sound and is explained by an
     in-proof comment at 7953–7955; the issue's docstring language
     ("applied to the residual's own primitivity") is slightly out of
     sync with the actual structure — documentary, not a defect.

4. **Public wrapper composition**
   (`quadraticIntegerRootFactors?_factor_irreducible_of_primitive`,
   `Basic.lean:8054`): signature has no file-private names. Internally
   uses `by_cases hres` to dispatch to `_residual_irreducible` or
   `_factor_irreducible_of_ne_residual`. This is the wrapper consumed by
   the umbrella at `IntReductionMod.lean:1254` and by the in-flight
   #4587 refactor.

   Pre-existing surface-area note (NOT introduced by #4582):
   `_factor_irreducible_of_ne_residual` is non-`private` but its
   signature references file-private names. Cleanup opportunity for
   #4587, not a blocker.

5. **Visibility audit**: `git diff 0ffd01cd^..0ffd01cd | grep
   '^[-+].*private'` shows exactly three lines — two new `private`
   helpers added (`irreducible_of_size_two_primitive`,
   `quadraticIntegerRootFactors?_residual_irreducible`) and the
   un-private of `squareFreeCore_leadingCoeff_pos_of_ne_zero` (needed
   by the bridge to discharge `hcore_pos`). No other surface-area
   expansion.

6. **Reverse primitivity bridge**
   (`IntReductionMod.zpoly_primitive_of_toPolynomial_isPrimitive`,
   `IntReductionMod.lean:970`): direction Mathlib → executable, as
   specified. Goes through `C_dvd_iff_dvd_coeff` plus `content_dvd_coeff`
   for the divisibility, applies Mathlib's `IsPrimitive` to get
   `IsUnit (content f)`, then `Int.isUnit_iff` + non-negativity of
   `Hex.DensePoly.content = Int.ofNat ∘ contentNat` rules out `-1`,
   giving `content = 1`. No `Int.gcd`-semantics reversal.

7. **Umbrella composition**
   (`factor_quadratic_branch_entry_irreducible_of_quadraticRoots`,
   `IntReductionMod.lean:1223`): four-way composition verified.
   (a) Branch-shape lemma (line 1239); (b) `_factor_irreducible_of_primitive`
   per core factor with `hcore_primitive` from
   `zpoly_primitive_of_toPolynomial_isPrimitive ∘
   normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive` (#4545)
   and `hcore_pos` from
   `squareFreeCore_leadingCoeff_pos_of_ne_zero` (lines 1242–1255);
   (c) reassembly lift (1259); (d) sign-normalisation (1263).

   `hcomplete` taken as explicit hypothesis (line 1234), as the issue
   specifies; its discharger is in flight at #4594. `hf_ne` is genuinely
   consumed (no underscore) — the slow-quadratic sibling's
   underscore-premature issue flagged in #4584 does not recur here.

### Inventory deltas

* `HexBerlekampZassenhausMathlib/IntReductionMod.lean`: 0 sorry, 0
  axiom, 0 native_decide, 0 TODO/FIXME (verified via `Grep`).
* `HexBerlekampZassenhaus/Basic.lean`: 1 sorry (line 5816,
  `isIrreducible_iff` placeholder). Pre-existing: `git show
  0ffd01cd^:...` and `git show 0ffd01cd:...` both report 1. PR adds 0
  new sorries.
* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` succeeds
  (3431/3431). Single sorry warning is the pre-existing
  `isIrreducible_iff` — no new warnings introduced.
* `python3 scripts/check_dag.py`: clean.
* `git diff --check`: clean.

### Suggested follow-ups (non-blocking)

1. Mark `_factor_irreducible_of_ne_residual` `private` for symmetry with
   `_residual_irreducible` — pre-existing un-private state, can fold
   into #4587.
2. Reconcile docstring on `_residual_irreducible` size-2 case with the
   inline-via-core proof structure — trivial documentary edit, can fold
   into #4587.

### Approval

Audit posted as
[PR #4582 comment 4469825466](https://github.com/kim-em/hex/pull/4582#issuecomment-4469825466)
and #4595 closed. No soundness, signature drift, visibility regression,
or surface-area surprises.

## Current frontier

Four of five HO-1 per-arm umbrellas are now landed and reviewed:
* small-mod singleton (#4564, reviewed),
* constant (#4572, reviewed under #4583),
* slow-quadratic (#4580, reviewed under #4584),
* fast-quadratic (#4582, reviewed under #4595 — this session).

The remaining open arm is slow-path exhaustive (#4561, blocked on
#4567 `factorsModP_monic`). The `hcomplete` discharger work is
in flight at #4585 (constant arm), #4594 (fast + slow quadratic
together).

## Next step

The natural next worker tasks are either:
1. The in-flight `hcomplete` discharger #4594 (quadratic arms,
   `feature` label) — substantial implementation work composing
   the splitter's product identity with `expandRepeatedPartFactorArray`
   consumption.
2. The `_factor_irreducible_of_ne_residual` privatisation cleanup
   folded into #4587 (in-flight refactor).
3. Start the HO-1 capstone wiring for the three reviewed-and-landed
   arms (#4170), leaving exhaustive behind a sorry.

## Blockers

None.
