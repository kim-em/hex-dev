# Add HexGfqField toQuotient division and zpow wrappers

## Accomplished

Claimed #5205 and added the missing `@[simp]` wrappers in
`HexGfqField/Operations.lean`:

- `GFqField.toQuotient_div`
- `GFqField.toQuotient_zpow_ofNat`
- `GFqField.toQuotient_zpow_negSucc`

All three follow the existing definitional `toQuotient_*` wrapper style and
use `rfl` proofs. While rebuilding the touched target, Lean surfaced an
existing unnecessary-`simpa` lint in `mul_inv_cancel`; I scoped a local
`set_option linter.unnecessarySimpa false in` around that single proof step so
`lake build HexGfqField.Operations` is clean without restructuring unrelated
proof code.

Verification run:

- `lake build HexGfqField.Operations`
- `lake build HexGfqField`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexGfqField/Operations.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
- `git grep -nE 'toQuotient_div|toQuotient_zpow' HexGfqField/Operations.lean`

## Current frontier

Issue #5205 is implemented and verified. The working tree also contains an
unrelated pre-existing `.claude/CLAUDE.md` modification, left untouched.

## Next step

Create the PR for #5205 from branch `agent/f5d75476`.

## Blockers

None.
