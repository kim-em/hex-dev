# Progress — 2026-05-30T16:58Z — session 90d5300c (`/work` → `/feature` → skip)

## Accomplished

Manual `/work` dispatch (escape hatch) — claimed directive `#5773`
(HO-5b "rewrite `hex-berlekamp-zassenhaus-performance.md` against
`hex/isabelle` ratio verdicts") as the oldest unclaimed `directive`,
oriented on its current state, and skipped with a documented
technical-blocker comment.

State of HO-5b on `main` at orient time:

- The headline section already runs `hex/isabelle` ratio verdicts on
  eight of nine registered scientific bench-target groups (split
  family public/fast/slow; degree/height matrix public/fast/slow;
  HO-2 adversarial singletons; cascade-trigger fallback-probe). Prior
  partial PRs `#5822`, `#5826`, `#5832`, `#5833`, `#5837` landed
  these surfaces.
- The §Concerns section already drops the "no comparator metadata"
  bullet and records the not-met fallback-probe verdict (`n = 13`
  adjusted ratio `7.07`) plus the cap-hit rungs `n ∈ {15, 18, 22,
  24}`.
- The internal-model verdicts are demoted to §Appendix.
- The remaining registered target `runFastPathPrecisionLocalChecksum`
  has no Isabelle pairing on `main`; it is documented in §Concerns as
  a "semantic mismatch" exclusion.

Open partial PR `#5844` (`agent/8a84e793`, auto-merge SQUASH queued,
CI green at orient time) wires six per-rung
`runIsabellePrecisionLocalRung{1..6}Checksum` registrations in
`HexBerlekampZassenhaus/Bench.lean` and adds a §"Precision-local
asymmetric ratio ladder" section to the perf report, with the
ratio interpretation declared as a tripwire (Lean measures
fast-path setup, Isabelle measures full factor on the same input;
`Lean_setup / Isabelle_full ≤ 1×` is no conclusion, `> 1×` is a
hard fail). All six table rows are `pending` — the actual sweep was
deferred for "first available quiet-load slot on `carica`".

This session's blocker:

1. `bz_isabelle` wrapper binary not present in any current
   worktree (`.cache/oracles/bz-isabelle/wrapper/bz_isabelle` is
   absent across `4a24aa41`, `90d5300c`, `b8f57f34` and the three
   `repair-*` worktrees). The previous session's worktree (`8a84e793`)
   was removed; the wrapper would have to be rebuilt fresh, including
   the AFP archive download (`afp-2026-05-29.tar.gz`, ~1 GB) and the
   GHC compile against the wrapper theory.
2. The new Lean targets exist only on PR `#5844`'s branch. To run the
   pairing sweep this session would have had to either rebase onto
   that branch (other-session ownership) or wait for `#5844` to
   auto-merge.
3. Current `carica` 1/5/15-minute load is `5.89/4.96/4.79`. The
   previous session at load `5.51` explicitly declined to run the
   sweep on quiet-hardware grounds; the SPEC-recommended threshold
   is below either of these.

Skipped via `coordination skip 5773` with the technical-blocker
reason; full state recorded in
[issue comment 4583485642](https://github.com/kim-em/hex/issues/5773#issuecomment-4583485642).

## Survey of remaining `feature`-eligible unclaimed work

- `#2564` HO-1 BZ van Hoeij CLD rewrite — architectural rewrite +
  headline correctness theorem; explicitly out of single-session
  scope.
- `#4334` HexLLL Phase 4 inconclusive scientific verdicts — blocked
  by the same `carica` quiet-load constraint that blocks `#5773`'s
  precision-local sweep. The issue body itself records prior skips
  at loads from `3.7` through repeated `10–30+` spikes.
- `#5735` HexPolyZMathlib positive-degree Grace-Walsh-Szego — depends
  on `#5765`; reduces to a one-line invocation once `#5765` lands.
- `#5765` HexPolyZMathlib higher-degree open Schmeisser source
  theorem — issue body itself declares "not a single-worker-session
  task"; two prior workers (`33fb739f`, `9c7728c3`) skipped it; next
  step is planner-level decomposition (Grace apolarity port vs.
  per-degree narrowing).

The `agent-plan`-only unclaimed items (`#5817`, `#5819`, `#5839`,
`#5842`) either require planner dispatch into a worker label or are
known-hard regions (`#5839`'s 3-term Plücker identity, `#5842`'s
~111-reference proof migration with documented `whnf` timeout risk
from prior session `4dc8257d`).

## Current frontier

`#5773` is back at `replan` for the next planner pass — the
remaining scope is captured in PR `#5844`'s description plus the
skip comment. The fallback-probe not-met verdict at `n = 13` and
`n = 11/12`'s `~4×-7×` slowdowns are the load-bearing pieces of
evidence for HO-1 (`#2564`) and HO-5d (`#5817` / `#5819` / `#5831`).

## Next step

A future session on quiet-load `carica` with `bz_isabelle` built,
after PR `#5844` merges, can:

1. Rebase to `main` (which then contains the precision-local
   targets).
2. Run the sweep command documented in PR `#5844`'s description and
   in the report's §"Precision-local asymmetric ratio ladder" §
   "Comparator run status" subsection:
   ```sh
   HEX_BZ_ISABELLE="$PWD/.cache/oracles/bz-isabelle/wrapper/bz_isabelle" \
   lake exe hexbz_bench run \
       Hex.BerlekampZassenhausBench.runFastPathPrecisionLocalChecksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung1Checksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung2Checksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung3Checksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung4Checksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung5Checksum \
       Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung6Checksum \
       --outer-trials 3 \
       --export-file reports/bench-results/hex-berlekamp-zassenhaus-<sha>-precision-local-pairing.json
   ```
3. Fill in the six `pending` rows in §"Precision-local asymmetric
   ratio ladder" with raw + adjusted ratios and the expected
   "tripwire passes" status.
4. Close `#5773`.

## Blockers

For this session: technical blocker documented above (no
`bz_isabelle`, high load, source code on unmerged branch). No
blocker for a later quiet-load session.
