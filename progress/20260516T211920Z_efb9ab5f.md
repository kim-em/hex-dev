# Session efb9ab5f — #4008 assembled per-branch factorWithBound output theorem

## Accomplished

Closed the #4008 deliverable: a single per-branch output theorem
covering every entry of `Hex.factorWithBound f B`, dispatched on the
fast / slow case-split exposed by `factorWithBound_entry_mem_raw_source`.

* `HexBerlekampZassenhaus/Basic.lean`
  - de-privatised `factorFastFactorsWithBound` and
    `factorSlowFactorsWithBound` so the bridge layer can express
    per-branch hypotheses;
  - added Mathlib-free
    `reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`:
    given `reassemblyExpansionComplete d coreFactors` and irreducibility
    of every entry of `coreFactors`, every raw factor in
    `reassemblePolynomialFactors d coreFactors` is `ZPoly.Irreducible`.
    The `X`-power half discharges through
    `xPowerFactorArray_irreducible`.

* `HexBerlekampZassenhausMathlib/Basic.lean`
  - de-privatised `zpolyIrreducible_normalizeFactorSign_of_zpolyIrreducible`
    so the assembled output theorem can apply it to lift raw-factor
    irreducibility through the `collectFactorMultiplicities`
    sign-normalisation step;
  - added the assembled output theorem
    `factorWithBound_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`
    and its default-precision specialisation
    `factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`.

The single `h_raw` hypothesis asks the consumer to prove raw-factor
irreducibility for whichever path fires (fast BHKS output when
`factorFastFactorsWithBound = some _`, slow exhaustive output when it is
`none`).  Downstream consumers compose this hypothesis from the existing
per-branch core-irreducibility theorems (#4004/#4005/#4006/#4007) via
`reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`
together with `reassemblyExpansionComplete`.

## Current frontier

The assembled output theorem is in place and consumable by:

* `factorWithBound_entries_irreducible` (#3987, now unblocked);
* `factor_irreducible_of_nonUnit` (#4170 HO-1 capstone, the
  `factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`
  specialisation matches its target shape).

## Next step

Pick up #3987 next: it now has both #3996 (foundational `ZPoly.Irreducible`
witnesses) and #4008 (this PR) merged.  The remaining work is to
discharge `h_raw` by case-analysing the fast/slow branch dispatch and
threading each sub-branch's core-irreducibility theorem through
`reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`.

## Blockers

None for this session.
