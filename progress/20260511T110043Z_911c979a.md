# 2026-05-11 11:00 UTC — work session (#3307)

## Accomplished

Claimed #3307 and worked in `HexPolyFp/SquareFree.lean` on the derivative-active
provider for `yunFactorsLevelCompletes`.

Added `yunFactorsLevelCompletes_of_derivative_active_state`, a reusable partial
specialization of `yunFactorsLevelCompletes_of_size_bound` for the initial
derivative split. It handles the `fuel = 0` contradiction from `hzero`/`hfuel`,
constructs the derivative-split `yunFactorsPairwiseReachable` witness, derives
the initial `c.size + g.size` fuel bound from `div_gcd_mul_reconstruct`, and
applies the recursive completion theorem under an explicit state-provider
hypothesis.

Verified with:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep reports only the pre-existing tracked
`squareFreeAuxRevContribution_derivative_active_pow_obligation` `sorry`.

## Current frontier

The derivative-active completion is not fully closed. The remaining frontier is
the concrete state-provider proof required by the generic size-bound theorem:
for every derivative-split reachable Yun state, prove
`squareFreeContributionReachable c ∧ c.isZero = false ∧ w.isZero = false`.

## Next step

Prove the reachable-state provider separately, likely by factoring smaller
lemmas for nonzero quotient/tail preservation and the size-one current
polynomial case before using it to remove the explicit `hstate` parameter from
the derivative-active specialization.

## Blockers

No tooling blockers. The attempted all-in-one local state-provider proof
compiled too slowly and exposed the need for the state argument to be factored
into smaller lemmas.
