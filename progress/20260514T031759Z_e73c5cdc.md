# 2026-05-14 03:17 UTC — feature session (e73c5cdc) on #3891

## Accomplished

- Added `squareFreeCore_normalizeFactorSign_of_ne_zero` and
  `squareFreeCore_shouldRecord_of_degree_pos` helpers next to the
  existing `squareFreeCore_*` cluster in
  `HexBerlekampZassenhaus/Basic.lean`.
- Proved `factorFastWithBound_product_of_constant_branch`, mirroring
  the recently-landed slow analog with the same
  `factorizationOfFactors_product_of_filtered_product` + append-`#[1]`
  pattern.
- Introduced shared helper
  `factorFastWithBound_product_of_squareFreeCore_emit` that handles
  any fast branch emitting `prefix ++ #[squareFreeCore]` with
  positive-degree core (normalize + recordable for prefix landed in
  PR #3911, for the core via the new helpers above).
- Proved `factorFastWithBound_product_of_small_mod_branch` for the
  `primeData.factorsModP.size ≤ 1` branch (both `B = 1` and `B ≥ 2`
  forks), reusing the shared emit helper.

## Decomposition

Created three sub-issues for the remaining branches and assembly:

- #3935 — quadratic branch dispatch (may share helpers with #3913
  slow-path quadratic)
- #3936 — BHKS core-success branch dispatch (canonicality of
  `factorFastCoreWithBound` output; the hard one)
- #3937 — final assembly (blocked on #3935 and #3936)

Released claim on #3891 via `--partial` PR; planner will decide
whether to close it as fully covered by the sub-issues or narrow to
residual.

## Current frontier

- BZ-core sorry count unchanged at 2 (the two original
  `factor_product_of_bound` and `isIrreducible_iff` placeholders at
  3832 and 3847).
- New helpers and dispatch lemmas compile cleanly under
  `lake build HexBerlekampZassenhaus.Basic` and
  `lake build HexBerlekampZassenhaus`.

## Next step

Next worker on this cluster picks up #3935 or #3936 (independent),
then #3937 once both land.

## Blockers

None.
