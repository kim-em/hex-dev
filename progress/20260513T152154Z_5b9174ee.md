# Session 5b9174ee — HexPolyFp derivative-active state strategy (#3736)

## Accomplished

- Checked the human-oversight queue first. All visible human-oversight
  directives were either dependency-blocked or marked for replan, so none were
  claimable.
- Claimed and re-skipped stale or invalid executable issues encountered while
  selecting work:
  - #3627: depends on a Mathlib bridge benchmark/report premise that was
    superseded by the Mathlib-free benchmark doctrine and HO-33.
  - #3563: its solve-column target treats Gram-Schmidt projection coefficients
    as original-row coordinates in a Gram system, which does not match the
    current `coeffs` semantics.
  - #3526: already implemented on current `main` by the existing
    `ForwardRecoveryInputs` to `factorFast ≠ none` bridge in
    `HexBerlekampZassenhausMathlib/Recovery.lean`.
  - #3284: blocked by the newer derivative-active state-strategy split because
    the final unconditional assembly would need the rejected universal raw-state
    provider.
- Claimed #3736 and added
  `yunFactorsLevelCompletes_of_derivative_active_stateProvider` in
  `HexPolyFp/SquareFree.lean`.
- The new wrapper restricts the state payload to
  `yunFactorsDerivativeActiveReachable` states generated by the derivative split
  and Yun tail steps, avoiding a universal provider over arbitrary raw reachable
  states.

## Current frontier

#3736 now exposes the intended weakened completion surface for downstream
derivative-active work. The existing
`squareFreeAuxRevContribution_derivative_active_pow_obligation` sorry remains
unchanged and is still the tracked final pow assembly hole.

## Next step

Land this PR, then unblock #3737 so it can wire the normalized/derivative-active
completion helper against `yunFactorsLevelCompletes_of_derivative_active_stateProvider`.

## Blockers

- The final derivative-active pow branch still depends on follow-up wiring and
  the existing tracked proof hole in `HexPolyFp/SquareFree.lean`.
- Several old queue items need planner triage because they refer to stale
  benchmark doctrine or already-implemented proof surfaces.
