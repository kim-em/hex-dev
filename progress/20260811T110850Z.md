# Accomplished

- Repaired the generic fact fold so its current program version is established
  by the leading instance event and threaded independently through every rule
  and equality-transport replay.
- Added a negative mutation test showing that a transport which claims a
  different program version is rejected.
- Updated the SPEC to state this version provenance explicitly.

# Current frontier

The one-instance canary now authenticates later fact events against its
independently carried version. Multiple instance events still require a
program-and-version state transition.

# Next step

Generalize the fold state to reconstruct programs and advance the version at
each instance event.

# Blockers

None.
