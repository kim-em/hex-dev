# 20260603T034948Z work #6418

## Accomplished

- Claimed issue #6418 (BHKS D1 final assembly: prove `factorFast_terminates`
  after closed producers and prime-search resolution) and landed the canonical
  conditional theorem name in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Renamed the existing prime-search-safe conditional final theorem from
  `factorFast_terminates_of_factorFastCapSeparationInputsAndCanonicalRecoveryTailInputs`
  to the SPEC-canonical name `factorFast_terminates`, with the same conditional
  signature consuming `(primeData, rows_pos, trueSupports, capInputs,
  recoveryInputs)`.  The signature is the narrowed form required by #6415
  (which documented `Hex.finitePrimeSearchNoneQuadratic` as an executable
  counterexample to the false hypothesis-free target).
- Updated the docstring to (a) cross-reference SPEC D1 and the
  `finitePrimeSearchNoneQuadratic` counterexample explaining why the
  hypothesis-free target is unsound, and (b) describe the existing assembly
  chain that the body composes — `factorFast_ne_none_of_factorFastCapSeparationInputsAndCanonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`
  → `factorFast_ne_none_of_canonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`
  → `factorFast_ne_none_of_core_recovery_on_schedule`, whose inner case-split
  on `factorFastFactorsWithBound` at `Hex.factorFastPrecisionCap f`
  discharges the constant, small-mod-singleton, quadratic short-circuit
  branches via their unconditional `some _` returns, and the BHKS branch via
  the recovery-on-schedule witness extracted from the closed packages.
- Did not change executable algorithms; no new `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME` introduced.

## Current frontier

`HexBerlekampZassenhausMathlib.BHKS.factorFast_terminates` is now the
canonical final BHKS D1 target for the current executable `factorFast` API.
Callers supply prime-data plus the two closed input packages
(`FactorFastCapSeparationInputs`, `CanonicalRecoveryTailInputs`) and get
`Hex.factorFast f ≠ none`.  The remaining mathematical obligations are
producing concrete instances of those two packages for specific inputs
(cap-separation bridge / cut / analytic comparison witnesses on one side,
canonical-support representations and expected-true-factor packages on the
other).

## Next step

Construct concrete (per-input or universal) producers for
`FactorFastCapSeparationInputs` and `CanonicalRecoveryTailInputs` for
non-pathological inputs, so that `factorFast_terminates` becomes applicable
without further hypotheses on the input polynomial beyond the
`choosePrimeData?` success witness.

## Blockers

None for this slice.  The deliverables of #6418 are all satisfied: a
canonically-named `factorFast_terminates` conditional theorem exists, its
proof composes the existing case-split chain over
`factorFastFactorsWithBound` branches, and the nontrivial BHKS branch
routes through the existing closed-package wrappers from #6416 and #6417.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed (exit 0).
- `git diff --check` passed.
- `git grep -n 'factorFast_terminates' HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  finds the single canonical theorem declaration.
- Added-line forbidden-token grep over `Recovery.lean` found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
