## Accomplished

Claimed #2983, confirmed it was stale because `Hex.GramSchmidt.Int.basis_span`
is already proved on current `origin/main`, and marked it for replan with that
reason.

Claimed #2990 and added
`derivativeSplit_quotient_common_dvd_derivative_one` in
`HexPolyFp/SquareFree.lean`. The theorem assembles the existing quotient
common-divisor-to-gcd step, the power successor lemma, the nonzero gcd helper,
and `dvd_one_of_all_powers_dvd_nonzero` to prove that any common divisor of the
square-free quotient and its derivative divides `1`.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  reports only the pre-existing unrelated sorries at lines 2675, 3696, 3766,
  and 3823 after this edit.

## Current frontier

The quotient common-divisor theorem requested by #2990 is now proof-bearing.
The surrounding Yun pairwise-invariant plumbing still needs a stronger
recursive common-divisor fact for all reachable Yun states before its remaining
holes can be discharged.

## Next step

Prove the recursive reachable-state common-divisor theorem needed by
`yunFactorsPairwiseInvariant_of_derivative_split_common_dvd_one`, or split that
stronger statement into a new focused issue if it proves larger than one
session.

## Blockers

None for the theorem added in this session.
