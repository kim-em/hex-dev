# Progress: #6923 — review PR #6920 (BZ recovery scaled-recovery decoupling)

Review session. No source changes; verdict posted as a comment on PR
#6920 (self-approval blocked on the shared agent account).

## Accomplished

Reviewed PR #6920 (`BZ recovery: decouple stale scaled recovery lemmas
from representation predicate`), which closes #6888 on the directive
#2564 recovery-model migration. Verdict: **approve**, no blocking
findings.

The PR swaps the hypothesis of ~9 stale scaled-product recovery lemmas
in `HexBerlekampZassenhausMathlib/Basic.lean` from
`RepresentsIntegerFactorAtLift core d factor S` to the explicit old-shape
congruence
`reduceModPow (scaledLiftedFactorProduct core d S) d.p d.k = reduceModPow factor d.p d.k`.

## Key finding (recorded on the PR)

The swap is **definitionally transparent**: `RepresentsIntegerFactorAtLift`
is defined (`Basic.lean:2283-2287`) as *exactly* that congruence, so the
old code typechecked only because `hrep` was defeq to it. The lemmas
already threaded it straight into `centeredLiftPoly_eq_of_reduceModPow_eq`,
which consumes the raw `reduceModPow = reduceModPow` shape. Making it
explicit is precisely the decoupling #6888 wants: when #6890 redefines the
predicate to the `RecoveredAtLift` monic-coordinate carrier
(`Basic.lean:2298`), these stale lemmas are unaffected.

Soundness checks:
- All conclusions byte-identical; no theorem weakened.
- `RepresentsIntegerFactorAtLift` not redefined (out of scope; #6890's job).
- No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
- `scripts/check_dag.py` → exit 0.
- No `hrep :=` named-argument call sites, so the `hrep`→`hscaled` rename
  is safe.
- The one pre-seam external call site (`Basic.lean:14570`) passes its
  local `hrep` positionally and still elaborates by defeq — no error
  before the pre-existing #6804 seam.

## Method note

Verified by static analysis (definition + call-site/defeq audit) rather
than a full `lake build`: the `HexBerlekampZassenhausMathlib` Mathlib
layer is not CI-built and a whole-library build is expected-red at the
#6804 `scale`→`dilate` seam, so a build adds no signal over the defeq
argument. Author reports the build reaching only that seam, now at
`Basic.lean:16475`.

## Current frontier

Review complete. PR #6920 is ready to merge once ubuntu `build` /
`conformance` finish green (`build-macos` already green; these checks do
not exercise the Mathlib layer).

## Next step

On merge, #6888 closes and #6890 (redefine the predicate to the
monic-coordinate carrier) becomes unblocked.

## Blockers

None. CI build/conformance were still in progress at review time.
