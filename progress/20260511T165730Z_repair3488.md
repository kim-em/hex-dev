# Repair PR #3488 merge conflict

## Accomplished

- Claimed PR #3488 for repair.
- Checked out `agent/2ec88212` and merged current `origin/main`.
- Resolved the `HexPoly/Euclid.lean` conflict by dropping duplicate finite
  coefficient-family helpers that now exist earlier on `main` and keeping the
  current convolution-to-truncated-product bridge.
- Verified with `lake build HexPoly.Euclid`, `lake build HexPoly`,
  `python3 scripts/check_dag.py`, `git diff --check HEAD`, and a banned-marker
  scan for `native_decide`/`axiom` in `HexPoly/Euclid.lean`.

## Current frontier

- PR #3488 has a local merge commit resolving the conflict and is ready to push.
- The unrelated local `.claude/CLAUDE.md` edit remains unstaged.

## Next step

- Push `agent/2ec88212` with `--force-with-lease` and mark PR #3488 salvaged.

## Blockers

- None.
