# Session 77de1d27 — issue #3737

## Accomplished

- Claimed issue #3737.
- Updated `HexPolyFp/SquareFree.lean` so
  `yunFactorsLevelCompletes_of_derivative_active_reachable` consumes only the
  restricted `yunFactorsDerivativeActiveReachable` call-path state provider.
- Replaced the duplicate state-provider wrapper with
  `yunFactorsLevelCompletes_of_derivative_active_initial_split`, a direct
  initial-split helper for `g = gcd f f'` and `c = f / g`.
- Verified with:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

The normalized derivative-active completion helper is wired for downstream use.
The only banned-marker scan result is the pre-existing tracked
`squareFreeAuxRevContribution_derivative_active_pow_obligation` hole.

## Next step

Use the new initial-split helper in the follow-on derivative-active pow branch
assembly issue (#3738).

## Blockers

- None for this issue.
