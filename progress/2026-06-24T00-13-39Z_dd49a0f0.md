## Accomplished

Added M1 recovery-data fast-core endpoints in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`:

- `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveryData`,
  which feeds per-support `monicTarget factor` product congruences through
  `honestCongr_of_product_congr_monicTarget` into
  `BHKS.cutProjectionHypotheses_of_recoveryData`.
- `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_coreLiftDataRecoveryData`,
  which specializes the path to `Hex.ZPoly.coreLiftData core B primeData` and
  calls `coreLiftData_subset_congr_monicTarget` for each support before using
  the honest-scale bridge.

Verified with `lake build HexBerlekampZassenhausMathlib`.  The build completed
successfully with pre-existing warnings.  The diff adds no `sorry`, `axiom`, or
`native_decide`.

## Current frontier

The M1 fast-core irreducibility route now has a direct call-site that threads
the #8327 subset congruence and #8328 honest-scale correspondence into
`cutProjectionHypotheses_of_recoveryData`, without extracting scale congruence
from the dilation-coordinate `RecoveredAtLift` witness.

## Next step

Use the new `coreLiftDataRecoveryData` endpoint from the higher-level fast-path
capstone once the surrounding caller has the per-support `monicTarget factor`
mod-`p` representation/product data and cofactor leading-coefficient split.

## Blockers

No code blockers.  The worktree still contains the pre-existing dirty
`.claude/CLAUDE.md`, which is off-limits and was not touched.

`/reflect` was requested by the session template but is not available in this
environment (`zsh: no such file or directory: /reflect`).
