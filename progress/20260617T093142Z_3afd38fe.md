# Progress — 2026-06-17 (session 3afd38fe, review #7721)

## Accomplished

Audited `cutProjectionHypotheses_of_trueFactors`
(`HexBerlekampZassenhausMathlib/Lattice.lean:1547`) and the keystone helpers it
threads (`mem_prefixSubmodule_of_normSq_le:1213`,
`projFirst_mem_projectedRowSpanInt_of_mem_prefixSubmodule:1518`). **Verdict:
PASS.** Posted to #7721 with file:line evidence and closed the issue.

### Findings (all three deliverables)

1. **Norm bridge faithful (factor-of-4 preserved).** `hnorm` (`:1563`) consumes
   `TrueFactorCLDTightNormBound.four_mul_sq_norm_le`, which is genuinely the
   tight `4·∑ v[i]² ≤ bhksCutRadiusSq4` form (`:715`), not a relabelled loose
   bound. The ℝ→ℤ→ℚ cast chain (`hsum`/`hZ`) keeps the `4·` factor and all `≤`
   (no `<` slip, no sign error). The keystone's hypothesis is the same tight
   `4·‖v‖²` shape (`:1220`).

2. **Projection index-faithful.** `hproj` (`:1580`) uses `projFirst` =
   `Fin.castAdd L.coeffWidth` (value-preserving on the first `factorCount`
   block), and `project_eq` (`:303`) is stated at index `⟨i.val, …⟩` — same
   value, no off-by-one across the `factorCount`/`coeffWidth` split. The
   `(bhksProjectedRows L).factorCount = L.factorCount` identity is definitional,
   so no support cast swaps the indicator.
   `projFirst_mem_projectedRowSpanInt_of_mem_prefixSubmodule` transports
   prefix-span membership by span-induction over actual reduced rows; the
   lattice hypothesis is not dropped.

3. **No CutRetention re-entry; no vacuous arm.** Proof term routes through
   `mem_prefixSubmodule_of_normSq_le` / `projFirst_…` / `project_eq` /
   `traceReducedMatrix_memLattice_iff` only — never `CutRetention` or
   `cutProjectionHypotheses_of_retention`. `hbasis` is load-bearing (feeds
   `hind` → keystone's `gramDet_pos`). Both `data` and `tight` arms are
   consumed and neither is vacuously satisfiable (both constrain the concrete
   `trueFactorCLDVector`).

### Why this is the *sound* route

The refuted `CutRetention` shape concluded sublattice membership from a norm
bound **alone**. This producer does not: `mem_prefixSubmodule_of_normSq_le`
requires **both** `hmemRed : memLattice reduced v` (structural, from
`trueFactorCLDVector_memLattice`) **and** the tight norm bound. The keystone
`exists_top_index_normSq_le_of_memLattice` (`HexGramSchmidt/Int.lean:7229`) is
the standard `‖b*_k‖² ≤ ‖v‖²` fact via integer-coefficient `c[k]²≥1` and
orthogonality. Sound.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Lattice` — clean.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token scan over the route files (Lattice / GramSchmidt.Int /
  HexLLLMathlib.Basic) — none.

## Current frontier

Producer #7721 is sound. Consumer audit #7717 (of #7675) treats it as a given;
nothing here invalidates that.

## Next step

None for this review. No code or docstring change was warranted (the "does not
pass through CutRetention" docstring and the tight-bound docstrings are
accurate).

## Blockers

None.
