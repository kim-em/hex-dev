# 20260603T081023Z work #2567

## Accomplished

Added the sum-shape counterpart of the recent `*PointwiseAuxiliaryBoundsAndPaperThreshold`
constructors in `HexBerlekampZassenhausMathlib/Recovery.lean`:

- `FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold` —
  packages the cap-separation input record from the explicit finite-sum bound
  on the corrected auxiliary squared-l2 (the same RHS shape consumed by
  `ofBridgeDataCorrectedAuxiliaryL2normSq`) together with a paper-threshold
  bound on `coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree`.
  Internally composes `ofBridgeDataCorrectedAuxiliaryL2normSq` with
  `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree` (which discharges
  the strict cap-arithmetic comparison from `hpaper`, the choose/precision
  equations, and `C ∈ [0, 2]`).
- `factorFast_terminates_ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold` —
  final HO-4 wrapper threading the sum-shape constructor through
  `factorFast_terminates`, exposing `Hex.factorFast f ≠ none` directly from the
  sum-form bound + `hpaper` + recovery tail package.

The new wrapper is the sum-shape twin of the existing pointwise wrapper
`factorFast_terminates_ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
(commit `ce48ab75`, PR #6466). Callers who already have a closed BHKS Lemma
3.2 sum-form estimate can now route through it without first decomposing the
bound into separate pointwise vector and correction ingredients.

## Current frontier

The HO-4 final-assembly surface now offers both pointwise and sum-shape final
wrappers consuming the deepest available BHKS Theorem 5.2 ingredients (the
cap-lift bridge data plus a paper-threshold-shape estimate). Both bottom out
in `factorFast_terminates` (which combines `FactorFastCapSeparationInputs`
with `CanonicalRecoveryTailInputs`) and bypass the intermediate
`FactorFastCapSeparationInputs` record on the caller side.

Remaining HO-4 work is unchanged: bound the canonical
`coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree` product
by `bhksPaperThresholdReal squareFreeCore C` for the actual cap-lift bridge
(the genuine BHKS §5 inequality content, identified by the previous session as
"the largest remaining analytic gap"), and produce the sum-form (or pointwise)
auxiliary squared-l2 estimate from BHKS Lemma 3.2.

## Next step

The two remaining analytical providers are independent and can be tackled in
either order:

1. **`hpaper` from cap-lift bridge data.** Prove the
   `coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree ≤
   bhksPaperThresholdReal squareFreeCore C` inequality for the actual cap-lift
   `badVectorWitnessOfFactorFastCapLiftData`. This is the genuine BHKS §5
   Theorem 5.2 inequality content.

2. **`hauxiliary_sq_bound` from BHKS Lemma 3.2 sum form.** Produce the sum-form
   (or pointwise) corrected auxiliary squared-l2 bound for the cap-lift bridge
   data, bottoming out in BHKS Lemma 3.2 (resultant-based bad-vector size).

## Blockers

No new technical blocker.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus` passed.
- `git diff` shows only additions (238 lines, no deletions).
- Added-line forbidden-token grep over the touched file found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
