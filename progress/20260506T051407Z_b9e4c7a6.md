# 2026-05-06 05:14 UTC — work session (b9e4c7a6)

## Accomplished

Processed the unclaimed queue under the worker flow.

Attempted to claim the open `human-oversight` issue #2245 first, but the claim
lost a race and `coordination` reported it was already claimed by another
agent.

Claimed #2437 and found it oversized and mislayered as written. The issue mixed
array/structured-loop correspondence, row-pivot determinant soundness, and API
cleanup, while the no-pivot Bareiss proof and Desnanot-Jacobi infrastructure
live in `HexMatrixMathlib` rather than the Mathlib-free `HexMatrix` core.
Decomposed it into:

- #2438 — prove the Bareiss array loop matches the structured pivot loop.
- #2439 — prove row-pivoted Bareiss structured-loop soundness in
  `HexMatrixMathlib`.
- #2442 — resolve the orphan core `bareiss_eq_det` theorem surface after the
  Mathlib-side theorem exists.

Claimed #2438 and landed a partial first slice in `HexMatrix/Bareiss.lean`:

- added `getEntry_matrixToRows`;
- added `rowsToMatrix_matrixToRows`;
- added public `bareiss_eq_bareissData_det`, separating the executable array
  determinant from the packaged `BareissData.det` proof target.

Verified:

- `lake build HexMatrix.Bareiss`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg "^axiom |^private axiom |native_decide|TODO|FIXME" HexMatrix/Bareiss.lean`

The only Lean warning in the touched file is the pre-existing
`Hex.Matrix.bareiss_eq_det` `sorry`.

## Current frontier

#2438 remains partial. The next missing bridge is the row-operation and loop
correspondence layer:

- array `set!`/`get!` lemmas for `swapRowsArray`;
- `stepArray` correspondence with `stepMatrix`;
- `findPivotArray?` correspondence with `findPivot?`;
- a loop-level bridge from `pivotArrayLoop` to `pivotLoop`.

## Next step

Continue #2438 by proving focused `Array.get!`/`setIfInBounds` helper lemmas
for in-bounds matrix rows, then use them to prove
`swapRowsArray (matrixToRows M)` corresponds to `rowSwap M`.

## Blockers

No external blocker. The main technical obstacle is that `swapRowsArray` and
`stepArray` use `Array`'s unchecked `get!`/`set!` API, so the bridge needs
explicit in-bounds lemmas before simple matrix extensionality proofs will
close.
