# Progress - 2026-06-16T15:18:12Z (session 2ba8ac5f, feature #7599)

## Accomplished

Claimed #7599 after the higher-priority directive #2564 claim failed because
the directive is still dependency-blocked.

Updated `HexPolyFp/Basic.lean` to expose the FpPoly evaluation simp surface:

- Added public `@[simp]` `eval_zero`.
- Promoted `eval_C`, `eval_X`, and `eval_monomial` to `@[simp]`.
- Promoted `eval_add` and `eval_sub` to `@[simp]`.
- Added public `@[simp]` `eval_neg`, proved by unfolding `DensePoly.neg` and
  using the existing subtraction evaluation theorem plus the `ZMod64`
  `ZeroSubNegLaw` instance.
- Promoted `eval_C_mul` and `eval_mul` to `@[simp]`.
- Made `eval_one` public and `@[simp]`.

Committed the code change as `feat: expose FpPoly eval simp lemmas`.

Verification completed:

- `lake build HexPolyFp`
- `lake build` (green; existing unnecessary-`simpa` warnings in
  `HexGFqRing/Operations.lean`)
- `python3 scripts/check_dag.py`
- `rg -n "\bsorry\b|\baxiom\b|\bnative_decide\b|TODO|FIXME" HexPolyFp/Basic.lean`
  returned no matches
- `git diff --check`

## Current frontier

The issue deliverables are complete. The full requested simp promotion stayed
green, so no fallback comments for intentionally untagged homomorphism lemmas
were needed.

The worktree still has a pre-existing uncommitted `.claude/CLAUDE.md` change
that this session did not touch.

## Next step

Create the PR for #7599 with the code commit and this progress file.

## Blockers

No blockers for #7599. The worker-flow `/reflect` and second-opinion commands
were not available as callable commands in this non-interactive Codex session.
