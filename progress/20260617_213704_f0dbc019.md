# Progress — 2026-06-17 (session f0dbc019, /work → /feature #7799)

## Accomplished

Discharged #7799: the positive-degree Mathlib Berlekamp factor irreducibility
bridge. Removed the `irreducible_of_mem_berlekampFactor` sorry and threaded the
positive-degree guard through the Zassenhaus Mathlib consumer chain.

### `HexBerlekampMathlib/Basic.lean` (deliverables 1–3)

- `natDegree_toMathlibPolynomial_pos_of_degree?_pos`: a factor with positive
  executable degree transports to positive Mathlib `natDegree` (leading
  coefficient nonzero, transport injective).
- `irreducible_of_mem_berlekampFactor` now takes `hf_pos : 0 < f.degree?.getD 0`
  and `[Fact (Nat.Prime p)]`, deriving each factor's positivity via
  `berlekampFactor_factors_pos_degree` and routing through the executable
  producer `berlekampFactor_factors_irreducible` (#7802) plus the transport
  primitive. The explicit `[Field]` was dropped in favour of `[PrimeModulus]`
  so the field instance matches the producer's (`zmod64FieldOfPrime`), avoiding
  an instance diamond on the `.factors` term.
- `irreducible_of_mem_berlekampFactor_of_gcd_eq_one` and
  `irreducible_of_berlekampFactor_factors_length_le_one` carry the same guard.

### Zassenhaus chain (deliverable 4)

- `monicModularImage_modP_degree?_pos_of_factorsModPBerlekampForm`: extracted
  from the inline proof of `factorsModP_natDegree_pos_of_factorsModPBerlekampForm`
  (which now reuses it). Derives positivity of the monic modular image from a
  positive-degree input under `isGoodPrime`.
- Threaded `0 < core.degree?.getD 0` from
  `factors_irreducible_of_factorsModPBerlekampForm` /
  `factors_irreducible_of_choosePrimeData_of_some` up through
  `exists_factor_of_modPIndex`, `existsUnique_modPFactorSubset_of_choosePrimeData(_of_some)`,
  `modPSubsetPartitionHypotheses_of_choosePrimeData`, `modPFactor_index_cover`,
  `representsModP_correspondent`, the `henselSubsetCorrespondence*` /
  `liftedFactorSubsetPartition*` success-descent and outer-bound lemmas
  (`Basic.lean`), and the singleton/disjoint lemmas (`IntReductionMod.lean`).
- The thread terminates where positivity is already in scope: the toMonic path
  (`hcore_lc_pos`/`hcore_pos`, bridged via `toMonic_monic_degree_eq_of_pos_degree`),
  and the exhaustive/singleton branch-entry lemmas, which already derive
  `0 < (normalizeForFactor f).squareFreeCore.degree?.getD 0` internally from the
  branch-shape degree guard.

Deliverable 5 (`factor_irreducible_of_nonUnit`) needs no change: it remains a
pre-existing sorry and does not consume the chain's conclusion shape.

## Verification

- `lake build HexBerlekampMathlib` → success.
- `lake build HexBerlekampZassenhausMathlib` → 3790 jobs, success.
- `lake build HexBerlekamp` → success.
- `grep -c sorry HexBerlekampMathlib/Basic.lean` → 0 (target removed).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/`TODO`/
  `FIXME`. No new unused-variable warnings (all added positivity hypotheses are
  used).

## Current frontier

`irreducible_of_mem_berlekampFactor` is sorry-free and the whole
`HexBerlekampZassenhausMathlib` (merge-gating) library stays green with the
guard threaded.

## Next step

The capstone `factor_irreducible_of_nonUnit`
(`HexBerlekampZassenhausMathlib/FactorSoundness.lean`) remains a sorry; closing
it is the next headline for the integer-side factorization soundness.

## Blockers

None for this issue.
