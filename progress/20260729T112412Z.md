# Semantic replay and policy-session merge

## Accomplished

- Merged exact policy-session tip `c538f5cd` into semantic-replay head
  `b16b69f2`.
- Preserved the representation-free arbitrary-function policy vertical,
  including structural matching, dynamic scoped contraction, external policy
  choice, and immutable instance, equality, and fact recipes.
- Preserved the sealed semantic registry and exact
  `(RuleKey, role, schema)` fact-schema coverage and dispatch contract.
- Resolved the Lake target conflict as a strict union of structural matcher,
  policy session, policy function, and semantic replay modules and
  conformance targets.
- Retained both policy-session and semantic-replay SPEC sections without
  introducing a central function switch or interval-representation dependency.
- Built the focused semantic replay, policy session, and policy function
  targets, followed by the 39-job interval, matcher, scope, policy, registry,
  dyadic, payload, and semantic conformance set.
- Rechecked the merge diff, conflict markers, and banned proof tokens.

## Current frontier

The representation-free policy vertical and package-owned semantic replay now
coexist on one branch. Frozen policy recipes retain exact package replay
addresses, while generic semantic fact dispatch remains function-agnostic.

## Next step

Contribute theorem schemas for the policy function package at its exact retained
fact address, then add instance and equality semantic schemas and chronological
event replay without introducing a generic sine or rational case split.

## Blockers

None. The policy function recipes are structurally retained but are not yet
decoded into typed semantic evidence; instance and equality formats remain
explicitly deferred.
