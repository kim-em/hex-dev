# HO-1 hcore_monic Gap 1 review after helper-5 substrate

Session: `6e5eb88f` (work, issue #5625)

## Accomplished

Claimed review issue #5625 and re-read the requested context:
#2564, #4880, #4940, and #4946, plus the current relevant declarations in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`,
`HexBerlekampZassenhausMathlib/Basic.lean`, and
`HexBerlekampZassenhaus/Basic.lean`.

Confirmed the post-#4946 state:

- helper 1's primitive/positive-leading product-congruence sibling is present;
- helper 5's executable primitive/positive-leading substrate is present;
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
  exists, but intentionally still retains `hcore_monic`;
- helpers 2-4 remain the real blocker because the Hensel-lift substrate still
  requires per-output monicness, matching #4940's architectural finding.

Posted the review recommendation on #2564 and #4880:

- try Option B first, the executable-side monicising-by-scaling path;
- keep Option A as viable but larger, because it replaces the
  `hd_liftedFactor_monic` invariant through the recombination proof chain.

Created follow-up feature issue #5627:
`HO-1 Gap 1: add executable monicised-core transform for Hensel lift refactor`.

## Current frontier

The next concrete Gap 1 implementation frontier is #5627: add a small,
Mathlib-free executable monicised-core transform in
`HexBerlekampZassenhaus/Basic.lean`, without yet wiring it into
`henselLiftData` or `exhaustiveCoreFactorsWithBound`.

## Next step

A feature worker should claim #5627 and implement the monicised-core transform
plus local sanity lemmas/guards. After that lands, a follow-up can decide
whether the transform is wired through `henselLiftData` directly or through
`exhaustiveCoreFactorsWithBound`.

## Blockers

No blocker for the review itself. The underlying Gap 1 remains blocked on
the helper-2/3/4 Hensel-lift monicness chain until an Option B executable
monicising refactor or the larger Option A invariant redesign lands.

Verification completed:

- `gh issue view 2564 --comments --json comments --jq '.comments[-1].body'`
- `gh issue view 4880 --comments --json comments --jq '.comments[-1].body'`
- `gh issue view 5627 --json title,body,labels,state --jq '{title:.title,state:.state,labels:[.labels[].name], body:.body}'`
