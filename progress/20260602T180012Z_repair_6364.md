# 20260602T180012Z repair #6364

## Accomplished

- Claimed PR #6364 for repair.
- Merged current `origin/main` into `agent/14532541-v2`.
- Resolved the conflict in `HexBerlekampZassenhausMathlib/Recovery.lean` by
  keeping the `CanonicalRecoveryInputs` packaging from `main` and retaining the
  PR's cap-positive canonical-support wrapper.
- Removed the duplicate `one_le_factorFastPrecisionCap` declaration introduced
  by overlapping base and PR changes in `HexBerlekampZassenhausMathlib/BHKSBound.lean`.
- Updated the indicator-side wrapper call in `Recovery.lean` to pass
  `hcomparison.hadamard_l2norm_lt_divisor` to the raw cap-separation theorem.

## Current frontier

PR #6364 is locally repaired against the current base and ready to push after
committing this merge repair.

## Next step

Push the repaired PR branch with `--force-with-lease` and mark PR #6364
salvaged.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `python3 scripts/check_dag.py` passed.
- `lake build` passed.
- `git diff --check` passed.
