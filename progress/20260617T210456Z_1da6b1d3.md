# Progress — 2026-06-17 21:04:56 UTC (session 1da6b1d3, /work → /feature #7835)

## Accomplished

Claimed #7835 and added the aggregate CLD tail substrate for the BHKS lattice
correction.

- `HexBerlekampZassenhaus/Basic.lean`: added `aggregateCldTail`, which folds the
  selected lifted factors' `cldQuotientMod` polynomials first and applies
  `psiCut` once to the aggregate coefficient.
- Added #7817 wraparound guards showing `psiCut 5 4 1 (311 + 311)` stays small
  while the sum of two separately cut residues is large.
- `HexBerlekampZassenhausMathlib/Lattice.lean`: added `selectedFactors` for a
  proof support and definitional bridge lemmas exposing `aggregateCldTail` as a
  single cut over the folded `cldQuotientMod` sum.

Verification:

- `lake build HexBerlekampZassenhaus` succeeded.
- `lake build HexBerlekampZassenhausMathlib.Lattice` succeeded. It reported only
  pre-existing warnings in `HexGramSchmidtMathlib` / `HexLLLMathlib`, not in the
  touched files.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` clean.
- Added-line forbidden-token scan over touched Lean files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The executable aggregate-tail helper and proof-facing unfolded form are present.
Current `BhksLatticeBasis` still stores per-factor `cldRows`; #7835 explicitly
kept those current callers intact.

## Next step

Follow-up #7837 can replace the tight per-factor column-bound route with the
aggregate CLD cut, using `aggregateCldTail` / `selectedFactors` instead of
assuming `sum_i psiCut(c_i)` is bounded by aggregate recovery data.

## Blockers

None for #7835. The worktree still has a pre-existing unrelated
`.claude/CLAUDE.md` modification; it was not touched or staged.
