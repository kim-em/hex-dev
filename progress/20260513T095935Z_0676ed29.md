## Accomplished

- Claimed #3712, the remaining HO-21 HexBerlekamp FLINT comparator wiring
  issue.
- Wired fixed per-rung FLINT comparator registrations in
  `HexBerlekamp/Bench.lean` for `nmod_poly.is_irreducible` against
  `runRabinTestChecksum` and `nmod_poly.factor_distinct_deg` against
  `runDistinctDegreeChecksum`.
- Densified the Rabin ladder to
  `8,10,12,16,20,24,32,40,48,56,64` and the distinct-degree ladder to
  `12,16,20,24,32,40,48,64,80,96`.
- Regenerated the HexBerlekamp benchmark export at
  `reports/bench-results/hex-berlekamp-ad74d65.json` and updated
  `reports/hex-berlekamp-performance.md` with comparator tables, trend
  narrative, and gating-goal status.
- Verified `lake build HexBerlekamp` and `lake exe hexberlekamp_bench verify`.

## Current frontier

The required comparators are wired and measurable, but the fixed process-call
shape has no eligible FLINT rung: per-call process overhead dominates every
FLINT median, so the report records the gating-goal verdict as blocked/no
eligible rung rather than re-promoting HexBerlekamp to Phase 4.

## Next step

Open the PR for #3712. A follow-up should either amortise the persistent FLINT
driver across measured inner repeats or replace the process-call comparator
with an FFI shim before HexBerlekamp is conditionally re-promoted.

## Blockers

The FLINT comparator measurements are dominated by process-call overhead in
the current fixed benchmark harness shape.
