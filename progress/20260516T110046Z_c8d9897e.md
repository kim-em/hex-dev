# 2026-05-16T11:00Z - work session c8d9897e

## Accomplished

Claimed #4334 and rechecked the HexLLL Phase 4 benchmark blocker on current
branch commit `15cbed4323073d2afdaa592bd0091d6ac2d78958`.

Confirmed the smoke benchmark surface is still wired:

- `lake exe hexlll_bench verify` passed all 52 registered benchmarks.
- `lake exe hexlll_bench list` listed all 52 registrations.
- `python3 scripts/status.py HexLLL` still reports Phase 4 ready with
  `done_through: 3`.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.

Ran a bounded probe of one affected scientific registration:

```sh
lake exe hexlll_bench run Hex.LLLBench.runSizeReduceChecksum \
  --export-file /tmp/hex-lll-probe-c8d9897e-20260516T105941Z.json
```

The probe was not promotable evidence: on `carica` at
`2026-05-16T11:00:23Z`, with load averages observed during the session at
`8.66 10.35 9.58` and earlier `13.91 11.47 9.77`, the harness measured
`spawn_floor_nanos = 1319676041`, killed the first row at the
`maxSecondsPerCall = 5s` cap (`n = 128`), and produced zero
verdict-eligible rows. This matches the existing issue history and is a
measurement-environment failure, not a HexLLL performance verdict.

Skipped #4334 with the measured environment blocker, then claimed #4412 after
confirming #4469 had closed.

Added
`exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd` to
`HexBerlekampZassenhausMathlib/Basic.lean`. The theorem packages
`mem_T_iff_exists_irreducibleFactor_representingSubset` into the
cover-at-min-facing shape: for `i ∈ T`, it returns an irreducible factor `f`
and representing subset `S` with `f ∣ target`,
`f ∣ recombinationCandidate d T`, `S ⊆ J`, `i ∈ S`, and
`RepresentsIntegerFactorAtLift core d f S`.

Verified #4412 with:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|^\s*sorry\b' HexBerlekampZassenhausMathlib/Basic.lean`
  reports only the pre-existing file-level sorries at lines 162 and 241.

## Current frontier

#4334 remains technically valid but cannot be completed on this busy pod host.
The report `reports/hex-lll-performance.md` still records inconclusive verdicts
from `reports/bench-results/hex-lll-e211854d1435.json`, so `HexLLL` remains at
`done_through: 3`.

#4412 is complete and ready for PR creation.

The worktree still has the pre-existing local `.claude/CLAUDE.md` modification
not touched by this session.

## Next step

Create the PR for #4412. Separately, rerun the affected HexLLL scientific
benchmarks only on quiet/dedicated hardware with a stable spawn floor, then
update `reports/hex-lll-performance.md` and bump `HexLLL.done_through` only if
all Phase 4 gates are met.

## Blockers

Current `carica` load and process-spawn overhead make scientific benchmark
promotion impossible for #4334 in this session. No blocker for #4412.
