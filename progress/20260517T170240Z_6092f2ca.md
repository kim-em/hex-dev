# Session 6092f2ca — #4738 scaled prefix-none for primitive recursive coverage

## Accomplished

Claimed and discharged #4738 after PR #4762 (scaled cover-at-min chain)
landed on `main` and supplied the missing
`coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
input. Initially claimed #4604 but switched after observing the new
`main`: #4604 still blocks on #4759/#4758, while #4738 is now unblocked.

Added `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core_scaled`
to `HexBerlekampZassenhausMathlib/Basic.lean` immediately after the
existing unscaled primitive prefix-none. The proof mirrors the unscaled
version; the only non-cosmetic change is a one-line
`simp only [polyProduct_liftedSubsetSelectedList_eq_liftedFactorProduct]`
that rewrites the inline `Array.polyProduct split.1.toArray` (under
`Hex.DensePoly.scale (Hex.DensePoly.leadingCoeff core) _`) into
`liftedFactorProduct d T`, after which the goal folds into
`scaledRecombinationCandidate core d T` by definitional unfolding of
`scaledLiftedFactorProduct`.

The cover-at-min step routes through the scaled analogue, threading the
same `hcore_ne / hcore_primitive / hcore_lc_pos / hd_liftedFactor_monic /
hd_liftedFactor_natDegree_pos / hprecision / hpartition /
htarget_dvd_core` chain. `hd_modulus` is preserved in the signature (as
`_hd_modulus`) for API uniformity with the unscaled primitive prefix-none
even though the scaled cover-at-min wrapper does not consume it.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic`
* `lake build HexBerlekampZassenhausMathlib`
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/Basic.lean`
  — 10 / 10 against `origin/main`; no new entries.

## Current frontier

#4647 (primitive recursive recombination coverage assembly) is now
unblocked at the precursor level: #4736 / #4737 / #4738 are all in
`main`. The remaining work is the recursive coverage theorem itself,
which composes the scaled support / cover-at-min / prefix-none surface
with `scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`.

## Next step

A worker claims #4647 and assembles
`scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
(or its primitive + positive-leading variant) using the now-landed scaled
chain.

## Blockers

None for this PR.
