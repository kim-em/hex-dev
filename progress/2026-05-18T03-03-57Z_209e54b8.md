# 2026-05-18T03:03Z — feature session (#4905)

Session type: feature. Branch: `agent/209e54b8`.

## Accomplished

* Issue #4905: added a private abstract-bound sibling
  `zpoly_primitive_scaledRecombinationCandidate_of_bound` to
  `HexBerlekampZassenhausMathlib/Basic.lean`, alongside the existing
  `zpoly_primitive_scaledRecombinationCandidate` capstone in the A2 primitive
  recovery chain.

  The sibling takes `B' : Nat`,
  `hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B'`, and
  `hprecision : 2 * B' < d.p ^ d.k` in place of the core-shape
  `defaultFactorCoeffBound core` precision constraint. The body mirrors
  the original but feeds the abstract `B'` into
  `size_centeredLiftPoly_eq_of_pos_leadingCoeff_bound`. The bound is a
  single inequality on the leading coefficient (not the universal
  `∀ i, ...` shape used by factor-side `_of_bound` siblings) because
  `defaultFactorCoeffBound_valid` is only invoked at one coefficient
  in this proof — the leading coefficient.

* Rewired `zpoly_primitive_scaledRecombinationCandidate` as a thin
  wrapper that instantiates `B' := defaultFactorCoeffBound core` and
  discharges `hcore_lc_le` via
  `defaultFactorCoeffBound_valid core hcore_ne core (dvd_refl core)
  (core.size - 1)`. External call site at line 10683 (inside
  `exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core`)
  is unchanged.

## Current frontier

The deferred scaled-side
`exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
sibling — explicitly out of scope here, to be picked up by a follow-on
issue.

## Next step

Follow-on issue can now propagate the bound upward through the
scaled-side `exists_representingSubset_*` bridge at line 10645 (the
unscaled-side bridges were already covered by #4902).

## Blockers

None. Verification (`lake build HexBerlekampZassenhausMathlib.Basic`,
`lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`,
`git diff --check`, forbidden-token grep) all pass; the
`HexBerlekampZassenhausMathlib.Basic` build has the same set of
pre-existing `whnf` timeouts and kernel unknown-constant errors as
`origin/main` (verified by stash + rebuild comparison), with line
numbers shifted by ~27 lines to account for the new theorem.
