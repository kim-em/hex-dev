# Session a4873826 — issue #3549

## Accomplished

- Claimed #3549 after higher-priority human-oversight items were either blocked
  or marked for replan, and #3127 lost a claim race.
- Added rational adjacent-row-swap coefficient helpers in
  `HexGramSchmidt/Basic.lean`:
  - lower previous row coefficient swap,
  - lower current row coefficient swap,
  - pivot coefficient formula.
- Added integer wrappers for the same helper surface.
- Replaced the three targeted `coeffs_adjacentSwap_*` proof holes in
  `HexGramSchmidt/Update.lean`.
- Verified with:
  - `lake build HexGramSchmidt.Update`
  - `lake build HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean HexGramSchmidt/Update.lean`

## Current frontier

The adjacent-swap coefficient theorem slice from #3549 is complete. Remaining
`sorry` occurrences in `HexGramSchmidt/Update.lean` are the pre-existing Gram
determinant and scaled-coefficient adjacent-swap holes, not part of this issue.

## Next step

Downstream work can consume the proved coefficient formulas when assembling the
integer scaled-coefficient adjacent-swap and Cramer bridge slices.

## Blockers

- None for this PR.
