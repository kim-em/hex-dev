# Progress — 2026-06-15 (session faadb23e, feature)

## Accomplished

Issue #7342: added one-sentence, backtick-led Phase 6 docstrings to the
nine undocumented private helpers of the `moveColumnToLastValues`
column-permutation / cofactor-sign cluster in
`HexMatrix/Determinant.lean` (lines ~6150–6255):

- `moveColumnToLastValues_last`, `moveColumnToLastValues_castSucc`,
  `moveColumnToLastValues_nodup`,
  `moveColumnToLastValues_mem_permutationVectors`,
  `skipIndex_eq_raiseFinAbove`, `moveColumnToLastValues_toList`,
  `detSign_moveColumnToLastValues`, `neg_one_pow_mul_self`,
  `cofactorSign_col_eq_move_mul_last`.

Doc-only change: no signatures, statements, proofs, annotations,
declaration order, simp sets, or imports touched. Each docstring leads
with the backticked name and states what the lemma asserts plus its role
in the Laplace-by-column expansion machinery, matching the house style
of the documented Phase 6 clusters (HexLLL `stepBound_arith` family,
HexMatrix/Basic.lean fold-sum helpers).

## Verification

- `lake build HexMatrix.Determinant`: green (376 jobs).
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Determinant.lean`: `27 0` (additions only).
- Confirmed each of the nine names has a preceding `/-- ... -/`.
- No banned vocabulary or em-dashes on added lines.

## Current frontier

This doc cluster is complete. Other Phase 6 doc batches remain unclaimed
(#7344 HexGramSchmidt Int Bareiss helpers, #7346 HexLLL base-2^K packing).

## Next step

None for this issue.

## Blockers

None.
