# 2026-05-11 04:20 UTC — work/feature session (88eef54e)

## Accomplished

- Checked the unclaimed queue and human-oversight directives; all human-oversight
  items visible in orientation were blocked, and #3242/#3215 were already claimed.
- Claimed #3237 for the reachable Yun residual derivative-zero invariant.
- Oriented on `HexPolyFp/SquareFree.lean` around
  `yunFactorsPairwiseReachable`, the derivative-split base lemma, the Yun
  contribution residual predicates, and existing pairwise/reachability helpers.
- Added the reusable transition lemma
  `yunStep_tail_common_dvd_one_of_common_dvd_one`, carrying the current
  common-divisor-one hypothesis from a Yun state to its next
  `(gcd c w, w / gcd c w)` state.
- Verified the touched area with `lake build HexPolyFp.SquareFree`,
  `lake build HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`,
  and the requested banned-marker scan.
- Decomposed the remaining hard proof into #3255 after confirming that a direct
  structural induction over `yunFactorsPairwiseReachable` does not bridge the
  `step` constructor to the target state.

## Current frontier

- #3237 is partially addressed by a compiled helper lemma.
- #3255 tracks the remaining strengthened Frobenius-stability invariant needed
  to prove `yunFactorsPairwiseReachable_coprime_residual_derivative_zero` and
  feed the derivative-active residual provider.

## Next step

- Prove #3255's strengthened invariant, reusing
  `derivativeSplit_residual_derivative_zero_of_coprime` for the base case and
  `yunStep_tail_common_dvd_one_of_common_dvd_one` for the transition of the
  coprimality hypothesis.

## Blockers

- No external blocker. The remaining work is proof-design complexity: the
  reachable-state `step` constructor points from a predecessor to the next
  state, so the target derivative-zero theorem needs a stronger invariant than
  plain structural induction.
