# 2026-05-05 09:10 UTC — feature session (7bb8f11f)

## Accomplished

Claimed #2326 and added reusable scalar-normalization lemmas to
`HexPolyFp/Basic.lean` for nonzero `ZMod64` scalars over prime moduli:

- `C_mul_eq_scale`, `scale_scale`, and `scale_one_left` are now public.
- Nonzero scalar scaling preserves `size` and `degree?`.
- Positive degree gives nonzero leading coefficient.
- Scaling by the inverse leading coefficient makes a positive-degree
  polynomial monic.
- Irreducibility transfers across nonzero scalar scaling.
- Divisibility transfers across scalar scaling, including the normalized
  associate dividing the original polynomial.

Removed the now-redundant private copies of the promoted scale lemmas from
`HexPolyFp/SquareFree.lean`; existing square-free proofs use the shared API.

## Current frontier

`HexBerlekamp/RabinSoundness.lean` can now import the monic-associate
normalization facts from `HexPolyFp.Basic` instead of depending on
SquareFree-private helper proofs.

## Next step

Use these lemmas to prove the monic irreducible factor descent issue blocked
on #2326, normalizing an arbitrary irreducible positive-degree factor by its
inverse leading coefficient.

## Blockers

None.
