# HexMatrix split plan review

## Accomplished

- Audited the current `HexMatrix` computational import graph against the proposed base / row-reduce / determinant / Bareiss split.
- Checked downstream computational imports in `HexGramSchmidt`, `HexLLL`, `HexBerlekamp`, bench, conformance, and manual modules.
- Reviewed `HexMatrixMathlib` bridge imports and downstream Mathlib consumers, including `HexGramSchmidtMathlib` and `HexLLLMathlib`.
- Reviewed release-sync behavior in `scripts/release/sync_released.py`, `released.yml`, `synced.json`, and CI/conformance wiring.

## Current frontier

- The split is plausible, but the plan undercounts downstream dependency updates and has release-bootstrap hazards around `synced.json` placeholders and new repo lakefiles.
- The determinant/row-reduce computational edge is clean at import level except for the known dead `Leibniz -> RowEchelon` import.
- The Mathlib bridge split is feasible only if `HexMatrixMathlib.Basic` is split first and downstream bridge dependencies are updated more broadly than the draft says.

## Next step

- Feed the issue list from this review back into the split plan before implementation, especially the `synced.json` bootstrap rule and the `HexGramSchmidtMathlib` dependency partition.

## Blockers

- None for the review. Network access was not needed.
