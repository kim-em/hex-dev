# Audit #5088 — fpPoly_leadingCoeff_ne_zero_of_size_pos cluster (post-#5092)

Issue: https://github.com/kim-em/hex/issues/5094
Audit comment: https://github.com/kim-em/hex/issues/5094#issuecomment-4482313874

## Accomplished

- Confirmed all 9 pre-rewire sites listed in #5088's table are
  rewired onto `fpPoly_leadingCoeff_ne_zero_of_size_pos`:
  - 7 in `Basic.lean` at 1247, 4911, 5185, 5730, 5827, 6007, 6165.
  - 2 in `IntReductionMod.lean` at 337, 487.
- Verified the declaration at `Basic.lean:1227` (`theorem`, not
  `private theorem` — cross-file visibility justified by the two
  `IntReductionMod.lean` call sites).
- Verified docstring shape (contract-first sentence + one-clause
  semantic composition note naming
  `Hex.FpPoly.leadingCoeff_eq_coeff_pred` and
  `Hex.DensePoly.coeff_last_ne_zero_of_pos_size`), no PR/session
  metadata or provenance hints.
- Verified hoist position: between `monicModPImage_monic_of_ne_zero`
  (1215) and `monicModPImage_dvd_self_of_ne_zero` (1234); minimal
  given Site 1 sits inside the latter, and the preceding sibling's
  body does not mention the helper.
- Verified cross-file occurrence counts: `Basic.lean` 8 (1+7),
  `IntReductionMod.lean` 2, `Recovery.lean` 0; declaration unique.
- Out-of-scope observations: noted that the two non-`≠ 0`
  `leadingCoeff_eq_coeff_pred` users (`Basic.lean:5350` and 5734)
  have differing shapes and do not warrant a unified sibling
  helper, and that the upstream
  `HexPolyFp/SquareFree.lean:1243 private theorem
  leadingCoeff_ne_zero_of_isZero_false` is a public-promotion
  candidate (4 in-file uses) but a HexPolyFp-side planner decision.

## Verification

- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` exit 0.
- Audit comment posted with PASS/PASS/PASS verdicts.
- No source files modified; only the progress file under
  `progress/` is staged.

## Current frontier

Cluster `fpPoly_leadingCoeff_ne_zero_of_size_pos` is closed under
its three-invariant audit. The post-#5081 Shape-B audit (#5085) is
still blocked on #5081 in CONFLICTS/repair-claimed; the
post-#5084 Monic-route inversion audit (#5090) closed via #5093.

## Next step

Open PR closing #5094.

## Blockers

None.
