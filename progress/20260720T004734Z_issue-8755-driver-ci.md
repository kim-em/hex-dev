# Accomplished

- Inspected the exact failing CI substep for PR #8839 and reproduced it with
  `scripts/ci/run_oracles.sh`.
- Confirmed the implementation was correct but the committed `HexRoots`
  emission fixture still recorded the pre-normalization driver precisions.
- Regenerated the 14 affected result records from
  `hexroots_emit_fixtures`; polynomial inputs and semantic output shapes are
  unchanged, while stored centres and precisions reflect the intentional
  deeper global normalization prefix.
- Re-emitted and byte-compared the fixture and rebuilt
  `HexRoots.Conformance` successfully. The local FLINT oracle is unavailable,
  so the pushed CI rerun remains the authoritative external numeric check.

# Current frontier

The diagnosed fixture drift is fixed locally and ready to push to PR #8839.

# Next step

Push the amended commit, monitor the replacement Actions run through every
substep, and merge once green.

# Blockers

None.
