# 20260602T120038Z dd3ebfa8 — Issue #6286

## Accomplished

- Claimed #6286 after verifying prerequisite #6316 was closed.
- In `HexPolyFp/SquareFree.lean`, moved
  `squareFreeAuxRevResidualSatisfied_of_invariant` earlier so it can support
  the weighted-product proof chain.
- Threaded the global `residualInvariant` through the weighted-product
  contribution correctness lemmas.
- Refactored the derivative-active contribution proof to prove both:
  - raw executable tail correctness for `pthRoot contribution.2`; and
  - normalized tail correctness for `pthRoot (normalizeMonic contribution.2).2`.
- Closed the derivative-active weighted-product obligation through
  `derivative_active_raw_tail_weighted_product_bridge_via_normalized`, so the
  raw executable recursion is unchanged while the proof consumes the normalized
  tail payload at the bridge boundary.

## Current frontier

The product proof now composes the #6316 raw-tail/normalized-tail bridge with
the normalized tail provider. The broad raw state-provider hypothesis still
remains on `squareFree_weightedProduct`: it is still used by the raw
`yunFactorsLevelCompletes` measure proof, not by the normalized tail product
bridge.

## Next step

Replan the remaining API-surface work around the raw level-completion
dependency. The likely next issue should either narrow/replace the raw
`yunFactorsLevelCompletes` provider or prove an alternate completion/product
invariant that avoids requiring raw `squareFreeContributionReachable` at every
derivative-active Yun state.

## Blockers

- Full completion of #6286 is blocked by the remaining raw
  `yunFactorsLevelCompletes` dependency described above.

## Verification

- `lake build HexPolyFp.SquareFree` — green.
- `lake build HexPolyFp` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token diff grep over `HexPolyFp/SquareFree.lean` found no added
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
