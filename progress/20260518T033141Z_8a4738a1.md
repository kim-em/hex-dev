# Session 8a4738a1 — abstract-bound siblings of `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate*`

Closes #4902.

## Accomplished

* Added `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate_of_bound`
  (monic-flavour abstract-bound sibling) in `HexBerlekampZassenhausMathlib/Basic.lean`.
  Takes universal `B' : Nat` plus `hvalid : ∀ g, toPolynomial g ∈ normalizedFactors candidate → ∀ i, (g.coeff i).natAbs ≤ B'`
  and precision `2 * B' < d.p ^ d.k`; routes through
  `representingSubset_subset_of_dvd_recombinationCandidate_of_bound`.
* Added `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate_of_primitive_pos_lc_core_of_bound`
  (primitive + positive-leading unscaled flavour) with the analogous universal
  bound shape; routes through
  `representingSubset_subset_of_dvd_recombinationCandidate_of_primitive_pos_lc_core_of_bound`.
* Rewired both originals (`exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
  and `..._of_primitive_pos_lc_core`) as thin wrappers that instantiate
  `B' := Hex.ZPoly.defaultFactorCoeffBound core` and discharge `hvalid` by
  chaining `g ∣ candidate ∣ target ∣ core` and invoking
  `defaultFactorCoeffBound_valid`. The vestigial `_htarget_dvd_core`
  parameter became non-vestigial as `htarget_dvd_core` in the wrappers
  (consumers already passed it positionally).

## Verification

* `lake build HexBerlekampZassenhausMathlib`: failures match `origin/main`
  exactly (same error identities, shifted by the inserted 181 lines). No
  new errors in the 9500–11300 line range of `HexBerlekampZassenhausMathlib/Basic.lean`
  that contains my edits.
* `python3 scripts/check_dag.py`: clean.
* `git diff --check`: clean.
* No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on the diff against
  `origin/main`.

## Current frontier

Layer 1 abstract-bound siblings now cover the unscaled-side
per-normalised-factor bridge layer (monic + primitive). The scaled-side
bridge (`exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core`)
still lacks its `_of_bound` sibling and depends on
`zpoly_primitive_scaledRecombinationCandidate_of_bound`, which is the
next prerequisite. Layers above this (`mem_T_iff_*`, `coverAtMin_*`,
`recombinationSearchModAux_*`) remain pending.

## Next step

A planner can open follow-on issues for:
* `zpoly_primitive_scaledRecombinationCandidate_of_bound` (Mignotte-style
  primitivity bridge that actually consumes precision; this is the
  blocker for the scaled-flavour sibling).
* `exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
  (gated on the above).
* Continuing the `_of_bound` propagation chain into `mem_T_iff_*` and
  beyond.

## Blockers

None for this session.
