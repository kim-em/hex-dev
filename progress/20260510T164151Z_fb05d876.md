## Accomplished

Claimed #3018 and updated the HexBZ normalization proof chain in
`HexBerlekampZassenhaus/Basic.lean`.

Replaced the too-strong exact integer reassembly statements for
`normalizeForFactor_reassembles`, `reassembleNormalizedFactors_product`, and
`normalizedConstantFactors_product` with the strongest axiom-free invariant
available from the current primitive square-free decomposition surface:
rational association of the extracted `X`-free primitive core with the
square-free core times repeated part.

Added local Rat-scale composition and primitive-squarefree bridge helpers,
using `ZPoly.primitiveSquareFreeDecomposition_reassembly_over_rat`,
`ZPoly.primitiveSquareFreeDecomposition_primitive`, and
`ZPoly.content_mul_primitivePart`.

Verified:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean`
  reports only pre-existing markers outside this normalization bridge chain.

## Current frontier

The primitive square-free portion of normalization is now explicit and
proof-backed as a rational-associate invariant. The exact final public
factorization product theorem still needs a later layer that accounts for the
rational units introduced by primitive representatives.

## Next step

Continue #2762 from this corrected theorem shape: either add a separate bridge
that proves exact reassembly under the additional primitive/content facts needed
for executable representatives, or thread rational-associate normalization into
the downstream factorization product contract.

## Blockers

None.
