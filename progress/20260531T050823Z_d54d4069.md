# Progress — 2026-05-31T05:08Z — session d54d4069 (#4334)

## Accomplished

Claimed #4334 (HexLLL Phase 4 inconclusive scientific verdicts) via
`pod once 4334` (one-shot priority dispatch). Built `hexlll_bench`
and ran a single-target spawn-floor probe of `runSwapStepChecksum`
under `--max-seconds-per-call 1.5`.

Result:

- per-spawn floor (harness self-measurement): **1.287 s**
- verdict: `inconclusive`, every measurement killed at cap
- advisory: "every measurement hit the wallclock cap"

This matches the prior six worker skips (sessions 33fb739f,
9c7728c3, 4a24aa41, 977ed76a, 9156c117, 00db978d, ffcdc6bc) and is
~39× worse than the ~33 ms spawn floor in both committed scientific
artifacts (`hex-lll-e211854d1435.json`, `hex-lll-885431e.json`).

Host state at probe time:

- load avg 4.91 / 3.96 / 3.25 (1-min jumped from 3.75 to 4.91
  during the orient window)
- many concurrent `/private/tmp/lean-bench/.lake/build/bin/timed_regions_benchmark_test _child --bench LeanBench.Test.TimedRegions.tinyFn`
  child processes (same documented contention pattern as prior skips)

## Current frontier

The diagnostic chain documented by session ffcdc6bc (commit
2765ae18, progress/20260530T191150Z_ffcdc6bc.md) is unchanged:

- Issue deliverable 3 (formula vs implementation) is already
  discharged by PR #2823 in code; the post-#2823 artifact
  `hex-lll-885431e.json` shows all five formerly-inconclusive
  registrations as consistent.
- The 885431e artifact is itself stale because PR #4189 densified
  the parametric schedules.
- Only deliverables 1 (re-run) and 4 (done_through bump) remain;
  both require a stable spawn floor on quiet/dedicated hardware
  per the issue body's own re-dispatch guidance.

## Next step

Identical to ffcdc6bc's recommendation:

1. Successor session on quiet hardware (no concurrent worker
   sessions, no foreground `lean` builds, no stale lean-bench
   children) runs

   ```sh
   lake exe hexlll_bench run \
     Hex.LLLBench.runSizeReduceChecksum \
     Hex.LLLBench.runGramSchmidtCoeffChecksum \
     Hex.LLLBench.runFirstShortVectorHarshCubicChecksum \
     Hex.LLLBench.runOfBasisHarshCubicChecksum \
     Hex.LLLBench.runFirstShortVectorRandomBoundedChecksum \
     --export-file reports/bench-results/hex-lll-<commit>-quiet.json
   ```

   then updates `reports/hex-lll-performance.md` and bumps
   `libraries.yml` `HexLLL.done_through: 3 → 4`.

2. Or migrate the scheduled release-candidate benchmarking workflow
   (per SPEC/benchmarking.md §"Scientific timing runs") to
   dedicated hardware and let it produce the artifact out-of-band
   of pod worker sessions.

## Blockers

`carica` spawn floor 1.287 s ≫ the ~30 ms required for the
scientific bench to produce verdict-eligible rows within the
configured `maxSecondsPerCall` budgets. Same shared-host blocker
documented on the issue and on six prior worker sessions. The
issue body's `quiet/dedicated hardware with a stable spawn floor`
precondition is not satisfied during this pod operation.
