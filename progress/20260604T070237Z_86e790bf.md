# 2026-06-04 — Stage P-bridge sub-piece 1: determinantal identification of `bareissGramCanonicalCoeff`

Session: `86e790bf`, issue
[#6526](https://github.com/kim-em/hex/issues/6526).

## Accomplished

Landed the determinantal identification of `bareissGramCanonicalCoeff` as a
trailing-column entry of the no-pivot Bareiss pass on an augmented
`(n + 1) × (n + 1)` matrix, in `HexGramSchmidtMathlib/Int.lean`:

- **`augmentedGram (b : Matrix Int n m) (a : Fin n) : Hex.Matrix Int (n + 1)
  (n + 1)`** — upper `n × n` block is `gramMatrix b`; trailing column at
  upper rows is the standard basis vector `δ_a`; trailing row is all zeros.
- **`augmentedGram_upper_block` / `augmentedGram_trailing_col` /
  `augmentedGram_trailing_row`** — entry-level structural lemmas.
- **`noPivotLoop_augmentedGram_invariant`** (private) — the combined
  invariant on the no-pivot Bareiss trajectory: after `fuel + 1 ≤ n`
  iterations on a non-singular Gram-side prefix, the augmented loop matches
  the Gram loop on `step`, `prevPivot`, `singularStep`, the upper `n × n`
  block, and carries the canonical row-coefficient at slot `a` in its
  trailing column. Proved by induction on `fuel` with explicit case analysis
  on whether the current step updates each entry.
- **`bareissGramCanonicalCoeff_eq_augmentedGram_entry`** — the directive's
  main deliverable: `(bareissGramCanonicalCoeff b fuel i)[a]` equals the
  trailing-column entry of the augmented loop at row `i` lifted to
  `Fin (n + 1)`.
- **`bareissGramCanonicalCoeff_eq_borderedMinor_aug`** — the optional
  direct bordered-minor form, composing the main identification with
  `BareissNoPivotInvariant.trailing_eq`. Routed through a locally-inlined
  `trailing_eq_at_step_local` because the HexMatrixMathlib version is
  private.

Supporting helpers added: `matrix_entry_eq_of_eq` (Fin-index reindexing
without `rw` motive issues), `noPivotLoop_singularStep_none_of_succ`
(singular states persist under further iteration).

Verified:

- `lake build` clean across `HexGramSchmidt`, `HexGramSchmidtMathlib`,
  `HexLLL`, `HexLLLMathlib`, `HexGramSchmidt.Conformance`,
  `HexLLL.Conformance`, `HexMatrix.Conformance`.
- `grep -c sorry HexGramSchmidt/Int.lean` = 3 (unchanged).
- `grep -c sorry HexGramSchmidtMathlib/Int.lean` = 0 (unchanged from main).
- No new `axiom`, no `native_decide`.

## Current frontier

Sub-piece 1 of Stage P-bridge ([#6525](https://github.com/kim-em/hex/issues/6525))
is landed. The remaining sub-pieces — Bareiss-Desnanot divisibility on the
augmented trajectory, then the `StepWitness` instance — are tracked by the
parent #6525.

## Next step

A follow-up session takes the bordered-minor identification together with
existing Bareiss-Desnanot infrastructure to derive integer divisibility of
the canonical Bareiss step numerator by `state.prevPivot`. That divisibility
is what `StepWitness b` packages.

## Blockers

None.

## Commits

To be added: `feat: determinantal identification of bareissGramCanonicalCoeff
via augmented Gram matrix (#6526)`.
