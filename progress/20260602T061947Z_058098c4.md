# Progress: BHKS D1 bridge foundation (#6260)

## Accomplished

Added `ExecutableBadVectorWitness.BadVectorBridgeData` in
`HexBerlekampZassenhausMathlib/BadVector.lean`.

The new record packages the project-level bridge assumptions needed by the
BHKS D1 setup path:

- integer true factors indexed compatibly with lifted factors;
- coefficientwise Hensel-lift congruences modulo `p^k`, including a selected
  local-factor congruence;
- per-coefficient precision separation
  `2 * bhksCoeffBound input j < p^k`;
- projected-vector correction arrays for vectors in `L' \ W`;
- the corrected auxiliary-polynomial identity;
- the three existing algebraic bridge fields needed by cap separation.

Added:

- `BadVectorBridgeData.auxiliaryCorrections`;
- `BadVectorBridgeData.auxiliary_eq'`;
- `BadVectorBridgeData.toProjectedBadVectorSetupBridge`.

These forget the stronger project-level data to the existing
`ProjectedBadVectorSetupBridge` callback surface consumed by
`TerminationBound.lean`.

Verification:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check -- HexBerlekampZassenhausMathlib/BadVector.lean`
- Forbidden-token diff grep over
  `HexBerlekampZassenhausMathlib/BadVector.lean` found no added `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

`BadVector.lean` now has a stronger reusable BHKS bridge-data package and a
lossless path from that package to the existing compact bridge.  The `H`
polynomial remains abstract, matching `badVectorWitnessOfLiftData` in
`Recovery.lean`; callers instantiate the structural `auxiliary_eq` field with
the canonical corrected construction for their chosen `H`.

## Next step

Use `BadVectorBridgeData` in the sibling BHKS bridge issues to prove the
remaining algebraic fields from executable CLD/Hensel data:

- rational coprimality of input and auxiliary polynomial;
- `p^(k*d)` resultant divisibility;
- final assembly of `ProjectedBadVectorSetupBridge` and cap wrapper.

## Blockers

None for this session.  The worktree still contains an unrelated pre-existing
local modification to `.claude/CLAUDE.md`, which was not touched or staged.
