# Worker session 036fcdb3: skipped #5655 with dependency analysis

Session UUID prefix: `036fcdb3` (`/feature` via `/work`).

## Accomplished

Claimed #5655, analysed the dependency chain, added
`depends-on: #5805`, and skipped with a clear blocker note.

The analysis: the private theorem
`gramDetVecEntry_eq_leadingPrefix_bareiss` (`HexGramSchmidt/Int.lean:4378`)
needs to handle the singular branch using the helper
`leadingPrefix_gram_bareiss_toNat_eq_zero_of_noPivot_singular`
(landed in #5828/PR #5834), which takes `hentry_regular` — the
universally-quantified exact-division step relation for the no-pivot
Bareiss row update against the Gram row invariant — as a hypothesis.

There are only two ways to supply `hentry_regular` inside the proof:

1. **Discharge internally.** Requires per-coefficient divisibility
   `prevPivot ∣ (pivot · coeff[i][a] − M[i][k] · coeff[k][a])` at every
   reachable regular step, which is exactly the cofactor / Sylvester
   work of #5805 (and its prerequisite #5836, the
   Gram-trajectory Desnanot–Jacobi identity in
   `HexMatrix/Determinant.lean`).

2. **Thread `hentry_regular` through the consumer chain.** Reaches
   `gramDetVec_eq_gramDet` (`HexGramSchmidt/Int.lean:4652`) and from
   there `HexLLL/Basic.lean:432`, where the caller is Mathlib-free
   and has no way to construct `hentry_regular`. Issue deliverable
   #5 also forbids changing public `gramDet` / `gramDetVec` /
   `scaledCoeffs` API shapes, so this path is closed.

Hence the issue cannot land cleanly until #5805 lands. Added
`depends-on: #5805` (which transitively requires #5836). Skip
comment routes the issue to the next planner via the `replan` label.

## Current frontier

#5655 is now `blocked` + `replan` pending the #5836 → #5805 chain. The
`HexGramSchmidt/Int.lean` sorry stays put.

## Next step

A planner should close #5655 forward-linked to #5805 once #5805
lands (since `hentry_regular` then becomes vacuous on the Gram
trajectory and the cascade goes away), or the planner can decide
to land the public-API cascade now if Mathlib-bridge consumers
absorb the parameter. The cleaner path is to wait for #5805.

Independent next worker can pick up #5836 (Sylvester identity,
labelled `agent-plan` only) as the foundational unblocker for the
whole chain.

## Blockers

None for this session — skip executed cleanly.
