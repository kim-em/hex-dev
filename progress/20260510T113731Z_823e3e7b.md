## Accomplished

Skipped #2978 after finding a concrete counterexample to the requested
`IsEchelonForm.spanCoeffs_complete` theorem: the current `spanCoeffs`
algorithm reads pivot coordinates independently, so it is not complete for
general echelon forms with nonzero entries above later pivots.

Claimed #2980 and landed the private pivot lookup correctness layer in
`HexMatrix/RREF.lean`: `pivotIndex?_pivot` and `pivotIndex?_free_none`, with
auxiliary recursion lemmas. Decomposed the remaining nullspace soundness work
into #2998 (prove generated basis vectors kill `D.echelon`) and #2999
(transport that helper back to `M`).

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`

## Current frontier

#2980 is partially complete: the private pivot lookup API needed by the
nullspace basis formula is now proof-bearing. The row-by-row dot-product proof
for `D.echelon * E.nullspace.get k = 0` remains the next hard step.

## Next step

Land the partial PR for #2980. After it lands, work #2998, then #2999.

## Blockers

#2978 needs planner attention because the theorem is false for plain
`IsEchelonForm`; it likely needs either an RREF-specific statement or a
back-substitution-based `spanCoeffs` implementation.
