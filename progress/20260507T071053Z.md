## Accomplished

- Rewrote `SPEC/Libraries/hex-berlekamp-zassenhaus.md` to specify the
  van Hoeij CLD recombination architecture: three top-level functions
  (`factorSlow`, `factorFast`, `factor`) with conditional fast /
  unconditional slow / fallback combinator design, all backed by full
  Mathlib-bridge proofs and zero axioms.
- The SPEC pins the algorithmic decisions: BHKS-style CLD invariant
  (`Φ(g) := f · g'/g`), all-coefficients lattice (BHKS eq. 5.1, row
  transpose), BHKS Lemma 5.1 coefficient bound with Landau's
  inequality, two-sided cut `Ψ`, Gram–Schmidt cut + projection +
  rref + equivalence-class identification (Lemma 3.3), exact-division
  verification certifying `L' = W` (Lemma 3.4), precision schedule
  starting at `a = 4` and capping at a Lean-computable
  `bhksBound : ZPoly → Nat`.
- Documents 10 implementation pitfalls and 4 groups of proof
  obligations (A1–A5 slow path, B1–B9 fast path conditional
  correctness, C1–C2 combinator, D1 BHKS Theorem 5.2 as leaf
  performance theorem with full pathway).
- BHKS Theorem 5.2 (the multi-month formalisation) is structurally a
  leaf: nothing else depends on it. HO-4 (a separate issue) owns it.
- `lake build` green after the SPEC change.

## Current frontier

- The SPEC PR branch `spec/bz-cld-recombination` is ready to push.
- After it merges, four `human-oversight` issues need filing: HO-1
  (rollback + implementation, Groups A/B/C), HO-2 (adversarial
  fixtures), HO-3 (Bench.lean), HO-4 (BHKS Theorem 5.2 leaf).

## Next step

- Push branch and open the SPEC PR.
- File the four human-oversight issues, cross-linked with the SPEC
  PR URL and with explicit `depends-on:` lines.

## Blockers

- None.
