# HexPolyZ Phase 6 grind automation coverage for Mignotte.lean (#7988)

## Accomplished

Added `@[grind =]` to the eight equation-shaped base-case/zero
characterising lemmas in `HexPolyZ/Mignotte.lean` that already carried
`@[simp]`, following the `@[simp, grind =]` convention from
`HexModArith/Basic.lean` and `HexMatrix/Basic.lean`:

- `binom_zero_right` (`binom n 0 = 1`)
- `binom_zero_succ` (`binom 0 (k+1) = 0`)
- `floorSqrt_zero`, `ceilSqrt_zero`
- `coeffNormSq_zero`, `coeffL2NormBound_zero`
- `mignotteCoeffBound_zero`, `defaultFactorCoeffBound_zero`

All eight are genuinely `grind =`-shaped (LHS a function application of
a fixed/variable argument, RHS a literal), so no `grind`-loop risk.

Premise held: these are the exact lemmas the issue flagged, all
equation-shaped. No contrived annotations forced; the four `*_eq` /
`*_eq_*` restatement lemmas (`mignotteCoeffBound_eq`,
`defaultFactorCoeffBound_eq`, `coeffNormSq_eq_sum`,
`coeffL2NormBound_eq_ceilSqrt_coeffNormSq`) were left alone — they are
`rfl`-unfoldings, not base-case rewrites, and were not part of the
`@[simp]` surface.

## Justification for no in-file proof shortening

No proof inside `Mignotte.lean` shortens from these additions: the zero
lemmas are leaf base cases, not premises of later in-file proofs. The
payoff is **downstream `grind` availability** — these
`defaultFactorCoeffBound` / Mignotte-bound zero rewrites are consumed by
the Hensel/BZ precision schedule and the `*Mathlib` Mignotte bridge, per
the issue's premise note. This is deliverable 2's "confirm a downstream
consumer benefits" path.

## Verification

- `lake build HexPolyZ` green; `Mignotte` compiles in ~0.78s (no
  slowdown vs. baseline, no loop).
- `git diff --check` clean; no new `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `python3 scripts/check_dag.py` exits 0.
- Diff is exactly the eight annotation lines; no `done_through` bump, no
  `*Mathlib` bridge touched.

## Current frontier

Mignotte.lean grind pass complete.

## Next step

Sibling pass #7989 (HexPolyFp Frobenius.lean) remains unclaimed.

## Blockers

None.
