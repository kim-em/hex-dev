# Progress — 2026-06-16T06:40:20Z (session c411912f, once #7550)

## Accomplished

Landed the **structural-narrowing** half of #7550 (premise-corrected
directive). The original issue was skipped as unsound because the bare
`exists_subset`/`hexists_lifted` quantifier (over *all* irreducible factors of
`core`) is a false proposition — negative-leading-coefficient divisors can never
be represented. The corrected directive narrows the quantifier to
sign-normalized representatives first.

Changes in `HexBerlekampZassenhausMathlib/Basic.lean`:

- Narrowed `exists_subset` in `HenselSubsetCorrespondenceHypotheses` and
  `HenselSubsetCorrespondenceRest` (and thereby the inherited
  `LiftedFactorSubsetPartition`) with a leading
  `Hex.normalizeFactorSign factor = factor →` premise.
- Narrowed the two substrate constructors' `hexists_lifted` hypotheses
  (`henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData_success_descent`,
  `slowPathHenselSubstrate_of_toMonicChoosePrimeData_success_descent`) to the
  same sign-normalized shape — making them *satisfiable* (the bare form was
  unsound).
- Added two public helper lemmas:
  `normalizeFactorSign_eq_self_of_leadingCoeff_nonneg` and
  `exists_signNormalized_irreducible_factor` (extracts a sign-normalized
  irreducible divisor of a nonzero non-unit by normalizing in `Polynomial ℤ`).
- Re-pointed every in-scope consumer to supply the sign proof: the recovering
  uniqueness chain (already carries `hfactor_norm`), the two
  `normalizedFactors`-based extractors (derive sign from `normalize p = p`),
  the `liftedTrueSupports` surjectivity step, and the two empty-`J`
  contradiction sites (now use `exists_signNormalized_irreducible_factor`).

Verification: `lake build HexBerlekampZassenhausMathlib.Basic` (0 errors;
remaining `sorry` warnings, e.g. `Hex.ZPoly.isIrreducible_iff:233`, are
pre-existing and tracked by #4170/#4819). `python3 scripts/check_dag.py` clean.
`git diff --check` clean. No `sorry`/`axiom`/`native_decide` in added lines. No
executable-layer (`HexBerlekampZassenhaus/`) change.

## Current frontier

The structures are narrowed and all consumers compile. The substrate
constructors now take a satisfiable (narrowed) `hexists_lifted`.

## Next step

Filed #7555 (producer): discharge the narrowed `hexists_lifted` from core
facts via `toMonicLiftData_represents_lifted_of_modP`, deriving
`Hex.ZPoly.Primitive factor` (Gauss; publicize private
`zpoly_primitive_of_dvd_primitive_basic`, `Basic.lean:10204`) and
`1 ≤ factor.degree?.getD 0` (private
`one_le_degree_getD_of_irreducible_dvd_primitive`,
`IntReductionMod.lean:2785`). Then rewire the substrate constructors to feed
the producer; handle the `hunscaled` support field separately.

## Blockers

None for the narrowing. The worktree carries pre-existing unstaged
modifications to `.claude/*` from a prior session; left untouched and unstaged.
