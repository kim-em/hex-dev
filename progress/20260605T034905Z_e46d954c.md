# 2026-06-05 03:49Z — adjacent-swap scaledCoeffs unchanged entries (#6571)

## Accomplished

Added unchanged-entry support for adjacent swaps:

- In `HexGramSchmidt/Basic.lean`, added Rat and Int coefficient invariance
  lemmas for the three lower-triangular regions outside the changed swap
  entries: rows before `k - 1`, rows after `k` with columns before `k - 1`,
  and rows after `k` with columns after `k`.
- In `HexGramSchmidtMathlib/Update.lean`, added the corresponding
  `scaledCoeffs_adjacentSwap_before`, `scaledCoeffs_adjacentSwap_above_low`,
  and `scaledCoeffs_adjacentSwap_above_high` lemmas.

Verification:

- `lake build HexGramSchmidtMathlib` passed.
- `git diff --check` passed.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`
  in the touched Lean files was empty.

## Current frontier

The changed-entry lemmas plus the new unchanged-entry lemmas now cover the
lower-triangular adjacent-swap regions needed by downstream `swapStep_valid`
work.

## Next step

Use the new unchanged-entry lemmas when proving `LLLState.Valid.ν_eq` for the
swapped state in #6565.

## Blockers

None. Pre-existing/off-scope dirty file left untouched: `.claude/CLAUDE.md`.
Pre-existing warnings still appear in dependencies/build output
(`HexGramSchmidt/Basic.lean:1606`, `HexGramSchmidt/Int.lean:4939`,
`HexGramSchmidtMathlib/Int.lean:2842`, and unused-argument warnings in existing
`HexGramSchmidtMathlib/Update.lean` lemmas).
