## Accomplished

Claimed #4771 and executed the first Phase 6 polish slice on
`HexGF2/Basic.lean` (the GF2Poly core API up to and including `monomial`).

- Added concise caller-facing docstrings to the three previously
  undocumented one-hot lemmas (`oneHotWord_bit_self`,
  `oneHotWord_bit_ne`, `oneHotWord_ne_zero`) and to the five `ofWords`
  boundary lemmas (`words_ofWords_empty`, `ofWords_empty`,
  `words_ofWords_single_zero`, `words_ofWords_pair_zero`,
  `words_ofWords_single_nonzero`).
- Tagged `normalizeWords_get?_getD` and `coeffWords_normalizeWords`
  with `@[simp]`: both are unconditional canonical-representative
  rewrites that strip a redundant `normalizeWords` application from
  coefficient queries, leaving the raw word array as the simp normal
  form. The existing explicit consumers (`coeff_ofWords`'s simp set in
  this file and the `simpa only` call in `HexGF2/Multiply.lean`) are
  unaffected; the full `HexGF2` module rebuilds clean.

Verification run:

- `lake build HexGF2` (full HexGF2 module, 48 jobs successful)
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -c "sorry|axiom|native_decide|TODO|FIXME" HexGF2/Basic.lean`
  unchanged vs `origin/main`.

## Current frontier

#4771 is implemented. No theorem statements, executable definitions,
representation choices, or FFI symbols were changed. The only diff is
8 added docstrings and 2 added `@[simp]` attributes on unconditional
identities, all confined to the `GF2Poly` core API region of
`HexGF2/Basic.lean`.

## Next step

A follow-up Phase 6 polish slice for the rest of `HexGF2/Basic.lean`
(the bitwise XOR / shift / multiplication helpers below `monomial`)
is still open as out-of-scope for #4771; the planner should create
that issue.

## Blockers

None.
