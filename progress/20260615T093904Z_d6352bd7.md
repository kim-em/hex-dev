# Progress — 2026-06-15 (session d6352bd7, feature)

## Accomplished

Claimed and implemented issue #7471. Added the proof-facing
`BHKS.CutRetention` certificate in `HexBerlekampZassenhausMathlib/Lattice.lean`
and the constructors that turn a Gram-Schmidt cut-retention fact plus the
landed B5 indicator norm bound into `CutProjectionHypotheses` for
`Hex.bhksProjectedRows`.

Added
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_retention` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`, so the named
fast-core irreducibility surface can consume `CutRetention` directly and
delegate through the existing `_of_cut` theorem.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Lattice`: green.
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`: green
  with pre-existing imported warnings only.
- `lake build`: green with pre-existing `HexGFqRing.Operations` lint warnings.
- `python3 scripts/check_dag.py`: green.
- `git diff --check`: clean.
- Added-line forbidden-token scan over touched Lean files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The forward cut membership now has a Mathlib-facing producer once callers
supply the actual Gram-Schmidt retention certificate. The analytic retention
theorem itself is still the remaining bridge from the executable private cut
predicate / reduced-basis data to `BHKS.CutRetention`.

## Next step

Prove or expose the concrete Gram-Schmidt cut-retention theorem for
`Hex.bhksProjectedRows`, then instantiate `BHKS.CutRetention` at the actual
fast-path lift data.

## Blockers

No blockers for #7471's composition layer. The remaining work is the separate
analytic GS-cut retention proof.
