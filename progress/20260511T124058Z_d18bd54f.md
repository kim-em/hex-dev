# 2026-05-11 12:40 UTC — work session (d18bd54f)

## Accomplished

- Claimed #3416 after reviewing the unclaimed queue and selecting the HexLLL Phase 4 completion audit as the highest-impact unclaimed issue because it unblocks `HexBerlekampZassenhaus -> Phase 4`.
- Audited `PLAN/Phase4.md`, `SPEC/benchmarking.md`, `SPEC/profiling.md`, `SPEC/Libraries/hex-lll.md`, `HexLLL/Bench.lean`, `lakefile.lean`, `.github/workflows/ci.yml`, `libraries.yml`, and `reports/hex-lll-performance.md`.
- Confirmed the Phase 4 artefacts are present: benchmark executable, CI smoke commands, comparator metadata, profile coverage for all three `phase4.input_families`, headline report sections, and empty Concerns.
- Updated `libraries.yml` so `HexLLL.done_through` advances from `3` to `4`.
- Tightened the HexLLL headline report wording so `scripts/check_phase4.py` can match the exact comparator names from `libraries.yml`.
- Repaired a local generated build-artifact problem by deleting stale `HexMatrix.Determinant` dynamic/export artefacts and rebuilding them; no source files were changed for that.
- Verified:
  - `python3 scripts/status.py HexLLL`
  - `python3 scripts/status.py | rg -n 'HexBerlekampZassenhaus -> Phase 4|HexLLL -> Phase 5|HexLLL.done_through|HexBerlekampZassenhaus.done_through'`
  - `python3 scripts/check_phase4.py`
  - `python3 scripts/check_dag.py`
  - `lake exe hexlll_bench list`
  - `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)"; export HEX_LLL_ISABELLE_SVP; lake exe hexlll_bench verify`
  - `git diff --check`

## Current frontier

- `HexLLL` now reports as ready for Phase 5 with `done_through: 4`.
- `HexBerlekampZassenhaus` now reports as ready for Phase 4 rather than waiting on `HexLLL.done_through >= 4`.
- The first plain `lake exe hexlll_bench verify` run failed only because the verified-Isabelle comparator setup was cold and ran inside benchmark warmup; the CI-style pre-exported `HEX_LLL_ISABELLE_SVP` run passed all 32 registrations.

## Next step

- Create the PR for #3416 with `libraries.yml`, `reports/hex-lll-performance.md`, and this progress file.

## Blockers

- `.claude/CLAUDE.md` remains dirty and unrelated/off-limits; it was not touched.
