# 2026-05-14 03:09 UTC — work session cd4fb8fa (issues #3926, #3929)

## Accomplished

Claimed #3926, attempted the residual size/state threading, and found that
the issue still combined two independent proof obligations: fuel-bound
transport through the recursive `squareFreeAuxRev` pairwise proof, and the
callback signature cascade that consumes those bounds.

Decomposed #3926 into:

- #3929 for the fuel-bound transport helpers.
- #3930 for the residual size/state callback cascade, depending on #3929.

Claimed #3929 and added two private helper surfaces in
`HexPolyFp/SquareFree.lean`:

- `pthRoot_fuel_bound_or_one_of_derivative_zero`, splitting the
  derivative-zero recursive branch into the constant-one case or the existing
  nonconstant `pthRoot` fuel decrease.
- `yunFactorsWithLevel_pthRoot_tail_fuel_bound`, translating the existing
  scaled contribution-tail validity lemma into the `yunFactorsWithLevel ... []`
  residual-tail shape used by the pairwise recursion.

Verification run:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

#3929 is complete. The next proof step is #3930: update the
pairwise-coprime residual callback signature to carry the fuel-bound and
derivative-active-state data, using the helpers added here.

## Next step

Claim #3930 after this PR lands and thread the richer residual callback through
`squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant` and the
downstream `_pairwise_coprime_*` cascade.

## Blockers

None for #3929. #3930 is intentionally blocked on #3929.
