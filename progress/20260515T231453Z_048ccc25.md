2026-05-15 23:14 UTC - HO-43 Round 3 decomposition (#4153 → #4339 #4340 #4341)

## Accomplished

Claimed feature issue #4153 (HO-43 Round 3 relocation of
`HexGramSchmidt/Int.lean` bridge-bound theorems). After orienting on
the current code state, decomposed into three sub-issues anchored on
the seven current direct `Matrix.bareiss_eq_det` users:

- #4339 (Round 3a) — Cramer/Bareiss + det-product chain. Eliminates 5
  of 7 direct uses: `scaledCoeffMatrix_bareiss_eq_det`,
  `gramDet_rat_eq_progressMatrix_zero_det`,
  `scaledCoeffMatrix_det_eq_gramDet_mul_coeffs`,
  `scaledCoeffs_eq_scaledCoeffMatrix_det`,
  `leadingGramMatrixInt_det_eq_gramDet_int_of_nonneg`, plus the
  transitive `progressMatrix_zero_eq_castIntDetMatrix`,
  `gramDet_eq_prod_normSq_core/full`, `gramDet_succ_rat`,
  `scaledCoeffRows_lower_eq_coeffs`, `scaledCoeffs_eq`,
  `leadingGramMatrixInt_det_eq_gramDet_int`,
  `gramDet_pos_core/full`, `basis_normSq_core/full`,
  `normSq_latticeVec_ge_min_basis_normSq`,
  `scaledCoeffMatrix_det_eq_zero_of_singularStep_lt`. Moves the
  private cast helpers (`det_intCast`, `castIntDetMatrix`, etc.)
  used exclusively by the relocated chain.
- #4340 (Round 3b, depends on 3a) — row-add and adjacent-swap chain.
  Eliminates the `gramDet_rowAdd_earlier` direct use plus the
  transitive `scaledCoeffs_rowAdd_*` and
  `gramDet_rowSwap_adjacent_pivot_product` chain.
- #4341 (Round 3c, depends on 3a) — independence chain plus the
  cascading HexLLL consumer refactor. Eliminates the final
  `gramDet_pos_of_det_positive` direct use, relocates
  `independent_of_det_positive` and `independent_one`, and refiles
  `HexLLL/Basic.lean`'s `identity_independent` /
  `independent_of_upperTriangular_pos_diag` (plus their callers in
  `HexLLL/Bench.lean`, `HexLLL/EmitFixtures.lean`,
  `HexBerlekampZassenhaus/Basic.lean`) into the bridge.

Left a `Decomposed into #4339, #4340, #4341` breadcrumb comment on
#4153 explaining the rationale, then released the claim via
`coordination skip 4153` so a planner can triage. The original audit
in the issue body listed stale targets (`gramDet_pos_core`,
`gramDet_pos`) that don't directly use `bareiss_eq_det` in the
current code, and missed the current direct user
`gramDet_pos_of_det_positive` — the decomposition rebases on the
actual current direct-user set rather than the audit's list.

## Current frontier

Sub-issues #4339, #4340, #4341 are unclaimed (3b and 3c are
`blocked` until 3a closes). #4339 is the next actionable feature
work; estimated ~1000 line move, ~15 theorem relocations, several
private helpers to migrate or de-`private`.

## Next step

A future worker claims #4339 and lands the Cramer/Bareiss + det-product
chain relocation. After 3a merges, `check-blocked` unblocks #4340 and
#4341, which can land in parallel.

## Blockers

None. The decomposition is mechanical; each sub-issue has an
independently verifiable bareiss-elimination check.
