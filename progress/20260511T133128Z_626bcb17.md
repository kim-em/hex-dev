# 2026-05-11 13:31 UTC — work #3446

## Accomplished

Claimed #3446 after releasing stale/malformed queue items:

- #3284 was skipped for replan because its prerequisite #3418 closed without the derivative-active state provider it depends on.
- #3444 was skipped for replan because it had title `--help` and an empty body.

In `HexPolyFp/SquareFree.lean`, added normalized-state proof infrastructure:

- `squareFreeContributionReachable_of_monic`
- `normalizedDerivativeActiveState_of_monic_nonzero`

These isolate the valid normalized/monic route to
`squareFreeContributionReachable c ∧ c.isZero = false ∧ w.isZero = false`
without asserting the false raw-state provider over arbitrary derivative-active
Yun reachability states.

Verification run:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep still reports only the pre-existing tracked
`squareFreeAuxRevContribution_derivative_active_pow_obligation` `sorry`.

## Current frontier

The normalized/monic state bridge is available, but the full derivative-active
chain still needs preservation facts showing the reached states satisfy the
monic/nonzero hypotheses needed by this bridge.

## Next step

Prove the monic/nonzero preservation lemmas for the normalized
derivative-active Yun chain, then thread them into the completion wrapper.

## Blockers

None for the local helper landed here. The broader #3445 parent remains a
multi-step proof and must avoid the stale raw-state provider shape from #3418.
