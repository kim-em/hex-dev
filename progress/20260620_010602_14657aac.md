# Phase 6 grind coverage — HexGF2 Basic.lean / Irreducibility.lean missed lemmas (#8133)

## Accomplished

Re-scanned the four `@[simp]`-only equation lemmas flagged by #8133 and
promoted three to `@[simp, grind =]`:

- `HexGF2/Basic.lean:121` — `coeffWords_normalizeWords`
  (`coeffWords (normalizeWords words) n = coeffWords words n`). LHS head
  `coeffWords`; RHS drops the wrapper. Eligible.
- `HexGF2/Irreducibility.lean:147` — `powWitness?_eq_getElem?`
  (`cert.powWitness? k = cert.powChain[k]?`). `rfl` projection, LHS head
  `powWitness?`, RHS `getElem?` (not the `getD`/`dite` offender). Eligible.
- `HexGF2/Irreducibility.lean:155` — `bezoutWitness?_eq_getElem?`
  (`cert.bezoutWitness? i = cert.bezout[i]?`). Same shape. Eligible.

## Documented exclusion

`HexGF2/Basic.lean:666` — `degree_eq_of_degree?_eq_some`
(`p.degree = d` under `h : p.degree? = some d`). Left `@[simp]`-only.
`grind =` rejects it with a hard build error:

```
invalid pattern(s) for `degree_eq_of_degree?_eq_some` [degree #2]
the following theorem parameters cannot be instantiated: d, h
```

The only LHS pattern `grind` can form is `p.degree`, which does not
mention `d` or `h` — both appear solely on the RHS / in the hypothesis,
so E-matching cannot instantiate them. This is the documented-exclusion
outcome the issue allows, not a missed promotion.

## Current frontier

`lake build HexGF2` green (52 jobs). Full downstream build clean (579
jobs): `HexGF2.EmitFixtures`, `HexGFq`, `HexGFq.CrossCheck`,
`HexGF2Bench`. `scripts/check_dag.py` exit 0. No grind
heartbeat/timeout/"Try this" warnings. Annotation-only; no proof bodies,
signatures, or imports touched; no `sorry`/`axiom`/`native_decide`.

## Next step

None for this issue. Per the issue body, the excluded `getD`/`Ne`/`↔`
lemmas (`normalizeWords_get?_getD`, `monomial_ne_zero`,
`isZero_eq_*_iff_words_*`) were deliberately not touched.

## Blockers

None.
