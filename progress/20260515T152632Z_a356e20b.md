# 20260515T152632Z — BZ small-mod singleton composition (issue #4238)

## Accomplished

- **Deliverable 1**: added `Hex.isGoodPrime_modP_isZero_false` and the
  full provenance chain culminating in
  `Hex.choosePrimeData?_factorsModP_berlekamp_form`.  The latter mirrors
  the existing `_prime` / `_fModP_eq` / `_isGoodPrime` chains and exposes
  the executable `factorsModP` array as the Berlekamp factor output for
  the monic modular image of the selected candidate.  An internal
  `factorsModPBerlekampForm` Prop bundles the nonzero-image and field
  witnesses through the prime-selection fold.

- **Deliverable 2**: added `toMathlibPolynomial_C` and
  `toMathlibPolynomial_scale` in `HexBerlekampZassenhausMathlib.Basic`,
  packaging the executable `DensePoly.C` and `DensePoly.scale`
  constructors across the Mathlib bridge to `Polynomial.C` and
  `Polynomial.C ∗ toMathlibPolynomial`.

- **Deliverable 3**: added
  `squareFreeCore_irreducible_of_small_mod_singleton_of_choosePrimeData`
  in `IntReductionMod`, dropping the explicit `hirr_fModP` hypothesis
  from the existing composition.  The proof routes through deliverable 1
  to identify `factorsModP` with the Berlekamp factor output, applies
  `irreducible_of_berlekampFactor_factors_length_le_one` to obtain
  Mathlib irreducibility of the monic image, and uses deliverable 2 plus
  `associated_isUnit_mul_left_iff` to transfer to Mathlib irreducibility
  of `fModP`.  Finally the existing
  `Hex_ZPoly_Irreducible_of_irreducible_modP` lifts to integer-level
  irreducibility of `core`.

- Exposed `Hex.choosePrimeData?`, `Hex.choosePrimeData?_prime`, and
  `Hex.choosePrimeData?_fModP_eq` (previously `private`) so the
  small-mod singleton consumer in `HexBerlekampZassenhausMathlib` can
  name the selection witness directly.

## Current frontier

`lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib` and
`python3 scripts/check_dag.py` both succeed.  No new theorem-level
`sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` introduced.

## Next step

The new composition theorem still takes `hsquareFree_monic` as an
explicit precondition on the monic modular image.  The deliverable's
"deriving it from" wording assumed this could fall out of `squareFreeModP
core p` directly; in practice the executable `Hex.DensePoly.gcd` is not
yet normalized in a way that survives `DensePoly.scale`, so a
`gcd_monicModularImage_derivative_eq_one` lemma — most cleanly via
`derivative_scale` on `FpPoly p` plus a `Monic`-based gcd-equal-one
characterization — is the natural follow-up issue.

A separate consumer (or a follow-up planner issue) can then plug
`hsquareFree_monic` automatically from `Hex.squareFreeModP core p`.

## Blockers

None for this PR.
