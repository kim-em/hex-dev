# 20260511T025135Z — agent/4b90b335 — feature #3210

## Accomplished

Resolved Phase-2 review finding #3210 (HexBZ public `factor` combinator
divergence from SPEC equation). The issue required picking one of two
mutually-exclusive paths:

- **Path 1**: restore the SPEC equation `factor f = (factorFast f).getD
  (factorSlow f)` in `HexBerlekampZassenhaus/Basic.lean`.
- **Path 2**: update the SPEC equation to match the current
  Mignotte-bounded implementation.

Default per the issue was Path 1. I attempted Path 1 first and verified
empirically that it cannot accommodate the existing conformance fixture
corpus.

### Path 1 empirical result

Changing `factor` to `(factorFast f).getD (factorSlow f)` builds cleanly
but `lake exe hexbz_emit_fixtures` hangs at 99% CPU for 5+ minutes
without emitting any fixture record. Root cause is documented in
`HexBerlekampZassenhaus/Bench.lean:19-22`: "Full `factorFast` on `X^4 +
1` and `Phi_15` currently exceeds the smoke verifier's one-call
budget." `adv/x4_plus_1` is in the public conformance fixture set
(`HexBerlekampZassenhaus/EmitFixtures.lean` `cases_pinned`), and any
public `factor` call routing through `factorFast` at the full
`bhksBound`-derived cap inherits the same intractability for
irreducible inputs whose mod-`p` factorisation has more than one
factor. Narrowing the fixture set to dodge this would silently weaken
the conformance corpus; the bench-tractability argument is genuinely
the binding constraint.

### Path 2 execution

- Reverted my `factor` change in `HexBerlekampZassenhaus/Basic.lean`.
  The implementation remains exactly as it was on `main`:
  `def factor (f : ZPoly) : Factorization :=
     factorWithBound f (ZPoly.defaultFactorCoeffBound f)`.
- Edited `SPEC/Libraries/hex-berlekamp-zassenhaus.md` to align the
  Top-level API block, the "Recombination" overview, and the Group C
  correctness sketch with this Mignotte-bounded public combinator.
  Documented the bench-tractability rationale and clarified that
  `factorFast` is retained at the full BHKS cap as a proof-facing
  entry point (so the Group B conditional-correctness theorem and
  the Group D BHKS Theorem 5.2 termination guarantee still attach to
  the BHKS-capped combinator).

### Verification

All required commands from the issue pass:

- `lake build HexBerlekampZassenhaus` — clean (3 pre-existing sorry).
- `lake build HexBerlekampZassenhaus.Conformance` — clean.
- `lake exe hexbz_bench list` — 13 registered benchmarks listed.
- `bash scripts/ci/run_oracles.sh` — all 16 oracles pass.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `rg '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean`
  — 3 markers, matching the pre-existing Group B/C `sorry` count
  (lines 2033, 2660, 2675).

## Current frontier

SPEC and code now agree on `factor f := factorWithBound f
(defaultFactorCoeffBound f)`. Human-oversight #2637 needs the
cross-link from path 2's "Update the SPEC equation" workflow; I'll
post that on the issue thread as part of the PR.

## Next step

Open the PR closing #3210; comment on #2637 with the SPEC-edit
cross-link.

## Blockers

None.
