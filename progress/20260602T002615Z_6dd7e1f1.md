# Progress — 2026-06-02T00:26:15Z — session 6dd7e1f1

Issue #6172: BZMathlib: non-monic slowPathHenselSubstrate constructor via
`(toMonic core).monic`.

## Accomplished

* Generalised the two private analytic obligations in
  `HexBerlekampZassenhausMathlib/Basic.lean`
  (`henselSubsetCorrespondence_analytic_obligation` and
  `liftedFactorSubsetPartition_analytic_obligation`) to take
  `primeData : Hex.PrimeChoiceData` as an explicit parameter instead of
  hardcoding it as `Hex.choosePrimeData core`.  The same single sorry
  per obligation now covers both the monic
  (`Hex.choosePrimeData? core = some primeData`) and non-monic
  (`Hex.ZPoly.toMonicPrimeData? core = some primeData`) branches; no new
  theorem-level sorrys were introduced.
* Updated the existing `_of_choosePrimeData` callers
  (`henselSubsetCorrespondenceHypotheses_of_choosePrimeData`,
  `liftedFactorSubsetPartition_of_choosePrimeData`) to pass `primeData`
  through to the generalised obligations.  External signatures unchanged.
* Added sibling lemmas
  `henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData` and
  `liftedFactorSubsetPartition_of_toMonicPrimeData`, parallel to the
  existing `_of_choosePrimeData` versions, consuming the
  `Hex.ZPoly.toMonicPrimeData? core = some primeData` witness directly.
* Added the new substrate constructor
  `slowPathHenselSubstrate_of_toMonicChoosePrimeData` (adjacent to
  `slowPathHenselSubstrate_of_choosePrimeData`).  It takes
  `hselected`, `hcore_lc_pos`, `hcore_pos`, `hcore_sqfree`,
  `hB_ne_zero` — no `hcore_monic`, no `hchoose`.  `hcore_lc_pos` is
  necessary because the `_of_monicPrimeData` umbrella lemmas
  (`Hex.ZPoly.toMonicLiftData_liftedFactor_monic_of_monicPrimeData`,
  `_natDegree_pos_of_monicPrimeData`,
  `_injective_of_monicPrimeData`) require it; the public-API caller
  (`(Hex.normalizeForFactor f).squareFreeCore`) provides it.
* Existing monic-core constructor signature and behaviour unchanged.
* `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, and `git diff --check` all pass.
* `rg -n "axiom|native_decide" HexBerlekampZassenhausMathlib/Basic.lean`
  shows no hits.  Sorry count in the file unchanged at 3 (lines 257,
  16609, 16770).

## Current frontier

`slowPathHenselSubstrate_of_toMonicChoosePrimeData` is ready to consume
in #4170's slow-path arm.  The capstone consumer can now apply it at
`core = (Hex.normalizeForFactor f).squareFreeCore` without needing the
core to be monic.

## Next step

Open the PR for #6172; do not touch #4170 in this session (out of
scope per the issue).

## Blockers

None.
