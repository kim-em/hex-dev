# 20260509T183115Z - Issue #2834

## Accomplished

Claimed feature issue #2834 and verified the current `HexPolyFp` baseline.
Added `yunFactors_pairwise_coprime_nil_of_invariant` in
`HexPolyFp/SquareFree.lean`, exposing the completed Yun-loop pairwise induction
through the strengthened `yunFactorsPairwiseInvariant`/`yunFactorsPairwiseReady`
payload from #2832.

Created follow-up issue #2845 for the missing derivative-split readiness proof
needed before `squareFreeAuxRev_pairwise_coprime_nil_core` can pass a real
invariant and the known-false arbitrary-state theorem can be removed.

Verification completed:

- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The `rg` scan still reports the pre-existing `sorry`s in
`HexPolyFp/SquareFree.lean`: product reconstruction, the old arbitrary
Yun-loop pairwise theorem, the tail cross-coprime theorem, and the square-free
steps theorem. No new banned marker was introduced.

## Current frontier

The consumer theorem over `yunFactorsPairwiseInvariant` is now available, but
#2834 cannot be fully discharged until a derivative-active initial state
readiness theorem is proved. That missing readiness theorem is tracked by
#2845.

## Next step

Prove #2845, then replace the old arbitrary-state
`yunFactors_pairwise_coprime_nil` call sites with the invariant-gated theorem
and continue into the #2816 tail cross-coprimality work.

## Blockers

The issue body assumed #2832 supplied enough invariant evidence for the caller;
in the current code, `yunFactorsPairwiseInvariant_of_derivative_split` still
requires `yunFactorsPairwiseReady` as an explicit hypothesis.
