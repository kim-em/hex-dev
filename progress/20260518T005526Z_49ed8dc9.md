# Session 49ed8dc9 — #4869 scaledRecombinationSearchMod product lemmas

## Accomplished

Added three private structural-invariant lemmas in
`HexBerlekampZassenhaus/Basic.lean` mirroring the unscaled siblings at
lines 7652–7705 / 7928–7936:

* `scaledRecombinationSearchModAux_product` — induction on fuel,
  candidate `let` adapted to insert `DensePoly.scale coreLc` before
  centred-lifting. Otherwise identical to the unscaled aux proof.
* `scaledRecombinationSearchMod_product` — trivial wrapper at
  `fuel = localFactors.length + 1`.
* `recombineScaledExhaustive_product` — unfolds the executable
  wrapper and applies the search-mod lemma.

Placed adjacent to the existing scaled siblings
(`_normalizeFactorSign`, `_shouldRecord`,
`recombineScaledExhaustive_*`) immediately after
`recombineScaledExhaustive_shouldRecord`.

Verification:

* `lake build HexBerlekampZassenhaus` — clean (pre-existing
  warnings at 2291 and 6436 unchanged).
* `lake build HexBerlekampZassenhausMathlib` — clean.
* `python3 scripts/check_dag.py` — passes.
* `git diff --check` — clean.
* No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` on
  the diff.

## Current frontier

Three structural recovery lemmas now exist for the scaled
recombination chain. The next prerequisite in the #4639 chain
(#4870) can now swap `exhaustiveCoreFactorsWithBound` to
`recombineScaledExhaustive` and rewire its downstream `_product`
consumers.

## Next step

#4870 (blocked on this PR + #4866) → call-site swap +
downstream `exhaustiveCoreFactorsWithBound_product` rewire.

## Blockers

None.
