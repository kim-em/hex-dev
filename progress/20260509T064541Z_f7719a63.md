# 2026-05-09 (UTC) — issue #2743 (HexGfqField Phase-4 evidence package)

## Accomplished

- Added `libraries.yml: HexGfqField.phase4.input_families` with four
  entries (`dense-canonical-reduction`, `field-arithmetic`,
  `field-exponentiation`, `field-inversion-division`) covering the
  registered bench targets in `HexGfqField/Bench.lean`.
- Ran the scientific `lake exe hexgfqfield_bench run` over all eight
  parametric registrations at commit `523de1997cfb` on `carica`;
  exported `reports/bench-results/hex-gfq-field-523de1997cfb.json`.
- Recorded developer-local samply profiles for each of the four
  `phase4.input_families` (`runOfPolyReprChecksum`, `runMulChecksum`,
  `runZPowChecksum`, `runInvDivChecksum`) and analysed leaf cost
  categories + inclusive-cost rankings using a small Python
  symbol-resolution + Lean-name demangling helper against the
  samply `--unstable-presymbolicate` `.syms.json` sidecars.
- Wrote `reports/hex-gfq-field-performance.md` with the five
  mandated subsections: Bench targets, Verdicts, Comparator ratios
  (none declared in SPEC), Profile (one case per input family with
  leaf-cost categorisation, inclusive-cost ranking, and dominant-cost
  narrative), Concerns.
- Filed #2784 to widen the `paramSchedule` beyond `{2, 3, 4, 6}` so
  the seven currently-inconclusive verdicts can return `consistent
  with declared complexity`. The single `consistent` verdict
  (`runZPowChecksum`) at the same schedule confirms the cost-model
  declarations are correct and only the rung spacing is too narrow
  to fit the slope.

## Current frontier

- `HexGfqField` keeps `done_through: 3` in `libraries.yml`. The
  report's `§Concerns` has the live #2784 entry, so the SPEC's
  "remain at `done_through: 4` only while §Concerns is empty" rule
  prevents the bump.
- All four input-family profiles attribute their dominant inclusive
  cost to a registered bench target via `GFqRing.reduceMod`,
  `GFqRing.mul`, `GFqField.zpow`/`pow.go`, and
  `GFqField.inv`/`DensePoly.xgcd`. The Attribution Rule is
  satisfied.

## Next step

- A follow-on session for #2784 adds certificate-checked irreducible
  `F_7` moduli at higher degrees (probably degrees 8/12/16/24), wires
  them into `bundleForN`, replaces the four-rung schedule, re-runs
  the bench, refreshes `reports/hex-gfq-field-performance.md
  §Verdicts`, and bumps `HexGfqField.done_through` to `4` once
  `§Concerns` is empty.

## Blockers

- None. #2784 is self-contained Phase-4 calibration work and does
  not depend on any other open issue.
