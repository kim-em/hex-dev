# 2026-06-01T11:13Z - HBZMathlib no-associates packaging (#5893)

## Accomplished

- Claimed feature issue #5893.
- Added `zpoly_eq_of_toPolynomial_associated_of_primitive_pos_leading`, a
  Mathlib bridge lemma turning associateness of transported primitive
  positive-leading `Hex.ZPoly` values into equality.
- Added `zpoly_not_associated_of_ne_of_primitive_pos_leading`.
- Added public `factor_entries_not_associated`, packaging
  `Hex.factor_pairwise_first`, `Hex.factor_entries_primitive`, and
  `Hex.factor_entry_leadingCoeff_pos` into a pairwise non-association theorem
  for `(Hex.factor f).factors.toList`.

## Current frontier

- The no-associates clause is now available as conditional Mathlib-side
  packaging under the raw-primitive hypothesis from the sibling wrapper.
- The full HO-1 headline theorem still needs to assemble the conditional
  primitive and irreducibility hypotheses into the final public contract.

## Next step

- Continue the HO-1 assembly by combining product, scalar, primitive,
  irreducible, positive-multiplicity, positive-leading, and pairwise
  non-association clauses into the final headline theorem shape.

## Blockers

- `lake build HexBerlekampZassenhausMathlib` still fails in the pre-existing
  stale `HexBerlekampZassenhausMathlib/IntReductionMod.lean` call sites around
  `squareFreeModP`, `liftedFactorSubsetPartition_of_choosePrimeData`,
  `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`, and missing
  `monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`.
