# 2026-05-09 (UTC) — issue #2693 (HexLLL fpLLL comparator)

## Accomplished

- Claimed human-oversight issue #2693 after #2692 raced and was already claimed.
- Extended `scripts/oracle/lll_fpylll.py` with `--bench-checksum`, a whitespace
  matrix input mode that runs `fpylll.LLL.reduction(delta = 0.75)` and prints
  the same first-row checksum used by `Hex.LLLBench.intVectorChecksum`.
- Wired `fpLLL via fpylll` into `HexLLL/Bench.lean` as informational
  process-call fixed benchmarks for the bottom rung of each Phase-4 input
  family: BZ recombination, random-bounded `n = 30`, and harsh-cubic `n = 15`.
- Added fixed Lean companion registrations for the two parametric bottom rungs
  so `lean-bench compare` can report ratios against the fpylll process targets.
- Recorded expected hashes for all new fixed comparator targets and documented
  the fpylll source/version range plus the informational-classification
  rationale in the bench module docstring.

## Current frontier

- HexLLL now has fpylll informational comparator registrations that build,
  verify, and compare cleanly on the three bottom-rung shared inputs.
- The verified-Isabelle gating comparator from #2692 remains separate work.

## Next step

- Land the #2693 PR. A follow-up Phase-4/report worker can cite the new
  comparator registrations and ratios when assembling the HexLLL headline
  performance report.

## Blockers

- None for #2693. Local fpylll verification used a repo-local venv under
  `.cache/oracles/fpylll-venv` because Homebrew Python rejects `--user`
  package installs; this cache is intentionally untracked.
