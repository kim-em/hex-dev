# 20260504T083539Z — feature session c10b2f32

Issue: #1910 — HexPolyFp: close coeff-fold pth coefficient cancellation.

## Accomplished

- Proved `coeffFoldPowerCoeff_prime_coeff` in `HexPolyFp/SquareFree.lean`
  (previously `sorry`).
- Strategy: rewrite into the polynomial-coefficient form via
  `powLinear_coeffFold_prime_coeff_expansion`, then induct on the bound `m`.
  - Base `m = 0`: `coeffFold g 0 = 0`, and `(powLinear (0 : FpPoly p) p).coeff n = 0`
    via `powLinearBinom_mul_zero` for `p ≥ 1`.
  - Step `m + 1`: rewrite `coeffFold g (m + 1) = coeffFold g m + coeffTerm g m`,
    apply `powLinear_add_prime` and `DensePoly.coeff_add` (using
    `zmod64_add_zero_zero_coeff`), reuse `powLinear_coeffTerm_coeff`
    for the new contribution, and case-split on `n % p`, `n / p < m`,
    `n / p = m` to align with `n = p * m`.

## Verification

- `lake build HexPolyFp.SquareFree` green.
- `lake build HexPolyFp` green.
- `python3 scripts/status.py HexPolyFp` reports HexPolyFp ready (Phase 5
  loop) — sorry count in `HexPolyFp/SquareFree.lean` 6 → 5, downstream
  `coeffFoldPowerCoeff_prime_coeff_of_mod_*`, `powLinear_coeffFold_prime_coeff`,
  `powLinear_prime_coeff`, `pthRoot_pow_prime_coeff` all elaborate.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- No `axiom`, `native_decide`, `TODO`, or `FIXME` introduced; only the
  target `sorry` was removed (line 1011).

## Current frontier

The Yun-style cancellation chain is closed through
`pthRoot_pow_prime_coeff`. Remaining sorries in
`HexPolyFp/SquareFree.lean` (5) live further down the file (around
lines 1931, 1942, 2440, 2446, 2550), unrelated to coeff-fold expansion.

## Next step

PR posted; CI will auto-merge if green. No follow-up from this session.

## Blockers

None.
