## Accomplished

- Claimed #2647 (`HexBerlekamp: prove DDF product reconstruction`).
- Replaced the `prod_distinctDegreeFactor` proof `sorry` in
  `HexBerlekamp/DistinctDegree.lean` with checked product-preservation
  lemmas for `finishDegreePower`, `distinctDegreePowerLoop`, and
  `distinctDegreeLoop`.
- Reused the existing `isUnitPolynomial_of_dvd_isUnitPolynomial` bridge from
  `HexBerlekamp.RabinSoundness` to show that multiplying by a non-unit gcd
  cannot make the DDF accumulator a unit.
- Verified:
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekamp/DistinctDegree.lean`

## Current frontier

- `prod_distinctDegreeFactor` is closed.
- The existing `distinctDegreeFactor_bucket_invariants` theorem still has its
  pre-existing `sorry` and remains out of scope for #2647.

## Next step

- Push branch `agent/1540bc0c` and open the PR for #2647.

## Blockers

- None.
