# 20260609T192011Z (#6692)

## Accomplished

Proved the sharp phase-one Newton envelope bound in
`HexPolyZ/Mignotte.lean`. The named private wrapper
`sqrtAux_phase_one_near_or_sq_le` now uses exactly `n.log2 + 1` fuel rather
than the previous `2 * n.log2 + 1`.

The proof adds a direct accounting route:

- `sqrtAux_contract_of_not_done`: if a fuel-bounded run has not stopped with
  `y * y ≤ n` and has not entered `sqrtNearEnvelope`, every executed step was
  in the very-far branch and the accumulated contraction gives
  `32^fuel * y ≤ 17^fuel * x`.
- `phase_one_power_contradiction`: with `fuel = n.log2 + 1`, the final
  very-far assumption would force `16 * 1024^fuel ≤ 289^fuel * n`, contradicting
  `n < 2^fuel` and `289 * 2 ≤ 1024`.
- `sqrtAux_phase_one_sharp_core`: combines the two facts for the initial state
  `x = n`.

Executable definitions (`sqrtStep`, `sqrtAux`, `floorSqrt`) are unchanged.
The previous private `2 * log2 + 1` phase-one core and its private two-step
log-drop-only helpers were removed after the sharp accounting made them dead
code.

## Current frontier

The residual #6668 / #6692 proof obligation is closed on the `HexPolyZ`
side. Downstream phase-two/final `floorSqrt_sq_le` work can now consume the
sharp phase-one handoff.

## Next step

Use `sqrtAux_phase_one_near_or_sq_le` in the downstream floor-square lower-bound
composition once the phase-two near-envelope theorem is ready.

## Blockers

None. Verification performed:

- `lake build HexPolyZ` (clean apart from pre-existing `HexPolyZ.Basic`
  unnecessary-`simpa` warnings)
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over touched Lean files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
