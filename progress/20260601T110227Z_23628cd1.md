# 2026-06-01T11:02Z - HBZMathlib primitive headline packaging (#5905)

## Accomplished

- Claimed feature issue #5905.
- Added `factor_entries_primitive_of_chosen_raw_primitive` in
  `HexBerlekampZassenhausMathlib/Basic.lean`, re-exporting the landed
  Mathlib-free primitive-entry wrapper under the bridge namespace.
- Added `factor_headline_contract_core_with_primitive`, a sibling of the
  existing headline core contract whose per-entry clause packages
  `Hex.ZPoly.Primitive entry.1` together with transported Mathlib
  irreducibility.
- Verified `lake build HexBerlekampZassenhausMathlib.Basic`.
- Ran a second-opinion review; it found no correctness regression in the
  scoped packaging diff.

## Current frontier

- #5905's primitive clause is now exposed at the Mathlib bridge surface under
  the existing chosen-raw-factor primitive hypothesis.
- The no-associates headline clause remains separate follow-up work (#5893).
- The underlying irreducibility theorem in this bridge still depends on the
  pre-existing `factor_irreducible_of_nonUnit` sorry.

## Next step

- Use the new primitive-strengthened contract when assembling the final
  five-clause HO-1 headline theorem, or combine it with the no-associates
  packaging once #5893 lands.

## Blockers

- `lake build HexBerlekampZassenhausMathlib` still fails in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` with the same stale
  API/signature errors recorded by the previous session; the touched
  `Basic.lean` target builds.
