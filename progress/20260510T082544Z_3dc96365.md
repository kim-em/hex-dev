# 20260510T082544Z - Issue #2967

## Accomplished

Claimed #2967 and repaired the HexBerlekampZassenhaus benchmark smoke surface.

`HexBerlekampZassenhaus/Bench.lean` no longer registers full `factorFast`
singleton smoke runs for the `X^4 + 1` and `Phi_15` adversarial inputs. Those
calls exceed even a 20-second one-call cap under `lean-bench verify`. They are
replaced by smoke-safe fast-path setup targets:

- `runFactorFastSetupAdvX4Plus1Checksum`, recording the public fast precision
  cap and pinned `p = 5` modular split profile.
- `runFactorFastSetupAdvPhi15Checksum`, recording the public fast precision cap
  and pinned `p = 31` modular split profile.

The public `factor` adversarial singleton targets remain registered, and the
quadratic adversarial full `factorFast` target remains registered because it is
smoke-tractable.

Verification completed:

- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify`
- `python3 scripts/check_phase4.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

`hexbz_bench verify` now reports all 13 benchmark registrations passing.

## Current frontier

The HO-3 smoke gate is green for the current HexBZ benchmark registrations.
Full `factorFast` on `X^4 + 1` and `Phi_15` remains too expensive for smoke
verification and should be treated as scientific-only until the BHKS fast path
gets cheaper or termination work changes the execution profile.

## Next step

Continue HO-3 by adding or refining scientific benchmark runs and report
artifacts, or continue HO-2/HO-4 support work as separately scoped issues.

## Blockers

No blockers for #2967. An unrelated pre-existing `.claude/CLAUDE.md` worktree
modification was present and left untouched.
