# Progress - 2026-06-16T12:21:05Z (session 939d6cb6, work)

## Accomplished

Checked the directive queue first. Claiming directive #2564 failed because
coordination reported it as dependency-blocked, so this session did not work on
it.

Claimed #6771 next because it was the oldest critical-path feature, then
sanity-checked the premise against the current code and related issue metadata.
The forward fast-core irreducibility wrapper already exists, but #6771 still
asks for unconditional fast `h_raw` assembly before a producer for the required
`ForwardRecoveryInputs` / `CutProjectionHypotheses` package is available. Its
body also contradicts current #6779 metadata: #6779 now says the old B8
bad-vector route is off the correctness path and no longer a #6771 blocker.
Marked #6771 `replan` with that concrete stale-premise reason.

Claimed #7580 and polished the `HexPolyZ` unit and rational-cast API in
`HexPolyZ/Basic.lean`:

- added `isUnit_C_one`, `isUnit_of_eq_one`, `isUnit_of_eq_neg_one`, and
  `isUnit_neg_one`;
- made `coeff_toRatPoly` a simp lemma so coefficientwise rational-cast goals
  normalize directly.

Verification:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "\bsorry\b|\baxiom\b|\bnative_decide\b|TODO|FIXME" HexPolyZ/Basic.lean`

## Current frontier

#7580 is complete. The `HexPolyZ` unit predicate now has direct constructors
for both equality-to-unit forms, including the `-1` polynomial normal form, and
`toRatPoly` coefficient normalization is available to simp.

## Next step

Open the PR for #7580. The next worker should leave #6771 to planner triage and
can pick the next ready feature item, likely #7582 if still unclaimed.

## Blockers

None for #7580. The worktree still has an unrelated pre-existing modification
to `.claude/CLAUDE.md`; this session did not touch or stage it.
