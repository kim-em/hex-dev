# 2026-06-04 15:54Z — decircularise 3 bridge theorems via scaledCoeffs_lower_eq_det_scaledCoeffMatrix (#6552)

## Accomplished

Inverted the call graph for the strictly-lower-entry Cramer/Bareiss bridge
chain in `HexGramSchmidtMathlib/Int.lean`: the unconditional Rat-level
`scaledCoeffs_lower_eq_det_scaledCoeffMatrix` is now the foundation, and
all five bridge theorems consume it directly.

Concretely:

- Moved `scaledCoeffs_lower_eq_det_scaledCoeffMatrix` from line 4367 to
  line 2433 (right before `scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`).
- Replaced its old proof (which routed through
  `_of_no_singular` → `scaledCoeffs_eq_scaledCoeffMatrix_det` →
  `scaledCoeffs_eq_scaledCoeffMatrix_bareiss` → `_bareiss_of_no_singular`,
  i.e. circularly through the bridge theorems we want to decircularise)
  with a self-contained direct proof: case-split on
  `noPivotLoop … .singularStep`; the non-singular branch chains the
  Mathlib-free `scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix` with
  `noPivotLoop_full_eq_borderedMinor_at_trailing`,
  `scaledCoeffMatrix_eq_borderedMinor`,
  `Matrix.bareiss_eq_noPivotLoop_last_of_no_singular`, and the
  `bareiss_eq_mathlib_det` / `det_eq` cross-bridge; the singular branch
  composes `getArrayEntry_scaledCoeffRowsSchur_eq_zero_of_singularStep_lt`
  with `scaledCoeffMatrix_det_eq_zero_of_singularStep_lt`.
- Rewrote `scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular` as a
  5-line consumer of the new theorem: `exact_mod_cast` back to Int, then
  apply `bareiss_eq_mathlib_det.trans det_eq.symm`. The `h_nonsing`
  hypothesis is now unused (renamed to `_h_nonsing`) since the unconditional
  identity covers both branches.
- Rewrote `scaledCoeffs_eq_scaledCoeffMatrix_bareiss` (case-split version)
  with the same 5-line shape; no longer case-splits internally.
- Rewrote `scaledCoeffRows_lower_eq_coeffs` and `scaledCoeffs_eq` to compose
  the new theorem with `scaledCoeffMatrix_det_eq_gramDet_mul_coeffs`
  directly (saves the `scaledCoeffMatrix_bareiss_eq_det` hop).
- Removed the now-redundant `scaledCoeffs_lower_eq_det_scaledCoeffMatrix_of_no_singular`
  (old line 3204) and `scaledCoeffs_lower_eq_zero_of_singularStep_lt`
  (old line 4348); they were used only by the old `_lower_eq_det_scaledCoeffMatrix`
  proof, which is now inlined into the new direct proof.

## Acceptance criteria

- `grep -c sorry HexGramSchmidt/Int.lean` = 2 (unchanged: docstring word at
  line 6136 + `#5655`-tracked sorry). ✓
- `grep -c sorry HexGramSchmidtMathlib/Int.lean` = 0. ✓
- `lake build` clean (244 jobs). ✓
- No new `sorry`, `axiom`, or `native_decide`. ✓
- Net -47 lines in `HexGramSchmidtMathlib/Int.lean` (90 insertions, 137
  deletions). ✓
- The three target theorem proofs are clearly shorter than the prior
  case-split chain: `_bareiss_of_no_singular` shrank from 22 lines of
  bordered-minor sync to 5; `scaledCoeffRows_lower_eq_coeffs` lost the
  `scaledCoeffs_eq_scaledCoeffMatrix_bareiss` + `scaledCoeffMatrix_bareiss_eq_det`
  hops; `scaledCoeffs_eq` is now 2 lines.
- No `_bareiss_of_no_singular` ↔ `_lower_eq_det_scaledCoeffMatrix` cycle:
  the new direct proof of the foundation grounds in low-level
  Mathlib-free helpers (`scaledCoeffs_lower_eq_noPivotLoop_scaledCoeffMatrix`)
  + the cross-bridges (`bareiss_eq_mathlib_det`, `det_eq`) + the singular
  cascade (`scaledCoeffMatrix_det_eq_zero_of_singularStep_lt`,
  `getArrayEntry_scaledCoeffRowsSchur_eq_zero_of_singularStep_lt`).

## Current frontier

Deliverable 1 of parent #6549 (now closed) is complete. Umbrella issue
#6458 closes by transitive closure once this lands.

## Next step

None for this session — the directive's scope is fully delivered. A
future cleanup might also retire `scaledCoeffMatrix_bareiss_eq_det`
(now used only inside this file) if a sweep over `Update.lean` migrates
its remaining users.

## Blockers

None.
