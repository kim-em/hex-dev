## Accomplished

- Claimed #2682 (HexBerlekamp: prove DDF power-loop bucket divisibility
  invariant) and added two private lemmas in
  `HexBerlekamp/DistinctDegree.lean`:
  - `finishDegreePower_bucket_dvd_diff`: discharges all three exit cases
    of `finishDegreePower`, with the scalar-unit residual case handled
    via the explicit `acc * residual ∣ diff` hypothesis.
  - `distinctDegreePowerLoop_bucket_dvd_diff`: threads an abstract
    step-preserving predicate `P : FpPoly → FpPoly → Prop` through
    induction on the loop's fuel.  The three hypotheses
    (`hP_acc_dvd`, `hP_finish_unit`, `hP_step`) match the loop's
    branching: trivial divisibility on entry, the scalar-unit finish,
    and gcd-extraction preservation.
- The abstract `P` factoring keeps this issue self-contained while
  letting the parent #2648 plug in a concrete invariant
  ("`acc ∣ diff` together with a square-free residual coprime to
  `acc`") once the supporting square-free / multiplicity preservation
  lemmas land.
- Verified with `lake build HexBerlekamp` (clean, only the pre-existing
  `distinctDegreeFactor_bucket_invariants` sorry remains in the file),
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

- The new lemmas are unused so far; the consumer is the existing
  `sorry` at `distinctDegreeFactor_bucket_invariants` in the same
  file (issue #2648).
- The unrelated pre-existing `.claude/CLAUDE.md` modification and
  untracked `.claude/commands/`, `.claude/skills/` are documented in
  earlier turns and excluded from this PR.

## Next step

- Issue #2648 (parent) can now consume both #2681 (outer Frobenius
  state) and #2682 (this) once the square-free / coprime invariant
  preservation lemmas exist.  Concrete obligations a follow-up will
  need:
  - product-of-coprime-divisors lemma `a ∣ d ∧ b ∣ d ∧ gcd(a, b) = 1 →
    a*b ∣ d` for `FpPoly` (Bezout-style proof from existing
    `xgcd_bezout`),
  - square-free preservation under division by a divisor,
  - coprime extraction `c ∣ residual ∧ residual square-free →
    gcd(c, residual / c) = 1`.
  These naturally split into separate sub-issues.

## Blockers

- None for #2682.
