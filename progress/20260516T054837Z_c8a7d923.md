# HexBZ exactQuotient non-monic wrapper (#4409)

## Accomplished

- Claimed #4406 after all higher-priority directives were unavailable to claim.
- Determined #4406 was too large for one session because it requires the full
  reverse support theorem for recombination candidates, while the current API
  only exposes forward support plus a monic exact-quotient converse.
- Decomposed #4406 into #4409, #4410, and #4412, left the required
  `Decomposed into #4409, #4410, #4412` breadcrumb, and released #4406 for
  replan triage.
- Claimed #4409 and added
  `exactQuotient?_eq_some_of_divMod_eq_of_shouldRecord` in
  `HexBerlekampZassenhaus/Basic.lean`.
- Documented the non-monic boundary: product equality alone does not determine
  executable integer `DensePoly.divMod`; downstream proofs must also provide
  the concrete zero-remainder `divMod` result.

## Current frontier

#4409 is implemented and ready for PR. The next structural work is #4410,
which should prove the reverse candidate-support prerequisite using the new
wrapper only if it can also establish the required executable `divMod` result.

## Next step

Open the PR for #4409, then let #4410 consume the new exact-quotient wrapper.

## Blockers

None for #4409. #4410 remains the substantive reverse-support proof.
