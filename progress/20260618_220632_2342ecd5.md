# Progress — 2026-06-18 22:06 UTC (session 2342ecd5, /work → /feature #7987)

## Accomplished

Completed #7987 (HexMatrix Phase 6 grind automation coverage for
`RowEchelon.lean`).

Added `@[grind =]` (as `@[simp, grind =]`) to the 14 equation-shaped public
characterising lemmas of the row/column elementary operations, all of which
already carried `@[simp]`:

- Row ops: `row_rowSwap_left`, `row_rowSwap_right`, `row_rowScale_self`,
  `row_rowAdd_dst`, `row_rowAdd_src_of_ne`, `rowSwap_self`, `rowScale_one`,
  `rowAdd_zero`.
- Column ops: `col_colAdd_dst`, `col_colAddRight_dst`, `col_colAdd_src_of_ne`,
  `col_colAddRight_src_of_ne`, `colAdd_zero`, `colAddRight_zero`.

Matches the `@[simp, grind =]` convention in `HexMatrix/Basic.lean`.

## Decisions

- **Deliverable 2 (proof shortening):** no in-file proof shortens. Every
  annotated lemma already fired as `simp`, so in-file callers already used
  it as a rewrite; converting to `@[simp, grind =]` adds nothing in-file.
  The payoff is downstream `grind` availability in the consuming layers
  (`Determinant`, `RREF`, `Bareiss`), which now have these elementary-op
  rewrites as `grind` equations.
- Did not blanket-annotate. The other `@[simp]` lemmas in the file
  (`getElem`-shaped readers like `rowSwap_getElem` are `theorem` without
  `@[simp]`; the `_of_ne` row/col variants without `@[simp]`) were left
  alone per the issue's "only the genuinely useful grind rewrites" rule.
  The 14 chosen are exactly the issue's enumerated set.

## Verification

- `lake build HexMatrix` → 389 jobs, `Build completed successfully`.
  `RowEchelon` built in 4.4s; downstream `RREF`/`Determinant`/`Bareiss`
  all green — no `grind`-loop or slowdown.
- `git diff --check` clean; 14 insertions / 14 deletions (annotation-only).
- No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
- `python3 scripts/check_dag.py` → exit 0.

## Current frontier

`RowEchelon.lean` grind-annotation pass done. Sibling Phase 6 passes
(#7988 HexPolyZ Mignotte, #7989 HexPolyFp Frobenius) remain unclaimed.

## Next step

Pick up #7988 or #7989 (independent parallel passes) in a future session.

## Blockers

None.

Note: worktree carries a pre-existing unrelated modification to
`.claude/skills/agent-worker-flow/SKILL.md` from before this session; not
touched or staged.
