# HexMatrixMathlib/RankSpanNullspace Phase 6 grind coverage (#8104)

## Accomplished

Promoted the 3 equation-shaped public `@[simp]` lemmas in
`HexMatrixMathlib/RankSpanNullspace.lean` to `@[simp, grind =]`:

- `vectorEquiv_apply` — `vectorEquiv v i = v[i]`
- `vectorEquiv_symm_apply` — `(vectorEquiv.symm f)[i] = f i`
- `matrixEquiv_row` — `Matrix.row (matrixEquiv M) i = vectorEquiv (Hex.Matrix.row M i)`

Re-read each at promotion: all three conclusions are literal `lhs = rhs`
(no `↔`/`<`/`≤`/conjunction). `vectorEquiv_symm_apply` has a `getElem`
LHS head — `getElem` with a bounds proof is `Array.getInternal`, not the
`Array.getD`→`dite` offender, so `grind =` accepts it (confirmed: clean
build, no panic, no "Try this" suggestion).

The remaining `@[simp]` lemmas in the file are `private`
(`foldl_finRange_eq_sum`, `vectorEquiv_mulVec`, etc.) and outside the
sweep's public-API scope.

## Current frontier

`lake build HexMatrixMathlib` green (RankSpanNullspace 3.4s — no grind-loop
slowdown). `scripts/check_dag.py` exit 0. Annotation-only; no proof bodies,
signatures, or imports changed.

## Next step

None for this issue. Disjoint from in-flight #8099 (`HexMatrixMathlib/Basic.lean`,
`Determinant/Core.lean`).

## Blockers

None.
