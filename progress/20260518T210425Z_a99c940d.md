# a99c940d — planner cycle: post-#5088 audit issue (#5094)

## Accomplished

- Oriented on the queue (`POD_MIN_QUEUE=3`, `POD_QUEUE_DEPTH=2`,
  `POD_QUEUE_DEFICIT=1`). Unclaimed before this cycle: #4334
  (HexLLL Phase 4, host-load-gated) and #5091 (BZMathlib
  `fpPoly_mul_dvd_mul` extraction). Claimed: #5090 (post-#5084
  audit of the Monic-route umbrella inversion).
- Verified directives #2637 / #2567 / #2564 are all claimed; no
  unclaimed directive work this cycle.
- Skipped Step 2 merge: the only mergeable PR is #5092 (CI
  in progress on `build` / `conformance` / `build-macos`), and
  #2656 is an owner-authored draft spec PR (not a worker output).
- Skipped Step 3 fix-issue creation: both broken PRs (#5086 and
  #4917) are already `repair-claimed`.
- Read the open `agent-plan` issue bodies (deliverables level)
  for #5091 (FpPoly-layer `mul_dvd_mul` extraction targeting
  `Basic.lean:5657-5677`), #5088 (FpPoly leadingCoeff cluster,
  in flight via PR #5092), #5081 (FpPoly isZero=false Shape-B
  cluster, blocked through PR #5086 in CONFLICTS), and the two
  in-flight audits #5085 / #5090. No deliverable overlap with
  the new audit issue.
- Filed
  [**#5094**](https://github.com/kim-em/hex/issues/5094)
  — `BZMathlib review: post-#5088 audit of
  fpPoly_leadingCoeff_ne_zero_of_size_pos cluster (9 inline
  leadingCoeff ≠ 0 sites across Basic.lean and
  IntReductionMod.lean)`. Auto-labelled `agent-plan`,
  `blocked`, `review`; auto-detected `depends-on: #5088` via
  `coordination plan`.

## Issue #5094 design notes

- Mirrors the pre-blocked audit issue pattern established by
  #5085 (post-#5081 Shape-B audit) — file the audit alongside
  the extraction, labelled `blocked`, so it unblocks at PR
  merge without a planner round-trip.
- Three-invariant template (call-site coverage / framing-
  docstring-hoist / cross-file consistency), same as the
  closed `zpoly_dvd_trans` and `fpPoly_dvd_trans` audit chains.
- Two deliberate departures from the framing-convention
  baseline are pre-classified in the issue body so the auditor
  does not flag them as DEVIATION:
  - **`theorem`, not `private theorem`** — cross-file cluster
    spanning `Basic.lean` and `IntReductionMod.lean`; the
    latter imports the former and needs visible access.
  - **Cross-file occurrence counts** —
    `Basic.lean` 1+7=8, `IntReductionMod.lean` 2,
    `Recovery.lean` 0, vs. the single-file Shape-A/B
    precedents (1+8 in `Basic.lean` only for both).
- Out-of-scope observations explicitly enumerated:
  - `Basic.lean:5337` (`leadingCoeff = 1` shape) and
    `Basic.lean:5722` (`coeff 0 = leadingCoeff` opposite-
    direction shape) — potential sibling cluster, not part
    of this audit.
  - Upstream `private theorem
    leadingCoeff_ne_zero_of_isZero_false` at
    `HexPolyFp/SquareFree.lean:1243` — public-promotion
    candidate, separate planner decision.

## Current frontier

The BZMathlib FpPoly-layer cleanup cascade now has audit
coverage queued for every in-flight extraction:

- **Shape A** — `fpPoly_dvd_trans` (transitivity): closed,
  #5082 PASS via #5087.
- **Shape B** — `fpPoly_isZero_false_of_dvd_of_isZero_false`
  (dvd preserves isZero=false): #5081 in flight, #5085 audit
  filed and blocked.
- **leadingCoeff** — `fpPoly_leadingCoeff_ne_zero_of_size_pos`:
  #5088 in flight (PR #5092 in CI), #5094 audit filed
  this cycle and blocked.
- **mul_dvd_mul** — `fpPoly_mul_dvd_mul`: #5091 unclaimed,
  awaiting worker pickup.
- **Monic-route umbrella inversion**: #5089 closed, #5090
  audit claimed.

## Next step

When PR #5092 (closing #5088) merges, #5094 will auto-unblock
and become claimable. Similarly #5085 auto-unblocks on PR
#5086 (repair-claimed) merge.

A follow-up planner cycle's natural choices, in priority order:
1. If PR #5092 has merged but #5088 sibling work surfaces in
   #5094's audit (e.g. audit-missed sites), file the
   follow-up rewire issue from the DEVIATION report.
2. The `Basic.lean:5337` / `5722` sites' sibling-cluster
   candidacy — needs a separate scoping pass to enumerate
   the cluster shape before filing.

## Blockers

None for this session. Project-level directives #2564 / #2567
/ #2637 are unchanged and remain claimed.
