# 2026-05-11 00:28 UTC — work session (da81800e)

## Accomplished

- Reviewed #3153 (HexPolyFp scaled square-free weighted product
  reconstruction) and determined the lingering
  `squareFreeAuxRevContribution_derivative_active_pow_obligation` sorry
  requires two independent deep mathematical facts that are not
  currently in the file:
  1. **Algebraic identity**: `(yunFactorsContributionWithLevel c g
     multiplicity 1 fuel).1 * pow (...).2 multiplicity = pow f
     multiplicity` for derivative-active `f`.
  2. **Residual derivative-zero**: `(DensePoly.derivative (...).2).isZero
     = true` under the same hypotheses.
- Decomposed #3153 into two sub-issues capturing each fact and skipped
  the parent with the standard `Decomposed into #3196, #3197`
  breadcrumb:
  - #3196 HexPolyFp: prove scaled Yun loop algebraic product identity
  - #3197 HexPolyFp: prove scaled Yun loop residual derivative-zero
    invariant
- Claimed #3196 and landed the algebraic core for the scaled Yun
  product identity:
  - `yunFactorsContributionWithLevel_pow_step_algebra`: the
    single-iteration algebraic identity threading `pow c (base * level)
    * pow w base` through one Yun step into the next level's residual
    form, packaged with the existing gcd/division reconstruction
    lemmas and `pow_mul_base`/`pow_add_exp`. The proof is a tight
    calc chain over `DensePoly.mul_assoc_poly` and
    `DensePoly.mul_comm_poly`; the codebase lacks a `CommRing`
    instance on `DensePoly`, so `ring` is unavailable and the
    associativity/commutativity steps use `congrArg` to apply
    `mul_assoc_poly` and `mul_comm_poly` under outer multiplications.
  - `yunFactorsLevelCompletes`: a recursive termination predicate
    for the scaled Yun loop, structured to mirror the loop's
    recursion through `(gcd c w, w / gcd c w, base, level + 1)`.
  - `yunFactorsContributionWithLevel_pow_invariant_of_completes`:
    the conditional product invariant. With
    `yunFactorsLevelCompletes c w base level fuel`, the loop's
    contribution-times-residual-power identity recovers
    `pow c (base * level) * pow w base`. The induction is on `fuel`
    with case splits on `isOne c` and `isOne (c / gcd c w)`, using
    the step-algebra lemma for the algebraic recursion.
- Verified:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
    shows only the two pre-existing proof holes (lines 2960 and
    4482, the `squareFreeAuxRevContribution_derivative_active_pow_obligation`
    and the tail-cross coprimality cluster).

## Current frontier

- The algebraic content of Yun's product identity is now in place as
  a conditional invariant. The remaining piece for #3196 is the
  *termination* proof: showing that for derivative-active `f` with
  `f.size < fuel + 1`, the loop's initial state
  `(f / g, g, multiplicity, 1, fuel)` satisfies
  `yunFactorsLevelCompletes`, so the unconditional public theorem
  `yunFactorsContributionWithLevel_pow_invariant` can drop the
  termination hypothesis.
- The termination proof needs the standard size-decreases argument:
  at each Yun step either `c.size` strictly decreases (when
  `c ∤ w`, gcd properly divides `c`) or `w.size` decreases by
  `c.size - 1` (when `c | w`, the loop advances `w → w / c`).
  Combined with the initial `c.size + w.size = f.size + 1 ≤ fuel + 1`
  bound and the `c.size = 1` finalizer, the loop completes within
  `fuel` iterations.

## Next step

- A successor agent can pick up the termination proof using the
  `yunFactorsLevelCompletes` predicate as the bridge contract,
  combining the gcd-size lemmas already available in
  `HexPoly.Euclid` with the divisibility helpers in this file.

## Blockers

- None for this slice.
