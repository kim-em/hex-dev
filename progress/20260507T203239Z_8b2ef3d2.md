# 2026-05-07 20:32 UTC — BHKS CLD lattice basis (8b2ef3d2)

## Accomplished

Claimed issue #2603 and added the executable BHKS all-coefficients CLD lattice
basis surface in `HexBerlekampZassenhaus/Basic.lean`:

- `bhksCutThresholds` for the per-coordinate `ell_j` array.
- `BhksLatticeBasis` carrying precision parameters, lifted factors, CLD rows,
  thresholds, and the square row-basis matrix.
- `bhksLatticeBasis` building the SPEC row-basis shape
  `[ I_r | A_tilde ; 0 | diag(p^(a-ell_j)) ]`.
- Guards for dimensions, indicator columns, CLD-column wiring, diagonal-scale
  wiring, and a fixture check distinguishing the CLD basis from the old
  additive coefficient shape.

Verification passed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The executable BHKS lattice basis can now be built from lifted factors and the
CLD/cut helpers from #2599. The old additive recombination path and public
factorization behavior remain unchanged.

## Next step

Feed `BhksLatticeBasis.basis` into the future fast-path LLL cut/projection
slice, then add RREF equivalence-class recovery and exact-division
verification.

## Blockers

No blockers for this slice. The pre-existing pod-managed `.claude/CLAUDE.md`
worktree modification remains untouched.
