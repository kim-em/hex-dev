# Work session: HexConway _2_1 axiom discharge + parent #2244 decomposition

## Accomplished

- Claimed parent issue #2244 (label: `human-oversight`,
  `feature`).
- After scoping, decomposed #2244 into three sub-issues:
  - #2255: discharge `_2_1_monic` and `_2_1_degree_pos` axioms.
  - #2256: discharge `?_degree_pos` axiom by case analysis.
  - #2257: discharge `?_irreducible` axiom (requires Rabin →
    `FpPoly.Irreducible` bridge — the existing Mathlib bridge
    `rabin_irreducible` in `HexBerlekampMathlib/Basic.lean:243` is
    itself currently `sorry`).
- Posted decomposition breadcrumb on #2244 and skipped it via
  `coordination skip` (parent gets `replan` for the next planner).
- Claimed sub-issue #2255 and executed it:
  - Added private helper `one_ne_zero_two : (1 : ZMod64 2) ≠ 0` in
    `HexConway/Basic.lean`.
  - Redefined `luebeckConwayPolynomial_2_1` via the record literal
    pattern (analogous to `HexConway/Bench.lean`'s degree-6 entries),
    so `Monic` reduces to `rfl` and `degree` reduces to `decide`.
  - Restated `luebeckConwayPolynomial?_hit_2_1` using
    `DensePoly.ext_coeff` + `DensePoly.coeff_ofCoeffs` + per-`n`
    case match (the previous `rfl` no longer types under the
    redefinition).
  - Replaced both `axiom` declarations with `theorem`s.
  - Updated `supportedEntry_2_1`'s `isSupported` field from `rfl`
    to the restated simp lemma.

## Verified

- `lake build HexConway.Basic`
- `lake build HexConway`
- `lake build` (full)
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg "^axiom |^private axiom " HexConway/Basic.lean` shows only the
  remaining out-of-scope axioms `?_irreducible` and `?_degree_pos`,
  tracked by sub-issues #2257 and #2256 respectively.

## Current frontier

`HexConway/Basic.lean` no longer has agent-introduced axioms about
the concrete `C(2, 1)` polynomial. Two universally-quantified axioms
remain in that file (`?_irreducible`, `?_degree_pos`) plus one
pre-existing `sorry` (`luebeckConwayPolynomial_2_1_irreducible`,
out of scope).

## Next step

Sub-issues #2256 and #2257 remain unclaimed. #2256 (`?_degree_pos`
case analysis) is independent and should be tackled by a future
worker. #2257 likely needs the Rabin → `FpPoly.Irreducible` bridge as
a separate prerequisite (currently the only existing bridge is the
`sorry`'d `rabin_irreducible` in `HexBerlekampMathlib`). A future
planner may want to file a fourth sub-issue dedicated to that bridge.

## Blockers

None for the work landed this session.
