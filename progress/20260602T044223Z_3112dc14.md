# Session 20260602T044223Z (agent/3112dc14)

Issue: #6221 — BHKS bad-vector: repair auxiliaryPolynomial shape after CLD cut correction.

## Accomplished

- Claimed #6221 and audited the BHKS all-coefficients lattice block shape against
  the existing bad-vector polynomial API.
- Added `BHKS.auxiliaryPolynomialWithCorrections`, which subtracts explicit
  diagonal-row correction coordinates from the CLD block.
- Kept `BHKS.auxiliaryPolynomial` as the zero-correction compatibility wrapper.
- Updated `ExecutableBadVectorWitness.IsBhksBadVectorSetup` and
  `ProjectedBadVectorSetupBridge` so the proof-facing bad-vector setup carries
  correction coordinates explicitly.
- Added coefficient lemmas for both the corrected constructor and the
  compatibility wrapper in `BadVectorAuxiliary`, plus a true-factor indicator
  sanity lemma showing the corrected polynomial is zero when the diagonal
  corrections exactly account for the CLD block.
- Verified `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`,
  `lake build HexBerlekampZassenhausMathlib.BadVector`,
  `lake build HexBerlekampZassenhausMathlib`, full `lake build`,
  `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token diff
  grep over touched Lean files.

## Current frontier

The bad-vector polynomial API now has an explicit corrected shape for downstream
BHKS Lemma 3.2 work. The executable projected rows still store only projected
factor coordinates; successor work must recover/provide the diagonal correction
array when constructing the concrete bad-vector bridge.

## Next step

Open a PR closing #6221. Downstream #5512 and #5204 should consume
`auxiliaryPolynomialWithCorrections`, `coeff_auxiliaryPolynomialWithCorrections`,
and `ProjectedBadVectorSetupBridge.auxiliaryCorrections`.

## Blockers

None.
