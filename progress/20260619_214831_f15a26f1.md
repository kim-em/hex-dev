# Phase 6 grind coverage for HexBerlekampZassenhausMathlib/Lattice.lean (#8110)

## Accomplished

Promoted the 7 equation-shaped public `@[simp]` lemmas in
`HexBerlekampZassenhausMathlib/Lattice.lean` to `@[simp, grind =]`:

`indicatorVector_apply_mem`, `indicatorVector_apply_not_mem`,
`classIndicatorArray_size`, `projFirst_apply`, `intVectorToRat_zero`,
`intVectorToRat_add`, `intVectorToRat_intSmul`.

Re-read each at the point of promotion: all are literal `lhs = rhs`
(the two indicator lemmas carry a membership hypothesis, fine for
`grind =`). LHS heads are constant/projection/round-trip
(`indicatorVector`, `.size`, `projFirst`, `intVectorToRat`) — no
`dite`/`Array.getD`/`let`-wrapped conclusions, so none hit the
known `grind =` pattern-rejection traps. No exclusions.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Lattice` green (16s, no
grind-loop slowdown). Full CI-gated `lake build
HexBerlekampZassenhausMathlib` green, 0 errors. `scripts/check_dag.py`
exit 0. Annotation-only; no proof bodies, signatures, or imports
changed, no new `sorry`/`axiom`/`native_decide`.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps: #8104
(HexMatrixMathlib/RankSpanNullspace.lean), #8108
(HexPolyMathlib/Euclid.lean).

## Blockers

None.
