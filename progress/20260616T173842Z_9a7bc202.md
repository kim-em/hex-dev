# Work: canonical TrueFactorCLDVectorData producer (#7616)

## Accomplished

Closed the #7593 deliverable-3 residual obligations left open by PR #7613, in
`HexBerlekampZassenhausMathlib/Lattice.lean` (all additive, no `sorry`/`axiom`):

- `rowCombination_getElem_eq_sum` — entry of a row combination as an explicit
  `Finset.sum` over the row index (with `finRange_foldl_add_eq_sum` /
  `foldl_add_eq_acc_add_listSum` fold-to-sum bridges).
- `BhksBlockForm` + `bhksLatticeBasis_blockForm` — the `[ I | A_tilde ; 0 | diag ]`
  block-form predicate, definitional for `Hex.bhksLatticeBasis`.
- `trueFactorCLDVector_project_of_blockForm` — projected first block equals
  `indicatorVector S` (deliverable-3 projection identity).
- `trueFactorCLDVector_coeff_of_blockForm` — trailing CLD coordinates equal the
  support sum of executable CLD columns (deliverable-3 coefficient identity).
- `trueFactorCLDVectorData` — the canonical producer at `bhksLatticeBasis`,
  discharging both coordinate identities structurally and taking the cut-radius
  norm bound as an explicit certificate.
- `trueFactorCLDNormBound_of_cldTail_sq_sum_le` — connects the
  `TrueFactorCLDNormBound` field to `Hex.bhksCutRadiusSq4` (deliverable 4),
  reducing it to a single CLD-tail squared-sum obligation. The `3·factorCount`
  slack in `4·factorCount + coeffWidth·factorCount²` absorbs the `0/1` first
  block.

## Current frontier

Deliverable 3 (coordinate identities) is fully proved. Deliverable 4 (norm
bound) is connected to the cut radius and reduced to the explicit residual
`htail`: the CLD-tail squared-sum `≤ coeffWidth·factorCount²`.

## Next step

Discharge the residual CLD-tail magnitude bound (filed as a prerequisite). This
needs a high-bit magnitude estimate on the executable `cldCoeffs`/`psiCut`
output: the available surfaces (`auxiliaryPolynomial_coeff_sq_le`,
`abs_phi_coeff_le_of_monic_factor`) bound CLD entries only by the
hypothesis-gated `Hex.bhksCoeffBound`, which is generally `≫ 1` and which
`bhksCutRadiusSq4` does not reference.

## Blockers

The CLD tail bound `≤ coeffWidth·factorCount²` requires each column-sum
`|∑_{i∈S} cldRows[i][j]| ≤ factorCount`, i.e. raw high-bit entries of size
`O(1)`. With `|cldRows[i][j]| ≤ bhksCoeffBound` the sum is only `≤ r·bhksCoeffBound`,
so the cut radius and the available coefficient bound do not yet meet. This is
the genuine analytic gap, now isolated behind `trueFactorCLDNormBound_of_cldTail_sq_sum_le`
rather than papered over with a `sorry`.
