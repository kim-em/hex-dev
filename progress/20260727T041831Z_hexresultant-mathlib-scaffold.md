# HexResultantMathlib scaffolding

## Accomplished

- Activated `HexResultantMathlib`, registered its default Lake target, and
  moved its immutable SPEC into the active per-library layout.
- Added the specified six-module source tree and umbrella.
- Implemented the finite-sum bivariate `specialize` definition and
  `evalBivariate` without installing a global Mathlib ring instance on
  `DensePoly`.
- Elaborated the complete public correspondence, common-root,
  specialization, root-product, and discriminant theorem signatures against
  the pinned Mathlib revision.
- Advanced `HexResultantMathlib.done_through` to 1 after targeted build,
  status, DAG, copyright, line-count, forbidden-form, and whitespace checks.

## Current frontier

The companion's Phase 1 data surface is implemented and its propositional
correspondence obligations are explicit sorries, as permitted for
scaffolding. The parent value PR and its independent review continue in the
background.

## Next step

Publish this stacked scaffold PR, then start the first proof milestone by
proving the specialization definitions' coefficient/evaluation laws and the
Mathlib correspondence for the already-proved dense operations.

## Blockers

None.
