# 20260512T020429Z (review session, #3652)

## Accomplished

`HexConway/Bench.lean`: dropped six benchmark-local duplicate Luebeck
Conway polynomials (`luebeckConwayPolynomial_X_6` for X ∈ {2,3,5,7,11,13})
along with their `_monic` theorems, and rewired the `Nonempty` instances
and `tier1_X_6Ref` initializers to reference the source-table
declarations in `Hex.Conway` (already present in `HexConway/Basic.lean`).
The six private `one_ne_zero_*` helpers that only discharged the
duplicate `normalized` proofs are likewise removed.

Net diff: −113 / +22 lines. No benchmark registration name, `expectedHash`,
schedule, complexity model, smoke/scientific setting, or `run...Checksum`
behavior changes.

Verification:

- `lake build HexConway.Bench` — succeeds (585 jobs).
- `lake exe hexconway_bench list` — 9 registrations, all preserved.
- `lake exe hexconway_bench verify` — all 9 pass.
- `lake build HexConway` — succeeds.
- `python3 scripts/check_dag.py` — clean.
- `python3 scripts/status.py HexConway` — `Phase 6 (proof polishing)`,
  unchanged.
- `rg '^axiom\b|native_decide|TODO|FIXME|sorry' HexConway/Bench.lean`
  — no matches.
- `git diff --check` — clean.

## Current frontier

`HexConway/Bench.lean` is now the single source-of-truth caller of the
committed Tier 1 entries via `Conway.luebeckConwayPolynomial_X_*` /
`_monic`.

## Next step

None for this issue. The HexConway PLAN is on Phase 6 (proof polishing);
unrelated to this benchmark-maintenance review.

## Blockers

None.
