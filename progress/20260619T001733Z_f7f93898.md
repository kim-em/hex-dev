# HexMatrix Phase 6 grind automation coverage for Determinant.lean (#8028)

## Accomplished

Promoted the 29 clean equation-shaped `@[simp]` lemmas enumerated in the
issue's representative list to `@[simp, grind =]` in
`HexMatrix/Determinant.lean`, matching the Phase 6 sweep convention.
Each was re-read and confirmed to be a literal `lhs = rhs` form before
promotion:

- index plumbing: `skipIndex_val_of_lt`, `skipIndex_val_of_not_lt`,
  `skipIndex_last`, `skipIndex2_val_of_lt_p`, `skipIndex2_val_of_between`,
  `skipIndex2_val_of_ge_q`
- minor extraction: `deleteRowCol_entry`, `deleteRowCol_last_last`
- cofactor algebra: `cofactorSign_of_even`, `cofactorSign_of_odd`,
  `cofactor_of_even`, `cofactor_of_odd`, `cofactor_last_last`
- determinant base cases: `det_leadingPrefix_zero`, `det_one_by_one`,
  `det_two_by_two`
- column-matrix entries: `columnSumMatrix_entry`, `columnTupleMatrix_entry`,
  `columnTupleVectorFn_apply`, `columnTupleMatrix_reconstructInjTuple_entry`,
  `firstColumns_entry`, `nMatrix_entry`, `basisVec_getElem`
- adjugate / selection: `adjugate_get`, `adjugate_zero_zero`,
  `adjugate_last_last`, `setRow_get_self`, `reconstructInjTuple_getElem`,
  `sortInjPerm_getElem_val`

The conditional value lemmas (`skipIndex_val_of_*`, `skipIndex2_val_of_*`,
`cofactorSign_of_*`, `cofactor_of_*`) carry an arithmetic side hypothesis,
which `grind =` discharges as a side goal — they are genuine oriented
rewrites, not poor unconditional ones. The ite/dite-RHS forms
(`basisVec_getElem`) are complete equations grind case-splits.

## Exclusions (left `@[simp]`-only, per deliverable 2)

Seven `@[simp]` lemmas were deliberately not promoted — they are the
intermediate helpers of the recursive column-expansion / row-move
machinery the issue flagged for grind-loop caution, and were not in the
planner's enumerated promotion set:

- `columnChoiceMatrix_entry` — `match`-RHS, ordered-expansion helper
- `columnTupleMatrix_compose_perm_entry` — higher-order lambda LHS
  (`fun i => s[sigma[i]]`), an unreliable grind pattern
- `columnSumMatrixWithPrefix_entry`, `columnSumMatrixWithSuffix_entry` —
  `dite`-RHS recursive prefix/suffix-partial helpers
- `assembleColumnsSuffix_left`, `assembleColumnsSuffix_right` —
  index-dependent (Fin-cast subscript) LHS, recursive assembly helpers
- `rowMoveUp_zero` — base case of the recursive `rowMoveUp` def

These remain `@[simp]`-only to keep scope to the planner's curated set and
avoid loop risk in the recursive clusters; they can be promoted later if a
downstream `grind` goal needs them.

## Current frontier

`lake build HexMatrix` green; `HexMatrix.Determinant` builds in 23s with
no grind-loop slowdown (sibling `RREF` 13s; the larger Determinant module
is consistent with its size, no runaway). `python3 scripts/check_dag.py`
exit 0. `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
`TODO`/`FIXME`. Annotation-only: 29 insertions / 29 deletions, no proof
bodies changed, no new imports, no Mathlib dependency, no `*Mathlib` file
touched.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps: #8029
(HexGF2/Euclid.lean), #8030 (HexGFqField/Operations.lean repr cluster).

## Blockers

None.
