# Session cb2e261a - issue #3109

## Accomplished

- Claimed #3109 and added the signed primitive square-free reassembly API in
  `HexPolyZ/Basic.lean`.
- Proved `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`
  by combining the landed `primitive_mul` theorem with primitive witnesses for
  the rational square-free core and repeated part.
- Proved `primitiveSquareFreeDecomposition_reassembly_signed` by turning the
  existing rational reassembly associate into an integer sign using
  `rational_associate_primitive_unit` and `toRatPoly` injectivity.
- Verified with:
  - `lake build HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^\\s*sorry\\b|^axiom\\b|native_decide|TODO|FIXME' HexPolyZ/Basic.lean`

## Current frontier

`HexPolyZ/Basic.lean` now exposes the Mathlib-free signed primitive reassembly
corollaries needed by downstream Berlekamp-Zassenhaus integer recombination
work.

## Next step

Downstream workers can consume `primitiveSquareFreeDecomposition_reassembly_signed`
and the primitive product corollary in the Z-level recombination chain.

## Blockers

- None for this issue.
