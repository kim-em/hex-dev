# Work session 98cd5b86 — HO-15 HexLLL done_through rollback

## Accomplished

- Claimed human-oversight issue #3658 (HO-15).
- Rolled back `libraries.yml: HexLLL.done_through: 4 → 3`. The structured
  `phase4` block (comparators, input_families) is unchanged, preserving
  guidance for the conditional re-promotion in HO-18 (#3662).
- Verified `python3 scripts/check_dag.py` (silent pass) and
  `python3 scripts/status.py HexLLL` (reports Phase 4 with `done_through: 3`
  and the expected `on complete: libraries.yml HexLLL.done_through: 4`).
- `git diff libraries.yml` is the single-line state-field edit promised by
  the issue.

## Current frontier

- HexLLL is back at Phase 4. The honest measurement / persistent-subprocess
  fix lands via HO-16 (#3659, Isabelle wrap), HO-17 (#3660, fpylll wrap),
  and HO-18 (#3662, regenerate report and conditionally re-promote).
- SPEC amendment PR #3657 (comparator-fairness and trend-anchoring) is
  still open with CI queued; this rollback is mechanically independent
  but doctrinally lives under that amendment.

## Next step

- Push branch `agent/98cd5b86` and open the rollback PR closing #3658.
- After this lands, HO-16 / HO-17 can claim and rewrap the comparators;
  HO-18 then regenerates the report and conditionally re-promotes.

## Blockers

- None for this rollback. The follow-ups depend on the persistent-subprocess
  work landing first.
