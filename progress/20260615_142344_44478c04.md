# Progress — 2026-06-15 (session 44478c04, work)

## Accomplished

Worked two issues this session.

### #7477 — diagnosed as blocked, released

Premise-checked `feat: non-circular lifted product divisor implies
support subset` against current `main`. Its route terminates in the
clean-half lemma `toMonicLiftData_subset_of_liftedFactorProduct_map_dvd`
(`P_S ∣ P_T ⟹ S ⊆ T`) plus `congr_centeredLiftPoly`, both landed only in
the open (not merged) sibling PR #7483 — none of #7483's five lemmas are on
`main`. The decomposition that created #7477 predates the clean half landing
on a branch, so its `depends-on` never recorded #7483. Posted a concrete
comment, recorded `depends-on: #7483` (auto-unblocks when #7483 merges), and
skipped to release the claim. Re-deriving the clean-half cluster inline would
duplicate the entire sibling PR — the anti-pattern the doctrine forbids.

### #7476 — BHKS §5 reachable-core l2 bridge (this PR)

Residual reachability surface for the BHKS l2 bridge landed by #7460.

- Deliverable 1 (nonzero constant coefficient) **already existed**:
  `Hex.squareFreeCore_coeff_zero_ne_zero` (HexBerlekampZassenhaus/Basic.lean).
  Referenced, not re-proved.
- Deliverable 2 (positive `bhksDegree`): added
  `bhksDegree_eq_toMonic_degree` (the BHKS degree parameter is definitionally
  `(Hex.ZPoly.toMonic core).degree`) and
  `one_le_bhksDegree_squareFreeCore_of_toMonic_degree`, which sources positive
  degree from the lift pipeline's own `0 < (toMonic core).degree` premise.
  Documented that this is **not** derivable from `HasPositiveDimension`
  (counterexample `f = X^5`: `extractXPower` strips the visible X-power so
  `squareFreeCore = 1`, `bhksDegree = 0`, yet `HasPositiveDimension` holds via
  `f.degree?.getD 0 = 5`) — exactly the sanity-check the issue flagged.
- Deliverable 3 (wrapper): `one_lt_l2norm_squareFreeCore` and the
  lift-premise variant `one_lt_l2norm_squareFreeCore_of_toMonic_degree`,
  applying `one_lt_l2norm_toPolynomial_of_const_degree`. Feeds
  `bhksPaperAuxiliaryFactorReal_pos` for capstone #7441.

All in `HexBerlekampZassenhausMathlib/BHKSBound.lean`, non-circular, no
`sorry`/`axiom`/`native_decide` on added lines.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BHKSBound` (see PR CI).
- Pre-existing `sorry` warnings in `HexHenselMathlib/Correctness.lean` and
  `HexBerlekampMathlib/Basic.lean` are unrelated (Mathlib bridge files, not
  CI-built).

## Current frontier

#7476 deliverables landed. The consumer (#7441) must thread the reachable-core
degree premise `0 < (toMonic core).degree`, not the lattice-dimension surface.

## Next step

Capstone #7441 (auxiliary domination) consumes this lower bound together with
#7440's quantitative bound. #7477 becomes claimable once #7483 merges.

## Blockers

None for #7476. #7477 blocked on PR #7483 merging.
