# 2026-05-18 06:21 UTC — #4942 helper-5 substrate investigation (7b3f7f5c)

## Accomplished

Completed the helper-5 substrate investigation for `#4880`'s Gap 1
`hcore_monic` drop:

* Confirmed the audit's "not feasible at the existing API surface"
  claim for `exhaustiveCoreFactorsWithBound_monic` — the
  `∀ q, Monic q` conclusion is genuinely false for non-monic
  primitive core, since the product chain `polyProduct emitted = core`
  forces emitted leading coefficients to multiply to `leadingCoeff
  core > 1`.
* Identified that the umbrella consumer
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` needs only
  `hpos_lc` (not `Monic`) and `hdegree` — the non-monic substrate
  `expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
  (`Basic.lean:11545`) already supplies the executable contract, and
  the quadratic-arm discharger `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  (`IntReductionMod.lean:2294`) demonstrates the rewiring pattern.
* Recommended **Option A** (weakened-conclusion sibling of
  `exhaustiveCoreFactorsWithBound_degree_pos`). The relaxed proof
  swaps `hq_monic` for `(Primitive q, normalizeFactorSign q = q,
  shouldRecord q = true)`.
* Filed sub-issue `#4946` with the precise prototype spec: a
  six-lemma `_primitive` substrate chain (mirroring the existing
  `_normalizeFactorSign` / `_shouldRecord` chains exactly) plus
  the relaxed `_degree_pos_of_primitive_pos_lc_core` sibling, all
  in `HexBerlekampZassenhaus/Basic.lean`.
* Posted the full investigation findings as a breadcrumb on `#4942`
  and `coordination skip`-ed it with the `Decomposed into #4946`
  pointer.

No code changes in this session.

## Current frontier

`#4946` is unclaimed and immediately actionable; its body contains
proof schemes for every lemma in the substrate chain with line
references to the mirror lemmas. The follow-up umbrella rewiring
(a primitive + pos-lc sibling of
`reassemblyExpansionComplete_exhaustive_of_ne_zero`) is not yet
filed — it depends on `#4946` and is co-scoped with `#4880`'s
umbrella drop, so a planner should file it once helpers 1–4
substrate progress warrants.

## Next step

Worker claims `#4946`, executes the substrate chain, lands the
relaxed `_degree_pos` sibling. Once landed, a planner files the
Mathlib-bridge umbrella rewiring sub-issue.

## Blockers

None for `#4942`'s investigation. `#4880`'s umbrella drop remains
blocked on `#4939`, `#4940`, the pending helper 3/4 substrate
issues, and now also on `#4946` and the umbrella-rewiring follow-up.
