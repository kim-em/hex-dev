# Progress — 2026-05-30T15:42Z — session f3b42f6b (#5773 partial)

## Accomplished

Worked on directive #5773 (HO-5b): "rewrite
hex-berlekamp-zassenhaus-performance.md against hex/isabelle ratio
verdicts". Four prior partial PRs (#5822, #5826, #5832, #5833) had
landed the per-rung Isabelle comparator registrations and ratio
ladders for the split-family and degree/height matrix schedules.
This session extends the comparator surface to the HO-2 adversarial
singletons and the cascade-trigger fallback-probe family — the two
remaining scientific bench-target groups identified by the
14:58Z session.

- Built `bz_isabelle` from cached AFP/Berlekamp_Zassenhaus heap in
  this worktree (`~/.isabelle/Isabelle2025-2/heaps/...` already
  cached; the GHC compile took ~2 minutes).
- Added three per-input `setup_fixed_benchmark runIsabelleAdv*Checksum`
  registrations on the HO-2 adversarial fixtures
  (`advX4Plus1`, `advPhi15`, `advSwinnertonDyerSD3`); the fourth,
  `advQuadSqrt2Sqrt3`, is already covered by the canonical
  `runIsabelleFactorChecksum`. The first two carry
  `expectedHash := some (Hashable.hash (checksumCanonicalLeanFactorization
  (factor input)))` and the bench harness reports
  `expected hash: matches` for both; `advSwinnertonDyerSD3` uses
  `expectedHash := none` because the full integer factor exceeds the
  verifier's per-call budget. Commit `e9b6ea1c` (registration) +
  `9472c377` (report) before rebase; `06597d62` + `b5a72e7b` after
  rebase onto `adced7e3`.
- Added seven per-rung
  `setup_fixed_benchmark runIsabelleFallbackProbeN{N}Checksum`
  registrations for each rung of `fallbackProbeSchedule = #[11, 12,
  13, 15, 18, 22, 24]`. All use `expectedHash := none` to keep
  elaboration off the cascade-affected Lean `factor` call path; the
  observed Isabelle hashes are recorded post-hoc against the known
  `(X-1)...(X-n)` reference factorisation.
- Refreshed the canonical comparator pair
  (`runIsabelleFactorChecksum`, `runIsabelleFactorBaselineChecksum`)
  at the same commit; the new median baseline is `7.004 ms`
  (previously `6.430 ms` from `2f4ef93-split-ladder.json`). Both
  ratios in the new sub-sections use the refreshed baseline.
- Ran 3-trial sweeps; total sweep time ~5 minutes. Load avg at
  start `5.96/6.21/7.83`.

Headline ratios:

- HO-2 `advX4Plus1`: adj `7.35e-8` (Lean `13,605,560×` faster) — met
- HO-2 `advQuadSqrt2Sqrt3` (full factor): adj `0.0761` (Lean `13.14×`
  faster) — met
- HO-2 `advPhi15`: adj `0.520` (Lean `1.92×` faster) — met
- Fallback probe `n = 11`: adj `4.04` (Lean `4.04×` slower) — **not met**
- Fallback probe `n = 12`: adj `5.24` (Lean `5.24×` slower) — **not met**
- Fallback probe `n = 13`: adj `7.07` (Lean `7.07×` slower) — **not met**
- Fallback probe `n ∈ {15, 18, 22, 24}`: Lean exceeded `8.0 s` cap

The fallback-probe gating-goal verdict at the largest eligible rung
`n = 13` is **not met** — the first not-met scientific-bench verdict
on the registered surface. This is the bench-surface manifestation
of the `DensePoly.gcd-not-monic → isGoodPrime-rejects-square-free →
fallbackPrimeChoiceData → wrong reduction` cascade documented in
`reports/bz-vs-isabelle-investigation.md` §3 / §8.4.

`runFastPathPrecisionLocalChecksum` is the remaining registered
scientific bench target without an Isabelle pairing; the report's
new §Concerns bullet justifies the omission as a semantic-mismatch
case (Lean measures fast-path *setup* cost, not full factor), with
the surface kept in §Appendix as an internal-model verdict.

Export artefacts (post-rebase):

- `reports/bench-results/hex-berlekamp-zassenhaus-e9b6ea1c-ho2-singletons.json`,
  SHA-256 `c40f3176420d2ad77c809b64fd4cae55586678d49ddd907b337f22db08128f52`.
- `reports/bench-results/hex-berlekamp-zassenhaus-e9b6ea1c-fallback-probe.json`,
  SHA-256 `0db10787cdf780d9a2ffc012784a76323d1a66b0575db0460d5ed2bcd3524439`.
- `reports/bench-results/hex-berlekamp-zassenhaus-e9b6ea1c-canonical-refresh.json`,
  SHA-256 `7250de7eb915652680e813e2dc1427ad9d429b847af366866f7f35a1facf4db7`.

## Current frontier

Eight of the registered scientific bench-target groups now have
ratio verdicts on the verified-Isabelle BZ comparator:

- split family (public, fast, slow) — met
- degree/height matrix (public, fast, slow) — met
- HO-2 adversarial singletons (per-input fixed rungs) — met
- fallback-probe family (cascade trigger) — **not met**

The only remaining unwired registered scientific target is
`runFastPathPrecisionLocalChecksum`, deliberately excluded as a
semantic-mismatch case (setup probe versus full-factor comparator).

The `bz_isabelle` binary is built in this worktree
(`.cache/oracles/bz-isabelle/wrapper/bz_isabelle`); a follow-up
session on the same host can reuse it without the multi-step setup.

## Next step

Closing this issue requires the orchestrator's judgement on whether
the precision-local surface needs a different comparator (e.g. a
modular-lift-only Isabelle probe matching the Lean setup operation),
or whether the headline shape is complete with the semantic-mismatch
exclusion documented in §Concerns. The not-met fallback-probe verdict
is now the primary load-bearing piece of evidence for HO-1 (#2564)
and HO-5d (#5817 / #5819 / #5831).

## Blockers

None for this PR. Build/verify clean on this branch:
`lake build HexBerlekampZassenhaus.Bench` (164 jobs, only
pre-existing sorry warnings); `lake exe hexbz_bench verify` (18
smoke targets pass).
