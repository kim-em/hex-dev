# 2026-05-15T22:01:49Z — session 67938840 (/work → /feature)

## Accomplished

- Claimed #4153 (HO-43 Round 3: relocate HexGramSchmidt/Int.lean
  bridge-bound theorems).
- Audited the Mathlib-free consumers of the slated bridge theorems
  before any code edit, per the issue's step 5 ("If
  `HexGramSchmidt/Update.lean` or `HexLLL/Basic.lean` still calls the
  Int bridge names slated for relocation, narrow or refile the
  residual consumer work before proceeding").
- Found two distinct blockers and skipped #4153 with a detailed
  refile note. Added `depends-on: #4324`.

## Current frontier

#4153 is on the `replan` queue waiting for a planner cycle. The blockers:

1. **Round 2 residual still open as #4324.** Although #4153's stated
   dependency #4157 is closed, it was closed in favour of #4324 (its
   refile that bundles the consumer-side `LLLState.sizeReduceColumn`
   discharge refactor with the relocation). #4324 is open and
   claimed by session `d0a6d43b`. Until #4324 lands,
   `HexGramSchmidt/Update.lean` still calls `gramDet_rowAdd_earlier`
   (L134) and `scaledCoeffs_eq` via the local `scaledCoeffs_eq_fin`
   adapter (L141, used at L166/168/169/219/220/261/262) — both
   slated for Round 3 relocation.

2. **`HexLLL/Basic.lean` L82-97 dead `Hex.Matrix.*` re-exports.**
   `Hex.Matrix.gramDet_eq_prod_normSq` / `gramDet_pos` /
   `basis_normSq` are thin Mathlib-free wrappers calling the slated
   `GramSchmidt.Int` theorems. `rg` shows zero external Lean
   callers, but they are Mathlib-free consumers that still block
   moving the underlying theorems to `HexGramSchmidtMathlib/Int.lean`
   (the wrapper would need to either import the bridge — forbidden —
   or be deleted/relocated). These are not covered by #4324's
   deliverables.

The matching grep on `HexGramSchmidtMathlib/Int.lean` (already
present) shows the bridge home already has the two non-singular and
case-split scaledCoeffs_eq_scaledCoeffMatrix_bareiss theorems
moved (lines 1-105); the bulk of Round 3's slated audit list still
lives in `HexGramSchmidt/Int.lean`.

## Next step

Next planner cycle should either:

- (a) Extend #4324's scope so it also deletes or relocates the dead
  `Hex.Matrix.gramDet_eq_prod_normSq` / `gramDet_pos` / `basis_normSq`
  re-exports in `HexLLL/Basic.lean`, then close #4324 → unblock #4153.
- (b) File a small prep sub-issue covering only the
  `HexLLL/Basic.lean` re-export cleanup, link both #4324 and that
  prep issue as `depends-on` predecessors of #4153, and proceed.

Either way, the Round 3 worker who picks #4153 back up needs both
the `Update.lean` `sizeReduce_*` consumers and the `Basic.lean`
re-exports cleared before the bridge relocation can land at a
single coherent commit.

## Blockers

- #4324 open (claimed by `d0a6d43b`).
- Re-exports cleanup not yet tracked as an issue (action for next
  planner per skip comment).
