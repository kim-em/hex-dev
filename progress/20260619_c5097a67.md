# HexGFqRing Phase 6 grind automation coverage (#8010)

## Accomplished

Promoted the 11 equation-shaped public `@[simp]` lemmas in
`HexGFqRing/Basic.lean` to `@[simp, grind =]`, matching the ongoing
Phase 6 grind-coverage sweep:

`FpPoly.degree_C`, `reduceMod_zero`, `reduceMod_one`, `repr_ofPoly`,
`reduceMod_idem`, `reduceMod_self`, `reduceMod_add_left_reduceMod`,
`reduceMod_add_right_reduceMod`, `reduceMod_mul_left_reduceMod`,
`reduceMod_mul_right_reduceMod`, `ofPoly_reduceMod`.

All 11 confirmed to be literal `lhs = rhs` forms on re-reading. The
three lemmas the issue flagged for exclusion are left `@[simp]`-only:
`eq_zero_iff_repr_eq_zero` and `ne_zero_iff_repr_ne_zero` (both `↔`),
and `degree_repr_lt_degree` (`<`).

## Current frontier

`lake build HexGFqRing` green (`HexGFqRing.Basic` 758ms — no grind-loop
slowdown; the idempotence/round-trip lemmas all strictly reduce inner
`reduceMod` structure and terminate). Downstream `Operations` and
`Conformance` build clean. `scripts/check_dag.py` exit 0. Annotation-only;
no proof bodies changed, no new imports, no `*Mathlib` file touched.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps: #8012
(HexGF2/Multiply.lean), #8014 (HexModArith/Ring.lean).

## Blockers

None.
