# 8bd659e7 — feature #4852

## Accomplished

Added `Hex.Matrix.det_desnanot_jacobi_int` to `HexMatrix/Determinant.lean`,
the Mathlib-free unscaled Desnanot-Jacobi identity for `Int` matrices.
The theorem specialises the existing `det_desnanot_jacobi_mul`
(`det M * X = det M * Y`) to `R = Int`, then cancels the common
`det M` factor via `Int.eq_of_mul_eq_mul_left` on the hypothesis
`det M ≠ 0`. One-line proof body, no new banned constructs.

## Current frontier

`lake build HexMatrix.Determinant`, `lake build HexMatrix`, and
`python3 scripts/check_dag.py` all clean. `git diff --check` clean.
No `sorry|axiom|native_decide|TODO|FIXME` in touched files.

## Next step

Downstream HO consumer (#4844, scaled-Gram-Schmidt adjacent-swap
bordered-minor identities) can now reach for the unscaled form on
the Mathlib-free side via `det_desnanot_jacobi_int`, supplying
`det M ≠ 0` from the leading-principal-minor invariants maintained
at the call site.

## Blockers

None.
