# Unscaled-primitive prefix-none `_of_bound` (issue #4951)

## Accomplished

* Filed the third (unscaled primitive + positive-leading-coefficient)
  member of the prefix-none `_of_bound` trio for API parity:
  `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core_of_bound`
  inserted at `HexBerlekampZassenhausMathlib/Basic.lean:13018`,
  immediately before the existing wrapper.
* Rewired the existing
  `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core`
  as a thin delegator into the new `_of_bound` sibling, threading
  `defaultFactorCoeffBound core` for `B'`, the leading-coefficient
  bound via `defaultFactorCoeffBound_leadingCoeff_natAbs_le`, and
  the universal `g ∣ core` bound via `defaultFactorCoeffBound_valid`.

## Pattern

Followed the scaled precedent
(`liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core_scaled_of_bound`,
PR #4948) for hypothesis shape: Option A — universal `g ∣ core`
bound at the theorem signature, with the candidate-specific
`hvalid'_T : ∀ g ∈ normalizedFactors (toPolynomial (recombinationCandidate d T)), …`
constructed locally inside the inner `some` branch by chaining
`g ∣ recombinationCandidate d T ∣ target ∣ core` and applying `hvalid`.

The cover-at-min call is rerouted through
`coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
(added by #4929); the rest of the proof body — wrapper extraction,
case-split on `shouldRecordPolynomialFactor` / `exactQuotient?`,
`pairwise_disjoint` + `unique_up_to_associated` contradiction —
carries over verbatim.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — no new errors
  or warnings beyond the `origin/main` baseline; baseline errors at
  lines 15820, 15931, 16119, 16165 and sorry warnings at lines
  15610, 15705 shifted by exactly 115 lines (matching the insertion
  size).
* `python3 scripts/check_dag.py` — clean.
* `git diff --check` — clean.
* `git diff origin/main` — no new `sorry`, `axiom`, `native_decide`,
  `TODO`, or `FIXME`.

## Current frontier

The unscaled-primitive prefix-none `_of_bound` trio is now complete:

* monic — `#4931` (still blocked on `#4927`)
* unscaled primitive + pos-lc — **this PR**
* scaled primitive — `#4932` (PR `#4948` open)

## Next step

Per the issue's "Out of scope" note, the natural follow-up is the
next layer up:
`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core_of_bound`.
File as a separate issue once this lands.

## Blockers

None.
