# 2026-05-07 13:51 UTC — summarize session (712b3644)

## Accomplished

Claimed #2557 and produced the twenty-fifth Phase 4/5 checkpoint covering
work merged since #2502 closed at 2026-05-06T15:29:15Z. Twenty-seven PRs
have merged in that window (planner orientation counted twenty-three; the
delta is the four most recent: #2556 HexLLLMathlib bump, plus the three CI
doctrine PRs #2559, #2560, #2561).

## Twenty-fifth Phase 4/5 checkpoint

### Quality metrics at this checkpoint

- `scripts/check_dag.py`: passes.
- `scripts/status.py`: 12 Mathlib-free libraries at `done_through ≥ 4`
  (ready for Phase 5); 3 at Phase 4 work-in-progress (HexHensel,
  HexConway, HexGfq); 1 (HexBerlekampZassenhaus) advanced from 2 to 3
  this cycle and is now blocked on HexHensel reaching 4. Mathlib
  bridges: most at `done_through: 3` (Phase 4 ready), with HexLLLMathlib
  bumped from 1 to 2 and now ready to start Phase 3 conformance.
- 237 Lean `sorry` occurrences across 33 files (unchanged from planner
  orientation count). The recently-merged work is structural narrowing —
  splitting broad `sorry`s into focused obligations and proving helper
  layers — rather than net `sorry` removal. The count holds steady while
  the queue shape improves.
- 0 Lean `axiom` occurrences.
- 1 textual `native_decide` hit, in a comment in
  `HexArith/CrossCheck.lean` explaining the project-wide ban — not a
  use site.

### What landed since #2502, by library

**HexGramSchmidt (Phase 5).** Decomposition + helper layer for the
row-add / size-reduction proof block in `Update.lean`:

- determinant column-add API in `HexMatrix/Determinant.lean` (#2533,
  ex-#2517) opening the `gramDet_sizeReduce` proof path.
- projection-coefficient algebra lemmas (#2527, ex-#2524).
- projection-combination span bridges via `strictPrefixRows` and
  `projectionCoeffVector` (#2547, ex-#2537).
- earlier-row reduction-zero helper layer (#2551, ex-#2538): zero/self/
  dot-zero/basis-row-at-its-own-prefix cases.
- `gramDetVec` leading-prefix bridge narrowed (#2540, ex-#2529): public
  bridge proved, residual obligation moved to a focused Bareiss-prefix
  invariant.
- `scaledCoeffs` diagonal bridge narrowed twice (#2534, ex-#2530;
  #2545, ex-#2530 follow-on): zero-tail semantics, array-loop bridge.
- `scaledCoeffs` lower bridge narrowed (#2549, ex-#2531): now composes
  three focused facts (executable below-diagonal Bareiss, Bareiss=Leibniz,
  Cramer Leibniz).

**HexMatrix / HexMatrixMathlib (row-pivoted Bareiss).**

- `pivotLoop` branch API exposed (#2528, ex-#2521).
- regular no-swap invariant + sign helpers in
  `HexMatrixMathlib/Determinant/Bareiss.lean` (#2536, ex-#2522).
- pivot-search-failure determinant-zero surface (#2548, ex-#2523).
- intermediate Bareiss state bridge (#2514, ex-#2438): array-bridge
  helpers eliminating implementation detail from downstream proofs.

**HexBerlekamp / HexGF2 Rabin soundness.**

- absolute Rabin degree-divisibility theorem proved in
  `HexBerlekamp/RabinSoundness.lean` (#2518, ex-#2370).
- packed GF2 Rabin quotient-period bridge proved in
  `HexGF2/RabinSoundness.lean` (#2520, ex-#2444).
- intermediate quotient theorems used by both (#2509, #2508 partial,
  #2512, #2513).

**HexPolyFp / HexPolyZ.**

- HexPolyZ Mignotte default-bound dominance (#2506).
- HexPolyFp quotient root-count bound (#2507).
- HexPolyFp quotient eval algebra cores (#2515, ex-#2508).

**Conformance and Phase advancement.**

- HexBerlekampZassenhaus conformance audit + bump 2 → 3 (#2546,
  ex-#2541): added focused Lean conformance guards for additional
  `choosePrimeData`, `henselLiftData`, and `factorWithBound` cases;
  fixture diff is clean against the FLINT oracle.
- HexLLLMathlib Phase 2 review bump 1 → 2 (#2556, ex-#2542): scaffold
  re-read, no new Phase 2 gaps found.

**CI doctrine.**

- push-trigger scoped to `main`, concurrency groups, doctrine doc
  (#2559).
- Conformance workflow collapsed to one ubuntu job, Mathlib cache
  hard-fail gate, bench `verify` moved into the ubuntu job (#2560).
- new oracles must extend the existing single ubuntu job (#2561).

### Open proof frontier

The unclaimed feature queue concentrates on three chains.

**HexGramSchmidt row-add chain — `HexGramSchmidt/Update.lean`
size-reduction theorem block.** Public API target is #2519 (the named
`basis_sizeReduce` … `scaledCoeffs_sizeReduce_above_pivot` block).
Dependency layout, leaves first:

- #2550 (unblocked) — finish the source-row earlier reduction-zero
  theorem; residual of #2538.
- #2543 (unblocked) — `gramDet_rowAdd_earlier` bridge in `Int.lean`,
  prerequisite for the Update-side `gramDet_sizeReduce` discharge.
- #2539 (unblocked, depends on #2538-now-closed) — private list-level
  `basisRows` row-add invariance using #2550's source-row theorem
  plus existing `projectionCoeff_sub_smul` helpers.
- #2544 (blocked on #2543) — call-site wrapper:
  `gramDet_sizeReduce` becomes one line.
- #2525 (blocked on #2539) — public `GramSchmidt.Rat` row-add basis +
  coefficient theorems.
- #2526 (blocked on #2525) — integer wrappers over `Matrix Int`.
- #2519 (blocked on #2526 and #2532-redirected-to-#2543/#2544) — the
  public `Update.lean` block.

**HexMatrixMathlib row-pivoted Bareiss chain — capstone for
`HexMatrix/Bareiss.lean::bareiss_eq_det`.** PRs in flight on the
foundation:

- #2552 (has-pr) — pivot-search and rowSwap transport helper surfaces.
- #2553 (blocked on #2552) — logical-source row-swap transport.
- #2554 (blocked on #2553) — recursive regular-loop invariant.
- #2442 (unblocked, depends on now-merged #2535's successors) —
  resolve the orphan `sorry` in `HexMatrix/Bareiss.lean` once the
  Mathlib-side proof composes through the proved row-pivot theorems
  rather than wrapping the orphan core theorem.

**HexHensel Phase 4 completion.** Two issues address the same bump:

- #2555 (has-pr, critical-path) — verification pass that the existing
  Phase 4 artifacts (`status/hex-hensel.benchmarks-reviewed`, the
  quadratic multifactor registration, CI smoke) actually satisfy
  `PLAN/Phase4.md`, then bump `HexHensel.done_through: 4`.
- #2558 (unblocked, planner-filed alternative) — full audit/run/bump
  pass against the SPEC API, including the linear-vs-quadratic
  `compare` group on shared `(n, k)` inputs. If #2555 lands first,
  #2558 becomes redundant; the planner should reconcile.

### Stale parent triage this cycle

- **#2538 closed in favor of #2550.** #2538's earlier-row reduction
  helper layer was implemented (zero/self/dot-zero/own-prefix cases) and
  shipped as #2551; the residual source-row theorem (`reduceAgainstBasis
  ((basisRows rows).take k).reverse rows[j]! = 0` for `j < k`) moved to
  the narrower #2550.
- **#2535 closed in favor of #2552, #2553, #2554.** A first attempt at
  the full row-pivoted invariant found that unfolding `Matrix.rowSwap`
  inside bordered-minor extensionality goals leaves hard `Vector.set`
  obligations; the pivot-search and rowSwap transport helpers (#2552),
  source-fixed `BareissNoPivotInvariant` row-swap transport (#2553), and
  recursive regular-loop invariant (#2554) are the smaller stable units.
- **#2336 released as stale** in the same triage cycle:
  `degree_dvd_of_irreducible_dvd_xPowSubX` was already proved on
  `origin/main`.

### Limitations / honest framing

- The 25th checkpoint window contains zero net `sorry` removals: the
  count was 237 at planner orientation and is 237 now. The merged work
  is decomposition + helper layers, not capstone proofs. The unmerged
  capstones (Bareiss `bareiss_eq_det`, the Update.lean size-reduction
  block, `gramDetVec_eq_gramDet`, `scaledCoeffs_eq` diagonal/lower
  bridges) are still the bottleneck.
- HexMatrixMathlib Bareiss has been split twice now (#2439 → #2521-#2523,
  then the residual #2535 → #2552-#2554). The pattern is real and
  productive but the deepest invariant (the recursive `pivotLoop`
  theorem in #2554) has not been attempted yet — it is still gated on
  helper layers landing.
- The HexHensel Phase 4 bump has two overlapping open issues (#2555
  and #2558) addressing the same `done_through: 3 → 4` action with
  different mandates (verification vs. full audit). One is a planner
  duplicate; this should be reconciled by closing the redundant issue
  rather than by both worker sessions racing to bump `libraries.yml`.
- `HexLLLMathlib` is freshly at Phase 2 — Phase 3 conformance has not
  started. The bump to `done_through: 2` reflects scaffold review only.
- Three open `human-oversight` issues (#2565 BZ adversarial conformance
  inputs; #2566 missing `HexBerlekampZassenhaus/Bench.lean`; #2567 BHKS
  Theorem 5.2 D1 obligation) sit outside the agent feature queue per
  the summarize "Out of scope" rule and remain owner-decisions.

## Current frontier

Unblocked unclaimed feature issues this session leaves on the queue:
#2550, #2543, #2539, #2442, #2558. PR-in-flight: #2552, #2555.

## Next step

The next planner cycle should reconcile #2555 vs #2558 (close one) and
re-rank the unblocked queue against critical path. The next worker
sessions should pick #2543 or #2550 to start collapsing the
HexGramSchmidt row-add chain bottom-up, and #2552's CI to clear so
#2553/#2554 can unblock.

## Blockers

The worktree still has the pod-injected `.claude/CLAUDE.md` modification
that this session must not touch (off-limits). Otherwise none.
