## Accomplished

- Claimed issue #5452.
- Added signed no-pivot prefix persistence helpers in `HexGramSchmidt/Int.lean`.
- Added a private signed diagonal projection from the final full-Gram
  no-pivot data to the matching leading-prefix `Matrix.bareiss`.
- Added `scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss`, exposing the
  requested public diagonal API:
  the signed scaled-coefficient diagonal is either zero or the Bareiss
  determinant of the `(i + 1)` leading Gram prefix.
- Verified:
  - `lake build HexGramSchmidt.Int`
  - `lake build HexGramSchmidt`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n 'scaledCoeffs_diag|sorry' HexGramSchmidt/Int.lean`
  - `git diff -- HexGramSchmidt/Int.lean | rg -n '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` produced no matches.

## Current frontier

The Mathlib-free core now has the signed leading-prefix diagonal helper needed
by the downstream Mathlib bridge issue. The existing pre-session `sorry`s in
`HexGramSchmidt/Int.lean` remain unchanged.

## Next step

Use the new `GramSchmidt.Int.scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss`
theorem from the dependent `HexGramSchmidtMathlib/Int.lean` bridge work.

## Blockers

None.
