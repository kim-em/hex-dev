# 2026-05-09 (UTC) — issue #2692 (HexLLL Isabelle comparator)

## Accomplished

- Claimed human-oversight issue #2692.
- Added `scripts/oracle/setup_lll_isabelle.sh`, which downloads the Zenodo
  `Experiments_LLL.zip` archive, verifies SHA-256
  `5c975aeb2033540b8f9a05d2ffac87dca0f258e887a5807edefbe60178a547e0`,
  unpacks it under `.cache/oracles/lll-isabelle`, applies any local patches,
  builds `svp_verified`, and prints the binary path.
- Added `scripts/oracle/patches/lll-isabelle/README.md` documenting that no
  Haskell-extraction patches are currently needed.
- Wired the verified Isabelle LLL Haskell extraction into
  `HexLLL/Bench.lean` as fixed process-call comparator registrations paired
  with Lean fixed squared-norm registrations for the BZ recombination,
  random-bounded, and harsh-cubic Phase-4 input rungs.
- Verified the setup script on this machine and ran targeted benchmark checks:
  `lake build HexLLL.Bench`, `lake exe hexlll_bench list`,
  targeted `verify` for the BZ Lean/Isabelle pair, BZ `compare`, and
  random-bounded `n = 30` `compare`.

## Current frontier

- The Isabelle comparator is registered and callable. The bottom BZ and
  random-bounded comparisons agree on squared-norm output hashes locally.
- Full large-rung timing is intentionally left to scheduled or headline
  Phase-4 benchmarking because the larger fixed cases have high wallclock
  caps.

## Next step

- Land the PR for #2692, then continue the HexLLL Phase-4 queue with the
  fpLLL informational comparator (#2693) and the headline report issue.

## Blockers

- None for #2692.
