# Fast raw guarded irreducibility for the quadratic branch (#8096)

## Accomplished

Added the fast-path quadratic raw guarded irreducibility producer needed by
the default-factor capstone successor #8068 (directive #2564):

- `Hex.factorFastFactorsWithBound_eq_some_of_quadratic`
  (`HexBerlekampZassenhaus/Basic.lean`) — a public Basic.lean equation lemma
  identifying the fast raw array as
  `reassemblePolynomialFactors (normalizeForFactor f) coreFactors` on the
  quadratic short-circuit (`1 < B`, nonzero core degree,
  `quadraticIntegerRootFactors? = some coreFactors`). Needed because
  `reassemblePolynomialFactors` is `private`, so the Mathlib layer cannot
  construct the equation by `unfold` itself.
- `factorFastFactorsWithBound_raw_irreducible_of_quadratic`
  (`HexBerlekampZassenhausMathlib/IntReductionMod.lean`) — the corrected
  guarded conclusion `∀ raw ∈ rawFactors.toList,
  shouldRecordPolynomialFactor (normalizeFactorSign raw) = true →
  ZPoly.Irreducible raw`. The guard is dropped (the quadratic branch forces
  `degree ≠ 0`, so every raw factor is unconditionally irreducible). Reuses
  the same substrate as the entry umbrella
  `factor_quadratic_branch_entry_irreducible_of_quadraticRoots`, minus the
  branch-shape/sign-normalisation steps.

## Current frontier

`lake build HexBerlekampZassenhausMathlib` green (3790 jobs, zero errors),
`HexBerlekampZassenhaus.Basic` green, `scripts/check_dag.py` exit 0,
`git diff --check` clean, no new `sorry`/`axiom`/`native_decide`. Executable
factorization behavior, the quadratic detector, and the BHKS core/recovered-lift
path are untouched — additive theorems only.

## Next step

The sibling small-mod singleton raw producer is #8097 (still unclaimed). With
both fast sub-branch producers plus the existing constant and recovered-lift
producers, #8068 has the full guarded `h_raw` disjunct family.

## Blockers

None.
