# Issue 8750 benchmark re-scaffolding premise check

## Accomplished

- Claimed issue #8750 through GitHub assignment because the pod `coordination`
  command and `$POD_SESSION_ID` were unavailable.
- Implemented and built experimental versions of all proposed scaffolding
  changes: Gaussian-integer Taylor centre, higher primitive schedules, uniform
  half-integer product driver family, wider compare schedule, and an
  achieved-precision refinement ladder.
- Verified target-to-achieved refinement pairs `63→69`, `123→133`, `243→261`,
  `483→517`, and `963→1029` by evaluation during `lake build`.
- Ran the affected primitive scientific benchmarks. The integer Taylor centre,
  larger witness/Newton schedules, and achieved-precision refinement ladder all
  remained inconclusive, contradicting the issue's premise.
- Reverted all experimental source changes, posted the measurements and
  technical blocker on issue #8750, and released the GitHub assignment.

## Current frontier

The source branch is unchanged. Issue #8750 needs a revised benchmark design;
no PR was opened because the requested exit condition is not attainable by the
specified re-scaffolding.

## Next step

Redesign the Taylor and witness families so coefficient bit-length is controlled
independently of degree, and re-derive `refineTo`'s achieved-precision wall model
from its observed approximately linear scaling before issuing a new directive.

## Blockers

- Integer-centred Taylor measured approximately `n^2.80`, not `n²`.
- Wilkinson witness/Newton inputs enter multiword coefficient arithmetic before
  their timed bodies leave the startup band.
- Achieved-precision `refineTo` measured approximately `t^1.13`, not `t²`.
