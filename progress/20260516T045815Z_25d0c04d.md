# Session 25d0c04d - HexPoly Dense API review

Session: `25d0c04d-8c27-4ece-b30c-32d026010843`
Issues: `#4334`, `#4397`

## Accomplished

Claimed #4334 first because it was the oldest unclaimed work item. Re-ran the
five affected HexLLL scientific registrations on `carica`; the run reproduced
the environmental blocker with zero verdict-eligible rows, per-spawn floors
around `1.27-1.30s`, and cap kills on the small-rung `sizeReduce`,
`gramSchmidtCoeff`, and `ofBasisHarshCubic` measurements. Removed the local
probe artifact and skipped #4334 with that concrete blocker.

Claimed #4397 and reviewed `HexPoly/Dense.lean` against the HexPoly SPEC and
Phase 6 API bar. Added public DensePoly characterising lemmas for `isZero`,
`degree?`, `support`, and normalized-array reconstruction:

- `DensePoly.isZero_eq_true_iff`
- `DensePoly.isZero_eq_false_iff`
- `DensePoly.degree?_eq_none_iff`
- `DensePoly.degree?_eq_some_of_pos_size`
- `DensePoly.mem_support`
- `DensePoly.support_zero`
- `DensePoly.ofCoeffs_toArray`
- `DensePoly.ofCoeffs_empty`
- `DensePoly.ofCoeffs_replicate_zero`

Kept `DensePoly.coeff_monomial` as an explicit rewrite lemma after a
second-opinion check and downstream spot-build showed that promoting it to
`[simp]` breaks existing proofs that intentionally rewrite it later. Removed
duplicate private reconstruction lemmas from `HexPoly/Euclid.lean`.

Verification run:

- `lake build HexPoly.Dense`
- `lake build HexPoly`
- `python3 scripts/check_dag.py`
- `python3 scripts/status.py HexPoly`
- `git diff --check`
- `rg -n '^\\s*axiom\\b|\\bnative_decide\\b|TODO|FIXME|\\bsorry\\b' HexPoly/Dense.lean`
- `lake build HexPolyFp.Quotient`
- `lake build HexBerlekamp.RabinSoundness`

Quality metric remained unchanged: `82` Lean placeholder matches, no matches
in `HexPoly/Dense.lean`.

## Current frontier

`HexPoly/Dense.lean` now exposes the core reconstruction and support/degree
lemmas that downstream proofs should not have to duplicate locally. HexPoly is
still in Phase 6 (`done_through: 5`); this was a focused Dense API review, not
a full Phase 6 completion audit.

## Next step

Continue Phase 6 review work across the remaining HexPoly surfaces, especially
the larger `HexPoly/Euclid.lean` API and any remaining private helper lemmas
that have become broadly useful enough to promote.

## Blockers

#4334 remains blocked on benchmark hardware conditions. The host still has a
high child-process spawn floor for HexLLL scientific timing, so that issue
should wait for quiet/dedicated hardware or the release-candidate benchmark
workflow.
