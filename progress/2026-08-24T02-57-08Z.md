# BZ Mathlib Phase-3/Phase-4 evidence code (branch wave/bz-mathlib-phase34)

## Accomplished

- Phase 3: `conformance/HexBerlekampZassenhausMathlib/Conformance.lean`
  (tactic-surface core profile, Oracle: none / Mode: always), wired into
  the `HexConformance` globs, with the runtime-contract `## Conformance`
  section added to the library SPEC. Builds green; all `#guard_msgs`,
  factor-list pins, and `#print axioms` checks pass.
- Phase 4 (code half): build-only proof-track probes under
  `bench/HexBerlekampZassenhausMathlib/ProofProbe/` (Baseline, Factor4/8/12,
  Repeated8, Irreducible4/8/16, Kernel4/8) with the matched
  FactorPolyTests `import all` closure in every module; lean_libs
  `HexBerlekampZassenhausMathlibProofProbe` (CI) and
  `...ProofProbeScientific` (release); sweep runner
  `scripts/bench/bz_mathlib_sweep.py` + manifest tests; ci.yml wiring;
  `proof_probes` declaration in libraries.yml (required by
  `check_benches_mathlib_free.py`; `done_through` untouched); SPEC
  `## Phase-4 proof evidence` section.
- Confirmed #2200 and #2201 are resolved in the tree (closed on GitHub;
  `factor_unique`/`List.Perm` replaced by the multiset-based
  `factorize_unique*` family in `FactorSoundness.lean`; the Mignotte
  bridge lives in `FactorBound.lean` via `HexPolyZMathlib.Mignotte`).

## Current frontier

All targets build green; checkers pass (`check_dag`, `check_phase4`,
`conformance_targets --check`, `check_benches_mathlib_free`, the sweep
manifest unittests).

## Next step

The externally serialized measurement window runs
`scripts/bench/bz_mathlib_sweep.py` on the designated shared host (see
the SPEC section for the canonical invocation), then the headline
report and the `done_through` bumps land with the measured artifacts.

## Blockers

None.
