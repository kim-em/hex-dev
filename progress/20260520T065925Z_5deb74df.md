## Accomplished

Claimed #5632 and added the derivative-free finite multiset conversion theorem
`Polynomial.rootsRadiusProduct_le_of_forall_count_radius_le` in
`HexPolyZMathlib/SchurSzego.lean`.

The proof factors through a private real-multiset helper: count domination for
all thresholds above `1` implies product domination for multisets whose entries
are all at least `1`. The public wrapper applies this helper to the exterior
root factors `‖z‖ / r`, preserving multiplicities through `Multiset`.

## Current frontier

#5632 is implemented and ready for PR. The final Schmeisser source wrapper
still needs the independent analytic count-domination theorem from #5631 before
#5633 can assemble both pieces.

## Next step

Create the PR for #5632. After #5631 lands, use this theorem with the
de Bruijn-Springer count source theorem to discharge #5633.

## Blockers

None for #5632.

Verification completed:

- `lake build HexPolyZMathlib`
- `python3 scripts/oracle/mahler_schur_counterexample.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- scoped banned-token grep on the `HexPolyZMathlib/SchurSzego.lean` diff

The whole-worktree banned-token grep also sees unrelated existing additions in
`.claude/CLAUDE.md`; this session did not touch that file.
