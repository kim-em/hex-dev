# 20260518T183708Z — issue #5057 (review, post-#5052 audit of `zpoly_size_pos_of_pos_lc`)

## Accomplished

- Posted single PASS audit comment on issue #5057
  ([link](https://github.com/kim-em/hex/issues/5057#issuecomment-4480759520))
  covering all three invariants for the `zpoly_size_pos_of_pos_lc`
  helper landed in PR #5058 (closed #5052).

- **Invariant 1 (call-site coverage): PASS.**
  - `rw [Array.back?_eq_getElem?]` → 2 hits, both inside helper
    bodies (`Basic.lean:4464` in `zpoly_size_pos_of_monic`,
    `Basic.lean:8105` in `zpoly_size_pos_of_pos_lc`). No inline
    callers remain.
  - `have h…_size_pos : 0 < ….size := by` → 3 hits in `Basic.lean`
    (lines 5252, 5278, 5747), each classified by source hypothesis:
    `0 < degree?.getD 0` (×2) and `≠ 0` (×1). None is pos_lc-source.
  - `zpoly_size_pos_of_pos_lc` → 3 hits in `Basic.lean` (1
    declaration at 8100 + 2 rewire-site calls at 8185 and 8374).
    Former 8214 → 8185, former 8425 → 8374, matching the two sites
    #5058 rewired.

- **Invariant 2 (framing/docstring): PASS.**
  - Docstring at `Basic.lean:8099` is the single contract-first
    sentence required: `/-- A `Hex.ZPoly` with positive leading
    coefficient has positive stored size. -/`. Mirrors sibling
    `zpoly_ne_zero_of_pos_lc` (`Basic.lean:8090`,
    `/-- A `Hex.ZPoly` with positive leading coefficient is
    nonzero. -/`) and matches `_of_monic` companion's framing.
  - No `(#NNNN)` references, session metadata, or "extracted
    from …" provenance hints.
  - Visibility: `private theorem` as required.

- **Invariant 3 (cross-file consistency): PASS.**
  - `git grep -c` → 3 in `Basic.lean`, 0 in `IntReductionMod.lean`,
    0 in `Recovery.lean`. Helper not duplicated or prematurely
    exported.
  - Exactly one `private theorem` declaration anywhere in BZMathlib.
  - Cluster (`zpoly_ne_zero_of_pos_lc` line 8091 +
    `zpoly_size_pos_of_pos_lc` line 8100) is contiguous.

## Verification

- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git status` — only `.claude/commands/` and `.claude/skills/`
  untracked (pre-existing harness state, no new code edits this
  turn).
- Audit comment posted via `gh issue comment 5057 --body-file …`.

## Current frontier

Issue #5057 ready to close. All three invariants pass.

## Next step

`gh issue close 5057 --comment "..."` with brief PASS summary, per
the #5041 close pattern.

## Blockers

None.
