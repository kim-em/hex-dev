# 2026-05-05 10:30 UTC — work session (8430483f)

## Accomplished

Claimed and discharged #2247 (HexPolyZ: Rat quotient pow-divisibility
inductive lemma).

In `HexPolyZ/Basic.lean`, after `rat_dvd_cofactor_derivative`:

* Defined `private def ratPolyPow (d : DensePoly Rat) : Nat → DensePoly Rat`
  as a direct recursion (`0 ↦ 1`, `n + 1 ↦ d * ratPolyPow d n`).
* Added `@[simp]` definitional reductions `ratPolyPow_zero`,
  `ratPolyPow_succ`, plus `ratPolyPow_one : ratPolyPow d 1 = d`.
* Proved auxiliary lemma
  `ratPolyPow_succ_dvd_a_mul_derivative` by induction on `m`:
  if `d ∣ a * d'` then `ratPolyPow d (m + 1) ∣ a * derivative (ratPolyPow d (m + 1))`.
* Proved the main theorem
  `rat_pow_dvd_repeated_of_quotient_common_divisor` by induction on `n`:
  - Base case `n = 0` reduces to the existing
    `rat_common_divisor_quotient_derivative_dvd_repeated`.
  - Step case applies `DensePoly.dvd_gcd` and the product rule to
    split into four divisibility legs; the trickiest leg
    `(d * a) * (derivative(ratPolyPow d (k + 1)) * Q)` uses the
    auxiliary lemma after extracting `d ∣ a * d'` via
    `rat_dvd_cofactor_derivative` from the `d ∣ derivative(quotient)`
    hypothesis.

All new declarations are `private`. No `axiom`, no `native_decide`,
no `sorry`. The only remaining `sorry` warning in `HexPolyZ/Basic.lean`
is the pre-existing one in `primitiveSquareFreeDecomposition_squareFreeCore`
which is out of scope for this issue (and tracked for the blocked
follow-up #2129).

Verification run:
* `lake build HexPolyZ.Basic` — clean build, single pre-existing sorry warning.
* `lake build HexPolyZ` — clean.
* `python3 scripts/check_dag.py` — no output (pass).
* `git diff --check` — no output (pass).
* `python3 scripts/status.py HexPolyZ` — `ready: yes`.

## Current frontier

Next up in the chain:

* **#2248** (size-bound consumer): now unblocked. The new
  `ratPolyPow` API and `rat_pow_dvd_repeated_of_quotient_common_divisor`
  give the iterated divisibility input it needs. The remaining work
  there is the polynomial-power size argument that yields
  `d.size ≤ 1`.
* **#2206** (square-free theorem itself): still blocked on #2248.
* **#2129** (square-free core final assembly): blocked on #2206.

## Next step

Ship the PR for #2247, then a separate session can claim #2248 and
use this lemma to derive `d.size ≤ 1`.

## Blockers

None for this session.
