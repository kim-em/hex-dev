# #3592: HexBZ Bench — review Phase 4 registration API after retune

Session: `6698df64`
Branch: `agent/6698df64`

## Accomplished

Narrow review pass on `HexBerlekampZassenhaus/Bench.lean` after the
#3513 retune landed (commit `d68d65f`). Verified that all 17 registered
benchmark names are stable across `lake exe hexbz_bench list` and
`reports/hex-berlekamp-zassenhaus-performance.md`, so no
registration-site rename needed propagation into the report.

Concrete edits, all narrow:

- **Module docstring**: replaced the stale "CI-smoke benchmark root"
  framing with a Phase 4 framing that names every registered family
  (split / compare / degree-height / precision-local / HO-2 adversarial
  singletons), labels each as scientific or smoke, and notes that
  comparator ratios + headline report are the remaining Phase 4 gate
  per `SPEC/benchmarking.md`. Also added the invariant "each
  `setup_benchmark` has an adjacent cost-model derivation comment."
- **Extracted `smokeSchedule := #[1, 2, 3, 4]`** as a named constant
  alongside `splitScientificSchedule`, replacing four inline
  `paramSchedule := .custom #[1, 2, 3, 4]` duplicates
  (`runFactorSlowChecksum` and the three `runFactor*CompareChecksum`
  registrations). Makes the smoke-vs-scientific separation visible from
  the schedule constants themselves.
- **Refreshed `smokeInput` docstring** — the old "used only for smoke
  verification" framing was wrong after #3513, since the same prep now
  feeds the scientific `n = 2..5` ladder for the public and fast
  targets in addition to the smoke ladder.
- **Added docstrings** to the four undocumented constant prep helpers
  (`prepAdvX4Plus1`, `prepAdvQuadSqrt2Sqrt3`, `prepAdvSwinnertonDyerSD3`,
  `prepAdvPhi15`), matching the documentation convention used by every
  other helper in the file.

Verification:

- `lake build HexBerlekampZassenhaus.Bench` — clean.
- `lake exe hexbz_bench list` — same 17 entries, same complexity tags.
- `lake exe hexbz_bench verify` — all 17 pass.
- `python3 scripts/check_dag.py` — clean.
- `python3 scripts/status.py HexBerlekampZassenhaus` — unchanged
  (`done_through: 3`, blocked by `HexBerlekamp` and `HexLLL`).
- `git diff --check` — clean.
- `grep -nE '^axiom\b|native_decide|TODO|FIXME|sorry' …Bench.lean` —
  clean.

## Current frontier

The bench registration surface now reads as a Phase 4 benchmark module
rather than a smoke-only scaffold, and the smoke vs scientific schedule
split is named at the constant level. No algorithm or schedule-value
changes; cost-model derivation comments unchanged.

## Next step

The narrower follow-ups #3514 (profile coverage for every declared
input family) and #3527 (final Phase 4 report reconciliation) still
own the deeper Phase 4 evidence work. This review intentionally did
not enter their scope.

## Blockers

None for this issue. HexBZ Phase 4 promotion remains gated on
`HexBerlekamp.done_through ≥ 4` and `HexLLL.done_through ≥ 4`, per
`scripts/status.py`.
