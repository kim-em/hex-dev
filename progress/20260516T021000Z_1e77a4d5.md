# Session 1e77a4d5 — HO-4 factorFast capstone

Session: `1e77a4d5-0094-4f6b-8101-7586ed257bf1`
Branch: `agent/1e77a4d5`

## Accomplished

Claimed and executed issue #4360 (HexBZ HO-4: factorFast capstone from
cap-separation canonical indicators).

Added `BHKS.factorFast_ne_none_of_capSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`
in `HexBerlekampZassenhausMathlib/Recovery.lean`, immediately after
`factorFast_ne_none_of_forwardInputs_at_cap`. The new theorem composes

- `ForwardRecoveryInputs.ofCapSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`
  (cap-separation canonical-indicator constructor at the public `factorFast`
  lift shape), and
- `factorFast_ne_none_of_forwardInputs_at_cap` (the
  `factorFast`-success forward inference at the canonical terminal precision)

into a single application. HO-4 leaf consumers that arrive with executable
cap separation, B7 non-degeneracy of the canonical support-driven indicator
partition, and the A2/exact-division candidate facts can now call the
capstone directly rather than inlining the two-step chain at every call
site.

The proof is a single term-mode composition. The binder list threads all
hypotheses of both pieces through; `hp` and `hk` remain explicit because
they pin the lift-data shape to the `precisionForCoeffBound
(factorFastPrecisionCap _) primeData.p` form expected by the producer
wrapper. `hk` carries the `factorFastPrecisionCap` equation between the
outer polynomial and the normalized square-free core — callers discharge it
at the leaf.

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Recovery` and
`lake build HexBerlekampZassenhausMathlib` pass.
`python3 scripts/check_dag.py` passes. `git diff --check` clean.

## Next step

The Mignotte-precision capstone (the symmetric companion to this one,
sitting atop the Mignotte canonical-indicator wrappers from issue #4359
once they land) is the natural follow-up, listed as out of scope here.

## Blockers

None.
