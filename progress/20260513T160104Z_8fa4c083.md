# Session 8fa4c083 — HexMatrix fixed-column Laplace (#3746)

## Accomplished

- Claimed #3746 after confirming no claimable human-oversight issue was available.
- Added `skipIndex_injective` and a private order-preserving
  `moveColumnToLastValues` permutation in `HexMatrix/Determinant.lean`.
- Proved `det_eq_foldl_laplace_col` by moving the requested column to the final
  position, applying the existing final-column Laplace expansion, and transporting
  the permutation/cofactor signs back.
- Ran the requested checks for the touched determinant surface.

## Current frontier

`HexMatrix/Determinant.lean` now exposes arbitrary fixed-column Laplace expansion.
The dependent fixed-row theorem #3744 can use transpose or the new column theorem
once this PR lands.

## Next step

Create the PR for #3746 and let CI validate the determinant build in the full
workflow.

## Blockers

- Existing unrelated `sorry` remains in `HexMatrix/Bareiss.lean`.
- A pre-existing unstaged `.claude/CLAUDE.md` modification is present in the
  worktree and was not touched or staged by this session.
