# b2370f15 - work #5215

## Accomplished

- Claimed issue #5215 as a one-shot feature task.
- Added
  `IntReductionMod.smallModSingletonBranchPreconditions_of_choosePrimeData`,
  packaging the normalized square-free core primitive fact, selected-prime
  leading-coefficient nonvanishing, `choosePrimeData?` witness, and singleton
  factor-count witness.
- Added
  `IntReductionMod.squareFreeCore_irreducible_of_smallModSingletonBranch`,
  specialising the package to the square-free core irreducibility bridge.
- Rewired
  `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData` to
  consume the new branch package instead of rebuilding the primitive and
  leading-coefficient facts inline.
- Verified with `lake build HexBerlekampZassenhausMathlib.IntReductionMod`,
  `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`, `python3 scripts/check_dag.py`,
  and `git diff --check`.
- Attempted `/reflect`; it is not available as a shell command in this
  non-interactive session.

## Current frontier

The small-mod singleton branch now has a reusable bridge substrate in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` for the HO-1 capstone.
The full `git diff | rg ...` forbidden-token check is polluted by a
pre-existing `.claude/CLAUDE.md` worktree modification; the same check scoped
to the touched Lean file is empty.

## Next step

Use `squareFreeCore_irreducible_of_smallModSingletonBranch` from the remaining
`factor_irreducible_of_nonUnit` assembly instead of reconstructing `hprim`,
`hlc_map_ne`, `hchoose`, and `hsmall` separately.

## Blockers

None for #5215. A pre-existing `.claude/CLAUDE.md` modification remains
uncommitted and untouched.
