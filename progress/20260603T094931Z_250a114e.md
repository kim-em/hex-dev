# 20260603T094931Z work #6477

## Accomplished

Claimed one-shot issue #6477 and added a compiling base-case helper in
`HexGramSchmidt/Int.lean`:

* `schurSigma_foldl_eq_noPivotCorrection_zero` proves the `q = 0` case of
  the σ-chain/Bareiss correction invariant. It rewrites the empty fold to the
  two column-zero Schur entries, uses `outer_ih` at `l = 0`, obtains the first
  nonzero pivot from `noPivotLoop_initial_gram_diag_ne_zero_of_lt`, unfolds the
  regular first no-pivot Bareiss step, and closes by direct `stepMatrix`
  simplification plus Gram symmetry.

Also decomposed the remaining proof into:

* #6483 — Bareiss correction successor step for the induction step.
* #6484 — final fold-induction assembly, blocked on #6483.

## Current frontier

The original #6477 theorem is not complete in this partial PR. The base case is
factored out and builds; the remaining hard proof boundary is the successor
recurrence showing that one σ-chain body application advances the closed-form
Bareiss correction term from `q` to `q + 1`.

## Next step

Prove #6483 first. After that lands, #6484 should be a short induction:
dispatch `q = 0` via `schurSigma_foldl_eq_noPivotCorrection_zero`, split the
successor fold with `List.foldl_append`, substitute `outer_ih` reads, orient the
Gram no-pivot symmetric entry, and apply the #6483 recurrence.

## Blockers

No repository blocker. The blocker for finishing #6477 in one pass is proof
size: the successor step needs its own reusable exact-division/no-pivot
recurrence lemma before the fold assembly can stay maintainable.

## Verification

- `lake build HexGramSchmidt` passed.
- No new `sorry`. No `axiom`. No `native_decide`.
- Pre-existing warnings remain in `HexGramSchmidt/Int.lean` at the existing
  `sorry` declarations and unused `h_partial`; this session did not add them.
- `/reflect` was unavailable as a command/tool in this non-interactive session.
