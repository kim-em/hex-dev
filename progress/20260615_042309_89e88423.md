# Progress — 2026-06-15 04:23 UTC (session 89e88423, feature)

## Accomplished

Issue #7439 partial: added the compiled BHKS analytic bridge in
`HexBerlekampZassenhausMathlib/BHKSBound.lean`.

- `two_le_support_card_of_const_degree`: a nonzero constant coefficient plus
  `1 ≤ bhksDegree f` gives
  `2 ≤ (HexPolyZMathlib.toPolynomial f).support.card`.
- `one_lt_l2norm_toPolynomial_of_const_degree`: packages that support-card
  bridge with `one_lt_l2norm_toPolynomial_of_two_le_support` to conclude
  `1 < l2norm (toPolynomial f)`.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.BHKSBound`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep for `sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`

Committed as `3986b353 feat: bridge BHKS l2 support lower bound`.

## Current frontier

The reachability-specific facts requested by #7439 are still open:

- `(normalizeForFactor f).squareFreeCore.coeff 0 ≠ 0`
- `1 ≤ bhksDegree (normalizeForFactor f).squareFreeCore` from the intended
  cap-lift hypotheses

The generic bridge now closes the final support-card/l2norm step once those two
facts are available.

## Next step

Clarify or strengthen the reachable-core surface before proving the remaining
facts. In particular, `HasPositiveDimension` unfolds to
`liftedFactors.size + f.degree?.getD 0 ≥ 1`; by itself it does not force
positive degree if the lifted-factor array is nonempty.

## Blockers

The issue premise says `rows_pos`/`choose_eq` should imply positive core degree,
but the current executable surface does not make that implication immediate:
`bhksLatticeBasis.coeffWidth` is the degree, while `factorCount` comes from
lifted factors. A separate hypothesis such as a slow-path/small-factor exclusion
or an explicit core-degree premise may be needed for the reachability theorem.
