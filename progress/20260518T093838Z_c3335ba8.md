# Session c3335ba8 — planner: file next-layer `_of_bound` siblings for `reassemblyExpansionComplete_exhaustive_*`

## Accomplished

* Filed **#4974** — *BZMathlib: abstract-bound siblings of
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` (monic +
  primitive_pos_lc_core)*. Blocked on the currently-claimed #4971
  (`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`).
  The issue bundles two `_of_bound` siblings (monic at line 3644, primitive
  variant at line 3804 in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`),
  matching the precedent at #4936 for paired primitive / primitive_pos_lc_core
  lifts.
* Plan body at `plans/c3335ba8-1.md` documents the four-parameter
  `_of_bound` block, both consumers' single `hprecision` consumption
  sites (line 3693 in the monic original, line 3855 in the
  primitive variant), thin-wrapper instantiation
  (`B' := defaultFactorCoeffBound squareFreeCore`), and the umbrella
  composition at line 3999 that becomes unblocked once #4974 +
  #4970 + #4971 land.

* Attempted to merge all three mergeable PRs (#4973, #4972, #2656).
  - #4973 / #4972: rejected by base-branch policy because the `build`
    check has not yet reported (`conformance` + `build-macos` are
    green). Expected — will auto-merge on `build` completion.
  - #2656: still a draft. Not mergeable.

## Queue posture

* `POD_QUEUE_DEFICIT=1` at orient time; filed exactly one new
  atomic issue (#4974), restoring the queue.
* No `directive` issues actionable beyond what is already in
  flight; #2637 / #2567 / #2564 remain open and untouched.
* `replan`-labelled triage handled by `/replan`, not duplicated here.

## Cascade snapshot (A2 `_of_bound` consumer chain)

```
#4944 ✓ MERGED    Basic.lean: exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_*_of_bound
                                ↓
#4971 CLAIMED     IntReductionMod.lean: exhaustiveCoreFactorsWithBound_expansion_preconditions_*_of_bound
                                ↓                   ↓
#4974 NEW BLOCKED   reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound
                  + reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound

#4947 PR #4973    Basic.lean: factor_exhaustive_branch_entry_core_zpolyIrreducible_*_of_bound
                                ↓
#4970 BLOCKED     IntReductionMod.lean: factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound

(#4974 + #4970 together unblock the umbrella `_of_bound` lift at line 3972.)
```

## Current frontier

The next planner cycle's natural target — once #4974 + #4970 +
the claimed #4971 land and the PRs at #4973 / #4972 merge — is the
public umbrella `_of_bound` sibling of
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
(line 3972 in `IntReductionMod.lean`). Its body at lines 3997–4001
consumes `hprecision` at exactly two call sites — the inner
`reassemblyExpansionComplete_exhaustive_of_ne_zero` call and the
forwarded argument to the aux — both of which #4974 + #4970 lift
into `_of_bound` form.

## Next step

Exit. Workers will pick up #4974 once #4971's PR lands.

## Blockers

None.
