# 2026-05-16T04:12:33Z — feature session 5055ffd4

## Accomplished

Closed #4376 via PR: added `HexBerlekampZassenhausMathlib.henselLiftData_liftedFactor_monic`,
a thin umbrella wrapper that re-exposes per-output monicness of
`Hex.henselLiftData` by delegating to the executable
`Hex.ZPoly.multifactorLiftQuadratic_each_monic` proof landed via #4386.

The wrapper takes `Monic core`, the quadratic multifactor lift invariant on
the lifted modular factor list, `1 < p`, and `1 ≤ B`, and concludes that
every `(henselLiftData core B primeData).liftedFactors[i]` is monic.

Placed the lemma directly after `monic_primitive_sign_normalized_of_monic`
since the latter is the eventual consumer of `Monic factor` once the
recursive coverage proof needs to discharge the normalisation hypotheses
of `recombinationCandidate_eq_factor_of_recovery`.

## Current frontier

`henselLiftData_liftedFactor_monic` is the executable-surface half of
step 1 of #4363. The integer-factor monic capstone via
`RepresentsIntegerFactorAtLift` (step 2 of #4363) remains, as does the
optional companion theorem that derives `QuadraticMultifactorLiftInvariant`
from `choosePrimeData`-shaped inputs (mentioned as optional in the
issue body).

## Next step

A separate sub-issue (or the parent #4363 narrowed appropriately) should
land the integer-factor monic capstone. The recursive coverage proof
#4301 also has its prefix-none prerequisite (#4367) unblocked now that
#4372 has merged.

## Blockers

None.
