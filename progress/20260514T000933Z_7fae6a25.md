# Progress — session 7fae6a25

## Accomplished

Claimed #2803 first, verified its prerequisite #3087 was stale and had closed
forward to #3871/#3872/#3873, then skipped #2803 for replan with that reason.

Claimed #3802 and completed the reusable Mathlib-free auxiliary-matrix assembly
layer in `HexMatrix/Determinant.lean`:

- added `det_auxAdjugateM`, computing the determinant of the auxiliary adjugate
  matrix as the Desnanot corner-product difference;
- added `det_desnanot_jacobi_mul`, the scaled Mathlib-free Desnanot-Jacobi
  identity obtained from `M * auxAdjugateM M = auxP M`, `det_mul_auxAdjugateM`,
  `det_auxAdjugateM`, and `det_auxP`;
- decomposed the remaining #3802 work into #3878 (generic unscaled
  Mathlib-free Desnanot-Jacobi) and #3879 (Bareiss bordered-minor
  specialization), with #3879 blocked on #3878.

Verification:

- `lake build HexMatrix.Determinant`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexMatrix HexMatrix.lean`
  only reported the pre-existing `HexMatrix/Bareiss.lean:1120` sorry.

## Current frontier

The Mathlib-free auxiliary determinant layer is assembled, but #3802 still
needs the unscaled cancellation/universal-polynomial step before the Bareiss
specialization can move out of the Mathlib bridge.

## Next step

Claim #3878 and prove the generic unscaled Mathlib-free Desnanot-Jacobi theorem
from `det_desnanot_jacobi_mul`.

## Blockers

None for the landed scaled identity. #3879 is intentionally blocked on #3878.
