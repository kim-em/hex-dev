# Progress - 2026-06-16 (session 50296636, work #7631)

## Accomplished

Started by reading the latest progress note and the worker flow. Tried to claim
directive #2564 first, but `coordination claim` refused it because the issue is
blocked by dependencies.

Claimed #7620, read the corrected body, and found it still depends on two
prerequisites that are not landed:
- #7628 / PR #7633 for prefix row-span helpers.
- #7631 for the CLD-tail norm certificate.

Recorded both dependencies on #7620 with `coordination add-dep` and released the
claim with `coordination skip`.

Claimed #7631 and added the algebraic CLD-tail reduction in
`HexBerlekampZassenhausMathlib/Lattice.lean`:
- `cldTailSq_le_of_colBound`: a per-column `natAbs ≤ factorCount` bound on the
  true-factor CLD tail implies the required tail squared-sum bound.
- `normBound_of_colBound`: packages that tail bound through the existing
  `trueFactorCLDNormBound_of_cldTail_sq_sum_le` producer.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Lattice` succeeded.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` succeeded.
- Added-line scan found no new `sorry`, `axiom`, `native_decide`, `TODO`, or
  `FIXME`.

## Current frontier

#7631 is only partially closed. The remaining work is the executable true-factor
CLD analysis that proves the `hcol` hypothesis consumed by
`normBound_of_colBound`.

Also note that #7631's displayed `factorCount` column bound only yields the
existing loose `TrueFactorCLDNormBound` (`≤ bhksCutRadiusSq4`). The downstream
prefix-cut assembly in #7620 needs the tighter normalized bound
`4 * ‖trueFactorCLDVector‖² ≤ bhksCutRadiusSq4`, so the replan should decide
whether #7631 is meant to close the loose #7593 residual only or should be
strengthened for #7620.

## Next step

Prove or refute the executable per-column true-factor CLD bound for supports,
using `Hex.cldCoeffs`, `Hex.psiCut`, and the true lifted-factor congruence
surface. If the target is for #7620 rather than the older #7593 residual,
strengthen the statement to the factor-of-4-tight norm certificate before
assembling `CutProjectionHypotheses`.

## Blockers

#7620 is blocked on #7628/#7633 and the remaining #7631 analytic bridge.
The worktree still contains the pre-existing unrelated `.claude/CLAUDE.md`
modification; it was not touched or staged.
