# 20260510T083034Z - PR #2970 repair

## Accomplished

Claimed repair for PR #2970 and diagnosed the failed Linux `build` check.  The
failure was from `scripts/check_phase4.py`, which rejected the changed
`runFactorFastSetupAdvX4Plus1Checksum` benchmark registration because its
adjacent comment no longer matched the cost-model derivation heuristic.

Updated the two changed fast-path setup benchmark comments in
`HexBerlekampZassenhaus/Bench.lean` so both singleton registrations explicitly
state the declared constant `n + 1` cost model for their pinned `n = 0`
schedules, while keeping the `setup_benchmark` lines on the checker's expected
diff-derived line numbers.

Verification completed:

- `python3 scripts/check_phase4.py`
- `lake build HexBerlekampZassenhaus.Bench`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide' HexBerlekampZassenhaus/Bench.lean`

## Current frontier

PR #2970's local failure mode is repaired.  The branch is ready to push and mark
salvaged after committing this progress note with the comment fix.

## Next step

Push the repaired PR branch with `--force-with-lease` and mark PR #2970
salvaged.

## Blockers

None.  A pre-existing local `.claude/CLAUDE.md` modification was present before
repair checkout and was left untouched.
