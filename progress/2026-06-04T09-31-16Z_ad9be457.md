# 2026-06-04 ad9be457 — expose canonicity of `bareissGramRowInvariant_noPivotLoop_initial`

## Accomplished

Closed sub-A of #6533 (#6535): exposed the canonicity proof from
`bareissGramRowInvariant_noPivotLoop_initial` so callers can discharge
the `StepWitness.Cell` canonicity gate directly. Specifically:

- Refactored `bareissGramRowInvariant_noPivotLoop_initialAux` to return
  `{ hinv' // ∀ i, hinv'.coeff i = bareissGramCanonicalCoeff b (elapsed + fuel) i }`
  instead of the bare row invariant. The recursion already threaded
  `IsCanonicalAt` through every regular step via
  `bareissGramRowInvariant_regular_step_coeff_canonical`; this change
  surfaces it.
- Added two coefficient-propagation lemmas needed for the singular and
  done branches of the aux:
  - `bareissGramCanonicalCoeff_eq_of_done` — propagation past the
    boundary via `noPivotLoop_id_at_done` + repeated
    `bareissGramCanonicalCoeff_succ_done`.
  - `bareissGramCanonicalCoeff_eq_of_singular` — propagation past a
    singular hit via `noPivotLoop_id_at_singular_fixedpoint` +
    repeated `bareissGramCanonicalCoeff_succ_singular`.
- Side-stepped the Nat/Fin matrix-indexing motive issue at the singular
  fixed-point by introducing
  `bareissGramCanonicalCoeff_succ_singular_of_state_eq`: takes the
  state equality as a free-variable equation and discharges via
  `subst`, avoiding `rw` through dependent matrix-access proofs.
- `bareissGramRowInvariant_noPivotLoop_initial` is now `(aux 0 fuel …).1`;
  the new private theorem
  `bareissGramRowInvariant_noPivotLoop_initial_canonical` is
  `(aux 0 fuel …).2` (with `simpa` to normalise `0 + fuel = fuel`).

## Current frontier

- All sub-A deliverables for #6533 are in. The parent #6533 still
  awaits its remaining sub-pieces (the bridge from canonicity to a
  full singular ≡ Bareiss correspondence).

## Next step

A successor can claim the next sub-issue of #6533 and use the new
`bareissGramRowInvariant_noPivotLoop_initial_canonical` to invoke
`StepWitness` along the no-pivot Gram trajectory.

## Blockers

None.

## Verification

- `lake build HexGramSchmidt HexGramSchmidtMathlib HexLLL HexLLLMathlib
  HexGramSchmidt.Conformance HexLLL.Conformance` clean.
- `grep -c sorry HexGramSchmidt/Int.lean` = 3 (unchanged; same two
  declarations + one docstring mention).
- No new `axiom`, `sorry`, or `native_decide`.
