# 2026-05-14 04:42 UTC — work session 0a492e85 on #3958

## Accomplished

- Claimed #3958 and scoped it to the `HexPolyFp/SquareFree.lean`
  pairwise-coprime core.
- Added a proof-facing `SquareFreeAuxRevPairwiseResidualProvider` carrying
  the current fuel/size bound, nonzero/reachability facts, derivative-active
  branch fact, and the local derivative-active state provider.
- Refactored `squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant`
  to consume that richer provider directly in the derivative-active branch.
- Added the bounded recursive fuel transport inside the core proof, using
  the existing derivative-zero and Yun residual-tail fuel helpers.
- Preserved the existing downstream pairwise-coprime wrapper cascade by
  keeping the previous residual-invariant proof as
  `squareFreeAuxRev_pairwise_coprime_nil_core_of_residual_invariant`.

## Current frontier

- The core theorem now exposes the size/state-aware callback surface needed
  by the residual threading work.
- The public and wrapper-level pairwise-coprime theorems still use the old
  residual-invariant callback; threading the richer provider through those
  wrappers remains the next issue's scope.

## Next step

- Claim the dependent pairwise-coprime assembly issue and thread a concrete
  residual provider through the downstream wrapper cascade.

## Blockers

- None for #3958.
