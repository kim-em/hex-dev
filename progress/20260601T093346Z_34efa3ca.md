# 2026-06-01T09:33Z — HexLLL spawn-floor filter unblock (#5859)

## Accomplished

- Claimed #5859 after directive candidates were either already claimed or
  blocked, and after #5765's body identified itself as planner-scope rather
  than a bounded feature-worker task.
- Updated all HexLLL parametric scientific benchmark registrations in
  `HexLLL/Bench.lean` to set `signalFloorMultiplier := 1.0`.
- Documented the rule in `SPEC/benchmarking.md §Spawn-floor filter`: scientific
  registrations may disable only the spawn-floor filter when they use warm
  child-side inner-repeat batches and fixed SPEC/report ladders, while keeping
  caps, models, ladders, and JSON metadata intact.
- Added the HexLLL-specific cross-reference in `SPEC/Libraries/hex-lll.md`.

## Current frontier

#5859's infrastructure blocker is addressed by the SPEC-sanctioned
signal-floor loosening path rather than by changing carica host startup. The
host still reports a high `spawn_floor_nanos` (~1.29s in the local probe), but
HexLLL parametric rows are no longer filtered out solely for being below
`10 × spawn_floor`.

## Next step

Open the PR for #5859. After it lands, #4334 can retry the HexLLL scientific
sweep with the updated registrations; any remaining `killed_at_cap` rows should
be treated as benchmark/runtime findings rather than spawn-floor filtering.

## Blockers

None for this scope.
