# Session a5fe6f96 — HO-4 Mignotte-precision factorFast capstone

Session: `a5fe6f96-3a2f-4368-9db2-4ce7928a3c89`
Branch: `agent/a5fe6f96`

## Accomplished

Claimed and executed issue #4368 (HexBZ HO-4: Mignotte-precision factorFast
capstone parallel to cap-separation).

Added two new declarations to
`HexBerlekampZassenhausMathlib/Recovery.lean`:

1. `ForwardRecoveryInputs.ofMignottePrecisionCanonicalIndicatorsExpectedFactorsAtPrecisionForCoeffBound`
   (inserted after `ofMignottePrecisionCanonicalIndicatorsExpectedFactors`).
   Mirrors `ofCapSeparationCanonicalIndicatorsAtPrecisionForCoeffBound` on
   the Mignotte chain: drops the `mignotte_precision` side condition by
   pinning the lift-data shape to
   `d.k = precisionForCoeffBound (factorFastPrecisionCap f) d.p` via `hp`
   and `hk`, then discharges the bound through
   `mignotte_precision_of_liftData_precisionForCoeffBound_factorFastPrecisionCap`
   from `TerminationBound.lean`.

2. `factorFast_ne_none_of_mignottePrecisionCanonicalIndicatorsExpectedFactorsAtPrecisionForCoeffBound`
   (inserted after the cap-separation capstone, currently the file's last
   theorem). Composes the new wrapper with
   `factorFast_ne_none_of_forwardInputs_at_cap`, exposing the
   `Hex.factorFast f ≠ none` conclusion directly from Mignotte
   canonical-indicator inputs at the public `factorFast` lift shape
   `(Hex.normalizeForFactor f).squareFreeCore` /
   `henselLiftData … (precisionForCoeffBound (factorFastPrecisionCap f)
   primeData.p) primeData`.

The capstone's parameter block follows the cap-separation capstone's
structure verbatim except for the documented swap: the
`(localFactorIndex localFactorDegree H)` / `hcap_le` / `C` / `hC_nonneg` /
`hC` / `hcap` block is replaced by `lattice_eq_indicators` plus the
`selectedFactors` / `htrue` / `hselected` / `hproduct` Mignotte block, so
the two capstones read uniformly at call sites.

Both new declarations carry doc-comments pointing to the producer/consumer
pair, matching the cap-separation analogues' style.

The optional looser-wrapper variant (parallel to
`ofMignottePrecisionCanonicalIndicators` without `ExpectedTrueFactors`)
is intentionally not added: it has no callers in-tree, so adding it now
would be speculative breadth.

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Recovery` and
`lake build HexBerlekampZassenhausMathlib` pass.
`python3 scripts/check_dag.py` passes. `git diff --check` clean.
`rg '\baxiom\b|native_decide|TODO|FIXME|sorry'` reports no new matches.

## Next step

The BHKS Lemma 3.2 bad-vector leaf (converting a failed `factorFast`
recovery into `IsBhksBadVectorSetup`) is the natural next HO-4 leaf step,
mentioned in earlier progress notes but still unplanned. Mignotte-only
callers can now invoke the capstone directly rather than threading the
two-step `ForwardRecoveryInputs` construction by hand.

## Blockers

None.
