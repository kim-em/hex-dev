# b7e76d8d - feature #6049

## Accomplished

- Claimed #6049 after releasing stale blocked claims on #5805 and #6030.
- Added `stepScaledRows_rows_size` in `HexGramSchmidt/Int.lean`, proving the local row-mutating update preserves every in-bounds row length when started from square row storage.
- Threaded `state_array.matrix.size = n` and per-row matrix-size invariants through `scaledCoeffArrayLoop_lower_matches_target_column`, `scaledCoeffArrayLoop_lower_matches`, and `scaledCoeffArrayLoop_lower_zero_of_singular_before_target`.
- Updated the two top-level scaled-coefficient callers to provide the initial `gramRows` size facts.
- Verified with `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token diff grep for `sorry`, `axiom`, `native_decide`, `TODO`, and `FIXME`.

## Current frontier

#6049 is complete as proof infrastructure. The executable `scaledCoeffArrayLoop` still intentionally uses `Matrix.stepArray`.

## Next step

A follow-up can use the newly threaded matrix-size invariants plus `stepScaledRows_rows_size` to replace the generic array rebuild with a semantics-preserving row-mutating scaled-row step.

## Blockers

- None for #6049.
- #5805 remains blocked on #6031, and #6030 remains blocked on #6041/#6044.
- A pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and is not part of this work.
