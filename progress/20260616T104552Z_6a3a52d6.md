# Progress - 2026-06-16T10:45:52Z (session 6a3a52d6, work)

## Accomplished

Claimed #7559 and added the C=2 joint BHKS cap wrappers in
`HexBerlekampZassenhausMathlib/Recovery.lean`.

The new `FactorFastCapSeparationInputs.ofBridgeDataC2Auxiliary` specializes the
existing joint Recovery wrapper to `C = 2` for the zero-correction auxiliary
polynomial.  It consumes the concrete shortness certificate and
`BHKS.auxiliary_le_paperThreshold`, so callers can preserve the joint
`‖core‖₂^aux.natDegree * ‖aux‖₂^input.natDegree` product without reintroducing
split `h_coeff` / `h_aux` paper-threshold obligations.

Also added `factorFast_terminates_ofBridgeDataC2Auxiliary`, composing the same
C=2 cap-separation package with `factorFast_terminates` so downstream capstone
callers can go straight to `Hex.factorFast f ≠ none`.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check HEAD`
- Added-line forbidden-token scan over the committed `Recovery.lean` diff found
  no new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The C=2 zero-correction auxiliary route is now available as both a
Recovery-side cap-separation constructor and a final `Hex.factorFast f ≠ none`
wrapper.  The corrected-auxiliary and factored sibling surfaces remain in place
for callers that still need them.

## Next step

Use `factorFast_terminates_ofBridgeDataC2Auxiliary` at the downstream fast-BHKS
capstone call site that has the zero-correction `H` identification, the
shortness certificate, and the core degree/norm side conditions.

## Blockers

The `/second-opinion` wrapper did not return usable output in this session; no
matching review subprocess was visible after it stopped producing output.

The worktree still contains an unrelated pre-existing modification to
`.claude/CLAUDE.md`; this session did not touch or stage it.
