# Progress: #6819 partial recovered support contract

UTC 2026-06-14, work session for feature issue #6819.

## Accomplished

- Claimed #6819 after the directive #2564 claim failed because that issue is
  dependency-blocked.
- Added a recovered-coordinate support field
  `support_subset_of_dvd_liftedRecoveryCandidate` to
  `LiftedFactorSubsetPartition`.
- Added the corresponding initial evidence field to
  `InitialLiftedFactorSubsetPartitionEvidence`.
- Threaded the new recovered support field through the existing transported,
  `choosePrimeData`, `toMonicPrimeData`, and successful-descent partition
  constructors.
- Added caller-facing recovered support wrappers parallel to the existing
  scaled support wrappers:
  `representingSubset_subset_of_dvd_liftedRecoveryCandidate_of_primitive_pos_lc_core`
  and its `_of_bound` variant.

Verification completed:

- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Basic.lean`

## Current frontier

This is a partial migration. The partition/evidence contracts now expose a
recovered-candidate support obligation, but downstream scaled-candidate
consumers have not been rewritten to consume it. The existing scaled support
field remains in place for compatibility while the late recursive search family
is migrated.

`lake build HexBerlekampZassenhausMathlib.Basic` and
`lake build HexBerlekampZassenhaus` were both attempted, but this worktree
remained stuck in `HexBerlekampZassenhaus/CrossCheck.lean` under concurrent
heavy Lean jobs on the machine. I terminated my build processes rather than
leave background jobs running. No Lean build completed for this patch.

The second-opinion helper was also attempted on the diff, but did not return
within the session budget and was terminated.

## Next step

Continue #6819 by migrating the first recovered-carrier exact-recovery caller
family around `natDegree_toPolynomial_eq_sum_of_represents_of_bound` /
`representsIntegerFactorAtLift_monic_of_bound` so it routes through
`liftedRecoveryCandidate` and `RecoveredAtLift.candidate_eq_of_bound`, then
thread the resulting signatures into the recursive search block.

## Blockers

Full Lean verification was blocked by concurrent long-running Lean builds on
the host. The patch is intentionally small, but it still needs
`lake build HexBerlekampZassenhausMathlib.Basic` before being treated as
complete.
