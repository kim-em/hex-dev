# 20260510T033114Z - Issue #2933

## Accomplished

Claimed issue #2933 and updated the Yun pairwise-coprime proof cluster in
`HexPolyFp/SquareFree.lean` to use the normalized executable gcd witness.

The internal `squareFreeFactorCoprimeRel` and the public
`squareFree_pairwise_coprime` statement now require
`(normalizeMonic (DensePoly.gcd a.factor b.factor)).2 = 1` rather than the
false raw executable `DensePoly.gcd a.factor b.factor = 1` shape.  The
reachable Yun ready/invariant consumers now derive that normalized equality
from the existing common-divisor provider.

I added the reusable `leadingCoeff_scale_of_ne_zero_of_nonzero` helper in
`HexPolyFp/Basic.lean`, plus local `normalizeMonic_nonzero_monic` and
`normalizeMonic_eq_one_of_dvd_one` lemmas in `HexPolyFp/SquareFree.lean`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean HexPolyFp/Basic.lean`

The final grep still reports the pre-existing `sorry`s in
`HexPolyFp/SquareFree.lean`; no new banned markers or proof holes were added.

## Current frontier

The pairwise-coprime consumer layer no longer depends on the false raw-gcd
monicity target from #2919.  Once #2924 supplies the reachable common-divisor
provider, the Yun ready/invariant assembly can use it directly to obtain the
normalized gcd equality facts for emitted tail factors.

## Next step

Use the normalized relation from #2933 when assembling #2920 after the
reachable common-divisor provider from #2924 lands.

## Blockers

None.
