# 2026-05-09 (UTC) — repair PR #2794

## Accomplished

- Claimed PR #2794 (`bench: wire HexLLL fpylll comparator`) for repair.
- Diagnosed the remote `conformance` failure as an external Lean toolchain
  download error (`elan` release URL returned HTTP 502), then reproduced the
  HexLLL oracle path locally with fpylll 0.6.4.
- Found and fixed a local compatibility bug in `scripts/oracle/lll_fpylll.py`:
  fpylll no longer exposes `IntegerMatrix.hnf()`, so the python-flint-free
  fallback now uses exact row-lattice membership over an independent column
  minor. The preferred python-flint HNF path remains unchanged.
- Verified the repaired HexLLL oracle path and fpylll benchmark registrations.

## Current frontier

- PR #2794 is mergeable locally with the fpylll-only oracle fallback repaired.
- The branch still has a pre-existing unstaged `.claude/CLAUDE.md` worktree
  modification from the pod environment; this repair did not stage or modify it.

## Next step

- Push the repaired PR branch and mark PR #2794 salvaged.

## Blockers

- None.
