# Capstone foundation: classical recombination made provable + reconstruction proved

## Accomplished
- **#8401 (merged):** Converted `scaledRecombinationSmart*` (the 3-way mutual
  `Aux`/`SizeLoop`/`CandLoop` recombination loop, the small-`r` classical tier core)
  from `partial def` to a **structurally-recursive `def`** via an explicit `fuel`
  counter (decremented on every recursive call; wrapper supplies
  `budget + (r+1)(2r+3)`, which provably dominates the true depth so behavior is
  byte-identical — 100/0 FLINT, 50/0 trace, candidatesTried unchanged). Then proved
  `scaledRecombinationSmart{Aux,SizeLoop,CandLoop}_product`: a returned `some factors`
  reconstructs the target. A `partial def` had no equation lemmas; this unblocks all
  classical-tier proofs.

## Key facts established this session
- The self-certifying `factorHybrid` (#8398) **already discharges product preservation**,
  so the swap's remaining critical path is the classical tier's **per-factor
  irreducibility**, not product.
- `scaledRecombinationSmart` is a structural mirror of the proven `scaledRecombinationSearchMod`;
  the reconstruction invariant (record only `exactQuotient?`-verified divisors, recurse
  on the quotient) transfers directly.

## Proof-engineering notes (for the next worker)
- Mutual structural recursion on `fuel`: removing `termination_by` lets Lean infer it
  for the *defs*; for the *theorems* it needs an explicit `termination_by fuel` per
  theorem in the mutual block (after each body, before the next `theorem`).
- The Mathlib-free executable layer has **no `set` tactic** (and no `by_contra`/`push_neg`/
  `ring`) — see the `hex-lean-mathlib-boundary` skill. Fold lets by inlining the term in
  `cases hquot : exactQuotient? target <expr>`, not `set`.
- `split at h` is blocked by `let` bindings — `simp only [] at h` (zeta) first.
- After `cases hquot : E with | some _ =>`, use `simp only [hquot] at h` (rewrites AND
  iota-reduces the match) rather than `rw [hquot] at h` (leaves a stuck `match some _`).

## Next step (capstone, remaining)
1. **Classical-tier per-factor irreducibility** (the hard remaining proof): mirror
   `factorWithBound_entries_irreducible` (the #7738 Hensel-subset correspondence in
   `HexBerlekampZassenhausMathlib/{IntReductionMod,PartitionRefinement}.lean`) for the
   smart core, building on the #8401 reconstruction.
2. Swap `factor := factorHybrid`; re-point Mathlib-free `factor_*` (product via
   `factorHybrid_product`; entries via a `factorHybrid f = factorizationOfFactors f
   (factorHybridFactors f)` bridge) and the Mathlib-layer `factor_product` /
   entries-irreducible / `FactorSoundness`.
3. Conformance.lean expectations + baselines; retire the cap-based combinator from the
   public path only (keep `factorWithBound`/`factorFast` defs).

## Blockers
None. The hard, high-risk part (partial-def → provable, reconstruction) is banked; the
remaining irreducibility proof is a known pattern (mirror the existing apparatus), larger
but lower-risk.
