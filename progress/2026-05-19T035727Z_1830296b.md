# 2026-05-19T03:57:27Z — HexPolyMathlib `ofPolynomial_*` Phase 6 wrappers

Session: `1830296b` (feature, issue #5243)

## Accomplished

Added three `@[simp]` wrappers to `HexPolyMathlib/Basic.lean`, immediately
after `ofPolynomial_one`, lifting the previous Phase 6 batch
(`toPolynomial_C`, `toPolynomial_neg`, `toPolynomial_sub`) to the
`ofPolynomial_*` side:

- `ofPolynomial_C` — `Hex.DensePoly.ext_coeff` + `coeff_C` case split on `n = 0`.
- `ofPolynomial_neg` — discharges the `(0 : R) - 0 = 0` boundary via
  `coeff_neg`.
- `ofPolynomial_sub` — same shape via `coeff_sub`.

All proofs follow the suggested shapes from the issue and mirror the
existing `toPolynomial_*` siblings.

## Current frontier

- `lake build HexPolyMathlib.Basic`, `lake build HexPolyMathlib`, and
  `lake build` all succeed.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` clean; no `sorry`, `axiom`, `native_decide`, `TODO`,
  or `FIXME` in the diff.

## Next step

- `ofPolynomial_add` and `ofPolynomial_mul` remain (issue lists them as
  explicit follow-up scope — larger proofs reusing the antidiagonal
  manipulation from `toPolynomial_mul`).

## Blockers

None.
