# 20260602T214759Z work #6392

## Accomplished

- Claimed #6392 and implemented a partial BHKS actual-cap analytic slice in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- Added
  `factorFastCapLift_input_l2norm_sq_le_coeffNormSq_add_one`, giving the
  concrete squared input-polynomial l2-norm bound for the actual
  `factorFastCapLiftData` witness.
- Added `FactorFastCapLiftAnalyticComparison.ofL2normBounds`, which
  instantiates
  `ExecutableBadVectorWitness.l2norm_product_lt_divisor_of_l2norm_bounds` at
  the actual cap-lift witness and packages the result as
  `FactorFastCapLiftAnalyticComparison`.

## Current frontier

The actual-cap comparison still needs the concrete auxiliary-polynomial
l2-norm bound for the relevant bad-vector/correction data and the final strict
cap-arithmetic inequality.  This slice removes the witness-level plumbing
between those bounds and `FactorFastCapLiftAnalyticComparison`.

## Next step

Prove a correction-aware auxiliary l2-norm bound, or specialize the existing
`BHKS.auxiliaryPolynomial_l2norm_sq_le` to the exact actual-cap `H` used by the
bridge data, then prove the strict product inequality against
`(factorFastCapLiftData f primeData).p ^
((factorFastCapLiftData f primeData).k * localFactorDegree)`.

## Blockers

No build blocker.  The full #6392 deliverable remains open because the strict
cap arithmetic and auxiliary-bound instantiation are not yet closed.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
