# 20260602T200034Z work #6359

## Accomplished

- Claimed #6359 after confirming there were no unclaimed directive issues and
  selecting the unblocked BHKS D1 prerequisite over the stale final assembly
  issue #6346, which still depends on the open #6345 PR.
- Added actual-cap `factorFast` bridge-data projection lemmas in
  `HexBerlekampZassenhausMathlib/Recovery.lean` for:
  - corrected auxiliary-polynomial identity;
  - selected local-factor degree positivity;
  - rational coprimality of the input/auxiliary pair over `ℚ`;
  - `p^(k*d)` resultant divisibility.

## Current frontier

The actual-cap cap-separation side now has named field-level accessors against
`badVectorWitnessOfFactorFastCapLiftData`.  These lemmas still consume an
already assembled `BadVectorBridgeData` package; proving a closed constructor
for that package remains future work.

## Next step

Use the new actual-cap projection lemmas when constructing or narrowing the
closed `BadVectorBridgeData` producer for canonical true supports, then compose
that producer into the existing cap-separation wrappers.

## Blockers

No technical blocker for this slice.  The final `factorFast_terminates`
assembly remains blocked on the open #6345 residual PR and the still-missing
closed bridge-data constructor.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
