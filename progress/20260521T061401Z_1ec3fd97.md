# 2026-05-21T06:14Z — work issue #5730

## Accomplished

Claimed #5730 after all open directives were blocked, #5721 was claimed by
another agent, and #4334 was skipped because the host load was unsuitable for
scientific benchmark promotion.

Added a proof-facing monicised-core prime/lift data surface in
`HexBerlekampZassenhaus/Basic.lean`:

- `monicisedCorePrimeData?` and `monicisedCorePrimeData`, selecting
  `PrimeChoiceData` from `(Hex.MonicisedCoreData.ofCore core).monicCore`;
- `monicisedCoreLiftDataWithMonicPrime`, routing through
  `monicisedCoreLiftData` with monic-core-selected prime data;
- provenance lemmas for primality, good-prime status, and
  `factorsModPBerlekampForm` over the actual monic Hensel input;
- a definitional equality exposing the resulting `henselLiftData` shape.

Verified:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib.Basic`
- `git diff --check`
- `git diff -- HexBerlekampZassenhaus/Basic.lean | rg -n -e
  '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returns no matches

## Current frontier

Downstream HO-1 proof code now has an aligned internal data source whose
`factorsModP` are known to factor the monicised core passed to Hensel lifting,
without asserting that the original-core `choosePrimeData` factors apply
unchanged to non-monic cores.

## Next step

Use this monicised-core prime data surface in the exhaustive-branch correctness
wrappers that currently depend on an `hcore_monic` identity rewrite.

## Blockers

None for #5730. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification, which this session left untouched.
