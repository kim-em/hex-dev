# HexPolyMathlib Basic.lean Phase 6 grind automation coverage (#8070)

## Accomplished

Promoted all 25 equation-shaped public `@[simp]` lemmas in
`HexPolyMathlib/Basic.lean` to `@[simp, grind =]`, matching the ongoing
Phase 6 grind-coverage sweep:

`coeff_ofPolynomial`, `coeff_toPolynomial`, `ofPolynomial_zero`,
`ofPolynomial_one`, `ofPolynomial_C`, `ofPolynomial_neg`, `ofPolynomial_sub`,
`ofPolynomial_add`, `ofPolynomial_monomial`, `toPolynomial_zero`,
`toPolynomial_C`, `toPolynomial_one`, `toPolynomial_monomial`,
`toPolynomial_add`, `toPolynomial_neg`, `toPolynomial_sub`, `toPolynomial_mul`,
`toPolynomial_derivative`, `toPolynomial_ofPolynomial`, `ofPolynomial_toPolynomial`,
`equiv_apply`, `equiv_symm_apply`, `ofPolynomial_mul`, `natDegree_toPolynomial`,
`leadingCoeff_toPolynomial`.

All 25 confirmed on re-reading to be literal `lhs = rhs` forms with clean
def/projection LHS heads (`coeff`, `ofPolynomial`, `toPolynomial`, `natDegree`,
`leadingCoeff`, `equiv`/`equiv.symm`) — none `Array.getD`/`dite`-headed, so no
`grind =` pattern-rejection crash. The `degree?.getD 0` term in
`natDegree_toPolynomial` is on the RHS only, which `grind =` accepts.

No non-equation `@[simp]` lemmas exist in the file: the three divisibility
lemmas (`toPolynomial_dvd`, `ofPolynomial_dvd`, `toPolynomial_dvd_iff`) carry
no `@[simp]` and were untouched (the last is an `↔`).

## Current frontier

`lake build HexPolyMathlib.Basic` green (`HexPolyMathlib.Basic` 1.0s — no
grind-loop slowdown; the transport/round-trip lemmas all strictly reduce
structure and terminate). Build log clean of `error`/`warning`/`Try this`.
Downstream `HexPolyZMathlib`, `HexGFqMathlib` build clean, and the full
merge-gating `HexBerlekampZassenhausMathlib` library builds successfully
(zero `error:`). `scripts/check_dag.py` exit 0; `git diff --check` clean.
Annotation-only: 25 insertions / 25 deletions, no proof body, statement, or
import changed, no new declaration.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps: #8072
(HexPolyZMathlib/Basic.lean), #8074 (HexGramSchmidtMathlib/Basic.lean).

## Blockers

None.
