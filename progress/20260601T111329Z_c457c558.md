# c457c558 — work #5915

## Accomplished

- Claimed #5915 after checking the unclaimed queue and directive queue.
- Added `det_plucker_three_term_basisVec_of_gt_p3_of_nDet` in
  `HexMatrix/Determinant.lean`. This private bridge rewrites the ordered
  `q > p3` basis-vector Plucker case through the existing
  `mDet_basisVec_eq_signed_nDet_of_lt` API and discharges the common
  cofactor-sign normalization from the raw six-minor `nDet` relation.
- Verified `lake build HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the new-line forbidden-token grep.
- Filed successor issue #5919 for the remaining raw ordered four-row `nDet`
  Plucker determinant kernel and left a `Decomposed into #5919` breadcrumb on
  #5915.

## Current frontier

#5915 is partially advanced but not complete: the actual determinant kernel

```lean
nDet B p2 p3 h23 * nDet B p1 q h1q -
  nDet B p1 p3 h13 * nDet B p2 q h2q +
  nDet B p1 p2 h12 * nDet B p3 q h3q = 0
```

still needs a Mathlib-free proof for `p1.val < p2.val < p3.val < q.val`.

## Next step

Work #5919: prove the raw ordered four-row `nDet` identity as an actual
determinant identity, then feed it to
`det_plucker_three_term_basisVec_of_gt_p3_of_nDet`.

## Blockers

None for the partial bridge. Full #5915 completion is blocked on #5919.
