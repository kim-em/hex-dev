# Incremental Phase 6 grind audit since #8146 (#8161)

## Accomplished

Audited the eight grind-sweep PRs merged since the #8146 audit: #8150,
#8145, #8142, #8143, #8140, #8137, #8136, #8134 (Phase 7 doc PRs #8147,
#8131 out of scope — no grind annotations).

**Deliverable 1 — ineligible-promotion check: clean.** Extracted every
`@[grind =]` / `@[simp, grind =]` added across the batch. All have an
`Eq` (literal `lhs = rhs`) conclusion; none are `<` / `≤` / `Ne` / `∧`
/ `Associated` / `let`-wrapped. Consistent with the green builds (an
ineligible shape is a hard build error). No locally-disabled lemma
masked behind a green build.

**Deliverable 2 — missed-eligible / sibling scan.** Scanned every swept
file (excluding the out-of-scope #8159 `HexGF2/Basic.lean` and #8160
`HexMatrix/Basic.lean`+`Determinant.lean`) for bare `@[simp]`-only
lemmas with `Eq`/`Iff` conclusions. Also checked multi-attribute
`@[simp, …]` forms — none without `grind`. Findings:

- **`HexGFq/Basic.lean:612` `inv_zero`** (`(0 : GFq p n h)⁻¹ = 0`):
  the one shape-eligible candidate (literal `=`, sibling
  `HexGF2.Field.inv_zero` already carries `grind =`). Tested promotion
  to `@[simp, grind =]`; **`grind =` rejects it** (hard build error):

  ```
  invalid pattern(s) for `inv_zero`
    [@OfNat.ofNat (@GFqField.FiniteField #3 #1 _ _ _ _) `[0] _]
  the following theorem parameters cannot be instantiated:
    n : Nat
    h : Conway.SupportedEntry p n
  ```

  Root cause: grind keys the lemma on the `(0 : GFq p n h)` literal,
  whose `GFqField.FiniteField` carrier holds `n`/`h` as **erased
  instance args**, so neither can be instantiated from the pattern. The
  GF2n sibling succeeds because there the type indices appear
  explicitly in the keyed term. Reverted to `@[simp]`-only and added a
  one-line exclusion comment (per the #8143/#8134 documentation
  pattern). This refines the dropped-parameter rule from #8150: not
  only does a dropped **data** parameter block `grind =`, but a
  parameter that survives only as an **erased instance arg of the
  keyed term's type** is equally uninstantiable.

- **`HexGF2/Field.lean` `frobeniusIter_{zero,succ,zero_eq,one_eq}`**
  (L1189/L1194/L1682/L1721): `Eq`-shaped and eligible, but already
  promoted by the **currently-open PR #8157** (issue #8153). Left
  untouched to avoid a conflict with the in-flight sweep.

- All other bare `@[simp]`-only lemmas in the batch are correctly
  excluded: `<`-headed (`p_pos`, `p_lt_R`, `mk_p_pos`, `mk_p_lt_R` in
  Montgomery/Context; `modulus_nonconstant`, `degree_pos`,
  `degree_lt_word` in HexGFq/Basic), data-param-drop (`p_odd_nat`),
  `let`-wrapped (`linearHenselStep_g/h`), `getD`-head
  (`transformedCoeffs_getD_top`), and the six ring-identity lemmas in
  `HexModArith/Ring.lean` (`mul_zero`/`zero_mul`/`mul_one`/`one_mul`/
  `pow_zero`/`pow_one`) that grind's ring normalizer already derives
  (documented reject in #8137).

The recurring `Iff`-exclusion failure mode that #8146 corrected has
**not recurred**: the only post-#8146 `Iff`-shaped candidate
(`mem_rootsOfCoeffList`) is being promoted by #8157, and no other
swept file holds an unpromoted `↔`-shaped `@[simp]` lemma.

## Verification

- `lake build HexGFq` green (524 jobs) after the comment-only change.
- `python3 scripts/check_dag.py` exit 0.
- Diff is one comment block in `HexGFq/Basic.lean` (5 insertions); no
  annotation actually ships (the tested promotion was reverted). No
  proof body, signature, import, or `*Mathlib` file touched. No
  `axiom`/`sorry`/`native_decide`. `done_through` unchanged.

## Current frontier

Audit cadence is current through the post-#8146 batch. The next audit
should start after #8157 (and any further sweeps) merge.

## Next step

None for this issue. A future audit can fold in PR #8157 once merged.

## Blockers

None.
