# Sealed semantic replay linkage

## Accomplished

- Restacked the package-owned semantic replay layer onto
  `agent/interval-package-replay` at `dd81fd53`.
- Made the semantic registry constructor private and its checked builder
  opaque.
- Paired semantic packages positionally with the sealed executable package
  registry and required exact bidirectional coverage for every fact
  `(RuleKey, role, schema)` declaration.
- Reported instance and equality formats as exact deferred keys rather than
  silently accepting them without theorem schemas.
- Exercised the public sealed path from `Registry.buildWithin`, through
  `Registry.invokePlanned`, to `ReplaySnapshot.freeze`; the semantic dispatcher
  consumes the resulting immutable `Entry.replayKey`.
- Added two opaque packages which reuse fact schema seven, plus conformance for
  retained replay, missing and duplicate schemas, package-count mismatch,
  foreign package ownership, foreign body encoding, and deferred-role
  isolation.
- Rebuilt the semantic, package-registry, payload-arena, payload-session,
  dyadic-interval, and dyadic-rules conformance targets and
  `HexIntervalExperiment`.

## Current frontier

Every executable fact replay format in a checked package registry now has
exactly one theorem schema in the same package position and at the same full
replay key. Generic lookup remains independent of function identities.

## Next step

Add package-owned instance and equality theorem schemas, then compose the
fact-rule and meet evidence in a chronological forward checker for the
centered Mathlib vertical.

## Blockers

None. Instance/equality replay, chronological trace validation, and final
target closure remain explicit deferred obligations.
