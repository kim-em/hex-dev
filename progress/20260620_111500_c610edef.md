# HexMatrixMathlib Phase 5 closeout (#8246)

Session type: feature (via /work). UTC 2026-06-20T11:15.

## Accomplished

Confirmed the Phase 5 exit criteria for `HexMatrixMathlib` and bumped
`libraries.yml[HexMatrixMathlib].done_through` 4 → 5.

Evidence gathered on current `main` (HEAD 91a9d73e):

- **Zero proof-term `sorry`.** `grep -rn '\bsorry\b' HexMatrixMathlib/`
  returns the single hit `Determinant.lean:24`, which sits inside the
  `/-- … -/` docstring (lines 23-28) of `bareiss_eq_det` — prose
  referencing the once-`sorry`-bound Mathlib-free orphan, not a proof
  term. The theorem itself (`:29-31`) is proven by composing
  `bareiss_eq_mathlib_det` with `det_eq`.
- **No `axiom` / `native_decide`.** `grep -rn -E 'axiom|native_decide'
  HexMatrixMathlib/` → 0 hits.
- **`lake build HexMatrixMathlib` green** (2663 jobs, pinned toolchain).
- **`#print axioms`** on `bareissDet_eq_det` and `bareiss_eq_det` both
  report only `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.
- **Conformance:** `HexMatrixMathlib` is a `noncomputable` Mathlib bridge
  with no oracle entry in `scripts/ci/run_oracles.sh`; the executable
  `HexMatrix` carries conformance, this bridge only proves the Mathlib
  correspondence. No suite to run here.
- `python3 scripts/check_dag.py` exit 0 after the edit;
  `python3 scripts/status.py` now advances `HexMatrixMathlib` to
  Phase 6 (proof polishing).

The expected case held: the library was already clean. Only the
`done_through` bump was needed.

## Current frontier

`HexMatrixMathlib` enters Phase 6 (proof polishing / dead-code +
docstring audit), tracked as a separate future issue — not started here.

## Next step

Planner files / a worker claims the HexMatrixMathlib Phase 6 audit.

## Blockers

None.
