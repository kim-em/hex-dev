# Progress — session 52a5da91 (one-shot #7620)

## Accomplished

Claimed #7620 (feat: route BHKS prefix survivor span into
CutProjectionHypotheses, critical-path) via `pod once`. **Diagnosed a
factor-of-4 calibration defect in the directive's route and skipped to
replan** (no code change), with a machine-checked comment on the issue.

The defect: the executable prefix cut `bhksWithinGramSchmidtCut`
(`Basic.lean:4975`) retains row `i` iff `4·(d_{i+1}/d_i) ≤ bhksCutRadiusSq4`,
i.e. `‖b*_i‖² ≤ bhksCutRadiusSq4/4` (def docstring: `bhksCutRadiusSq4 =
4·radiusSq`, `radiusSq = factorCount + coeffWidth·factorCount²/4`). The
survivor-span argument bounds the top index `m` by `‖v‖² ≥ ‖b*_m‖²`, so `m`
survives only if `‖v‖² ≤ radiusSq = bhksCutRadiusSq4/4`. Therefore:

- Deliverable 1's stated hypothesis `‖v‖² ≤ bhksCutRadiusSq4` is **false**
  (counterexample `factorCount=1, coeffWidth=0, reduced=[[2]]`: `‖![2]‖²=4 ≤
  bhksCutRadiusSq4=4` but `projectedRowSpanInt=⊥`). Sound hypothesis:
  `4·‖v‖² ≤ bhksCutRadiusSq4`.
- Deliverable 4 consumes #7616's `TrueFactorCLDNormBound`
  (`‖full‖² ≤ bhksCutRadiusSq4`), which is **4× too loose** to feed the cut
  (needs `≤ bhksCutRadiusSq4/4`). The gap is entirely in the CLD tail: #7616
  bounds each column at `factorCount`; the design radius needs `factorCount/2`.

## Current frontier

Infrastructure for the sound lemma exists and is reachable:
`exists_highest_nonzero_coeff` / `prefixSpan_basis_of_rowCombination` /
`rowCombination_normSq_ge_of_orthogonal_coeff_sq_ge_one` /
`normSq_latticeVec_ge_min_basis_normSq` (`HexGramSchmidt/Int.lean:7092–7219`),
`lllSteered_independent` (`HexLLLMathlib/Independent.lean:2436`),
`basis_normSq` / `gramDet_eq_prod_normSq` (`HexGramSchmidtMathlib/Int.lean`).
The blocker is the premise calibration, not missing infra.

## Next step (for the replan)

Corrected, factor-4-aware decomposition (detailed in the issue comment):
1. Survivor-span lemma at hypothesis `4·‖v‖² ≤ bhksCutRadiusSq4` (needs a
   top-index norm-bound strengthening of `normSq_latticeVec_ge_min_basis_normSq`,
   the `gramDetVec`/`basis_normSq` bridge into `bhksWithinGramSchmidtCut`, and a
   fold characterization `survives i → i < bhksCutPrefixCount`).
2. Projection of the retained full-row span into `projectedRowSpanInt`.
3. A **new** tight certificate `‖trueFactorCLDVector L S‖² ≤ bhksCutRadiusSq4/4`
   (CLD per-column bound `factorCount/2`) — added as a prerequisite, replacing
   the insufficient #7616 `TrueFactorCLDNormBound`.

## Blockers

Premise defect (factor-4) routed to replan. No code landed by design (per
"Directives are hypotheses" — diagnose, don't silently weaken or bash a
partial). Pre-existing unrelated `.claude/*` modifications in the worktree
were not touched or staged.
