# 2026-05-11 12:24 UTC — work/feature session (3107bbf6)

## Accomplished

- Claimed #3405 for HexBZ Phase 4 benchmark widening.
- Added `runFastPathPrecisionLocalChecksum` to `HexBerlekampZassenhaus/Bench.lean`.
- Added deterministic encoded `(degree, height, precision, localFactorCount)` fixtures that vary Hensel precision and local factor count while avoiding full hard `factorFast` calls.
- Registered the new benchmark with an adjacent BHKS plus quadratic multifactor lifting cost-model comment.
- Verified `lake build HexBerlekampZassenhaus.Bench`, `lake exe hexbz_bench list`, `lake exe hexbz_bench verify`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

- HexBZ now has a stable benchmark target exposing the precision and local-factor-count axes needed before the Phase 4 report/profile slice.
- The target uses split deterministic inputs over the supported benchmark prime `31`, so the smoke verifier stays bounded.

## Next step

- Open the PR for #3405.
- Follow-up #3404 can add `libraries.yml` input-family metadata for the expanded benchmark surface.

## Blockers

- No blocker for #3405.
- The pre-existing `.claude/CLAUDE.md` worktree modification remains untouched.
