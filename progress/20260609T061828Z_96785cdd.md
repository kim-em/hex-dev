# 20260609T061828Z (#6619)

## Accomplished

- Claimed directive #6619 and added HexLLL certified-path benchmark wiring in
  `HexLLL/Bench.lean`.
- Extended the persistent fpylll bench driver with a `CERT\t` request mode that
  returns the flat `(B', U, V)` payload consumed by
  `LLLProvider.certifyFlat`.
- Registered fixed certified-path targets across the random-bounded and
  harsh-cubic ladders, paired checker-only targets for checker cost share, and
  bottom-rung public-dispatch smoke targets that fail if a loaded provider
  records zero accepted candidates.
- Updated `reports/hex-lll-performance.md` with the new certified-path targets,
  smoke metrics for certified path/checker share/rejection rate, and the
  remaining `verified Isabelle certified-LLL` executable gap.

## Current frontier

The Hex certified path itself is bench-registered and verified locally. The
external `verified Isabelle certified-LLL` comparator named in
`libraries.yml` is not yet wired: this repo has no setup script, persistent
driver, or binary-path override for AFP
`Modular_arithmetic_LLL_and_HNF_algorithms`, so certified-vs-Isabelle-certified
ratios remain unmeasured.

## Next step

Add the Isabelle certified-output comparator executable/driver, then run the
full certified ladder against it and replace the report's unmeasured ratio
field with exported benchmark rows.

## Blockers

- Missing external executable infrastructure for `verified Isabelle
  certified-LLL`.
- Attempted `/reflect`; it is not available as an executable command in this
  `codex exec` session.

Verification performed:

- `lake build HexLLL.Bench`
- `python3 scripts/oracle/lll_fpylll_bench_driver.py` checksum and `CERT`
  smoke requests
- `lake exe hexlll_bench run Hex.LLLBench.runDispatchedFirstShortVectorRandomBounded30Checksum`
- `lake exe hexlll_bench run Hex.LLLBench.runCertifiedFirstShortVectorRandomBounded30Checksum`
- `lake exe hexlll_bench run Hex.LLLBench.runCertifiedCheckerRandomBounded30Checksum`
- `lake exe hexlll_bench verify`
- `python3 scripts/ci/check_benches_mathlib_free.py`
- `git diff --check`
- `lake build`
- touched-file grep for `sorry`, `axiom`, and `native_decide`

`python3 scripts/check_phase4.py --all` was also tried, but it fails on
pre-existing unrelated cost-comment debt across other bench files.
