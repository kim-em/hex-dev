# PNT+ Table 10 pointwise rows 90 and 95

## Accomplished

- Preserved `ae4e207e143539eef0f7ee5e696d0a23d27a604b` and
  `6880b6e623bb248df85ed74d80c60851be35364f` exactly and added this layer as
  a new local child of the convex-row batch.
- Audited pinned PNT+ `BKLNW_table10_rows_90_95.lean`: all ten declarations
  share `row_bound_pointwise` and one numeric premise combining the upper
  power endpoint with lower-endpoint exponential bounds. Exact-rational
  exponential windows leave positive slack for every coordinate.
- Added a Mathlib-free two-row package authenticating row order, endpoints,
  coefficients, ten listed/corrected cells, and ten rational majorants in one
  bounded payload. One generic action installs exactly ten facts.
- Added ordinary-kernel semantics lifting the rational exponential powers to
  real exponentials and proving the complete numeric premise. Exact tuple
  membership plus the finite-column hypothesis yields the source-shaped
  `rowOfMem`; generic schema/chronology replay and `ProofFrontend` close all
  ten facts.
- Added bare-endpoint, wrong-column, cross-row-order, and wrong-replay-target
  mutations. Failures retain no draft and do not retry; an ordinary theorem
  proves the rational majorant exceeds the bare row-90/k=1 target.
- Updated both SPECs and the source-pinned inventory to record 72/87 accepted
  target calls. The remaining 15 targets, all 38 supporting `a₂` sites,
  surrounding `B_8_exact` reductions, arbitrary rows, and production scale
  remain pending.
- Registered the three modules and added the exact proof-only Lake freshness
  exemption.
- Passed focused and full relevant PNT builds, inventory validation and exact
  source verification, freshness, copyright, line-count, DAG, Phase-4, trust,
  release-manifest, Mathlib-free bench, diff-hygiene, and banned-mechanism
  checks.

## Current frontier

The remaining fifteen Table 10 target calls form three five-column providers:
the row-43 to `19 * log 10` transition, the `19 * log 10` to row-44
transition, and row 13800.7464. The 38 `a₂` helpers and surrounding
`B_8_exact` dispatch remain separate acceptance work.

## Next step

Keep this clean local stack unpushed until the parent selects the Table 10
publication sequence. Audit the two logarithmic transition groups together
before choosing between them and the large-decimal pointwise row.

## Blockers

None.
