# 2026-05-20T15:12Z — feature session b10cedb6

## Accomplished

Claimed #5686, assessed it as too large for one session, and decomposed it into:

- #5692 for Mathlib-free integer row-update linearity lemmas.
- #5693 for one-step `BareissGramRowInvariant` preservation.
- #5694 for recursive `Matrix.noPivotLoop` packaging.

Released #5686 with the required `Decomposed into #5692, #5693, #5694`
breadcrumb, then claimed and completed #5692.

In `HexGramSchmidt/Int.lean`, added private fold/dot/row-combination lemmas
for the integer fraction-free Bareiss numerator update:

- left-oriented dot linearity for `Vector.ofFn fun a => x * u[a] - y * v[a]`;
- right-oriented dot linearity for the same update;
- `Matrix.rowCombination` transport for the updated coefficient vector.

## Current frontier

#5692 is ready for PR. #5693 is the next proof frontier: use these update
lemmas to construct the regular-branch `BareissGramRowInvariant` successor
state.

## Next step

Open a PR for #5692. A successor should claim #5693 after #5692 merges.

## Blockers

No external blocker. `lake build HexGramSchmidt.Int` succeeds, with the two
pre-existing `sorry` warnings in `HexGramSchmidt/Int.lean` still present.
