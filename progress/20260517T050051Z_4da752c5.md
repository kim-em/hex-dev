# 20260517T050051Z — planner session 4da752c5

## Accomplished

Created one new feature issue covering the next gap in the
`factor_irreducible_of_nonUnit` (#4170) arm coverage:

* **#4565** — HO-1 substrate: assemble fast-path constant arm
  umbrella for `factor_irreducible_of_nonUnit`. Plans a sibling
  to the small-mod singleton arm umbrella that landed yesterday
  in #4564 (commit d1e19cf2). Covers the
  `(normalizeForFactor f).squareFreeCore.degree?.getD 0 = 0` case
  of `factorFastFactorsWithBound`'s first dispatch
  (`HexBerlekampZassenhaus/Basic.lean:5081`), proposing a new
  branch-shape lemma `factorWithBound_entry_mem_constant_branch_raw`
  in `HexBerlekampZassenhaus/Basic.lean` and a new umbrella
  `factor_constant_branch_entry_irreducible_of_choosePrimeData`
  in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`. Plan
  body explicitly flags the `±1`-constant filtering sub-problem
  and recommends composing through the existing
  `reassemblePolynomialFactors_constant_irreducible_or_repeated_or_one`
  classifier (`HexBerlekampZassenhaus/Basic.lean:5876`).

## Current frontier

* Queue depth advanced from 0 → 1, matching POD_QUEUE_DEFICIT=1.
* `factor_irreducible_of_nonUnit` (#4170) arm coverage status
  after this planning cycle:
  * Small-mod singleton arm: ✅ landed (#4564).
  * Fast-path constant arm: 📋 planned (#4565, this cycle).
  * Slow-path exhaustive arm: 🚧 planned, blocked on substrate
    #4563 (claimed).
  * Slow-path quadratic arm: ❓ not yet planned — next planner
    candidate; existing
    `quadraticIntegerRootFactors?_factor_irreducible_of_ne_residual`
    in `HexBerlekampZassenhaus/Basic.lean:7529` provides most of
    the irreducibility content, the residual handling is the
    nontrivial piece.
  * BHKS fast arm: ⏸ gated on directive #2567.

## Next step

After #4565 lands, the next planner cycle should plan the
slow-path quadratic arm umbrella for `factor_irreducible_of_nonUnit`
(composing the existing
`quadraticIntegerRootFactors?_factor_irreducible_of_ne_residual`
and `reassemblePolynomialFactors_quadratic_irreducible_or_repeated_or_residual`
infrastructure with a new branch-shape lemma for
`quadraticIntegerRootFactors? = some _`). Once the constant,
quadratic, exhaustive, and singleton arms are all in place, the
HO-1 capstone reduces to dispatching on `factorFastWithBound`'s
case structure plus the BHKS gate from directive #2567.

## Blockers

None for this planner session.
