# 20260602T100200Z e0e88db5 — Issue #6305

## Accomplished

- Checked directive issues #2567 and #2564; both were blocked by coordination.
- Claimed #6282 and released it as stale because its dependency #6281 was decomposed into #6300, #6301, and #6302, with the actual successor work blocked.
- Claimed #6286 and released it as stale because #6285 was partially replanned and the remaining normalized residual payload work is now tracked by blocked #6304.
- Completed #6305 by adding safe extended-GCD automation lemmas in `HexArith/ExtGcd.lean`:
  - marked existing one-way Bezout and combined-spec theorems as `@[simp]`;
  - added projected Bezout simp lemmas for `Nat`, `Int`, pure-int reference, and `UInt64`;
  - added direct Bezout-to-gcd simp lemmas for the public `Nat`, `Int`, and `UInt64` APIs.
- Added a compact `ProofMode` section in `HexArith/Conformance.lean` showing automation closes representative extended-GCD goals for `Nat`, signed `Int`, nonnegative `Int`, and `UInt64`.

## Current frontier

HexArith extended-GCD public specifications are now easier for downstream proofs to consume without unfolding recursive Euclidean implementations or the GMP-backed wrapper.

## Next step

Create the PR for #6305. Separate planners should re-triage #6282 and #6286 based on their stale dependency chains.

## Blockers

None for #6305. #6282 and #6286 were blocked or stale as described above.

## Verification

- `lake build HexArith.ExtGcd` — green
- `lake build HexArith.Conformance` — green
- `lake build HexArith` — green
- `python3 scripts/check_dag.py` — exit 0
- `git diff --check` — clean
- Forbidden-token diff grep over `HexArith/ExtGcd.lean` and `HexArith/Conformance.lean` found no added `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
