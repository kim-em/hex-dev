# hex-primality milestone 0: hex-arith amendments

## Accomplished

- Planned the complete implementation of
  `SPEC/Libraries/hex-primality.md` as an 18-PR sequence (plan reviewed
  and approved interactively; decisions: `Hex.Rand` lands here
  implementing the hex-finite-field SPEC, `pock3` carries a sqrt
  witness field `w` instead of calling the non-kernel-reducible
  `Nat.sqrt`, milestone 1 goes table-first with the sieve as a
  follow-up proof-internals swap).
- Landed milestone 0 in `HexArith`:
  - `powModNat` gains the `p = 0 → 0` guard (previously
    `powModNat a n 0 = a ^ n`, an unreduced power), `powModNatGo` and
    `bitLength` gain `@[expose]`, `powModNat_eq` is exported,
    `powModNat_modulus_zero` added, and the unconditional
    `powMod_eq_powModNat` plus `@[csimp] powModNat_eq_powMod` name
    `powModNat` the kernel-facing specification and `powMod` the
    runtime twin.
  - `instDecidablePrime` re-routed through `isPrimeTrial` (replacing
    the linear `prime_iff_forall_lt` body; the SPEC's claim that no
    instance existed was stale), and `exists_trial_divisor` exported;
    new `exists_prime_dvd` and `exists_prime_le_sqrt`.
  - `HexArith/SPEC/hex-arith.md` documents the specification/twin pair
    and the primality decision surface;
    `SPEC/Libraries/hex-primality.md` corrected where it was stale
    (existing instance, sympy installed in CI, kernel-exposure section
    now describes the landed state).
- Full tree build green (9818 jobs).

## Current frontier

PR for milestone 0 on branch `m0-primality-hexarith`. Next PRs per the
approved plan: scaffolding + committed `primeTable` at `10^4` (PR2),
`hotPathCandidates` migration (PR3), sieve (PR4/PR5), order and
Miller-Rabin (PR6/PR7), `Hex.Rand` (PR8), Pocklington (PR9+).

## Next step

Open the milestone 0 PR, then branch PR2 from it: `lean_lib
HexPrimality`, umbrella, `libraries.yml` entry (active), and
`HexPrimality/Table.lean` with the committed 1229-entry table, binary
search, and both membership directions.

## Blockers

None.
