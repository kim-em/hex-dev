# Non-monic expansion-complete helpers (#4778)

## Accomplished

Added the non-monic substrate chain extending the #4773 → #4774
`exactQuotient?_eq_some_of_pos_lc_pos_degree_mul_eq` lift up to the
public-API array-level `factorPower` surface used by the quadratic-arm
discharger:

* `consumeExactPower_pow_mul_of_not_dvd_of_pos_lc` (private, adjacent to
  `consumeExactPower_pow_mul_of_not_dvd`): single-factor extraction that
  drops `Monic q` in favour of `0 < leadingCoeff q`, routing through
  `exactQuotient?_eq_some_of_pos_lc_pos_degree_mul_eq`.
* `expandRepeatedPartFactorsAux_residual_eq_one_of_pow_decomposition_of_pos_lc`
  (private, adjacent to the monic list-level helper): identical induction
  structure, replacing `consumeExactPower_pow_mul_of_not_dvd` with the
  non-monic head-extraction helper.
* `expandRepeatedPartFactorArray_residual_eq_one_of_pow_decomposition_of_pos_lc`
  (public, intermediate array-level wrapper): delegates the list-level
  proof to the array-toList surface.
* `expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
  (public, the surface consumed by the quadratic-arm discharger): same
  `simpa [Factorization.factorPower]` unification as the monic wrapper.

Each new theorem's docstring names the downstream consumer
(`reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`,
#4747 residual) and the substrate chain (#4773 → #4774 → here).

The issue spec only asked for three theorems; I added an additional
intermediate `_of_pow_decomposition_of_pos_lc` array-level theorem to
mirror the existing monic two-step (polyPow → factorPower) wrapping
pattern, so the public factorPower surface has the same shape as its
monic sibling.

## Verification

* `lake build HexBerlekampZassenhaus.Basic` — passes.
* `lake build HexBerlekampZassenhaus` — passes.
* `python3 scripts/check_dag.py` — passes (no output).
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhaus/Basic.lean` additions
  for `sorry|axiom|native_decide|TODO|FIXME` — 0 new entries (the only
  remaining `sorry` in the file is the pre-existing one at line 6375).

## Current frontier

The non-monic public surface
`expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
is now available to discharge the
`reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
(#4747 residual) precondition that needs to admit a primitive
positive-leading non-monic core factor (e.g. `2X + 3` from the
counter-example `(X-1)(2X+3) = 2X^2 + X - 3`).

## Next step

#4747 residual assembly: combine the new public surface with the
pairwise non-association lemma `quadraticIntegerRootFactors?_pairwise_not_associated`
(separate sub-task, #4785) and the structural decomposition to
discharge the quadratic-arm `reassemblyExpansionComplete` discharger.

## Blockers

None.
