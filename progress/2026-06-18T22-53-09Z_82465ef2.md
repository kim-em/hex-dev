# Phase 6 grind automation coverage for HexHensel/Linear.lean (#8003)

## Accomplished

Promoted the four clean equation-shaped public `@[simp]` lemmas in
`HexHensel/Linear.lean` to `@[simp, grind =]`:

- `coeff_coeffwiseDiv` (line 32)
- `coeff_liftScaledIncrement` (line 91)
- `modP_add` (line 606)
- `modP_mul` (line 636)

Each was verified to be a literal `lhs = rhs` before annotating.

Deliberately left `@[simp]`-only:
- `linearHenselStep_g` (line 131) and `linearHenselStep_h` (line 143):
  their RHS is a `let e := …` block, not a bare `lhs = rhs`, so `grind =`
  does not apply (same rationale as the `let`-bound forms in #7974).

## Verification

- `lake build HexHensel` → `Build completed successfully (112 jobs)`;
  `HexHensel.Linear` built in 1.3s, no grind-loop slowdown.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `done_through` and FFI/executable code untouched.

## Current frontier

Diff is exactly the four `@[simp]` → `@[simp, grind =]` edits.

## Next step

Open PR closing #8003. Sibling Phase 6 files (#8006 Dense.lean,
#8008 Basic.lean) remain unclaimed for other workers.

## Blockers

None.
