# 9ea22f20 - replan triage

## Accomplished

Triaged the two `replan` candidates returned by `coordination
list-replan`:

- **#6086 (HexPolyFp: closed SquareFree decomposition provider
  wrappers)** — *Still valid, body stale.* The body still listed
  `#6107` as the live prerequisite, but #6107 has since been closed
  (raw 4-fact provider shape provably false; superseded by
  normalized provider in #6116) and the actual prerequisite chain
  now runs through #6125 (mechanical consumer refactor) blocked on
  #6124 (raw tail reachability invariant). Updated the body to
  reflect the current `depends-on: #6125` chain and the upstream
  history (#6107 → #6119 → #6124+#6125). Removed `replan`, added
  `blocked` (since #6125 is itself blocked on #6124).

- **#6119 (HexPolyFp: refactor derivative-active provider chain to
  consume normalized state provider)** — *Worker-decomposed, full
  coverage.* The skipping session left a decomposition comment
  splitting the work into #6124 (raw tail reachability invariant)
  and #6125 (mechanical provider-consumer refactor of the five
  named lemmas + closed `yunFactorsDerivativeActiveReachable_stateProvider`).
  Together the sub-issues cover the parent's deliverables exactly
  with no residual scope. Closed #6119 with a forward-link comment
  rather than re-opening for claim or re-creating the sub-issues.

`coordination list-replan` is empty after both dispositions.

## Current frontier

Replan queue cleared. The downstream chain for closed SquareFree
provider wrappers is now:

- #6124 (open, claimable) — raw tail reachability invariant.
- #6125 (blocked on #6124) — mechanical consumer refactor + closed
  named state provider.
- #6086 (now blocked on #6125) — closed SquareFree decomposition
  provider wrappers.
- #6079 (downstream, untouched) — public no-provider SquareFree
  wrappers.

## Next step

Pod's next planner cycle has no replan work to do. Fresh planning
remains the next planner's job per the `/replan` skill scope; this
session deliberately did not create new `agent-plan` issues.

## Blockers

None for this triage session. No code touched, no PR opened.
