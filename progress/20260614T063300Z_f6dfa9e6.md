# Progress: #6079 — HexPolyFp public SquareFree wrappers

## Accomplished

- Ran `/work`, surveyed unclaimed issues. Directive #2564 is `blocked`
  (not actionable). Chose feature #6079 over #6786 (B-builder, on the
  fragile non-CI `HexBerlekampZassenhausMathlib` layer) for tractability
  in a CI-built library, and over the trivial linter cleanups #7046/#7047
  on importance.
- Verified the premise: #6079's blocker #6376 (closed provider package)
  is now closed, and the provider-instantiated underlying theorems
  `squareFree_weightedProduct` and `squareFree_pairwise_coprime` already
  exist and are fully closed in `HexPolyFp/SquareFree.lean`, instantiating
  the Yun raw-state provider internally (`yunDerivativeActiveRawStateProvider_holds`).
- Added the two requested public `squareFreeDecomposition_`-prefixed
  wrappers (`squareFreeDecomposition_weightedProduct`,
  `squareFreeDecomposition_pairwise_coprime`) as pure delegations,
  mirroring the existing `squareFreeDecomposition_factors_squareFree`
  pattern. No provider arguments exposed in the statements.
- Left `squareFreeDecomposition_multiplicity_pos` and
  `squareFreeDecomposition_factors_squareFree` intact (deliverable 4).

## Verification

- `lake build HexPolyFp` — clean (98 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.

## Current frontier

#6079 is complete. The change is confined to `HexPolyFp/SquareFree.lean`.
The worktree carries pre-existing unrelated `.claude/` modifications from a
prior session that this session did not touch and did not stage.

## Next step

Push branch and open PR closing #6079.

## Blockers

None.
