# Progress — #7362 InitialLiftedFactorSubsetPartitionEvidence producer

## Accomplished

Closed the producer gap for
`InitialLiftedFactorSubsetPartitionEvidence core (Hex.ZPoly.toMonicLiftData core
B primeData)` (issue #7362). Added
`initialLiftedFactorSubsetPartitionEvidence_of_toMonicChoosePrimeData` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`, producing all five fields of
the structure from a `toMonicPrimeData?` selection witness plus the standard
non-monic core side conditions (positive leading coefficient, positive degree,
primitivity, `B ≠ 0`, precision bound).

Field strategy:
- `cover`: each lifted index maps back to a mod-`p` index of the monic
  correspondent `(toMonic core).monic` via `modPFactor_index_cover`; the resulting
  (non-monic, leading-coeff ±1) integer divisor is sign-normalised to a monic one,
  then the monic correspondent is inverted with `exists_dvd_core_of_dvd_toMonic`
  (#7532) to land an integer factor of `core`.
- `pairwise_disjoint`: a local `descent` helper sends each lifted representation to
  its mod-`p` subset (monic correspondent + `toMonicLiftData_unique_subset`);
  non-association transfers through `associated_of_associated_monicCorrespondent`
  (#7533) and `modPFactorSubset_disjoint_of_choosePrimeData` (#7542) gives mod-`p`
  disjointness, reflected back by `liftedSubsetOfModPSubset_disjoint_iff`.
- `unique_up_to_associated`: each factor equals its own `liftedRecoveryCandidate`
  (primitive, positive-leading), collapsing association to equality, then
  `toMonicLiftData_unique_subset`.
- `support_subset_of_dvd_liftedRecoveryCandidate` and `liftedRecoveryCandidate_eq`:
  thin wrappers over the landed `toMonicLiftData_*` recovery analytics.

Supporting work:
- New private helper `one_le_degree_getD_of_irreducible_dvd_primitive`: an
  irreducible integer divisor of a primitive polynomial has positive degree (a
  degree-0 divisor would be a unit constant). This supplies the `hfdeg` premise of
  `exists_monicCorrespondent_of_dvd` that the #7455 keystone took as a hypothesis.
- De-privatised four reusable helper lemmas in `Basic.lean`
  (`leadingCoeff_normalizeFactorSign_nonneg`,
  `zpoly_primitive_liftedRecoveryCandidate`,
  `leadingCoeff_liftedRecoveryCandidate_pos`,
  `toPolynomial_normalizeFactorSign_associated`) so the producer, which lives in
  the downstream `IntReductionMod.lean`, can use them. Removing `private` only
  widens visibility and does not change any proof.

Placement note: the producer lives in `IntReductionMod.lean` rather than next to
`slowPathHenselSubstrate_of_toMonicChoosePrimeData_success_descent` (Basic.lean)
because the `pairwise_disjoint` field needs the mod-`p` squarefreeness datum
(`modPFactorSubset_disjoint_of_choosePrimeData`, #7542) which lives in
`IntReductionMod.lean`, downstream of `Basic.lean`. Wiring the producer into the
upstream slow-path substrate is therefore deferred (the substrate cannot see
downstream modules); it remains a follow-up cleanup.

## Verification

- `lake build HexBerlekampZassenhausMathlib` — 0 errors.
- `lake build HexBerlekampZassenhaus` — build completed successfully (497 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` on added lines.

## Current frontier

The producer exists and compiles. The structure is sound as stated; no field
forced the refuted fixed-subset correspondence contract.

## Next step

A successor could adopt the producer in place of the `hinitial` hypothesis of
slow-path substrate assemblies once those assemblies live downstream of (or move
alongside) the squarefreeness datum.

## Blockers

None.
