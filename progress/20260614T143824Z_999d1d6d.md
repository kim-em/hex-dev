# Session 999d1d6d — feature #7198: reroute productCongruence chain to dilate model

## Accomplished

Completed all of issue #7198 (deliverables 2-4), the residual scale→dilate
recovery remodel after #7196 landed deliverable 1.

Root cause: #7196 changed `RepresentsIntegerFactorAtLift core d factor S`
(`Basic.lean:2316`) from defeq-to-a-scale-congruence into
`Nonempty (RecoveredAtLift core d factor S)`. The old scale conclusion
`reduceModPow (DensePoly.scale (lc core) product) = reduceModPow factor`
is unsound (`scale c` coeff `c·pₙ` vs `dilate c` coeff `cⁿ·pₙ`, disagree
off the monic case).

- **Deliverables 2-3 (`Recovery.lean`, fast path, monic-gated)** — commit
  `40d0ef72`. Replaced the unsound `productCongruence_of_representsIntegerFactorAtLift`
  and its `productCongruencesOf*` indexed forms with `dilatedRecovery_of_*`
  forms that convert a `RepresentsIntegerFactorAtLift` certificate into the
  dilated centered-lift recovery equality via the existing
  `dilatedCenteredLift_of_representsIntegerFactorAtLift` (`Basic.lean:9411`).
  Threaded the monic-fast-path side conditions through
  `ofMignottePrecisionCanonicalIndicatorsExpectedFactorsAtPrecisionForCoeffBound`,
  the `factorFast_ne_none_of_mignottePrecisionCanonicalSupports*` wrappers,
  and the `product_congr` fields of `CanonicalRecoveryInputs` /
  `CanonicalRecoveryTailInputs` with their constructors. Monic hypotheses are
  supplied by callers that already carry them
  (`ofForwardRecoveryInputsCanonicalRepresentations`), so nothing false is
  assumed about the non-monic `squareFreeCore`.

- **Deliverable 4 (`IntReductionMod.lean`, slow path, non-monic)** — commit
  `3bf01369`. The `MonicReductionCorrespondence.scaled_recovery_of_bound`
  field concluded `scaledRecombinationCandidate core d S = factor`, but
  `scaledRecombinationCandidate` (`Basic.lean:2396`) is the stale scale-model
  def. The executable `Hex.scaledRecombinationSearchModAux`
  (`HexBerlekampZassenhaus/Basic.lean:6708`) builds its candidate via
  `dilate coreLc (centeredLiftPoly product)` = `liftedRecoveryCandidate`.
  Renamed the field to `recovery_of_bound`, retargeted its conclusion to
  `liftedRecoveryCandidate core d S = factor`, swapped its hypotheses for what
  `RecoveredAtLift.candidate_eq_of_monic_dvd` (`Basic.lean:2781`) needs
  (monic-coordinate precision, `hmonic_ne`, `hfactor_norm`, the certificate),
  and reproved the constructor obligation. The field was unconsumed except by
  its own obligation, so no callers broke.

## Verification

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — green (3734 jobs).
- `lake build HexBerlekampZassenhausMathlib.Recovery` — residual errors are
  ONLY the separate #7122 group (`factorFast_ne_none_of_forwardInputs_on_schedule`
  whnf/isDefEq timeouts + unknown constant). The two in-scope errors (old
  `:1260` scale-unfold and `:1869` scale-vs-dilate mismatch) are gone. Per the
  issue, the module is not fully green until #7122 lands.
- `python3 scripts/check_dag.py` — exit 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines; no banned
  vocabulary on added lines.

## Current frontier

#7198 is fully delivered (2-4). The Recovery module's remaining red is the
#7122 migration only.

## Next step

#7122 (`factorFast_ne_none_of_forwardInputs_on_schedule` heartbeat/unknown-
constant group) is the next thing standing between Recovery and a green build.

## Blockers

None for this issue.

## Notes

- Worktree carries pre-existing uncommitted edits to off-limits `.claude/`
  files from a prior session; left untouched and excluded from the commits.
- Branch was behind origin/main at session start (missing #7196); fast-forwarded
  before working.
