# Issue 9691 progress

- Session type: feature
- Refactored Hermite coefficient-growth instrumentation to run through
  `Matrix.Hermite.checkedRun` with a shadow-matrix accumulator, eliminating the
  duplicated principal and fallback schedules.
- Kept the transcript format unchanged and retained the final agreement check;
  it now checks both the production result and the accumulator shadow against
  `Matrix.hnf`.
- Added build-time shadow-agreement guards for dense, deficient, tall, and
  conjugate inputs.
- Verified `lake build HexHermite`, `lake build hexhermite_bench`, all four full
  committed growth ladders byte-for-byte, all 92 benchmark registrations with
  FLINT and PARI available, and the Mathlib-free bench boundary check.
- Added no `sorry`, `axiom`, `native_decide`, imports, or runtime oracle
  dependencies.
- Remaining work: PR review, CI, and merge.
