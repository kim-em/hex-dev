# Issue #4200

## Accomplished

- Claimed feature issue #4200 after directive #3945 was rejected by
  `coordination claim` as needing replan.
- Added `Hex.Berlekamp.berlekampFactor_factors_ne_nil` in
  `HexBerlekamp/Factor.lean`, exposing the existing length-monotonicity fact
  needed to rule out an empty Berlekamp factor list.
- Added
  `HexBerlekampMathlib.irreducible_of_berlekampFactor_factors_length_le_one`
  in `HexBerlekampMathlib/Basic.lean`, proving that a monic square-free
  finite-field input is irreducible when executable Berlekamp factorization
  emits at most one factor.
- Filed successor issue #4216 for the remaining Zassenhaus prime-choice
  provenance bridge and final `IntReductionMod` small-mod singleton assembly.
- Left existing `.claude/CLAUDE.md` worktree modification untouched.
- Verified:
  - `lake build HexBerlekamp.Factor`
  - `lake build HexBerlekampMathlib.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The Berlekamp no-split irreducibility substrate is now available.
- The original #4200 branch theorem is not fully closed yet: it still needs a
  public bridge from `Hex.choosePrimeData core` / `PrimeChoiceData.factorsModP`
  to the concrete Berlekamp run over the monic modular image, plus the
  associated good-prime square-free and leading-coefficient facts.

## Next step

- Pick up #4216 and expose the prime-choice provenance theorem, then use the
  new Berlekamp singleton theorem to remove the explicit `hirr_fModP`
  dependency from the small-mod singleton Zassenhaus branch theorem.

## Blockers

- None for the partial Berlekamp substrate.
