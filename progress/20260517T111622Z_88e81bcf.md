# 20260517T111622Z — review session 88e81bcf (#4659)

Session UUID prefix: 88e81bcf. Issue: #4659 — review PR #4657 scaled
primitive recombination recovery substrate.

## Accomplished

Reviewed PR #4657 against #4652, stale predecessor #4643, and directive #2564.
The new primitive non-monic recovery surface is formulated around the scaled
candidate:

- `scaledRecombinationCandidate core d S`
- `scaledRecombinationCandidate_eq_factor_of_recovery`
- `scaledRecombinationCandidate_eq_factor_of_henselSubsetCorrespondence`

The PR does not reintroduce the known-false theorem shape
`recombinationCandidate d S = factor` for primitive non-monic cores from only
the scaled modular recovery hypothesis. Existing monic recovery declarations
remain intact.

Applied one small review fix directly to PR #4657 in commit `278b9b64`:
clarified that the monic comparison between the scaled candidate and the
existing recombination candidate is through existing monic scaling and
product-transport lemmas, not definitional equality.

Left the concrete review result on PR #4657:
https://github.com/kim-em/hex/pull/4657#issuecomment-4470400967

Verification run on the reviewed PR branch:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- compared `rg -n "sorry|axiom|native_decide|TODO|FIXME"
  HexBerlekampZassenhausMathlib/Basic.lean` against `origin/main`; both have
  the same 10 pre-existing matches.

## Current frontier

PR #4657 has no blocking review findings from this session. Downstream HO-1
work should consume the scaled-candidate substrate rather than the unscaled
`recombinationCandidate` in primitive non-monic contexts.

## Next step

Merge PR #4657 when repository policy permits, then continue with the
downstream #4638 sub-chain (#4644, #4646, #4647, #4648).

## Blockers

None for this review. GitHub would not accept a formal approval review from
this account because it owns the PR, so the result was posted as a PR comment.
