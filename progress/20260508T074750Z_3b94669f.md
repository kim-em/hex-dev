## Accomplished

- Claimed issue #2660.
- Added the public `Factorization` record and `Factorization.product` in
  `HexBerlekampZassenhaus/Basic.lean`.
- Changed `factorSlow`, `factorFast`, `factorWithBound`, and `factor` to return
  the new record API while keeping array-based recombination internals private.
- Added signed-content scalar extraction and multiplicity aggregation for
  repeated polynomial factors.
- Added `Hex.ZPoly.Irreducible`, `ZPoly.isIrreducible`, the iff bridge, and the
  decidability instance expected by downstream APIs.
- Updated BZ conformance, fixture emission, the python-flint oracle, and the
  Mathlib-facing BZ bridge to consume the `Factorization` API.

## Current frontier

- The executable API now separates scalar content from polynomial factors and
  preserves product through `Factorization.product`.
- The current theorem bridge for `ZPoly.isIrreducible_iff` follows the existing
  file pattern of proof-facing `sorry` placeholders; the executable checker and
  instance surface exist and build.

## Next step

- Future proof work should discharge `ZPoly.isIrreducible_iff` together with
  the surrounding BZ correctness placeholders.

## Blockers

- None for the executable API migration.
