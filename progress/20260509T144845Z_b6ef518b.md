# 20260509T144845Z - HexMatrix determinant inverse permutation infrastructure

## Accomplished

Read the latest progress note and worker-flow instructions, then inspected the
unclaimed queue. Human-oversight issue #2697 was refused by coordination as
`replan`; #2674 and #2679 were claimed in turn, diagnosed as stale because the
required determinant/Gram bridge infrastructure is still absent, and released
with concrete comments pointing to the prerequisite chain.

Claimed #2778 and added the first checked prerequisite in
`HexMatrix/Determinant.lean`: `inversePermutationVector` plus lookup,
membership, nodup, and involution lemmas for the existing
`Vector (Fin n) n` permutation representation.

Verification completed:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

The local patch covers inverse-permutation construction but does not complete
#2778's public `det_transpose` theorem. The remaining pieces are inverse sign
parity, product reindexing through the inverse permutation, and the final
Leibniz-sum substitution.

GitHub GraphQL rate limit is exhausted (`remaining: 0`), so this session could
not create decomposition issues or a PR via `coordination create-pr`.

## Next step

Once GitHub API quota resets, either split #2778 into ordered sub-issues for
inverse sign parity / product reindexing / `det_transpose`, or continue on the
claimed issue by proving those remaining lemmas directly on top of the new
inverse-permutation API.

## Blockers

GitHub GraphQL API rate limit exhaustion blocks coordination issue creation and
PR publication from this session.
