# 20260509T221352Z - Issue #2849

## Accomplished

Claimed feature issue #2849 and added derivative-split pairwise-readiness
wiring in `HexPolyFp/SquareFree.lean`.

Added:

- `yunFactorsPairwiseReady_of_reachable_common_dvd_one`
- `yunFactorsPairwiseReady_of_derivative_split_common_dvd_one`
- `yunFactorsPairwiseInvariant_of_derivative_split_common_dvd_one`

These lemmas recursively turn the current-tail common-divisor payload from
reachable Yun states into `yunFactorsPairwiseReady`, then expose an adjacent
derivative-split invariant constructor whose caller no longer supplies readiness
manually.

Verification completed:

- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The `rg` check still reports the pre-existing proof holes in
`HexPolyFp/SquareFree.lean`; this session did not add any new `sorry`, `axiom`,
or `native_decide`.

## Current frontier

Derivative-split pairwise readiness can now be assembled from per-step monic
gcd and common-divisor obligations over the reachable Yun recursion states.

## Next step

Prove the derivative-split algebra that supplies those per-step monic/common-
divisor obligations, then use the new invariant constructor to replace the
known-false arbitrary `yunFactors_pairwise_coprime_nil` path.

## Blockers

None for this scoped wiring issue.
