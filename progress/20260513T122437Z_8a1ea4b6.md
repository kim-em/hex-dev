# HO-25: HexMatrix FLINT fmpz_mat_det informational comparator

Session: `8a1ea4b6-463d-4902-b986-1f5d73fa1753`
Branch: `agent/8a1ea4b6`

## Accomplished

- Added `phase4.comparators` entry to `libraries.yml: HexMatrix`
  declaring `FLINT fmpz_mat_det via python-flint` as the informational
  comparator with structural-layer rationale matching the SPEC text.
- Wired `runFlintBareissDet` in `HexMatrix/Bench.lean` against the
  shared `fmpz_mat.det` op exposed by
  `scripts/oracle/flint_bench_driver.py` (no driver extension
  needed; HO-25 consumes the existing op rather than extending it).
- Registered twelve paired `setup_fixed_benchmark` rungs at
  `n ∈ {16, 24, 32, 48, 64, 96, 128, 192, 256, 320, 384, 512}`,
  densifying outward from the parametric `[8, 12, 16]` schedule to
  surface FLINT's eligible regime while staying inside the 10 s
  per-call hard ceiling. All 24 paired rungs verify with matching
  observed hashes (sign convention agrees between Hex's row-pivoted
  Bareiss and FLINT's multimodular CRT).
- Measured FLINT driver per-call overhead at median **55.2 ms**
  (11-spawn sample, matches HO-22's 56.3 ms figure within noise; the
  driver shape is identical across families).
- Regenerated `reports/hex-matrix-performance.md` with Verdicts
  refreshed at commit `77870fc-dirty`, a new Comparator Ratios
  subsection with per-rung table (raw + adjusted ratios, eligibility
  flags), a trend narrative, and an adverse-trend Concern. Exported
  full bench run to `reports/bench-results/hex-matrix-77870fc.json`
  (27 results: 3 parametric + 24 fixed-comparator pairs).

## Current frontier

The Comparator Ratios subsection shows three eligible rungs
(`n = 320, 384, 512`) with adjusted ratios `0.057x → 0.052x →
0.049x`: FLINT spends ~5% of Hex's wall time on the same
determinant surface and the gap widens with `n`. Recorded as an
adverse-trend Concern per SPEC; the comparator is informational so
no gating-goal verdict is required.

## Next step

Ship the PR closing `#3673`. Follow-up HOs `#3674` (HexGfqRing /
`fq_default`) and `#3675` (HexGF2 / NTL GF2X) remain unclaimed.

## Blockers

None for this issue. The `HexMatrix/Bareiss.lean` `sorry` warning is
pre-existing and unrelated.
