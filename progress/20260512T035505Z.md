## Accomplished

Reviewed the revised benchmark-CI plan against the current SPEC,
PLAN, CI workflow, Lake targets, and representative bench modules.
Built a read-only Lean import walker for bench roots and confirmed the
non-Mathlib computational benches sampled (`HexGfq.Bench`,
`HexGfqField.Bench`, `HexGF2.Bench`, `HexGF2Bench`, `HexLLL.Bench`)
do not transitively import upstream `Mathlib.*`.

Checked branch-protection required contexts via `gh api`; current
contexts are `build`, `build-macos`, and `conformance`, not
`affected-benches-check`. Noted that `gh pr diff 3678` failed due an
API connection error, so the review used local merged files and git
history.

## Current frontier

Plan review is complete. Main findings: the revised direction is
right, but HO-alpha misses `HexGF2Mathlib/Bench.lean` /
`hexgf2mathlib_bench`; the lint should cover all `*_bench` exe roots
plus canonical bench files, not only `Hex*/Bench.lean`; and the
working tree contains uncommitted implementation changes that should
not leak into a doc-only revert PR.

## Next step

If proceeding, prepare a doc-only revert of PR #3678's doctrine, then
separate implementation PRs for deleting Mathlib bridge benches,
adding the structural lint, and adding the timing budget wrapper.

## Blockers

No blockers for the review. Direct PR diff inspection was blocked by
transient GitHub API connectivity.
