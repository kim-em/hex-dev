# 20260509T132309Z — PR #2794 CI fpylll bench repair

## Accomplished

Claimed repair for PR #2794 (`bench: wire HexLLL fpylll comparator`) and
diagnosed the failed Linux `build` check. The failing step was the CI bench
smoke gate: the new HexLLL fpylll fixed benchmark registrations shell out to
`python3 scripts/oracle/lll_fpylll.py --bench-checksum`, but the Ubuntu CI
build job installed only `libgmp-dev`. The conformance job installed fpylll
and passed; the build job did not, so the fpylll child exited with code 1.

Extended the existing Ubuntu `build` job dependency setup in
`.github/workflows/ci.yml` to install `libfplll-dev` plus the Python runtime
packages `cysignals` and `fpylll`. This keeps the workflow topology unchanged
and leaves the informational comparator as a real external run rather than a
stub.

Verification run locally:

- `python3 scripts/check_dag.py`
- `python3 scripts/check_phase4.py`
- `lake build`
- full CI bench smoke command from `.github/workflows/ci.yml`, with
  `fpylll 0.6.4` and `cysignals` on `PATH`

All passed. A first targeted `hexlll_bench verify` reproduced the missing
dependency, and a later full smoke attempt accidentally used a nested login
shell that reset `PATH`; the final full smoke run used `set -e` and confirmed
the repaired environment before executing the benchmark suite.

## Current frontier

PR #2794 is repaired locally. The only intentional code change in this repair
session is `.github/workflows/ci.yml`; `.claude/CLAUDE.md` was already dirty
and remains untouched/off-limits.

## Next step

Commit the CI dependency repair, push the PR branch with `--force-with-lease`,
then mark PR #2794 salvaged.

## Blockers

None.
