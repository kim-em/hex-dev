# HO-44 Bareiss orphan removal (#3900)

## Accomplished

Skipped stale directive #2637 after confirming the current tree already has the
`Factorization` API, signed scalar handling, multiplicity collection, product
theorem, and Mathlib bridge surface described by the old `Array ZPoly` issue
body; the remaining irreducibility capstone is already tracked by later HO-1
issues.

Claimed #3900 and removed the orphan Mathlib-free
`Hex.Matrix.bareiss_eq_det` declaration from `HexMatrix/Bareiss.lean`.

Verification passed:

* `rg -n 'Hex\.Matrix\.bareiss_eq_det|Matrix\.bareiss_eq_det' HexMatrix HexMatrix.lean HexGramSchmidt HexGramSchmidt.lean`
* `rg -n '^\s*sorry\b' HexMatrix/Bareiss.lean`
* `lake build HexMatrix HexMatrixMathlib HexGramSchmidt HexGramSchmidtMathlib HexLLL HexLLLMathlib`
* `python3 scripts/check_dag.py`
* `git diff --check`

The Lake build still reports pre-existing `sorry` warnings in GramSchmidt/LLL
files outside this change.

## Current frontier

The forbidden Mathlib-free determinant equality surface is gone from
`HexMatrix/Bareiss.lean`; bridge-side determinant theorems remain in
`HexMatrixMathlib`.

## Next step

Open the #3900 PR with the single deletion and successful verification.

## Blockers

None.
