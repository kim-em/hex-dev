# 20260509T222243Z - Issue #2883

## Accomplished

Claimed feature issue #2883 and added row-operation bridge benchmark coverage in
`HexMatrixMathlib/Bench.lean`.

The bench module now has deterministic fixtures and paired checksum targets for:

- `runHexRowSwapBridgeChecksum` / `runMathlibRowSwapChecksum`
- `runHexRowScaleBridgeChecksum` / `runMathlibRowScaleChecksum`
- `runHexRowAddBridgeChecksum` / `runMathlibRowAddChecksum`

Each target is registered with `setup_benchmark` using the advertised `O(n^2)`
model and adjacent cost-model comments. The module docstring documents the
corresponding CLI `compare` groups.

Verification completed:

- `lake build HexMatrixMathlib.Bench`
- `lake exe hexmatrixmathlib_bench list`
- `lake exe hexmatrixmathlib_bench verify`
- small-domain `compare` smoke checks for all three row-operation pairs
- `lake build HexMatrixMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrixMathlib/Bench.lean`

The small-domain compare checks used a single `n = 128` rung only to confirm
hash agreement, so their scaling verdicts were below resolution as expected.
They all reported agreement on the common parameter. Build warnings were the
pre-existing `sorry` warnings in unrelated matrix modules.

## Current frontier

`HexMatrixMathlib` benchmark coverage now includes the SPEC-advertised row-swap,
row-scale, and row-add bridge surfaces.

## Next step

Land the PR for #2883. Future Phase 4 work can address rank/span/nullspace
benchmarks once those theorem surfaces are ready.

## Blockers

None for #2883.
