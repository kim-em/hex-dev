# doc session (issue #7222)

## Accomplished

Phase 6 docstring batch 2 for `HexMatrix/Determinant.lean`: added a
one-sentence, backtick-led `/-- … -/` docstring to all 23 undocumented
declarations in the `lowerFinLast`/`raiseFinAbove` inversion-count,
`foldCount` full-nodup, and `peelLastVector` insertion cluster
(lines ~1383–1729): `lowerFinLast`, `raiseFinAbove`,
`insertAt_get_raiseFinAbove`, `raiseFinAbove_lt_iff`,
`inversionFold_map_raiseFinAbove`, `inversionCount_map_raiseFinAbove`,
`inversionFold_map_raiseFinAbove_self`,
`inversionCount_map_raiseFinAbove_append_self`,
`foldCount_map_castSucc_ge`, `foldl_ignore`, `foldCount_finRange_ge`,
`foldCount_perm`, `fin_mem_of_full_nodup_for_count`,
`list_perm_finRange_of_full_nodup`, `foldCount_full_nodup_ge`,
`lowerFinLast_castSucc`, `finLast_idxOf_lt_of_full_nodup`,
`peelLastVector`, `peelLastVector_castSucc_toList`,
`list_nodup_of_map_injective`, `peelLastVector_nodup`,
`list_insertIdx_eraseIdx_getElem`, `insertAt_peelLastVector`.

No declaration in the cluster already carried a docstring, so none was
skipped.

## Current frontier

Doc-only diff, 41 insertions, 0 deletions, no signature/proof changes.
`lake build HexMatrix.Determinant` green, `scripts/check_dag.py` exits 0,
`git diff --check` clean, no banned vocabulary or em-dashes on added
lines.

## Next step

The issue's named follow-up batch-3 cluster: the
`permutationVectors_complete`/`detSign_*`/`detProduct_*` block beginning
at L1757, intentionally out of scope here.

## Blockers

None.
