# 2026-05-21T08:04Z — feature issue #5759

## Accomplished

Claimed #5759 and added named `[simp]` constructor-characterisation lemmas for
the HexArith Barrett and Montgomery contexts.

In `HexArith/Barrett/Context.lean`, added lemmas for `BarrettCtx.mk`'s stored
lower/upper modulus proofs, reciprocal word, and reciprocal Nat value:
`mk_p_gt`, `mk_p_lt`, `mk_pinv`, and `mk_pinv_toNat`.

In `HexArith/Montgomery/Context.lean`, added lemmas for `MontCtx.mk`'s stored
oddness witness, inverse word, inverse correctness fact, `R^2` word, `R^2`
correctness fact, and specialized derived modulus facts:
`mk_p_odd`, `mk_p'`, `mk_p'_eq`, `mk_r2`, `mk_r2_eq`, `mk_p_odd_nat`,
`mk_p_pos`, and `mk_p_lt_R`.

Verification:

- `lake build HexArith.Barrett.Context`
- `lake build HexArith.Montgomery.Context`
- `lake build HexArith`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token scan over the target context diff returned no matches.

## Current frontier

#5759 is complete. The sibling automation issue #5758 remains available for a
future worker and can now use stable constructor lemmas instead of unfolding
context constructors.

## Next step

Open the PR for #5759 with the two context-file edits and this progress note.

## Blockers

None. The worktree still contains a pre-existing `.claude/CLAUDE.md`
modification, left untouched.
