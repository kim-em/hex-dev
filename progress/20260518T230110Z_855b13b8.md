## Accomplished

- Claimed feature issue #4929.
- Added
  `coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
- Rewired the existing
  `coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_primitive_pos_lc_core`
  theorem as a thin `defaultFactorCoeffBound core` wrapper.
- Reused the existing normalized-factor association bridge pattern to derive
  the single-factor coefficient bound for the extracted cover witness `f`.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic` reached through the edited
  cover-at-min declarations, then failed in the pre-existing
  `HexBerlekampZassenhausMathlib/Basic.lean` health areas: whnf heartbeat
  timeouts, the dependent-elimination failure at line 5840, and unknown kernel
  constants around the existing choose-prime-data lemmas.
- `lake build HexBerlekampZassenhausMathlib` failed with the same
  `HexBerlekampZassenhausMathlib.Basic` diagnostics.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean | grep -E '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true`
  produced no output.

## Current frontier

- #4929 deliverables are implemented for the primitive positive-leading-core
  cover-at-min layer.
- Downstream consumers continue to call the original theorem signature.

## Next step

- Publish the PR for #4929.
- Follow-on issue #4951 can then lift the
  `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core` tier to
  the abstract-bound shape.

## Blockers

- Full builds of `HexBerlekampZassenhausMathlib.Basic` and
  `HexBerlekampZassenhausMathlib` remain blocked by pre-existing large-file
  heartbeat and dependent unknown-constant failures.
