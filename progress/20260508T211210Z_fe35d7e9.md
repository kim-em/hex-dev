## Accomplished

- Claimed issue #2722 for HexModArith Phase-4 evidence restoration.
- Added common-domain Barrett/Montgomery compare registrations to `HexModArith/Bench.lean` while preserving the existing hot-loop benchmark families.
- Ran the HexModArith scientific benchmark suite and committed the exported artefact at `reports/bench-results/hex-mod-arith-f5bfa6409349.json`.
- Added `reports/hex-mod-arith-performance.md` with bench targets, verdicts, internal compare evidence, profile status, and an explicit Concern.
- Filed follow-up #2756 for the remaining usable-profile coverage blocker.

## Current frontier

- HexModArith has traceable scientific benchmark verdicts and a headline report, but cannot be re-promoted to Phase 4 yet because local `samply record --save-only` attempts produced zero sampled stacks.
- `HexModArith.done_through` remains 3.

## Next step

- Complete #2756 by producing usable profiles for `word-residue-core`, `barrett-hot-loop`, and `montgomery-hot-loop`; then clear the report Concern and restore `HexModArith.done_through: 4` if the attribution review is clean.

## Blockers

- Usable profile data is unavailable from the local samply attempts in this session.
