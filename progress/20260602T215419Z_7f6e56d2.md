# 20260602T215419Z work #2567

## Accomplished

- Claimed directive #2567 and advanced the HO-4/BHKS D1 final assembly surface.
- Added `BHKS.CanonicalRecoveryTailInputs` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`, packaging the canonical
  support recovery facts that remain after cap separation supplies `L' = W`.
- Added `CanonicalRecoveryTailInputs.ofExpectedFactors`, which internalizes
  the unconditional support-partition nonempty/bounds fields.
- Added
  `factorFast_ne_none_of_capSeparationBridgeDataCanonicalRecoveryTailInputs_internalCapPositiveAndPrimeLowerBound`,
  composing the actual-cap `BadVectorBridgeData` cap-separation wrapper with
  the packaged canonical recovery tail inputs.

## Current frontier

The final `factorFast_terminates` theorem is still not closed.  The actual-cap
assembly now has a smaller support-side call surface: bridge data plus cut,
analytic comparison, prime-choice/precision equations, and one packaged
canonical recovery tail input.

## Next step

Construct closed providers for the canonical true supports and the
`BadVectorBridgeData` package, then instantiate `CanonicalRecoveryTailInputs`
from the closed expected-factor/Mignotte congruence facts.

## Blockers

No blocker for this packaging slice.  The remaining work is the mathematical
closed-provider construction for bridge data and expected recovery facts.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
