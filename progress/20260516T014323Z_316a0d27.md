# Session 316a0d27 — /work meta dispatch

## Accomplished

- Triaged the four unclaimed issues at orient time (#4301, #4334,
  #4354, #4355). #4355 was claimed by a parallel agent before this
  session could pick it up.

- Claimed and skipped **#4354** (HO-43 prerequisite: Mathlib-free
  `gramDet_pos_of_upperTriangular_pos_diag`) after detailed
  architectural analysis posted as
  https://github.com/kim-em/hex/issues/4354#issuecomment-4465019693.
  Summary of the analysis: the issue's suggested proof shape —
  track Bareiss diagonal entries as "products of M[i][i]²" via the
  `bareiss_eq_noPivotLoop_last_of_no_singular` route — requires
  proving the no-pivot bordered-minor invariant chain inside
  `hex-matrix` or `hex-gram-schmidt`, which
  `SPEC/Libraries/hex-matrix.md` explicitly forbids
  (`NonzeroBareissPivots`, `BareissNoPivotInvariant`, and "the
  no-pivot bordered-minor invariant proof chain culminating in
  `bareissNoPivot_eq_det`" are listed as forbidden in
  `hex-matrix`). A previous worker (b3c85d46) flagged the same
  blockage in different words ("cascading consumer refactor needs
  a Mathlib-free triangular-Bareiss positivity lemma (new
  development)") on the parent #4341 before it was closed
  planning-stale. The Cauchy–Schwarz lower-bound route is
  mathematically sound (worked out new[1][1] ≥ b[0][0]² · b[1][1]²
  for the 2 × 2 case) but extending it through k − 1 Bareiss steps
  requires tracking the full trailing block, which is the
  bordered-minor invariant in product-of-norms language — same
  magnitude of work as the bridge-side proof. Comment proposes two
  options: (A) accept ~200–500 lines of Cauchy–Schwarz tracking, or
  (B) redesign consumers (move `Matrix.identity_independent` and
  `Matrix.independent_of_upperTriangular_pos_diag` to a Mathlib
  layer, or introduce a det-based independence witness).

- Claimed and skipped **#4301** (HexBZ recursive rest-search
  coverage). The most recent planner replan-triage comment had
  already marked the issue blocked on (1) #4355 and (2) an unscoped
  "prefix-none under partition" sub-issue, and explicitly said
  "Marking blocked and removing replan so workers do not re-claim
  and hit the same wall." The `blocked` label was auto-stripped by
  the orphan-housekeeping cycle because the body lacked
  `depends-on:` lines, which is why the issue surfaced as
  unclaimed. Honoured the planner's intent: skipped with that
  reason, then ran `coordination add-dep 4301 4355` so the
  `blocked` label persists and the housekeeping cycle keeps it.

- Left **#4334** (HexLLL Phase 4 inconclusive verdicts) unclaimed:
  pod host is under heavy concurrent load right now (`uptime` =
  load avg 5.87/4.94/4.75; 38 concurrent lake/lean/claude
  processes). The issue body and two prior skips on it document
  that scientific verdict promotion requires "quiet/dedicated
  hardware with a stable spawn floor"; another worker-side skip on
  the same environmental reason would just churn the issue without
  changing the queue.

## Current frontier

For the HO-43 / HO-44 chain (which #4354, #4351, #4053, #3977,
#3899, #3900 all sit on): the next productive step depends on a
planner-level architectural decision about how to discharge
upper-triangular `gramDet` positivity Mathlib-free. The choice
points are written out in the #4354 comment.

For the HexBZ recursive-coverage chain: #4355 is in flight and is
prerequisite (1). The unscoped "prefix-none under partition"
prerequisite (2) on #4301 needs a planner to write up as a sub-
issue before #4301 itself becomes workable.

#4334 needs the release-candidate scheduled benchmark workflow on
dedicated hardware (per `SPEC/benchmarking.md` §"Scientific timing
runs") rather than another worker claim on the pod host.

## Next step

A planner cycle (`/plan` or `/replan`) is what advances the queue
from here. Concretely:

1. Decide between Options A and B in the #4354 comment, and either
   rewrite #4354's deliverable accordingly or move the LLL-facing
   independence helpers into the bridge layer.
2. Scope the "prefix-none under partition" sub-issue called out in
   the #4301 replan triage.
3. Run #4334 on the dedicated benchmark workflow rather than
   reclaiming it on a contended pod host.

## Blockers

None new from this session. The blockers documented above are
queue-state observations, not per-session blockers.
