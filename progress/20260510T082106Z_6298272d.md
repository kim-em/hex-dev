# 20260510T082106Z - Issue #2960

## Accomplished

Claimed #2960 and reviewed the landed HexBerlekampZassenhaus benchmark surface
against HO-3 (#2566), HO-2 (#2565), `SPEC/benchmarking.md`, `PLAN/Phase4.md`,
PR #2931, and PR #2951.

The review confirmed that `HexBerlekampZassenhaus/Bench.lean` and the
`hexbz_bench` Lake target exist and expose the public factorization paths,
degree/height schedules, tractable HO-2 adversarial singleton targets, and the
reduced SD3 modular split smoke target.  It also found that the smoke gate is
not fully green: `lake exe hexbz_bench verify` fails for
`runFactorFastAdvPhi15Checksum` and `runFactorFastAdvX4Plus1Checksum`, both
killed at the `maxSecondsPerCall` cap.

Posted the coverage verdict on #2566 and filed follow-up #2967 for the narrow
benchmark-harness repair.

Verification completed:

- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify` (failed on the two fast adversarial singleton
  smoke targets noted above)
- `python3 scripts/check_phase4.py`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

HO-3 benchmark coverage is present but not yet closure-ready because the
`hexbz_bench verify` smoke gate fails on two `factorFast` adversarial singleton
registrations.

## Next step

Pick up #2967 to make the `Phi_15` and `X^4 + 1` `factorFast` adversarial
singleton coverage smoke-safe while preserving benchmark visibility and
distinct `none` checksums.

## Blockers

No blocker for the review issue.  HO-3 closure remains blocked on #2967.
