# BZMathlib: abstract-bound siblings of `exists_mem_representedSubset_of_degree_cover` (#4912)

## Accomplished

* Added `exists_mem_representedSubset_of_degree_cover_of_bound` (monic
  abstract-bound sibling) in `HexBerlekampZassenhausMathlib/Basic.lean`,
  taking `B' : Nat`, `hvalid : ∀ g ∈ gs, ∀ i, (g.coeff i).natAbs ≤ B'`,
  and `hprecision : 2 * B' < d.p ^ d.k`. Body mirrors the existing
  monic original, routing the per-factor `natDegree` identity through
  `natDegree_toPolynomial_eq_sum_of_represents_of_bound`.
* Added
  `exists_mem_representedSubset_of_degree_cover_of_primitive_pos_lc_core_of_bound`
  (primitive abstract-bound sibling), adding `hcore_lc_bound :
  (leadingCoeff core).natAbs ≤ B'` per the precedent set by
  `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`.
* Rewired both originals as thin wrappers that instantiate
  `B' := defaultFactorCoeffBound core` and discharge `hvalid` per-factor
  via the `g ∣ target ∣ core` chain plus `defaultFactorCoeffBound_valid`.
  The primitive wrapper additionally discharges `hcore_lc_bound` via
  `defaultFactorCoeffBound_valid core hcore_ne core hcore_dvd_self
  (core.size - 1)` and `leadingCoeff_eq_coeff_last`.
* Verified no new errors near edits (lines 9574-9989) — all build
  failures match `origin/main` (with line numbers shifted by ~167 to
  account for the new code). DAG check passes; no new sorry / axiom /
  native_decide / TODO / FIXME.

## Current frontier

The reverse-coverage degree-cover layer (#4912) is now abstract-bound,
joining the per-factor natDegree-sum layer (#4900 / #4904) and the
per-normalized-factor bridge layer (#4902, in flight).

## Next step

The `mem_T_iff_exists_irreducibleFactor_representingSubset*_of_bound`
layer (lines ~10082 / 10293) can now be tackled once #4902 lands. It
depends on both the per-normalised-factor bridge `_of_bound`
(landing in #4902) and this degree-cover `_of_bound` (this PR).

## Blockers

None.
