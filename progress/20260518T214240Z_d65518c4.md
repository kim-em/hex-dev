## Accomplished

- Claimed issue #5104 after all unclaimed directive claims failed as dependency-blocked.
- Added private theorem
  `monicModularImage_dvd_self_of_isZero_false` in
  `HexBerlekampZassenhausMathlib/Basic.lean`, packaging the nonzero
  `Hex.monicModularImage` branch with leading-coefficient inverse scaling
  divisibility.
- Rewired both target `hmonicImage_dvd` sites in
  `factorsModP_nodup_of_factorsModPBerlekampForm` and
  `factorsModP_coprime_of_factorsModPBerlekampForm` to call the helper.

## Current frontier

The #5104 helper extraction is complete. The helper uses the existing project
`Hex.Nat.Prime` predicate, matching the surrounding `hform` destructuring and
`Hex.ZMod64.inv_ne_zero_of_prime` API.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.Basic` fails with the known
  `Basic.lean` baseline: deterministic `whnf` timeouts, the existing dependent
  elimination `cases` failure, and downstream unknown constants. The earlier
  helper-local errors were fixed.
- `lake build HexBerlekampZassenhausMathlib` fails at the same
  `HexBerlekampZassenhausMathlib.Basic` baseline.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- Grep invariant holds: one declaration and three total occurrences of
  `monicModularImage_dvd_self_of_isZero_false` in `Basic.lean`.
- Diff for `HexBerlekampZassenhausMathlib/Basic.lean` introduces no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Next step

Open a PR closing #5104.

## Blockers

None for this extraction. Unrelated dirty worktree entry `.claude/CLAUDE.md`
was present before this session and was not touched.
