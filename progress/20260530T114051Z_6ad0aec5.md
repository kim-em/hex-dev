**Accomplished**

- Claimed #5785 and regenerated the `HexArith` Phase-4 profile narrative in
  `reports/hex-arith-performance.md` under lean-bench timed-region filtering.
- Built `hexarith_bench` against lean-bench `91412dba8350`, fixed the stale
  local package build cache so the timed-region sidecar was actually linked,
  and ran `scripts/profile/run_profile.sh` for all three
  `phase4.input_families` entries:
  `runMontgomeryMulChain 131072`, `runPowMod 16384`, and
  `runIntExtGcdShapes 24576`.
- Recorded the filtered leaf-cost splits, inclusive hot paths, child rows, and
  diagnostics blocks with retained bench-thread sample counts in the thousands.
- Checked the Attribution rule: all dominant inclusive costs remain attributable
  to existing `HexArith/Bench.lean` registrations, so no follow-up audit issue
  was filed.
- Verified:
  - `lake build hexarith_bench`
  - `lake exe hexarith_bench list`
  - `lake exe hexarith_bench verify`
  - `lake build`

**Current frontier**

- The report now reflects the new timed-region-filtered profiles rather than
  the old whole-child samply captures.
- The only non-session dirty file observed was the pre-existing
  `.claude/CLAUDE.md` edit; this session did not touch it.

**Next step**

- Open the PR for #5785 with the report and this progress file.

**Blockers**

- None.
