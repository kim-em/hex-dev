# 20260517T043445Z — feature session 52d84c63 (#4562)

## Accomplished

Resolved #4562 (HO-1 substrate: small-mod singleton arm umbrella for
`factor_irreducible_of_nonUnit`). Added the per-branch umbrella
`factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
at the end of `HexBerlekampZassenhausMathlib/IntReductionMod.lean`
(immediately after `liftedFactorSubsetPartition_outerBound_of_choosePrimeData`,
inside the `HexBerlekampZassenhausMathlib` namespace).

The umbrella consumes:
* `Hex.factorWithBound_entry_mem_small_mod_singleton_raw`
  (branch-shape lemma, executable side) for the
  raw-factor-from-reassembly identification;
* `IntReductionMod.squareFreeCore_irreducible_of_small_mod_singleton_of_choosePrimeData_squareFreeModP`
  for singleton-core irreducibility from the chosen prime's Berlekamp form;
* the #4545 substrate dischargers
  (`normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive`,
  `choosePrimeData?_leadingCoeff_castRingHom_ne_zero`) for `hprim` and
  `hlc_map_ne`;
* `Hex.reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`
  to lift singleton-core irreducibility to raw factor irreducibility under
  the `hcomplete` side condition;
* `zpolyIrreducible_normalizeFactorSign_of_zpolyIrreducible` for the
  raw→entry sign-normalisation lift.

Proof is ~30 lines, no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
The single small helper (`h_core_array : ∀ factor ∈ #[core].toList, ...`)
is inlined as an anonymous `have` since it's a 4-line reduction.

### Collateral fix

`HexBerlekampZassenhaus/Basic.lean:3369`: removed `private` from
`def quadraticIntegerRootFactors?`. The function's signature appears in
the public theorem `factorWithBound_entry_mem_small_mod_singleton_raw`,
so any external consumer that wants to state the branch-shape lemma's
`hquadratic` hypothesis needs to be able to name it. The function had
already been exposed via that public signature — the `private` keyword
prevented downstream consumers in `HexBerlekampZassenhausMathlib` from
even stating the proposition, despite the executable theorem's docstring
explicitly calling out "Mathlib-side irreducibility proofs" as the
intended consumer. One-token change, no semantic impact, no test fallout.

### Build state

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` clean
- Sorry inventory unchanged in all touched files
  (`IntReductionMod.lean`: 0; `HexBerlekampZassenhaus/Basic.lean`: 1;
  `HexBerlekampZassenhausMathlib/Basic.lean`: 11).

## Current frontier

The HO-1 capstone `factor_irreducible_of_nonUnit` (#4170) now has both
per-branch arms either complete or composable:

* Small-mod singleton arm (this issue, #4562):
  `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  — full umbrella, ready to plug in.
* Slow-path exhaustive arm (#4561): blocked on substrate #4563
  (the four `factorsModP_*_of_factorsModPBerlekampForm` dischargers
  for the `QuadraticMultifactorLiftInvariant` boundary hypotheses).
  Once #4563 lands, #4561 becomes a small assembly mirroring this PR.
* Slow-path constant/quadratic sub-branches: not yet specced.
* Fast BHKS arm: gated on #2567.

## Next step

Two natural follow-ups:

1. Pick up #4563 to unblock #4561. That work involves four discharger
   theorems and possibly new Berlekamp-level prerequisites in
   `HexBerlekamp/Factor.lean` (specifically `berlekampFactor_factors_monic`
   and `berlekampFactor_factors_pairwise_coprime`). Larger than this
   session's scope but well-specced.

2. Once #4561 lands too, the HO-1 capstone can compose both arms with
   `factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible` (the
   #4008 raw-to-entry framework) and the existing reassembly lifts.

## Blockers

None for #4562 itself.
