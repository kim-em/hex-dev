# Array `back?` cleanup

## Accomplished

- Replaced the obsolete `DensePoly.leadingCoeff` indexed `getD` workaround with `coeffs.back?.getD 0` now that core exposes `Array.back?` across module boundaries.
- Rephrased downstream last-coefficient proofs through `DensePoly.leadingCoeff_eq_coeff_last` instead of unfolding the representation.
- Built `HexPoly` successfully and used the first clean CI build to find five downstream proofs that still depended on definitional unfolding.
- Rephrased the remaining downstream proofs through the public leading-coefficient, empty-array, and zero-polynomial theorems and recorded exact runtime-neutral benchmark exemptions for every affected factorization path.

## Current frontier

- A second clean CI build confirmed the Hensel and fast-division repairs and exposed three final proof mismatches, which are now repaired for another clean verification.

## Next step

- Push the final proof repairs and let the restarted CI run the full build and plot check in its provisioned environment, then merge after it succeeds.

## Blockers

- The existing local `.lake/build` contains incompatible cached oleans from the previously checked-out toolchain. A full `lake build` reaches the modified modules successfully but reports those stale unrelated headers; the build directory was preserved. The local plot check also lacks Matplotlib, which CI installs before running it.
