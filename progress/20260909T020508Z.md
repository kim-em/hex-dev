# Array `back?` cleanup

## Accomplished

- Replaced the obsolete `DensePoly.leadingCoeff` indexed `getD` workaround with `coeffs.back?.getD 0` now that core exposes `Array.back?` across module boundaries.
- Rephrased downstream last-coefficient proofs through `DensePoly.leadingCoeff_eq_coeff_last` instead of unfolding the representation.
- Built `HexPoly` successfully; a broader targeted build rebuilt all modified downstream modules without source failures.

## Current frontier

- The cleanup is ready for fresh CI verification and review.

## Next step

- Run the full build in a clean environment and merge after CI succeeds.

## Blockers

- The existing local `.lake/build` contains incompatible cached oleans from the previously checked-out toolchain. A full `lake build` reaches the modified modules successfully but reports those stale unrelated headers; the build directory was preserved.
