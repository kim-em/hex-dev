# HexPolyZ SquareFreeRat predicate slice

## Accomplished

- Claimed #2169 and split its remaining hard quotient-by-gcd proof into successor issue #2172.
- Weakened private `SquareFreeRat` in `HexPolyZ/Basic.lean` from literal rational gcd equality with `1` to the associate-invariant `size ≤ 1` condition.
- Updated `squareFreeRat_one` for the new predicate.
- Verified with `lake build HexPolyZ.Basic`, `lake build HexPolyZ`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexPolyZ/Basic.lean`.

## Current frontier

The private predicate no longer requires monic-normalized rational gcd output. The remaining proof frontier is #2172: prove the `DensePoly Rat` quotient-by-gcd square-free lemma against the new `size ≤ 1` conclusion.

## Next step

Prove #2172, then use that lemma in the derivative-active branch work tracked by #2129.

## Blockers

- `HexPolyZ/Basic.lean` still has the pre-existing `sorry` in the derivative-active branch of `primitiveSquareFreeDecomposition_squareFreeCore`.
- The worktree had a pre-existing `.claude/CLAUDE.md` modification; this session did not touch it.
