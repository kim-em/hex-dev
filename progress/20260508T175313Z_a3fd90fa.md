## Accomplished

- Claimed human-oversight issue #2692 for the verified Isabelle LLL comparator.
- Added `scripts/oracle/setup_lll_isabelle.sh`, which downloads Zenodo 2636367, verifies SHA-256 `5c975aeb2033540b8f9a05d2ffac87dca0f258e887a5807edefbe60178a547e0`, unpacks, applies optional local patches, builds `svp_verified`, and prints the binary path.
- Added `scripts/oracle/patches/lll-isabelle/README.md` documenting the currently-empty patch convention.
- Wired smoke fixed benchmark pairs in `HexLLL/Bench.lean` for the three HexLLL Phase 4 input families, with Lean and Isabelle entries returning the squared norm of the first reduced vector and comparing through lean-bench fixed benchmarks.
- Extended the existing single CI ubuntu job's apt install step with `ghc`, `unzip`, and `curl`, preserving the no-fan-out CI rule.

## Current frontier

- The verified-Isabelle setup path and lean-bench comparator plumbing are working and smoke-verified.
- The current benchmark entries use proof-safe identity representatives for the three input-family registrations; nondegenerate scientific ladder representatives still need a follow-up that supplies independence witnesses without adding proof debt.

## Next step

- Replan the remaining #2692 scope into a narrow follow-up that replaces the smoke identity representatives with canonical nondegenerate per-rung inputs and records their expected hashes.

## Blockers

- No tooling blocker for the setup script or smoke comparator path.
- The nondegenerate fixed-input expansion needs either reusable concrete independence lemmas for benchmark fixtures or a small API that carries already-certified benchmark inputs without forcing large determinant proofs in `Bench.lean`.
