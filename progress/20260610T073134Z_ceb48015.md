# 20260610T073134Z (#6719 replan triage)

## Accomplished

Triaged #6719 ("BZMathlib: compose raw slow-trial irreducibility wrappers")
as a replan candidate.

Disposition: **Still valid, body stale**. The underlying scope (expose a
slow-trial raw-factor irreducibility theorem on the Mathlib side at the
shape needed by the HO-1 capstone #6672) is still meaningful, but two
pieces of the body had drifted from the landed code:

- The stated dependency `#6718` was closed as a broad replan parent, not
  with the raw theorem. Its successor `#6726 → #6732` (PR #6739, commit
  `0c4b8cc7`) landed `exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
  at `HexBerlekampZassenhaus/Basic.lean:13443`, covering all three
  emitted-factor families.
- The wrapper names `factorWithBound_slowTrial_entry_zpolyIrreducible_of_raw`
  and `factor_slowTrial_entry_zpolyIrreducible_of_raw`, originally added
  in PR #6699, were deleted in PR #6705 ("expose raw irreducibility bridge
  before capstone") and replaced by the unified
  `factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible` /
  `factor_entries_irreducible` bridge over a three-branch dispatch.

Rewrote the issue body to cite the landed raw theorem, point at the
current unified Mathlib-side wrappers (with line numbers), and restate
the slow-trial-arm discharge as the remaining deliverable. Removed the
`replan` label so a feature worker can claim it.

## Current frontier

#6719 is once again claimable as a plain `feature` issue. Its sole
deliverable is: prove the slow-trial arm of the unified `h_raw`
hypothesis under default-bound branch conditions, composing the now-
landed `exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
with `defaultFactorCoeffBound_valid`,
`normalizeForFactor_squareFreeCore_primitive`, and
`squareFreeCore_leadingCoeff_pos`.

`#6726` is also still on `coordination list-replan`; its successor
`#6732` merged via PR #6739, so a separate triage pass should close
`#6726` with a forward link. Out of scope for this once-dispatched
session.

## Next step

A feature worker can claim #6719 and implement the slow-trial discharge
in `HexBerlekampZassenhausMathlib/Basic.lean`, then HO-1 capstone #6672
becomes retryable.

## Blockers

None. `/reflect` follows below; no PR for this replan session.
