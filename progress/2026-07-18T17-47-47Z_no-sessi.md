# Accomplished

- Resolved the final independent-review blocker for PR #8767 by narrowing the Phase-4 performance comparator to python-flint on the shared whole-polynomial surfaces; retained MPSolve as a local correctness oracle only.
- Scoped every HexRoots benchmark surface to either python-flint or the exact `no-comparable-surface-in-named-comparator` absence reason.
- Re-ran python-flint on the current fixed-separation ladder, committed the export, reconciled every report ratio, and removed the obsolete seeded-family SVG.
- Clarified the benchmark doctrine so expected divergence against a structurally different informational comparator is evidence rather than an automatic unresolved Concern.
- Re-profiled the final 131077-bit `runRefineTo` case: 4026 samples, 98.6% classified, with GMP division dominating.
- Passed Phase-4, DAG, conformance-matrix, Mathlib-free, full 9357-job build, and all 14 HexRoots benchmark verification checks.
- Obtained a fresh independent review confirming `done_through: 4` is now defensible with no substantive blocker.
- Rebased over the concurrent correction that demoted `runIsolate` after its `n⁵` derivation was found unsound; preserved its old ladder only as explicitly unregistered comparator-shape evidence, regenerated the canonical artifact, and profiled the final fixed degree-8 case (3822 samples, 97.6% classified).

# Current frontier

The comparator/doctrine amendment is ready to commit and push to PR #8767.

# Next step

Push the amendment, wait for refreshed GitHub CI, confirm mergeability, and merge PR #8767.

# Blockers

None.
