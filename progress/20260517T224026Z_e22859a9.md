# BZ Mathlib-free small-mod singleton transfer (#4839) — diagnosis and decomposition

## Accomplished

Claimed parent #4839, decomposed it into sub-issues #4850 (singleton Berlekamp
irreducibility in a Rabin-soundness-importing module) and #4851 (Mathlib-free
Gauss lemma + small-mod singleton assembly), and skipped the parent with a
breadcrumb comment.

After claiming #4850 and exploring the executable invariants, I diagnosed a
fundamental obstruction to its stated deliverable 2 (Mathlib-free
`gcd_monicModularImage_derivative_eq_one`): the executable `DensePoly.gcd`
is the raw last-nonzero remainder of the Euclidean algorithm (no monic
normalisation), so scaling both inputs by a unit `c` scales every
intermediate remainder by `c`, giving `gcd(scale c f, scale c g) = scale c
(gcd f g)`. For `monicModularImage f = scale (lc f)⁻¹ f`, the executable
gcd with the derivative therefore equals `DensePoly.C (lc f)⁻¹`, not `1`,
unless `f` was already monic.

Concrete counterexample worked out over `F₅`:

- `core = 2X² + 1`, `p = 5`.
- `isGoodPrime` accepts: `p ≥ 3`, `lc 2 mod 5 = 2 ≠ 0`,
  `gcd(2X²+1, 4X) = 1` (exact executable equality).
- `monicModularImage(modP 5 core) = scale 3 (2X²+1) = X² + 3`.
- `gcd(X²+3, 2X) = 3` (not `1`).

The Mathlib-side analog (`gcd_monicModularImage_derivative_eq_one` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean:308`) is unsound at the
relevant step: it routes through `HexBerlekampMathlib.toMathlibPolynomial_gcd`
(`HexBerlekampMathlib/Basic.lean:263`), which is currently `sorry` and is in
fact wrong as an executable equality, because Mathlib's `gcd` normalises but
the Hex executable `DensePoly.gcd` does not.

Skipped #4850 with a detailed comment outlining a relaxed-signature
reformulation: factor a `berlekampFactor_singleton_irreducible` variant in
`HexBerlekamp.RabinSoundness` that takes a relaxed square-free hypothesis
(`isUnitPolynomial (gcd f f') = true` or equivalently
`∀ g, g ∣ f → g ∣ f' → isUnitPolynomial g`), which propagates cleanly through
`isUnitPolynomial_of_squareFree_of_squared_dvd` and
`common_dvd_one_of_squareFree_mul`. The relaxed hypothesis is what
`monicModularImage (modP p core)` actually satisfies under `isGoodPrime`.

Annotated #4851 with a note that its plan (Gauss lemma + assembly) should be
re-checked against whatever shape #4850 ends up taking — if #4850 lands an
`FpPoly.Irreducible` witness on `modP p core` directly rather than on
`monicModularImage (modP p core)`, the bridging in #4851 simplifies.

No code changes in this session. `.claude/CLAUDE.md` shows a worktree-only
pod-injected diff which is environment-local.

## Current frontier

- #4839 parent: `replan` (decomposed into #4850 and #4851).
- #4850 sub-issue: `replan` after the diagnosis above. Needs reformulation
  around a relaxed Berlekamp-soundness signature; the planner should decide
  whether to rewrite the body in place or close and open a successor.
- #4851 sub-issue: `blocked` on #4850; plan may need to follow the
  reformulation outcome.

## Next step

The next planner triages the three replan issues. The recommended direction
on #4850 (or successor) is:

1. Add `isUnitPolynomial_of_dvd_isUnitPolynomial`-routed variants of
   `isUnitPolynomial_of_squareFree_of_squared_dvd` (line 2378),
   `common_dvd_one_of_squareFree_mul` (line 2431),
   `exists_reduced_crtZeroOne_kernelWitness_of_squareFree_split` (line 2453),
   `exists_reduced_crtZeroOne_kernelWitness_of_squareFree_monic_split`
   (line 2883), `irreducible_of_no_kernelWitnessSplit_squareFree` (line 3003),
   and `berlekampFactor_singleton_irreducible` (line 3330), all in
   `HexBerlekamp/RabinSoundness.lean`, taking the relaxed hypothesis.
2. In a new module (`HexBerlekampZassenhaus/SmallModSingleton.lean` or
   similar) importing the new variant plus
   `HexBerlekampZassenhaus.Basic`, derive the relaxed hypothesis on
   `monicModularImage (modP p core)` from `isGoodPrime core p = true` via
   the divisibility identity `g ∣ scale c f ↔ g ∣ f` (for unit `c`).
3. Expose the resulting `FpPoly.Irreducible (monicModularImage (modP p core))`
   theorem for downstream Gauss-lemma assembly in #4851 (or its successor).

## Blockers

The Mathlib-free Gauss lemma assembly in #4851 is independently blocked on
new Mathlib-free substrate: `ZPoly.modP_mul`, `DensePoly.content_mul`, and
leading-coefficient / degree preservation under `ZPoly.modP` when the
leading coefficient is admissible. None are in the codebase yet.
