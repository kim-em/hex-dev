# 2026-06-01T10:43Z - HO-1 Option API record-shape contracts (#2564 partial)

## Accomplished

- Claimed directive issue #2564 and advanced the explicit-failure `Option`
  API surface for the Berlekamp-Zassenhaus factoring path.
- Added success-equality transport lemmas for `factorWithBound?` and `factor?`.
- Added scalar, positive-multiplicity, sign-normalization,
  positive-leading-coefficient, recording-filter, and no-duplicate-key
  contracts for successful `factorWithBound?` / `factor?` outputs.
- Verified `lake build HexBerlekampZassenhaus`, `lake build`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the added-line
  forbidden-token grep for `HexBerlekampZassenhaus/Basic.lean`.

## Current frontier

- This is a partial HO-1 contribution. It does not remove remaining total
  fallback call sites, implement the full van Hoeij CLD rewrite, or prove the
  five-clause Mathlib headline correctness theorem.
- The new contracts make the `Option`-returning no-admissible-prime surface
  easier to consume downstream for the scalar, multiplicity, and
  no-duplicate-key clauses.

## Next step

- Thread the successful-`Option` contracts into Mathlib-side callers that can
  propagate explicit no-prime failure, then continue reducing direct reliance
  on `choosePrimeDataWithFallback` in production factoring branches.

## Blockers

- Full discharge of #2564 remains larger than this session: it still requires
  the BHKS van Hoeij CLD headline proof architecture and closure of the
  remaining `HexBerlekampZassenhausMathlib/` obligations.
