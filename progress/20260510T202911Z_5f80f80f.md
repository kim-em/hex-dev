# 2026-05-10 20:29 UTC — work session (5f80f80f)

## Accomplished

- Claimed #3121 and added the residual derivative-zero API shape in
  `HexPolyFp/SquareFree.lean`:
  - `yunFactorsResidualDerivativeZero`
  - `yunFactorsContributionResidualDerivativeZero`
  - bridge lemmas from contribution residual facts to the concrete
    `yunFactors` loop residual.
- Verified the change with:
  - `lake build HexPolyFp.SquareFree`
  - `lake build HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`

## Current frontier

- The API now exposes the exact `loop.2` residual derivative-zero fact
  expected by the blocked tail-cross issue, but it still depends on a
  contribution-level completion invariant.
- Existing proof holes remain in the product reconstruction cluster and
  `yunFactors_squareFreeAuxRev_tail_cross_coprime`; no new `sorry` was added.

## Next step

- Prove the contribution-level residual completion invariant for the
  derivative-active caller shape, then pass the resulting
  `yunFactorsResidualDerivativeZero` fact into #3122's narrowed tail-cross
  theorem.

## Blockers

- The fully mathematical residual completion argument is still missing; this
  session only landed the loop/contribution alignment API needed to state it
  cleanly.
