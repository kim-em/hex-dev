# Session 2be1d372 — feature #5072

## Accomplished

Rewired 4 inline `cases h : <poly>.isZero` reverse-direction blocks in
`HexBerlekampZassenhausMathlib/Basic.lean` onto
`Hex.DensePoly.isZero_eq_false_iff.mpr`:

| Site | Polynomial               | Size-positivity hypothesis |
| ---- | ------------------------ | -------------------------- |
| 1    | `g₁`                     | `hg₁_size_pos`             |
| 2    | `g₂`                     | `hg₂_size_pos`             |
| 3    | `h`                      | `hh_size_pos`              |
| 4    | `Hex.ZPoly.modP p f`     | `hmodP_size_pos`           |

Sites 1–3 collapse `have hX_isZero : X.isZero = false := by cases hz : ...`
8-line blocks to a 2-line `:=` term. Site 4 is the outer goal of
`modP_isZero_false_of_monic`; the `cases` block IS the proof body, so it
collapses to a single `exact (Hex.DensePoly.isZero_eq_false_iff _).mpr
hmodP_size_pos` line (no enclosing `have`).

`git diff --stat`: `Basic.lean | 42 ++++++-------- ; 1 file changed,
7 insertions(+), 35 deletions(-)` (net −28).

Greppable invariants:

- `cases hz?\b ... .isZero` in `Basic.lean`: 4 → 0 (the unchanged
  `Basic.lean:1318` site uses `cases hzero :`, not `cases hz :`, so it
  did not match the regex even before this PR — the issue's predicted
  baseline of 5 was off by one but the semantic invariant holds: the
  one surviving inline-cases site is the out-of-scope
  FpPoly-divisibility site).
- `isZero_eq_false_iff` in `Basic.lean`: 4 → 8 (+4).
- `Array.isEmpty_iff_size_eq_zero` in `Basic.lean`: 6 → 2 (−4).
- `simpa [Hex.DensePoly.isZero, …]` in
  `HexBerlekampZassenhausMathlib/`: 1 → 0 (site-4 dual flagged by
  #5055's `420e42bd` progress note).

`lake build HexBerlekampZassenhausMathlib.Basic` and `lake build
HexBerlekampZassenhausMathlib` both reproduce the 16-error baseline:
11 whnf timeouts + 1 dependent-elimination `cases` at line 5825 + 4
kernel unknown constants. `python3 scripts/check_dag.py` exits 0;
`git diff --check` clean; no new `sorry`/`axiom`/`native_decide`/`TODO`.

## Current frontier

BZMathlib's reverse-direction `isZero = false` plumbing is now
consolidated on `Hex.DensePoly.isZero_eq_false_iff.mpr` for every
site whose size-positivity is in scope. The only remaining inline
`cases ... isZero` block in `Basic.lean` is the
FpPoly-divisibility site at `Basic.lean:1318`, which derives
`isZero = false` from `factor ∣ core` + `core ≠ 0` rather than from
a size hypothesis; it needs a different rewire path
(`Hex.isZero_false_of_ne_zero` composed with a
divisibility-preserves-nonzero step in `FpPoly`).

## Next step

The sibling cleanup #5073 (`IntReductionMod.lean:1500`
`by_cases hderiv` block onto `Bool.eq_false_iff.mpr`) closes out
the BZMathlib reverse-direction cascade once #5068
(`IntReductionMod.lean:1446`, currently claimed) lands.

After both land, a planner cycle can file the residual
FpPoly-divisibility rewire at `Basic.lean:1318`.

## Blockers

None.
