# Capstone re-diagnosis: HO-1 fast-BHKS-core arm is separation-free

## Accomplished

- **Salvage #8064 resolved as no-op.** The van Hoeij floor-skip/hoist fix
  (`cldCoeffFloorGate` / `factorFastCoreLoop` / `factorFastCoreWithBound_unfold`)
  already landed on main via PR #8076. Cherry-pick of bf0036c6 produced an empty
  commit. #8064 stays open only for the deeper high-degree scaling concern.

- **Packed-UInt64 chain confirmed self-progressing.** #8251 (packed monic kernel)
  running in pod; unblocks #8248, then #8249. The earlier `pod once 8248` was
  correctly skipped (depends on unlanded #8251). No re-dispatch needed.

- **Corrected the capstone #8068 diagnosis (the main result of the turn).**
  The prior triage marked #8068 `blocked on #6779/#2564`. Source audit shows the
  fast BHKS-core arm closes through the *forward* cut route (`W ⊆ L'`),
  separation-free:
  `factorFastCoreWithBound_some_factor_irreducible_of_cut`
  (PartitionRefinement.lean:659, "no dependency on the reverse L'=W separation")
  → `cutProjectionHypotheses_of_trueFactors` (Lattice.lean:2132, complete) →
  two per-support certificates. The prior triage traced only the `recoveredLift`
  consumer wrapper (which does need L'=W). Posted the corrected critical path to
  #8068, removed the `blocked` label.

- **Mapped the full obligation graph** (via Explore + direct reads). The capstone
  `factor_irreducible_of_nonUnit` (FactorSoundness.lean:18) reduces via
  `factor_entries_irreducible` (Basic.lean:443) to a single `h_raw`. Every arm is
  CLOSED (constant, small-mod-singleton #4564, quadratic #4571, slow-modular
  IntReductionMod.lean:6423, slow-trial) EXCEPT the FAST-BHKS-core arm. So Kim's
  "(a)/(b) singleton+quadratic" producers are already landed; the entire remaining
  capstone is one directive.

- **Rewrote #8068** to the accurate forward-cut producer directive and re-dispatched
  via `pod once` (PID 44041, 'work'). Deliverables: ForwardRecoveryInputs producer,
  hcut producer (the math heart: TrueFactorLift + TightColumnBound -> the complete
  `tightNormBound_of_lift` / `fourMulNormSq_le_of_proj_col`), hpartition,
  scheduled-loop determinism (`_on_schedule`), outer wiring into h_raw.

## Current frontier

The capstone's only open arm is the FAST-BHKS-core forward-cut producer. The
consumer `factorFastCoreDefault_factor_zpolyIrreducible_of_forwardInputs`
(PartitionRefinement.lean:1114) is built; the worker must produce its inputs from
a genuine fast-core success. The deepest piece is building `TrueFactorLift` +
`TightColumnBound` (the BHKS forward CLD-shortness substrate) from the executable
lift data; everything above it is sorry-free.

## Next step

Watch the #8068 worker (PID 44041). If it lands the forward-cut producer, the HO-1
capstone closes. If it finds an unsound obligation, expect a concrete missing-lemma
comment (not a #6779 detour). Separately, packed-UInt64 #8248/#8249 unblock when
#8251 lands.

## Blockers

None on the capstone's critical path. #6779/#2564 are confirmed OFF it (they remain
relevant only to the `recoveredLift` route and other consumers).
