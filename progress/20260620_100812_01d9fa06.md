# HexMatrix Phase 6 closeout — exit-criteria re-check and done_through 5→6 (#8238)

Session type: feature. UTC 2026-06-20T10:08.

## Accomplished

Re-confirmed the four Phase 6 exit criteria for `HexMatrix`
(`mathlib: false`, deps `[]`) on the post-#8230 tree, fixed the one gap
the re-check turned up, and bumped
`libraries.yml[HexMatrix].done_through: 5 → 6`.

Evidence per criterion:

- **Linter clean (criterion 1) → pass.** Deleted the `HexMatrix/*`
  library oleans to defeat a cache-hit false-clean, then
  `lake build HexMatrix`: 0 `warning:`, 0 `error:`. The only non-`✔`
  output is 11 `info: Try these:` grind pattern suggestions from
  `Bareiss.lean` (`@[grind]`-tagged theorems); these are info-level
  suggestions, not warnings, and predate this work.
- **Docstring coverage (criterion 2) → pass.** Scanned every public
  `def`/`theorem`/`structure`/`class`/`instance` in the importable
  surface (`Basic`, `Determinant`, `RowEchelon`, `RREF`, `Bareiss`,
  `Conformance`): 0 missing docstrings. `Bench.lean` (25) and
  `EmitFixtures.lean` (1) defs lack docstrings but are roots of separate
  `lean_exe` targets (`hexmatrix_bench`, `hexmatrix_emit_fixtures`),
  imported by nothing — executable scaffolding outside the importable
  library, and explicitly out of the issue's "core math surface and
  public API" scope.
- **Dead code (criterion 3) → fixed, then pass.** A repo-wide
  reachability scan over the 504 private declarations (attribute-tagged
  simp/grind/instance treated as roots) found **9 unreferenced private
  theorems** in `Determinant.lean`:
  `detProduct_congr_matrix`, the four `insertAt_*_get_*`
  prefix/boundary/last lemmas (1002-1030 region),
  `colReplace_columnChoiceMatrix_none_self`,
  `colReplace_columnChoiceMatrix_set_some`,
  `colReplace_columnSumMatrixWithPrefix_extend`, and
  `cofactorSign_succ_last_eq_neg`. Removed them; rebuild green. A second
  scan caught **3 cascade orphans** (helpers whose only users were the
  deleted lemmas): `insertAt_get_castSucc_of_lt`,
  `insertAt_get_last_of_castSucc_last`, `setColumnChoice`. Removed those
  too; rebuild green; a third scan converged clean (0 remaining). Total:
  12 dead declarations, 177 lines. Helpers still in use
  (`insertAt_get_self`, `foldl_det_product_congr`,
  `list_getElem_insertIdx_succ_of_le`, the `@[grind =]`
  `columnChoiceMatrix_entry`) were verified live and kept.
- **Native path / irreducibility (criterion 4) → pass.**
  `grep -rn native_decide HexMatrix` empty; no `sorry`, no `axiom`.
  Irreducibility / field-construction N/A — `HexMatrix` is a
  determinant / linear-algebra library and exposes no such claims.
- **Performance regression check → unaffected.** All removed
  declarations were unreferenced proofs, not part of the executable
  Bareiss path, so the computational path is byte-identical. No bench
  run needed for a dead-proof removal.

## Verification

- `lake build HexMatrix` green (389 jobs), 0 warnings, 0 errors.
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` lists `HexMatrix` at "Phase 7
  (user-facing documentation)" / done_through 6.
- `git diff` touches only `HexMatrix/Determinant.lean` (177 deletions)
  and `libraries.yml` (the `5 → 6` bump).

## Current frontier

`HexMatrix` is now Phase 6 complete and eligible for its Phase 7
reference chapter (`HexManual/Chapters/HexMatrix.lean`), a separate
future issue.

## Next step

A planner can file the `HexMatrix` Phase 7 chapter issue (the bump
unblocks it). Unrelated unclaimed work remains: #8231 (HexGFq Phase 7
chapter), #8237 (HexGramSchmidtMathlib docstrings).

## Blockers

None.
