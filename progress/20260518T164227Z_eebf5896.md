# Feature session — issue #5034 (normalizeForFactor product-primitivity helper)

## Accomplished

Closed out the post-#4991 helper-extraction cascade on the
`normalizeForFactor`-decomposition substrate by extracting the
product-form primitivity helper sibling of the squareFreeCore-only
helper that landed in PR #5012.

Added `normalizeForFactor_squareFreeCore_mul_repeatedPart_primitive_of_ne_zero`
to `HexBerlekampZassenhausMathlib/IntReductionMod.lean` and rewired
the three call sites that previously inlined the 10-line
`hcore_ne` / `hprod_prim` boilerplate:

* Site 1 — `normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive`
* Site 2 — `normalizeForFactor_repeatedPart_toPolynomial_isPrimitive`
* Site 3 — inside the `hcore_sq_ne` derivation in
  `normalizeForFactor_squareFreeCore_toPolynomial_squarefree`
  (also dropped the now-unused outer `hcore_ne` block at lines
  941-943 and the obsolete `Executable SquareFreeRat invariant`
  comment).

## Placement deviation from issue

The issue specified placing the helper "adjacent to the existing
helper `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`
(around line 1060)". That placement is dependency-incompatible:
the three call sites at lines 663/687/935 precede the existing
helper at 1052, so a sibling at ~1080 would be a forward
reference. Placed the new helper just after
`toPolynomial_isPrimitive_of_zpoly_primitive` (line 633) instead,
which is the closest sensible position that precedes site 1. The
existing helper at line 1052 remains in place — its call sites
(2498, 2986, 3086) are all downstream of its definition.

## Verification

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` —
  16-error baseline unchanged (all in `Basic.lean`, none in
  `IntReductionMod.lean`); 2 pre-existing `sorry` warnings at
  `Basic.lean:15226/15321` carry over.
* `lake build HexBerlekampZassenhausMathlib` — same 16-error
  baseline, no new errors elsewhere.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` introduced.
* Grep counts match issue:
  * `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`:
    4 → 2 (new helper body + docstring on
    `_toPolynomial_isPrimitive`).
  * `extractXPower_core_ne_zero_of_ne_zero`: 4 → 2 (new helper
    body + the `set core`-style site at 1389).
* Diffstat: 1 file changed, +20 / -31 (issue predicted ~+12 / -27;
  larger -count because also dropped the obsolete outer
  `hcore_ne` block + comment at site 3).

## Current frontier

Issue #5034 complete; ready to push and create PR.

## Next step

`coordination create-pr 5034`. The other unclaimed issue (#5035,
`zpoly_dvd_trans` helper extraction in `Basic.lean`) remains
available for the next worker.

## Blockers

None.
