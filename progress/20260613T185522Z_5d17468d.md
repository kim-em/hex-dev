# Progress: #6858 — explicit-witness resultant divisibility

## Accomplished

Added the #6858 composition theorem in
`HexBerlekampZassenhausMathlib/Resultant.lean`:

- `det_dvd_of_cols_dvd`, a determinant helper showing that any injected family
  of `d` entrywise-`m`-divisible columns gives `m ^ d ∣ det`.
- `commonFactor_dvd_resultant`, which instantiates the #6857 Sylvester column
  reduction from explicit witnesses `f = q*a + C m*r` and `g = q*b + C m*s`,
  applies the determinant divisibility helper to the pivot columns, and rewrites
  the Sylvester determinant to `Polynomial.resultant`.
- `commonFactor_dvd_resultant_of_monic_cofactor`, the caller-friendly monic
  cofactor form deriving the pivot coefficient from `b.Monic` and the exact
  cofactor degree equality.

## Current frontier

#6858 is implemented and verified locally. The theorem remains Hensel/CLD-free
and over `Polynomial ℤ`; the downstream public map-to-`ZMod (p^k)` wrapper is
still #6849.

## Next step

Use the new monic-cofactor resultant divisibility theorem from #6849 to package
the p-adic/common-factor wrapper needed by the BHKS bad-vector chain.

## Blockers

None for this session. The local build still reports pre-existing lint warnings
in surrounding Resultant code and HexPolyZ, but the target builds successfully.
