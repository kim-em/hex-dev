# Accomplished

- Completed all 108 preregistered schema-v5 proof-probe pairs on designated
  shared host `chungus2`; the host protocol admitted the run but fail-closed
  validation exposed two unsound preregistered tactic budgets and a redundant
  high-magnitude null control.
- Posted the concrete counterevidence on directive issue #9025 rather than
  weakening validation or committing the failed artifact.
- Added `HexRCF.ProofProbe.Control`, which runs two independent degree-50
  `by rcf` elaborations and composes their results for an honest expensive
  same-module null control. A direct diagnostic measured it at 2.559 times the
  baseline build magnitude on `chungus2`.
- Bumped the proof-sweep contract to schema v6, replaced the redundant null,
  and set measured-plus-headroom regression bounds of 2 s / 12 s / 30 s from
  the complete v5 evidence.
- Corrected the SPEC's cost interpretation: kernel replay, carrier degree,
  atom count, and coefficient growth explain the observed ordering better
  than nominal atom degree alone.
- Passed the scientific and ordinary proof-probe builds, all 67 sweep tests,
  Phase-4/DAG/trust-surface/Mathlib-free-bench checks, and diff validation.
- Obtained an independent Sol GO on the exact corrective diff.

# Current frontier

The schema-v6 contract correction is ready to commit and open as a milestone
PR. It deliberately contains no release artifact or phase-status advance.

# Next step

Merge the corrective PR after exact-head review and CI, then run schema v6 on
`chungus2`, regenerate all current RCF evidence, and complete the Phase-4
report/status milestone.

# Blockers

None.
