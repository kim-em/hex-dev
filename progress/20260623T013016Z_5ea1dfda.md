# Core-coordinate fast recovery (additive) — #8300

Session type: feature (one-shot `/once`). UTC 2026-06-23T01:30.

## Accomplished

Landed step 1 of the #8297 additive remodel (parent #8293): a core-coordinate
(van Hoeij `M1`) fast recovery **alongside** the existing M2 path, with nothing
rerouted, in `HexBerlekampZassenhaus/Basic.lean` (Mathlib-free layer, +119 lines,
purely additive). `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
green (498 + 3784 jobs). No `sorry`/`axiom`/`native_decide` in the diff (the two
`HexBerlekampZassenhausMathlib` sorries are pre-existing, in files I did not touch).

New executable definitions, mirroring the `bhksRecover?` family but built on the
`monicTarget` lift coordinate:

- `bhksIndicatorCandidateCore?` — per-indicator reconstruction producing the M1
  candidate `normalizeFactorSign (primitivePart (centeredLiftPoly ((ℓf · ∏ selected)
  % p^k) modulus)))`, the executable image of `RecoveredAtLiftM1.candidate_eq`
  (#8298). No `dilate` — the recovery lands in `core`'s own coordinate.
- `bhksIndicatorCandidatesCoreStep` / `bhksIndicatorCandidatesCore?` — the plural
  fold over the RREF indicator vectors.
- `bhksRecoverClassifiedCore` — the classifier, sharing the CLD lattice / indicator
  partition machinery verbatim, differing only in the per-indicator candidate shape.
- `coreRecover?` — the public `Option (Array ZPoly)` surface (`.toOption` of the
  classifier), the coordinate-faithful counterpart to `bhksRecover?`.

Smoke checks (`private coreRecoverSmoke?` driver + eight `#guard`s) exercise
`coreRecover?` end-to-end on the non-monic multi-factor inputs `(2x+1)(x⁴+1)`,
`(3x+2)(x²+2)`, `(6x²-1)(x+5)`, `2x²-3x+1`, selecting the good prime via
`choosePrimeData?` and lifting through `coreLiftData` at `factorFastPrecisionCap`.
All four recover the expected factors in `core`'s own coordinate and multiply
back to the input.

## Current frontier

The core recovery executable now exists, is green, and is verified on real inputs
— but dead: `factorFastCoreLoop` still routes through `bhksRecoverClassified` /
`toMonicLiftData` (M2). `cldCoeffFloor`, `factorFastCoreWithBound`'s call sites,
and every Mathlib recovery/floor proof are untouched. Public `factor` and the
conformance/`hexbz_bench verify` battery are unchanged.

## Next step

The deferred chain recorded on #8300 (a planner files these):
2. Prove `coreRecover?`'s output is `core`'s factorization (or
   `coreRecover? core = bhksRecover? core`), reusing #8298's `RecoveredAtLiftM1`
   bridge lemmas. Green.
3. Swap `factorFastCoreWithBound`'s call sites onto the core recovery + the
   `cldCoeffFloor` ripple to the core-coordinate `bhksCoeffBound core` (#8292).
4. Remove the dead `toMonicLiftData` recovery path.

## Blockers

None.
