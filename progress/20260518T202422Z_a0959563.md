# Post-#5077 audit of `fpPoly_dvd_trans` cluster — PASS

Issue: https://github.com/kim-em/hex/issues/5082
Audit comment: https://github.com/kim-em/hex/issues/5082#issuecomment-4481774865
Audit target tree: `df8da6df0ce6bbe97c0519ac70a02a148ad3e085` (post-#5083, which
closed #5077).

## Accomplished

Posted the audit comment on #5082. All three invariants pass.

- **Invariant 1 — call-site coverage**: 8 `Hex.FpPoly p`-level divisor-
  transitivity sites in `HexBerlekampZassenhausMathlib/Basic.lean` are
  routed through `fpPoly_dvd_trans` (`1358`, `1383`, `4916`, `4972`,
  `5058`, `5684`, `5758`, `5835`). All remaining `mul_assoc_poly` /
  `Hex.FpPoly.mul_assoc` / `refine ⟨_ * _, ?_⟩` matches in the BZMathlib
  tree classify cleanly as helper-internal (`Basic.lean:1296`, `1298`,
  docstring `1288`), the parallel `zpoly_dvd_trans` cluster's helper-
  internal site (`Basic.lean:2064`), an algebraic-associativity
  rearrangement inside `hrawGcd_sq_dvd_prod` to build the product-of-
  divisors-divides-product witness (`Basic.lean:5661`, `5666`, `5669`,
  `5675`, `5677`) — the actual transitivity step in that proof is
  already routed through `fpPoly_dvd_trans` at `5684` — or a ZPoly-level
  one-step `x ∣ y → x ∣ z * y` derivation in `dvd_foldl_one_of_mem`
  (`IntReductionMod.lean:2549-2552`) which is the wrong shape and the
  wrong layer for this helper.
- **Invariant 2 — framing, docstring, hoist**: `private theorem
  fpPoly_dvd_trans` declared at `Basic.lean:1287-1298` with a contract-
  first docstring that names the bespoke `instDvdOfAddOfMul` instance.
  No PR-number / session-metadata / provenance references. Hoist is
  minimal: the immediately-preceding `theorem modP_dvd_modP_of_dvd`
  does not reference the helper; the immediately-following
  `monicModPImage_dvd_monicModularImage_of_dvd_of_choosePrimeData?_some`
  is the lowest call site.
- **Invariant 3 — cross-file consistency**: 9 occurrences in
  `Basic.lean` (1 def + 8 uses), 0 in `IntReductionMod.lean`, 0 in
  `Recovery.lean`. Exactly one `private theorem fpPoly_dvd_trans`
  declaration in the tree.

#5083's worker rewired 8 sites — three more than the issue body's
projected upper bound of 5 (3 rcases-form + 2 calc-form). The
additional sites are genuine `Hex.FpPoly p`-level transitivity
derivations the pre-#5077 planning pass had not enumerated. This is
over-coverage, not deviation.

## Current frontier

The `fpPoly_dvd_trans` cluster is closed and matches the parallel
`zpoly_dvd_trans` cluster (audited in #5048/#5057/#5069/#5076) in
framing, hoist, and coverage discipline. No follow-up issue required
from this audit.

## Next step

None for this audit. A future planner cycle may consider whether the
algebraic-associativity rearrangement on `Basic.lean:5661-5677` is a
candidate for a separate `fpPoly_mul_dvd_mul` extraction, but that is
outside the scope of the `fpPoly_dvd_trans` cluster.

## Blockers

None.
