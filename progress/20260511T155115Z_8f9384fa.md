# Work session 8f9384fa — partial HexGfq degree-16 packed witnesses

## Accomplished

- Checked the unclaimed queue and attempted human-oversight directives first; all available directives were blocked or marked for replan.
- Claimed #3462, found its placement conflicted with `SPEC/Libraries/hex-matrix.md` because the Bareiss correctness proof infrastructure lives in `HexMatrixMathlib`, and skipped it for replan.
- Claimed #3461 and discharged the degree-16 packed `GF2Poly.Irreducible` witnesses in `HexGfq/CrossCheck.lean` and `HexGfq/EmitFixtures.lean` by reusing `GF2Poly.gf65k_modulus_irreducible`.
- Tried local Berlekamp certificate proofs for the degree-16 generic and degree-32 witnesses, but backed them out after they made `HexGfq.CrossCheck` spend minutes in kernel reduction and still time out.
- Verified the partial subset with `lake build HexGfq.CrossCheck`, `lake build HexGfq.EmitFixtures`, `python3 scripts/status.py HexGfq`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned-marker scan.

## Current frontier

- #3461 is only partially complete: the degree-16 packed witnesses are done, while degree-16 generic and both degree-32 witnesses remain.
- The naive generated-certificate approach is not viable for these harness-local generic/high-degree proofs without either precomputed compact certificate data or a shared bridge from the packed GF2 irreducibility theorem to the matching `FpPoly 2` modulus.

## Next step

- Replan the remaining #3461 scope into smaller work: first prove or expose a reusable packed-to-generic irreducibility bridge for `Conway.packedGF2FpPoly`, then use it for the remaining generic witnesses; handle degree-32 packed separately with a compact certificate if no common witness already exists.

## Blockers

- None for the landed partial subset.
- The remaining witnesses need a proof strategy that avoids large kernel reduction in `HexGfq.CrossCheck`.
