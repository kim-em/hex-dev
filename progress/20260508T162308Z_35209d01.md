## Accomplished

- Claimed feature issue #2661 for HexBerlekampZassenhaus Factorization conformance fixtures.
- Added SPEC edge-case fixture coverage for signed scalars, zero/constants, `X`, `-X`, `X^2`, repeated `(X - 1)` factors, and signed content cases.
- Tightened `scripts/oracle/bz_flint.py` to require the new `Factorization` JSON shape, reconstruct products exactly, and reject constant, duplicate, nonprimitive, or negative-leading polynomial factors.
- Added a narrow quadratic integer-root factor split in `HexBerlekampZassenhaus.Basic` so the new `-X^2 + 1` and `2(X - 1)(X + 1)` oracle cases produce irreducible linear factors.
- Regenerated `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl`.

## Current frontier

- The BZ fixture stream now has 24 factor cases and the committed JSONL exercises the signed-scalar plus multiplicity representation directly.
- The low-bound CLD guard remains intact; the quadratic root split applies outside that existing `B = 1` fast-path guard and on the slow path.

## Next step

- Review and merge the PR for #2661, then continue with downstream BZ bridge/proof issues that depend on the Factorization conformance surface.

## Blockers

- None for this issue.
