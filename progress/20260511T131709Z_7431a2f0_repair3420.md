# 2026-05-11 13:17 UTC — repair session (7431a2f0)

## Accomplished

- Claimed PR #3420 for repair after `coordination list-pr-repair` reported it as the top failed candidate.
- Checked PR metadata: `build` failed on Linux, while `build-macos` and `conformance` passed, and the PR was mergeable against `main`.
- Read the failed CI log and confirmed the Lean build and benchmark smoke checks reached the LLL oracle setup path; the failure was repeated `curl: (22) The requested URL returned error: 504` while fetching the Isabelle LLL archive from Zenodo.
- Confirmed the PR branch itself only adds the BZ Phase 4 benchmark report, exported benchmark JSON, profile artefacts, and its prior progress note.
- Chose a minimal repair rerun commit rather than changing benchmark/report content for an external transient download failure.
- Verified the repaired branch locally with:
  - `python3 scripts/check_phase4.py`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `lake build`
  - the full CI benchmark smoke sequence from `.github/workflows/ci.yml`, including `scripts/oracle/setup_lll_isabelle.sh`, `hexlll_bench verify`, and `hexbz_bench verify`

## Current frontier

- PR #3420 still represents the first HexBerlekampZassenhaus Phase 4 benchmark evidence slice and does not claim Phase 4 completion.
- The original Linux CI failure reproduced as an external Zenodo 504 in the log, while the same oracle setup path completed successfully in local verification.

## Next step

- Commit this progress note, force-push the PR branch, and mark PR #3420 salvaged.

## Blockers

- The original failed CI run depended on Zenodo availability for `scripts/oracle/setup_lll_isabelle.sh`.
- `.claude/CLAUDE.md` remains dirty from pre-existing pod-managed content and was not touched for this repair.
