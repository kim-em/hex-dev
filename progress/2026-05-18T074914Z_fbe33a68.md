# 2026-05-18T07:49:14Z — fbe33a68

Session type: `feature` (auto-dispatched via `/work`).

## Accomplished

* Claimed and implemented issue #4956: added
  `reassemblyExpansionComplete_singleton_of_irreducible_of_pos_lc` to
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` (immediately
  after the monic-flavoured sibling at line 2298).
* Used Option B from the issue's deliverable: routed through the
  array-level non-monic public surface
  `Hex.expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
  (`HexBerlekampZassenhaus/Basic.lean:11777`, #4778), with
  no-tail-divisibility discharged by the generic
  `factorPower_cover_not_dvd_tail_of_irreducible_squarefree` helper
  (#4807). Option A (route through the companion leaf substrate
  #4955) was not available since that issue is claimed by a parallel
  agent.
* Singleton-shape preconditions (`hirr_arr`, `hprod`, `hnorm`,
  `hpos_lc_arr`, `hdegree_arr`, `hlen'`) mirror the existing
  monic-arm setup at lines 2188-2203. The `factorPower`-decomposition
  collapse and `hfuel'` use definitional reduction of
  `#[core].toList.zip [k]` to `[(core, k)]`, plus
  `Hex.ZPoly.one_mul_zpoly` for the residual `1 * factorPower core k`
  step.

## Current frontier

* PR `73cdb692` rebased onto current `origin/main`.
* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` cannot
  reach IntReductionMod because of the pre-existing
  `HexBerlekampZassenhausMathlib/Basic.lean` whnf-timeout pile (same
  baseline acknowledged in the issue). The error/warning set against
  baseline differs only in line numbers shifted by the two main-side
  commits I rebased over (#4954, #4957); no new errors.

## Next step

* Push the branch and open the PR closing #4956.
* The singleton-arm companion leaf substrate #4955 (claimed by a
  different agent) provides an alternative route (Option A); when it
  lands a future cleanup PR may switch this discharger to use the
  leaf rather than the array-level surface.

## Blockers

* None.
