# 20260518T233715Z — HexPoly Phase 6 additive simp wrappers

Session type: `/feature` worker on issue #5165.

## Accomplished

Added three typeclass-specialized `@[simp]` wrappers to
`HexPoly/Operations.lean`, completing the additive corner of the
Phase 6 simp surface:

- `coeff_add_semiring` (line 356) — `[Lean.Grind.Semiring S]`,
  discharges the `0 + 0 = 0` hypothesis of `coeff_add` via `grind`.
- `coeff_sub_ring` (line 379) — `[Lean.Grind.Ring S]` (rings
  supply additive inverses; semirings don't), discharges
  `0 - 0 = 0`.
- `coeff_neg_ring` (line 399) — `[Lean.Grind.Ring S]`, normalises
  `(0 : S) - p.coeff n` to `-(p.coeff n)`. The simpa form from the
  issue body didn't close; replaced with `rw [h]; grind`.

`lake build HexPoly.Operations` and `lake build HexPoly` succeed.
`python3 scripts/check_dag.py` passes. `git diff --check` clean,
no new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

Surface is now in place. Downstream call sites in
`HexPolyFp/{Compose,Basic,QuotientFrobenius}.lean` (per the issue
body) still plumb explicit zero-laws by hand and would simplify
using these new simp lemmas.

## Next step

Migration of the existing `HexPolyFp` call sites to drop the
explicit `hzero_add` / `hzero_sub` plumbing is a natural follow-up
Phase 6 review issue (out of scope here, per the issue).

## Blockers

None.
