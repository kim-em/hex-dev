# 2026-05-14 09:42 UTC — feature session (dabf4de8) on #3972

## Accomplished

Claimed #3972 (`HexGramSchmidt: prove no-pivot Bareiss leading-prefix
data projection`) and landed a substantial set of private helpers in
`HexGramSchmidt/Int.lean`. The synced-loop projection that the parent
assembly needs is proven, plus the auxiliary fuel/fixed-point/invariant
infrastructure required to lift it to `Matrix.bareissNoPivotData`:

- `leadingPrefix_stepMatrix_eq`: a single `Matrix.stepMatrix` update
  commutes with `Matrix.leadingPrefix` (proved by case split on the
  trailing / pivot-column-below / unchanged branches of `stepMatrix`).
- `noPivotLoop_sync_leadingPrefix_aux`: by induction on fuel, running
  `Matrix.noPivotLoop` on the full and on its `K`-leading prefix from
  two synchronized `BareissState`s preserves the bookkeeping fields and
  the leading-prefix matrix equation, while `fuel + state.step < K`.
- `noPivotLoop_id_at_done`: at the boundary, further fuel is a no-op.
- `noPivotLoop_id_at_singular_fixedpoint`: a state with
  `singularStep = some state.step` and a zero pivot at that step is a
  `noPivotLoop`-fixed point.
- `noPivotLoop_add`: fuel composition,
  `noPivotLoop (a + b) state = noPivotLoop b (noPivotLoop a state)`.
- `noPivotLoop_singular_inv`: structural invariant for runs starting at
  `singularStep = none`: the result is either still
  `singularStep = none`, or it has `singularStep = some k.val` for
  `k : Fin n` with `matrix[k][k] = 0`, `step = k.val`, and `k.val + 1 < n`.
- `noPivotLoop_full_eq_leadingPrefix_at_gramDetVecEntry`: the diagonal
  `(r, r)` entry and the `singularStep` field agree between the runs of
  `Matrix.noPivotLoop r` on the full Gram matrix and on its
  `(r+1)`-leading prefix — i.e., the projection at the executable
  `Matrix.noPivotLoop` level that is the heart of the requested
  `bareissNoPivotData` projection.

Build clean: `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt`,
`python3 scripts/check_dag.py`, and `git diff --check` all pass. Sorry
count unchanged in `HexGramSchmidt/Int.lean` (the 4 pre-existing holes
are still the only ones, no new `axiom`, `native_decide`, `TODO`, or
`FIXME`).

## Current frontier

The remaining piece is the final `Matrix.bareissNoPivotData`-level
wrapper around `noPivotLoop_full_eq_leadingPrefix_at_gramDetVecEntry`,
i.e., a single private theorem of the form

```
private theorem gramDetVecEntry_bareissNoPivot_full_eq_leadingPrefix
    (b : Matrix Int n m) (r : Nat) (hr : r < n) :
    gramDetVecEntry (Matrix.bareissNoPivotData (Matrix.gramMatrix b))
        ⟨r + 1, Nat.succ_lt_succ hr⟩ =
      gramDetVecEntry
        (Matrix.bareissNoPivotData
          (Matrix.leadingPrefix (Matrix.gramMatrix b) (r + 1)
            (Nat.succ_le_of_lt hr)))
        ⟨r + 1, Nat.lt_succ_self _⟩
```

The infrastructure for the proof is now in place — combine fuel
composition with the diagonal-entry sync, the boundary no-op for the
prefix's `(r+1)`-th fuel step, and the singular fixed-point lemma
applied via the singular invariant. The proof has not yet been written
in this session.

## Next step

A follow-up session can land the final `bareissNoPivotData`-level
wrapper using the helpers already in place. The case split on
`s_pref.singularStep` (delivered by `noPivotLoop_singular_inv`)
gives two regimes — singular at step `s ≤ r - 1` (both runs collapse to
the singular fixed point) and non-singular (boundary no-op for the
prefix, diagonal preservation past step `r` for the full case) — which
each yield a direct comparison of `gramDetVecEntry` values.

## Blockers

- None for the helper infrastructure landed in this session. The
  remaining wrapper is a bounded follow-up and does not require any
  new generic infrastructure beyond what is now in
  `HexGramSchmidt/Int.lean`.
