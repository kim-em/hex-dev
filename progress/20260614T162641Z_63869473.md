# Phase 6 docstrings: HexPolyFp/Quotient.lean C-reduction & power-sum cluster

Session type: feature (#7275)

## Accomplished

Added one-sentence backtick-led docstrings to all 11 private theorems in
the constant-reduction / `evalCoeffPowerSumUpTo` cluster of
`HexPolyFp/Quotient.lean` (lines ~1828–1955):
`evalCoeffPowerSumUpTo_le_extend_base`, `evalCoeffPowerSumUpTo_le_extend`,
`eval_eq_coeff_power_sum_upTo_bound`, `C_add_eq`, `C_sub_eq`, `C_mul_C_eq`,
`reduce_C_val_eq`, `reduce_C_add`, `reduce_C_sub`, `reduce_C_mul`,
`evalCoeffPowerSumUpTo_add`. Doc-only, no statement/proof/name/attribute
changes. `lake build HexPolyFp.Quotient` passes.

## Current frontier

Cluster fully documented.

## Next step

Other HexPolyFp doc batches target `Basic.lean` and `SquareFree.lean`.

## Blockers

None. Note for directive authors: the issue body said the docstring goes
*above* the `omit [...] in` line. That does not parse in Lean v4.30.0-rc2 —
a doc comment before `omit ... in` is rejected. The docstring must sit
*between* the `omit` line and the `private theorem` keyword. Placed it
there for the three `omit`-guarded lemmas.
