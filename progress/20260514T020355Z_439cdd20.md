# Session 439cdd20 — BZ factorSlowWithBound_product helper lemmas (#3889 → #3896, #3897 + partial PR)

## Accomplished

- Decomposed parent issue #3889 (HexBZ core: prove
  `factorSlowWithBound_product` unconditionally) into two sub-issues
  after determining the exhaustive branch needs a sign-cancellation
  argument that is substantial standalone infrastructure:
  - #3896: per-factor canonicality helpers + constant + quadratic
    branch dispatch lemmas;
  - #3897: exhaustive-branch dispatch + final assembly (depends on
    #3896).
- Skipped #3889 with the `Decomposed into #3896, #3897` breadcrumb.
- Claimed and partially executed #3896:
  - `signedContentScalar_zero`,
    `factorizationOfFactors_product_of_zero` (the f = 0 case lemma).
  - `xPowerFactorArray` canonicality lemmas (entries are always
    `ZPoly.X`, normalized + recordable).
  - `linearFactorForRoot` canonicality lemmas (leading coefficient
    1, size 2 polynomial, normalized + recordable).
  - `repeatedPartFactorArray` canonicality lemmas for `f ≠ 0`,
    deriving individual positivity of `repeatedPart` from new
    `HexPolyZ` lemmas.
  - In `HexPolyZ/Basic.lean`: added
    `leadingCoeff_squareFreeCore_nonneg` and
    `leadingCoeff_repeatedPart_nonneg`; promoted
    `ne_zero_of_primitive`,
    `leadingCoeff_ne_zero_of_ne_zero`, and
    `densePoly_eq_C_coeff_zero_of_size_le_one` from private to
    theorem-level visibility for downstream use.
  - Foundational `squareFreeCore_ne_zero_of_ne_zero` and
    `squareFreeCore_leadingCoeff_pos_of_ne_zero` lemmas.

## Current frontier

`factorSlowWithBound_product` is not yet proved. The remaining
ingredients for the sub-issue #3896 deliverable are:
- `squareFreeCore_eq_one_of_constant_of_ne_zero`: requires combining
  the per-factor positivity helpers with the
  `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`
  / `_leadingCoeff_pos` invariants. The Mathlib-free environment
  here lacks `by_contra` / `push_neg`, which made an initial draft
  fail; needs structural rewrites using `Nat.ne_of_gt` /
  `match`-on-cases / explicit `omega` instead.
- `factorSlowWithBound_product_of_constant_branch` and
  `factorSlowWithBound_product_of_quadratic_branch`: depend on the
  `squareFreeCore_eq_one` helper and on the per-factor canonicality
  lemmas this PR lands.

## Next step

Push the per-factor canonicality + zero-case helpers as a partial
PR closing #3896 with `--partial`, so the planner can create a
follow-up issue narrowed to the remaining
`squareFreeCore_eq_one_of_constant_of_ne_zero` + constant +
quadratic dispatch lemmas. Sub-issue #3897 (exhaustive branch and
final assembly) remains blocked on the constant + quadratic
dispatch sub-issue.

## Blockers

- No `by_contra` / `push_neg` in the Mathlib-free side, so any
  proof using those needs to be rewritten with `match` / `omega`
  / explicit case-splits.
