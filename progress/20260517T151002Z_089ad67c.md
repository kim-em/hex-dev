# Session 089ad67c — #3977 Nat-level diagonal synchronization

## Accomplished

Claimed #3977 after #4746 was claimed by another worker. Added the
Mathlib-free Nat-level diagonal synchronization
`scaledCoeffRows_diag_toNat_eq_gramDetVecEntry` in
`HexGramSchmidt/Int.lean`, instantiating the landed
`scaledCoeffArrayLoop_diag_matches` invariant at the initial Gram state.

Rewired `gramDetVec_eq_gramDet` to use the new Nat-level theorem directly
instead of passing through the old Int-valued diagonal capstone. Added the
public packaging theorem `scaledCoeffs_diag_toNat` for the same Nat-level
diagonal fact on `scaledCoeffs`.

Verification run:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Compared `sorry|axiom|native_decide|TODO|FIXME` scans for
  `HexGramSchmidt/Int.lean` and `HexMatrix/Bareiss.lean` against
  `origin/main`; no new entries.

## Current frontier

#3977 is only partially complete. The executable/Nat-level bridge required by
`gramDetVec_eq_gramDet` is now in place, but the old Int-valued diagonal
theorem still requires a nonnegativity bridge for the Gram no-pivot diagonal
slot.

## Next step

Plan or implement the bridge-layer nonnegativity support needed to recover the
unconditional Int-valued `scaledCoeffRows_diag_eq_gramDetVecEntry` /
`scaledCoeffRows_diag_eq_gramDet` capstone, then use it to discharge the
remaining `scaledCoeffs_diag` path.

## Blockers

The remaining Int equality is blocked on proving
`x = Int.ofNat x.toNat` for the relevant no-pivot Gram determinant diagonal,
which crosses into determinant/Gram nonnegativity infrastructure outside this
Mathlib-free slice.
