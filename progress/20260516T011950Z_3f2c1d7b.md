# Progress 20260516T011950Z — session 3f2c1d7b

Session type: review (`/review 4352`).

## Accomplished

Audited the in-tree polynomial-resultant API for the HO-4 / BHKS Group D
bad-vector proof route, as requested by #4352. Posted the audit comment at
https://github.com/kim-em/hex/issues/2567#issuecomment-4464966478.

Findings:

- `HexBerlekampZassenhausMathlib/Resultant.lean` already supplies definition,
  zero/gcd criterion (in ℤ→ℚ form), and the BHKS-shaped Hadamard bound
  `|Res(f, g)| ≤ ‖f‖₂^(deg g) · ‖g‖₂^(deg f)` on top of Mathlib's
  `Polynomial.resultant` and `Polynomial.sylvester`.
- `HexBerlekampZassenhausMathlib/BadVector.lean` chains the lower/upper bound
  pair into the executable BHKS Lemma 3.2 contradiction at
  `ExecutableBadVectorWitness.no_bhks_bad_setup_of_l2norm_upper_lt_divisor`.
- `BHKSBound.lean`, `Recovery.lean`, `TerminationBound.lean`, `Lattice.lean`,
  and `Basic.lean` consume the contradiction package; none references the raw
  resultant API directly.

No code changes. No new follow-up agent-plan issue: the remaining HO-4 work
(B7 indicator packaging #4348, and the not-yet-planned BHKS Lemma 3.2 leaf
producing an `IsBhksBadVectorSetup` from an actual failed-recovery CLD
vector) consumes this API rather than extending it.

Verifications run: `python3 scripts/check_dag.py` (clean), `git diff --check`
(clean). No Lean smoke probe needed — the surfaces are already imported by
the umbrella and used by `BadVector.lean`.

## Current frontier

Issue #4352 closed by the audit comment. HO-4's open frontier remains:

- #4348 (claimed by another session): cap-separation B7-indicator
  packaging in `Recovery.lean`.
- Unplanned: the BHKS Lemma 3.2 leaf that constructs an
  `IsBhksBadVectorSetup` from an actual failed `factorFast` recovery, then
  drives `no_bhks_bad_setup_of_l2norm_upper_lt_divisor` to contradiction
  at precision `≥ bhksBound f`.

## Next step

Whoever picks up the BHKS Lemma 3.2 leaf can consume the resultant
contradiction API directly. No bridge work blocks it.

## Blockers

None.
