# Ramanujan theta family restack

## Accomplished

- Restacked the complete bounded Ramanujan theta family onto the supported
  executable assembly without changing the checked provider, ordinary theorem
  adapters, source-correlated inventory rows, or conformance guards.
- Reauthenticated the combined Lake registration and verified the exact pinned
  PNT source, inventory tests, focused Lean targets, static checks, trust
  surface, and factor-sweep freshness.

## Current frontier

- The `[3, 599)` range certificate and point certificate at `599` replace the
  complete file-local Ramanujan native-check family and its local Chebyshev
  import through localized ordinary-kernel rewrites.
- All numerical tactic occurrences in the pinned PNT corpus are classified.

## Next step

- Treat the remaining FKS2Floor boundary as two provider projects: extend the
  checked theta sweep through `22027`, then add a checked exponential/polynomial
  affine-cover provider for the 24-piece cover.

## Blockers

- Hex does not yet provide the affine-form enclosure engine required by the
  pinned FKS2Floor `cover_cmp` declaration; the existing theta provider also
  stops below that file's required `22027` bound.
