# Core aggregate-residue cut surface — #8290

Session type: feature (one-shot `/once`). UTC 2026-06-23T05:50.

## Accomplished

Added the non-`RecoveredLift` BHKS short-vector/cut surface in
`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`:

- `BHKS.AggregateResidueData`, the per-support aggregate centered-residue and
  Mignotte-bound input shape.
- `BHKS.supportShortVectorData_of_aggregateResidue`, producing
  `SupportShortVectorData (bhksLatticeBasis f p a liftedFactors) S` directly
  over the concrete executable BHKS basis.
- `BHKS.cutProjectionHypotheses_of_aggregateResidue`, feeding those short
  vectors into `cutProjectionHypotheses_of_shortVectors`.

This bypasses the monic `RecoveredLift` record entirely and works for the
core-coordinate basis once the caller supplies aggregate-residue data. No
`sorry`, `axiom`, or `native_decide` was added.

Verification:

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound`
- `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
- `git diff --check`

`/reflect` was attempted but is not available as a shell command in this
session.

## Current frontier

The period-adjusted lattice/vector and forward-cut plumbing over the concrete
core basis is green. The remaining gap is proving `AggregateResidueData` for
true supports from the core/non-monic CLD residue route: scale invariance plus
`coreLiftData_liftedFactor_hensel_semantics`.

## Next step

Prove the core aggregate-residue lemma that instantiates
`AggregateResidueData (bhksLatticeBasis core p a liftedFactors) S core p a` for
each true support, using the #8307 per-factor Hensel congruence and the
non-monic coefficient bound `abs_factorDeriv_coeff_le_bhksCoeffBound`.

## Blockers

The full #8290 deliverable is not complete in this session: the aggregate
residue proof itself is still missing. The landed surface is intended as a
partial PR so the residual can be replanned cleanly.
