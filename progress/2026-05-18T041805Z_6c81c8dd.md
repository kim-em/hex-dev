# 2026-05-18 04:18 UTC — feature 6c81c8dd

## Accomplished

Closed issue #4921: added abstract-bound sibling
`exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
in `HexBerlekampZassenhausMathlib/Basic.lean`, and rewired the existing
core-shape theorem at the former line 11286 as a thin wrapper that
instantiates `B' := Hex.ZPoly.defaultFactorCoeffBound core`.

The new `_of_bound` body mirrors the existing core-shape proof verbatim
except at two call sites:

* candidate-side natDegree decomposition now routes through
  `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum_of_bound`
  (landed via #4920), threading `B'`, `hcore_lc_bound`, `hprecision`;
* per-factor natDegree identity now routes through
  `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`
  (landed via #4904), threading `B'`, `hvalid g hg`, `hcore_lc_bound`,
  `hprecision`.

The wrapper preserves the existing parameter order so the in-tree call
site at the old line 11576 (now shifted) inside
`mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
type-checks unchanged.

## Current frontier

Pure additive feature work; the wrapper's discharge derivations
(`hcore_size_pos`, `hcore_lc_bound`, `hvalid`) are textually identical
to the unscaled primitive wrapper at lines 9920–9997 in the same file,
consistent with the issue spec.

## Next step

The downstream scaled cover consumer
`mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
becomes plannable once #4907 (scaled per-factor bridge `_of_bound`,
separately claimed) lands. Not in scope here.

## Blockers

None. Pre-existing heartbeat-timeout / kernel-unknown-constant errors
on this file (lines 5803, 5925, 6073, 6226, 6245, 6560, 6624, 14542→14636,
14653→14747, 14860→14954, 14906→15000) are out of scope per the issue
verification section; their line numbers shifted by +94 after this edit,
consistent with the added theorem length, and no new errors appear.
