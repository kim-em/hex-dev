# 20260602T145150Z a2054704 — Issue #6344

## Accomplished

- Claimed feature issue #6344.
- Added two unconditional support-partition shape lemmas in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  `ForwardRecoveryInputs.supportPartitionByMinColumn_class_nonempty` and
  `ForwardRecoveryInputs.supportPartitionByMinColumn_class_lt`.
- Added a packaged canonical-supports recovery input record
  `BHKS.CanonicalRecoveryInputs f primeData` bundling every hypothesis
  consumed by `factorFast_ne_none_of_mignottePrecisionCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound`
  other than the `factorFast`-pipeline scaffolding.
- Added the `CanonicalRecoveryInputs.ofExpectedFactors` constructor
  discharging the two unconditional shape facts internally so callers
  only supply the genuinely mathematical inputs.
- Added the final wrapper
  `factorFast_ne_none_of_canonicalRecoveryInputs` composing the bundle
  with the canonical-supports recovery capstone.

## Current frontier

The canonical-supports recovery capstone now consumes a single
`CanonicalRecoveryInputs` package instead of the eleven separate
hypotheses required by the underlying wrapper. Successor BHKS
final-assembly work supplies a `CanonicalRecoveryInputs` value at the
actual `factorFast` cap lift by combining the bridge-data lattice
equality (already exposed by #6342) with the expected true-factor
package and per-class Mignotte product congruence.

## Next step

Construct a `CanonicalRecoveryInputs` value at the actual cap lift by
deriving `lattice_eq_indicators` from
`projectedRowSpan_eq_trueFactorIndicatorLattice_of_factorFastCapLift_bridge`
and supplying the `ExpectedTrueFactors` package via the UFD
factorization of the normalized square-free core.

## Blockers

- None for this packaging layer.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — passed.
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement` — passed.
- `lake build HexBerlekampZassenhausMathlib` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
