# 2026-06-02T03:37Z — HexPolyFp neutral-law simp pass

## Accomplished

- Claimed #5204 and marked it for replan: its prerequisite #5224 was closed as plan-stale after the requested `cldCoeffs <= bhksCoeffBound` theorem was disproved; replacement #6217 is still open and explicitly says #5204 must be adjusted.
- Claimed #5512 and marked it for replan for the same stale #5224 surface; its true-factor/precision bridge cannot choose hypotheses until #6217 resolves the CLD redesign.
- Claimed #6231 and completed the HexPolyFp neutral-law simp pass:
  - added `@[simp]` to `add_zero`, `zero_add`, `neg_zero`, `add_left_neg`, `add_right_neg`, `sub_zero`, `zero_sub`, and `sub_self` in `HexPolyFp/Basic.lean`;
  - left `sub_eq_add_neg` untagged to avoid changing subtraction into addition as a default simplification rule;
  - added a local `simp` smoke example in the neutral-laws block;
  - adjusted now-redundant proof script lines in `HexPolyFp/SquareFree.lean` and `HexPolyFp/Compose.lean` exposed by the stronger simp normal forms.

## Current frontier

The HexPolyFp simp surface is coherent across the full `HexPolyFp` library. Wider BHKS D1 work remains gated on the #6217 redesign decision for executable CLD coefficient semantics.

## Next step

Create the PR for #6231. Planner/replan should retarget #5204 and #5512 after #6217 lands or otherwise chooses the replacement CLD bound.

## Blockers

None for #6231. #5204 and #5512 are blocked/stale on #6217.
