# 2026-05-14 12:53Z — claim/feature session for #4018

## Accomplished

- Claimed #4018 (HexBerlekamp: prove singleton Berlekamp factorization
  implies Fp irreducibility).
- Oriented across `HexBerlekamp/Factor.lean`,
  `HexBerlekamp/Irreducibility.lean`,
  `HexBerlekamp/RabinSoundness.lean`,
  `HexBerlekamp/DistinctDegree.lean`,
  `HexBerlekampMathlib/Basic.lean`, and
  `SPEC/Libraries/hex-berlekamp-mathlib.md`.
- Determined the deliverable splits cleanly into a tractable
  loop-tracing half and a deep finite-field completeness half. The
  Mathlib-bridge SPEC documents the 4-step CRT-based completeness
  argument, and `HexPoly/Euclid.lean` exposes a Mathlib-free
  `polyCRT`, but the kernel-structure / `X^p - X` product
  infrastructure does not yet exist.
- Landed the loop-tracing half: in `HexBerlekamp/Factor.lean`,
  - `splitFirstFactor?_length_succ_of_some` — successful split
    extends factor list by one.
  - `berlekampFactorLoop_length_ge` — output length is monotone in
    input length.
  - `splitFirstFactor?_singleton_none_iff`,
    `splitWithWitnesses?_none_iff_forall` — unfolding lemmas.
  - Public:
    `kernelWitnessSplit?_none_of_berlekampFactor_factors_length_le_one`
    — `(berlekampFactor f hmonic).factors.length ≤ 1` implies every
    fixed-space kernel witness yields `kernelWitnessSplit? = none`.
- Created sub-issue #4023 for the algebraic completeness residual
  (no-kernel-split → irreducible for square-free monic, Mathlib-free).
- Left `Decomposed into #4023` breadcrumb on #4018.

## Current frontier

The parent #4018 will be `replan`-marked once the partial PR lands.
The structural lemma is in place; remaining work is the deep
finite-field completeness step (tracked in #4023) and final
trivial assembly of the parent target theorem.

## Next step

Land the partial PR with `--partial` to close out this session's work.
A future planner triages #4018 (likely by narrowing it to a one-line
assembly after #4023 lands, or closing it as covered).

## Blockers

None. #4023 is the residual; it is itself a substantial finite-field
theorem and will likely need further decomposition by the worker who
claims it.
