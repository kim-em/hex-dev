# Partial Option-native modular slow path (#6597)

## Accomplished

Claimed #6597 after skipping stale directive #2567 with a concrete issue
comment: current SPEC says unconditional `factorFast_terminates` is false for
the finite-prime implementation, and the target must be conditional on
`choosePrimeData?`.

Started the #6597 refactor in `HexBerlekampZassenhaus/Basic.lean`:

- added canonical Option-returning modular slow raw/API names:
  `factorSlowModularFactorsWithBound`,
  `factorSlowModularWithBound`, and `factorSlowModular`;
- removed the redundant `factorSlowModularWithBound?` /
  `factorSlowModular?` shim definitions and updated the three-tier
  `factorWithBound` dispatch to use `factorSlowModularWithBound`;
- added branch compatibility lemmas showing successful modular Option output
  agrees with the existing total raw modular view;
- restored a modular/trial agreement `#guard` on
  `exhaustiveNonMonicQuadraticGuard`;
- updated `HexBerlekampZassenhausMathlib/Basic.lean` bridge statement shapes
  from `factorSlowModularWithBound?` to `factorSlowModularWithBound`.

Verification run:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify --filter runFactor`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line grep for `sorry`, `axiom`, `native_decide`, `TODO`, `FIXME`

All passed. Existing warning output includes known dependency/bridge warnings.

## Current frontier

This is a partial refactor, not the full #6597 deliverable. The old total
compatibility names `factorSlow`, `factorSlowWithBound`, and
`factorSlowFactorsWithBound` remain so the existing proof layer and callers
continue to build. The canonical Option-valued modular names are now present
and the `?`-suffixed modular shims are gone.

## Next step

Continue #6597 by migrating the remaining callers/proofs off the total
compatibility names:

- pivot Bench / Conformance / CrossCheck total callers to `factorSlowTrial`
  or `factor`;
- update raw-source theorem statements to use
  `factorSlowModularFactorsWithBound f B = some rawFactors` instead of
  `rawFactors = factorSlowFactorsWithBound f B`;
- rename/product-proof compatibility lemmas to the final
  `factorSlowModular*` names and remove the old total modular surface.

## Blockers

None. The remaining work is mechanical but broad.
