# Progress — 2026-06-18 (session 8dfdd143, /once → #7872)

## Accomplished

Landed the aggregate CLD residue from a `RecoveredLift` in the monic coordinate
(#7872), in `HexBerlekampZassenhausMathlib/CLDColumnBound.lean`.

### Generic CLD residue bridge

Generalised `residue_cldQuotientMod_eq_phi_coeff` to
`residue_eq_phi_coeff_of_congr`: for a monic divisor `g ∣ f` whose precision
`p^a` separates the Mignotte bound, the centred residue of *any* `q` with
`g * q ≡ f * g' (mod p^a)` is the integer `Phi`-column coefficient. The
per-factor bridge is now the `q := cldQuotientMod f g p a` corollary; the
aggregate work instantiates it with `q := supportCldSum`, legitimate because
`supportCldSum` satisfies the same congruence against the *whole* factor.

### Headline: `recoveredLift_aggregate_residue`

For `RecoveredLift L S`, `leadingCoeff f = 1` (monic coordinate, so the `dilate`
in `recovered_eq` is the identity), monic `factor`, and per-selected-factor
monic/positive-degree/modular-cofactor data `hfac` (the Hensel factorisation
`∏ gᵢ ≡ f mod pᵃ`):

```
centeredResiduePow p a (∑ i ∈ S, centeredResiduePow p a ((cldQuotientMod f gᵢ p a).coeff j))
  = (phi (toPolynomial f) (toPolynomial factor)).coeff j
∧ |(phi (toPolynomial f) (toPolynomial factor)).coeff j| ≤ bhksCoeffBound f j
```

This is exactly the `hagg` / `hy` input of `two_mul_natAbs_sum_psiCut_period_le`
(#7869), with `y = phi(...).coeff j`, `B = bhksCoeffBound f j`.

Proof routes through CLD additivity (`supportProduct_cldSum_congr_of_factors`,
mirroring the `TrueFactorLiftSemantics` version but parametrised by explicit
per-factor hypotheses so it applies to `RecoveredLift`, which carries no semantic
package), `supportProduct ≡ factor (mod pᵃ)` from `recovered_eq` + `dilate_one`,
derivative-of-congruence transport (`congr_derivative`), and the generic residue
bridge at `g := factor`. Doctrine respected: the only integer-`Phi` object is
`phi(f, factor)` for the whole factor (legitimate, `factor ∣ f` from
`factor_mul`); no per-`gᵢ` integer columns, no raw `∏ gᵢ = factor`.

Supporting helpers added: `centeredResiduePow_emod_self`,
`centeredResiduePow_emod_eq`, `coeff_listSum`, `listSum_emod_eq`,
`sum_filter_univ_eq_listSum`, `congr_derivative`.

## Verification

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound` → 3772 jobs,
  `Build completed successfully`, 0 `error:`, no warnings in the file.
- `lake build HexBerlekampZassenhausMathlib` (the CI-gated library) → 3790 jobs,
  `Build completed successfully`, 0 `error:`.
- `git diff -U0 … | grep -iE 'sorry|axiom|native_decide'` on added lines → none.
- `python3 scripts/check_dag.py` → exit 0.

## Current frontier

`recoveredLift_aggregate_residue` is the `hagg`/`hy` producer for the
period-aware column bound. It is the weak aggregate residue (`psiCut` once after
summing) — the only thing a `RecoveredLift` can soundly support per the period
trap (#7866/#7867); the per-factor tight column remains a lattice-basis redesign.

## Next step

The period-aware column bound that chains `recoveredLift_aggregate_residue` into
`two_mul_natAbs_sum_psiCut_period_le` (the "next" consumer named in #7872), under
the parent #7870.

## Blockers

None for this issue.

Note: worktree carries a pre-existing unrelated `.claude/skills/...` modification
from before this session; not touched or staged.
