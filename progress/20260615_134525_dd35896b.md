# Progress — 2026-06-15 (session dd35896b, one-shot #7474)

## Accomplished

Issue #7474 (non-circular lift-stage subset-uniqueness core for
`InitialLiftedFactorSubsetPartitionEvidence`). Built and verified the
**combinatorial clean half** of the core plus a transport primitive, all in
`HexBerlekampZassenhausMathlib/Basic.lean`, none assuming any
`LiftedFactorSubsetPartition` / `InitialLiftedFactorSubsetPartitionEvidence`:

- `exists_modPSubset_map_eq` — every lifted-factor subset is the image of a
  mod-`p` factor subset under `liftedSubsetOfModPSubset` (the index embedding is
  a bijection between equal-cardinality finite index types).
- `toMonicLiftData_isCoprime_liftedFactorProduct_complement` — for
  `d := toMonicLiftData core B primeData` with a prime selected by the monic
  transform, the mod-`p` reduction of any lifted-factor subset product is
  coprime to the reduction of its complement product. Discharges the
  `QuadraticMultifactorLiftInvariant` / monic / irreducible / nodup inputs of
  `henselLiftData_liftedSubset_complement_isCoprime_mod_p` straight from
  `toMonicPrimeData?`.
- `toMonicLiftData_liftedFactor_map_irreducible` — each lifted factor reduces
  mod `p` to an irreducible (its `modPFactor`).
- `toMonicLiftData_subset_of_liftedFactorProduct_map_dvd` — **clean half**: if
  the mod-`p` reduction of the `S`-product divides that of the `T`-product, then
  `S ⊆ T`. (A shared index `i ∈ S \ T` would make the irreducible reduction of
  `liftedFactor d i` divide both the `T`-product and its complement product,
  contradicting their coprimality.)
- `congr_centeredLiftPoly` — `centeredLiftPoly q m ≡ q (mod m)`; a reusable
  transport fact for the remaining hard half.

`lake build HexBerlekampZassenhausMathlib.Basic` is green; no
`sorry`/`axiom`/`native_decide`/`TODO` on added lines.

## Current frontier

The **analytic hard half** is not yet landed: the bridge from candidate
divisibility to mod-`p` product divisibility, i.e.

> `liftedRecoveryCandidate core d S ∣ liftedRecoveryCandidate core d T`
> ⟹ `P_S ∣ P_T` in `ZMod p [X]`  (`P_U := (toPolynomial (liftedFactorProduct d U)).map ρ`).

Composing it with the landed clean half
(`toMonicLiftData_subset_of_liftedFactorProduct_map_dvd`) and
`RecoveredAtLift.candidate_eq_of_monic_dvd` then yields the four deliverables.

## Next step

Land the hard half, then the four deliverable wrappers. Validated route:

1. `Associated ((toPolynomial (liftedRecoveryCandidate core d U)).map ρ)
   ((P_U).comp (C ↑(leadingCoeff core) * X))` for each `U`, via:
   - `congr_centeredLiftPoly` (landed) + `congr_of_dvd_modulus` (descend
     `p^k → p`) + `HexHenselMathlib.zpoly_congr_toPolynomial_map_eq` give
     `(toPolynomial (centeredLiftPoly (liftedFactorProduct d U) (p^k))).map ρ = P_U`;
   - `HexPolyZMathlib.toPolynomial_dilate` + `Polynomial.map_comp` give the
     `comp (C ↑(leadingCoeff core) * X)` form (`= Φ(P_U)`);
   - `HexPolyZMathlib.toPolynomial_eq_C_content_mul_primitivePart` +
     `↑(content r) ≠ 0` (forced by `Φ(P_U) ≠ 0`) give the `primitivePart`
     associated step;
   - `toPolynomial_scale_neg_one` gives the `normalizeFactorSign` associated step.
   Needs the threaded hypothesis `(↑(leadingCoeff core) : ZMod p) ≠ 0`
   (`p ∤ lc core`, listed in the route's "hypotheses to thread").
2. The `X ↦ u·X` substitution helpers (`u := ↑(leadingCoeff core)`, a unit since
   `p ∤ lc core`): `(a.comp (C u * X)).comp (C u⁻¹ * X) = a` (via `comp_assoc` /
   `mul_comp` / `C_comp` / `X_comp` / `mul_inv_cancel₀`), giving
   `Φ(P_S) ∣ Φ(P_T) ⟹ P_S ∣ P_T` (apply `eval₂RingHom C (C u⁻¹ * X)` via
   `map_dvd`) and `Φ(P_U) ≠ 0` from `P_U ≠ 0`. (Drafted this session but not
   landed — `← Polynomial.C_mul` left a metavariable and the
   `eval₂RingHom _ p = p.comp _` rfl-rewrite did not fire; needs a cleaner
   formulation.)
3. Deliverable 1 `representsAtLift_subset_of_dvd_liftedRecoveryCandidate`:
   `RecoveredAtLift.candidate_eq_of_monic_dvd` rewrites `f = candidate(S)`;
   `f ∣ candidate(T)` then maps through `map_intCast_zmod_dvd_of_zpoly_dvd` and
   the two `Associated`s to `Φ(P_S) ∣ Φ(P_T)`, then (2) gives `P_S ∣ P_T`, then
   the landed clean half gives `S ⊆ T`. Deliverables 2/3 are the
   recombination/scaled wrappers via the `*_eq_factor_of_recovery`
   interconversions; deliverable 4 (disjoint) is the shared-mod-`p`-factor
   ⟹ associated contrapositive.

## Blockers

None technical — the route is sound and every cited lemma exists. The remaining
hard half is a multi-step Mathlib-polynomial chain (content/primitivePart mod-`p`
associatedness, the linear-substitution automorphism) that did not fit this
session's budget. Landed pieces are independently verified building blocks.
