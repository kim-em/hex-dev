# HexMatrix bordered-minor row duplicate determinant zero

## Accomplished

- Claimed feature issue #2112.
- Added `det_eq_zero_of_row_eq` in `HexMatrix/Determinant.lean`, reusing the existing `rowAddDuplicate` Leibniz cancellation.
- Proved `det_borderedMinor_succ_eq_zero_of_row_duplicate` for successor bordered minors whose new border row duplicates the `k`-th prefix row.
- Split the remaining column-duplicate case into follow-up issue #2119 and left the required decomposition breadcrumb on #2112.
- Verified `lake build HexMatrix.Determinant`, `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a placeholder scan of `HexMatrix/Determinant.lean`.

## Current frontier

The row-duplicate half of #2112 is ready as a partial PR. The column duplicate case remains blocked on a small determinant-column infrastructure lemma, likely either a duplicate-column zero theorem via value-swapping permutation vectors or a reusable `det_transpose`.

## Next step

Work #2119 to prove the column-duplicate bordered-minor determinant zero lemma, then return to the successor off-diagonal product identity assembly.

## Blockers

- `lake build HexMatrix` still reports pre-existing `sorry` warnings in `HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`; no new placeholders were added in `HexMatrix/Determinant.lean`.
