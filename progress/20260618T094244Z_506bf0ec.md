# Progress — HexGF2Mathlib Phase 5 (#7900, session 506bf0ec)

## Accomplished

Closed 4 of 5 ring-equivalence sorries in `HexGF2Mathlib/Basic.lean`
(the `Hex.GF2Poly ≃+* Hex.FpPoly 2` bridge):

- `toFpPoly_one` — coefficientwise via a `coeff_one_eq` helper
  (`(1:GF2Poly).coeff i = decide (i = 0)`), matching `DensePoly.C 1`.
- `toFpPoly_add` — `coeff_add_semiring` + `coeff_add_eq_bne`, Bool case split,
  `grind` for the `ZMod64 2` `1+1=0`.
- `ofFpPoly_toFpPoly`, `toFpPoly_ofFpPoly` — both via a new public
  `coeff_ofFpPoly` (`(ofFpPoly p).coeff j = decide (p.coeff j ≠ 0)`).

New supporting API (all in-file): `coeffOfFp_toNat_lt`,
`coeffOfFp_testBit_zero`, `zmod2_toNat_{zero,one}`, `zmod2_one_ne_zero`,
`zmod2_cases`, `packWord_foldl_testBit` (induction on the OR-fold range,
`UInt64.toNat_shiftLeft`/`Nat.testBit_shiftLeft`/`testBit_mod_two_pow`),
`packWord_testBit`. Relocated `bit_eq_one_eq_testBit` to the top of the
namespace so the new helpers can use it.

## Verification

- `lake build HexGF2Mathlib` → `Build completed successfully (1131 jobs)`,
  zero `error:`. Only warnings: the remaining `toFpPoly_mul` sorry and the
  out-of-scope `Field.lean` 12-sorry cluster.
- `git diff origin/main -- HexGF2Mathlib/Basic.lean | grep '^+'` has no
  added `sorry`/`axiom`/`native_decide`.

## Current frontier

`toFpPoly_mul` is the only remaining sorry; `def equiv` consumes it.

## Next step

#7901: add a public carryless-convolution coeff lemma to `HexGF2/Multiply.lean`
(from the private `coeffWords_mulWords_contrib`/`clmulCoeffAt_sourcePairCoeff`
by reindexing word/bit decomposition to the flat diagonal `s + t = n`) plus a
`ZMod64 2` parity-sum bridge, then close `toFpPoly_mul`.

## Blockers

`toFpPoly_mul` needs infrastructure the #7900 premise underestimated: GF2Poly
exposes only the word-level `coeff_mul = coeffWords (mulWords ...)`; no
coefficient-level convolution lemma exists, and the convolution internals are
`private` to `HexGF2/Multiply.lean`. Diagnosis posted on #7900; scoped to #7901.
