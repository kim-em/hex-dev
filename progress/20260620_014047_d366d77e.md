# Audit incremental Phase 6 grind sweep since #8117 (#8146)

## Accomplished

Audited the Phase 6 grind-coverage PRs that merged after #8117
(2026-06-19T23:10:25Z): #8120 (HexMatrixMathlib/RankSpanNullspace),
#8122 (HexGFqMathlib/GF2q), #8125 (HexBerlekampZassenhausMathlib
Basic + LiftBridge), #8130 (HexGF2/Field Newton cluster), #8134
(HexBerlekampZassenhaus/Basic), #8136 (HexGF2/Field GF2n enumeration),
#8137 (HexModArith/Ring identity cluster).

**Deliverable 1 — ineligible-promotion check.** Every `@[grind =]` /
`@[simp, grind =]` added across the batch has an `Eq` or `Iff`
conclusion (literal `lhs = rhs`). None are `<`/`≤`/`Ne`/`∧`/
`Associated`/`let`-wrapped. The merged green builds plus the local
`lake build HexGF2.Field` rebuild are mechanical proof no rejected
shape slipped through.

**Deliverable 2 — missed-eligible / sibling check.** One genuine
oversight found and fixed:

- `HexGF2/Field.lean:1965` `mem_rootsOfCoeffList`
  (`β ∈ rootsOfCoeffList cs ↔ evalCoeffList cs β = 0`) promoted
  `@[simp]` → `@[simp, grind =]`. #8136's progress note left it out as
  "`↔`-shaped", but `grind =` accepts `Iff` — the same file already
  carries `reducePoly_eq_iff_mod_eq` as a `@[grind =]` Iff, and the
  audit issue explicitly corrects the no-`↔` premise. Its `evalCoeffList`
  body siblings (`evalCoeffList_nil/_cons`) are already `grind =`.

## Confirmed exclusions (left `@[simp]`-only, with rationale)

- `HexGF2/Field.lean` `frobeniusIter_zero/_succ/_zero_eq/_one_eq`:
  the Frobenius-iterate cluster is deliberately driven by forward
  `@[grind =>]` lemmas (`frobeniusIter_mul`, `_add_eq`, `_fixed_add`,
  `_fixed_mul`); `frobeniusIter_succ` is a term-doubling equation
  (`f a (k+1) = f a k * f a k`) whose promotion to a global `grind =`
  rewrite risks E-matching blowup. Explicit design-rationale exclusion.
- `HexBerlekampZassenhaus/Basic.lean` `transformedCoeffs_getD_top`:
  `Array.getD` LHS head (unfolds to `dite`), rejected by `grind =` —
  documented in #8134.
- `HexModArith/Ring.lean` `mul_zero/zero_mul/mul_one/one_mul/pow_zero/
  pow_one`: ring-identity normalization on the `Lean.Grind.Semiring`
  `ZMod64` instance collapses each LHS to a ground term / bare bound
  variable, so no usable pattern remains — documented in #8137.

The four `*Mathlib` bridge files (#8120/#8122/#8125) carry 0 bare
`@[simp]` lemmas; their remaining public theorems are deep
non-`@[simp]` proof-content lemmas outside the `@[simp]`-equation sweep
unit, so no missed siblings there.

## Current frontier

`lake build HexGF2.Field` green (Field elaborates in 1.7s — no
grind-loop slowdown). `scripts/check_dag.py` exit 0. Annotation-only;
no proof bodies, signatures, imports touched; no `sorry`/`axiom`/
`native_decide`; `done_through` unchanged.

## Next step

None for this issue. A future planner may note that `↔`-shaped `mem_*`
membership-characterization lemmas are valid `grind =` targets and
should not be excluded for being `Iff`.

## Blockers

None.
