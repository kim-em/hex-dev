# 20260602T211927Z work #2567

## Accomplished

- Claimed directive #2567 after #2564 correctly failed claim as blocked.
- Added the actual-cap canonical-indicator wrapper
  `factorFast_ne_none_of_factorFastCapLiftBridgeDataCanonicalIndicatorsAtPrecisionForCoeffBound_internalCapPositiveAndPrimeLowerBound`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The new wrapper accepts `BadVectorBridgeData` indexed by
  `badVectorWitnessOfFactorFastCapLiftData` and the named
  `FactorFastCapLiftAnalyticComparison`, matching the support-side actual-cap
  surface and avoiding restating the underlying `badVectorWitnessOfLiftData`
  expression or raw l2norm inequality at the final D1 call site.

## Current frontier

The canonical-indicator and canonical-support capstone surfaces now both have
actual-cap bridge-data wrappers with internalized cap positivity and prime
lower-bound side conditions.  The final `factorFast_terminates` theorem still
needs the closed mathematical producers for the remaining canonical recovery
inputs and the strict actual-cap Hadamard/l2norm inequality.

## Next step

Use the actual-cap indicator/support wrappers once the strict comparison from
#6371 and the remaining closed canonical true-support/recovery packages land,
then assemble the public `factorFast_terminates` theorem.

## Blockers

- #6371 remains the open strict cap-lift Hadamard/l2norm inequality.
- Closed constructors for the canonical true-support recovery inputs and
  bridge data are still missing.
- #2567 is too large to discharge completely in this session; this PR is a
  coherent partial D1 assembly-surface improvement.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
