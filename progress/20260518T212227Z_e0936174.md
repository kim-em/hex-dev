# Extract `monicModularImage_eq_scale_inv_leadingCoeff_of_isZero_false`

Issue: https://github.com/kim-em/hex/issues/5096

## Accomplished

- Added private theorem
  `monicModularImage_eq_scale_inv_leadingCoeff_of_isZero_false` to
  `HexBerlekampZassenhausMathlib/Basic.lean` immediately after
  `monicModPImage_eq_monicModularImage`.
- Rewired the four inline `Hex.monicModularImage f =
  Hex.DensePoly.scale (Hex.DensePoly.leadingCoeff f)⁻¹ f` derivations in
  `Basic.lean` to call the helper:
  - the two `g₁` / `g₂` sites in `factorsModP_nodup_of_factorsModPBerlekampForm`;
  - the local `let u` site in `gcd_monicModularImage_derivative_eq_one_local`;
  - the `g'` site in `factors_irreducible_of_factorsModPBerlekampForm`.

## Current frontier

The #5096 helper extraction is complete and ready for PR. The only unrelated
dirty worktree entry observed during the session is `.claude/CLAUDE.md`, which
was not touched.

## Next step

Open a PR closing #5096.

## Blockers

None. `lake build HexBerlekampZassenhausMathlib.Basic` and
`lake build HexBerlekampZassenhausMathlib` still fail with the existing
16-error `Basic.lean` baseline: deterministic `whnf` timeouts, the known
dependent-elimination `cases` failure, and existing unknown constants.
