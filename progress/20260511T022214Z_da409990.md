# 2026-05-11 02:22 UTC — feature session (da409990)

## Accomplished

- Claimed #3219 and proved the executable size and divisibility size
  bounds for `FpPoly p`. New theorems in `HexPolyFp/Basic.lean`,
  inserted right after the existing `degree?_mul_eq_add_degree?`:
  - `size_pos_of_ne_zero` — size positivity from inequality with the
    zero polynomial (used by every other proof in this batch).
  - `size_mul_eq_add_sub_one` — over a prime modulus, the size of a
    product of two nonzero polynomials is `a.size + b.size - 1`.
    Proof reuses `ZMod64.coeff_mul_at_top` and the existing private
    `coeff_mul_eq_zero_above_top`, and integrates the prime-field
    no-zero-divisors fact via `ZMod64.eq_zero_or_eq_zero_of_mul_eq_zero`.
  - `mul_ne_zero_of_ne_zero` — corollary used in the chain below; this
    replaces the previously private duplicate of the same lemma in
    `HexPolyFp/SquareFree.lean` (removed at line 3672).
  - `size_le_of_dvd_of_ne_zero` — divisibility bound from
    `size_mul_eq_add_sub_one` after splitting `b = a * c`.
  - `size_div_add_size_eq_size_add_one_of_dvd` — the additive identity
    `(c / d).size + d.size = c.size + 1` when `d ∣ c` and `c ≠ 0`.
    Proof routes through `DensePoly.div_mul_add_mod` plus
    `DensePoly.mod_eq_zero_of_dvd`, then applies `size_mul_eq_add_sub_one`
    to the cancelled product.
  - `size_div_lt_of_size_gcd_pos` — Yun-style strict descent: if
    `1 < (gcd c w).size` and `c.isZero = false`, then
    `(c / gcd c w).size < c.size`. Direct corollary of the additive
    identity.
- Removed the now-redundant private `mul_ne_zero_of_ne_zero` in
  `HexPolyFp/SquareFree.lean` (lines 3672–3735); the single existing
  caller `powLinear_ne_zero` already qualifies the name through the
  `FpPoly` namespace and now resolves to the public version.

## Verification

- `lake build HexPolyFp.Basic` — clean.
- `lake build HexPolyFp HexBerlekamp` — clean (only pre-existing
  unrelated linter warnings in `HexBerlekamp/RabinSoundness.lean` and
  the `HexMatrix.Bareiss` sorry warning that this issue does not
  touch).
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/Basic.lean`
  — no matches; no banned markers introduced.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry'
  HexPolyFp/SquareFree.lean` — only pre-existing entries; no new
  banned markers.

## Current frontier

- These size lemmas unblock the foundational step required by #3220
  (Yun loop termination) and downstream by #3209 / #3153.

## Next step

- Pick up #3220 (`yunFactorsContributionWithLevel` termination) now
  that the gcd / div size bounds are public.

## Blockers

- None for this slice.
