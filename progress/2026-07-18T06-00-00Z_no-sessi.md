# Issue 8750 round-four validation

## Accomplished

- Implemented the round-four fixed-benchmark demotions, canonical bounded-height
  inputs, pinned NK certify fixture, smooth fixed-separation driver family,
  widened compare ladder, and fixed achieved-precision refinement input.
- Built and verified all 14 registrations successfully.
- Ran the retained parametric set on a quiet 96-CPU host (load 2.26, two
  runnable processes).
- Confirmed `runMahlerPrec` and `runIsolate` are consistent and that `runRefine1`
  should take its specified fixed-benchmark fallback.
- Found `runIsolateAll` and all three widened compare registrations remain
  inconclusive on the quiet host, contradicting the retained-parametric exit.
- Reverted all experimental source changes, posted the measurements on #8750,
  and released the assignment.

## Current frontier

No source PR is open. The tracked source tree is unchanged; only session progress
files are untracked. The less-ambitious policy needs one final scope correction:
demote the four remaining inconclusive driver/compare registrations too.

## Next step

Update #8750 so only `runMahlerPrec` and `runIsolate` require parametric
consistency, with `runIsolateAll` and the compare group retained as fixed or
informational regression/conformance measurements.

## Blockers

- `runIsolateAll`: inconclusive through degree 14 (`cMin=4392`, `cMax=6920`).
- Compare group: residuals `+0.991`, `-0.314`, and `-0.382` against `n⁵`.
