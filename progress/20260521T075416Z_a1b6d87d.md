# 2026-05-21T07:54Z — work issue #5731

## Accomplished

Claimed #5731 after confirming the visible directive issues were dependency-blocked.

Added three monicised-core lifted-factor wrappers in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `monicisedCoreLiftData_liftedFactor_monic_of_monicPrimeData`
- `monicisedCoreLiftData_liftedFactor_natDegree_pos_of_monicPrimeData`
- `monicisedCoreLiftData_liftedFactor_injective_of_monicPrimeData`

These route through the #5730 `Hex.monicisedCorePrimeData?` alignment surface,
derive monicness of `(Hex.MonicisedCoreData.ofCore core).monicCore` from
positive leading coefficient and positive degree, and reuse the existing
Hensel/multifactor invariant wrappers.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `git diff --check`
- `git diff -- HexBerlekampZassenhausMathlib/Basic.lean | rg -n -- '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

The reusable substrate requested by #5731 is available for monic-prime
monicised-core lift data. It does not rewrite non-monic cores back through the
ordinary `henselLiftData core` path.

## Next step

Use these wrappers in the downstream Gap 1 consumer package so the exhaustive
branch can stop requiring `hcore_monic` for lifted-factor monicness, positive
degree, and injectivity.

## Blockers

No blocker for this issue. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification, left untouched.
