# HexGramSchmidt Int grind automation coverage (#8044)

## Accomplished

Promoted all eight public equation-shaped `@[simp]` theorems in
`HexGramSchmidt/Int.lean` to `@[simp, grind =]`:

`bareissGramCanonicalCoeff_zero`,
`bareissGramCanonicalCoeff_succ_regular`,
`bareissGramCanonicalCoeff_succ_processed`,
`bareissGramCanonicalCoeff_succ_singular`,
`bareissGramCanonicalCoeff_succ_done`, `gramDet_zero`,
`coeffs_mul_basis_eq_castIntMatrix`, and `normSq_map_intCast`.

Re-read the four successor equations before promotion. Each rewrites
`bareissGramCanonicalCoeff b (fuel + 1) i` to either the `fuel` case or an
explicit vector whose recursive coefficient reads are at `fuel`, so the
orientation strictly decreases the fuel argument. No theorem in the promoted
set is an iff, inequality, or proof-valued equality.

## Current frontier

`lake build HexGramSchmidt` completed successfully; `HexGramSchmidt.Int` built
in 26s with no runaway grind behavior. Extra downstream checks also completed:
`lake build HexLLL`, `lake build HexGramSchmidtMathlib`, and
`lake build HexLLLMathlib`. The Mathlib-facing builds replayed pre-existing
unused-variable / `push_neg` deprecation warnings outside the touched file, with
no grind warnings or errors. `python3 scripts/check_dag.py` exited 0. `git diff
--check` is clean, and the requested forbidden-token added-line scan is clean.

## Next step

None for this issue. The remaining visible Phase 6 sweep item is #8048 for
`HexArith/Montgomery/Context.lean`.

## Blockers

None.
