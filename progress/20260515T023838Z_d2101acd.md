# Session d2101acd — work #4006

## Accomplished

- Claimed #4105 first, verified that its dependency #4120 was still open and
  that `FpPoly.eval_mul` was absent, then released #4105 as stale.
- Claimed #4006 and added
  `HexBerlekampZassenhausMathlib.factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_count`,
  a slow exhaustive branch composition theorem connecting recorded
  `factorWithBound` entries identified with exhaustive square-free-core raw
  factors to `Hex.ZPoly.Irreducible` under the existing core count equality
  hypothesis.
- Verified `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`,
  `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the affected-file grep for `axiom`,
  `native_decide`, `TODO`, `FIXME`, and `sorry`.

## Current frontier

#4006 is only partially advanced. The bridge can now consume a core count
equality for recorded exhaustive-branch entries, but the unconditional
exhaustive-core irreducibility theorem still needs the missing executable
completeness/count-lower-bound work. The open #4126/#4112 line appears to be
the relevant prerequisite.

## Next step

Finish the executable exhaustive recombination completeness/count lower-bound
chain, then replace the explicit count-equality hypothesis in the #4006 bridge
composition with the resulting theorem.

## Blockers

- The full #4006 deliverable is blocked on an unresolved proof that exhaustive
  recombination selects all recovering lifted-factor subsets, yielding the
  needed core factor count equality.
