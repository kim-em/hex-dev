# HexHensel quadratic lift loop

## Accomplished

- Claimed #2148 after #2146 was lost to a claim race.
- Added modulus-weaken helpers, the `iterateQuadraticHensel_invariant` loop proof, and the public `henselLiftQuadratic_spec` binary product-congruence theorem in `HexHensel/QuadraticMultifactor.lean`.
- Verified `lake build HexHensel.QuadraticMultifactor`, `lake build HexHensel`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of the touched Lean file.

## Current frontier

#2148 is complete for the binary loop surface. The recursive `multifactorLiftQuadratic_spec` placeholder remains in `HexHensel/QuadraticMultifactor.lean` and is explicitly out of scope for this issue.

## Next step

Use `henselLiftQuadratic_spec` to prove the recursive `multifactorLiftQuadratic_spec` product assembly in a follow-up issue.

## Blockers

- None for the completed binary theorem.
- The build still reports pre-existing `sorry` warnings in upstream modules and the out-of-scope recursive quadratic multifactor theorem.
