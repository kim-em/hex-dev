# 2026-05-15 07:36 UTC — feature session, issue #4175

## Accomplished

- Skipped #4170 (`factor_irreducible_of_nonUnit` sorry): blocked on
  open #4149 (exhaustive `hcount` from default-precision coverage),
  open #4006 (slow-exhaustive irreducibility composition), and the
  out-of-scope BHKS termination/correctness obligation for the fast-BHKS
  branch. The bridge layer's count-eq scaffolding is in place but
  unusable until an unconditional count is supplied. The small-mod
  singleton branch additionally needs a `good prime + size ≤ 1 ⇒
  Z-irreducible` lemma that does not currently exist in core. Posted a
  branch-by-branch analysis comment on the issue and proposed a five-
  way decomposition.
- Skipped #4171 (`factor_unique` sorry): the theorem statement is too
  strong without a sign-normalization hypothesis. Concrete counter-
  example: at `f = x² - 1`, both `{scalar := 1, factors := #[(x-1,1),
  (x+1,1)]}` and `{scalar := 1, factors := #[(-x+1,1),(-x-1,1)]}` have
  product `x² - 1`, scalar `1`, irreducible factors, but factor lists
  are not permutations. Posted analysis with counter-example and the
  two viable reformulations.
- Skipped #4064: the `kernelWitnessSplit?_some_of_…` composition
  requires `f | (witness product)` derived from
  `IsFixedSpaceKernelPolynomial`, which in turn needs (a) a Frobenius-
  to-divisibility bridge from the kernel matrix predicate to
  `f | linearPow w p - w`, and (b) the witness-substitution identity
  `linearPow w p - w = (values p).foldl (· * (w - C ·)) 1`. (b) lives in
  open #4160 which itself depends on the compose substitution lemmas
  in #4159's chain (now this PR for the Sub B half).
- Claimed and implemented #4175 (HexPolyFp.Compose: compose_mul_X_sub_C
  + foldl transport + prime-field product transport). After fast-
  forwarding to `origin/main` (PR #4174 and PR #4177 had landed during
  the session), added three public theorems to
  `HexPolyFp/Compose.lean`:
  - `compose_mul_X_sub_C : compose (a * (X - C c)) w = compose a w * (w - C c)`
  - `compose_foldl_X_sub_C : compose (xs.foldl (· * (X - C ·)) init) w =
      xs.foldl (· * (w - C ·)) (compose init w)`
  - `compose_primeFieldLinearProduct :
      compose ((values p).foldl (· * (X - C ·)) 1) w =
        (values p).foldl (· * (w - C ·)) 1`

  Proof structure (per the issue's prescribed approach):
  - `mulXSubCListAux c prev cs`/`mulXSubCList c cs` define the
    coefficient list of `(ofCoeffs cs.toArray) * (X - C c) + C prev`.
  - `mulXSubCListAux_getD` closed-forms the list's `.getD n` to
    `(if n = 0 then prev else cs.getD (n-1) 0) - c * cs.getD n 0`.
  - `composeScalarCoeffList_mulXSubCListAux` is the inductive
    recurrence (proves both base case and step), with the inductive
    step packaged through `alg_compose_step` for the polynomial
    commutative-ring rearrangement and `fp_add_acm_rearrange` for the
    add-comm-monoid AC step (both proved manually using FpPoly
    `add_assoc`, `add_comm`, `mul_comm`, `left_distrib`, `right_distrib`
    plus `DensePoly.neg_mul_right_poly`).
  - `composeScalarCoeffList_trim` proves trim invariance of
    `composeScalarCoeffList` and
    `compose_ofCoeffs_eq_composeScalarCoeffList` bridges
    `compose (ofCoeffs L.toArray) w` to `composeScalarCoeffList L w`
    even when `L` has trailing zeros.
  - `mul_X_sub_C_eq_ofCoeffs_mulXSubCList` proves the polynomial
    equality `a * (X - C c) = ofCoeffs (mulXSubCList c a.toArray.toList).toArray`
    by `ext_coeff` with case analysis on `n`, reusing the existing
    `coeff_monomial_mul`, `coeff_scale`, and `coeff_sub`/`coeff_add`
    lemmas.

- Verified: `lake build HexPolyFp.Compose`, `lake build HexPolyFp`,
  `lake build HexBerlekamp.RabinSoundness`, `python3 scripts/check_dag.py`,
  and `git diff --check` all pass. No new `axiom`, `native_decide`,
  `TODO`, `FIXME`, or theorem-level `sorry` in the affected file.

## Current frontier

- #4175 is fully discharged. The Sub C sibling (#4176) for
  `compose (linearPow X p - X) w = linearPow w p - w` is still open and
  can now reuse `compose_mul_X_sub_C` for the X-side analog
  `compose (a * X) w = compose a w * w` (or prove it directly).
- The downstream witness substitution (#4160) becomes tractable once
  #4176 lands.

## Next step

- Land #4176 (`compose_linearPow_X_sub_X`), then #4160 (witness-level
  product identity and divisibility corollary).

## Blockers

None for this PR.
