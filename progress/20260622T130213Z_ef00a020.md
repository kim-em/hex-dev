# BHKS-faithful core-coordinate lift (#8293) — diagnosed unsound, skipped

Session type: feature (one-shot `/once`). UTC 2026-06-22T13:02.

## Accomplished

Premise-checked #8293 ("BHKS-faithful local-factor lift — recover over core's
own local factors, not the toMonic dilation") before any code. The directive's
load-bearing claim — "factor `core mod p` into monics, Hensel-lift in core's
variable (`henselLiftData core …` already lifts against an arbitrary `f`), so
`core ≡ ℓf·∏fᵢ (mod p^a)` and each `fᵢ ∣ core`" — is **false**: the entire
Hensel-lift stack is monic-only on both axes. Posted a concrete diagnostic
(issue comment) and `coordination skip` (→ replan). No code, no branch commits,
no PR.

Evidence (all source-cited in the issue comment):
- `berlekampFactorsModP` (`Basic.lean:935-946`) factors the **monic associate**
  `monicModularImage fModP`, so `choosePrimeData? core`'s `factorsModP` are monic
  with product ≡ `core/ℓf (mod p)`, not `core`.
- `QuadraticMultifactorLiftInvariant` (`QuadraticMultifactor.lean:574-586`)
  requires `∏ ≡ core (mod p)` + `Monic` leading factor
  (`QuadraticLiftLoopInvariant`, :118-122) — both fail for non-monic core.
- `QuadraticMultifactorLiftInvariant_of_choosePrimeData` (`Basic.lean:2724-2737`)
  takes `hcore_monic` + `hproduct_mod_p` explicitly.
- `divModMonicModSquareAux` (`Quadratic.lean:67-85`) hardwires a **monic**
  divisor (uses `rem.leadingCoeff`, no `/q.leadingCoeff`; docstring :64-66).
- `bhksIndicatorCandidate?` (`Basic.lean:5555-5557`) hardwires
  `dilate (leadingCoeff f)` — the monic-transform recovery, not a thin reroute.
- Both fast (`toMonicLiftData`, :7069) and slow
  (`exhaustiveCoreFactorsWithBound`, :7630) paths lift the monic transform; no
  direct core-coordinate lift exists.
- The cited evidence file
  `reports/20260622-core-coordinate-cut-viability.md` is absent from the repo.

Secondary: "each `fᵢ ∣ core`" holds only mod `p^a` (ℓf a unit), not over ℤ
(directive's own `2x²-3x+1`: monic `x-1/2 ∤ core` over ℤ), so #8292's
`abs_factorDeriv_coeff_le` (needs `core = g·h` over ℤ) still does not apply to
the monic `fᵢ` directly.

## Current frontier

Issue marked `replan`. The genuine missing bridge is **new
leading-coefficient-tracking Hensel-lift infrastructure**: a unit-leading-
coefficient (mod `p^a`) division kernel + a multifactor-lift invariant that
absorbs `ℓf` into the complement and proves `core ≡ ℓf·∏fᵢ (mod p^k)`, plus the
`bhksIndicatorCandidate?` recovery-transform change and the CI-gated Mathlib
ripple (one PR, per the soundness rule).

## Next step

A planner re-scopes #8293 so the lc-tracking lift stack is the first explicit
deliverable, with the recovery-transform change + scaffolding ripple gated on
it. Until that exists the "route recovery through henselLiftData core" framing
cannot be executed.

## Blockers

Directive premise unsound as stated (see above). No build/CI work was reached.
