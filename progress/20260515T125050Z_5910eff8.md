# Issue #4216

## Accomplished

- Checked the unclaimed queue and first selected #4173, then released it as stale:
  its #4206 prerequisite closed by replan, but the Rabin soundness residuals
  #4218/#4223 still block the final certificate composition.
- Claimed #4218 next, then released it as blocked by in-flight #4223 because
  `rabin_irreducible` cannot be reassembled without the direct forward theorem.
- Claimed #4216 and added proof-facing provenance for `Hex.choosePrimeData`:
  - `choosePrimeData_prime`
  - `choosePrimeData_fModP_eq_modP`
- Refactored the internal `choosePrimeData?` fold through the private
  `choosePrimeDataScoreStep` helper so the provenance proofs can follow the
  selected score through the scan without changing executable behavior.
- Verified:
  - `lake build HexBerlekampZassenhaus`
  - `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`

## Current frontier

- `Hex.choosePrimeData core` now exposes selected-prime primality and the
  stored modular image equality needed by the small-mod singleton bridge.
- The final #4216 branch theorem is not complete. The remaining missing
  provenance is the selected `factorsModP`/Berlekamp-output alignment plus the
  good-prime square-free and leading-coefficient facts in a form consumable by
  `IntReductionMod.squareFreeCore_irreducible_of_small_mod_singleton`.

## Next step

- Expose a public proof-facing statement connecting
  `(Hex.choosePrimeData core).factorsModP` to the Berlekamp factorization of
  the selected monic modular image, then use
  `HexBerlekampMathlib.irreducible_of_berlekampFactor_factors_length_le_one`
  to discharge the explicit `hirr_fModP` hypothesis in the small-mod singleton
  theorem.

## Blockers

- #4173 remains blocked on Rabin soundness residuals, especially #4223.
- #4218 should wait for #4223 to land.
