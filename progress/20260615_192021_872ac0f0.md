# Progress — 2026-06-15 19:20:21 UTC (session 872ac0f0, feature)

## Accomplished

Issue #7509: exposed the executable scaled-search recovered/dilate proof
surfaces in `HexBerlekampZassenhausMathlib/Basic.lean`.

- Promoted the recovered prefix-none theorem to
  `RecoveredScaledSearch.prefixNone_of_bound`.
- Promoted the abstract-bound coverage theorem to
  `RecoveredScaledSearch.covers_of_bound`.
- Promoted the default-bound coverage wrapper to
  `RecoveredScaledSearch.covers`.
- Repointed the fixed-factor scaled-search capstone to consume
  `RecoveredScaledSearch.covers_of_bound`.
- Updated docstrings so the public API describes the executable
  recovered/dilate coordinate rather than the older scaled-candidate
  coordinate.

## Current frontier

The recovered/dilate intermediate surfaces requested by #7509 are now public.
The older scaled-coordinate prefix theorem remains available for current
callers. The requested grep found no remaining statements combining
`scaledRecombinationSearchModAux` with `scaledRecombinationCandidate` in
`HexBerlekampZassenhausMathlib/Basic.lean`.

## Next step

Downstream workers can import and consume the `RecoveredScaledSearch.*`
surfaces when migrating the executable scaled-search correctness chain.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib`: green.
- `lake build HexBerlekampZassenhaus`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `rg -n "scaledRecombinationSearchModAux.*scaledRecombinationCandidate|scaledRecombinationCandidate.*scaledRecombinationSearchModAux" HexBerlekampZassenhausMathlib/Basic.lean`: no matches.
- `git diff --check`: clean.
- Added lines contain no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
