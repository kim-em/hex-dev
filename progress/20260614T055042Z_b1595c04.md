**Accomplished**

Claimed #7044 and completed the Mathlib-free fast-path migration:
`factorFastCoreWithBound` now lifts `ZPoly.toMonicLiftData core ...`, and
`bhksIndicatorCandidate?` reconstructs candidates by centered lifting in the
monic-transform coordinate and applying `ZPoly.dilate (leadingCoeff f)`.

Updated the non-monic cubic regression so the fast path must return three
factors rather than deferring to the slow path. Adjusted the immediate
Mathlib-facing theorem names and wrapper assumptions toward the
dilated-centered-lift shape.

Verification completed:

- `lake build HexBerlekampZassenhaus.Basic` passed.
- `lake build HexBerlekampZassenhaus.CrossCheck` passed.
- `git diff --check` passed.
- Added-line grep over touched Lean files found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.

**Current frontier**

`lake build HexBerlekampZassenhausMathlib.Basic` reaches the bridge and fails
in the existing recovery proof layer. The remaining failures are not executable
counterexamples; they are proof-shape mismatches where old scaled-product
theorems still expect
`reduceModPow (scaledLiftedFactorProduct ...) = reduceModPow factor`, while
the current `RepresentsIntegerFactorAtLift` package carries a monic-coordinate
witness plus dilation equality.

**Next step**

Repair the Mathlib bridge by adding/proving the conversion from
`RepresentsIntegerFactorAtLift core d factor S` to the dilated centered-lift
reconstruction facts consumed by the updated BHKS recovery wrappers, then
finish updating `Recovery.lean` and `PartitionRefinement.lean` around
`ZPoly.toMonicLiftData`.

**Blockers**

None for the executable path. Full #7044 completion remains blocked on the
Mathlib bridge proof repair described above.
