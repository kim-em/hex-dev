# Progress: #6838 — precision-gated liftedRecoveryCandidate equality

## Accomplished

Landed `liftedRecoveryCandidate_eq_factor_of_congruence_of_bound` in
`HexBerlekampZassenhausMathlib/Basic.lean`, the load-bearing recovery
bridge for the dilation-coordinate model.

A prior partial PR (#6854, merged as `039a66ad` after this branch's
original cut) already landed the bulk of #6838's deliverables:

- `liftedRecoveryCandidate_eq_factor_of_recovery_of_bound` (+ default
  wrapper `liftedRecoveryCandidate_eq_factor_of_recovery`),
- the size/natDegree facts
  `natDegree_toPolynomial_liftedRecoveryCandidate_eq_sum` (+ `_of_bound`),
- the primitive/content facts `zpoly_primitive_liftedRecoveryCandidate`
  (+ `_of_bound`), and the `size_dilate_eq_of_monic_of_ne_zero` helper.

The gap that remained: the merged `_of_recovery_of_bound` theorem takes
the recovered inner equality
`dilate lc(core) (centeredLiftPoly product) = factor` as a *direct*
hypothesis, leaving its `B'`/`hvalid`/`hprecision` arguments vestigial
(underscore-prefixed). That inner equality does not hold in the
executable search: the dilated centred product carries `lc(core)^i` as
content, so the recovered factor is its *primitive part*, not the
dilated polynomial. The merged theorem is true but not consumable by
the real recovery chain.

`liftedRecoveryCandidate_eq_factor_of_congruence_of_bound` closes the
gap with load-bearing precision. Inputs are the pieces the
dilation-coordinate correspondence actually produces:

- `hcong : reduceModPow (liftedFactorProduct d S) = reduceModPow
  monicFactor` plus the Mignotte bound on `monicFactor` recover the
  centred lift of the selected product as `monicFactor` exactly
  (`centeredLiftPoly_reduceModPow_eq` + `..._of_coeff_natAbs_le`);
- the dilation keystone `hdilate : primitivePart (dilate lc(core)
  monicFactor) = factor` (equality refinement of
  `HexPolyZMathlib.dilate_recovery`) transports it to the integer factor.

This is the shape #6839 routes its redefined `RepresentsIntegerFactorAtLift`
exact-recovery wrappers through.

## Verification

- The only elaboration error in `HexBerlekampZassenhausMathlib.Basic` is
  the pre-existing #6804 `scale`→`dilate` seam at `:16402`
  (`scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`
  consuming the old `scale`-shape `hprefix`). Lean elaborated past it
  (warnings at `:16512`, `:18040` follow), confirming the new theorem at
  `~5092` compiled. The module is tolerated red on `main`; CI does not
  build this layer.
- `python3 scripts/check_dag.py` exits 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.

## Current frontier

The dilation-coordinate recovery surface for `liftedRecoveryCandidate`
is complete: abstract precision-gated equality, primitive/content, and
size/natDegree facts all present.

## Next step

#6839 redefines `RepresentsIntegerFactorAtLift` to the dilation
congruence and routes its exact-recovery wrappers through
`liftedRecoveryCandidate_eq_factor_of_congruence_of_bound`. #6804 then
closes the `scale`→`dilate` seam at `:16402`, after which the whole
module builds green.

## Blockers

None for this issue. The module's red baseline at `:16402` is the open
#6804 seam, untouched here.
