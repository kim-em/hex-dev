# #6781 resolved (two-tier dispatch); #6782 fired

## Accomplished

- Reviewed #6781's resolution (PR #6784, merged; issue closed): two-tier
  operand-adjusted dispatch (wide operands >= 180 bits steer from n=30,
  narrow from n=40; nothing below n=30 ever steered, protecting
  bz-recombination). Harsh-cubic dispatched curve is now monotone; n=35
  improved 1.81x (gate >= 1.5x), n=30 1.22x; NormSq hashes unchanged
  (same shortest vector); steered tally 15/0; checker dispatch untouched.
- Premise correction recorded by the worker: random-bounded n=30 is NOT
  steerable — the steered float reduction of that swap-forcing fixture
  fails (delta, 11/20) certification, so the path falls back and costs
  19.2 ms vs 14.8 ms exact. The RB 30->45 flat segment is therefore the
  honest min of the available algorithms, not a routing artifact; that
  go/no-go clause was relaxed accordingly. The intervalWins-shaped
  product predictor was measured and rejected (would regress harsh-cubic
  n=20 by 26% — same entanglement as #6757).
- Post-merge steered-evidence refresh (exports/figures/reports) appears
  pending — no refresh commit after f07ab141 yet; the harsh-cubic figure
  still shows the old bump until it lands.
- Unblocked and fired #6782 (packed word-scale gate removal) via wt
  (~/worktrees/hex/hex-issue-6782); its body already instructs
  serializing carica sweeps.

## Current frontier

#6782 in flight (SPEC PR first — the regime gate is SPEC-mandated).
#6781's evidence refresh pending.

## Next step

Watch for #6781's evidence refresh and #6782's PRs.

## Blockers

None.
