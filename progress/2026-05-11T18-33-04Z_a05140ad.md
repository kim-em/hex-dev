## Accomplished

- Claimed issue #3552.
- Added list-level prefix congruence and adjacent swapped-predecessor reduction helpers in `HexGramSchmidt/Basic.lean`.
- Exposed rational and integer row-swap basis helpers for rows before the adjacent swap and for the swapped predecessor row.
- Replaced the corresponding `basis_adjacentSwap_of_lt` and `basis_adjacentSwap_prev` sorries in `HexGramSchmidt/Update.lean`.
- Verified `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt.Update`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

- The prefix-preservation and swapped-predecessor basis layer for adjacent swaps is available.
- The remaining adjacent-swap current-row, suffix, coefficient, scaled-coefficient, and determinant formulas in `HexGramSchmidt/Update.lean` still contain the existing sorries.

## Next step

- Use the new predecessor helper to prove the swapped-current residual formula or the lower coefficient adjacent-swap formulas in a follow-up issue.

## Blockers

- None.
