# HexMatrix Determinant docstrings (#8223)

## Accomplished

Added concise docstrings to the 13 public `@[simp, grind =]` theorems in
`HexMatrix/Determinant.lean` that lacked them (the Phase 6 public-docstring
gap from the #8195 audit punch-list):

- `columnTupleMatrix_entry`, `columnTupleVectorFn_apply`
- `sortInjPerm_getElem_val`, `reconstructInjTuple_getElem`,
  `columnTupleMatrix_reconstructInjTuple_entry`
- `firstColumns_entry`, `setRow_get_self`, `adjugate_get`
- `skipIndex2_val_of_lt_p`, `skipIndex2_val_of_between`, `skipIndex2_val_of_ge_q`
- `nMatrix_entry`, `basisVec_getElem`

Each docstring states the computed value / index identity, one or two
sentences in the surrounding style. No proof, signature, or attribute
changes.

## Current frontier

`lake build HexMatrix` green (389 jobs). `runLinter HexMatrix.Determinant`
now has no `docBlame` section at all — every docstring lint in the file
is cleared. The only remaining sections are `simpNF` and
`unusedArguments`, which are the other two (out-of-scope) Phase 6 gaps.

`done_through` left at 5 per the issue — the 5→6 bump is gated on the
simpNF gap too.

## Next step

The simpNF cleanup (~40 lints) is the last remaining HexMatrix Phase 6
gap; whichever issue closes it should also bump `done_through` 5→6.

## Blockers

None.
