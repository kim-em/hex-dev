# 2026-05-19T19:50 UTC — HexGramSchmidtMathlib singular-prefix zero propagation

## Accomplished

- Claimed and executed #5534 (HexGramSchmidtMathlib Phase 5: prove Gram
  singular-prefix zero propagation).
- In `HexMatrixMathlib/Determinant/Bareiss.lean`:
  - Promoted `borderedMinor_corner_eq_leadingPrefix` from `private` to public.
    It is the matrix-level corner identity `borderedMinor M k _ ⟨k,_⟩ ⟨k,_⟩
    = leadingPrefix M (k+1) _`; needed by the new bridge below.
  - Added `noPivotLoop_invariant_of_singularStep_eq_none`, the outcome-driven
    companion of `noPivotLoop_invariant`: when the no-pivot Bareiss loop run
    from a state satisfying `BareissNoPivotInvariant` does NOT record a
    singular step during its `fuel` iterations, the invariant continues to
    hold afterward. This is the no-pivot analog of the existing
    `pivotLoop_invariant_of_singularStep_eq_none`.
- In `HexGramSchmidtMathlib/Int.lean`:
  - Added private helpers:
    - `matrix_diag_at_fin_eq` — substitution-friendly Fin-index helper for
      the diagonal `M[i][i]` entry under a Fin equality.
    - `noPivotLoop_extends_singularStep` — once the no-pivot pass is
      singular at index `k`, additional fuel does not change the state.
    - `noPivotLoop_prefix_state_at_singular` — from a partial pass recording
      a singular step at index `s`, derive that the `s`-fueled prefix is
      non-singular, has reached `step = s`, and has zero diagonal at
      `(s, s)`. Matrix-entry transfer uses `Matrix.noPivotLoop_diag_of_le_step`.
    - `bareissNoPivotInvariant_diag_eq_leadingPrefix_det` — specialization
      of `trailing_eq` to the diagonal corner, with the step value as a
      free variable so `subst` can handle the dependent rewrite.
    - `leadingPrefix_det_eq_zero_of_noPivotLoop_singularStep` — composes
      the above to identify `det(leadingPrefix M (s+1)) = 0` at the moment
      of singularity.
    - `gramSchmidtNormProduct_eq_zero_of_le` — multiplicative zero
      propagation via `gramSchmidtNormProduct_succ`.
  - Added the public deliverable theorem
    `leadingPrefix_gram_bareiss_eq_zero_of_singularStep_lt`: from a
    partial-pass no-pivot singular step at index `s` strictly before slot
    `r + 1`, the public row-pivoted Bareiss determinant of the `(r + 1)`
    leading Gram prefix is zero. Proof composes
    `leadingPrefix_det_eq_zero_of_noPivotLoop_singularStep` with
    `gramDet_eq_prod_normSq_uncond` and `gramSchmidtNormProduct_succ` to
    propagate zero from `s + 1` to `r + 1`, then converts back to bareiss
    form via `HexMatrixMathlib.bareiss_eq_det` and the existing
    `leadingGramMatrixInt_eq_leadingPrefix_gram` and
    `leadingGramMatrixInt_det_eq_gramDet_int` bridges.

## Current frontier

- The new public theorem is now available to discharge the singular branch
  of the `gramDetVecEntry_eq_leadingPrefix_bareiss` placeholder
  (`HexGramSchmidt/Int.lean:3064`, still `sorry`-marked). Combined with the
  existing non-singular branch helper
  `bareissNoPivotData_diag_eq_leadingPrefix_bareiss_of_prefix_nonsingular`
  and the full-to-prefix projection
  `gramDetVecEntry_bareissNoPivot_full_eq_leadingPrefix`, the placeholder
  is now mechanically discharge-able.

## Next step

- Open a follow-up issue to replace the `gramDetVecEntry_eq_leadingPrefix_bareiss`
  placeholder in `HexGramSchmidt/Int.lean:3064` by case-splitting on the
  no-pivot loop's singular step at fuel `r` and consuming
  `bareissNoPivotData_diag_eq_leadingPrefix_bareiss_of_prefix_nonsingular`
  (non-singular branch) together with the new
  `leadingPrefix_gram_bareiss_eq_zero_of_singularStep_lt` (singular branch,
  both sides vanish).

## Blockers

- None for this issue. `lake build` is green and no new
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` were introduced.
