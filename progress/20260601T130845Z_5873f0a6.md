# 5873f0a6 - feature #5971

## Accomplished

- Claimed #5971 after confirming no claimable directive issue was returned.
- Audited `HexPoly/Operations.lean` for conservative `grind =` coverage on
  operation characterising lemmas.
- Added `@[simp, grind =]` to coefficient laws for scale, shift, shifted scale,
  add, sub, negation, zero coefficients, and derivative.
- Added `@[simp, grind =]` to small zero/constant wrappers for scale, shift,
  compose, add, sub, negation, eval, and derivative.
- Left the schoolbook multiplication coefficient theorem unannotated because it
  exposes the nested fold representation and is a poor global grind rule.
- Verified with `lake build HexPoly`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token diff grep.

## Current frontier

#5971 is implemented. The HexPoly operation surface now has conservative grind
characterisation on the same normalization lemmas downstream proofs already use
with `simp`.

## Next step

Open the PR for #5971 and mention that `coeff_mul` remains intentionally
unannotated due to fold expansion/search risk.

## Blockers

None for this issue. A pre-existing `.claude/CLAUDE.md` worktree modification
was left untouched.
