# 20260513T182327Z (#3564, session ed5742a7)

## Accomplished
- Discharged the `scaledCoeffMatrix_det_eq_gramDet_mul_coeffs` Cramer
  bridge in `HexGramSchmidt/Int.lean`, completing the assembly from
  #3540's decomposition.
- Added supporting helper lemmas:
  - `dot_castIntRow_basis_eq_zero_of_lt` — original rows are orthogonal
    to later-index Gram-Schmidt basis vectors.
  - `foldl_finRange_truncate_zero_above` — a `Fin m` foldl truncates to
    a `Fin k₀` foldl when the proof-indexed body vanishes on indices
    `≥ k₀`.
  - `dot_castIntRow_castIntRow_eq_dot_basisPrefixProjection` — orthogonality
    bridge that swaps `castIntRow b i` for the basis-prefix projection
    when dotted with an earlier original row.
  - `foldl_finRange_succ_isolate_last` — isolates the final term in a
    `Fin (k+1)` foldl when prior terms vanish.
  - `dot_basis_basisPrefixProjection_eq_coeff_mul_normSq` and
    `dot_basis_basisPrefixProjection_eq_origProjCoords_mul_normSq` —
    extract the projection coefficient (in two coordinate systems)
    weighted by `normSq(basis[j])`.
  - `gramDet_succ_rat` — `(gramDet (j+1) : Rat) = (gramSchmidtNormProduct j) * normSq(basis[j])`.
- Moved `scaledCoeffMatrix_det_eq_gramDet_mul_coeffs` and the dependent
  `scaledCoeffRows_lower_eq_coeffs` later in the file so they can refer
  to the now-needed `basisPrefixProjection` / `originalProjectionCoords`
  / `gramDet_eq_prod_normSq_core` machinery defined further down.

## Current frontier
- The Cramer expansion identifies the determinant as
  `originalProjectionCoords[Fin.last j] * (gramDet (j+1) : Rat)`, then
  cancels through `normSq(basis[j])` to land on
  `(gramDet (j+1) : Rat) * coeffs[i][j]`. The cancellation argument
  uses `H2 = H3` to identify the basis-side and original-row-side
  projections.

## Next step
- None for #3564. Downstream uses of `scaledCoeffs_eq` no longer carry
  a `sorry` through the determinant bridge.

## Blockers
- None.
