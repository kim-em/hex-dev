## Accomplished

- Claimed #5355 after confirming there were no unblocked directive issues.
- Added `support_monomial` and `mem_support_monomial` in `HexPoly/Dense.lean`.
- Added a private list-filter helper for the singleton support proof.
- Verified with:
  - `lake build HexPoly`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^@\[simp\] theorem support_monomial' HexPoly/Dense.lean`
  - `rg -n '^theorem mem_support_monomial' HexPoly/Dense.lean`
  - `git diff -- HexPoly/Dense.lean | rg -n '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

#5355 is complete locally. The change is scoped to `HexPoly/Dense.lean`
and exposes direct monomial-support characterisations mirroring the existing
constant-polynomial support API.

## Next step

Review and merge the PR for #5355. The remaining Phase 6 monomial eval and
Mathlib derivative bridge issues can proceed independently.

## Blockers

None.
