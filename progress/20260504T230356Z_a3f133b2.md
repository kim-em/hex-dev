# HexBerlekampZassenhaus modular factor data (#2082)

## Accomplished

- Claimed issue #2082.
- Replaced `PrimeChoiceData.factorsModP` and prime scoring with a Berlekamp-factor-based modular irreducible factor list.
- Added fixed-prime `Lean.Grind.Field` evidence and monic modular-image plumbing needed by the executable Berlekamp factorizer.
- Updated `HexBerlekampZassenhaus/EmitFixtures.lean` comments so they no longer describe the old square-free bucket limitation.
- Verified `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested stale-text search.

## Current frontier

The BZ prime-choice data now counts and carries Berlekamp modular factors instead of square-free decomposition buckets. The helper proof obligations follow the existing Phase 5 theorem-scaffold style and do not introduce executable placeholder data.

## Next step

Open a PR closing #2082 with the implementation, fixture-comment update, and this progress file.

## Blockers

None.
