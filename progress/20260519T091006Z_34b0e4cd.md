# Session 34b0e4cd - issue #5368

## Accomplished

- Claimed issue #5368.
- Added `rowAdd_getElem_src_of_ne` in `HexMatrix/RowEchelon.lean`.
- Added the simp wrapper `row_rowAdd_src_of_ne` next to the existing row-add row lemmas.
- Verified with:
  - `lake build HexMatrix.RowEchelon`
  - `lake build HexMatrix`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n 'row_rowAdd_src_of_ne|rowAdd_getElem_src_of_ne' HexMatrix/RowEchelon.lean`
  - `git diff -- HexMatrix/RowEchelon.lean | rg -n '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

Issue #5368 is complete locally. The row-add API now has a named source-row wrapper for the common `src ≠ dst` case.

## Next step

Create the PR for issue #5368 and let CI validate the branch.

## Blockers

None.
