# 2026-05-19 04:35 UTC — feature session (cb1d7734)

## Accomplished

Issue #5257: added `HexPolyMathlib.ofPolynomial_monomial` as a `@[simp]`
wrapper that normalises `ofPolynomial (Polynomial.monomial n c)` to
`Hex.DensePoly.monomial n c`. Insertion only, after `ofPolynomial_sub`
and before `toPolynomial_zero` in `HexPolyMathlib/Basic.lean`. Proof
uses `Hex.DensePoly.ext_coeff` + `coeff_ofPolynomial` +
`Hex.DensePoly.coeff_monomial` + `Polynomial.coeff_monomial`, splitting
on `i = n`. The `i ≠ n` branch needed `Ne.symm hi` (and a trailing
`rfl`) because `Polynomial.coeff_monomial` writes the discriminator as
`if n = i`, opposite to `Hex.DensePoly.coeff_monomial`'s `if i = n`.

`lake build HexPolyMathlib.Basic`, `lake build HexPolyMathlib`,
`lake build`, and `python3 scripts/check_dag.py` all clean.
Diff contains no `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`.

## Current frontier

`ofPolynomial_monomial` is in. The symmetric `toPolynomial_monomial`
companion is explicitly out of scope per the issue and remains
unimplemented; it is the natural follow-up.

## Next step

Open the follow-up issue for `toPolynomial_monomial` (separate worker
can take it). After that, the remaining bridge gaps to consider are
`ofPolynomial_X` / `ofPolynomial_C_mul_X_pow`, which the issue flags as
a different design decision because `Hex.DensePoly` has no separate `X`
definition.

## Blockers

None.
