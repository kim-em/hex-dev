# Progress — 2026-05-08T00:10 UTC — agent/5b63e371

## Accomplished

Claimed issue #2519 and proved three of the four remaining scaled
size-reduction formulas in `HexGramSchmidt/Update.lean`:

- `scaledCoeffs_sizeReduce_lower`
- `scaledCoeffs_sizeReduce_other_row`
- `scaledCoeffs_sizeReduce_above_pivot`

Added two private local helpers in `Update.lean`: a Fin-indexed wrapper around
`scaledCoeffs_eq`, and an integer-cast injectivity bridge through `Rat`.

Created follow-up issue #2631 for the remaining pivot formula and left a
`Decomposed into #2631` breadcrumb on #2519. The pivot formula needs a
determinant-level scaled-coefficient row-add bridge because the rational
coefficient pivot theorem has a nonzero-norm hypothesis while the public scaled
formula does not.

Verification run:

- `lake build HexGramSchmidt.Update`
- `lake build HexGramSchmidt`
- `lake build HexLLL`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "axiom|native_decide|TODO|FIXME" HexGramSchmidt/Update.lean`

## Current frontier

`HexGramSchmidt.Update` now has the non-pivot scaled size-reduction formulas
proved from `scaledCoeffs_eq`, coefficient row-add formulas, and
`gramDet_sizeReduce`. The only remaining `sorry` in the size-reduction theorem
block is `scaledCoeffs_sizeReduce_pivot`.

## Next step

Resolve #2631 by proving the pivot formula, likely via a small determinant or
scaled-coefficient row-add bridge that handles the zero Gram-determinant case
without assuming the source Gram-Schmidt norm is nonzero.

## Blockers

No blocker for the partial PR. Completing the original full #2519 scope depends
on the new pivot bridge tracked by #2631.
