# Migrate HexPolyZ/Basic.lean to coeff_*_ring wrappers — issue #5218

Session: 4f044b1b-8061-462b-9397-53ed4a259339, type feature worker.

## Accomplished

- `HexPolyZ/Basic.lean`: added the three `DensePoly` typeclass
  instances for `Rat` immediately before `namespace ZPoly` (lines
  17–25), mirroring the precedent trio in `HexPolyFp/Basic.lean:52–72`:
  `AddZeroLaw Rat` and `SubZeroLaw Rat` are discharged by `grind`,
  matching the inline `(by grind)` style previously used at lines 2570
  and 2571. `ZeroSubNegLaw Rat` is also `grind` — the issue's primary
  suggestion `exact Rat.zero_sub a` fails (constant not found at
  toolchain v4.30.0-rc2), and the `by simp` fallback errored with
  `simp made no progress`, but `grind` closes the goal cleanly.
- `HexPolyZ/Basic.lean`: migrated the two call sites in
  `rat_dvd_cofactor_derivative` (lines 2570–2573 on `origin/main`,
  now 2579–2580 post-edit). Deleted both inline
  `have hzero_add` / `have hzero_sub` declarations; replaced
  `rw [DensePoly.coeff_sub _ _ n hzero_sub]` with
  `rw [DensePoly.coeff_sub_ring]` and
  `rw [DensePoly.coeff_add _ _ n hzero_add]` with
  `rw [DensePoly.coeff_add_semiring]`. The trailing `grind`
  closes the residual unchanged.
- `lake build HexPolyZ.Basic` green; `lake build HexPolyZ` green;
  `lake build` 244/244 green; `python3 scripts/check_dag.py` exit 0;
  `git diff --check` clean. Net diff +9 / -4 (≈7 lines) on the one
  target file. No `sorry` / `axiom` / `native_decide` introduced.
- Verification checks from the issue body all pass:
  `git grep -cE 'DensePoly\.coeff_(add|sub|neg) _ _' HexPolyZ/Basic.lean`
  empty (the two `_ _` underscore-shape sites are gone — the other
  call sites in the file use explicit polynomial expressions, which
  are out of scope per the issue's specific pattern);
  `git grep -nE 'DensePoly\.(AddZeroLaw|SubZeroLaw|ZeroSubNegLaw) Rat'`
  reports the three new instances at lines 17/20/23.

## Current frontier

After this PR the remaining `DensePoly.coeff_(add|sub|neg)` call
sites in `HexPolyZ/Basic.lean` (lines 1073, 1083–1086, 1172–1181,
1826–1924, 2201–2316) use explicit polynomial arguments rather than
the `_ _` underscore-shape and are not eligible for the `coeff_*_ring`
wrapper migration without first generalising the rewrite shape.
`HexHensel/Linear.lean` still carries 8 `DensePoly Int` call sites
that need the parallel `AddZeroLaw Int` / `SubZeroLaw Int` instance
work (per the sibling issue mentioned in #5218's "Out of scope").

## Next step

Hand off to the auto-merge queue. Follow-up work suitable for a
planner:
1. The `AddZeroLaw Int` / `SubZeroLaw Int` instance pair to
   unblock the `HexHensel/Linear.lean` migration (referenced by the
   previous progress file `20260519T022251Z_f51e3e9f.md` and by the
   "Out of scope" section of #5218).
2. Optionally, a second pass on `HexPolyZ/Basic.lean` to migrate
   the explicit-argument `coeff_*` sites by first rewriting them to
   the `_ _` underscore-shape, then collapsing into `coeff_*_ring`.

## Blockers

None.
