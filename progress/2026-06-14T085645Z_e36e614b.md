# Session 2026-06-14T08:56Z — feature (e36e614b)

## Accomplished

Claimed #7109 (feat: resultant valuation for the cut CLD short-vector
auxiliary polynomial — residual of unsound #6949). Did the premise check
the directive explicitly requested before coding, found the proposed
proof route structurally inadequate, posted a concrete diagnosis, and
skipped the issue back to `replan`.

Key findings (full detail in the issue comment):

- The proposed route instantiates `sylvester_mulVec_cld_syzygy`
  (`Resultant.lean:658`) with `g := aux`, which requires a polynomial
  syzygy `aux*q - input*q' ≡ 0 (mod p^k)`. The cut breaks this:
  `aux ≢ cldQuotientMod (mod p^k)`.
- `auxCutBridge` (`CLDColumnBound.lean:560`) is only a per-coefficient
  scalar identity with a *position-dependent* weight `p^{ℓ_j}`
  (`ℓ_j = bhksCoeffCutThreshold`). Position-dependent coefficient scaling
  is not a polynomial multiplication, so it cannot assemble the
  `p^k`-divisible Sylvester columns `pow_dvd_resultant_of_sylvester_cols`
  needs. This is the #6949 defect relocated, not resolved.
- Concrete `#eval` evidence: `input=(x-2)(x-3)`, `p=5`, `k=2`, `q=x-2`.
  `bhksCoeffCutThreshold` gives `ℓ_j=3` so `p^{ℓ_j}=125 > p^k=25`; the
  centered CLD coeffs `(-3,1)` are below the cut, so `cldCoeffs = #[0,0]`
  and `aux = 0`. Then `aux*q - input*q' = -input ≢ 0 (mod 25)`: the
  `hsyz` hypothesis is literally false for `g := aux`.
- Structural reason this is not an edge case: `p^{ℓ_j} ≥ 2·B_j+1` while
  the exact CLD coeff is `≤ B_j`, so the cut returns 0 on the entire
  bound-recovered regime. All nonzero `aux` content (hence all real
  resultant-valuation content) lives in the under-recovered bad-vector
  regime — the genuine research-level BHKS Lemma 3.2, not derivable from
  the cited merged building blocks. The achievable exponent for the
  cut/scaled combination there is also not established.

No code changes, no `sorry`/`axiom`, theorem not weakened. The directive
author anticipated exactly this exit ("the cut may change the attainable
valuation ... post concrete evidence and leave this claimable").

## Current frontier

#7109 is back in the `replan` queue with a detailed obstruction comment.
The `resultant_divisible_by_p_pow` field in `IsBhksBadVectorSetup` /
`BadVectorBridgeData` / `ProjectedBadVectorSetupBridge` remains a bare
assumption (`BadVector.lean:1038, 1288, 1472`).

## Next step

A planner should reformulate #7109. Two plausible directions noted in the
comment: (a) a matrix/determinant-level argument handling the diagonal
`p^{ℓ_j}` scaling between `sylvester input aux` and
`sylvester input (Σ vecᵢ cldQuotientMod_i)`, with the achievable exponent
re-derived for the cut; or (b) reconsider whether this field should be
derived here at all vs. supplied from the van Hoeij correspondence that
governs the bad-vector regime.

## Blockers

The honest blocker: proving the divisibility on the bad-vector regime is
BHKS Lemma 3.2 for the cut vector — research-level, not session-sized,
and not closable via the building blocks the plan cited.
