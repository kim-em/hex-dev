# Accomplished

- Repaired the tactic candidate search so only failed hypothesis probes are
  observed; live planner, schema-selection, and quote-fidelity failures now
  propagate with their specific diagnostics.
- Added isolated negative checks for a missing replay address and for an exact
  address selecting the wrong schema, while retaining the malformed-quote
  check.
- Generalized `SchemaName`, `EmitPackage`, and `SchemaTable` over a frontend
  handle type. The Mathlib-free core no longer exposes `Lean.Name`; the
  Mathlib companion instantiates the handle with `Lean.Name`.
- Corrected the SPEC to distinguish the canary's exact trace-shape gate from
  `Session.complete`, and documented that semantic-package ownership and
  coverage remain an explicit precondition for dynamic assembly.
- Passed the three focused interval builds plus copyright, import-DAG,
  trust-surface, Phase 4, whitespace, and banned-construct checks.

# Current frontier

- Exact-address lookup is unambiguous and frontend-polymorphic, but the canary
  still checks selected names against a preassembled sine proof.

# Next step

- Tie emitter package ownership and coverage to the semantic registry, then
  use selected handles and the chronological evidence table to construct
  replay applications directly.

# Blockers

- None.
