# BZ Basic.lean monolith split (#8599)

## Accomplished

- Split `HexBerlekampZassenhaus/Basic.lean` (~19.1k lines) into 16
  dependency-ordered leaves and `HexBerlekampZassenhausMathlib/Basic.lean`
  (~22.7k lines) into 16 leaves, deleting both monoliths with no shim.
- Cut only at flat-namespace declaration boundaries so each leaf wraps
  uniformly (`namespace Hex` for the executable library; `namespace ... +
  noncomputable section + open Polynomial` for the Mathlib region, with the
  computable #7474 tail as its own `ToMonicUniqueness` leaf outside the
  noncomputable section).
- Each leaf `public import`s its predecessor (chain re-export), the
  executable leaves also `public meta import` it (for `#guard`/native eval),
  and every leaf `import all`s all earlier leaves to reconstitute the
  single-module reducibility the proofs need (the non-`@[expose]`
  `factorTrial` defeq forced this).
- Repointed importers: umbrellas `public import` every leaf; in-library
  siblings import the tail leaf; external consumers (bench, conformance,
  HexManual, reports) import the umbrella; `IrreducibleCertTest` `import
  all`s the 16 executable leaves for kernel transparency of checker bodies.
- Narrowed `maxHeartbeats` (per request): dropped the executable file's two
  bare global `set_option maxHeartbeats 800000` (nothing needed them once
  `import all` restored transparency); in the Mathlib file scoped the bare
  `3000000` to `factor_entries_not_associated` via `... in` and dropped the
  redundant `4000000`/`2000000` cluster entirely.
- Verification: full `lake build` green (4118 jobs); HexConformance,
  hexbz_bench, emit-fixtures, and the three spike exes build;
  `check_dag.py` passes; declaration count conserved (exec 891, Mathlib
  534); leaf bodies reconstruct the original namespace bodies line-for-line;
  no `sorry`/`axiom`. Codex second opinion found no blockers/major issues;
  addressed its one minor note (stale `Basic.lean` comment references).

## Current frontier

- Both splits landed on branch `issue-8599`, committed; PR pending.

## Next step

- Open the PR, then watch for merge conflicts / CI.

## Blockers

- None.
