# 2026-05-07 17:17 UTC — HO-1 rollback partial (30838663)

## Accomplished

Claimed human-oversight issue #2564 and completed the first required rollback
PR deliverable by changing `HexBerlekampZassenhaus.done_through` from 3 to 0 in
`libraries.yml`. The `HexBerlekampZassenhausMathlib` bridge phase marker was
left at 3, as requested by the issue.

Verification passed:

- `python3 scripts/status.py HexBerlekampZassenhaus`
- `python3 scripts/status.py`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

The executable HexBerlekampZassenhaus library is now back at Phase 1 readiness
against the post-#2563 van Hoeij CLD spec. The remaining HO-1 implementation
work is still open: `factorSlow`, `factorFast`, `factor`, the BHKS CLD lattice
rewrite, and the corresponding Mathlib bridge proof obligations.

## Next step

Open a partial PR for the rollback, leaving #2564 marked for replan so the
implementation rewrite can be handled separately.

## Blockers

The remaining implementation/proof rewrite is too large for this rollback-only
partial PR. The worktree still contains the pre-existing pod-managed
`.claude/CLAUDE.md` modification, left untouched.
