# issue #8530 — level-aware classical decline boundary

(Note: this session follows the issue-8533 session whose progress file is
stamped `20260702T130000Z` — a future-rounded timestamp; this file's real
UTC stamp sorts before it.)

**Accomplished**
- Re-checked #8530's stated blockers before starting (the issue is filed
  as blocked): #8417 closed with zero real sorries in
  `HexBerlekampZassenhaus*` (blocker 1), #8517 merged so the dispatch
  shape is stable (blocker 2), #8529 merged so re-measurement was
  required (blocker 4). Blocker 3 (the merge-required lattice conformance
  case, #8531) is still open — see "Current frontier".
- Re-measured the SD5(x)·SD5(x+1) witness (deg 64, r = 32) on the
  post-#8529 executable via a temporary compiled probe (repurposed
  `hex_classical_spike`, restored afterwards): classical decline burn
  4.71 s over the full 262144 candidates, hybrid total 16.6 s,
  tier = "lattice", correct two 16-blocks. The wasted probe is now ~28%
  of wall time (was ~90% pre-#8529) — still worth removing, and the fix
  is structural for larger r.
- Implemented `levelAwareSubsetBudget` (Basic.lean, next to
  `defaultSubsetBudget`): tightens the candidate budget up front to the
  largest cumulative subset-size-level boundary `∑_{d≤k} C(r-1,d)` that
  fits; unchanged when every level fits (2^(r-1) ≤ budget, r ≤ 20 at the
  default). `scaledRecombinationSmart` applies it; `candidatesTried` /
  `budgetExhausted` semantics unchanged. #guards pin r ∈ {3, 19, 20}
  unchanged and r = 32 → 206368, r = 64 → 41728 (cross-checked against
  an independent Python computation).
- Re-pointed the two proof sites that spell the budget out:
  `classicalCoreFactorsWithBound_spec` (Basic.lean) and the classical
  irreducibility bridge in `IntReductionMod.lean` (new `set budget :=`
  line). The `RecoveredSmartSearch.{covers,trustworthyNone}_of_bound`
  lemmas are universally quantified over budget/fuel, so no proof content
  changed. Added the level-aware clause to the SPEC's classical-tier
  algorithm step.
- Verified: `HexBerlekampZassenhaus`, `HexBerlekampZassenhausMathlib`,
  `HexConformance` all green; fresh emission byte-identical to committed
  `bz.jsonl`; `bz_trace_gate.py` 50/0; `bz_flint.py` 100/0; post-change
  probe declines at exactly 206368 candidates (4.06 s) and the hybrid
  still lands on tier = "lattice" with the correct factorization.

**Current frontier**
- #8531 (merge-required lattice conformance case) is still open and
  unclaimed. It was a stated pre-condition of #8530; with this PR landing
  first, #8531's trace assertions should pin the *new* boundary
  (`subsetCandidates = 206368`, `declined = true`, tier = "lattice") —
  and its corpus cost estimate drops (the decline burn is 4 s, not 25 s).
  Commented on #8531 accordingly.

**Next step**
- Land #8531's conformance case so the lattice dispatch boundary is
  CI-visible (separate issue/PR).

**Blockers**
- None.
