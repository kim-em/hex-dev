# 20260602T112255Z 1d74b912 — Issue #6311

## Accomplished

- Claimed and completed issue #6311.
- Tagged every public `luebeckConwayPolynomial_<p>_<n>_monic` theorem in
  `HexConway/Basic.lean` with `@[simp]`.
- Tagged the existing public `luebeckConwayPolynomial_2_<n>_degree_pos`
  theorems with `@[simp]`.
- Tagged the generic lookup/conway-polynomial monicity and nonconstant
  lemmas with `@[simp]` so supported-entry and lookup consumers can use
  the same automation surface.
- Added proof-mode conformance examples in `HexConway/Conformance.lean`
  for lookup-shaped monicity, lookup-shaped positive-degree, combined
  lookup facts, and direct concrete-entry `simp` closure.

## Current frontier

HexConway's committed Luebeck slice now exposes monicity and
positive-degree facts through the expected simp surface. The proof-mode
cluster documents the usable downstream shapes.

## Next step

Sibling Phase 6 issue #6314 remains a viable next target. SquareFree
issue #6286 should wait for its prerequisite #6316 to land.

## Blockers

None for this issue. Directive issues #2564 and #2567 remain blocked by
their listed dependencies.

## Verification

- `lake build HexConway.Basic` — green.
- `lake build HexConway.Conformance` — green.
- `lake build HexConway` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token diff grep over `HexConway/Basic.lean` and
  `HexConway/Conformance.lean` found no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
- PCRE scan found no remaining untagged public
  `luebeckConwayPolynomial_<p>_<n>_monic` or `_degree_pos` theorems.
