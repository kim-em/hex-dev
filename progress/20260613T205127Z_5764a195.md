# Progress: #6889 — split Yun normalized product invariant

## Accomplished

In `HexPolyFp/SquareFree.lean`, split the derivative-active Yun product theorem:

- `yunFactorsContributionWithLevel_raw_pow_invariant` now contains the exact raw contribution identity and is the only local theorem that consumes `YunDerivativeActiveLevelStateProvider`.
- `yunFactorsContributionWithLevel_normalized_pow_invariant` now exposes the normalized-tail product form without taking a raw/level provider. It consumes `YunDerivativeActiveNormalizedStateProvider` directly and takes the raw exact product identity as an explicit input.
- Updated `squareFreeAuxRevContribution_derivative_active_pow_obligation` to build the raw and normalized product facts separately, so the normalized-tail product path no longer routes through the combined raw/level theorem surface.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added lines contain no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The normalized-tail product invariant is separated from the false raw level-provider path. The public weighted-product theorem still takes `YunDerivativeActiveRawStateProvider hp` because the executable raw tail recursion still needs raw `pthRoot contribution.2` reachability/fuel facts; that is distinct from proving the normalized-tail product identity.

## Next step

The follow-up that removes raw Yun state from the public square-free weighted-product surface should replace the remaining raw-tail recursion dependency with a normalized-tail-only assembly theorem or a separate proof that raw `pthRoot` validity follows from the normalized carrier used by the executable path.

## Blockers

None for this split. The worktree has a pre-existing uncommitted `.claude/CLAUDE.md` edit that this session did not touch.
