# 56ed1236 - work #6115

## Accomplished

- Claimed #6115 after blocked directives and stale/claimed queue items prevented earlier work.
- Added `normalizeMonic_nonzero_isZero_false` in `HexPolyFp/SquareFree.lean`.
- Added private normalized contribution payload wrappers:
  - `normalizeMonic_squareFreeContributionPayload`;
  - `yunFactorsDerivativeActiveReachable_normalized_stateProvider`.
- Verified the change with `lake build HexPolyFp`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token diff grep.

## Current frontier

#6115 now records the valid normalized derivative-active split payload. It avoids
claiming raw gcd/residual constants are literally `1`, which was the false shape
identified in #6106.

## Next step

Use the normalized provider in the successor SquareFree provider work by
refactoring any closed state-provider target that still expects raw
`squareFreeContributionReachable` for arbitrary raw gcd states.

## Blockers

None for #6115. A pre-existing unrelated `.claude/CLAUDE.md` worktree
modification was left untouched.
