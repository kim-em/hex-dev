# 20260602T125942Z 017d0b7d — Issue #2567

## Accomplished

- Followed directive priority. Could not claim #6335 because it was already
  claimed by another agent; claimed directive #2567 instead.
- Verified the named prerequisite issues for the current BHKS D1 assembly lane
  (#5216, #5204, #5224, #5223) are closed.
- Added
  `BHKS.factorFast_ne_none_of_capSeparationBridgeDataCanonicalIndicatorsAtPrecisionForCoeffBound`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The theorem is a bridge-data capstone wrapper: it accepts the packaged
  `BadVectorBridgeData` plus cut/resultant side conditions, assembles
  `ExecutableCapSeparationHypotheses`, and composes with the existing
  cap-separation canonical-indicator `factorFast` success theorem.

## Current frontier

The public unconditional theorem
`factorFast_terminates : ∀ f : Hex.ZPoly, Hex.factorFast f ≠ none` is still not
proved. The new wrapper narrows the final assembly surface by removing one
manual cap-separation packaging step, but the genuinely mathematical recovery
facts remain explicit: cut hypotheses, bad-vector bridge data, resultant-bound
strict inequality, non-degeneracy, per-indicator candidate reconstruction, and
product equality.

## Next step

Discharge the remaining executable recovery facts for the scheduled cap lift
and compose them through the new bridge-data wrapper, or decompose #2567 into
the remaining independent proof packages if those facts are still too broad for
one worker session.

## Blockers

- No technical build blocker.
- Full #2567 remains mathematically incomplete because the unconditional
  production of the recovery hypotheses listed above is not yet available in
  the codebase.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green.
- `lake build` — green.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Diff forbidden-token grep over `HexBerlekampZassenhausMathlib/Recovery.lean`
  found no added `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
