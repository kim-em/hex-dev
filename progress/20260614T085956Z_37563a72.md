# Session 37563a72 — feature (#7112)

## Accomplished

Added Phase 6 `/-- ... -/` docstrings to the four private
inverse/power specification lemmas in `HexModArith/Basic.lean`:

- `invBezout_mul_mod_eq_one` — Bézout cofactor to modular inverse step.
- `nat_mod_mul_pow_square_even` — even case of one square-and-multiply step.
- `nat_mod_mul_pow_square_odd` — odd case of one square-and-multiply step.
- `pow_go_toNat` — loop invariant of the `pow.go` tail recursion.

Docstring-only: 17 insertions, 0 deletions. No statement, proof,
attribute, import, or namespace touched. `lake build HexModArith.Basic`
clean with no new warnings; `git diff --check` clean; no banned tokens
or em-dashes on added lines.

## Current frontier

`HexModArith/Basic.lean` docstring coverage for the inverse/power
cluster is complete.

## Next step

The sibling carry/borrow private-lemma cluster (~lines 228-384) and
`HexModArith/Ring.lean` (#7113) remain for full Basic.lean Phase 6
docstring coverage. Note: this does not by itself license a
`done_through: 6` bump.

## Blockers

None.
