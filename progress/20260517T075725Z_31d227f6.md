# 20260517T075725Z — planner session 31d227f6

## Accomplished

Filed one new feature issue at `POD_QUEUE_DEFICIT = 1`:

* **#4599** HO-1 substrate: `factorFastFactorsWithBound` dispatch
  classifier (plan body in `plans/31d227f6-1.md`). Mirrors the existing
  slow-side `factorSlowFactorsWithBound_branch`
  (`HexBerlekampZassenhaus/Basic.lean:5043`) with a nine-way classifier
  for the fast path: (a) constant, (b) B = 0 none, (c–e) B = 1
  sub-cases, (f–i) B > 1 sub-cases.  Substrate for the HO-1 capstone
  `factor_irreducible_of_nonUnit`
  (`HexBerlekampZassenhausMathlib/Basic.lean:237`, #4170): the capstone
  proof currently has no reusable fast-side classifier and would
  otherwise have to inline the nested-`if` dispatch case-split.  No
  dependencies — actionable immediately.

## Survey notes

* Open PRs: #4596 (PR for #4587 slow-quadratic refactor, CI in
  progress — not yet mergeable) and #2656 (kim-em's draft SPEC PR,
  not planner-mergeable).  Nothing to merge.
* No PR-repair candidates (`coordination list-pr-repair` empty).
* `coordination list-replan` reports only #4593 outstanding; #4593
  was already decomposed into #4597 (Mathlib-bridge variant) by an
  earlier worker session — replan triage is the `/replan` cycle's
  job, not mine.
* No directives need new infrastructure issues: the six open
  directives (#3900, #3899, #3662, #2637, #2567, #2564) are either
  long-running (BHKS, BZ rewrite) or library-specific cleanup
  unrelated to current HO-1 substrate flow.
* HO-1 substrate landscape after this filing:
  - Per-arm umbrellas: 4 of 5 landed (constant #4565, small-mod
    singleton #4564, fast-quadratic #4571, slow-quadratic #4575);
    exhaustive #4561 blocked on #4567 (claimed).
  - `hcomplete` dischargers in flight: #4585 (constant arm, claimed),
    #4594 (quadratic arms, open), #4597 (small-mod singleton arm,
    open).
  - Slow-quadratic refactor #4587 in flight via PR #4596.
  - Dispatch classifier: now tracked as #4599 (this filing).
* `scripts/status.py` shows HexBerlekampZassenhaus still on Phase 2
  (scaffolding review) gating its `…Mathlib` partner on Phase 3; no
  cross-library dispatch is currently bottlenecked beyond the HO-1
  chain.

## Current frontier

Queue replenished to `min_queue = 3` (was 2: #4334 LLL-bench
environmental wait + #4594 quadratic-arm `hcomplete`; now +#4599).
The HO-1 capstone partial assembly (4 of 6 arms wired, exhaustive +
BHKS sorry-ed) becomes filable once #4585, #4594, #4597, #4587 all
close — those four are all in flight as of this session, so a
follow-up planner cycle one or two iterations out should carve that
assembly issue.

## Next step

For the next planner cycle: if `POD_QUEUE_DEFICIT > 0` and the four
HO-1 substrate pieces above have landed, file the partial-assembly
issue depending on #4599 + #4585 + #4594 + #4597 (+ #4587 if not
absorbed by its merge). Otherwise, look at the `_hsquareFree`
parameter drop on `prod_berlekampFactor` noted in
`progress/20260517T120000Z_9184c97e.md`'s "Next step" §2 — a small,
no-dependency cleanup.

## Blockers

None for the planner.
