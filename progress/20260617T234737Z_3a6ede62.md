# Session 3a6ede62 — expose fast-core recovery witnesses (#7846)

## Accomplished

Claimed #7846 (child of directive #2564, HO-1 critical path) after the
directive #2564 itself failed to claim — it is deadlocked by a circular
dependency (see Blockers).

Landed **deliverable 1** of #7846: a proof-facing extractor over the BHKS
fast-recombination loop that retains the lift data the per-factor success
lemmas discard.

- `HexBerlekampZassenhaus/Basic.lean`:
  - `bhksRecoverClassified_success_indicatorCandidates` (private) — unfolds a
    `.success` classified recovery to the positive-dimension witness, the
    `bhksIndicatorCandidates? ... = some candidates` equality, and
    non-degeneracy.
  - `factorFastCoreWithBound_some_classifiedSuccess` (private) — recursion over
    the precision schedule extracting the successful precision index.
  - `factorFastCoreWithBound_some_indicatorCandidates` (public) — composes the
    two; conclusion uses only public symbols (no private `bhksRecoverClassified`),
    exposing `∃ k' hrows, bhksIndicatorCandidates? ... = some coreFactors ∧
    nondegenerate ∧ product`.
- `HexBerlekampZassenhausMathlib/Recovery.lean`:
  - `representsIntegerFactorAtLift_of_indicatorCandidates` — assembles
    `ForwardRecoveryInputs` from the public success data and applies
    `bhksIndicatorCandidates?_canonicalRepresentations`, yielding the selected
    lifted-factor subset witness `RepresentsIntegerFactorAtLift` for each
    emitted candidate.

Together these expose all three things the issue said the existing API
discarded: the successful precision, the `bhksIndicatorCandidates?` equality,
and the per-candidate selected-subset witness.

Builds green: `HexBerlekampZassenhaus`, `HexBerlekampZassenhausMathlib`
(Recovery + downstream). Zero new sorry/axiom.

## Current frontier

Deliverables 2 and 3 of #7846 remain. They are the *derivation* of the
`TrueFactorLift` / `RecoveredLift` family from the per-candidate witnesses
(not an assumption — the issue forbids restating `∀ S, TrueFactorLift`).

## Next step

Deliverable 2: map `RepresentsIntegerFactorAtLift core d factor T` into the
shape `recoveredLiftOfSubset` / `trueFactorLiftOfSubset` consume. **Key
finding for the next worker:** there is a normalization-regime mismatch.
`recoveredLiftOfSubset.recovered_eq` is `dilate (leadingCoeff f)
(centeredLiftPoly (liftedFactorProduct d T) (p^a)) = factor`, and is used in
the **monic** setting (`trueFactorLiftSemanticsOfToMonicSubset` instantiates
`f := (toMonic core).monic`, so `leadingCoeff = 1`). The witness from
deliverable 1 is over the non-monic `core` and carries
`primitivePart (dilate (leadingCoeff core) monicFactor) = factor` with
`monicFactor ∣ (toMonic core).monic`. Bridging requires routing through the
`toMonic` normalization (where `lc = 1` collapses the dilate, and the
centered monic representative coincides with `monicFactor`), reusing the
existing `centeredLiftPoly_*_eq_factor_of_recovery*` lemmas at
`HexBerlekampZassenhausMathlib/Basic.lean:5606–5672`. This is a genuine
proof effort, not a rename.

Deliverable 3 (the `∀ S : trueSupports` family for
`cutProjectionHypotheses_of_trueFactors`) builds on deliverable 2 by
re-indexing the per-candidate (`Fin`-indexed) witnesses through
`supportPartitionByMinColumn` / `liftedFactorSubsetsOfSupports`.

## Blockers

Directive #2564 (the HO-1 capstone, project's stated top priority) is
**deadlocked**: it `depends-on` #4818 and #4172 (both open), which are
`blocked` on #6672, which is `blocked` on #2564 — a dependency cycle that
`coordination check-blocked` cannot break (none of the cycle's deps will
ever close on their own). `coordination claim 2564` fails with "blocked by
dependencies". Both #4818 and #4172 reduce to the single keystone sorry
`factor_irreducible_of_nonUnit` (`HexBerlekampZassenhausMathlib/
FactorSoundness.lean:20`), on which `Hex.ZPoly.isIrreducible_iff`
(`Basic.lean:235`) also depends. Worth surfacing to a planner: the cycle
should be broken so the keystone can be claimed directly under #2564.
