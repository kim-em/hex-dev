# HexConway Phase 7 reference chapter (#8210)

## Accomplished

- Authored `HexManual/Chapters/HexConway.lean`, a Verso reference
  chapter for the Tier 1 Conway-polynomial lookup layer. Sections:
  introduction (three-tier framing), the lookup surface
  (`luebeckConwayCoeffs?`, `luebeckConwayPolynomialOfCoeffs`,
  `luebeckConwayPolynomial?`), the `SupportedEntry` witness, a worked
  `#guard` example over `(2, 3)` plus unsupported `(2, 7)`/`(2, 0)`, the
  per-entry irreducibility theorem (representative
  `luebeckConwayPolynomial_2_3_irreducible`), and cross-references.
- Wired the chapter into `HexManual.lean` (import + `{include 0 ...}`).
- Bumped `libraries.yml[HexConway].done_through` 6 → 7.

## Decisions

- Embedded `SupportedEntry` via `{name}` + prose rather than
  `{docstring}`: its fields carry no docstrings, and `{docstring}` on a
  structure errors on undocumented fields. This keeps the computational
  library untouched (no recompile, no risk).
- Validated all `#guard` values against the fast `HexConway` library in
  a throwaway file before the slow `HexManual` build.

## Verification

- `lake build HexManual` green (warnings are pre-existing, in
  `HexGF2Mathlib`).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` lists `HexConway` under "Fully done".
- No `axiom`/`sorry`/`native_decide`; no Mathlib dep added to the
  computational library (only `HexManual` chapter + `libraries.yml`
  touched).

## Current frontier / Next step

`HexConway` Phase 7 complete. Sibling Phase 7 chapters still open:
#8211 (HexPolyMathlib) remains unclaimed.

## Blockers

None.
