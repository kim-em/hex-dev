**Accomplished**
- Reviewed the proposed follow-up plan against source locations in `Basic.lean`,
  `FactorSoundness.lean`, `Recovery.lean`, `BadVector.lean`,
  `BHKSBound.lean`, `TerminationBound.lean`, `HotPathDiscriminant.lean`,
  `Bench.lean`, the Isabelle wrapper, and lean-bench internals.
- Verified the SPEC D1 antecedent bug: the executable fast path calls
  `choosePrimeData?` on `(normalizeForFactor f).squareFreeCore`.
- Verified cited Layer-A helper lemmas exist, and spot-checked that the D1
  recovery infrastructure is sorry-free but still requires substantial producer
  records beyond a good-prime witness.
- Ran a small local probe against the cached Isabelle wrapper showing steady
  same-session requests are sub-millisecond for small split/adversarial inputs;
  this weakens the plan's blanket "820 ms per request inside the persistent
  server" diagnosis.

**Current frontier**
- Final response should emphasize that D1 is plausible as a staged proof effort
  but not "mere hypothesis discharge" in the cheap sense: `BadVectorBridgeData`,
  `CutProjectionHypotheses`, canonical recovery tail inputs, schedule reachability,
  and exact expected-factor packages remain real proof work.
- Benchmark plan needs a stronger diagnosis phase that separates Lean child
  startup, `setup_bz_isabelle.sh`, Haskell process startup, first request, steady
  in-server requests, JSON/marshalling, and actual `factor_int_poly` work.

**Next step**
- Deliver the evidence-based review with file:line references and prioritize
  blockers/landmines.

**Blockers**
- None for the review. The local `setup_bz_isabelle.sh` invocation fails in this
  sandbox because Isabelle attempts blocked `sysctl` calls, so wrapper setup
  timing is not cleanly measurable here.
