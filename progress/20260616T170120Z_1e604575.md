# Work: BHKS cut projection composition (#7594) — diagnosed unsound, routed to replan

## Accomplished

- Claimed #7594 (`feat: produce BHKS cut projection from separation and
  true-factor vectors`) as a one-shot `feature` worker. The stale `blocked`
  label (deps #7592/#7593 both closed) was cleared via `coordination
  check-blocked` before claiming.
- Investigated the directive premise instead of executing it. Found it stale
  on two independent grounds and routed #7594 to `replan` with a corrected
  dependency graph. No code, no PR — this was a premise-defect diagnosis.

## Current frontier

#7594 is the final composition in the chain `#7591→#7592→#7593→#7594→#6771`:
produce `CutProjectionHypotheses` (forward `W ⊆ L'` membership) from a prefix
survivor-span fact plus a true-factor norm bound. Both proof inputs are open:

- **#7620** (OPEN, `replan`, empty body): the *sound* full-lattice prefix
  survivor-span proved directly into `CutProjectionHypotheses`. This replaced
  #7592's `CutRetention` proof half, which was found unsound (quantifies over
  arbitrary projected `ℤ^r` vectors, dropping the lattice hypothesis;
  `IsEmpty (CutRetention P)`). #7592 landed only its executable prefix cut
  (PR #7617).
- **#7616** (OPEN): the true-factor `‖v‖² ≤ bhksCutRadiusSq4` norm-bound
  producer (#7593 deliverables 3–4). #7593 landed only deliverables 1–2
  (PR #7613): the vector, structural membership, and the obligation structures.

Additionally the directive's named mechanism — "Feed
`cutProjectionHypotheses_of_retention`" (`Lattice.lean:526`) — consumes a
`CutRetention` (`Lattice.lean:497`), which is unsatisfiable; #7575 (to produce
it) was rejected (`cb79aefd`). The sound route retires that mechanism.

## Next step

A planner should rewrite #7594's body to compose **#7620**'s
`CutProjectionHypotheses` producer with **#7616**'s norm bound, dropping the
`cutProjectionHypotheses_of_retention` / `CutRetention` framing. #7594 stays
blocked until both land. Real work order: land #7620 and #7616 first.

## Blockers

#7594 cannot be executed until #7620 and #7616 land. Executing it now would
require either duplicating those issues' claimed lemmas or introducing
sorries — both forbidden. Dependency graph corrected via `coordination
add-dep 7594 7620` and `... 7616`; issue marked `replan`.
