# Session 20260604T045221Z

## Accomplished

Reviewed the proposed SPEC tightening around singular-step handling for
the column-major Bareiss scaled-coefficient kernel and the per-row Schur
kernel.

Checked [HexGramSchmidt/Int.lean](../HexGramSchmidt/Int.lean) at the
kernel definitions and singular lemmas:

- `scaledCoeffArrayLoop` writes the current column, then halts on a
  zero non-final pivot.
- `scaledCoeffRowsSchur` writes every visited cell with `j <= i`.
- Existing singular lemmas characterize columns strictly past the
  singular step on the column-major side as unwritten zero cells.

## Current frontier

The SPEC tightening is directionally correct but should be edited before
merge. The main wording issue is that "column zero" is Mathlib-free as a
cell characterization, not as a structural zero case.

## Next step

Apply the review's replacement text to the relevant
`SPEC/Libraries/hex-gram-schmidt.md` bullets, then re-read the full
Mathlib-bridge paragraph for timelessness.

## Blockers

None.
