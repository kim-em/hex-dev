# 20260515T230028Z — refactor: relocate zmod64FieldOfPrime out of HexBerlekamp.Basic

Issue: #4329. Branch: `agent/6eb17838`.

## Accomplished

- Created `HexPolyFp/PrimeField.lean` as the single Mathlib-free home for the
  `Lean.Grind.Field (ZMod64 p)` instance, with its supporting prime-modulus
  helpers exposed under `Hex.ZMod64`: `one_ne_zero_of_prime`,
  `inv_zero_of_zero`, `inv_ne_zero_of_prime`, `inv_inv_of_prime`. The
  `Lean.Grind.Field` instance itself is `Hex.zmod64FieldOfPrime` and takes
  `[ZMod64.PrimeModulus p]` as its sole non-`Bounds` hypothesis.
- Added the new module to the `HexPolyFp.lean` umbrella so consumers pick the
  instance up via the existing `import HexPolyFp` chain.
- Deleted the 100-line inline copy in `HexBerlekamp/Basic.lean` (the
  `private` `zmod64ZPow`, four `zmod64_*` helpers, and the `zmod64FieldOfPrime`
  instance). `HexBerlekamp/Basic.lean` now relies on the shared instance via
  the `[ZMod64.PrimeModulus p]` context already attached to the matrix-kernel
  lemmas.
- Replaced the corresponding block in `HexBerlekampZassenhaus/Basic.lean`
  with a six-line `@[reducible] private def fieldOfNatPrime` adapter that
  bridges the local `Nat.Prime p` (which resolves to `Hex.Nat.Prime p` inside
  `namespace Hex`) to the shared field instance through
  `ZMod64.primeModulusOfPrime`. All ten `smallPrimeCandidates` entries and the
  `fallbackPrimeChoiceData` candidate now read `field := @fieldOfNatPrime N
  bounds_N prime_N`. The single remaining caller of the old helper,
  `monicModularImage_monic`, now invokes the public
  `ZMod64.inv_ne_zero_of_prime` from the shared module.

## Verification

- `lake build HexBerlekamp HexBerlekamp.RabinSoundness HexBerlekamp.Factor
  HexBerlekamp.Irreducibility HexBerlekamp.DistinctDegree
  HexBerlekampZassenhaus` — passes; pre-existing `sorry` warnings in
  `HexGramSchmidt`, `HexLLL.Basic`, `HexMatrix.Bareiss`, and
  `HexBerlekampZassenhaus/Basic.lean:5552` only.
- `python3 scripts/check_dag.py` — passes silently.
- `git diff --check` — clean.
- `HexBerlekamp/Basic.lean`: 984 → 883 lines.
- `HexBerlekampZassenhaus/Basic.lean`: 8554 → 8443 lines.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`
  in the diff.

## Current frontier

Sibling dedupe issue #4330 (`linearPow_zero_of_pos` between
`HexPolyFp/Compose.lean` and `HexBerlekamp/Basic.lean`) is the natural
next cleanup pass through the same audit. The shared
`HexPolyFp/PrimeField.lean` is now available as a precedent for the same
"single home, public re-exports" pattern.

## Next step

Land the PR; close issue via squash-merge.

## Blockers

None.
