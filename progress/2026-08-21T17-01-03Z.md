# Supported tactic final cleanup

## Accomplished

- Removed the unused public program/input/event quotation surface, including
  the unexercised polymorphic event constructors, while retaining only the
  checked endpoint, interval, raw-cut, and precision quotation used by emitted
  arithmetic proofs.
- Made `interval_bound` render its concrete selected lower and upper cuts and
  the original queried expression; pinned the representative `[2, 4]` text.
- Made false singleton-equality targets return the same exact derived-endpoint
  diagnostic as false inequalities and added a regression.
- Made the automatic-regularization depth cost explicit in the tactic report
  and current-state specifications: term depth 32 corresponds to about 16
  nested arithmetic operations along a source spine.

## Current frontier

The supported tactic behavior is unchanged apart from clearer diagnostics and
the removal of dead exported Meta helpers.

## Next step

Publish the cleanup on PR #9351 and let exact-head automatic CI supersede the
previous approved run.

## Blockers

None.
