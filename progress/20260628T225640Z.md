# Released repo CI pin diagnosis

## Accomplished

- Compared `/private/tmp/hex-gs-repro`, `/private/tmp/hex-lll-repro`,
  `/private/tmp/released-head/*`, and the monorepo checkout.
- Confirmed the macOS repro clones pin and build against
  `hex-matrix@0b7d7242225e73bda36ea0d58ccdc526363ae4c1`, whose source
  contains `HexMatrix.DotProduct`, `Vector.dotProduct_sub_smul_left`, and
  `@[expose] def Vector.dotProduct`.
- Found older released-head clones where `hex-gram-schmidt` and `hex-lll`
  pin `hex-matrix@85c2bb9b2f93677fe1435d898c81a56b9e7b33e8`; that older
  matrix commit lacks the new generic `HexMatrix.DotProduct` split and the
  generic `Vector.dotProduct_sub_smul_left` theorem.
- Verified the current local `hex-gs-repro` and `hex-lll-repro` builds
  succeed against the newer `0b7d7242` dependency.

## Current frontier

- The likely issue is stale/different released-repo CI input, not a
  Linux-vs-macOS Lean module-system difference.

## Next step

- On a machine with GitHub access, inspect the failing workflow run's
  `headSha` and the `lake-manifest.json` in that exact commit, then compare
  `.lake/packages/HexMatrix`'s checked-out SHA printed during CI.

## Blockers

- `gh` could not reach `api.github.com` from this sandbox, so live run
  metadata and logs could not be fetched here.
