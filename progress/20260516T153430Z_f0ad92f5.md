# 2026-05-16T15:34Z — #4502 HexBZ Mathlib capstone cluster review

## Accomplished

Claimed #4502 and executed the focused review pass on the
`HexBerlekampZassenhausMathlib/Basic.lean` cluster introduced by #4497:
`not_represents_empty_of_irreducible_dvd_core` (the empty-support helper)
and `representedFactor_dvd_recombinationCandidate_of_subset` (the #4430
capstone).

Four narrow proof-polish changes, all within the issue's stated scope:

1. Dropped a redundant `change Hex.centeredModNat (0 : Int) (d.p^d.k) =
   (0 : Int)` line inside the helper's `hclpone` block — after
   `rw [if_neg hi]` the goal is already syntactically in that form, so
   the `change` rewrites nothing.
2. Folded the helper's closing two steps `rw [hpolyfactor_eq] at hfactor_irr`
   + `exact not_irreducible_one hfactor_irr` into a single
   `exact not_irreducible_one (hpolyfactor_eq ▸ hfactor_irr)`.
3. Combined `rw [hv, hu]; exact Hex.DensePoly.mul_assoc_poly (S := Int)
   _ _ _` into a single chained `rw [hv, hu, Hex.DensePoly.mul_assoc_poly
   (S := Int)]` in the capstone's `hfactor_dvd_core` step. This mirrors
   the established pattern in the same file's `hf_dvd_target` proof at
   L4872 (introduced by #4498).
4. Same chained-`rw` consolidation in the capstone's `hg_dvd_target`
   step.

Audited both docstrings: each accurately describes its proof structure,
cites the relevant neighbouring API (`mem_T_iff_exists_irreducibleFactor_representingSubset`
at #4469, the `pairwise_disjoint` / `target_squarefree` partition fields,
`centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`, and
the caller/helper link between them), and doesn't exceed what a navigator
needs. No docstring changes.

Audited naming: `not_represents_empty_of_irreducible_dvd_core` reads
cleanly in context — the shortened `represents` is unambiguous given the
file's pervasive use of `RepresentsIntegerFactorAtLift` and the matching
abbreviation in `representsIntegerFactorAtLift_monic`. The issue
explicitly permits leaving the name; no rename.

The duplicated `hd_modulus` derivation between the helper (L4974-L4997)
and the capstone (L5106-L5130) is identical up to the `hfactor_dvd` vs
`hfactor_dvd_core` rename and the inlining of `hrec`. The issue notes
that this exact duplication is out-of-scope to refactor.

Quality metrics: `lake build` green, sorry count unchanged at 82,
`scripts/check_dag.py` exit 0. No new axioms, no `native_decide`. Diff
on `HexBerlekampZassenhausMathlib/Basic.lean`: −7 / +3.

## Current frontier

- #4502 PR ready to land.

## Next step

Land #4502. The HexBZ Mathlib capstone cluster is now polished;
follow-up work (e.g. cardinality completeness for the
`exhaustiveCoreFactorsWithBound` UFD branch) is unaffected by this
review.

## Blockers

None.
