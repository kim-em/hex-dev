# 2026-06-01T09:51Z — HexMatrix Pluecker helper substrate (#5851)

## Accomplished

- Claimed #5851 and inspected the current Pluecker helper block in
  `HexMatrix/Determinant.lean`.
- Added the missing `mDet_add_v` and `mDet_smul_v` wrappers promised by the
  issue text, proving additivity and homogeneity of `mDet` in the augmented
  vector column via `mMatrix_eq_colReplace_last` and existing
  `det_colReplace_*` lemmas.
- Added `mDet_eq_sum_basisVec`, expanding `mDet B v p` as the finite sum over
  standard basis-vector columns. This packages the first step of the suggested
  final Pluecker proof strategy without adding `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The full universal 3-term Pluecker identity remains unproved. The remaining
work is the sign-cancellation step after rewriting each basis-vector summand
with `mDet_basisVec_eq_zero_of_eq`,
`mDet_basisVec_eq_signed_nDet_of_lt`, and
`mDet_basisVec_eq_signed_nDet_of_gt`.

## Next step

Create or prove a focused sign-normalization lemma for the three ordered rows
`p1 < p2 < p3` and an arbitrary basis row `q`, then use
`mDet_eq_sum_basisVec` to reduce the general vector statement to that lemma.

## Blockers

No build blockers. The remaining proof is larger than this session's bounded
helper substrate and should continue from the compiled lemmas above.
