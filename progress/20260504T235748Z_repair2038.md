# PR #2038 repair

## Accomplished

- Claimed PR #2038 for repair.
- Merged `origin/main` into `agent/9db668be`.
- Resolved the `.github/workflows/conformance.yml` conflict by preserving the matrix-based `oracle-pyflint` job from the PR and adding the `HexPolyZ` and `HexGfqRing` oracle entries from `main`.
- Verified the conformance matrix invariant and ran `lake build` successfully.

## Current frontier

The PR branch is locally merge-conflict-free and builds after the merge resolution.

## Next step

Push the resolved PR branch with `--force-with-lease` and mark PR #2038 salvaged.

## Blockers

- None.
