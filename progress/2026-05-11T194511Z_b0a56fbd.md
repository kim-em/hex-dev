# 2026-05-11 19:45 UTC — review session (b0a56fbd)

## Accomplished

- Claimed #3571 for HexConway Tier 1 committed-table API Phase 6 polish.
- Reviewed `HexConway/Basic.lean` for names, docstrings, `[simp]`
  hit/miss lemmas, and the theorem surface around monicity, positive
  degree, and irreducibility.
- Applied two small polishes:
  - Replaced the inline 10-line `Hex.Nat.Prime 2` proof inside
    `supportedEntry_2_1` with a direct reference to `prime_two`
    (which is already proven a few hundred lines earlier in the same
    file). `supportedEntry_2_{2..6}` already reuse the prime via
    `supportedEntry_2_1.prime`, so this brings the first entry in
    line with the rest.
  - Dropped `private` from `prime_two` (and added a docstring) so
    downstream callers can stop re-deriving the witness inline. The
    proof appears as an inline duplicate in `HexGfq/CrossCheck.lean`,
    `HexGfq/EmitFixtures.lean`, `HexGfqField/EmitFixtures.lean`,
    `HexGF2Mathlib/Field.lean` (twice), and `HexPolyFp/SquareFree.lean`;
    a follow-up can replace those with `Conway.prime_two` without any
    further HexConway-side work.
- Verified:
  - `lake build HexConway.Basic`
  - `lake build HexConway`
  - `lake build HexGfq` (no new warnings vs baseline)
  - `python3 scripts/status.py HexConway` (Phase 6, ready)
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry'
    HexConway/Basic.lean HexConway/Conformance.lean`

## Current frontier

- Review findings beyond the applied changes: the file's 36 per-prime
  table entries each carry public `_monic`, `_irreducible`, and a
  `_hit` `[simp]` lemma; the wrapper-level surface exposes
  `luebeckConwayPolynomial?_irreducible` and
  `luebeckConwayPolynomial?_degree_pos` via case-splits backed by 36
  private `luebeckConwayPolynomialOfCoeffs_p_n_irreducible` helpers.
  A parallel `luebeckConwayPolynomial?_monic` would round out the
  surface but, under the issue's "preserve the proof strategy" rule,
  would require an additional 36 helper proofs (matching the existing
  pattern) and is not currently consumed downstream — `FiniteField`
  needs irreducibility and positive degree, not monicity. Leaving the
  wrapper unintroduced and the per-entry `_monic` theorems as the
  monicity surface.

- `luebeckConwayPolynomial?_miss_two_zero` is asymmetric (only one
  miss case) and unused externally, but it is `@[simp]` and removing
  it could affect simp normalisation of `luebeckConwayPolynomial? 2 0`
  in unknown call sites; left untouched.

## Next step

- Let CI validate the PR once opened. A follow-up task can replace
  the five inline `primeTwo` duplicates listed above with
  `Conway.prime_two`.

## Blockers

- None.
