# Phase 6: public nullspaceBasisMatrix column bridge (issue #6138)

## Accomplished

- Added public theorem `Matrix.nullspaceBasisMatrix_col` in
  `HexMatrix/RREF.lean` linking the `k`-th column of the public
  `nullspaceBasisMatrix M` to the `k`-th vector in `nullspace M`. Proof
  reuses the internal `IsRREF.nullspace_get` after unfolding the public
  wrappers down to `rref_isRREF M`.
- Replaced the internal-bridge fragment of the private helper
  `nullspaceBasisMatrix_entry_eq_basis_coeff` in
  `HexBerlekamp/RabinSoundness.lean` with a call to the new public
  theorem. The helper no longer references `Matrix.IsRREF.nullspace`
  directly; it unfolds `fixedSpaceKernelVectors`, rewrites by the public
  bridge, and discharges the entrywise step with `simp [Matrix.col]`.

## Current frontier

- Public nullspace API surface now exposes the column/vector bridge,
  matching the Phase 6 review follow-up.
- Phase 6 sibling task #6136 (making `IsRREF.nullspaceMatrix` entry
  characterisation lemmas public) is still open.

## Next step

Land the PR for #6138; next worker can pick up #6136 (visibility-only
change for the `nullspaceMatrix_*` entry lemmas) or #6137 (UInt64
wide-word API review).

## Blockers

None. `lake build` is green, `python3 scripts/check_dag.py` is clean,
and no forbidden tokens (`sorry`, `axiom`, `native_decide`, `TODO`,
`FIXME`) were introduced.
