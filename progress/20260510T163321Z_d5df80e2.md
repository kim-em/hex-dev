## Accomplished

Claimed repair PR #3042 and diagnosed the failed CI as a stale-base build
failure in `HexMatrix/RREF.lean`: the branch was still based before the
mainline fix that removed a duplicate private `foldl_add_eq_acc_ring`
declaration.

Merged `origin/main` into `agent/bef3737a`, bringing in the RREF fix while
leaving the PR's benchmark evidence changes intact.

Verified:

- `lake build HexMatrix.RREF`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`
  reports only existing `sorry`s at lines 116, 849, and 854.
- `lake exe hexarith_bench list && lake exe hexarith_bench verify && lake exe hexmatrix_bench list && lake exe hexmatrix_bench verify`
- `lake build $(python3 scripts/conformance_targets.py --space-separated) hexmatrix_emit_fixtures`

## Current frontier

PR #3042 is locally repaired and verified. The branch contains the original
HexArith phase 4 evidence refresh plus the merge from current `origin/main`.

## Next step

Push `agent/bef3737a` with `--force-with-lease` and mark PR #3042 salvaged.

## Blockers

None.
