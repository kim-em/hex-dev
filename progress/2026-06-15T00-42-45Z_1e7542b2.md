# Session: one-shot dispatch on #6779 (B-builder / B8 separation)

Session type: `/once` work (feature). Outcome: premise check → skip for replan. No code changes.

## Accomplished

- Claimed #6779 explicitly (`coordination claim 6779 --label feature`).
- Verified on `main`@`0cb5fcb4` that the deliverable is not achievable:
  - The precondition in the body ("after #2564 lands the van Hoeij CLD restructure") is unmet — #2564 is still OPEN and itself `blocked by dependencies` (claim fails).
  - No in-repo producer of `resultant_divisible_by_p_pow`: both `:=` sites in `HexBerlekampZassenhausMathlib/BadVector.lean` (`:1238` input param `hdiv`, `:1688` forward of the assumed field) are passthroughs. Field docstring (`:1394-1404`) confirms it is an honest assumed field.
  - Deriving the clause on the cut aux is false-as-typed (settled A-line evidence: `v_p(Res)=0`); packaging it as a bare assumption is forbidden by the body.
- Posted an evidence-based premise-check comment (issue #6779 comment 4703628916) and `coordination skip 6779`.
- Flagged the dependency/cycle tension for the owner: recommend removing backwards `#2564->#4172`/`#2564->#4818` edges (per the existing #2564 comment) rather than re-adding `#6779->#2564`, which would re-create the capstone cycle.

## Current frontier

#6779 is back in `replan`. The single missing piece is the resultant-divisibility derivation, gated entirely on #2564's van Hoeij CLD restructure.

## Next step

Owner/planner: resolve the #2564 cycle (drop backwards edges), let #2564 land the restructure, then re-derive this finisher against the restructured lattice.

## Blockers

#2564 (van Hoeij CLD restructure) — open and itself blocked.
