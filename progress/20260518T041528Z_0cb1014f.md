# 2026-05-18T04:15Z — work session (issue #4334)

Session: `0cb1014f` (`/work` meta dispatch).

## Accomplished

- Oriented via `coordination orient`. All three open `directive` issues
  (#2637, #2567, #2564) are `blocked`. The only unclaimed work item is
  #4334 (HexLLL Phase 4 scientific-bench environmental investigation).
- Claimed #4334.
- Confirmed cheap probes pass:
  - `lake exe hexlll_bench list` reports 10 parametric + 42 fixed = 52
    registrations.
  - `lake exe hexlll_bench verify` reports `all 52 benchmark(s) passed`.
- Ran a spawn-floor probe on the smallest affected parametric
  registration `Hex.LLLBench.runSizeReduceChecksum` with
  `--max-seconds-per-call 5`. Result: per-spawn floor 1.286s, every
  measurement killed at the 5s cap, zero verdict-eligible rows —
  identical pattern to the 30+ prior skip cycles documented on the
  issue.
- Host load on `carica` across the session: entry 14.73/13.46/14.30,
  mid-probe 8.09/11.16/12.99, post-probe 6.99/10.53/12.67. Same
  inflated-spawn-floor regime as every prior skip.
- Skipped #4334 with documentation matching the standing skip pattern.

## Current frontier

#4334 remains an environmental gate. The verdict-promotion path
described in `SPEC/benchmarking.md` §"Scientific timing runs" requires
quiet/dedicated hardware with a stable spawn floor; `carica` continues
to fail that precondition under the current concurrent-pod load.

## Next step

Either:

- wait for `carica` to be idle (1-min load avg in the low single
  digits sustained for the duration of a scientific run), then
  re-dispatch; or
- run the release-candidate benchmarking workflow on dedicated
  hardware per `SPEC/benchmarking.md`.

No code change is owed by this session.

## Blockers

Environmental only: host contention on `carica`. Same root cause as
the prior 30+ skip cycles. Not addressable from a worker session.
