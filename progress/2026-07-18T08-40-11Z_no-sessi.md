# Issue 8750 final Phase-4 completion

## Accomplished

- Implemented the owner-approved final split: only `runMahlerPrec` and
  `runIsolate` remain parametric; every other operation is a canonical fixed
  regression benchmark.
- Added smooth fixed-separation, bounded-height root, pinned-NK certification,
  achieved-precision refinement, and shared strategy fixtures.
- Produced the quiet-machine export
  `reports/bench-results/hex-roots-973c2cd-round5.json`: both parametric
  verdicts consistent, all fixed hashes stable.
- Verified the three fixed strategy cases agree on hash
  `0xda631bdf13415a4f`; the conformance suite retains broader strategy coverage.
- Added fixed-separation profile coverage, updated the headline report and
  input-family metadata, cleared Concerns, and advanced `HexRoots.done_through`
  to 4.
- Corrected the report filename to the Phase-4 checker convention
  `reports/hex-roots-performance.md` and updated references.
- Passed full `lake build`, bench `verify`, Phase-4/DAG/conformance checks, and
  the Mathlib-free bench audit.

## Current frontier

Implementation and validation are complete; ready to commit and publish the PR.

## Next step

Monitor the opened PR for merge conflicts and CI failures, repairing any that
arise.

## Blockers

None.
