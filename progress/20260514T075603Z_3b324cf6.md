# 2026-05-14 07:56 UTC — feature session on #3975

## Accomplished

- Claimed #3975 and exposed the narrow no-pivot array/matrix step
  bookkeeping API from `HexMatrix/Bareiss.lean` that
  `scaledCoeffRows_diag_eq_gramDetVecEntry` (parent #3541) needs to
  state a `scaledCoeffArrayLoop ↔ Matrix.noPivotLoop` relation.
- Dropped `private` from `Matrix.getEntry`, `matrixToRows`,
  `rowsToMatrix`, `stepArray`, the round-trip lemmas
  `getEntry_matrixToRows` / `rowsToMatrix_matrixToRows`, the pointwise
  bridge `getEntry_stepArray_matches`, and `stepArray_size`. Added a
  docstring to each so they read as a deliberate public API rather than
  incidental file-local helpers.
- Added one new matrix-level bridge theorem `rowsToMatrix_stepArray`:
  `rowsToMatrix (stepArray rows n k pivot prevPivot) n =
    stepMatrix (rowsToMatrix rows n) k pivot prevPivot`. Proven by
  packaging the existing pointwise `getEntry_stepArray_matches` at the
  matrix level via `Vector.ext`.
- Verified `lake build HexMatrix.Bareiss`, `HexMatrix`,
  `HexGramSchmidt.Int`, `HexGramSchmidt`. Sorry count unchanged (1 in
  `HexMatrix/Bareiss.lean` for `bareiss_eq_det`; 4 in
  `HexGramSchmidt/Int.lean` for the existing private holes, including
  `scaledCoeffRows_diag_eq_gramDetVecEntry`).
- `python3 scripts/check_dag.py` and `git diff --check` clean.

## Current frontier

- The new public API is ready for the `scaledCoeffRows_diag_eq_gramDetVecEntry`
  worker (the parent #3541) to consume.

## Next step

- A planner can re-narrow #3541 to consume the now-public
  `Matrix.{rowsToMatrix, stepArray, rowsToMatrix_stepArray, …}` API.
  Sibling sub-issues #3972 (leading-prefix data projection) and #3973
  (singular-prefix zero tail) remain independent and still feed the
  capstone bridge `gramDetVecEntry_eq_leadingPrefix_bareiss`.

## Blockers

- None.
