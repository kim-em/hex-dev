# 2026-06-09 03:48 UTC — one-shot D4 (#6613)

## Accomplished

- Claimed #6613 (D4: `certCheck` + `certCheck_sound`).
- Diagnosed placement contradiction: directive asks for `certCheck_sound`
  in `HexLLL/Basic.lean` (Mathlib-free), but the only path to prove it
  composes `Hex.lllReducedInt_sound` (D2 soundness), which lives in
  `HexLLLMathlib/Basic.lean:718` and uses Mathlib tactics (`linarith`,
  `push_cast`, `mul_le_mul_*`, `div_le_div_iff₀`, etc.). `HexLLL/Basic.lean`
  imports only `HexGramSchmidt` + `HexMatrix` (both Mathlib-free), so
  cannot import the soundness without becoming Mathlib-dependent.
- Posted issue comment with concrete evidence and reissue suggestion
  (natural split: `certCheck` Mathlib-free in `HexLLL/Basic.lean`,
  `certCheck_sound` in `HexLLLMathlib/Basic.lean` — matches the existing
  pattern for `lllReducedInt`/`lllReducedInt_sound`).
- Skipped #6613 (label flipped to `replan`, left claimable for an
  updated directive).

## Current frontier

D4 blocked on directive update. The Bool def `certCheck` is trivial
infra; the soundness theorem's placement is the open question.

## Next step

Author of #6613 (or next planner) decides between:
1. Reissue with `certCheck_sound` placed in `HexLLLMathlib`.
2. Or first decompose a Mathlib-free port of `lllReducedInt_sound`
   (~300 lines of Rat arithmetic currently using `linarith`/Mathlib
   ordered-field lemmas), then keep D4 Mathlib-free.

## Blockers

- Placement contradiction in #6613 deliverables vs. existing D2
  proof location (HexLLLMathlib).
