# 20260517T072231Z — review session 6b31e652 (#4584)

## Accomplished

Reviewed PR #4580 (merged commit `309a5eaa`, closing #4575 — slow-path
quadratic arm umbrella for HO-1 capstone `factor_irreducible_of_nonUnit`).
Review covers the seven deliverables in issue #4584.

**Review verdict**: PR #4580 is correct as merged. The single localised
`sorry` at `HexBerlekampZassenhausMathlib/IntReductionMod.lean:1258` is
bounded as the issue text claims. Build is green on PR HEAD via
`lake build HexBerlekampZassenhausMathlib.IntReductionMod`.

**Findings posted** as a comment on PR #4580
(<https://github.com/kim-em/hex/pull/4580#issuecomment-4469713651>):

1. **Cross-PR finding**: PR #4582 (in flight, fast-path quadratic arm)
   does NOT consume the new
   `reassemblePolynomialFactors_quadratic_irreducible_or_residual_of_expansionComplete`
   classifier that PR #4580 introduced. PR #4582 instead added
   `quadraticIntegerRootFactors?_factor_irreducible_of_primitive` plus
   the reverse primitivity bridge
   `IntReductionMod.zpoly_primitive_of_toPolynomial_isPrimitive`, then
   composed those with the existing generic reassembly lift
   `reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`.
   Deliverable 2's premise that the two arms share the new classifier
   verbatim does not hold. Once #4582 lands, the slow-quadratic
   umbrella can be rewritten in the same shape, eliminating the sorry
   without #4579 work.

2. **`_hf_ne` underscore is premature**: the residual sorry-discharge
   path needs `f ≠ 0` for the `IsPrimitive` of `squareFreeCore`. Once
   the sorry is filled (either via #4579 or via the cross-PR refactor),
   the underscore must come off. Recommended: drop now, accept the
   transient unused-variable warning.

3. **Unconditional classifier
   `reassemblePolynomialFactors_quadratic_irreducible_or_repeated_or_residual`**
   (added in earlier work) has **zero** remaining callers; the new
   conditional classifier added by this PR has **one** caller (the
   slow-quadratic umbrella itself). The PR added a new sibling rather
   than deriving one from the other.

4. **`_of_choosePrimeData` suffix**: same flag as review #4577. Defer
   to that review's resolution for the cross-cutting naming convention.

5. **`hcomplete` discharge feasibility**: not generically true for the
   quadratic core shape. Same gap as review #4577 deliverable 3 for
   the constant arm.

### Inventory verified

* `HexBerlekampZassenhausMathlib/IntReductionMod.lean`: 1 `sorry`
  (line 1258, documented residual sub-case for #4579). 0 `axiom`, 0
  `native_decide`, 0 `TODO`/`FIXME`.
* `HexBerlekampZassenhaus/Basic.lean`: 1 pre-existing `sorry` (line
  5770, `isIrreducible_iff` placeholder, unchanged by PR #4580).
* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` ✓
  (full mathlib target builds cleanly with only the expected sorry
  warnings).

### Follow-up issues filed

* **#4587**: Rewrite slow-quadratic umbrella onto
  `_factor_irreducible_of_primitive` + reassembly lift, eliminating
  the sorry, the `_of_expansionComplete` classifier, and the
  separate #4579 deliverable. Supersedes #4579. (Filed as
  `depends-on: #4582`; PR #4582 landed at `0ffd01cd` while this
  review was being filed, so unblocked immediately afterward.)
* **#4588** (depends on #4587): Audit quadratic reassembly classifiers
  in `Basic.lean` — both currently have zero or one caller and likely
  become dead after #4587.
* **#4590**: HO-1 `hcomplete` discharge across arm umbrellas
  (cross-cutting with #4577 and #4585). Noted on filing that #4585
  already commits to a concrete strategy for the constant arm;
  added a comment recommending the next planner narrow this to the
  three remaining arms (small-mod singleton, slow-quadratic,
  fast-quadratic) or close in favour of three sibling issues.

## Current frontier

Review of PR #4580 complete. The slow-path quadratic arm umbrella
stands as the third landed HO-1 per-arm umbrella (alongside constant
#4565 and small-mod singleton #4564); the fourth (fast-path
quadratic #4571) lands with PR #4582 in flight. Three follow-up
issues filed cover the cross-PR refactor opportunity (#4587), the
classifier audit (#4588), and the cross-arm `hcomplete` substrate
gap (#4590).

## Next step

For the planner:

1. #4587 is now unblocked (PR #4582 landed); a feature worker can
   apply the refactor and close #4579 with a forward link to that PR.
2. #4588 unblocks after #4587 — likely closed automatically if #4587
   deletes both classifiers, or narrowed to whichever survives.
3. #4590 needs the planner's scope-narrowing decision (per the
   in-thread comment) before a worker claims it.

## Blockers

None.
