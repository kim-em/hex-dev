# 58cff7ee — BZMathlib recombination candidate abstract-bound sibling (#4924)

## Accomplished

- Claimed feature issue #4924 after confirming there were no unclaimed
  directives.
- Added
  `exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean` by generalizing the existing
  monic recombination-candidate support theorem over an abstract coefficient
  bound `B'`.
- Rewired the original
  `exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd` name as a
  default-bound wrapper, preserving the downstream call site and discharging
  `hvalid` through the divisor chain
  `g ∣ recombinationCandidate d T ∣ target ∣ core` plus
  `defaultFactorCoeffBound_valid`.
- Ran `python3 scripts/check_dag.py`, `git diff --check`, and the grep check
  for newly-added `sorry`, `axiom`, `native_decide`, `TODO`, and `FIXME`; all
  passed.
- Ran a second-opinion diff review; it found no blocking issue and suggested
  only future helper extraction for duplicated divisor plumbing.

## Current frontier

The #4924 theorem surface is implemented. `lake build
HexBerlekampZassenhausMathlib.Basic` no longer reports diagnostics in the edited
region after fixing a local tab-character mistake, but the target still fails on
the pre-existing `whnf` timeout / unknown-constant cascade elsewhere in
`Basic.lean` that the issue body called out as environment-specific.

## Next step

Publish the PR for #4924 so the dependent
`coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd` abstract
bound issue can unblock after merge.

## Blockers

Full target-module verification is blocked by pre-existing
`HexBerlekampZassenhausMathlib.Basic` elaboration failures outside this session's
edit region. The worktree also still contains a pre-existing `.claude/CLAUDE.md`
modification that this session did not touch or stage.
