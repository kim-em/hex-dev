# 2026-05-15 05:59 UTC - thirty-first Phase 4/5 checkpoint (07d7d0eb)

## Accomplished

Claimed summarize issue #4152 and recorded this checkpoint. Earlier in the
session, #4153 was released as stale because the Round 2 residual still leaves
`HexGramSchmidt/Update.lean` and `HexLLL/Basic.lean` consuming the Int-layer
bridge theorem names; #4075 was decomposed into #4159 and #4160 after the
missing non-modular `FpPoly` compose/substitution laws were identified. The
pre-existing pod-managed `.claude/CLAUDE.md` modification was left untouched.
No source code, SPEC, top-level PLAN, or AGENTS file was changed.

Since summarize issue #3519 closed at 2026-05-13T07:09:35Z, the merge wave has
moved three main fronts forward: HexBerlekampZassenhaus factor coverage and
irreducibility bridges, HexBerlekamp Rabin-soundness algebra, and
HexGramSchmidt/HexMatrix Bareiss boundary work.

### HexBerlekampZassenhaus executable and Mathlib bridges

- The exhaustive branch now has UFD plumbing, concrete-output hooks, and count
  packaging in place: exhaustive-core UFD bounds and bridges (#4108, #4113),
  search selection helpers (#4130), core irreducibility under count hypotheses
  (#4131), recombination search step completeness (#4136), recovered-subset to
  concrete-output bridge (#4141), and BHKS count threading through
  `factorFastCoreWithBound` (#4150).
- The fast-core / B8 side gained the small-mod singleton bridge (#4123),
  fast-core irreducible count bridge (#4134), and partition-refinement count
  bridge (#4140).
- The remaining Group A/B frontier is now explicit: #4128 is ready to assemble
  exhaustive count equality from coverage, #4148 has an open PR for lifted
  subset coverage, #4149 waits on #4148, and #4006/#4008/#3987 remain the
  per-branch irreducibility chain downstream of #4128.

### HexBerlekamp Rabin soundness

- The scalar `FpPoly` evaluation stack advanced through monomial, constant,
  add/sub, multiplication, and `linearPow` evaluation helpers (#4107, #4111,
  #4097, #4129, #4138).
- The CRT/kernel side gained the zero-one witness package (#4070), CRT
  representative nonconstancy (#4088), Frobenius kernel reduction invariance
  (#4089), and Berlekamp CRT kernel witness (#4109).
- The prime-field product chain landed linear product coverage (#4098) and the
  headline variable identity `primeFieldProduct_X_eq_xPowSubX` (#4143). The
  next real prerequisite is #4159: non-modular `FpPoly` compose/substitution
  laws, after which #4160 can finish witness-level substitution. The older
  #4075 body has been superseded by those two narrower issues.

### HexGramSchmidt, HexMatrix, and HO-43

- The no-pivot / Bareiss route advanced through scaled coefficient loop
  bookkeeping (#3981, #3995, #4016, #4026, #4029, #4054, #4137), lower-column
  capture/sync packages (#4072, #4080, #4101, #4110, #4118, #4122, #4135), and
  the no-singular pivotLoop/noPivotLoop equality (#4147).
- HO-43 Round 1 relocated the public LLL independence bridge to
  `HexLLLMathlib/Independent.lean` (#4146). Round 2 partially relocated the
  adjacent-swap bridge theorems to `HexGramSchmidtMathlib/Update.lean` (#4154).
- The Round 2 size-reduce residual is now the controlling blocker. Until
  `LLLState.sizeReduceColumn` stops discharging `ν_eq`/`d_eq` through
  `gramDet_sizeReduce` and `scaledCoeffs_sizeReduce_*`, Round 3 cannot move the
  Int-layer determinant bridge chain without breaking Mathlib-free builds or
  importing bridge modules from core files.

### Other merged movement

- HexGfq continued certificate work with the N32 quotient-step and wrapper
  facts (#3990, #3998, #4048, #4082).
- HexPolyZ / HexBZ Mathlib gained mod-p and lifted-subset transport helpers
  (#4033, #4046, #4091, #4092, #4095).
- The coordination layer renamed the old human-oversight label to `directive`
  (#4117), matching the current worker-flow priority rule.

## Current frontier

`coordination orient` currently reports unclaimed #4128 and #4159, claimed
#4145 and #4152, and open PR #4158 for #4148. The visible highest-priority
frontier is:

- Directives: #3899 is still the active HO-43 boundary relocation directive.
  Its next concrete step is not #4153 as originally written; it is the
  size-reduce residual #4157, blocked on the `LLLState.ν_eq` / executable
  scaled-coefficients refactor tracked by #3916.
- HexBerlekamp Rabin chain: #4159 unblocks #4160, which should replace #4075 as
  the witness-product substitution path. #4063/#4064/#4076 remain blocked
  behind that substitution and the downstream gcd distribution/composition.
- HexBZ Mathlib Group A/B: #4128 is ready and unblocks #4006; #4148 is in PR
  and #4149 waits on it. The per-branch irreducibility chain remains
  #4128 -> #4006 -> #4008 -> #3987.
- HexGramSchmidtMathlib: #4145 is claimed for the Cramer no-pivot bridge; #4053
  and #3977 remain blocked behind the HO-43 relocation state.

## Quality metrics

- `python3 scripts/status.py`: ready dispatch targets include Phase 6 for
  HexArith, HexPoly, HexModArith, HexGF2, HexHensel, HexConway, and
  HexPolyMathlib; Phase 5 for HexMatrix, HexGramSchmidt, HexPolyZ,
  HexGfqField, HexGfq, HexMatrixMathlib; Phase 4 for HexLLL, HexPolyFp,
  HexModArithMathlib, HexGramSchmidtMathlib, HexPolyZMathlib, and
  HexHenselMathlib; and Phase 1 for HexBerlekampZassenhaus.
- Blocked phase targets remain HexGfqRing, HexBerlekamp, HexLLLMathlib,
  HexBerlekampMathlib, HexGF2Mathlib, HexGfqMathlib, and
  HexBerlekampZassenhausMathlib.
- Lean `sorry` grep count: 87.
- Lean `axiom` declarations: 0.
- `native_decide` textual hits in Lean files: 0.

## Next step

Pick #4128 if prioritizing the BZ irreducibility chain, or #4159 if
prioritizing the Berlekamp Rabin chain. For HO-43, do not retry #4153 until the
size-reduce residual is unblocked; the actionable work is the #3916-style
consumer refactor that removes the remaining bridge-bound calls from
`LLLState.sizeReduceColumn`.

## Blockers

No blocker for this summary. Project-level blockers are the explicit dependency
chains above plus the still-open directives.
