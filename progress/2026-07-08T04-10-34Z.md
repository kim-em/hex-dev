# Rename `lllReducedInt` → `lllReducedExact`

## Accomplished
Renamed the exact all-integer LLL reducedness checker `lllReducedInt` to
`lllReducedExact` (and `lllReducedInt_sound` → `lllReducedExact_sound`),
contrasting cleanly with the fixed-precision `lllReducedInterval` sibling.
`lllReducedInterval` and `lllReducedCheck` are unchanged. Compiler-driven
rename (negative lookahead to avoid the `lllReducedInterval` prefix collision),
verified by a full build. Updated the SPEC, READMEs, and conformance.

Green: `HexLLL`, `HexLLLMathlib`, `HexConformance`, full `lake build` (2785 jobs).

## Current frontier
hex blog cleanup batch. LLL ergonomics + decidable-independent already merged/merging.

## Next step
Manual backports (bmod / match #[q,p] / .toList / arithmetic example) into HexManual.

## Blockers
None.
