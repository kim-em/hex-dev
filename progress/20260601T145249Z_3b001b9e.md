# 3b001b9e - work #6016

## Accomplished

- Checked directives first; all open directives were blocked by dependencies.
- Claimed #5805, found it stale because prerequisite #5848 was closed as
  replan without delivering the Gram-trajectory cofactor-preservation consumer,
  and skipped #5805 with a concrete replan reason.
- Claimed #6016 after #6032 raced to another agent.
- Replaced the hot `HexGramSchmidt.Int` initial array constructors with
  `Array.ofFn` for `gramRows` and `Array.replicate` for `zeroRows`, removing
  dead `Array.range.map` bounds branches from `GramSchmidt.Int.data` setup.
- Added focused harsh-cubic evidence at `n = 45, 50, 55` in
  `reports/bench-results/hex-lll-3b001b9e-harsh-cubic-array-cleanup.json`.
- Updated `reports/hex-lll-performance.md` with the new local medians and noted
  that the comparator Concern remains open.
- Verified with `lake build HexMatrix HexGramSchmidt HexLLL`,
  `lake exe hexlll_bench verify`, the targeted harsh-cubic benchmark run,
  `python3 scripts/status.py HexLLL`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token diff grep.

## Current frontier

#6016 has a narrow executable cleanup and benchmark evidence. The local medians
improved from `205.469 ms`, `384.772 ms`, `672.593 ms` to `204.684 ms`,
`381.692 ms`, `666.008 ms` at `n = 45, 50, 55`, with unchanged hashes, but this
does not clear the harsh-cubic Lean/Isabelle comparator Concern.

## Next step

Open a partial PR for #6016 so the cleanup and evidence are visible, then file
or let planning create a successor for deeper `scaledCoeffRows` / trailing
Bareiss operand-growth work.

## Blockers

No blocker for the partial cleanup. Full #6016 completion still needs a larger
optimization than constructor cleanup; the existing exact-integer Bareiss update
path remains the likely frontier.
