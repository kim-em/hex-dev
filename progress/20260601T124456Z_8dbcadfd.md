# 20260601T124456Z — 8dbcadfd: #5214 BZMathlib slow-path Hensel substrate package

## Accomplished

Added the slow-path Hensel substrate bundle requested by #5214 in
`HexBerlekampZassenhausMathlib/Basic.lean`:

* New `structure SlowPathHenselSubstrate (core B primeData)` packaging the
  seven hypothesis components the bound-specialised exhaustive-branch
  theorems
  `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
  and
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
  consume on the lifted-Hensel side:
  `HenselSubsetCorrespondenceHypotheses`, `LiftedFactorSubsetPartition core
  d Finset.univ core`, per-lifted-factor `Monic`, per-lifted-factor
  positive transported `natDegree`, `Function.Injective (liftedFactor d)`,
  and the Mignotte `modulus`/`precision` bounds on `d.p ^ d.k`.

* New `slowPathHenselSubstrate_of_choosePrimeData` constructor: given
  `Hex.choosePrimeData? core = some primeData` together with monicness,
  positive degree, and `Squarefree (toPolynomial core)`, plus `B ≠ 0`,
  produces a `SlowPathHenselSubstrate core B primeData`. The constructor
  composes the existing non-circular primitives — the analytic-obligation
  helpers (line 16601, 16702), the `_of_monicPrimeData` lifted-factor
  umbrellas (lines 7130, 7194, 7274), and `precisionForCoeffBound_spec`.

No new analytic obligation is introduced; the analytic content remains
isolated in the two `sorry`-bearing helpers documented in the file.

## Current frontier

`factor_irreducible_of_nonUnit` (#4170) can now obtain its slow-path
substrate as a single value, rather than reconstructing the seven
components independently. The bundle matches the lifted-Hensel side of
the bound-specialised exhaustive-branch consumers exactly.

## Next step

#4170's slow-path arm calls `slowPathHenselSubstrate_of_choosePrimeData`
on the monic square-free core and projects its fields into the bound-
specialised exhaustive-branch theorem.

## Blockers

None.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — green.
* `lake build HexBerlekampZassenhausMathlib` — green.
* `python3 scripts/check_dag.py` — green.
* `git diff --check` — green.
* `git diff | grep -nE '^\+.*(axiom|native_decide|TODO|FIXME)'` — empty.
* Pre-existing `sorry`-bearing decls in `Basic.lean` unchanged (lines
  255, 16601, 16702); no new sorries.
