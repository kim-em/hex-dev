# HexPoly Dense constructor API review

Session: `ef9c5e55` (work, issue #5672)

## Accomplished

Claimed #5635 first, discovered the requested `IntReductionMod` transport
was blocked by stale `HexBerlekampZassenhausMathlib.Basic` surfaces still
threading `henselLiftData core` while the executable path now uses
`monicisedCoreLiftData core`. Decomposed #5635 into #5673, #5674, and
#5675, then released the parent for replan. Briefly claimed #5673 and
released it after confirming the membership-only slice is too tightly coupled
to the monicised Hensel correspondence migration.

Claimed #5672 and reviewed `HexPoly/Dense.lean` from
`DensePolyNormalized` through `ofCoeffs_replicate_zero`. Posted the review
comment on #5672. Implemented the small in-scope polish found by the review:
added missing docstrings for three public helper theorems and added `@[simp]`
to `mem_support` and `ofCoeffs_replicate_zero`.

## Current frontier

`HexPoly.Dense` constructor/support/toArray API is in good Phase-6 shape for
the reviewed slice. Remaining HO-1 monicised-core work should continue from
the replan/decomposition around #5673, #5674, and #5675.

## Next step

Open the PR for #5672 with the `HexPoly/Dense.lean` polish and review comment.

## Blockers

None for #5672.

Verification completed:

- `lake build HexPoly.Dense`
- `lake build HexPoly`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexPoly/Dense.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

Unrelated worktree state:

- `.claude/CLAUDE.md` was already modified and was left untouched.
