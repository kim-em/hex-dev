# 2026-06-04T12:49:22Z — Schur singular cascade + bridge wrapper (#6534)

Session UUID prefix: 72c27fea
Issue: #6534

## Accomplished

- Added Mathlib-free public theorem
  `getArrayEntry_scaledCoeffRowsSchur_eq_zero_of_singularStep_lt` to
  `HexGramSchmidt/Int.lean`, immediately after the non-singular capstone
  `getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing`.
- Statement: when the no-pivot Bareiss pass over the full Gram matrix
  records an early singular step at column `s < j`, every Schur kernel
  entry `rows[i][j]` in the column-`j`, weak-lower triangle (`j ≤ i`)
  vanishes; takes `StepWitness b` as hypothesis.
- Proof: assembles the row-`s` zeros by combining the non-singular
  Schur≡Bareiss correspondence at fuel `s`
  (`getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing` from
  sub-D of #6533) with the column-zero structural lemma at the singular
  step (`leadingPrefix_gram_zero_pivot_column_zero_of_singular_step`).
  Strong induction on `j' ∈ (s, j]` propagates the zeros via the Schur
  recurrence `rows[i'][j'] = rows[j' - 1][j' - 1] · gram[i'][j'] -
  schurSigma i' j'`. The σ-fold collapse is handled by sub-induction on
  the iteration prefix `k ∈ [s, j' - 1]`: the killing iteration `p = s`
  makes all three relevant Schur entries zero (so the σ-body `exactDiv`
  of zero by the prev-pivot returns zero), and subsequent iterations
  preserve zero via the outer IH at columns `(s, j')`.
- Added Mathlib-side bridge wrapper
  `scaledCoeffs_lower_eq_zero_of_singularStep_lt` to
  `HexGramSchmidtMathlib/Int.lean`, immediately after
  `StepWitness.ofGram`. The wrapper threads `StepWitness.ofGram b`
  through the Mathlib-free cascade to lift the result to
  `GramSchmidt.entry (scaledCoeffs b) i j = 0` on `Fin`-indexed inputs.
- `lake build` clean across all 244 targets and the broader
  HexLLL/HexLLLMathlib + Conformance targets (2673 jobs).
- Sorry counts unchanged: `HexGramSchmidt/Int.lean` still 3
  (pre-existing at lines 6149 and 6463, plus one prose reference at
  line 6136); `HexGramSchmidtMathlib/Int.lean` still 0.
- No new `axiom`, `native_decide`. The new cascade proof does not
  transitively cite `getArrayEntry_scaledCoeffRowsSchur_eq` (the
  residual sorry at line 6463).

## Current frontier

Stage C₂ done; together with sub-D of #6533, this is the second piece
needed to close #6532 (Stage C' Schur ≡ Bareiss / cascade
decomposition). The new cascade lemma is the singular companion to the
non-singular `getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing`,
and the bridge wrapper is now available for downstream consumers.

## Next step

Consume both halves (non-singular sub-D + singular cascade) to
discharge the residual sorry at line 6463 of `HexGramSchmidt/Int.lean`
(`getArrayEntry_scaledCoeffRowsSchur_eq`), which closes #6532.

## Blockers

None.

## Notes

- The σ-fold sub-induction is keyed on the prefix length `k` from `s`
  up to `j' - 1`. At the killing iteration (`s = k + 1`), the body's
  numerator is `rows[s][s] · σ_prev + rows[i'][s] · rows[j'][s] = 0`
  regardless of `σ_prev`; the prev-pivot `rows[s-1][s-1]` need not be
  characterized because `exactDiv 0 _ = 0`. For iterations past the
  killing one, the outer cascade IH (at `s < k + 1 < j'`) makes all
  three Schur entries zero.
- The boundary case `s = 0` is absorbed into the base of the inner
  sub-induction (`k = 0`, empty fold): the σ-initialiser
  `rows[i'][0] · rows[j'][0]` vanishes because `rows[j'][0] = 0`
  (column-zero of the singular step at `s = 0`, applied at `c = j' ≥ 1`).
- The outer strong induction on `j' ∈ (s, j]` is set up by reverting
  the dependent hypotheses (`hj'j`, `hsj'`, `i'`, `hj'i'`, `hi'n`)
  before `Nat.strongRecOn` and re-introducing them inside the step,
  matching the pattern used by the non-singular capstone.
