**Accomplished**

- Claimed #5789 and regenerated the `HexGramSchmidt` Phase-4 profile section
  under `lean-bench-samply` timed-region filtering.
- Ran filtered profiles for the three `phase4.input_families` entries:
  `runGramDetVecChecksum 40`, `runAdjacentSwapChecksum 192`, and
  `runAdjacentSwapGramDetQuotient 16`, each with `target-nanos = 5000000000`.
- Updated `reports/hex-gram-schmidt-performance.md` with the emitted
  diagnostics blocks, leaf-cost categories, inclusive Hex hot-path rankings,
  and attribution narrative.
- Recorded that no Attribution-rule follow-up issue surfaced.
- Verified:
  - `lake build hexgramschmidt_bench`
  - `lake build HexGramSchmidt hexgramschmidt_bench`
  - `git diff --check`
  - targeted report scan for `axiom` / `native_decide`

**Current frontier**

- The `HexGramSchmidt` performance report now reflects bench-thread,
  timed-region-filtered profiles instead of whole-child `samply` captures.
- `/reflect` was requested by the workflow but is not available as a shell
  command in this session.

**Next step**

- Open the PR for #5789.

**Blockers**

- None.
