# 20260609T045926Z (#6609 partial, decomposed into #6631)

## Accomplished

- Claimed #6609 (recursive Yun scalar split synchronization). Read the
  surrounding `yunFactorsContributionWithLevel` recursion, the asymmetric
  scalar gcd bridges from #6626 (PR #6628, merged), the substrate
  lemmas `yunFactorsContributionWithLevel_normalized_tail_reconstruct`
  and `yunFactorsContributionWithLevel_normalized_tail_product_bridge`,
  and the existing `yunScalarStep` from PR #6607.
- Diagnosed structural complexity: `isOne c` is not preserved by
  scalar multiplication, so the natural induction on fuel splits into
  four sub-cases per step on `(c.isOne, (C u_c * c).isOne)`. Two of
  the four (TF, FT) require analyzing recursion through constant `c`,
  using the fact that `gcd (C k) X` is a unit polynomial for `k ≠ 0`.
- Landed the foundation as a single new private theorem
  `yunFactorsContributionWithLevel_scalar_step_bridge` in
  `HexPolyFp/SquareFree.lean`: given asymmetric nonzero scalars
  `u_c, u_w` on `(c, w)` with `gcd c w ≠ 0`, produces the explicit
  scalar `v ≠ 0` and the synchronization equations for the gcd, `c/y`,
  and `w/y` simultaneously. Composes `gcd_C_mul_left_C_mul_right_eq_C_mul_gcd`
  with two applications of `div_C_mul_C_mul_of_dvd` (one on `gcd_dvd_left`,
  one on `gcd_dvd_right`).
- Decomposed the residual into successor issue #6631 (recursive scalar
  contribution sync) with full case-analysis breakdown and the constant-c
  sub-lemma shape. Left a `Decomposed into #6631` breadcrumb on #6609 and
  marked the parent `replan` via the partial-PR path.

Verification on `agent/f791daa2`:

- `lake build HexPolyFp.SquareFree`: green.
- `lake build HexPolyFp`: green.
- `python3 scripts/check_dag.py`: clean.
- `git diff --check`: clean.
- Added-line forbidden-token grep (`sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`): none.

PR: https://github.com/kim-em/hex/pull/6632

## Current frontier

Step-bridge landed in 33 added lines (one new private theorem). The
recursive sync proper requires the four-way case split per induction
step plus the constant-c sub-lemma. Successor issue #6631 captures
this with full scope.

## Next step

Successor #6631:

1. Prove a sub-lemma that starting from `(C k, w', base, level, fuel)`
   with `k ≠ 0, w' ≠ 0`, the recursion produces `(C s, C t * w0)` for
   nonzero `s, t`. Uses the fact that divisors of `C k` are constants.
2. Recursive scalar sync theorem with full case analysis using the step
   bridge for FF, the sub-lemma for TF/FT, and trivial sync for TT.
3. Compose with the existing `normalized_tail_reconstruct` /
   `normalized_tail_product_bridge` substrate.

## Blockers

#6631 is currently labeled `blocked` on #6609 by `coordination plan`'s
auto-dependency detection (the body includes `depends-on: #6609`). When
the planner triages #6609's `replan` state — either closing it with a
forward link to #6631 or narrowing its body — `check-blocked` will
clear the label and #6631 becomes claimable.
