## Accomplished

- Reviewed `/Users/kim/.claude/plans/make-a-plan-for-hashed-island.md`.
- Checked the referenced `pod` dispatch, coordination, lock, worker launch,
  and test code, plus the current `hex` pod config and `/plan` command.
- Assessed the proposed `replan` worker design against planner-lock behavior,
  queue-balance dispatch behavior, issue-list filtering, and rollout order.

## Current frontier

- Ready to report review findings. Main concern is that a raw
  `gh issue list --label replan` helper would bypass the trusted-issue intake
  path and should probably be scoped through the existing issue filtering
  machinery.

## Next step

- If implementing the plan, adjust `cmd_list_replan` to reuse the trusted
  issue filter and add tests for lock-held dispatch behavior with both empty
  and non-empty drain queues.

## Blockers

- None.
