# Progress: #6890 — multiplicative closure for the recovered lifted predicate

Feature session. Residual of #6890 after decomposition into #6932/#6819/#6840/
#6804: repair or restate `representsIntegerFactorAtLift_mul_of_monic_core`
against the recovered/monic-coordinate carrier landed by #6938.

## Directive-premise check

The directive presumed `RepresentsIntegerFactorAtLift` had *become* the
recovered/monic-coordinate predicate. In the merged state it has not:
`RepresentsIntegerFactorAtLift` is still the old scaled-product predicate
(`Basic.lean:2283`) and #6938 added `RecoveredAtLift` (`Basic.lean:2298`) as a
*parallel* carrier ("lives next to the old ... while consumers migrate"). The
old theorem `representsIntegerFactorAtLift_mul_of_monic_core` therefore still
compiles and is correct for the still-live old predicate; nothing was broken.

The genuine residual is multiplicative closure for the *new* carrier, which had
only `candidate_eq_of_bound`. I added it rather than disturbing the old theorem.

## Accomplished

- `HexPoly/Euclid.lean` (Mathlib-free): added Gauss's lemma in primitive-part
  form, `DensePoly.primitivePart_mul : primitivePart (p * q) = primitivePart p
  * primitivePart q`, plus two supporting lemmas `scale_left_cancel` and
  `primitivePart_scale_of_primitive`. The proof reuses the
  `scale (content p * content q) (primitivePart p * primitivePart q)`
  decomposition already established inside `content_mul`, absorbing the positive
  content scalar via `primitivePart_scale_of_primitive`. Zero-content factors
  collapse through `primitivePart_eq_zero_of_content_eq_zero`.

- `HexPolyZ/Basic.lean`: thin `ZPoly` wrapper `Hex.ZPoly.primitivePart_mul`.

- `HexBerlekampZassenhausMathlib/Basic.lean`: `RecoveredAtLift.mul` — disjoint-
  union multiplicative closure for the recovered carrier. Witness monic
  coordinate is the product of the two witnesses. The `congr` field combines
  `liftedFactorProduct_union_of_disjoint` with `Hex.ZPoly.congr_mul`; the
  `dilate_eq` field combines `HexPolyZMathlib.dilate_mul` (dilation is
  multiplicative) with `Hex.ZPoly.primitivePart_mul` (Gauss).

Improvement over the old theorem: `RecoveredAtLift.mul` needs **no monic-core
hypothesis**. The carrier separates the mod-`p^k` congruence (on the unscaled
`liftedFactorProduct`) from the dilation by `leadingCoeff core`, so closure
holds for arbitrary cores — the old proof needed `leadingCoeff core = 1` only to
collapse `scaledLiftedFactorProduct` to `liftedFactorProduct`.

Verified:
- `lake build HexPoly.Euclid`, `lake build HexPolyZ.Basic`,
  `lake build HexBerlekampZassenhausMathlib.Basic` — all succeed, no errors.
  (`HexBZMathlib.Basic` now builds clean; the `Basic.lean:16475` hard-red noted
  in the previous progress file is no longer present.)
- `python3 scripts/check_dag.py` — exit 0.
- Added lines: no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

`RecoveredAtLift` now has both exact-recovery (`candidate_eq_of_bound`) and
multiplicative closure (`mul`). The old scaled-product theorem is left in place
for the still-live old predicate; it can be removed by #6819 once downstream
consumers finish migrating off `RepresentsIntegerFactorAtLift`.

## Next step

#6819 migrates lifted-partition/substrate consumers onto `RecoveredAtLift`; the
recursive scaled-recombination search migration is #6840. Either can now consume
`RecoveredAtLift.mul` for monic-witness recombination without a Gauss gap.

## Blockers

None.
