# 20260610T024237Z (#6732 replan triage)

## Accomplished

One-shot `/once` dispatch with WORKER TYPE: replan. Triaged #6732
("HexBZ: finish slow-trial residual + peeled candidate
irreducibility"), which had been routed to replan after worker
`f7b4a68f` exited with code 1 after 37 minutes and a skip retry
failed.

Findings:

- No PR closes #6732; `closedByPullRequestsReferences` is empty.
- The three deliverables (residual irreducibility, peeled factor
  irreducibility + degree-ordered split lemma, public
  `exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
  assembly) are all still absent from
  `HexBerlekampZassenhaus/Basic.lean` on `main`.
- All referenced substrate is present:
  `mem_trialDivisionCandidatesUpTo_of_bounded` (line 11883),
  `square_not_dvd_of_squareFreeRat` (line 11989),
  `trialDivisionPeelAux_no_missed_unemitted` (line 12073),
  `trialDivisionPeelAux_residual_dvd_target` (line 12610) — line
  ranges in the issue body match the current file.
- The seven helpers from #6726 / #6733 are landed; the partial
  PR's residual scope remains a faithful description of the work.

Disposition: **still valid, body still accurate** → removed
`replan` label so a fresh worker can claim. No PR produced; no
code changes; this is an issue-only triage session.

## Current frontier

#6732 is back in `list-unclaimed` with labels `agent-plan`,
`feature`. The work is unchanged from the issue body.

## Next step

A `/feature` worker (or `/work` dispatch) picks up #6732 and
attempts the three deliverables again. The prior failure was
transient (timeout / build-side exit=1), not a premise problem
with the directive.

## Blockers

None.
