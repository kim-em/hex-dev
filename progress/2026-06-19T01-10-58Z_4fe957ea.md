# HexPoly Euclid.lean Phase 6 grind coverage (#8042)

## Accomplished
- Promoted all 14 equation-shaped public `@[simp]` characterising lemmas in
  `HexPoly/Euclid.lean` to `@[simp, grind =]`:
  - Unconditional `lhs = rhs` (11): `leadingCoeff_zero`, `leadingCoeff_C`,
    `leadingCoeff_one`, `gcd_zero_zero`, `zero_mod_eq_zero_core`, `mod_mod`,
    `content_mul_primitivePart`, `content_scale_neg_one`, `content_scale_int`,
    `content_zero`, `content_C`.
  - Conditional `hyp → lhs = rhs` (3): `mod_eq_zero_of_dvd`,
    `primitivePart_eq_zero_of_content_eq_zero`,
    `primitivePart_eq_self_of_content_eq_one`.
- All re-read at promotion point; each confirmed literal/conditional
  equation-shaped (no iff/inequality/membership/proof-valued in the set).
- Annotation-only: no proof bodies changed, no imports added, no `*Mathlib`
  file touched. Used `grind =` (equational), not `grind =>`.

## Verification
- `lake build HexPoly` → Build completed successfully (7 jobs).
- `HexPoly.Euclid` elaborated in 4.5s — no grind-loop slowdown. Identity/
  projection rewrites reduce to operation-free RHSs; structural rewrites
  (`mod_mod`, `content_mul_primitivePart`, `content_scale_*`) strictly reduce
  nesting, so no cycle against terminal rewrites.
- `python3 scripts/check_dag.py` → exit 0. `git diff --check` clean. No added
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier
Issue #8042 fully discharged. Sibling Phase 6 sweep files remain (#8035 Nat/
Prime.lean, #8040 GF2 Field.lean).

## Next step
Open PR closing #8042.

## Blockers
None.
