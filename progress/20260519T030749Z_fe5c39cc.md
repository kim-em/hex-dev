# Scope-skip on #5222 (HexPolyZMathlib Mahler-derivative bound)

Session: fe5c39cc-12f5-4842-b2fc-937ff2229085, type feature worker.

## Accomplished

- Triaged the unclaimed queue and selected issue #5222 (Mahler-measure
  derivative bound) over #4334 (HexLLL Phase 4 inconclusive verdicts)
  because `uptime` reported `load averages: 11.68 11.75 12.44`, the
  same load regime that has driven repeated skips on #4334 per its
  audit history. #5218 was already claimed by a peer agent.
- Audited Mathlib for the target inequality
  `M(p.derivative) ≤ p.natDegree * M(p)`. Findings:
  - `Polynomial.mahlerMeasure` and the integral / product
    representations are at
    `.lake/packages/mathlib/Mathlib/Analysis/Polynomial/MahlerMeasure.lean`
    (Barroero & Wilson, 2025); `mahlerMeasure_mul`,
    `mahlerMeasure_eq_leadingCoeff_mul_prod_roots`,
    `mahlerMeasure_le_sqrt_sum_sq_norm_coeff`,
    `norm_coeff_le_choose_mul_mahlerMeasure` all present.
  - `rootSet_derivative_subset_convexHull_rootSet` (Gauss–Lucas) is
    at `.lake/packages/mathlib/Mathlib/Analysis/Complex/Polynomial/GaussLucas.lean`.
  - `grep` over `.lake/packages/mathlib` for
    `derivative.*mahlerMeasure|mahlerMeasure.*derivative` returns
    zero matches — the issue's headline statement is absent from
    Mathlib.
- Verified that the elementary fallback
  `M(p.derivative) ≤ p.natDegree * l2norm p` (provable from
  `||p'||_2 ≤ deg · ||p||_2` and Landau) does not substitute for
  the target: the BHKS Lemma 5.1 chain runs
  `|[x^j] (f · g'/g)| ≤ C(n-1, j) · M(f) · M(g')/M(g)`, and
  swapping in the `l2norm` form leaves a residual `||g||_2 / M(g)`
  ratio that worst-cases to `~2^{deg g}` (Mignotte coefficient
  bound), exponential blowup vs the SPEC's
  `B_j := C(n-1, j) · n · ||f||_2`.
- Skipped #5222 with a detailed comment laying out: (i) the Mathlib
  availability audit above, (ii) the standard proof outline
  (Robinson/Schur symmetric form `|p̃|=|p|` on `|z|=1` →
  Gauss–Lucas on `p̃` → comparison `M(p') ≤ M(p̃')`), (iii) three
  decomposition paths for the next planner (upstream Mathlib PR;
  three-step in-project decomposition; or BHKS strategy revision
  per SPEC §B2 "...").

## Current frontier

#5222 is marked `replan`. No code changes in this worktree (the
pre-existing `.claude/CLAUDE.md` and `.claude/{commands,skills}/`
diff is pod-injected scaffolding and is not part of the project
tree — left untouched per the off-limits files rule).

## Next step

Planner triage of #5222. Recommended: take Path A (upstream
`Polynomial.mahlerMeasure_derivative_le_natDegree_mul` to Mathlib,
then transport in 30 lines) — this lemma is a natural extension of
the existing Barroero–Wilson Mahler-measure API and is more
appropriate for Mathlib than a project-local proof. If
Path B is chosen, sub-issue 3 (the `M(p') ≤ M(p̃')` analytic
comparison) is the open-ended step and should be sized at 2–3
sessions.

## Blockers

The proof of `M(p') ≤ deg(p) · M(p)` requires new Mathlib-style
infrastructure (Schur reflection of complex polynomials, Jensen on
`log|p'/p̃'|`) that does not exist in the current Mathlib pin and
does not naturally factor into a single-session sub-task at the
granularity of the existing `HexPolyZMathlib/Mignotte.lean`
proofs. Mathlib has Gauss–Lucas (used in step 2 of the outline)
but lacks the Robinson-form construction and the unit-circle
modulus-comparison lemmas needed for steps 1 and 3.
