# PR #9138 review

## Accomplished

- Reviewed the guarded `directLeaf`, arbitrary `SupportMeta` proof contract,
  head-forced and unforced traversal call graphs, Mathlib iterator proofs, and
  the counted benchmark mirror.
- Confirmed `directLeaf_eq` proves exact observational equality with the old
  leaf, including `tried` and every `DirectSplit` field.
- Found that a surviving production leaf evaluates the candidate prefilter
  twice: once in `directLeaf` and again inside `tryDirectCandidate`. The counted
  mirror evaluates it once, so it is extensionally equivalent but not an exact
  operational/cost mirror.
- Confirmed arbitrary `SupportMeta` records cannot change traversal arithmetic:
  their proof fields constrain every value accessible through a valid lifted
  index. The use of `getD` does not enforce array size, so short arrays can
  inhabit the structure when omitted factor metadata equals the zero default.
- Confirmed metadata is redundantly rebuilt for every cardinality level and
  recursive quotient search even though the lifted basis is fixed.
- Built `HexBerlekampZassenhausMathlib.Classical.CombinationIterator`,
  `HexBerlekampZassenhausMathlib.Classical.SearchCompleteness`, and
  `HexBench.FactorService` successfully (3908 jobs).

## Current frontier

Review complete. The main actionable finding is the duplicated prefilter and
resulting mismatch with the benchmark mirror; metadata lifetime is a secondary
performance opportunity, not a correctness defect.

## Next step

Refactor the post-guard candidate path so `directLeaf` does not call a helper
that repeats the guard, then either hoist `supportMeta basis` to the whole
search or document and benchmark why per-level reconstruction is preferable.

## Blockers

None.
