## Accomplished

Claimed #3034 after all unclaimed human-oversight issues were rejected by
`coordination claim` as blocked.

Added the executable-cap BHKS separation proof layer in
`HexBerlekampZassenhausMathlib/TerminationBound.lean`: introduced
`BHKS.ExecutableCapSeparationHypotheses`, proved
`BHKS.no_projected_not_indicator_of_factorFastPrecisionCap_le`, and exposed
`BHKS.projectedRowSpan_eq_trueFactorIndicatorLattice_of_cap` by combining the
cap-level bad-vector contradiction with the existing abstract `L' = W`
separation theorem.

Verified:

- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhausMathlib.TerminationBound`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/TerminationBound.lean`
  produced no matches.

## Current frontier

The cap/resultant layer now has a reusable theorem turning the remaining
failed-recovery-to-bad-vector bridge into executable-cap `L' = W` separation.
Downstream recovery-success work can consume
`BHKS.projectedRowSpan_eq_trueFactorIndicatorLattice_of_cap`.

## Next step

Prove the concrete bridge that constructs
`ExecutableBadVectorWitness.IsBhksBadVectorSetup` from a projected vector in
`L' \ W` during a failed executable recovery run, then use the new cap
separation theorem in the recovery-success issue.

## Blockers

None for this proof layer.
