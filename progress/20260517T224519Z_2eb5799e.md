# Replan triage 2eb5799e

## Accomplished

Triaged three `replan` candidates returned by `coordination list-replan`:

- **#4839** (BZ Mathlib-free: finish small-mod singleton irreducibility
  transfer) — worker-decomposed. Sub-issues #4850 and #4851 fully cover
  the parent (deliverables 1+2 → #4850; deliverables 3+4 → #4851).
  Closed with forward links.
- **#4844** (HexGramSchmidt: prove adjacent-swap bordered-minor
  identities for scaled coefficients) — worker-decomposed. Sub-issues
  #4852, #4853, #4854 fully cover the parent (Desnanot-Jacobi
  substrate + the above-prev and above-curr identities). Closed with
  forward links.
- **#4850** (BZ Mathlib-free: place small-mod singleton Berlekamp
  irreducibility in Rabin-importing module) — plan stale. The worker
  audit established that deliverable 2
  (`gcd_monicModularImage_derivative_eq_one` as an executable
  equality) is provably false because executable `DensePoly.gcd` is
  not monic-normalized — scaling both inputs by a unit scales the
  output. Re-decomposed along the worker's recommended relaxed-
  signature axis into:
    - **#4857** — Berlekamp soundness: relax `hsquareFree` precondition
      to non-normalized form (`isUnitPolynomial`/common-divisor) through
      the `RabinSoundness` chain culminating in
      `berlekampFactor_singleton_irreducible`.
    - **#4858** — Place small-mod singleton Berlekamp irreducibility
      in Rabin-importing module (v2), `depends-on: #4857`. Discharges
      the relaxed hypothesis from `isGoodPrime` invariants via unit-
      scale divisibility, then composes with the singleton-length
      bridge from PR #4836.
  Closed #4850 with forward links to #4857 / #4858.

Re-pointed #4851's stated dependency from #4850 to #4858 (in-body
`depends-on:` line + every reference, plus an explanatory comment).
Its Gauss-lemma transfer and branch-level `ZPoly.Irreducible`
assembly scope is unchanged.

Also closed an accidental issue #4855 created by piping `--help` into
`coordination plan` (the script does not implement `--help` and instead
created an issue titled `--help`).

## Current frontier

`coordination list-replan` is empty. The Berlekamp small-mod singleton
work is now staged as #4857 (unblocked, signature-relaxation work) →
#4858 (blocked on #4857) → #4851 (now blocked on #4858, was blocked on
#4850).

## Next step

Workers can claim #4857 (the only newly-unblocked replacement). The
planner-lock is held by `pod` while this replan session ran; pod will
release it on exit.

## Blockers

None on the triage side. Downstream: #4851 stays blocked on the
relaxed-signature path through #4857 / #4858.
