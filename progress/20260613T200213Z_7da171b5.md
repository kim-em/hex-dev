# Progress: #6925 — complete HexGramSchmidt Phase 5 bump

## Accomplished

Claimed #6925 and verified `HexGramSchmidt` against the Phase 5 exit criteria.
The implementation has no `sorry` / `admit` / `native_decide` markers in
`HexGramSchmidt/`, `lake build HexGramSchmidt` succeeds, and
`lake build hexgramschmidt_bench` succeeds.

Cross-checked the public surface against
`SPEC/Libraries/hex-gram-schmidt.md`: the integer/rational `basis` and
`coeffs`, `gramDet`, `gramDetVec`, per-row Schur `scaledCoeffs`, size-reduction
update formulas, and adjacent-swap executable/update API are present. The
Mathlib-free core intentionally leaves the determinant-crossing swap
scaled-coefficient identities in `HexGramSchmidtMathlib`, as documented in
`HexGramSchmidt/Update.lean`.

Bumped `libraries.yml` `HexGramSchmidt.done_through` from 4 to 5.

Verification run:

- `lake build HexGramSchmidt`
- `lake build hexgramschmidt_bench`
- `python3 scripts/status.py` shows `HexGramSchmidt -> Phase 6`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "\bsorry\b|\badmit\b|native_decide|TODO|FIXME" HexGramSchmidt`

The Lean builds still report the pre-existing lint warnings in
`HexGramSchmidt/Basic.lean:1606` and `HexGramSchmidt/Int.lean:4939`.

## Current frontier

`HexGramSchmidt` is now ready for Phase 6 proof-polishing work. Downstream
`HexGramSchmidtMathlib` remains at Phase 4 and can advance against the completed
core library.

## Next step

Open the PR for #6925 with the metadata bump and verification note.

## Blockers

None.
