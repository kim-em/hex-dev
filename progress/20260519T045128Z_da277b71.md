# Session da277b71 — HexPolyZMathlib: decomposed Mahler/Boyd derivative bound (#5235)

## Accomplished

Claimed issue #5235 (sub-issue 3/3 of the Robinson-form chain). After
orienting on `HexPolyZMathlib/RobinsonForm.lean`,
`HexPolyZMathlib/Mignotte.lean`, and the Mathlib Mahler-measure /
JensenFormula / CircleAverage modules, concluded the analytic step

    M(p.derivative) ≤ M(p.robinsonForm.derivative)

is genuinely beyond a single session. The pointwise inequality
`|p'(z)| ≤ |p.robinsonForm.derivative(z)|` on `‖z‖ = 1` does not hold in
general (the issue body itself flagged this), so `circleAverage_mono`
cannot be applied directly; the standard Boyd 1980 argument uses a
factor-replacement / one-parameter family construction that warrants its
own session(s).

Decomposed #5235 per Step 4b of `agent-worker-flow`:

- Opened #5265 "HexPolyZMathlib: prove M(p.derivative) ≤ natDegree p ·
  M(p) for complex polynomials (Mahler/Boyd analytic step)". Carries the
  analytic deliverable in isolation with a proof-shape sketch
  (Jensen-on-p' → analytic comparison → Jensen-on-q' →
  `mahlerMeasure_robinsonForm_derivative`), explicit upstream-API
  references (`logMahlerMeasure_def`, `circleAverage_mono`,
  `circleAverage_mono_on_of_le_circle`,
  `Mathlib.Analysis.Complex.JensenFormula`), and a permission to
  decompose further into single-factor-replacement sub-lemmas.

- Opened #5266 "HexPolyZMathlib: transport M(p.derivative) ≤ natDegree p
  · M(p) to Polynomial Int" with `depends-on: #5265`. One-line corollary
  using existing `natDegree_map_intCast`, `support_map_intCast`,
  `norm_coeff_map_intCast` plus `Polynomial.derivative_map`.

- The optional BHKS Φ-coefficient form (deliverable 3 of #5235) was
  intentionally not split off into a third sub-issue — the comment on
  #5235 records this so a successor planner can scope it after #5266
  lands with a concrete transport signature.

- Posted the `Decomposed into #5265, #5266` breadcrumb on #5235 and
  released the claim via `coordination skip` (replan label).

No code changes; the working tree is unchanged from `origin/main` at
`4b96f86b`.

## Current frontier

#5265 is the open analytic frontier. The remaining Mathlib gap to close
it is the polynomial-specific Mahler/Boyd comparison; the supporting
infrastructure (Jensen formula via `AnalyticOnNhd.circleAverage_log_norm`,
`logMahlerMeasure_def`, `circleAverage_mono`,
`norm_eval_robinsonForm_eq_norm_eval`, and
`mahlerMeasure_robinsonForm_derivative`) is all already in place.

## Next step

A successor worker on #5265 should start by deciding whether the proof
goes via (a) a `circleAverage` factor-by-factor comparison after
replacing one `(X - α)` at a time, or (b) the `circleAverage_mono` route
with an intermediate inequality coming from a Jensen-formula corollary
applied to `p · q.derivative / q` (or similar). Either way the work is
likely to warrant a further single-factor-replacement sub-issue.

If after a real attempt the worker concludes #5265 is infeasible with
current Mathlib, the parent's escape hatch (filing a SPEC-clarification
issue against `SPEC/Libraries/hex-berlekamp-zassenhaus.md` line 396 to
revise BHKS Lemma 5.1 away from the Mahler-on-Mahler step — Path C of
#5230's analysis) remains available.

## Blockers

None. #5265 is fresh and unclaimed; #5266 is `blocked` on #5265 by
design.
