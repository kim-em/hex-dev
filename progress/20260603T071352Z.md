# Critiqued Schur/Bareiss proof plan

## Accomplished

Reviewed the residual `getArrayEntry_scaledCoeffRowsSchur_eq` sorry plan
against the current `HexGramSchmidt/Int.lean` definitions, the existing
Bareiss-side characterizations, `schurSigma`, `schurScaledCoeffEntry`, and
the Mathlib-free determinant/Bareiss infrastructure.

No code or proof changes were made.

## Current frontier

The residual `j ≤ i` case remains open. The plan's high-level determinant
story is directionally right for the non-singular strict-lower case, but
needs tighter indexing, a direct `borderedMinor`/Desnanot-Jacobi formulation,
and a different singular-case story. The singular cascade claim through
zero Schur diagonals is not justified as stated because `schurSigma` uses
ordinary integer division through `Matrix.exactDiv`, and a zero divisor does
not support the proposed exact-division reasoning.

## Next step

Before dispatching another proof worker, narrow the directive to either the
structural `j = 0` slice or to a precise non-singular `schurSigma` invariant
phrased in terms of existing `Matrix.borderedMinor` trailing determinants.
Avoid assigning the singular Schur-zero lemma until the exact singular
semantics are pinned.

## Blockers

No environment blockers. The proof-plan blocker is specification precision:
the intended σ-chain invariant and the singular behavior need exact Lean
statements before implementation work is likely to progress.
