# 20260603T205236Z work #2567

## Accomplished

Completed the remaining "factored variants of the other three
`*AndPaperThreshold` builders" plumbing called out in
`progress/20260603T100234Z_34a2ee77.md` next-step (1).  Three new
factored-paper-threshold sibling constructors plus three matching
`factorFast_terminates_*AndFactoredPaperThreshold` wrappers in
`HexBerlekampZassenhausMathlib/Recovery.lean`:

* `FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndFactoredPaperThreshold`
  — pointwise vector/correction estimates + factored BHKS Theorem 5.2 LHS
  sub-bounds (`coeffPow ≤ bhksPaperCoeffNormFactorReal core` and
  `auxPow ≤ bhksPaperDegreeFactorReal core * bhksPaperConstantFactorReal
  core C * bhksPaperLogFactorReal core`).
* `FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSqAndFactoredPaperThreshold`
  — structured corrected RHS sum + factored sub-bounds.
* `FactorFastCapSeparationInputs.ofBridgeDataAndFactoredPaperThreshold`
  — bridge data alone (auxiliary l2 bound derived internally via
  `BadVectorBridgeData.auxiliaryPolynomial_l2norm_le_sqrt_of_bridge_data`)
  + factored sub-bounds (auxiliary-power factor lives on the
  bridge-derived `Real.sqrt RHS`).  Delegates to the factored
  l2norm sibling so the factored cap-arithmetic chain is reused.

Each constructor dispatches through
`bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_factored`
(introduced in #6487).  The matching `factorFast_terminates_*` wrappers
compose with `factorFast_terminates` so callers can land directly on
`Hex.factorFast f ≠ none`.

## Current frontier

Both surfaces of the `*AndPaperThreshold` builder family — the
combined-product axis (existing) and the factored-LHS axis (now
complete) — are exposed on all four sibling builders
(`PointwiseAuxiliaryBounds`, `CorrectedAuxiliaryL2normSq`,
`AuxiliaryL2norm`, and the bridge-only `ofBridgeData`), with matching
`factorFast_terminates_*` final wrappers.

Callers needing the factored BHKS Theorem 5.2 inequality content
(`‖core‖₂^(2n-1)` decoupled from `n · (2C)^(n²) · (log ‖core‖₂)^n`) can
now feed every level of input shape — pointwise, structured RHS, direct
l2norm, or bridge-only — without re-deriving the combined product
inequality.

## Next step

The two genuine analytic gaps from the previous progress file remain:

1. **Coefficient-power sub-bound** —
   `coeffL2NormBound ^ aux.natDegree ≤ bhksPaperCoeffNormFactorReal core`
   from the degree relation `aux.natDeg ≤ 2n - 1` combined with the
   integer ⌈√⌉ vs. real l2-norm relation already encoded by
   `l2norm_toPolynomial_sq_le_coeffNormSq`.
2. **Auxiliary-power sub-bound** —
   `auxiliaryBound ^ input.natDegree ≤ bhksPaperDegreeFactorReal *
   bhksPaperConstantFactorReal C * bhksPaperLogFactorReal`, the genuine
   BHKS Lemma 3.2 / Theorem 5.2 content.

Either can be picked up independently; the factored axis now exposes
each as its own consumer.

## Blockers

None.  The new constructors and wrappers are pure compositions over
existing infrastructure.

## Verification

- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  passed.
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing.
- Diff: 745 added lines, zero semantic deletions
  (`git diff --stat` reports 20 "deletions" — these are spurious unified-
  diff alignment artefacts from inserting similarly-shaped wrappers
  before existing ones; `grep -c '^theorem factorFast_terminates_ofBridgeData'`
  rises from 5 to 8, exactly the three new wrappers, with all five
  pre-existing wrappers intact).
