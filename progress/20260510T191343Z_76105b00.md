## Accomplished

Ran the worker queue selection flow, checked human-oversight items first, and
confirmed all listed human-oversight issues were blocked by dependencies.

Claimed and triaged four stale/premature feature issues:

- `#2803`: marked for replan because the expected executable irreducibility
  surface is still absent; `factor_product_of_bound` and
  `ZPoly.isIrreducible_iff` remain proof holes in
  `HexBerlekampZassenhaus/Basic.lean`.
- `#3099`: marked for replan because it depends on helper lemmas from PR
  `#3096`, which is still open and not present on this branch.
- `#3102`: marked for replan because it assumes `#3092` has delivered
  `content_scale_int` and `rational_associate_primitive_unit`, but `#3092` is
  still open/claimed and those lemmas are absent locally.
- `#2917`: marked for replan because its tail-cross prerequisite is still the
  residual work in `#3099`; `yunFactors_squareFreeAuxRev_tail_cross_coprime`
  remains a proof hole in `HexPolyFp/SquareFree.lean`.

Fast-forwarded to current `origin/main` after `#3074` landed, then claimed
`#3075` and proved the suffix RHS fold-alignment helper in
`HexMatrix/Determinant.lean`:

- Added `foldl_det_sum_nested_start` and its zero-start specialization to
  normalize nested fold accumulators after `foldl_det_sum_flatMap`.
- Added private `columnTupleVectors_suffix_rhs_step`, rewriting the suffix RHS
  into an outer fold over the peeled column and an inner recursive suffix RHS,
  using the landed coefficient and assembled-column cast wrappers.

Verified:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`
  returned no matches.

## Current frontier

`#3075` is ready for PR. The dependent `#3076` can proceed after this helper
lands.

The worktree still has the pre-existing generated `.claude/CLAUDE.md` diff; I
left it untouched.

## Next step

Open the PR for `#3075`. Once PR `#3096` and issue `#3092` land, the replanned
HexPolyFp/HexPolyZ follow-up issues can be made unblocked again.

## Blockers

None for `#3075`. The worktree still has the pre-existing generated
`.claude/CLAUDE.md` diff; I left it untouched.
