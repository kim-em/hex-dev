# 20260510T190011Z — feature session 0475de5e

## Accomplished

Claimed issue #3061 and proved row-operation inverse preservation lemmas in
`HexMatrix/RowEchelon.lean`.

Added row-operation cancellation lemmas for:
- double row swaps,
- scaling by `s` then `s⁻¹` and conversely under `s ≠ 0`,
- row-add by `s` and `-s` for distinct source/destination rows.

Used those to prove preservation of both inverse fields for row swap, row scale,
and row add:
- `rowSwap_left_inverse_preserve`
- `rowSwap_right_inverse_preserve`
- `rowScale_left_inverse_preserve`
- `rowScale_right_inverse_preserve`
- `rowAdd_left_inverse_preserve`
- `rowAdd_right_inverse_preserve`

Verification run:
- `lake build HexMatrix.RowEchelon`
- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RowEchelon.lean HexMatrix/RREF.lean HexMatrix/Basic.lean`

The marker scan reports only the pre-existing `HexMatrix/RREF.lean:437` sorry.

## Current frontier

Issue #3061 is ready for PR. The new lemmas are in `HexMatrix/RowEchelon.lean`
and do not change executable definitions.

## Next step

Open the PR closing #3061. After it lands, #3080 can use the row-operation
multiplication and inverse-preservation surface for `eliminateColumn` transform
preservation.

## Blockers

None.
