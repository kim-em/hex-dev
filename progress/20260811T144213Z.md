# Accomplished

- Reconciled the proof-premise slice with current `main` after the preceding
  sine quotation PR merged.
- Preserved both the live quotation gate from `main` and the generic
  assumption, generated-top, and ordered-input proof assembly introduced by
  this slice.
- Resolved the SPEC overlap by retaining the proof-term provenance boundary.

# Current frontier

The reconciled tree contains the reviewed proof-premise API together with the
already-merged live sine quotation check. Focused proof-emitter and sine
conformance builds are the merge gate for the reconciliation.

# Next step

Push the reconciled head, rerun exact-head CI, and merge the slice before
retargeting the next proof-registry/frontend PR.

# Blockers

None.
