# Progress — 2026-06-14 (session 53389f07, work)

## Accomplished

Issue #6866: added `HexBerlekampZassenhausMathlib` to the existing
`.github/workflows/ci.yml` ubuntu `build` job as a sequential
`lake build HexBerlekampZassenhausMathlib` step after the main Hex build.
No workflow, job, matrix, or parallelism was added.

Updated `SPEC/CI.md` so the documented `ci.yml` `build` job contents include
the HO-1 Mathlib bridge build.

Before editing, verified that the old blocker was cleared:
`lake build HexBerlekampZassenhausMathlib` completed successfully on this
worktree. The branch is one commit behind `origin/main`, but the upstream
commit only touches `HexPoly/Euclid.lean` and a progress file, not the BZ
Mathlib or CI files.

## Current frontier

The CI gate now checks that the BZ Mathlib bridge elaborates without hard
errors. It does not make existing `sorry` warnings fatal.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- workflow inspection confirmed one ubuntu job (`build`), the existing
  macOS job (`build-macos`), and no `strategy`/`matrix`.

Additional local sanity check:

- `lake exe cache get` reported the Mathlib cache was already decompressed,
  but `bash scripts/ci/check_no_mathlib_rebuild.sh` still failed locally
  because this worktree has zero `*/Mathlib/*.olean` paths under `.lake`.
  This appears to be a local cache-layout issue independent of this diff;
  the actual Lean build targets above completed successfully.

## Next step

Open the PR for #6866. CI will exercise the new required build step in the
GitHub Actions cache layout.

## Blockers

None for this issue.
