# 2026-06-01T10:30Z - HO-1 public product API wrappers (#2564 partial)

## Accomplished

- Claimed directive issue #2564.
- Added public product-preservation wrappers in `HexBerlekampZassenhaus/Basic.lean`:
  `factorSlow_product`, `factorFast_product_of_some`, and `factor_product`.
- Verified `lake build HexBerlekampZassenhaus`.
- Confirmed `HexBerlekampZassenhausMathlib.Basic` replayed/built during the
  bridge build attempt.

## Current frontier

- This is a small partial HO-1 API hardening step. It exposes product contracts
  already proved internally for the bounded/private implementation surfaces.
- The full HO-1 headline theorem and remaining irreducibility/no-associates
  clauses remain open.

## Next step

- Continue assembling the irreducibility arm around the existing
  `factor_entries_irreducible` scaffolding, or repair the stale
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` call sites so the full
  bridge target builds again.

## Blockers

- `lake build HexBerlekampZassenhausMathlib` currently fails in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` on pre-existing stale
  API/signature mismatches around `squareFreeModP`,
  `liftedFactorSubsetPartition_of_choosePrimeData`,
  `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`, and missing
  `monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`.
