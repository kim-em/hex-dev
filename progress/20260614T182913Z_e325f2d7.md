# Progress — 2026-06-14 (session e325f2d7, feature)

## Accomplished

Issue #7311: added one-sentence Phase 6 docstrings to the eight
undocumented private theorems in the `rat_scale_*` / `rat_dvd_*`
rational-scaling-through-product cluster of `HexPolyZ/Basic.lean`
(lines ~1823–1928):

- `rat_scale_mulCoeffStep`, `rat_scale_mulCoeffStep_fold`,
  `rat_scale_mulCoeffOuter_fold`, `rat_scale_mulCoeffSum_of_ne_zero`,
  `rat_scale_mul_scale`, `rat_dvd_scale_of_dvd`,
  `rat_leadingCoeff_ne_zero_of_pos_size`, `rat_div_mul_cancel_of_ne`.

Doc-only: no signatures, statements, proofs, `@[simp]`/`@[grind]`
annotations, declaration order, or imports touched. Each docstring
leads with the backticked symbol and states the lemma's role in the
scaling-distributes-over-product / divisibility argument, matching the
house style of the already-documented `rat_divMod_remainder_degree_lt_core`.

## Verification

- `lake build HexPolyZ.Basic`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexPolyZ/Basic.lean`: `18 0` (additions only).
- No banned vocabulary or em-dashes on added lines.

## Current frontier

This cluster is fully documented. The rest of `HexPolyZ/Basic.lean`
(declarations outside lines ~1823–1928) remains for separate Phase 6
batches.

## Next step

None for this issue. Other unclaimed Phase 6 doc batches remain in the
queue (#7314 HexConway, #7317 HexMatrix Bareiss).

## Blockers

None.
