## Accomplished

Claimed and completed #3024, the abstract BHKS separation interface for
`L' = W`.

In `HexBerlekampZassenhausMathlib/Lattice.lean`, added:

- `BHKS.SeparationHypotheses`, packaging the existing `W <= L'` cut evidence
  plus a reverse contradiction hypothesis for vectors in `L' \ W`.
- `BHKS.projectedRowSpan_eq_trueFactorIndicatorLattice`, proving equality by
  `le_antisymm` from those hypotheses.

Verified:

- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/Lattice.lean`

## Current frontier

The Mathlib bridge now has the proof-facing capstone theorem that turns a
future bad-vector contradiction for every vector in `L' \ W` into the abstract
equality `projectedRowSpanInt L = trueFactorIndicatorLattice trueSupports`.

## Next step

Continue the HO-4 chain by instantiating the reverse contradiction hypothesis
from concrete failed-recovery data, or use the separate cap-bound wrapper issue
to package the numerical contradiction needed by that instantiation.

## Blockers

None for this interface layer.
