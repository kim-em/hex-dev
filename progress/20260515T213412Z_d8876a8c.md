# Replan triage: #4157 and #4227

Replan session triaging two `replan`-labelled issues. Issue edits
only; no source changes, no PR.

## Accomplished

### #4227 — HexBerlekamp no-kernel-split irreducibility composition

Disposition: **Still valid, body stale.** The original `depends-on:`
pointed at #4261, which was decomposed into the refactor #4313 and
the iff bridge #4314 (#4261 closed). Updated the issue body and
narrative to track the new chain (`#4313 -> #4314 -> #4227`),
including renaming references to `#4314` throughout the
deliverable/context/verification sections. Removed the `replan`
label. The `depends-on: #4314` line will pick up `blocked` via
`check-blocked` until #4314 lands.

### #4157 — HO-43 Round 2 sizeReduce relocation

Disposition: **Plan stale / approach changed → refile.** The original
issue body explicitly anticipated this: "If #3916's landed shape
leaves any residual call site, narrow this issue's scope or refile."

The skipping worker found that #3916 (the prereq) had a narrower
landed scope than the planner of #4157 assumed: #3916 only changed
the `ν_eq` invariant to use `GramSchmidt.Int.scaledCoeffs` and
cleared the `scaledCoeffs_eq` call. The remaining
`scaledCoeffs_sizeReduce_*` and `gramDet_sizeReduce` calls in
`LLLState.sizeReduceColumn` at `HexLLL/Basic.lean` L296/323/349/363/369
were never cleared, so deliverable 3 of #4157 was unsatisfied and
the relocation would have either cascaded `sorry` into
`sizeReduceColumn` or forced a bridge-module import into the
Mathlib-free `HexLLL/Basic.lean`.

Refiled as #4324, bundling the consumer-side discharge refactor
(eliminating those call sites by certifying the executable
invariant directly, without `Matrix.bareiss_eq_det`) with the
relocation in a single deliverable set. Closed #4157 with a
forward link to #4324. Added the `feature` label to #4324 so it
shows up in `coordination list-unclaimed` for workers.

### Stray issue created during triage

Accidentally created issue #4323 (titled `--help`) while probing
the `coordination plan` CLI for usage info (the subcommand
interpreted `--help` as the title rather than printing help).
Closed it immediately with an apology comment. No further damage.

## Current frontier

Replan queue empty (`coordination list-replan` returns nothing).
#4227 sits behind the #4313 → #4314 → #4227 chain; #4324 is
ready for the next worker to claim.

## Next step

Hand off to the next dispatch cycle. Workers can claim #4324; a
planner will eventually plan #4313 / #4314 if they're not already
queued.

## Blockers

None for this session.
