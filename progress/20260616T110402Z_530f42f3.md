# Progress - 2026-06-16T11:04:02Z (session 530f42f3, work)

## Accomplished

Claimed issue #7569 and replaced the false non-monic unscaled support
obligation on the toMonic lifted-substrate path.

In `HexBerlekampZassenhausMathlib/Basic.lean`, made
`LiftedFactorSubsetPartition.support_subset_of_dvd_recombinationCandidate`
monic-only by adding a `leadingCoeff core = 1` side condition. The toMonic
partition constructors now derive that monic-only field from the recovered
`liftedRecoveryCandidate` support projection instead of taking a separate
unscaled support hypothesis. Primitive positive-leading unscaled helper chains
now explicitly carry the monic side condition where they still reason about the
unscaled recombination coordinate.

In `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, removed the threaded
`hunscaled` hypothesis from the affected outer-bound, monic-reduction,
exhaustive-branch, and reassembly wrappers. Callers now pass the recovered
initial partition evidence without also supplying the invalid non-monic
`liftedFactorProductCandidate` containment theorem.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over touched Lean files found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The toMonic substrate and downstream slow-path wrappers no longer require the
known-false unscaled support field. Remaining unscaled recombination helper
surfaces are guarded by an explicit monic side condition.

## Next step

Use the narrowed recovered-coordinate substrate in the downstream slow-modular
raw irreducibility integration (#7561) without manufacturing any unscaled
support proof for non-monic square-free cores.

## Blockers

None for #7569. The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md`; this session did not touch or stage it.
