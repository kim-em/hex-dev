# HexHenselMathlib Phase 6 exit-criteria audit (#8234)

Session type: review. UTC 2026-06-20T09:53.

## Accomplished

Audited `HexHenselMathlib` (a `mathlib: true` bridge library,
`done_through: 5`, deps `[HexHensel, HexPolyMathlib]`) against the four
Phase 6 exit criteria. Verdict: **(a) — bump `done_through` 5 → 6**; all
four criteria pass after one trivial inline fix.

- **Linter (criterion 1) → clean after fix.** `lake build` replays
  nothing on a cache hit, so I forced a fresh recompile (deleted the
  `Basic`/`Correctness` oleans, rebuilt). That surfaced three
  `unusedVariables` warnings in `hensel_extends`
  (`Correctness.lean:69/70/74`: `hprod`, `hbez`, `hmonic`). The proof of
  `hensel_extends` needs only `hk` — the three preconditions are part of
  the parallel hypothesis block it shares with `hensel_correct` /
  `hensel_degree`. Fixed by underscoring the binders (signature-
  preserving; `hensel_extends` has zero callers, so removal would be
  risk-free too, but underscoring keeps the three-theorem family's
  signatures parallel for the future Phase 7 chapter). Fresh rebuild
  after the fix: zero warnings. No mathlib `runLinter`/`#lint` binary is
  built; as with the sibling Mathlib-layer audits the criterion is Lean's
  built-in linters via a clean compile.
- **Docstring coverage (criterion 2) → pass.** Every public *and*
  private declaration in both files already carries a `/-- -/`
  docstring: `Basic.lean` 11/11 public theorems; `Correctness.lean`
  13 public + 6 private decls, all documented (verified the openers are
  `/--`, not section `/-!`). No gaps.
- **Dead code (criterion 3) → pass.** All six private helpers
  (`isCoprime_of_zpoly_bezout`, `natDegree_eq_of_monic_map_eq`,
  `exists_C_pow_mul_of_map_pow_eq`, `map_C_pow_mul_eq_zero_of_map_modP_eq_zero`,
  `quadraticMultifactorLiftInvariant_of_multifactorLiftInvariant_congr`,
  `multifactorLiftList_map_eq_quadratic`) are referenced ≥1×. Six public
  theorems (`hensel_correct`, `hensel_extends`, `hensel_degree`,
  `quadraticHenselStep_bezout_correct`,
  `quadraticHenselStep_unique_mod_pow_two_mul`,
  `quadraticMultifactorLiftInvariant_of_multifactorLiftInvariant`) have no
  in-repo consumer, but these are the bridge's headline
  correspondence/uniqueness API — intentional exported roots, not dead
  code (consistent with how the sibling audits treat public
  correspondence theorems).
- **Native / oracle + irreducibility (criterion 4) → pass.** No
  `native_decide` / `sorry` / `axiom` (grep). `#print axioms` on
  `hensel_correct`, `hensel_unique`,
  `multifactorLift_eq_multifactorLiftQuadratic`: all
  `[propext, Classical.choice, Quot.sound]`. Irreducibility N/A — this is
  a Mathlib polynomial bridge, not an irreducibility decision procedure.

`lake build HexHenselMathlib` green (fresh recompile, zero warnings).
`libraries.yml[HexHenselMathlib].done_through 5 → 6`; `check_dag.py`
exit 0.

## Current frontier

`done_through: 6` — `HexHenselMathlib` is now Phase 7 eligible. A future
planner can author its Phase 7 reference chapter (`HexManual`).

## Next step

Phase 7 reference chapter for `HexHenselMathlib` (separate future issue,
explicitly out of scope here).

## Blockers

None.

## Note (not blocking)

The three underscored hypotheses in `hensel_extends` are mathematically
redundant (the extends property follows from the congruence-mod-base
lemmas, which need only `hk`). Removing them would strengthen the API
with zero caller churn; left in place to keep the
`hensel_correct`/`hensel_extends`/`hensel_degree` signatures parallel.
Several theorem names carry 5+ qualifiers
(`quadraticMultifactorLiftInvariant_of_multifactorLiftInvariant_congr`);
a future cleanup could shorten them, out of scope for this low-risk audit.
