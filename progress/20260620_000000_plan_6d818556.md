# Planner cycle — Phase 7 chapter dispatch (#8129)

## Accomplished

Planner session. Queue deficit was 1 (`POD_QUEUE_DEFICIT=1`,
`min_queue=3`, depth=2). Created **one** atomic feature issue:

- **#8129** feat: HexModArithMathlib Phase 7 — HexManual reference
  chapter. The first *Mathlib-bridge* Verso chapter (existing
  `HexGFqRing` chapter documents an executable lib); establishes the
  pattern for remaining `*-mathlib` chapters.

Survey findings:
- The Phase 6 grind-coverage sweep is **essentially complete** across
  executable + Mathlib libs — no clearly-unswept files remain (scanned
  HexArith/Matrix/GFq/GFqField/PolyMathlib/MatrixMathlib/Poly/GFqRing
  for `plain_simp ≥ 4 ∧ grind ≤ 2`: zero hits). Remaining plain
  `@[simp]` lemmas are the deliberately-excluded `↔`/`<`/`≤`/`Ne`/`∧`
  categories.
- Next frontier is **Phase 7 (user-facing docs)**. Three libs ready:
  HexPoly (in flight, #8126/#8127), HexModArithMathlib (→ #8129),
  HexGFqMathlib (still uncovered, 606 lines / 2 files — left for a
  future cycle, likely split).

## Current frontier

- Unclaimed: #8124, #8128 (Phase 6 grind sweeps), #8129 (new).
- Claimed: #8123, #8121. PR #8127 (HexPoly Phase 7) build/conformance
  IN_PROGRESS — not merged this cycle.
- PR #2656 (spec) is an intentional draft; left unmerged.
- Directive #2564 (BZ van Hoeij rewrite) blocked; left untouched per
  directive rules. Dependents #8068/#6779/#4818 remain blocked on it.

## Next step

Next planner: consider a HexGFqMathlib Phase 7 chapter issue (likely
split into Basic.lean + GF2q.lean), once #8129 lands the bridge-chapter
pattern. Watch PR #8127 to merge when CI goes green.

## Blockers

None for planning. Directive #2564 remains the long-pole correctness
item, blocked and unclaimed.
