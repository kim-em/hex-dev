# Work session: HexPolyFp Yun offset invariant

## Accomplished

- Claimed #2170 and replaced the stale derivative-active Yun initial-state contribution statements in `HexPolyFp/SquareFree.lean`.
- Reproved the targeted initial-state `done` and `tail` lemmas from `yunFactors_reconstruction_invariant`, with conclusions expressed as the offset-form `weightedProduct` of the factors emitted by `yunFactors`.
- Updated the local reconstruction wrappers to propagate the corrected offset-form invariant instead of the false `pow f multiplicity` target.
- Isolated the remaining derivative-active assembly back to `pow f multiplicity` as `squareFreeAuxRevContribution_derivative_active_pow_obligation`.

## Current frontier

The targeted stale invariant placeholders are removed. `HexPolyFp/SquareFree.lean` now has four `sorry`s: the new named derivative-active assembly obligation plus the three pre-existing coprime/square-free obligations later in the file.

## Next step

Prove `squareFreeAuxRevContribution_derivative_active_pow_obligation` by combining the offset-form Yun weighted product with the factorisation algebra relating the initial derivative split back to `pow f multiplicity`.

## Blockers

No technical blocker found. The remaining derivative-active assembly is broader than the invariant repair completed here.
