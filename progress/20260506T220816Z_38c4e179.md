# 2026-05-06 22:08 UTC — work session (38c4e179)

## Accomplished

Claimed #2336, verified it was stale because
`degree_dvd_of_irreducible_dvd_xPowSubX` is already proved on current
`origin/main`, and released it for replan with that reason.

Claimed #2500, assessed it as too broad for one proof session, and decomposed it
into:

- #2529 for the `gramDetVec` leading-prefix Bareiss bridge.
- #2530 for the `scaledCoeffs` diagonal bridge.
- #2531 for the below-diagonal Cramer/scaled-coefficient bridge, depending on
  #2530.

Marked #2500 for replan with the required `Decomposed into #2529, #2530, #2531`
breadcrumb, then claimed #2530.

For #2530, updated `HexGramSchmidt/Int.lean` by adding reusable array-write
facts for `writeScaledColumn` diagonal entries and reducing the broad
`scaledCoeffRows_diag_eq_gramDet` placeholder to two explicit theorem-level
obligations:

- `scaledCoeffRows_diag_eq_noPivotData_diag`
- `noPivotData_gram_diag_eq_gramDet`

Verification completed:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Int.lean`

## Current frontier

#2530 has a narrower proof skeleton and local array lemmas, but still needs the
two named theorem-level obligations above to eliminate the diagonal helper
`sorry`s completely.

## Next step

Prove `scaledCoeffRows_diag_eq_noPivotData_diag` by relating
`scaledCoeffArrayLoop` to `Matrix.noPivotLoop` diagonal writes, using
`getArrayEntry_writeScaledColumn_diag` for the step where the diagonal is
recorded.

## Blockers

No technical blocker. The worktree still has a pre-existing unrelated
`.claude/CLAUDE.md` modification that was not touched.
