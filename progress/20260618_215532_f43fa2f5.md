# 20260618_215532 — f43fa2f5 (feature, via /work)

## Accomplished

- **#7982**: Phase 6 grind-automation pass for `HexPolyFp/Enumeration.lean`.
  - `@[simp] → @[simp, grind =]` on the already-simp equation-shaped facts:
    `coeffLists_zero`, `coeffLists_succ`, `coeffLists_length`,
    `polysBelowDegree_length`, and the iff `mem_polysBelowDegree_succ_iff_degree_getD_lt`.
  - `@[grind =]` (not previously simp) on the two characterizing iffs:
    `mem_coeffLists_iff` and `mem_polysBelowDegree_iff_degree_getD_lt`.
  - Payoff demonstrated: `mem_polysBelowDegree_succ_iff_degree_getD_lt`
    now closes by `grind` (was `exact mem_polysBelowDegree_iff_degree_getD_lt (Nat.succ_pos d)`),
    using the registered conditional iff.
  - Deliberately left `coeffLists_nodup` / `polysBelowDegree_nodup`
    unannotated: `Nodup` propositions offer grind no useful rewrite or
    instantiation pattern. Forward-only specializations
    (`length_of_mem_coeffLists`, `degree_getD_lt_of_mem_polysBelowDegree`,
    `mem_polysBelowDegree_of_degree_getD_lt`) left unannotated too — they
    are subsumed by the iff rewrites for grind.
  - No executable behavior change, no `done_through` bump, no new forbidden
    tokens. `lake build HexPolyFp` green; `check_dag` exit 0.

- **#6672 coordination fix** (no code): its `depends-on: #7975` auto-cleared
  when #7975 closed, but #7975 closed as *premise-unsound, folded into
  directive #2564* — its fast-BHKS raw-irreducibility substrate has no
  producer from executable success (product preservation does not imply
  per-factor irreducibility; that is the open BHKS `W ⊆ L'` cut soundness).
  Re-added `depends-on: #2564` and commented, so the queue no longer
  presents the capstone as ready while the directive's soundness theorem is
  unproved.

## Current frontier

`HexPolyFp/Enumeration.lean` Phase 6 grind coverage complete. Sibling
modules under HexPolyFp already covered (`Basic.lean` via #7958).

## Next step

Other libraries' Phase 6 grind passes continue as separate issues. The
HO-1 capstone cluster (#6672 / #4818 / #2564) is blocked on the open BHKS
recombination-soundness theorem and is planner/directive-level work, not a
single feature session.

## Blockers

None for #7982.
