# Session 20260602T015900Z (a9a484aa) — feature / #6200

## Accomplished
- Added two private lemmas in `HexPolyFp/SquareFree.lean` (placed in the
  monic-related cluster between `ne_zero_of_monic_fpoly` and
  `monic_of_mul_eq_monic_of_monic`):
  - `normalizeMonic_fst_eq_one_of_monic`: monic input ⇒ recorded unit
    scalar `= 1`. Proves `f.isZero = false` inline via the contradiction
    `leadingCoeff f = 1 ∧ leadingCoeff 0 = 0` (using the existing
    `eq_zero_of_isZero_true` and `DensePoly.leadingCoeff_zero`), then
    rewrites with `normalizeMonic_nonzero` and discharges with `hmonic`.
  - `normalizeMonic_eq_self_of_monic`: monic input ⇒ polynomial component
    is the input unchanged. Avoids inverting `(1 : ZMod64 p)` by routing
    through `normalizeMonic_reconstruct` and the `.1 = 1` lemma above,
    collapsing the resulting `DensePoly.C 1 * x = x` to `one_mul`.

## Current frontier
- Issue's verification list is satisfied: `lake build HexPolyFp` is green,
  `python3 scripts/check_dag.py` exits 0, `git diff --check` is clean,
  and the forbidden-token grep is empty.

## Next step
- PR for #6200 → land → unblocks #6125 (consumer-refactor of
  derivative-active provider consumers, which needed exactly this
  `normalizeMonic` self-collapse on raw monic residuals).

## Blockers
- None.
