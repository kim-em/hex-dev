# 2026-05-14 13:16 UTC — work session d1022cd9 (issue #4015)

## Accomplished

- Skipped stale #3977 after observing its `depends-on: #3976` cleared via
  decomposition rather than completion; added `depends-on: #4015` so it
  blocks until the real state relation lands.
- Claimed #4015 and proved the loop-level diagonal state relation in
  `HexGramSchmidt/Int.lean`:
  - `scaledCoeffArrayLoop_done`, `scaledCoeffArrayLoop_id_at_done`,
    `scaledCoeffArrayLoop_singular_branch`,
    `scaledCoeffArrayLoop_last_step`,
    `scaledCoeffArrayLoop_regular_branch`: structural unfolding helpers
    mirroring the existing `noPivotLoop_*` branch lemmas.
  - `getArrayEntry_foldl_setArrayEntry_col_ne`,
    `getArrayEntry_writeScaledColumn_of_col_ne`: column-write isolation
    facts for the bookkeeping layer.
  - `setArrayEntry_size`, `setArrayEntry_rows_size`,
    `foldl_setArrayEntry_size`, `foldl_setArrayEntry_rows_size`,
    `writeScaledColumn_size`, `writeScaledColumn_rows_size`: size
    invariants used by the diagonal-write bridge.
  - `getArrayEntry_eq_rowsToMatrix`: the array-vs-matrix view bridge at
    a single entry.
  - `scaledCoeffArrayLoop_diag_matches`: the main #4015 deliverable. It
    states that, after running both the scaled-coefficient array loop and
    `Matrix.noPivotLoop` for the same fuel from compatible non-singular
    states, each diagonal coefficient at index `i` either matches the
    matrix-level diagonal of the no-pivot Bareiss state (when no singular
    step has been hit at or before `i`) or is recorded as zero (when
    singular at some `s ≤ i`). The disjunction folds in the encoded zero
    tail used by `gramDetVecEntry`, covering the early-singular
    no-pivot branch only to the extent needed for the diagonal slot.

## Current frontier

`HexGramSchmidt/Int.lean` now exposes the loop-level diagonal relation
to the final assembly issue #3977. The remaining work on that issue is
the toNat / `Int.ofNat` reconciliation between `coeffs[i][i]` and
`Int.ofNat (gramDetVecEntry ...)`, which routes through the
non-negativity of the no-pivot Bareiss diagonal on Gram matrices.

## Next step

Land this PR and unblock #3977 (assemble `scaledCoeffRows_diag_eq_gramDetVecEntry`).
The reconciliation likely needs the still-sorried
`gramDetVecEntry_eq_leadingPrefix_bareiss` or a direct non-negativity
fact for the no-pivot Bareiss diagonal on Gram matrices.

## Blockers

None for #4015. The worktree's pre-existing `.claude/CLAUDE.md`
modification is untouched and unstaged.

## Verification

- `lake build HexGramSchmidt.Int` ✓
- `lake build HexGramSchmidt` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Int.lean HexMatrix/Bareiss.lean`
  reports only the pre-existing sorries at lines 1456, 1493, 1516, 3207
  of `HexGramSchmidt/Int.lean` and 1161 of `HexMatrix/Bareiss.lean`; no
  new sorries were introduced.
