# HexPoly Phase 6 - DensePoly coefficient-law polish

Session: `5186f391` (feature, issue #5592)

## Accomplished

Reviewed the `scale`/`shift`/`add`/`sub`/`neg` coefficient-law cluster in
`HexPoly/Operations.lean` for Phase 6 API shape and automation safety.

Added two narrowly scoped semiring/ring normalizers:

- `@[simp] DensePoly.scale_zero_left_semiring`: scalar multiplication by
  `0` normalizes to the zero dense polynomial.
- `@[simp] DensePoly.zero_sub_ring`: subtraction from zero normalizes to
  polynomial negation.

Both wrappers are proved by extensionality from the existing coefficient
normalization lemmas; no executable definitions or multiplication/division
proofs were changed.

Verification run:

- `lake build HexPoly.Operations`
- `lake build HexPoly`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Scoped diff grep for added `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  in `HexPoly/Operations.lean`

## Current frontier

The executable DensePoly operation coefficient surface now has matching
left-zero simplifiers for scalar multiplication and subtraction, alongside
the existing coefficient and right-zero simplifiers.

## Next step

Future Phase 6 slices can review other `HexPoly/Operations.lean` clusters,
such as multiplication, evaluation, composition, and derivative laws.

## Blockers

None for this slice. The worktree still contains a pre-existing
`.claude/CLAUDE.md` modification outside this issue's scope; it was left
untouched and is not part of this session's commit.
