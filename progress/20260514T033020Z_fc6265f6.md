# 2026-05-14 03:30 UTC — work session fc6265f6 (issue #3932)

## Accomplished

Claimed #3932 and added a proof-friendly reduced-degree surface for N32
quotient-witness work:

- Added `DensePoly.trimTrailingZerosList_length_le` and
  `DensePoly.size_ofCoeffs_le`, showing normalization cannot increase the
  stored coefficient count.
- Added `Berlekamp.degree?_getD_lt_of_size_le` and
  `Berlekamp.checkPowChainLinearIncrementalQuotientWitnessStep_of_entry_size_bounds`,
  letting quotient-witness step proofs discharge reduced-degree premises from
  small size bounds instead of unfolding polynomial degree computation.
- Added N32 step-0 reduced-degree witnesses in `HexGfq/CrossCheck.lean` for
  the two literals that blocked the direct proof attempt.

Verification run:

- `lake build HexBerlekamp.Irreducibility`
- `lake build HexGfq.CrossCheck`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexBerlekamp HexGfq/CrossCheck.lean`

The banned-marker scan reports only the pre-existing N16/N32
`generic_irr` placeholders in `HexGfq/CrossCheck.lean`, which #3932 leaves
out of scope.

During triage, #2803 was found stale/false on the current executable surface:
`factor (X^2 - 1)^2` emits `X+1`, `X-1`, and `X^2-1`, so the claimed
`factor_irreducible_of_nonUnit` theorem cannot hold until factor output is
refined.

## Current frontier

#3932 is complete. The degree side of the N32 quotient-witness step can now
be proved from literal coefficient-array sizes and the known N32 modulus
degree.

## Next step

Claim #3933 to add the sibling reflected coefficient-equality surface, then
assemble the final N32 quotient-witness theorem in the downstream issue.

## Blockers

None for #3932.
