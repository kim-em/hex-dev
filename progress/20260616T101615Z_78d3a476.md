# Progress - 2026-06-16T10:16:15Z (session 78d3a476, work)

## Accomplished

Started from the unclaimed queue.  #7559 raced and was already claimed by
another agent, so this session did not touch it.

Claimed #7555 and found it stale: the current tree already contains the
narrowed `toMonic` lifted-existence producer.  Closed #7555 with concrete
evidence pointing to `toMonicLiftData_represents_lifted_of_modP` and
`henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData_success_descent`.

Claimed #7563 and checked the requested unscaled-support theorem.  The premise
is false for the non-monic `toMonic` coordinate: for
`core = (X + 1) * (2 * X + 1)`, the monic-coordinate factor `X + 2` dilates
back to `X + 1`, while the different unscaled monic factor `X + 1` gives a
different singleton support whose unscaled product is divisible by the same
integer factor.  Commented this counterexample shape on #7563 and skipped it
for replan.

Claimed #7537 and completed the HexMatrix Phase 6 identity-row / Gram cluster
review.  Added `@[simp]` annotations to:

- `Matrix.transpose_one`
- `Matrix.dotProduct_basis_basis`
- `Matrix.gramMatrix_one`
- `Matrix.submatrix_one`

Verification:
- `lake build HexMatrix.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over `HexMatrix/Basic.lean` found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The HexMatrix identity/Gram cluster is documented, Mathlib-free, and now has
the expected simp normalization surface for identity transpose, basis-row dot
products, identity Gram matrices, and identity leading principal submatrices.

The `toMonic` non-monic support path should not try to prove an unscaled
`liftedFactorProductCandidate` support theorem.  Downstream work should use the
recovered-coordinate field or redesign/drop the obsolete unscaled
`support_subset_of_dvd_recombinationCandidate` requirement for the `toMonic`
path.

## Next step

Open the PR for #7537.  A planner should reframe #7563 around removing or
replacing the stale unscaled support field rather than producing it.

## Blockers

None for #7537.  The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md`; this session did not touch or stage it.
