## Accomplished

Claimed #3037 and added the suffix assembled-column match-up helpers in
`HexMatrix/Determinant.lean`.

Added a private array-preserving `vectorLengthCast`, the core
`assembleColumnsSuffix_insertAt_last` lemma, and a determinant-term wrapper
`det_columnTupleMatrix_assembleColumnsSuffix_insertAt_last` for rewriting the
exact `det (columnTupleMatrix source (columnTupleVectorFn ...))` shape used by
the ordered-column suffix induction.

Verified:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`
  returns no matches.

## Current frontier

The assembled-column transport for the suffix step is packaged. The sibling
coefficient-product match-up and the final
`det_columnSumMatrixWithSuffix_eq_sum` induction remain out of scope.

## Next step

Use the new determinant-term wrapper together with the sibling coefficient
lemma to complete the final suffix induction for the ordered-column determinant
expansion.

## Blockers

None.
