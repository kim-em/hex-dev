# Issue #4199

## Accomplished

- Claimed feature issue #4199 after #4212 was marked for replan due to a stale theorem shape.
- Added `reassemblyExpansionComplete` to name the condition that `reassemblePolynomialFactors` takes the expanded branch rather than the non-decomposed repeated-part fallback.
- Added `reassemblePolynomialFactors_mem_xPower_or_core_of_expansionComplete`, splitting complete-expansion reassembly membership into extracted `X`-power factors or supplied core factors.
- Added exhaustive-branch wrappers for `factorWithBound` and public default `factor` entries:
  - `factorWithBound_entry_mem_exhaustive_branch_xPower_or_core_of_reassemblyComplete`
  - `factor_entry_mem_exhaustive_branch_xPower_or_core_of_reassemblyComplete`
- Verified:
  - `lake build HexBerlekampZassenhaus`
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`

## Current frontier

- The residual repeated-part fallback is now excluded from exhaustive-branch `factorWithBound` / default `factor` branch analysis whenever the caller supplies the expansion-complete side condition.
- The remaining mathematical work for #4170 is to prove that side condition for the relevant default branch outputs, then combine this shape theorem with irreducibility of `X` and exhaustive core factors.

## Next step

- Prove `reassemblyExpansionComplete` for the default exhaustive branch from the product/coverage facts for `exhaustiveCoreFactorsWithBound`.

## Blockers

- None for the API slice completed here.
