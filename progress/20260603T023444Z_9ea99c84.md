# 20260603T023444Z work #6427

## Accomplished

- Claimed issue #6427 after both directive issues were unavailable to this
  session (`#2564` dependency-blocked, `#2567` needing replan).
- Updated `HexBerlekampZassenhausMathlib/Recovery.lean` so the final HO-4
  assembly-facing comments describe a conditional, prime-search-safe success
  theorem for the current finite-prime-search executable API.
- Added
  `BHKS.factorFast_terminates_of_factorFastCapSeparationInputsAndCanonicalRecoveryTailInputs`,
  a conditional final target proving `Hex.factorFast f ≠ none` from the
  packaged cap-separation inputs and canonical recovery tail inputs.  The
  required
  `Hex.choosePrimeData? (Hex.normalizeForFactor f).squareFreeCore = some primeData`
  witness is carried by `capInputs.choose_eq`.

## Current frontier

The false hypothesis-free `factorFast_terminates` target has an explicit
conditional replacement for the current executable API.  Remaining D1 work can
continue producing the closed `FactorFastCapSeparationInputs` and
`CanonicalRecoveryTailInputs` packages that feed this theorem.

## Next step

Work the remaining D1 producer issues, especially the cap-separation and
canonical recovery tail providers, then close final assembly by applying the
new conditional theorem.

## Blockers

- No code blocker for this slice.
- The unconditional `∀ f, Hex.factorFast f ≠ none` theorem remains false unless
  prime selection changes to an unbounded or otherwise complete chooser.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over `Recovery.lean` found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
