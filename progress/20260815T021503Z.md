# Local #9233 exact-branch preparation

## Accomplished

- Replayed the #9233 exact branch-proof canary and its prior reconciliation
  onto exact prepared #9232 head
  `670daf3c1396635ae7f03ab56512104d575380c7`.
- Preserved the current conformance registration list and added only the
  isolated `HexIntervalMathlib.ExactBranchConformance` root. Refreshed the
  narrow exact-blob proof-only freshness exemption for the combined Lake file.
- Confirmed the conformance source is byte-for-byte identical to the previously
  audited repaired #9233 version.
- Audited the retained-tree and proof linkage. The root replays the exact live
  backward contraction before using the package-owned coverage theorem. The
  left child is seeded with `[1/4,1/2]` and replays the distance rule; the right
  child is seeded with `(1/2,3/4]`, replays exact empty from its guard, and
  closes through a checked refutation schema rather than runtime contradiction.
- Confirmed `replaySplit` consumes both conditional child proofs under the two
  exact branch assumptions. Pointwise coverage selects the matching proof, and
  both live child traces reject replacement of their side fact by the sibling's
  fact. The final ordinary theorem exercises the contraction, split, child
  replay, refutation, and join and has only the permitted standard axioms.
- Built `HexIntervalMathlib.ExactBranchConformance` and rebuilt arithmetic,
  centered, large-cosine, precision-log, and full PNT conformance targets.
- Passed file, copyright, DAG, trust-surface, conformance-target, PNT inventory,
  factor-freshness, release-manifest, manual-split, release-sync, Phase 4, and
  Mathlib-free bench checks. The explicit banned-mechanism scan is clean.

## Current frontier

- The local #9233 candidate is clean on top of the prepared #9232 boundary.
  Existing arithmetic, PNT, log, and cosine sources and registrations remain
  unchanged.

## Next step

- After the preceding stack edges land, rebase this feature boundary onto its
  final parent, refresh the exact Lake exemption, then push for exact-head
  review and CI.

## Blockers

- None. Remote PR and CI state were intentionally left untouched.
