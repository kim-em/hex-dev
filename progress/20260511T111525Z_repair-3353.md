# 2026-05-11 11:15 UTC — repair PR #3353

## Accomplished

Claimed repair PR #3353 and reproduced the failing conformance check locally:
`HexBerlekampZassenhausMathlib.Recovery` failed because the proof of
`bhksRecover_eq_some_of_recovery` unfolded `Hex.bhksRecover?` through an
implementation detail that is now hidden behind the private BHKS recovery
classifier.

Added the public proof-facing lemma
`Hex.bhksRecover?_eq_some_of_checks` in `HexBerlekampZassenhaus/Basic.lean`,
which derives `bhksRecover? f d = some candidates` from the executable guard
facts without exposing the private classifier. Updated
`HexBerlekampZassenhausMathlib/Recovery.lean` to use that lemma.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build`
- `lake build $(python3 scripts/conformance_targets.py --space-separated) hexpoly_emit_fixtures hexpolyfp_emit_fixtures hexberlekamp_emit_fixtures hexmatrix_emit_fixtures hexbz_emit_fixtures hexpolyz_emit_fixtures hexgf2_emit_fixtures hexgfq_emit_fixtures hexgfqring_emit_fixtures hexgfqfield_emit_fixtures hexgramschmidt_emit_fixtures hexhensel_emit_fixtures hexconway_emit_fixtures hexlll_emit_fixtures`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

PR #3353 has a local repair for the failed conformance job. The remote checks
still show the old failed `conformance` result until the repaired branch is
pushed and CI reruns.

## Next step

Commit the repair, force-push the PR branch with lease, and mark PR #3353
salvaged.

## Blockers

None.
