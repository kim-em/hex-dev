# Strassen-Winograd multiplication: SPEC section implemented end to end

## Accomplished

Orchestrated the full implementation of the `hex-matrix` SPEC section
"Strassen-Winograd multiplication" (#8665), in three dependency-ordered waves
of directive issues, each executed by a dedicated agent in its own seeded
worktree, each PR reviewed in-session plus a Codex second opinion, and merged
only on green CI:

- Wave 1 (#8672, #8673, #8674): the three named correctness lemmas — the
  Winograd seven-product identity (with matrix distributivity), the padding
  lemma, and the block-decomposition of `mul` (with `fromBlocks`/`toBlocks`
  and `Vector.dotProduct_append`).
- Wave 2 (#8678): `StrassenConfig`/`Valid`/`strassenDefault`/`mulStrassen`
  (well-founded on `n + m + k`, per-level padding to `(h+h) × (w+w)` so no
  dimension casts arise) and `mulStrassen_eq_mul` via `fun_induction`,
  composing exactly the wave-1 lemmas. `Valid` is `@[expose]`d for
  downstream validity proofs.
- Wave 3 (#8682, #8685, #8683 + #8686): conformance guards (default-config
  guard dimension derived from the cutoff, so re-measurement cannot degrade
  it); the bench driver with the exact `7 ^ Nat.log2 n` declared model,
  the measured `strassenDefault.cutoff = 96` (chungus2, row-of-rows
  backing), the committed scaling figure (fitted slopes 3.01 vs 2.94,
  1.28× at n = 1024) and `libraries.yml` phase4 metadata; and the
  demonstration-config work, split per its own too-large clause into the
  verified full-range Barrett reduce + two-word accumulator and then the
  periodic-reduction kernel `strassenBarrett` with its `Valid` proof —
  measured honestly at 7–10× slower than the default base kernel, so the
  trivial `strassenDemo` ships as the demonstration per SPEC honesty
  constraint (b).

Review rounds produced real fixes beyond the agents' work: `@[expose]` on
`StrassenConfig.Valid`, the cutoff-derived conformance guard, tightened
cutoff/crossover claims in the bench prose (96 is the leaf-64-class
representative, not the sweep minimizer; the figure boundary is the
configured cutoff, not a measured intersection), per-registration cost-model
comments for `check_phase4.py`, and a non-erasable checksum sink in the
comparison driver.

## Current frontier

The SPEC section is complete on `main`; whole-graph build, conformance, and
bench-verify are green. Follow-ups filed, none blocking: #8688 (switch hot
ring-typed callers to `mulStrassen`, the SPEC's deferred point 2), #8687
(delayed-kernel constant factors), #8652 (flat backing + view-based
recursion, which will lower and re-measure the crossover).

## Next step

#8688 is the natural next unit of work; it is small, mechanical, and pays
out the asymptotic win to real callers. The seeded worktrees
`hex-dev-issue-{8666,8667,8668,8676,8679,8680,8681,8681-kernel}` under
`~/worktrees/hex-dev/` are merged and disposable, but contain untracked
review-session notes, so they were left for a human sweep.

## Blockers

None.
