# Progress — 2026-06-17 (session fe8f92c8, /once #7851)

## Accomplished

Diagnosed #7851 ("period-reduced carry lemma for fast-BHKS W ⊆ L'") as an
already-satisfied premise and skipped it for replan with concrete evidence
(no code changes — the correct outcome per the "Directives are hypotheses,
not specs" doctrine).

The issue's deliverable — a carry lemma (carry identity + carry bound +
lattice realization → `W ⊆ L'`) feeding the #7675 fast disjunct — is already
landed sorry-free. The three requested steps map one-to-one onto existing
declarations:

- **Carry identity** `∑ h_i = t_j·q_j + (c_j − ∑ low_i)/b` ≡ `hagg` inside
  `BHKS.two_mul_natAbs_sum_psiCut_le` (`CLDColumnBound.lean:920-922`).
- **Carry bound** `2·|carry| ≤ T.card ≤ factorCount = r` (i.e. `|carry| ≤ r/2`),
  conclusion of `two_mul_natAbs_sum_psiCut_le` (`CLDColumnBound.lean:908/952`) —
  strictly tighter than the issue's proposed `(r+1)/2`.
- **Lattice realization → W ⊆ L'**: `tightColumnBound_of_lift` (`:1100`) →
  `trueFactorCLDTightNormBound_of_lift` (`:1206`) →
  `cutProjectionHypotheses_of_trueFactors` (`Lattice.lean:1681`) →
  `trueFactorIndicatorLattice_le_projectedRowSpan` (`:1429`), wired into the
  #7675 consumer `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_lift`
  (`PartitionRefinement.lean:601`).

Key correctness point: `bhksCutRadiusSq4 = 4·factorCount + coeffWidth·factorCount²`
(`Lattice.lean:940`). The landed tight `2·|col| ≤ r` gives `4·col² ≤ r²` →
`4r + n·r²` exactly. The issue's looser `(r+1)/2` would give `n·(r+1)² > n·r²`
and would NOT fit the cut radius — so the existing bound is not just present but
necessarily tighter than what the issue proposed.

The "dead premise" framing (#7805/#7817) was a misreading: `two_mul_natAbs_sum_psiCut_le`
bounds the *aggregate* cut via support-sum hypotheses, not the raw term-by-term
sum; #7817's wraparound guard confirms the aggregate stays small.

## Verification

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound
  HexBerlekampZassenhausMathlib.PartitionRefinement` →
  `Build completed successfully (3777 jobs)`, zero `error:`.
- `grep sorry` over CLDColumnBound.lean / Lattice.lean / PartitionRefinement.lean → none.

## Current frontier

The genuine residual on the fast disjunct is **producing the per-support
`TrueFactorLift` / `TrueFactorLiftSemantics` / separation (`hp`/`hk`/`hsep`)
family** from the executable `factorFastCoreWithBound` first-success recovery.
`tightColumnBound_of_lift` consumes that family as a hypothesis; nothing in-tree
constructs it from concrete data (docstring `PartitionRefinement.lean:596-600`).

## Next step

An updated/replan issue should target the `TrueFactorLift` family producer for
the first successful recovery (the #7805 → #6672 line), not a re-derivation of
the carry bound.

## Blockers

None. Diagnosis complete; #7851 marked `replan` with comment
https://github.com/kim-em/hex/issues/7851#issuecomment-4736495307.
