# Session f39a6df1 — #4736 scaled candidate support direction

## Accomplished

Claimed #4736 and added the scaled-candidate support boundary in
`HexBerlekampZassenhausMathlib/Basic.lean`.

Moved `scaledRecombinationCandidate` earlier so it is available to
`LiftedFactorSubsetPartition`, extended the partition with
`support_subset_of_dvd_scaledRecombinationCandidate`, transported that field
through `liftedFactorSubsetPartition_transport`, and exposed
`representingSubset_subset_of_dvd_scaledRecombinationCandidate_of_primitive_pos_lc_core`
as the primitive positive-leading wrapper for successor issues.

Updated the existing analytic obligation package and constructor docs to cover
the new scaled-support field.  The change reuses the existing localized
analytic obligation; it does not add a new `sorry`, `axiom`, `native_decide`,
`TODO`, or `FIXME`.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Compared the forbidden-marker scan in
  `HexBerlekampZassenhausMathlib/Basic.lean` against `origin/main`; no new
  entries, only line shifts.

## Current frontier

#4736 is complete.  The scaled support API is now available for #4737's scaled
cover-at-min chain.

## Next step

Claim #4737 and build the scaled cover-at-min theorem on top of
`representingSubset_subset_of_dvd_scaledRecombinationCandidate_of_primitive_pos_lc_core`.

## Blockers

None.
