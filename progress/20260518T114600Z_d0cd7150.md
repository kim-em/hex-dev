# Session d0cd7150 — BZMathlib: refresh stale Gap 1/3 framing in IntReductionMod exhaustive-arm umbrella docs (#4989)

## Accomplished

Pure-textual docstring/comment refresh in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` per #4989, addressing
five fragments that predated the #4940 helper-2/3/4 audit and the A2
public-umbrella `_of_bound` capstone (#4982):

1. **Edit 1** — `_aux` docstring Gap 3 addendum (`**#4982:** …`) appended
   immediately after the existing `**#4848:**` addendum, recording that
   the abstract-bound `_of_bound` sibling now provides the abstract-bound
   route for the `hprecision` shim.
2. **Edit 2** — `_aux` docstring Gap 1 addendum (`**#4880 / #4940:** …`)
   appended after the Gap 3 addendum, recording the directive-level
   blocker from the #4940 helper-2/3/4 audit.
3. **Edit 3** — `_aux` closing paragraph rewritten from the original
   "Downstream consumers … must thread the three shim premises until the
   substrate work lands …" to the new "Downstream consumers … route
   through the public umbrella's `_of_bound` sibling at line 4328 …; see
   the **#4880 / #4940** addendum below" form.
4. **Edit 4** — Two wrapper inline comments on the public umbrella
   signature refreshed (Gap 1: directive-level blocked per #4880 / #4940;
   Gap 3: routes via the `_of_bound` sibling at line 4328).
5. **Edit 5** — Public umbrella wrapper docstring lead-in rewritten to
   record the post-#4982 status of Gap 3 (abstract-bound route via the
   `_of_bound` sibling) and the directive-level status of Gap 1.

The issue's example text referenced "line 4284" for the `_of_bound`
sibling; I updated all three line references (Edit 1 addendum, Edit 3
closing paragraph, Edit 4 Gap 3 comment, Edit 5 docstring) to the actual
post-edit line of the theorem definition, which is line 4328.

Diff: +44 / -18 = +26 net lines across one file
(`HexBerlekampZassenhausMathlib/IntReductionMod.lean`), well under the
issue's ≤ 50 line cap. No proof bodies, signatures, or parameter
orderings touched.

## Verification

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod`: same
  18-error baseline as `origin/main` (`789b66c7`). All errors are
  pre-existing in `HexBerlekampZassenhausMathlib/Basic.lean`
  (`:6274`, `:6608`, `:6672`, `:15877`, `:15988`, `:16195`, `:16241`,
  + heartbeat timeouts), none in `IntReductionMod.lean`.
* `python3 scripts/check_dag.py` exits 0.
* `git diff --check` clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
  | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` empty.
* All three issue-specified greps return empty:
  - `git grep -n "until a \`Monic\`-relaxation refactor" …` — empty.
  - `git grep -n "abstract-bound refactor at the wrapper's call site" …` — empty.
  - `git grep -n "must thread the three shim premises until the substrate work lands" …` — empty.

## Current frontier

With this PR landed, the `_aux` and public-wrapper docstrings in
`IntReductionMod.lean` reflect the current state of the three shim
premises:

* Gap 2 (`hcomplete`): discharged internally via #4848 (unchanged).
* Gap 3 (`hprecision`): abstract-bound route via `_of_bound` sibling
  (PR #4982).
* Gap 1 (`hcore_monic`): directive-level blocked per #4880 / #4940
  audit.

## Next step

Per the issue's "Out of scope" section: this is doc hygiene only. The
substrate work items themselves (#4880 directive-level Gap 1 resolution;
#4170 / #4819 HO-1 capstone consumers) are tracked separately.

## Blockers

None for this issue.
