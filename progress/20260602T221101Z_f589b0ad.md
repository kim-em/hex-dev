# 20260602T221101Z work #2567

## Accomplished

- Claimed directive #2567 and landed another narrow HO-4 slice for the
  actual-cap analytic-comparison surface.
- Added
  `ExecutableBadVectorWitness.l2norm_product_lt_divisor_of_auxiliary_bound`
  in `HexBerlekampZassenhausMathlib/TerminationBound.lean`: a specialisation
  of `l2norm_product_lt_divisor_of_l2norm_bounds` that discharges the
  input-side l2 bound automatically from
  `inputPolynomial_l2norm_le_coeffL2NormBound`, so callers only have to
  supply the auxiliary l2 bound and the cap arithmetic against
  `coeffL2NormBound W.input`.
- Added
  `factorFastCapLift_l2norm_product_lt_divisor_of_auxiliary_bound` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`: the
  `factorFastCapLiftData`-specialised version of the above wrapper, whose
  conclusion matches the
  `FactorFastCapLiftAnalyticComparison.hadamard_l2norm_lt_divisor` field.

## Current frontier

The actual-cap analytic-comparison path now has a two-input producer surface
(`hauxiliary` + `hstrict`) that lands directly inside
`FactorFastCapLiftAnalyticComparison`'s shape.  The auxiliary-polynomial
l2norm bound (for the corrected `auxiliaryPolynomialWithCorrections` shape
carried by the actual-cap witness) and the strict cap-arithmetic inequality
remain the next analytic inputs.

## Next step

Prove an `auxiliaryPolynomialWithCorrections_l2norm_sq_le` companion to
`BHKS.auxiliaryPolynomial_l2norm_sq_le` so the cap-lift witness's auxiliary
side can supply the `hauxiliary` argument of the new wrapper.  Once the
auxiliary bound and the `BHKSBound.lean` cap estimate are in place,
`FactorFastCapLiftAnalyticComparison` can be produced end-to-end without
restating any analytic clause.

## Blockers

No technical blocker for this slice.  The full `factorFast_terminates`
theorem still requires the remaining corrected-auxiliary l2norm bound and
the strict cap-arithmetic inequality.

## Verification

- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Lean files found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
