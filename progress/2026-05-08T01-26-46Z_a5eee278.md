# Progress — 2026-05-08T01:26 UTC — agent/a5eee278

## Accomplished

Claimed #2631, confirmed the remaining `scaledCoeffs_sizeReduce_pivot`
proof cannot be closed through `coeffs_sizeReduce_pivot` because the latter
requires a nonzero source GS norm. Decomposed #2631 into #2642 (the
foundational determinant/scaled-coefficient row-add bridge) and #2643 (the
final public theorem specialization), linked #2643 as depending on #2642, and
marked #2631 for replan.

Claimed #2642 and added a compiled `HexMatrix.Determinant` column-replacement
linearity surface: `Matrix.colReplace`, `Matrix.colReplace_get`,
`Matrix.det_colReplace_add`, and `Matrix.det_colReplace_smul`.

## Current frontier

The determinant-column replacement lemmas compile and are ready for the
Gram-Schmidt bridge. The remaining #2642 work is to connect
`GramSchmidt.scaledCoeffMatrix` under row-add to `Matrix.colReplace` and then
normalize the leading-Gram determinant term back to the public `gramDet`
surface.

## Next step

Finish #2642 by proving the `scaledCoeffMatrix`/row-add bridge in
`HexGramSchmidt/Int.lean`, using the new `Matrix.det_colReplace_*` lemmas.
After that, #2643 should be a short specialization with `c = -r` in
`HexGramSchmidt/Update.lean`.

## Blockers

No external blocker. The local proof obligation still needs a careful bridge
between the determinant of the leading Gram matrix and the existing
`Int.ofNat (gramDet ...)` API in the degenerate case.
