# 2026-05-14 05:17 UTC — repair session on #3951

## Accomplished

- Claimed PR #3951 for repair and checked out `agent/b07ddb62`.
- Diagnosed the failure as a PR-merge-context Lean breakage in
  `HexBerlekampZassenhaus/Basic.lean`: the branch alone built, but merging
  current `origin/main` exposed stale membership proofs around
  `reassemblePolynomialFactors`.
- Merged `origin/main` into the PR branch.
- Updated the slow exhaustive branch, fast constant branch, fast square-free
  core emit branch, and fast quadratic branch product proofs to split on the
  duplicate-core `reassemblePolynomialFactors` branch before rewriting array
  membership/product goals.
- Verified locally with:
  - `lake build HexBerlekampZassenhaus.Basic`
  - `lake build hexarith_bench hexmatrix_bench hexpoly_bench hexpolyz_bench hexpolyfp_bench hexmodarith_bench hexgramschmidt_bench hexgf2_bench hexgfqring_bench hexgfqfield_bench hexgfq_bench hexlll_bench hexhensel_bench hexberlekamp_bench hexbz_bench hexconway_bench`
  - `lake build $(python3 scripts/conformance_targets.py --space-separated) hexpoly_emit_fixtures hexpolyfp_emit_fixtures hexberlekamp_emit_fixtures hexmatrix_emit_fixtures hexbz_emit_fixtures hexpolyz_emit_fixtures hexgf2_emit_fixtures hexgfq_emit_fixtures hexgfqring_emit_fixtures hexgfqfield_emit_fixtures hexgramschmidt_emit_fixtures hexhensel_emit_fixtures hexconway_emit_fixtures hexlll_emit_fixtures`

## Current frontier

- PR #3951 is locally green after merging current `main` and repairing the
  affected proof obligations.

## Next step

- Commit the repair, push `agent/b07ddb62`, and mark PR #3951 salvaged.

## Blockers

- None.
