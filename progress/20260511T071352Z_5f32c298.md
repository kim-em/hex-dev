# 2026-05-11 07:13 UTC — work/feature session (5f32c298)

## Accomplished

- Confirmed the open human-oversight directives were blocked by dependencies, then claimed #3273.
- Added `exists_adjacent_descent` in `HexMatrix/Determinant.lean`, proving that a full nodup permutation vector with positive inversion count has an adjacent descent.
- Added `perm_eq_identity_of_inversionCount_zero`, proving that a permutation vector with zero inversions is the identity vector.
- Both lemmas reuse the existing `Fin.last` peeling/insertion infrastructure and keep executable definitions unchanged.

## Current frontier

- `lake build HexMatrix.Determinant` passes.
- `lake build HexMatrix` passes, with only the pre-existing `HexMatrix/Bareiss.lean` sorry warning.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean` reports no matches.

## Next step

- #3274 can use these two lemmas plus the previously landed adjacent-swap helpers to prove `detSign_composePermutationValues`.

## Blockers

- None for this issue.
