# HexPolyZMathlib Schmeisser source wrapper

Session: `5a2a4f3b` (work, issue #5605)

## Accomplished

Claimed #5605 after blocked directive issues could not be claimed.

Added `Polynomial.rootsRadiusProduct_le_of_schmeisserComposition_of_source`
to `HexPolyZMathlib/SchurSzego.lean`, giving the arbitrary-radius
coefficient-form wrapper around the missing de Bruijn-Springer /
Schmeisser source inequality.

Verified that the touched module and full `HexPolyZMathlib` build pass, and
that the Schur endpoint counterexample oracle still reports the expected
failure of the ruled-out route.

Created successor issue #5610 for the remaining analytic source theorem.

## Current frontier

The local Schur-Szego API now has both radius-one and arbitrary-radius
wrappers that preserve `Polynomial.roots` multiplicities once the analytic
source inequality is supplied.

The actual de Bruijn-Springer / Schmeisser Lemma 9 proof is still absent from
Mathlib and this repository under the obvious search terms.

## Next step

Prove the source theorem requested in #5610:
`rootsRadiusProduct r (schmeisserComposition n f g).roots <=
rootsRadiusProduct r f.roots` for every positive radius when all roots of `g`
lie in the closed unit disk.

## Blockers

No blocker for the wrapper landed here. Completing #5605 in full is blocked
on the hard analytic de Bruijn-Springer / Schmeisser formalization now tracked
by #5610.

Verification completed:

- `lake build HexPolyZMathlib.SchurSzego`
- `lake build HexPolyZMathlib`
- `python3 scripts/oracle/mahler_schur_counterexample.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexPolyZMathlib/SchurSzego.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returned no matches
