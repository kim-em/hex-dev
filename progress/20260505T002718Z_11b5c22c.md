# HexGF2 inverse bridge partial

## Accomplished

- Claimed #2109, verified it was stale on current `origin/main`, and skipped it:
  the targeted `ratGcdLaws` placeholders are already gone.
- Claimed #2122.
- Proved `xgcd_left_mul_mod_eq_one_of_irreducible_of_nonzero_reduced` in
  `HexGF2/Euclid.lean`.
- Proved `mul_mod_xgcd_left_mod_eq_one_of_irreducible_of_nonzero_reduced` in
  `HexGF2/Euclid.lean`.
- Added the private helper `one_mod_eq_one_of_degree_pos`.
- Verified with `lake build HexGF2.Euclid`, `lake build HexGF2`,
  `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

## Current frontier

#2122 is partially complete. The polynomial xgcd inverse bridge now works both
before and after reducing the xgcd left coefficient modulo the irreducible.

## Next step

Finish `packedReduceWord_clmul_packedInvWord_eq_one`. The missing ingredient is
a clean bridge from the two-word `clmul` result
`ofWords #[(clmul w inv).2, (clmul w inv).1]` to multiplication of
`ofUInt64` polynomials, accounting for normalization of zero and nonzero
single-word arrays.

## Blockers

- `packedReduceWord_clmul_packedInvWord_eq_one` still has its existing `sorry`.
- `lake build HexGF2` also reports a pre-existing `sorry` in
  `HexGF2/Conformance.lean`.
