# 2026-05-15 08:00 UTC — partial work session, issue #3662

## Accomplished

- Claimed directive issue #3662.
- Densified the `HexLLL/Bench.lean` scientific `lll.firstShortVector`
  schedules:
  - `random-bounded`: `#[30, 45, 60, 75, 90, 120, 150, 180]`
  - `harsh-cubic`: `#[15, 20, 25, 30, 35, 40, 45, 50, 55]`
- Added cached fixture refs plus paired Lean/Isabelle fixed norm-squared
  targets for every new in-fill rung, replacing the old random-bounded
  `240` comparator rung with `150` and `180`.
- Added local expected-hash helpers so Lean and Isabelle fixed targets share
  the same expected semantic norm-squared hash at each rung.
- Verified:
  - `lake build HexLLL.Bench`
  - `lake exe hexlll_bench list`
  - `python3 scripts/check_phase4.py`
  - `python3 scripts/check_dag.py`
  - `git diff --check`

## Current frontier

- The benchmark registration surface is ready for the HO-18 scientific run.
- `reports/hex-lll-performance.md` is still the pre-existing report and has
  not been regenerated for the densified ladder.
- Informational `fpLLL via fpylll` still has only the existing bottom-rung
  fixed checksum targets; the gating Isabelle ladder is the part expanded in
  this session.

## Next step

- Run the densified Lean/Isabelle comparator sweep, measure comparator
  overhead, and regenerate `reports/hex-lll-performance.md` with raw and
  overhead-adjusted ratios, trend narrative, gating verdicts, and any
  required audit-found Concerns.
- Decide in the follow-up whether to add matching densified fpylll fixed
  targets or to keep fpylll as bottom-rung informational context.

## Blockers

- None for the registration work. The full report regeneration requires a
  longer benchmark run than this session completed.
