# 2026-05-19T20:48 UTC — Gram determinant Bareiss bridge

## Accomplished

- Claimed and executed #5545.
- Moved the Mathlib-side `gramDetVecEntry_eq_leadingPrefix_bareiss` proof below the singular-prefix zero propagation theorem it needs.
- Replaced the previous circular proof through `gramDetVecEntry_eq_gramDet` with a direct no-pivot case split:
  - the prefix-nonsingular branch reads the no-pivot diagonal and handles later singular steps at or after the target slot;
  - the singular branch uses `leadingPrefix_gram_bareiss_eq_zero_of_singularStep_lt` so both sides reduce to zero.
- Exposed the existing Mathlib-free helper `noPivotLoop_step_monotone` so the bridge proof can show later singular steps do not precede the target slot.

## Current frontier

- The non-circular Mathlib bridge theorem is now available in `HexGramSchmidtMathlib.Int`.
- The private Mathlib-free placeholder in `HexGramSchmidt/Int.lean` remains untouched, as requested, because its remaining proof path depends on bridge-layer determinant facts.

## Next step

- A later API-placement issue can decide whether to add a small Mathlib-free wrapper around the new bridge surface or leave downstream consumers on `HexGramSchmidtMathlib.Int`.

## Blockers

- None for this issue. Verified with `lake build HexGramSchmidtMathlib.Int`, `lake build HexGramSchmidtMathlib`, `lake build HexGramSchmidt`, and `git diff --check`.
