# PNT+ Table 10 coupled logarithmic transition

## Accomplished

- Preserved the exact local Table 10 stack through
  `b7f98f1a0c96b2ab21b4c4da14c7c642881885ff` and pinned the final ten target
  occurrences to `BKLNW_tables.lean:815–816`,
  `BKLNW_table10_rows_20_43.lean`, and `BKLNW_table10_rows_misc.lean` at PNT+
  revision `21998bb6196b56789f72a52656a781a75e134eb0`.
- Added one Mathlib-free two-row package authenticating the exact row order,
  coefficients, ten coordinates, exponential bases, logarithmic endpoint
  `43.75`, and the two tail bounds `3.17e-10` and `2.16e-13`. One bounded
  action produces exactly ten facts.
- Proved `2.3025850924 < Real.log 10 < 2.302585094` from a finite logarithm
  series, then proved the row chronology and both real exponential-tail bounds
  with exact `rpow` identities and a kernel-checked power comparison.
- Reused the shared convexity family to cover both complete intervals and
  exposed exact tuple-membership wrappers for row 43 and row `19 * log 10`.
  Generic schema, chronology, and `ProofFrontend` replay close every fact.
- Added false-target, wrong-coordinate, crossed-row, exponential-base,
  logarithmic-endpoint, and both tail mutations. Every mutation rejects
  without drafts or retries; the false bare row-43 target is separately
  refuted as an ordinary theorem.
- Updated both SPECs and the source-pinned inventory: all 87 target-numeric
  occurrences are accepted after localized PNT+ rewrites, while all 38
  supporting `a₂` sites and surrounding `B_8_exact` dispatch remain pending.
- Registered the modules and exact proof-only Lake exemption. Focused runtime,
  Mathlib, conformance, inventory/source, freshness, copyright, line-count,
  DAG, Phase-4, trust, release-manifest, Mathlib-free bench, diff-hygiene, and
  banned-mechanism checks pass.

## Current frontier

The Table 10 numeric target layer is complete after localized rewrites. The
family itself is still pending on the 38 supporting `a₂` proof sites and the
surrounding `B_8_exact` dispatch; arbitrary row generation and production
performance are also future work.

## Next step

Keep this complete local stack unpushed until the parent selects publication
sequencing. A later landing should rebase the whole Table 10 stack together
and rerun exact-head review and CI.

## Blockers

The attempted aggregate `HexIntervalMathlibExperiment` build filled the shared
filesystem while rebuilding thousands of uncached Mathlib modules and failed
when it could no longer create `.olean` files. The 2.8 GB of cache artifacts
created by that attempt were removed; all focused and source/static gates are
green. This is an environmental aggregate-build limitation, not a failure in
the changed targets.
