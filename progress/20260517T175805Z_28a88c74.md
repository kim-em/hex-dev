# Progress 2026-05-17T17:58Z — assemble repeatedPart factorPower exponent list (#4759)

## Accomplished

- Added
  `HexBerlekampZassenhausMathlib.IntReductionMod.normalizeForFactor_repeatedPart_isFactorPower_polyProduct_of_irreducible_factors_cover`,
  the #4611/#4746 capstone in `Hex.Factorization.factorPower` shape used by the
  downstream `expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition`
  wrapper. Given a covering irreducible decomposition of `squareFreeCore`
  whose entries are normalize-fixed (`Hex.normalizeFactorSign q = q`), the
  theorem produces an exponent list whose `Hex.Factorization.factorPower`
  fold reconstructs `(normalizeForFactor f).repeatedPart`.
- Added a small private helper
  `HexBerlekampZassenhausMathlib.IntReductionMod.List.nodup_of_prod_squarefree`
  noting that a squarefree list product of irreducibles is `Nodup`, used
  inside the capstone to discharge the pairwise-distinctness side condition
  on the transported core-factor list.
- Added a private `zip_map_self` lemma to fold `l.zip (l.map f)` into a
  self-map, used in the polynomial-level reformulation of the exponent
  fold.

## Current frontier

- `HexBerlekampZassenhausMathlib.IntReductionMod` builds; `lake build
  HexBerlekampZassenhausMathlib` is green. No new `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME` introduced.
- The downstream blockers
  (#4747 quadratic discharger, #4627 exhaustive-arm completeness, #4597
  small-mod singleton bridge) should now be unblockable once #4759 closes.

## Next step

- Open PR closing #4759.

## Blockers

- None for this issue. Downstream consumers will pick up the new theorem in
  follow-up issues.
