# Progress — 2026-06-18 (session eec9b7b3, /work)

## Accomplished

Claimed #7856 first as the oldest/highest-priority unclaimed feature. The
premise check failed: #7854 had been re-scoped to centered `RecoveredLift` only,
while the requested unconditional fast raw irreducibility theorem still needs an
actual-success producer for `CutProjectionHypotheses`, candidate-size equality,
and support-partition count. Commented the exact missing theorem shape on #7856
and released it with `coordination skip` for replan.

Claimed and completed #7857. Re-read the HexPoly spec, Phase 6 plan, and both
HexPoly Phase 6 review reports. Audited the closed follow-up clusters in
`HexPoly/Operations.lean`, `HexPoly/Euclid.lean`, `HexPoly/Conformance.lean`,
and the downstream compose surface. Added the small residual public Monic
characterization `monic_iff_leadingCoeff_eq_one`, then bumped
`libraries.yml` so `HexPoly.done_through = 6`.

## Current frontier

`HexPoly` now reports Phase 7 readiness. The larger fast-BHKS path remains
blocked on an actual-success cut/partition producer, with #7876 appearing to be
the next substrate issue in that direction.

## Next step

Open the #7857 PR. For the fast-BHKS path, replan #7856 after a theorem packages
actual fast-core success into the `rawIrreducible_of_cut` certificate set.

## Blockers

None for #7857. #7856 is blocked by missing BHKS actual-success
`hcut`/`hsize`/`hpartition` substrate, documented on the issue.
