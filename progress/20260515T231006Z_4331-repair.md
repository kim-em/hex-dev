# 2026-05-15 23:10 UTC - PR #4331 repair

## Accomplished

Claimed repair PR #4331 (`agent/c8f16208`) after its Ubuntu `build` check
failed in the CI `Bench verify` smoke gate. The PR was mergeable; the failing
log showed all benchmark registrations passed, but the aggregate smoke gate
took 307s against the 300s hard cap.

Rebased the PR branch onto current `origin/main` to pick up the latest merged
state. No conflicts occurred, and the PR's functional diff remained scoped to
`HexBerlekampZassenhausMathlib/Basic.lean` plus the original worker progress
file.

Verification run:

- `lake build` passed.
- Cold local benchmark-budget reproduction passed every benchmark registration
  but exceeded the hard cap because this worktree rebuilt large benchmark
  targets from scratch.
- Warm-cache reproduction of the same CI command passed at 222s total under the
  300s hard cap.
- `python3 scripts/check_dag.py` passed.

## Current frontier

PR #4331 is ready to force-push with the rebase and this repair progress note.
The failing CI symptom was a tight budget overrun, not a benchmark-result
failure.

## Next step

Push `agent/c8f16208` with `--force-with-lease`, then mark PR #4331 salvaged.

## Blockers

None for this repair.
