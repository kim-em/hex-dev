# Progress - 2026-06-17 (session 8574e0bf, work #6771)

## Accomplished

Claimed #6771 after the highest-priority directive #2564 reported as blocked
on claim. Verified #6771's listed dependencies (#7584, #7585, #7594) are closed
and that the forward-route substrate exists.

Added `factorFastCoreDefault_factor_zpolyIrreducible_of_forwardInputs` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`. This specializes the
landed forward-count wrapper
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_forwardInputs` to the
normalized square-free core with the public default coefficient bound
`Hex.ZPoly.defaultFactorCoeffBound f`, leaving the concrete lift precision `k`
and fuel explicit. The proof derives nonzero core from
`squareFreeCore_leadingCoeff_pos_of_ne_zero` and then delegates to the general
forward-input wrapper.

Verification:
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over the touched Lean file found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

This is a partial capstone step, not the full #6771 raw fast-disjunct closure.
The new theorem closes the default-bound direct-core specialization that
downstream fast-branch callers need, but it deliberately does not claim the
scheduled-loop terminal-cap theorem or the raw reassembly lift.

While attempting a stronger raw-reassembly theorem, the shape mismatch became
clear: the existing forward-input wrapper proves a direct
`factorFastCoreWithBound core B primeData k fuel` result whose
`ForwardRecoveryInputs` live at the same `k`. The executable fast dispatcher
starts at `initialHenselPrecision a` and may succeed at a later schedule member.
Pretending the existing theorem discharges terminal-cap inputs for that scheduled
loop would be unsoundly typed.

## Next step

Add the missing scheduled-loop bridge: from a `ForwardRecoveryInputs` package at
a schedule target `target ∈ henselPrecisionSchedule ...` and a recovery equality
at `target`, expose the corresponding successful core factor array for the
outer `factorFastCoreWithBound` loop with the same factor list. Once that bridge
pins the successful array to the terminal/default-cap inputs, lift through
`reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`
using an expansion-complete proof for the fast core factors.

## Blockers

No code blocker for the landed direct-core specialization. The remaining #6771
deliverables require the scheduled-loop bridge described above before the raw
fast-disjunct theorem can be typed honestly.
