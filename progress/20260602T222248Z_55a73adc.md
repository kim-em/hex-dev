# 20260602T222248Z work #2567

## Accomplished

- Claimed directive #2567 for session `55a73adc-0cc6-4534-8259-5fdb796903d9`.
- Added `BHKS.FactorFastCapSeparationInputs` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`, packaging the actual-cap
  cap-separation side of the remaining HO-4 assembly: selected local factor,
  auxiliary polynomial, cap precision inequality, cut constant bounds, cut
  hypotheses, `BadVectorBridgeData`, analytic comparison, prime-choice
  equation, and normalized-core precision equation.
- Added
  `factorFast_ne_none_of_factorFastCapSeparationInputsCanonicalRecoveryTailInputs`,
  composing the new cap-side package with `CanonicalRecoveryTailInputs` so
  the current final assembly surface is two coherent bundles rather than a
  long mixed argument list.

## Current frontier

The public `factorFast_terminates` theorem is still not closed.  The final
call shape is now narrower: callers provide one cap-separation package and one
canonical recovery-tail package for the actual `factorFast` cap lift.

## Next step

Construct closed providers for `FactorFastCapSeparationInputs` and
`CanonicalRecoveryTailInputs`, especially the actual `BadVectorBridgeData`
package, the cap/normalized-core precision equations, and the expected true
factor/Mignotte congruence tail.

## Blockers

No blocker for this packaging slice.  The remaining work is the mathematical
provider construction for the two new/existing packages.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
