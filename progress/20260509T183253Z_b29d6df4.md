# 20260509T183253Z - Issue #2844

## Accomplished

Checked the unclaimed queue and first claimed #2679, then found its prerequisite
#2779 had been worker-decomposed into #2838-#2841 and the required
`det_gramMatrix_nonneg` theorem is still blocked in #2841. Released #2679 for
replan with that stale-dependency reason. Attempted to claim #2834 next, but
lost the claim race and did not work on it.

Claimed #2844 and added
`HexBerlekampZassenhausMathlib/BHKSBound.lean`. The new file packages the
executable `Hex.bhksBound` cap into named SPEC Group-D components:
`bhksDegreeFactor`, `bhksFourPowFactor`, `bhksCoeffNormFactor`,
`bhksLog2Factor`, and `bhksThresholdNatBound`. It proves the executable cap is
definitionally equal to the packaged integer expression and exposes both `Nat`
and `Real` dominance forms for later analytic BHKS threshold work. It also adds
short max-precision bridge lemmas relating the BHKS cap and
`Hex.ZPoly.defaultFactorCoeffBound`.

Imported the new bridge file from `HexBerlekampZassenhausMathlib.lean`.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.BHKSBound`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/BHKSBound.lean HexBerlekampZassenhausMathlib.lean`

## Current frontier

The BHKS cap now has stable named components in the Mathlib bridge layer, but
this is only the arithmetic packaging slice. The later HO-4 work still needs
the bad-vector lower bound, theorem 5.2 specialization, and the final
`factorFast_terminates` theorem.

## Next step

Use the new `bhksBound_eq_thresholdNatBound` and
`bhksThresholdNatBound_real_le_bhksBound` names in the next BHKS termination
support issue, rather than unfolding `Hex.bhksBound` directly.

## Blockers

None for #2844. The worktree still has a pre-existing unrelated dirty
`.claude/CLAUDE.md` change that this session did not touch or commit.
