# 2026-05-07 23:07 UTC - twenty-sixth Phase 4/5 checkpoint (b682ddbc)

## Accomplished

Claimed #2618 and recorded the twenty-sixth Phase 4/5 checkpoint covering
work merged since #2557 / PR #2571 landed at 2026-05-07T15:37:52Z.
Twenty-one PRs merged in that window:

- #2563, #2570, #2573, #2576, #2577, #2578, #2581, #2582, #2588,
  #2589, #2590, #2591, #2595, #2596, #2597, #2601, #2604, #2609,
  #2611, #2615, and #2620.

Quality metrics at this checkpoint:

- `scripts/status.py`: many Mathlib-free libraries remain ready for Phase 5;
  Mathlib bridge libraries are mostly in Phase 4; `HexBerlekampZassenhaus`
  is back at Phase 1 after the van Hoeij CLD reorientation; and
  `HexLLLMathlib`, `HexBerlekampMathlib`, `HexGfqMathlib`, and
  `HexBerlekampZassenhausMathlib` are blocked on upstream Phase 4 completions.
- Source scan: 233 Lean `sorry` hits, 0 Lean `axiom` hits, and one textual
  `native_decide` reference in a comment in `HexArith/CrossCheck.lean`.

### Merged work by area

**HexBerlekampZassenhaus / HO-1 BHKS recovery.** The dominant merge wave
rebuilt the fast path around van Hoeij's CLD lattice rather than the older
additive LLL shape:

- #2563 reoriented the SPEC toward the BHKS CLD lattice.
- #2582 rolled back premature BZ readiness so the phase counter matches the
  new HO-1 implementation frontier.
- #2589 audited the executable CLD surface and #2591 audited the bridge proof
  surface.
- #2595 added the BHKS precision bound.
- #2601 split the public slow / fast / fallback API while leaving the fast
  branch conservative.
- #2604 added CLD coefficient, threshold, and cut helpers.
- #2609 added the CLD lattice basis.
- #2611 added the LLL cut and projection surface.
- #2615 added indicator-vector reconstruction and exact-division checking.
- #2620 added RREF-based equivalence-class extraction from projected rows.

The pipeline now has executable stages for bound -> CLD rows -> lattice ->
LLL cut/projection -> RREF equivalence classes -> indicator reconstruction.
The current missing piece is the fixed-precision glue that calls all of those
stages and returns verified factors; #2619 is claimed for that.

**HexGramSchmidt / LLL row-add layer.** The row-add proof chain moved
substantially:

- #2576 proved the private `basisRows` row-add invariance.
- #2573 proved `gramDet_rowAdd_earlier` in the integer layer.

The remaining rational coefficient surface is PR-backed via #2610/#2606, and
the older integer-wrapper issue #2526 had its stale `replan` state removed
because the dependency plan is still valid. #2526 is visible as unclaimed, but
it still depends on #2606 landing.

**HexMatrix / HexMatrixMathlib Bareiss.** The row-pivoted Bareiss chain
advanced but remains proof-frontier work:

- #2570 exposed pivot-search and rowSwap transport helper surfaces.
- #2590 added HexMatrixMathlib determinant bridge benchmark coverage.
- #2596 proved the `HexMatrix.RREF` span-containment equivalence wrapper.

The current chain is #2553/#2623 for logical-source row-swap transport, then
#2554 for the recursive regular-loop invariant, then #2442 for resolving the
orphan core `bareiss_eq_det` theorem surface.

**GF2 / Berlekamp / Phase 4 cleanup.**

- #2597 proved GF2 fixture modulus witnesses.
- #2581 added HexLLLMathlib Phase 3 conformance.
- #2578 completed the HexConway Phase 4 benchmark audit.
- #2577 completed the HexPolyMathlib Phase 4 audit.
- #2588 added HexGfq Phase 4 benchmarks.

These PRs mostly shrink review/conformance gaps rather than changing the
central HO-1 path.

### Open queue shape

Open `agent-plan` issues at the checkpoint:

- #2621 has PR #2622: prove product preservation for
  `HexBerlekamp.berlekampFactor`.
- #2619 is claimed: wire the BHKS fixed-precision recovery helper.
- #2613 has PR #2617: audit HO-2 adversarial BZ fixture cases.
- #2606 has PR #2610: expose rational Gram-Schmidt row-add coefficient
  formulas.
- #2585 is blocked: audit resultant infrastructure for BHKS D1.
- #2554 is blocked on #2553: row-pivoted Bareiss regular-loop invariant.
- #2553 has PR #2623: Bareiss logical-source row-swap transport.
- #2550 has PR #2572: source-row earlier reduction-zero theorem.
- #2544 has PR #2605: `gramDet_sizeReduce` call-site wrapper.
- #2526 is unclaimed but dependency-gated by #2606: integer row-add wrappers.
- #2519 is blocked: public size-reduction theorem block.
- #2442 is blocked on #2554: orphan Bareiss core theorem surface.

Open PRs at the checkpoint were #2623 (`BLOCKED`), #2622 (`CLEAN`), and
#2617/#2610/#2607/#2605/#2572 (`BEHIND`). The five auto-merge PRs called out
by the planner (#2572, #2605, #2607, #2610, #2617) remain open and behind at
this checkpoint rather than merged.

Planner-triaged actions noted for this cycle:

- #2526 had `replan` removed; the plan is still valid once #2606 lands.
- #2572, #2605, #2607, #2610, and #2617 entered the auto-merge lane during
  the cycle, but by this checkpoint they are still open and need main-branch
  catch-up.

## Current frontier

The critical executable frontier is HO-1 BHKS: #2619 should produce the
fixed-precision recovery helper over the now-merged CLD lattice, LLL
projection, RREF equivalence classes, and indicator reconstruction stages.
After that, the next feature issue should wire `factorFastCoreWithBound` to
call the helper and decide precision-loop failure modes.

The proof frontier is split between:

- GramSchmidt size-reduction: #2606/#2610 -> #2526 -> #2519, with #2544/#2605
  also waiting for its PR to catch up.
- Bareiss row-pivot correctness: #2553/#2623 -> #2554 -> #2442.

## Next step

Let the PR-backed dependencies land or be repaired first. Fresh workers should
prioritize #2619 if it becomes available again, then #2526 only after #2606
merges. Repair sessions should handle the behind PRs rather than new workers
duplicating those branches.

## Blockers

No blocker for the checkpoint itself. The worktree still contains the
pre-existing pod-managed `.claude/CLAUDE.md` modification, which this session
left untouched.
