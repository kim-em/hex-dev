# Session 20260602T043547Z (agent/583c716d)

Issue: #6031 — HexMatrix: instantiate Gram cofactor Plucker from consecutive-top basis cases.

## Accomplished

- Claimed #6031 after confirming the directive issues were blocked.
- Added `Matrix.det_plucker_three_term_consecutive_top` in
  `HexMatrix/Determinant.lean`.
- The theorem packages the Mathlib-free vector-column Plucker identity for
  the Gram/Bareiss row pattern `alpha < k < k+1` in `Fin (k + 2)`.
- Reused the closed #6030 basis-vector cases for `q < alpha`,
  `alpha < q < k`, and the three equality cases, avoiding any Mathlib or
  bridge-layer import.
- Verified:
  - `lake build HexMatrix`
  - full `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - forbidden-token diff grep over `HexMatrix/Determinant.lean`

## Current frontier

The consecutive-top Plucker identity is now available as a named Hex-local
theorem over the existing `mDet`/`nDet` substrate. The surrounding structural
minor-recognition helpers for `borderedMinor`, `bareissDesnanotIndex`, and
`leadingPrefix` remain unchanged and can be composed by the Gram/Bareiss
consumer.

## Next step

Open a PR closing #6031.

## Blockers

None.
