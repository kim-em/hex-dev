# Accomplished

- Put the iterated-quadratic-norm irreducibility certificate on the production
  singleton-irreducibility path, gated on the modular support width, and proved
  it sound end to end. `Hex.ZPoly.factorize` now answers a certified row with
  the same `Factorization` any other singleton proof produces.
- Moved the closed-form radicand search into production as
  `Hex.QuadraticNormCertificate.recover?` / `certify?`, in a new Mathlib-free
  module whose only claim on the rest of the system is
  `check_of_certify?`. Retired the bench prototype and its `HexQuadraticNormProbe`
  library; the probe now times the production definitions.
- Closed the composition obligation the two dependency issues left open:
  `signPatternPoly_ofFn` identifies #9168's fold/list sign-pattern product with
  #9169's `Fin n -> Bool` `Finset.prod`, which is what lets
  `irreducible_int_of_map_eq_signPoly` discharge the executable check.
  `irreducible_of_check` and `irreducible_of_quadraticNormCertified` follow, and
  they discharge the new arm of `factorClassicalFactors_factor_irreducible`.
- Kept both conformance arms honest. `adv/swinnerton_dyer_sd6` now has to be
  answered by the certificate, and a new `adv/multiquadratic_nonnorm_64` --
  the minimal polynomial of `sqrt2+sqrt3+sqrt5+sqrt7+sqrt11+sqrt13+sqrt6`, whose
  last sign is determined by the first two, so it generates the same field but is
  *not* an iterated quadratic norm -- has to be refused by the certificate and
  answered by the lattice.
- Verified: full `lake build` (9,754 jobs), `HexConformance`, `HexReleaseTests`,
  all 22 oracles green, fixtures re-emitted byte for byte, trace gate green,
  copyright/line-count/DAG/manifest/phase-4 lints, and a clean trust surface
  across 421 files.

# Current frontier

The measurement half. Two full corpus sweeps at the integrated revision, one
with the width floor closed and one open, so the before/after attributes to this
change and not to unrelated drift on `main`. Then the paired certificate probe,
the off-corpus witnesses, regenerated cactus and runtime-by-degree figures, the
rank table around the Hex/Isabelle elbow, and the report rewrite.

# Next step

Finish the measurements, rewrite
`reports/hexbz-quadratic-norm-certificate.md` around them, open the PR, and
report the measured cumulative ratio over ranks 125-140 back on #9126 against
its 0.85x criterion.

# Blockers

None.
