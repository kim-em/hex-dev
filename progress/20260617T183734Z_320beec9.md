# Progress — 2026-06-17 (session 320beec9, /work → /feature #7800)

## Accomplished

### #7800 — repaired the HexBerlekamp FLINT comparator eligibility

Claimed #7800 after #7802 was lost to a claim race (session `b3648b20`
won it; coordination returned `CLAIM FAILED`).

**Deliverable 1 (timing-shape fix).** Root-caused the no-eligible-rung
blocker: the fixed FLINT registrations measured one persistent-driver
call per child, and the child auto-tuner's `minTotalSeconds` floor
(default `1 ms`) was already cleared by the `~52 ms` driver spawn, so
every FLINT median pinned at the startup floor (`inner_repeats = 1`).
Fix is config-only, using levers the lean-bench framework already
provides: two named configs in `HexBerlekamp/Bench.lean`,
`flintCompareConfig` (`warmupFirstIter := true`, `minTotalSeconds :=
0.2`) on the FLINT targets and `leanCompareConfig` (matching
`minTotalSeconds`) on the paired Lean targets. `warmupFirstIter` spawns
the driver out of the timed region; the raised floor amortises
steady-state work across inner repeats. FLINT medians dropped from
`~52 ms` to `19 µs … 504 µs`.

**Deliverables 2 & 3 (re-measure + honest verdict).** Re-ran both
gating surfaces on `carica` at commit `3ad7817b`, exported JSON
artefacts (hashes recorded in the report), and rewrote the report's
`## Comparator Ratios` and `## Concerns` sections from the derived data.
The eligible range is now `n = 24 … 64` (Rabin) and `n = 24 … 96`
(DDF) rather than empty — but FLINT is `~1700x` / `~2900x` faster than
Lean at the largest eligible rung, so **both gating goals fail**. Left
the Phase-4 concern in place (deliverable 3): this is an
algorithm/constant-factor gap in the executable Lean kernels, not a
harness artefact. Did **not** touch `libraries.yml` (`done_through`
stays 3 — Phase 4 is correctly not re-claimed).

## Verification

- `lake build hexberlekamp_bench` clean (549 jobs).
- `lake exe hexberlekamp_bench list` / `verify` — 46 benchmarks pass.
- `python3 scripts/check_phase4.py` → exit 0 (changed registrations: 0;
  `setup_fixed_benchmark` lines are not matched by the parametric
  `SETUP_RE`, so no derivation-comment obligation).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no `sorry`/`axiom`/`native_decide`/`TODO`/
  `FIXME` on added lines.

## Current frontier

Comparator measurement is now valid and the gating verdict is honest.
The blocker is no longer the harness; it is the Lean Rabin /
distinct-degree kernels themselves being orders of magnitude slower
than FLINT's tuned C.

## Next step

Closing the gating gap is a separate, large effort: either optimise the
executable `runRabinTestChecksum` / `runDistinctDegreeChecksum` kernels
(word-level arithmetic, sparse Frobenius) toward FLINT's constant
factors, or a SPEC PR re-scoping the gating bar. Not in scope for #7800.

## Blockers

- HexBerlekamp Phase 4 remains blocked on the algorithm-speed gap (now
  measured precisely), not on the comparator harness.
