# Session dcd8e0d2 (one-shot, directive #6499)

## Accomplished

Diagnosed directive #6499 ("Redesign BareissGramInitialRegularStepQuotientProvider,
rename, complete migration") as having two unsound premises that warrant
stopping and posting per the directive's own "STOP and post" clause.

- Verified the type-level unsoundness #6496 reported (universal hinv
  quantification permits non-canonical coefficient vectors for which the
  quotient witness does not exist).
- Verified that the directive's claim "Constructs a concrete provider/witness
  using existing Mathlib-bridge infrastructure" is wrong: the bridge layer
  `HexGramSchmidtMathlib/Int.lean` has 3 active sorries at lines 1688
  (`scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular`), 1740
  (`scaledCoeffRows_lower_eq_coeffs`), and 1752 (`scaledCoeffs_eq`). The first
  is exactly the link a sorry-free OfGram constructor would need.
- Verified that the residual sorry at `HexGramSchmidt/Int.lean:6039` requires
  new Schur-side recurrence characterizations the theorem's own docstring
  (lines 6011-6027) enumerates as missing.

Posted detailed diagnostic comment to
https://github.com/kim-em/hex/issues/6499#issuecomment-4617251315
covering both blockers with file:line references, a difficulty-layer mapping
of the six bundled deliverables, and a suggested factoring (bridge math
first, then mechanical refactor).

Skipped #6499 via `coordination skip` with the same reasoning, leaving it
claimable for an updated directive (directives bypass replan triage per
project convention).

## Current frontier

#6499 sits open with the diagnostic comment and skip note attached. No
source changes; `agent/dcd8e0d2` has only the inherited working-tree
modifications it started with (the `.claude/CLAUDE.md` Pod-agent stanza
already present at session start, plus the inherited untracked
`.claude/commands/` and `.claude/skills/`).

## Next step

For the project owner: re-issue as a sequenced trio matching the
suggested factoring,

1. Fill `scaledCoeffs_eq_scaledCoeffMatrix_bareiss_of_no_singular` (bridge
   math; unblocks OfGram).
2. Prove `schurSigma`-chain matches the bordered-minor Bareiss determinant
   for diagonal and strict-lower entries, or route through (1) to close
   the residual sorry at `HexGramSchmidt/Int.lean:6039`.
3. With (1) and (2) landed, the Provider redesign + rename + theorem
   migration becomes a structural refactor with no remaining math
   obligations.

The post-#6498 5+-qualifier rename pass is independently valuable and
can ride with (3) without waiting for (1)/(2).

## Blockers

None — exit is clean (comment + skip).
