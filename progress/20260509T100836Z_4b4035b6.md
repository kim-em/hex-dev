# Progress: 2026-05-09T10:08:36Z (session `4b4035b6`)

## Accomplished

- Claimed issue #2784 (HexGfqField Phase-4 schedule widening) and
  worked it through three certificate-modulus rounds.
- Generated certificate-checked irreducible monic polynomials over
  `F_7` at three new degrees and embedded them in
  `HexGfqField/Bench.lean`:
  - `m_p7_n8 := x^8 + x + 3`,
  - `m_p7_n12 := x^12 + x^2 + x + 2`,
  - `m_p7_n16 := x^16 + 2x + 3`.
  Each carries a Berlekamp Rabin `IrreducibilityCertificate`
  verified through `checkIrreducibilityCertificateLinearIncremental`
  with `decide`-style kernel reduction (heartbeats raised
  per-degree).
- Extended `bundleForN` so each schedule entry maps to a real
  degree-`n` modulus (no degree-2 fallback for `8, 12, 16`).
- Replaced `paramSchedule := .custom #[2, 3, 4, 6]` and
  `paramCeiling := 6` with
  `paramSchedule := .custom #[2, 3, 4, 6, 8, 12, 16]` and
  `paramCeiling := 16` on all eight `setup_benchmark`
  registrations.
- Re-ran `lake exe hexgfqfield_bench run` for every parametric
  registration at commit `e63b09b22fb3` on `carica`. JSONL artefact
  committed at `reports/bench-results/hex-gfq-field-e63b09b22fb3.json`
  (SHA-256
  `0c3edd4be3ce59a5626d2835a4a85bf8c1f63541296bf45639d0f59c6cb3cc4e`).
- `runNegSubChecksum` is now `consistent with declared complexity`
  (β=−0.074, comfortably inside the 0.15 default tolerance) — net
  +1 consistent verdict over the prior `{2, 3, 4, 6}` schedule.
- The other seven verdicts moved in the right direction (six
  registrations have measurably smaller |β| residuals at the
  widened ladder than at the original four-rung ladder; one,
  `runZPowChecksum`, flipped from a four-rung "consistent" verdict
  to "inconclusive" once more high-n rungs revealed the same
  small-n constant-factor signature the others show).
- Refreshed `reports/hex-gfq-field-performance.md`: rewrote
  §Verdicts with the new commit/host/JSONL provenance, added the
  before/after β table, and updated §Concerns to track the residual
  six inconclusive verdicts. `HexGfqField.done_through` stays at
  `3` because §Concerns is non-empty per the Phase-4 exit criteria.
- `python3 scripts/check_dag.py` and `python3 scripts/check_phase4.py`
  both succeed.
- `lake exe hexgfqfield_bench list` and `verify` both pass; verify
  reports all 8 benchmarks ok.

## Current frontier

- HexGfqField-Bench compile time at the wider ladder is ~10 minutes
  on `carica` (degree-16 certificate check dominates at
  `maxHeartbeats 1500000000`, `maxRecDepth 524288`). Degree-24 was
  investigated but its certificate check pushes well past that and
  is not practical for routine CI.
- The C-column on every inconclusive registration shows the same
  shape: high C at `n=3, 4`, then a stable plateau from `n=6`
  onwards. The negative β is dominated by those low-n rungs sitting
  above the plateau. Default `verdictWarmupFraction := 0.2` only
  drops one rung; raising it would launder the verdict per the
  SPEC.

## Next step

- File a follow-on issue (or let a planner triage the parent
  `#2784` `replan`) deciding between two paths for the residual
  six inconclusive registrations:
  1. Push the ladder higher than `n=16` (degree-24+). This needs a
     materially faster certificate-check path (e.g. a `simp`-only
     route or a dedicated `decide`-friendly kernel-reducible
     evaluator) before it can land in CI within budget.
  2. File per-registration finding-issues against the implementation
     under test. The current data argues for case (1) being the
     more honest reading because the asymptotic plateau is visible
     from `n=6` onwards.
- HexGfqField cannot reach `done_through: 4` until §Concerns is
  empty.

## Blockers

- None for this session — the partial PR is publishable. The
  remaining work depends on a strategic decision about where the
  per-registration constant-factor signal stops being miscalibration
  and starts being a tracked finding.
