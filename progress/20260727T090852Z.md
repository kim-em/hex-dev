# Interval centered vertical post-merge evidence

## Accomplished

- Merged current `origin/main` into the centered-interval branch, resolving the
  single CI target-list conflict by retaining both the interval replay targets
  and main's new LLL Gram benchmark.
- Rebuilt the combined repository successfully (`lake build`, 9,468 jobs).
- Reran the complete five-sample experiment from clean merge commit
  `4123772f`, because `lakefile.lean` is a hashed measurement input, and
  synchronized the prose report with the new raw timings.
- Rechecked schema-v3 provenance, explicit/null axiom reporting, source-hash
  count, structural benchmark lint, DAG validity, and diff cleanliness.

## Current frontier

The post-merge evidence is ready to commit and push. Draft PR #8929 then needs
an updated body, the independent-review disposition comment, and its new GitHub
Actions run inspected before it is marked ready and merged.

## Next step

Commit and push the regenerated evidence, update the PR metadata, monitor the
actual Actions jobs, merge when green, then branch the next interval scaling
vertical from updated main.

## Blockers

None.
