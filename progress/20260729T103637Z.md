# Accomplished

- Merged the proof-payload arena onto the engine-owned structural matcher
  scheduler while preserving dynamic arbitrary scopes, matcher cursor
  scheduling, and package-owned scope vetoes.
- Resolved the two direct merge conflicts by combining planned registry
  outcomes with scoped dispatch validation and retaining both matcher and
  payload Lake targets.
- Fixed payload traversal so a nonempty `InstantiationRequest.scopes` list
  survives both payload-use collection and relocation instead of being
  replaced by the structure field's default empty list.
- Made the public/private matcher boundary executable in conformance: frozen
  entries retain the exact action structural inputs and matcher epoch, while
  no matcher cursor enters the payload protocol.
- Migrated the scope and matcher package conformance drivers to the explicit
  planned/dropping-drafts registry APIs.
- Rebuilt the complete 29-job interval, matcher, scope, registry, dyadic, and
  payload target set successfully; `scripts/check_dag.py`, diff hygiene, and
  added-line trust scans also pass.

# Current frontier

The matcher scheduler and immutable payload arena now compose without an
engine-private cursor crossing the registry or replay boundary. The next
session layer can freeze a planned outcome and submit its relocated outcome as
one atomic transaction. It must continue to distinguish package-local draft
labels from frozen arena identifiers; the present verbatim action copy is
valid specifically because `Action` contains no package-produced
`PayloadId`.

# Next step

Restack the checked payload-session layer on this integration head, preserve
the complete matcher action during semantic replay, and add rollback canaries
for freeze-success followed by reply rejection or engine resource failure.

# Blockers

None.
