# Progress: HexArith wide-word automation smoke checks (#6291)

## Accomplished

Claimed #6291 and added equation-style automation-facing projection lemmas in
`HexArith/UInt64/Wide.lean`:

- `UInt64.toNat_addCarry_proj`
- `UInt64.toNat_subBorrow_proj`

Added proof-mode conformance examples in `HexArith/Conformance.lean` for:

- `UInt64.mulFull` low-word-first reconstruction;
- `UInt64.addCarry` exact-sum reconstruction;
- `UInt64.subBorrow` wrapped-subtraction reconstruction.

Verification:

- `lake build HexArith.UInt64.Wide`
- `lake build HexArith.Conformance`
- `lake build HexArith`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over touched files

## Current frontier

#6291 is complete and ready for PR creation. The changes are scoped to
automation annotations/conformance examples and do not alter executable
definitions or FFI wiring.

## Next step

Open a PR for #6291.

## Blockers

None for #6291. The worktree still contains the unrelated pre-existing local
modification to `.claude/CLAUDE.md`, which was not touched or staged.
