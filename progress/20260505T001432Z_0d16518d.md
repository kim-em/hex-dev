# HexMatrix duplicate-border column determinant

## Accomplished

- Claimed feature issue #2119 after human-oversight issue #1997 was rejected by coordination as needing replan.
- Added value-swap permutation helpers in `HexMatrix/Determinant.lean` to pair Leibniz determinant terms for duplicate columns.
- Proved `det_eq_zero_of_col_eq`.
- Proved `det_borderedMinor_succ_eq_zero_of_col_duplicate` for the successor bordered-minor case where the new border column duplicates the previous boundary column.

## Current frontier

The row-duplicate and column-duplicate bordered-minor zero lemmas are both available for successor minors. This closes the duplicate-border column case split from #2112/#2119 and leaves the nonduplicate term reindexing/product work to the existing blocked successor issues.

## Next step

Use the row and column duplicate-zero lemmas to discharge duplicate cases when assembling the successor off-diagonal residual theorem after the nonduplicate term partition work lands.

## Blockers

- `lake build HexMatrix` succeeds but still reports pre-existing `sorry` warnings in `HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`.
- The full nonduplicate successor off-diagonal product identity remains out of scope for this issue.
