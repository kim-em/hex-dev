# 2026-05-21T02:51Z — issue #5693

## Accomplished

Claimed #5693 and added a private regular-step packaging lemma for
`BareissGramRowInvariant` in `HexGramSchmidt/Int.lean`. The new slice exposes
chosen row-combination coefficients from the current invariant, builds the
fraction-free Bareiss-updated coefficient vector for active trailing rows,
proves the support bound for that updated vector, and packages preservation of
the invariant for the next no-pivot state under an explicit exact-division entry
relation suitable for the later loop theorem.

Verified:

- `lake build HexGramSchmidt.Int`
- `git diff --check`
- `git diff -- HexGramSchmidt/Int.lean | rg -n -e '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

The one-step invariant package is available locally. The remaining exact
division/dot-product relation is still an explicit hypothesis for the future
recursive no-pivot preservation issue.

## Next step

Use `bareissGramRowInvariant_regular_step` from the dependent no-pivot loop
issue to package recursive `Matrix.noPivotLoop` preservation, discharging the
entry relation from Bareiss exact-division facts.

## Blockers

None for this slice.
