# HexArith Phase 6 grind coverage for Barrett/Reduce.lean (#8037)

## Accomplished

Promoted the three equation-shaped public `@[simp]` characterising
lemmas in `HexArith/Barrett/Reduce.lean` to `@[simp, grind =]`,
continuing the Phase 6 grind automation sweep:

- `BarrettCtx.toNat_pinv` — `ctx.pinv.toNat = barrettRadix / p.toNat`
  (unconditional literal equation).
- `toNat_barrettReduce_eq_mod` — `(barrettReduce ctx T).toNat =
  T.toNat % p.toNat`, conditional on `hT : T.toNat < p.toNat * p.toNat`.
- `barrettReduce_eq_self_of_lt` — `barrettReduce ctx T = T`, conditional
  on `hT : T < p`.

All three re-read at the cited line numbers (46, 140, 163). The two
conditional rewrites are accepted by `grind =`, which discharges the
side hypothesis as a subgoal. The two `barrettReduce` rewrites have
distinct LHS head patterns (`(barrettReduce …).toNat` vs
`barrettReduce …`) and reduce to `barrettReduce`-free RHSs, so no cycle.
The other public lemmas in the file (`barrettReduce_lt`, an inequality;
`barrettReduce_eq`, untagged) are left as-is.

## Current frontier

`lake build HexArith` green (`HexArith.Barrett.Reduce` 933ms — no
grind-loop slowdown). Full library builds clean (61 jobs).
`scripts/check_dag.py` exit 0. `git diff --check` clean; no added-line
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`. Annotation-only; no
proof bodies changed, no new imports, no `*Mathlib` file touched.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps at session start:
#8034 (HexModArith/HotLoop.lean), #8035 (HexArith/Nat/Prime.lean).

## Blockers

None.
