# Complete cross-history replay driver

## Accomplished

- Added a function-agnostic trace driver which folds the engine's authoritative
  fact/instance chronology and consumes both detailed arrays exactly once in
  append order.
- Bound equality transport to the exact retained edge and made equality
  availability advance only through each instance's explicit, contiguous
  `newEqualities` suffix, with owner, generation, endpoint, and domain checks.
- Accumulated proof-indexed program cursors and semantic stability through
  every instance, checked the final program/version and all array counts, and
  exposed target closure as `Evidence` over the caller's exact initial facts.
- Added arbitrary-function conformance for successful rule replay, successful
  post-instance equality transport, and fail-closed ordering, omission,
  duplication, ownership, and final-snapshot mutations.
- Built `HexInterval.Experiment.TraceReplay` and
  `HexInterval.TraceReplayConformance` successfully.

## Current frontier

The actual package-owned sine-shaped session now replays from caller input to
proof-bearing evidence through one authoritative interleaving. The separate
transparent proof-emitter canary supplies the ordinary downstream theorem;
the generic driver itself contains no operation or function cases.

## Next step

Review the trace API together with the proof-emission frontend, then exercise
it with the nontrivial real-sine target, multiple arbitrary propagators, reused
equalities, and several interleaved fact events.

## Blockers

None.
