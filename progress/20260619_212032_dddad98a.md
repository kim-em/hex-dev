# HexModArithMathlib Phase 6 grind coverage (#8106)

## Accomplished

Promoted the equation-shaped public `@[simp]` lemmas in
`HexModArithMathlib/Basic.lean` to `@[simp, grind =]`, matching the
ongoing Phase 6 grind-coverage sweep:

`ofZMod_toZMod`, `toZMod_ofZMod`, `ofZMod_zero`, `toZMod_zero`,
`toZMod_one`, `ofZMod_one`, `toZMod_add`, `toZMod_neg`, `toZMod_sub`,
`toZMod_mul`, `toZMod_natCast`, `toZMod_intCast`, `toZMod_pow`,
`equiv_apply`, `equiv_symm_apply`.

`val_toZMod` was already `@[simp, grind =]` (left untouched, as the issue
instructed).

**Count note:** the issue body said "17 public `@[simp]` lemmas, of which
exactly one is already `grind =`" implying 16 to promote. The file
actually carries **16** `@[simp]` lemmas total (1 already promoted, 15
remaining), so 15 were promoted, not 16. All 15 confirmed to be literal
`lhs = rhs` forms on re-reading at the point of promotion: round-trips
(`ofZMod`/`toZMod` mutual inverse), identity/constant transport
(`zero`/`one`), ring-homomorphism pushdowns (`add`/`neg`/`sub`/`mul`/`pow`,
`natCast`/`intCast`), and the two `equiv` apply/symm unfoldings. None are
`↔`/`<`/`≤`/conjunction/`let`-wrapped, and all LHS heads are plain def
heads (`ofZMod`, `toZMod`, `equiv`, `equiv.symm`) — no `dite`/`getD`
pattern traps. No exclusions. No `private` `@[simp]` lemmas in the file.

The `equiv_apply` → `toZMod` cascade noted in the issue is one-directional
(rewrites `equiv a` to `toZMod a`, after which the `toZMod_*` pushdowns
apply), not a loop, so promoting both is safe — confirmed by the build.

## Current frontier

`lake build HexModArithMathlib` green; `HexModArithMathlib.Basic` 2.1s —
no grind-loop slowdown, zero new errors/warnings/`Try this` info. The
CI-gated `lake build HexBerlekampZassenhausMathlib` (the merge-gating
bridge) builds clean (`Build completed successfully`, 3790 jobs); its only
warning, `Recovery.lean:1874 unused variable`, is pre-existing and
untouched by this annotation-only change. `scripts/check_dag.py` exit 0.
Annotation-only: no proof bodies, signatures, or imports changed; no new
`sorry`/`axiom`/`native_decide`.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps at session start:
#8102 (HexBerlekampMathlib/Basic.lean), #8104
(HexMatrixMathlib/RankSpanNullspace.lean).

## Blockers

None.
