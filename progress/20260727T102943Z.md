# Interval scale review dispositions

## Accomplished

- Re-ran the source-matched scale sweep after moving list-derived limit
  construction outside the timed checker loops and asserting both parity
  variants before timing.
- Validated the replacement record's commit, clean input tree, 105 case
  counters and calibrations, 525 compiled samples, 21 forced-kernel points,
  unchanged source hashes, conformance provenance, and ordinary theorem axiom
  sets of exactly `[propext, Quot.sound]`.
- Folded the independent review's accounting caveats into the report and SPEC:
  equality-recipe program lookups need an edge-growth experiment and explicit
  charging or validated random access; builder timings are not retained-size
  proxies; and certificate construction confounds the current kernel lookup
  comparison.

## Current frontier

One calibration reached the eight-round limit while a separate cold repository
build was saturating the host, and the resulting compiled samples have much
higher dispersion than the committed baseline record. The run is structurally
valid but is not the evidence quality intended for the revised report.

## Next step

Commit the review dispositions to establish a clean source point, repeat the
sweep after the competing build has drained with a larger calibration-round
allowance, then regenerate every report number from that clean record.

## Blockers

None.
