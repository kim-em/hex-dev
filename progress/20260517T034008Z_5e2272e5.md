# 20260517T034008Z — feature session 5e2272e5 (#4559)

## Accomplished

Resolved #4559 (HO-1 substrate: `henselLiftData liftedFactor injective`
umbrella from `factorsModPBerlekampForm`) in
`HexBerlekampZassenhausMathlib/Basic.lean`.

Added the new theorem
`henselLiftData_liftedFactor_injective_of_factorsModPBerlekampForm`
(line 4613), inserted immediately after
`henselLiftData_liftedFactor_natDegree_pos_of_factorsModPBerlekampForm`
(line 4549, the natural-degree analog landed in #4555/#4556) and before
the private helper `zpoly_size_pos_of_monic`.  Signature mirrors
`_of_choosePrimeData` (line 4260) for the first nine parameters
(`core, B, primeData, hcore_monic, hp_prime, hp, hB, hfactors_monic,
hproduct_mod_p`) plus `hcoprime` and `hnonempty`, drops
`hfactorsModP_nodup`, and adds `hform : factorsModPBerlekampForm core
primeData` and `hgood : isGoodPrime core primeData.p = true` in its
place.  No `hcore_pos` premise (the `Nodup` discharge routes through
`isGoodPrime`'s modular squarefreeness alone, unlike the natural-degree
analog).

Proof is the two-line composition the issue spec'd: discharge
`hfactorsModP_nodup` via
`factorsModP_nodup_of_factorsModPBerlekampForm core primeData hform
hgood`, then apply `_of_choosePrimeData` with the original nine premises
plus the discharged nodup.  Sorry-free.

## Build state

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓ (exit 0)
- `git diff --check` clean
- `rg sorry|axiom|native_decide|TODO|FIXME` on
  `HexBerlekampZassenhausMathlib/Basic.lean`: the four pre-existing
  theorem-level `sorry`s at lines 162, 241, 8206, 8328 unchanged in
  count (line numbers for the last two shifted by my insertion);
  zero `axiom`, zero `native_decide`, zero new `TODO`/`FIXME`.

## Current frontier

`#4559` resolves with the umbrella in place.  Together with the
natDegree analog landed in #4556 and the monic/coprime/nonempty/B
companions in the broader `_of_choosePrimeData` family, the
`factorsModPBerlekampForm`-parametrised umbrellas now cover the
`Function.Injective (liftedFactor …)` and `0 < …natDegree` premises
the outer-bound HO-1 slow-path wrapper consumes.

## Next step

Natural follow-ups for the HO-1 chain:

1. An HO-1 assembly pass that wires the
   `_of_factorsModPBerlekampForm` family (this PR's umbrella plus
   #4556's natDegree analog) and
   `henselSubsetCorrespondenceHypotheses_outerBound_of_choosePrimeData`
   (#4543) into the slow-path arm of `factor_irreducible_of_nonUnit`
   (#4170), leaving the BHKS branch and other unsupplied hypotheses
   behind `sorry`s.  This would let a follow-up planner narrow #4170's
   residual scope to the BHKS-only substrate.

2. The small-mod singleton substrate constructor flagged in #4543's
   "Out of scope" — derive `hselected`/`hprim`/`hlc_map_ne` from the
   `normalizeForFactor` + `isGoodPrime` invariants for the small-mod
   branch.  Most tractable remaining substrate piece for a partial
   HO-1 deliverable that covers more than the exhaustive arm.

## Blockers

None for #4559 itself.
