# HexMatrix successor off-diagonal reindexing helper layer

## Accomplished

- Claimed #2117.
- Added compiled helper infrastructure in `HexMatrix/Determinant.lean`:
  `detProduct_congr_matrix`, `detTerm_congr_matrix`, two private `insertAt`
  access lemmas, `detTerm_leadingPrefix_borderedMinor_succ_eq`, and
  `detTerm_borderedMinor_succ_insertAt_last`.
- Verified the partial helper layer with `lake build HexMatrix.Determinant`,
  `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`,
  and a placeholder scan of `HexMatrix/Determinant.lean`.
- Split the remaining #2117 scope into #2130 and #2131, and left a
  decomposition breadcrumb on #2117.

## Current frontier

#2117 now has a reusable compiled foundation for term-level rewrites through
the successor leading-prefix block and the final-row/final-column insertion
case. The true off-diagonal cases remain separated by row placement.

## Next step

Land #2130 next: prove the old-boundary-row off-diagonal `detTerm` reindexing
helper, including the product and sign relation. Then use it in #2131 for the
earlier-prefix-row placement.

## Blockers

- The full #2117 deliverable is not complete in this PR; the remaining work is
  intentionally split into #2130 and #2131.
- `lake build HexMatrix` still reports pre-existing `sorry` warnings in
  `HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`; this session did not add
  placeholders in `HexMatrix/Determinant.lean`.
