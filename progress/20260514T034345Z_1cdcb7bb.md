# 2026-05-14 03:43 UTC — work session (1cdcb7bb) on #3947

## Accomplished

- Claimed HO-46 (#3947) after confirming HO-45 was already claimed.
- Wired FLINT `fq_default` into `HexGfqField/Bench.lean` via the shared
  `Hex.BenchOracle.Flint` persistent-subprocess helper.
- Extended `scripts/oracle/flint_bench_driver.py` with the missing
  `fq_default` operations needed by the field bench surface: subtraction,
  negation, inversion, division, and signed/natural powers.
- Added fixed paired Hex / FLINT registrations across the certificate-backed
  `#[2, 3, 4, 5, 6, 8]` ladder for dense reduction, field arithmetic,
  exponentiation, and inversion/division.
- Regenerated `reports/hex-gfq-field-performance.md` around the new export
  `reports/bench-results/hex-gfq-field-flint-fq-default.json`, including
  FLINT raw/adjusted ratios and the process-call measurement caveat.
- Added `HexGfqField.phase4.comparators` metadata and advanced
  `HexGfqField.done_through` from 3 to 4.

## Current frontier

- `HexGfqField` now reports Phase 5 readiness from `scripts/status.py`.
- The fixed comparator registrations verify cleanly, but LeanBench's fixed
  runner isolates each comparator child, so the FLINT medians are dominated by
  Python startup/import time. The report records that this is informational
  measurement shape, not a gating verdict.

## Next step

- Phase 5 planning can start for `HexGfqField` once this PR lands.
- A later benchmark-quality pass could add a larger runtime-threaded field
  ladder if the project wants comparator ratios less dominated by process
  startup.

## Blockers

- None for HO-46.
