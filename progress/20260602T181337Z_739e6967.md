# 20260602T181337Z work #6345

## Accomplished

- Followed the directive queue first: claimed #2567, found that the full HO-4
  directive is already decomposed into narrower D1 issues, and moved to the
  #6345 per-class Mignotte product-congruence slice.
- Added three Recovery-side bridge theorems:
  `productCongruence_of_representsIntegerFactorAtLift`,
  `productCongruencesOfSelectedRepresentations`, and
  `productCongruencesOfCanonicalSupportRepresentations`.
- These convert per-class `RepresentsIntegerFactorAtLift` certificates plus
  executable selected-product identifications into the exact `hproduct` family
  consumed by
  `factorFast_ne_none_of_mignottePrecisionCanonicalSupportsExpectedFactorsAtPrecisionForCoeffBound`.

## Current frontier

The canonical recovery wrapper no longer needs callers to restate the modular
product equality directly once they have the selected-subset product equality
and the Hensel representation certificate.  The remaining #6345 work is to
construct the canonical support-class subset array and prove the selected
arrays from `selectedFactorArraysOfSupports` match those subset products.

## Next step

Prove the canonical support-class subset identification, likely by defining the
`LiftedFactorSubset` induced by each `supportPartitionByMinColumn` member list
and relating `selectedFactorsOfMembers` to
`liftedSubsetSelectedList` / `liftedFactorProduct` via
`polyProduct_liftedSubsetSelectedList_eq_liftedFactorProduct`.

## Blockers

None for the bridge landed here.  The full #2567 directive remains too broad
for one PR and continues through #6345, #6359, #6371, and blocked final
assembly #6346.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Recovery.lean`
  found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
