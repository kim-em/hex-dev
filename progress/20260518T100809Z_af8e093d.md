# 2026-05-18T10:08Z — #4977 review: A2 `_of_bound` cascade closure audit

## Accomplished

Claimed and executed issue #4977, a read-only review producing a single
audit comment on the issue thread (no Lean changes, no PR). Comment:
https://github.com/kim-em/hex/issues/4977#issuecomment-4476595180.

The comment delivers all four sections required by the issue:

* **Section 1 — Cascade inventory.** 31 landed `_of_bound` siblings in
  `HexBerlekampZassenhausMathlib/{Basic,IntReductionMod}.lean`, each
  mapped to its concrete-form wrapper, the closing PR, and the variant
  tag (M / PPL / OB / IB). Plus four in-flight entries (#4970 claimed,
  #4974 open, #4976 blocked on #4970+#4974, #4902 has-pr).
* **Section 2 — Chokepoints.** All 56 hits of
  `grep "2 \* Hex.ZPoly.defaultFactorCoeffBound"` classified as
  wrapper signature (rewired) / internal body (precision-agnostic) /
  unlifted wrapper (actionable). Five unlifted wrappers on the
  exhaustive arm — four already tracked (#4974, #4970, #4976), one
  (slow-quadratic arm) recommended to leave unlifted. Plus an
  orthogonal unscaled-`recombinationCandidate` sub-cascade of
  thirteen wrappers, recommended to leave unlifted except for the
  in-flight #4902 unless `factorFast` (directive #2567) picks them up.
  Includes a dead-wrapper finding:
  `representedFactor_dvd_recombinationCandidate_of_subset`
  (`Basic.lean:12556`) has no in-tree consumers other than docstrings.
* **Section 3 — Capstone readiness.** For each of #4170, #4172, #4818,
  #4819, #4825: whether the abstract-bound axis is on its path, and
  what residuals remain after the cascade closes. Verdicts strictly
  scoped to the abstract-bound axis, per the issue's framing
  paragraph excluding Gap-1 / #2567.
* **Section 4 — Next planner action.** Recommendation: file zero
  additional `_of_bound` issues on the cascade core. #4970, #4974,
  #4976 cover the residual exhaustive-arm work; two optional cleanup
  issues noted but not recommended for filing.

## Current frontier

The A2 `_of_bound` cascade on the slow-exhaustive arm is one PR away
from full closure. Once #4974 lands (currently unclaimed, no `_aux`
dependency), the umbrella aux #4970 + public umbrella #4976 unblock in
sequence and the abstract-bound surface reaches the top of
`IntReductionMod.lean`.

## Next step

Per Section 4 of the audit: no follow-on `_of_bound` issues to file.
Next planner cycle should wait for #4970 / #4974 / #4976 to land and
then assess whether #4170 / #4819 can be picked up (residuals remain:
Hensel substrate bundle, singleton substrate, Gap 1 / #4880, BHKS /
#2567).

## Blockers

None for this audit session.
