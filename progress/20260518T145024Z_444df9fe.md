# 2026-05-18 14:50 UTC — forty-third Phase 4/5 checkpoint (444df9fe)

Claimed summarize issue #5018 and recorded this checkpoint covering
the merged-PR wave since #4980 closed at `2026-05-18T10:54:50Z`.
Window upper bound is the issue's creation timestamp
`2026-05-18T14:40:16Z` (per the issue body's pin instruction).

```
gh pr list --state merged --limit 30 \
    --json mergedAt,number,title \
    --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
  | awk '$1 > "2026-05-18T10:54:50Z"' \
  | awk '$1 <= "2026-05-18T14:40:16Z"' \
  | wc -l
```

= **16 new PRs merged**, matching the issue body's headline estimate
exactly.

PR #5016 (closing #5014, `hcore_lc_bound` → `hcore_lc_le` parameter
rename on 7 `_of_bound` Basic.lean siblings) merged at
`2026-05-18T14:43:05Z`, ~3 min after this issue was created — it
lands in the *next* summary window, not this one.

## `coordination orient` snapshot

```
=== DIRECTIVES (highest priority) ===
#2637 HexBerlekampZassenhaus: Factorization record API + sign/multiplicity bugs
#2567 HO-4: Formalise BHKS Theorem 5.2 (factorFast termination, leaf theorem)
#2564 HO-1: BZ recombination — rollback and rewrite to van Hoeij CLD

=== Unclaimed work items ===
#4334 HexLLL Phase 4: investigate inconclusive scientific benchmark verdicts
      (host-load gated; load 12.10 / 13.42 / 28.26 on 24 cores at
      session start — far above the quiet-host threshold the
      scientific verdict promotion requires per SPEC/benchmarking.md)
#5017 BZMathlib: rewire inline arg-position `.2.1` projection at
      Basic.lean:5597 to zpoly_primitive_of_monic
      (out-of-scope follow-up filed by #5013, atomic readability gain)

=== Claimed work items ===
#5018 Summarize forty-third Phase 4/5 checkpoint           (this session)
#5015 BZMathlib: suppress 5 unused hcore_ne warnings by
      underscore-prefixing at _of_bound Basic.lean signatures
      (mid-flight; was "queued" at issue-creation time)

=== Issues with open PRs ===
#4902 BZMathlib: abstract-bound siblings of
      exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate
      (monic + primitive unscaled)                       (PR #4917, 12 h)

=== Blocked issues (full chain) ===
#4951 [#4929] liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core
              (unscaled primitive)
#4934 [#4931] recombinationSearchModAux_some_and_covers_* (monic)
#4931 [#4927] liftedFactorSubsetPartition_prefix_none (monic)
#4929 [#4925] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_*
#4927 [#4924] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd
#4925 [#4919] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_*
#4924 [#4918] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd
#4919 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset_*
#4918 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset (monic)
#4880 [#2564] BZ Mathlib-free: drop hcore_monic Gap 1 from umbrella
#4832 [#4821, #4830, #4831] BZMathlib: lifted partition recombination support
#4831 [#4821, #4830] BZMathlib: lifted partition disjointness + assoc uniq
#4830 [#4821, #4821] BZMathlib: initial lifted partition cover field
#4825 [#4680, #2567] BZ Mathlib-free: ZPoly.Irreducible per factor.factors entry
#4821 [#4680] BZMathlib: discharge executable Hensel subset analytic obligation
#4819 [#4821] BZMathlib: prove factor_irreducible_of_nonUnit
#4818 [#4825] BZ: prove executable ZPoly isIrreducible_iff
#4680 [#2567] HO-1 substrate: compose ModP and Hensel lift constructors
#4172 [#4170] HO-1 residual: executable irreducibility checker theorem
#4170 [#2567] HO-1 residual: executable factor irreducibility bridge

=== Open pull requests ===
#4917 [agent/8a4738a1] exists_representingSubset_of_mem_normalizedFactors_*
                       (repair-claimed) [CONFLICTS]
#2656 [spec/real-roots-and-sturm] spec: hex-real-roots and hex-sturm (planned)
```

Compared to the previous summarize window: #4334 has reappeared in
the unclaimed queue (was absent at the time of the #4980 summary).
Repeated worker skips on host-contention measurement failures —
recorded by every recent attempt — match the host-load reading at
the start of this session.

## Per-theme PR groupings (16 PRs)

### A2 abstract-bound (`_of_bound`) capstone — public umbrella (1 PR)

The slow-path exhaustive arm's A2 cascade reaches its public
surface. Both `hprecision` consumers of the umbrella body — the
inner `reassemblyExpansionComplete_exhaustive_of_ne_zero` call (via
prior-window #4979) and the forwarded `hprecision` to the aux (via
prior-window #4978) — already had `_of_bound` siblings going into
this window; the umbrella `_of_bound` itself is the final
public-surface step.

* **#4982** — abstract-bound sibling of
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
  (public umbrella `_of_bound`, closes #4976).

After this PR, every public surface in the slow-path exhaustive
arm's `_of_bound` chain — substrate → aux → umbrella — is lifted to
the abstract-`B'` interface. The original surfaces continue to type-
check as thin instantiations.

### Helper-extraction + boilerplate-cleanup cascade in `HexBerlekampZassenhausMathlib/` (15 PRs)

The post-#4991 cascade runs through the Mathlib-bridge layer of
`HexBerlekampZassenhausMathlib/Basic.lean` and
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`, replacing 33+
inline boilerplate sites with named helpers. Five new helpers land
in the cluster `Basic.lean:6707-6749` (which now houses the full
Monic-route projection family).

**Initial extraction + 17-site rewire (1 PR):**

* **#4991** — extract `defaultFactorCoeffBound_leadingCoeff_natAbs_le`
  and rewire 17 `hcore_lc_le` boilerplate sites across `Basic.lean`
  and `IntReductionMod.lean` (closes #4987).

**Further helper extractions in the same idiom (4 PRs):**

* **#5001** — extract `zpoly_ne_zero_of_monic` and
  `zpoly_ne_zero_of_pos_lc`, rewire 9 `hcore_ne` derivation sites in
  `IntReductionMod.lean` (closes #5000).
* **#5009** — extract `zpoly_lc_pos_of_monic`, rewire 4
  `hcore_lc_pos` derivation sites (closes #5005).
* **#5012** — extract
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`, rewire 3
  `IntReductionMod` sites (closes #5007).
* **#5013** — extract `zpoly_primitive_of_monic`, rewire 3
  `hcore_primitive` Monic-route sites (closes #5010).

After this wave the Monic-route helper cluster is **complete** at
`Basic.lean:6707-6749`:
`zpoly_size_pos_of_monic` (pre-existing) /
`zpoly_ne_zero_of_monic` / `zpoly_lc_pos_of_monic` /
`zpoly_primitive_of_monic`.

**Boilerplate cleanup at the rewire sites (5 PRs):**

* **#4998** — drop now-unused `hcore_dvd_self` / `hcore_size_pos`
  `have`s at the 5 `IntReductionMod` `_of_bound` thin-wrapper rewire
  sites (#4991 follow-up, closes #4996).
* **#4999** — same cleanup at the 12 `Basic.lean` `_of_bound`
  thin-wrapper rewire sites (closes #4997).
* **#5006** — extend `defaultFactorCoeffBound_leadingCoeff_natAbs_le`
  rewire to 7 `hcore_lc_bound` body sites in `Basic.lean` (closes
  #5002).
* **#5008** — drop orphan `hcore_size_pos` at
  `IntReductionMod.lean:3423` (#4996 missed site, closes #5004).
* **#5011** — drop now-unused outer `hcore_size_pos` /
  `hcore_dvd_self` `have`s at 7 `hcore_lc_bound` rewire sites in
  `Basic.lean` (closes #5003).

**Docstring framing normalisation (5 PRs):**

* **#4986** — normalise `_of_bound` docstring framing in
  `IntReductionMod` cap layer (closes #4984).
* **#4988** — normalise `_of_bound` "sibling of" → "variant of"
  framing in `Basic.lean` and umbrella cap (closes #4985).
* **#4992** — refresh stale Gap 1/3 framing in `IntReductionMod`
  exhaustive-arm umbrella docs (closes #4989).
* **#4994** — refresh stale `_of_bound`-future framing in two
  `Basic.lean` rewired-wrapper docstrings (closes #4990).
* **#4995** — drop stale two-step body paragraph and bare-line refs
  on exhaustive-arm umbrella (closes #4993).

All `_of_bound` siblings across `Basic.lean` and `IntReductionMod`
now share the canonical "Abstract-bound variant of …" framing.

## Status snapshot

* **`libraries.yml`**: no commits in window
  (`git log --since="2026-05-18T10:54:50Z" --until="2026-05-18T14:40:16Z" -- libraries.yml`
  empty). The next `done_through` bump remains gated on the HO-1
  critical-path chain.
* **In-flight PRs at orient time** (`2026-05-18T14:46Z`):
  * **PR #4917** (abstract-bound siblings of
    `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
    for #4902, monic + primitive unscaled) — `repair-claimed` with
    `CONFLICTS`, branch `agent/8a4738a1`. Has now sat in this state
    across **four** consecutive summarize windows. This PR continues
    to block the monic + primitive_pos_lc_core `_of_bound` substrate
    chain in `Basic.lean`: #4918, #4919, #4924, #4925, #4927, #4929,
    #4931, #4934, #4951.
  * **PR #2656** (`spec/real-roots-and-sturm`) — long-running SPEC
    PR, status `planned`. Not part of any active phase.
* **Claimed but no PR yet**:
  * **#5015** (suppress 5 unused `hcore_ne` warnings by
    underscore-prefixing at `_of_bound` Basic.lean signatures) —
    claimed by another session ~26 min before this summarize ran.
    Independent from the cascade PRs above; touches the same file
    but disjoint tokens.
* **Directive closures in window**: none.
  `gh issue list --label directive --state closed` filtered to
  `> 2026-05-18T10:54:50Z` is empty. #2637, #2567, #2564 all still
  open.
* **Re-opened from prior windows**: **#4334** (HexLLL Phase 4
  inconclusive verdicts) is back in the unclaimed queue. Skipped
  again at the start of this session on host-load grounds (12.10 /
  13.42 / 28.26 on 24 cores; the issue's own body explicitly
  carves out "re-dispatch only when the host is idle enough for a
  stable spawn floor").

## HO-1 critical-path commentary (capstone #4170 / sub-capstone #4825)

The BZ Mathlib-free chain remains gated on #4680 (Hensel subset
analytic obligation composition) and ultimately on directive #2567
(BHKS Theorem 5.2).

This window's contribution to that path is to close the public
umbrella surface of the A2 `_of_bound` cascade (#4982), which is the
natural pre-requisite for any future precision-threshold lowering in
the `recombinationSearchModAux_*` /
`exhaustiveCoreFactorsWithBound_*` chain on the slow-path exhaustive
arm. Both consumers of the umbrella's body now have `_of_bound`
siblings: the inner discharger (#4979, last window) and the aux
(#4978, last window). With #4982 in place, the substrate /
aux / umbrella triplet is fully lifted.

The cascade's downstream consumer — the BZ Mathlib-free capstone
(#4170) — is **unchanged**, still gated on #2567. The helper-
extraction cascade is orthogonal to the critical path; it sits at
the Mathlib-bridge layer and discharges boilerplate, not analytic
obligations.

Downstream of the public-umbrella capstone, the unscaled-monic side
of the recursive-coverage layer (#4918 / #4919 / #4924 / #4925 /
#4927 / #4929 / #4931 / #4934 / #4951) remains queued behind the
repair-claimed PR #4917 (covering #4902). The four-window streak in
`CONFLICTS` state is the primary risk to the monic-side cascade's
forward motion.

## #4880 umbrella drop posture

The post-#4991 helper-extraction cascade is **independent** of
#4880's residual Gap-1 / Gap-3 obstruction at the umbrella surface.
The new helpers
(`defaultFactorCoeffBound_leadingCoeff_natAbs_le`,
`zpoly_ne_zero_of_monic`, `zpoly_ne_zero_of_pos_lc`,
`zpoly_lc_pos_of_monic`,
`normalizeForFactor_squareFreeCore_primitive_of_ne_zero`,
`zpoly_primitive_of_monic`) sit at the Mathlib-bridge layer and
discharge inline derivation boilerplate at `_of_bound` rewire sites
— they do not touch the umbrella's `hcomplete` /
`hcore_monic` premise surface.

The current state is unchanged from the forty-second checkpoint:

* Helper 1 substrate (`factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`):
  **closed** via #4945.
* Helper 5 substrate (`reassemblyExpansionComplete_exhaustive_of_ne_zero`):
  **closed** via #4953 + #4952 + #4957.
* Helpers 2, 3, 4: **architecturally infeasible** per #4940's
  audit. The `multifactorLiftQuadratic_each_monic` per-output
  monicness conclusion in `HexHensel/QuadraticMultifactor.lean:1196`
  is false for non-monic primitive core; helpers 3/4 inherit the
  blocker through helper 2's internal use.

The umbrella drop remains **directive-level blocked**. Closing it
requires either Option A (consumer-side recombination invariant
relax: "all-but-one monic; residual carries `lc(core)`") or Option B
(executable-side monicising-by-scaling refactor of
`exhaustiveCoreFactorsWithBound`
at `HexBerlekampZassenhaus/Basic.lean:5495`).

Per #4940's recommendation, planner cycles should **not** file
substrate sub-issues beneath #4880; the umbrella's residual Gap 1
is documented in the docstring at
`HexBerlekampZassenhausMathlib/IntReductionMod.lean:3589` as
architecturally infeasible at the present API surface.

## Carryover for the next summarize

* **#5016** (closing #5014, `hcore_lc_bound` → `hcore_lc_le`
  parameter rename) — merged at `2026-05-18T14:43:05Z`, ~3 min
  after this issue's creation timestamp. Lands in the next window.
* **#5015** (underscore-prefix 5 unused `hcore_ne` parameters at
  `_of_bound` Basic.lean signatures) — claimed and in flight; its
  PR will land in the next window. Closes out the
  `hcore_ne`-related warning set at the rewire sites and
  completes the post-#4991 cleanup tail.
* **#5017** (rewire inline arg-position `.2.1` projection at
  `Basic.lean:5597` to `zpoly_primitive_of_monic`) — unclaimed.
  Single-line atomic follow-up filed by #5013; brings the
  Monic-route helper cluster's call-site coverage of `Basic.lean`
  to 100% (named-projection + arg-position).
* **PR #4917** (abstract-bound siblings of
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
  for #4902) — repair-claimed, `CONFLICTS`. Has now sat in this
  state across four summarize windows; the monic-side recursive-
  coverage `_of_bound` chain (#4918 / #4919 / #4924 / #4925 /
  #4927 / #4929 / #4931 / #4934 / #4951) remains queued behind it.
  If the repair agent abandons PR #4917, the underlying issue
  #4902 returns to the queue.
* **#4334** (HexLLL Phase 4 inconclusive verdicts) — unclaimed,
  host-load gated. Skipped repeatedly across multiple sessions on
  measurement-infrastructure failures. Requires quiet/dedicated
  hardware per `SPEC/benchmarking.md`.
* **Blocked chain** (transitive on #2567 / #4680):
  #4818 / #4819 / #4821 / #4825 / #4830 / #4831 / #4832 /
  #4170 / #4172.
* **#4880 umbrella drop**: directive-level blocked on Option A /
  Option B. Workers should not claim this issue (per its own body).
* **Slow-path `hcomplete` drop residual**: the slow-path
  exhaustive umbrella still threads `hcomplete` implicitly via its
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` internal
  call. The natural follow-on now that #4982 has stabilised the
  umbrella's `_of_bound` signature.
