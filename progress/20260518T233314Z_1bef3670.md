## Accomplished

- Claimed feature issue #5164 (`HexConway Phase 6: add general
  luebeckConwayPolynomial?_monic and conwayPoly_monic wrappers`).
- Added 36 `private theorem luebeckConwayPolynomialOfCoeffs_p_n_monic`
  siblings in `HexConway/Basic.lean`, one per committed Tier 1 entry
  `(p, n) ∈ {2, 3, 5, 7, 11, 13} × {1..6}`, each mirroring the existing
  `_irreducible` sibling shape: rewrite
  `luebeckConwayPolynomialOfCoeffs p [...] = luebeckConwayPolynomial_p_n`
  via `Option.some.inj` against the matching `?_hit_p_n` hypothesis,
  then close with the corresponding `luebeckConwayPolynomial_p_n_monic`
  witness.
- Added public `theorem luebeckConwayPolynomial?_monic` immediately after
  `luebeckConwayPolynomial?_irreducible` and before the
  `structure SupportedEntry` declaration. Same `split` + `cases hcoeffs`
  shape as `?_irreducible`, dispatching each `(p, n)` arm to the new
  per-entry `_monic` sibling, plus a trailing `none` arm.
- Added public `theorem conwayPoly_monic` at the end of the file
  (after `conwayPoly_irreducible`, before `end Conway`), reusing
  `?_monic` against `luebeckConwayPolynomial?_conwayPoly`. Matches
  `conwayPoly_irreducible` / `conwayPoly_nonconstant` shape verbatim.
- No `@[simp]` on the new general wrappers; only one-sentence
  docstrings on the public ones, matching the existing pattern.
- No call sites rewired (out of scope per issue body).

## Current frontier

- Issue #5164 deliverables are complete.

Verification:

- `lake build HexConway.Basic` passed.
- `lake build HexConway` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `git grep -n "luebeckConwayPolynomial?_monic\\|conwayPoly_monic" HexConway/Basic.lean`
  shows both new public declarations and their call site in
  `conwayPoly_monic`.
- `git diff -- HexConway/Basic.lean | rg -n "sorry|axiom|native_decide|TODO|FIXME"`
  produced no matches.

## Next step

- Publish the PR for #5164.

## Blockers

- None.
