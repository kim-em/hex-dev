# 20260510T033027Z - Issue #2927

## Accomplished

Claimed issue #2927 after skipping #2934 because its required PR #2931 was
still open. Added a local Yun product-rule bridge in
`HexPolyFp/SquareFree.lean`:

- `dvd_add_poly`, `dvd_mul_left_of_dvd`, and `dvd_mul_right_of_dvd`
- `yunStep_common_dvd_derivative_product`
- `yunStep_common_dvd_derivative_current`

The bridge proves that a common divisor of the Yun quotient step factors
`z = c / gcd c w` and `y = gcd c w` divides `derivative c`, via
`DensePoly.derivative_mul` and the existing `div_gcd_mul_reconstruct`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean HexPolyFp/Basic.lean`
- `git diff -U0 -- HexPolyFp/SquareFree.lean HexPolyFp/Basic.lean | rg '^\+.*(axiom\b|native_decide|TODO|FIXME|sorry)'`

The touched cluster still has the four pre-existing `sorry` warnings; no new
`sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` marker was added.

## Current frontier

The derivative-current bridge is available for the reachable Yun
common-divisor provider. The stronger literal target `d ∣ derivative z` is
not derivable from `d ∣ z` and `d ∣ y` alone; the proved local bridge records
the product-rule consequence that follows directly from the step split.

## Next step

Use the bridge while assembling the derivative-split common-divisor theorem,
where the reachable initial-state hypotheses can supply the remaining gcd
minimality/cancellation argument needed to drive common divisors to units.

## Blockers

None for this issue. Residual benchmark issue #2934 remains blocked until PR
#2931 lands.
