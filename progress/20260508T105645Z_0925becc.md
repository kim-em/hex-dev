## Accomplished

- Claimed repair for PR #2680.
- Diagnosed the failing `conformance` check as a stale `HexBerlekampZassenhaus` fixture: the emitter now serializes `factor` results as `[scalar, [[factor, multiplicity], ...]]`, while the committed fixture still used the old flat factor-list shape.
- Updated `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl` to match the current `hexbz_emit_fixtures` output.
- Verified:
  - `diff -u conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl /tmp/hexbz-fresh.jsonl`
  - `python3 scripts/oracle/bz_flint.py --check`
  - `lake build`
  - `PATH=/tmp/hex-oracle-venv/bin:$PATH scripts/ci/run_oracles.sh`

## Current frontier

- PR #2680's previous conformance failure is repaired locally.
- The branch still has an unrelated pre-existing modification to `.claude/CLAUDE.md`; this repair did not stage or modify it.

## Next step

- Push the repair commit and mark PR #2680 salvaged.

## Blockers

- None.
