# Session d5a49a35 — feature, issues #4076 → #4226 → #4228

## Accomplished

- Decomposed #4076 (final no-kernel-split irreducibility composition)
  into two sub-issues:
  - #4226: matrix↔algebraic kernel bridge
    (`IsFixedSpaceKernelPolynomial ↔ f ∣ FpPoly.linearPow w p - w`)
  - #4227: residual composition (depends on #4226)
- Decomposed #4226 into:
  - #4228: compose-form Frobenius identity
    `compose w (FpPoly.linearPow FpPoly.X p) = FpPoly.linearPow w p`
    (foundational unit for the matrix bridge)
  - #4226 residual: matrix-bridge proof (depends on #4228)
- Claimed and discharged #4228 in `HexPolyFp/Compose.lean`. Added:
  - `linearPow_linearPow_mul`: `linearPow (linearPow a m) n =
    linearPow a (m * n)`.
  - `linearPow_zero_of_pos`: `linearPow 0 n = 0` for `0 < n`.
  - `composeCoeffPowerSumUpTo_subst_linearPow_X`: substitution helper
    showing the compose power-sum at `linearPow X p` equals
    `linearPow` of the corresponding power-sum at `X`.
  - `composeCoeffPowerSumUpTo_X_coeff`: closed-form coefficient lemma
    for the power-sum at `X`.
  - `composeCoeffPowerSumUpTo_self_X_eq_self`: polynomial
    reconstruction.
  - `compose_w_linearPow_X` (public): the headline theorem.
- Added two imports to `HexPolyFp/Compose.lean`:
  - `HexPolyFp.SquareFree` (for `linearPow_add_prime`,
    `linearPow_mul_base`).
  - `HexPolyFp.QuotientFrobenius` (for
    `Quotient.linearPow_C_pow_prime`).
- Verified:
  - `lake build HexPolyFp.Compose`
  - `lake build HexPolyFp`
  - `lake build HexBerlekamp.RabinSoundness HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
    `sorry` in the diff.

## Current frontier

- The compose-form Frobenius identity is now available and exported.
- The matrix bridge in #4226 (its residual) can now factor through this
  identity plus a `berlekampColumn` closed-form characterization and
  `mulVec`/`coeffVector` linearity.

## Next step

- Pick up #4226 next. With `compose_w_linearPow_X` in hand, the
  matrix-bridge proof reduces to:
  - characterise `berlekampMatrix[i][j]` as
    `coeffVector f (linearPow X (p * j) mod f) [i]`,
  - prove `(berlekampMatrix · coeffVector f w) =
    coeffVector f (linearPow w p mod f)` for `w.size ≤ basisSize f`,
  - state the iff
    `IsFixedSpaceKernelPolynomial f hmonic w ↔
       f ∣ FpPoly.linearPow w p - w`.
  Once #4226 lands, #4227 composes everything to discharge the
  original #4076 deliverable.

## Blockers

None for #4228.
