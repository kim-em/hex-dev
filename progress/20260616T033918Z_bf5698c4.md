# Progress — 2026-06-16 03:39:18 UTC (session bf5698c4, work)

## Accomplished

Claimed issue #7541 and added the missing recovered-coordinate support
projection siblings in `HexBerlekampZassenhausMathlib/Basic.lean`:

- `representingSubset_subset_of_dvd_liftedRecoveryCandidate`
- `representingSubset_subset_of_dvd_liftedRecoveryCandidate_of_bound`

Both wrappers mirror the existing default/monic recombination-candidate support
wrappers but take `f ∣ liftedRecoveryCandidate core d T`, and both prove
`S ⊆ T` directly through
`LiftedFactorSubsetPartition.support_subset_of_dvd_liftedRecoveryCandidate`.
They do not route through `liftedFactorProductCandidate`,
`recombinationCandidate`, or monic equality rewrites.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic` succeeded.
- `lake build HexBerlekampZassenhausMathlib` succeeded.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` succeeded.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Basic.lean` found no new `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
- `/reflect` is unavailable in this environment (`zsh: no such file or
  directory: /reflect`).

## Current frontier

The default/monic wrapper family now has recovered-candidate support projections
matching the existing primitive-positive-core recovered wrappers.

## Next step

Open the PR for #7541. Later migration work can start switching callers away
from the old unscaled `recombinationCandidate` support wrappers where the
recovered-coordinate candidate is available.

## Blockers

None.
