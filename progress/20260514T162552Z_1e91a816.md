# Session 2026-05-14 16:25Z (1e91a816)

## Accomplished

- Skipped #3974 (`HexGramSchmidt: assemble no-pivot Gram determinant
  vector prefix bridge`). The proof of
  `gramDetVecEntry_eq_leadingPrefix_bareiss` reduces (via the helpers
  from #4002/#4029) to a `bareissNoPivotData → bareiss` bridge on the
  `(r+1) × (r+1)` leading prefix. Closing that gap needs the
  bordered-minor / `NonzeroBareissPivots` / `bareissNoPivot_eq_det`
  chain, which `SPEC/Libraries/hex-matrix.md` forbids in `hex-matrix`
  and the parent #3538 Q2 explicitly says to stop and route through
  HO-43 for. Skip note posted on the issue suggests relocating the
  theorem to `HexGramSchmidtMathlib/Int.lean`.

- Closed issue #4028 (`HexMatrix: prove noPivotLoop bordered-minor sync
  at trailing-block entry`). Added private helpers in
  `HexGramSchmidt/Int.lean` mirroring the existing leading-prefix
  infrastructure:

  - `borderedMinor_entry_eq_source`: bordered-minor entry equals the
    source matrix at the dependently-lifted index.
  - `liftBorderedIdx` plus `liftBorderedIdx_val_lt`,
    `liftBorderedIdx_val_eq_k`, `liftBorderedIdx_at_kStep`: explicit
    index lift between `Fin (k + 1)` (bordered-minor coordinate) and
    `Fin n` (source coordinate).
  - `borderedMinor_entry_eq_lift`: `borderedMinor` entry equals source
    at `liftBorderedIdx`-lifted indices.
  - `borderedMinor_stepMatrix_eq`: one Bareiss step commutes with
    `borderedMinor` when the bordering anchors lie in the trailing
    block (`k ≤ row.val`, `k ≤ col.val`) and the step is strictly
    before `k`. This is the bordered-minor analog of
    `leadingPrefix_stepMatrix_eq`.
  - `noPivotLoop_sync_borderedMinor_aux`: fuel-induction sync between
    `noPivotLoop` on the full `n × n` matrix and on the `(k + 1) × (k + 1)`
    bordered minor, parameterized by matching bookkeeping fields and
    the bordered-minor identification.
  - `noPivotLoop_full_eq_borderedMinor_at_trailing`: entry-level
    corollary giving
    `s_full.matrix[row][col] = s_bm.matrix[Fin.last k][Fin.last k]`
    after `k` iterations from the respective initial states (plus
    `singularStep` agreement).

  No `axiom`, `native_decide`, `TODO`, `FIXME`, or new theorem-level
  `sorry`. The four existing `sorry`s in
  `HexGramSchmidt/Int.lean` are unchanged.

## Current frontier

The bordered-minor sync infrastructure for #4028 is in place. Its
intended consumer is #3994 (the `scaledCoeffArrayLoop` Cramer
no-pivot invariant), which is blocked on the symmetry / transpose
sub-issue identifying `borderedMinor M k hk ⟨i,_⟩ ⟨j,_⟩` with
`borderedMinor M k hk ⟨j,_⟩ ⟨i,_⟩` for Gram matrices.

## Next step

Pick up the next unclaimed feature (Berlekamp or Hensel group A,
depending on what's available after queue refresh).

## Blockers

`gramDetVecEntry_eq_leadingPrefix_bareiss` (#3974) remains blocked at
the Mathlib-free / Mathlib-bridge boundary; needs a planner-led
relocation rather than another worker attempt.
