# Session 6501ba15 — /work triage

Worker session that started with three issues nominally unclaimed
(#4334, #4611, #4676) and cleared the queue by routing each through
the appropriate replan path after structural assessment.

## Accomplished

* **Skipped #4676** (`HO-1 substrate (#4618 sub): descend repeatedPart
  power divisibility to Polynomial Z`) with stale-dependency reason:
  the rational-side theorem
  `normalizeForFactor_repeatedPart_map_intCast_dvd_squareFreeCore_map_intCast_pow`
  consumed here lives in #4704 (claimed by session `01c09a70`, in
  progress); the infrastructure helpers
  (`divRadical_dvd_radical_pow_of_ne_zero`,
  `rp_dvd_sf_pow_of_associated`, `toPolynomial_scale`) are in PR
  #4705, still in CI. The body's `depends-on: #4675` is stale — #4675
  was decomposed during session `9383b3a1`. Suggested replan to update
  to `depends-on: #4704`.

* **Skipped #4647** (`HO-1 substrate (#4638 sub): primitive recursive
  recombination coverage`) — this one was the most substantive
  assessment. The recursive proof identifies
  `recombinationCandidate d S_cov = f_cov` via
  `recombinationCandidate_eq_factor_of_recovery_of_monic_core`
  (`HexBerlekampZassenhausMathlib/Basic.lean:3656`), which collapses
  `scaledLiftedFactorProduct core d S = liftedFactorProduct d S` only
  under `lc(core) = 1`. For primitive non-monic core this equality is
  false — the concrete counterexample on #4643/#4652 (modulus 101,
  `lc(core) = 2`, `factor = 2X + 1`) yields unscaled candidate
  `X - 50`, not `2X + 1`, after the `primitivePart ∘ normalizeFactorSign`
  composite. #4652 explicitly forbids attempting
  `recombinationCandidate d S = factor` from primitive + pos-lc
  hypotheses alone, yet the recursive theorem must do so (or have an
  `Associated`/`exactQuotient?` bridge) to pass through
  `exactQuotient?_recombinationCandidate_eq_some_of_eq_factor`. The
  helpers #4644/#4646 landed do supply
  `representsIntegerFactorAtLift_primitive` and the
  empty-prefix/prefix-none/degree-sum primitive variants, but no
  primitive-pos-lc analog of `recombinationCandidate_eq_factor_of_recovery`
  exists (only `scaledRecombinationCandidate_eq_factor_of_recovery`).
  Recommended replan options: (a) modify the executable to use the
  scaled candidate (out of scope for one session), (b) add a precursor
  proving the primitivePart-collapse identity reconciling the #4643
  counterexample, or (c) re-scope #4647 to a weaker conclusion.

* **Skipped #4696** (`HO-1 substrate (#4679 sub):
  henselLiftData_represents_modP_of_lifted (descent)`) with two real
  blockers: (1) `HexHenselMathlib.hensel_unique` cited by the body
  for uniqueness modulo `p^k` is still `sorry`
  (`HexHenselMathlib/Correctness.lean:228`); precursor #4700 was
  decomposed into #4706 (coprime cancellation) and #4707 (inductive
  assembly), both still open. (2) Transport from lifted-factor mod
  `p` to mod-`p` factor product goes through
  `Hex.ZPoly.multifactorLiftQuadratic_each_congr_mod_base`
  (`HexHensel/QuadraticMultifactor.lean:1038`), which requires
  `hf_monic`; the descent theorem signature in #4696's body has no
  monic hypothesis on `core`. The forward sibling #4695 hasn't
  landed either, so there's no precedent on the non-monic bridge.

## Current frontier

After this session the `coordination list-unclaimed` queue is empty.
The active in-flight work concentrated in claimed/PR'd issues:
#4704 (rational repeatedPart specialisation, claimed by `01c09a70`),
#4705 (helper infrastructure PR in CI), #4691 (claimed `monicModPImage`
algebra), the #4706/#4707 hensel_unique chain.

#4334 (HexLLL Phase 4 inconclusive bench verdicts) was already
`replan`-flagged by another session before I got to it; the host load
(`carica` showing load avg 12.29) is well above the threshold prior
workers documented as causing the inconclusive verdicts.

## Next step

For the planner triage cycle:

1. Update #4676's body to `depends-on: #4704` (and transitively #4705).
2. Decide #4647's resolution path among the three options outlined in
   the skip comment; this is a design call, not a worker choice.
3. Update #4696 either to `depends-on: #4707` plus a monic hypothesis,
   or split a monic-bridge precursor that landed before the descent
   itself can proceed.

For workers, once #4704 lands, #4676 unblocks; once #4707 lands plus
the monic gap is resolved, #4696 unblocks; #4647 needs planner input
before any worker should retry it.

## Blockers

No personal blockers. The session deliberately routed three issues to
`replan` rather than attempting partial work that would either need
`sorry` placeholders (which the issue bodies forbid) or step on
in-flight sessions.
