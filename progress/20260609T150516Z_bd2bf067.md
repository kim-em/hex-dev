# 20260609T150516Z (#6668)

## Accomplished

Claimed #6668 and added an executable Newton-iterator phase-one substrate in
`HexPolyZ/Mignotte.lean`.

The landed proof establishes that, for any starting positive state `x`, running
`sqrtAux n (2 * x.log2 + 1) x` either stops with the lower-square bound
`y * y ≤ n` or reaches `sqrtNearEnvelope n y`. The specialization from
`x = n` is packaged as `sqrtAux_phase_one_near_or_sq_le`.

Supporting private lemmas added:
- `sqrtStep_ge_of_sq_le`
- `sqrtAux_eq_self_of_sq_le`
- `sqrtAux_append`
- `sqrtAux_preserves_sq_or_near`

## Current frontier

The proof uses the executable `floorSqrt` fuel budget `2 * n.log2 + 1`.
Issue #6668 requested the sharper phase-one budget `n.log2 + 1`; that
strengthening remains open.

## Next step

Strengthen the phase-one wrapper from `2 * n.log2 + 1` fuel to
`n.log2 + 1` fuel, likely by adding a bound that relates the very-far
condition `16 * n ≤ x * x` to the remaining log-distance from the initial
state `x = n`, rather than spending two fuel ticks for every possible
`log2 x` decrease.

## Blockers

No build blockers. Verification performed:

- `lake build HexPolyZ.Mignotte`
- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

Pre-existing linter warnings remain in `HexPolyZ/Basic.lean`.
