**Accomplished**

- Claimed #5757 (HexGramSchmidt Phase 5: prove reachable Gram noPivot
  exact-division step).
- After orientation discovered the deliverable cannot be satisfied as
  scoped: the `hentry_regular` premise on
  `bareissGramRowInvariant_noPivotLoop` is universally quantified over
  `hinv_state : BareissGramRowInvariant b state`, whose `mem_span`
  field is existential and projected by `Classical.choose`. With
  non-trivial kernel of `transpose b`, different witnesses for the
  same invariant produce different next-step coefficient numerators,
  so coefficient-level divisibility is not witness-independent and the
  universal premise is unsatisfiable.
- Verified counterexample mechanics on
  `b = [[2,0,0,0,0],[0,2,0,0,0],[2,2,0,0,0],[0,0,3,0,0]]` and
  `b = [[1,1,0],[1,0,1],[0,1,1],[2,2,2]]`. In each case, kernel-shifted
  alternative witnesses change the numerator delta but happen to
  preserve divisibility because the alternate trajectory naturally
  carries a `prevPivot` factor — a fact that needs the deterministic
  trajectory to state, not the abstract witness structure.
- Decomposed #5757 into three sub-issues with dependency chain
  #5803 → #5804 → #5805:
  - #5803: refactor `BareissGramRowInvariant` to bundle deterministic
    `coeff : Fin n → Vector Int n` (drops `Classical.choose`).
  - #5804: prove coefficient-level Bareiss divisibility on the
    deterministic Gram trajectory (Sylvester-style at coefficient level).
  - #5805: discharge `hentry_regular` via #5804 + #5756's
    `dot_rowCombination_exactDiv_eq_of_eq_mul_right`, expose
    consumer-facing variants without the `hentry_regular` parameter.
- Skipped #5757 with the standard `Decomposed into #X, #Y, #Z`
  breadcrumb comment.

**Current frontier**

- #5803 is the next actionable sub-issue: a self-contained structural
  refactor of `BareissGramRowInvariant` and its 5 consumers
  (initial, regular_step, noPivotLoop, noPivotLoop_initial,
  noPivotLoop_initial_gram_entry_eq_dot_of_rowInvariant) in
  `HexGramSchmidt/Int.lean` lines 2250-2667.
- The downstream Phase 5 chain (#5755, #5748, #5668) remains blocked
  on the unrolled #5803 → #5804 → #5805.

**Next step**

- Next worker should claim #5803 and execute the refactor. It does
  not require new mathematics, only swapping the existential
  `mem_span` field for an explicit `coeff` field and propagating the
  read-pattern change through five helpers.

**Blockers**

- None directly. #5757 is forwarded to the planner via the `replan`
  label; planner closes it once the sub-issues fully cover it.
