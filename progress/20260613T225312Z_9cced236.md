# Progress: #6963 — early recovered-coordinate exact-recovery package

Feature session for #6963.  Claimed the focused BZ recovery slice after checking
the unclaimed queue and finding no unclaimed directives.

## Accomplished

- In `HexBerlekampZassenhausMathlib/Basic.lean`, moved the centered-lift
  reduce-roundtrip helper before the early Hensel subset correspondence package
  so the recovered-coordinate exactness API can be used there without a forward
  reference.
- Moved `liftedRecoveryCandidate_eq_factor_of_congruence_of_bound` and
  `RecoveredAtLift.candidate_eq_of_bound` before the
  `existsUnique_recoveringLiftedFactorSubset_*` cluster.
- Replaced the old scaled-centered-lift conclusion of
  `existsUnique_recoveringLiftedFactorSubset_of_henselSubsetCorrespondence_of_bound`
  and its wrappers with a recovered-coordinate package:
  `∃ hrec : RecoveredAtLift core d factor S,
    liftedRecoveryCandidate core d S = factor`.
- Made the required bound explicit on the hidden `hrec.monicFactor`, and made
  the factor sign-normalization hypothesis explicit.  This avoids adding an
  unsound bridge from `RepresentsIntegerFactorAtLift` back to the old
  scaled-product congruence.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Basic` now advances past the early
  exact-recovery package.  It remains red at the next migration surfaces:
  old monic-core/direct callers around `Basic.lean:8716`, `8729`, `9108`,
  `9446`, `9457`, `9556`, `9863`, and `10355`; late direct callers around
  `14303`, `14425`, and `14648`; recursive scaled-search callers around
  `16042`, `16424`, and the executable candidate-shape mismatch at `16524`.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` succeeded.
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Basic.lean`
  found no new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The claimed early exact-recovery package no longer depends on the old
scaled-product equality.  The first remaining failures are the monic-core and
direct recombination callers already split into #6964; later failures match the
late recursive scaled-search surface tracked by #6965/#6840.

## Next step

#6964 should migrate `representsIntegerFactorAtLift_mul_of_monic_core` and the
direct recombination-candidate callers to construct/use recovered-coordinate
witnesses or recovered candidate equalities.

## Blockers

None for #6963.  The module stays red until the sibling migration surfaces are
completed.
