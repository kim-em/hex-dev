## Accomplished

- Proved the generic coefficient-dominance form of Pellet's theorem from Rouché, including the zero-radius edge case and boundary nonvanishing.
- Decoded arbitrary executable `pelletAt` checks into exact real dominance and connected Taylor shifts back to original polynomial roots with multiplicity preserved.
- Proved `Hex.witness` root counts and boundary exclusion at the base, doubled, and quadrupled circumscribed radii.
- Proved the Pellet atom disjunct contains one interior simple root unique throughout its closed certified disc, and exposed cluster count/boundary semantics.
- Added dyadic left-shift casts, exposed the shared fold/degree and `rootsInDisc` bodies needed by downstream modules, and documented the shared RootFree lemmas.
- Passed fresh focused builds, the HexRootsMathlib umbrella build, structural checks, and the complete 9,398-job `lake build` after repairing a stale-artifact visibility check.
- Obtained an independent Claude Opus review; it found no soundness blocker and recommended keeping the generic and executable material together. Its requested fresh compile is green.

## Current frontier

- Plan item 20 is ready to rebase onto merged `main` and publish as the PR closing #8809.
- Plan item 21 is claimed as #8811 with the general certificate and speculative-guard contracts checked against `Component.certify?`.

## Next step

- Rebase this single Pellet commit onto current `main`, open its PR with auto-merge, then implement the general certificate branch analysis and `Certifier.Preserves` instances while CI runs.

## Blockers

- None.
