# #8868 SD5/SD6 scheduled comparator evidence

**Accomplished**

- Claimed GitHub issue #8868 via `gh` after `coordination` was unavailable on PATH.
- Moved the SD5/SD6 seam evidence in
  `reports/hex-berlekamp-zassenhaus-performance.md` from "spike follow-up"
  language to a scheduled factor-sweep comparator record.
- Recorded the traceable artefact, command, host, commit, toolchain, corpus hash,
  10s cap, cross-check status, and SD5/SD6 rows for `hex-factor`,
  `hex-classical-nodecline`, `hex-lattice`, and `isabelle-bz`.
- Corrected the SHA-256 listed for
  `reports/bench-results/hexbz-factor-sweep-bc958d84-carica.json` in
  `reports/hexbz-factor-sweep.md` so it matches the committed file.
- Ran `git diff --check` and a JSON consistency script verifying the report rows
  against the sweep artefact.

**Current frontier**

- Documentation-only change. No Lean sources or benchmark artefacts were changed.
- The requested `$second-opinion` wrapper was invoked, but Claude Opus returned a
  weekly-limit error before producing review output.

**Next step**

- Commit the report update, open the PR, then check mergeability and CI.

**Blockers**

- Claude Opus second-opinion output was unavailable due the wrapper's weekly
  limit (`resets 1pm Australia/Sydney`).
