# 2026-05-18T23:56Z — HexGF2 Phase 6 monomial simp wrappers

Session type: `/feature` worker.
Issue: #5170 — HexGF2 Phase 6: add `degree_monomial`,
`isZero_monomial_eq_false`, `monomial_ne_zero` simp wrappers.

## Accomplished

Added three one-line `@[simp]` wrapper theorems in
`HexGF2/Basic.lean`, placed immediately after `degree?_monomial`:

- `degree_monomial (n : Nat) : (monomial n).degree = n` — via
  `degree_eq_of_degree?_eq_some (degree?_monomial n)`.
- `isZero_monomial_eq_false (n : Nat) : (monomial n).isZero = false`
  — via `isZero_false_of_degree?_eq_some (degree?_monomial n)`.
- `monomial_ne_zero (n : Nat) : monomial n ≠ 0` — via
  `ne_zero_of_degree?_eq_some (degree?_monomial n)`.

Each carries a Phase-6 docstring matching the surrounding style and is
marked `@[simp]`, completing the `monomial`-side characterising triple
in symmetry with the existing `0` triple (`isZero_zero`,
`degree?_zero`, `degree_zero`).

Verification:
- `lake build HexGF2.Basic` — clean.
- `lake build HexGF2` — clean (48 jobs).
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- No `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` introduced.

## Current frontier

The three new declarations are now available to downstream `simp`
calls. The inline reconstruction at `HexGF2/Field.lean:2207`
(noted in the issue as deliberately out-of-scope) is now eligible
for a follow-up migration onto `degree_monomial`.

## Next step

Follow-up issue (out of scope here): migrate existing call sites
that reconstruct `(monomial n).degree`, `(monomial n).isZero`, or
`monomial n ≠ 0` inline onto the new wrappers.

## Blockers

None.
