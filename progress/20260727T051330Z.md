# Issue #8888 witness-splitting profile

## Accomplished

- Extended the compiled `RELIFT_PROFILE=berlekamp` callbacks with witness-phase
  timings, dynamic counters, matched baseline/remainder-reuse/linear-stop arms,
  and full-factor checksums across the degree 6--24 split grid, phi15, SD3,
  and SD4.
- Identified candidate traversal as the main split-grid growth source: at
  degree 24, 576 of 622 witness visits scanned already-linear leaves.
- Added a proved `f.size ≤ 2` fast path to `Berlekamp.fullySplit`, preserving
  the exact factor list and order while reducing split-grid witness visits to
  46 and improving complete factorization by 1.46--2.26x.
- Recorded the method, counts, timings, and remaining non-linear remainder-reuse
  opportunity in the witness-splitting report and raw CSV.
- Added exact linear, quadratic, and mixed-degree conformance guards plus a
  fixed `X^5 - X` production benchmark for CI-visible recursion coverage.
- Obtained the requested Claude Opus second opinion and addressed its reporting,
  regenerated-data, coverage, invariant-checking, and error-message findings.
- Passed `lake build`, `HexConformance`, the compiled profile checks, and
  `check_benches_mathlib_free.py`. The new fixed benchmark passes `verify`.

## Current frontier

The implementation, proofs, refreshed measurements, and documentation are
staged and ready for review on branch `issue-8888`.

## Next step

Commit and push the branch, open the pull request, monitor exact Actions job
states and failed logs, then enable squash auto-merge once CI is green.

## Blockers

None. The aggregate local `hexberlekamp_bench verify` cannot run its existing
FLINT comparator registrations because `python-flint` is absent locally; the
new complete-factor benchmark itself reports `[ok]`, and CI installs FLINT.
