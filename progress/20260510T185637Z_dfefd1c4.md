## Accomplished

Claimed #2916 and advanced the tail-cross square-free proof prerequisites in
`HexPolyFp/SquareFree.lean`.

Added:

- `pthRoot_dvd_self_of_derivative_zero`, proving the formal `p`th root divides
  its source in the derivative-zero branch.
- `yunFactors_repeated_dvd_repeated_of_acc` and nil specialization, proving
  the final repeated component emitted by `yunFactors` divides the incoming
  repeated polynomial.
- A right-side Yun quotient coprimality helper plus a packaged
  current-vs-repeated coprimality theorem from the existing common-divisor
  provider.

Verified:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`

The `HexPolyFp/SquareFree.lean` `sorry` count remains the pre-existing three
holes; this session did not add a new hole or banned marker.

## Current frontier

The arbitrary `yunFactors_squareFreeAuxRev_tail_cross_coprime` statement is
still too strong. The remaining proof should be narrowed around reachable
states and should package the missing divisibility bridge from factors emitted
by recursive `squareFreeAuxRev (pthRoot loop.2)` back to the repeated component
`loop.2`.

## Next step

Prove a focused tail-factor divisibility lemma for the pth-root descent, then
use the new current-vs-repeated coprimality helper to finish the narrowed cross
theorem.

## Blockers

None technically blocking, but the remaining cross theorem is a larger proof
slice than this session could safely finish after orienting.
