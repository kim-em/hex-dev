# Worker session: HO-21 partial (rollback) + decomposition

## Accomplished

- Claimed HO-21 (#3669): "HexBerlekamp: rollback done_through 5 → 3
  and wire FLINT gating comparators".
- Assessed scope: deliverables 2–4 (wire two `gating` FLINT comparators,
  densify ladders, regenerate `reports/hex-berlekamp-performance.md`)
  require committed benchmark hardware time and substantial new
  `setup_fixed_benchmark` registrations following the `HexLLL/Bench.lean`
  per-rung-fixed pattern. Decomposed the wire+report bundle into a
  successor sub-issue.
- Filed #3712 ("HexBerlekamp: wire FLINT gating comparators and
  regenerate report — HO-21 part 2") with the deliverables 2–4 scope,
  citing the shared FLINT driver (`scripts/oracle/flint_bench_driver.py`,
  `Hex.BenchOracle.Flint.runOp`) from HO-20 (#3668) as the wiring
  surface and naming `HexLLL/Bench.lean`'s
  `runFpylllFirstShortVector*` / `runIsabelle*` registrations as the
  canonical per-rung-fixed shape.
- Left a `Decomposed into #3712` breadcrumb on HO-21 so the planner
  can close the parent with a forward link after deliverable 5
  (conditional re-promotion, already specified by the HO as a separate
  follow-up PR) is also filed.
- Landed HO-21 deliverable 1 (rollback): `libraries.yml`
  `HexBerlekamp.done_through: 5 → 3`. `scripts/check_phase4.py` and
  `scripts/check_dag.py` both pass; `scripts/status.py` now reports
  `HexBerlekamp -> Phase 4 (waiting on HexPolyFp.done_through >= 4)`,
  the correct post-#3667 state until the gating comparators are wired.
  `lake build HexBerlekamp` still succeeds (Phase-5 proof work
  remains in the code; only the gate field moves).

## Current frontier

HO-21 deliverable 1 (rollback) is locally complete and ready to land
as the parent's `--partial` PR. The sub-issue #3712 carries
deliverables 2–4. The HO-21 follow-up for deliverable 5 (conditional
re-promotion) is already described in the parent issue body and will
land after #3712 produces the gating-goal verdicts.

## Next step

Push the rollback commit and open `coordination create-pr 3669
--partial` so HO-21 routes to `replan` for the planner to close with
a forward link to #3712 once #3712 has scope/title that fully covers
the remaining deliverables.

## Blockers

None.
