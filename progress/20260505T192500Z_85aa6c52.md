# 2026-05-05 19:25 UTC — work session (85aa6c52)

## Accomplished

Claimed #2244 (`human-oversight`) and discharged the lone remaining
`axiom` in `HexConway/Basic.lean`:

* Switched `import HexBerlekamp` to `import HexBerlekamp.RabinSoundness`
  to side-step the pre-existing `HexBerlekamp` umbrella `dyld: missing
  symbol called` precompile failure when verifying locally.
* Added `prime_two : Hex.Nat.Prime 2` and the corresponding
  `instPrimeModulusTwo : ZMod64.PrimeModulus 2` so `Berlekamp.rabinTest_imp_irreducible`
  is reachable for `(p = 2)` entries.
* Added `cert_2_1 : Berlekamp.IrreducibilityCertificate` (powChain
  `#[1, 1]`, empty bezout) and proved
  `cert_2_1_linear_check` by `decide`. The (2, 1) entry — `X + 1`
  over `F_2` — has no maximal proper divisors of `n = 1`, so the
  Bezout array is empty and the linear cert check reduces in the
  kernel.
* Discharged the existing `sorry` at
  `luebeckConwayPolynomial_2_1_irreducible` by composing
  `Berlekamp.rabinTest_imp_irreducible`,
  `Berlekamp.checkIrreducibilityCertificateLinear_rabinTest`, and
  `cert_2_1_linear_check`.
* Converted `axiom luebeckConwayPolynomial?_irreducible` to a
  sorry-stubbed `theorem` so the axiom is removed (per the project
  ban from #2237) while the remaining 35 per-entry witnesses are
  tracked grep-ably.

Verification run:
* `lake env lean HexConway/Basic.lean` (only expected sorry warning).
* `lake build HexBerlekamp.RabinSoundness` (clean, modulo
  pre-existing sorries on lines 459 and 470).
* `python3 scripts/check_dag.py`.
* `git diff --check`.
* `rg "^axiom |^private axiom " HexConway/Basic.lean` returns no hits.

The full `lake build HexConway` umbrella build still fails at the
pre-existing `HexBerlekamp` precompile dyld step (not introduced by
this session).

## Current frontier

The cert-based pattern is established for the (2, 1) entry. The
remaining 35 table entries (primes 2/3/5/7/11/13 × degrees 1–6, minus
(2, 1)) still need per-entry irreducibility witnesses to fill the
sorry on `luebeckConwayPolynomial?_irreducible`.

Existing tracker issue **#2286** already covers this work in detail,
including the foundational `rabinTest`-evaluator dependency in
**#2285**. A duplicate sub-issue I opened earlier in the session
(#2346) was closed in favor of #2286.

## Next step

Land the foundational evaluator from #2285, then revive #2286 to
sweep the remaining 35 entries via the cert pattern committed here.

## Blockers

None for this session. The pre-existing `HexBerlekamp` umbrella
`dyld: missing symbol called` precompile failure is orthogonal to
this work and noted in earlier progress entries.
