# 2026-05-04T07:41:53Z — HexGfqRing python-flint oracle (#2002)

## Accomplished

- Added a `gfqring` JSONL fixture kind to `scripts/oracle/common.py`
  carrying `(p, modulus, a, b, c, n)` in a single record, with
  matching schema validation.
- Added `Hex.Conformance.Emit.emitGfqRingFixture` to emit those
  records from Lean.
- Created `HexGfqRing/EmitFixtures.lean` with seven fixtures spanning
  `p ∈ {5, 7, 11}` and modulus degree `d ∈ {2, 4, 6}`, including a
  zero-operand edge case at `p=5, d=4`.  Each case emits `add`,
  `mul`, `nsmul`, and `reduce` results, with `nsmul` exercising the
  scalar baked into the fixture and `reduce` exercising an unreduced
  polynomial of degree at least the modulus degree.
- Created `scripts/oracle/gfqring_flint.py` cross-checking each op
  through `flint.nmod_poly(p)` modulo the polynomial modulus.
- Wired `hexgfqring_emit_fixtures` into `lakefile.lean` and added
  build / cross-check / oracle steps to the `oracle-pyflint` job in
  `.github/workflows/conformance.yml`.
- Committed `conformance-fixtures/HexGfqRing/gfqring.jsonl` as the
  regression sample.

## Verification

- `lake build hexgfqring_emit_fixtures` green.
- `python3 scripts/oracle/gfqring_flint.py --check` → 28 case(s)
  checked, 0 failure(s).
- `lake exe hexgfqring_emit_fixtures | python3
  scripts/oracle/gfqring_flint.py` → same.
- `python3 scripts/oracle/{poly,matrix,berlekamp,bz}_flint.py
  --check` all still green (regression check on `common.py`).
- `python3 scripts/check_dag.py`, `python3 scripts/check_phase4.py`,
  `python3 scripts/conformance_targets.py --check` all green.

## Current frontier

- Issue #2002 closed by this PR.

## Next step

- Sibling oracles for HexGfqField (#2003) and HexGfq (#2004).  Both
  can reuse the new `gfqring` schema or build their own, depending on
  what python-flint's `fq_default` / `fq_nmod_ctx` exposes.

## Blockers

- None.
