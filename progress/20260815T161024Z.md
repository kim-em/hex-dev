# BKLNW ladder conformance repair

## Accomplished

- Replaced the smaller-ladder mutation guard's accidental call to the
  schema-1 power-433 planner with the actual schema-2 ladder planner.
- Added a positive control over all ten smaller source rows requiring the
  ladder planner to succeed with a proof-payload draft, and a paired negative
  control requiring every decoded mutation to fail with no drafts. Removing
  the ladder validity branch makes the negative control fail.
- Reworded both interval SPECs to state the current eleven power and twenty-two
  exponential declarations without historical count commentary.
- Clarified that runtime ladder acceptance authenticates an exact source row
  and its metadata, while the kernel theorem consumes the checked all-natural
  inequality; no mutation is claimed to isolate the endpoint-pinned final
  conjunct.
- Rebuilt the focused and retained PNT targets and reran source inventory,
  static, trust, Mathlib-free, freshness, diff, and banned-token checks.

## Current frontier

PR #9288 contains the complete BKLNW interface acceptance slices plus the
review-required conformance and documentation repairs.

## Next step

Push the repaired head, verify the exact PR edge, and bind replacement CI to
that head for final independent review.

## Blockers

None.
