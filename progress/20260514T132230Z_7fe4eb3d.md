# 2026-05-14 13:22 UTC — work session 7fe4eb3d (issue #3994)

## Accomplished

- Claimed #3994 (HexGramSchmidt: assemble scaledCoeffArrayLoop Cramer
  no-pivot invariant from step correspondence) and oriented on the
  parent goal: prove that the lower-triangle storage of
  `scaledCoeffArrayLoop` records the no-pivot fraction-free
  Bareiss-style value obtained by applying the same update sequence to
  `GramSchmidt.scaledCoeffMatrix b i j hji`.
- Mapped the proof chain required to discharge the parent:
  (A) lift the single-step `stepMatrix` correspondence to a full-loop
      array→`Matrix.noPivotLoop` sync in the lower-triangle (off-diagonal)
      case;
  (B) a bordered-minor `noPivotLoop` sync analogous to the
      `noPivotLoop_sync_leadingPrefix_aux` from PR #4000, identifying
      the full-Gram trajectory's `(i, j)` entry with the `(last, last)`
      corner of a `noPivotLoop` trajectory on
      `borderedMinor (gramMatrix b) j _ ⟨i, _⟩ ⟨j, _⟩`;
  (C) a transpose argument using symmetry of the Gram matrix to
      connect the natural-orientation bordered minor with the
      `scaledCoeffMatrix b i j hji = borderedMinor (gramMatrix b) j _
      ⟨j, _⟩ i` orientation — without going through `bareiss_eq_det`,
      which is explicitly out of scope.
- Verified that (A) is essentially the off-diagonal analog of the
  in-flight #4015 / PR #4026 `scaledCoeffArrayLoop_diag_matches`
  (251 lines of dense state-relation proof), so (A) alone is roughly
  one session's worth of work; combined with (B) and the careful
  encoding of singular zero-tail behaviour, the parent is at least
  two sessions.
- Decomposed #3994 into:
  - #4027 (HexGramSchmidt: prove scaledCoeffArrayLoop no-pivot
    lower-triangle sync) — sibling to #4015, blocked on #4015 closing.
  - #4028 (HexMatrix: prove `noPivotLoop` bordered-minor sync at
    trailing-block entry) — blocked on #4027.
- Left the `Decomposed into #4027, #4028` breadcrumb comment on #3994
  and skipped the claim, routing the parent through replan triage.

## Current frontier

Parent #3994 is now in `replan` with sub-issues #4027 (blocked on
#4015) and #4028 (blocked on #4027) covering the two large independent
proof pieces. The remaining Gram-symmetry transpose hop will be a
small successor to land once both sub-issues close.

## Next step

Wait for #4015 / PR #4026 to merge, then claim #4027 (which mirrors
the proven diagonal sync pattern). After #4027 lands, #4028 follows.
Once both close, a small follow-up issue can land the symmetry hop
and rewrite `scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss`.

## Blockers

- #4027 depends on #4015 (in flight as PR #4026).
- #4028 depends on #4027.

## Verification

No code changes this session — orientation and decomposition only.
- `coordination orient` confirms #3994 is now `replan`, #4027 / #4028
  are open and correctly blocked.
- `git diff --check` ✓ (no working-tree changes).
