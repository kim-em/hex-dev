# Progress — 20260614T204436Z (session ecfc3a69, feature)

## Accomplished

Issue #7339: added one-sentence Phase 6 docstrings to the ten
undocumented private rational-polynomial arithmetic helpers in
`HexPolyZ/Basic.lean` (lines ~2079–2241):

`rat_eq_add_mul_of_sub_eq_mul`, `rat_add_sub_add_right`,
`rat_sub_zero_right`, `rat_zero_mod_eq_zero`,
`rat_sub_self_right_add`, `rat_mul_left_remainder_delta`,
`rat_foldl_mulCoeffStep_select`, `rat_foldl_mulCoeffStep_outer`,
`rat_foldl_select_index`, `rat_coeff_mul_at_top`.

Doc-only change: no signatures, statements, proofs, names, attributes,
simp/grind annotations, declaration order, or imports touched. Each
docstring is backtick-led, one sentence, present tense, stating what the
lemma asserts and its role in the DensePoly Rat multiplication /
remainder arithmetic behind the rational Euclidean / square-free
machinery.

## Verification

- `lake build HexPolyZ.Basic`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexPolyZ/Basic.lean`: `28 0` (additions only).
- No banned vocabulary or em-dashes on added lines.

## Current frontier

This cluster (lines ~2079–2241) is now documented. Other Phase 6 doc
batches remain unclaimed (#7342 Determinant.lean, #7344 GramSchmidt
Int.lean).

## Next step

None for this issue.

## Blockers

None.
