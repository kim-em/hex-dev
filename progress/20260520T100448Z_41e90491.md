# Montgomery powMod API polish

Session: `41e90491` (work, issue #5671)

## Accomplished

Claimed #5671 after blocked directive claims and a race on #5670. Polished the
public `HexArith.powMod` theorem surface in
`HexArith/Montgomery/Context.lean`.

Added direct characterising lemmas for modulus zero, positive-exponent zero
base, and multiplication compatibility of bases. Marked the existing base
reduction theorem as a conditional `@[simp]` lemma, since it strictly removes a
modulus operation from the base and does not unfold the implementation.

## Current frontier

The Montgomery executable path is unchanged. The `powMod` API now has a tighter
set of wrappers around `powMod_eq` for common downstream uses.

## Next step

Create the PR for #5671. The adjacent Phase 6 item #5672 remains a review of
`HexPoly/Dense.lean`; #5670 was claimed by another agent during this session.

## Blockers

None for #5671.

Verification completed:

- `lake build HexArith.Montgomery.Context`
- `lake build HexArith`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexArith/Montgomery/Context.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

The issue-requested whole-diff forbidden-token scan is noisy because
`.claude/CLAUDE.md` was already modified outside this session and contains the
project policy text. That file was left untouched and excluded from the commit.
