**Accomplished**

Reviewed commit 8eea2eea for issue #8530, focusing on the level-aware budget arithmetic, smart recombination result changes, trace counters, and the two proof-site updates.

**Current frontier**

The main implementation concern is that `levelAwareSubsetBudget` is applied only by the public wrapper. Recursive quotient searches still inherit an arbitrary remaining budget, so a later decline after a successful peel can still burn partway into a quotient size level.

**Next step**

Decide whether issue #8530 requires level-boundary decline for every recursive quotient search. If so, cap each recursive `scaledRecombinationSmartAux` entry by the current local-factor count rather than only the initial wrapper call.

**Blockers**

None.
