**Accomplished**

- Claimed #1972 after the human-oversight issue #1968 was already claimed by another agent.
- Added `HexLLL/Conformance.lean` coverage for the downstream `lll.firstShortVector` and `lll.shortVectors` surfaces.
- Added a deterministic BZ-style integer coefficient basis with one row per lifted local factor and guards for its row shape.
- Checked that the short-vector wrappers expose row 0 and the ordered `toArray` view of `lll` on that basis.

**Current frontier**

- HexLLL conformance now names and typechecks the BZ-facing short-vector API without changing executable LLL code or Phase 5 proofs.
- Verification passed:
  - `lake build HexLLL.Conformance`
  - `lake build HexLLL`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME" HexLLL/Conformance.lean`

**Next step**

- Add the corresponding Phase 4 benchmark coverage for `lll.firstShortVector` under #1973.

**Blockers**

- None for #1972.
