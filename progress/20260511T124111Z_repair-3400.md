# 2026-05-11 12:41 UTC — repair PR #3400

## Accomplished

Claimed repair for PR #3400 and diagnosed the failing Ubuntu `build` check.
The PR is mergeable; the failed CI run died during the bench smoke gate when
`scripts/oracle/setup_lll_isabelle.sh` hit transient Zenodo 504 responses while
fetching the LLL Isabelle oracle archive.

Verified locally that the PR code path and the failed oracle bench are healthy:

- `python3 scripts/check_dag.py`
- `python3 scripts/check_phase4.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/TerminationBound.lean`
- `scripts/oracle/setup_lll_isabelle.sh`
- `lake build HexBerlekampZassenhausMathlib.TerminationBound`
- `lake build`
- `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)" lake exe hexlll_bench list`
- `HEX_LLL_ISABELLE_SVP="$(scripts/oracle/setup_lll_isabelle.sh)" lake exe hexlll_bench verify`

## Current frontier

No Lean repair was needed. The PR's Mignotte precision helper builds, and the
bench that failed remotely passes once the external oracle archive fetch
completes.

## Next step

Push this progress-only repair commit to retrigger CI, then mark the PR
salvaged.

## Blockers

None.
