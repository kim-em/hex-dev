# 2026-05-21T06:15Z — work issue #5732

## Accomplished

Claimed #5732 and assessed the requested de Bruijn-Springer analytic source
theorem. Mathlib does not currently expose a Grace-Walsh/de Bruijn-Springer
composition theorem to invoke, so the full positive-degree analytic proof is
too large for one session.

Added a partial derivative-free open-domain source surface in
`HexPolyZMathlib/SchurSzego.lean`:

- `rootsStrictlyOutsideRadiusCount` for strict exterior root counts over
  `Polynomial.roots`, preserving multiplicity.
- `openExteriorRootCountDominatedFrom`,
  `schmeisserCompositionOpenZeroControl`, and
  `graceWalshSzegoOpenZeroControlAtDegree`.
- Degree-zero witness
  `graceWalshSzegoOpenZeroControlAtDegree_zero`.
- Wrappers from the open source hypothesis to strict radius count domination.

Verification:

- `lake build HexPolyZMathlib.SchurSzego`
- `lake build HexPolyZMathlib`
- `git diff --check`
- `git diff -- HexPolyZMathlib/SchurSzego.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

Also ran the `second-opinion` skill on the Lean diff; it recommended adding
the open degree-zero witness, which is now included.

## Current frontier

The PR is partial. The API now has an explicit strict/open multiplicity-count
surface parallel to the existing closed-count surface, but it does not prove
the positive-degree de Bruijn-Springer/Grace-Walsh-Szego analytic theorem.

## Next step

Prove or further decompose the positive-degree analytic source theorem behind
`graceWalshSzegoOpenZeroControlAtDegree n`, then derive the closed-radius
wrapper needed by #5734.

## Blockers

The remaining blocker is mathematical/formalization depth: the analytic
de Bruijn-Springer source theorem is not available in Mathlib and still needs
a real proof plan or further decomposition. The worktree still contains an
unrelated pre-existing `.claude/CLAUDE.md` modification, left untouched.
