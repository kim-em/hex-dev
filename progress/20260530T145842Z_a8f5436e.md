# Progress — 2026-05-30T14:58Z — session a8f5436e (#5773 partial)

## Accomplished

Worked on directive #5773 (HO-5b): "rewrite
hex-berlekamp-zassenhaus-performance.md against hex/isabelle ratio
verdicts". Three prior partial PRs (#5822, #5826, #5832) had landed
the per-rung Isabelle comparator registrations and ratio ladders for
the split-family schedule (public / fast / slow combinator). This
session extends the comparator surface to the second scientific
schedule (degree/height matrix).

- Built `bz_isabelle` from cached AFP/Berlekamp_Zassenhaus heap on
  carica (download + extract + extract Hex_BZ_Export + GHC compile
  totalled ~2 minutes, far below the multi-hour first-run estimate
  cited by earlier sessions; the heap was already in
  `~/.isabelle/Isabelle2025-2/heaps/...`).
- Added eight new
  `setup_fixed_benchmark runIsabelleDegreeHeight{D}x{H}Checksum`
  registrations in `HexBerlekampZassenhaus/Bench.lean`, one per rung
  of `degreeHeightSchedule = #[3002, 4002, 4008, 5008, 6032]` and the
  smaller-degree `slowDegreeHeightSchedule = #[1002, 2002, 3008]`.
  Each uses
  `expectedHash := checksumCanonicalLeanFactorization (factor input)`
  to enforce factor-multiset agreement against Lean at elaboration
  time. All eight tagged `scheduled-hardware`. Commit `0368ffe9`.
- Ran a 3-trial Lean+Isabelle sweep at commit `0368ffe9` pairing the
  three parametric Lean targets (`runFactorDegreeHeightChecksum`,
  `runFactorFastDegreeHeightChecksum`,
  `runFactorSlowDegreeHeightChecksum`) with the eight new Isabelle
  registrations. Total sweep time ~2.5 min. Load avg at start
  `4.28/5.29/4.89`. Per-rung spread `0.55–6.41 %` on the Lean
  parametrics, `<1 %` on the Isabelle fixed targets. All
  `expected hash: matches` checks passed, confirming factor-multiset
  agreement.
- Added §Degree/height scaling ladder, §… fast path, §… slow backstop
  to the report, plus an explanatory paragraph in §Comparator Ratios
  noting that the Isabelle medians here are fresh (not borrowed from
  the prior `2f4ef93-split-ladder.json`), and a clarification about
  why the parametric-Lean and fixed-Isabelle observed hashes don't
  directly compare even when the factorisations agree.
- Updated §Concerns: replaced the "degreeHeightSchedule /
  slowDegreeHeightSchedule not yet wired" bullet with a summary
  noting six parametric Lean targets are now covered across the two
  scientific schedules. Remaining surface (`precisionLocalSchedule`,
  HO-2 adversarial singletons, `fallbackProbeSchedule`) is documented
  as the next worker's scope.

Headline ratios:

- `runFactorDegreeHeightChecksum` @ `6 × 32`: adj `0.4323` (Lean 2.31× faster) — met
- `runFactorFastDegreeHeightChecksum` @ `6 × 32`: adj `0.4210` (Lean 2.38× faster) — met
- `runFactorSlowDegreeHeightChecksum` @ `3 × 8`: adj `0.0233` (Lean 42.89× faster) — met

The `6 × 32` rung is the tightest gating-goal margin currently in
the report (`Lean 2.31× faster`); the public combinator is moving
toward but well clear of `1×` on this matrix's range.

Export artefact:
`reports/bench-results/hex-berlekamp-zassenhaus-0368ffe9-degree-height.json`,
SHA-256 `5815f944cbade82ebadeadf13e53cb59c6269edc2c4c996433ba72341d637241`.

## Current frontier

Six of the registered scientific bench targets now have ratio
verdicts (public / fast / slow combinator on the split family AND on
the degree/height matrix). The remaining ones —
`runFastPathPrecisionLocalChecksum`, the five HO-2 adversarial
singletons, and `runFactorFallbackProbeChecksum` — still need new
`setup_fixed_benchmark runIsabelle*Checksum` registrations on their
respective prepared inputs and a bench run.

The `bz_isabelle` binary is now built in this worktree
(`.cache/oracles/bz-isabelle/wrapper/bz_isabelle`); a follow-up
session on the same host can reuse it without the multi-step setup.

## Next step

A successor session can:

1. Add `runIsabellePrecisionLocalParam{N}Checksum` registrations for
   the six rungs of `precisionLocalSchedule` and pair with
   `runFastPathPrecisionLocalChecksum`. Note that the fast-path
   setup target uses `checksumOptionFactorization` plus
   precision/local-factor mixing; the pairing semantics need a
   slightly different `expectedHash` than the canonical-factor
   variant.
2. Add the five HO-2 singleton Isabelle counterparts on the pinned
   `n = 0` fixed inputs (`advX4Plus1`, `advQuadSqrt2Sqrt3`,
   `advPhi15`, `advSwinnertonDyerSD3`) plus the two fast-path setup
   variants.
3. Decide whether `runFactorFallbackProbeChecksum` ratios are worth
   wiring — its inputs overlap with the split family at larger `n`.

## Blockers

None for this PR. Full discharge of #5773 still requires the three
remaining surfaces above. Build/verify clean on this branch: `lake
build HexBerlekampZassenhaus` (143 jobs, only pre-existing sorry
warnings); `lake exe hexbz_bench verify` (18 smoke targets pass).
