# Session d9bed085 — HexGramSchmidt replacement-column RHS bridge (#3778)

## Accomplished

- Checked the unclaimed queue and attempted the highest-priority available work.
- Released stale or blocked issues #3087, #3239, and #3741 with concrete
  dependency-state reasons.
- Claimed #3778 and added two private wrappers in
  `HexGramSchmidt/Int.lean`:
  - `dot_basisPrefixProjection_eq_castIntGram`, rewriting the prefix-projection
    dot product to the cast original Gram entry.
  - `scaledCoeffMatrix_replacementColumn_solve_intGram`, strengthening the
    replacement-column solve theorem to expose that cast original Gram entry
    directly.
- Verified:
  - `lake build HexGramSchmidt.Int`
  - `lake build HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Int.lean`

## Current frontier

The Cramer replacement-column RHS now has a local theorem in the exact
cast-original-Gram shape requested by #3778. Downstream determinant-linearity
assembly can use `scaledCoeffMatrix_replacementColumn_solve_intGram` instead of
rewriting through the projection vector.

## Next step

Continue with the downstream Cramer determinant assembly issues that consume
the strengthened solve theorem.

## Blockers

None for #3778. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification that this session did not touch.
