# HexIntFactor certified generalized divisor sums

- Date/time: 2026-08-26 02:23:12 UTC
- Session type: feature
- Replaced divisor enumeration in `sigma` with the product of exact geometric
  sums over the certified prime-power entries.
- Preserved the `k = 0` route as a direct call to `numDivisors`, avoiding the
  degenerate geometric quotient.
- Proved each closed-form entry equals its finite power sum, proved power sums
  over the Cartesian divisor expansion factor by entries, and retained
  `sigma_eq_sum` against the ascending public divisor enumeration.
- Added native and kernel conformance cases for units, mixed factors, prime
  powers, `k = 0`, `k > 1`, a 65-bit result, and a subject with more than a
  trillion divisors.
- Extended the independently checked PARI fixtures from `σ₁` to `σ₀`, `σ₁`,
  and `σ₂` over every committed divisor-function subject.
- Added controlled scientific ladders for growing prime-power exponents and
  growing certified factor counts. The committed runs are consistent with the
  declared soft-linear big-integer and factor-count models.
- Updated the SPEC with the exact formula, its checked-primality precondition,
  the no-enumeration route, and the benchmark family.
- Verification completed: `lake build HexIntFactor HexIntFactorMathlib
  HexConformance`; `lake exe hexintfactor_bench list`; `lake exe
  hexintfactor_bench verify`; the PARI oracle driver; and both new scientific
  `run` targets.
- Both factor libraries remain free of `sorry`, `axiom`, and `native_decide`.
- Remaining directive work is tracked by the rest of the #9619 publication DAG.
