# Session c546ccd6 — 2026-05-14T08:56Z (feature)

## Accomplished

- Triaged unclaimed `feature` queue. Skipped #3969 (BZ `isIrreducible_iff`
  capstone) with stale-dependency reason: the original `depends-on: #3953`
  closed via decomposition into #3986 + #3987, and the actual factor-entry
  irreducibility invariant lives in still-open #3987. Added
  `depends-on: #3987` so the planner picks the new dep up automatically.
- Decomposed #3964 (parent: full scaledCoeffArrayLoop Cramer no-pivot
  invariant) into two sub-issues:
  - #3993 — step-level correspondence `stepScaledRows` ↔ `Matrix.stepMatrix`
    under exact divisibility (this session).
  - #3994 — successor fuel/state invariant lifting the step correspondence
    to the full loop and connecting to `scaledCoeffMatrix`. Depends on #3993.
- Claimed and executed #3993. Added two new private lemmas in
  `HexGramSchmidt/Int.lean` (immediately before
  `end StepScaledRowsBookkeeping`):
  - `getArrayEntry_stepScaledRows_matches_stepMatrix` — per-entry
    correspondence parameterised over a generic matrix view and a
    per-call divisibility hypothesis. Case-splits on whether `(i, j)` is
    a trailing-block update, a pivot-column zeroing, a strictly-left
    preserved entry, or a row at or above the pivot.
  - `rowsToMatrix_stepScaledRows_eq_stepMatrix_of_dvd` — matrix-level
    bridge derived from the per-entry version. Convenient shape for the
    successor full-loop invariant proof.

## Current frontier

The single-step bridge between the array-storage `stepScaledRows` (which
uses Lean `Int` `/`) and the matrix-storage `Matrix.stepMatrix` (which
uses `Matrix.exactDiv`) is now in place under a `prevPivot ∣ Bareiss-numerator`
hypothesis. The divisibility hypothesis is what the caller (full-loop
invariant proof, #3994) will discharge from Bareiss bordered-minor
identities.

## Next step

Successor #3994: lift the single-step bridge to the whole
`scaledCoeffArrayLoop` by induction on fuel, matching its matrix
trajectory against `Matrix.noPivotLoop`, and identify
`scaledCoeffMatrix b i j hji =
  Matrix.borderedMinor (Matrix.gramMatrix b) j.val hj_lt_n
    ⟨j.val, hj_lt_n⟩ i`
to read the (i, j) entry as the Cramer Bareiss determinant.

## Blockers

None for #3993. #3969 (the parent capstone session opened with) is
correctly blocked on #3987's still-pending factor-entry irreducibility
invariant.
