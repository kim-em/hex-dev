# Progress: #7000 — HexArith.Int ExtGcd Bezout-projection docstrings

(work session; also recorded a premise-gap diagnosis for #6989)

## Accomplished

### #7000 (landed)

Added concise docstrings to the two undocumented public integer Bezout-projection
theorems in `HexArith/ExtGcd.lean`:

- `HexArith.Int.extGcd_bezout_proj` — projection form of the Bezout certificate,
  stated against the returned gcd component `(extGcd a b).1`.
- `HexArith.Int.extGcd_bezout_gcd` — Bezout certificate stated directly against
  `Int.gcd a b`.

Docstring-only; no statements, proofs, attributes, or imports changed. Style
imitates the neighbouring `extGcd_fst` / `extGcd_bezout` docstrings.

Verification: `lake build HexArith.ExtGcd`, `lake build HexArith`,
`python3 scripts/check_dag.py`, `git diff --check` all pass. No
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.

### #6989 (skipped → replan, premise gap)

Claimed and premise-checked #6989 (migrate scaled search proof helpers to
`liftedRecoveryCandidate`). `HexBerlekampZassenhausMathlib/Basic.lean` is red on
`main` (11 elaboration errors; the layer is not CI-built). All 11 are the same
family: a `RepresentsIntegerFactorAtLift` / `RecoveredAtLift` witness fed to a
recovery lemma still expecting the old scaled-congruence hypothesis. The issue's
prescribed route (`RecoveredAtLift.candidate_eq_of_bound`) needs a Mignotte bound
on the hidden `monicFactor`; that bound is not establishable — a generic valid
witness (`monicFactor := (c + p^k) * factor` for monic core) has unbounded
coefficients, the witness is opaque (`Nonempty`), and the consumer only bounds
integer divisors of `core` (which `monicFactor` is not). #6990 added only the API
that *consumes* the bound (`candidate_eq_of_bound` is used solely inside the
never-called `existsUnique_recoveringLiftedFactorSubset..._of_bound`); nothing
*produces* it. Posted a detailed diagnostic and `coordination skip`ped for replan.

## Current frontier

#7000 complete. #6989 returned to the queue with a concrete missing-prerequisite
diagnosis.

## Next step

- #6989 needs a new prerequisite issue: a lemma constructing a canonical centered
  `RecoveredAtLift` witness whose `monicFactor` is Mignotte-bounded by
  `defaultFactorCoeffBound core` (or a structural enrichment carrying the bound),
  then re-point #6989 to depend on it. The 11 sites + the lifted prefix-none chain
  are one coupled PR (no green seam).

## Blockers

- #7005 (monicGcd swap) blocked: prerequisite PR #7003 still open, CI pending.
- #6376 effectively blocked on the same square-free chain.
