# HexPolyZ quotient gcd absorption

## Accomplished

- Claimed feature issue #2127.
- Added `rat_dvd_mul_right`, `rat_common_divisor_quotient_derivative_dvd_repeated`, and `rat_quotient_derivative_gcd_dvd_repeated` in `HexPolyZ/Basic.lean`.
- Proved that the computed gcd of `quotientRat` and its derivative divides `repeatedRat`, using `rat_div_gcd_mul_reconstruct`, `rat_derivative_mul`, and `DensePoly.dvd_gcd`.
- Created successor issue #2152 for the residual quotient gcd equals `1` proof and added it as an extra dependency of #2129.
- Verified `lake build HexPolyZ.Basic`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

`HexPolyZ/Basic.lean` now has the key Rat absorption helper needed before the final quotient-by-gcd square-free lemma.

## Next step

Prove #2152: use the absorption helper plus a multiplicity/unit argument to show `DensePoly.gcd quotientRat (DensePoly.derivative quotientRat) = 1`.

## Blockers

- The targeted downstream `primitiveSquareFreeDecomposition_squareFreeCore` `sorry` remains; #2152 and then #2129 are needed before it can be removed cleanly.
