# 2026-05-07 04:32 UTC — work session (4772e5e5)

## Accomplished

Claimed #2531 and narrowed the scaled coefficient lower bridge in
`HexGramSchmidt/Int.lean`.

The broad `scaledCoeffRows_lower_eq_coeffs` proof is no longer a direct
`sorry`; it now composes three focused facts:

- the executable `scaledCoeffRows` below-diagonal entry equals the Bareiss
  determinant of `GramSchmidt.scaledCoeffMatrix`;
- Bareiss agrees with the Leibniz determinant on that Cramer matrix;
- the Leibniz determinant of the Cramer matrix gives
  `gramDet b (j + 1) * coeffs[i,j]`.

Verification completed:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check HEAD~1..HEAD`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Int.lean`

The second-opinion wrapper was attempted but produced no output and was stopped
after stalling.

## Current frontier

The public theorem `scaledCoeffs_eq` remains unchanged and has no theorem-body
`sorry`. The remaining lower-bridge work is split into the executable array-loop
invariant and the Cramer determinant identity.

## Next step

Prove `scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss` by relating
`scaledCoeffArrayLoop` entries to the Bareiss update state, or prove
`scaledCoeffMatrix_det_eq_gramDet_mul_coeffs` from the reconstruction and
orthogonality APIs.

## Blockers

No technical blocker. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification not touched by this session.
