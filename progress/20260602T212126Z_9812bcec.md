# 20260602T212126Z work #6371

## Accomplished

- Claimed #6371 after directive #2567 was already claimed and directive #2564
  was dependency-blocked.
- Added
  `ExecutableBadVectorWitness.l2norm_product_lt_divisor_of_l2norm_bounds` in
  `HexBerlekampZassenhausMathlib/TerminationBound.lean`.
- The new theorem turns separate input/auxiliary l2-norm bounds plus a strict
  cap-arithmetic bound into the exact Hadamard/l2norm strict inequality
  consumed by the BHKS bad-vector contradiction and actual-cap comparison
  surfaces.

## Current frontier

The actual-cap analytic leaf now has a generic witness-level arithmetic bridge
that can be instantiated with
`badVectorWitnessOfFactorFastCapLiftData`.  The remaining work is to supply
the concrete input and auxiliary l2-norm bounds and the final cap-arithmetic
strict bound for that witness.

## Next step

Instantiate the new witness-level lemma using
`auxiliaryPolynomial_l2norm_sq_le`, the input polynomial norm bound, and the
`BHKSBound.lean` cap estimates, then feed the resulting strict inequality into
`FactorFastCapLiftAnalyticComparison.ofBridgeData` or the named comparison
package expected by the final cap-separation assembly.

## Blockers

No technical blocker for this slice.  The strict actual-cap inequality is not
fully closed yet; this PR contributes the reusable product-comparison step
needed to close it.

## Verification

- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/TerminationBound.lean` found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
