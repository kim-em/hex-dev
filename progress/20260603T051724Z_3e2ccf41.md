# 20260603T051724Z work #6448

## Accomplished

- Claimed issue #6448 (BHKS recovery-tail packaging: derive
  `ForwardRecoveryInputs.ExpectedTrueFactors` from a successful
  `bhksIndicatorCandidates?` fold plus a monic recovery target) and
  landed the constructor plus its supporting array- and per-index-level
  lemmas across `HexBerlekampZassenhaus/Basic.lean` and
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- `Hex.bhksIndicatorCandidates?_size_eq` (public): extracts
  `candidates.size = indicators.size` from a successful fold by inducting
  on the underlying `bhksIndicatorCandidatesStep` accumulator.
- Per-candidate property lemmas at the `bhksIndicatorCandidate?` layer:
  `_leadingCoeff_nonneg` (from `normalizeFactorSign` being a fixed point on
  the candidate), `_primitive` (from `normalizeCandidateFactor_primitive` +
  `normalizeFactorSign_primitive`), and `_positive_degree` (from
  `shouldRecord` ruling out the unit-constant case after
  `Primitive + 0 ≤ leadingCoeff` collapses constants to `±1` via
  `Int.natAbs_eq_iff`). Supporting helper
  `normalizeCandidateFactor_primitive` proves primitivity of the inner
  factor whenever it is nonzero, using `primitivePart_primitive`,
  `primitivePart_eq_zero_of_content_eq_zero`, and
  `DensePoly.content_scale_neg_one`.
- Array-level (`bhksIndicatorCandidates?_*`) public lemmas for
  `dvd`/`primitive`/`leadingCoeff_nonneg`/`positive_degree`: thread the
  per-candidate facts through the existing
  `bhksIndicatorCandidates?_all_of_candidate` fold-all infrastructure.
- Per-index `getD` lemmas in the `BHKS.ForwardRecoveryInputs` namespace
  (`bhksIndicatorCandidates?_getD_dvd`, `_getD_primitive`,
  `_getD_leadingCoeff_nonneg`, `_getD_positive_degree`,
  `_getD_monic_of_monic`): convert the array-level
  `∀ factor ∈ candidates.toList, P factor` shape to the per-index
  `∀ i, i < indicators.size → P (candidates.getD i 0)` shape consumed by
  `ExpectedTrueFactors`, using the size identity plus
  `List.getElem_mem`.
- `monic_of_dvd_monic_of_leadingCoeff_nonneg`: a primitive integer
  divisor of a monic polynomial with nonnegative leading coefficient is
  monic. Composes `Hex.ZPoly.leadingCoeff_mul_of_nonzero` with
  `Int.eq_one_of_mul_eq_one_right` on the leading-coefficient identity
  `leadingCoeff g * leadingCoeff q = 1`.
- `BHKS.ForwardRecoveryInputs.expectedTrueFactors_of_monic`: the
  final constructor. Given any `ForwardRecoveryInputs f d` (which carries
  the executable `bhksIndicatorCandidates? = some expectedFactors` witness
  and `product_eq`) plus `DensePoly.Monic f`, packages all six
  `ExpectedTrueFactors` fields by routing through the per-index getD
  lemmas and the monic transfer.
- No executable definitions changed; no new `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.

## Current frontier

Both recovery-tail inputs at the `factorFastCapLiftData` cap are now
producible from a `ForwardRecoveryInputs` package and a monic target:
- `ExpectedTrueFactors` via `expectedTrueFactors_of_monic` (this PR).
- The per-canonical-class `RepresentsIntegerFactorAtLift` certificates
  are the remaining piece — sibling issue #6450 covers them.

Once #6450 lands, the two outputs compose with the existing closed
constructor `CanonicalRecoveryTailInputs.ofCanonicalSupportRepresentations`
([Recovery.lean:3794](../HexBerlekampZassenhausMathlib/Recovery.lean#L3794))
to give a closed producer
`CanonicalRecoveryTailInputs.ofForwardRecoveryInputs` that feeds
`factorFast_terminates`.

## Next step

Either:
- Implement the sibling #6450 per-canonical-class
  `RepresentsIntegerFactorAtLift` constructor (the forward direction of
  `bhksIndicatorCandidate?_eq_some_of_mignottePrecision`), or
- Continue with the cap-side strict-arithmetic comparison (#6445).

## Blockers

None for this slice.

## Verification

- `lake build HexBerlekampZassenhaus` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed (exit 0).
- `git diff --check` passed.
- Added-line forbidden-token grep over `Basic.lean` and `Recovery.lean`
  found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
