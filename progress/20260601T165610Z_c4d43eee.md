# c4d43eee - work #6050

## Accomplished

- Skipped stale/blocked issue candidates #5525 and #5910 after their issue
  bodies and dependency comments showed they were not executable.
- Claimed #6050.
- Switched `HexGramSchmidt/Int.lean` `scaledCoeffArrayLoop` regular branches
  from `Matrix.stepArray` to the row-mutating `stepScaledRows` path.
- Changed `stepScaledRows` trailing updates to use `Matrix.exactDiv`, letting
  the row-mutating array/matrix correspondence match `Matrix.stepMatrix`
  directly without a separate exact-divisibility premise.
- Updated the lower-entry and diagonal loop proof plumbing to carry matrix
  shape invariants through `stepScaledRows`.
- Added fixed harsh-cubic Lean/Isabelle comparator evidence for `n = 45, 50,
  55` in `reports/hex-lll-performance.md` and
  `reports/bench-results/hex-lll-c4d43eee-step-scaled-rows-harsh-cubic.json`.

## Current frontier

The row-mutating scaled-coefficient update is in place and verified against
the existing public `HexGramSchmidt`/`HexLLL` surfaces. The committed report
artifact records matching Lean/Isabelle hashes at the requested harsh-cubic
rungs.

## Next step

Create the PR for #6050. Follow-up performance work can compare a broader
`runOfBasisHarshCubicChecksum` parametric rerun if a planner wants more than
the fixed Lean/Isabelle comparator slice requested here.

## Blockers

- No blocker for this slice.
- Pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and
  is not part of this work.
