## Accomplished

- Claimed feature issue #5129 (`HexArith Phase 6: add Barrett mulMod
  identity and commutativity lemmas`).
- Added three public lemmas in `HexArith/Barrett/Context.lean`,
  immediately after the existing `mulMod_zero_*` simp lemmas:
  - `mulMod_one_left` (simp): `ctx.mulMod 1 a = a` for `a < p`.
  - `mulMod_one_right` (simp): `ctx.mulMod a 1 = a` for `a < p`.
  - `mulMod_comm`: `ctx.mulMod a b = ctx.mulMod b a` for `a, b < p`.
- Identity lemmas route through `toNat_mulMod` and discharge via
  `UInt64.toNat_inj` plus `Nat.mod_eq_of_lt`, using `ctx.p_gt` to
  establish `(1 : UInt64) < p`. Commutativity routes through the
  same `toNat_mulMod` characterisation plus `Nat.mul_comm`.
- No unfolding of the executable Barrett implementation; no edits
  to `Reduce.lean`, conformance fixtures, Montgomery, or CI.

## Verification

- `lake build HexArith.Barrett.Context` — clean (26 jobs).
- `lake build HexArith` — clean (57 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — exit 0.
- `git diff HexArith/Barrett/Context.lean | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no matches.
- Greppable invariant: all three declarations present in
  `HexArith/Barrett/Context.lean`.

## Current frontier

Issue #5129 deliverables are complete. Ready to publish PR.

## Next step

Push branch and `coordination create-pr 5129`.

## Blockers

None.
