# Session 3b5023d0 — feat #4970

## Accomplished

* Closed #4970 by adding the abstract-bound (`_of_bound`) sibling of
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux` in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.
* The new private theorem
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
  replaces the existing inner-form `hprecision` premise on the squarefree
  core with the four-parameter abstract-bound block `(B', hcore_lc_le,
  hvalid, hprecision)`. The proof body mirrors the original verbatim
  with the single forward call to
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
  swapped for its `_of_bound` sibling (#4947), threading the new bound
  parameters.
* The original aux is rewired as a thin wrapper instantiating
  `B' := Hex.ZPoly.defaultFactorCoeffBound core` and discharging
  `hcore_lc_le` / `hvalid` via `defaultFactorCoeffBound_valid` paired
  with `Hex.DensePoly.leadingCoeff_eq_coeff_last`, mirroring the
  rewiring idiom from the sibling layers (#4935 / #4936 / #4938 /
  #4944 / #4947).
* Public umbrella
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
  signature unchanged; it continues to consume the rewired thin
  wrapper.

## Verification

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — matches
  `origin/main` baseline (same pre-existing Basic.lean whnf-timeout
  error set).
* `lake build HexBerlekampZassenhausMathlib` — matches baseline.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
  | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` — empty.

## Current frontier

A2 abstract-bound cascade now reaches one layer above the
`HexBerlekampZassenhausMathlib/Basic.lean` public bridge: from the
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
(#4944) → `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
(#4947) chain in `Basic.lean`, through the new aux `_of_bound` in
`IntReductionMod.lean`.

## Next step

Per #4970's "Out of scope" block: a follow-on planner cycle can file
the `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
sibling plus the public umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_of_bound`
as separate atomic issues. The public umbrella's discharge of
`hcomplete` via `reassemblyExpansionComplete_exhaustive_of_ne_zero`
must move to its `_of_bound` companion first.

## Blockers

None.
