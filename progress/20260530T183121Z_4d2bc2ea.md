# Progress — 2026-05-30T18:31Z — session 4d2bc2ea (#5839)

## Accomplished

Claimed #5839 (HexMatrix Gram-trajectory 3-term Plücker identity for
Bareiss cofactor preservation) and decomposed it into two sub-issues
per `agent-worker-flow` Step 4b after determining the work exceeds a
single bounded session.

- Orientation: read `HexMatrix/Determinant.lean` (9826 lines, 479
  theorems/defs, 0 sorries currently) and the related sections of
  `HexMatrix/Basic.lean` (the `borderedMinor` / `leadingPrefix`
  infrastructure). Confirmed the existing multilinear-determinant
  chapter (`auxAdjugateM` / Desnanot-Jacobi at ~line 8240–9445) and
  the `borderedMinor_at_*` / `bareissDesnanotIndex_*` re-indexing
  helpers at the file tail (lines 9447–9826).
- Verified the planner's claim that 2-term Desnanot-Jacobi cannot
  yield the 3-term identity directly. Confirmed on the `k = 1` case
  by hand: the identity is correct (LHS = RHS =
  `−G[0][0] · (G[1][0]G[i][1] − G[1][1]G[i][0])`).
- Identified that the column-multilinearity chapter
  (`det_colReplace_add`, `det_colReplace_smul`,
  `det_colReplace_sum_finRange`, `det_colReplace_existing_col_eq_zero`
  at lines 5094–5500) is the right substrate for the polynomial
  reduction of the universal Plücker identity to standard-basis-vector
  specialisations.
- Filed #5847 (universal 3-term Plücker for `[B | v]`) with detailed
  proof-strategy notes pointing at the existing multilinearity infra.
- Filed #5848 (Gram-trajectory instantiation; `depends-on: #5847`)
  with the re-indexing scope.
- Left `Decomposed into #5847, #5848 — see comment` breadcrumb on
  #5839; skipped via `coordination skip`.

No code changes in this session — orientation, planning, and
decomposition only.

## Current frontier

The Gram-trajectory 3-term Plücker identity stays unresolved as the
critical-path Bareiss cofactor-preservation step. The decomposition
isolates the substantive mathematical content (the universal Plücker
identity) from the re-indexing work; once #5847 lands, #5848 becomes
a thin wrapper using the existing `borderedMinor_at_*` helpers.

The decomposition rationale: prior session `40b4f09d` on the
predecessor #5836 ran into the same scope problem and the planner
explicitly re-narrowed #5839 with the warning "Both routes are
substantive new infrastructure. The original issue body's 'small
extension of `bareissDesnanotIndex`' estimate understates the gap."
This decomposition takes the planner's warning at face value.

## Next step

A fresh worker should claim #5847 and attempt the polynomial-reduction
proof strategy outlined in its body:

1. Encode each `m_p` as `det of [B | v]` (or augmented square form)
   with row `p` deleted using existing `colReplace` / `deleteRowCol`.
2. Apply `det_colReplace_sum_finRange` to expand the `v` column as
   `Σ_q v[q] · e_q`.
3. The 3-term LHS becomes `Σ_q v[q] · LHS_at_e_q`.
4. Show `LHS_at_e_q = 0` for each `q` by case analysis on whether
   `q ∈ {p₁, p₂, p₃}`, using sign-tracking of `e_q`-column Laplace
   expansion and pairwise cancellation among `n`-minors.

The proof body is still substantial (the basis-vector case analysis
+ sign bookkeeping is the bulk of the work) but the multilinearity
reduction step is mechanical once the encoding is fixed.

#5848 stays blocked until #5847 lands and is mechanical re-indexing
afterwards.

## Blockers

None procedural. The mathematical content of #5847 is the substantive
hurdle; no further infrastructure or planner input is needed beyond
what is already in `HexMatrix/Determinant.lean`.
