# 20260530T120931Z — d09b00cd

Session type: feature
Issue: #5803 HexGramSchmidt Phase 5: refactor BareissGramRowInvariant to
bundle explicit coefficients.

## Accomplished

- Replaced the existential `mem_span` field with a deterministic
  `coeff : Fin n → Vector Int n` field on `BareissGramRowInvariant`,
  with `coeff_supp` carrying the upper-triangular support equation
  and `entry_eq_dot` rephrased as a dot against
  `Matrix.rowCombination b (coeff i)`. Dropped all `Classical.choose`
  plumbing and the `noncomputable` annotations from
  `bareissGramRowInvariantCoeff`,
  `bareissGramRowInvariantStepCoeff`,
  `bareissGramRowInvariant_regular_step`,
  `bareissGramRowInvariant_noPivotLoop`, and
  `bareissGramRowInvariant_noPivotLoop_initial`.
- Reseated `bareissGramRowInvariant_initial` on the explicit identity
  basis (`Vector.ofFn fun k => if i = k then 1 else 0`); the entry
  equation uses `Matrix.IsRREF.rowCombination_single` directly.
- Updated `bareissGramRowInvariantCoeff_support` and
  `bareissGramRowInvariantStepCoeff_support` to read `hinv.coeff_supp`.
- `bareissGramRowInvariantCoeff_row` becomes the trivial
  `Matrix.rowCombination b _` identity.
- `bareissGramRowInvariant_exists_rowVec` reconstructs the previous
  existential shape so downstream singular-step consumers
  (`noPivotLoop_initial_gram_entry_eq_dot_of_rowInvariant`) keep their
  current signatures.
- `lake build HexGramSchmidt` green; only pre-existing sorry on
  `gramDetVecEntry_eq_leadingPrefix_bareiss` (#5668 chain) remains.

## Current frontier

- The `hentry_regular` premise on
  `bareissGramRowInvariant_noPivotLoop[_initial]` now quantifies over
  the deterministic `hinv_state.coeff`. The successor work (filed under
  the parent #5757 decomposition) can discharge it using
  `dot_rowCombination_exactDiv_eq_of_eq_mul_right` (#5756, already
  merged) plus coefficient-level Bareiss divisibility.
- After `hentry_regular` is dischargeable, the consumer-facing
  variant of
  `noPivotLoop_initial_gram_entry_eq_dot_of_rowInvariant` without the
  premise (the goal of #5755) becomes a straightforward rewrap.

## Next step

- Pick up the next sub-issue in the chain (#5757 → #5755 → #5748 →
  #5668) that discharges `hentry_regular` via reachable Gram noPivot
  exact-divisibility.

## Blockers

None.
