# PNT+ Table 10 large-decimal pointwise row

## Accomplished

- Preserved the exact Table 10 stack through
  `9d5c8739db57d9e37a50283e1551720e14d596e5` and audited the remaining
  large-decimal and coupled-logarithm target providers against pinned PNT+.
- Selected row `13800.7464` as the smaller independent bridge: all five
  declarations use `row_bound_pointwise`, while the two remaining groups share
  additional `log 10`, real-power, and convex endpoint dependencies.
- Added a Mathlib-free fixed-row package authenticating the exact fixed-point
  row, upper endpoint, `A₁`, `A₂`, epsilon, a shared `1e-100` tail, and five
  listed/corrected source cells. One bounded action installs exactly five
  facts.
- Reused the existing ordinary-kernel PNT exponential-tail theorem to prove
  both real exponential terms lie below the authenticated tail, then proved
  the complete five pointwise premises. Exact tuple membership and finite
  column dispatch are exposed by `rowOfMem`; generic schema, chronology, and
  `ProofFrontend` replay close every fact.
- Added bare-target, wrong-column, wrong fixed-point-row, and wrong replay
  target mutations. Each malformed payload is rejected without a draft or
  retry, and the bare rational-majorant failure is an ordinary theorem.
- Updated both SPECs and the exact-source inventory to 77/87 accepted target
  calls. The ten coupled logarithmic targets, all 38 supporting `a₂` sites,
  surrounding `B_8_exact` reductions, arbitrary rows, and production scale
  remain pending.
- Registered the modules and added the exact proof-only Lake exemption.
- Passed focused and retained PNT builds, exact-source inventory verification,
  freshness, copyright, line-count, DAG, Phase-4, trust, release-manifest,
  Mathlib-free bench, diff-hygiene, and banned-mechanism checks.

## Current frontier

Only ten executable Table 10 target numerics remain: five for row 43 to
`19 * log 10`, and five for `19 * log 10` to row 44. They share log/rpow
endpoint bounds but use different coefficient rows. The 38 `a₂` helpers and
surrounding `B_8_exact` dispatch remain separate work.

## Next step

Keep the local stack unpushed until the parent selects publication sequencing.
Treat the remaining ten target calls as one coupled logarithmic provider so
the `log 10` and real-power bridge is authenticated once.

## Blockers

None.
