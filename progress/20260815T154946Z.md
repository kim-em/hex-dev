# Complete BKLNW acceptance restack

## Accomplished

- Replayed the parameterized BKLNW power-ladder and exponential-bound
  acceptance slices as a literal two-commit stack on exact regularization main
  `b6ff729665bff8ea0bc1abc7b660183b63a9dbb2`.
- Preserved the regularization registrations and refreshed the exact
  proof-only Lake freshness exemption for the combined tree.
- Confirmed byte identity for all six runtime/proof/conformance feature files
  against the preserved pre-regularization stack. `git range-diff` reports an
  unchanged power patch and only expected regularization/Lake/progress
  reconciliation in the exponential patch.
- Built both BKLNW runtime modules, both Mathlib proof modules, both
  conformance modules, and the retained Table 12 and FKS2 conformance targets.
- Reverified the source-pinned PNT inventory, inventory tests, static/DAG/phase
  checks, trust surface, Mathlib-free boundary, freshness data, diff hygiene,
  and the bans on `native_decide`, `axiom`, and `sorry` in the new modules.

## Current frontier

The complete pinned `BKLNW_a2_bounds` migration covers all eleven power-upper
declarations and all twenty-two exponential lower/upper declarations through
bounded parameterized schemas and ordinary-kernel replay. It deliberately
claims neither LeanCert API compatibility nor general arbitrary-input support.

## Next step

Publish `agent/interval-pnt-bklnw-complete` as one non-draft PR against main,
verify its exact GraphQL edge, and let the repository CI review the combined
acceptance slice.

## Blockers

None.
