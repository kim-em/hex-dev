# Session cf40d34c — feature, issue #4254

## Accomplished

- Claimed #4254 after all reported directive issues were rejected by
  coordination as blocked or needing replan.
- Added a private bridge in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`:
  `gcd_monicModularImage_derivative_eq_one`, deriving the executable
  Berlekamp square-free gcd check for `monicModularImage f` from the
  original modular image's `DensePoly.gcd f f' = 1`.
- Added the public wrapper
  `squareFreeCore_irreducible_of_small_mod_singleton_of_choosePrimeData_squareFreeModP`,
  which obtains `squareFreeModP` from the selected good-prime witness and
  feeds the derived monic-image square-free proof into the existing
  small-mod singleton composition theorem.
- Updated the existing explicit-square-free theorem comment to point callers
  at the new wrapper when they want the selected prime data to supply the
  square-free precondition automatically.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
  - `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - Diff scan found no new `axiom`, `native_decide`, `TODO`, `FIXME`, or
    `sorry` in the affected Lean file.

## Current frontier

- The `hsquareFree_monic` hypothesis remains available for direct callers,
  but downstream users with `choosePrimeData? core = some primeData` can now
  call the squareFreeModP wrapper instead of providing it manually.

## Next step

- Wire this wrapper into the next small-mod singleton consumer or planner
  residual that needs to discharge the explicit `hsquareFree_monic`
  argument.

## Blockers

- None for #4254.
