# HexMatrix old-boundary off-diagonal term rewrite

## Accomplished

- Claimed #2150 and added the old-boundary successor `detTerm` rewrite in `HexMatrix/Determinant.lean`.
- Added the supporting row-swapped leading-prefix equality that identifies the remaining rows/columns with `borderedMinor M k hk i ⟨k, hk⟩`.
- Verified the new theorem packages the sign flip, product reindexing, and boundary entry `M[⟨k, hk⟩][j]`.
- Ran `lake build HexMatrix.Determinant`, `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexMatrix/Determinant.lean`.

## Current frontier

Issue #2150 is complete. The new theorem is ready for the dependent prefix-row off-diagonal reindexing work.

## Next step

Proceed with #2131, which can use the old-boundary term rewrite before handling the earlier-prefix row case.

## Blockers

- No blocker for this task.
- `lake build HexMatrix` still reports pre-existing proof-placeholder warnings in `HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`.
