# Work session: HexMatrix prefix-row deletion minor

## Accomplished

- Claimed #2220 after confirming it was the only unclaimed work item and that its dependency #2178 is closed.
- Read the required HexMatrix SPEC/design/Phase 5 context and the #2181 worker analysis explaining why the original standard-bordered-minor target is infeasible.
- Added `Matrix.prefixRowDeletionMinor` in `HexMatrix/Determinant.lean` for the prefix-row final-column case, representing rows `{0..k} \ {r} ∪ {i}` and columns `{0..k-1} ∪ {j}`.
- Added simp entry lemmas for the before-deleted-row, shifted-row, and border-row cases.
- Verified:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - touched-file scan for `axiom`, `native_decide`, `TODO`, `FIXME`, and `sorry`

## Current frontier

#2220 remains partially complete. The row-deletion minor and entry API are in place, but the requested prefix-row sum theorem still needs the determinant-term reindexing: inserting the final column at an arbitrary prefix row contributes a non-adjacent insertion sign and a product over the row-deletion minor.

## Next step

Prove the sign/product package for
`insertAt (Fin.last (k + 1)) (v.map Fin.castSucc) r.castSucc.castSucc`,
then sum over `permutationVectors (k + 1)` to obtain the row-deletion determinant contribution.

## Blockers

- No build blocker for the partial change.
- The remaining proof needs a new arbitrary-prefix insertion sign/product lemma; the existing adjacent old-boundary row-swap theorem only covers `r = Fin.last k`.
