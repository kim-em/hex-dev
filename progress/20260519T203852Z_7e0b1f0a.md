# HexGF2 pureClmul API polish

## Accomplished

- Claimed and executed #5538 (`HexGF2 Phase 6: audit pureClmul API quality`).
- Audited `HexGF2/Clmul.lean` against the Phase 6 API-quality criteria and the
  packed GF(2) extern contract.
- Added pure-reference API wrappers:
  - `pureClmul_comm`
  - `pureClmul_xor_right`
- Kept `pureClmul`, `clmul`, the `@[extern]` boundary, and the C shim
  unchanged.
- Verified:
  - `lake build HexGF2.Clmul`
  - `lake build HexGF2`
  - `python3 scripts/check_dag.py`
  - `git diff --check -- HexGF2/Clmul.lean`
  - no new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in
    `HexGF2/Clmul.lean`

## Current frontier

- `HexGF2/Clmul.lean` now exposes both runtime and pure-reference statements
  for commutativity and XOR-linearity on each side, so downstream users do not
  need to route pure-reference reasoning through private fold helpers.

## Next step

- Continue Phase 6 API audits on adjacent HexGF2 proof surfaces, especially
  packed multiplication lemmas that consume the word-level `clmul` contract.

## Blockers

- None for this issue. An unrelated local modification to `.claude/CLAUDE.md`
  was present before this work and was left untouched.
