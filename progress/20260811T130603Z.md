# Optional-hypothesis reification

## Accomplished

- Kept target graph construction strict while making unsupported or
  over-budget hypotheses optional inputs that leave the graph unchanged.
- Preserved hard errors for malformed and ambiguous package registrations.
- Compared only the target's operation and node prefixes against the temporary
  exponential proof fixture, so later packages do not disable the tactic.
- Added regressions for unsupported real bounds, extended operation tables,
  and two same-node seed assumptions.

## Current frontier

The package-driven goal frontend now remains applicable in richer local and
package contexts without weakening target validation or proof checking.

## Next step

Land the reviewed frontend, then discharge its node meanings and ordered seeds
through the package-composed semantics layer.

## Blockers

None.
