# Progress — 2026-06-18 (session ad06cd14, /work → /feature #7861)

## Accomplished

Discharged the four packed `GF2Poly` finite-index bijection theorems in
`HexGF2Mathlib/Basic.lean`, the Phase 5 capstone (deliverables 1 & 2 of #7861):

- `toNat_lt_of_degree_lt`
- `ofNatBelowDegree_reduced`
- `toNat_ofNatBelowDegree`
- `ofNatBelowDegree_toNat`

All four reduce to coefficient extensionality plus the already-landed bit
lemmas from the helper layer (#7860/#7862/#7881): `toNat_testBit_eq_coeff`,
`coeff_ofNatBelowDegree_of_lt`, `coeff_ofNatBelowDegree_eq_false_of_bound`, and
`Hex.GF2Poly.{ext_coeff, coeff_eq_false_of_reduced_bound_le}`.

Two small private helpers added:

- `lt_two_pow_of_testBit_false`: all bits `≥ k` clear ⟹ `n < 2 ^ k`, via
  `Nat.lt_of_testBit` against `2 ^ k` (`testBit_two_pow_self` /
  `testBit_two_pow_of_ne`).
- `reduced_of_coeff_eq_false_above`: the converse of
  `coeff_eq_false_of_reduced_bound_le` (coeffs vanishing from `bound` up ⟹
  `IsZero ∨ degree < bound`), via the executable `degree?` API.

The packed↔`FpPoly 2` ring-equivalence sorries (`toFpPoly_one`,
`ofFpPoly_toFpPoly`, `toFpPoly_ofFpPoly`, `toFpPoly_add`, `toFpPoly_mul`) and
`Field.lean` sorries are untouched, per the issue's deliverable 3.

## Verification

- `lake build HexGF2Mathlib` → `Build completed successfully (1131 jobs)`, 0
  `error`. Remaining `Basic.lean` sorries are only the 5 ring-equiv ones
  (lines 173-193).
- `lake build HexGFqMathlib` (downstream importer) → green, 0 `error`. Change
  is purely additive (proofs only, no statement changed), so downstream is
  unaffected.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  on added lines.

## Current frontier

`HexGF2Mathlib` Basic.lean finite-index bijection is complete. The remaining
Phase 5 work is the packed↔`FpPoly 2` ring-equivalence (the 5 untouched
sorries) and the `Field.lean` sorries.

## Next step

Prove the packed↔`FpPoly 2` ring-equivalence sorries to advance
`done_through` to 5.

## Blockers

None for this issue.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
