# issue-8545: cross-system factorization benchmark suite — complete

Session type: feature (issue #8545), branch `issue-8545`.

All six deliverables implemented, verified, and with a first recorded sweep.

## Accomplished

- **1 Corpus.** `scripts/bench/gen_factor_corpus.py` +
  `bench/corpus/hexbz-factor-corpus.jsonl` (205 instances, 10 families, degrees
  3..1030). Byte-identical regen (`--check`); 198 `expectedFactorDegrees`
  cross-check clean vs python-flint. Hoeij coefficients vendored.
- **2 hex service + library.** `hexbz_factor_service` (4 entries) over the
  Isabelle line protocol. Additive library change (`scaledRecombinationFull`,
  `classicalCoreFactorsToCompletion`, `factorClassicalNoDecline`) reusing the
  proven loops with #8530 tightening removed; production `factor` and CI-gated
  Mathlib proofs untouched. Guarded on 3- and 6-factor reducibles.
- **3 External drivers.** `bz_flint_service.py`, `bz_pari_service.py`,
  `bz_ntl_service.cc` + `setup_bz_ntl_driver.sh`, and the build-spike-gated
  `isabelle-lll` driver (`bz-lll-isabelle/` + `setup_bz_lll_isabelle.sh`) wrapping
  AFP `LLL_Factorization` via `factorize_int_poly_generic LLL_factorization`.
  Shared `serve()` in `factor_service_common.py`. FLINT + NTL agree with hex.
- **4/5 Orchestrator + records.** `factor_sweep.py`: warm-spawn, overhead,
  parameterized cutoff, kill+respawn on timeout, cross-check, durable
  env/config/results JSON. Hardened per Codex review.
- **6 Plots + report + SPEC.** `hexbz-cactus.py` (21 deterministic SVGs),
  `reports/hexbz-factor-sweep.md`, SPEC/benchmarking.md cross-system-comparator
  addendum, BZ performance report link.

## First recorded sweep (carica, 10 s)

`reports/bench-results/hexbz-factor-sweep-663e07e2-carica.json`
(sha256 c98c1fa1…). Cross-check: all answering systems agree. flint/ntl 204/205
(miss SD₉ deg 512); hex-factor 180; hex-lattice 177; hex-classical-nodecline 180
(25 timeouts = the classical wall); hex-fast 114.

## Verification vs issue

Corpus byte-identical ✓; charts deterministic ✓; every report number pinned to a
SHA-256 artifact ✓; cross-system degree multisets agree ✓; not-CI (no workflow
changes) ✓.

## Blockers / carica follow-ups

PARI (cypari2 not installed locally) and both Isabelle systems (heavy AFP build;
isabelle-lll gated on a build spike) run on carica by re-invoking the sweep with
those systems. The committed record covers the six locally-available curves.

## Next step

Second opinion done (Codex, fixes applied). Open PR; check merge conflicts + CI.
