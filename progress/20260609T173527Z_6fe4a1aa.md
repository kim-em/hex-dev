# 20260609T173527Z (#6671 partial)

## Accomplished

Moved the generic raw-to-recorded-entry irreducibility bridge in
`HexBerlekampZassenhausMathlib/Basic.lean` above the public
`factor_irreducible_of_nonUnit` capstone declaration:

- `zpolyIrreducible_normalizeFactorSign_of_zpolyIrreducible`
- `factorWithBound_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`
- `factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`
- `factorWithBound_entries_irreducible`
- `factor_entries_irreducible`

This is a declaration-order-only move of existing proved theorems. It changes
no executable definitions and introduces no new proof obligations.

Opened residual issue #6704 and left the required `Decomposed into #6704`
breadcrumb on #6671 for the remaining branch-umbrella/module-split work.

## Current frontier

The capstone declaration in `Basic.lean` can now see the raw bridge, but it
still cannot see the branch-specific irreducibility umbrellas in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` because that module
imports `Basic.lean`.

The remaining work is to make the final public capstone surface acyclic with
both the raw bridge and branch umbrellas visible, either by moving the public
capstone/wrappers to a post-`IntReductionMod` module or by extracting lower
reduction-mod prerequisites so the branch API can sit below `Basic.lean`.

## Next step

Work #6704: choose and implement the smallest acyclic module layout that lets
`factor_irreducible_of_nonUnit` and its immediate public wrappers see the
branch umbrellas without an import cycle.

## Blockers

No blocker for the partial slice. Verification performed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check -- HexBerlekampZassenhausMathlib/Basic.lean`
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Basic.lean`: no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`
- Tracked Lean `sorry` count over `HexBerlekampZassenhausMathlib`, `HexBerlekampZassenhaus`, and `HexPolyZ` remained `12`

Pre-existing local modification in `.claude/CLAUDE.md` was not touched for this
work item and should remain out of this PR.
