# Issue #8852 symmetry-aware integer Gram rows

## Accomplished

- Claimed issue #8852 and rebased `issue-8852` onto current `origin/main`.
- Confirmed the generic `gramMatrix` remains outside the sound optimization
  surface and that the two integer runtime paths construct Gram rows
  independently.
- Added isolated Gram-construction and consumer benchmarks for square and wide
  lattice shapes; both benchmark executables compile.
- Recorded matched Gram–Schmidt baselines: construction is about 45% of the
  square Schur consumer at `n = 32` and 81–88% of the wide consumer at common
  rungs, decisively clearing the Amdahl gate.
- Split the interval probes into a focused `hexlll_gram_bench` executable after
  confirming that unrelated `hexlll_bench` expected-hash initializers consume
  roughly 80 seconds before a 9.4 µs `n = 3` requested probe.
- Recorded matched interval baselines: construction is 28–48% of the square
  checker and 58–71% of the wide checker, so this distinct path also clears
  the Amdahl gate.
- Added `gramRowsImpl`, which computes only `i ≤ j` dot products and mirrors
  the cached upper triangle, with `@[csimp] gramRows_eq_impl` proved from
  integer dot-product commutativity.
- Routed `lllReducedInterval` through `gramRows`; the adjusted Mathlib
  soundness proof builds without changing the generic `gramMatrix` surface.
- Recorded clean matched after measurements. Square/wide `gramRows` improved
  by 22.6%/42.7%, the square/wide Schur consumers by 11.2%/32.5%, and the
  square/wide interval checker by 10.3%/30.0% at their largest benchmark
  rungs. All result-hash ladders match their baselines.
- Added the committed artefacts, checksums, Amdahl shares, and before/after
  tables to the Gram–Schmidt and LLL performance reports.
- Added the expanded Gram–Schmidt bench and focused interval bench to the
  existing single-job CI smoke tail; each local `verify` completes in under a
  second.
- Passed the full 9,459-target `lake build`, the 9,091-target conformance and
  fixture build, copyright/line-count/Mathlib-free lints, `git diff --check`,
  and forbidden-construct scans. The two new CI smoke entries pass together
  in 5 seconds against the 360-second repository cap.
- Completed the required Claude Opus review. It found the Lean proof,
  `csimp` transfer, checker reroute, arithmetic, and CI integration sound, and
  confirmed `gramRowsImpl` appears in the compiled checker IR.
- Addressed its release-layout finding by moving the focused driver under the
  published `bench/HexLLLBench/` tree, removed the now-dead generic-Gram bridge
  lemma, and documented the single-trial, small-integer, reject-path,
  constant-Boolean, spawn-floor, and periodic-fixture limits of the evidence.
- Rebased onto `origin/main` at `815dcbba`, preserving the upstream interval
  and Strassen CI targets while resolving the one target-list conflict.
- Repeated the full 9,463-target build, affected conformance/fixture targets,
  policy lints, forbidden-construct scan, generic-`gramMatrix` no-diff check,
  and the 3-second focused bench smoke pass on the rebased tree.
- Opened PR #8923. Its first CI run exposed missing explicit cost-model wording
  on two new Gram benchmark registrations; updated those adjacent derivations
  before rerunning the checker.

## Current frontier

Both gates are positive and the clean measurements show end-to-end
improvements above 5% on every square and wide consumer ladder. The
complete local verification pass is green, the required Claude second opinion
has been addressed, and the clean rebased branch is ready for PR/CI.

## Next step

Open the pull request, inspect exact CI job state and logs, resolve any failure,
then enable the requested squash auto-merge once green.

## Blockers

None.
