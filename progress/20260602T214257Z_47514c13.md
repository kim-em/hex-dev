# 20260602T214257Z work #2567

## Accomplished

- Claimed directive #2567 and landed a narrow HO-4 partial slice for the
  actual-cap l2norm-bound surface.
- Added
  `ExecutableBadVectorWitness.inputPolynomial_l2norm_le_coeffL2NormBound` in
  `HexBerlekampZassenhausMathlib/TerminationBound.lean`.
- Added
  `factorFastCapLift_inputPolynomial_l2norm_le_coeffL2NormBound` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`, specializing the generic
  input-polynomial bound to `badVectorWitnessOfFactorFastCapLiftData`.

## Current frontier

The actual-cap product-comparison path now has a named input-polynomial l2norm
bound for the factor-fast cap-lift witness.  The auxiliary-polynomial l2norm
bound and the strict cap-arithmetic inequality remain the next analytic inputs
needed to instantiate
`ExecutableBadVectorWitness.l2norm_product_lt_divisor_of_l2norm_bounds`.

## Next step

Prove the auxiliary-polynomial bound for the actual cap-lift witness from
`BHKS.auxiliaryPolynomial_l2norm_sq_le`, then combine it with the input bound
and `BHKSBound.lean` cap estimates to produce a
`FactorFastCapLiftAnalyticComparison`.

## Blockers

No technical blocker for this slice.  The full `factorFast_terminates` theorem
still requires the remaining auxiliary-bound and strict cap-arithmetic leaves.

## Verification

- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Lean files found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
