# 20260603T071000Z work #2567

## Accomplished

Added a sibling chain to the existing pointwise-bound HO-4 assembly that
consumes `‖aux‖ ≤ auxiliaryBound` directly, bypassing the structured
corrected-RHS sum form:

* `FactorFastCapSeparationInputs.ofBridgeDataAuxiliaryL2norm`
* `FactorFastCapSeparationInputs.ofBridgeDataAuxiliaryL2normAndPaperThreshold`
* `factorFast_terminates_ofBridgeDataAuxiliaryL2normAndPaperThreshold`

Each is a thin composition of existing infrastructure
(`factorFastCapLift_l2norm_product_lt_divisor_of_auxiliary_bound`,
`bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree`,
`factorFast_terminates`).

## Current frontier

`HexBerlekampZassenhausMathlib.Recovery` and the full
`HexBerlekampZassenhausMathlib` library build clean.  Two parallel chains now
expose the actual-cap `factorFast_terminates` conclusion:

1. Pointwise: caller supplies pointwise `vectorSquareBound`,
   `correctionWeightedBound`, `hauxiliaryBound_sq` (structured RHS),
   `hpaper`.
2. Direct l2norm (new): caller supplies a single `‖aux‖ ≤ auxiliaryBound`
   plus `hpaper`.

The remaining HO-4 work is the genuine BHKS §5 analytic content:

* Derive a concrete `‖aux‖ ≤ B` estimate for the actual cap-lift bad-vector
  bridge from L'-membership (BHKS Lemma 3.2), or
* Prove the `hpaper` paper-threshold-shape inequality
  `coeffL2NormBound^aux.natDeg * B^input.natDeg ≤ bhksPaperThresholdReal core C`.

Both routes feed into the new direct-l2norm wrapper without further
hypothesis plumbing.

## Next step

Specialise `ofBridgeDataAuxiliaryL2normAndPaperThreshold` to an
auxiliary-bound accessor derived from L'-membership and the LLL-cut radius
`B'` (BHKS Lemma 3.2): for `v ∈ L' \ W`, give `‖aux‖² ≤ B'²` as a derived
estimate, so the caller no longer has to construct `auxiliaryBound` ad hoc.
That accessor reduces the open analytic content to the paper-threshold
inequality alone.

## Blockers

None.  The new wrappers are pure compositions of existing infrastructure.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus` passed.
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing.
