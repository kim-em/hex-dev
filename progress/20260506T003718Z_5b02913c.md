# 2026-05-06 00:37 UTC — feature session (5b02913c)

## Accomplished

Claimed and implemented #2387 in a new module
`HexMatrixMathlib/Determinant/Bareiss.lean` (added to
`HexMatrixMathlib.lean`).

New definitions:

- `NonzeroBareissPivots (M : Hex.Matrix Int n n) : Prop` —
  `∀ k : Fin n, det (leadingPrefix M (k.val + 1) _) ≠ 0`, the hypothesis
  used to drive the no-pivot Bareiss soundness proof.
- `BareissNoPivotInvariant source state : Prop` (structure with five
  fields) — bordered-minor invariant of the no-pivot Bareiss recurrence:
  - `singular_none`: `state.singularStep = none`
  - `step_le`: `state.step ≤ n`
  - `prevPivot_eq`: `state.prevPivot = det (leadingPrefix source state.step _)`
  - `prevPivot_ne`: `state.prevPivot ≠ 0`
  - `trailing_eq`: trailing entries `(i, j)` with `state.step ≤ i.val`
    and `state.step ≤ j.val` equal `det (borderedMinor source state.step _ i j)`.

The diagonal-entry consequence (`state.matrix[k][k]` agrees with the
leading-prefix determinant of size `k + 1`) follows from `trailing_eq`
at `i = j = ⟨k, _⟩` together with the new private helper
`borderedMinor_corner_eq_leadingPrefix` (matrix-level identity
`borderedMinor M k _ ⟨k, _⟩ ⟨k, _⟩ = leadingPrefix M (k + 1) _`).

New theorems:

- `bareissNoPivotInvariant_initial`: the invariant holds at the
  `noPivotInitialState`. `det_one_by_one` reduces the bordered-minor
  determinant to a single entry, which is the source entry by
  `borderedMinor_entry_last_last`.
- `bareissNoPivotInvariant_step` (private): one regular step of
  `noPivotLoop` preserves the invariant. The hard work is invoking
  `desnanot_jacobi_borderedMinor` (from the parent module) plus
  `bareissExactDiv_borderedMinor_of_mul_eq` to feed the `hexact`
  premise of `Hex.Matrix.stepMatrix_borderedMinor_update`.
- `noPivotLoop_invariant`: induction on fuel — under the assumption
  that all future leading-prefix determinants are nonzero, the loop
  preserves the invariant. The pivot at step `k` is nonzero by
  `hpivots ⟨k, hk⟩` after rewriting through the invariant.
- `bareissNoPivotInvariant_holds`: under `NonzeroBareissPivots M`, the
  invariant holds for the full no-pivot loop run from the initial state.
- `noPivotLoop_singularStep_eq_none` and
  `bareissNoPivotData_singularStep_eq_none`: immediate corollaries —
  no singular branch under `NonzeroBareissPivots`.

Verified: `lake build HexMatrixMathlib.Determinant.Bareiss`,
`lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`,
`git diff --check`. No new `axiom`, `native_decide`, `TODO`, `FIXME`,
or theorem-level `sorry` in the new module.

## Current frontier

The Bareiss invariant + no-singular-branch is now available as
`HexMatrixMathlib.bareissNoPivotInvariant_holds` /
`HexMatrixMathlib.bareissNoPivotData_singularStep_eq_none`. The remaining
work in the Bareiss chain is the no-pivot capstone (#2324, currently
`replan`), which composes this invariant with `det_eq` and a final
"diagonal of last step = det M" identification to close
`bareissNoPivot_eq_det`. Then the public row-pivoting wrapper
`bareissDet_eq_det` (#2315) and `bareiss_eq_det` (the existing `sorry`
in `HexMatrix/Bareiss.lean:507`) follow.

## Next step

#2324 (no-pivot capstone) needs replanning to drop its `replan` label
since its prerequisites (this issue + #2399 + #2385) are now landed.

## Blockers

None for the work landed.
