# Progress - 2026-06-16T09:03:14Z (session a2a0a7a9, work)

## Accomplished

Claimed issue #7540 and added the C=2 joint BHKS auxiliary-domination surface.

In `HexBerlekampZassenhausMathlib/BadVectorAuxiliary.lean`, added
`BHKS.auxiliary_le_paperThreshold`, specializing the reusable
`jointAux_le_paperThreshold` theorem to the concrete zero-correction
`BHKS.auxiliaryPolynomial` using the existing degree and squared-l2 auxiliary
facts.

In `HexBerlekampZassenhausMathlib/Recovery.lean`, added
`FactorFastCapSeparationInputs.ofBridgeDataAuxiliaryL2normAndJointPaperThreshold`,
a cap-lift constructor that consumes one joint
`‖core‖₂^aux.natDegree * auxiliaryBound^input.natDegree` paper-threshold
premise and routes it through the existing l2norm joint cap-arithmetic chain,
without introducing final split `h_coeff` / `h_aux` obligations.

Verification:
- `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`
- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over touched Lean files found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The concrete C=2 zero-correction auxiliary theorem and a Recovery-facing joint
consumer surface are available.  A fully closed cap-lift wrapper that
specializes directly to `BHKS.auxiliaryPolynomial` was investigated but proved
too expensive to elaborate because the witness type expands a large cap-lift
expression; callers should feed `BHKS.auxiliary_le_paperThreshold` into the
new joint constructor instead.

## Next step

Use the new joint constructor in the downstream #7559/#7521 capstone wiring,
supplying the exact auxiliary l2norm estimate and the C=2 paper-threshold
bound from `BHKS.auxiliary_le_paperThreshold`.

## Blockers

None for #7540.  The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md`; this session did not touch or stage it.
