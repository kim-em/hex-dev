# Accomplished

- Put the iterated-quadratic-norm irreducibility certificate on the production
  singleton-irreducibility path of `Hex.ZPoly.factorize`, gated on the modular
  support width, and proved it sound end to end. A certified row returns the
  same `Factorization` any other singleton proof produces.
- Moved the closed-form radicand search into production as
  `Hex.QuadraticNormCertificate.recover?` / `certify?`, whose only claim on the
  rest of the system is `check_of_certify?`. Retired the bench prototype and its
  `HexQuadraticNormProbe` library; the probe now times the production definitions.
- Closed the composition obligation the two dependency issues left open.
  `signPatternPoly_ofFn` identifies #9168's fold/list sign-pattern product with
  #9169's `Fin n -> Bool` `Finset.prod` -- by induction on the radicand count,
  splitting the last sign off with `Fin.snocEquiv` on one side and the last fold
  step on the other -- which is what lets
  `irreducible_int_of_map_eq_signPoly` discharge the executable check.
  `irreducible_of_check` needs no hypothesis on its input.
- Kept both conformance arms honest: `adv/swinnerton_dyer_sd6` must now be
  answered by the certificate, and a new `adv/multiquadratic_nonnorm_64` --
  the minimal polynomial of `sqrt2+...+sqrt13+sqrt6`, whose last sign is
  determined by the first two, so it generates the same field but is not an
  iterated quadratic norm -- must be refused by it and answered by the lattice.
- Measured before and after at one revision, two runs a side, by raising the
  width floor past every reachable width rather than comparing against `main`,
  so unrelated drift is not credited to this change. Solved 377 -> 383 of 392;
  total over the rows both sides solve 17.225 s -> 8.965 s; `sd5` 71.007 ->
  6.820 ms; `sd6` 8.148 s -> 27.055 ms; `sd7`, `hoeij_S7`, `hoeij_S8`,
  `sd6_shift1`, `sd6_shift5` from timeout to between 31 ms and 3.98 s.
- Worst cumulative Hex/Isabelle over ranks 125-140: **1.012x -> 0.707x**,
  against #9126's 0.85x criterion. The go/no-go modelled 0.708x.
- Full `lake build`, `HexConformance`, `HexReleaseTests`, all 22 oracles green,
  fixtures re-emitted byte for byte, trace gate green, 421 files clean on the
  trust surface, figures regenerated and `--check` clean.

# Current frontier

Nothing open in this workstream except the PR itself.

# Next step

Open the PR, take a second opinion, and report the measured rank ratio back on
#9126.

# Blockers

None.

# Notes for the next session

- The published sweep record's commit is compared against `HEAD` over Hex
  factorization sources, and `lakefile.lean` counts. Merging `main` therefore
  invalidates a sweep taken before the merge, even when the merge touches no
  factorization code. Merge first, then take the published sweep.
- `factor_sweep.py` marks a record dirty on any `git status --porcelain` output,
  untracked files included. Commit before measuring.
- Whole-corpus sweeps on this host carry occasional 1.5x transients across a run
  of adjacent rows. A paired in-process A/B between two binaries settles what a
  sweep diff cannot; run 2 of a sweep is cheap and worth having.
