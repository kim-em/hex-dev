# Quadratic root-factor side-condition package (#4841)

## Accomplished

Claimed #2564 first because it was the oldest directive, confirmed it is now a
stale HO-1 umbrella, and recorded live dependencies on #4818, #4819, #4821, and
#2567 before releasing it as blocked.

Claimed #4747, fast-forwarded the worktree to current `origin/main` so the
merged #4785 pairwise non-association theorem was present, then decomposed the
remaining work into #4841 and #4842 after finding that the final assembly still
needed missing public side-condition packaging plus a factorPower fuel bound.

Implemented #4841 in `HexBerlekampZassenhaus/Basic.lean`:

* Promoted `quadraticIntegerRootFactors?_normalizeFactorSign`,
  `quadraticIntegerRootFactors?_shouldRecord`, and
  `quadraticIntegerRootFactors?_product` from private helpers to public
  theorem surfaces.
* Added `quadraticIntegerRootFactors?_leadingCoeff_pos`, packaging the emitted
  factors' positive-leading-coefficient fact for non-monic expansion consumers.
* Added `quadraticIntegerRootFactors?_degree_pos_of_primitive`, proving emitted
  factors have positive degree when the quadratic core is primitive and has
  positive leading coefficient.
* Added the private helper `linearFactorForRoot_degree_pos`.

## Current frontier

#4842 is the next #4747 successor. It should use the new public side-condition
theorems together with the #4759 repeated-part factorPower decomposition and
#4785 pairwise non-association theorem to prove the quadratic
`reassemblyExpansionComplete` discharger. Its remaining nontrivial ingredient
is the exponent-list fuel bound.

## Next step

Claim #4842 after #4841 lands, prove the factorPower decomposition fuel bound,
then assemble
`reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

## Blockers

None for #4841. #4842 is intentionally blocked on #4841.
