# Progress: #6918 — review PR #6917 (explicit-witness resultant divisibility)

Review session. No source changes; findings posted as a PR comment on #6917
(self-approval is blocked since all agent commits share one account).

## Accomplished

Reviewed PR #6917 (closes #6858), which adds three theorems to
`HexBerlekampZassenhausMathlib/Resultant.lean`:

- `det_dvd_of_cols_dvd` (+ helper `prod_cols_dvd_prod_univ_of_injective`):
  generalises `det_dvd_of_left_cols_dvd` from the leading `castAdd` block to an
  arbitrary **injective** column family — `m ^ d ∣ det` when any `d` distinct
  columns are entrywise `m`-divisible.
- `commonFactor_dvd_resultant`: instantiates the #6857 Sylvester column
  reduction from explicit witnesses `f = q*a + C m*r`, `g = q*b + C m*s`, kills
  the determinant factor `(b.coeff (n-d))^d` to `1` via the pivot hypothesis,
  and rewrites the Sylvester determinant to `Polynomial.resultant`.
- `commonFactor_dvd_resultant_of_monic_cofactor`: caller-friendly monic-cofactor
  form deriving the pivot coefficient from `b.Monic`.

**Verdict: approve.** Verified locally:

- `lake build HexBerlekampZassenhausMathlib.Resultant` succeeds (the Mathlib
  layer is not CI-built, so this needed a local build on the PR commit after
  `lake exe cache get`). No `sorry`/`axiom`/`native_decide` in added lines.
- `scripts/check_dag.py` exit 0; `git diff --check` clean. CI
  `build`/`conformance`/`build-macos` all green.
- The composition is sound: `resultant f g` is definitionally the Sylvester
  determinant (`Resultant.lean:255` `rfl`), so the final rewrite has no
  sign/unit gap. Stays over `Polynomial ℤ` / `Matrix _ ℤ`; no `ZMod (p^k)`
  wrapper and no Hensel/CLD dependency (deliverable 2 confirmed).

## Key findings (recorded on the PR)

1. Non-blocking: `hq_monic` and `hq_deg` are non-load-bearing in
   `commonFactor_dvd_resultant`. `sylvester_commonFactor_colReduce` requires
   neither monicity nor `q.natDegree = d`; the `m^d` divisibility comes purely
   from the column-reduction structure and degree bounds. The two
   `have hq_*_used := hq_*` lines are dead code silencing the unused-binder
   linter. Recommend dropping the hypotheses or underscore-prefixing the
   binders and deleting the `_used` haves. The same hypotheses thread unused
   through the monic-cofactor wrapper, so #6849 will be forced to supply proofs
   the theorem ignores.
2. Minor: the new `hpiv_inj` proof (~line 844) uses the deprecated
   `Fin.coe_natAdd` (build warns → `Fin.val_natAdd`); consistent with several
   pre-existing occurrences in the file.

## Current frontier

Review complete. PR is green and mergeable.

## Next step

#6849 packages the public map-to-`ZMod (p^k)` wrapper consuming
`commonFactor_dvd_resultant_of_monic_cofactor`.

## Blockers

None.
