# 20260517T130609Z — feature session 05c449e7 (#4709 skip)

Session UUID prefix: 05c449e7. Issue: #4709 — HO-43 Round 1: relocate
HexLLL determinant-bound theorems to HexLLLMathlib.

## Accomplished

Verified that issue #4709 is stale: HO-43 Round 1 already landed in
PR #4480 (commit a33037df, 2026-05-16) under the title "feat: relocate
LLL triangular independence helpers to bridge layer". That PR moved
`identity_independent`, `gramMatrix_leadingRows_eq_submatrix`,
`independent_of_upperTriangular_pos_diag`, and the `LLLState.sizeReduce`
independence preservation theorem out of `HexLLL/Basic.lean` and into
`HexLLLMathlib/Independent.lean`, plus relocated the only downstream
consumer (`bhksLatticeBasis_independent`) into
`HexBerlekampZassenhausMathlib/BHKSIndependent.lean`.

The four verification commands the issue lists all pass on the current
tree:

- `lake build HexLLL HexLLLMathlib HexGramSchmidt HexGramSchmidtMathlib`
  — green (only pre-existing sorries warn).
- `python3 scripts/check_dag.py` — exit 0.
- `rg -n 'Matrix\.det' HexLLL/Basic.lean` — zero hits.
- `rg -n '^import Mathlib' HexLLL HexGramSchmidt` — zero hits.

The two named theorems from the issue body that do not appear in the
tree at all (`adjacentSwap_independent`,
`det_submatrix_gram_eq_gramDet_int`) suggest the original directive's
inventory was drafted against a pre-#4146 / pre-#3918 snapshot; the
gramDet-based independence rewrite (PR #3918) replaced the
det-based statement surface entirely, after which the surviving
det-bound consumer (`sizeReduce_independent`) was relocated in #4480.

Issue #4709 was skipped via `coordination skip` with a breadcrumb
linking to PR #4480 and reporting the four verifications passing,
routing it to the next planner's replan-triage cycle.

## Current frontier

The parent directive #3899 remains open. Rounds 2 and 3 are the
remaining work:

- Round 2: relocate `HexGramSchmidt/Update.lean`'s bridge-bound
  theorems (`gramDet_sizeReduce`, `scaledCoeffs_sizeReduce_*`,
  `gramDet_adjacentSwap_of_ne`, `scaledCoeffs_adjacentSwap_lower_*`,
  plus their private helpers) into a new
  `HexGramSchmidtMathlib/Update.lean`.
- Round 3: relocate `HexGramSchmidt/Int.lean`'s bridge-bound theorems
  (the seven direct `Matrix.bareiss_eq_det` users plus the transitive
  closure) into a new `HexGramSchmidtMathlib/Int.lean`.

After Round 3, HO-44 (#3900) can delete the orphan
`Hex.Matrix.bareiss_eq_det` sorry from `HexMatrix/Bareiss.lean`.

## Next step

Planner should triage #4709's `replan` label: either close as
superseded by #4480, or — if a Round-2 issue does not yet exist —
narrow #4709's body to the Round-2 deliverables (a new
`HexGramSchmidtMathlib/Update.lean` bridge file plus the named
theorem list above) and drop the Round-1 deliverables.

## Blockers

None for this session. Rounds 2 and 3 of #3899 remain the open work
for HO-43.
