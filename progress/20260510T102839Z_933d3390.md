## Accomplished

Claimed #2964 after confirming the human-oversight issues were dependency-blocked.
Assessed #2964 as too large for one proof session and decomposed it into
#2986, #2987, #2988, and #2990, with dependencies matching the proof layers.
Marked #2964 as `replan` via `coordination skip`.

Continued on #2986 and added two private helper lemmas in
`HexPolyFp/SquareFree.lean`: `pow_succ` and `derivative_pow_succ`.
These establish the executable `pow` successor recurrence and the recursive
product-rule derivative form needed by the next square-free quotient proof
layer.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "axiom|native_decide" HexPolyFp/SquareFree.lean`

## Current frontier

#2986 is ready to publish as a small prerequisite PR. The stronger closed
scalar derivative-of-power formula remains for the successor algebra issue if
that proof needs it; the current PR intentionally lands only the recurrence
facts that compile cleanly.

## Next step

Create the PR for #2986. After it merges, #2987 can use `pow_succ` and
`derivative_pow_succ` while proving the derivative-split power-gcd induction.

## Blockers

None for #2986. The worktree also contains an unrelated pre-existing
`.claude/CLAUDE.md` modification, left untouched.
