# Number-field PR consolidation CI repair

## Accomplished

- Monitored aggregate PR #9012 through its first consolidated CI run.
- Diagnosed the sole failure as stale deterministic box/precision rows in the
  committed `HexNumberField` conformance fixture after integrating current
  `main`.
- Regenerated the number-field fixture and confirmed it matches fresh emission
  byte-for-byte.
- Confirmed the downstream number-field-tower fixture already matches fresh
  emission.

## Current frontier

- The aggregate branch has one fixture-only CI repair ready to push.
- Library compilation, conformance compilation, and bench verification were
  green on the failed run; only fixture freshness failed.

## Next step

- Push the fixture update, monitor the replacement CI run through auto-merge,
  then close every still-open PR subsumed by #9012.

## Blockers

- Local FLINT/PARI Python bindings are not installed, so the optional local
  independent oracle skips. CI has these dependencies; its first run showed
  the emitted mathematical result rows were unchanged and failed specifically
  on the committed-fixture freshness diff.
