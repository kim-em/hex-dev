**Accomplished**

- Took the third pass on directive #5770 (process-jargon docstring purge).
  Built on PRs #5780 / #5798 which already cleared most prose hits.
- Cleaned `HexBerlekampZassenhaus/Bench.lean` of the residual `smoke` prose
  (15 hits): `smoke ladder` -> `fast schedule`, `smoke-safe` ->
  `verify-budget-safe`, `smoke-intractable` -> `exceeds the verify budget`,
  `smoke verifier` -> `verify mode`. Identifier names (`smokeInput`,
  `smokeSchedule`, `bzClassicalSmokeComplexity`) untouched per the
  prose-only scope of the issue. Resolved a conflict against the recently
  merged #5802 Isabelle BZ comparator scaffolding that shared the same
  module docstring.
- Cleaned `HexGramSchmidtMathlib/Int.lean` of the residual `bridge` prose
  (23 hits): replaced "bridge" / "bridge lemma" / "bridge layer" with
  "identity", "identification", "Mathlib-side", "supporting lemma", or
  the name of the actual theorem being routed. Module docstring rewritten.
- Cleaned `HexBerlekampZassenhausMathlib/Basic.lean` of the
  project-internal "substrate" tag (15 hits): bulk replace_all
  `substrate` -> `supporting lemma`. The docstring tags like
  `**#XXXX HO-1 substrate -- ...**` become
  `**#XXXX HO-1 supporting lemma -- ...**`.

**Verified**

- `lake build HexBerlekampZassenhaus.Bench HexGramSchmidtMathlib` is green.
- `lake build HexBerlekampZassenhausMathlib.Basic` exits with three
  `Type mismatch` errors at lines 5099, 5994, 6280; these are
  pre-existing on `origin/main` (verified by stashing my changes and
  re-building) and are unrelated to this PR's prose-only changes. They
  trace to PR #5779's `isGoodPrime` / `squareFreeModP` semantics change
  and will need a separate fix.
- `git diff origin/main..HEAD` is docstring/comment-only; no
  identifier, signature, or tactic body is touched.

**Current frontier**

Remaining residual sweep hits after this PR lands:

- `HexBerlekampZassenhausMathlib/IntReductionMod.lean`: 40 `substrate`,
  29 `bridge`, and 5 `consumer` hits. I prepared a `substrate` ->
  `supporting lemma` replace_all on this file too, but the rebase
  collided extensively with PR #5810 (the exhaustive-arm transport
  refactor), so I dropped that part of the work and left it for a
  follow-up worker.
- `HexBerlekampZassenhausMathlib/Basic.lean`: still has 46 `bridge` and
  23 `consumer` hits to be cleaned in a follow-up.
- `HexGF2/Multiply.lean`: 8 `gate` hits are all variable names in the
  theorem signature `(zc gate : Bool)`; identifier-level, out of scope
  for #5770.

**Next step**

Open the PR, then leave #5770 claimable for the next worker to clear
the BZ-Mathlib `bridge`/`consumer` residual. The directive remains
open by design (per the issue's "sweep remaining libs" deliverable).

**Blockers**

None for this PR. The pre-existing build failure in BZ-Mathlib Basic
does not block prose-only changes from landing; CI's main job already
reflects whatever state main is in.
