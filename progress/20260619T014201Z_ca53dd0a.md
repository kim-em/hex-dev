# HO-1 FactorSoundness non-associated headline (#6868)

## Accomplished

Added the public `factor_headline` theorem to
`HexBerlekampZassenhausMathlib/FactorSoundness.lean`, the strengthened HO-1
headline contract required by directive #2564. It mirrors the existing
`factor_headline_primitive` bundle (under `f ≠ 0`) but replaces the syntactic
distinct-key clause `a.1 ≠ b.1` with genuine pairwise non-association after
transport to `Polynomial ℤ`:

`List.Pairwise (fun a b => ¬ Associated (toPolynomial a.1) (toPolynomial b.1))`.

The non-association clause is discharged via the existing
`factor_entries_not_associated` (Basic.lean), feeding the raw-source
primitivity hypothesis internally from
`Hex.factor_chosen_raw_primitive_of_ne_zero f hf`. The remaining clauses
(product preservation, primitive + Mathlib irreducibility per entry, positive
multiplicities, signed-content scalar) are reused by destructuring
`factor_headline_primitive`.

Packaging-only, per the issue: no branch irreducibility or raw primitive facts
re-proved. `factor_headline_contract_core` and the primitive intermediate
wrapper are untouched, so existing consumers are unaffected.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.FactorSoundness` green (3.3s; full
build 3736 jobs, exit 0). `scripts/check_dag.py` exit 0. No new
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines. The only
remaining warning in the file is the pre-existing `sorry` in
`factor_irreducible_of_nonUnit` (line 18), out of scope for this packaging
issue.

## Next step

None for this issue. A future issue could discharge the
`factor_irreducible_of_nonUnit` sorry, which would propagate genuine
irreducibility (rather than sorry-backed) through the whole headline surface.

## Blockers

None.
