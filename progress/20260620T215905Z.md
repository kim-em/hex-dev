# Two-track plan: fast executable + dead-slow D1/D2 (with Codex second opinion)

## Accomplished

- **Audited both tracks to ground truth.** Whole-tree grep: the executable
  (all Mathlib-free libs) is **sorry-free**; exactly two real sorries remain,
  both in the bridge — `Basic.lean:235` (`isIrreducible_iff`, C1) and
  `FactorSoundness.lean:20` (the HO-1 capstone, #8068).

- **Wrote the two-track plan** to
  `reports/20260621-fast-executable-and-deadslow-plan.md` and got a **Codex
  adversarial second opinion**, which corrected several stale claims. Folded the
  corrections back into the report.

- **Track 2 (proof the fast path always runs) is much further along than the
  plan assumed:**
  - **D2 landed.** `choosePrimeData?_none_implies_huge` is proved sorry-free at
    `HotPathDiscriminant.lean:244` (#6509 closed).
  - **D1's assembly is built and sorry-free.**
    `factorFast_terminates_of_choosePrimeData` (`Recovery.lean:7977`) proves the
    SPEC D1 from four provider packages; the `factorFast_ne_none_of_*` cascade and
    the `FactorFastCapSeparationInputs.ofBridgeData*` reduction constructors all
    exist. The single deep open input is `BadVectorBridgeData`
    (`BadVector.lean:1399`), field `resultant_divisible_by_p_pow`.

- **Dispatched two replan workers (no automatic pod workers running):**
  - **#8248** (PID 10850) — Track-1 perf substrate (packed UInt64 divMod/gcd;
    #8251 monic-division already landed). #8249 blocks on it.
  - **#6779** (PID 12204) — Track-2 D1 producibility re-scope. Posted the precise
    question: can `BadVectorBridgeData.resultant_divisible_by_p_pow` be discharged
    by the un-cut-`N` producer `cld_syzygy_pow_dvd_resultant_of_congr`
    (`CLDColumnBound.lean:652`, #7075) against the current van Hoeij CLD lattice,
    **without** the #2564 restructure?

## Current frontier

Track 1 = pure performance: #8248 -> #8249 substrate, then #8062-item4
(Berlekamp-mod-p, `choosePrimeData?` is ~90% of factorFast), #8064 high-degree
tail, then benchmark to hex/isabelle <= 1x. Packed kernels are
correctness-sensitive: install only by proved / `@[csimp]`-equivalent replacement.

Track 2 = D1 only (D2 done). D1's assembly is complete; the verdict hinges on
#6779: is `BadVectorBridgeData` producible now (un-cut `N` / #7075), or does it
genuinely need #2564? Did NOT blind-dispatch the headline #2564 (scope ambiguous:
the executable + forward bridge already implement van Hoeij CLD).

## Next step

Read the #6779 verdict. If (a) producible-now: drop #2564 dependency, produce
`BadVectorBridgeData`, D1 closes. If (b) genuinely cut-bound: re-scope #2564 to its
real remaining delta and drive it. Track-1: watch #8248 -> dispatch #8249.

## Blockers

D1's depth depends entirely on the #6779 verdict. #8068 capstone (separate track)
also rides the shared forward `cut` producer.
