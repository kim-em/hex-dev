# 2026-05-07 14:40 UTC — feature session (a32002c6)

## Accomplished

Claimed and completed #2550 — the source-row earlier reduction zero
theorem for `HexGramSchmidt/Basic.lean`.

Added (all `private`):

- Linearity helpers `dot_add_left`, `dot_smul_left`,
  `projectionCoeff_add_left`, `projectionCoeff_smul_left`,
  `subtractProjection_add_left`, `subtractProjection_smul_left`,
  `reduceAgainstBasis_add_left`, `reduceAgainstBasis_smul_left`.
- Vector helpers `zero_add_vec`, `smul_zero_vec`.
- Bridge helpers `reduceAgainstBasis_append`,
  `basisRows_get!_dot_eq_zero_of_list` (list-level mirror of the
  existing matrix-level pairwise-orthogonality theorem).
- Auxiliary basis-row reduction:
  `reduceAgainstBasis_basisRows_take_get!_eq_zero` — for `ℓ < k ≤ rows.length`,
  reducing `(basisRows rows)[ℓ]!` against `((basisRows rows).take k).reverse`
  yields `0`. Decomposes the prefix into `take (ℓ+1) ++ drop (ℓ+1)`,
  invokes `reduceAgainstBasis_eq_self_of_forall_dot_zero` on the later
  rows by pairwise orthogonality, and finishes with the existing
  `reduceAgainstBasis_basisRows_get!_succ_eq_zero`.
- ProjectionCombination reduction:
  `reduceAgainstBasis_projectionCombination_eq_zero` — if every
  contributing basis row reduces to `0` and the accumulator does too,
  the combination reduces to `0`. Induction on the basis list using
  the new add/smul linearity.
- Final theorem requested by #2538:
  `reduceAgainstBasis_basisRows_take_source_eq_zero` —
  `reduceAgainstBasis ((basisRows rows).take k).reverse rows[j]! = 0`
  for `j < k ≤ rows.length`. Proof composes the existing
  `basisRows_get!_eq_reduceAgainstBasis_forward` reconstruction with
  the auxiliary basis-row theorem and the projectionCombination
  reduction.

Verification:

- `lake build HexGramSchmidt.Basic` (only pre-existing sorries on
  `GramSchmidt.Rat.basis_span` and `GramSchmidt.Int.basis_span`).
- `lake build HexGramSchmidt`.
- `python3 scripts/check_dag.py`.
- `git diff --check`.
- `rg '^axiom\b|native_decide|\bTODO\b|\bFIXME\b' HexGramSchmidt/Basic.lean`
  → no matches.

## Current frontier

The source-row earlier reduction theorem is now available in
`HexGramSchmidt/Basic.lean`. It is the input that #2539 needs to
prove the `basisRows` row-add invariance theorem.

## Next step

#2539 (HexGramSchmidt.Basic: prove basisRows row-add invariance) is
unblocked and is the natural follow-up.

## Blockers

None.
