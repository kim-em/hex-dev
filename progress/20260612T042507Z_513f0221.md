# Progress: #6782 — SPEC prerequisite for unconditional packed certificate

## Accomplished

Amended `SPEC/Libraries/hex-lll.md` §Certified external dispatch so the
same-lattice product-equality certificate is specified as unconditional
packed single-point evaluation rather than a word-scale regime with a
materialized-comparison fallback.

Kept the timeless SPEC style: the clause preserves the bound/injectivity
argument, soundness/completeness statement, word-scale `O(n²)`
big-by-small cost claim, and the wide-entry bit-operation parity note,
without issue/PR narration.

Ran `python3 scripts/check_dag.py` and `git diff --check`.

## Current frontier

This is the first, SPEC-only prerequisite requested by #6782. No Lean code
or benchmark fixture was changed in this session.

## Next step

After the SPEC prerequisite lands, remove the regime predicate from
`HexLLL/Basic.lean`'s `mulEqCert`, collapse `mulEqCert_iff` to the packed
branch, and run the paired before/after certified-checker benchmark sweep
from #6782.

## Blockers

Implementation and measurements are intentionally blocked on landing this
SPEC prerequisite first.
