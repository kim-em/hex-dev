# Progress: #6987 docstrings + #6819 / #6983 diagnoses

`/work` meta session. Triaged the unclaimed queue, landed one docstring
issue, and released two issues with measured diagnostics after their
premises turned out to be larger/blocked than the bodies stated.

## Accomplished

- **#6987 (landed):** Added docstrings to the 8 undocumented public
  declarations in `HexGramSchmidt/Int.lean` (`rowsToMatrix`,
  `gramDet_zero`, `scaledCoeffs_upper`, `normSq_map_intCast`,
  `normSq_latticeVec_ge_min_basis_normSq`, `gramDetVec_eq_gramDet`,
  `scaledCoeffs_diag_of_nonneg`,
  `scaledCoeffMatrix_rowSwap_adjacent_pivot_transpose`). Purely additive
  (31 insertions, 0 deletions), docstring-only. `lake build
  HexGramSchmidt.Int` clean (the `h_partial` warning at 4943 is
  pre-existing); `check_dag` exit 0; no banned vocabulary.

- **#6819 (released, blocked on #6973-lineage):** Re-baselined the
  residual on `main` (98e715b7): `HexBerlekampZassenhausMathlib.Basic` is
  hard-red with 11 errors, all `hrep : RepresentsIntegerFactorAtLift`
  passed where the recovery wrappers expect the old scaled-product
  equality. They cannot be migrated without a coefficient bound on the
  carrier witness `RecoveredAtLift.monicFactor`; the consumers only bound
  `factor`, and `monicFactor = content · factor` so the bound does not
  transfer (and the scaled-product equality is not derivable from the
  carrier). Posted the diagnostic, added `depends-on: #6973`, skipped.

- **#6983 (released, sound but large):** Confirmed the non-monic-gcd
  scalar leak in `HexPolyFp/SquareFree.lean` (`yunFactorsWithLevel:2104`,
  `yunFactorsContributionWithLevel:2144`). Validated the cleanest fix
  direction by probe: `y := (normalizeMonic (DensePoly.gcd c w)).2` keeps
  emitted `z` and residual `w/y` monic, so no scalar accumulation is
  needed (smaller than the issue's Option 1). Measured blast radius:
  42 file-wide errors incl. 8 hard kernel timeouts (`normalizeMonic` adds
  reduction depth) and a cascading unknown-constant. Atomic, no
  intermediate compiling checkpoint, so no partial PR. Reverted the probe
  (tree clean), posted the scope data, skipped for a dedicated session.

## Current frontier

#6987 PR open. #6819 correctly blocked on the #6973 lineage
(now decomposed into #6989 bound-shape search helpers / #6990 monic-witness
bound threading / #6991 guard). #6983 awaiting a dedicated full-budget
session.

## Next step

- #6990 (monic-witness bound threading) is the keystone that unblocks
  #6819's 10 bound-dependent consumer sites; #6989 owns the executable
  scale-vs-dilate step (error 16622). Note the open question on #6990's
  proposed contract `∀ hrec, factor ∣ core → monicFactor bounded`: it is
  not a theorem (content is unbounded), so it must be discharged at
  concrete construction sites, not abstractly.
- #6983 needs a session that can rebuild `squareFreeAuxRevContribution_correct`
  off the false `YunDerivativeActiveRawStateProvider` after the monic-gcd
  change.

## Blockers

None for #6987. #6819 blocked on #6990/#6989. #6983 is a multi-session
atomic fix.
