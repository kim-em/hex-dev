# 2026-05-21T06:14Z — work issue #5738

## Accomplished

Claimed #5738 and added the Montgomery hot-loop smart constructor surface in
`HexModArith/HotLoop.lean`:

- `Hex.MontCtx.ofOddModulus`, building the wrapper from the indexed modulus,
  a proof that it fits below `UInt64.word`, and the underlying odd-modulus
  side condition.
- `[simp]` characterisation lemmas for the stored word modulus, its Nat value,
  and the delegated `_root_.MontCtx.mk` context.
- Updated `HexModArith/Conformance.lean` to use the smart constructor for the
  three Montgomery conformance contexts instead of manual record literals.

Verified:

- `lake build HexModArith.HotLoop`
- `lake build HexModArith.Conformance`
- `lake build HexModArith`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexModArith/HotLoop.lean HexModArith/Conformance.lean | rg -n -e
  '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returns no matches

## Current frontier

The Montgomery half of the hot-loop smart-constructor API is in place. The
Barrett half is tracked separately by #5737 and was already claimed by another
session during this one.

## Next step

Once #5737 lands, downstream callers can use smart constructors for both
hot-loop wrapper types. A later Phase 6 follow-up can address the report's
separate grind-coverage recommendation.

## Blockers

None. The worktree still contains an unrelated pre-existing
`.claude/CLAUDE.md` modification, which this session left untouched.
