## Accomplished

Claimed and completed #2997, the derivative-split quotient power gcd
successor step.

In `HexPolyFp/SquareFree.lean`, added private local divisibility helpers:

- `pow_succ_dvd_mul_of_dvd_left_of_pow_dvd_right`
- `pow_succ_dvd_mul_of_pow_dvd_left_of_dvd_right`
- `pow_succ_dvd_cofactor_mul_derivative`

Used them to prove the requested private theorem
`derivativeSplit_quotient_pow_succ_dvd_gcd`.

Verified:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  reports only pre-existing sorries in unrelated later proof obligations.

## Current frontier

The quotient-power successor lemma is available for the derivative-split
assembly path in `HexPolyFp/SquareFree.lean`.

## Next step

The next worker can continue with the final assembly issue that uses this
successor step to close the repeated-gcd forcing argument.

## Blockers

None.
