# Session b5a82ffe — BHKS A2 candidate equality wrapper (#3529)

## Accomplished

- Claimed #3529.
- Added `ForwardRecoveryInputs.candidatesOfMignottePrecision`, a narrow
  Mathlib-facing theorem that folds the single-indicator Mignotte
  reconstruction theorem over an expected indicator array to produce the
  executable `bhksIndicatorCandidates?` equality.
- Rewired `ForwardRecoveryInputs.ofMignottePrecisionCandidateProducts` to use
  the new candidate-equality theorem directly.
- Verified:
  `lake build HexBerlekampZassenhausMathlib.Recovery`,
  `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, and `git diff --check`.
- Checked
  `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/Recovery.lean HexBerlekampZassenhausMathlib/Basic.lean`;
  it reports only pre-existing `sorry`s in
  `HexBerlekampZassenhausMathlib/Basic.lean`.

## Current frontier

The A2 candidate-reconstruction packaging for #3529 is now exposed both as a
standalone folded candidate equality and as a full `ForwardRecoveryInputs`
constructor.

## Next step

Downstream BHKS recovery work can consume
`ForwardRecoveryInputs.candidatesOfMignottePrecision` when it only needs the
candidate fold equality, or
`ForwardRecoveryInputs.ofMignottePrecisionCandidateProducts` when constructing
the full recovery bundle.

## Blockers

None for this slice.
