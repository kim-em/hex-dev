# 2026-05-05T05:49:49Z — feature session ef90442b — issue #2284

## Accomplished

- Dropped the three `*HotRepeats` constants
  (`rowUpdateHotRepeats`, `gramDetVecHotRepeats`,
  `scaledCoeffsHotRepeats`) and the `repeatNatChecksum` /
  `repeatIntChecksum` helpers from `HexGramSchmidt/Bench.lean`.
- Rewrote `runGramDetVecChecksum`, `runScaledCoeffsChecksum`,
  `runSizeReduceChecksum`, `runAdjacentSwapChecksum` as
  single-shot bodies; the lean-bench parametric warm-mode
  auto-tuner picks `inner_repeats` itself.
- Added `signalFloorMultiplier := 1.0` to the two row-update
  registrations (matching the gramDetVec / scaledCoeffs
  registrations) so the linear helpers' rows survive the
  signal-floor filter.
- Updated the file-level docstring to drop the four "wraps a
  fixed hot loop" mentions; the complexity definitions and
  per-registration comments were already in per-call terms.

## Verification

- `lake build HexGramSchmidt.Bench` clean.
- `lake exe hexgramschmidt_bench verify` — 10/10 ok.
- For each affected target,
  `lake exe hexgramschmidt_bench run NAME`:
  - `runGramDetVecChecksum`: verdict consistent, repeats ×2^4–×2^6.
  - `runScaledCoeffsChecksum`: verdict consistent, repeats ×2^5–×2^8.
  - `runSizeReduceChecksum`: verdict consistent, repeats ×2^9–×2^11.
  - `runAdjacentSwapChecksum`: verdict consistent, repeats ×2^9–×2^11.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.

## Current frontier

Issue #2284 is the third human-oversight bench-cleanup issue;
the parallel HexConway (#2282) and HexLLL (#2283) cleanups
landed earlier today as #2289 and #2290. With this PR all
three Bench.lean modules have shed the hand-rolled hot loops.

## Next step

None for this corner. Other agents continue on the remaining
Phase-5 algebra work (Rabin bridges, Berlekamp soundness,
HexMatrix prefix sums, etc.).

## Blockers

None.
