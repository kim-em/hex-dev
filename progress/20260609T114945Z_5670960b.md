# 20260609T114945Z (#6649/#6655)

## Accomplished

Selected #6649 after directive audit found only blocked or already-claimed
directive work. Claimed #6649, confirmed the direct two-step log2 amortization
premise is false (`n = 21`, `x = 7` gives non-stopping steps `7 -> 5 -> 4`
with `Nat.log2` unchanged), and decomposed the parent into:

- #6655: phase-one Newton descent envelope.
- #6656: phase-two/final `floorSqrt_sq_le` composition, blocked on #6655.

Released #6649 with the required `Decomposed into #6655, #6656` breadcrumb.

Claimed #6655 and landed a compiled partial substrate in
`HexPolyZ/Mignotte.lean`:

- `sqrtStep_far_contracts`: if `4 * n ≤ x * x` and `0 < x`, one Newton step
  satisfies `8 * sqrtStep n x ≤ 5 * x`.
- `sqrtStep_far_lt_self`: the same far-above-root condition forces strict
  descent.

## Current frontier

#6655 is partially complete. The reusable far-state contraction lemmas are in
place, but the full phase-one envelope within `n.log2 + 1` steps remains open.

## Next step

Use `sqrtStep_far_contracts` to prove an iterated geometric descent statement
for `sqrtAux`: from `x = n`, repeated far-state steps enter the chosen near-root
envelope before the phase-one fuel budget is exhausted.

## Blockers

No technical blocker for the partial substrate. Verification performed:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`
