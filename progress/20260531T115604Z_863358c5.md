# 2026-05-31T11:56Z — session 863358c5 (`/once` on #5843)

## Accomplished

Triaged `#5843` (HO-5d-1b-cleanup) and decomposed it. The literal
"remove `fallbackPrimeChoiceData`" deliverable cannot be reached in a
single session because **neither of the two routes the issue body
named has a turnkey prerequisite landed**:

- Route (a) "unreachability proof from #5817 + #5819" — #5819 (PR
  #5846) extended `choosePrimeData?` to fall through to
  `extendedSmallPrimeCandidates` with fuel `128`, but the search is
  still bounded and no `choosePrimeData?_isSome_of_<precondition>`
  theorem exists; the existence statement would itself need a
  Mathlib-free number-theoretic argument (or a `sorry` carried into
  the public API).
- Route (b) "public-API Option cascade" — `factorWithBound?` and
  `factor?` exist as additive wedges (#5840), but the total
  `factorWithBound` / `factor` are still the canonical public
  entry points, the Mathlib bridge `Hex.factor_product` is stated
  on the total form, and the `choosePrimeDataWithFallback` /
  `fallbackPrimeChoiceData` cluster reaches ~78 internal references
  inside `HexBerlekampZassenhaus/Basic.lean` alone.

Residual consumers of total `Hex.choosePrimeData` survived the prior
HO-5d-2a/2b/2c/2d/2e statement-level migration (`#5853–#5857`):

- `HexBerlekampZassenhaus/Conformance.lean` — 9 `#guard` blocks.
- `HexBerlekampZassenhaus/SmallModSingleton.lean` — 1 theorem
  statement (`squareFreeCore_irreducible_of_small_mod_singleton`).
- `HexBerlekampZassenhausMathlib/Basic.lean` — 9 `_of_choosePrimeData`
  bridge wrappers still binding `let primeData := Hex.choosePrimeData core`
  in their statements (the `henselSubsetCorrespondence` /
  `liftedFactorSubsetPartition` cluster at lines 16341–17029).
- `HexBerlekampZassenhausMathlib/IntReductionMod.lean` — 6 proof-body
  `unfold Hex.choosePrimeData` patterns surviving below the
  statement-migrated signatures from #5855.
- `HexPolyZ/Mignotte.lean` — 1 reference.
- `reports/scratch/FactorTrace.lean` — 1 non-critical reference.

Filed 5 sub-issues covering the residual scope:

- `#5865` — `HexBerlekampZassenhaus/Conformance.lean` `#guard`
  migration (independent).
- `#5866` — `SmallModSingleton.lean` statement + `HexPolyZ/Mignotte.lean`
  + scratch (independent).
- `#5867` — `HBZMathlib/Basic.lean` `_of_choosePrimeData` bridge
  wrappers (independent).
- `#5868` — `HBZMathlib/IntReductionMod.lean` proof bodies,
  `depends-on: #5867` (downstream lemma signatures must migrate
  first).
- `#5869` — `HBZ/Basic.lean` executable-surface refactor + symbol
  removal, `depends-on: #5865, #5866, #5867, #5868` (the
  architectural closer; picks route (a) or (b) at claim time).

Left a breadcrumb comment on `#5843` and ran
`coordination skip 5843 "Decomposed into #5865, #5866, #5867, #5868, #5869 — see comment"`
so the next planner can either close it (sub-issues fully cover) or
narrow it to a residual scope.

## Current frontier

The 5 sub-issues are filed; `#5869` is the meaty one (picks the
architectural route). `#5865–#5867` are parallel-safe and could be
claimed by the next worker dispatch.

Baseline `lake build HexBerlekampZassenhaus` clean before decomposition
(only the pre-existing `HexBerlekampZassenhaus/Basic.lean:7536` `sorry`
warning).

## Next step

A planner closes `#5843` once `#5865–#5869` are filed (already done
here) — or narrows its body to the architectural closer if any
remaining work doesn't fit the 5 sub-issues. Workers can claim
`#5865`, `#5866`, `#5867` in parallel; `#5868` unblocks after `#5867`;
`#5869` after all four.

## Blockers

None on the decomposition itself. `#5869` will require a non-trivial
architectural decision (Option cascade vs proved unreachability with
sorry) at claim time; that issue's body documents both routes.
