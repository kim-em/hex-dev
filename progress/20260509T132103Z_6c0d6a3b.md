# 20260509T132103Z — HexConway Phase-4 evidence restoration (#2741)

## Accomplished

Resolved issue #2741 for the implemented `HexConway` Tier 1 surface.
Added `HexConway.phase4.input_families` metadata for the committed
Luebeck table path, restored `HexConway.done_through` to 4, committed a
scientific benchmark export at
`reports/bench-results/hex-conway-e7bf7c23bbb5.json`, and added the
headline report at `reports/hex-conway-performance.md`.

The scientific run at `e7bf7c23bbb5` on `carica` reported
`runLuebeckConwayPolynomialLookupChecksum` as `consistent with declared
complexity` over ordinals `1..36`; all eight fixed Tier 1 support and
irreducibility registrations matched their expected hashes. `samply`
produced empty sample tables on this macOS host, so the report records a
fallback `/usr/bin/sample` profile against a long-running LeanBench child
for the same Tier 1 lookup target.

## Current frontier

`HexConway` now satisfies the post-#2687 Phase-4 report/metadata
requirements for its current implemented API surface. Tier 2 full Conway
compatibility verification and Tier 3 on-demand Conway search remain
unimplemented and are therefore not advertised as Phase-4 input families
in this evidence package.

## Next step

Push the branch and open the PR closing #2741.

## Blockers

None.
