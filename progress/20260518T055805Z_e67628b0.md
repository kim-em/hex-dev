# 2026-05-18 05:58 UTC — Helper 2 substrate investigation (#4940, e67628b0)

Claimed feature issue #4940 ("BZMathlib: substrate investigation for
helper 2 — `henselLiftData_liftedFactor_monic_of_choosePrimeData`
primitive+pos-lc sibling") and produced a design recommendation
(Option C: architectural blocker) without code changes.

## Accomplished

* Re-read `HexHensel/QuadraticMultifactor.lean:1196`
  (`multifactorLiftQuadratic_each_monic`) and the upstream
  `henselLiftQuadratic_g_monic` / `_h_monic` lemmas.  Confirmed:
  `henselLiftQuadratic_h_monic` (line 781) requires `hf_monic`
  essentially; its proof routes through
  `monic_reduceModPow_of_congr_mul_monic_monic` (line 723) which forces
  the cofactor's leading coefficient to `lc(f)`.  For non-monic
  primitive core with `n` mod-`p` factors, the executable's `n-1`
  sequential splits produce `n-1` monic extracted factors and one
  non-monic residual factor with leading coefficient `lc(core)`.  The
  audit's claim — "the conclusion `∀ i, Monic liftedFactors[i]` is
  false for non-monic core" — is confirmed.

* Surveyed the three architectural options:
  - **Option A** (consumer-side substitution): traced the
    `hd_liftedFactor_monic` consumer chain from
    `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
    (line 14562) down through
    `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
    (line 14159),
    `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
    (line 14078),
    `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core`
    (line 14014), to
    `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
    (line 13651).  Every member of the existing `_of_primitive_pos_lc_core`
    family retains `hd_liftedFactor_monic` as a hypothesis — the
    chain's relaxation so far has touched core monicness only, not
    lifted-factor monicness.  Relaxing it requires co-redesigning the
    recombination invariants across the `_of_primitive_pos_lc_core`
    chain (≥ 10–15 sub-issues) plus the underlying
    `scaledRecombinationSearchMod` substrate in
    `HexBerlekampZassenhaus/Basic.lean`.
  - **Option B** (umbrella-internal scaling shim): traced
    `Hex.exhaustiveCoreFactorsWithBound`
    (`HexBerlekampZassenhaus/Basic.lean:5495`); it calls
    `henselLiftData core ... primeData` on the raw `core` with no
    monicising step.  Introducing scaling requires either modifying
    the executable's call site or introducing a `henselLiftData_scaled`
    variant; both induce cascading proof updates across
    `HexBerlekampZassenhausMathlib`.  Directive-level, not
    substrate-level.
  - **Option C** (architectural blocker): close #4880 as
    architecturally infeasible at present API surface, with the
    residual Gap 1 documented as removable only via a directive-level
    refactor (Option A or Option B).  The Gap 1 docstring's
    "internal monicising-by-scaling refactor" clause is the only
    viable removal path; the wrapper-relaxation clause (now landed via
    #4879) is necessary but not sufficient.

* **Recommendation: Option C.**  Posted the full audit confirmation,
  option survey, and recommendation as
  https://github.com/kim-em/hex/issues/4940#issuecomment-4474800987,
  then `coordination skip --replan`'d the issue with a one-line
  summary so the next planner cycle can act on the recommendation.

## Consequence for in-flight substrate issues

* Helper 1 substrate (#4939, currently claimed) has a relaxable
  conclusion (weakened mod-`p` product congruence), so its outcome is
  independent of this recommendation.  However, if the only downstream
  consumer is #4880's umbrella, #4939 inherits the same blocker; the
  planner should re-triage it based on whether the weakened
  conclusion has independent value.
* Helpers 3, 4 (the `_natDegree_pos` and `_injective` siblings) route
  through helper 2 internally and inherit the same architectural
  blocker.  Substrate sub-issues for them should not be filed.
* Helper 5 (`reassemblyExpansionComplete_exhaustive_of_ne_zero`)
  traces back to the same recombination substrate; same blocker.

## Current frontier

The four pending substrate sub-issues for helpers 2–5 of #4880 are
not actionable.  Future planner cycles should not file them; they
should close #4880 (with the residual Gap 1 documented) and refer
forward to a directive-level refactor for the removal.

## Next step

Future planner cycle:
1. Close #4940 (recommendation delivered).
2. Re-triage #4939 (helper 1 substrate) based on independent value.
3. Close #4880 with residual Gap 1 documented as architecturally
   infeasible at present API surface.
4. Update the umbrella docstring at
   `HexBerlekampZassenhausMathlib/IntReductionMod.lean:3589`'s Gap 1
   description to reference the Hensel-lift monicising refactor as
   the only viable removal path.

## Blockers

None for this issue — the investigation is complete and the
recommendation is posted.  The downstream consequence (closing #4880)
is planner-level work.
