# Session 28f404ce — partial #4013

## Accomplished

- Claimed #4013 and focused on the N32 generic irreducibility certificate
  in `HexGfq/CrossCheck.lean`.
- Added the cheap wrapper facts needed by the final N32 certificate
  assembly:
  - `genericN32_basisSize`
  - `genericN32_nPos`
  - `genericN32_finalEntry`
- Confirmed the existing executable Bezout `#guard` remains in place.
- Filed successor issue #4047 for the remaining kernel proof of the N32
  Bezout boolean and final `generic_irr` assembly, and marked it blocked on
  #4013.

## Current frontier

The N32 pow-chain proof and the small wrapper facts compile. The only
remaining N32 certificate gap is turning the executable
`Berlekamp.checkRabinBezoutWitnesses` guard into a theorem without using
`native_decide` or full monolithic kernel normalization.

## Next step

After this partial PR lands, claim #4047 and prove:

```lean
private theorem genericN32_bezout :
    Berlekamp.checkRabinBezoutWitnesses
      genericMod genericMod_monic genericN32SamePrimeCert = true
```

Then assemble `genericN32Cert_check` and replace the N32 `generic_irr`
placeholder via the existing Rabin soundness route.

## Blockers

The direct proof routes tried in this session were too expensive in kernel
mode: bounded coefficient equality over the full Bezout identity,
per-coefficient `decide`, and matching literal witnesses against
`DensePoly.xgcd` all caused `HexGfq/CrossCheck.lean` to spend minutes in
the N32 proof area. A narrower Bezout-specific checker/decomposition is
needed.
