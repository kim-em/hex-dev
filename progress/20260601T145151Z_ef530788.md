# ef530788 - work #5980

## Accomplished

- Claimed #5980 after checking the unclaimed work queue.
- Verified the post-#5979 witness-shaped fast-path dispatch already compiles
  through the named product and branch proof regions.
- Cleaned the remaining stale fallback wording in the core-success product
  helper and removed two unused `choosePrimeData` compatibility rewrites from
  `factorFastWithBound_product_of_some`.
- Verified with `lake build HexBerlekampZassenhaus`,
  `python3 scripts/check_dag.py`, `git diff --check`, the
  `choosePrimeData?_isSome` grep, and the forbidden-token diff grep.

## Current frontier

#5980 is reduced to proof-hygiene cleanup on top of the already-landed
witness-shaped dispatch. The product proofs now read directly through the
`choosePrimeData? = some primeData` witnesses without leftover fallback-path
setup.

## Next step

Open the PR for #5980. Follow-up work can continue from the fast-path branch
theorems without relying on the old total `choosePrimeData` fallback shape.

## Blockers

No blocker for #5980. The pre-existing `.claude/CLAUDE.md` worktree
modification was left untouched and excluded from this session's commit.
