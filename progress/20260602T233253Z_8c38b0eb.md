# 20260602T233253Z work #2567

## Accomplished

- Claimed directive #2567 and landed the next HO-4 BHKS bad-vector norm
  slice in `HexBerlekampZassenhausMathlib/BadVector.lean`.
- Added
  `BHKS.auxiliaryPolynomialWithCorrections_l2norm_sq_le`, the summed
  squared-`l2norm` lift of
  `BHKS.auxiliaryPolynomialWithCorrections_coeff_sq_le`.
- The new theorem bounds the corrected auxiliary polynomial by twice the
  existing CLD column Cauchy--Schwarz bound plus twice the summed diagonal
  correction contribution
  `Σⱼ correction_j² · p^(2·(k - cut(j)))`.

## Current frontier

The corrected auxiliary-polynomial side now has both per-coefficient and
squared-`l2norm` bounds.  The remaining HO-4 path still needs the diagonal
correction norm bound and the cap-level arithmetic that compares this bound
against the resultant divisor threshold, then plumbing into the closed
bad-vector/canonical-recovery package.

## Next step

Prove a bound on the summed diagonal correction contribution for the actual
cap-lift witness, then combine it with
`BHKS.auxiliaryPolynomialWithCorrections_l2norm_sq_le` to supply the
auxiliary-polynomial norm side needed by the executable bad-vector exclusion
theorem.

## Blockers

No Lean blocker for this slice.  The second-opinion wrapper was available but
did not return output before it was stopped, so no external review result was
incorporated.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVector:olean` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Lean file found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
