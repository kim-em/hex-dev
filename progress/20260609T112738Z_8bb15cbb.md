# 20260609T112738Z (#2567)

## Accomplished

Claimed directive issue #2567 and verified the two SPEC-named HO-4/D1+D2
surfaces in `HexBerlekampZassenhausMathlib/`.

`choosePrimeData?_none_implies_huge` was already present in
`HexBerlekampZassenhausMathlib/HotPathDiscriminant.lean`.

Added the missing SPEC D1 wrapper
`factorFast_terminates_of_choosePrimeData` in
`HexBerlekampZassenhausMathlib/Recovery.lean`. The wrapper case-splits the
executable
`Hex.choosePrimeData? (Hex.normalizeForFactor f).squareFreeCore ≠ none`
success, selects the resulting `PrimeChoiceData`, and dispatches to the
existing packaged BHKS/recovery assembly theorem `factorFast_terminates`.
The mathematical provider packages remain explicit; no new executable
definitions or mathematical assumptions were introduced.

## Current frontier

HO-4 final naming alignment is complete for the current bridge architecture:
the D1 name is now available in `Recovery.lean`, and the D2 name remains
available in `HotPathDiscriminant.lean`.

## Next step

Open the PR for #2567. Downstream callers can import
`HexBerlekampZassenhausMathlib` and refer to both documented theorem names.

## Blockers

None. Verification performed:

- `lake build HexBerlekampZassenhausMathlib` completed successfully.
- `python3 scripts/check_dag.py` completed successfully.
- `git diff --check` completed successfully.
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Recovery.lean`
  found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

Build output included pre-existing warnings in unrelated modules, including
existing `sorry` warnings in Mathlib-side files; this session added no new
forbidden tokens.
