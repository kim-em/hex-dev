# Package BHKS fast-support partition equality (#8057)

## Accomplished

Added `factorFastCoreWithBound_some_partition_eq_normalizedFactors_card` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean` (outside `namespace
BHKS`, beside the #8069 hsize producer). It is the partition-equality sibling of
`factorFastCoreWithBound_some_size_eq_indicators`: it specializes the existing
inner producer
`BHKS.supportPartitionByMinColumn_length_eq_normalizedFactors_card_of_toMonicPrimeData`
to the accepted `toMonicLiftData core B primeData` precision and presents the
canonical lifted true-support family `liftedTrueSupports core
(toMonicLiftData core B primeData)` at the projected-row index type
`Fin (projectedRowsOfLiftData core (toMonicLiftData core B primeData) rows_pos).factorCount`.

This is the exact `hpartition` shape consumed by
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_cut`, so #8058's
fast-raw capstone can supply it with the same calling convention it already uses
for the hsize witness.

Key fact verified while scoping: the index types coincide *definitionally*, so
the body is a direct application with no transport. The chain is
`(projectedRowsOfLiftData core d rows_pos).factorCount`
= `(bhksProjectedRows (latticeBasisOfLiftData core d) rows_pos).factorCount`
= `(latticeBasisOfLiftData core d).factorCount`
= `(bhksLatticeBasis core d.p d.k d.liftedFactors).factorCount`
= `d.liftedFactors.size` = `LiftedFactorIndex d`, each step a `let`/field
projection. The result-type ascription on `liftedTrueSupports` reuses that
defeq, and the inner producer's `r := d.liftedFactors.size` unifies against the
consumer's `r := projectedRows.factorCount`.

Deliverable #2 fallback applied: #8051's cut producer
(`BHKS.cutProjectionHypotheses_of_recoveredLift`, PR #8060) is parameterized
over a *free* `trueSupports`, so it names no support family. Per the issue, the
existing `liftedTrueSupports` family is used with an explicit statement.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.PartitionRefinement` green
(`PartitionRefinement` 4.4s, no slowdown). Full `lake build
HexBerlekampZassenhausMathlib` green. `scripts/check_dag.py` exit 0. `git diff
--check` clean. No forbidden tokens added. Annotation-free pure addition; no
existing proof, statement, or import touched.

The pre-existing `FactorSoundness.lean:18 declaration uses sorry`
(`factor_irreducible_of_nonUnit`) is the eventual public consumer and is out of
scope here. The `Recovery.lean:1874 unused variable` warning is also
pre-existing.

## Next step

#8058 (assemble BHKS fast raw irreducibility from cut data) can now consume
both local producers: `factorFastCoreWithBound_some_size_eq_indicators` (hsize)
and `factorFastCoreWithBound_some_partition_eq_normalizedFactors_card`
(hpartition), plus the #8060 cut package, leaving only the success-disjunct
plumbing.

## Blockers

None.
