# 20260609T143305Z (#4821/#4825/#6630/#6656/#6668)

## Accomplished

Audited and released several stale or prematurely unblocked claims:

- #4821: still blocked by missing non-circular analytic Hensel subset
  correspondence substrate. Posted concrete `Basic.lean` evidence and
  `coordination skip`ped.
- #4825: stale against the current three-tier `factor` combinator. The
  small-mod singleton pieces have landed, but the capstone still lacks
  slow-trial residual irreducibility and the #4680 Hensel analytic substrate.
  Posted evidence and skipped.
- #6630: still depends on `floorSqrt_sq_le`; #6649 decomposed that residual
  into #6655/#6656 and the theorem is not on `main`. Posted evidence and
  skipped.
- #6656: depends on #6655, but #6655 was closed in favour of #6668 for the
  remaining phase-one wrapper. Posted evidence and skipped.

Claimed #6668 and landed a compiled partial helper in `HexPolyZ/Mignotte.lean`:

- `sqrtAux_near_or_sq_le`: once an iterate is inside `sqrtNearEnvelope`, any
  remaining `sqrtAux` run either stops with `y * y ≤ n` or remains inside the
  near-root envelope.

## Current frontier

#6668 is partially complete. The new near-envelope preservation/stopped-result
helper gives the final phase-one wrapper a clean handoff once an iteration
enters `sqrtNearEnvelope`, but the iterated very-far induction itself is still
open.

## Next step

Finish #6668 by proving the wrapper that consumes
`sqrtStep_very_far_two_step_log2_lt`: starting from `x = n`, within the first
`n.log2 + 1` fuel, `sqrtAux` either stops or reaches `sqrtNearEnvelope`.

## Blockers

No blocker for the partial helper. Verification performed:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
