# Progress 2026-06-08T11:44Z — agent/7bf24745

## Accomplished

Closed issue #6588 (sub-issue B of #6576): packaged the prefix LLL
invariants under `swapStep` in `HexLLLMathlib/Independent.lean`.

- `swapStep_basisNormSq_below`: for `i + 1 < k`, the squared norm of the
  `i`-th Gram-Schmidt basis row is unchanged by `swapStep s k`.
- `swapStep_coeffs_below`: for `j < i` and `i + 1 < k`, the rational
  Gram-Schmidt coefficient at `(i, j)` is unchanged.
- `prefixLLLReduced`: predicate stating the first `k` rows satisfy size
  reduction and the adjacent Lovász condition; coincides with
  `isLLLReduced` at `k = n` and is vacuously true at `k ≤ 1`.
- `prefixLLLReduced.mono`: shrinking the prefix weakens the predicate.
- `swapStep_prefixLLLReduced`: `prefixLLLReduced s.b k δ` is preserved
  by `swapStep s k` with the prefix shrunk to `max (k - 1) 1`.

Both `_below` lemmas reuse the existing `basis_adjacentSwap_of_lt` and
`coeffs_rowSwap_adjacent_before` facts from `HexGramSchmidt`. The
preservation theorem handles the `k = 0` and `k ≥ n` edge cases by
falling back to `swapStep = id` plus the monotonicity lemma.

## Current frontier

Sub-issues A (size reduction) and C (Lovász correspondence) of #6576
remain claimable; D (loop induction) consumes all three.

## Next step

Pick up sub-issue A, C, or D as they become available.

## Blockers

None.
