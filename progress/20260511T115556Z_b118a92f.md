# 2026-05-11 11:55 UTC — feature #3393

## Accomplished

Claimed #3393 after the available human-oversight directives were blocked and
#3392 was claimed by another agent.

Added `BHKS.badVectorWitnessOfLiftData` in
`HexBerlekampZassenhausMathlib/Recovery.lean`, packaging the executable
bad-vector witness whose lattice and projected rows are exactly the
forward-recovery lift-data rows.

Added `BHKS.projectedRowsOfLiftData_eq_trueFactorIndicatorLattice_of_cap`,
which specializes the executable-cap separation theorem to the
`ForwardRecoveryInputs.lattice_eq_indicators` shape while leaving the residual
B7/A2 recovery obligations explicit.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build HexBerlekampZassenhausMathlib.TerminationBound`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/Recovery.lean HexBerlekampZassenhausMathlib/TerminationBound.lean`

## Current frontier

The cap-level `L' = W` theorem is now available directly in the recovery
module's projected-row vocabulary. Later work still needs to discharge the
indicator matching, candidate reconstruction, and product-check inputs.

## Next step

Use the new specialized theorem when assembling `ForwardRecoveryInputs` for
the BHKS fast-path termination proof.

## Blockers

None.
