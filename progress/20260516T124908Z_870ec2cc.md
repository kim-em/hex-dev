# 2026-05-16T12:50Z — #4500 HO-43 Round 2 relocation

## Accomplished

Claimed #4500 (HO-43 Round 2 relocation), executed the move of the
determinant/Bareiss bridge cluster from `HexGramSchmidt/Int.lean` and
`HexGramSchmidt/Update.lean` to `HexGramSchmidtMathlib/Int.lean` and
`HexGramSchmidtMathlib/Update.lean`.

The relocation closure (~1913 source lines) covers:

- `HexGramSchmidt/Int.lean` lines 2927-2948 (`scaledCoeffRows_lower_eq_scaledCoeffMatrix_bareiss`
  sorry'd helper + `scaledCoeffMatrix_bareiss_eq_det`).
- `HexGramSchmidt/Int.lean` lines 2986-4521 (Helpers for
  `gramDet_eq_prod_normSq_core` through `scaledCoeffs_eq`).
- `HexGramSchmidt/Int.lean` lines 4875-4988
  (`scaledCoeffMatrix_det_eq_zero_of_singularStep_lt`).
- `HexGramSchmidt/Int.lean` lines 5570-5760 (adjacent-swap pivot
  Gram-determinant product chain ending with
  `gramDet_rowSwap_adjacent_pivot_product`).
- `HexGramSchmidt/Update.lean` lines 226-275 (`gramDet_adjacentSwap_pivot`
  and `adjacentSwap_gramDetNumerator_dvd`).

The three `Matrix.bareiss_eq_det` occurrences in the relocated content
were rewritten to `HexMatrixMathlib.bareiss_eq_det` per the established
bridge-side naming convention.

Three fixups surfaced after the move:

1. `foldl_sum_start_rat`'s `nil` case (line ~265) and an anonymous
   foldl-with-acc inside `dot_prefixCombination_right_rat` (line ~326)
   had `simp; grind` — in the bridge file context, `simp` (with
   Mathlib's stronger simp set) closes the goal entirely, leaving
   `grind` with no goals. Trimmed both to bare `simp`.
2. `dot_castIntRow_castIntRow_eq_dot_basisPrefixProjection` (line
   ~1517) had a redundant `push_cast` that fired on an already-cast
   goal — removed.
3. `HexLLLMathlib/Basic.lean` consumes `Hex.GramSchmidt.Int.scaledCoeffs_eq`
   (now in the bridge); added `import HexGramSchmidtMathlib.Int`.

Quality metrics: build green across `HexGramSchmidt`,
`HexGramSchmidtMathlib`, `HexLLL`, `HexLLLMathlib`, `HexMatrix`,
`HexMatrixMathlib`, and the full `lake build`. `scripts/check_dag.py`
clean. Sorry count unchanged at 82. No axioms. No `native_decide`.
No `Matrix.bareiss_eq_det` references in `HexGramSchmidt/Int.lean`.
No new `^import Mathlib` lines in the core (`HexGramSchmidt`, `HexLLL`).
`git diff --check` clean.

## Current frontier

- #4500 PR ready to land.
- HO-43 directive: with #4499 + #4500 merged, the determinant/Bareiss
  bridge cluster is fully on the bridge side. The Mathlib-free orphan
  `Hex.Matrix.bareiss_eq_det` in `HexMatrix/Bareiss.lean:1342` can now
  be deleted by HO-44 (#3900).

## Next step

Land #4500. The HO-44 successor (#3900) can then delete the
sorry-bound orphan, completing the HO-43 → HO-44 cleanup.

## Blockers

None.
