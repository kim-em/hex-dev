# Issue #9661 progress

End-to-end certification of `HexBerlekampZassenhausMathlib` through Phase 7.

## Accomplished

- Re-audited the Phase-3 owned executable surface: polynomial reification,
  certificate replay, term assembly, and the `factor_poly`, `irreducibility`,
  `factor_poly!`, and `irreducibility!` tactic forms. The existing conformance
  driver covers ordinary and edge inputs, adversarial and negative diagnostics,
  and proof replay. Transported arithmetic remains owned by
  `HexBerlekampZassenhaus`.
- Re-ran the Phase-4 proof probes, matched baselines, benchmark verification,
  computational-owner comparison checks, fixture reproduction, and trace
  oracle. The declared proof-probe root, five-section report, provenance, and
  comparator classifications remain consistent.
- Confirmed the curated and `All` trust surfaces are free of `sorry`, `axiom`,
  and `native_decide`; rebuilt the complete regression surface.
- Completed the Phase-6 linter, API, naming, docstring, dead-code, import,
  namespace, automation, and emitted-term audit. Shortened the only public
  context-sentence declaration to `BHKS.recoveredShortVector`. Remaining
  linter reports are the repository-tolerated `simpNF` and intentional
  typeclass-argument findings.
- Re-audited and built the manual chapter. Added the README's exact
  public-umbrella checks, factorization term, and irreducibility example to the
  existing published `FactorPolyTests` regression module.
- Advanced `done_through` sequentially from 4 to 7, with separate commits for
  Phases 5, 6, and 7.

## Verification

- `lake build HexBerlekampZassenhausMathlib HexFactorizationModules HexReleaseTests HexConformance HexManual`
- `lake build HexBerlekampZassenhausMathlibProofProbe HexBerlekampZassenhausMathlibProofProbeScientific hexbz_bench hexbz_emit_fixtures`
- `lake exe hexbz_bench verify`
- `lake exe hexbz_bench compare runFactorCompareChecksum runFactorSlowCompareChecksum`
- fixture byte comparison, FLINT oracle when available, and the trace gate
- Phase 4, Phase 7, DAG, release-manifest, manual-split, and trust-surface checks

## Remaining

Publish the PR, obtain the requested independent review, and resolve any CI or
merge-base changes before merge. The phase counter delta is `4 -> 7`; the
published trust surface remains at zero forbidden declarations.
