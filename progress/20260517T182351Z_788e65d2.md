# Session 788e65d2 — #4733 capstone assembly

## Accomplished

Claimed and executed #4733 (HO-1 substrate, parent #4695): the forward
`represents_lifted_of_modP` direction of `HenselSubsetLiftHypotheses`.

Added to `HexBerlekampZassenhausMathlib/Basic.lean`, after the
`henselLiftData_liftedFactorProduct_monic` packaged-input helper (~line
7006):

* `modP_isZero_false_of_monic` (private): for a monic integer polynomial
  `f` and `1 < p`, `(Hex.ZPoly.modP p f).isZero = false`. Discharges the
  premise of `monicModularImage_modP_eq_of_monic` from monicness alone.
* `monicModPImage_modP_eq_self_of_monic` (private): consequence of the
  above plus the existing `monicModularImage_modP_eq_of_monic`. For
  monic `f` and prime `p > 1`, the `monicModPImage` of `Hex.ZPoly.modP p
  f` is just `Hex.ZPoly.modP p f` itself.
* `henselLiftData_represents_lifted_of_modP` (public): the capstone. A
  monic integer divisor of monic `core` that is represented modulo
  `primeData.p` by the modular-factor subset `S` is represented modulo
  `primeData.p ^ B` on the integer side by the canonical lifted subset
  `liftedSubsetOfModPSubset primeData d hsize S`.

Proof skeleton (`hensel_unique`-driven, as the issue directs):

1. Extract the integer-side complement `q` from `factor ∣ core` (so
   `core = factor * q`).
2. Set Mathlib polynomial aliases `f, g, h, g', h'`.
3. Monicness of `g` from `henselLiftData_liftedFactorProduct_monic`
   (#4752) and `g'` from `hfactor_monic`.
4. `(g.map φ_pB) * (h.map φ_pB) = f.map φ_pB` from
   `henselLiftData_liftedFactorProduct_subset_complement_congr_core`
   (#4752) plus `zpoly_congr_toPolynomial_map_eq` and
   `Polynomial.map_mul`.
5. `(g'.map φ_pB) * (h'.map φ_pB) = f.map φ_pB` trivially from
   `core = factor * q`.
6. `g.map φ_p = g'.map φ_p` bridged via
   `toPolynomial_liftedSubset_map_intCast_zmod_eq_toMathlibPolynomial`,
   the `RepresentsIntegerFactorModP` definition,
   `monicModPImage_modP_eq_self_of_monic`, and
   `toMathlibPolynomial_modP_eq_map_intCast_zmod`.
7. `g.natDegree = g'.natDegree` from `hg1` plus `Monic.natDegree_map`
   on both sides (using the automatic `Nontrivial (ZMod p)` instance
   under `Fact (Nat.Prime p)`).
8. `h.map φ_p = h'.map φ_p` from cancelling `g'.map φ_p` on both sides
   of `g.map φ_p * h.map φ_p = g'.map φ_p * h'.map φ_p`, via
   `mul_left_cancel₀` and the monic-implies-non-zero fact for
   `g'.map φ_p`.
9. `IsCoprime (g.map φ_p) (h.map φ_p)` from
   `henselLiftData_liftedSubset_complement_isCoprime_mod_p` (#4761,
   landed as PR #4763).
10. Feed everything to `HexHenselMathlib.hensel_unique`, take the first
    output `g.map φ_pB = g'.map φ_pB`, convert via
    `zpoly_congr_of_toPolynomial_map_eq` to
    `Hex.ZPoly.congr (liftedFactorProduct d liftedS) factor
    (primeData.p ^ B)`, and close `RepresentsIntegerFactorAtLift` via
    `Hex.ZPoly.reduceModPow_eq_of_congr` plus the
    `scaledLiftedFactorProduct = liftedFactorProduct` collapse for
    monic `core`.

Signature deviation from the issue sketch: the proof needs the same
analytic hypotheses as the existing `#4763` coprimality theorem (monic
`core`, `_root_.Nat.Prime primeData.p`, quadratic lift invariant, `1 ≤
B`, factors-monic, factors-product-mod-p, factors-irreducible,
factors-nodup) plus `Hex.DensePoly.Monic factor`. These are exactly the
inputs the constructor successor #4697 will package from
`Hex.choosePrimeData` boundary facts — the issue allows "equivalent
variants" provided they compose with #4697, which this signature does.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic`
* `lake build HexBerlekampZassenhausMathlib` (full library, clean)
* `python3 scripts/check_dag.py` (no output, clean)
* `git diff --check` (clean)
* `git show origin/main:HexBerlekampZassenhausMathlib/Basic.lean | grep -cE "^\s*sorry\s*$|by sorry"` → 3
* `grep -cE "^\s*sorry\s*$|by sorry" HexBerlekampZassenhausMathlib/Basic.lean` → 3
  (no new theorem-level `sorry` introduced)
* No `axiom`, `native_decide`, `TODO`, or `FIXME` introduced.

## Current frontier

* #4733 closes. Unblocks #4697 (HenselSubsetLiftHypotheses constructor)
  which in turn unblocks #4680 (analytic obligations composition).
* The HO-1 capstone chain (#4170) is now one substrate ring closer: the
  forward Hensel transport is done; the wrapping constructor remains.

## Next step

A planner re-triages: with #4733 merged, #4697 becomes claimable.

## Blockers

None.
