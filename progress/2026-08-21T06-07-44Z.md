# Finite-field SPEC review fixes

## Accomplished

- Rebased the finite-field SPEC rewrite onto current `origin/main` in a clean
  topic worktree, excluding unrelated changes from the original worktree.
- Obtained an independent high-effort review and verified every finding against
  the repository.
- Fixed the linear-factor split predicate, prime-field total fallback contract,
  imperfect-field square-free theorem boundary, extension benchmark placement,
  recursive EDF proof payload and termination measure, and finite-field oracle
  ownership.
- Clarified the coefficient Frobenius consumer through `frobInv`, avoided a
  global `IsCharP` parent instance, specified the Mathlib-free index-codec
  re-proofs, capped the crossover benchmark, and completed Lake/dependency
  manifest obligations.
- Removed review-history prose so the SPEC is a standalone current-state design.
- Checked balanced Markdown fences, local links, trailing whitespace, and the
  diff against upstream.

## Current frontier

- `SPEC/Libraries/hex-finite-field.md` is implementation-ready and staged for a
  focused documentation PR from `spec-finite-field-review-fixes`.

## Next step

- Commit and push the focused branch, open the upstream PR, verify job-level CI,
  and merge when green.

## Blockers

- None.
