# 2026-05-11T19:38Z — HexPoly/Dense Phase 6 API polish

Session: `/work` → claimed #3584 (HexPoly: review Dense Phase 6 API, label `review`).

## Accomplished

Polished `HexPoly/Dense.lean` for Phase 6 API quality, narrowly within
the scope set by issue #3584:

- Tagged `coeff_ofCoeffs`, `coeff_ofCoeffs_list`, and `coeff_C` with
  `@[simp]`. The `C` case follows Mathlib's `Polynomial.coeff_C`
  convention; the `ofCoeffs` companions let `simp` normalise the
  coefficient of an `ofCoeffs`-built polynomial without an explicit
  `rw`.
- Promoted `ext_coeff` to the `@[ext]` extensionality lemma (and
  removed `@[ext]` from `ext_of_size_eq`). `ext_coeff` asks only for
  coefficient agreement and is the form downstream callers actually
  reach for; the size-equality variant remains available by name.
- Added docstrings to every public theorem that another file could
  reasonably import: `trimTrailingZerosList_getD`,
  `trimTrailingZerosList_normalized`, `coeff_ofCoeffs`, `coeff_C`,
  `coeff_monomial`, `coeff_ofCoeffs_list`, `ext_coeff`, `size_zero`,
  `coeffs_C_zero`, `coeffs_C_of_ne_zero`, `size_C_le_one`,
  `degree?_C_getD`, matching the SPEC §Naming and documentation rule.

Did *not* tag `coeff_monomial` with `@[simp]`: trying it broke a
delicate manual `rw [DensePoly.coeff_monomial]` in
`HexBerlekamp/RabinSoundness.lean:636` after a preceding `simp` step
had already pre-normalised the goal. Fixing that downstream rewrite is
out of scope for this Phase 6 *Dense.lean* slice (RabinSoundness is a
correctness module, not the API surface), so the `@[simp]` was
withdrawn rather than touched downstream. Leaving the lemma untagged
preserves the current downstream proof shape; a future Phase 6 pass on
the Berlekamp side can revisit the tag.

## Verification

- `lake build HexPoly.Dense` — clean, no warnings.
- `lake build HexPoly` — clean.
- `lake build HexPolyFp HexPolyZ HexGfqRing HexBerlekamp HexConway` —
  clean (warnings unchanged from baseline: unused section variables in
  `HexBerlekamp/RabinSoundness.lean` lines 1134/1197/1210, present
  before the edit).
- `lake build HexPolyMathlib HexPolyZMathlib HexHensel HexHenselMathlib
  HexBerlekampMathlib HexBerlekampZassenhaus HexBerlekampZassenhausMathlib
  HexGfqField HexGfq HexGF2` — clean (pre-existing `sorry`s in
  `HexGfq/CrossCheck.lean` only).
- `python3 scripts/check_dag.py` — clean.
- `python3 scripts/status.py HexPoly` — Phase 6 (proof polishing),
  unchanged.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPoly/Dense.lean`
  — no matches.

## Current frontier

`HexPoly/Dense.lean` Phase 6 API surface is polished within the
declared scope. The remaining Dense-file work (e.g. adding a
characterising `size_C_of_ne_zero`, splitting `degree?_C_getD` into
two cleaner lemmas, audit of `support`/`toArray` annotations) is not
required by issue #3584 and would be churn beyond the "narrow wrapper
lemmas, theorem names, docstrings, and `[simp]` annotations" remit.

## Next step

After this PR lands, the natural follow-ups are the sibling Phase 6
reviews already declared out of scope here:
`HexPoly/Operations.lean`, `HexPoly/Euclid.lean`,
`HexPoly/Conformance.lean`, `HexPoly/Bench.lean`,
`HexPoly/EmitFixtures.lean`. Each will likely benefit from the same
treatment — promote characterising lemmas to `@[simp]`, document
public theorems, and audit `[ext]` placement.

## Blockers

None.
