# HexConway Phase 6 grind automation (#7973)

Session type: feature. Branch `agent/2feaffd9`.

## Accomplished

`@[grind]` automation pass over `HexConway/Basic.lean` (3826 lines).
Added 121 annotations across the full characterising-lemma surface and
shortened 3 proofs:

- **Equational lookup lemmas** (`luebeckConwayPolynomial?_hit_*` ×36,
  `_miss_*` ×3): `@[simp]` → `@[simp, grind =]`. These ground rewrites
  expose the committed Conway table to `grind`. Mirrors the sibling
  precedent #7960 (HexGramSchmidt), which used `@[grind =]` only.
- **Per-(p,n) ground Prop facts** (`_monic` ×36, `_degree_pos` ×6,
  `_irreducible` ×36): `@[grind .]`. A bare `@[grind]` on these
  hypothesis-free facts emits an `info: Try this … [grind .]`
  suggestion; the `.` form is `grind`'s recommended explicit usage for
  ground facts and builds silently. Patterns key on the committed
  polynomial constant, so no loop and no perf concern.
- **Generic projections** (`luebeckConwayPolynomial?_irreducible`,
  `?_monic`, `?_degree_pos`, `?_degree_eq`): `@[grind →]` forward rules
  firing on a `luebeckConwayPolynomial? p n = some f` hypothesis.
- **Payoff**: `conwayPoly_nonconstant` / `conwayPoly_irreducible` /
  `conwayPoly_monic` proofs collapse to `by grind`, chaining the
  existing `@[grind =>]` lookup rule with the new `→` projections.

The parametric `conwayPoly_*` wrappers keep their existing
`@[grind =>]` + `grind_pattern` form (correct for lemmas with a free
`SupportedEntry` variable); the `.` / `→` forms above are for the
ground and forward-implication shapes respectively.

## Verification

- `lake build HexConway` green, zero warnings/info.
- `git diff --check` clean; no new sorry/axiom/native_decide/TODO/FIXME.
- `python3 scripts/check_dag.py` exits 0.

## Current frontier

Issue deliverables complete. `libraries.yml done_through` intentionally
NOT bumped (out of scope per issue). No `*Mathlib` bridge touched.

## Next step

None for this issue. A future Phase 6 exit-criteria audit handles the
`done_through` bump for HexConway.

## Blockers

None.
