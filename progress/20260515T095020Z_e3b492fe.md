# Issue #4180

## Accomplished

- Claimed feature issue #4180 after all listed directives were either blocked
  or marked for replan and therefore not claimable.
- Added two executable-search bridge theorems in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  - `recombinationSearchMod_isSome_of_liftedSubset_candidate_eq_factor`
  - `recombinationSearchMod_isSome_of_liftedSubset_factor_dvd`
- These connect a proof-side `LiftedFactorSubset` containing the first local
  factor to `Hex.recombinationSearchMod_isSome_of_step`, using the existing
  ordered split bridge, candidate equality, `shouldRecord`, exact quotient,
  and recursive rest-search witness.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The first-success coverage theorem requested by #4180 is not complete.
- The new bridge handles the local executable success step once a candidate
  equality and a recursive rest-search success witness are available.
- The full #4180 theorem still needs the global first-success/uniqueness
  argument that identifies the actual returned split and proves membership or
  association for the requested irreducible factor.

## Next step

- Prove the first successful split's candidate represents an irreducible
  divisor of `core`, then use `HenselSubsetCorrespondenceHypotheses.unique_subset`
  to associate it with the requested factor and connect the resulting search
  success to `exhaustiveCoreFactorsWithBound`.

## Blockers

- No technical blocker for this partial bridge.
- Completing #4180 still requires additional global recursion/first-success
  structure beyond the local step lemma added here.
