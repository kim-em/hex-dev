# Accomplished

- Closed the mid-epoch growth hole: when a matcher exhausts an old frozen
  ceiling after the live network has grown, committing the reply requeues
  renewal even if the program-growth wakeup was previously queue-suppressed.
- Changed matcher visit accounting to charge the authenticated cursor delta.
  Structural retries now replay committed evidence at zero additional visit
  cost, while backwards progress and epoch transplants are rejected.
- Included source structural inputs in the cached policy cost of every retained
  suggestion and added an exact one-short traversal-budget canary.
- Replaced the registry smoke test with a real arbitrary-function callback.
  The callback recognizes a `sin (-x)` node from its bounded batch and
  immutable program view, emits `sin x`, `-(sin x)`, an equality, and an
  arbitrary scoped contractor, and reaches checked engine admission.
- Updated the SPEC and the stale lower-level cursor comment to match the live
  scheduler boundary.
- Built the complete 30-job interval target set successfully, ran
  `scripts/check_dag.py`, checked the diff, and found no banned proof shortcut
  in changed Lean code.

# Current frontier

The matcher scheduler now has conformance evidence for append-only growth
during an unfinished epoch, exact retry charging and cursor authentication,
retained-policy resource accounting, and a package-owned sine-shape
instantiation from callback through admission.

# Next step

Push the reviewed commit, let CI and the independent review run, then merge the
stack in dependency order before carrying matcher provenance into proof-payload
session and replay integration.

# Blockers

None.
