# PNT+ Table 10 convex-row batch

## Accomplished

- Preserved `ae4e207e143539eef0f7ee5e696d0a23d27a604b` as the exact parent and
  audited all 82 then-pending Table 10 numeric premises against pinned PNT+
  revision `21998bb6196b56789f72a52656a781a75e134eb0`.
- Identified rows 60, 65, 70, 75, 80, and 85 as the largest coherent batch
  sharing the same integer-endpoint convexity reduction. Rows 43/19·log 10,
  90/95, and 13800.7464 require different logarithmic or pointwise providers.
- Added one Mathlib-free six-row package authenticating 30 exact source
  coordinates and 60 endpoint inequalities, with bounded 30-fact planning,
  payload retention, coordinate diagnostics, and row-order authentication.
- Generalized the existing ordinary-kernel convexity lemma from row 25 to any
  interval beginning at 20, then proved `rowCell` and source-shaped `rowOfMem`
  for every authenticated coordinate. Generic schema/chronology replay and
  `ProofFrontend` close all 30 facts.
- Added false-endpoint, wrong-column, cross-row-order, and wrong-replay-target
  guards. All fail without drafts or precision retries; successful planning
  produces one 216-cell payload and exactly 30 retained facts.
- Corrected stale inventory prose: the generated 87 count is executable
  endpoint tactic occurrences, not distinct row/column declarations. This
  batch replaces 60 margin endpoint calls and, via its stronger row-60/k=5
  result, the two calls in the separate bare theorem. The inventory therefore
  records 62/87 covered and 25 target calls still pending.
- Updated both SPECs, source-pinned inventory evidence, Lake registrations,
  and the exact proof-only freshness exemption.
- Passed focused and retained PNT builds, exact-source inventory verification,
  inventory validation, copyright/line-count/DAG/Phase-4/trust/release/
  Mathlib-free static checks, factor freshness, diff hygiene, and the added-line
  banned-mechanism scan.

## Current frontier

The Table 10 migration remains partial. The remaining 25 executable target
calls use the row-43/logarithmic transition, the 19·log 10 transition,
pointwise rows 90/95, or the large decimal row. All 38 supporting `a₂` calls,
the surrounding `B_8_exact` reductions, arbitrary rows, and performance at
full table scale remain outside this batch.

## Next step

Keep this clean local stack unpushed until the parent selects the next Table 10
publication sequence. The next coherent numeric provider is the five-column
pointwise rows 90 and 95; the logarithmic transition should remain a separate
constant/logarithm slice.

## Blockers

None.
