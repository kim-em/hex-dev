# Session 9156c117 progress

## Accomplished

Triaged the unclaimed queue and committed work on the most tractable
item.

- Skipped #5817 (HO-5d-2 BZ proof-side migration): an active session
  (`700d7817`, iteration 20 at claim time) is concurrently migrating
  ~111 theorem references in the same `HexBerlekampZassenhaus/Basic.lean`
  file under #5842, and was actively building the Mathlib bridge. The
  proof-side migration of #5817 cannot proceed without colliding.
- Skipped #2564 (HO-1 BZ recombination): decomposition into #2637,
  #5214, #5215, #4170, #2567 remains adequate; three other sessions
  reached the same conclusion earlier in the day.
- Skipped #4334 (HexLLL Phase 4 bench): host load 5.60/5.96/5.74 with
  three active claude-code workers and a 100% CPU lean process — does
  not match the "quiet hardware" precondition the issue requires.
- Skipped #5765 (Schmeisser higher-degree source theorem): the issue
  body itself states this is planner-only work and two prior feature
  sessions released without progress.

Decomposed the newly-filed #5847 (universal 3-term Plücker identity)
into two sub-issues:

- #5849 — helpers (definitions + linearity + basis-vector evaluation)
- #5851 — final 3-term assembly (depends-on #5849)

The decomposition flagged a dimensional typo in the parent: the
signature `(B : Matrix R (n + 2) (n + 1))` is inconsistent with the
strategy's "`m_p` is `(n+1) × (n+1)`" and "`n_{p,q}` is `n × n`"
claims, which require `B : Matrix R (n + 2) n` so that
`[B | v]` is `(n+2) × (n+1)`. Both sub-issues adopt the corrected
signature.

Then claimed #5849 and built nearly the full deliverable in
`HexMatrix/Determinant.lean` (+576 lines):

- `skipIndex2 p q hpq` injects `Fin n` into `Fin (n + 2)` skipping
  two distinct positions in increasing order.
- `mMatrix B v p` / `mDet B v p`: the `(n+1) × (n+1)` minor of
  `[B | v]` with row `p` deleted (B columns at positions `0..n-1`,
  `v` at the last column).
- `nMatrix B p q hpq` / `nDet B p q hpq`: the `n × n` minor of `B`
  with rows `p, q` deleted (in increasing order).
- `mMatrix_eq_colReplace_last`: expose `mMatrix` as a `colReplace`
  on the last column over any base — the API the assembly uses to
  apply `det_colReplace_sum_finRange`.
- `basisVec q`: standard basis vector with `1` at position `q`.
- `foldl_add_zero_body` and `foldl_add_with_unique_match`: two
  general-purpose foldl helpers for Laplace-on-single-nonzero-entry.
- `det_eq_signed_minor_of_col_basis`: when a column of an
  `(n+1) × (n+1)` matrix equals a basis vector `e_q`, Laplace along
  it collapses to `cofactorSign q col * det (deleteRowCol q col)`.
- Basis-vector evaluation theorems covering all three Fin (n+2) cases:
  - `mDet_basisVec_eq_zero_of_eq` for `q = p` (returns 0)
  - `mDet_basisVec_eq_signed_nDet_of_lt` for `p.val < q.val`
  - `mDet_basisVec_eq_signed_nDet_of_gt` for `q.val < p.val`
- Supporting chained-skip identities
  `skipIndex_skipIndex_eq_skipIndex2_of_lt` / `_of_gt` that recover
  `nMatrix` row indexing from `mMatrix`'s nested `skipIndex` after
  deleting `r_q`.

Verified `lake build HexMatrix` and `python3 scripts/check_dag.py`
pass cleanly. `git diff` contains no `sorry`, `axiom`,
`native_decide`, `TODO`, or `FIXME`.

## Current frontier

Sub-issue #5849 is substantially delivered. The one remaining
deliverable from the original body — the explicit Vector-form
`mDet_add_v` / `mDet_smul_v` linearity wrappers — is unnecessary as
a separate lemma: the assembly (#5851) can apply
`det_colReplace_add` / `det_colReplace_smul` directly through
`mMatrix_eq_colReplace_last`, the foundation that the Vector-form
wrappers would package over. The PR notes this and leaves the
explicit Vector-form lemmas optional.

## Next step

Open a PR on #5849, then exit. The assembly issue #5851 picks up
naturally on `mMatrix_eq_colReplace_last` plus the three basis-vector
evaluation theorems.

## Blockers

None for #5849.

The skipped issues #5817, #2564, #4334, #5765 all have clear
unblocking conditions documented on the skip comments.
