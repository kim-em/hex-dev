# Hybrid BZ migration — product-certified hybrid + capstone re-scope

## Accomplished
- **#8397 (merged):** `factorHybrid`/`factorHybridTraced` — cost-based dispatch,
  classical-first → lattice on decline → trial backstop, traced. Documented the
  deviation from the directive's up-front cost estimator (classical-first
  dominates: classical declines cheaply, lattice grinds the cap).
- **#8398 (merged):** made `factorHybridTraced` **self-certifying** (accept a
  tier's `Factorization` only when `Factorization.product φ = f`, else the proven
  `factorSlowTrial` backstop) and proved `factorHybrid_product` unconditionally.
  No public swap; gates unchanged (100/0 FLINT, 50/0 trace). Also defense-in-depth.
- **#8382 deferred** with evidence: the sound form of its headline
  incremental-precision lift needs a per-remainder completeness certificate =
  #8395's scope; `exhaustiveLiftBound` is already tight Mignotte (no free win);
  remaining levers modest/risky and only informational-gated. Cross-linked #8395.
- Fixed a local build breakage: PR #8391 vendored `released/*` as submodules; a
  prior `git reset --hard` left them uninitialised. `git submodule update --init`.

## Current frontier
- **#8383 + #8384 are one capstone** (full analysis posted on both issues). The
  public `Hex.factor` carries proven contracts in BOTH layers:
  - Mathlib-free: `factor_product`, `factor_entry_*`, `factor_pairwise_first`,
    `factor_scalar` (via `factor_eq_factorWithBound_default`).
  - Mathlib layer: `HexBerlekampZassenhausMathlib/Basic.lean:397-436` proves
    `Hex.factor`'s recorded **entries are irreducible**, bridged through
    `factor_eq_factorWithBound_default` into `factorWithBound_entries_irreducible`
    (the #7738 Hensel-subset-correspondence apparatus in IntReductionMod.lean /
    PartitionRefinement.lean); `FactorSoundness.lean` consumes the Mathlib-free trio.
  Swapping `factor := factorHybrid` breaks the irreducibility bridge, and a runtime
  `product = f` certificate cannot restore it (necessary-not-sufficient). So the
  swap must land WITH the classical-tier irreducibility re-proof, or it regresses a
  proven soundness theorem (forbidden).

## Next step (the capstone, in order)
1. Make `scaledRecombinationSmart*` provable: it is a 3-way mutual `partial def`
   (Aux/SizeLoop/CandLoop, Basic.lean ~7771-7815) with ZERO lemmas. The proven
   analog `scaledRecombinationSearchModAux` (~7714) is instead a fuel-indexed
   structural `def` proved by `induction fuel`. Either add an explicit fuel
   parameter (mirror searchMod; behavior-preserving) or a lexicographic
   `termination_by` on (budget, list-being-processed). Fuel is the safe route.
2. Mirror searchMod's suite for the smart core: `_product` (template at
   `scaledRecombinationSearchModAux_product` ~12481), `_normalizeFactorSign`,
   `_primitive`, `_shouldRecord` (~12369-12387). The reconstruction invariant is
   identical (record `candidate` only when `exactQuotient? target candidate = some
   quotient`, recurse on quotient).
3. `factorClassical_product` + entry invariants (via `factorizationOfFactors_*`).
4. `factorLattice` product + entries (reuse `factorFastCoreWithBound_product` and
   the exhaustive-branch irreducibility apparatus; relate the smart/lattice core to
   the searchMod/exhaustive core where possible).
5. `factor := factorHybrid`; re-point Mathlib-free `factor_*` (product via
   `factorHybrid_product`, entries via the `factorizationOfFactors f raw` bridge —
   prove `factorHybrid f = factorizationOfFactors f (factorHybridFactors f)` once);
   re-point Mathlib-layer `factor_product`, entries-irreducible, FactorSoundness.
6. Conformance.lean expectations (it references `factorWithBound`/`factor`),
   EmitFixtures (already routes through `factor`), baselines; full gate green.
   Retire the cap-based combinator from the public path only — keep `factorWithBound`
   /`factorFast` defs (the lattice tier and ~dozens of proofs depend on them).

## Blockers
None hard. The capstone is large (partial-def proof + dual-layer re-pointing) and
is the right unit for a focused session; foundational product-soundness is banked.
