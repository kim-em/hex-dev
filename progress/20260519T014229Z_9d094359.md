# 2026-05-19T01:42Z — review #5182 (HexPolyFp/Basic.lean coeff_*_ring migration)

## Accomplished

- Migrated every `rw [DensePoly.coeff_add _ _ _ <hyp>]` /
  `coeff_sub _ _ _ <hyp>` / `coeff_neg _ _ <hyp>` call site in
  `HexPolyFp/Basic.lean` to the typeclass-specialized wrappers
  `coeff_add_semiring` / `coeff_sub_ring` / `coeff_neg_ring`
  introduced in #5169 and fixed in #5188.
- Deleted all 17 in-proof `have hzero_add : (0 : ZMod64 p) + 0 = 0`
  / `have hzero_sub` declarations and the cross-proof `let hzero_*`
  bindings that fed them, since the new wrappers absorb the discharge.
- Replaced two `exact zmod_add_zero_zero.symm` / `exact
  zmod_sub_zero_zero` term uses with `by grind`; deleted the
  `zmod_sub_zero_zero` private helper that became unused as a result.
  `zmod_add_zero_zero` survives because line 1412 still needs the
  discharge in `exact` position (grind cannot close `0 + 0 = 0` on
  ZMod64 there); per the issue body this is an accepted residual.
- One ripple: `coeff_neg_ring` produces `-x` where `coeff_neg`
  produced `0 - x`. The `rfl` line in
  `scalar_linear_factor_mul_dividedDifference_coeff` zero case
  relied on the old shape and was changed to `grind`.
- Diff: 60 insertions, 93 deletions in one file. Generic-shape
  rewrite count (`coeff_(add|sub|neg) _ _ _`) drops 57 → 0.

## Verification

- `lake build HexPolyFp.Basic` green
- `lake build HexPolyFp` green
- `lake build` green (244/244)
- `python3 scripts/check_dag.py` clean
- `git diff --check` clean
- No `sorry`, `axiom`, `native_decide`, `TODO`, `FIXME` introduced

## Current frontier

Sibling issues #5180 (`Compose.lean`), #5181 (`Quotient.lean` +
`QuotientFrobenius.lean`), #5183 (`SquareFree.lean`) cover the
remaining `HexPolyFp` files and are independent of this PR.

## Next step

Land this PR; pick up a sibling migration issue.

## Blockers

None.
