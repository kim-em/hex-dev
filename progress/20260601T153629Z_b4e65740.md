# b4e65740 - work #6055

## Accomplished

- Checked the available queue, claimed #5940, and skipped it as stale because
  prerequisite #5945 was closed after #6045 recorded the counterexample to the
  requested exact-degree-two open source theorem.
- Claimed #6055.
- Made `HexGFqField.invPoly` private and made the xgcd-shaped
  `toQuotient_inv_of_ne_zero` helper private.
- Removed the public `HexGFqField.repr_inv_of_ne_zero` theorem that exposed the
  inverse polynomial witness.
- Kept the public field-level inverse surface in `HexGFqField/Operations.lean`
  and documented `inv_zero`, `div_eq_mul_inv`, `mul_inv_cancel`, and
  `inv_mul_cancel`.
- Updated `HexGFq/Basic.lean` to stop re-exporting the xgcd-shaped inverse
  representative theorem and instead expose Conway-wrapper field-level inverse
  facts.
- Verified with `lake build HexGFqField`, `lake build HexGFq`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token
  grep over touched Lean files.

## Current frontier

#6055 is implemented as a focused API-boundary cleanup. The selected inverse
representative remains executable internally, while public callers now get
field-level inverse/division facts rather than the xgcd witness.

## Next step

Open the PR for #6055. A separate worker can handle the sibling prime-evidence
cleanup tracked outside this issue.

## Blockers

No code blocker. The worktree still has a pre-existing uncommitted
`.claude/CLAUDE.md` change that this session did not modify or include.
