# 75f181ff - work #6142

## Accomplished

- Claimed and executed feature issue #6142 (ordered `nMatrix` row-move
  determinant sign infrastructure in `HexMatrix/Determinant.lean`,
  decomposed from #6104).
- Added a private `rowMoveUp M src k h` defined by recursion on `k`:
  `rowMoveUp M src (k+1) h = rowMoveUp (rowSwap M ⟨src+k⟩ ⟨src+k+1⟩) src k _`.
  Each step swaps the moving row one position left; `k = 0` is the
  identity. Placed near the `nMatrix` / `nDet` block, before `twoColMatrix`.
- Proved the determinant sign theorem
  `det_rowMoveUp : det (rowMoveUp M src k h) = (-1 : R) ^ k * det M`
  by induction on `k`, using `det_rowSwap` and
  `Lean.Grind.Semiring.pow_succ` + `grind` for the algebraic step.
- Proved four row-content lemmas for case analysis by consumers:
  `rowMoveUp_row_of_lt` (`i.val < src`), `rowMoveUp_row_of_gt`
  (`src + k < i.val`), `rowMoveUp_row_eq_src` (`i.val = src`,
  returns `M[⟨src + k, h⟩]`), and `rowMoveUp_row_between`
  (`src < i.val ≤ src + k`, returns `M[⟨i.val - 1, _⟩]`).
- All lemmas are `private` and Mathlib-free; no `axiom`, `sorry`,
  `native_decide`, `TODO`, or `FIXME` introduced.
- Verified: `lake build` (244 jobs clean), `python3 scripts/check_dag.py`
  (DAG OK), `git diff --check` clean, forbidden-token diff grep empty.

## Current frontier

#6142 is complete and ready for PR. The downstream consumers (#6143's
four one-row `nMatrix setRow` transports and #6144's double-row
transport, both blocked on #6142) can now pair the row-content lemmas
with `nMatrix_entry` and `skipIndex2` value lemmas to identify the
moved matrix with the corresponding `nMatrix B a b hab`, multiplying
the appropriate `(-1)^k` from `det_rowMoveUp`.

## Next step

Create the PR for #6142. Once it lands, #6143 unblocks; once both #6143
and #6144 land, #6104's transport scope is fully covered and the raw
`nDet` Plucker assembly in #6105 can proceed.

## Blockers

None.
