# Planner session 144a86d7

## Accomplished

Filed #4866 (BZ Mathlib-free: add `recombineScaledExhaustive` and
structural invariant lemmas) as the first prerequisite sub-issue of
#4626, per the /replan handoff on closed #4639. The issue depends on
#4863 (currently claimed) and lands the scaled sibling of
`recombineExhaustive` plus its three structural invariant lemmas
(`_normalizeFactorSign`, `_shouldRecord`,
`_eq_of_scaledRecombinationSearchMod_some`). All additive in
`HexBerlekampZassenhaus/Basic.lean`; mirrors the existing unscaled
surface.

`recombineScaledExhaustive_product` was explicitly excluded — the
scaled candidate's product divides the core but does not equal it, so
that lemma needs a reshaped post-condition and belongs in a later
sub-issue together with its callers.

`coordination orient` reported `POD_QUEUE_DEFICIT=1` and one new
atomic issue was filed; no `set-target` adjustment per the planner
default.

## Current frontier

`#4863 → #4866` is the new dependency edge for the executable scaled
exhaustive bridge. Once both land, the planner can file the two
remaining /replan-requested sub-issues:

* call-site swap of `exhaustiveCoreFactorsWithBound` to the scaled
  wrapper (with `_product` post-condition reshape), and
* the scaled membership wrapper
  `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.

The successor wrapper-relaxation issue (the three `..._of_henselSubsetCorrespondence`
boundary changes) depends on both. #4640 (drop hcore_monic Gap 1) then
unblocks naturally.

## Next step

A future planner refiles the second prerequisite (call-site swap +
`_product` reshape) once #4866 is merged, or the worker on #4866 files
it as an in-line follow-on if the diff stays small.

## Blockers

None for this planner. #4866 is gated on the open #4863.
