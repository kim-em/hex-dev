# #6164: exact quotient monicity for Yun derivative-active step

## Accomplished

- Skipped #6125 (`HexPolyFp: refactor derivative-active provider consumers
  after tail invariant`) with a technical-blocker explanation: its declared
  dependency #6129 was closed as superseded (not completed), and the
  follow-on substrate work moved to #6155 (OPEN, blocked on #6163 and
  #6164). The 5 consumers' raw conclusions cannot be derived from the
  normalized provider without the Yun residual monic-preservation
  invariant tracked in #6155.
- Claimed and executed #6164. Added the substrate the Yun derivative-active
  monic invariant (#6155) needs from the exact-quotient side:
  - `ne_zero_of_monic_fpoly`: a monic `FpPoly p` is nonzero (uses
    `zmod64_one_ne_zero_of_prime` plus the defeq
    `leadingCoeff (0 : FpPoly p) = 0`).
  - `monic_of_mul_eq_monic_of_monic`: from `q * b = a` with `a` and `b`
    both monic, conclude `Monic q` via `FpPoly.leadingCoeff_mul` plus the
    nonzero witnesses derived above.
  - `monic_div_gcd_left_of_monic`: corollary for `c / gcd c w` via
    `div_gcd_mul_reconstruct`.
  - `monic_div_gcd_right_of_monic`: corollary for `w / gcd c w` via
    `div_gcd_right_mul_reconstruct`. This is the residual threaded into
    the next Yun derivative-active state.

All four lemmas are `private` and live in `HexPolyFp/SquareFree.lean`
immediately after the `gcd_mul_div_*_reconstruct` block (around line
1965), keeping the scope local per the issue's "exact quotient/residual
monicity for the derivative-active Yun transition" boundary.

## Current frontier

- `lake build HexPolyFp` is green.
- `lake build HexBerlekamp HexHensel HexPolyZ` is green (no downstream
  regression).
- `python3 scripts/check_dag.py` clean.
- No forbidden tokens added.

## Next step

- Land this PR.
- #6155 can now compose `monic_div_gcd_left_of_monic` /
  `monic_div_gcd_right_of_monic` with the gcd-output monicity that
  #6163 will supply, to prove the Yun derivative-active monic invariant
  by induction.
- Once #6155 lands, #6125 (still `replan`) can be re-pointed at #6155
  rather than the superseded #6129.

## Blockers

None for this issue.
