# HO-1 rollback slice (#2564)

## Accomplished

Claimed directive #2564 and completed its dedicated rollback PR slice:
`libraries.yml` now sets `HexBerlekampZassenhaus.done_through` from `2`
back to `0`, matching the issue's rollback deliverable for the post-#2563
CLD/Factorization rewrite.

Verified:

* `python3 scripts/status.py`
* `python3 scripts/check_dag.py`
* `git diff --check`

`scripts/status.py` now reports `HexBerlekampZassenhaus -> Phase 1` and
blocks `HexBerlekampZassenhausMathlib` on `HexBerlekampZassenhaus.done_through >= 3`.

## Current frontier

The rollback is complete. The implementation half of #2564 remains open:
the core still needs the SPEC-compliant `factorSlow`, `factorFast`, and
`factor` architecture with van Hoeij CLD recombination and the corresponding
Mathlib bridge proof obligations.

## Next step

Create successor implementation issues or claim an existing narrowed
implementation issue for the Phase 1 restart, beginning with a scoped
surface for the `Factorization`/slow-path public API before the CLD fast path.

## Blockers

None for the rollback slice.
