# 20260509T174105Z - Issue #2832

## Accomplished

Claimed #2832 and added the private Yun pairwise readiness boundary in
`HexPolyFp/SquareFree.lean`:

- `yunFactorsCurrentTailCoprime`, recording that the current emitted factor is
  coprime to every factor emitted by the tail Yun loop.
- `yunFactorsPairwiseReady`, a recursive invariant carrying that cross
  coprimeness obligation through each Yun step.
- `yunFactorsPairwiseInvariant`, packaging existing derivative-split
  reachability with the new readiness invariant.
- Step and derivative-split constructors for the packaged invariant.
- `yunFactors_pairwise_coprime_nil_of_ready`, proving the Yun output list is
  pairwise coprime once the readiness invariant is available.

Verification completed:

- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep output still shows the four pre-existing `sorry` markers in
`HexPolyFp/SquareFree.lean`; this session did not add new proof holes.

## Current frontier

The structural pairwise proof no longer needs an arbitrary-state theorem. The
remaining mathematical frontier is proving `yunFactorsPairwiseReady` for the
derivative-active initial split, i.e. discharging the concrete gcd/division
algebra behind `yunFactorsCurrentTailCoprime`.

## Next step

Prove the derivative-split readiness theorem using the existing gcd
divisibility/Bezout APIs, then change `yunFactors_pairwise_coprime_nil` to
consume `yunFactors_pairwise_coprime_nil_of_ready` for reachable Yun states.

## Blockers

No environmental blockers. The remaining blocker is proof-local algebra:
showing that `z = c / gcd c w` is coprime to every tail factor emitted from
`(gcd c w, w / gcd c w)` for derivative-active reachable states.
