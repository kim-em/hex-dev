# 2026-05-14 10:43 UTC — work session 93717627 (issue #4004)

## Accomplished

- Claimed #4004 after inspecting the unclaimed queue and selecting the active
  HexBZ irreducibility-chain prerequisite.
- In `HexBerlekampZassenhaus/Basic.lean`, exposed theorem-level wrappers for:
  - irreducibility of extracted `X`-power normalization factors;
  - constant-branch reassembly classification into irreducible `X` factors,
    repeated-part fallback factors, or the filtered core factor `1`;
  - irreducibility of factors emitted by `splitIntegerRootFactorsAux`;
  - quadratic-root branch classification into proven irreducible
    normalization/root factors, repeated-part fallback factors, or the optional
    residual from the quadratic splitter.

## Current frontier

#4004 now has the Mathlib-free branch-local facts needed by the later
per-branch irreducibility assembly. The optional quadratic residual and
repeated-part fallback are identified explicitly rather than hidden inside the
root-factor proof, so successor issues can either discharge them with a more
general linear primitive irreducibility lemma or avoid them when the branch
classifier proves they are filtered/expanded away.

## Next step

Continue with the remaining per-branch HexBZ irreducibility issues, especially
the small-mod, exhaustive-core, and fast-core branch proofs before the final
`factorWithBound_entries_irreducible` assembly.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhaus.Basic` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhausMathlib/Basic.lean`
  reports only pre-existing theorem-level placeholders.
