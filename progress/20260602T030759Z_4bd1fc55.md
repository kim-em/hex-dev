# Issue #6213: HexLLL per-family comparator plots

## Accomplished

- Claimed directive issue #6213 with `coordination claim 6213 --label work`.
- Added fpylll harsh-cubic fixed targets and benchmark registrations for `n = 20, 25, 30, 35, 40, 45, 50, 55` in `HexLLL/Bench.lean`, completing the existing `n = 15` ladder.
- Parameterized `scripts/plots/hex-lll-comparator.py` with required `--family {random-bounded,harsh-cubic}` selection, family-specific regex tables, default fpylll exports, and family-suffixed output paths.
- Renamed the old random-bounded plot to `reports/figures/hex-lll-comparator-random-bounded.svg`.
- Ran the nine-target fpylll harsh-cubic benchmark and committed the export target `reports/bench-results/hex-lll-fpylll-harsh-cubic-4a69408a680d.json` with SHA-256 `d726d21de7dbea2c8c251b8d5b36df062205b696d84f3dba95b46169c366ba3b`.
- Generated `reports/figures/hex-lll-comparator-harsh-cubic.svg` and regenerated the random-bounded plot via the new CLI.
- Updated `reports/hex-lll-performance.md` with both family plot embeds, the new harsh-cubic fpylll run metadata, per-rung harsh-cubic fpylll context, and the expanded fixed-target inventory.

## Current frontier

- The directive deliverables are implemented and verified locally.
- Rendered both SVGs through `rsvg-convert` and inspected the PNGs at `/tmp/hex-lll-plot-check/random-bounded.png` and `/tmp/hex-lll-plot-check/harsh-cubic.png`.
- The random-bounded plot shows the expected three curves over `n = 30..180`.
- The harsh-cubic plot shows Lean crossing Isabelle near `n = 45`, with fpylll below both at the largest rungs.
- The worktree still contains the unrelated pre-existing `.claude/CLAUDE.md` modification; this session did not touch or stage it.

## Next step

- Commit the HexLLL/report/script/progress changes, push the session branch, and open the PR for #6213.

## Blockers

- None.
- Verification passed: `lake build HexLLL.Bench`, `scripts/plots/hex-lll-comparator.py --family random-bounded`, `scripts/plots/hex-lll-comparator.py --family harsh-cubic`, `jq` target-count check for all nine fpylll harsh-cubic results, `rsvg-convert` rendering plus visual inspection of both plots, full `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, stale old-SVG reference check, and forbidden-token diff grep for `axiom` / `native_decide` in touched Lean changes.
