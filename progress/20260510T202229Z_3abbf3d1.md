# 2026-05-10 20:22 UTC — work session (3abbf3d1)

## Accomplished

- Claimed #3126 and verified the baseline `HexPoly` build.
- Added `DensePoly.nat_eq_one_of_content_eq_one_of_nat_dvd_coeff`, a public
  content helper showing that a natural divisor of every coefficient of a
  primitive integer polynomial must be `1`.
- Built `HexPoly.Euclid` and `HexPoly`, ran `scripts/check_dag.py`, and checked
  the diff for whitespace errors.
- Split the remaining hard regularity/Gauss lemma into follow-up #3133 and
  left the required decomposition breadcrumb on #3126.

## Current frontier

- The full #3126 theorem still needs the primitive-polynomial regularity
  argument modulo an arbitrary natural divisor: from `content p = 1` and
  coefficientwise divisibility of `p * q`, prove coefficientwise divisibility
  of `q`.
- #3133 is the focused follow-up for that McCoy/Dedekind-Mertens-style
  coefficient proof.

## Next step

- Prove #3133 in `HexPoly/Euclid.lean`, using the new content helper rather
  than duplicating content-gcd plumbing; then assemble the exact theorem shape
  needed by the `HexPolyZ.primitive_mul` wrapper issue.

## Blockers

- No technical blocker for the helper landed here.
- The remaining theorem is substantially harder than a local diagonal
  coefficient induction because `content p = 1` does not make any single
  coefficient of `p` invertible modulo a composite divisor.
