# Session 3dec3a92 — work #4106

## Accomplished

- Claimed #4106 and implemented scalar linear-factor root divisibility in
  `HexPolyFp/Basic.lean`.
- Added a private scalar divided-difference coefficient construction and
  coefficientwise reconstruction lemma for
  `f = C (eval f c) + (X - C c) * q`.
- Proved public theorem:
  - `FpPoly.X_sub_C_dvd_of_eval_eq_zero`
- Verified:
  - `lake build HexPolyFp.Basic`
  - `lake build HexBerlekamp.RabinSoundness`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -U0 -- HexPolyFp/Basic.lean | rg` for new `axiom` /
    `native_decide` / `TODO` / `FIXME` / `sorry`

## Current frontier

#4106 is complete. The new theorem provides the scalar root-to-linear-factor
divisibility hook needed by the prime-field product chain decomposed from
#4096.

## Next step

Use `FpPoly.X_sub_C_dvd_of_eval_eq_zero` in the successor Berlekamp
prime-field product proof to show every scalar root contributes its listed
linear factor.

## Blockers

- No blocker for #4106.
- Verification replayed an existing `sorry` warning in `HexMatrix/Bareiss.lean`;
  this session did not touch that file.
