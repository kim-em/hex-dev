# 2026-05-05 12:17 UTC — work session (c2a99029)

## Accomplished

Claimed #2351 after #2245 could not be claimed directly because it
needs replan. Investigated the `HexGfqField/Bench.lean` deterministic
modulus family with python-flint and found every scheduled modulus
reducible, including the degree-2 fallback:

* degree 2 factors as two linear factors.
* degrees 33, 49, 65, 97, 129, 193, 257, 513, 1025, 2049, 4097, 8193,
  and 16385 all factor nontrivially.

Fast-forwarded to `origin/main` to pick up the incremental Rabin
certificate checker and completed the bench-side cleanup:

* Replaced the generated `F_8191` modulus schedule with certificate-backed
  `F_7` moduli in degrees 2, 3, 4, and 6.
* Removed all 14 `sorry` irreducibility placeholders from
  `HexGfqField/Bench.lean`.
* Routed each bench modulus through `Berlekamp.rabinTest_imp_irreducible`
  using literal Rabin certificates; degree 6 uses the incremental checker.
* Updated all benchmark registrations to the proved degree schedule
  `#[2, 3, 4, 6]`.

Verification:

* `lake env lean HexGfqField/Bench.lean` succeeded.
* `python3 scripts/check_dag.py` succeeded.
* `python3 scripts/status.py HexGfqField` succeeded.
* `git diff --check` succeeded.
* `rg "sorry" HexGfqField/Bench.lean` returned no hits.
* `rg "^axiom |^private axiom |native_decide|TODO|FIXME" HexGfqField/Bench.lean`
  returned no hits.

`lake build HexGfqField`, `lake build HexGfqField.Bench`, and
`lake exe hexgfqfield_bench list` still hit the pre-existing
`HexBerlekamp` umbrella precompile failure:
`dyld: missing symbol called`.

## Current frontier

`HexGfqField/Bench.lean` no longer carries false irreducibility claims.
The scientific benchmark now covers a small proved schedule rather than
the previous large reducible generated family.

## Next step

If large-degree `HexGfqField` benchmarks are still required, create a
fresh issue to design a scalable source of proved irreducible large
moduli. The old deterministic family must not be reused for field
benchmarks because the scheduled moduli are reducible.

## Blockers

The `HexBerlekamp` umbrella target still fails locally with
`dyld: missing symbol called`, blocking Lake build/exe commands that
try to precompile the umbrella target even when the touched bench module
checks directly.
