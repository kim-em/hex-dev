# fast-BHKS multi-factor producer (#8282 steps 2-3): assembled except hsize

Session type: one-shot `/once` (feature). Issue #8282 (steps 2-3 of #8280).

## Accomplished

Built `factorFastCore_irreducible_of_liftedTrueSupport`
(`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`), the separation-free
self-contained multi-factor core producer prescribed by step 2: it discharges
**every** open hypothesis of the keystone endpoint
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift` at the
loop success precision `k'` from core facts — `hbasis`, the
`recoveredLiftOfLiftedTrueSupport` lift family, `hf_lc`, `hfactor_monic`, `hp`,
`hk`, `hsep`, `hthr`, `hfac`, `hbound`/`hcore_bound` (via (★)/(★★)), and
`hpartition`. The whole `HexBerlekampZassenhausMathlib` library builds with one
isolated `sorry` (`hsize`). Added `import …BHKSIndependent` to PartitionRefinement.
Committed on `agent/e5e21342` (`06de712`, not for merge).

Verified the index alignment is genuinely definitional
(`bhksLatticeBasis_factorCount_eq` is `rfl`), so `liftedTrueSupports core d`
feeds the 807 endpoint's `Fin (bhksProjectedRows L hrows).factorCount`
`trueSupports` directly.

## Current frontier — one residual, concretely characterised

`hsize` cannot be discharged from the listed leaf set. Root cause: the keystone
`recoveredLiftOfLiftedTrueSupport` is over the **monic** basis
(`bhksLatticeBasis (toMonic core).monic …`), which (since `RecoveredLift` is
basis-indexed) forces the 807 endpoint's `L` to the monic basis, so its `hsize`
is over the **monic** equivalence-class count. The executable
`factorFastCoreWithBound core …` runs the lattice on **`core`**
(`bhksRecoverClassified core …` → `bhksLatticeBasis core …`), so
`factorFastCoreWithBound_some_size_eq_indicators` only certifies the **core**
count. Bridging needs `core-classes = monic-classes`, reachable only via
monic-basis reverse separation — which the Constraints forbid.

Both obvious fixes are blocked: a core-basis keystone needs `Monic core`
(`recoveredLiftOfRepresents`), false for the non-monic squareFreeCore; and no
`toMonic`-invariance of `bhksEquivalenceClassIndicators` size exists.

Action taken: posted the concrete obstruction to #8282 and `coordination skip`
(→ replan). Step 3 (the `h_raw` dispatch) was not started — it depends on a
sorry-free step-2 producer.

## Next step

An updated premise needs one of: (a) a **core-basis** separation-free
recovered-lift keystone `RecoveredLift (bhksLatticeBasis core …) U` for
`U ∈ liftedTrueSupports core d`; or (b) a basis-bridge
`(indicators (core projected)).size = (indicators (monic projected)).size`
under the success-precision bounds. Then the scaffold on `agent/e5e21342`
closes step 2 by replacing the one `sorry`, and step 3 is the mechanical
dispatch in `factor_entries_irreducible` / `factor_irreducible_of_nonUnit`.

## Blockers

`hsize` residual genuinely needs reverse separation; reported per the issue's
own Constraints rather than bashed through with sorry/axiom or a #6779 route.
