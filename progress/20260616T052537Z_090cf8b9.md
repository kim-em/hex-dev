# Progress - 2026-06-16 (session 090cf8b9, work)

## Accomplished

Claimed #7521 and added the Recovery-side joint BHKS cap-lift API surfaces:

- `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_l2norm_joint`
- `FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSqAndJointPaperThreshold`
- `factorFast_terminates_ofBridgeDataCorrectedAuxiliaryL2normSqAndJointPaperThreshold`

The new corrected-squared wrapper preserves the single joint product
`l2norm(core)^aux.natDegree * auxiliaryBound^input.natDegree` and routes the
actual-cap comparison through `FactorFastCapLiftAnalyticComparison.ofL2normBounds`
instead of the older `coeffL2NormBound` strict-comparison constructor.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over `Recovery.lean` found no new `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The API threading piece is in place, but #7521 is not fully complete. The
dependency text for #7520 expected a standalone joint BHKS domination lemma; the
landed code currently exposes the lower-level `coeffL2NormBound_absorb_log`
arithmetic component instead. The #7441 capstone call site also remains blocked
on that standalone joint-domination producer, so there was no sound call site to
repoint at `C := 2` in this session.

## Next step

Prove or expose the standalone joint domination theorem that supplies the
`hjoint` premise for the new wrapper, then instantiate
`factorFast_terminates_ofBridgeDataCorrectedAuxiliaryL2normSqAndJointPaperThreshold`
at `C := 2` in the #7441 capstone route.

## Blockers

The capstone-specific deliverable needs the missing standalone joint-domination
producer, not another split `h_coeff` / `h_aux` wrapper.
