# PR #2036 repair

## Accomplished

- Claimed PR #2036 for repair.
- Merged `origin/main` into `agent/ca715d7e`.
- Resolved the `.github/workflows/conformance.yml` conflict by preserving both the HexPolyZ pyflint oracle steps from the PR and the HexGfq pyflint oracle steps from `main`.
- Verified `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, `python3 scripts/conformance_targets.py --check`, the HexPolyZ fixture diff, and `scripts/oracle/polyz_flint.py`.

## Current frontier

PR #2036 is locally mergeable after an additive workflow conflict resolution. The unrelated local `.claude/CLAUDE.md` modification remains unstaged and was not included in the repair.

## Next step

Push the repaired PR branch with `--force-with-lease` and mark PR #2036 salvaged.

## Blockers

None.
