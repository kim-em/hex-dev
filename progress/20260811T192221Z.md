# Accomplished

- Added an exact dyadic contraction-to-split conformance canary. A live
  centered backward contractor narrows the source from the whole line to
  `[1/4,3/4]`, after which the retained state is split exactly at `1/2`.
- Emitted the retained tree with the generic `BranchProof` fold. The root
  replays the live contraction before applying package-owned exact coverage;
  the left child proves the distance target and the right child closes through
  a package-owned exact-empty refutation.
- Made both exact child assumptions load-bearing and added sibling-substitution
  mutation checks that the branch frontend rejects.
- Added the module to the existing `HexConformance` target and documented the
  concrete canary in the HexInterval SPEC.
- Verified `lake build HexConformance`, `git diff --check`, the theorem's axiom
  report, and a forbidden-token scan for `native_decide`, `axiom`, and `sorry`.

# Current frontier

The exact-domain path now has one end-to-end useful theorem whose proof is
emitted from a live post-contraction branch tree with exact child inputs.

# Next step

Review whether later examples should reuse these package-owned schemas or add a
nested/child-local split once the corresponding runtime acceptance cases are
ready for theorem-level coverage.

# Blockers

None.
