# 2026-05-06 18:46 UTC — work session (6d1f618f)

## Accomplished

Claimed #2510 and advanced the row-pivoted Bareiss array/structured bridge in
`HexMatrix/Bareiss.lean`:

- added generic pointwise bridge lemmas for row swaps and pivot search over
  array rows matching a structured matrix;
- rewrote `stepArray` as a range-built array recurrence using the same
  `exactDiv` operation as `stepMatrix`;
- proved the entrywise `stepArray`/`stepMatrix` correspondence;
- exposed public `pivotLoop` and added `bareissData_eq_finish_pivotLoop`, giving
  downstream determinant proofs a structured `BareissState` path from
  `bareissData`.

Verification run:

- `lake build HexMatrix.Bareiss`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n 'axiom |native_decide|TODO|FIXME' HexMatrix/Bareiss.lean`

No new `axiom`, `native_decide`, `TODO`, or `FIXME` hits were introduced. The
only remaining `sorry` in `HexMatrix/Bareiss.lean` is the pre-existing public
`bareiss_eq_det` capstone.

## Current frontier

#2510 has a usable structured pivot-loop bridge for #2439 via
`bareissData_eq_finish_pivotLoop`. The private executable `pivotArrayLoop`
remains in the file, but `bareissData` now packages the structured loop state
through array storage.

## Next step

Use `bareissData_eq_finish_pivotLoop` from `HexMatrixMathlib` to prove the
row-pivoted determinant soundness theorem tracked by #2439.

## Blockers

No technical blockers. The worktree still has the pre-existing unrelated
`.claude/CLAUDE.md` modification, which this session did not touch.
