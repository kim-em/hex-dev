# HexPoly Phase 7 — HexManual reference chapter (#8126)

## Accomplished

Wrote `HexManual/Chapters/HexPoly.lean`, the second Phase 7 reference
chapter (after `HexGFqRing`), documenting the normalized dense-polynomial
layer. Sections: introduction, core types (`DensePoly`,
`DensePolyNormalized`, `ext_coeff`), constructors/normalization
(`trimTrailingZeros`, `ofCoeffs`, `ofList`, `zero`, `C`, `monomial`),
structural queries (`size`, `isZero`, `coeff`, `degree?`, `support`,
`toArray` plus the `coeff_*` characterisations), principal operations
(`add`/`sub`/`neg`/`scale`/`shift`/`mul`/`eval`/`compose`/`derivative`
with their coefficient laws), the Euclidean layer (`leadingCoeff`,
`Monic`, `divModMonic`, `divMod`, `div`, `mod`, `modByMonic`, `gcd`,
`xgcd`), key correctness theorems (`divMod_spec`, `div_mul_add_mod`,
`mod_degree_lt_of_pos_degree`, `gcd_dvd_left/right`, `dvd_gcd`,
`xgcd_bezout`), and cross-references to `HexPolyMathlib`/`HexPolyZ`/
`HexPolyFp`. Two compiling worked examples: integer arithmetic over
`DensePoly Int` and division/gcd over `DensePoly Rat`.

Wired the chapter into `HexManual.lean` (import + `{include}`) and bumped
`libraries.yml` `HexPoly.done_through` 6 → 7.

Added field docstrings to the `DensePoly` structure (`coeffs`,
`normalized`) in `HexPoly/Dense.lean` — Verso `{docstring Hex.DensePoly}`
on a structure requires every field documented.

## Current frontier

`lake build HexManual` green (249 jobs, no warnings — all code-block
column limits satisfied, all `{docstring}`/`{name}` references resolve
against the live API). `scripts/check_dag.py` exit 0. `git diff --check`
clean. No `sorry`/`axiom`/`native_decide`.

## Next step

None for this issue. Remaining Phase 6 grind sweeps still unclaimed:
#8121, #8123, #8124 (HexGF2/Field, HexGFqField). Future Phase 7 chapters
for other `done_through ≥ 6` libraries.

## Blockers

None.
