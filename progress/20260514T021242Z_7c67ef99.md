# 2026-05-14 02:12 UTC — work session 7c67ef99 (issue #3908)

## Accomplished

Claimed #3908 and completed the first LLL state split:

- `HexLLL/Basic.lean` now states `LLLState.ν_eq` as a direct executable
  invariant against `GramSchmidt.Int.scaledCoeffs`, rather than as the
  rational `d[j+1] * coeffs[i][j]` bridge.
- `LLLState.sizeReduceColumn` now proves the `ν` field update using
  executable scaled-coefficient size-reduction formulas for lower, pivot,
  above-pivot, and other-row cases.
- `LLLState.ofBasis` and the conformance `stateOf` constructor now discharge
  `ν_eq` by definitional equality with `GramSchmidt.Int.data`.
- Added `HexLLLMathlib.lllState_ν_eq_coeffs` as the bridge theorem that
  recovers the old rational Gram-Schmidt coefficient relation outside the
  Mathlib-free state invariant.
- Created follow-up #3914 for the residual Mathlib-free row-add update proof
  work and left a decomposition breadcrumb on #3908.

## Current frontier

The executable LLL state no longer calls `GramSchmidt.Int.scaledCoeffs_eq`.
The remaining direct size-reduction dependencies in `HexLLL/Basic.lean` are:

- `GramSchmidt.Int.scaledCoeffs_sizeReduce_pivot`
- `GramSchmidt.Int.gramDet_sizeReduce`

The `gramDet_sizeReduce` chain still reaches `gramDet_rowAdd_earlier`, whose
proof in `HexGramSchmidt/Int.lean` uses `Matrix.bareiss_eq_det`. That residual
is isolated in #3914.

## Next step

Land this partial PR for #3908, then have a worker take #3914 to make the
GramSchmidt size-reduction update formulas Mathlib-free enough for the final
#3908 verification grep to go clean.

## Blockers

None for the partial state split. Full #3908 completion is blocked on the new
Mathlib-free `gramDet` and scaled-coefficient row-add preservation proofs
tracked by #3914.
