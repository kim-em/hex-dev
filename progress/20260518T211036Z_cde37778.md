# cde37778 — extract fpPoly_mul_dvd_mul helper (issue #5091)

## Accomplished

- Added `private theorem fpPoly_mul_dvd_mul` to
  `HexBerlekampZassenhausMathlib/Basic.lean` beside the existing
  `fpPoly_dvd_trans` helper.
- Proved the helper directly from the executable `Hex.FpPoly p`
  divisibility witnesses using only `Hex.FpPoly.mul_assoc` and
  `Hex.FpPoly.mul_comm`.
- Rewired the `hrawGcd_sq_dvd_prod` proof in
  `quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared`
  to a single call:
  `fpPoly_mul_dvd_mul hrawGcd_dvd_g hrawGcd_dvd_tail`.
- Removed the inline `refine ⟨ka * kb, ?_⟩` witness and the
  five-step `calc rawGcd * ka ...` associativity/commutativity chain.

## Current frontier

The FpPoly divisibility helper band now has first-class helpers for
both bespoke transitivity (`fpPoly_dvd_trans`) and product-of-divisors
composition (`fpPoly_mul_dvd_mul`). The targeted recursive proof now
shows the high-level square-divisibility step directly instead of
spelling out witness reassociation locally.

## Next step

A future planner cycle can dispatch the post-extraction audit issue
mentioned in #5091, checking the same invariants used for earlier
FpPoly helper extractions.

## Blockers

None. Verification preserves the known `Basic.lean` failure baseline:
`lake build HexBerlekampZassenhausMathlib.Basic` and
`lake build HexBerlekampZassenhausMathlib` both fail only at the
pre-existing 16 `Basic.lean` errors, shifted by the helper insertion.
`python3 scripts/check_dag.py` and `git diff --check` exit 0.
