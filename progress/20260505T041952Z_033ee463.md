# Work session: HexLLL.sizeReduceColumn ν_eq witness field

## Accomplished

- Claimed #2212 (label: `feature`).
- In `HexLLL/Basic.lean`, replaced the remaining `sorry` in
  `LLLState.sizeReduceColumn.ν_eq` with a complete proof, decomposed by
  the four-case row/column split:
  - Row `i ≠ k`: `ν' = s.ν.set k rowK` agrees with `s.ν` at row `i` by
    `Vector.getElem_set_ne`; the row's coeffs entries are unchanged via
    `GramSchmidt.Int.coeffs_sizeReduce_other_row`. Reduces to `s.ν_eq`.
  - Row `i = k`, column `l < j.val`: the foldl writes
    `(s.ν.get k).get l - r * (s.ν.get j).get l`; the `.set j` overwrite
    does not touch position `l`. Coeffs get
    `coeffs_sizeReduce_lower`. Combined via `s.ν_eq` for `(k, l)` and
    `(j, l)` and a `grind` ring step.
  - Row `i = k`, column `l = j.val`: the `.set j` overwrite gives
    `(s.ν.get k).get j - r * Int.ofNat dj1`. To avoid the `hnorm`
    precondition of `coeffs_sizeReduce_pivot`, the proof bridges through
    `scaledCoeffs`: combine `s.ν_eq`, `s.d_eq`, and
    `scaledCoeffs_eq` to identify `s.ν[k][j]` with
    `scaledCoeffs(s.b)[k][j]` in `Int`, then apply
    `scaledCoeffs_sizeReduce_pivot` (no preconditions) and the
    `scaledCoeffs_eq`/`gramDet_sizeReduce` round trip to convert back.
  - Row `i = k`, column `l > j.val` (with `l < k.val`): row entry
    unchanged; coeffs get `coeffs_sizeReduce_above_pivot`. Reduces to
    `s.ν_eq`.
- Added two private helper lemmas:
  - `foldl_set_outerSubMul_get_eq` — generic `xs : List (Fin n)` form,
    proved by induction.
  - `foldl_finRange_set_outerSubMul_get_eq` — specialized to
    `List.finRange jVal`, bridges via `List.foldl_map` to the generic
    helper.
- Verified:
  - `lake build HexLLL.Basic`
  - `lake build HexLLL`
  - `lake build` (full)
  - `python3 scripts/status.py HexLLL` (still `Phase 5`, ready)
  - `python3 scripts/check_dag.py` (clean)
  - `git diff --check` (clean)
  - No `axiom`, `native_decide`, `TODO`, `FIXME`, or new `sorry` in
    the touched file. The `sizeReduceColumn.ν_eq` proof field sorry is
    gone.

## Current frontier

`LLLState.sizeReduceColumn` proof fields are fully discharged
(`d_eq` was already proven in #2219; `ν_eq` is now proven). The
remaining HexLLL Phase 5 sorries are at higher layers:
`sizeReduce_d`, `sizeReduce_basis`, `swapStep_*` proof fields,
`swapStep_memLattice_iff`, and `lllAux` termination.

## Next step

Sibling issues (e.g. `sizeReduce_d`, `swapStep` proof fields) can now
build on the discharged `sizeReduceColumn` proof obligations.

## Blockers

None.
