# 2026-05-15 23:41 UTC - #4301 partial

## Accomplished

Claimed #4339 first, audited the requested HO-43 Round 3a relocation, and
marked it for replan because the current split would strand row-add and
adjacent-swap consumers in Mathlib-free modules after relocating their shared
Cramer/Bareiss dependencies.

Claimed #4301 and added
`recombinationSearchModAux_isSome_of_liftedSubset_candidate_eq_factor_of_matches`
in `HexBerlekampZassenhausMathlib/Basic.lean`. The new lemma generalises the
existing one-step recombination-search success bridge from the universal
lifted-factor list to an arbitrary matched remaining-index set `J`, using
`LiftedFactorListMatches` and
`liftedSubsetSplit_mem_subsetSplitsWithFirst_of_matches`.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

#4301 still needs the full recursive coverage theorem over
`LiftedFactorSubsetPartition`. The landed helper discharges the executable
one-step descent for the branch where a representing subset `S ⊆ J` contains
`J.min'`.

## Next step

Use the new matched-rest one-step lemma inside the `LiftedFactorSubsetPartition`
induction. The remaining proof must split on whether the requested factor's
representing subset contains `J.min'`, use `cover_at_min` otherwise, and
transport both partition and list-matching state across the recursive call.

## Blockers

No technical blocker for the new helper. The full #4301 theorem remains a
larger induction and is being published as partial progress.
