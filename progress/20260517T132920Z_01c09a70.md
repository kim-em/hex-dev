# 20260517T132920Z — session 01c09a70 — #4704 feature

## Accomplished

- Claimed and completed #4704 (HO-1 substrate sub of #4675): added
  `IntReductionMod.normalizeForFactor_repeatedPart_map_intCast_dvd_squareFreeCore_map_intCast_pow`,
  the rational-side divisibility theorem
  `∃ N, (toPolynomial (normalizeForFactor f).repeatedPart).map intCast ∣
       ((toPolynomial (normalizeForFactor f).squareFreeCore).map intCast) ^ N`
  over `Polynomial ℚ`.
- Added two private helpers to keep the
  `unfold primitiveSquareFreeDecomposition` case-split self-contained and
  avoid `whnf` heartbeat issues at the call site:
  `psd_repeatedPart_eq_one_of_derivative_isZero` (Case B) and
  `psd_repeatedPart_eq_ratPolyPrimitivePart_gcd_of_derivative_not_isZero`
  (Case C).

## Frontier

The proof composes the new helpers as planned in the issue body:

1. Reduce to the `core`-level statement via
   `extractXPower_core_{ne_zero,primitive}_of_ne_zero` and
   `primitivePart_eq_self_of_primitive`.
2. Move into `HexPolyMathlib.toPolynomial (toRatPoly _)` form via
   `toPolynomial_toRatPoly_eq_map_intCast`.
3. Extract the rational reassembly unit through
   `primitiveSquareFreeDecomposition_reassembly_over_rat core` and use
   `primitiveSquareFreeDecomposition_primitive` + the primitivity rewrite
   to land in `toRatPoly core = scale unit (toRatPoly sf * toRatPoly rp)`,
   yielding `Associated P (S * R)` after applying `toPolynomial`.
4. Case-split on `(DensePoly.derivative (toRatPoly (primitivePart core))).isZero`:
   - Case B (derivative vanishes): the two helpers identify `rp = 1`, and
     `eq_C_of_derivative_eq_zero` + `isUnit_C` + `isUnit_iff_ne_zero` make
     `P` a unit, so `R = 1 ~ gcd P 0 = P`.
   - Case C (derivative nonzero): the helpers identify
     `rp = ratPolyPrimitivePart (gcd ratPrimitive derivative)`, and
     `ratPolyPrimitivePart_rational_associate` + `toPolynomial_gcd_associated`
     give `Associated R (gcd P P.derivative)` after cancelling the unit
     scalar `C w`.
5. The abstract `rp_dvd_sf_pow_of_associated` (landed in #4705) closes it.

## Next step

The downstream `#4611` exponent-extraction step now has its prerequisite
satisfied and should be unblocked once `coordination check-blocked` runs.
`#4676` (the integer Gauss descent) likewise consumes this theorem.

## Blockers

None encountered. The branch was originally based on `agent/9383b3a1`
(PR #4705); after #4705 squash-merged to `main`, the branch was reset
to `origin/main` and the new theorem re-applied cleanly.
