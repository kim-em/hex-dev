# 20260509T182309Z - Issue #2830

## Accomplished

Claimed #2830 and added private determinant proof infrastructure in
`HexMatrix/Determinant.lean` for the inserted-last inverse-permutation step:

- lookup through `insertAt` at positions lifted by `raiseFinAbove`;
- finite-count lemmas showing a full `Fin n` permutation has `n - i.val`
  entries above the insertion point;
- the shape theorem identifying
  `inversePermutationValues (insertAt (Fin.last n) (v.map Fin.castSucc) i)`
  as the old inverse mapped by `raiseFinAbove i` with `i` appended;
- the insertion-count contribution theorem for the inverse vector.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "^axiom\\b|native_decide|TODO|FIXME|sorry" HexMatrix/Determinant.lean`

## Current frontier

#2830 now supplies the inverse-side shape/count lemma needed by #2828. The
remaining downstream work is to use this theorem with the existing forward
insertion count to prove the inverse-permutation inversion-count parity step.

## Next step

Create the PR for #2830. After it lands, #2828 can prove
`inversionCount_inversePermutationValues_mod_two` and unblock the determinant
transpose sign-preservation chain.

## Blockers

None for #2830.
