# HexPolyFp SquareFree Pascal helper cleanup (issue #6187)

## Accomplished

- Claimed #6155 first, then skipped it for replanning after finding the requested raw `yunFactorsDerivativeActiveReachable_monic` invariant conflicts with the existing raw `DensePoly.gcd` behavior documented in `HexPolyFp/SquareFree.lean`; the file already exposes only normalized-gcd monicity for that path.
- Claimed #6187 and removed the duplicated private `choose_eq_zero_of_lt` and `choose_self` lemmas from `HexPolyFp/SquareFree.lean`.
- Replaced all four SquareFree call sites with the public `Hex.Nat.choose_eq_zero_of_lt` and `Hex.Nat.choose_self` lemmas from `HexArith.Nat.Prime`.

## Current frontier

- #6187 is ready for PR.
- #6155 needs planner adjustment: either weaken the requested invariant to normalized states or change the executable/reachability substrate to thread normalized gcds.

## Next step

- Open the PR for #6187.
- A planner should re-scope #6155 before another worker attempts it.

## Blockers

- None for #6187. `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the touched-file forbidden-token diff scan all passed.
