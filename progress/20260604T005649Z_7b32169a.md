# 20260604T005649Z work #2567

## Accomplished

Added eight primed-target downstream siblings in
`HexBerlekampZassenhausMathlib/Recovery.lean` that state the BHKS
Theorem 5.2 auxiliary-power sub-bound against the named
`bhksPaperAuxiliaryFactorReal core C` target instead of the unfolded
three-way product
`bhksPaperDegreeFactorReal core * bhksPaperConstantFactorReal core C *
bhksPaperLogFactorReal core`.

Constructors (four `FactorFastCapSeparationInputs.ofBridgeData*'`):

* `ofBridgeDataPointwiseAuxiliaryBoundsAndFactoredPaperThreshold'`
* `ofBridgeDataCorrectedAuxiliaryL2normSqAndFactoredPaperThreshold'`
* `ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold'`
* `ofBridgeDataAndFactoredPaperThreshold'` (noncomputable)

Final wrappers (four `factorFast_terminates_*'`):

* `factorFast_terminates_ofBridgeDataPointwiseAuxiliaryBoundsAndFactoredPaperThreshold'`
* `factorFast_terminates_ofBridgeDataCorrectedAuxiliaryL2normSqAndFactoredPaperThreshold'`
* `factorFast_terminates_ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold'`
* `factorFast_terminates_ofBridgeDataAndFactoredPaperThreshold'`

Each primed sibling has the same signature as its unprimed
counterpart except the `h_aux` RHS uses the named
`bhksPaperAuxiliaryFactorReal` target.  Six of the eight implementations
are one-step `simpa [bhksPaperAuxiliaryFactorReal] using h_aux` shims
that dispatch to the unprimed variant.  The two siblings whose `h_aux`
LHS contains the bridge-derived `Real.sqrt` of the structured
corrected RHS sum (`ofBridgeDataAndFactoredPaperThreshold'` and
`factorFast_terminates_ofBridgeDataAndFactoredPaperThreshold'`) trip a
whnf heartbeats timeout under `simpa`, so they use the direct rewrite
`bhksPaperAuxiliaryFactorReal_eq_product _ _ ▸ h_aux` instead.

## Current frontier

The named-auxiliary target is now exposed at every downstream surface in
the cap-separation/factor-fast chain that consumes the factored BHKS
Theorem 5.2 sub-bounds.  Callers proving the auxiliary-power sub-bound
directly against `bhksPaperAuxiliaryFactorReal core C` can hit any of
the eight primed siblings without unfolding the three-way product.

The genuine analytic content remaining for HO-4 is the BHKS Lemma 3.2 /
Theorem 5.2 auxiliary-power inequality
`auxiliaryBound ^ input.natDegree ≤
bhksPaperAuxiliaryFactorReal core C` for the actual cap-lift bad-vector
auxiliary polynomial (resultant + Hadamard on the bad-vector
polynomial), and the coefficient-power sub-bound on
`bhksPaperCoeffNormFactorReal` for non-l2-norm-shaped `h_coeff`
hypotheses.

## Next step

1. The genuine BHKS Lemma 3.2 content: discharge
   `auxiliaryBound ^ input.natDegree ≤
   bhksPaperAuxiliaryFactorReal core C` from a Hadamard / resultant
   argument on the bad-vector polynomial.  This is the substantial
   analytic content that remains.

2. With the named target now plumbed through all eight wrappers,
   callers that build the auxiliary-power bound via the named
   `bhksPaperAuxiliaryFactorReal` definition can land an end-to-end
   `factorFast f ≠ none` consumer without the rewrite-at-callsite
   workaround.

## Blockers

None.  Pure compositions over the existing unprimed wrappers and the
`bhksPaperAuxiliaryFactorReal_eq_product` shim.

## Verification

* `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
  passed (3780 jobs, full library green, no new warnings introduced).
* Added-line forbidden-token grep
  (`sorry|axiom|native_decide|TODO|FIXME`) returns nothing on the new
  lemmas.
* No changes to `factor`, `factorFast`, `factorSlow`, or any existing
  theorem statement; the eight new primed siblings are strictly
  additive.
