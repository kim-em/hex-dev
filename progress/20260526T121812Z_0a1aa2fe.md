# 2026-05-26T12:18Z — work issue #5733

## Accomplished

Claimed #5733 and extended `MonicisedCoreTransportPackage` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` with the three
monicised-core lifted-factor facts:

- lifted factors are monic;
- lifted factors have positive Mathlib natural degree;
- `liftedFactor d` is injective.

The fields are keyed by the explicit
`Hex.monicisedCorePrimeData? core = some primeData` alignment hypothesis, so
they use the #5731 monicised-core proof surface from positive leading
coefficient and positive degree rather than rewriting through an
`hcore_monic` identity.  Added the consumer theorem
`monicisedCoreTransportPackage_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`
for the normalized square-free core.

Commit:

- `c549f1fb feat: thread monicised lifted facts through transport package`

Verification:

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check HEAD`
- `git show --format= -- HexBerlekampZassenhausMathlib/IntReductionMod.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returned no matches.

## Current frontier

#5733 is complete for the transport-package layer.  Downstream consumers can
extract the three lifted-factor facts without an `hcore_monic` premise once
they have the explicit monicised-prime-data alignment witness.

## Next step

Open the PR for #5733 with the transport-package commit and this progress file.

## Blockers

No blocker for this issue.  The worktree still contains an unrelated
pre-existing `.claude/CLAUDE.md` modification, left untouched.
