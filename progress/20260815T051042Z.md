# PNT+ BKLNW power-fold acceptance

## Accomplished

- Audited pinned PNT+ `BKLNW_a2_bounds.lean:cert_pow433_upper` and its exact
  `LeanCert.CertifiedBounds.BKLNW.pow433_upper` dependency.
- Added a Mathlib-free eight-atom fold certificate and bounded generic package
  session for the `M = 433` source leaf.
- Proved a parameterized ordinary-kernel two-band bound for the exact source
  sum and replayed the checked certificate through chronology and
  `ProofFrontend` to the PNT+ theorem shape.
- Added structurally decoded negative mutations for the limit, cardinality,
  exponent, and endpoint, plus an independent proof that the smaller endpoint
  is false and guarded axiom reports.
- Recorded partial evidence while leaving the broad BKLNW interface pending,
  documented the fixed-canary limitations, and refreshed the exact proof-only
  Lake exemption.
- Built the experiment libraries and complete conformance target, verified the
  inventory against the pinned source checkout, and ran the repository static,
  trust, bench-import, inventory, and freshness checks.

## Current frontier

The fixed `M = 433` power-provider leaf is locally complete. The analytic
theorem accepts alternative sound dyadic exponents and rational endpoints, but
the runtime operation deliberately remains tied to the source-pinned 433 case.

## Next step

After independent review and any required ancestry refresh, extend the
certificate language to the smaller BKLNW scaling ladder or tackle the
remaining exponential-bound declarations before classifying the imported
interface as complete.

## Blockers

None for this canary. Production balanced aggregation, comparative scale
measurements, and the rest of the imported BKLNW interface remain future work.
