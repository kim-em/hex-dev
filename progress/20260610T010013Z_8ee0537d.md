# 20260610T010013Z (#6726)

## Accomplished

Claimed #6726 (third worker session on this issue after #6729 partial merge).
Added a compiled substrate of seven helper lemmas in
`HexBerlekampZassenhaus/Basic.lean` that the residual irreducibility
proof requires:

- `ZPoly_dvd_trans`: transitivity of `∣` on `ZPoly`, composing the
  witness multiplications explicitly. The file does not import a
  general `dvd_trans`.
- `trialDivisionPeelAux_factor_dvd_target`: every emitted factor divides
  the peel target. Direct chain from the product invariant
  `residual * polyProduct factors = target` and
  `dvd_polyProduct_toArray_of_mem`.
- `trialDivisionPeelAux_residual_dvd_target`: the peel residual divides
  the peel target. Direct from the product invariant.
- `isUnit_of_dvd_primitive_size_one`: a size-`1` divisor of a primitive
  polynomial is a `ZPoly` unit. Used to derive positive degree of any
  non-unit divisor of a primitive core.
- `natAbs_coeff_scale_neg_one`: `(scale (-1) p).coeff i` and `p.coeff i`
  share `natAbs`. Transports universal coefficient bounds across sign
  normalization.
- `scale_neg_one_neg_one`: `scale (-1)` is an involution on `ZPoly`.
- `dvd_scale_neg_one`: `scale (-1) p ∣ p`, witnessing the associate
  relationship between a polynomial and its sign-flip.

These compile clean (verified `lake build HexBerlekampZassenhaus.Basic`).

## Current frontier

The residual irreducibility theorem itself is *not* in this PR. I
attempted it inline but ran into multiple substrate gaps (no `set`
tactic, `Nat.le_or_lt` missing, omega context confusion across nested
`have` blocks, and surface differences between `(1 : ZPoly).size` and
`(DensePoly.C 1).size` after rewriting) that the partial substrate
above does not yet bridge cleanly within a single session. The proof
sketch is laid out below for the successor.

## Next step

Two sub-issues capture the remaining work:

1. **Residual irreducibility** (the deliverable 4 residual case): use
   the seven helpers above plus `square_not_dvd_of_squareFreeRat`,
   `trialDivisionPeelAux_no_missed_unemitted`, and
   `mem_trialDivisionCandidatesUpTo_of_bounded` to discharge the
   appended residual case under the (`primitive`, `SquareFreeRat`,
   universal divisor bound) hypotheses.
2. **Peeled bounded candidate irreducibility** (deliverable 5
   completion): supply a degree-ordered prefix-split lemma for
   `trialDivisionPeelAux` and use it together with the residual
   substrate to discharge each emitted peel.1 factor.

Once both land, assemble `exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
covering split.1 (existing `splitIntegerRootFactorsAux_factor_irreducible`)
+ peel.1 + appended residual.

## Blockers

No technical blocker in the compiled substrate. The remaining
assembly is larger than this session.

Verification performed:

- `lake build HexBerlekampZassenhaus.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexBerlekampZassenhaus/Basic.lean`
  finds no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
