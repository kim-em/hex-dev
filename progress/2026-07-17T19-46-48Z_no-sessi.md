# Issue 8750 limb-model validation

## Accomplished

- Implemented the third revised directive's scalar limb models with constants
  frozen from direct Taylor-output measurements at two separated degrees.
- Verified the pinned certify fixture's NK prep assertion at every scheduled
  degree from 16 through 256.
- Ran all five affected kernels. Every normalized constant still fell strongly
  against its derived limb model (`β=-0.433` through `-0.698`), triggering the
  directive's explicit fallback.
- Reverted all experimental source edits.
- Filed lean-bench issue #67 for structured operation-count and operand-growth
  reporting, and hex-dev issue #8758 for the Phase-4 doctrine decision.
- Posted measurements and links on #8750 and released its assignment.

## Current frontier

The source tree has no issue-8750 code changes. The progress files are the only
untracked session artifacts. Issue #8750 cannot satisfy its all-thirteen scalar
verdict exit under the current harness doctrine.

## Next step

Resolve #8758, informed by lean-bench#67, then update #8750's exit criteria or
implement the structured harness capability before resuming benchmark changes.

## Blockers

- Maximum output coefficient width is not a valid scalar proxy for average
  limb cost across the triangular Taylor computation.
- The required Phase-4 doctrine/capability decision is external to #8750.
