**Accomplished**

Reviewed the #8531 conformance change at the emitter, hybrid trace, trace gate,
oracle, CI, and fixture levels. Checked the actual code paths for
`emitHybridTracedCase`, `factorHybridTraced`, `bz_flint.py`, `bz_trace_gate.py`,
and the CI conformance tail.

**Current frontier**

Review-only session. The main risks are semantic sharp edges in deriving
`classicalFactor` from the hybrid trace, the exact strictness desired for the
trace gate's `subsetCandidates` comparison, and CI time budget margin.

**Next step**

If editing the implementation, tighten the emitter derivation around trial
fallback/certification-miss semantics, decide whether the trace gate should pin
`subsetCandidates` exactly or only upper-bound it, and consider avoiding duplicate
BZ fixture emission in CI.

**Blockers**

Claude second-opinion wrapper failed with a local API connection error, so the
review proceeded from direct code inspection only.
