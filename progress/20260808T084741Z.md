# Arbitrary-function semantic replay restack

## Accomplished

- Reconciled the package-owned semantic replay layer with current `main` and
  the repaired policy-session foundation.
- Preserved the newer repository conformance targets alongside the semantic
  replay target.
- Rebuilt semantic replay, policy-session, arbitrary-function, and payload
  conformance on the current Lean toolchain and dependency revisions.

## Current frontier

The arbitrary-function session retains exact package-owned theorem recipes for
instantiation, equality, and contraction facts, and all three roles dispatch to
typed semantic replay without a central function language.

## Next step

Obtain exact-head review, merge the repaired policy-session base, retarget this
PR to `main`, and then merge it before landing chronological replay.

## Blockers

None.
