# 2026-06-19 — feature session (issue #8058)

Session type: `/work` → feature. Branch `agent/bcb43bed`.

## Accomplished

Closed issue #8058: assembled the fast-branch raw irreducibility capstone from
the #8051 recovered-lift cut package.

Added `factorFastFactorsWithBound_raw_zpolyIrreducible_of_recoveredLift` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`. It is the raw
(`factorFastFactorsWithBound`) analogue of the existing core-level
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift`:

- Consumes a successful `factorFastFactorsWithBound` dispatch (`hfast`), a
  successful fast-core run (`hcore`) at the executable
  `start = initialHenselPrecision B`, the per-support `BHKS.RecoveredLift`
  family with its monicity / prime / Hensel-precision / threshold / per-index
  congruence side conditions (the #8051 cut-package inputs), and the
  `hsize`/`hpartition` certificates.
- Builds the forward cut hypothesis through the #8051 producer
  `BHKS.cutProjectionHypotheses_of_recoveredLift`
  (`CLDColumnBound.lean`), then routes through `rawIrreducible_of_cut` so the
  count-equality / UFD-partition / `reassemblyExpansionComplete` plumbing is not
  re-derived.

The cut lattice basis is pinned to
`BHKS.latticeBasisOfLiftData core (toMonicLiftData core B primeData)`, which is
defeq to the `projectedRowsOfLiftData` data that `rawIrreducible_of_cut`
expects, so the produced `CutProjectionHypotheses` and the `trueSupports`
indices align by defeq with no bridge lemma.

## Verification

- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement` — clean.
- `lake build HexBerlekampZassenhausMathlib.FactorSoundness` — clean (the
  shipped `factor_irreducible_of_nonUnit` `sorry` is pre-existing, out of scope).
- `lake build HexBerlekampZassenhausMathlib` — Build completed successfully,
  0 errors.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line grep: no new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

The fast raw-branch capstone is now a thin assembly over the recovered-lift cut
package. The downstream public assembly (`factor_irreducible_of_nonUnit`) can
consume this for the fast-core success disjunct without re-deriving BHKS
cut/partition plumbing.

## Next step

Wire the slow modular branch and trial-division branch analogues, then the final
`factor_irreducible_of_nonUnit` branch assembly (the still-`sorry` headline in
`FactorSoundness.lean`). See the depending issues #8067 / #8068.

## Blockers

None for this issue.
