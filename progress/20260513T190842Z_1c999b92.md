# Session 1c999b92 - issue #3093 (partial)

## Accomplished

- Claimed #3093 (HexBZ core: Z-level integer reassembly).
- Added the private helper `extractXPower_core_primitive_of_ne_zero` in
  `HexBerlekampZassenhaus/Basic.lean`. The lemma establishes that
  `(ZPoly.extractXPower (ZPoly.primitivePart f)).core` is primitive whenever
  `f ≠ 0`, which is a prerequisite for upgrading the rational-associate
  reassembly to integer signed reassembly via
  `primitiveSquareFreeDecomposition_reassembly_signed`.
- Proof uses `extractXPower_product` to relate `shift xData.power xData.core`
  to `primitivePart f`, transfers `content xData.core ∣ ·` divisibility
  through the shift's zero/nonzero coefficient split, and applies
  `DensePoly.nat_eq_one_of_content_eq_one_of_nat_dvd_coeff` against the
  unit content of `primitivePart f`.
- Verified with `lake build HexBerlekampZassenhaus.Basic`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

The primitivity helper is in place. The capstone theorem
`reassemblePolynomialFactors_product_eq_input` (deliverable 1 of #3093)
remains to be assembled. The remaining work needs the signed reassembly's
`ε ∈ {±1}` to be aligned with `sign(leadingCoeff f)`, which in turn
requires ZPoly-level `leadingCoeff` infrastructure (multiplication, scale,
shift, `extractXPower` preservation, and non-negativity facts for the
`normalizePrimitiveSign`-normalized `squareFreeCore` / `repeatedPart`).

## Next step

Landing as a partial PR via `coordination create-pr 3093 --partial` so that
the primitivity helper is preserved and a planner can carve out the capstone
work as a follow-up issue. The follow-up will use the helper landed here
together with `primitiveSquareFreeDecomposition_reassembly_signed` from
HexPolyZ, plus inline `leadingCoeff` sign infrastructure (or further
decomposition).

## Blockers

- None for the primitivity helper. The capstone requires `leadingCoeff`
  multiplication/scale/shift lemmas for ZPoly that are not yet in the
  codebase; a follow-up worker will need to prove them inline or split
  them out.
