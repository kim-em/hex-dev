# Session ac760ecb — GramSchmidt size-reduce pivot bridge cleanup (#3919)

## Accomplished

- Claimed feature issue #3919.
- Added `GramSchmidt.Int.scaledCoeffs_rowAdd_pivot` in
  `HexGramSchmidt/Int.lean`, packaging the pivot row-add formula at the
  `scaledCoeffs` API level.
- Replaced the proof of `scaledCoeffs_sizeReduce_pivot` in
  `HexGramSchmidt/Update.lean` so it delegates to the new helper instead
  of directly invoking the determinant bridge lemmas.
- Verified:
  - `lake build HexGramSchmidt.Update`
  - `lake build HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`

## Current frontier

`scaledCoeffs_sizeReduce_pivot` no longer mentions
`scaledCoeffs_eq_scaledCoeffMatrix_det`,
`scaledCoeffMatrix_rowAdd_pivot_det`, or
`leadingGramMatrixInt_det_eq_gramDet_int` directly. The new helper still
packages the existing determinant-backed row-add identity in
`HexGramSchmidt/Int.lean`.

## Next step

Open the PR for #3919 and let CI validate the targeted proof-surface
cleanup.

## Blockers

None.
