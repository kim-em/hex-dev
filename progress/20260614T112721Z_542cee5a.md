# Session 20260614T112721Z (feature)

Issue #7155: HexPolyFp `SquareFree.lean` Phase 6 docstrings for the
`powLinearBinom` term/sum cluster (batch 2).

## Accomplished

Added one-sentence backtick-led `/-- ... -/` docstrings to the 22
undocumented declarations from `mul_powLinearBinom_scaled_right`
(line ~332) through `zmod64_index_fold_add_pow_prime` (line ~633).
Covers the binomial-term/partial-sum definitions, Pascal-rule
recurrences, the binomial theorem `powLinear_add_binom_sum`, the
prime-degree vanishing-middle-term lemmas, the freshman's-dream
`powLinear_add_prime`, and the fold-distribution / Frobenius-on-fold
helpers.

Docstring-only: `git diff` shows 22 added lines, each a complete
`/-- ... -/` block; no statement, proof, attribute, import, or
namespace touched.

## Current frontier

Stopped at the conceptual boundary at line ~640, where the already
documented `weightedProduct` begins the square-free-decomposition
product cluster. No `/-! ###` banner exists in this range; the
boundary is the start of the next semantic cluster.

## Verification

- `lake build HexPolyFp.SquareFree` green, no warnings.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean.
- No sorry / axiom / native_decide / TODO / FIXME / em-dash / banned
  vocabulary on added lines.

## Next step

Batch 3 continues top-down from `weightedProduct_nil` (line ~644) /
the `weightedProduct` cluster, then onward through the still
~hundred-plus undocumented declarations in the file.

## Blockers

None.
