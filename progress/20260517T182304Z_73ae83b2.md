# HexHensel Phase 6: polish Basic.lean reduction/lift API docs and simp surface

Session: 73ae83b2

## Accomplished

Phase 6 polish pass over `HexHensel/Basic.lean` per #4772:

- Added caller-facing docstrings to public declarations that were
  missing them: `ZPoly.intModNat`, `ZPoly.coeff_modP`,
  `ZPoly.coeff_reduceModPow`, `ZPoly.coeff_reduceModPow_eq_zero_of_emod`,
  `ZPoly.coeff_reduceModPow_eq_emod_of_pos`, `FpPoly.coeff_liftToZ`.
- Added "why this exists" docstrings to private helpers:
  `Hex.list_getD_map_range`, `ZPoly.intModNat_eq_emod`,
  `ZPoly.intModNat_sub_self_emod`, `ZPoly.intModNat_eq_of_congr`,
  `FpPoly.zmod64_toNat_one_of_one_lt`, `FpPoly.zmod64_toNat_zero`,
  `FpPoly.liftToZ_size_pos_of_monic`.
- Added `@[simp]` to two unconditional canonical-form round-trip lemmas:
  `ZPoly.modP_reduceModPow` and `FpPoly.modP_liftToZ`. Both are equations
  with no side conditions and rewrite toward the canonical (lower-power /
  unlifted) form.
- Did *not* add `@[simp]` to `reduceModPow_idempotent` — its `0 < p^k`
  side condition has free `p, k` that would block rewriting, which the
  issue explicitly warns against.

No changes to definitions, theorem statements, argument order, or
behavior. Build is clean (`lake build HexHensel`); no new
`sorry|axiom|native_decide|TODO|FIXME` entries.

## Current frontier

PR ready to push and create.

## Next step

`coordination create-pr 4772 "doc: polish HexHensel Basic.lean reduction/lift API"`.

## Blockers

None.
