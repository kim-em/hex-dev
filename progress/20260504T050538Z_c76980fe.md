# 2026-05-04 05:05Z — feature session, issue #2020

## Accomplished

- Closed #2020 (HexGfqField: bump conformance fixture sizes toward
  SPEC profile-size ceilings).
- `HexGfqField/Conformance.lean`: replaced the degree-2 modulus
  `x^2 + x + 1` with the degree-4 modulus `x^4 + 2` (irreducible
  over `F_5`; absent both linear and quadratic factors). Updated
  every fixture and expected value to operate in `F_5[x]/(x^4+2)`.
  All `#guard` literals are derived by hand from the
  `x^4 = -2 ≡ 3 (mod 5)` reduction rule (or by extended Euclidean
  algorithm for the inverses), not by run-and-paste.
- Preserved typical / edge / adversarial structure: typical degree-1
  fixture `a = 2 + 3x`, edge degree-3 fixture `b = 4 + x + x^3`,
  generator `xv = x`, adversarial `high = x^4 → 3`. The role
  previously played by `x^2 → 4 + 4x` (a polynomial that needs
  reduction) is now `x^5 → 3x`.
- `lake build HexGfqField` green; `HexGfqField.Conformance`
  elaborates in 1.2s, well under the 2s budget.

## Current frontier

C1 cluster (per-library fixture-size bumps) progress: HexGfqField
done. Sibling unclaimed bumps remain for HexGfqRing (#2019),
HexBerlekamp (#2018), HexPolyZ (#2017), HexPolyFp (#2016),
HexPoly (#2015), HexLLL (#2014), HexGramSchmidt (#2013), and
HexMatrix (#2012).

## Next step

Workers picking up the remaining C1 bumps can mirror the approach:
identify the SPEC ceiling for the relevant size axis, choose the
target value, hand-derive the new expected `#guard` literals from
the algebraic contract (so the run-and-paste rule is satisfied),
and verify the per-file elaboration budget.

## Blockers

None.
