# Restack PNT import classification hygiene

## Accomplished

- Replayed the four-record import classification repair onto exact main
  `5c3ffe79ef0c2cd98fb381c4c8445b92261ddb6e` without conflicts, preserving the
  merged supported-contract registrations and SPEC changes.
- Confirmed the feature patch is equivalent to the reviewed local commit and
  still changes only the four import decisions, their migration digest, the
  precise aggregate summary, and session progress.
- Passed inventory validation, exact pinned-source verification, all 28
  inventory unit tests, and the static, DAG, freshness, and diff checks.

## Current frontier

The publication branch records six accepted and ten pending raw import sites;
the dependency-interface layer remains two accepted and four pending.

## Next step

Open the upstream non-draft PR and let its normal exact-head CI run.

## Blockers

None.
