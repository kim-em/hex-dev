# 2026-05-17 03:25 UTC — thirty-sixth Phase 4/5 checkpoint (1c004790)

Claimed summarize issue #4557 and recorded this checkpoint covering
the merged PR wave since #4512 closed at 2026-05-16T16:54:32Z. The
in-flight PR #4556 noted in the issue body has since merged
(2026-05-17T03:23:56Z), bringing the actual count to **19** merged
PRs in this window.

## Accomplished

Read the 19 PRs merged after #4512 closed, the last ~15 progress
entries (covering the 2026-05-16T16Z–2026-05-17T03Z window), the
current `coordination orient` frontier and `python3 scripts/status.py`
output, and the still-open `agent-plan` issue bodies for the HexBZ
Mathlib coverage stack, HO-1 executable irreducibility tail, and the
HexGramSchmidt chain.

The dominant change since the previous checkpoint is the HO-1
substrate buildout that the prior checkpoint's "Next step" §2 called
for. The two substrate items #4170's status section flagged as
"not yet tracked as issues" (Hensel substrate bundle + small-mod
singleton discharge) are now both in tree at the outer coefficient
bound `B = defaultFactorCoeffBound f` actually used by the public
`Hex.factor f` entry point, leaving only the BHKS fast branch
(directive #2567) as the residual gate on the #4170 capstone.
HexGfqRing Phase 7 also closed this cycle, and `python3 scripts/status.py`
now reports `HexGfqRing` as **Fully done**.

- **HO-1 substrate — outer-bound exhaustive-branch wrappers**
  (#4537, #4527, #4531, #4533, #4535). The exhaustive-branch
  irreducibility bridge was lifted from a hard-coded bound to the
  outer bound `B = defaultFactorCoeffBound f` (#4537), the per-branch
  `factorWithBound`-output theorem assembled (#4533), the
  Mathlib-bridge exhaustive-core factor irreducibility proved
  (#4531), the bundled `exhaustiveCoreFactorsWithBound` coverage
  theorem assembled (#4527), and the Mathlib-free side consolidated
  as `factorWithBound` entries being `ZPoly.Irreducible` (#4535).

- **HO-1 substrate — Hensel-lift state constructors at outer B**
  (#4546, #4550, #4548, #4552, #4554, #4556). The
  `HenselSubsetCorrespondenceHypotheses` constructor at the outer
  bound landed parametric in `(core, B)` plus an outer-bound
  specialisation (#4546). The `LiftedFactorSubsetPartition`
  `Finset.univ` core constructor landed with the same parametric +
  outer-bound shape (#4550). The abstract
  `henselLiftData_liftedFactor_natDegree_pos` theorem plus a
  `choosePrimeData` umbrella with the per-modular-factor positivity
  premise still explicit landed (#4548). The
  `hfactors_natDegree_pos` discharge from `factorsModPBerlekampForm`
  + Berlekamp factor-degree positivity landed in two halves —
  `berlekampFactor_factors_pos_degree` Mathlib-free in
  `HexBerlekamp/Factor.lean` and
  `factorsModP_natDegree_pos_of_factorsModPBerlekampForm` on the
  Mathlib-bridge side (#4552). The squarefree-over-ℤ bridge for
  `toPolynomial squareFreeCore` from the executable `SquareFreeRat`
  invariant + Gauss-style primitivity landed in
  `IntReductionMod.lean` and migrated the
  `LiftedFactorSubsetPartition` outer-bound constructor along the
  import direction (#4554). The composed umbrella that drops the
  `hfactors_natDegree_pos` premise outright merged as #4556.

- **HO-1 substrate — small-mod singleton discharge** (#4545). The
  small-mod singleton branch's `hprim` and `hlc_map_ne` premises
  were discharged from the executable `normalizeForFactor` /
  `isGoodPrime` invariants via `Hex.ZPoly.Primitive`
  →`Polynomial.IsPrimitive` and the integer-content bridge.

- **HO-1 substrate — Nodup family** (#4534, #4540, #4526, #4524,
  #4522). The Mathlib-free `berlekampFactor.factors.Nodup` (#4534)
  and the Mathlib-bridge `Nodup` wrappers (#4540) decomposed the
  earlier #4528 surface and made the no-square invariant polymorphic
  in the `Lean.Grind.Field` instance. The
  `henselLiftData_liftedFactor_injective_of_choosePrimeData`
  umbrella for the #4301 capstone landed (#4526). The recursive
  rest-search coverage with matching predicate landed (#4524) and
  the `LiftedFactorListMatches.nodup` helper from per-`J`
  injectivity landed (#4522).

- **HexGfqRing Phase 7**: `HexManual` chapter written and
  `done_through` bumped to 7 (#4532). `python3 scripts/status.py`
  now lists `HexGfqRing` under **Fully done**.

- **Doc / decision artefact**: #4541 resolved #4539 with the
  route-3 abstract-bound refactor recommendation — this is the route
  the subsequent #4537 / #4527 / #4531 / #4533 / #4535 chain
  implemented. (#4539 was the open monotonicity question the
  previous checkpoint did not enumerate.)

## Current frontier

Open PRs:

- (none under agent branches; the in-flight #4556 noted by the
  summarize issue body has merged.)
- #2656: unrelated SPEC draft for hex-real-roots / hex-sturm.

Unclaimed `agent-plan` work (after this summarize claim):

- #3970: HexBZ Mathlib Factorization capstones
  (`factor_product`, `irreducibleByFactorization_iff`,
  `checkIrreducibleCert_sound`, `factor_unique`). Body declares
  `depends-on: #3892 / #3953 / #3969`, all of which are closed.
  Substantively this is the next critical-path piece on the BZ
  Mathlib coverage stack once `factor_irreducible_of_nonUnit`
  (#4170) lands; the issue body does not list #4170 as a dependency,
  but the Factorization-record uniqueness and capstone proofs will
  need the executable-factor irreducibility surface. A planner pass
  may want to either gate #3970 on #4170 or scope it to the
  uniqueness / certificate-soundness pieces that can be proved
  independently of `factor_irreducible_of_nonUnit`.
- #4334: HexLLL Phase 4 inconclusive scientific benchmark verdicts.
  Repeatedly skipped for host-contention reasons since 2026-05-15;
  per `SPEC/benchmarking.md` re-dispatch waits on quiet or
  dedicated hardware.

Claimed work:

- #4557 (this summarize task).

Blocked chains to keep visible:

- HO-1 executable irreducibility tail: #4172 → #4170 → #2567.
  This cycle's substrate buildout discharges #4170's items 1
  (Hensel substrate bundle) and 2 (small-mod singleton substrate);
  the residual is item 3 (BHKS fast branch from directive #2567).
  `coordination orient` reports #4170 as `[Blocked on #2567]`,
  reflecting this reduction. The #4170 issue body's status section
  is stale relative to the substrate work landed this cycle and
  should be refreshed by the next planner pass — items 1 and 2 are
  no longer untracked.
- BZ Mathlib coverage capstones: #3970 (unclaimed, see above).
- HexGramSchmidt scaledCoeffRows diagonal determinant chain:
  #3977 → #4053 → #3899. Unchanged from the previous checkpoint;
  no work landed against this chain this cycle.
- HO-18 / HexLLL Phase 4 report: directive #3662 still gated on
  #4334.
- Open directives: #2564 (HO-1 rewrite), #2567 (HO-4 BHKS 5.2 —
  now the sole remaining gate on #4170 modulo the planner's
  partial-close option for the slow + singleton arms), #2637
  (Factorization record API), #3662 (HO-18 perf report), #3899
  (HO-43), #3900 (HO-44 — the single-file `bareiss_eq_det` orphan
  deletion in `HexMatrix/Bareiss.lean:1342`).

## Status survey

`python3 scripts/status.py` highlights this cycle:

- **HexGfqRing** — now reported under **Fully done** after #4532
  closed Phase 7.
- **HexBerlekampZassenhausMathlib (Phase 3 wait)** — the HO-1
  substrate bundle landed end to end at the outer coefficient
  bound; the library still reports `waiting on
  HexBerlekampZassenhaus.done_through ≥ 3` and continues to absorb
  capstone-prep work in the meantime.
- **HexLLL (Phase 4)** — no measurement progress this cycle;
  #4334 remains environmentally gated.
- **HexGramSchmidt / HexGramSchmidtMathlib** — no movement this
  cycle on the #3977 → #4053 → #3899 chain.
- Other ready libraries (HexArith, HexPoly, HexMatrix, HexModArith,
  HexGF2, HexPolyZ, HexPolyFp, HexGfqField, HexBerlekamp,
  HexHensel, HexGramSchmidtMathlib, HexHenselMathlib, HexGF2Mathlib)
  unchanged from the previous checkpoint.

## Next step

1. Either claim #3970 (HexBZ Mathlib Factorization capstones) as
   soon as a planner has resolved its scope vs. #4170, or surface a
   planner pass to refresh #4170's status section (items 1 and 2
   now have landed substrate) and decide whether to (a) wait on
   directive #2567 for the BHKS fast branch, or (b) scope #4170 to
   a partial close covering the slow + singleton arms only with the
   fast arm conditional on a `factorFast` hypothesis.
2. Re-dispatch #4334 (HexLLL Phase 4 verdicts) only when quiet or
   dedicated hardware is available per `SPEC/benchmarking.md`.
3. Trigger the `replan` triage on directives #3899 (HO-43) and
   #3900 (HO-44) — the orphan `bareiss_eq_det` deletion in
   `HexMatrix/Bareiss.lean:1342` is still the single residual HO-44
   deliverable and remains unblocked since the previous checkpoint.
4. Pick up the HexGramSchmidt #3977 → #4053 → #3899 bridge chain
   when an agent with Mathlib-bridge bandwidth is free; it has had
   no movement for two checkpoint cycles.

## Blockers

No blocker for this checkpoint.
