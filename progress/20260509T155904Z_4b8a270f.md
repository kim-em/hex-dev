# 20260509T155904Z - Issue #2809

## Accomplished

Started by claiming #2670, then released it for replan after confirming that
the current Mathlib-free Gram-Schmidt consumers still require a Bareiss-to-det
bridge and removing the core theorem would only move the proof hole.

Claimed human-oversight issue #2809 and repaired the HexLLL benchmark findings:

- changed `runGramSchmidtCoeffChecksum` from a sub-resolution parametric sweep
  to `runGramSchmidtCoeffLoopChecksum`, a fixed-iteration wrapper with prep
  still hoisted out of the timed body;
- raised the harsh `LLLState.ofBasis` timing budget enough to clear the
  child-process signal floor;
- recalibrated `lll.firstShortVector` family-specific models for
  random-bounded and harsh-cubic fixtures;
- added `reports/hex-lll-performance.md` and merged the repair JSON rows into
  `reports/bench-results/hex-lll-e211854d1435-repair-fixed.json`.

Verification completed:

- `lake build HexLLL.Bench`
- `lake exe hexlll_bench list`
- `lake exe hexlll_bench run Hex.LLLBench.runSizeReduceChecksum`
- `lake exe hexlll_bench run Hex.LLLBench.runGramSchmidtCoeffLoopChecksum`
- `lake exe hexlll_bench run Hex.LLLBench.runOfBasisHarshCubicChecksum`
- `lake exe hexlll_bench run Hex.LLLBench.runFirstShortVectorRandomBoundedChecksum`
- `lake exe hexlll_bench run Hex.LLLBench.runFirstShortVectorHarshCubicChecksum`
- `lake exe hexlll_bench verify Hex.LLLBench.runGramSchmidtCoeffLoopChecksum Hex.LLLBench.runOfBasisHarshCubicChecksum Hex.LLLBench.runFirstShortVectorRandomBoundedChecksum Hex.LLLBench.runFirstShortVectorHarshCubicChecksum Hex.LLLBench.runSizeReduceChecksum`
- `python3 scripts/check_dag.py`
- `python3 scripts/check_phase4.py`
- `git diff --check`

## Current frontier

The five #2809 registrations now produce acceptable verdicts in individual
scientific runs on `carica`. HexLLL remains at `done_through: 3`; the report
keeps comparator/profile gaps in Concerns rather than claiming Phase 4
completion.

The worktree still has the pre-existing unstaged `.claude/CLAUDE.md`
modification, which was not touched.

## Next step

Create the PR for #2809 from pushed branch `agent/4b8a270f` once the GitHub
API rate limit clears.

## Blockers

`coordination create-pr 2809` failed after the commit because GitHub API calls
started returning `GraphQL: API rate limit already exceeded for user ID 477956`.
The branch was pushed manually with `git push -u origin agent/4b8a270f`, but the
issue label has not been swapped to `has-pr`.

Full HexLLL Phase-4 re-promotion also remains blocked on comparator/profile
evidence, including #2810 for fpLLL ratios on a fpylll-equipped benchmark host.
