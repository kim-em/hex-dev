# a540951f — feature #4919 primitive mem_T abstract-bound sibling

## Accomplished

- Claimed issue #4919 after losing the claim race on #4918.
- Added
  `mem_T_iff_exists_irreducibleFactor_representingSubset_of_primitive_pos_lc_core_of_bound`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
- Rewired the existing
  `mem_T_iff_exists_irreducibleFactor_representingSubset_of_primitive_pos_lc_core`
  as a thin `defaultFactorCoeffBound core` wrapper, preserving its public
  signature and downstream call sites.
- Threaded the abstract bound through the primitive per-normalized-factor
  bridge and primitive degree-cover `_of_bound` helper.

## Current frontier

The primitive per-`T` membership-equivalence theorem now has an abstract-bound
sibling. The local substrate required an additional
`(Hex.DensePoly.leadingCoeff core).natAbs ≤ B'` hypothesis for the primitive
degree-cover call; the wrapper discharges it with
`defaultFactorCoeffBound_leadingCoeff_natAbs_le`.

## Next step

Continue the dependent abstract-bound chain, especially #4925 once this PR and
the parallel monic sibling #4918 land.

## Blockers

`lake build HexBerlekampZassenhausMathlib.Basic` still fails in the known
pre-existing timeout and unknown-constant clusters outside this edit region
around lines 4907, 5153, 5461, 5548, 5595, 5832, 5945, 6090, 6234/6253,
6568/6632, 15447, 15558, and 15746/15792. No new diagnostics were reported in
the edited theorem region.
