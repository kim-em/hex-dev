# Progress: HexMatrix Phase 6 Desnanot surface removal (#6273)

## Accomplished

Removed the Mathlib-free Desnanot-Jacobi public surface from
`HexMatrix/Determinant.lean`.

The deletion covers the HexMatrix-local auxiliary matrix cluster
(`auxAdjugateM`, `auxP`, product/determinant helpers, and the scaled/unscaled
Desnanot-Jacobi capstones) plus the Bareiss Desnanot reindexing helpers that
only fed that relocated proof path.

Kept the public `det_mul` determinant multiplicativity theorem and its private
Cauchy-Binet helper substrate in `HexMatrix/Determinant.lean`.

Audited `HexMatrixMathlib/Determinant/DesnanotJacobi.lean`: it still uses its
own private auxiliary matrix proof and does not need the removed HexMatrix
cluster.

Verification:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `lake build HexMatrixMathlib.Determinant.DesnanotJacobi`
- `lake build HexMatrixMathlib`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over `HexMatrix/Determinant.lean` found no added
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Forbidden Desnanot-name grep over `HexMatrix/` found no remaining matches.

## Current frontier

`HexMatrix/Determinant.lean` no longer exposes or proves Desnanot-Jacobi
through the Mathlib-free layer. The Mathlib-side Desnanot-Jacobi file remains
self-contained.

The full build passed with existing linter warnings in
`HexGFqRing/Operations.lean`, outside this session's touched files.

## Next step

Continue the remaining Phase 6 follow-up issues from the review report:
infrastructure visibility classification, row-operation docstring/grind
coverage, Bareiss module comments/data projections, and conformance fixture
annotations.

## Blockers

None for this session. The worktree still contains an unrelated pre-existing
local modification to `.claude/CLAUDE.md`, which was not touched or staged.
