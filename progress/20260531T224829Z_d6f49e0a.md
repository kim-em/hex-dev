# Issue #5867 — witness-form `choosePrimeData?` bridge wrappers

## Accomplished

- Added `Hex.choosePrimeData_eq_of_choosePrimeData?_some` in
  `HexBerlekampZassenhaus/Basic.lean`.
- Migrated the remaining public
  `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`,
  `henselSubsetCorrespondenceHypotheses_outerBound_of_choosePrimeData`, and
  `liftedFactorSubsetPartition_of_choosePrimeData` wrappers in
  `HexBerlekampZassenhausMathlib/Basic.lean` to take explicit
  `primeData : Hex.PrimeChoiceData` and
  `hchoose : Hex.choosePrimeData? core = some primeData`.
- Made
  `existsUnique_modPFactorSubset_of_choosePrimeData_of_some` take explicit
  `primeData` rather than an implicit witness, and updated its internal caller.
- Renamed the two lifted-hypothesis wrapper witness binders from `_hchoose` to
  `hchoose` while consuming them in the proof bodies, so all nine listed
  wrappers expose the witness-form premise.
- Updated nearby docstrings that still described the old
  `let primeData := Hex.choosePrimeData core` public wrapper surface.
- Added a local Mathlib-side adapter
  `squareFree_common_of_squareFreeModP` and used it to migrate two stale
  `gcd = 1` consumers to the current executable `squareFreeModP` API.

## Current frontier

The nine listed public bridge wrapper signatures no longer bind
`let primeData := Hex.choosePrimeData core`. The two remaining
`let primeData := Hex.choosePrimeData core` matches are the private analytic
obligation helpers that the migrated public wrappers transport through using
`Hex.choosePrimeData_eq_of_choosePrimeData?_some`.

`lake build HexBerlekampZassenhausMathlib` still fails at
`HexBerlekampZassenhausMathlib/Basic.lean:6314`. This is the remaining
pre-existing square-free API mismatch: `factors_irreducible_of_factorsModPBerlekampForm`
still feeds `Hex.isGoodPrime_squareFreeModP`, whose type is now
`Hex.squareFreeModP core primeData.p`, to
`gcd_monicModularImage_derivative_eq_one_local`, which expects the older strict
`Hex.DensePoly.gcd ... = 1` form.

## Next step

Migrate `gcd_monicModularImage_derivative_eq_one_local` or its downstream
caller away from the strict `gcd = 1` premise. The clean direction is likely a
common-divisor or `gcdIsUnit` version of the Mathlib bridge theorem consumed by
`factors_irreducible_of_factorsModPBerlekampForm`, matching the
`squareFree_common_of_squareFreeModP` adapter used for the nodup and coprime
helpers.

## Blockers

The build verification criterion is blocked by the remaining pre-existing
strict-gcd consumer at `HexBerlekampZassenhausMathlib/Basic.lean:6314`.
No new `axiom` or new proof `sorry` was introduced. `/reflect` is not
available in this non-interactive shell context (`zsh: no such file or
directory: /reflect`).
