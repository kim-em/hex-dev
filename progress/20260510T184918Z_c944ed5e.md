# 2026-05-10 18:49 UTC — feature session #3086

## Accomplished

- Claimed issue #3086 (HexBZ core: prove core factor product preservation
  under bound hypothesis) on `agent/c944ed5e` from `origin/main`.
- Added three new `private` theorems to `HexBerlekampZassenhaus/Basic.lean`
  immediately after `recombineExhaustive_product`:
  * `bhksRecover?_product` — if `bhksRecover? f d = some candidates` then
    `Array.polyProduct candidates = f`. Proof unfolds the executable, walks
    the row-positivity / non-degenerate-partition / candidate / product
    branches via `dif_pos` and case analysis, and extracts the success
    invariant from the inner `polyProduct == f` BEq check.
  * `factorFastCoreWithBound_product` — if
    `factorFastCoreWithBound core B primeData k fuel = some coreFactors`
    then `Array.polyProduct coreFactors = core`. Proof is by induction on
    `fuel`, threading through `bhksRecover?_product` in the success
    branch and using the inductive hypothesis in the
    precision-doubling fallback.
  * `exhaustiveCoreFactorsWithBound_product` — unconditional product
    preservation: `Array.polyProduct (exhaustiveCoreFactorsWithBound core B
    primeData) = core` in every branch (B = 0 → singleton `#[core]`,
    `recombineExhaustive` returns `#[]` → falls back to `#[core]`,
    `recombineExhaustive` returns the search output → product equals
    `core` via `recombineExhaustive_product`).
- Verified `lake build HexBerlekampZassenhaus.Basic`,
  `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, and
  `git diff --check`. The full HexBerlekampZassenhaus build replays/builds
  586 jobs successfully. The three pre-existing `sorry` placeholders in
  the file (lines 1769, 2264, 2279) are unchanged; no new `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME` markers were introduced.

## Current frontier

The two product-preservation bridges that `factor_product_of_bound`
(line 2278) and #3087 directly consume are now available as
`exhaustiveCoreFactorsWithBound_product` (slow path) and
`factorFastCoreWithBound_product` (fast path). The slow-path bridge does
not require the SPEC bound hypothesis — the fallback `#[core]` shape
recovers it structurally — but it is still consumable as written by the
forthcoming capstone.

## Next step

#3087 (assemble `factor_product_of_bound`) should now be unblocked from
the product-preservation side. The remaining prerequisites are #3084
(polyPow-fold bridge) and #3085 (Z-level reassembly).

## Blockers

None.
