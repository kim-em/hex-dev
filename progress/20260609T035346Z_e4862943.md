# 20260609T035346Z (#6626)

## Accomplished

- Claimed parent #6609, but found the recursive Yun scalar split
  synchronization requires an asymmetric gcd-scalar bridge
  (`gcd(C u_c * c, C u_w * w) = C v * gcd(c, w)` for `u_c ≠ u_w`).
  Existing `gcd_C_mul_C_mul_eq_C_mul_gcd` only handles the same-scalar
  case. Decomposed #6609 into foundation issue #6626 (this work) plus a
  residual recursive-assembly issue, leaving a `Decomposed into #6626`
  breadcrumb and `coordination skip`ed the parent.
- Claimed #6626 and added the asymmetric scalar bridges in
  `HexPolyFp/SquareFree.lean`:
  - `fpPoly_C_mul_C_eq`: `C a * C b = C (a * b)`. Extracted as a top-level
    helper from inline blocks in `gcd_C_mul_C_mul_eq_C_mul_gcd` and
    `div_C_mul_C_mul_of_dvd`.
  - `C_mul_dvd_of_C_mul_dvd_unit_swap`: swap the scalar in a
    `C u * d ∣ a` divisibility hypothesis to any other nonzero scalar
    `v`, by rescaling the witness with `C (u * v⁻¹)`.
  - `gcd_C_mul_left_C_mul_right_eq_C_mul_gcd`: the asymmetric scalar
    gcd bridge proper. Proof routes through divisibility manipulation
    (gcd c w | d via dvd-of-scaling, and `C u_c⁻¹ * d ∣ gcd c w` via the
    dual divisions on c and w with a unit-scalar swap on the w side),
    then converts the resulting associate identity to a `C v` factor via
    `eq_C_of_mul_eq_one`.
  - `gcd_C_mul_left_eq_C_mul_gcd` and `gcd_C_mul_right_eq_C_mul_gcd`:
    one-sided corollaries with the other scalar specialized to 1.
- Deliverable 4 (`div_C_mul_left_C_mul_right_of_dvd`) was already
  satisfied by the existing `div_C_mul_C_mul_of_dvd` at line 1633, whose
  signature is already asymmetric (`{u v : ZMod64 p}`). No new
  divisibility lemma needed; the existing surface stays as-is.

Verification run on `agent/e4862943`:

- `lake build HexPolyFp.SquareFree`: green.
- `lake build HexPolyFp`: green.
- `lake build HexBerlekampZassenhaus`: green (no regressions downstream).
- `python3 scripts/check_dag.py`: clean.
- `git diff --check`: clean.
- Added-line forbidden-token grep (`sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`): none.

## Current frontier

#6626 foundation deliverables 1–4 landed in 295 added lines, all
private theorems in `HexPolyFp/SquareFree.lean` between the existing
symmetric scalar bridges. The asymmetric proof avoided `grind` for
polynomial-ring algebra by using explicit `DensePoly.mul_assoc_poly` /
`DensePoly.mul_comm_poly` chains and `congrArg` substitutions; `grind`
handled `ZMod64` field arithmetic only.

## Next step

Successor to #6609 (recursive Yun scalar split synchronization):
use the new asymmetric bridges to thread a scalar trace through one
step of `yunFactorsContributionWithLevel`, then combine with
`yunScalarStep` plus the existing
`yunFactorsContributionWithLevel_normalized_tail_reconstruct` and
`yunFactorsContributionWithLevel_normalized_tail_product_bridge`
substrate to produce the contribution/residual sync between raw
scalar-multiple and normalized states.

## Blockers

None.
