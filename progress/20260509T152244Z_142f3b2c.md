# 20260509T152244Z - Issue #2770

## Accomplished

Claimed #2770 after #2697 rejected claiming as `needs replan`.
Packaged the row-pivoted Bareiss singular branch in
`HexMatrixMathlib/Determinant/Bareiss.lean`:

- failed pivot search under `BareissPivotInvariant` now proves the original
  source determinant is zero by transporting the zero pivot column through the
  logical no-pivot source and applying `det_eq_zero_of_bareiss_failed_column`;
- recursive `pivotLoop` singular-step theorems now propagate that result
  through regular no-swap and swap branches;
- initial-state and `bareissData` wrappers are available for the final
  row-pivoted Bareiss capstone.

Verification completed:

- `lake build HexMatrixMathlib.Determinant.Bareiss`
- `lake build HexMatrixMathlib.Determinant`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrixMathlib/Determinant/Bareiss.lean`

## Current frontier

The singular pivotLoop theorem surface for #2770 is implemented and locally
verified. The worktree still has the pre-existing unstaged
`.claude/CLAUDE.md` modification, which was not touched.

## Next step

Use the new `bareissData_singularStep_ne_none_det_eq_zero` wrapper in the final
public `bareiss_eq_det` capstone that composes `bareissData_eq_finish_pivotLoop`,
`finish`, and `BareissData.det`.

## Blockers

None.
