# 2026-05-17 07:36 UTC — slow-quadratic umbrella refactor onto `_of_primitive`

Session type: feature worker (#4587).

## Accomplished

- Rewrote `factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`
  in `HexBerlekampZassenhausMathlib/IntReductionMod.lean` to follow the
  same shape as the fast-path quadratic umbrella
  `factor_quadratic_branch_entry_irreducible_of_quadraticRoots`:
  compose `factorWithBound_entry_mem_slow_quadratic_branch_raw` →
  `quadraticIntegerRootFactors?_factor_irreducible_of_primitive` (per
  core factor) →
  `reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`
  → `zpolyIrreducible_normalizeFactorSign_of_zpolyIrreducible`.
  Primitivity comes from `IntReductionMod.zpoly_primitive_of_toPolynomial_isPrimitive`
  on `normalizeForFactor_squareFreeCore_toPolynomial_isPrimitive`;
  positive leading coefficient comes from
  `squareFreeCore_leadingCoeff_pos_of_ne_zero`. The previously-required
  manual residual-ne-one + `hcore_lc_pos` boilerplate is gone.
- Dropped the `_` prefix on the previously-unused `_hf_ne` parameter —
  it is now used to discharge primitivity / positive leading coefficient.
- Deleted the now-orphan classifier
  `reassemblePolynomialFactors_quadratic_irreducible_or_residual_of_expansionComplete`
  from `HexBerlekampZassenhaus/Basic.lean`; its only caller was the
  slow-quadratic umbrella, which no longer needs it.
- Deleted the now-dead Mathlib-bridge helper
  `quadraticIntegerRootFactors?_residual_irreducible_of_isPrimitive`
  (~170 lines) from `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.
  This helper was landed by PR #4586 (closing #4579) to discharge the
  residual sub-case of the slow-quadratic umbrella. After this refactor
  the umbrella routes the residual through the
  Mathlib-free `_factor_irreducible_of_primitive` helper from
  PR #4582, so the Mathlib-bridge version has no remaining consumers
  (per the #4587 issue: "the Mathlib-bridge residual-irreducibility
  theorem becomes unnecessary as a separate deliverable").
- Re-privatised `integerRootCandidates`, `splitIntegerRootFactorsAux`,
  `splitIntegerRootFactorsAux_product`, and
  `splitIntegerRootFactorsAux_polyProduct_leadingCoeff_pos` in
  `HexBerlekampZassenhaus/Basic.lean`. PR #4586 promoted these to
  public to support the deleted Mathlib-bridge helper; after deletion
  they have no external consumers and revert to internal helpers.
  (The unrelated public `splitIntegerRootFactorsAux_factor_irreducible`
  theorem is unchanged.)
- Updated the umbrella docstring to reflect the new composition list
  and note that this umbrella shares its shape with the fast-path
  quadratic arm.

`rg "sorry|axiom|native_decide|TODO|FIXME"
HexBerlekampZassenhausMathlib/IntReductionMod.lean` returns no matches
(IntReductionMod's sorry count drops from one to zero).

All four landed arm umbrellas (constant, small-mod singleton,
fast-quadratic, slow-quadratic) now share the same generic reassembly
lift `reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`.
Net diff: 80 insertions, 293 deletions across the two production files.

## Verification

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — pass.
- `lake build HexBerlekampZassenhausMathlib` — pass (full subtree, 3468
  jobs).
- `python3 scripts/check_dag.py` — pass.
- `git diff --check` — pass.

## Current frontier

`#4170` (HO-1 capstone `factor_irreducible_of_nonUnit`) can now consume
all four arm umbrellas in the same shape. The remaining pre-capstone
substrate work I see open: `#4585` (constant-arm `hcomplete` discharger,
claimed), `#4591` (small-mod singleton `hcomplete` discharger,
unclaimed), and `#4561` (slow exhaustive arm umbrella, blocked on #4567).

## Next step

Hand off; the parent issue #4579 is already closed by PR #4586.

## Blockers

None.
