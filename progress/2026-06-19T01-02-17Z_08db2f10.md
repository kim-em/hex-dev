# 2026-06-19 — feature session (08db2f10)

Session type: `/work` → feature. Issue #8034.

## Accomplished

Phase 6 grind automation coverage for `HexModArith/HotLoop.lean`.
Promoted the second-tier equation-shaped public `@[simp]` lemmas to
`@[simp, grind =]`, matching the headline-lemma convention already in
this file (landed by #6239). Sixteen lemmas promoted:

- `MontResidue.toUInt64_eq_val`, `MontResidue.toNat_eq_val`
- `BarrettCtx.ofModulus_modulus`, `ofModulus_modulus_eq`,
  `ofModulus_toUInt64Ctx_pinv`
- `BarrettCtx.mulMod_one_left`, `mulMod_zero_left`, `mulMod_one_right`,
  `mulMod_zero_right`
- `MontCtx.modulus_ofOddModulus`, `modulus_toNat_ofOddModulus`,
  `toUInt64Ctx_ofOddModulus`
- `MontCtx.fromMont_mulMont_toMont_one_left`, `…_zero_left`,
  `…_one_right`, `…_zero_right`

`MontResidue.toNat_lt` left `@[simp]`-only as instructed — it is an
inequality (`a.toNat < p`), not an equation. The plain `theorem`s
(`mulMod_comm`/`mulMod_assoc`, `fromMont_mulMont_toMont_comm`/`_assoc`)
remain non-`grind` so commutativity introduces no cycle.

No grind loop: the new oriented rewrites have distinct LHS heads from the
headline `mulMod_eq_mul` / `fromMont_mulMont_toMont` rewrites and their
RHSs contain no `mulMod`/`mulMont`. `HexModArith.HotLoop` builds in
~0.9s, no slowdown.

## Verification

- `lake build HexModArith` → Build completed successfully (82 jobs).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`. Diff is exactly 16 attribute changes.

## Current frontier

Done. Diff confined to `HexModArith/HotLoop.lean`.

## Next step

None for this issue. Other Phase 6 files continue in the sweep.

## Blockers

None.
