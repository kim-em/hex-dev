# a685a787 - replan triage

## Accomplished

- Triaged the three `replan` candidates returned by `coordination list-replan`.
- #6041 (q-before raw nDet Plucker): worker-decomposed into #6093 and #6094.
  Sub-issues fully cover the parent, so closed #6041 with a forward-link
  comment to #6093 and #6094.
- #6093 (ordered four-basis Plucker for `twoColDet`): plan/approach stale.
  The previous worker correctly observed that #6052 only supplies bilinear
  expansion of `twoColDet B u v` over arbitrary `u, v`, not the
  Grassmann-Plucker identity itself. Created replacement #6099 that
  re-states the deliverable and suggests a deeper proof route
  (`twoColDet_basisVec_basisVec_of_lt` to a signed minor identity, then
  Laplace-expand the four-row substrate inside `HexMatrix/Determinant.lean`).
  Closed #6093 linking forward to #6099.
- Updated #6094 to depend on #6099 instead of the closed #6093 (no other
  body changes; the `blocked` label still applies because #6099 is open).
- #6044 (q-between raw nDet Plucker): body stale. The earlier worker
  produced a concrete 4x2 counterexample showing the stated rearrangement
  `D23*D01 - D03*D12 + D02*D13` evaluates to 18, not 0. Updated the body
  with the canonical Plucker shape `D23*D01 - D02*D13 + D03*D12 = 0`
  (in `nDet` form), retargeted the dependency from #6052 to #6099, and
  swapped `replan` for `blocked`.
- Accidentally created issue #6098 by invoking `coordination plan --help`;
  closed it immediately with an explanatory comment.

## Current frontier

`coordination list-replan` returns no candidates. The frontier in this
corner is #6099 (the replanned `twoColDet` Plucker substrate), which is
the open dependency for both #6094 (q-before specialization) and #6044
(q-between specialization).

## Next step

A planner or worker should pick up #6099 next. If the Laplace/expansion
route on the four-row substrate turns out to be too large for a single
session, the worker should decompose into a prerequisite four-row
Grassmann-Plucker minor lemma plus a thin consumer specialization, per
the suggested approach in the issue body.

## Blockers

- None for this triage session.
- #6094 and #6044 both remain blocked on #6099.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not
  touched.
