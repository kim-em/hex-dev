# Progress - 2026-06-16T14:55:33Z (session 8bf979a1, work #7591)

## Accomplished

Claimed #7591 and exposed the proof-facing BHKS projected-row trace surface.

In `HexBerlekampZassenhaus/Basic.lean`:
- Made the BHKS cut/projection/matrix conversion helpers public where downstream
  proofs need them.
- Added `bhksRowsArrayToMatrix_row` and `bhksRowsArrayToMatrix_toArray`, giving
  the `.toArray` bridge from `lll.shortVectorsUnchecked` back to the steered LLL
  matrix order.
- Added `BhksProjectedRowsTrace` with the unchecked reduced rows, reduced matrix,
  Gram determinant vector, retained source row indices, projected rows aligned
  with those indices, and the original executable cut output.
- Added `bhksProjectedRowsTrace_reducedMatrix_eq` and `bhksProjectedRows_eq_trace`.
  The production `bhksProjectedRows` body remains the direct old hot path, so the
  trace does not add eager Gram-determinant work to factor recovery.

In `HexBerlekampZassenhausMathlib/Lattice.lean`:
- Imported the Mathlib-side LLL independence/lattice theorem and added
  `BHKS.traceReducedMatrix_memLattice_iff`, proving that the trace reduced matrix
  spans the same integer row lattice as the original BHKS basis.

Verification:
- `lake build HexLLL`
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib.Lattice`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan: no new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

Second-opinion review flagged that an earlier version routed production
`bhksProjectedRows` through the full trace and would have added extra
Gram-determinant work; I fixed that before committing by restoring the direct
executable body and keeping compatibility as a theorem.

## Current frontier

The BHKS executable cut still has the same runtime shape, while proof-facing code
can now name the reduced LLL rows/matrix, retained indices, and lattice
preservation bridge.

## Next step

Follow-on issues #7592/#7593 can use `BhksProjectedRowsTrace` and
`BHKS.traceReducedMatrix_memLattice_iff` to build the cut-separation prefix
certificate and place true-factor CLD vectors in the BHKS lattice.

## Blockers

No blocker for this trace/span bridge. Existing dependency warnings replay from
`HexGramSchmidtMathlib` and `HexLLLMathlib` during the `Lattice` build, but this
session did not touch those files.
