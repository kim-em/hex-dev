# 2026-05-06 18:44 UTC — work session (d156fa33)

## Accomplished

Claimed #2491 and finished the packed GF2 quotient Frobenius fixed-point
degree bound in `HexGF2/Field.lean`:

- added `frobeniusIter_universal_fixed_degree_le`;
- added the Rabin-facing contrapositive
  `not_forall_frobeniusIter_eq_self_of_pos_lt_degree`;
- added an existential non-fixed element form;
- added the specialized `X` non-fixed theorem
  `frobeniusIter_X_ne_self_of_pos_lt_degree`.

Verification run:

- `lake build HexGF2.Field`
- `lake build HexGF2`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom |native_decide|TODO|FIXME|sorry' HexGF2/Field.lean`

`lake build HexGF2` still reports the pre-existing
`HexGF2/RabinSoundness.lean:272` `sorry` warning. This session introduced no
new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

#2491 is complete. The next downstream GF2 Rabin step is #2444, which can use
`frobeniusIter_X_ne_self_of_pos_lt_degree` to rule out a fixed quotient class
of `X` for positive remainders below the irreducible degree.

## Next step

Create the PR for #2491.

## Blockers

No blockers for #2491. The worktree still has the pre-existing unrelated
`.claude/CLAUDE.md` modification, which this session did not touch.
