# Phase 6 grind coverage: Factor.lean + Redc.lean (#8081)

## Accomplished

Promoted the two unconditional equation-shaped public `@[simp]` lemmas in
`HexBerlekamp/Factor.lean` to `@[simp, grind =]`:

- `factorProduct_nil : factorProduct [] = 1` (`rfl`)
- `Factorization.product_def : result.product = factorProduct result.factors` (`rfl`)

Both are literal `lhs = rhs` with def/projection-headed LHSs (not
`getD`/`dite`-headed), so they pass grind's pattern selection.

**Excluded `HexArith/Montgomery/Redc.lean`'s `redc_m_spec`** from `grind =`.
Its conclusion is `let m := Tlo * ctx.p'; m.toNat = (Tlo.toNat * ctx.p'.toNat)
% UInt64.word` — the `let`-binding makes the conclusion a `let`-expression, not
a bare `Eq`, so grind rejects it at registration: `invalid E-matching equality
theorem, conclusion must be an equality`. This is a permanent structural
property of the lemma shape, not a fixable regression, so it stays `@[simp]`-only
(mirroring the `↔`/`<` exclusions documented in earlier sweep progress files).
No follow-up issue is warranted.

## Current frontier

`lake build HexBerlekamp.Factor` (1.5s) and `HexArith.Montgomery.Redc` (586ms)
green — no grind-loop slowdown. Parent targets `HexBerlekamp` and `HexArith`
build clean (479 jobs, no errors/warnings), covering downstream consumers.
`scripts/check_dag.py` exit 0. `git diff --check` clean; no new
`sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines. Annotation-only;
no proof bodies, signatures, or imports changed. No `*Mathlib` file touched.

## Next step

Sibling #8082 (`HexArith/Barrett/ReduceNat.lean`, two conditional `grind =`
candidates) remains. Note the `let`/non-`Eq`-conclusion trap found here: any
sweep lemma whose statement wraps its equality in a `let` cannot be `grind =`.

## Blockers

None. (Separately: diagnosed and skipped #8068 this session — its fast `h_raw`
disjunct is not assemblable; the BHKS-core arm is gated on #6779/#2564. See the
issue comment.)
