# Session d29eb8f9 — #4896 abstract-bound siblings of `not_represents_empty_of_irreducible_dvd_core` (monic + primitive)

## Accomplished

Added two private abstract-bound siblings in
`HexBerlekampZassenhausMathlib/Basic.lean` and rewired the
existing two empty-subset utility theorems as thin wrappers:

* `not_represents_empty_of_irreducible_dvd_core_of_bound` (monic),
  inserted before the existing
  `not_represents_empty_of_irreducible_dvd_core`. Takes
  `B' : Nat`, `hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'`, and
  `hprecision : 2 * B' < d.p ^ d.k` in place of the core-shape
  `defaultFactorCoeffBound core` precision constraint. Proof body
  mirrors the original; only the single recovery-lemma call is
  retargeted to the `_of_bound` sibling
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`
  (landed in #4878).

* `not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core_of_bound`,
  inserted before the existing primitive variant. Same shape as the
  monic sibling but additionally takes
  `hcore_lc_bound : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B'`,
  because `hvalid` only bounds factor coefficients and the proof
  needs to bound the leading coefficient of `core`. The
  `hcore_size_pos` derivation (previously needed to extract the
  leading-coefficient bound from `defaultFactorCoeffBound_valid`)
  is unused in the `_of_bound` sibling and moves into the wrapper.

The wrappers preserve the existing signatures exactly. The monic
wrapper has two external call sites (lines ~11450, ~12498) and the
primitive wrapper has one (line ~12785); none require updates.

No new public API: both new theorems are `private`. No `axiom`,
`sorry`, or `native_decide`.

## Verification

* Build baseline checked: pre-existing whnf timeouts and four
  unknown-constant kernel errors at lines 4750, 5031, 5357, 5443,
  5705, 5827, 5975, 6147, 6128, 6462, 6526, 13885, 13996, 14203,
  14249 match origin/main exactly (with the ~60-line offset from my
  additions). No new errors anywhere; my edit range (lines
  ~11058-11337) is clean.
* `git diff --check` against origin/main — clean.
* `python3 scripts/check_dag.py` — passes.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean |
  grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.

## Current frontier

The monic-side abstract-bound chain is now propagated through:

* Bottom-level recovery (#4873 → #4878)
* Recovery candidate (#4883 → #4888, #4882 → #4889)
* `representsIntegerFactorAtLift_monic` (#4890 → #4894)
* `representsIntegerFactorAtLift_primitive` (closed via #4899)
* Empty-subset utilities (#4896 — this PR)

Issue #4895 (next-layer `natDegree_toPolynomial_eq_sum_of_represents`
monic sibling) and follow-ups for `representedFactor_dvd_*` and
`recombinationSearchModAux_*` remain unclaimed/in-flight.

## Next step

Propagate `_of_bound` through the next-layer consumers of these
empty-subset utilities — specifically
`representedFactor_dvd_recombinationCandidate_of_subset` (line
~11365) and `recombinationSearchModAux_*` (line ~12450+). These can
now take `B'` and threaded `hvalid` directly to the empty-subset
helpers instead of re-deriving the core-shape precision constraint
at each call site.

## Blockers

None.
