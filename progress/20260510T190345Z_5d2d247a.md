# 2026-05-10 19:03 UTC — feature session (5d2d247a)

## Accomplished

- Proved `eliminateColumn_transform_preserve` in `HexMatrix/RREF.lean`
  (#3080). The new lemma shows that running `eliminateColumn E T pivotRow
  col` preserves the same-operation invariant `T * M = E` (transform-times-
  original equals current echelon) when both `T` and `E` are updated by the
  same row-add operations. The proof reuses the per-step
  `rowAdd_transform_mul_preserve` from `HexMatrix/RowEchelon.lean` (closed
  by #3060), threaded through a list-induction over `eliminateColumn`'s
  fold.
- Skipped #3088 (HexPolyFp Yun stepsSquareFree). Direct evaluation
  showed the public theorem `squareFree_factors_squareFree` is literally
  false on existing `#guard` fixtures: for `polyFive #[1,1,1]` over
  `F_5`, the emitted factors `4+4X+4X²` and `2` have
  `gcd factor (derivative factor)` equal to `3` and `2` respectively, not
  `1`. Documented the counterexample in a comment on the issue and
  marked the issue `replan` since the public statement (the issue
  forbids changing it) needs amendment first.

## Current frontier

- `eliminateColumn_transform_preserve` is in place, ready to feed into
  the `rrefLoop` outer invariant.
- The `squareFree_factors_squareFree` chain in `HexPolyFp/SquareFree.lean`
  is blocked on a spec defect; the next planner needs to revise the
  public statement (e.g. to use `normalizeMonic` like
  `squareFree_pairwise_coprime` does) before any worker can land the
  derivative-split Yun proof.

## Next step

- For #3080's downstream: a follow-up will prove the corresponding
  `rrefLoop` invariant lemma using `rowSwap_transform_mul_preserve` and
  `rowScale_transform_mul_preserve` plus this new lemma.
- For #3088's replan: the planner should amend the public theorem (and
  the predicate chain) to compare modulo monic normalization.

## Blockers

- #3088 is permanently blocked under its current spec; only a planner
  revising the public theorem statement can unblock the work.
