# 20260603T031618Z work #6417

## Accomplished

- Claimed issue #6417 (BHKS D1 prerequisite: construct closed
  `CanonicalRecoveryTailInputs` at `factorFast` cap) and added a closed
  producer in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- `BHKS.CanonicalRecoveryTailInputs.ofCanonicalSupportRepresentations` accepts
  the support-partition shape facts (`projected_nonempty`, `classes_two`),
  the nonzero core fact, the expected true-factor `ExpectedTrueFactors`
  package, and one `RepresentsIntegerFactorAtLift` certificate per canonical
  support class (against the canonical `liftedFactorSubsetsOfSupports` array).
  The per-class Mignotte product congruence is derived internally by composing
  `ForwardRecoveryInputs.selectedFactorArraysOfSupports_polyProduct` with
  `ForwardRecoveryInputs.productCongruencesOfCanonicalSupportMemberRepresentations`,
  and the unconditional `class_nonempty` / `class_bounds` shape facts are
  routed through the existing `ofExpectedFactors` constructor.
- The producer leaves the actual-cap `FactorFastCapSeparationInputs` surface
  untouched and so remains compatible with the closed cap-separation producer
  from #6416 and with the final wrappers
  `factorFast_ne_none_of_factorFastCapSeparationInputsCanonicalRecoveryTailInputs`,
  `factorFast_ne_none_of_factorFastCapSeparationInputsAndCanonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`,
  and
  `factorFast_terminates_of_factorFastCapSeparationInputsAndCanonicalRecoveryTailInputs`.

## Current frontier

The recovery-tail side at the actual `factorFast` cap branch now has a closed
constructor that fixes the call shape of the per-class Mignotte product
congruence: callers supply `RepresentsIntegerFactorAtLift` certificates against
the canonical `liftedFactorSubsetsOfSupports` array, rather than a precomposed
modular reduction equation.  Producing those certificates for the canonical
true supports is the remaining recovery-side D1 obligation.

## Next step

Either:
- (recovery side) construct the per-canonical-class
  `RepresentsIntegerFactorAtLift` certificates and the `ExpectedTrueFactors`
  package at `factorFastCapLiftData f primeData` from the squarefree-core
  factorisation data, feeding them into
  `CanonicalRecoveryTailInputs.ofCanonicalSupportRepresentations`; OR
- (cap side) continue producing closed cap-separation bridge / cut / analytic
  comparison witnesses at the actual cap to feed
  `FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSq`.

Both feed
`factorFast_terminates_of_factorFastCapSeparationInputsAndCanonicalRecoveryTailInputs`.

## Blockers

No technical blocker for this slice.  The new producer is purely additive:
the existing `CanonicalRecoveryTailInputs.ofExpectedFactors` and downstream
wrappers continue to compile and consume the same record shape.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed (exit 0).
- `git diff --check` passed.
- Added-line forbidden-token grep over `Recovery.lean` found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
