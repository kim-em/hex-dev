# 20260517T060203Z — feature session 211f1eaa (#4334)

## Accomplished

Claimed #4334 (HexLLL Phase 4: investigate inconclusive scientific
benchmark verdicts).  Verified the measurement environment per the
issue's standing instruction by probing
`Hex.LLLBench.runSizeReduceChecksum` (the canonical canary used in
every prior skip).  Same blocker as 16 prior attempts: per-spawn
floor measured 1.256 s (vs ~33 ms in the e211854d artifact),
killed_at_cap at param 128 with `maxSecondsPerCall = 2 s`, zero
verdict-eligible rows.  Probe artifact:
`/tmp/hex-bench-probe/probe-sizereduce.json`.

Host context at probe time:
- `hostname` = carica (T6020, 24 CPUs)
- `uptime` = load avg 6.38 / 7.83 / 8.91 immediately before the probe;
  1-min load was 5.97 at claim time but the 5-min and 15-min averages
  remained in the elevated 8-9 range.
- Top-CPU processes are macOS daemons (`tccd`, `log stream`,
  `diagnosticd`, `syspolicyd`, `coreaudiod` all 17-56% CPU), plus VSCode
  Pylance helper at 17.5% and a tcc-notifier python at 11.7%. No
  competing Lean builds, but enough background load to keep the
  per-spawn floor ~38× the e211854d baseline.

`lake exe hexlll_bench list` (implicitly via the probe run) and
`lake exe hexlll_bench verify` are not re-run here because the smoke
verifier passed in every prior attempt and the substantive blocker is
upstream of compilation.

## Current frontier

`reports/bench-results/hex-lll-e211854d1435.json` remains the latest
committed scientific artifact, with `inconclusive` verdicts for the
five registrations listed in the issue body
(`runSizeReduceChecksum`, `runGramSchmidtCoeffChecksum`,
`runFirstShortVectorHarshCubicChecksum`,
`runOfBasisHarshCubicChecksum`,
`runFirstShortVectorRandomBoundedChecksum`).  `HexLLL.done_through`
stays at 3 in `libraries.yml`.

## Next step

Per the issue body's standing instruction:

> Re-dispatch only when the host is idle enough for a stable spawn
> floor, or run the release-candidate benchmarking workflow on
> dedicated hardware.

Two paths forward, both outside this session's reach:

1. Wait for carica to quiet down (1-min load consistently below ~1.5
   for several minutes, with no competing Lean builds or VSCode helper
   spikes).  Empirically the spawn floor needs to drop to roughly the
   e211854d baseline (~33 ms) before any of the five affected
   registrations will produce verdict-eligible rows under their declared
   `maxSecondsPerCall`.
2. Schedule a release-candidate benchmarking run on dedicated hardware
   per `SPEC/benchmarking.md` §"Scientific timing runs".  This is the
   doctrinally sanctioned path and avoids the contention loop entirely.

No code changes this session.  Skipping with `needs replan` so the
issue returns to the queue for a later attempt under quieter
conditions.

## Blockers

Measurement environment (host contention on carica).  The same
blocker the issue body already documents and that 16 prior worker
sessions have hit.  Not a code, build, or Lean-toolchain issue.
