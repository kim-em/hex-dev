# Progress: HexGFqField inverse/division automation smoke checks (#6259)

## Accomplished

Completed #6259 after first triaging higher-priority directive issues and
the focused #6268 follow-up:

- Directive issues #2564 and #2567 were rejected by `coordination claim`
  because both are blocked by open dependencies.
- Claimed #6268, inspected the requested theorem shape, and marked it
  `replan`: `BareissGramRowInvariant` alone records row representation but
  not the exact-division/minor provenance needed to make `Matrix.exactDiv`
  commute through row combinations for arbitrary states.
- Restored #6259 from the mistaken earlier replan label, claimed it, and
  implemented the requested automation smoke checks.

Code changes:

- Added `@[simp]` to `Hex.GFqField.div_eq_mul_inv`, a one-way definitional
  rewrite from division to multiplication by inverse.
- Added three examples in `HexGFqField/Conformance.lean` checking:
  - `simp` proves `(0 : F)⁻¹ = 0`;
  - `simp` rewrites `x / y` to `x * y⁻¹`;
  - `grind` proves `x ≠ 0 → x * x⁻¹ = 1`.
- Cleaned the local `Lean.Grind.Field` instance proof to use the new simp
  surface.

Verification:

- `lake build HexGFqField.Conformance` passes.
- `lake build HexGFqField` passes.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- Forbidden-token diff grep over touched GFq files found no added `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

#6259 is complete with only the intended smoke examples and the narrow
`div_eq_mul_inv` simp annotation. The only warnings seen during builds are
pre-existing `HexGFqRing/Operations.lean` unnecessary-`simpa` linter warnings,
not introduced in this session.

## Next step

Create the PR for #6259. #6268 needs replanning around a stronger invariant
or a trajectory-specific theorem that carries exact-division/minor provenance.

## Blockers

None for #6259. #6268 is blocked on a corrected theorem shape or strengthened
state invariant; the arbitrary-state theorem requested in the issue is not
derivable from the current `BareissGramRowInvariant` fields alone.
