# Work session 9cd62063 — feature #3543

## Accomplished

- Claimed issue #3543 (HexLLL: prove Gram determinant and lattice norm bridges).
- Replaced the four `sorry` proofs in `HexLLL/Basic.lean` for
  `Matrix.gramDet_eq_prod_normSq`, `Matrix.gramDet_pos`, `Matrix.basis_normSq`,
  and `Matrix.normSq_latticeVec_ge_min_basis_normSq` with thin delegations to
  the matching `GramSchmidt.Int` API.
- The `Matrix.independent` and `Matrix.memLattice` predicates are
  definitionally equal to their `GramSchmidt.Int.*` counterparts, so the
  hypotheses pass through directly without conversion.
- `lake build HexLLL.Basic` and `lake build HexLLL` succeed.
  `python3 scripts/check_dag.py` and `git diff --check` are clean.
  `rg '^axiom\b|native_decide|TODO|FIXME' HexLLL/Basic.lean` returns nothing.
  Remaining `sorry`s in `HexLLL/Basic.lean` are at lines 470, 473, 482, 487,
  492, 568, 573, 597, 604, 611 — all out-of-scope (swapStep updates,
  sizeReduce side facts, `lllAux` termination).

## Current frontier

- The four targeted holes are removed from `HexLLL/Basic.lean`. They still
  transitively depend on the matching `GramSchmidt.Int.*` sorries, tracked by
  #3565 (residual triangular GS determinant product bridge) and successors.

## Next step

- Push branch and open PR closing #3543.

## Blockers

- None.
