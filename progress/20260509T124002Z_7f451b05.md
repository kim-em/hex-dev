# 20260509T124002Z - HexBerlekamp Phase-4 evidence restore (#2739)

## Accomplished

Claimed issue #2739 and restored `HexBerlekamp` Phase-4 evidence after
the audit rollback. Added `HexBerlekamp.phase4.input_families` metadata
to `libraries.yml`, promoted `HexBerlekamp.done_through` from 3 to 4,
created `reports/hex-berlekamp-performance.md`, and committed the
scientific benchmark export at
`reports/bench-results/hex-berlekamp-e7bf7c2.json`.

Scientific `hexberlekamp_bench` run at `e7bf7c23bbb5` on `carica`
reported all four registered targets consistent with their declared
complexities:

- `runBerlekampMatrixChecksum`: quadratic, parameters `16..192`
- `runRabinTestChecksum`: cubic, parameters `8..64`
- `runBerlekampFactorChecksum`: quadratic, parameters `16..256`
- `runDistinctDegreeChecksum`: cubic, parameters `12..96`

Recorded one `samply` profile per Phase-4 input family under
`/tmp/hex-profiles/` and summarized the attribution review in the
headline report.

Verification completed:

- `python3 scripts/check_dag.py`
- `python3 scripts/check_phase4.py`
- `lake exe hexberlekamp_bench list && lake exe hexberlekamp_bench verify`
- `lake build`

## Current frontier

The HexBerlekamp Phase-4 evidence package is ready for PR creation.
The worktree still has a pre-existing unstaged `.claude/CLAUDE.md`
change that was not touched for this issue.

## Next step

Create the PR closing #2739 with the report, benchmark artefact,
`libraries.yml`, and this progress file staged.

## Blockers

None.
