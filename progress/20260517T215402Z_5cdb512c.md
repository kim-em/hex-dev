# Small-mod singleton executable branch substrate (#4826)

## Accomplished

Added `choosePrimeData?_berlekampFactor_factors_length_le_one_of_small` in
`HexBerlekampZassenhaus/Basic.lean`. The theorem exposes the executable
small-mod singleton shape fact: when `choosePrimeData?` succeeds and the
stored `factorsModP` array has size at most one, the underlying Berlekamp
factor list for the selected monic modular image also has length at most one.

This avoids importing `HexBerlekamp.RabinSoundness` into the BZ executable
module. A direct wrapper from this shape fact to `FpPoly.Irreducible` was
attempted, but elaborating the Rabin soundness theorem in this file timed out
before producing a usable proof term.

Verification:

* `lake build HexBerlekampZassenhaus` — passes.
* `python3 scripts/check_dag.py` — passes.
* `git diff --check` — clean.
* `rg -n "sorry|axiom|native_decide" HexBerlekampZassenhaus/Basic.lean` —
  no new entries; the existing `isIrreducible_iff` `sorry` remains.

## Current frontier

This is a partial #4826 result. The executable branch can now recover the
selected Berlekamp singleton-list length without re-running the prime-selection
fold, but the full Mathlib-free `ZPoly.Irreducible` witness is still missing.

## Next step

Prove the remaining transfer in a module that can afford the Rabin soundness
dependency, or factor a cheaper theorem out of `HexBerlekamp.RabinSoundness`
so `HexBerlekampZassenhaus/Basic.lean` can derive `FpPoly.Irreducible` from
the singleton length plus square-free monic image without timing out. Then
compose that modular irreducibility witness with the integer primitive /
leading-coefficient-good-prime invariants to produce the branch-level
`ZPoly.Irreducible` theorem required by #4825.

## Blockers

The direct executable-file proof of modular irreducibility timed out at
elaboration when importing and applying `Berlekamp.berlekampFactor_singleton_irreducible`.
