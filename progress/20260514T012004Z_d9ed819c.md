# Session d9ed819c — `/work` triage on #3879 and #3885

## Accomplished

- Claimed #3879 (HexMatrix Bareiss bordered-minor Desnanot
  specialization). Verified its dependency #3878 was closed
  `NOT_PLANNED` with explicit triage that the deliverable's placement
  (`HexMatrix/Determinant.lean`) violates SPEC/Libraries/hex-matrix.md
  lines 131-144 and PLAN/Conventions.md Q2 — cancellation of `det M`
  needs `MvPolynomial`-style infrastructure that is banned from the
  Mathlib-free library. Skipped #3879 (replan) with a pointer back to
  the prior triage and the two re-host paths the planner already
  enumerated.

- Claimed #3885 (HexGfq N32 generic-irr wrapper). Orientation showed
  the work needs (1) literal degree-32 certificate data — 33 powChain
  entries + 32 quotient witnesses + 1 Bezout witness, all hand-pasted
  because `native_decide` is banned, (2) 32 per-step `decide`-closed
  kernel proofs, and (3) the basis-size / final-entry / Bezout
  assembly that replaces the `sorry`. The previous split (#3866 →
  #3867) was deemed leaky precisely because step (1) and the wrapper
  were merged into one claim.

- Decomposed #3885 into three sized-for-one-session sub-issues with
  ordering deps wired:

  - #3893 emits the literal N32 certificate (powChain + quotients +
    Bezout) and validates it via two `#guard` boolean checks at
    elaborator time. No theorem-level proof. Models on
    `HexGfq/EmitFixtures.lean`'s emit pattern.
  - #3894 (depends on #3893) proves
    `checkPowChainLinearIncrementalQuotientWitnesses ... = true` via
    32 separately-named per-step `decide`-closed lemmas, then lifts
    through `checkPowChainLinearIncremental_of_quotientWitnesses`.
  - #3895 (depends on #3893, #3894) proves the basis-size,
    final-entry, and Bezout sub-lemmas, assembles
    `checkIrreducibilityCertificateLinearIncremental ... = true`,
    and replaces the N32 `generic_irr` `sorry` via
    `rabinTest_imp_irreducible` +
    `checkIrreducibilityCertificateLinearIncremental_rabinTest`.

- Skipped #3885 (replan) with a `Decomposed into #3893, #3894, #3895`
  comment.

## Current frontier

The N32 wrapper path is now scoped into three single-session sub-issues.
The Bareiss bordered-minor specialization (formerly #3879) needs a
planner decision on re-hosting vs. rewiring against the existing
Mathlib-side proof; nothing further is actionable from a worker
session without that call.

## Next step

Future workers can pick up #3893 first (data emission). #3894 and
#3895 are blocked on their predecessors and will unblock automatically
via `coordination check-blocked`.

For the Bareiss side, a planner needs to either re-file #3879 against
`HexMatrixMathlib/Determinant/` or replace it with a `HexMatrix/Bareiss`
rewiring issue that consumes the existing Mathlib-side
`desnanot_jacobi_borderedMinor`.

## Blockers

None for this session. The worktree's pre-existing
`.claude/CLAUDE.md` modification from pod bootstrap remains
unstaged, as do the untracked `.claude/commands/` and
`.claude/skills/` directories; none were touched.

No code changes landed — both claimed issues were stale-plan skips,
one via direct `coordination skip`, the other via decomposition.
