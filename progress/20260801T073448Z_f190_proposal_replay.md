# F190 checked proposal replay

## Accomplished

- Added an unforced, streaming cardinality-one-to-three sweep with separate
  combinatorial/filter/construction/exact-division counters.  The first exact
  factor and quotient retain their complementary modular support without a
  second scan.
- Added leading-column CLD preparation and deterministic 4/8/12/16-column
  prefix lattices.  Production prepares the maximum prefix once and stops at
  the first exact partition; the wider widths remain available to the phase
  profiler for stability measurements.
- Added a proof-carrying proposal boundary.  Proposed pieces must reconstruct
  the original polynomial exactly, and every piece is rerun through the
  unrestricted proved classical factorizer before the total factorizer accepts
  the new `replay` branch.  The Mathlib product and irreducibility theorems use
  only these exact checks and the existing classical theorem.
- Preserved `factorClassical` as the unrestricted public classical method and
  made the proposal decision an additive internal routing layer.
- Added proposal traces/profiling, a conformance replay sentinel, trace
  baselines, SPEC text, and mathematician-facing manual documentation.
- Addressed an independent review of the implementation, including unrestricted
  replay, CLD attempts without a successful peel, normalization/core routing,
  prefix reuse, a distinct cardinality-cap decline, and public-surface docs.
- Verified `lake build`, `lake build HexConformance`, the published trust-surface
  check, byte-identical fixture regeneration, the deterministic trace gate, and
  python-FLINT cross-checks (105 cases, no failures).
- Ran the complete 392-row pinned factor sweep.  Hex now answers 376 rows rather
  than 375; `hoeij_F190` is the only status change.  Three final warm-process
  runs took 7.109, 7.123, and 7.115 seconds and returned degrees 10, 90, and 90.
  `hoeij_M12_f132` remains at approximately 1.64 seconds.

## Current frontier

The implementation and measurements satisfy issue #9119 and are ready for a
pull request.  On F190 the sweep visits 599 leaves, constructs and exactly
divides one degree-10 factor, and the first 40-dimensional (36 local factors
plus four CLD columns) native certified reduction proposes the stable 18/18
residual split.  The remaining end-to-end cost is chiefly classical replay of
the two degree-90 pieces.

## Next step

Open the pull request with the complete before/after and phase measurements,
watch the exact GitHub Actions job states, fix any CI-only failures, and merge
when green.

## Blockers

None.
