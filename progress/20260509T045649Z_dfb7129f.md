## Accomplished

- Claimed #2730 and restored HexPolyMathlib Phase-4 evidence after the audit
  rollback recorded in #2708:
  - added two `HexPolyMathlib.phase4.input_families` entries to
    `libraries.yml` (`dense-mathlib-conversion`,
    `euclidean-bridge-transport`);
  - recorded a scientific `hexpolymathlib_bench run` export at
    `reports/bench-results/hex-poly-mathlib-0a0ff08951c2.json` covering all
    five registered targets (`runToPolynomialChecksum`,
    `runOfPolynomialChecksum`, `runRoundTripChecksum`, `runGcdBridgeChecksum`,
    `runXGcdBridgeChecksum`); every verdict came back consistent with
    declared complexity;
  - produced `reports/hex-poly-mathlib-performance.md` with bench targets,
    verdicts, comparator-ratio narrative (no external comparator named in
    the SPEC), profile coverage for both families (single representative
    `samply --unstable-presymbolicate` runs, child-only sample aggregation,
    leaf cost categorised across {own, GMP, allocation, Lean runtime,
    other}), and an empty Concerns section;
  - bumped `HexPolyMathlib.done_through` from 3 to 4.
- Verified with `python3 scripts/check_dag.py`,
  `python3 scripts/check_phase4.py`, `git diff --check`, and
  `lake exe hexpolymathlib_bench list && lake exe hexpolymathlib_bench verify`.

## Current frontier

- `scripts/status.py` now reports `HexPolyMathlib` ready for Phase 5.
- The worktree still has the unrelated pre-existing `.claude/CLAUDE.md`
  modification that previous turns also documented; it is excluded from
  this PR.

## Next step

- Continue with the remaining Phase-4 audit-rollback restorations on
  unblocked libraries; HexHensel (#2736) and HexConway (#2741) are the
  next obvious depth-1 targets.

## Blockers

- None for #2730.
