# Session 57068556 — feature (#7111)

## Accomplished

Added Phase 6 docstrings to the ten private carry/borrow limb-arithmetic
specification lemmas in `HexModArith/Basic.lean` (`add_carry_{toNat,lt}`,
`add_noCarry_reduce_{toNat,lt}`, `add_noCarry_noReduce_{toNat,lt}`,
`sub_noBorrow_{toNat,lt}`, `sub_borrow_{toNat,lt}`). Each docstring names
its `add`/`sub` branch, states the representative value (`_toNat`) or the
`< p` canonical-range bound (`_lt`) it proves, and notes the `p < 2^64`
side condition where the complement/modulus word is used.

Docstring-only: 29 insertions, 0 deletions; no statement, proof, attribute,
import, or namespace touched.

## Verification

- `lake build HexModArith.Basic` clean (63 jobs, no new warnings).
- `git diff --numstat`: 29/0 (insertions only).
- `git diff --check` clean; no `sorry`/`axiom`/`native_decide`/`TODO`/
  `FIXME`/em-dash on added lines.
- `python3 scripts/check_dag.py` exits 0.

## Current frontier

Sibling issues #7112 (inverse/power private lemmas in the same file) and
#7113 (Ring.lean negation/ring-axiom lemmas) remain unclaimed; landing
both plus this completes `HexModArith/Basic.lean` docstring coverage.

## Next step

Claim #7112 or #7113 to finish the HexModArith Phase 6 docstring cluster.

## Blockers

None.
