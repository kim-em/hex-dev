# Discharge `luebeckConwayPolynomial?_degree_pos` axiom

## Accomplished

- Replaced the `axiom luebeckConwayPolynomial?_degree_pos` in
  `HexConway/Basic.lean` with a `theorem` that case-splits on
  `luebeckConwayCoeffs? p n` via `split at hcoeffs` after unfolding,
  then applies a uniform helper to each of the 36 table entries.
- Added two private helpers in `HexConway/Basic.lean`:
  - `zmod64_one_ne_zero_of_one_lt`: `(1 : ZMod64 p) ≠ 0` for `1 < p`,
    using the existing `ZMod64.natCast_eq_natCast_iff`.
  - `ofCoeffs_degree_pos_of_back_ne_zero`: a `DensePoly.ofCoeffs` array
    with size ≥ 2 and nonzero last entry produces a polynomial whose
    `degree?.getD 0` is positive. Uses only public APIs
    (`coeff_ofCoeffs`, `coeff_eq_zero_of_size_le`).
- The 37 split branches (36 table hits + 1 `none` catch-all) are
  dispatched uniformly: in the catch-all `cases hcoeffs` closes
  via `none ≠ some` discrimination, and in each hit the helper +
  `simp` + `(1 : ZMod64 p) ≠ 0` closes the goal.

## Verification

- `lake build HexConway` ✓
- `lake build` (full) ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `rg "^axiom luebeckConwayPolynomial\?_degree_pos" HexConway/Basic.lean`
  returns no hits ✓
- The two unrelated leftover items (`luebeckConwayPolynomial_2_1_irreducible`
  sorry on line 121, and `axiom luebeckConwayPolynomial?_irreducible` on
  line 127) remain unchanged — they are tracked under separate sub-issues
  of #2244.

## Current frontier

`HexConway/Basic.lean` now has zero axioms tied to degree positivity.
The remaining axiom (`luebeckConwayPolynomial?_irreducible`) and the
single sorry (`luebeckConwayPolynomial_2_1_irreducible`) both sit on
the Rabin-bridge work tracked separately.

## Next step

Issue #2256 is fully addressed; create PR.

## Blockers

None.
