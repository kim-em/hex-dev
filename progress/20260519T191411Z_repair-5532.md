# 20260519T191411Z — repair session for PR #5532

## Accomplished

- Claimed PR #5532 (`feat: polish DensePoly constructor API`) for repair after `coordination list-pr-repair` reported it as failed.
- Diagnosed the failure: the PR adds `@[simp]` to `DensePoly.coeff_monomial`, which made `simpa using DensePoly.coeff_monomial 1 1 0` over-simplify its hypothesis to `True` in `HexPolyFp/Basic.lean`. The same problem hit all three checks (`build`, `build-macos`, `conformance`) at line 1009.
- Rewrote the two affected proofs (`coeff_X_zero`, `coeff_X_one`) to use `exact DensePoly.coeff_monomial ...` after `unfold FpPoly.X`, matching the pattern already used for the general `coeff_X` lemma below them.
- Verified:
  - `lake build HexPolyFp.Basic`
  - `lake build`

## Current frontier

- PR #5532 is repaired locally on `agent/671634a5` and ready to push.

## Next step

- Commit the proof fix together with this progress file, push with `--force-with-lease`, and mark PR #5532 salvaged.

## Blockers

- None.
