## Accomplished

Claimed and completed #3001.

In `HexGramSchmidt/Basic.lean`, added private prefix-span transport helpers:

- `rowCombination_eq_foldl_rows`
- `prefixSpan_zero`
- `prefixSpan_rowCombination_of_rows`
- `prefixSpan_mono_succ`
- `prefixSpan_mono_le`
- `prefixSpan_matrix_row`
- strict-prefix row-combination span helpers

Used those helpers to prove `Hex.GramSchmidt.Rat.basis_span` by induction over
the inclusive prefix. The proof transports arbitrary row combinations once each
source prefix row is known to lie in the target prefix span, and uses
`basis_decomposition` plus the strict-prefix combination lemmas for the newest
row.

Also discharged `Hex.GramSchmidt.Int.basis_span` as the immediate wrapper around
the rational theorem for `castIntMatrix`.

Verified:

- `lake build HexGramSchmidt.Basic`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean`
  returns no matches

## Current frontier

`HexGramSchmidt/Basic.lean` no longer has theorem-level `sorry`s for the prefix
span invariance surface. Remaining GramSchmidt sorries are in other files and
were not touched.

## Next step

Continue with the downstream GramSchmidt/LLL issues that depend on
`Rat.basis_span` and the integer wrapper now being available.

## Blockers

None.
