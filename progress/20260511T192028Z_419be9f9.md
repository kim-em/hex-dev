# Feature session 419be9f9 — issue #3566

## Accomplished

- Claimed issue #3566 (HexGramSchmidt: derive GS norm ratio from
  determinant product bridge) and verified the dependency #3565 was
  closed via decomposition into #3575 + PR #3576.
- Added `GramSchmidt.Int.gramSchmidtNormProduct_succ`, a private
  one-step extension lemma for the `List.finRange`-based norm-squared
  product over the Gram-Schmidt basis.
- Proved the public `GramSchmidt.Int.basis_normSq` theorem, deriving
  the norm-squared ratio directly from the still-`sorry`'d
  `gramDet_eq_prod_normSq` and `gramDet_pos` API plus
  `Rat.mul_div_cancel` cancellation.
- Verified `lake build HexGramSchmidt.Int`, `lake build HexGramSchmidt`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the targeted
  banned-marker scan — no new sorries/axioms/native_decide.

## Current frontier

- `HexGramSchmidt/Int.lean` has 7 remaining sorries (was 8); the
  `basis_normSq` one is now closed. The remaining seven are owned by
  other issues (`gramDet_eq_prod_normSq` via #3575/PR #3576,
  `gramDet_pos`, and the scaled-coefficient/normSq_latticeVec sorries).

## Next step

- Push branch `agent/419be9f9` and run `coordination create-pr 3566`.

## Blockers

- None.
