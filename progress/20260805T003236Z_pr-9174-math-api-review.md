# PR #9174 mathematical and API review

## Accomplished

Reviewed `HexBerlekampZassenhausMathlib/SquareClass.lean` against issue #9167
and the tower-theorem proof in
`reports/hexbz-quadratic-norm-certificate.md`. Checked the strengthened
`built ++ rest` induction case-by-case, the positional-subset meaning of
`Independent`, `IsSquareIn`, the all-roots definition of `adjoinSqrt`, and the
interfaces needed by the primitive-element/minimal-polynomial follow-up.

The degree theorem has no mathematical correctness blocker. The four induction
branches implement toggling the new radicand in the paper's symmetric
difference, with the selected/second-conclusion branch correctly cancelling
the nonzero square factor. `not_isSquareIn_of_perm_cons` is sufficient for the
paper's distinguished-index contradiction; a full basis of square-root
products is not required.

Identified remaining API work: an end-to-end theorem connecting the executable
Boolean independence check to `Independent`, ergonomic erase-index/root
nonmembership wrappers for the follow-up, and optional dependency/naming
cleanup. Observed but did not touch the concurrent uncommitted
`adjoinSqrt_mono` change in the worktree.

## Current frontier

PR #9174 is mathematically ready modulo review-policy decisions about whether
the Boolean decision theorem and follow-up convenience lemmas belong in this PR
or the already-planned follow-up issues.

## Next step

Prove `independentSquareClasses ds = true ↔ Independent ds.toList` (including
the perfect-square and bitmask-enumeration bridges), and add an erase-index or
prefix/suffix corollary turning a chosen root's membership in the field of all
other radicands directly into a contradiction.

## Blockers

None. Live `gh` access was unavailable in this sandbox; the complete issue
directive was recovered from the user-supplied prior conversation log.
