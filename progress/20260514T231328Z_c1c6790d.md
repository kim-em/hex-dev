# Session c1c6790d — work #4061

## Accomplished

- Read the latest progress entry and oriented against `PLAN.md`,
  `PLAN/Conventions.md`, and `SPEC/Libraries/hex-berlekamp-mathlib.md`.
- Checked the human-oversight queue; #3945 could not be claimed because it is
  marked `replan`.
- Claimed #4075, found its dependency was stale because #4074 was closed by
  decomposition while successor #4085 remains open, and released #4075 for
  replan with that blocker.
- Claimed #4061 and added the CRT witness assembly layer in
  `HexBerlekamp/RabinSoundness.lean`:
  `exists_reduced_crtZeroOne_kernelWitness_of_coprime_split` and
  `exists_reduced_crtZeroOne_kernelWitness_of_squareFree_split`.
- Verified `lake build HexBerlekamp.RabinSoundness`,
  `lake build HexBerlekamp`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a diff grep for `axiom` / `native_decide` /
  `TODO` / `FIXME` / `sorry`.

## Current frontier

#4061 now has a reduced zero-one CRT witness theorem for a monic coprime
product split, plus a square-free wrapper that derives the coprime surface from
`common_dvd_one_of_squareFree_mul` when the executable gcd is known monic.

## Next step

Downstream #4076 can use this theorem as the algebraic contradiction source,
provided its caller can supply the monic-factor/gcd-monic bridge for the
chosen reducible split. Separately, #4075 should wait for #4085, the residual
variable prime-field product identity issue.

## Blockers

- #4075 is blocked on #4085.
- The completely general "arbitrary reducible monic square-free `f`" wrapper
  still needs a local theorem extracting a monic coprime split, or proving
  monicity of `DensePoly.gcd a b`, from the current reducible split surface.
