# Progress — 30884263 (feature)

## Accomplished

Closed #7276: added Phase 6 docstrings to the 7 private theorems in the
row-swap permutation cluster of `HexGramSchmidt/Basic.lean`
(`rowSwap_toList_get!_of_lt`, `_left`, `_right`, `_of_gt`,
`basisMatrix_rowSwap_of_before`, `basisMatrix_rowSwap_adjacent_prev`,
`basisMatrix_rowSwap_adjacent_curr`). Each docstring is a single
backtick-led sentence stating what the lemma asserts. Doc-only; no
statement, proof, name, attribute, import, or namespace changed.

## Current frontier

`lake build HexGramSchmidt.Basic` green, `scripts/check_dag.py` exits 0,
`git diff --check` clean, no banned vocabulary or em-dashes on added lines.

## Next step

Open PR closing #7276.

## Blockers

None.
