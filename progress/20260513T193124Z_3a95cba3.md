# Session 3a95cba3 — rational row-span transport (#3789)

## Accomplished

- Added `intVectorToRat` lemmas to `HexBerlekampZassenhausMathlib/Lattice.lean`:
  `intVectorToRat_zero`, `intVectorToRat_add`, `intVectorToRat_intSmul`.
- Proved `intVectorToRat_mem_span_rat_of_mem_span_int`: integer span membership
  carries over to rational span membership of the pointwise rational cast,
  by `Submodule.span_induction`.
- Proved `span_rat_image_eq_of_span_int_eq`: equal ℤ-spans of two integer
  vector sets give equal ℚ-spans of their pointwise rational casts.
- Defined `trueFactorIndicatorLattice_rat` as the rational counterpart of
  `trueFactorIndicatorLattice`.
- Added the rational/integer row-shape bridges
  `row_projectedRowsRatMatrix_eq_intVectorToRat`,
  `range_row_projectedRowsRatMatrix_eq_image`, and
  `range_intVectorToRat_indicatorVector_eq_image`.
- Proved the main transport
  `projectedRowSpaceRat_eq_trueFactorIndicatorLattice_rat`: the integer
  `L' = W` from `BHKS.projectedRowSpan_eq_trueFactorIndicatorLattice_of_cap`
  lifts to a rational row-space equality consumable by the executable RREF
  row-span theorem (#3756).
- Build: `lake build HexBerlekampZassenhausMathlib.Lattice` clean,
  `lake build HexBerlekampZassenhausMathlib` clean.
- `python3 scripts/check_dag.py` clean.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` in
  `Lattice.lean`.

## Current frontier

The rational row-span transport step (first leg of B7) is now Mathlib-side
infrastructure. Downstream consumers needing
`Submodule.span ℚ (Set.range ... projectedRowsRatMatrix ...)
  = Submodule.span ℚ (Set.range ... intVectorToRat (indicatorVector ...))`
from the integer-side cap separation can read it off
`projectedRowSpaceRat_eq_trueFactorIndicatorLattice_rat`.

## Next step

The follow-on B7 sub-issues are:
- #3791 — RREF column-signature ↔ row-span agreement (uses #3756 and the
  rational row span proved here).
- The final `EquivalenceClassRecoveryHypotheses.ofIndicatorLattice`
  assembly that composes #3789 + #3791 + class-emission ordering.

## Blockers

None for this slice.
