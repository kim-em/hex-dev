# 20260609T122334Z (#6645)

## Accomplished

Claimed directive #6645 and finalized the committed HexLLL five-way
comparator plot/report pass.

- Extended `scripts/plots/hex-lll-comparator.py` with the committed
  `verified Isabelle certified-LLL` series for both `random-bounded` and
  `harsh-cubic`, including an override flag and a fifth marker style.
- Regenerated both committed SVGs:
  `reports/figures/hex-lll-comparator-random-bounded.svg` and
  `reports/figures/hex-lll-comparator-harsh-cubic.svg`.
- Updated `reports/hex-lll-performance.md` so the comparator section records
  the five labelled series, the certified-vs-Isabelle-certified shared-rung
  verdicts, and the now-met native comparator largest-rung verdicts.
- Replaced the stale harsh-cubic adverse native-gate concern with the actual
  remaining limitation: the Isabelle-certified series has one committed point
  per family, enough for the five-way plot marker and smoke verdict but not a
  full certified-vs-certified trend.

## Current frontier

The DG4 deliverable is ready for PR. The plotter and report now consume all
committed DG2/DG3 evidence. The committed Isabelle-certified evidence is still
only one smoke row per family, so the report is explicit about the shared-rung
scope rather than claiming an unmeasured full ladder.

## Next step

Open the PR for #6645 with the four touched deliverable files plus this
progress note. Do not include the unrelated pre-existing `.claude/CLAUDE.md`
worktree modification.

## Blockers

None for this PR. Verification performed:

- `python3 -m py_compile scripts/plots/hex-lll-comparator.py`
- `python3 scripts/plots/hex-lll-comparator.py --family random-bounded`
- `python3 scripts/plots/hex-lll-comparator.py --family harsh-cubic`
- SVG legend grep confirms both regenerated figures contain all five labels:
  Lean native, verified Isabelle native LLL, Lean certified, verified Isabelle
  certified-LLL, and fpLLL via fplll-ffi.
- Stale report wording grep found no remaining four-curve or adverse-gate
  phrasing.
- `git diff --check`

`python3 scripts/check_phase4.py --all` was also tried and failed on existing
unrelated cost-model-comment findings across other bench files; this change
does not touch those files.
