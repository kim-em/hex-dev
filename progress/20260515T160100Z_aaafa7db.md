# Issue #4179

## Accomplished

- Claimed feature issue #4179.
- Added `HexBerlekampZassenhausMathlib.recombinationCandidate_eq_factor_of_recovery`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
- The proof uses `Monic core` to rewrite
  `scaledLiftedFactorProduct core d S` to `liftedFactorProduct d S`,
  consumes
  `polyProduct_liftedSubsetSelectedList_eq_liftedFactorProduct`, applies the
  existing scaled centered-lift recovery theorem, and finishes through
  primitive-part and sign normalization.
- Added only a private local helper for `DensePoly.scale (1 : Int)`.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The monic-core recombination equality is now available for downstream
  should-record and exact-quotient composition.
- Existing equality-based consumers did not need signature changes.
- Existing repository warnings remain unchanged, including pre-existing
  `sorry` warnings in BZ/Mathlib dependency files.

## Next step

- Use `recombinationCandidate_eq_factor_of_recovery` in the downstream
  first-success / coverage assembly chain once the remaining #4149 residual
  issues are ready.

## Blockers

- None for issue #4179.
