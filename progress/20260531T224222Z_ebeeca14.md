# HO-5d-1b cleanup B — explicit prime witness migration

Issue: #5866.
Session UUID prefix: ebeeca14.

## Accomplished

Restated `Hex.BerlekampZassenhaus.SmallModSingleton.squareFreeCore_irreducible_of_small_mod_singleton`
to take an explicit `primeData : Hex.PrimeChoiceData` plus
`hchoose : Hex.choosePrimeData? ... = some primeData`, and changed the
proof tail to pass that witness through directly rather than using the
total `Hex.choosePrimeData` self-substitution.

Updated `reports/scratch/FactorTrace.lean` so the trace report inspects
`Hex.choosePrimeData? f` once and prints either the selected prime or
`none`, avoiding the total `Hex.choosePrimeData` fallback path.

Removed the remaining stale textual `choosePrimeData` reference from
`HexPolyZ/Mignotte.lean`; the Mignotte lemma now describes its downstream
provenance generically as the selected-prime primality lemma.

Verification:

- `rg -n "\bHex\.choosePrimeData\b" HexBerlekampZassenhaus/SmallModSingleton.lean HexPolyZ/Mignotte.lean reports/scratch/FactorTrace.lean | rg -v "choosePrimeData\?|choosePrimeDataScore|choosePrimeDataResult"` returns no hits.
- `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib HexPolyZ` rebuilt the touched `HexPolyZ` and `HexBerlekampZassenhaus.SmallModSingleton` targets, then failed in the pre-existing `HexBerlekampZassenhausMathlib/Basic.lean` type mismatch described below.

## Current frontier

The issue-local migration is complete. I found no current caller of the
Mathlib-free `SmallModSingleton.squareFreeCore_irreducible_of_small_mod_singleton`
in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`; the Mathlib file
already uses the explicit-witness `_of_choosePrimeData_squareFreeModP`
theorem in the singleton branch.

## Next step

After the pre-existing Mathlib build break is repaired, rerun
`lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib HexPolyZ`
to get a clean end-to-end verification for this PR.

## Blockers

The requested combined build is blocked by the known pre-existing
`HexBerlekampZassenhausMathlib/Basic.lean` failures at lines 5099, 5994,
and 6280: `Hex.isGoodPrime_squareFreeModP` returns `Hex.squareFreeModP`,
but those sites still expect the stronger
`Hex.DensePoly.gcd ... = 1` shape. This is the same blocker recorded in
`progress/20260531T112000Z_bda15569.md`.
