# 2026-05-11 07:27 UTC — work/feature session (385d9436)

## Accomplished

- Claimed #3286 (`Fix PR #3276 Linux benchmark gate failure`).
- Diagnosed PR #3276's failed Linux `build` check as a cold-cache
  HexLLL Isabelle comparator setup issue, not a regression in the
  BZ l2-norm proof change.
- Reproduced the failure locally with a cold `HEX_ORACLE_CACHE`:
  `lake exe hexlll_bench verify` failed only
  `Hex.LLLBench.runIsabelleHarshCubicNormSq15`, matching CI.
- Updated `.github/workflows/ci.yml` to prewarm
  `HEX_LLL_ISABELLE_SVP` in the existing single ubuntu bench step
  before `hexlll_bench list` / `verify`.
- Added a cache-local stale-aware lock to
  `scripts/oracle/setup_lll_isabelle.sh` so concurrent callers cannot
  race while creating the shared archive/source/build cache.

## Current frontier

- Verified on the repair branch:
  - `lake exe hexlll_bench list`
  - `lake exe hexlll_bench verify` with `HEX_LLL_ISABELLE_SVP`
    prewarmed
  - cold-cache reproduction fails without prewarm and passes with
    prewarm
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- The only uncommitted non-session change is the pre-existing
  `.claude/CLAUDE.md` modification, left out of the repair.

## Next step

- Create the PR for #3286 and let CI rerun the benchmark smoke gate.
- Once the CI repair lands or is available to the pull-request merge
  ref, rerun PR #3276's Linux `build` check.

## Blockers

- None.
