# Progress: BHKS D1 step 2 — auxiliaryPolynomial l2-norm bound (#5204)

## Accomplished

Landed the three new declarations specified by #5204 in
`HexBerlekampZassenhausMathlib/BadVector.lean`:

- `BHKS.cldColumnNormBound input p : Nat`, defined as
  `Σ_{j<n} (Hex.bhksCoeffBound input j)^2` with
  `n := input.degree?.getD 0`.
- `BHKS.auxiliaryPolynomial_coeff_sq_le` — per-coefficient
  Cauchy–Schwarz bound on `(auxiliaryPolynomial input liftData vec).coeff j`.
- `BHKS.auxiliaryPolynomial_l2norm_sq_le` — the BHKS Lemma 3.2 squared
  `l2norm` bound on the transported Mathlib polynomial.

The hypothesis exposed to callers is the BHKS Lemma 5.1 column bound on
the executable `cldCoeffs` entries, uniformly over both the lifted-factor
index and the coefficient index:
```
h : ∀ (i : Nat), i < liftData.liftedFactors.size → ∀ (j : Nat),
    ((Hex.cldCoeffs input liftData.p liftData.k
        (liftData.liftedFactors.getD i 0)).getD j 0).natAbs ≤
      Hex.bhksCoeffBound input j
```
Callers discharge this from
`Hex.BHKS.abs_cldCoeffs_le_bhksCoeffBound` (#6220) once they have the
per-(factor, coefficient) exact integer representative plus the Hensel
precision separation.

The proof packages three private helpers:
`range_foldl_add_int_eq_sum`, `range_foldl_add_nat_pow_eq_sum`
(`List.range`-foldl to `Finset.sum` conversions), and
`auxiliaryPolynomial_coeff_eq` (the coefficient formula on the
zero-correction wrapper). The Cauchy–Schwarz step uses
`Finset.sum_mul_sq_le_sq_mul_sq`; the uniform column bound is then
`Σ_i c_{i,j}^2 ≤ r · B_j^2` combined with Cauchy–Schwarz to give the
`r · B_j^2` leading factor in the per-coefficient inequality and the
`r · cldColumnNormBound` factor in the squared-l2norm inequality.

Verification:
- `lake build HexBerlekampZassenhausMathlib.BadVector` clean.
- `lake build HexBerlekampZassenhausMathlib` clean.
- `python3 scripts/check_dag.py` exits 0.
- No new `sorry`, `axiom`, `native_decide`, `TODO`, `FIXME` in the
  added lines.

## Current frontier

`HexBerlekampZassenhausMathlib/BadVector.lean` now ends with
`auxiliaryPolynomial_l2norm_sq_le` placed adjacent to
`auxiliaryPolynomialWithCorrections` and the existing
`bhks_bad_vector_resultant_lower_bound`, which referenced
`auxiliaryPolynomial_l2norm_sq_le` in its docstring as a forward
promise; that promise is now fulfilled.

## Next step

Step 3 of the BHKS bad-vector theorem split
(`reports/bhks-bad-vector-theorem-shape.md §3`): chain
`bhks_bad_vector_resultant_lower_bound` (existing) and the new
`auxiliaryPolynomial_l2norm_sq_le` into the cut-radius separation
theorem
`bhks_bad_vector_norm_sq_gt_cutRadiusSq4_of_paperThreshold_le`. That
issue is blocked on this one plus #5201 (step 1).

## Blockers

None for this issue. The exposed hypothesis form is what step 3 will
need to instantiate against the as-landed column-bound lemma.
