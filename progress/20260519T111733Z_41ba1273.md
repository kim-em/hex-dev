# Session 41ba1273 - HexArith Phase 6 powMod identity wrappers (#5437)

## Accomplished

- Claimed issue #5437 (HexArith Phase 6: add powMod identity wrappers).
- Added three new `powMod` identity wrappers in `HexArith/Montgomery/Context.lean`:
  - `powMod_zero_base (n p) (hp : p > 0) : powMod 0 n p = 0 ^ n % p` (`@[simp]`)
  - `powMod_one_base (n p) (hp : p > 0) : powMod 1 n p = 1 % p` (`@[simp]`)
  - `powMod_succ (a n p) (hp : p > 0) : powMod a (n + 1) p = (a * powMod a n p) % p`
- All three proofs go through the existing `powMod_eq` characterisation, leaving
  `powMod`, `powModWordOdd`, `powModNat`, and the Montgomery execution path
  untouched.
- Verified:
  - `lake build HexArith.Montgomery.Context`
  - `lake build HexArith`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -- HexArith/Montgomery/Context.lean | rg -n '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'` prints nothing.

## Current frontier

`powMod` now exposes wrappers for the three small identity cases that
downstream finite-field / Rabin-style code needs (base = 0, base = 1, exponent
successor) without rewriting through `powMod_eq` manually. The Montgomery
implementation is unchanged.

## Next step

Commit, push, open the PR closing #5437.

## Blockers

None.
