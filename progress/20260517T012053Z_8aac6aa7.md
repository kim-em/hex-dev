# 20260517T012053Z — feature session 8aac6aa7 (#4547)

## Accomplished

Resolved #4547 (HO-1 substrate: `henselLiftData_liftedFactor_natDegree_pos`
discharge).

### `HexBerlekampZassenhausMathlib/Basic.lean` (+230 lines)

Added two theorems immediately after
`henselLiftData_liftedFactor_injective_of_choosePrimeData` (line 4132):

1. **`henselLiftData_liftedFactor_natDegree_pos`** — abstract-invariant
   base theorem. Takes the recursive `QuadraticMultifactorLiftInvariant`,
   per-factor monicness, mod-`p` product congruence, and a per-modular-
   factor natural-degree positivity premise (on the lifted-to-`ZPoly`
   form). Concludes `0 < (toPolynomial (liftedFactor d i)).natDegree` for
   every index `i`.

   **Proof path**:

   - Each lifted factor is monic (via
     `henselLiftData_liftedFactor_monic`).
   - Per-output mod-`p` congruence (via
     `multifactorLiftQuadratic_each_congr_mod_base`) identifies
     `lifted = liftedFactor d i` with `liftToZ factorsModP[i.val]`
     modulo `primeData.p`.
   - The mod-`p` reduction equality
     `modP p lifted = primeData.factorsModP[i.val]` follows from
     `Hex.ZPoly.modP_eq_of_congr` together with
     `Hex.FpPoly.modP_liftToZ`.
   - Both the lifted factor and `liftToZ factorsModP[i.val]` are monic,
     so their `Polynomial.map (Int.castRingHom (ZMod p))` images have
     the same `natDegree` as the unmapped polynomials (via
     `HexPolyZMathlib.natDegree_map_intCast_zmod_eq_of_leadingCoeff_ne_zero`).
   - The Mathlib bridge
     `toMathlibPolynomial_modP_eq_map_intCast_zmod` lines up both
     `natDegree`s as `natDegree (toMathlibPolynomial factorsModP[i.val])`.
   - The premise then discharges positivity.

2. **`henselLiftData_liftedFactor_natDegree_pos_of_choosePrimeData`** —
   convenience wrapper composing
   `Hex.ZPoly.QuadraticMultifactorLiftInvariant_of_choosePrimeData`
   with the base theorem above. Matches the parameter shape of the
   existing `_of_choosePrimeData` umbrellas (monicness, injectivity),
   adding the explicit `hfactors_natDegree_pos` premise. The premise
   is taken explicitly, mirroring how the injectivity umbrella takes
   `hfactorsModP_nodup` explicitly — discharging it from `choosePrimeData`
   invariants requires composing with `factorsModPBerlekampForm` and
   the Berlekamp factor-degree positivity, which lives in a separate
   substrate task.

### Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓ (replayed from cache)
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `grep -nE "sorry|axiom" HexBerlekampZassenhausMathlib/Basic.lean`
  shows only the two pre-existing sorries at lines 162 and 241
  (corresponding to the issue's referenced lines 160 and 239).
  No new axioms, no new `sorry`, no new `native_decide`, `TODO`,
  or `FIXME`.

## Premise shape note

The issue's illustrative signature suggested
`∀ g ∈ primeData.factorsModP, 0 < g.natDegree`, but `Hex.FpPoly`
(= `DensePoly (ZMod64 p)`) has no `natDegree` field — only `degree?`.
The natural Mathlib-bridge phrasing is to work on the `liftToZ`-then-
`toPolynomial` image, which is what the consumer needs directly. The
adopted shape:

```lean
∀ g ∈ primeData.factorsModP,
  0 < (HexPolyZMathlib.toPolynomial (Hex.FpPoly.liftToZ g)).natDegree
```

matches the issue's authorisation that the "concrete hypothesis list
may differ from the illustrative shape above; the constraint is that
the consumed hypotheses must all be dischargeable from `choosePrimeData`
/ Berlekamp invariants without introducing new substrate gaps."

## Current frontier

#4547 closes with the two theorems above. Combined with the existing
monicness and injectivity umbrellas (and #4543 still open for
`HenselSubsetCorrespondenceHypotheses` constructor), every
`liftedFactor`-shaped hypothesis on
`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
(line 7590) is now dischargeable from `choosePrimeData` /
`henselLiftData` invariants and per-modular-factor structural facts.

## Next step

The natural successor on the HO-1 chain is the sibling sub-issue #4543
(`HenselSubsetCorrespondenceHypotheses` constructor at outer bound)
for the exhaustive branch. After it lands, the remaining substrate
gap for the slow-path arm of #4170 is the `LiftedFactorSubsetPartition
Finset.univ core` constructor.

## Blockers

None.
