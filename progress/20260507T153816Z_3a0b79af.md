# 2026-05-07 15:38 UTC — feature session (3a0b79af)

## Accomplished

Claimed and completed #2539 — `basisRows`/`basisMatrix` row-add
invariance theorems for `HexGramSchmidt/Basic.lean`.

Added (all `private`):

- `basisRows_take_set_rowAdd` — for `j < k < rows.length`, the first
  `i` basis rows produced on `rows.set k (rows[k]! + c • rows[j]!)`
  agree with the first `i` basis rows on `rows`. Proof inducts on the
  prefix length: the inductive step rewrites `(basisRows _)[i]!` via
  `basisRows_get!_eq_reduceAgainstBasis_take` against the equal
  prefix from the inductive hypothesis. `i ≠ k` uses
  `List.getElem?_set_ne` to recover the original row;  `i = k` uses
  `reduceAgainstBasis_add_left` and `reduceAgainstBasis_smul_left`
  (linearity of source) plus the source-row reduction theorem
  `reduceAgainstBasis_basisRows_take_source_eq_zero` to discharge
  the `c • reduce(rows[j]!)` term.
- `basisRows_set_rowAdd` — closed-form list-level theorem requested
  by #2539 deliverable 1: `basisRows (rows.set k (rows[k]! + c •
  rows[j]!)) = basisRows rows`. Specialises the take lemma at
  `i = rows.length` and uses `List.take_of_length_le`.
- `basisMatrix_rowAdd` — matrix-level theorem requested by deliverable
  2: `basisMatrix (Matrix.rowAdd b src dst c) = basisMatrix b` for
  `src.val < dst.val`. Wraps `basisRows_set_rowAdd` through the
  `toList` representation, using `Vector.toList_set` and
  `Vector.getElem_toList` to bridge the dense matrix and list views.

## Branching

This branch was based on PR #2572 (issue #2550, source-row earlier
reduction zero theorem) since #2539's stated `depends-on: #2538` was
satisfied through the decomposition #2538 → #2550 (PR #2572 still
open). When #2572 merges, this PR's diff against main will narrow to
the three new theorems.

## Verification

- `lake build HexGramSchmidt.Basic` → clean (only pre-existing
  `basis_span` sorries on lines 1523 and 1575).
- `lake build HexGramSchmidt` → clean.
- `lake build` → clean (15 jobs).
- `python3 scripts/check_dag.py` → silent (clean).
- `git diff --check` → clean.
- `rg '^axiom\b|native_decide|\bTODO\b|\bFIXME\b' HexGramSchmidt/Basic.lean`
  → no matches.
- No new theorem-level `sorry` introduced.

## Current frontier

The private list- and matrix-level row-add invariance theorems are
now available in `HexGramSchmidt/Basic.lean`. They are the building
blocks for #2525 (public `GramSchmidt.Rat.basis` row-add theorem
surface).

## Next step

#2525 (HexGramSchmidt.Basic: expose rational row-add basis and
coefficient formulas) is unblocked once #2572 and this PR land.
#2525 needs a coefficient-matrix companion theorem; the linearity
helpers from #2572 plus `basisMatrix_rowAdd` should give it.

## Blockers

None for the implementation. Merge ordering depends on PR #2572
landing first (clean rebase expected).
