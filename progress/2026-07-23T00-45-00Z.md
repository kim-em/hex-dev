# factor_poly / irreducibility tactic family: roadmap complete and merged

Closes the arc opened in `progress/2026-07-22T18-31-36Z.md` (plan:
`~/.claude/plans/i-d-like-to-prepare-magical-sutherland.md`).

## Accomplished

All nine PRs of the roadmap merged to main, each after a Codex second
opinion, with `main` green at `ed3471eb` (full build, 9455 jobs):

- #8856 `DensePoly.beqCoeffs` (kernel-reducible polynomial equality; the
  derived `DecidableEq` now routes through `List`).
- #8857 decide-slot irreducibility entry points (`checkMonicCert`,
  `irreducible_of_modPCert`, `irreducible_of_linear`).
- #8858 `CertReify` split into the Mathlib-free layers (`Hex.CertReify`).
- #8859 `factor_poly` / `irreducibility` elaborators and tactics for
  `FpPoly p` + the `Hex.FactorTactic.Provider` name-probe hook. Review fix:
  non-monic Berlekamp gcd leaves are now `normalizeMonic`-normalized.
- #8860 `ZPoly` provider (`ZPoly.Factored`, `IrredWitness` covers). Review
  fix: budget guard on the `primeConst` witness (kernel `isNatPrime` is Θ(c)).
- #8861 `Polynomial (ZMod q)` provider (`Hex.FactoredPoly`, parser-with-proof
  over user syntax). Review fix: bounds/budget checks reordered before the
  Θ(q) primality trial.
- #8863 `Polynomial ℤ` provider, multi-prime certificates,
  `irreducible_cert` deleted (subsumed by goal-mode `irreducibility`), and
  the kernel-checked `irreducibility!` / `factor_poly!` decide fallbacks
  (the initial "structurally infeasible" diagnosis was wrong — Kim's
  `decide +kernel` correction held; the real obstacle was the per-file
  `import all` closure, and Swinnerton-Dyer kernel-checks in seconds).
- #8862 Eisenstein-after-shift certificates, Mathlib-free (translation
  algebra + coefficient-level Eisenstein criterion; `X⁴+1` now certified in
  the free layer). Review fix: prime cap 128 (kernel `isPrimeTrial` replay
  hits `maxRecDepth` just above 150).
- #8865 top-level SPEC sweep.

Coverage now: complete for `FpPoly` (within the replay budget); for integer
polynomials, prime-constant / linear / single-prime / Eisenstein-after-shift
in the free layer, multi-prime degree obstructions in the Mathlib layer, and
the kernel-decide bang forms (size ≤ 13) for everything else.

## Current frontier

Feature complete and merged. The stray
`progress/20260722T235316Z_factor-poly-spec-review.md` is the #8865 review
note, committed alongside this one.

## Next step

Nothing scheduled. Natural follow-ups if wanted: an opt-in provider dispatch
that surfaces mid-pipeline errors (noted in #8863's review), pretty
`X`-notation display for Mathlib-side factor lists, and richer certificate
languages for Swinnerton-Dyer-class inputs (free-layer multi-prime soundness
was estimated at 1-2 weeks in planning).

## Blockers

None.
