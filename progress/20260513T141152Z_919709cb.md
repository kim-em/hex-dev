# Session 919709cb — HexBZ profile coverage for declared input families

Closes #3514.

## Accomplished

- Recorded representative profiles for each `phase4.input_families` entry
  not already covered by #3506 / commit `06d996d`:
  - `public-factor-combinator`: `runFactorChecksum --param 5`.
  - `cld-fast-path`: `runFactorFastChecksum --param 5`.
  - `exhaustive-slow-backstop`: `runFactorSlowChecksum --param 4`.
  - `ho2-adversarial-recombination`: `runFactorAdvPhi15Checksum --param 0`
    (recorded but init-dominated; shape coverage redirected through the
    public-factor-combinator profile because the HO-2 singletons share
    the same `factor`/`factorFast` entry points).
- Added a `§Profile coverage table` to
  `reports/hex-berlekamp-zassenhaus-performance.md` mapping every
  declared family to a bench target, parameter, commit, sample weight,
  and dominant inclusive Hex.* attribution.
- Per `SPEC/profiling.md §Required output`, each new family entry
  records its `samply` command line, child-row metadata
  (`inner_repeats`, `per_call_nanos`, result hash), worker-thread
  sample weight, leaf-category split, and top inclusive Hex.* ranking.
- Updated `§Concerns` to record that profile coverage is now traceable
  for every declared family per
  `SPEC/profiling.md §Coverage requirement`, leaving verdict and
  comparator concerns for separate slices.

## Methodology note

The four new family entries categorise samples at worker-thread scope
including `_child` process startup and Lean module initialisation; the
earlier `degree-height-matrix` entry uses a tighter categorisation
scoped to allocator and GMP leaves. The two are not directly
comparable on category percentages but both satisfy the SPEC's
case-per-family shape-coverage requirement. The methodology
distinction is called out at the top of `§Profile` so a reader can
align the two views.

## Current frontier

- Phase 4 verdicts remain inconclusive on the retuned schedules;
  `§Concerns` still lists the BHKS-scaling and HO-3 gaps that
  `#3513` (now closed via PR #3516) deferred to the next planner.
- `libraries.yml` has no comparator metadata for
  `HexBerlekampZassenhaus`; final Phase 4 completion needs a
  comparator (informational or gating) declared and wired before
  `done_through` advances past `3`.

## Next step

The next worker-led slice that follows this is the Phase 4 final
report reconciliation (`#3527`, currently blocked on `#3514`), which
combines the profile coverage landed here with the verdict and
comparator concerns into a coherent completion package.

## Blockers

None. The profile artefacts are developer-local at
`/tmp/hex-profiles/hex-bz-{public,cld,slow,adv-phi15}-db43025.{json,syms.json}`
and are not committed per `SPEC/profiling.md`. The
`degree-height-matrix` artefact (`hex-bz-degree-height-child-06d996d`)
referenced by the existing subsection remains developer-local on the
host where #3506 was recorded.
