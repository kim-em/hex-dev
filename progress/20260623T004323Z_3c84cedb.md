# M1 (monicTarget-coordinate) recovery math — #8296

Session type: feature (one-shot `/once`). UTC 2026-06-23T00:43.

## Accomplished

Landed the additive, green-landable "new math" the BHKS core-coordinate swap
(#8295, parent #8293) consumes, all in `HexBerlekampZassenhausMathlib/Basic.lean`
(additive only, no executable-def change, no existing-proof reroute). Full
CI-gated `lake build HexBerlekampZassenhausMathlib` green (3784 jobs, 0 errors);
all four new theorems have clean axiom cones (`#print axioms` =
`[propext, Classical.choice, Quot.sound]`, no `sorryAx`/`ofReduceBool`).

Deliverable 1 — the `monicTarget` mod-bridge:
- `scale_congr_of_congr` — constant scaling preserves coefficientwise congruence
  mod `m`.
- `monicTarget_congr_scaleInv` — `monicTarget core p k ≡ core·ℓf⁻¹ (mod p^k)`
  (immediate from `monicTarget = reduceModPow (scale ℓf⁻¹ core) p k`).
- `leadingCoeff_scale_monicTarget_congr_core` — the headline bridge
  `ℓf·monicTarget core p k ≡ core (mod p^k)`, from the
  `leadingCoeffInverse_mul_emod` (#8294) unit residue via `Int.ModEq`.
- `scaledLiftedFactorProduct_congr_core_of_product_congr_monicTarget` — from the
  M1 lift congruence `∏ liftedFactors ≡ monicTarget core p k (mod p^a)`, derive
  `scaledLiftedFactorProduct core d S ≡ core (mod p^a)` (the exact hypothesis the
  existing Mignotte recovery consumes, delivered in `core`'s own coordinate).

Deliverable 2 — core-coordinate recovery capstone:
- `centeredLift_scaledLiftedFactorProduct_eq_core_of_product_congr_monicTarget` —
  threads the bridge into the existing
  `centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision` (factor :=
  core) to prove the executable centred lift of the `ℓf`-scaled lifted product
  recovers `core` exactly, with **no** `dilate`.

Deliverable 3 — standalone M1 recovery witness (the `RecoveredAtLift` analogue):
- `RecoveredAtLiftM1` structure (fields `monicFactor`, `congr`, `recovered_eq`,
  `monic_dvd : monicFactor ∣ monicTarget core p k`).
- `RecoveredAtLiftM1.candidate_eq` — the generic recovery formula
  `primitivePart (centeredLiftPoly ((ℓf·∏ S) % p^k)) = factor`, transporting the
  witness `congr` through `ℓf`-scaling. Stated standalone, so it lands green
  without rerouting any M2 (`RecoveredAtLift`) consumer.

## Current frontier

The reusable M1 congruence/recovery math now exists and is green. Nothing
consumes it yet — `factorFastCoreWithBound` still routes through
`toMonicLiftData`/M2, and the recovery/floor are still `(toMonic core).monic`-keyed
(per #8295's scope, that swap is the atomic remodel this PR de-risks).

## Next step

The remaining piece of the parent swap (#8295) is the **per-subset → proper
factor** correspondence: deriving the M1 lift congruence
`∏_{i∈S} liftedFactor ≡ (monic coordinate factor of S) (mod p^a)` from a
successful `coreLiftData` recovery, and showing the recovered primitive factor
*divides core* and *equals core's corresponding irreducible factor*. That is the
structural recovery-direction remodel (partition/correspondence + the zero-period
bad-vector substrate; see hex-lean-mathlib-boundary §"recovery direction" /
§"period trap"), not additively landable — it must land atomically with the
executable reroute. This PR supplies the congruence substrate that remodel
consumes (the `RecoveredAtLiftM1` witness + its `candidate_eq`, and the
`scaledLiftedFactorProduct ≡ core` bridge).

## Blockers

None for the delivered scope. The per-subset factor correspondence is the parent
swap's own remodel (#8295), gated on soundness substrate, not this additive PR.
