# 20260518T171342Z — session 3150c28b — review #5039

## Accomplished

- Claimed and executed audit issue #5039 (forward-reference and
  alternative-idiom scan of the post-#4991 Monic-route cluster).
- Located the seven cluster helpers in `HexBerlekampZassenhausMathlib/Basic.lean`
  (lines 139, 6720, 6734, 6742, 6749, 6754, 8142) and confirmed
  `IntReductionMod.lean` imports `Basic.lean`, so no above-helper sites
  exist outside `Basic.lean`.
- Forward-reference scan: walked every `Hex.DensePoly.Monic` hypothesis
  introduced above each helper line, cross-checked against inline
  derivations of the five cluster shapes (`0 < ?.size`, `? ≠ 0`,
  `0 < lc ?`, `content ? = 1`, `normalizeFactorSign ? = ?`). Confirmed
  `Basic.lean:5338` (in flight as #5038) is the only above-helper
  inline-discharge site. Surfaced one non-deviation: `Basic.lean:5597`
  already calls `zpoly_primitive_of_monic` as a forward reference (this
  landed in #5022 and demonstrates Lean accepts theorem-body forward
  references within a single `namespace` block).
- Alternative-idiom scan: ran the four greps from the issue body
  verbatim; classified all 8 + 11 + 0 + 23 = 42 hits. Zero deviations
  beyond the known #5038 case. The `Nat.pos_of_ne_zero` and
  `Nat.eq_zero_or_pos.size` hits are all on `Hex.FpPoly`,
  `Hex.DensePoly Rat`, or `degree?.getD 0` (not stored size), all of
  which are outside cluster scope. The `leadingCoeff = (1 : Int)` hits
  are either inside the helper bodies themselves, definitional unfolds
  of `Monic` consumed by `rw [hlead]` (which `zpoly_lc_pos_of_monic`
  cannot replace, since it gives `0 < lc`, not `lc = 1`), Mathlib's
  `Polynomial.leadingCoeff`, or `lc * lc = 1` product shapes.
- Posted the audit deliverable as a single comment on #5039
  (https://github.com/kim-em/hex/issues/5039#issuecomment-4480047849)
  with the verbatim greps, observed-vs-expected counts, per-cluster
  forward-reference table, and an explicit "no follow-up issues to
  draft" conclusion.
- Closed #5039 per the issue body's instruction ("if the audit finds
  no deviations beyond the known line-5338 case, say so explicitly and
  close the issue").

## Current frontier

- The Monic-route cluster's call-site coverage is now exhaustive under
  the four wider grep patterns. The only remaining hoist + rewire is
  #5038 (`Basic.lean:5338`).
- Aside from #5038, the cluster track lineage (#4991, #5001, #5009,
  #5013, #5022, #5026, #5028, #5029, #5033, #5036, #5037) is complete
  with respect to the three invariants from review #5031.

## Next step

- Land #5038 (line-5338 hoist + rewire). After that, the post-#4991
  Monic-route cluster is closed.
- Adjacent cluster audits (e.g. on
  `monic_primitive_sign_normalized_of_monic`,
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`, or
  cross-cluster framing) are explicitly out of scope here.

## Blockers

None. Read-only audit; source tree unchanged; `python3 scripts/check_dag.py`
exits 0; `git diff --check` clean.
