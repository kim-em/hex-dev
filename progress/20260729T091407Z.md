# Interval dynamic-scope follow-up

## Accomplished

- Closed generation laundering through a proposed equality endpoint which
  CSE-resolves to an older node, and added an exact generation-two canary.
- Rechecked the package-owned arbitrary-scope predicate at registry dispatch,
  including a raw-engine/reordered-port rejection canary.
- Charged both ordered reads and writes in application policy-surface counts,
  then refreshed the exact policy-frontier work totals.
- Rebuilt the full 22-job affected experiment/conformance set successfully.

## Current frontier

The dynamic-scope transition now carries stable application identity,
scope/equality causality, package validation, and resource-first semantic-key
accounting. Replay integration must consume the event's binding and
application deltas when this stack meets the semantic-replay branch.

## Next step

Run the complete affected conformance set, push the review fixes, and update
the stacked PR description with the equality-CSE and dispatch invariants.

## Blockers

None.
