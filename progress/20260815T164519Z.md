# Table 10 row-25 shard restack

## Accomplished

- Replayed the coordinate-pinned Table 10 shard onto exact main
  `4b5fd357bf59ac6cd61dfbd7b6f8fd3e29d4ef5c`, preserving the complete BKLNW
  migration and current public interval registrations.
- Rechecked the pinned row-25 tuple at `BKLNW_tables.lean:797`, the five exact
  margin declarations, the historical false-target comment, the row-values
  membership theorem, and the full Table 10 dispatcher shape.
- Added `sourceTable` and `row25OfMem`, which preserve the source tuple
  membership and `k ∈ Finset.Icc 1 5` hypotheses for the stronger checked
  numeric majorant used by the localized rewrite. The surrounding
  `B_8_exact` reduction remains explicitly outside this shard.
- Retained the coordinate-205 false-target diagnostic, no-draft/no-retry
  behavior, independent kernel falsity proof, bounded five-fact replay, and
  guarded axiom reports.
- Reconciled all Lake roots, conformance registration, exact freshness
  exemption, partial inventory evidence, and current SPEC text.
- Rebuilt the focused shard and retained BKLNW/Table 12/FKS2 targets and reran
  the pinned-source inventory, unit, static, DAG, phase, trust, Mathlib-free,
  diff, and banned-token checks successfully.

## Current frontier

The shard covers five of 87 Table 10 numeric target sites after a localized
PNT+ rewrite. The other 82 target sites, all 38 supporting `a₂` sites, the
surrounding `B_8_exact` reductions, arbitrary rows, and production batching
remain pending.

## Next step

Keep this clean local commit unpushed until the full FKS2 shard publication
sequence determines when the Table 10 acceptance probe should land.

## Blockers

None.
