# Issue #8867: BZ hybrid/lattice seam re-measure

## Accomplished

- Claimed #8867 via `gh` after `coordination` was unavailable on PATH.
- Rebuilt `hex_lattice_spike` and re-measured `hybrid` and `core` on the
  Swinnerton-Dyer ladder after #8854-era changes.
- Updated `bench/HexBench/LatticeSpike.lean` so `hybrid` prints the dispatcher
  trace fields needed for seam analysis: prime, lifted-factor count `r`, and
  subset candidates.
- Recorded the measured seam in `reports/bz-classical-spike-findings.md` and
  `reports/hex-berlekamp-zassenhaus-performance.md`: SD5 is still classical
  (`r = 16`), SD6 is the first measured lattice-decline rung (`r = 32`), and
  `levelAwareSubsetBudget` remains unchanged.
- Updated the BZ SPEC and `libraries.yml` comparator goal by regime: parity /
  small constant on classical small-to-medium `r`, strictly beat the verified
  classical Isabelle BZ reference on the lattice-backed large-`r` tail.
- Filed #8868 for the scheduled-hardware SD5/SD6 Isabelle comparator seam
  evidence that is not wired by this issue.
- Asked Claude Opus for a second opinion. It correctly flagged the inferred
  `r` values and SD5/SD6 regime wording; those concerns were addressed.

## Current frontier

The implementation is documentation/report/bench-spike only; no factorization
semantics changed. The current measured seam is between SD5 and SD6, not at SD4.

## Next step

Open the PR for #8867, check mergeability and CI, then enable squash auto-merge
without deleting the branch once checks are green.

## Blockers

None for #8867. Follow-up #8868 tracks scheduled-hardware comparator evidence
for the SD5/SD6 seam.
