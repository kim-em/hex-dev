# #8328 monicTarget factor bridge

## Accomplished

Added two algebraic M1 bridge wrappers in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `factorCongr_of_product_congr_monicTarget`, specializing the existing global
  `scaledLiftedFactorProduct_congr_core_of_product_congr_monicTarget` with
  `core := factor` to turn
  `liftedFactorProduct d S ≡ monicTarget factor d.p d.k` into the exact
  scale-coordinate correspondence consumed by `honestCongr_of_correspondence`.
- `honestCongr_of_product_congr_monicTarget`, composing that correspondence
  with the leading-coefficient split
  `leadingCoeff core = leadingCoeff factor * cofactorLc` to produce the
  per-support honest M1 congruence
  `scale (leadingCoeff core) (liftedFactorProduct d S) ≡ scale cofactorLc factor`.

Verified with `lake build HexBerlekampZassenhausMathlib`.  The build completed
successfully with pre-existing project warnings.  The diff adds no `sorry`,
`axiom`, or `native_decide`.

## Current frontier

The #8327 `monicTarget` subset congruence now has a direct factor-level bridge
into the honest scale-coordinate shape expected by downstream M1 recovery code.

## Next step

Use `honestCongr_of_product_congr_monicTarget` at the true-support recovery call
site that currently needs to assemble `hhonest` from the selected
`monicTarget factor` congruence and the leading-coefficient factorisation.

## Blockers

No code blockers.  The worktree still contains a pre-existing dirty
`.claude/CLAUDE.md`, which is off-limits and was not touched.

`/reflect` was requested by the session template but is not available in this
environment (`zsh: no such file or directory: /reflect`).
