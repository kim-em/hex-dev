# 2026-05-13T22-16-03Z — feature session on #3810

## Accomplished

- Claimed and executed #3810: added `auxAdjugateM` Laplace-collapse
  preliminaries to `HexMatrix/Determinant.lean`.
- Added two helper lemmas usable elsewhere:
  - `foldl_det_sum_finRange_succ_factor_skipIndex` — additive companion
    of the existing multiplicative `foldl_finRange_succ_factor_skipIndex`.
  - `det_eq_zero_of_row_eq_zero`, `det_eq_zero_of_col_eq_zero`, plus a
    size-agnostic primed variant `det_eq_zero_of_col_eq_zero'` for use
    on `Matrix R n n` when `n` is not known to be positive.
- Added the four preliminary families called out by #3810:
  - First-row Laplace 2-survivor collapse
    (`det_auxAdjugateM_eq_endpoint_collapse`).
  - Endpoint entry rewrites
    (`auxAdjugateM_apply_zero_zero_eq_det`,
    `auxAdjugateM_apply_zero_last_eq_cofactor`).
  - Minor-shape lemmas for both corners
    (`deleteRowCol_auxAdjugateM_zero_zero_apply_not_last`,
    `deleteRowCol_auxAdjugateM_zero_zero_apply_last_last`,
    `deleteRowCol_auxAdjugateM_zero_last_apply_ne_zero`,
    `deleteRowCol_auxAdjugateM_zero_last_apply_last_zero`).
  - Determinant collapses for both surviving cofactor minors
    (`det_deleteRowCol_auxAdjugateM_zero_zero`,
    `det_deleteRowCol_auxAdjugateM_zero_last`).
- Two commits, both pass `lake build HexMatrix.Determinant` and
  `lake build HexMatrix`. `git diff --check` is clean and no new
  banned markers (`sorry`/`axiom`/`native_decide`/TODO/FIXME) were
  introduced.

## Current frontier

The `det_auxAdjugateM` Desnanot-Jacobi capstone (the immediate next
step in #3800) reduces to combining the four families above with
`cofactor_of_even` / `cofactorSign_last_eq_pow` arithmetic.

## Next step

Open the PR for #3810 and exit. The capstone is a separate issue
(linked to #3800) and not in scope for this session.

## Blockers

None for this issue. A handful of dependent-type rewriting nuisances
in the Hex Mathlib-free Vector indexing showed up during the proof
and are noted in the commit messages; future work in this area
should prefer `simp only [eq]` over `rw [eq]` when the rewritten
expression appears inside a `getElem` with a dependent bound proof.
