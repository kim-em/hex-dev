# 2026-05-09 (UTC) — issue #2734 (HexGfqRing Phase-4 evidence)

## Accomplished

- Ran `lake exe hexgfqring_bench list && lake exe hexgfqring_bench verify`
  at commit `28f9f6c95543` on `carica`; both succeeded for all seven
  registered benchmarks.
- Ran the scientific `lake exe hexgfqring_bench run` over all seven
  parametric registrations and exported
  `reports/bench-results/hex-gfq-ring-28f9f6c95543.json`. All seven
  verdicts came back **consistent with declared complexity** at
  `|β| ≤ 0.064`.
- Recorded developer-local samply profiles via the `--profiler "samply
  record --save-only --unstable-presymbolicate"` form of
  `hexgfqring_bench profile` for the two `phase4.input_families`
  entries (`dense-reduction` via `runReduceModChecksum`,
  `quotient-arithmetic` via `runMulChecksum`), each at the top-of-
  ladder parameter `n = 256`.
- Symbolicated the resulting Firefox Profiler JSONs against samply's
  `.syms.json` sidecar with a small Python helper, classified leaf
  costs across {own, GMP, allocation, Lean runtime, other}, and
  produced inclusive-cost rankings restricted to
  `Hex.GFqRing.*`/`Hex.DensePoly.*`/`Hex.ZMod64.*` symbols.
- Rewrote `reports/hex-gfq-ring-performance.md` with the five
  mandated subsections: Bench targets, Verdicts, Comparator ratios
  (none declared in SPEC), Profile (one case per input family with
  leaf-cost categorisation, inclusive-cost ranking, and dominant-
  cost narrative attributing every dominant inclusive cost to a
  registered bench target), Concerns (empty).
- Bumped `libraries.yml: HexGfqRing.done_through` from `3` to `4`.
  `python3 scripts/check_dag.py` and `python3 scripts/check_phase4.py`
  both pass.

## Current frontier

- `HexGfqRing` is back at `done_through: 4` with traceable
  scientific verdicts and profile coverage for both input
  families. `§Concerns` is empty. The Attribution Rule is satisfied:
  every dominant inclusive cost in both profiled cases maps to a
  registered bench target via `GFqRing.reduceMod` or `GFqRing.mul`.

## Next step

- Other audit-rollback follow-ups remain open in the queue
  (`HexHensel` #2736, `HexBerlekamp` #2739, `HexConway` #2741); they
  follow the same workflow but operate on different bench surfaces.

## Blockers

- None.
