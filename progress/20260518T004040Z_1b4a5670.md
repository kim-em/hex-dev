# Session 1b4a5670 — BZMathlib abstract-bound siblings of A2 mignotte-precision recovery theorems

Issue: #4873.

## Accomplished

Added three abstract-bound `_of_bound` siblings in
`HexBerlekampZassenhausMathlib/Basic.lean` alongside their existing
core-shape counterparts, and rewrote each existing theorem as a thin
wrapper that delegates into the new variant with
`B' := Hex.ZPoly.defaultFactorCoeffBound core` and
`hvalid := defaultFactorCoeffBound_valid core hcore_ne factor hdvd`:

1. `centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision_of_bound`
   — primitive variant, body is one application of
   `Hex.centeredLiftPoly_eq_of_reduceModPow_eq` with the abstract `B'`.
2. `existsUnique_recoveringLiftedFactorSubset_of_henselSubsetCorrespondence_of_bound`
   — same shape as the original Group-A2 packaging theorem; body now
   calls the `_of_bound` recovery variant instead of the core-shape one.
3. `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`
   — wraps (1) with the executable-centred-lift normalisation
   (`centeredLiftPoly_reduceModPow_eq`).

The three original theorems keep their signatures and statements; their
bodies are now single delegations into the corresponding `_of_bound`.
The refactor-anchor docstring on
`centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision` is
replaced with a wrapper-relationship docstring (the anchor predicted
exactly this refactor and has been realised at the bottom three
theorems of the chain).

No downstream signatures changed. No new `sorry`, `axiom`,
`native_decide`, `TODO`, or `FIXME`.

## Current frontier

The abstract-bound abstraction now exists at the lowest three theorems
of the A2 recovery chain. Issue #4873 explicitly defers propagation up
through `recombinationCandidate_eq_factor_of_recovery_of_monic_core`,
`scaledRecombinationCandidate_eq_factor_of_recovery`,
`exhaustiveCoreFactorsWithBound_coverage_*`, and the umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` to a
separate follow-up issue.

## Next step

A follow-up issue can propagate the `_of_bound` abstraction up through
the remaining A2 chain so HO-1 callers can instantiate
`B' := defaultFactorCoeffBound f` directly, dropping the Gap 3 shim on
the umbrella signature.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — clean.
- `lake build HexBerlekampZassenhausMathlib` — clean.
- `lake build HexBerlekampZassenhaus` — clean.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git diff origin/main HexBerlekampZassenhausMathlib/Basic.lean |
  grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no matches.
