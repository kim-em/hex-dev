# 2026-06-02T02:20Z — feature session, issue #6044

## Accomplished

Added `det_plucker_three_term_nDet_of_between_p1_p2` to
`HexMatrix/Determinant.lean` as a direct specialization of
`nDet_plucker_four_row_canonical` (landed via PR #6202) at
`(r0, r1, r2, r3) = (p1, q, p2, p3)`. The deliverable's
multiplication order differs from the canonical statement only by
swapping the two off-diagonal product pairs; `grind` discharges the
swap.

## Current frontier

Pluker raw `nDet` q-between specialization is in place. The
sibling q-before sign-bookkeeping wrapper
`twoColDet_plucker_basisVec_of_lt_p1` (#6101) remains unclaimed and
can land independently against the same canonical theorem.

## Next step

A worker picks up #6101 or any downstream consumer (#6094, #6030)
that wires the q-between specialization into the universal
three-term Plucker assembly.

## Blockers

None.
