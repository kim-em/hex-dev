# Progress - 2026-06-16 (session d47f8d1f, review #7645)

## Accomplished

Claimed #7645 and reviewed the BHKS CLD norm-bound / cut-radius reduction
chain in `HexBerlekampZassenhausMathlib/Lattice.lean` and
`HexLLLMathlib/Basic.lean`.

Posted the verdict on #7645:
`https://github.com/kim-em/hex/issues/7645#issuecomment-4724535287`.

Conclusion: the reviewed chain is sound. The older reducers
`TrueFactorCLDNormBound`, `cldTailSq_le_of_colBound`, and
`normBound_of_colBound` are intentionally loose and prove only
`normSq <= bhksCutRadiusSq4`; the current tree also has the tight
`TrueFactorCLDTightNormBound` route, and the prefix survivor-span theorem
consumes the tight `4 * normSq <= bhksCutRadiusSq4` hypothesis rather than the
loose certificate.

Verified canonicity of the coordinate package: `TrueFactorCLDVectorData` fixes
the projected block to `indicatorVector S` and the coefficient block to the
`L.cldRows` column sums of the concrete `trueFactorCLDVector`; membership in the
BHKS lattice is structural via the row-combination definition, not an arbitrary
vector certificate.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Lattice` succeeded.
- `python3 scripts/check_dag.py` succeeded.
- Forbidden-token scan over `HexBerlekampZassenhausMathlib/Lattice.lean` and
  `HexLLLMathlib/Basic.lean` found no `sorry`, `axiom`, `native_decide`,
  `TODO`, or `FIXME`.

## Current frontier

No defect was found in #7645's reviewed surface, and no follow-up issue is
needed from this review. Downstream workers should continue through the tight
`TrueFactorCLDTightNormBound` / `mem_prefixSubmodule_of_normSq_le` route when
assembling prefix-cut projection facts.

## Next step

Close #7645 via the review PR/progress-only publish path.

## Blockers

None for #7645. The worktree still has a pre-existing unrelated
`.claude/CLAUDE.md` modification; this session did not touch or stage it.
