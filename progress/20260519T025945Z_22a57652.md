# Add HexGfqField repr_div and repr_zpow simp wrappers — issue #5219

Session: 22a57652-5c8a-4017-85cd-7925bf7afb8f, type review worker.

## Accomplished

- `HexGfqField/Operations.lean`: added three new `@[simp]` theorems
  immediately after `repr_pow` (post-#5210 line 791):
  - `repr_div` — `rfl`-proved, mirrors the `rfl` shape of `repr_pow`
    and consumes the `toQuotient_div` wrapper at line 654.
  - `repr_zpow_ofNat` — `rfl`-proved, consumes
    `toQuotient_zpow_ofNat` at line 661.
  - `repr_zpow_negSucc` — `rfl`-proved, consumes
    `toQuotient_zpow_negSucc` at line 668.
- All three closed cleanly by `rfl` because `repr x` unfolds to
  `GFqRing.repr x.toQuotient` literally (see `HexGfqField/Basic.lean:53`)
  and each `toQuotient_*` wrapper is itself `rfl`, so the composition
  reduces in one step. The `congrArg GFqRing.repr (...)` idiom used by
  `repr_inv_of_ne_zero` is only needed because `toQuotient_inv_of_ne_zero`
  has a side condition `hx : x ≠ zero`; the new wrappers are
  unconditional and don't need it.
- Kept the `repr_zpow_*` pair form rather than collapsing into a unified
  `repr_zpow (i : Int)` form, matching the `toQuotient_zpow_*` precedent
  exactly. No `match`-on-`Int` body was attempted; the unified form was
  noted as unsuccessful in #5205.
- No changes to any `def` or instance. No new imports.
- `lake build HexGfqField.Operations` 87/87 green;
  `lake build` 244/244 green; `python3 scripts/check_dag.py` exit 0;
  `git diff --check` clean; no `sorry` / `axiom` / `native_decide` /
  `TODO` / `FIXME` introduced. Net diff +21 lines in one file (well
  under the 30-line ceiling).

## Current frontier

The `repr_*` family in `HexGfqField/Operations.lean` now covers every
`toQuotient_*` wrapper that exists post-#5210: `add`, `mul`, `neg`, `sub`,
`inv_of_ne_zero`, `pow`, `div`, `zpow_ofNat`, `zpow_negSucc`,
`intCast`, `nsmul`, `zsmul`. The `congrArg GFqRing.repr` composition
idiom remains in use for the single conditional case (`repr_inv_of_ne_zero`).

## Next step

Hand off to the auto-merge queue.

## Blockers

None.
