# Planner session 0521e72e — queue refill (#5090 + #5091)

## Accomplished

- Oriented; queue state at start: `POD_MIN_QUEUE=3 POD_QUEUE_DEPTH=1
  POD_QUEUE_DEFICIT=2`. Open PRs: #5089 (mergeable, CI in progress
  during orient — merged before session end), #5086 (CONFLICTS,
  repair-claimed), #4917 (CONFLICTS, repair-claimed), #2656 (draft).
  No mergeable PR available for the merge step at the time it ran.
- Verified both broken PRs already in repair-claimed state — no fix
  issues required from this planner cycle.
- Reviewed existing `agent-plan` issues to confirm non-overlap.
  Distinguished the planned `fpPoly_mul_dvd_mul` extraction (Shape:
  `a ∣ b → c ∣ d → a*c ∣ b*d`) from the closed `fpPoly_dvd_trans`
  (transitivity), the open `fpPoly_isZero_false_of_dvd_of_isZero_false`
  (Shape B, `a ∣ b ∧ b.isZero = false → a.isZero = false`), and the
  open `fpPoly_leadingCoeff_ne_zero_of_size_pos` (no `Dvd` shape).
- Filed two atomic issues (target deficit = 2):
  - **#5090** (review, depends-on: #5084): post-#5084 audit of
    `monic_primitive_sign_normalized_of_monic` cluster inversion.
    Three-invariant template — call-site coverage / framing-docstring-
    hoist / cross-file consistency — with explicit checks for the
    13 corollary call sites, the umbrella's zero-non-corollary-caller
    property, and the post-#5084 first-class helper bodies.
  - **#5091** (feature): extract `private theorem fpPoly_mul_dvd_mul`
    in `HexBerlekampZassenhausMathlib/Basic.lean` and rewire the
    single 21-line calc chain at `Basic.lean:5657-5677`
    (`hrawGcd_sq_dvd_prod` inside
    `quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared`).
    Site explicitly flagged by the #5082 audit comment as out-of-
    scope for the `fpPoly_dvd_trans` cluster; this planner cycle
    promotes that observation to a tracked work item.
- #5090's depends-on header is detected by `coordination add-dep`;
  #5084 closed during the session, so the blocked label is not
  applied — #5090 is unblocked and claimable immediately, with the
  audit target tree already on `origin/main`.

## Current frontier

- 3 unclaimed work items post-session: #4334 (HexLLL Phase 4,
  host-load-gated, pre-existing), #5090 (new review, audit target on
  master), #5091 (new feature, single-site `fpPoly_mul_dvd_mul`
  extraction).
- 1 claimed work item: #5088 (`fpPoly_leadingCoeff_ne_zero_of_size_pos`,
  ~20 min in).
- 2 open PRs in CONFLICTS, both repair-claimed: #5086 (closes
  #5081, ~45 min) and #4917 (closes #4902, ~18 hours).
- Critical-path posture unchanged: directives #2564 / #2567 / #2637
  remain blocked; #4880 / #4825 / #4818 / #4170 / #4172 / #4680 /
  #4819 / #4821 / #4830 / #4831 / #4832 all remain blocked through
  one of the three.

## Next step

- Workers can pick up #5090 (audit target on master) and #5091
  (independent feature) in parallel. Both are single-concern,
  small-scope issues — #5090 is a comment-only audit, #5091 is a
  ~−11-net-line collapse.
- Dispatcher: next planner cycle will likely be triggered once
  workers drain enough of #5088 / #5090 / #5091 to push the
  unclaimed count back below `POD_MIN_QUEUE=3`.
- The audit pattern's natural follow-up — a post-#5091 audit issue
  for the `fpPoly_mul_dvd_mul` extraction — is **not** filed by this
  cycle. The #5091 body explicitly defers it to a later planner
  cycle (the helper is single-site; a closure audit can be filed
  after the worker's PR lands and the line numbers stabilise).

## Blockers

- None for this session. Project-level blockers (directives #2564 /
  #2567 / #2637) are unchanged; cascade work continues to advance at
  the BZMathlib (Mathlib-bridge) layer only.
