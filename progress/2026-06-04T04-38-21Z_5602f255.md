# Pod session 5602f255 — directive #6520 diagnostic STOP

## Accomplished

- Claimed and audited directive #6520 (Mathlib-free Schur singular cascade
  `getArrayEntry_scaledCoeffRowsSchur_eq_zero_of_singularStep_lt`).
- Posted a detailed STOP comment with concrete evidence, then released the
  directive via `coordination skip` so a fresh worker can pick up the
  re-decomposed version.

The directive premise is incorrect for `s ≥ 1`. The "diagonal cascade ⇒
`rows[s][s] = 0` via `exactDiv n 0 = 0`" sketch in the issue body does not
match the actual algebraic shape of `schurScaledCoeffEntry rows gram s s`:
the formula is `rows[s-1][s-1] * gram[s][s] - schurSigma rows s s`, the
σ-chain denominators are `rows[p-1][p-1]` for `p-1 ∈ [0, s-2]` (all
non-singular prefix, hence nonzero by `noPivotLoop_initial_gram_diag_ne_zero_of_lt`),
and `rows[s-1][s-1]` itself never appears as a denominator in that fold.
No `exactDiv` collapses, and the diagonal write does not reduce to `0` by
that route. The off-diagonal cascade at `j = s+1` has the same shape:
the σ-fold over `p ∈ [1, s]` is at non-singular denominators, and
collapsing it needs the Gram structural fact `M_s[i][s] = 0`, which in
the current Mathlib-free layer is `leadingPrefix_gram_zero_pivot_column_zero_of_singular_step`
(line 4273) — and that lemma takes `hquot : BareissGramInitialRegularStepQuotientProvider b`
as an explicit argument, while the requested signature has no `hquot`.

## Current frontier

Issue #6520 is unclaimed again with a STOP comment.  The umbrella
#6458 still needs:

* either (a) an updated #6520 with `hquot` in the signature, then a
  one-line bridge wrapper that builds `hquot` from Mathlib (matching
  the pattern used for every other consumer of the abstraction, e.g.
  `leadingPrefix_gram_bareiss_toNat_eq_zero_of_noPivot_singular`);
* or (b) a separate prior directive to assemble the non-singular
  Schur≡Bareiss correspondence as an `hquot`-parametrised lemma (uses
  `schurSigma_foldl_eq_noPivotCorrection_zero` and
  `schurSigma_noPivotCorrection_succ` as base and step) and then the
  singular cascade as a Mathlib-free consumer of it;
* or (c) moving `scaledCoeffs_lower_eq_zero_of_singularStep_lt` entirely
  to the bridge layer via the public
  `scaledCoeffs_diag_eq_zero_or_eq_leadingPrefix_bareiss`, accepting
  that this violates the no-proof-debt SPEC preference but does not
  require new Mathlib-free infrastructure.

## Next step

A planner picks up the STOP comment, chooses one of the
re-decompositions above (or proposes a fourth), and files an updated
directive.  No code on this branch.

## Blockers

None for the pod overall.  This session is one-shot dispatch and exits
on completion.
