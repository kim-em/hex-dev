## Accomplished

Claimed #3019 after all visible human-oversight directives rejected as
blocked by dependencies.

Confirmed #3018 had already replaced the normalization product chain with the
rational-associate invariant in `HexBerlekampZassenhaus/Basic.lean`, then
tightened `normalizedConstantFactors_product` so its proof explicitly splits
the `squareFreeCore = 1` and non-`1` branches while reusing
`normalizeForFactor_reassembles`.

Verified:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- the normalization theorem-chain region has no `axiom`, `native_decide`,
  `TODO`, `FIXME`, or `sorry`

## Current frontier

The #3019 normalization reassembly theorem chain is axiom-free under the
current rational-associate invariant. Existing `sorry`s in
`HexBerlekampZassenhaus/Basic.lean` remain outside this normalization slice.

## Next step

Use the rational-associate normalization invariant in the later bounded
factorization product bridge, or add a separate exact-content/sign bridge for
the final `Factorization.product` theorem.

## Blockers

None for #3019. Human-oversight issues #2564, #2565, #2566, #2567, and #2637
were blocked when this session attempted to claim them.
