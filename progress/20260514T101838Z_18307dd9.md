# 2026-05-14 10:18 UTC — work session 18307dd9 (issue #4003)

## Accomplished

- Claimed and worked #4003, the Mathlib-free factor-output membership
  classifier prerequisite for the Berlekamp-Zassenhaus irreducibility chain.
- In `HexBerlekampZassenhaus/Basic.lean`, exposed theorem-level wrappers for:
  - splitting `reassemblePolynomialFactors` membership into normalization
    prefix versus core-factor membership;
  - tracing `collectFactorMultiplicities` entries back to a sign-normalized
    raw factor;
  - tracing `factorizationOfFactors` entries back to the raw factor array;
  - classifying the slow-path raw factor array into constant,
    quadratic-root, or exhaustive branches;
  - tracing `factorWithBound` entries to either the fast raw array or the
    slow fallback raw array, with the collection/sign-normalization step
    included.

## Current frontier

#4003 is implemented on the Mathlib-free side. The remaining per-branch
irreducibility issues (#4004-#4007) can use the new wrappers to move from a
recorded `(ZPoly × Nat)` entry to the raw branch factors before proving the
branch-specific mathematical irreducibility facts.

## Next step

Create the PR for #4003. After it lands, continue with #4004 or another
blocked successor in the per-branch irreducibility chain.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhaus.Basic` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean`
  reports only the pre-existing `isIrreducible_iff` placeholder at line 4489.
