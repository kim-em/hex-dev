# #3518: HexMatrix Laplace transpose bridge partial

Session: `8304bffa`
Branch: `agent/8304bffa`

## Accomplished

- Claimed #3518 and implemented the reusable transpose bridge layer in
  `HexMatrix/Determinant.lean`:
  - `deleteRowCol_transpose`
  - `cofactor_transpose`
  - `det_eq_foldl_laplace_last_row`
- Verified `lake build HexMatrix.Determinant`.
- Verified `python3 scripts/check_dag.py`.
- Verified `git diff --check`.
- Verified the banned-marker grep for `HexMatrix`; only the pre-existing
  `HexMatrix/Bareiss.lean:1120` `sorry` remains.
- Decomposed the remaining #3518 scope into:
  - #3743 for arbitrary fixed-column Laplace expansion.
  - #3744 for arbitrary fixed-row Laplace expansion, depending on #3743.

## Current frontier

The arbitrary fixed-column theorem remains the hard residual. The key
remaining work is sign bookkeeping for transporting an arbitrary selected
column to `Fin.last n` while matching the deleted minor/cofactor signs.

## Next step

Work #3743 in `HexMatrix/Determinant.lean`, reusing
`det_eq_foldl_laplace_last`, `det_colPermute_vector`, and the new transpose
minor/cofactor bridges where helpful. Once #3743 lands, #3744 should be a
small transpose corollary.

## Blockers

None. The residual scope was split because the arbitrary-column transport is
large enough to deserve its own focused session.
