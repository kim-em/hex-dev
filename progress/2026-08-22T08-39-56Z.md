# 2026-08-22 — double-resultant evaluation eliminant (#9423)

## Accomplished

- Replaced `QAdjoin.Roots.evalRoot?`'s exact lazy-Horner disambiguation
  chain with a shared per-component double-resultant evaluation eliminant
  `evalEliminant f e = dilate D (Res_y(p, Res_z(e, S - G(y,z))))` with
  `G = clearedOuter f` (`HexNumberField/Roots.lean`; new `candidateLift`,
  `evalShifted`, `evalEliminant`; `componentRoots?` rewired; `evalRoot?`
  and `coeffRoot?` deleted). A compiled `#guard` pins `S^2(S^2-8)` for
  `X - sqrt2` over `Q(sqrt2)`.
- Rebuilt the companion surface (`HexNumberFieldMathlib/Roots.lean`):
  `evalEliminant_isRoot` / `evalEliminant_ne_zero` plus supporting
  specialization lemmas (`candidateLift_map`, `evalShifted_map_eval`,
  hom-generic `resultant_hom_eq_zero`, proof-local
  CommRing/IsDomain on `DensePoly ZPoly`), restated
  `retainZero?_correct`, repaired `componentRoots?_isSome/_sound/
  _complete` (`ComponentRoots.lean`), added `size_pos_of_core_degree`
  so the public statements keep their hypotheses. Zero sorries; all 23
  axiom guards pass (`evalRoot?_sound` guard replaced by
  `retainZero?_correct`).
- Root-ladder timings (chungus2): `runQAdjoinRootsLadder` rung 2
  >20 min (killed) -> 128 ms; rungs 1-6 now 3.9 ms / 128 ms / 1.19 s /
  7.9 s / 105.7 s. `runAlgebraicRootsLadder` rungs 2-6: 50 ms / 149 ms /
  0.71 s / 6.2 s. Raised the QAdjoin ladder `maxSecondsPerCall` to
  180 s so the shipped `[1,2,3,4,6]` schedule completes.
- Full battery green: `lake build HexNumberField HexNumberFieldMathlib
  HexNumberFieldTowerMathlib hexnumberfield_bench HexConformance`,
  `check_dag.py`, `check_phase4.py`, `git diff --check`;
  `hexnumberfield_bench verify` 24/30 ok (the 6 failures are the PARI
  pairs needing `cypari2`, unavailable on this host).

## Current frontier

Branch `wave/roots-refinement-fix` (on top of `wave/number-field-phase4`)
holds three commits: executable, companion, bench cap. Not pushed.

## Next step

Open the PR against `wave/number-field-phase4` (or merge into it) and
re-run the Phase-4 feasibility probe for the two root APIs; both ladders
now report finite per-call times at every shipped rung, though the
QAdjoin ladder's fitted exponent still reads ~n^0.45 above the declared
`n^5 log^2 n` across rungs 2-6 (C spread ~1.9, verdict "inconclusive"),
so the Phase-4 evidence turn should decide whether more rungs or a model
correction is warranted.

## Blockers

None.
