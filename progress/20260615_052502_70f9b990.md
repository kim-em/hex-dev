# Progress — 2026-06-15 (session 70f9b990, feature)

## Accomplished

Issue #7453: added `toMonicLiftData_represents_lifted_monicCorrespondent`
to `HexBerlekampZassenhausMathlib/Basic.lean` (after
`henselLiftData_represents_lifted_of_modP`, ~line 9382).

The theorem instantiates `henselLiftData_represents_lifted_of_modP` at
`M := (Hex.ZPoly.toMonic core).monic` and Hensel precision
`Hex.precisionForCoeffBound B primeData.p`, recording the conclusion against
`Hex.ZPoly.toMonicLiftData core B primeData` (definitionally
`henselLiftData M (precisionForCoeffBound B primeData.p) primeData`).

The analytic inputs are discharged exactly as in the existing
`*_of_monicPrimeData` discharge cluster (~lines 8164-8368): the
`toMonicPrimeData?` extractors (`_prime`, `_isGoodPrime`,
`_factorsModP_berlekamp_form`) plus the
`factorsModP_*_of_factorsModPBerlekampForm` family and
`QuadraticMultifactorLiftInvariant_of_choosePrimeData`. Monicness of `M`
comes from `toMonic_monic_isMonic_of_pos_degree` (needs `hcore_lc_pos`,
`hcore_pos`); the `_root_.Nat.Prime` form is `natPrime_of_hexNatPrime`;
`hfactors_irr` is `factors_irreducible_of_factorsModPBerlekampForm`.

`hB_ne_zero` is genuinely required for `1 ≤ precisionForCoeffBound B p`:
`precisionForCoeffBound_spec` only gives `2 * B < p ^ precision`, so with
`B = 0` precision `0` is admissible. No new positivity lemma was needed —
the four-line `by_contra`/`omega` derivation already used at lines
19849/20004 suffices.

This stops at the monic coordinate (no irreducibility of the correspondent,
no final existential for the original `factor`), matching the issue scope.
The recovery of the non-monic `factor` is #7452's transfer.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`: green, 0 errors.
- `lake build HexBerlekampZassenhaus`: green (497 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- Added lines: no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`. The only
  `sorry` warning in the module (Basic.lean:233) is pre-existing, far above
  the insertion point.

## Current frontier

`toMonicLiftData_represents_lifted_monicCorrespondent` is the first
instantiation of `henselLiftData_represents_lifted_of_modP` (previously
landed but unused). It supplies the monic-coordinate half of the
`hlifted_of_modP` shape consumed by the `henselSubsetLiftHypotheses_*`
constructors.

## Next step

Compose this monic-coordinate representation with #7452's
`RecoveredAtLift` transfer to produce the original-`factor`
`hlifted_of_modP` input the descent constructors at lines ~19453/19503
expect. That assembly is out of scope here per the issue.

## Blockers

None.
