# 2026-05-19T22:23 UTC — UInt64 wide arithmetic API review

## Accomplished

- Claimed and executed #5561.
- Reviewed `HexArith/UInt64/Wide.lean` against the Phase 6 API-polish criteria and its Barrett/Montgomery consumers.
- Added `@[grind =]` automation annotations to the existing stable characterising lemmas for `mulHi`, `addCarry`, and `subBorrow` without changing executable definitions or extern shims.
- Documented the private quotient-bound helper used by `toNat_mulHi`.

## Current frontier

- `HexArith/UInt64/Wide.lean` remains the public Layer 1 surface for wide-word arithmetic, with existing simp normal forms preserved for downstream proofs.

## Next step

- Continue Phase 6 review on adjacent HexArith API surfaces if more queue items are filed.

## Blockers

- None. Verified with `lake build HexArith.UInt64.Wide`, `lake build HexArith`, `python3 scripts/check_dag.py`, and `git diff --check`.
