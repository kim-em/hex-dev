# c9112c2d — HexLLL Phase 4 scientific verdicts (#4334)

## Accomplished

- Claimed feature issue #4334 (the only unclaimed issue).
- Verified the bench harness is healthy on current `main` (commit `8f1024d2`):
  - `lake exe hexlll_bench list` enumerates the five affected registrations.
  - `lake exe hexlll_bench verify` reports `all 52 benchmark(s) passed`.
- Ran a bounded scientific probe for
  `Hex.LLLBench.runSizeReduceChecksum` (one of the five inconclusive
  registrations) with `--max-seconds-per-call 0.5` and the declared
  `paramFloor=128`. The probe confirmed the host is still unsuitable for
  Phase 4 scientific verdict evidence:
  - per-spawn floor (harness self-measurement): **1.338 s**;
  - zero verdict-eligible rows: the only ladder row (`param=128`) was
    killed at the wallclock cap;
  - host load averages during the session ranged from **20.78 / 17.74 /
    16.78** at claim time through **23.35 / 18.16 / 17.04** mid-probe
    down to **20.40 / 18.79 / 17.39** after the probe — comparable to
    the load that forced the previous worker (session `30d1c30d`) to
    skip with the same symptoms (`22.05/17.03/16.61`, 1.325 s floor).
- Released #4334 as `replan` / blocked-by-environment.

## Current frontier

- Issue #4334 still requires quiet or dedicated benchmarking hardware,
  or the release-candidate workflow on dedicated hardware per
  `SPEC/benchmarking.md` §"Scientific timing runs". No source change to
  HexLLL is indicated by the probe; the symptom is pure measurement
  contention.
- No code changes were made in this session. The only worktree-dirty
  entries are the pre-existing `.claude/CLAUDE.md` modification and the
  untracked `.claude/commands/` and `.claude/skills/` from the worktree
  context; none touched.

## Next step

- Re-run #4334 only on quiet/dedicated hardware, or when `carica` is
  idle enough for a stable low spawn floor (e.g. `<` 0.2 s).
- Once a quiet run is available, run all five affected parametric
  HexLLL registrations into a clean `reports/bench-results/` artifact
  and update `reports/hex-lll-performance.md` / `libraries.yml` only
  if the Phase 4 gates are actually met.

## Blockers

- Current host contention prevents usable scientific benchmark verdicts
  for #4334. Identical pattern to the documented prior skips
  (`7d894bdb`, `30d1c30d`, and earlier).
