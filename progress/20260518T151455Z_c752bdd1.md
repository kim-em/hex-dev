# Session c752bdd1 — feature #5017

## Accomplished

- Claimed and closed #5017 ("BZMathlib: rewire inline arg-position
  `.2.1` projection at Basic.lean:5597 to `zpoly_primitive_of_monic`").
- Single-line edit in `HexBerlekampZassenhausMathlib/Basic.lean:5597`:
  the argument-position projection
  `(monic_primitive_sign_normalized_of_monic hcore_monic).2.1` is
  replaced by `(zpoly_primitive_of_monic hcore_monic)` inside the body
  of `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`.
  Pre-state grep counts (1 / 4) match post-state (0 / 5):
  - `git grep -c "(monic_primitive_sign_normalized_of_monic hcore_monic).2.1"`
    drops from 1 to 0 across Basic.lean + IntReductionMod.lean.
  - `git grep -c "zpoly_primitive_of_monic"` rises from 4 to 5
    (Basic.lean: 2→3 = 1 def + 1 existing rewire + 1 new rewire here;
    IntReductionMod.lean unchanged at 2 existing rewires).
- Issue body asserted "no parentheses needed" for the new form; this is
  wrong in argument position (without parens Lean would parse
  `zpoly_primitive_of_monic hcore_monic` as two separate arguments to
  the outer call). Kept the surrounding parens — same +1/−1 diff
  envelope, deliberately matching the existing arg-line shape.
- Build baseline preserved after a `git pull --ff-only` past the
  freshly-landed #5020: `lake build HexBerlekampZassenhausMathlib.Basic`
  emits the same 16 file-level errors as `origin/main` a2a4e714 (11
  whnf timeouts at 4863, 5144, 5470, 5557, 5604, 5988, 6136, 6308, 6289,
  15423, 15534 + 1 `cases` failure at 5866 + 4 kernel-unknown constants
  at 6623, 6687, 15741, 15787). Post-#5020 the 5 `unused variable
  hcore_ne` warnings are gone (underscore-prefixed by #5015→#5020);
  the two pre-existing `sorry` warnings at 15213 / 15308 remain.
- `python3 scripts/check_dag.py` exit 0; `git diff --check` clean; no
  `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` added.
- `git diff --stat origin/main -- HexBerlekampZassenhausMathlib/Basic.lean`
  reports `1 file changed, 1 insertion(+), 1 deletion(-)`, exactly the
  predicted envelope from the issue body.

## Current frontier

PR for #5017 pending push + `coordination create-pr`.

With this rewire, the Monic-implies-{`size_pos`, `ne_zero`, `lc_pos`,
`primitive`} helper-cluster sweep at
`HexBerlekampZassenhausMathlib/Basic.lean:6707–6749` is now at 100%
call-site coverage in both `Basic.lean` and `IntReductionMod.lean`.

## Next step

The post-#4991 helper rollout is exhausted as far as #5017's parent
issue body is aware. The next planner pass will need to identify the
next harmonisation target (or close out the cluster). No named
follow-up is visible in the queue at this UTC.

## Blockers

None for this PR. HexLLL Phase-4 verdict re-run (#4334) remains
blocked on a quiet host — `uptime` reported 15.16 / 13.40 / 19.80 at
session start.
