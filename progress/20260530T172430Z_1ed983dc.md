# Progress — 2026-05-30T17:24Z — session 1ed983dc (#5773)

## Accomplished

Worked on directive #5773 (HO-5b): "rewrite
hex-berlekamp-zassenhaus-performance.md against hex/isabelle ratio
verdicts". The prior session (8a84e793) had wired the six per-rung
`runIsabellePrecisionLocalRung{1..6}Checksum` registrations as the
ninth comparator pairing but left the ladder table `pending` on every
rung — the comparator sweep was blocked on building the `bz_isabelle`
wrapper and finding a quiet-load window on `carica`.

This session closed both blockers:

- Discovered that the `Berlekamp_Zassenhaus` AFP session heap was
  already built and cached on this `carica` instance under the
  Isabelle component path `/tmp/isabelle-afp` (registered via
  `isabelle components -l`). The first-run cost of the setup script
  (AFP download + session-heap build, previously estimated at 30
  minutes to a few hours) was therefore a no-op.
- Ran `scripts/oracle/setup_bz_isabelle.sh` to generate the Haskell
  extraction (`Hex_BZ.hs` via `Hex_BZ_Export` session) and compile
  the GHC driver binary against
  `scripts/oracle/bz-isabelle/Main.hs`. Total wallclock ≈ 2 minutes;
  binary at
  `.cache/oracles/bz-isabelle/wrapper/bz_isabelle`.
- Ran the precision-local comparator sweep against that wrapper:

  ```sh
  HEX_BZ_ISABELLE="$PWD/.cache/oracles/bz-isabelle/wrapper/bz_isabelle" \
  lake exe hexbz_bench run \
      Hex.BerlekampZassenhausBench.runFastPathPrecisionLocalChecksum \
      Hex.BerlekampZassenhausBench.runIsabellePrecisionLocalRung1Checksum \
      ... (rungs 2-6) ... \
      --outer-trials 3 \
      --export-file reports/bench-results/hex-berlekamp-zassenhaus-ea9b1d0b-precision-local.json
  ```

  Load averages `5.29/5.31/5.09` at sweep start (within the
  envelope of the other ladders in the report). Total sweep
  wallclock ≈ 2 minutes (six Isabelle medians × 3 trials × ~840 ms
  + Lean side and persistent-subprocess startup).
- Ran a same-window baseline refresh
  (`runIsabelleFactorBaselineChecksum = 7.500 ms`).
- Updated §"Precision-local asymmetric ratio ladder" with sweep
  command, export artefact paths + SHA-256s, the six-rung paired
  table, and trend + tripwire-verdict paragraphs. All six rungs
  pass the tripwire (adjusted ratio ≤ 0.0064); the largest rung
  `(d=8, h=32, k=128, r=8)` records Lean fast-path setup at
  `5.278 ms` vs Isabelle full factor at `829.838 ms` (adjusted),
  ratio `0.006361`.
- Updated §Concerns: dropped the "wired but not yet measured"
  bullet and the matching "ninth surface" parenthetical in the
  comparator-coverage summary; replaced with post-sweep
  tripwire-pass framing pointing back at the new ladder section.

Verification:

- `lake exe hexbz_bench verify` passes all 18 smoke targets.
- `lake build HexBerlekampZassenhaus.Bench` succeeds (164 jobs);
  only pre-existing `sorry` warning on `HexBerlekampZassenhaus.Basic`.
- Both new bench-results JSONs land under `reports/bench-results/`
  with SHAs cross-referenced from the report body.
- `git diff --check` clean.

## Current frontier

Every registered scientific bench-target group in
`HexBerlekampZassenhaus/Bench.lean` now has a paired ratio
verdict (gating or tripwire) against the verified-Isabelle BZ
comparator:

- split family (public, fast, slow) — gating: **met**
- degree/height matrix (public, fast, slow) — gating: **met**
- HO-2 adversarial singletons — gating: **met**
- fallback-probe family — gating: **not met** (architectural cascade,
  HO-1 + HO-5d scope)
- precision/local-factor family — tripwire: **passing** at every rung

The directive's verification criterion ("Each registered scientific
bench target has at least one ratio verdict, even if 'not met'") is
now satisfied across the surface.

## Next step

#5773 itself looks dischargeable as a full close from this session:
the headline rewrite is in, all nine comparator pairings have
measured ratio verdicts, and §Concerns accurately reflects the
post-rewrite state with the cascade-trigger fallback-probe not-met
verdict surfaced to HO-1 / HO-5d. Creating the PR with the standard
`coordination create-pr` (no `--partial`) is the right move.

The fallback-probe not-met verdict and the precision-local
tripwire-vs-gating asymmetry remain documented concerns that are
out of scope for HO-5b — they belong to HO-1 (#2564, architectural
rewrite) and the precision-local Lean target's design choice (the
fast-path-setup-only registration is deliberate to fit the `verify`
budget, not a bug).

## Blockers

None for this PR. The bench wrapper, the sweep, and the report
update all closed in this session. CI does not exercise the
verified-Isabelle comparator (the registrations are tagged
`scheduled-hardware`), so the post-sweep numbers do not affect the
gating CI surface; only the report content changes here.
