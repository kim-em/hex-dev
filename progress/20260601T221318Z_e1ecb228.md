# e1ecb228 - work #6132

## Accomplished

- Claimed and executed feature issue #6132.
- Made the four `rref` projection lemmas public in `HexMatrix/RREF.lean`:
  `Matrix.rref_transform_mul`, `Matrix.rref_rank_le_n`,
  `Matrix.rref_rank_le_m`, and `Matrix.rref_pivotCols_sorted`.
- Added concise wrapper-level docstrings for each projection theorem.
- Verified with `lake build HexMatrix`, `lake build`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token
  grep over the touched Lean diff.

## Current frontier

#6132 is complete. The RREF API now exposes direct public projection facts for
the computed transform equation, rank bounds, and pivot-column sortedness,
without requiring callers to destructure `rref_isRREF`.

## Next step

Create the PR for #6132. The remaining Phase 6 RREF polish items are separate
issues, including the `Matrix.spanCoeffs_sound` wrapper from #6131.

## Blockers

None.
