# Progress: /work triage — BZ-recovery carrier-swap migration family is coupled

`/work` meta session. Claimed and investigated three issues; landed no code but
produced evidence-based diagnoses that redirect the BZ-recovery migration work.
All three were released back to the queue with concrete findings.

## Accomplished

### #6819 (lifted partition consumers) — diagnosed, blocked-then-superseded

Established that `HexBerlekampZassenhausMathlib.Basic` (not CI-built) was hard-red
on main with consumer-migration breakage from the #6938 carrier swap
(`RepresentsIntegerFactorAtLift := Nonempty (RecoveredAtLift …)`). During the
session #6968 (mul wrapper) and #6969 (early recovery → monic coordinates, closed
#6963) both merged, reducing the error set. The residual 12 sites all share one
root cause: consumers pass `hrep : RepresentsIntegerFactorAtLift` where the old
*definitional* unfolding `(scaledLiftedFactorProduct core d S).reduceModPow … =
factor.reduceModPow …` is expected (these were defeq before the swap; now the
carrier is `dilate`-based and `monicFactor`-mediated). #6819 is the stale broad
parent; #6940 already decomposed it into #6963/#6964/#6965. Released (replan) with
a full site map.

### #6964 (monic-core direct callers) — premise mismatch, coupled with #6965

The 5 middle sites (`9462, 9473, 9572, 9879, 10371`) live in 4 widely-consumed
theorems (`natDegree_toPolynomial_eq_sum_of_represents_of_bound` 9441,
`representsIntegerFactorAtLift_monic_of_bound` 9544,
`representsIntegerFactorAtLift_primitive_of_bound` 9826,
`…_of_primitive_pos_lc_core_of_bound` 10346). The recovered-candidate helper
`RecoveredAtLift.candidate_eq_of_bound` requires a coefficient bound **on the
carrier witness `monicFactor`**, not on `factor`. Key findings:

- **Not soundly derivable.** `Nonempty (RecoveredAtLift …)` does not guarantee a
  *bounded* witness; an arbitrary congruent `monicFactor` may exceed `p^k/2`, so
  `recombinationCandidate d S = factor` is not provable from the new carrier +
  `factor`'s bound alone. The old scaled-equality predicate implicitly carried
  the bounded-witness info.
- **Large out-of-scope blast radius.** Unlike #6969's early-package theorems
  (2895/2924, which had *no* downstream callers and so could freely gain the
  witness-bound hypothesis), these four are consumed at `11327, 11509, 13583` and
  the coverage block `16047/16062/16429/16443`.
- **#6964 and #6965 are not independent.** #6965's coverage callers (16047-16443)
  call exactly this family and pass `hS_cov_rep : RepresentsIntegerFactorAtLift`
  into them. The witness-bound signature change must thread through #6965's
  surface in the same change.

Recommended (in the issue comment) re-scoping into one coordinated family
migration with an explicit witness-`monicFactor` bound. Released (replan).

### #6959 (Yun repeated-tail reachability) — sound, released for budget

`HexPolyFp.SquareFree` baseline is green (0 sorry/axiom). The premise is sound and
existing machinery is well-aligned (`squareFreeContributionReachable` already
encodes the size-one ⟹ =1 consequence at 6485; size-one helpers at 2414-2465).
But deliverable 1 — replacing the `YunDerivativeActiveRawStateProvider` assumption
with a *proven* reachability invariant (induction over the executable
`yunFactorsContributionWithLevel` recursion) then threading removal through a
5-theorem chain — is a focused full-session task in a CI-gated no-sorry file.
Released (replan) with "re-queue as-is; do not re-decompose."

## Current frontier

The BZ-recovery carrier-swap migration (#6938/#6951 landed the swap + closure;
#6963/#6968/#6969 landed the early/mul packages) leaves
`HexBerlekampZassenhausMathlib.Basic` red at 12 consumer sites. These split into
#6964 (5 middle, `9462-10371`), #6965 (6 late, `14319-16540`), and one producer
(`9124`, currently outside all three issues' stated scope).

## Next step

A planner should merge #6964 + #6965 (and likely fold in the `9124` producer and
the 11xxx/13xxx callers) into a single coordinated issue: migrate the
`represents` / `natDegree_toPolynomial_eq_sum` / `recombinationCandidate_eq_factor_of_recovery_*`
family to the recovered carrier, introducing an explicit witness-`monicFactor`
coefficient bound threaded from the call sites that own the analytic Hensel data
(mirroring how #6969 pushed that obligation upward for the leaf theorems).
#6959 and #6971 remain available as independent, full-session work items.

## Blockers

None that need a human. The release reasons are recorded on each issue.
