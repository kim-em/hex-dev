# d6760bb3 - work #6052

## Accomplished

- Claimed #6052 after #6064 raced and was already claimed by another agent.
- Added the Mathlib-free bridge `deleteRowCol_twoColMatrix_last_eq_mMatrix` in `HexMatrix/Determinant.lean`, identifying the final-column Laplace minor of `[B | u | v]` with `mMatrix B u p`.
- Exposed `twoColDet_eq_sum_mDet`, basis-vector right-column evaluation, ordered/reverse/equal basis-pair evaluations, and a bilinear basis-pair expansion for `twoColDet`.
- Verified with `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token grep over the touched Lean diff.

## Current frontier

The two-appended-column determinant substrate now reduces arbitrary appended columns to basis-pair terms, and each ordered basis-pair term reduces to signed `nDet` minors. This should give #6041 and #6044 the determinant-local expansion they need for the q-before and q-between raw Plucker kernels.

## Next step

Use the new `twoColDet_eq_sum_basisVec_pairs` plus `twoColDet_basisVec_basisVec_of_lt` / `_of_gt` / `_of_eq` in #6041 and #6044 to assemble the ordered raw `nDet` Plucker identities.

## Blockers

- No technical blocker for the completed slice.
- The pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and is not part of this work.
