# 20260609T145139Z (#c2af9f84)

## Accomplished

Claimed #6661 first because it was the oldest unblocked feature issue, then
audited its premise against the current source. The required recursive Yun
scalar synchronization theorem from #6660 was not present; #6660 had closed as
partial/replan and #6680 explicitly carried the remaining recursive theorem.
Marked #6661 replan with that blocker.

Claimed #6680 and landed a compiling partial substrate in
`HexPolyFp/SquareFree.lean`:

- `pow_C_mul_sync`, synchronizing powers of `C a * f` against powers of `f`.
- `C_mul_eq_one_inv_form`, extracting the inverse constant when `C a * f = 1`.
- `C_mul_C_mul_cancel_right`, cancelling the scalar power that appears in a
  mismatch branch.
- `yunFactorsContributionWithLevel_scalar_branch_sync`, the one-step
  contribution synchronizer covering all four `isOne z` /
  `isOne zScaled` quotient-branch combinations.

Filed successor issue #6688 for the remaining recursive induction over
`yunFactorsContributionWithLevel`.

Committed the partial milestone as:

- `2be3bb4e feat: add Yun scalar branch sync substrate`

## Current frontier

#6680 is partially complete. The quotient-branch scalar algebra is isolated and
compiled, but the recursive theorem requested by #6680 remains to be proved.
The induction still needs to combine:

- `yunFactorsContributionWithLevel_scalar_step_bridge`
- `yunFactorsContributionWithLevel_constant_scalar_sync`
- `yunFactorsContributionWithLevel_scalar_branch_sync`

Top-level `isOne c` / `isOne (C u_c * c)` mismatch branches are the remaining
case split, plus an explicit choice about whether the final theorem is fully
parametric or carries reachable/nonzero hypotheses to avoid the
`DensePoly.gcd c w = 0` corner.

## Next step

Work #6688 after this partial PR lands. Once #6688 supplies the recursive
scalar sync theorem, replan #6661 against the new theorem and compose it with
the normalized-tail reconstruction/product bridge.

## Blockers

No build blockers for the committed partial substrate.

Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`: no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
