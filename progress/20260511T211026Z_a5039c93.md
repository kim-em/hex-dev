# Work session a5039c93 — issue #3626

## Accomplished

- Claimed issue #3626 for the `HexPolyZMathlib` Phase 4 benchmark harness.
- Added `HexPolyZMathlib/Bench.lean` with lean-bench registrations for dense conversion, reverse conversion, equivalence round trips, and the Mignotte-bound bridge checksum.
- Added `hexpolyzmathlib_bench` to `lakefile.lean`.
- Added `HexPolyZMathlib` Phase 4 input-family metadata to `libraries.yml`.
- Extended the existing single ubuntu CI bench-smoke script with `hexpolyzmathlib_bench list` and `verify`.
- Verified `lake build HexPolyZMathlib.Bench`, `lake build HexPolyZMathlib`, `lake exe hexpolyzmathlib_bench list`, `lake exe hexpolyzmathlib_bench verify`, `python3 scripts/check_phase4.py`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexPolyZMathlib`, `git diff --check`, and the targeted banned-marker scan for the new bench file.

## Current frontier

- The benchmark harness slice for `HexPolyZMathlib` is implemented and smoke-verified.
- `HexPolyZMathlib.done_through` remains `3`; the headline report and full Phase 4 completion are out of scope for this issue.

## Next step

- Create the PR for issue #3626 and let CI run the new smoke target.

## Blockers

- None.
