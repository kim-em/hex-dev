# HexPolyZMathlib Phase 6 grind coverage (#8090)

## Accomplished

Promoted all 9 issue-listed equation-shaped public `@[simp]` lemmas in
`HexPolyZMathlib/RobinsonForm.lean` and `HexPolyZMathlib/Mignotte.lean` to
`@[simp, grind =]`.

Confirmed each promoted lemma is a bare equality conclusion, including the
conditional `robinsonFactor` equations:
`schurRootPath_zero`, `schurRootPath_one`, `robinsonFactor_of_norm_le`,
`robinsonFactor_of_one_lt_norm`, `robinsonForm_zero`, `coeff_map_intCast`,
`natDegree_map_intCast`, `support_map_intCast`, and `norm_coeff_map_intCast`.

## Current frontier

`lake build HexPolyZMathlib` is green. Target module timings were
`HexPolyZMathlib.Mignotte` 3.8s and `HexPolyZMathlib.RobinsonForm` 4.1s.

Downstream `lake build HexBerlekampZassenhausMathlib` is green, including
`HexBerlekampZassenhausMathlib.CLDColumnBound`, which directly imports both
touched modules. Existing warnings in downstream files remain unrelated.

`python3 scripts/check_dag.py` exited 0. `git diff --check` is clean. No new
`sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` was added.

## Next step

None for this issue.

## Blockers

None.
