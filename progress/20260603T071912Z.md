# Rechecked revised Schur/Bareiss plan

## Accomplished

Reviewed the revised D1-D4 plan against the current `HexGramSchmidt/Int.lean`
and HexMatrix determinant/Bareiss infrastructure. Confirmed the new plan fixes
the earlier singular-case overreach by deferring singular semantics, and that
the cited diagonal and lower Bareiss bridge lemmas are present.

No source or proof changes were made.

## Current frontier

The main remaining precision issue is D2's placement and statement. The repo
has strong `Matrix.borderedMinor`/`noPivotLoop` synchronization in
`HexGramSchmidt/Int.lean`, and Desnanot-Jacobi bridge theorems in
`HexMatrixMathlib`; a Mathlib-free D2 must avoid depending on those bridge
imports or explicitly use only the Mathlib-free exact-division/bordered-minor
surface plus local determinant expansion lemmas.

## Next step

Before dispatch, tighten the D2 directive to name the exact Lean theorem shape:
state it over `Fin n` with `j.val < i.val`, require
`(Matrix.noPivotLoop j.val (Matrix.noPivotInitialState (Matrix.gramMatrix b))).singularStep = none`,
and pin whether the target is the final `schurScaledCoeffEntry` determinant
or an intermediate `schurSigma` loop invariant.

## Blockers

No environment blockers. The only plan blocker is avoiding ambiguity about
whether D2 is Mathlib-free or Mathlib-bridge-assisted.
