# HexHensel Phase 6 — one-polynomial bridge normalizers

Session: `5620babb` (work, issue #5599)

## Accomplished

Moved the `modP_one` simp theorem from `HexHensel/Linear.lean` into
`HexHensel/Basic.lean`, so bridge users can normalize `ZPoly.modP p
(1 : ZPoly)` without importing linear Hensel lifting.

Added `ZPoly.reduceModPow_one_of_nontrivial`, with the required
`1 < p ^ k` hypothesis to avoid the false modulus `0` and `1` cases.

Added the short `[simp]` idempotence wrapper
`ZPoly.reduceModPow_reduceModPow` under `ZMod64.Bounds p`, routing through
the existing positive-modulus `ZPoly.reduceModPow_idempotent`.

Committed the code change as:

- `9d4aa7e6 feat: expose Hensel bridge one normalizers`

## Current frontier

`HexHensel.Basic` now owns the zero and one bridge normalizers for
`modP`/`reduceModPow`, plus the positive-modulus canonical-reduction
idempotence wrapper. `HexHensel.Linear` resolves `modP_one` from Basic.

## Next step

Future Phase 6 bridge-polish work can review whether more downstream Hensel
normalizers should move from implementation modules into `HexHensel.Basic`.

## Blockers

None for this slice.

Verification completed:

- `lake build HexHensel.Basic`
- `lake build HexHensel.Linear`
- `lake build HexHensel`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexHensel/Basic.lean HexHensel/Linear.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches
