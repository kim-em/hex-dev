# 20260609T124954Z (#6655 partial)

## Accomplished

Claimed feature issue #6655 and landed a coherent phase-one Newton substrate in
`HexPolyZ/Mignotte.lean` without changing executable definitions.

Added:

- `sqrtNearEnvelope`, the local near-root envelope `x * x < 16 * n`.
- `sqrtNearEnvelope_of_not_very_far`.
- `sqrtStep_very_far_contracts`: under `16 * n ≤ x * x`, one step satisfies
  `32 * sqrtStep n x ≤ 17 * x`.
- `sqrtStep_very_far_lt_self`.
- `sqrtStep_very_far_two_step_halves`: two very-far nonzero Newton steps halve
  the current state.
- `log2_lt_of_two_mul_lt`.
- `sqrtStep_very_far_two_step_log2_lt`: the two-step halving fact gives strict
  `Nat.log2` descent.

Created successor issue #6668 for the remaining iterated phase-one wrapper and
left the required `Decomposed into #6668` breadcrumb on #6655.

## Current frontier

#6655 is partially complete. The one-step/two-step descent facts now compile,
but the issue's full deliverable still needs an iterated theorem showing that
starting from `x = n`, the first `n.log2 + 1` fuel steps either stop or enter
`sqrtNearEnvelope`.

## Next step

Claim #6668 and prove the iterated phase-one wrapper from
`sqrtStep_very_far_two_step_log2_lt`. Once that lands, #6656 can consume the
phase-one envelope for the phase-two/final `floorSqrt_sq_le` composition.

## Blockers

None for this partial substrate.

Verification performed:

- `lake build HexPolyZ`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexPolyZ/Mignotte.lean`: no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
