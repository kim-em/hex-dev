# Session fb13f7e7 — work/feature, issue #4226

## Accomplished

- Claimed #4226 and re-read the current HexBerlekamp matrix-kernel
  context plus the relevant HexMatrix and HexPolyFp APIs.
- Added a generic Mathlib-free matrix lemma in `HexMatrix/Basic.lean`:
  `Matrix.sub_identity_mulVec`, proving that entrywise multiplication
  by `Q - I` is `Q * v - v`.
- Added the private fold helper `foldl_sum_sub` needed for that proof.
- Verified:
  - `lake build HexBerlekamp.Basic`
  - `lake build HexMatrix HexBerlekamp.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` in
    touched source files.

## Current frontier

- The generic matrix algebra piece needed to peel off the identity from
  `fixedSpaceMatrix = Q_f - I` is now available.
- I attempted the immediate `HexBerlekamp.Basic` wrapper
  `IsFixedSpaceKernelVector f hmonic v ↔ berlekampMatrix f hmonic * v = v`,
  but did not keep it: `IsFixedSpaceKernelVector` currently pins
  field-derived `ZMod64` operations explicitly, while `fixedSpaceMatrix`
  and the generic matrix lemma elaborate with the ordinary `ZMod64`
  numeral instances. The statements are mathematically the same, but
  Lean does not treat the generated `OfNat 1`/operation instances as
  definitionally equal in the wrapper proof.

## Next step

- Add a narrowly-scoped Berlekamp-local normalization lemma for the
  `ZMod64` operation instances, or refactor `IsFixedSpaceKernelVector`
  to use the same synthesized matrix-vector operation surface as
  `Matrix.nullspace_sound` and `fixedSpaceMatrix`.
- After that wrapper lands, continue with the column characterization of
  `berlekampMatrix` and the `Q_f * coeffVector w =
  coeffVector f (FpPoly.linearPow w p % f)` identity.

## Blockers

- The final #4226 deliverables remain open. This session only landed a
  reusable prerequisite for the matrix-kernel side of the bridge.
