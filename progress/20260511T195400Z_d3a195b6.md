# PR repair #3516 — stuck conformance restart

## Accomplished

- Claimed PR #3516 for repair after `coordination list-pr-repair` reported it
  as the only stuck candidate.
- Checked out branch `agent/0250d888` and diagnosed the PR as mergeable with
  passing CI build jobs and a still-running conformance job from
  2026-05-11T17:35:12Z.
- Verified the HexBZ bench slice locally with `lake build
  HexBerlekampZassenhaus.Bench`, `lake exe hexbz_bench list`, and `lake exe
  hexbz_bench verify`.
- Ran `python3 scripts/check_dag.py`, `python3 scripts/conformance_targets.py
  --check`, `python3 scripts/status.py HexBerlekampZassenhaus`, `git diff
  --check`, and the targeted banned-marker scan on
  `HexBerlekampZassenhaus/Bench.lean`.

## Current frontier

- PR #3516 appears healthy locally; the remote conformance check is stale rather
  than failed.
- The repair action is a minimal progress-only commit to trigger a fresh CI run.

## Next step

- Push `agent/0250d888` with `--force-with-lease` and mark PR #3516 salvaged.

## Blockers

- None.
