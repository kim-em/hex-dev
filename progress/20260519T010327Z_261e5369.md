# progress 20260519T010327Z — 261e5369

Session type: feature worker on issue #5189.

## Accomplished

Extended the `repr_*` simp surface in `HexGfqField/Operations.lean`
with three new wrappers paralleling the existing `toQuotient_*`
cluster (`toQuotient_intCast`, `toQuotient_nsmul`, `toQuotient_zsmul`):

- `repr_intCast` for integer casts
  (`repr (i : FiniteField …) = GFqRing.repr ((i : GFqRing.PolyQuotient f hf))`).
- `repr_nsmul` for natural scalar action
  (`repr (n • x) = GFqRing.repr (n • x.toQuotient)`).
- `repr_zsmul` for integer scalar action
  (`repr (i • x) = GFqRing.repr (i • x.toQuotient)`).

All three are `rfl`-shaped, carry `@[simp]`, and follow the existing
docstring style. They are inserted directly after `repr_pow`.

`lake build` (full), `lake build HexGfqField`, `python3
scripts/check_dag.py`, and `git diff --check` all pass. The only
warning emitted touches an existing pre-issue `simpa` lint at
`HexGfqField/Operations.lean:686`; not in scope.

## Current frontier

The Mathlib-free `repr_*` simp surface for `FiniteField` is now
complete for the unconditional unary/binary/scalar operations
(`zero`, `one`, `add`, `mul`, `neg`, `sub`, `pow`, `natCast`,
`intCast`, `nsmul`, `zsmul`).

## Next step

The conditional `repr_inv_of_ne_zero`, `repr_div`, `repr_zpow`
wrappers are explicitly out of scope per the issue and remain for a
future planner.

## Blockers

None.
