# 20260603T221535Z work #2567

## Accomplished

Added named-target shims for the BHKS Theorem 5.2 paper auxiliary
factor in `HexBerlekampZassenhausMathlib`:

* `bhksPaperAuxiliaryFactorReal_eq_product`
  (`HexBerlekampZassenhausMathlib/BHKSBound.lean`) — `rfl`-level
  unfold lemma relating the named target to the three-way product
  `bhksPaperDegreeFactorReal f * bhksPaperConstantFactorReal f C *
  bhksPaperLogFactorReal f`.  Lets callers rewrite between the
  named and unfolded forms without unfolding the definition by
  hand.

* `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_factored'`
  (`HexBerlekampZassenhausMathlib/Recovery.lean`) — a sibling of the
  existing `_factored` chain lemma whose `h_aux` argument is stated
  against `bhksPaperAuxiliaryFactorReal core C` directly instead of
  the three-way product.  Implementation is a one-line shim that
  dispatches to `_factored` via `simpa [bhksPaperAuxiliaryFactorReal]`.

## Current frontier

Downstream wrappers
(`ofBridgeDataPointwiseAuxiliaryBoundsAndFactoredPaperThreshold`,
`ofBridgeDataCorrectedAuxiliaryL2normSqAndFactoredPaperThreshold`,
`ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold`, and the four
`factorFast_terminates_…AndFactoredPaperThreshold` siblings) still
take `h_aux` in the unfolded three-way-product form.  Callers using
the named target can adapt with
`bhksPaperAuxiliaryFactorReal_eq_product.symm ▸ h_aux'` at the call
site, but a primed sibling on each wrapper would save the rewrite.

PR #6497 (just-landed on `origin/main`) added the orthogonal
`_l2norm` and `_l2norm_factored` chain siblings; the new
`_factored'` sits between `_factored` and `_l2norm`, parallel to
both.

## Next step

1. Primed siblings for each of the seven downstream wrappers
   listed above (mechanical signature copy, replace `h_aux` form,
   dispatch to `_factored'`).
2. The genuine BHKS Lemma 3.2 content: discharge
   `auxiliaryBound ^ input.natDegree ≤
   bhksPaperAuxiliaryFactorReal core C` from a Hadamard / resultant
   argument on the bad-vector polynomial.

## Blockers

None.  Pure compositions over existing non-negativity, monotonicity,
and integer-dominance pieces.

## Verification

- `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
  passed (3780 jobs, full library green).
- Added-line forbidden-token grep
  (`sorry|axiom|native_decide|TODO|FIXME`) returns nothing.
- Diff: 72 added lines, zero deletions (purely additive).
