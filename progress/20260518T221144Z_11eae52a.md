# 11eae52a — HexModArith ofNat operation lemmas (issue #5123)

## Accomplished

- Claimed feature issue #5123 after confirming the open directives are blocked.
- Added public docstringed `ZMod64` operation-characterisation lemmas in
  `HexModArith/Basic.lean`:
  `add_eq_ofNat`, `sub_eq_ofNat`, `mul_eq_ofNat`, and `pow_eq_ofNat`.
- Proved each lemma from the existing `toNat_*` representative theorem,
  `toNat_ofNat`, and `eq_iff_toNat_eq`, without unfolding executable
  implementations beyond the existing representative proofs.
- Checked `HexModArith/Ring.lean` and `HexModArith/Prime.lean`; no narrow
  direct `eq_iff_toNat_eq` + `toNat_*` + `toNat_ofNat` proof sites needed
  rewiring.
- Ran `lake build HexModArith.Basic`, `lake build HexModArith`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested
  grep invariant; all passed.

## Current frontier

Issue #5123 deliverables are complete. The change is API/proof polish only and
does not touch runtime implementations, conformance fixtures, SPEC files,
`PLAN.md`, or CI.

## Next step

Publish the PR for #5123 with `HexModArith/Basic.lean` and this progress note.

## Blockers

None for #5123. The worktree still contains a pre-existing `.claude/CLAUDE.md`
modification that this session did not touch or stage.
