# 2026-05-06 18:36 UTC — work session (f3f8a02d)

## Accomplished

Claimed #2500 and advanced the public integer Gram-Schmidt bridge theorems in
`HexGramSchmidt/Int.lean`:

- replaced the public `gramDetVec_eq_gramDet` proof body with a call to a
  focused no-pivot Bareiss leading-prefix invariant;
- replaced the public `scaledCoeffs_eq` and `scaledCoeffs_diag` proof bodies
  with calls to focused scaled-coefficient array-loop invariants;
- documented the three remaining private theorem-level proof obligations.

Verification run:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom |native_decide|TODO|FIXME' HexGramSchmidt/Int.lean`

The grep found no `axiom`, `native_decide`, `TODO`, or `FIXME`. This session
introduced three private theorem-level `sorry`s in `HexGramSchmidt/Int.lean`
and removed the direct `sorry`s from the three public theorem bodies named by
#2500.

## Current frontier

#2500 is partially complete. The public theorem statements now depend on
smaller private obligations:

- `gramDetVecEntry_eq_gramDet`
- `scaledCoeffRows_lower_eq_coeffs`
- `scaledCoeffRows_diag_eq_gramDet`

## Next step

Prove the three private loop/Bareiss invariants, starting with
`scaledCoeffRows_diag_eq_gramDet`, which is the smallest array-loop obligation.

## Blockers

No technical blockers. The worktree still has the pre-existing unrelated
`.claude/CLAUDE.md` modification, which this session did not touch.
