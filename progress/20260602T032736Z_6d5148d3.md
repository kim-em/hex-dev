# Issue #6030: HexMatrix consecutive-top Plucker basis cases

## Accomplished

- Claimed #6030 after confirming there were no unclaimed directives.
- Added the two missing private `mDet (basisVec q)` three-term Plucker
  helpers in `HexMatrix/Determinant.lean`:
  - `det_plucker_three_term_basisVec_of_lt_p1_of_nDet` for `q < p1`;
  - `det_plucker_three_term_basisVec_of_between_p1_p2_of_nDet` for
    `p1 < q < p2`.
- Routed both wrappers through the existing raw ordered `nDet` kernels and
  existing cofactor-sign shift infrastructure.
- Verified with `lake build HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token grep over the touched Lean diff.

## Current frontier

- #6030 deliverables are complete.
- The follow-up #6031 can now assemble the `hbasis` argument for the
  consecutive-top Gram cofactor Plucker specialization using the existing
  equality cases plus these q-before and q-between cases.

## Next step

- Open a PR closing #6030 with the Lean helper additions and this progress
  note.

## Blockers

- None.
