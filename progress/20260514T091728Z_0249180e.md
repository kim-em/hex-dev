# 2026-05-14 09:17 UTC — feature session (0249180e) on #3985

## Accomplished

- Claimed #3985 and added the N32 quotient-witness assembly in
  `HexGfq/CrossCheck.lean`:
  - `genericN32QuotientWitnesses_check` :
    `Berlekamp.checkPowChainLinearIncrementalQuotientWitnesses
       genericMod genericMod_monic genericN32SamePrimeCert
       genericN32Quotients = true`,
    proved via `checkPowChainLinearIncrementalQuotientWitnesses_of_steps`
    plus dispatch over `k < 32` to each step lemma.
  - `genericN32PowChain_check` :
    `Berlekamp.checkPowChainLinearIncremental
       genericMod genericMod_monic genericN32SamePrimeCert = true`,
    follows directly from
    `checkPowChainLinearIncremental_of_quotientWitnesses`.
- Added two small private helpers to discharge the first-entry
  obligation `cert.powChain[0]? == some (FpPoly.modByMonic genericMod
  FpPoly.X genericMod_monic)`:
  - `genericMod_modByMonic_X` : `FpPoly.modByMonic genericMod FpPoly.X _
    = FpPoly.X`, via `mod_eq_self_of_degree_lt` (deg X = 1 < 32).
  - `polyP2_zero_one_eq_X` : `(polyP2 #[0, 1] : FpPoly 2) = FpPoly.X`,
    via `simp` over `polyP2 / FpPoly.ofCoeffs / DensePoly.ofCoeffs /
    monomial / trimTrailingZeros` then `decide`.
- Verified `lake build HexBerlekamp.Irreducibility`,
  `lake build HexGfq.CrossCheck` (≈ 268 s),
  `lake build HexGfq`, `python3 scripts/status.py HexGfq`,
  `python3 scripts/check_dag.py`, and `git diff --check`.
- `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexBerlekamp
  HexGfq/CrossCheck.lean` reports only the pre-existing N16 and N32
  `generic_irr` placeholders (lines 350 and 1217).

## Current frontier

- N32 incremental linear-Rabin pow-chain check is now propositional
  through `genericN32PowChain_check`. The remaining piece needed to
  retire the N32 `generic_irr` placeholder is the basis-size /
  final-entry / Bezout assembly (#3895) plus
  `checkRabinBezoutWitnesses` (still a `#guard`).
- The `k < 32` dispatch uses a literal 32-arm `match` with a `k + 32`
  absurd fallback. With `cert.n` opaque to omega, the fallback must
  re-state `k + 32 < 32` via `show`.

## Next step

- Open the PR for #3985.

## Blockers

- None for #3985.
