# Quadratic multifactor invariant surface

## Accomplished

- Claimed feature issue #2139 after #2136 was already claimed by another agent.
- Added `QuadraticLiftLoopInvariant` to package the factor product congruence, Bezout congruence, and monic `g` condition consumed by `quadraticHenselStep`.
- Proved `quadraticLiftLoopInvariant_step` from the existing `quadraticHenselStep_spec` and `quadraticHenselStep_monic` theorems.
- Added `QuadraticMultifactorLiftInvariant`, mirroring the executable quadratic split tree and recursing over `henselLiftQuadratic`'s lifted complementary factor.
- Updated `multifactorLiftQuadratic_spec` to consume the quadratic invariant surface instead of the stale linear `MultifactorLiftInvariant`.

## Current frontier

`HexHensel/QuadraticMultifactor.lean` now has the invariant surface needed to scope the quadratic multifactor proof. The remaining frontier is the loop-level theorem for `henselLiftQuadratic`, then the recursive product proof for `multifactorLiftQuadratic_spec`.

## Next step

Prove that `iterateQuadraticHensel` preserves `QuadraticLiftLoopInvariant` through the doubling ladder and derive a `henselLiftQuadratic` product congruence modulo `p^k`.

## Blockers

- `multifactorLiftQuadratic_spec` remains the existing placeholder, now with the corrected quadratic invariant precondition.
- Builds still report pre-existing `sorry` warnings in dependencies and the final quadratic multifactor theorem.
