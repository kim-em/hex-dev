# 20260510T093941Z - Issue #2959

## Accomplished

Claimed #2959 and instantiated the BHKS Lemma 3.2 local-hypothesis layer in
`HexBerlekampZassenhausMathlib/BadVector.lean`.

The new section adds:

- `BHKS.auxiliaryPolynomial`: a `Hex.ZPoly`-valued definition of the BHKS
  bad-vector polynomial `H_v`, built from the existing `Hex.cldCoeffs`
  centred-cut CLD coefficient arrays scaled by the lattice vector entries.
- `ExecutableBadVectorWitness.projectedVectorFn`: promotion of the executable
  `Array Int` row to a `Fin r → ℤ` vector against the witness's projected
  factor count.
- `ExecutableBadVectorWitness.IsBhksBadVectorSetup`: bad-vector evidence
  attached to an existing witness — its `H` field is the canonical
  `BHKS.auxiliaryPolynomial`, and the same vector lies in `L'` (`projectedRowSpanInt`)
  but not in `W` (`trueFactorIndicatorLattice`).
- Three local-hypothesis theorems with `sorry` proofs (BHKS Lemma 3.2 content):
  `localFactorDegree_pos_of_bhks_bad`, `coprime_input_aux_over_rat_of_bhks_bad`,
  `resultant_divisible_by_p_pow_of_bhks_bad`.
- `ExecutableBadVectorWitness.no_bhks_bad_setup_of_l2norm_upper_lt_divisor`:
  the combined contradiction theorem chaining the three local hypotheses
  through the existing `no_badVector_of_l2norm_upper_lt_divisor`.

The design reuses `ExecutableBadVectorWitness` (per the issue's directive)
rather than introducing a parallel record — `IsBhksBadVectorSetup` is a
`Prop`-level structure attached to an existing witness.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/BadVector.lean`
  reports only the three explicit `sorry`s in the new local-hypothesis
  theorems and no new `axiom`, `native_decide`, `TODO`, or `FIXME` markers.

## Current frontier

The bad-vector packaging now reaches up to the analytic comparison: given
bad-vector evidence on a witness and the Hadamard/l2norm strict inequality,
`False` follows. The three `sorry`s in the local-hypothesis theorems are the
remaining BHKS Lemma 3.2 algebra (modular irreducible-factor witness, CLD
divisibility congruence, integer-vs-rational coprimality transport).

## Next step

Either discharge the three local-hypothesis `sorry`s using the executable
CLD/lift modular-irreducibility infrastructure, or proceed to the final
analytic contradiction (combining the bad-vector contradiction with the
`bhksThresholdNatBound` analytic comparison from `BHKSBound.lean`) toward
`factorFast_terminates`.

## Blockers

No blockers remain for #2959.
