# 20260609T105113Z (#75e494a8, work)

## Accomplished

- Claimed #2567, sanity-checked its premise, and released it with an issue
  comment because the directive body requests unconditional `factorFast`
  termination while the checked-in SPEC and implementation require a
  `choosePrimeData? = some ...` witness.
- Claimed #6414, found its assumed recursive Yun scalar synchronization
  prerequisite missing, added `depends-on: #6631`, commented with the concrete
  dependency mismatch, and released it.
- Claimed #6623 and added
  `CanonicalRecoveryTailInputs.ofForwardRecoveryInputsCanonicalRepresentations`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The new wrapper specializes `ForwardRecoveryInputs` at
  `factorFastCapLiftData`, derives the canonical representation certificates,
  and keeps the square-free-core monic, lifted-factor monic, and `2 < p^k`
  obligations explicit rather than assuming unavailable normalization facts.

## Current frontier

#6623 has a focused additive wrapper that matches the current sound API
surface. The remaining BHKS D1 work still needs callers/providers for the
explicit monic and cap-precision side conditions before the final conditional
fast-path theorem can be closed.

## Next step

Use the new actual-cap recovery-tail constructor from the cap/recovery assembly
path once the monic-core or explicit monic-normalization surface is available;
otherwise plan a narrow issue to provide that bridge or keep threading the
explicit hypothesis.

## Blockers

- #2567 is stale as written: unconditional `factorFast f ≠ none` is false for
  the current finite prime-search implementation.
- #6414 remains blocked on #6631, the recursive Yun scalar synchronization
  theorem.
