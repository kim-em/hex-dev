# 2026-05-07 23:11 UTC - BHKS fixed-precision recovery glue (216b5744)

## Accomplished

Claimed issue #2619 and added fixed-precision BHKS recovery glue in
`HexBerlekampZassenhaus/Basic.lean`:

- `bhksRecover?` now builds the CLD lattice from `LiftData`, runs the existing
  LLL cut/projection helper, extracts RREF equivalence-class indicators, verifies
  each indicator through `bhksIndicatorCandidate?`, and accepts only when the
  verified candidates multiply back to the target polynomial.
- Added small private helpers for degenerate indicator partitions and candidate
  collection.
- Added guards for a successful CLD fixture, a degenerate recovery input, and an
  exact-division failure.

Verification:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '#eval|native_decide|TODO|FIXME|^axiom\b' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The BHKS executable pipeline now has an end-to-end fixed-precision recovery
helper, but `factorFastCoreWithBound` still intentionally returns `none` and
does not call it.

## Next step

Wire `bhksRecover?` into the bounded fast precision loop with the requested
public-behavior review and failure-mode bookkeeping.

## Blockers

The CLD lattice independence theorem is still a proof placeholder, matching
the surrounding LLL proof frontier.
