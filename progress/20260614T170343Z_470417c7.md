# Feature session — issue #7286

## Accomplished
Added Phase 6 one-sentence docstrings to the 8 private theorems in the
split-factor product / `isNontrivialSplitFactor` nontriviality cluster of
`HexBerlekamp/Factor.lean`:
`splitFactorAt_mul_div_eq`, `kernelWitnessSplitAux_product_spec`,
`isNontrivialSplitFactor_not_zero`, `isNontrivialSplitFactor_degree_ne_zero`,
`isNontrivialSplitFactor_size_lt`, `isNontrivialSplitFactor_ne_input`,
`isNontrivialSplitFactor_ne_one`, `kernelWitnessSplitAux_nontrivial`.
Doc-only; no statement/proof/name/import changes.

## Current frontier
`lake build HexBerlekamp.Factor` exits 0; `python3 scripts/check_dag.py`
exits 0; `git diff --check` clean; diff is doc-only with no banned vocab.

## Next step
Remaining Factor.lean clusters are out of scope for this batch: the later
`kernelWitnessSplitAux_*` size/offset lemmas (~L232+), the
`foldl_mul_*` / `factorProduct_*` product-fold cluster, and the
`splitWithWitnesses` / `splitFirstFactor` families.

## Blockers
None.
