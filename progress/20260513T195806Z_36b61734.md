# Session 36b61734 — BHKS A2 candidate reconstruction capstone (#3790)

## Accomplished

- Claimed #3790, the HexBZ A2 reconstruction capstone.
- Made `Hex.bhksIndicatorSelectedFactors` public so downstream B7/A2 callers
  can state selected-factor product hypotheses directly.
- Added the Mathlib-free single-indicator theorem
  `Hex.bhksIndicatorCandidate?_eq_some_of_mignottePrecision`, composing:
  selected-factor extraction, modular product equality, centred Mignotte lift,
  primitive/nonnegative normalization, and monic positive-degree exact division.
- Added the Mathlib-facing wrapper
  `BHKS.bhksIndicatorCandidate?_eq_some_of_mignottePrecision`, deriving the
  coefficient bound from `defaultFactorCoeffBound_valid`.
- Added
  `ForwardRecoveryInputs.ofMignottePrecisionCandidateProducts`, which packages
  per-indicator selected-product facts through the existing
  `ofIndicatorCandidateFacts` fold.
- Verified:
  `lake build HexBerlekampZassenhaus.Basic`,
  `lake build HexBerlekampZassenhausMathlib.Recovery`,
  `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

The A2 candidate surface now exists as a reusable per-indicator theorem and a
`ForwardRecoveryInputs` constructor. Downstream #3529 can use this constructor
once it has true-factor arrays, selected-factor witnesses, and the
indicator-product congruences supplied by the B7 side.

## Next step

Continue the BHKS forward recovery chain by packaging these A2 facts into the
residual #3529 wrapper, or by advancing the B7 indicator-product/congruence
proofs that supply the `hproduct` hypotheses.

## Blockers

None for this slice. The exact-division layer currently requires expected
factors to be monic and positive-degree, matching the available #3798
`exactQuotient?` converse.
