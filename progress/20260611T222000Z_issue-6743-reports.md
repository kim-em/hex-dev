# Progress: #6743 — refresh committed carica certified ladders

## Accomplished

Post-merge evidence refresh for the requested-parameter margin (#6743,
merged as c3d2fecb). The stronger fplll request changes the certified
candidates, so the committed certified-ladder artifacts and the report
numbers/plots derived from them are regenerated at the merged commit.

Measured one combined 53-target carica run at c3d2fecb (certified +
checker + Isabelle-certified ladders, fplll-ffi + svp_certified). One
Isabelle rung (`HarshCubicNormSq25`) caught a neighbour-benchmark
transient (47.7 ms vs ~28 ms, non-monotonic) and was re-measured solo
(28.0 ms, restoring monotonicity); every other rung had <2.5% median
spread. The certified medians shifted within measurement noise; the
qualitative ratios, trends, and gating verdict are unchanged.

Artifacts:
- `reports/bench-results/hex-lll-certified-carica.json` refreshed
  (certified + Isabelle, 36 rows, commit c3d2fecb).
- `reports/bench-results/hex-lll-certified-835734e7.json` →
  `hex-lll-certified-c3d2fecb.json` (Lean-certified + checker, 34 rows).
- Plot source repointed (`scripts/plots/hex-lll-comparator.py`
  `DEFAULT_CERTIFIED`); both comparator SVGs regenerated.
- `reports/hex-lll-performance.md`: four ratio tables (certified/native,
  certified/Isabelle) + checker-share + speedups + SHA-256 pin + trend
  prose updated.
- `reports/hex-lll-scaling.md`: certified-row fits + provenance
  regenerated via `hex-lll-scaling.py --all`.

Native rows are untouched: the native reducer changed on main via #6763
(the sibling #6744 directive), whose native-curve refresh is that
directive's domain. This PR scopes strictly to the certified path and
reuses the committed native medians.

## Current frontier

Rejection stays 0/34; interval fallback stays 0 (verified separately on
the implementation PR). Report cross-checks: certified medians in the
tables match the artifacts exactly; both plot/scaling scripts run clean
against the refreshed files; no dangling references to the old export.

## Next step

None for this refresh. When #6744 re-measures the native curve, its PR
recomputes the certified/native ratio's native column.

## Blockers

None.
