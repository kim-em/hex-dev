# 2026-06-01T09:34Z — BZ split-prime regression guard (#2564 partial)

## Accomplished

- Claimed directive issue #2564 and scoped it against the current codebase.
  The repo already has the `factorSlow` / `factorFast` / `factor`
  architecture and emitted expected fixtures for the deterministic split
  regression family, but the full headline correctness theorem remains open.
- Added `HexBerlekampZassenhaus/CrossCheck.lean` guards that
  `choosePrimeData?` succeeds on `(X - 1) ... (X - 11)` and
  `(X - 1) ... (X - 24)`, the split-family cases called out in the
  investigation as previously falling through to the silent `p = 3`
  fallback cascade.
- Tried the analogous degree-72 in-module guard, but it made
  `CrossCheck` too expensive for normal builds; degree 72 remains covered by
  the existing emitted oracle fixture rather than by kernel reduction here.

## Current frontier

- This is a partial regression guard, not the HO-1 capstone. The public
  headline theorem and the remaining `sorry`s in
  `HexBerlekampZassenhausMathlib/` are still open.
- The total slow path still has documented fallback-prime surfaces; this
  session only guards the split-family prime-selection failure before the
  fallback can fire on the two cheap in-Lean regression inputs.

## Next step

- Continue HO-1 by either proving and threading a no-fallback
  `choosePrimeData?` witness through production callers, or by removing the
  remaining total fallback call sites and updating the large branch/product
  proofs accordingly.
- Keep the degree-72 check in emitted/oracle space unless a cheaper
  prime-selection witness is introduced.

## Blockers

- Full discharge of #2564 is still larger than one bounded worker session:
  it requires the Mathlib headline correctness theorem and removal/proof of
  the remaining analytic/recombination bridge obligations.
