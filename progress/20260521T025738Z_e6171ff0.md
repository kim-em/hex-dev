# 2026-05-21T02:57Z — work issue #5688

## Accomplished

Claimed #5688 after all directive claims were rejected as blocked and #5675
was already claimed. Added `HenselLiftDescentHypotheses` in
`HexBerlekampZassenhausMathlib/Basic.lean` to name the lifted-side descent
obligation independently of `HenselSubsetCorrespondenceHypotheses`.

Added `henselSubsetLiftHypotheses_of_forwardTransport_descent` and the
`choosePrimeData`/`monicisedCoreLiftData` wrapper
`henselSubsetLiftHypotheses_of_choosePrimeData_henselLiftData_descent`, so
callers can assemble `HenselSubsetLiftHypotheses` from explicit forward Hensel
transport plus explicit descent without first constructing the full lifted
subset correspondence package.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexBerlekampZassenhausMathlib/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returned no matches

Also attempted `lake build HexBerlekampZassenhausMathlib`; it built the edited
`Basic` module, then failed in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`
with the pre-existing timeout/unknown-constant diagnostics already described in
the #5688 issue body.

## Current frontier

The non-circular constructor surface is available in `Basic.lean`, but the hard
analytic descent proof is still an explicit caller obligation via
`HenselLiftDescentHypotheses.represents_modP_of_lifted`.

## Next step

Use the new descent-only package from the next HO-1 substrate issue that proves
the reverse lifted-to-mod-`p` descent from primitive Hensel uniqueness facts,
then feed it into
`henselSubsetLiftHypotheses_of_choosePrimeData_henselLiftData_descent`.

## Blockers

Full `lake build HexBerlekampZassenhausMathlib` is still blocked by existing
`IntReductionMod.lean` failures unrelated to this change.
