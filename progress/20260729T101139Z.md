# Structural matcher review fixes

## Accomplished

- Added `HexInterval.Experiment.StructuralMatcher` to the
  `HexIntervalExperiment` Lake roots, so the main experiment target now
  elaborates the reference cursor in DAG CI.
- Changed cursor stepping to use a final partial batch when the cumulative
  visit allowance is smaller than the nominal batch, and added a canary that
  consumes that remainder before reporting resource exhaustion.
- Marked the sine oddness matcher as a genuine whole-program watcher.
- Extended the engine-path canary through checked admission, program-watcher
  requeueing, renewal over the live appended arenas, and draining both the
  new and requeued matcher applications.
- Clarified both module and SPEC text that the public cursor is a transparent
  reference model; scheduler integration must hide and own it before it forms
  an authority boundary.
- Rebuilt the experiment root and all affected interval conformance targets:
  24 jobs completed successfully.

## Current frontier

The reference cursor now states and tests the intended ordering, visit-budget,
growth, renewal, and requeue behavior without claiming scheduler ownership it
does not yet have.

## Next step

Integrate one cursor per concrete matcher application into action polling and
reply admission, committing cursor progress only when the corresponding reply
is accepted.

## Blockers

None.
