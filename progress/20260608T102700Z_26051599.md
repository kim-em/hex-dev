# Session: /once dispatch on #6581 — three-tier factor combinator

## Accomplished

Claimed #6581 ("feat: factor as three-tier combinator …") and assessed
scope. The directive bundles four logically-coupled changes:

1. Rename `factorSlow*` → `factorSlowModular*` (~70 sites in
   `HexBerlekampZassenhaus/Basic.lean`).
2. Promote `exhaustiveIntegerTrialCoreFactorsWithBound` to
   `factorSlowTrial*` named entry points.
3. Rewire `factor` / `factorWithBound` as the three-tier combinator.
4. Convert `factorSlow*` signatures to `Option`-returning and remove
   `choosePrimeDataFallback`.

The combined blast radius — `HexBerlekampZassenhaus/Basic.lean`
(14912 lines, 70 `factorSlow*` references and ~150 `choosePrimeData`
references), `HexBerlekampZassenhausMathlib/Basic.lean` (17905 lines,
10+ references), plus `Conformance.lean`, `CrossCheck.lean`,
`Bench.lean`, `UFDPartition.lean`, `IntReductionMod.lean` — exceeds
one session.

Decomposed into three coherent independently-shippable sub-issues:

- #6582 — `feat: add factorSlowTrial named entry points`. Purely
  additive: wrap `exhaustiveIntegerTrialCoreFactorsWithBound` into
  `factorSlowTrialFactorsWithBound` / `factorSlowTrialWithBound` /
  `factorSlowTrial` with product invariant. No existing proof or
  definition is touched.
- #6583 — `feat: rewire factor as three-tier combinator using
  factorSlowTrial`. The bug fix: rewires `factor`/`factorWithBound`
  through a thin `factorSlowModular?` Option wrapper over the existing
  `factorSlow`, falling back to `factorSlowTrial`. Updates the
  case-split `factorWithBound_eq_factorizationOfFactors` from two-way
  to three-way and propagates to ~10 downstream theorems.
- #6584 — `refactor: rename factorSlow → factorSlowModular and drop
  choosePrimeDataFallback`. Mechanical rename + signature change to
  `Option Factorization` + removal of the silent fallback path.

#6583 depends on #6582; #6584 depends on #6583. The parent #6581 was
marked `replan` with a `Decomposed into #6582, #6583, #6584` breadcrumb.

## Current frontier

Parent #6581 is in `replan` waiting for the planner's triage cycle to
either close it (sub-issues cover it) or narrow its scope.

## Next step

A worker should claim #6582 first (additive, no existing proof
disturbed). After it lands, #6583 becomes unblocked and a worker fixes
the `factor (X² − L²)` bug there. After #6583 lands, #6584 can clean
up the rename + silent-fallback removal.

## Blockers

None. The three-way decomposition is clean and each sub-issue's
body is self-contained.
