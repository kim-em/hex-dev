# Progress: #6719 partial — exhaustive-trial core factor irreducibility Mathlib wrappers

## Accomplished

Landed two Mathlib-side wrappers at the end of
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`:

1. `exhaustiveIntegerTrialCoreFactorsWithBound_normalizeForFactor_factor_irreducible_of_bound`
   — abstract-bound specialisation of
   `Hex.exhaustiveIntegerTrialCoreFactorsWithBound_factor_irreducible`
   (`HexBerlekampZassenhaus/Basic.lean:13443`) over
   `(Hex.normalizeForFactor f).squareFreeCore`. Discharges the four core-shape
   hypotheses (`ne_zero`, `Primitive`, `0 < leadingCoeff`, `SquareFreeRat`)
   from `f ≠ 0` via
   `Hex.squareFreeCore_leadingCoeff_pos_of_ne_zero`,
   `IntReductionMod.normalizeForFactor_squareFreeCore_primitive_of_ne_zero`,
   and `Hex.ZPoly.primitiveSquareFreeDecomposition_squareFreeCore`. Leaves
   `hbound` as an explicit hypothesis.

2. `exhaustiveIntegerTrialCoreFactorsWithBound_normalizeForFactor_factor_irreducible_at_squareFreeCore_default`
   — intrinsic-core default-bound specialisation at
   `B := defaultFactorCoeffBound (normalizeForFactor f).squareFreeCore`,
   discharging `hbound` via `defaultFactorCoeffBound_valid` on the core.

Both build under `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
and propagate cleanly to `HexBerlekampZassenhausMathlib` and
`HexBerlekampZassenhaus`. `python3 scripts/check_dag.py` passes.

## Current frontier

The remaining work for full discharge of the slow-trial arm of
`factor_entry_zpolyIrreducible_of_chosen_raw_zpolyIrreducible`'s `h_raw`
(at the public bound `defaultFactorCoeffBound f`, not the intrinsic-core
bound) needs three further substrates:

* `(Hex.normalizeForFactor f).squareFreeCore ∣ f` Mathlib-free divisibility
  chain through `primitiveSquareFreeDecomposition_reassembly_signed` +
  `extractXPower_product` + `content_mul_primitivePart`. This is what
  lifts `defaultFactorCoeffBound_valid f` to a divisor coefficient bound
  on the squarefree core at the public `B = defaultFactorCoeffBound f`.
* `Hex.exhaustiveIntegerTrialCoreFactorsWithBound_degree_pos` and
  `_leadingCoeff_pos` Mathlib-free companion lemmas, mirroring
  `exhaustiveCoreFactorsWithBound_degree_pos` for the modular path.
  Needed by the `reassemblyExpansionComplete` fuel bound.
* `reassemblyExpansionComplete_exhaustive_integer_trial_of_ne_zero`
  Mathlib-side discharger, composing the above with
  `reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`.
  This is the analog of
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` for the modular
  path.

With those three in hand, the final discharge composes
(1) the divisor-bound bridge plus this PR's wrapper for core-factor
irreducibility, (2) the new degree/lc lemmas plus the new discharger for
the reassembly precondition, and (3)
`reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`
to lift to raw factors.

## Next step

Open #6719 as a partial PR landing the two wrappers above, and decompose
the residual into sub-issues for (a) the divisibility chain, (b) the
degree/lc companions, and (c) the reassembly-complete discharger.

## Blockers

None for this partial PR. The residual sub-issues are the natural
decomposition of the original directive.
