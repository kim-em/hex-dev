# 2026-06-04T12:09:16Z — Sub-D of #6533: capstone Schur ≡ Bareiss main theorem

Session UUID prefix: 7469b395
Issue: #6538

## Accomplished

- Added public theorem
  `getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing` to
  `HexGramSchmidt/Int.lean`, immediately after the σ-fold helper
  `schurSigma_foldl_eq_noPivotCorrection`.
- Statement: at each non-singular column `q < n`, the diagonal slot
  `rows[q][q]` and every below-column entry `rows[c][q]` (`c > q`) of the
  integral Schur kernel equal the corresponding `noPivotLoop`/Bareiss
  matrix entries at fuel `q`.
- Proved by strong induction on `q`:
  - Base `q = 0`: column-zero boundary of the Schur kernel
    (`getArrayEntry_scaledCoeffRowsSchur_col_zero`) plus
    `Matrix.noPivotLoop_zero_fuel`, closing both halves via
    `getArrayEntry_gramRows`.
  - Step `q' + 1`: assemble the σ-fold `correspondence` hypothesis from
    the strong IH at all levels `l ≤ q'` (using
    `noPivotLoop_prefix_none_of_final_none` to derive prefix
    non-singularity at every such `l`), then apply
    `schurSigma_foldl_eq_noPivotCorrection` at `p_out = q' + 1`. Combine
    with the Schur-side recurrence
    `getArrayEntry_scaledCoeffRowsSchur_eq_schurScaledCoeffEntry` to get
    `rows[a][q' + 1] = rows[q'][q'] · gram[a][q' + 1] - schurSigma rows a (q' + 1)`,
    substitute, and close by `grind` (the algebraic identity
    `α · β - (α · β - γ) = γ`).
- `lake build` clean across all 244 targets; pre-existing sorries at
  lines 6149 and 6463 of `HexGramSchmidt/Int.lean` are unchanged
  (`grep -c sorry HexGramSchmidt/Int.lean` still reports 3).
- The new proof does **not** cite
  `getArrayEntry_scaledCoeffRowsSchur_eq` (the residual sorry at
  line 6463) nor any lemma whose proof transitively does. Cited
  dependencies: `getArrayEntry_scaledCoeffRowsSchur_col_zero`,
  `getArrayEntry_scaledCoeffRowsSchur_eq_schurScaledCoeffEntry`,
  `schurSigma_foldl_eq_noPivotCorrection`,
  `noPivotLoop_prefix_none_of_final_none`, `getArrayEntry_gramRows`,
  `Matrix.noPivotLoop_zero_fuel`, plus Lean-core `Std.Legacy.Range`
  rewrite lemmas — all sorry-free.

## Current frontier

Sub-D done. The capstone of the #6533 workhorse — non-singular Schur ≡
Bareiss correspondence — is now available as a public theorem. The
remaining piece in the broader #6533 puzzle is the singular cascade
(parent #6532) and consuming the new correspondence to discharge the
residual sorry at line 6463 of `HexGramSchmidt/Int.lean`.

## Next step

Wire `getArrayEntry_scaledCoeffRowsSchur_eq_noPivotLoop_of_nonsing` into
the residual sorry at line 6463
(`getArrayEntry_scaledCoeffRowsSchur_eq`) for the strict lower
non-singular sub-case, in concert with the singular cascade from
parent #6532.

## Blockers

None.

## Notes

- The σ-fold helper's `correspondence` hypothesis quantifies `∀ l ≤ q',
  ...`. To supply this for arbitrary levels `l ≤ q'`, the main theorem
  must be proved by strong induction (regular induction at `q` only
  gives the conclusion at `q - 1`, not at every prior column). The
  proof uses `Nat.strongRecOn`, then assembles `correspondence` from
  `ih` plus `noPivotLoop_prefix_none_of_final_none` (which transports
  non-singularity from `q` to any prefix `l ≤ q`).
- Diagonal (`a = q' + 1`) and off-diagonal (`a = c > q' + 1`) cases
  share the algebraic shape `rows[q'][q'] · gram[a][q' + 1] -
  schurSigma rows a (q' + 1)`. The σ-fold helper requires
  `p_out ≤ a`, which holds in both cases (`q' + 1 ≤ q' + 1` for the
  diagonal, `q' + 1 ≤ c` for off-diagonal). Factoring this into a
  single intermediate `h_sigma_closed` keyed on `a` avoids duplicating
  the σ-fold call between the two branches.
- The `schurSigma` Id-do loop is converted to the helper's foldl form
  by `simp [Std.Legacy.Range.forIn_eq_forIn_range', Std.Legacy.Range.size]`
  on the explicit `show _ = _` form (same pattern used by the existing
  `schurSigma_invariant_under_row_reset` at line 1599).
