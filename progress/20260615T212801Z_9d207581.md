# Progress — 2026-06-15 (session 9d207581, work→feature)

## Accomplished

Issue #7354: added one-sentence Phase 6 docstrings to the 12
`luebeckConwayPolynomialOfCoeffs_{p}_{n}_irreducible` private wrappers
for p ∈ {2, 3}, n ∈ 1..6 in `HexConway/Basic.lean` (lines ~2305–2435).

Each docstring states that the coefficient-list constructor for the
committed `C(p, n)` Conway entry yields an irreducible `FpPoly`, via the
`luebeckConwayPolynomial?` table hit and the literal's irreducibility
proof, with `p`/`n` varied per declaration.

Doc-only change: no signatures, statements, proofs, declaration order,
or `private` modifiers touched. The p ∈ {5,7,11,13} wrappers were left
untouched (separate batch).

## Verification

- `lake build HexConway.Basic`: green (476 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexConway/Basic.lean`: `36 0` (additions only;
  3-line docstrings × 12).
- Added-line grep finds no `sorry` / `axiom` / `native_decide` /
  `TODO` / `FIXME`.

## Current frontier

The p ∈ {5,7,11,13} `luebeckConwayPolynomialOfCoeffs_{p}_{n}_irreducible`
wrappers (24 declarations, lines ~2437 onward) remain a separate Phase 6
doc batch.

## Next step

None for this issue. Other unclaimed Phase 6 doc batches and the two
deeper feature issues (#6777 C-dependence gap, #7357 Hensel lift
substrate) remain in the queue.

## Blockers

None.
