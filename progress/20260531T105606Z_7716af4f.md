# HO-5d-2c — HBZ Mathlib IntReductionMod bridge wrappers off total choosePrimeData

Issue: #5855.
Session UUID prefix: 7716af4f.

## Accomplished

Migrated all 14 bridge wrappers in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` to take an
explicit `primeData : Hex.PrimeChoiceData` parameter paired with
`hchoose : Hex.choosePrimeData? core = some primeData`, replacing
every occurrence of `Hex.choosePrimeData (Hex.normalizeForFactor f).squareFreeCore`
in the wrapper statements with `primeData`:

- `liftedFactorSubsetPartition_outerBound_of_choosePrimeData`
- `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
- `factor_small_mod_singleton_branch_entry_irreducible`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
- `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`
- `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
- `monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`

Pattern: proof bodies derive
`hpd : Hex.choosePrimeData core = primeData` from `hchoose` (via
`unfold Hex.choosePrimeData; rw [hchoose]`) and `subst hpd`, after
which the pre-existing proof bodies apply unchanged. Calls to
already-migrated wrappers within the file are threaded with the
local `(Hex.choosePrimeData ...)` and `hchoose` values.

The issue lists `squareFreeCore_irreducible_of_small_mod_singleton_of_choosePrimeData`
(line 420), `factor_constant_branch_entry_irreducible_of_choosePrimeData`
(line 2858), and `factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`
(line 3032) but each of these is either already migrated in a prior
PR or never mentioned `Hex.choosePrimeData` in its statement (their
`_of_choosePrimeData` suffix is documentary). No changes were
needed for those three.

Two non-bridge-wrapper theorems still mention `Hex.choosePrimeData`
in their statements and are out of scope per the issue's bridge-
wrapper list: `squareFreeCore_irreducible_of_small_mod_singleton`
(line 280, an unused `_hselected` equation) and
`monicReductionCorrespondence_of_normalizeForFactor_squareFreeCore`
(line 3647, structure-constructor helper).

## Current frontier

`lake build HexBerlekampZassenhausMathlib` fails at three
pre-existing call sites in
`HexBerlekampZassenhausMathlib/Basic.lean:5099,5994,6280`. These
`Hex.isGoodPrime_squareFreeModP ...` call sites now return
`Hex.squareFreeModP` (i.e. `gcdIsUnit gcd = true`) since the
upstream API change in #5779, but the local proofs still bind the
result at type `Hex.DensePoly.gcd ... = 1`. The failures are
identical to those reported in the previous session
(#5854 progress entry `20260531T101449Z_274301d2.md`); the
`HexBerlekampZassenhausMathlib` library has not been part of any
merge-gating CI workflow, so master CI is green despite the local
build failure.

Because Basic.lean fails before IntReductionMod.lean is reached,
the migration's verification step
`lake build HexBerlekampZassenhausMathlib succeeds` is not
exercised end-to-end. The first verification criterion — no bridge
wrapper statement mentions `Hex.choosePrimeData` directly — is
verified by grep on the migrated wrappers, and the migration
mirrors the pattern established by the merged HBZMathlib/Basic.lean
migration in PR #5862 (issue #5854).

## Next step

A separate PR is needed to migrate the three Basic.lean call sites
off the `gcd = 1` form to the post-#5779 `Hex.squareFreeModP` form,
after which `lake build HexBerlekampZassenhausMathlib` would
succeed and this PR's second verification criterion would pass.
Until then, the migration here is complete per the first
verification criterion.

## Blockers

Pre-existing Basic.lean build failure described above blocks the
end-to-end `lake build HexBerlekampZassenhausMathlib` verification
step. The migration itself (which is the main deliverable) is
complete and follows the established pattern from #5862.
