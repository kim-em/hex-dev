# Session a28a83bc — HexBZ Mathlib natDegree of recombinationCandidate (#4439)

Session: `a28a83bc-f169-4368-b084-14c8be4895ae`
Issues: `#4334` (skipped, environment), `#4430` (decomposed → #4439/#4440/#4441),
`#4439` (this PR)

## Accomplished

Skipped `#4334` after probing `Hex.LLLBench.runSizeReduceChecksum`: per-spawn
floor `1.310 s`, killed at cap with zero verdict-eligible rows on `carica`
load avg ~3/4/5. Same pattern as eleven prior environmental skips.

Claimed `#4430` (main candidate divisibility theorem) and decomposed it into
three sub-issues after determining the proof requires substantial degree-
counting infrastructure that does not exist anywhere in the codebase:

- `#4439` — natDegree of `recombinationCandidate` as a sum of selected
  lifted-factor natDegrees (this PR).
- `#4440` — analogous natDegree identity for a represented integer factor
  (blocked on #4439).
- `#4441` — `T = ⊔ S_g` reverse coverage plus main theorem assembly
  (blocked on #4439 + #4440).

Claimed `#4439` and landed it in `HexBerlekampZassenhausMathlib/Basic.lean`:

- New private helper `size_centeredLiftPoly_eq_of_monic`: under
  `Monic g` and `2 ≤ m`, the centred lift `Hex.centeredLiftPoly g m` has the
  same stored size as `g`. The proof reuses the existing leading-coefficient
  survival + `DensePoly.ofCoeffs` size-cap pattern from
  `monic_centeredLiftPoly_of_monic`.
- New public theorem
  `natDegree_toPolynomial_recombinationCandidate_eq_sum`:
  under `2 ≤ d.p^d.k` and monic lifted factors,

  ```
  (HexPolyZMathlib.toPolynomial (recombinationCandidate d T)).natDegree
    = ∑ i ∈ T,
        (HexPolyZMathlib.toPolynomial (liftedFactor d i)).natDegree
  ```

The proof chains existing infrastructure:

1. `liftedFactorProduct_monic` makes the product monic.
2. `monic_centeredLiftPoly_of_monic` carries monicness through the
   centred lift.
3. `monic_primitive_sign_normalized_of_monic`
   + `primitivePart_eq_self_of_primitive` collapse the
   `normalizeFactorSign ∘ primitivePart` outer normalisation.
4. `size_centeredLiftPoly_eq_of_monic` (new helper) preserves size.
5. `HexPolyMathlib.natDegree_toPolynomial` rewrites `natDegree` as
   `size - 1`.
6. `toPolynomial_liftedFactorProduct` + `Polynomial.natDegree_prod_of_monic`
   distribute the sum over the subset.

The hypothesis set is intentionally minimal — just modulus ≥ 2 and monic
lifted factors. Downstream consumers (`#4440`, `#4441`) can derive
`2 ≤ d.p^d.k` from `hcore_ne` + `hprecision` + monic-core via the existing
pattern in `representsIntegerFactorAtLift_monic`.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.Basic` — clean (only the two
  pre-existing `sorry` warnings at L162 and L241 remain).
- `lake build HexBerlekampZassenhausMathlib` — clean (3461 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `rg '^\s*axiom\b|\bnative_decide\b' HexBerlekampZassenhausMathlib/Basic.lean` — no matches.
- Sorry count in `HexBerlekampZassenhausMathlib/Basic.lean` unchanged at 2.

## Current frontier

`#4439` is complete. The candidate-side natDegree identity is now available
to downstream consumers.

## Next step

`#4440` is now unblocked: prove the represented-factor natDegree identity
using `recombinationCandidate_eq_factor_of_recovery` + `#4439`. Once `#4440`
lands, `#4441` (the `T = ⊔ S_g` reverse coverage + main theorem) becomes
the residual.

## Blockers

None.
