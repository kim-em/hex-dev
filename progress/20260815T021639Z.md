# FKS2 scale-probe review repair

## Accomplished

- Corrected the conformance description to state that representative cells use
  direct indexed schema dispatch and that the generic large-payload frontend
  fold is absent.
- Proved the doubled-error mutation mathematically false with a lower Taylor
  bound, and added replay-body/format rejection guards.
- Made the exact 128-tuple source correspondence load-bearing in
  `pnt_inventory.py --verify-source`, with positive and mutation unit tests.
- Added the Mathlib-free provider contract and exact resource envelope to the
  core SPEC, and recorded the proof-side heartbeat/depth and observed
  elaboration cost in the bridge SPEC.
- Tightened the payload atom bound to the segment's exact largest denominator
  and replaced the unused proof-frontend import with the direct frontend API.
- Renamed this probe's earlier progress files to the repository's sortable UTC
  timestamp convention.
- Re-ran focused/full builds, source/inventory/freshness tests, static guards,
  and the trust scan.

## Current frontier

The repaired PR candidate retains exactly 128/13,590 coverage and direct
indexed replay. The generic large-payload frontend fold remains explicitly
outside this probe.

## Next step

Push the superseding exact head, obtain a fresh exact-head review, and monitor
the replacement CI run to merge-readiness.

## Blockers

None.
