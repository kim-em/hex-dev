# Progress — session 9ddf9ed3

## Accomplished

Closed #3854: replaced three `sorry`s in `HexGramSchmidt/Update.lean`
with full proofs.

- `gramDet_adjacentSwap_of_ne` (line 397 in the original file): the
  leading Gram determinant at depth `t` is invariant under
  `adjacentSwap` when `t ≠ k.val`. Proved via two cases:
  - `t ≤ km1.val`: the leading prefix of `gramMatrix` is literally
    unchanged because both swapped rows lie outside `[0, t)`. New
    helper `leadingGramMatrixInt_rowSwap_outside`.
  - `k.val < t`: the leading prefix is a "row-and-column swap" of the
    original Gram matrix at the lifted indices `km1', k' : Fin t`.
    New helper `leadingGramMatrixInt_rowSwap_inside` expresses this
    as `(rowSwap (rowSwap M km1' k').transpose km1' k').transpose`.
    `det_rowSwap_transpose_rowSwap_transpose` chains
    `det_rowSwap` and `det_transpose` to cancel the two `-1` factors.
- `scaledCoeffs_adjacentSwap_lower_prev` (line 418).
- `scaledCoeffs_adjacentSwap_lower_curr` (line 425).

Both scaled-coefficient transports follow the
`scaledCoeffs_sizeReduce_lower` template: cast to `Rat` via a local
`intCast_rat_injective_local`, apply public `scaledCoeffs_eq`,
substitute the gramDet invariance (new) and the existing
`coeffs_adjacentSwap_lower_prev`/`coeffs_adjacentSwap_lower_curr`
rational coefficient identities, then cast back.

The inside-case matrix identity sidesteps `GramSchmidt.liftFinLE`
(private to `HexGramSchmidt/Int.lean`) by rewriting both
`leadingGramMatrixInt` references to `leadingPrefix (gramMatrix _)`
via the public `leadingGramMatrixInt_eq_leadingPrefix_gram` bridge.
Index equalities under row swaps are handled with `congrArg M.get`
and `Eq.trans` to avoid `rw`-motive failures on dependent Fin
bounds.

Verification:
- `lake build HexGramSchmidt.Update` — clean.
- `lake build HexGramSchmidt` — clean.
- `python3 scripts/check_dag.py` — exit 0.
- 7 `sorry`s remain in `Update.lean`, exactly the out-of-scope
  adjacent-swap pivot / above-pivot / divisibility cluster called out
  in the issue.

## Current frontier

`Update.lean` still has the seven pivot / above-pivot sorries that
need the integer divisibility argument through `adjacentSwapDenom`.

## Next step

Follow-up issue(s) on the remaining seven sorries.

## Blockers

None.
