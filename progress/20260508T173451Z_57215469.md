## Accomplished

- Claimed human-oversight issue #2695 for registering `Hex.LLLState.ofBasis` as its own HexLLL setup benchmark.
- Added three `LLLState.ofBasis` bench registrations in `HexLLL/Bench.lean`, covering the `bz-recombination`, `random-bounded`, and `harsh-cubic` Phase-4 input families.
- Added adjacent cost-model derivation comments for the Gram matrix construction plus the two Bareiss-shaped passes behind `gramDetVec` and `scaledCoeffs`.
- Verified all three new registrations at scientific settings; each reported `consistent with declared complexity` after widening the random-bounded ladder enough to keep verdict-eligible rows.

## Current frontier

- `lake exe hexlll_bench list` now shows the new `runOfBasis*Checksum` targets alongside the existing HexLLL targets.
- The bench fixture independence witness is a private `sorry`, matching the current state of non-identity LLL fixture independence proofs; the measured constructor path is executable and proof-erased.

## Next step

- Review and merge the PR for #2695, then continue with the dependent HexLLL Phase-4 work items that replace the identity end-to-end bench and wire comparators.

## Blockers

- None for #2695.
