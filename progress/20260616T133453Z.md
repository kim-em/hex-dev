# BHKS cut-semantics decision audit

## Accomplished

Reviewed the latest progress note, the BHKS fast-path SPEC, and the prior
conversation excerpts around #7590.  Confirmed that the current proof sketch for
BHKS Lemma 5.7 is prefix-indexed, while the executable cut under discussion is a
per-row Gram-Schmidt norm filter.

## Current frontier

The forward true-factor norm bound proves only that the true-factor lattice
vectors are short.  It does not prove that all non-true or later Gram-Schmidt
rows are long.  That upper/lower separation is the same semantic content needed
to exclude extra short lattice directions.

## Next step

Recommend changing the executable cut to a structurally prefix/sorted cut, then
decompose the worker tasks around the prefix survivor-span theorem, the
LLL-output span bridge, the true-factor CLD lattice vector construction, and the
final cut-projection producer.

## Blockers

Keeping the per-row cut would require an additional gap certificate whose hard
half appears to smuggle in the dead reverse separation argument.
