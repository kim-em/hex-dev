# PNT+ negative exponential table

## Accomplished

- Continued from the preserved natural-log edge
  `50c9f53b0e4af291eedaf66ba8a8b7172b582bf2` and audited the 108 remaining
  actual `LogTables.lean` tactic sites at pinned PNT+ commit
  `21998bb6196b56789f72a52656a781a75e134eb0`.
- Identified the largest coherent independent subgroup: all 79 direct Table-10
  upper bounds `exp_neg_10_lt` through `exp_neg_200_lt` at source lines
  244--322. Every argument is an integer number of sixths.
- Added a bounded Mathlib-free package authenticating the source coordinate,
  exact sixth count, fourteen-term anchor recipe, natural-power count, and
  decimal endpoint. The executable decoder checks the final rational power
  comparison as an exact natural-number cross product.
- Added one ordinary-kernel companion proving
  `exp (-1/6) < 846481724891 / 10^12` from `Real.exp_bound`, then using
  `Real.exp_nat_mul` and the authenticated integer inequality to export all 79
  exact source-shaped theorems.
- Exercised every source row through the package runtime. The tight
  `exp_neg_70_3_lt` row also crosses policy selection, payload chronology,
  semantic replay, and generic `ProofFrontend` closure. Unknown coordinates,
  insufficient terms, wrong sixths, cross-row payloads, source/output mismatch,
  and a false zero endpoint fail closed.
- Added guarded axiom reports for the first and last wrappers and the frontend
  theorem; each reports only `propext`, `Classical.choice`, and `Quot.sound`.
- Reclassified exactly these 79 inventory records as accepted after localized
  PNT+ rewrites. `LogTables.lean` now has 29 actual tactic sites still pending;
  no whole-interface or LeanCert API-compatibility claim is made.
- Updated both SPECs, Lake registrations, and the exact proof-only runtime
  freshness exemption.
- Built the focused runtime, Mathlib, conformance, Mathlib-free experiment,
  retained PNT targets, and the full `HexIntervalMathlibExperiment` target.
  Inventory check/source regeneration/tests, copyright, line-count, DAG,
  Phase-4, trust, release-manifest, Mathlib-free bench, freshness,
  diff-hygiene, and banned-mechanism checks all pass.

## Current frontier

The natural-log and direct negative-exponential slices together leave 29 actual
`LogTables.lean` tactic sites pending. They comprise small point exp/log/pi
facts, two-sided nested/large logarithms, and several large positive/negative
exponential comparisons rather than another uniform member of this sixth-power
source table.

## Next step

Preserve this local stack until the small-prime publication edge lands. The next
`LogTables` audit should group the remaining large natural/decimal logarithm
upper and two-sided bounds around the existing atanh provider, or separately
cover the small exp point facts with a precision-indexed Taylor schema.

## Blockers

None for the completed 79-row subgroup. The Li(2) interface remains deliberately
pending because its current public numerical bounds are admitted/LeanCert-owned;
this work does not import them as a provider.
