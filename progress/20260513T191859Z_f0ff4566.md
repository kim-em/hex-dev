# Session f0ff4566 — issue #3800 (decomposed) and #3809

## Accomplished

- Reviewed parent #3800 (Mathlib-free determinant infrastructure for
  Desnanot-Jacobi) and concluded it was too large for one session: the
  upstream Mathlib proof relies on general `det_mul` and a universal-
  polynomial specialisation over `MvPolynomial`, neither of which has a
  Hex counterpart.
- Decomposed #3800 into four sub-issues:
  - #3809 — auxAdjugateM / auxP definitions + matrix equality (this PR).
  - #3810 — `det (auxAdjugateM M)` via column multilinearity + Laplace.
  - #3811 — `det (auxP M)` via column multilinearity + Laplace.
  - #3812 — narrow det multiplication
    `det (M * auxAdjugateM M) = det M * det (auxAdjugateM M)`.
  Skipped #3800 with `Decomposed into #3809, #3810, #3811, #3812`
  breadcrumb for planner replan.
- Claimed and implemented #3809 in `HexMatrix/Determinant.lean`:
  - `Hex.Matrix.auxAdjugateM` and `Hex.Matrix.auxP` over
    `[Lean.Grind.CommRing R]`.
  - Entry simp lemmas `auxAdjugateM_apply_zero`,
    `auxAdjugateM_apply_last`, `auxAdjugateM_apply_middle`, and the
    `auxP_` counterparts.
  - `mul_auxAdjugateM_eq_auxP`: `M * auxAdjugateM M = auxP M`, via a
    private `mul_apply_of_col_eq` helper that reduces each column case
    to `mul_adjugate_apply_zero` / `mul_adjugate_apply_last` /
    `mul_one`.
- Verified with `lake build HexMatrix.Determinant`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the
  `sorry/axiom/native_decide/TODO/FIXME` grep. The only `sorry` found
  is the pre-existing `bareiss_eq_det` in `HexMatrix/Bareiss.lean`.

## Current frontier

`HexMatrix/Determinant.lean` exposes `auxAdjugateM`, `auxP`, the
entry simp lemmas, and the matrix equality
`M * auxAdjugateM M = auxP M`. The two determinant evaluations
(`det auxAdjugateM`, `det auxP`) and the narrow det-multiplication
needed for the scaled Desnanot-Jacobi identity remain in #3810, #3811,
#3812.

## Next step

Pick up #3810 (compute `det (auxAdjugateM M)`) and #3811 (compute
`det (auxP M)`) in parallel — both depend only on #3809 and use
column multilinearity + Laplace. After both, #3812 closes the loop
with the narrow det multiplication, yielding the scaled Desnanot-
Jacobi identity in a follow-on PR.

## Blockers

None for #3809 itself. The unscaled Desnanot-Jacobi identity (over
arbitrary `[Lean.Grind.CommRing R]`) will require a follow-on issue
once #3810/#3811/#3812 land, since cancelling `det M` requires either
a universal-polynomial trick or restriction to a domain.
