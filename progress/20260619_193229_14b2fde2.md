# Phase 6 grind coverage for HexPolyZMathlib/SchurSzego.lean (#8089)

## Accomplished

Promoted all 13 equation-shaped public `@[simp]` lemmas in
`HexPolyZMathlib/SchurSzego.lean` to `@[simp, grind =]`. The file had
exactly 13 `@[simp]` lemmas and zero grind tags before this change; every
one was a literal `lhs = rhs` (re-read at the point of promotion), so all
13 were promoted and zero remain `@[simp]`-only:

- `coeff_schmeisserComposition_of_le`, `coeff_schmeisserComposition_of_lt`
  (conditional `coeff … = …` equations)
- `schmeisserComposition_zero_left`, `schmeisserComposition_zero_right`,
  `schmeisserComposition_zero`, `schmeisserComposition_C_C`
- `schmeisserDerivativeKernel_zero`, `schmeisserDerivativeKernel_one`
- `rootsRadiusProduct_zero`, `rootsRadiusProduct_one`
- `filtered_norm_div_one`
- `rootsOutsideRadiusCount_zero`, `rootsStrictlyOutsideRadiusCount_zero`

No exclusions: the issue's full candidate list matched the file's entire
`@[simp]` set, and none had a non-equation conclusion or an `Array.getD`/
`dite`-headed LHS (the Phase 6 hard-crash trap). LHS heads are all plain
def-headed (`schmeisserComposition`, `schmeisserDerivativeKernel`,
`rootsRadiusProduct`, `rootsOutsideRadiusCount`,
`rootsStrictlyOutsideRadiusCount`, `Polynomial.coeff`) or `Multiset.prod`
(`filtered_norm_div_one`), all valid grind patterns that strictly reduce.

## Current frontier

`lake build HexPolyZMathlib.SchurSzego` green (7.5s — no grind-loop
slowdown). Full `lake build HexPolyZMathlib` green (3408 jobs, zero
errors/warnings/sorry), covering the downstream consumers
`HexPolyZMathlib.lean` and `HexPolyZMathlib/RobinsonForm.lean`.
`scripts/check_dag.py` exit 0. Annotation-only diff (13 insertions, 13
deletions); no proof bodies changed, no imports added, no `*Mathlib`
proof bodies touched.

## Next step

None for this issue. Sibling Phase 6 sweeps remain unclaimed: #8090
(HexPolyZMathlib RobinsonForm.lean + Mignotte.lean), #8092 (HexGF2Mathlib
Field.lean + Basic.lean).

## Blockers

None.
