# 2026-05-11 08:08 UTC — feature session (8822a5c5)

## Accomplished

Closed issue #3274 (capstone of #3260): proved
`detSign_composePermutationValues` and the inverse-orientation
corollary `detSign_eq_mul_detSign_composePermutationValues` in
`HexMatrix/Determinant.lean`.

The main theorem uses strong induction on
`inversionCount tau.toList` to reduce to the identity case via the
adjacent-descent witnesses landed in PR #3275 / issue #3273. The
inductive step transposes the descent pair using
`composePermutationValues_transposePermutationValues_right` to push
the transposition through the composition, applies
`detSign_transposeValues` on both `tau` and the composition, then
closes via `grind` on the sign algebra. The base case uses
`perm_eq_identity_of_inversionCount_zero` to reduce `tau` to the
identity vector and `detSign_identity` to evaluate its sign.

The corollary follows by rewriting with the main theorem and using
`detSign sigma * detSign sigma = 1` (cases on the parity of
`inversionCount sigma.toList`).

## Current frontier

This unblocks #3250 (generic column-permutation determinant), which
in turn unblocks #3243 (selected-minor specialization), and finally
#3238 (Cauchy-Binet sum-of-squares capstone).

## Next step

Next worker should claim #3250 — the reusable arbitrary
column-permutation determinant theorem — now that the multiplicativity
capstone has landed.

## Blockers

None for the Cauchy-Binet chain; subsequent issues are now unblocked
in dependency order.
