# Issue 9775 worker progress

- Date: 2026-08-28 02:00 UTC
- Session type: feature
- Replaced the quadratic suffix-scan factor-subject check with an exposed,
  structurally recursive strict-ascending check. Its accepted contract yields
  the same lower-bound and pairwise-distinct facts used by Pocklington
  soundness.
- Kept certificate search untrusted: it sorts whole factor entries into the
  canonical order, and the public checker still validates the resulting node.
  Bases, positive stored exponents, and child certificates remain attached to
  the same entries.
- Updated committed benchmark and manual certificates to canonical order and
  documented all asymptotically material checker operation counts.
- Added native guards over 2,048 entries and kernel replay probes over 1,024
  entries, including a duplicate in the final slot.
- Kept the independent Python checker and committed conformance fixture aligned
  with the canonical-order contract, including a distinct-but-unsorted negative
  case and producer-facing format documentation.
- Verification: `lake build HexPrimality HexPrimalityKernelProbe`,
  `lake build HexPrimalityKernelProbe`, and
  `lake build hexprimality_bench HexManual`,
  `lake build HexConformance`, and the fixture regeneration diff all completed
  successfully. The focused Python oracle branch regression passed; the full
  oracle self-check skipped because `cypari2` is not installed locally.
- Quality delta: no `sorry` or `axiom` added; no deliverables remain.
