# HexPolyZ Phase 7 reference chapter (#8190)

## Accomplished

Authored the `HexManual` reference chapter for `HexPolyZ` and bumped the
library to Phase 7.

- New `HexManual/Chapters/HexPolyZ.lean`: a Verso chapter modeled on
  `HexManual/Chapters/HexPoly.lean`. Sections: introduction; core type
  (`ZPoly` as `DensePoly Int`); content/primitive part + `dilate` +
  `IsUnit`, with a worked `#guard` example; congruence for Hensel
  lifting (`congr`, `coprimeModP`, equivalence + ring-compat laws);
  content multiplicativity (Gauss's lemma); the Mignotte coefficient
  bound (`binom`, `ceilSqrt`, `coeffNormSq`, `coeffL2NormBound`,
  `mignotteCoeffBound`, `defaultFactorCoeffBound`) with a worked
  example; key correctness theorems (the `≤ 2·normSq` overestimate, the
  uniform-bound domination lemmas, and positivity on nonzero input);
  cross-references to `HexPoly`, `HexPolyZMathlib`, and `HexHensel`.
- Wired the chapter into `HexManual.lean` (import + `{include 0 …}`).
- `libraries.yml[HexPolyZ].done_through: 6 → 7`.

Worked-example `#guard` values were pre-validated against the
Mathlib-free `HexPolyZ` library in a throwaway scratch file (since
building `HexManual` pulls Mathlib transitively and is slow), then the
scratch file was deleted before committing.

## Verification

- `lake build HexManual` green (2550 jobs); `HexManual.Chapters.HexPolyZ`
  builds with no errors or warnings (so every `{docstring}`/`{ref}`
  resolves and every embedded `#guard` typechecks).
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` moves `HexPolyZ` to "Fully done".
- No `axiom`/`sorry`/`native_decide` in the chapter; no Mathlib
  dependency added to `HexPolyZ` itself (Verso's Mathlib dep flows only
  through `HexManual`).

## Current frontier

Chapter complete and merged-ready. `HexPolyZ` is now `done_through: 7`.

## Next step

`HexLLL` Phase 7 chapter (#8194) is still unclaimed — the sibling work
item. `HexPolyZ` has no anchored tutorials (per `PLAN/Phase7.md` table),
so its Phase 7 is fully done with the reference chapter alone.

## Blockers

None.
