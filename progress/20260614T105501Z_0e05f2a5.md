# 20260614T105501Z — feature session 0e05f2a5 (#6454)

## Accomplished

- Claimed #6454 (BHKS D1 final composition wrapper
  `factorFast_terminates_of_forwardRecoveryInputs`).
- Re-applied the composition wrapper to
  `HexBerlekampZassenhausMathlib/Recovery.lean`, rebased onto current
  `origin/main` (commit `89c4ac96`). Body:
  `factorFast_terminates f primeData h.rows_pos trueSupports`
  `(FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold …)`
  `(CanonicalRecoveryTailInputs.ofForwardRecoveryInputsCanonicalRepresentations h …)`.
- Signature audit against current main (all three producers match the
  call exactly, parameter for parameter):
  - `factorFast_terminates` — Recovery.lean:7184 (takes `rows_pos`;
    wrapper supplies `h.rows_pos`).
  - cap-side `FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
    — Recovery.lean:5221, mirrored by the proven template
    `factorFast_terminates_ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
    at 7276 (the wrapper copies its argument list verbatim).
  - recovery-side `CanonicalRecoveryTailInputs.ofForwardRecoveryInputsCanonicalRepresentations`
    — Recovery.lean:4069 (9-arg list matches).
- Added-line forbidden-token grep clean (`sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`), `git diff --check` clean.

## Current frontier

`HexBerlekampZassenhausMathlib/Recovery.lean` is **hard-red on
origin/main** upstream of this purely-additive theorem. `lake build
HexBerlekampZassenhausMathlib.Recovery` fails with:

```
1139  unfold scaledLiftedFactorProduct failed
1235  unfold scaledLiftedFactorProduct failed
1844  Application type mismatch
2167  timeout at whnf (200000 heartbeats)
2131  timeout at isDefEq
2180  (kernel) unknown constant factorFast_ne_none_of_forwardInputs_on_schedule
```

These are the in-flight scale→dilate and henselLiftData→toMonicLiftData
remodel residue, not anything #6454 touches (my diff is one theorem at
line 7385). The kernel error at 2180 is a cascade from the 2137/2167
defeq timeout, so the whole file fails to produce oleans and the
additive wrapper cannot be verified.

## Next step

Land the two upstream owners of the red, then this issue is a trivial
rebuild-and-close:

- **#7121** (OPEN, claimed) — remove unsound scale congruence, reroute
  recovery consumers to the dilate model (errors 1139/1235/1844 +
  IntReductionMod 4097).
- **#7122** (OPEN, has-pr #7126 CONFLICTING) — transport factorFast
  schedule recovery from `henselLiftData` to `toMonicLiftData` (errors
  2131/2167/2180).

Once both land and `lake build HexBerlekampZassenhausMathlib` is green:
the wrapper on branch `agent/0e05f2a5` (commit `89c4ac96`, rebased on
current main) builds and closes #6454. No re-implementation needed.

## Blockers

Environmental, not a plan defect: Recovery.lean does not compile on
origin/main. #6454's stale deps (#6804/#7024/#7036/#7037/#7084/#7110)
all closed — #7110 in particular was closed COMPLETED but only produced
a doc premise-diagnosis (#7123), re-scoping the actual code fix into
#7121/#7122 — which is why #6454 incorrectly surfaced as unclaimed.
Re-pointed `depends-on: #7121, #7122` and skipped to `replan`.
