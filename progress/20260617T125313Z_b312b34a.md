# Progress - 2026-06-17 (session b312b34a, review #7750)

## Accomplished

Claimed #7750 and audited the Berlekamp-Zassenhaus scalar/multiplicity
conformance surface against the SPEC contract. Posted a PASS verdict comment on
#7750 with file:line evidence:
- `HexBerlekampZassenhaus/Conformance.lean` has an explicit
  `factorizationEdgeCases` table for zero, signed constants, `X`, `-X`,
  `X^2`, repeated roots, non-unit content, and negative repeated/content cases.
- `factorizationCaseMatches` compares the whole `Factorization` record before
  checking product preservation, so scalar and multiplicity defects are caught
  directly.
- `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl` serializes the same
  scalar and `(factor, multiplicity)` edge expectations.

Verification:
- `lake build HexBerlekampZassenhaus.Conformance` passed.
- `lake build HexBerlekampZassenhaus.CrossCheck` passed when rerun serially.

## Current frontier

The requested review deliverable is complete. Product-only guards remain in
some broader conformance and cross-check paths, but the dedicated edge-case table
covers the scalar/multiplicity clauses this issue asked about.

## Next step

Optional future coverage: add one non-edge fast/slow cross-check that compares a
full content-bearing repeated `Factorization` shape instead of only product
preservation.

## Blockers

None. An initial parallel build of `Conformance` and `CrossCheck` raced on an
empty Lean setup JSON for `HexBerlekamp.Basic`; the serial rerun completed
cleanly. The worktree still has a pre-existing unrelated `.claude/CLAUDE.md`
modification that was not touched.
