# Phase 6 review: Nat.Prime / Fermat API surface (issue #6182)

## Accomplished

- Added `reports/hex-arith-nat-prime-api-phase6-review.md`, the
  Phase 6 review of `HexArith/Nat/Prime.lean`. The report covers
  the public `choose` / `Prime` / Fermat / `isPrimeTrial` surface
  and lists concrete findings, residual-risk notes, and a
  no-follow-up cluster.
- Filed four follow-up issues for the actionable findings:
  - #6187 `HexPolyFp Phase 6: drop duplicated Pascal-recurrence
    helpers in SquareFree` — `HexPolyFp/SquareFree.lean:262-284`
    privately re-derives `choose_eq_zero_of_lt` and `choose_self`.
  - #6188 `HexArith Phase 6: expose Prime.coprime_of_not_dvd as
    public helper` — `HexModArith/Prime.lean:69-77` duplicates the
    private `coprime_of_not_dvd` body.
  - #6189 `HexArith Phase 6: expose Prime.dvd_mul as iff to match
    Mathlib shape` — current `dvd_mul` is forward-only; Mathlib's
    sibling is iff.
  - #6190 `HexArith Phase 6: docstring private combinatorial
    scaffolding in Nat.Prime` — `chooseTerm`, `chooseSum`,
    `chooseSum_succ_row`, `add_pow_chooseSum`, `chooseSum_prefix_mod`,
    `add_pow_prime_mod_of_choose_dvd`, `pow_prime_mod_from_add_pow`,
    `choose_succ_mul_eq`, `choose_prime_dvd_from_mul_identity`
    have no docstrings.

## Current frontier

- `HexArith` Phase 6 still owes the API polish from #6168 (wide-word
  reconstruction-lemma automation tuning) and the new #6188 / #6189
  / #6190 follow-ups on the prime surface.
- `HexPolyFp` Phase 6 picks up #6187 as an isolated cleanup.

## Next step

Land the PR for #6182 and let the four follow-ups flow through the
queue. The `libraries.yml[HexArith].done_through` bump waits on all
HexArith Phase 6 follow-ups (existing #6168 plus the new
#6188 / #6189 / #6190) per the parent issue's out-of-scope note.

## Blockers

None. `lake build HexArith` is green, `python3 scripts/check_dag.py`
is clean, `git diff --check` is clean, and no forbidden tokens
(`sorry`, `axiom`, `native_decide`, `TODO`, `FIXME`) were introduced.
