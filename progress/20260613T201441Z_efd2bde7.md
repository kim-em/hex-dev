# Progress: #6928 — review PR #6927 (map-to-ZMod resultant wrapper)

Review session. No source changes; posted an approval-verdict PR comment on
#6927 because same-account formal approval is not available in this agent setup.

## Accomplished

Reviewed PR #6927 against #6849 and the already-landed #6857/#6858 resultant
column-reduction infrastructure.

Findings:

- Verdict: approve. The public theorem `pow_dvd_resultant_of_map_dvd` preserves
  the needed map-to-`ZMod (p^k)` input shape and proves
  `((p ^ (k * d) : Nat) : ℤ) ∣ Polynomial.resultant f g`.
- The private `exists_divByMonic_witness` is the right replacement for the
  degree-agnostic witness extractor here: monic division gives the exact
  `f = q * (f /ₘ q) + C n * s` shape and cofactor degree control consumed by
  `commonFactor_dvd_resultant_of_monic_cofactor`.
- The strengthened hypotheses are justified by the current infrastructure.
  `f.Monic` supplies the unit pivot after swapping to `(g, f)`, and
  `d ≤ f.natDegree`, `d ≤ g.natDegree` are the side conditions needed for the
  swapped column-reduction call and the monic-division degree equalities.
- Downstream, the bad-vector layer uses `inputPolynomial` as the first
  resultant argument, and the Hensel/toMonic substrate tracks monic lifted
  factors. The remaining consumer still needs to provide input monicity and the
  two modular divisibility facts, but the theorem does not weaken the directive
  chain.

Verified locally on PR head:

- `lake build HexBerlekampZassenhausMathlib.Resultant` succeeds. Warnings are
  pre-existing `Resultant.lean` deprecation/unused-simp warnings plus
  pre-existing `HexPolyZ.Basic` linter warnings.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` is clean.
- Added Lean source lines contain no `sorry`, `admit`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`; the only whole-PR scan hit was the PR's
  own progress note stating that this scan was clean.

PR comment posted:
https://github.com/kim-em/hex/pull/6927#issuecomment-4699665859

## Current frontier

PR #6927 is locally verified and has an approve verdict. At review time,
GitHub showed `build` and `conformance` pending, with `build-macos` green.

## Next step

Let CI finish and merge #6927 if the remaining checks pass.

## Blockers

None for this review.
