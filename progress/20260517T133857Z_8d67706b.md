# 20260517T133857Z — work session 8d67706b (#4711 stale, #4715 review)

## Accomplished

Claimed #4711 first because it was the oldest unclaimed issue and blocked the
HO-43/HO-44 directive chain. Verified it was stale against current
`origin/main`: `HexGramSchmidtMathlib/Int.lean` already exists and is imported
from `HexGramSchmidtMathlib.lean`, the named Round 3 declarations are in the
bridge file, `git diff --stat origin/main -- HexGramSchmidt/Int.lean
HexGramSchmidtMathlib/Int.lean HexGramSchmidtMathlib.lean` is empty, and
`rg -n 'Matrix\.bareiss_eq_det' HexGramSchmidt HexGramSchmidt.lean` returns no
hits. Released #4711 with `coordination skip` as stale.

Claimed #4715 and completed the Factorization API closure-readiness audit for
directive #2637. Posted the audit comment on #2637:
https://github.com/kim-em/hex/issues/2637#issuecomment-4470847605

The audit found the Factorization record, signed scalar, multiplicity
collection, public entry points, and exact product theorem surface are present.
The remaining API closure blockers are the public irreducibility holes:
`Hex.ZPoly.isIrreducible_iff` in `HexBerlekampZassenhaus/Basic.lean` and
`factor_irreducible_of_nonUnit` in `HexBerlekampZassenhausMathlib/Basic.lean`.
Those are already tracked by #4172 and #4170 respectively. A duplicate
follow-up issue #4723 was created during the audit after a coordination warning,
then closed as duplicate once #4170/#4172 were re-read.

Verification run:

- `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `rg -n '^\s*axiom\b|\bnative_decide\b' HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
- `rg -n '^\s*(all_goals\s+)?sorry\b' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhausMathlib/Basic.lean`

The build and DAG check passed. The no-axiom/no-`native_decide` grep found no
real violations. The `sorry` grep reports the expected remaining BZ API holes
plus two separate HO-1 analytic substrate holes in
`HexBerlekampZassenhausMathlib/Basic.lean`.

## Current frontier

#2637 should stay open. Its Factorization record/product/signed-scalar/
multiplicity API portion is implemented, but the directive is not closure-ready
until #4170 and #4172 are discharged and #4716 completes the separate
conformance/oracle audit.

## Next step

Pick up #4716 for the conformance/oracle side of #2637, or continue the blocked
irreducibility chain by unblocking #4170/#4172 through their listed
dependencies.

## Blockers

#4170 is blocked on #2567. #4172 is blocked on #4170, #4192, #4193, #4194, and
#4173. No blocker remains for the #4715 review itself.
