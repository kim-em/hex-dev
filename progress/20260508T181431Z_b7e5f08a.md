## Accomplished

- Claimed feature issue #2716 for restoring HexArith Phase-4 performance evidence after the audit rollback.
- Added `HexArith.phase4.input_families` metadata in `libraries.yml` for modular multiplication chains, word-sized `powMod`, and bounded-word extended-GCD batches.
- Ran all six `HexArith/Bench.lean` scientific registrations and exported the machine-readable result to `reports/bench-results/hex-arith-b161a1b1626f.json`; every registration was consistent with its declared `n` model.
- Added `reports/hex-arith-performance.md` with bench targets, verdicts, comparator-ratio notes, profile status, and a remaining Concern.
- Added `reports/README.md` describing overwrite-on-rerun report semantics.
- Filed follow-up issue #2752 for the missing Phase-4 profile coverage and attribution review.

## Current frontier

- HexArith has traceable benchmark verdicts and structured input-family metadata, but it is not re-promoted to `done_through: 4` because profile coverage is still absent.
- A local `samply record --save-only` attempt produced no sampled stacks for the benchmark child process, so the report intentionally avoids claiming profile percentages.

## Next step

- Complete #2752 by capturing one representative profile for each HexArith input family, updating the report's §Profile section with leaf-cost and inclusive-cost analysis, and only then bumping `HexArith.done_through` back to 4 if Concerns are empty.

## Blockers

- None for the metadata/report snapshot in #2716.
- Re-promotion is blocked on profile data and attribution review tracked by #2752.
