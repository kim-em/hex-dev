# Progress - 2026-06-16 (session 5b860422, one-shot #7641)

## Accomplished

Claimed #7641 (feat: prove tight true-factor CLD norm certificate, critical-path)
via `pod once`. Added the tight true-factor CLD norm certificate reduction layer
to `HexBerlekampZassenhausMathlib/Lattice.lean`, parallel to the existing loose
certificate (`normBound_of_colBound` / `cldTailSq_le_of_colBound`).

New declarations:

- `TrueFactorCLDTightNormBound L S` — the four-times-stronger certificate
  `4·‖trueFactorCLDVector L S‖² ≤ bhksCutRadiusSq4 L`, equivalently the
  design-radius bound `‖v‖² ≤ factorCount + coeffWidth·factorCount²/4`
  (= `bhksCutRadiusSq4 / 4`). This is the bound the prefix cut of #7640 needs,
  because the retained rows sit at the design radius `r` where
  `bhksCutRadiusSq4 = 4·r²`; the loose `‖v‖² ≤ bhksCutRadiusSq4` is `4×` too weak.
- `TrueFactorCLDTightNormBound.toNormBound` — tight ⇒ loose (`4x ≤ R`, `x ≥ 0`
  forces `x ≤ R`), so the tight certificate feeds any loose consumer.
- `cldTailSq4_le_of_tightColBound` — `4·tail ≤ coeffWidth·factorCount²` from the
  tight per-column shape `2·|col j| ≤ factorCount` (i.e. `|col j| ≤ factorCount/2`,
  exactly the column shape the issue proposes). Uses `4·z² = (2·z)²` and
  `(2·z).natAbs = 2·z.natAbs` to reuse `intSq_le_of_natAbs_le`.
- `trueFactorCLDTightNormBound_of_cldTail_four_mul_sq_sum_le` — reduces the tight
  certificate to the tight tail bound: `first ≤ factorCount` (structural via
  `project_eq`) gives `4·first ≤ 4·factorCount`, and adding the tight tail yields
  `4·‖v‖² ≤ 4·factorCount + coeffWidth·factorCount² = bhksCutRadiusSq4`.
- `tightNormBound_of_colBound` — the headline producer: tight certificate from
  the tight per-column bound, the strengthened analog of `normBound_of_colBound`.

## Current frontier

The certificate is supplied at full strength (no weakening of the target
inequality) and conditional on the tighter per-column high-bit estimate
`2·|Σ i ∈ S, cldRows[i][j]| ≤ factorCount`. This mirrors the loose certificate,
which is itself conditional on the per-column bound `|col j| ≤ factorCount` and
whose `hcol` is likewise not discharged anywhere — both bounds are injected
hypotheses isolating the genuinely-missing analytic estimate.

The premise is sound (not unsoundly typed, not vacuous): the design radius
`bhksCutRadiusSq4 = 4·factorCount + coeffWidth·factorCount²` is itself built from
the `|col| ≤ factorCount/2` assumption (first block `≤ factorCount`, tail
`≤ coeffWidth·factorCount²/4`), so the column shape the issue proposes is exactly
the design assumption. I did not attempt the deep psiCut high-bit analysis that
would discharge it: each `psiCut` is an *integer*, so a per-term `≤ 1/2` bound is
impossible — the tight bound must come from cancellation/structure specific to
true-factor supports, a separate (large) analytic obligation.

## Next step

Discharge the tight per-column hypothesis `2·|col j| ≤ factorCount` for true
lifted-factor supports via a high-bit `psiCut` magnitude analysis on the
true-factor congruence surface (`Hex.cldCoeffs`, `Hex.psiCut`,
`cldQuotientMod`). This is the residual analytic prerequisite; once proved it
plugs directly into `tightNormBound_of_colBound`.

## Blockers

None for the landed reduction layer. `lake build
HexBerlekampZassenhausMathlib.Lattice` clean, `python3 scripts/check_dag.py`
exit 0, `git diff --check` clean, no new sorry/axiom/native_decide/TODO/FIXME.
Note: the worktree carries pre-existing unrelated `.claude/*` modifications from
before this session; not touched or staged.
