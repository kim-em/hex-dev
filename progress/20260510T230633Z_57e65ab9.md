# 2026-05-10 23:06 UTC - twenty-ninth Phase 4/5 checkpoint (57e65ab9)

## Accomplished

Claimed summarize issue #3174 from a clean `agent/57e65ab9` worktree on
`origin/main` and recorded this checkpoint. The pre-existing pod-managed
`.claude/CLAUDE.md` modification and the untracked `.claude/commands/`
and `.claude/skills/` directories were left untouched. No source code
was changed.

Since the previous summarize issue #3089 closed at 2026-05-10T19:19:17Z,
29 PRs have merged in roughly three hours forty minutes of focused work
(d4a2e4a, last merge, landed at 2026-05-10T22:56:45Z). The merged work
groups into six reasonably distinct streams.

### HexPolyFp Yun / square-free decomposition

- Yun reachable layer: square-free tail divisibility helpers (#3096),
  normalized square-free factor gcd theorem (#3105), reachable Yun
  repeated coprime helper (#3115), Yun residual derivative bridge
  (#3135), Yun residual completion bridge (#3159).
- Test guard: shifted square-free multiplicity regression test (#3136).

### HexPoly content / Gauss

- Primitive content layer: `content_scale_int` and
  `rational_associate_primitive_unit` (#3103), primitive content divisor
  helper (#3134), and two PRs that expose content divisibility helpers
  for downstream HexPolyZ consumers (#3113, #3120).

### HexMatrix.RREF correctness

- Loop invariants for the executable `rrefLoop`: RREF transform loop
  invariant (#3114), RREF transform inverse invariants (#3116), row
  operation inverse preservation (#3097), eliminateColumn transform
  preservation (#3101), RREF rank pivot shape (#3119), and column tuple
  length transport supporting the loop's pivot bookkeeping (#3128).

### HexMatrix / HexMatrixMathlib determinant chain

- Suffix-form expansion completion: align suffix determinant RHS fold
  (#3106), prove suffix determinant expansion (#3142), specialize
  determinant expansion to Gram matrices (#3162).
- Mathlib bridge: prove Mathlib Bareiss determinant bridge (#3132),
  bridge nullspace basis to Mathlib kernel (#3104).
- HexMatrixMathlib Phase 4 unblock pair: doc report of the row-op
  benchmark Phase 4 blocker (#3148), then the fix removing the blocker
  in the direct Mathlib benchmark path (#3152). The library's
  `done_through` is still `3` pending a successful rerun.

### HexBerlekampZassenhaus core capstone chain

- Bridge factorizationOfFactors product to filtered array polyProduct
  (#3090, formerly issue #3084).
- Core factor product preservation under bound hypothesis (#3095,
  formerly issue #3086). Both issues identified as critical-path at the
  previous checkpoint have now landed.

### Phase 5 sign-offs

- HexArith Phase 5 verification (#3139): `done_through: 4 -> 5`.
- HexModArith Phase 5 mark (#3149): `done_through: 4 -> 5`.
- HexGF2 Phase 5 mark (#3150): `done_through: 4 -> 5`.

### Two additional PRs in this window

- HexMatrix.RREF canonical column preservation helpers (#3164), the
  operation-level slice of the original #3111 / #3156 decomposition.
- The level-indexed Yun helpers (#3165) decomposition is in flight in
  PR #3172 and is not counted here.

### Quality metrics at this checkpoint

- `python3 scripts/status.py`: HexArith, HexModArith, and HexGF2 now
  enter Phase 6 (proof polishing) per their `done_through: 5` bumps in
  this window. HexBerlekampZassenhaus remains formally at Phase 1
  (`done_through: 0`) while the SPEC-driven implementation,
  conformance, and bench streams continue to land inside Phase 1's
  loop. HexBerlekampZassenhausMathlib remains at Phase 2
  (`done_through: 2`). HexLLL, HexPolyFp, HexGfqField,
  HexMatrixMathlib, HexModArithMathlib, HexGramSchmidtMathlib,
  HexPolyZMathlib, HexLLLMathlib, HexBerlekampMathlib, HexHenselMathlib,
  HexGF2Mathlib, and HexGfqMathlib are at Phase 4 ("performance &
  benchmarking" loop). The remaining libraries sit at Phase 5
  ("implementation work loop"). Planned-only libraries (HexRoots,
  HexRootsMathlib, HexResultant, HexResultantMathlib, HexNumberField,
  HexNumberFieldMathlib) remain at `done_through: 0`.
- `python3 scripts/check_dag.py`: passed (no output, exit 0).
- Lean `sorry` lines: 162 (down from 167 at the previous checkpoint;
  net -5 over 29 PRs). The smaller absolute drop versus the previous
  window reflects that several merged PRs in this window added new
  lemmas to support the loop invariant scaffolding rather than
  retiring existing proof holes. Heaviest residual concentrations
  remain in `HexBerlekampMathlib/Basic.lean` (21),
  `HexGramSchmidt/Update.lean` (18), `HexLLL/Basic.lean` (12),
  `HexHenselMathlib/Correctness.lean` (12), `HexGF2Mathlib/Field.lean`
  (12), `HexGF2Mathlib/Basic.lean` (11),
  `HexBerlekampZassenhausMathlib/Basic.lean` (11),
  `HexGfq/CrossCheck.lean` (10), `HexGramSchmidt/Int.lean` (8),
  `HexGfqMathlib/GF2q.lean` (8), `HexGfq/EmitFixtures.lean` (8),
  and `HexGfqMathlib/Basic.lean` (7). The HexBerlekampZassenhaus
  capstone chain's core file is now at 3 sorries in
  `HexBerlekampZassenhaus/Basic.lean` (same as previous checkpoint),
  and the Mathlib bridge sits at 11 (also unchanged).
- Lean `axiom` declarations: 0.
- `native_decide` textual hits in `.lean` files: 0. The
  `HexArith/CrossCheck.lean` comment counted at the previous
  checkpoint has been removed (this file underwent edits as part of
  the HexArith Phase 5 verification work, #3139). SPEC and PLAN
  references to `native_decide` as a banned tactic still exist by
  design.

### Open agent-plan frontier at the checkpoint

Unclaimed (work pickup queue):

- #2840 HexMatrix group injective column tuples into squared minors -
  oldest in the queue; #2855 closed in this window, so the cyclic
  dependency that the previous checkpoint flagged is now linearised.
- #2953 HexBZ core BHKS lattice basis independence - previously
  blocked on #2855, now unblocked since #2855 closed.
- #3122 HexPolyFp pthRoot tail cross-coprimality - depends on the
  claimed #3155 derivative-active Yun residual completion provider.
- #3153 HexPolyFp scaled square-free weighted product reconstruction.
- #3161 HexPolyFp switch square-free Yun execution to scaled
  multiplicities.
- #3163 HexMatrix.RREF lift canonical-column helpers into rrefLoop
  invariant - the residual scope of the #3111 -> #3156 decomposition.

Claimed and in flight:

- #3174 (this checkpoint).
- #3155 HexPolyFp derivative-active Yun residual completion provider.

With open PRs (`has-pr`):

- #3140 HexBZ verify Phase 1 scaffolding and bump status, PR #3143
  (critical-path).
- #3167 HexPoly primitive content Bezout certificate, PR #3173.
- (PR #3172 for the level-indexed Yun helpers parent issue #3165,
  which is currently in `replan` state rather than `has-pr`.)

Blocked:

- #3171 HexPolyFp switch Yun wrappers to scaled level semantics -
  `replan` and blocked, depends on #3165.
- #3169 HexPoly assemble primitive left-mul coefficient divisibility -
  depends on #3168.
- #3168 HexPoly primitive polynomial scalar annihilator lemma.
- #3166 HexPolyFp migrate Yun pairwise and square-free invariants to
  levels.
- #3158 HexMatrix.RREF package final `rref_isRREF` shape fields -
  blocked on #3163 (with #3156 closed and #3157 still tracking the
  no-pivot zero-column slice).
- #3154 HexBZ re-review post-Factorization scaffold for Phase 2.
- #3109 HexPolyZ signed primitive square-free reassembly corollaries.
- #3093 HexBZ core Z-level integer reassembly using HexPolyZ signed
  reassembly (Gauss content multiplicativity #3092 closed in this
  window; remaining dependencies prevent immediate pickup).
- #3087 HexBZ core `factor_product_of_bound` capstone - now waiting
  on #3093 / #3109 after #3084 / #3085 / #3086 landed in this window.
- #2917 HexPolyFp square-free pairwise-coprime theorem.
- #2841 HexMatrix integer Gram determinant nonnegative - depends on
  #2840.
- #2833 HexBZ Mathlib Factorization product and uniqueness capstones -
  depends on #2803.
- #2803 HexBZ Mathlib factor-entry irreducibility from executable
  factor invariant.
- #2679 HexGramSchmidt.Int specialize Gram determinant nonnegativity
  bridge.
- #2674 HexGramSchmidt.Update scaled size-reduction pivot after
  determinant bridges.

Human-oversight (all still open and `blocked`):

- #2564 HO-1 BZ recombination wrong lattice (van Hoeij CLD rewrite).
  No direct progress this period.
- #2565 HO-2 BZ adversarial conformance.
- #2566 HO-3 BZ `Bench.lean`.
- #2567 HO-4 BHKS Theorem 5.2 termination. Some capstone bridge work
  landed via #3090 / #3095, but the leaf theorem still cannot reduce
  to a leaf yet; it depends on #3087, which is now blocked on #3093.
- #2637 BZ Factorization record + sign/multiplicity bugs.

### Open PRs at the checkpoint

- PR #3173 `feat: add primitive content Bezout certificate` (closes
  #3167) - merge BLOCKED, conformance / build-macos SUCCESS, build
  in progress.
- PR #3172 `feat: add level-indexed Yun helpers` (part of the #3165
  partial slice) - merge BLOCKED, conformance / build-macos SUCCESS,
  build in progress.
- PR #3143 `HexBZ: verify Phase 1 scaffold` (closes #3140,
  critical-path) - merge BLOCKED, build / build-macos SUCCESS,
  conformance in progress.
- PR #2656 `spec: add hex-real-roots and hex-sturm` - still draft, all
  checks SUCCESS, planned spec-only PR.

## Current frontier

The single most-changed lane in this window is **HexMatrix.RREF**: six
loop-invariant PRs (#3114, #3116, #3097, #3101, #3119, #3128) plus the
canonical-column helpers (#3164) now provide the operation-level pieces
the final `rref_isRREF` shape needs. The remaining gap is #3163 (lift
the canonical-column helpers into the `rrefLoop` invariant) and #3157
(the no-pivot zero-column corollary), both of which gate #3158 (final
shape-field packaging). Once those three close, the `rref_isRREF`
broad `sorry` retires.

The **HexBerlekampZassenhaus core capstone chain** advanced
substantially: #3084, #3085, #3086, #3092 all closed in this window.
The next gate on #3087 is now #3093 (Z-level integer reassembly), which
in turn is blocked on #3109 (HexPolyZ signed primitive square-free
reassembly corollaries). #3109 is the new bottleneck for the BZ core
capstone.

The **HexMatrix determinant / Gram nonnegativity** chain
(#3076 -> #2855 -> #2840 -> ...) flagged as a cyclic blocker in the
previous checkpoint is now linearised: #3076 and #2855 closed in this
window. #2840 is unclaimed and is the new head of the chain
(#2840 -> #2841 -> #2679 -> #2674).

The **HexPolyFp square-free / Yun** lane is in the middle of a
mechanical migration to scaled / level-indexed semantics
(#3165, #3166, #3171, #3122, #3153, #3161). The level-indexed helper
slice is in PR #3172; the rest of the migration is unclaimed.

The **HexMatrixMathlib Phase 4** bump is unblocked code-wise (#3152
landed) but the library's `done_through` is still `3` pending a Phase 4
benchmark rerun against the fixed code. This is administrative work,
not new proof effort.

## Limitations and honest gaps

- HexBerlekampZassenhaus is still formally at Phase 1
  (`done_through: 0`). The capstone chain has shortened
  significantly in this window, but the leaf
  `factor_product_of_bound` (#3087) is now blocked on a different
  precondition (#3093 / #3109) rather than truly unblocked. The
  switch of bottlenecks is real progress but the library has not
  cleared Phase 1.
- The HexMatrixMathlib Phase 4 bump is gated by a benchmark rerun,
  not by code. The `done_through: 3` line in `libraries.yml`
  understates the engineering state of this library; the next planner
  needs to dispatch the Phase 4 verification rerun rather than
  treating this as an open implementation gap.
- The `sorry` count drop (-5 over 29 PRs) is modest because much of
  this window's work installed loop invariants that retire existing
  proof holes only when consumed by later PRs (notably #3158 and the
  Phase 1 verification stream for HexBerlekampZassenhaus). The
  effective reduction in proof debt is larger than the 5-line delta
  suggests, but the textual `sorry` count is the only honest metric.
- The HO-directed rewrites (#2564 in particular) remain logically
  unaddressed. The BZ core capstone chain shortening does not help
  HO-1 because the lattice choice is independent of the recovery
  bridge.
- The previously-named cyclic blocker on the Gram determinant chain
  (#3076 / #2855 / #2840) has been resolved by closures, not by
  re-cutting; the chain is now linear and unblocked at the head.

## Next step

Fresh feature workers should prefer, in order:

1. #3163 (lift canonical-column helpers into `rrefLoop` invariant) -
   one of two remaining gates for #3158 and therefore for retiring
   the broad `rref_isRREF` `sorry`.
2. #3157 (no-pivot zero-column corollary, the second gate for #3158).
3. #3109 (HexPolyZ signed primitive square-free reassembly
   corollaries) - the new bottleneck for the BZ core capstone
   #3087.
4. #2840 (HexMatrix group injective column tuples into squared
   minors) - head of the now-linearised Gram determinant chain;
   unblocks #2841 / #2679 / #2674.
5. #2803 (factor-entry irreducibility) - still the tightest
   bottleneck on the BZ Mathlib factorization capstone #2833.
6. #3122 / #3153 / #3161 / #3166 - the HexPolyFp scaled-multiplicity
   migration, all unclaimed except the in-flight #3155 / PR #3172
   slice.

Repair sessions should watch PRs #3173, #3172, and #3143; they are all
merge-BLOCKED only on in-progress checks at checkpoint time and should
land in the next CI cycle.

A planner cycle should:

- Dispatch the HexMatrixMathlib Phase 4 benchmark rerun against the
  post-#3152 code so the `done_through: 3 -> 4` bump can actually
  happen.
- Inspect whether #3087 can be re-cut to depend on #3109 directly,
  now that #3084 / #3085 / #3086 / #3092 have all closed.

## Blockers

None for this checkpoint. The pre-existing pod-managed
`.claude/CLAUDE.md` modification and the untracked `.claude/commands/`
and `.claude/skills/` directories were left untouched.
