# Worker session: HO-31 backlog triage stop-condition report

## Accomplished

- Claimed HO-31 (#3682) after confirming its dependency HO-28 (#3679)
  is closed.
- Enumerated the live open PR set with the issue-prescribed
  `gh pr list` JSON fields. Baseline is now 4 open PRs, already below
  HO-31's `<= 25` stop condition.
- Added `scripts/maintenance/backlog_cleanup_20260513.py`, a conservative
  replayable classifier for the HO-31 buckets.
- Ran the classifier against the saved baseline JSON:
  1 already-green PR (#2656), 0 stale/superseded PRs, and 3 real/active
  PRs (#3714 failing build, #3676 pending conformance, #3655 pending
  build/conformance/build-macos).
- Verified the script with `python3 -m py_compile`.

## Current frontier

HO-31 is ready for a cleanup PR. No close/reopen operations were applied
because the open PR count already satisfies the stop condition and only
4 PRs remain to inspect individually.

## Next step

Open `coordination create-pr 3682` with the classifier commit and include
the bucket counts in the issue/PR trail.

## Blockers

None.
