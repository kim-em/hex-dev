# 2026-05-16T15:31Z — #4496 HexGfqRing Phase 6 linearNSmul / nsmul.go bridge review

## Accomplished

Claimed #4496 (the final unreviewed Phase 6 surface in
`HexGfqRing/Operations.lean`) and landed the review polish for the
private `linearNSmul` / `nsmul.go` binary-decomposition bridge.

Concrete changes inside lines 614-720 of
`HexGfqRing/Operations.lean`:

- Added docstrings to the seven previously-undocumented private
  declarations in the cluster: `linearNSmul` (the recurrence form
  itself), `linearNSmul_zero`, `linearNSmul_succ`,
  `linearNSmul_double`, `linearNSmul_double_add_one`,
  `nsmul_go_eq_acc_add_linearNSmul`, `nsmul_eq_linearNSmul`. Each
  names the specific downstream consumer (recurrence base/step,
  binary-decomposition gateway, strong-recursion bridge, final
  public bridge), as required by deliverable 2.
- Removed the four `_raw` add-law wrappers
  (`linearNSmul_add_assoc_raw`, `linearNSmul_add_comm`,
  `linearNSmul_add_zero_raw`, `linearNSmul_zero_add_raw`). Each was
  a one-liner `apply ext; exact repr_X` shim shadowed by the
  existing public `repr_add_assoc` / `repr_add_comm` /
  `repr_add_zero` / `repr_zero_add`. The five call sites now use
  `ext (repr_X _ _ _)` directly, matching deliverable 3's example.
- Tightened `linearNSmul_double`'s succ case: the `2 * (n+1)`
  rewrite now produces `2*n + 1 + 1` directly (rather than
  `2*n + 2` followed by a `change` to expose the `?n + 1` pattern),
  so a single `rw` chain replaces the original
  rewrite-change-rewrite sequence.

The SPEC review (deliverable 1) confirms the cluster correctly
mediates between the binary-decomposition implementation `nsmul.go`
and the textbook recurrence shape `Lean.Grind.Semiring` axioms
consume: `linearNSmul` defines the textbook iterated sum,
`linearNSmul_double` / `linearNSmul_double_add_one` provide the
even/odd binary-decomposition gateways,
`nsmul_go_eq_acc_add_linearNSmul` is the strong-recursion lemma
closing the loop, and `nsmul_eq_linearNSmul` exposes the bridge
publicly for `nsmul_zero` / `nsmul_succ`. The SPEC's "Contents"
clause prohibits the textbook `n+1 ↦ pred + 1` recursion for the
implementation, which is honoured: `nsmul.go` runs binary
decomposition; `linearNSmul` is only a proof device.

Quality metrics: build green for `HexGfqRing.Operations` (1.1s),
`HexGfqRing HexGfqField HexGfq` (174 jobs, 332s for
`HexGfq.CrossCheck`). `scripts/check_dag.py` exit 0.
`git diff --check` exit 0. Sorry count unchanged at 82. No axioms.
No `native_decide`. No `TODO`/`FIXME`/`sorry` in
`HexGfqRing/Operations.lean`. `libraries.yml`
`HexGfqRing.done_through` unchanged at 5.

## Current frontier

- #4496 PR ready to land.
- With #4496, #4489 (PR #4495), and #4490 (PR #4493) all in flight,
  the narrow Phase 6 review surface of `HexGfqRing/Operations.lean`
  is fully covered. The Phase 6 exit-criteria pass (Mathlib-linter
  clean / docstring coverage / performance baseline) is the next
  natural step.

## Next step

Land #4496. The Phase 6 exit-criteria pass and a possible file
split (`Operations.lean` is currently 1160 lines) are appropriate
follow-up issues for a future planner.

## Blockers

None.
