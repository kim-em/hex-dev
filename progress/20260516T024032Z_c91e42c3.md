# 2026-05-16 02:40 UTC - thirty-fourth Phase 4/5 checkpoint (c91e42c3)

Claimed summarize issue #4369 and recorded this checkpoint covering the
merged PR wave since #4318 closed at 2026-05-15T21:32:46Z.

## Accomplished

Read the 23 PRs merged after #4318, the last 16 progress entries, the
current `coordination orient` frontier, and the still-open `agent-plan`
issue bodies in the HexBZ Mathlib coverage, HexBZ HO-4, and HO-43
chains.

The dominant change since the previous checkpoint is that the
HexBerlekamp matrix-bridge irreducibility composition shipped, and the
HexBZ HO-4 forward-recovery chain landed both a cap-separation
`factorFast` capstone and the parallel Mignotte canonical-indicator
wrappers needed for its symmetric companion. The BZ Mathlib coverage
chain decomposed once more around `LiftedFactorSubsetPartition`
prefix-none and integer-factor monicness, with most sub-issues now
either claimed or planned.

- **HexBerlekamp matrix-bridge irreducibility chain** closed end to
  end. #4320 / #4321 refactored `IsFixedSpaceKernelVector` onto
  Mathlib's default `Matrix.mulVec` instance and deduped the
  `basisSize` / `degree?` plumbing. #4326 proved
  `isFixedSpaceKernelPolynomial_iff_dvd_linearPow_sub_self`, the
  matrix → algebraic kernel iff. #4345 composed
  `irreducible_of_no_kernelWitnessSplit_squareFree` and
  `berlekampFactor_singleton_irreducible` on top of the iff, finishing
  the algebraic half of Berlekamp completeness for monic square-free
  inputs. Review #4332 audited the three-PR stack before #4345 landed.

- **HexBZ HO-4 forward-recovery chain** advanced from forward-recovery
  glue to a public capstone. #4346 added the matched-rest
  recombination-search bridge; #4347 proved
  `precisionForCoeffBound_spec` and the Mignotte termination wrappers
  (cap raised to 360 s as part of #4347's repair to handle GH-runner
  variance). #4349 added the matched-rest first-success witness; #4353
  added the matched-rest divisibility-to-`isSome` bridge. #4356 packaged
  cap-separation with the canonical B7 indicator array; #4358 derived
  primitivity and sign-normalisation from `Monic factor` alone. #4361
  added the symmetric Mignotte canonical-indicator wrappers, and #4364
  composed `factorFast_ne_none_of_capSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`,
  the cap-separation `factorFast` capstone. Review #4357 audited the
  in-tree resultant API for the bad-vector route and confirmed no
  bridge work is needed there. Open PR #4378 (issue #4368) lands the
  parallel Mignotte-precision `factorFast` capstone.

- **HexBZ Mathlib coverage chain** further decomposed and partially
  cleared. The previous-cycle target #4301 (recursive rest-search
  coverage) remains unclaimed and explicitly gated on the
  prefix-none-under-partition lemma plus a `Monic factor` witness at
  `cover_at_min`. The prefix-none deliverable from the previous
  checkpoint was split into #4365 (matched `subsetSplitsWithFirst`
  prefix characterization, closed after decomposition), #4366
  (partition structure from successful candidate quotient — now
  claimed), and #4367 (final assembler, blocked on #4366). The
  matched-prefix sub-chain produced #4370 (subset split mask
  converses, merged as #4375), with #4371 / #4372 still blocked on
  #4370's downstream pieces. The monic-factor capstone was split into
  #4373 (executable cofactor monic lemma, claimed), #4374 / #4376 /
  #4377 (multifactor + umbrella + integer-factor monic, all blocked
  on #4373).

- **HO-43 / HexLLL boundary**. #4327 finished the Round 2 `sizeReduce`
  relocation into `HexGramSchmidtMathlib/Update.lean`, adding three
  new `scaledCoeffs_rowAdd_*` packagings to `HexGramSchmidt/Int.lean`.
  #4337 decoupled `LLLState` executable data from a separate
  `LLLState.Valid` proposition, removing the dead `Hex.Matrix` cluster
  in `HexLLL/Basic.lean` and clearing the prerequisite for Round 3.
  #4336 recorded the HexLLL Phase 4 blocker as #4334.

- **HexBerlekamp infrastructure cleanup**. #4338 relocated
  `zmod64FieldOfPrime` into a new `HexPolyFp/PrimeField.lean`
  Mathlib-free home and deleted the duplicate copy in
  `HexBerlekampZassenhaus/Basic.lean`. #4343 made
  `FpPoly.linearPow_zero_of_pos` public in `HexPolyFp/Compose.lean`
  and removed the `_aux` copy in `HexBerlekamp/Basic.lean`.

- **Doc / SPEC**: #4316 decomposed an earlier matrix-bridge issue
  before #4320 / #4326 landed. #4189 finished densifying the HexLLL
  Isabelle bench ladder. PR #4347's repair also extended
  `SPEC/benchmarking.md` to record the 360 s CI hard cap with its
  small-runner-variance buffer.

## Current frontier

Open PRs:

- #4378 for #4368: Mignotte-precision `factorFast` capstone parallel
  to the cap-separation capstone landed in #4364.
- #2656: unrelated SPEC draft for hex-real-roots / hex-sturm.

Unclaimed `agent-plan` work (after this summarize claim):

- #4301: recursive BZ rest-search coverage. Effectively gated on
  #4367 (prefix-none assembler) and the monic-factor chain
  (#4373 → #4374 → #4376 → #4377); the issue body explicitly
  flags the open prerequisite.
- #4334: HexLLL Phase 4 inconclusive scientific benchmark verdicts.
  Two earlier attempts skipped this for host-contention reasons;
  per `SPEC/benchmarking.md` re-dispatch waits on quiet/dedicated
  hardware.

Claimed work:

- #4366: derive partition structure from successful recombination
  candidate quotient (BZ Mathlib coverage prefix-none chain).
- #4373: Monic cofactor lemma at `congr(g*h, f, m)` (BZ Mathlib
  monic-factor chain).
- #4369: this summarize task.

Blocked chains to keep visible:

- BZ Mathlib coverage prefix-none: #4366 (claimed) → #4367
  (blocked); #4371 → #4372 (both blocked on #4370 follow-ups, now
  that #4370 has merged the `check-blocked` cycle should unblock
  them shortly).
- BZ Mathlib coverage monic-factor: #4373 (claimed) → #4374 → #4376
  → #4377, then feeds back into the still-open #4301.
- BZ Factorization capstones beyond #4301: #4274 → #4006 → #4008 →
  #3987 → #3969 → #3970.
- HO-43 Round 3: #4337 (state validity decoupled) shipped, but the
  Round 3 relocation issue itself remains unclaimed/unplanned on
  current `main`.
- HO-44: deletion of `Hex.Matrix.bareiss_eq_det`'s orphan `sorry`
  still gated on HO-43 Round 3.
- HO-18 / HexLLL Phase 4 report: directive #3662 still gated on
  #4334.
- HO-1 / BZ Factorization record residuals: directives #2564 / #2637
  open at the directive layer.

`scripts/status.py` continues to report broad ready capacity across
Phase 4–6 libraries; the most active executable targets in this cycle
were HexBerlekamp Phase 4 (matrix-bridge composition shipped) and
HexBerlekampZassenhausMathlib Phase 3 (HO-4 capstone, BZ Mathlib
coverage decomposition). HexBerlekampZassenhausMathlib remains
`waiting on HexBerlekampZassenhaus.done_through ≥ 3`.

## Next step

Let the in-flight PR settle, then prioritize:

1. #4378 (Mignotte-precision `factorFast` capstone) merging and the
   BHKS Lemma 3.2 bad-vector leaf becoming the only unplanned HO-4
   leaf (the resultant audit in #4357 confirmed no bridge work).
2. The BZ Mathlib coverage chain landing #4366 → #4367 and #4373 →
   #4374/#4376/#4377, which together unblock the #4301 recursive
   coverage proof and the downstream #4274 → #4006 → #4008 → #3987 →
   #3969 → #3970 sequence to BZ Factorization capstones.
3. Re-dispatching #4334 (HexLLL Phase 4 verdicts) only when quiet or
   dedicated hardware is available per `SPEC/benchmarking.md`.

## Blockers

No blocker for this checkpoint.
