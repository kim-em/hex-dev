# 20260517T114129Z — work session 7b272065 (#4618 decomposition, #4674)

## Accomplished

Claimed #4618 after checking the unclaimed queue and confirming its dependency
#4617 was closed. Scoped #4618 and found it too large for one coherent session:
the requested theorem splits naturally into repeated-part primitivity, a
rational radical/divRadical power-divisibility bridge, and final Gauss descent.

Decomposed #4618 into:

- #4674: prove `normalizeForFactor_repeatedPart_toPolynomial_isPrimitive`;
- #4675: prove rational `repeatedPart` divides a power of `squareFreeCore`;
- #4676: descend the rational theorem to the final `Polynomial ℤ` statement.

Added the machine-readable `Decomposed into #4674, #4675, #4676` breadcrumb to
#4618 and released it with `coordination skip`.

Claimed and completed #4674 by adding
`IntReductionMod.normalizeForFactor_repeatedPart_toPolynomial_isPrimitive` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`. The proof mirrors the
existing square-free-core primitivity bridge, transporting executable product
primitivity to `Polynomial ℤ` and dividing out the square-free-core factor.

## Current frontier

#4675 is the next critical step for this chain. It needs the rational-side
bridge from the executable decomposition to Mathlib's
`radical`/`divRadical` API, using the #4617 char-zero gcd helper already in
`IntReductionMod.lean`.

## Next step

Claim #4675 and prove the rational theorem
`map repeatedPart ∣ (map squareFreeCore)^N`. A useful first helper is a local
Euclidean-domain theorem that `divRadical a` divides a power of `radical a` for
nonzero `a`.

## Blockers

None for #4675. #4676 is intentionally blocked on #4675.
