# Reconcile accepted PNT import sites

## Accomplished

- Reclassified four raw LeanCert import records whose exact enclosing workloads
  already have accepted package-owned rewrites: the Table4Ext ANT import, both
  BKLNW_a2 imports, and the LogTables tactic import.
- Attached the existing ordinary-kernel and conformance evidence to each import
  decision and refreshed only the inventory migration-record digest.
- Documented the exact import-site totals while preserving the distinction
  between bounded source rewrites and the still-pending general interfaces.
- Passed offline inventory validation, exact pinned-source regeneration,
  all 28 inventory unit tests, and static file checks.

## Current frontier

Six of sixteen raw import sites are accepted after localized rewrites and ten
remain pending. Two of six dependency interfaces are accepted and four remain
pending. The all-classified release gate therefore continues to reject the
inventory, now with 99 pending decisions.

## Next step

Replay this bookkeeping-only commit onto the final publication base after the
currently moving PNT branch settles.

## Blockers

None.
