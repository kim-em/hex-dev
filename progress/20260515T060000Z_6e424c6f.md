# 20260515T060000Z — exhaustive core count_ge / count_eq under irreducibility (#4128)

## Accomplished

- Added `exhaustiveCoreFactorsWithBound_factor_count_ge_of_irreducible` in
  `HexBerlekampZassenhausMathlib/Basic.lean`: under a per-factor `Irreducible
  (HexPolyZMathlib.toPolynomial factor)` hypothesis for every emitted factor,
  derives `normalizedFactors.card ≤ length` by applying the abstract
  `UFDPartition.normalizedFactors_card_le_length_of_irreducible_partition`
  after transporting product and irreducibility through `HexPolyZMathlib`.
- Added `exhaustiveCoreFactorsWithBound_factor_count_eq_of_irreducible`:
  combines `_count_le` (existing) with the new `_count_ge_of_irreducible`
  through `le_antisymm` to expose `length = card`.
- Added two convenience compositions —
  `exhaustiveCoreFactorsWithBound_factor_irreducible_of_irreducible`
  and `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_irreducible`
  — that route through the new equality and the existing
  `_irreducible_of_count` / `_zpolyIrreducible_of_count` theorems so
  downstream consumers (slow exhaustive branch) get a direct
  irreducibility/`Hex.ZPoly.Irreducible` API under the same irreducibility
  hypothesis #4149 will eventually supply via the default-precision coverage
  theorem.
- Verified `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`, `lake build
  HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, and `git diff
  --check`. No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

The exhaustive-core BHKS chain now mirrors the fast-core layer in
`HexBerlekampZassenhausMathlib/Basic.lean`:
- existing `_factor_count_le` upper bound,
- new `_factor_count_ge_of_irreducible` lower bound,
- new `_factor_count_eq_of_irreducible` equality,
- existing `_irreducible_of_count` / `_zpolyIrreducible_of_count` consumers,
- new `_irreducible_of_irreducible` / `_zpolyIrreducible_of_irreducible`
  convenience compositions.

The remaining gap for #4112 / #4006 is the open #4149 coverage theorem
that produces the irreducibility hypothesis from the
`HenselSubsetCorrespondenceHypotheses` / A1/A2 / default-precision package.

## Next step

A successor PR (after #4149 lands) should mirror
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`: a separate
exhaustive-branch file that uses #4149's coverage theorem to discharge the
irreducibility hypothesis and then calls the new
`_count_ge_of_irreducible` / `_count_eq_of_irreducible` wrappers to give the
unconditional count_ge / count_eq under A1/A2/default-precision.

## Blockers

None for this PR. #4149 remains open and blocks the deeper consumer that
turns the algorithm-side `HenselSubsetCorrespondenceHypotheses` into
the irreducibility witness this PR consumes.
