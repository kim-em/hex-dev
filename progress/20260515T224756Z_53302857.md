2026-05-15 22:47 UTC - HO-43 Round 3 prep (#4335)

## Accomplished

Claimed feature issue #4335 and cleared both `HexLLL/Basic.lean`
consumer clusters that blocked the HO-43 Round 3 relocation.

Deleted the dead `Hex.Matrix` public re-export cluster from
`HexLLL/Basic.lean`: `gramDet_eq_prod_normSq`, `gramDet_pos`,
`basis_normSq`, `normSq_latticeVec_ge_min_basis_normSq`, and the local
`gramSchmidtNormProduct` helper used only by that cluster.

Split executable `LLLState` data from its proof-facing interpretation:
`LLLState` now stores only `b`, `ν`, and `d`, while the former `ν_eq` and
`d_eq` proof fields live in the separate proposition `LLLState.Valid`.
This lets `LLLState.sizeReduceColumn` and `swapStep` remain executable
state updates without constructing bridge-bound proof fields in
`HexLLL/Basic.lean`. Added `LLLState.ofBasis_valid` for the canonical
constructor.

Adjusted local and bridge-facing APIs that previously read proof fields:
`potential_eq_gramDetProduct`, `swapStep_ν_eq`, `swapStep_d_eq`,
`HexLLLMathlib.lllState_ν_eq_coeffs`, and
`Hex.LLLState.sizeReduce_independent` now consume an explicit
`LLLState.Valid` witness where needed. Updated the conformance fixture
constructor to build the simplified executable record.

Removed the `hind` proof parameter from `lllAux`; it was only used to
thread proof-field-derived independence evidence through recursive calls.
The executable loop still requires the same public `lll` inputs.

## Current frontier

The required searches are clean:

- `rg -n 'Hex\.Matrix\.(gramDet_eq_prod_normSq|gramDet_pos|basis_normSq|normSq_latticeVec_ge_min_basis_normSq)'`
  returns zero hits.
- `rg -n 'GramSchmidt\.Int\.(gramDet_rowAdd_earlier|scaledCoeffs_rowAdd_)' HexLLL/Basic.lean`
  returns zero hits.
- `rg -n '^import .*Mathlib|Mathlib' HexLLL/Basic.lean` returns zero
  hits.

Verification completed:

- `lake build HexLLL`
- `lake build HexLLLMathlib`
- `lake build HexGramSchmidt HexGramSchmidtMathlib HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`

Build warnings are the pre-existing `sorry`/lint warnings in
`HexMatrix`, `HexGramSchmidt`, `HexLLL.Basic` termination scaffolding, and
`HexLLLMathlib.Basic`.

## Next step

Once this PR lands, unblock #4153 so HO-43 Round 3 can relocate the
bridge-bound `HexGramSchmidt/Int.lean` theorem chain, including
`gramDet_rowAdd_earlier` and the `scaledCoeffs_rowAdd_*` packagings.

## Blockers

None.
