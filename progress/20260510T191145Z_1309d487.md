# 2026-05-10 19:11 UTC — feature session #3092 (partial)

## Accomplished

- Claimed issue #3092 (HexPolyZ Gauss-style content multiplicativity)
  and reset the agent worktree onto `origin/main`.
- Updated `SPEC/Libraries/hex-poly-z.md` to admit the Gauss-style
  corollaries needed by the BZ Z-level reassembly chain: the
  primitive-product corollary, the rational-associate cancellation
  lemma, and the signed integer reassembly. The previous "Gauss's
  lemma is not needed in this library" wording is replaced with a
  precise statement of which corollaries live here.
- In `HexPoly/Euclid.lean`, proved `content_scale_int`:
  `content (scale c p) = Int.ofNat c.natAbs * content p`. This
  generalises `content_scale_neg_one`. Added private helpers
  `foldl_gcd_natAbs_mul_const_int` (gcd-of-products under fold),
  `foldl_gcd_natAbs_of_all_zero`, `trimTrailingZerosList_cons_int`,
  `all_zero_of_trimTrailingZerosList_nil`, and
  `foldl_gcd_natAbs_trim_eq` (trim-zeros invariance of the fold).
- Made `trimTrailingZerosList`, `trimTrailingZerosList_getD`, and
  `trimTrailingZerosList_normalized` non-private in `HexPoly/Dense.lean`
  so the Euclid-side fold-trim invariance lemma can name the
  trimming function directly.
- In `HexPolyZ/Basic.lean`, proved `rational_associate_primitive_unit`:
  if `p, q : ZPoly` are both primitive and nonzero with
  `toRatPoly p = scale unit (toRatPoly q)` for some `unit : Rat`, then
  `unit = 1 ∨ unit = -1`. The proof clears denominators using
  `Rat.div_mul_cancel`, lifts to an integer equation
  `scale (unit.den : Int) p = scale unit.num q`, takes contents
  via `content_scale_int` with both primitive sides to deduce
  `unit.den = unit.num.natAbs`, then uses the `Rat.reduced` invariant
  `gcd unit.num.natAbs unit.den = 1` to pin `unit.num.natAbs = 1`,
  hence `unit = ±1` via `Rat.ext`.
- Verified `lake build HexPoly HexPolyZ HexBerlekampZassenhaus`,
  `python3 scripts/check_dag.py`, and `git diff --check`. No new
  `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`.
- Created sub-issue #3102 to track the two remaining deliverables
  (the primitive-product Gauss corollary and its two consumers,
  `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`
  and `primitiveSquareFreeDecomposition_reassembly_signed`).
- Left a `Decomposed into #3102` breadcrumb on the parent issue
  #3092.

## Current frontier

Issue #3092 is partially complete. Three of five deliverables are
landed (SPEC, `content_scale_int`, `rational_associate_primitive_unit`).
The two remaining deliverables both require the primitive-product
corollary of Gauss's lemma over `Z`:
`Primitive p → Primitive q → Primitive (p * q)`. That lemma is the
single new piece needed; the consumer theorems then fall out via
the rational reassembly already in the file plus
`rational_associate_primitive_unit`.

## Next step

A worker should claim #3102, prove the primitive-product Gauss
corollary using the minimal-index argument over coefficients (the
`diagonalMulCoeffTerm`/`mulCoeffSum_eq_diagonal` machinery in
`HexPoly/Euclid.lean` is the natural starting point), then close out
deliverables 4 and 5 from #3092.

## Blockers

None. The partial PR ships independently; #3102 is unblocked.
