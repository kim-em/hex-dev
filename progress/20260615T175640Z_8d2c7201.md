# Progress — 2026-06-15 (session 8d2c7201, feature)

## Accomplished

Issue #7508: removed the stale
`support_subset_of_dvd_scaledRecombinationCandidate` field from
`InitialLiftedFactorSubsetPartitionEvidence` and deleted its initial-evidence
projector.

Kept `LiftedFactorSubsetPartition` unchanged. The initial-to-full partition
constructors and slow-path wrappers now take the still-live scaled-support
obligation as an explicit `hscaled` hypothesis, while recovered-coordinate
fields remain in `InitialLiftedFactorSubsetPartitionEvidence`.

## Current frontier

The initial evidence surface no longer asserts scale-coordinate support. The
full partition structure still carries that field for the older scale-based
chain, and callers that need a full partition must provide `hscaled` explicitly.

## Next step

Downstream issue #7362 can target the smaller initial evidence package. The
broader migration of the scale-based executable correctness chain to
`liftedRecoveryCandidate` remains separate.

## Blockers

None. Verified:

- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line grep found no new `sorry`, `axiom`, `native_decide`, `TODO`, or
  `FIXME`.
