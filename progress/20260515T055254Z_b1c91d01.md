# 2026-05-15 05:52 UTC — feature session, issue #4148

## Accomplished

- Exposed `Hex.normalizeFactorSign` and `Hex.coeff_centeredLiftPoly` so the
  bridge module can refer to them by name.
- Added the LiftedFactorSubset → executable list-partition bridge in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  - `liftedSubsetMask`, `liftedSubsetSelectedList`, `liftedSubsetRejectedList`.
  - `liftedSubsetSplit_mem_subsetSplits`: order-preserving partition lies in
    `Hex.subsetSplits d.liftedFactors.toList`.
  - `liftedSubsetSplit_mem_subsetSplitsWithFirst`: same partition lies in
    `Hex.subsetSplitsWithFirst d.liftedFactors.toList` whenever the head
    index `0` is in `S`.
- Added the executable / proof-side product equality:
  - `polyProduct_liftedSubsetSelectedList_eq_liftedFactorProduct`: pushes both
    products into `Polynomial \mathbb{Z}` via `polyProduct_toPolynomial` and
    `toPolynomial_foldl_mul`, then collapses the order difference using
    `List.Perm.prod_eq` and the index-list permutation
    `((List.finRange n).filter (\cdot \in S)).Perm S.toList`.
- Added the executable recombination candidate as a named definition,
  `recombinationCandidate d S`, matching the inline expression used inside
  `Hex.recombinationSearchModAux`.
- Reformulated A2 recovery against `centeredLiftPoly` directly:
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`. The
  proof reuses the existing
  `centeredLift_scaledLiftedFactorProduct_eq_of_mignottePrecision` after
  showing `centeredLiftPoly` is invariant under prior `reduceModPow` at the
  same modulus.
- Added the one-step executable witness theorems requested in deliverable 3:
  - `shouldRecordPolynomialFactor_of_irreducible_toPolynomial` and the
    candidate-specialised `shouldRecord_recombinationCandidate_of_eq_factor`.
  - `exactQuotient?_recombinationCandidate_eq_some_of_eq_factor` (under
    monicity / positive-degree / divisibility hypotheses on the recovered
    factor).

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`
  introduced in the staged diff.

## Next step

- Discharge the candidate-equals-factor hypothesis with concrete
  unscaled-Mignotte recovery + leading-coefficient-coprime-to-`p`
  reasoning, so the executable witnesses can be assembled directly from
  A2 recovery without an explicit equality assumption. This is the next
  logical successor in the #4127 chain, on top of the bridge landed here.

## Blockers

- None for #4148.
