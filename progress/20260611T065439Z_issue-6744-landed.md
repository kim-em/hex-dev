# #6744 steered reducer — implemented, measured, PRs open

## Accomplished

Implemented the approximation-steered native reducer end to end and passed
every go/no-go threshold on carica.

- `HexLLL/Basic.lean`: `SteeredState` (exact `b : Matrix Int n m` + untrusted
  `Float` `mu`/`bb`), `init`/`refreshRow`/`reduceColumn`/`sizeReduce`/`swap`/
  `prep`/`loop`/`finalSweep`/`fuel`/`steeredReduce`, with `.b` updates equal to
  the proven `GramSchmidt.Int.sizeReduce`/`adjacentSwap`. Lattice preservation
  proved Mathlib-free (`steeredReduce_memLattice_iff` via per-op
  `_memLattice_iff` lemmas mirroring the `LLLState` ones); float ops marked
  `@[irreducible]` after their lemmas so the fuel-loop induction stays tractable.
  `lllSteered` = input-size dispatch (`steerDimThreshold = 40`) → exact
  `lllNative` for small inputs, else `steeredReduce` + `lllReducedCheck` at
  `(δ, 11/20)` with `lllNative` fallback. `SteeredOutcome`/`SteeredTally`/
  `withRecordSteeredOutcome` fallback diagnostic. `lll` none-branch and
  `firstShortVectorUnchecked`/`shortVectorsUnchecked` (BZ consumer) repointed to
  `lllSteered`.
- `HexLLLMathlib/Independent.lean`: `lllSteered_{memLattice_iff,isLLLReduced,
  independent}` (compose `lllReducedCheck_sound` + `steeredReduce_memLattice_iff`
  + `lllNative_*` + `mono_η`); the five `lll_*` contract theorems rewired through
  them, preserved verbatim.
- `HexLLL/Bench.lean`: `runSteeredFallbackTally` (fails on any in-ladder
  fallback), pinned certified=11/fellBack=0.

Go/no-go (carica M2 Ultra; before `6f347b5d`, after `8c661b0a`): harsh n=55
5.99×; harsh exponent over 40–55 5.20→2.64; random n=180 2.36×; no rung
regresses (n<40 routes to the unchanged exact reducer); fallback 0 on every
rung; all conformance first-vector-norm hashes unchanged. Second opinion
(Codex) found no soundness holes; fixed the one item it flagged (stale `lll`
fallback doc comment).

## Current frontier

Two PRs open against main, both MERGEABLE (no conflicts):
- SPEC: https://github.com/kim-em/hex/pull/6762 (merge first)
- impl: https://github.com/kim-em/hex/pull/6763 (head 2e9267b6)
CI (build / build-macos / conformance) running.

## Next step

Watch CI; the conformance job runs `hexlll_bench verify` with the fplll-ffi
provider (the steered tally + certified/dispatched paths). Land SPEC #6762
before impl #6763. Post-merge: refresh the committed carica native ladders,
comparator sweeps, reports/hex-lll-performance.md and -scaling.md, and revisit
the harsh comparator figure's certified-curve omission (per the directive).

## Blockers

None.
