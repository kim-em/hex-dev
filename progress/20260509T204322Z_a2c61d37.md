# 20260509T204322Z - Issue #2820

## Accomplished

Claimed feature issue #2820. Added the private determinant sign bridge
`detSign_inversePermutationValues` in `HexMatrix/Determinant.lean`, deriving
inverse-permutation sign preservation directly from the existing
`inversionCount_inversePermutationValues_mod_two` parity theorem.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

`lake build HexMatrix` still reports pre-existing `sorry` warnings in
`HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`; this session did not touch
those files or add proof holes.

## Current frontier

The inverse-permutation determinant sign prerequisite for transpose assembly is
now available near the inverse-permutation parity theorem.

## Next step

Continue with the downstream determinant transpose assembly issue that consumes
`detSign_inversePermutationValues` alongside
`detProduct_transpose_inversePermutationValues`.

## Blockers

None.
