# 2026-05-16T02:17Z — `/work` session, claimed #4334 then #4362

## Accomplished

Tried to execute #4334 after the directive candidates were not claimable:

- #3899, #3662, #2637, and #2564 failed `coordination claim` with
  `needs replan` or `blocked by dependencies`.
- #4334 was claimable and was claimed.

For #4334, re-read the Phase 4 and benchmarking requirements, confirmed
`HexLLL.done_through` remains `3`, and ran the HexLLL benchmark smoke checks:

- `lake exe hexlll_bench list`
- `lake exe hexlll_bench verify`

The verifier passed all 52 registered benchmarks.

Then ran the five affected scientific registrations on current `main`
(`b1b135659f2ba567581a353b4bfcd9d7bcaf53f5`) into a temporary artifact:

- `Hex.LLLBench.runSizeReduceChecksum`
- `Hex.LLLBench.runGramSchmidtCoeffChecksum`
- `Hex.LLLBench.runFirstShortVectorHarshCubicChecksum`
- `Hex.LLLBench.runOfBasisHarshCubicChecksum`
- `Hex.LLLBench.runFirstShortVectorRandomBoundedChecksum`

The run reproduced the environmental failure mode described in the issue, not
usable Phase 4 evidence. The harness measured a per-spawn floor around
`1.293s` to `1.340s`; all five registrations produced zero verdict-eligible
rows. Two were killed at cap on the first parameter, and the others were below
the `10x` signal floor on every row. The temporary JSON artifact and oracle
cache were removed instead of being committed as scientific evidence.

After skipping #4334 for the host blocker, claimed newly available #4362
(`HexBZ Mathlib: prefix-none discharge for recursive coverage under
LiftedFactorSubsetPartition`). Implementation triage showed that #4362 is too
large for one session and naturally splits into missing proof APIs plus a final
assembler. Decomposed it into:

- #4365: characterize matched `subsetSplitsWithFirst` prefixes.
- #4366: derive partition structure from a successful recombination candidate
  quotient.
- #4367: assemble the final prefix-none lemma, blocked on #4365 and #4366.

Left the required `Decomposed into #4365, #4366, #4367` breadcrumb on #4362 and
released the claim with `coordination skip`.

Claimed #4365 as the first actionable successor. While inspecting the concrete
proof shape, found that #4365 itself depends on two smaller list/index
bookkeeping APIs. Decomposed it into:

- #4370: prove pure `subsetSplits` / `subsetSplitsWithFirst` mask-converse
  lemmas.
- #4371: convert matched lifted-factor masks into `LiftedFactorSubset` selected
  and rejected list identities.
- #4372: assemble the matched prefix characterization, blocked on #4370 and
  #4371.

Left a corrected `Decomposed into #4370, #4371, #4372` breadcrumb on #4365 and
released the claim with `coordination skip`.

Claimed #4370 and implemented the pure mask-converse API in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `subsetSplits_mem_exists_mask`
- `subsetSplitsWithFirst_mem_exists_tail_mask`

These are the list-level converse of the existing mask partition constructors
and unblock #4371's mask-to-`LiftedFactorSubset` bridge.

## Current frontier

#4334 still needs a quiet or dedicated benchmark host. This `carica` run was
not suitable for promotion or for updating `reports/hex-lll-performance.md`.

#4362 is now replaced by narrower successor issues. #4370 is complete in this
branch; after merge, #4371 becomes actionable. #4366 is independently
actionable. #4372 waits on #4371, and #4367 waits on #4365's replacement chain
plus #4366.

## Next step

Re-run #4334 only when the host has a stable low spawn floor, or use the
release-candidate benchmarking workflow on dedicated hardware. A usable run
should produce verdict-eligible rows for all five affected registrations before
the report or `libraries.yml` is changed.

For the HexBZ proof path, continue with #4371 or #4366 after this PR lands
rather than retrying #4362 or #4365 directly.

## Blockers

Current host measurement infrastructure is too noisy for this issue: per-spawn
floor was roughly `1.3s`, making the affected HexLLL scientific registrations
unresolvable under their declared schedules and caps.

Verification for #4370:

- `lake build HexBerlekampZassenhausMathlib.Basic` — succeeds with only
  pre-existing warnings.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Diff scan found no new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry`.
