# PNT+ medium-range Chebyshev fold

## Accomplished

- Replayed the generic positive-integer logarithm and bounded
  prime/prime-power fold work directly above current main
  `505a91875c3409781d34999d023d04b1a8833b1b`.
- Split all 11,723 coordinates into 46 ordinary-kernel certificates of at
  most 256 coordinates, arranged in twelve sequential modules and joined by
  one structural prefix theorem. The complete chain proves the exact final
  checker state `(118671, true)`.
- Closed the pinned real theorem shape
  `0 < x → x ≤ 11723 → Chebyshev.psi x ≤ 1.11 * x`, with the
  input bounds and natural floor reduction load-bearing.
- Added first, middle, and last prefix evidence; false composite-prime and
  false prime-power-base mutations; and guarded axiom reports.
- Reclassified only the exact `allChecks_11723` native-evaluation occurrence.
  The shared Chebyshev interface/import remains pending for the distinct
  FKS2-floor and Ramanujan theta consumers.
- Passed the focused full-chain and conformance builds, source-pinned
  inventory verification and tests, static/DAG/Phase-4 checks, trust and
  release checks, Mathlib-free bench boundary, freshness, and diff hygiene.

## Current frontier

- The complete package-owned `psi_num_2` replacement is implemented and
  locally checked. It deliberately exposes ordinary-kernel bounded replay,
  not a generic runtime `ProofFrontend` action or LeanCert-compatible API.
- A broad fresh-worktree `HexIntervalMathlibExperiment HexConformance` build
  reached 9,461 of 9,512 jobs before the shared filesystem filled while
  writing unrelated Mathlib homology and `HexGF2.Clmul` artifacts. The focused
  Chebyshev targets had already completed successfully.

## Next step

- Re-run the broad registered build when shared disk capacity is available,
  then sequence the local feature for review and CI if selected.
- Treat the remaining Chebyshev dependency work as separate source-shaped
  theta campaigns rather than a LeanCert compatibility layer.

## Blockers

- Only shared disk capacity blocks the broad fresh-worktree build; the
  Chebyshev arithmetic, chunk elaboration, real theorem bridge, and focused
  checks have no known blocker.
