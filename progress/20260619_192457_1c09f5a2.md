# HexArith/Barrett/ReduceNat Phase 6 grind coverage (#8082)

## Accomplished

Promoted the two equation-shaped public `@[simp]` lemmas in
`HexArith/Barrett/ReduceNat.lean` to `@[simp, grind =]`:

- `barrettReduceNat_eq_mod` (`barrettReduceNat p pinv T = T % p`)
- `barrettReduceNat_eq_self_of_lt` (`barrettReduceNat p pinv T = T`)

Both are literal `lhs = rhs` conclusions on re-reading; their
hypotheses (`1 < p`, `pinv = barrettRadix / p`, `T < barrettRadix`,
etc.) become grind side-conditions. The LHS reduces to a strictly
simpler RHS with no recursion, so neither rule loops.

## Current frontier

`lake build HexArith` green — `HexArith.Barrett.ReduceNat` 1.9s, no
grind-loop slowdown. Downstream consumers build clean: Reduce (688ms),
Context (655ms), CrossCheck (2.9s), Conformance (3.2s), and
`HexModArith.HotLoop` (2.4s). `scripts/check_dag.py` exit 0.
Annotation-only diff; no proof bodies, signatures, or imports changed;
no `*Mathlib` file touched; no new `sorry`/`axiom`/`native_decide`.

## Next step

None for this issue. Per the issue context, the sibling untagged
source file is `HexBerlekamp/Factor.lean` + `HexArith/Montgomery/Redc.lean`.

## Blockers

None.
</content>
</invoke>
