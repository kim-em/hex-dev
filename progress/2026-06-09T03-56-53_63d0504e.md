# D4: certCheck + certCheck_sound

## Accomplished

- Added `Hex.certCheck B B' U V δ η : Bool` in `HexLLL/Basic.lean`
  (Mathlib-free), composing `Matrix.sameLatticeCert` and `lllReducedInt`.
- Added `HexLLLMathlib.certCheck_sound` in `HexLLLMathlib/Basic.lean`,
  composing `Hex.Matrix.sameLatticeCert_sound` (HexLLL) and
  `HexLLLMathlib.lllReducedInt_sound` (above it in the same file). No
  validity hypothesis on `η`.
- `lake build HexLLL` and `lake build HexLLLMathlib` both green; zero
  `sorry`/`axiom` introduced.

## Current frontier

D4 lands the single trusted bridge that the certified-dispatch
correctness (D5c) depends on. The downstream dispatch wiring (D5b,
D5c) is now unblocked.

## Next step

D5b (the dispatch hook into `lll`) and D5c (the dispatched `lll`
re-proof using `certCheck_sound`) per the certified-external-dispatch
section of `SPEC/Libraries/hex-lll.md`.

## Blockers

None.
