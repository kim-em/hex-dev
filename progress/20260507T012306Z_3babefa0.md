# 2026-05-07 01:23 UTC — work session (3babefa0)

## Accomplished

Claimed #2530 and narrowed the `scaledCoeffs` diagonal proof in
`HexGramSchmidt/Int.lean`:

- added proved array/matrix bridges `getArrayEntry_gramRows` and
  `rowsToMatrix_gramRows`;
- removed the false raw no-pivot-data diagonal bridge, which did not account
  for later diagonal entries after an early singular no-pivot step;
- replaced it with the focused theorem-level obligation
  `scaledCoeffRows_diag_eq_gramDetVecEntry`, matching the singular-step
  zero-tail semantics used by `gramDetVecEntry`;
- kept the public theorem `scaledCoeffs_diag` proved without a theorem-body
  `sorry`.

Verification completed:

- `lake build HexGramSchmidt.Int`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Int.lean`

## Current frontier

The remaining diagonal proof work is now the exact executable-loop invariant
`scaledCoeffRows_diag_eq_gramDetVecEntry`, relating `scaledCoeffArrayLoop`
writes to no-pivot Bareiss determinant-vector entries, including the singular
tail case.

## Next step

Prove `scaledCoeffRows_diag_eq_gramDetVecEntry`, likely by relating
`scaledCoeffArrayLoop` states to `Matrix.noPivotLoop` states through
`rowsToMatrix_gramRows`, with a branch for early zero pivots that preserves
zero diagonal entries in the coefficient array.

## Blockers

No technical blockers. The worktree still has the pre-existing unrelated
`.claude/CLAUDE.md` modification, which this session did not touch.
