# SPEC oracle strategy Sage removal (#2005)

## Accomplished

- Claimed human-oversight issue #2005.
- Updated `SPEC/testing.md` so the oracle strategy assigns `python-flint`,
  `cypari2`, `fpylll`, and the committed Lübeck cache instead of Sage.
- Replaced the former Nix-Sage deployment section with a short note that
  Sage is not a project oracle and is only acceptable for ad-hoc local use.
- Verified `SPEC/testing.md` has no Sage references outside that note and
  passes `git diff --check`.

## Current frontier

`SPEC/testing.md` now matches the planned python-flint / cypari2 / fpylll
oracle issues. Some existing oracle issue bodies still mention older SPEC
line numbers or Sage as the stale context they replace, but their planned
deliverables use the same lighter wrappers.

## Next step

Land the documentation PR for #2005, then continue with the remaining
human-oversight SPEC/profile-size issue or the per-library oracle issues.

## Blockers

None.
