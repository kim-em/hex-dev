# Issue #6225: HexHensel Phase 6 invariant automation annotation audit

## Accomplished

- Claimed #6225 after #6222 was already claimed by another agent and the
  older directive issues were either blocked or already claimed.
- Audited the five Phase 6 review files:
  `HexHensel/Basic.lean`, `HexHensel/Linear.lean`,
  `HexHensel/Quadratic.lean`, `HexHensel/Multifactor.lean`, and
  `HexHensel/QuadraticMultifactor.lean`.
- Added conservative automation attributes to the explicit loop-invariant
  projection surfaces:
  - `LinearLiftLoopInvariant.product_congr`, `.bezout_congr`, and `.monic_g`
    now have `@[simp, grind =>]`.
  - `QuadraticLiftLoopInvariant.prod_congr`, `.bezout_congr`, and `.monic`
    now have `@[simp, grind =>]`.
  - `QuadraticLiftLoopInvariant.of_product_bezout_monic` now has
    `@[grind =>]` as a constructor rule.

## Current frontier

- #6225 deliverables are complete.
- No analogous projection namespace exists in `HexHensel/Basic.lean` or
  `HexHensel/Quadratic.lean`.
- `MultifactorLiftInvariant` and `QuadraticMultifactorLiftInvariant` are
  recursive predicate definitions without separate projection lemmas to
  annotate conservatively in this pass.

## Next step

- Commit the two touched Lean files plus this progress entry, then open a PR
  closing #6225 with the PR description noting omitted constructor/projection
  choices.

## Blockers

- None.
- Verification passed: `lake build HexHensel` (modified target timings included
  `HexHensel.Linear` at 1.3s and `HexHensel.QuadraticMultifactor` at 838ms),
  full `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, and
  forbidden-token diff grep over touched Lean files (no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`).
