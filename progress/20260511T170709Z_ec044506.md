# HexBZ symbolicated profile report slice

## Accomplished

- Claimed feature issue #3506.
- Captured a cleaner `degree-height-matrix` profile directly through the
  `hexbz_bench _child` path at commit `06d996d749e3`.
- Updated `reports/hex-berlekamp-zassenhaus-performance.md` with the
  reproducible profile command, hardware/commit details, leaf-cost category
  split, inclusive BZ function ranking, and dominant-cost narrative.
- Removed the profile-specific open concern from the report while leaving the
  broader Phase 4 verdict, schedule, and comparator concerns in place.
- Verified with `lake exe hexbz_bench list`, `lake exe hexbz_bench verify`, and
  `git diff --check`.

## Current frontier

- HexBerlekampZassenhaus now has one representative symbolicated
  `degree-height-matrix` profile slice recorded in the headline report.
- The report still intentionally does not claim Phase 4 completion.

## Next step

- Work the remaining BZ Phase 4 concerns: scientific verdict quality,
  comparator metadata/evidence, and the shared-domain compare surface tracked
  separately.

## Blockers

- None for #3506.
