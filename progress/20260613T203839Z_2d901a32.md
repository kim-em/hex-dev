# Progress: #6932 — recovered-coordinate lifted predicate carrier

Feature session for #6932. Partial PR path: the public predicate surface was
changed, and the remaining build failures are now localized to old
scaled-coordinate consumers.

## Accomplished

- Changed `RepresentsIntegerFactorAtLift` in
  `HexBerlekampZassenhausMathlib/Basic.lean` from the old scaled-product
  modular equality to a proof-level `Nonempty (RecoveredAtLift ...)` carrier.
- Kept `RecoveredAtLift` as the data-bearing `Type` structure with
  `monicFactor`, `congr`, and `dilate_eq`.
- Added bridge helpers under `namespace RepresentsIntegerFactorAtLift`:
  `ofRecovered` for packing a `RecoveredAtLift` witness and `elim` for
  locally exposing the monic-coordinate witness, congruence, and dilation
  equality.
- Confirmed the declaration and direct abstract structure/projection cluster
  gets past elaboration. The target build now fails later in known old-shape
  consumers.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.Basic` fails after the carrier
  cluster, with first errors at the old exact-recovery use near line 2793,
  monic multiplicative closure near line 8740, explicit Hensel forward
  transport near line 9092, and later exact-recovery/scaled-search callers.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- Added-line forbidden-token grep over the touched Lean diff finds no
  `sorry`, `admit`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The public predicate now has the corrected recovered-coordinate shape. The
remaining failures are consumers that still try to pass
`RepresentsIntegerFactorAtLift` to lemmas expecting the old equality

`reduceModPow (scaledLiftedFactorProduct core d S) = reduceModPow factor`.

Those callers need recovered-coordinate replacements rather than a global
bridge back to the false scaled-product statement.

## Next step

Migrate the old-coordinate exact-recovery and multiplicative-closure callers to
consume `RecoveredAtLift` via the new eliminator, or replace them with
recovered-candidate statements where the old scaled statement is no longer
sound.

## Blockers

`HexBerlekampZassenhausMathlib.Basic` does not build yet; this PR intentionally
stops at the carrier-surface boundary described by #6932 and should be treated
as partial work for successor migration issues.
