# Review: BHKS fast raw irreducibility cut assembly soundness (#8084)

## Accomplished

Soundness review of the recently-merged fast-core raw-irreducibility
assembly on the critical path of directive #2564. Verdict: **sound**, no
defect. Review report posted as the closing comment on #8084.

Traced the full chain:
`factorFastFactorsWithBound_raw_zpolyIrreducible_of_recoveredLift`
(`PartitionRefinement.lean:2012`) → `rawIrreducible_of_cut` →
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_cut` →
`_count_eq_of_cut` (`le_antisymm` of unconditional `count_le` and
forward-cut `count_ge_of_cut`) → UFD pigeonhole
`irreducible_of_partition_card_eq_normalizedFactors_card`.

Key findings:
- **Defeq alignment is structural**: `projectedRowsOfLiftData` is *defined
  as* `bhksProjectedRows (latticeBasisOfLiftData …) …`, and
  `HasPositiveDimension` is an abbrev for the exact `hrows` shape. One
  definitional unfold, same `latticeBasisOfLiftData` term on both sides —
  not a `getD`/default coincidence.
- **Forward-only cut is genuinely sufficient**: the reverse `L'=W`
  separation is dead because the unconditional upper bound `count_le`
  (product=core + non-unit factors via `shouldRecord`) caps `#emitted ≤
  #irreducible`, and forward `W⊆L'` supplies the matching lower bound via
  the partition-refinement injection. Equality + product forces each
  factor irreducible.
- **No vacuity**: `hfac`'s `getD i.val 1` default is rejected by the
  `0 < degree` clause; `trueSupports=∅` is blocked by `hpartition` vs
  `hdeg`; the forward membership is genuinely proved from LLL lattice
  geometry in `cutProjectionHypotheses_of_shortVectors`.
- **`#print axioms`**: all four declarations depend on only
  `propext, Classical.choice, Quot.sound`. No `sorryAx`, no
  `Lean.ofReduceBool`. The lone `Basic.lean` `sorry`
  (`isIrreducible_iff`, the checker bridge) is not in the cone.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.PartitionRefinement` green
(3784 jobs, zero errors); `scripts/check_dag.py` exit 0. Review-only, no
code changes.

## Next step

None for this issue. The genuine open substrate this assembly correctly
leaves as hypotheses is the first-success lift-family / `hpartition`
producer (#7786 deliverable 2, #6779, #2564) — not a defect here.

## Blockers

None.
