# 20260509T143437Z - Issue #2689

## Accomplished

Claimed #2689 after the oldest human-oversight item was dependency-blocked.
Fused the `LLLState.ofBasis` construction path by adding
`GramSchmidt.Int.data`, which runs the existing fraction-free scaled-coefficient
loop once and packages both the determinant vector `d` and scaled-coefficient
matrix `ν`.

Updated `GramSchmidt.Int.gramDetVec` and `GramSchmidt.Int.scaledCoeffs` to be
thin projections from that shared data package, and changed `LLLState.ofBasis`
to consume the package once instead of calling both wrappers separately.
Updated the HexLLL bench helper and ofBasis complexity model from two Bareiss
passes to one shared Bareiss-style pass. `reports/hex-lll-performance.md` does
not exist yet in this worktree, so there was no report section to update.

Verification completed:

- `lake build HexGramSchmidt.Int`
- `lake build HexLLL`
- `lake build HexLLL.Bench`
- `lake exe hexlll_bench list | rg -n "ofBasis|LLLState"`
- `lake exe hexlll_bench run --filter OfBasis --repeats 1 --max-seconds-per-call 180`
- `lake exe hexgramschmidt_emit_fixtures` compared against committed fixtures
  and `python3 scripts/oracle/gs_flint.py`
- `lake exe hexlll_emit_fixtures` compared against committed fixtures;
  `python3 scripts/oracle/lll_fpylll.py` skipped because `fpylll` is not
  installed
- `lake build`

## Current frontier

The fused executable path is implemented and locally verified. The one-repeat
ofBasis bench run was consistent with the updated one-pass complexity model.
The worktree still has the pre-existing unstaged `.claude/CLAUDE.md`
modification, which was not touched.

## Next step

Commit the four Lean source changes and this progress file, then create the PR
for #2689.

## Blockers

None for the implementation. Full HexLLL oracle execution is limited locally by
the missing `fpylll` Python dependency.
