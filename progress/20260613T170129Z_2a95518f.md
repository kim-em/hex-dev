# Progress: #6886 — recovered lift representation carrier

## Accomplished

Decomposed the too-broad parent #6881 into #6886, #6888, and #6890 after a
direct predicate swap exposed two non-local obligations: stale scaled-product
recovery wrappers at `HexBerlekampZassenhausMathlib/Basic.lean:2675` and monic
multiplicative closure at `:8639`.

For #6886, added the `RecoveredAtLift` carrier in
`HexBerlekampZassenhausMathlib/Basic.lean`. It stores the monic-coordinate
witness, the unscaled `liftedFactorProduct` congruence, and the dilation
primitive-part keystone. Added `RecoveredAtLift.candidate_eq_of_bound`, routing
the carrier through `liftedRecoveryCandidate_eq_factor_of_congruence_of_bound`.

## Current frontier

The corrected recovered-coordinate representation now exists alongside the old
`RepresentsIntegerFactorAtLift` scaled-product predicate, so downstream work can
migrate consumers without changing the public predicate in one step.

## Next step

#6888 should decouple stale scaled-product recovery lemmas from
`RepresentsIntegerFactorAtLift`, making the final public predicate swap in
#6890 mechanical.

## Blockers

`lake build HexBerlekampZassenhausMathlib.Basic` still fails at the pre-existing
#6804 scaled/dilate prefix mismatch near `Basic.lean:16447` after line shifts.
