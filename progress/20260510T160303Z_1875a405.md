## Accomplished

Claimed #3036 and added a private `partialColumnTupleCoeff_insertAt_last_fold`
helper in `HexMatrix/Determinant.lean`.

The helper packages the coefficient-side fold split for the suffix recursion:
`List.finRange (rem + 1)` is exposed as the cast-successor prefix plus the
last index, previous prefix entries rewrite through `insertAt_last_get_castSucc`,
and the newly inserted last entry rewrites through `insertAt_get_self`.

Verified:

- `lake build HexMatrix.Determinant`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`
  reports no matches.

`lake build HexMatrix` was attempted but currently fails in untouched
`HexMatrix/RREF.lean` with duplicate private declaration
`Hex.Matrix.IsRREF.foldl_add_eq_acc_ring`; this is outside the determinant
change.

## Current frontier

The coefficient fold arithmetic needed by the suffix determinant tuple
expansion is available as a focused private theorem. The final induction can
unfold `partialColumnTupleCoeff`, use this helper for the inserted-last prefix
case, and combine it with the sibling assembled-column helper from #3037.

## Next step

Use the new coefficient helper together with the assembled-column match-up to
finish the suffix determinant tuple expansion induction in #3033.

## Blockers

Full `lake build HexMatrix` is blocked by the existing `HexMatrix/RREF.lean`
duplicate private declaration error, unrelated to this issue.
