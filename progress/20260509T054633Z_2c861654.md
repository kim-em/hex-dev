# 2026-05-09 (UTC) — issue #2684

## Accomplished

- Closed #2684 with new lemmas in `HexBerlekampZassenhausMathlib/Resultant.lean`:
  - `abs_det_le_col_l2norm_prod` (column-form Hadamard, derived from the
    existing row form by transposing).
  - Two private helpers: `l2normSq_eq_sum_range` (squared `l2norm`
    expressed as a `Finset.range` sum) and `sum_indicator_Icc_eq_sum_range`
    (indicator-on-`Set.Icc` over `Fin (m+n)` collapses to a `Finset.range`
    sum after re-indexing).
  - Two private column-norm computations:
    `sylvester_col_l2normSq_left` (first `m` g-shift columns have squared
    column norm `(l2norm g)²`) and `sylvester_col_l2normSq_right` (last
    `n` f-shift columns have squared column norm `(l2norm f)²`).
  - Public theorem `abs_resultant_le_l2norm_pow`:
    `|resultant f g| ≤ (l2norm f) ^ g.natDegree * (l2norm g) ^ f.natDegree`.
- The proof routes through `Fin.prod_univ_add` to split the column product
  by the `Fin.castAdd` / `Fin.natAdd` block decomposition, exactly as the
  issue suggested.
- `lake build HexBerlekampZassenhausMathlib`, `python3 scripts/check_dag.py`,
  and `git diff --check` all pass; no new `axiom`, `native_decide`, or
  `sorry` markers.

## Current frontier

- `HexBerlekampZassenhausMathlib/Resultant.lean` now exposes the BHKS
  Mignotte-style integer resultant bound that #2639 and the wider BHKS
  bad-vector route in HO-4 (#2567) require.

## Next step

- A follow-on can plug `abs_resultant_le_l2norm_pow` into the BHKS bad-
  vector / Theorem 5.2 route. The existing
  `abs_resultant_le_sylvester_row_l2norm_prod` is now superseded for the
  Mignotte direction but remains as a row-shaped specialization.

## Blockers

None.
