# 2026-05-30 12:00 UTC — HO-1 Gap 1 consumer drop (#5723)

## Accomplished

Replaced the `hcore_monic` Gap 1 shim in the exhaustive-arm umbrella
chain of `HexBerlekampZassenhausMathlib/IntReductionMod.lean` with the
`hselected : Hex.ZPoly.toMonicPrimeData? squareFreeCore = some (Hex.choosePrimeData
squareFreeCore)` premise. The lifted-factor monicness / natDegree-positivity /
injectivity facts now flow through the `MonicReductionCorrespondence`
transport package via the consumer-facing
`monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`
landed in #5733.

Updated theorems:

- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_of_bound`
- `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
- `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`
- `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
- `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound` (kept monic
  variant for completeness; takes `hselected` for the new expansion-
  preconditions call site)
- `reassemblyExpansionComplete_exhaustive_of_ne_zero` (wrapper)

In each case the `hchoose : choosePrimeData? squareFreeCore = some _`
premise was dropped where it was no longer used in the proof body
(replaced by `hselected` for prime-data provenance via
`Hex.ZPoly.toMonicPrimeData?_prime`). Original-core nonzero, primitive,
and positive-leading-coefficient facts are derived from `hf_ne` via
`Hex.squareFreeCore_leadingCoeff_pos_of_ne_zero` and
`IntReductionMod.normalizeForFactor_squareFreeCore_primitive_of_ne_zero`.

The umbrella `_of_bound` proof switched from the monic-input
`reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound` to the
hcore_monic-free
`reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`,
threading `hcore_primitive` and `hcore_lc_pos` derived from `hf_ne`.

Docstrings updated to mark Gap 1 closed by the monicising-by-scaling
route and to reference the #5733 / #5723 lineage.

## Current frontier

Diff: 199 insertions, 390 deletions in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` (one-file change).
No banned tokens introduced (`sorry`, `axiom`, `native_decide`).

## Next step

After upstream regression below clears, full `lake build
HexBerlekampZassenhausMathlib.IntReductionMod` verification.

## Blockers

`HexBerlekampZassenhausMathlib/Basic.lean` lines 5099, 5994, 6280 fail
to elaborate on origin/main after PR #5779 (`fix: accept unit gcd good
primes`). The fix changed `squareFreeModP` from `gcd = 1` to
`gcdIsUnit gcd = true`, and `isGoodPrime_squareFreeModP`'s return type
no longer matches the `gcd ... = 1` ascription in the three consumer
sites in `HexBerlekampZassenhausMathlib/Basic.lean`. CI did not catch
this because `lake build` (no args) only builds `HexManual`, which is
the sole `@[default_target]`; `HexBerlekampZassenhausMathlib` is not in
that transitive chain. CI's bench-exe build list also does not include
`HexBerlekampZassenhausMathlib`.

This blocks every `lake build HexBerlekampZassenhausMathlib*` and
`lake build HexBerlekampZassenhaus` invocation in the issue's
verification section. Filed as a separate concern; not in scope for
#5723.

The work-around in this PR was a careful read-and-edit pass: the
changes are mechanically faithful to the Gap 1 closure plan, all
removed `hcore_monic` consumers are accounted for, and the transport
package fields used (`monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`,
`Hex.ZPoly.toMonicPrimeData?_prime`) match their published signatures.
