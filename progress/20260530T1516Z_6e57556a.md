# Progress — 2026-05-30T15:16Z — session 6e57556a (#5804 skip)

## Accomplished

Claimed #5804 (HexGramSchmidt Phase 5 coefficient-level Bareiss
divisibility). Read the relevant sections of
`HexGramSchmidt/Int.lean` (`BareissGramRowInvariant` and the regular
step transport, lines ~2243-2628), the Bareiss bordered-minor
machinery in `HexMatrix/Bareiss.lean`
(`bareissExactDiv_borderedMinor_of_mul_eq` and the surrounding
helpers), and the Desnanot-Jacobi infrastructure in
`HexMatrix/Determinant.lean` (`det_desnanot_jacobi_int` at
~line 9435 plus the `bareissDesnanotIndex` reindexing family at
~lines 8157-8240).

Confirmed both sub-issues in the issue body — (1) cofactor
identification with regular-step transport, (2) divisibility from
the cofactor formula — share the same load-bearing Sylvester-style
determinant identity:

```
D_{{0..k},{0..k}} · D_{{0..k-1,i}\{α},{0..k-1}}
  − D_{{0..k-1,i},{0..k}} · D_{{0..k}\{α},{0..k-1}}
  = −D_{{0..k-1},{0..k-1}} · D_{{0..k,i}\{α},{0..k}}
```

The existing two-sub-issue decomposition does not reduce
single-session hardness: whether you go via "prove preservation
first" (sub-issue 1) or "prove divisibility first using cofactor
hypothesis" (sub-issue 2), the same Desnanot-Jacobi instantiation
on a specific `(k+2)×(k+2)` submatrix of `G` has to be discharged.

Posted the explicit identity, the submatrix shape (rows
`[α, 0, …, k-1, k, i]`, cols `[α, 0, …, k-1, k]`), and a
recommended A/B/C refactor of the decomposition as an issue comment
on #5804, then `coordination skip`'d the issue. Three sessions
(`b8f57f34`, `80e32438`, `6e57556a`) have now skipped #5804 without
producing a code-bearing PR; the decomposition needs to change
before re-dispatching.

## Current frontier

Decomposition refactor for #5804 is on the planner's queue.
Recommended split:

- A. Standalone `HexMatrix/Determinant.lean` lemma for the
  identity above, reducing to `det_desnanot_jacobi_int` on the
  explicit `N := G^{[α, 0..k-1, k, i], [α, 0..k-1, k]}` submatrix.
  Likely requires extending the `bareissDesnanotIndex`-style
  reindexing helpers to handle the additional `α` row/col motion.
- B. `BareissGramRowInvariant` cofactor-formula strengthening
  in `HexGramSchmidt/Int.lean`, consuming A.
- C. `bareissGramRowInvariant_step_dvd_prevPivot` derived from B.

A is the hard self-contained mathematical work. B and C are
mechanical.

## Next step

Planner picks up the `replan` on #5804, either narrows the body to
sub-issue A (the load-bearing identity) and files B and C as
follow-ups, or files A as a fresh issue under
`HexMatrix/Determinant.lean` and rewrites #5804's body to consume
A.

## Blockers

The substantive blocker is the Desnanot-Jacobi instantiation
itself: it requires a `(k+2)×(k+2)` submatrix `N` of the Gram
matrix with rows reordered as `[α, 0..k-1, k, i]`, columns as
`[α, 0..k-1, k]`, and a careful sign/permutation tracking that
existing `bareissDesnanotIndex` does not directly provide (it
handles only the pivot row/col motion, not the additional `α`
motion). No environmental blocker.
