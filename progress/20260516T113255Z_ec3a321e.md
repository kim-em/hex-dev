# 2026-05-16 — HexBZ #4457 capstone: main candidate divisibility

## Accomplished

Landed `representedFactor_dvd_recombinationCandidate_of_subset` in
`HexBerlekampZassenhausMathlib/Basic.lean`, the #4430 capstone deliverable
that closes the BZ recombination divisibility chain on the Mathlib bridge
side.

Added:

- `not_represents_empty_of_irreducible_dvd_core` — private helper handling
  the `S = ∅` subcase of the squarefreeness contradiction. Collapses
  `centeredLiftPoly (scaledLiftedFactorProduct core d ∅) (d.p^d.k)` to
  `centeredLiftPoly 1 (d.p^d.k)` under monic core, derives
  `2 ≤ d.p^d.k` from `factor ≠ 0` (matching the
  `representsIntegerFactorAtLift_monic` pattern), and uses
  `centeredModNat_one_of_two_le` plus `DensePoly.coeff_C` to force
  `factor = 1`, contradicting `Irreducible 1` via `not_irreducible_one`.

- `representedFactor_dvd_recombinationCandidate_of_subset` — the capstone
  theorem. Splits on `toPolynomial factor`'s prime divisibility (from UFD
  on `Polynomial ℤ`) of `toPolynomial quotient * toPolynomial candidate`:

  - **Case A** (`factor ∣ candidate` poly side): transports back to
    `Hex.ZPoly` via `HexPolyZMathlib.equiv.injective` +
    `toPolynomial_ofPolynomial`.
  - **Case B** (`factor ∣ quotient` poly side): derives a contradiction.
    `S = ∅` is dispatched by the new helper; `S ≠ ∅` picks `i ∈ S ⊆ T`,
    applies `mem_T_iff_exists_irreducibleFactor_representingSubset`
    (#4469) at `i` to obtain an irreducible candidate divisor `g` with
    `i ∈ S_g`, then uses `pairwise_disjoint` contrapositively to force
    `Associated (toPolynomial factor) (toPolynomial g)`. Combined with
    `g ∣ candidate`, `factor² ∣ target` follows, contradicting
    `target_squarefree` via `Irreducible.not_isUnit`.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
  `sorry`; the two pre-existing `sorry`s at L162 and L241 are unchanged.

Both new declarations sit immediately after #4469's
`mem_T_iff_exists_irreducibleFactor_representingSubset`, per the
issue's library-placement guidance.

## Next step

Open the PR closing #4457. With this capstone in place, the BZ
recombination divisibility chain (#4430) is complete on the Mathlib
bridge surface.

## Blockers

None.
