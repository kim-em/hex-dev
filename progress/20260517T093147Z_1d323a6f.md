# 2026-05-17 09:31 UTC — thirty-seventh Phase 4/5 checkpoint (1d323a6f)

Claimed summarize issue #4622 and recorded this checkpoint covering
the merged-PR wave since #4557 closed at 2026-05-17T03:35:46Z. The
issue body's headline count of "22 PRs" was set when the planner
opened the issue at 09:21; two more PRs (#4619, #4620) merged
between then and this session claiming the issue, bringing the
actual count to **24** merged PRs in this window.

## Accomplished

Read the 24 PRs merged after #4557 closed, the last ~15 progress
entries (covering the 2026-05-17T03Z–2026-05-17T09Z window), the
current `coordination orient` frontier and `python3 scripts/status.py`
output, the still-open `agent-plan` issue bodies for the HO-1
substrate chain and the open directives.

The dominant theme this cycle is the buildout of the four **per-arm
umbrellas** for `factor_irreducible_of_nonUnit` (#4170) — constant,
small-mod singleton, fast-quadratic, slow-quadratic — together with
the `factorsModPBerlekampForm` discharger family that supplies their
boundary hypotheses, and the start of the
`reassemblyExpansionComplete` chain that drops the explicit
`hcomplete` premise per arm. `factorWithBound`'s slow path now
dispatches over a four-disjunct classifier (#4608); the exhaustive
arm umbrella (#4561) is the last per-arm piece still in flight.
The HexBZ Mathlib Factorization capstone issue (#3970) closed via
#4566, removing the `irreducibleByFactorization_iff` theorem-level
`sorry`.

- **HO-1 substrate — per-arm umbrellas for the slow path** (#4564,
  #4572, #4582, #4580). The four landed per-arm umbrellas now share
  a uniform `_of_choosePrimeData` shape:
  `factor_smallModSingleton_branch_entry_irreducible_of_choosePrimeData`
  (#4564), `factor_constant_branch_entry_irreducible_of_choosePrimeData`
  (#4572), `factor_quadratic_branch_entry_irreducible_of_quadraticRoots`
  / `factor_fast_quadratic_branch_entry_irreducible_of_choosePrimeData`
  (#4582), and `factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`
  (#4580). Constant arm aside, all three core-irreducible arms run
  on the generic reassembly lift
  `reassemblePolynomialFactors_factor_irreducible_of_complete_and_core_irreducible`.

- **HO-1 substrate — `factorsModP*` dischargers from
  `factorsModPBerlekampForm`** (#4570, #4573, #4574, #4601, #4560).
  All four boundary-hypothesis dischargers of
  `QuadraticMultifactorLiftInvariant` now land from the
  `factorsModPBerlekampForm` invariant supplied by the executable
  prime-data search: `_ne_nil_of_factorsModPBerlekampForm` (#4570),
  `_polyProduct_congr_of_factorsModPBerlekampForm` (#4573),
  `_coprime_of_factorsModPBerlekampForm` (#4574, with new Berlekamp-
  level `berlekampFactor_factors_pairwise_coprime` and the Option-3
  per-factor `monicModularImage` wrap), and
  `_monic_of_factorsModPBerlekampForm` (#4601). The
  `henselLiftData_liftedFactor_injective_of_factorsModPBerlekampForm`
  umbrella (#4560) composes the first three plus the modular-nodup
  family from the previous cycle, dropping `hproduct_mod_p` and
  related explicit premises from one of the three lifted-factor
  umbrella signatures.

- **HO-1 substrate — `reassemblyExpansionComplete` chain** (#4598,
  #4613, #4615). The constant-arm discharger
  `reassemblyExpansionComplete_constant_of_ne_zero` landed in
  `HexBerlekampZassenhaus/Basic.lean` (#4598) via three new lemmas in
  `HexPolyZ/Basic.lean` (derivative size bound,
  `derivative_zero_of_size_le_one`, and the structural companion
  `repeatedPart_eq_one_of_squareFreeCore_degree_zero`). The constant
  arm umbrella was then refactored to discharge its own `hcomplete`
  internally (#4615) — the first per-arm signature-drop pattern.
  Sibling arms still consume an explicit `hcomplete` and are blocked
  on the quadratic-arm reassembly-expansion-complete discharger
  (#4604, blocked on #4611) and the small-mod singleton variant
  (#4597, unclaimed). The Mathlib-free expansion helper
  `expandRepeatedPartFactorArray_residual_eq_one_of_pow_decomposition`
  plus the `ZPoly.Associated` definition and the
  `irreducible_not_dvd_of_not_associated` bridge landed in #4613 as
  one half of the #4594 substrate the quadratic-arm
  `reassemblyExpansionComplete` will consume.

- **HO-1 substrate — supporting executable helpers** (#4586, #4608,
  #4607). The Mathlib-bridge residual-irreducibility helper
  `quadraticIntegerRootFactors?_residual_irreducible_of_isPrimitive`
  landed in #4586, then was deleted again in #4596 once the
  slow-quadratic umbrella was refactored onto the Mathlib-free
  `_factor_irreducible_of_primitive` shape (see Refactor section
  below). `factorFastFactorsWithBound_branch` (#4608) is the
  nine-disjunct exhaustiveness classifier mirroring
  `factorWithBound`'s slow-side classifier. The unconditional
  quadratic reassembly classifier
  `reassemblePolynomialFactors_quadratic_irreducible_or_repeated_or_residual`
  was deleted as dead code (#4607) after its only callers were
  removed by the slow-quadratic refactor.

- **Refactor / API cleanup** (#4583, #4589, #4596, #4578, #4619).
  The constant arm umbrella body was tightened from 14 to 6 lines
  by promoting `normalizeFactorSign_one` and
  `shouldRecordPolynomialFactor_one` from `private` to public and
  inlining their bodies (#4583). `prod_berlekampFactor` was derived
  from `factorProduct_berlekampFactor` (#4589); the dead
  `_hsquareFree` premise it inherited was then dropped from the API
  surface (#4619, sole external caller in `HexBerlekampMathlib`
  contracted to call `factorProduct_berlekampFactor` directly). The
  slow-quadratic umbrella was rewritten onto the Mathlib-free
  `quadraticIntegerRootFactors?_factor_irreducible_of_primitive` plus
  the generic reassembly lift (#4596), deleting both the
  conditional-classifier route used by #4580 and the
  Mathlib-bridge residual-irreducibility helper landed by #4586
  (net 80 insertions / 293 deletions, IntReductionMod.lean sorry
  count drops from 1 to 0). The `polyProduct_map_liftToZ_congr_factorProduct`
  helper was derived from its `modP_eq` sibling in two combinators
  (#4578), shaving 33 lines.

- **HO-1 fix — HexBZ Mathlib Factorization capstone close** (#4566).
  `irreducibleByFactorization` was reworked to delegate to the
  Mathlib-free executable checker `Hex.ZPoly.isIrreducible
  (HexPolyZMathlib.ofPolynomial f)`, and `irreducibleByFactorization_iff`
  proved by composing `Hex.ZPoly.isIrreducible_iff`,
  `Hex.ZPoly.Irreducible_iff_polynomialIrreducible`, and
  `toPolynomial_ofPolynomial`. This closed unclaimed issue #3970
  (the HexBZ Mathlib Factorization capstones) and removed the
  `irreducibleByFactorization_iff` theorem-level `sorry` from
  `HexBerlekampZassenhausMathlib/Basic.lean`. The capstone `sorry`
  remaining at the top of that file is the HO-1
  `factor_irreducible_of_nonUnit` tracked by #4170.

- **Docs / progress entries** (#4592, #4600, #4620). Three review-
  session entries that landed as standalone docs PRs: #4592 (review
  of #4580 slow-quadratic umbrella, source of follow-up #4587 /
  #4588 / #4590), #4600 (review of #4582 fast-quadratic umbrella),
  and #4620 (review of #4608 fast-path dispatch classifier).

## Current frontier

Open PRs (from `coordination orient`):

- (no agent-branch PRs in flight; the four recent landings have all
  squash-merged.)
- #2656: unrelated SPEC draft for hex-real-roots / hex-sturm.

Unclaimed `agent-plan` work:

- #4334 (HexLLL Phase 4 inconclusive scientific benchmark verdicts —
  still environmentally gated per `SPEC/benchmarking.md`).
- #4597 (HO-1 substrate: small-mod singleton
  `reassemblyExpansionComplete` discharger, supersedes #4593).
- #4611 (HO-1 substrate: exponent-extraction
  `repeatedPart = ∏ q^{e_q}` from divisibility + irreducible cover —
  the sibling of #4613 needed to assemble #4604).

Claimed work (this summarize claim plus four others):

- #4622 (this summarize task).
- #4621 (review of #4613, claimed by a parallel agent — race for
  the review queue won by that session).
- #4617 (char-0 gcd `p ~ divRadical p` over Polynomial K — the
  Mathlib structural fact #4602 sub-issue B).
- #4605 (small-mod singleton arm `hchoose` premise discharger).
- #4561 (slow exhaustive arm umbrella — claimed ~5h, in flight).

Blocked chains to keep visible:

- HO-1 executable irreducibility tail: #4172 → #4170 → #2567. The
  per-arm umbrella roster is one short — the exhaustive arm (#4561)
  is the last in-flight arm and now has all four `factorsModP*`
  dischargers it consumes. After #4561 lands the capstone has all
  four arm umbrellas (constant ✓, small-mod singleton ✓,
  fast-quadratic ✓, slow-quadratic ✓, exhaustive in flight); the
  residual gates on #4170 are then (a) the per-arm
  `reassemblyExpansionComplete` dischargers (constant ✓, the other
  three blocked on #4604 ← #4611 + #4597 + #4605), (b) the BHKS
  fast branch via directive #2567, and (c) the capstone assembly
  itself.
- Mathlib structural fact for the quadratic-arm
  `reassemblyExpansionComplete`: #4604 ← #4611, with the
  Mathlib-side structural divisibility decomposed as
  #4618 ← #4617. The Mathlib-free expansion helper (#4613) has
  landed; #4611 (exponent extraction) is the unclaimed sibling that
  feeds the same assembly point.
- HO-44 single-file `bareiss_eq_det` orphan: directive #3900,
  unchanged since the previous checkpoint.
- HexGramSchmidt scaledCoeffRows diagonal determinant chain:
  #3977 → #4053 → #3899. Unchanged from the previous two
  checkpoints; no work landed against this chain this cycle.
- HO-18 / HexLLL Phase 4 report: directive #3662 still gated on
  #4334.
- Open directives: #2564 (HO-1 rewrite), #2567 (HO-4 BHKS 5.2 —
  the sole remaining branch-level gate on #4170 once the per-arm
  `reassemblyExpansionComplete` chain closes), #2637 (Factorization
  record API), #3662 (HO-18 perf report), #3899 (HO-43), #3900
  (HO-44).

## Status survey

`python3 scripts/status.py` is unchanged from the previous
checkpoint: `HexGfqRing` reported **Fully done**;
`HexBerlekampZassenhausMathlib` reported **Phase 3 wait** on
`HexBerlekampZassenhaus.done_through ≥ 3`; `HexLLL` still at
Phase 4 with #4334 environmentally gated; the HexGramSchmidt /
HexGramSchmidtMathlib pair unchanged. The 22 other libraries in
the matrix continue at their previous-checkpoint phases.

## Next step

1. Hand the open `reassemblyExpansionComplete` substrate to a
   feature worker — the quadratic-arm dependency #4611
   (exponent extraction) is the next critical-path piece (#4604
   blocks on it; the three remaining arms — small-mod singleton,
   fast-quadratic, slow-quadratic — all need #4604 plus, for the
   singleton, #4597).
2. Wait on #4561 (slow exhaustive arm umbrella, claimed). Once it
   lands the per-arm roster is complete and the residual is the
   per-arm `hcomplete`-drop pattern plus the capstone assembly.
3. Re-dispatch #4334 (HexLLL Phase 4 verdicts) only when quiet or
   dedicated hardware is available per `SPEC/benchmarking.md`.
4. Trigger the `replan` triage on directives #3899 (HO-43) and
   #3900 (HO-44) — both still carry the same orphan-deletion
   residual as the previous two checkpoints.
5. Pick up the HexGramSchmidt #3977 → #4053 → #3899 bridge chain
   when an agent with Mathlib-bridge bandwidth is free; three
   checkpoint cycles without movement.
6. The next planner should refresh #4170's status section: items 1
   and 2 of its "post-replan, 2026-05-17" listing have been landed
   substrate-wise for two checkpoints now, and the per-arm
   umbrella picture has changed substantially since that body was
   last revised.

## Blockers

No blocker for this checkpoint.
