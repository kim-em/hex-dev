# Factor irreducibility strategic fork review

## Accomplished

Reviewed the current capstone fork for `factor_irreducible_of_nonUnit`.
Read the recent conversation-log tail and the relevant code paths in
`HexBerlekampZassenhaus/Basic.lean`,
`HexBerlekampZassenhausMathlib/Basic.lean`,
`HexBerlekampZassenhausMathlib/FactorSoundness.lean`,
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`, and
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

Confirmed that `Hex.factor` is the total three-tier dispatcher
`factorWithBound f (defaultFactorCoeffBound f)`: fast if
`factorFastWithBound` succeeds, otherwise slow modular if it succeeds,
otherwise slow trial. The existing assembled theorem
`factor_entries_irreducible` only needs a branch-local raw-factor
irreducibility hypothesis for the actually chosen raw array; it does not
require proving `factorFastFactorsWithBound` or `factorFastWithBound` is
non-`none` globally.

Also confirmed that the local checkout has the older M2/toMonic
irreducibility surfaces but not the M1 names mentioned in the prompt, so the
recommendation treats those APIs as landed upstream context.

## Current frontier

The best short path for the capstone is branch-local assembly of the
`h_raw` hypothesis consumed by `factor_entries_irreducible`, with the fast-core
success arm supplied either by an M2-compatible cut or by a narrow M2-success
to M1-witness bridge. The full fast-success termination tower is not needed
for this capstone.

## Next step

Implement the capstone by case-splitting the default-bound raw-source
disjunction:

- fast `some rawFactors`: split fast branch shape and discharge constant,
  small-mod singleton, quadratic, and fast-core success arms;
- slow modular after fast `none`: use `slowModularRaw_irreducible_of_fast_none`;
- slow trial after fast `none`: use
  `factorSlowTrialFactorsWithBound_factor_irreducible_of_fast_none`, with the
  zero-input contradiction from fast `none`.

For the fast-core success arm, prefer a narrow producer from the actual M2
success to the proof package consumed by the existing irreducibility cut, rather
than swapping the executable to M1.

## Blockers

The checkout searched during this turn did not contain the stated landed M1
declarations (`RecoveredAtLiftM1`,
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredM1`,
`ForwardRecoveryInputsCore`, `coreRecover?_eq_some_of_forwardInputsCore`), so I
could not inspect their exact argument order locally.
