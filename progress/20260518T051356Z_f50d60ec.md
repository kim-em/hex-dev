# 2026-05-18 05:13 UTC — planner cycle: scaled _some_factor_associated_ public wrappers (f50d60ec)

Planner session.  `POD_MIN_QUEUE=3`, `POD_QUEUE_DEPTH=2`,
`POD_QUEUE_DEFICIT=1`.

## Accomplished

* Oriented: 3 open directives (#2637, #2567, #2564), one
  `repair-claimed` PR (#4917), no merge-ready agent PRs to land
  (PR #2656 is a human-authored spec PR, left alone).
* Surveyed the open `_of_bound` chain: per-T (#4918/#4919),
  exists_repSubset_of_mem_T (#4924/#4925/#4926), coverAtMin
  (#4927/#4929/#4930), prefix_none (#4931/#4932), and
  `_some_and_covers_` (#4934/#4935).  Identified the next-layer-up
  target: the public `_some_factor_associated_*` forwarders at lines
  13850 and 13894 of `HexBerlekampZassenhausMathlib/Basic.lean`,
  which #4935 already lists as out-of-scope follow-on.
* Verified call-site routing: only the scaled-primitive flavour at
  13850 and the `_of_primitive_pos_lc_core` wrapper at 13894 have
  in-tree consumers; the monic public wrapper at line 13808 has none
  (grep -rn confirms zero call sites), so left it out of this issue.
* Created issue **#4936**: "BZMathlib: abstract-bound siblings of
  scaled `_some_factor_associated_` public wrappers (primitive +
  primitive_pos_lc_core)", depends-on #4935.  Two `_of_bound`
  siblings + two thin-wrapper rewires in
  `HexBerlekampZassenhausMathlib/Basic.lean`.
* Plan body: `plans/f50d60ec-1.md`.

## Current frontier

The `_of_bound` chain is now planned up to and including the public
`_some_factor_associated_*` wrapper layer on the scaled-primitive
side.  The next consumer above
(`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
at line 13958, with the `_of_henselSubsetCorrespondence` wrapper just
above it) is the natural target once #4936 lands.

## Next step

For the next planner cycle: if `POD_QUEUE_DEFICIT > 0` and #4934
remains open, consider filing a sibling-of-monic issue for the public
monic forwarder at line 13808 — but only if a downstream consumer
exists.  More naturally, plan the `_of_bound` sibling for
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
once #4936 lands.

## Blockers

* Issue #4936 itself is `blocked` on #4935, which is blocked on
  #4932 → #4930 → #4926 → (#4923 - already merged).  The unblock
  cascade requires four claimed-and-merged feature issues before
  #4936 becomes workable.
* No directive closures in this cycle.  HO-1 critical path remains
  gated on #2567.
