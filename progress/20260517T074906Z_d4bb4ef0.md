# Session d4bb4ef0 — work / feature, decomposed #4593

## Accomplished

- Claimed #4593 (HO-1 substrate: discharge `reassemblyExpansionComplete`
  for the small-mod singleton arm).
- Surveyed the Mathlib-free UFD machinery on `Hex.ZPoly` and concluded
  the issue as written cannot be executed in one session: it asks for
  a structural lemma "`(normalizeForFactor f).repeatedPart` is a power
  of `(normalizeForFactor f).squareFreeCore` when the latter is
  irreducible", which requires UFD reasoning (irreducible-implies-prime,
  `normalizedFactors`, "square-free core captures all distinct
  irreducible factors of the primitive part") that does not exist on
  `Hex.ZPoly`. The cited decomposition invariants
  (`primitiveSquareFreeDecomposition_squareFreeCore` — `SquareFreeRat`
  only; `_squareFreeCore_repeatedPart_primitive` — product primitivity
  only) are insufficient.
- Identified the Mathlib bridge as a viable alternative:
  - `Hex.ZPoly.polynomialIrreducible_iff_irreducible`
    (`HexBerlekampZassenhausMathlib/Basic.lean:195`)
  - `IntReductionMod.normalizeForFactor_squareFreeCore_toPolynomial_squarefree`
    (`HexBerlekampZassenhausMathlib/IntReductionMod.lean:872`)
  - `primitiveSquareFreeDecomposition_reassembly_signed`
    (`HexPolyZ/Basic.lean:3329`)
  give the structural fact via Mathlib UFD on `Polynomial ℤ`.
- Opened sub-issue #4597
  ("HO-1 substrate: discharge reassemblyExpansionComplete for the
  small-mod singleton arm (Mathlib-bridge variant, supersedes #4593)")
  with a detailed proof outline that splits the work into:
  (1) Mathlib-side structural lemma using `Polynomial ℤ` UFD;
  (2) Mathlib-free expansion helper
      `expandRepeatedPartFactorArray_pow_singleton`;
  (3) the public discharger composing the two.
- Posted `Decomposed into #4597` breadcrumb on #4593 explaining the
  Mathlib-free / Mathlib-bridge split, then `coordination skip`ped
  #4593 for planner triage.

## Current frontier

- #4597 awaits a worker who will execute the Mathlib-bridge discharger.
- Sibling arms still pending:
  - constant arm: #4585 (claimed by `c45cfdca`).
  - quadratic arms: #4594 (unclaimed).
- HO-1 capstone integration (#4170) blocked on all four arm
  dischargers landing.

## Next step

Next worker on #4597 should:
1. Search Mathlib for the sharpest "irreducible squarefree element
   plus its associate factor → other side is a power" lemma; if
   absent, derive via `UniqueFactorizationMonoid.normalizedFactors`
   cardinality (parallel to `UFDPartition.lean` patterns).
2. Implement the Mathlib-free expansion helper first — it's the
   smallest concrete piece and validates the overall shape.
3. Confirm the leading-coefficient sign argument forces the unit in
   the structural lemma to be `+1` (uses
   `squareFreeCore_leadingCoeff_pos_of_ne_zero` and
   `primitiveSquareFreeDecomposition_leadingCoeff_repeatedPart_nonneg`).

## Blockers

None for #4597 itself; it is self-contained and unblocked.

`reassemblyExpansionComplete` discharge for the quadratic arms
(#4594) faces the same UFD-machinery question — the same Mathlib-bridge
route should apply, but the planner there may want to mirror the
#4597 restructure once #4597 lands and the pattern is validated.
