# Review session 0740f0b7 — HexHensel quadratic multifactor Phase 6 API

## Accomplished

- Claimed issue #3573 (HexHensel quadratic multifactor Phase 6 API review).
- Found four private theorems duplicated byte-for-byte between
  `HexHensel/Multifactor.lean` and `HexHensel/QuadraticMultifactor.lean`:
  `one_mul_zpoly`, `polyProduct_singleton`,
  `list_foldl_mul_eq_mul_foldl_one`, and `polyProduct_singleton_append`.
- Hoisted the four shared helpers in `HexHensel/Multifactor.lean` to
  be non-private and gave each a focused docstring. `QuadraticMultifactor.lean`
  imports `Multifactor.lean` and is in the same `Hex.ZPoly` namespace, so its
  callsites resolve unchanged.
- Deleted the duplicate copies in `HexHensel/QuadraticMultifactor.lean`.
- Verified naming/hypothesis parallelism between linear and quadratic specs
  (`henselLift_spec` vs `henselLiftQuadratic_spec`, `multifactorLift_spec`
  vs `multifactorLiftQuadratic_spec`). The quadratic side correctly drops
  `[PrimeModulus p]` (not needed) and keeps `_hk : 1 ≤ k` underscored for
  API parity with the linear surface.
- Built `HexHensel.Multifactor`, `HexHensel.QuadraticMultifactor`, `HexHensel`,
  and `HexBerlekampZassenhaus.Basic` to confirm no downstream breakage.
- Confirmed `python3 scripts/status.py HexHensel` still reports Phase 6 and
  `python3 scripts/check_dag.py` is clean; banned-marker scan empty.

## Current frontier

- `HexHensel/QuadraticMultifactor.lean` shrank from 372 to 340 lines.
- The quadratic multifactor API surface is now duplication-free against
  `Multifactor.lean`.

## Next step

- Push branch and open PR closing #3573.
- Note: a separate duplication still exists between
  `HexHensel/Linear.lean`'s private `congr_mul_reduceModPow_pair` and the
  identical helper in `QuadraticMultifactor.lean`. Linear.lean is out of
  scope for this issue; a future Phase 6 review on `Linear.lean` can
  hoist that helper similarly.

## Blockers

- None.
