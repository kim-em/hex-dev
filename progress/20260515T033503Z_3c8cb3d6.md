# progress 2026-05-15T03:35:03Z (agent 3c8cb3d6)

Worked on issue #4126 — HexBZ exhaustive: expose `recombinationSearchMod`
recovery completeness.

## Accomplished

- Promoted the slow-path recombination defs (`subsetSplits`,
  `subsetSplitsWithFirst`, `firstSome`, `recombinationSearchMod`,
  `recombinationSearchModAux`, `recombineExhaustive`) from `private` to public
  with module-doc comments, so the Mathlib bridge can reference them when
  chaining completeness lemmas.
- Added enumeration helpers in `HexBerlekampZassenhaus/Basic.lean`:
  - `subsetSplits_nil_mem`, `subsetSplits_cons_left_mem`,
    `subsetSplits_cons_right_mem` — inductive constructors that build
    `subsetSplits` membership from a list-shaped order-preserving partition.
  - `firstSome_isSome_of_mem` — existence companion to the already-present
    `firstSome_some`; reduces showing a `firstSome` is `isSome` to exhibiting
    one element that already succeeds.
  - `subsetSplitsWithFirst_mem_cons` was promoted to public to round out the
    "build a split that begins with the head" entry point.
- Added the Mathlib-free completeness theorem
  `recombinationSearchModAux_isSome_of_step`: given a single candidate from
  `(selected, rest) ∈ subsetSplitsWithFirst localFactors` whose centred-lift
  passes `shouldRecordPolynomialFactor` and exactly divides the running target,
  and given an `isSome` witness for the recursive search on the residual at the
  smaller fuel, the search at this step is itself `isSome`.
- Added the `recombinationSearchMod`-level wrapper
  `recombinationSearchMod_isSome_of_step` that specialises fuel to
  `localFactors.length`, and the connector
  `recombineExhaustive_eq_of_recombinationSearchMod_some` so consumers can
  replace `recombineExhaustive` with the recovered factor array once they
  have the search result.
- Added the trivial base lemma `recombinationSearchModAux_one`
  (`target = 1` ⇒ search returns `some []`) so an inductive proof can close
  the empty-residual case.

## Verification

- `lake build HexBerlekampZassenhaus.Basic` — clean.
- `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib` — clean
  (only pre-existing sorries remain).
- `python3 scripts/check_dag.py` — passes.
- `git diff --check` — clean.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`
  introduced (`Basic.lean` still has exactly one `sorry`, on the same
  declaration as before — line number shifted only because of the inserted
  doc comments).

## Current frontier

Group A coverage in `HexBerlekampZassenhausMathlib/Basic.lean`: the bridge now
has the executable hooks needed to argue that each irreducible integer factor
of `core` actually appears in
`Hex.exhaustiveCoreFactorsWithBound core B primeData` — namely #4127.

## Next step

#4127 picks up: use
`existsUnique_recoveringLiftedFactorSubset_at_defaultPrecision` together with
the step lemma added here to prove the coverage statement for the exhaustive
core branch.

## Blockers

None.
