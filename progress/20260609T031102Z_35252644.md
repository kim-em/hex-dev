# Deterministic BZ hot-path prime walk (#6601)

## Accomplished

Claimed #6601 after releasing stale directive #2567 with an issue comment
showing that its unconditional `factorFast` termination target is stale against
the current conditional SPEC D1.

Updated `HexBerlekampZassenhaus/Basic.lean` so the production hot-path prime
surface is static and deterministic:

- expanded `smallPrimeCandidates` to all 19 odd primes in `[3, 71]`;
- added `extendedSmallPrimeCandidates` for the 75 odd primes in `[73, 500]`;
- added `hotPathCandidates` for test-facing/scoring surfaces;
- changed `choosePrimeData?` to scan the small prefix first, then the fixed
  extended list, with no input-dependent fueled post-prefix walk on the search
  path;
- updated the `choosePrimeData?` provenance theorems to use the extended-list
  fold;
- added guards for the 19/75/94 candidate split and for support of prime 29.

Updated `HexBerlekampZassenhaus/Conformance.lean` guards and comments for the
new 29 candidate behavior.

Verification completed:

- `lake build HexBerlekampZassenhaus.Conformance`
- `/opt/homebrew/bin/timeout 600 lake exe hexbz_bench verify`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over touched Lean files

All completed checks passed.

## Current frontier

Full `lake build HexBerlekampZassenhaus` and
`lake build HexBerlekampZassenhausMathlib` were not completed locally because
both spent several minutes compiling `HexBerlekampZassenhaus.CrossCheck` after
the edited `Basic` and `Conformance` modules had already built. The runs were
stopped to avoid leaving long verification processes active.

## Next step

Open the PR for #6601. CI or a dedicated follow-up run should cover the full
CrossCheck-dependent package targets.

## Blockers

No implementation blocker. The only local verification gap is the long
`HexBerlekampZassenhaus.CrossCheck` compile path.
