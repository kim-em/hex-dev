# Accomplished

- Replaced the fixed sine/negation/transport proof assembly with a generic
  chronological fold over quoted fact events.
- Added an exact `(node, version)` evidence table. Rule and transport replay
  now resolve their previous facts and ordered dependencies from this table,
  select schemas by full replay address, and insert their proved result.
- Added generic empty dependency assembly and seeded the canary with distinct
  caller-assumption and generated-node-top proofs.
- Added a negative chronology test which reorders negation before its sine
  input and confirms that proof emission fails.
- Updated the SPEC to separate the function-independent event fold from the
  remaining fixed graph, program-extension, initial-fact, and target seams.
- Built `HexIntervalMathlib.SineTacticConformance` and ran copyright,
  line-count, DAG, diff, and banned-proof checks successfully.

# Current frontier

The proof emitter is now independent of the mathematical function for every
fact-producing event after the first instantiation. The real-sine canary still
uses a fixed initial graph and fixed constants to seed version-zero evidence,
and its instance replay still assumes one leading program extension.

# Next step

Derive the initial evidence table from the checked caller input and checked
program, then integrate generic program reconstruction so later and repeated
instantiations can update the program and seed new nodes at their exact
chronology positions.

# Blockers

None in this branch. The stacked PR base must eventually be reconciled with
the repaired exact head of PR #9201 before merge.
