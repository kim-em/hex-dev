# Issue #5217: Int coefficient zero-law wrappers

Session: 4a3fb8fb-581d-4697-8c23-1297927398b0, type work/feature.

## Accomplished

- Claimed #5217 after confirming all open directive issues were blocked.
- Added `DensePoly.AddZeroLaw Int`, `DensePoly.SubZeroLaw Int`, and
  `DensePoly.ZeroSubNegLaw Int` instances in `HexPolyZ/Basic.lean` before
  `namespace ZPoly`.
- Migrated all eight generic-shape Int coefficient rewrites in
  `HexHensel/Linear.lean` to `DensePoly.coeff_add_semiring` /
  `DensePoly.coeff_sub_ring`.
- Verified:
  - `lake build HexPolyZ.Basic`
  - `lake build HexHensel.Linear`
  - `lake build HexHensel`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - no added `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`
  - no remaining `DensePoly.coeff_(add|sub|neg) _ _` matches in
    `HexHensel/Linear.lean`
  - all three Int zero-law instances present in `HexPolyZ/Basic.lean`

## Current frontier

The Int-coefficient migration requested by #5217 is complete. The sibling
Rat-coefficient migration in `HexPolyZ/Basic.lean` remains open under #5218.

## Next step

Open the PR for #5217 and let the auto-merge queue handle it if CI stays
green.

## Blockers

None.
