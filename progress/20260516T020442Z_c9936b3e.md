# 2026-05-16T02:04Z — `/work` session, claimed #4359

## Accomplished

Added the Mignotte-precision canonical-indicator wrappers in
`HexBerlekampZassenhausMathlib/Recovery.lean`:

- `ForwardRecoveryInputs.ofMignottePrecisionCanonicalIndicators`, which wraps
  `ofMignottePrecisionCandidateProducts` with
  `expectedIndicatorArrayOfSupports trueSupports` and discharges
  `indicators_match` via
  `equivalenceClassIndicatorsOfLiftData_eq_expectedIndicatorArrayOfSupports`.
- `ForwardRecoveryInputs.ofMignottePrecisionCanonicalIndicatorsExpectedFactors`,
  which mirrors `ofMignottePrecisionExpectedFactors` for the canonical
  support-driven indicator array and extracts the product check from
  `ExpectedTrueFactors`.

## Current frontier

The Mignotte chain now has the same canonical-indicator convenience surface as
the cap-separation chain. HO-4 consumers can avoid restating the B7
indicator-array plumbing when they already have `trueSupports` and
`lattice_eq_indicators`.

## Next step

The adjacent capstone issue can compose the cap-level canonical-indicator
constructor with `factorFast_ne_none_of_forwardInputs_at_cap`, and later HO-4
leaf work can choose between the cap-separation and Mignotte entry points.

## Blockers

None.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — succeeds.
- `lake build HexBerlekampZassenhausMathlib` — succeeds with pre-existing
  warnings in unrelated modules.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `rg -n "\baxiom\b|native_decide|TODO|FIXME|sorry" HexBerlekampZassenhausMathlib/Recovery.lean`
  — no matches.
