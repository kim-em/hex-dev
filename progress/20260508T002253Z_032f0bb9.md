# Progress — 2026-05-08T00:22 UTC — agent/032f0bb9

## Accomplished

Claimed issue #2628 and added optional pinned modular-factor metadata for
`HexBerlekampZassenhaus` poly fixtures:

- added `modFactorPrime` / `modFactorDegrees` emission support in
  `Hex.Conformance.Emit`;
- updated `HexBerlekampZassenhaus/EmitFixtures.lean` with one metadata-bearing
  smoke case, `adv/x4_plus_1`, pinned at `p = 5` with degree multiset `[2, 2]`;
- extended `scripts/oracle/common.py` validation for the optional fields;
- extended `scripts/oracle/bz_flint.py` to independently factor the input
  modulo the pinned prime and check the sorted irreducible factor degree
  multiset before the existing integer factor signature check;
- regenerated `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl`.

Verification run:

- `lake exe hexbz_emit_fixtures`
- `python3 scripts/oracle/bz_flint.py --check`
- `python3 -m py_compile scripts/oracle/bz_flint.py`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

The BZ JSONL/oracle surface can now detect whether future adversarial fixtures
really have the intended pinned modular split, while existing plain `factor`
fixtures remain valid.

## Next step

Use the new metadata fields when adding the remaining HO-2 adversarial fixtures
from #2565: `adv/quad_sqrt2_sqrt3`, `adv/swinnerton_dyer_sd3`, and
`adv/phi15`, once their implementation prerequisites are ready.

## Blockers

None for this issue. The broader HO-2 fixture corpus still depends on the
corresponding implementation work described in #2565.
