# Progress: #6781 probe, #6782 packed certificate gate removal

## Accomplished

Claimed #6781 first and ran the requested boundary probe on `carica`.
Baseline public/exact dispatch timings confirmed the harsh-cubic n=35
non-monotonicity, but the obvious `steerDimThreshold := 30` experiment made
random-bounded n=30 regress from 15.315 ms to 24.650 ms and caused
`runSteeredFallbackTally` to fail. Posted the concrete table on #6781 and
released it for replan; no code from that experiment remains.

Claimed #6782 next. Removed the `mulEqCert` word-scale/dimension regime gate
from `HexLLL/Basic.lean`, deleted the unused `wordScale` scan, and simplified
`mulEqCert_iff` to the single packed proof path. Updated
`SPEC/Libraries/hex-lll.md` so the certified external-dispatch clause describes
unconditional packed product equality while retaining the word-scale cost
benefit and wide-entry overhead caveat.

Ran paired certified-checker measurements with `fplll-ffi` loaded:
`/tmp/hexlll-unpack-before.json` at 7c35354 and
`/tmp/hexlll-unpack-after2.json` at the branch head. All observed hashes were
identical. Two marginal outliers in full sweeps were rerun singly and landed
inside the directive bounds: harsh-cubic n=35 at 10.354 ms and random-bounded
n=75 at 41.120 ms.

Used the `second-opinion` skill for a final review; it endorsed the proof
change and asked that the SPEC keep the scan/repacking overhead caveat, which
is now included.

## Current frontier

`lake build HexLLL HexLLLMathlib` passes. `HEX_FPLLL_FFI_LIB="$(scripts/oracle/setup_fplll_ffi.sh)" lake exe hexlll_bench verify`
passes all 153 benchmarks. `python3 scripts/check_dag.py`, `git diff --check`,
and the added-line forbidden-token grep all pass.

The first `lake exe hexlll_bench verify` without `HEX_FPLLL_FFI_LIB` failed
only on provider-absent targets; the provider-backed rerun passed.

## Next step

Commit the #6782 changes and open a PR closing #6782. The PR body should
include the before/after export paths, note identical hashes, and call out the
two single-target reruns used to resolve marginal timing noise.

## Blockers

None for #6782. #6781 needs replanning because its random-bounded n=30
go/no-go premise failed under the direct threshold-30 experiment.
