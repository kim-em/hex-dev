# 20260602T131320Z 526922ae — Issue #6342

## Accomplished

- Claimed feature issue #6342.
- Added actual-cap BHKS recovery packaging in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  `badVectorWitnessOfFactorFastCapLiftData`,
  `FactorFastCapLiftAnalyticComparison`,
  `capSeparationOfBridgeDataAtFactorFastCapLift`, and
  `projectedRowSpan_eq_trueFactorIndicatorLattice_of_factorFastCapLift_bridge`.
- The new wrapper specializes the existing `badVectorWitnessOfLiftData` /
  `BadVectorBridgeData` / `ExecutableCapSeparationHypotheses` route to the
  `factorFast` cap lift of the normalized square-free core.

## Current frontier

The actual cap-lift now has a named projected-row-span equality theorem that
consumes the landed bridge package plus a cap-lift-indexed analytic comparison
package, rather than requiring callers to construct the raw cap-separation
record directly.

## Next step

Successor BHKS final-assembly work can provide or further discharge
`FactorFastCapLiftAnalyticComparison` from the auxiliary-polynomial l2norm and
cap arithmetic surfaces, then feed the resulting lattice equality into the
canonical support / expected-factor issues.

## Blockers

- None for this packaging layer.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — passed.
- `lake build HexBerlekampZassenhausMathlib.TerminationBound` — passed.
- `lake build HexBerlekampZassenhausMathlib` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `Recovery.lean` — no matches.
