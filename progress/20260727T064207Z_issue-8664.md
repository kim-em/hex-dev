# Issue #8664 periodic ZMod64 Strassen base kernel

## Accomplished

- Claimed issue #8664 and audited the already-landed accumulator, dot-product,
  base-kernel, and `StrassenConfig.Valid` proofs against the corrected premise.
- Replaced the impractical `2^32` reduction window with a proved-safe,
  operationally testable window and added leaf/full/window-sweep measurement
  coverage across `p = 5`, `65537`, and `2^31 - 1`.
- Updated `barrettBaseMul` for the flat row-major backing so operand rows are
  materialized once, matching `mulImpl` rather than once per output entry.
- Measured the optimized kernel from a clean implementation commit: direct
  leaves clear the gate by `3.02–17.50×`, shipped full Strassen by
  `13.49–15.87×`, and matched-cutoff controls by `5.79–16.72×`.
- Added compiled conformance for high-word carries, the window dispatch and
  flush boundaries, multiple windows with a partial tail, empty axes, and
  rectangular matrices.
- Narrowed timing to multiplication, interleaved configurations, made every
  comparison checksum-validating, and added the local driver to the existing CI
  executable build set.
- Ran the requested independent Opus review and incorporated its substantive
  performance, measurement, coverage, provenance, CI, and documentation findings.
- Passed targeted implementation, conformance, and comparison-executable builds;
  the earlier 9,140-target aggregate conformance build and Mathlib-free bench
  policy check were green before the review-driven refinements.

## Current frontier

The implementation, measurement generator, conformance, SPEC, JSON, and plot
are aligned. One final clean-tree timing run is needed after the generator's
window-sweep checksum was strengthened against `mulImpl` itself.

## Next step

Commit the aligned tree, rerun the comparison from that clean commit, refresh
the evidence if timing values move, rerun full verification, then push, open the
PR, monitor CI, and enable squash auto-merge.

## Blockers

None.
