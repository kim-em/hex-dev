# 2026-06-04T11:43:10Z — Sub-C of #6533: σ-fold ≡ noPivot Bareiss correction

Session UUID prefix: cffe0903
Issue: #6537

## Accomplished

- Added private theorem `schurSigma_foldl_eq_noPivotCorrection` in
  `HexGramSchmidt/Int.lean`, immediately after
  `schurSigma_noPivotCorrection_succ`.
- Statement: at step `q < p_out` of the σ-update fold for position
  `(a, p_out)`, the cumulative fold equals
  `matrix_q[q][q] * gram[a][p_out] - matrix_(q+1)[a][p_out]`.
- Proved by induction on `q`:
  - Base `q = 0`: inlined reproduction of the existing
    `schurSigma_foldl_eq_noPivotCorrection_zero` proof, adapted to the
    weaker `correspondence : ∀ l (hl : l ≤ q)` hypothesis (the existing
    zero helper requires `∀ l < p_out`, which we cannot synthesise when
    `q = 0` only gives `l = 0`).
  - Step `q' → q' + 1`: extend the fold via `List.range'_concat` and
    `List.foldl_append`, apply the IH to rewrite the prefix, then close
    with `schurSigma_noPivotCorrection_succ` supplied with
    - `hdenom`: `noPivotLoop_initial_gram_diag_ne_zero_of_lt` at
      `p = q' + 1`, `q = q'`,
    - `h_step_dvd`: from the matrix-level
      `noPivotLoop_initial_gram_bareiss_step_dvd` at fuel `q' + 1`,
      transported through the step / matrix symmetry, and
    - `h_next`: derived from `Matrix.noPivotLoop_regular_branch` and
      `Matrix.stepMatrix_update_eq` at fuel `q' + 1`, again identifying
      the unfolded `colK, rowK` Fin n indices with `⟨q' + 1, _⟩`.
- `lake build` clean across all targets; no new `sorry`, `axiom`, or
  `native_decide`. Pre-existing sorries at lines 6142 and 6452 of
  `HexGramSchmidt/Int.lean` are unchanged.

## Current frontier

Sub-C done. The σ-fold ↔ Bareiss correction correspondence is now
available as a private helper. Downstream consumers can use it to chain
the algebraic σ-update body with the matrix-level Bareiss trajectory.

## Next step

A parent assembly can use `schurSigma_foldl_eq_noPivotCorrection`
together with an outer induction supplying the `correspondence`
hypothesis (column-by-column row/diagonal sync) to discharge the full
σ-Schur ≡ noPivot Bareiss equivalence at fixed `p_out`.

## Blockers

None.

## Notes

- The directive prescribed "via the existing zero helper" for the base
  case, but the helper's `outer_ih` quantifier is `∀ l < p_out`, strictly
  stronger than the inductive `correspondence` quantifier `∀ l ≤ q`.
  Inlining the zero-helper proof (with `correspondence 0 ...` in place
  of `outer_ih 0 ...`) was the smallest change consistent with the
  inductive structure.
- The matrix-level `step = q' + 1` identification produced repeated
  `motive is not type correct` errors when rewriting Fin n indices
  directly. The workaround is to apply `congrArg` against the
  `Fin.ext h_step_q'_succ` equality at the right Fin n binder, then
  rewrite with the resulting matrix-entry equation. `simp only` is the
  only well-behaved tactic for the `(⟨step, _⟩ : Fin n) = ⟨q'+1, _⟩`
  substitution inside dependent matrix lookups.
- `set` is unavailable in this Mathlib-free file; substituted `let`
  bindings, then later rewrote to inline expressions where the `let`
  blocked `rw [stepMatrix_update_eq]` from firing through proof-bearing
  Fin indices.
- `convert ... using N` is also unavailable (Mathlib tactic), so all
  index-equality transports go through `congrArg` + `rw` or `▸`.
