# 2026-05-21T04:27Z — work issue #5694

## Accomplished

Claimed #5694 after the visible directive issues were rejected as blocked and
the first two regular queue items had already been claimed. Added the
`Matrix.noPivotLoop` preservation package for `BareissGramRowInvariant` in
`HexGramSchmidt/Int.lean`, together with the initial-state specialization for
`Matrix.noPivotInitialState (Matrix.gramMatrix b)`.

Added a consumer-facing extractor that packages an active row of the loop
result as an explicitly represented integer row combination of the original
input rows and rewrites the corresponding Bareiss matrix entry as a dot product
against an original basis row. The recursive package delegates the regular-step
exact-division entry relation to the one-step invariant hook from #5693, and
handles done/singular branches directly.

Verified:

- `lake build HexGramSchmidt.Int`
- `git diff --check`
- `git diff -- HexGramSchmidt/Int.lean | rg -n -e '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

#5694's recursive and initial no-pivot row-invariant packages are available.
The exact-division/dot-product relation remains an explicit regular-step
parameter, matching the one-step package inherited from #5693.

## Next step

Use `noPivotLoop_initial_gram_entry_eq_dot_of_rowInvariant` from downstream
#5677 to turn singular no-pivot trailing entries into dot products against
represented lattice row vectors, supplying the regular-step entry relation at
the call site or factoring it into a separate helper if it proves reusable.

## Blockers

None for this issue.
