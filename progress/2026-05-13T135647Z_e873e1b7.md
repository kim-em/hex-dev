# Session e873e1b7 — #3389 HexPoly: instantiate finite McCoy annihilator for DensePoly

## Accomplished

Added `dvd_last_q_coeff_mul_p_coeff_of_dvd_mul_coeff_of_q_above` to
`HexPoly/Euclid.lean`, the polynomial instantiation of the finite
coefficient-array McCoy annihilator (`finiteCoeffMcCoyAnnihilator`, landed in
PR #3471). Surface matches the issue's required signature: takes `p q :
DensePoly Int`, `d k : Nat`, divisibility of every coefficient of `p * q`,
and `hqAbove : ∀ s, k < s → d ∣ q.coeff s`; concludes
`∀ i, d ∣ q.coeff k * p.coeff i`.

The proof reuses `finiteCoeffMcCoyRow_of_truncated_product_coeff_dvd`
specialised to `pCoeff := p.coeff`, `qCoeff := q.coeff`, `bound := i` — the
content/Gauss row helper already handles all coefficients without an
auxiliary support hypothesis, so `hqAbove` is recorded for downstream
callsite uniformity (the "last non-divisible coefficient" pattern at
`natPrime_dvd_all_or_all_of_dvd_mul_coeff`) but is not consumed inside the
proof. Marked `_hqAbove` to flag this explicitly.

## Current frontier

- `lake build HexPoly.Euclid` ✓
- `lake build HexPoly` ✓
- `python3 scripts/check_dag.py` ✓
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPoly/Euclid.lean` ✓ (no matches)

## Next step

#3346 (HexPoly: wrap McCoy finite-grid descent as scalar annihilator) becomes
the next link in the chain to #3252 → #3127 → … → #3087 → #2803 / #2833.
The new theorem here is the private API piece those downstream wrappers
will lift.

## Blockers

None.
