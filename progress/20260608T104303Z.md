# 2026-06-08 10:43Z — fplll-ffi plan review

## Accomplished

- Reviewed the proposed `kim-em/fplll-ffi` plan against the local
  `/tmp/soplex-ffi` and `/tmp/csdp-ffi` reference repositories.
- Checked the installed fplll 5.5.0 headers and pkg-config metadata for the
  LLL API shape, transform semantics, default parameters, and link flags.
- Identified likely execution risks around static LGPL linkage, autotools/PIC
  builds, cross-platform C++ runtimes, GMP/MPFR library identity, transform
  orientation, and unchecked FFI marshalling.

## Current frontier

The plan is directionally viable, but the high-risk work is native build and
runtime packaging rather than the Lean-facing wrapper. The review response
captures concrete fixes to fold into the plan before implementation.

## Next step

If proceeding, start with the spike but expand its pass criteria to include
library identity/link audit, exception ABI canary, transform orientation, and
both Linux/macOS command lines before committing to the repository layout.

## Blockers

No implementation blockers encountered; this was a review-only turn.
