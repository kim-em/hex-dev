# Accomplished

- Restacked the proof-payload session on the arbitrary-scope matcher and
  payload-arena integration, preserving dynamic scope proposals and the exact
  structural matcher action during payload relocation.
- Made the proof arena and engine-owned matcher cursor commit atomically for
  accepted replies and roll back together after semantic rejection, engine
  resource refusal, fact-domain resource refusal, or cumulative arena
  exhaustion.
- Added executable package tests in which the package callback itself
  recognizes its operation in an engine-issued structural batch and returns a
  payload-bearing instantiation proposal.
- Added failure canaries for invalid matcher replies, matcher requeue
  exhaustion, malformed package evidence, cumulative proof-arena exhaustion,
  and fact-domain resource refusal. The malformed-evidence canary documents
  the intentional exception: its synthetic failed reply consumes the batch,
  retains no payload, and permanently marks the run incomplete.
- Updated older scope and matcher conformance callers for the registry's
  complete engine-plus-payload limit preflight.
- Built the full 29-job interval, scope, matcher, policy, registry, dyadic, and
  payload target set successfully. DAG, diff-hygiene, and added-line
  `native_decide`/`axiom`/`sorry` checks pass.

# Current frontier

The package invocation, proof-payload freezing, engine reply, and matcher
cursor now share one session-owned transaction. The lower matcher scheduler
still needs its independent review fixes for network growth during an active
epoch, retry visit accounting, and structural policy accounting before this
integration branch is restacked on its final head.

# Next step

Land the reviewed matcher-scheduler fixes, merge that head into this branch,
rerun the transaction canaries, and then connect the proof-producing policy
and semantic replay layers.

# Blockers

None.
