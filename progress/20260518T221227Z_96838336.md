# 96838336 — HexMatrix entry simp lemmas (issue #5124)

## Accomplished

- Claimed feature issue #5124 after confirming there were no unclaimed
  directive issues.
- Added public docstringed `@[simp]` lemmas in `HexMatrix/Basic.lean` for
  entry access through `Matrix.ofFn`, `Matrix.row`, `Matrix.col`, and
  `Matrix.transpose`.
- Added `@[simp] theorem Matrix.transpose_transpose` as a direct consequence
  of the new transpose entry lemma.
- Rewired the local `transpose_mul_of_mul_comm` and `transpose_one` proofs to
  use `transpose_getElem` instead of unfolding `transpose` and `col` directly.
- Ran `lake build HexMatrix.Basic`, `lake build HexMatrix`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested grep
  invariants; all passed.
- Checked the Lean diff for new `sorry`, `axiom`, `native_decide`, `TODO`, and
  `FIXME`; none were introduced.

## Current frontier

Issue #5124 deliverables are complete. The broader determinant, Bareiss, RREF,
span, and nullspace surfaces were intentionally left untouched.

## Next step

Publish the PR for #5124 with the focused proof/API polish and this progress
note.

## Blockers

None for #5124. The worktree still contains a pre-existing `.claude/CLAUDE.md`
modification that this session did not touch or stage.
