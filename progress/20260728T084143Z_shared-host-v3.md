# Accomplished

- Merged the designated-shared-host-v2 protocol in PR #9043 after green CI
  and independent review.
- Completed a six-round HexRealRootsMathlib sweep on `chungus2` CPU 22 and
  retained its rejected diagnostic artifact at
  `/tmp/real-roots-mathlib-shared-235cb0c4.json`.
- Traced the systematic pinned-CPU residual to `/proc/stat` IRQ/softirq ticks,
  which process `getrusage` cannot contain; after separating those ticks, 107
  of 108 recorded arms are below the existing foreign-work ceiling.
- Implemented designated-shared-host-v3 accounting and bounded whole-pair
  retries without changing the nominal interference ceiling or tactic budgets.
  Each attempt builds both arms in the fixed orientation, and only a wholly
  clean adjacent pair enters results.
- Retained the raw and signed IRQ-adjusted pinned-CPU residuals, and made
  pinned-CPU foreign time plus all SMT-sibling busy time share one conservative
  admission allowance.
- Made retry exhaustion emit an explicit incomplete artifact with every
  rejected pair attempt and no contaminated samples in summaries or budgets.
- Added a bounded quiet-core preflight before every pair attempt. It waits out
  sustained target/sibling bursts without spending the finite build-retry
  budget, retains every rejected observation, and fails to a partial artifact
  if the five-minute wait expires.
- Bumped the RealRootsMathlib and HexRCF proof-artifact schemas and documented
  the accounting/retry contract.
- Extended focused coverage to 71 tests. Phase-4, DAG, copyright, line-count,
  trust-surface, Mathlib-free bench, Python compilation, and diff checks pass.
- A live Baseline/Natural10 pair on CPU 22 rejected one contaminated complete
  pair, then admitted the second adjacent pair with complete accounting. The
  admitted aggregate interference was 0.0252 s and 0.0026 s.
- A live preflight pilot waited through eight busy two-second windows, rejected
  one subsequently contaminated complete pair, and admitted the next pair.
- Retained an explicit incomplete diagnostic artifact from the pre-preflight
  release attempt at `/tmp/real-roots-mathlib-shared-6a16367c.json`; it contains
  no contaminated result summaries.

# Current frontier

The corrected v3 protocol is implemented and locally validated on branch
`issue-8972-shared-host-v3`. PR #9045 needs a final exact-head independent
review and green CI before merge.

# Next step

Amend and review v3, update PR #9045, then run the same preregistered
HexRealRootsMathlib sweep from its clean merged commit while CI proceeds.

# Blockers

None.
