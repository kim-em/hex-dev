# 2026-05-07 17:34 UTC — HexGfq Phase 4 benchmarks (1c0000e8)

## Accomplished

Claimed #2583 and added `HexGfq/Bench.lean` with lean-bench registrations for
the public HexGfq constructor/conversion surface. The benchmark executable now
covers fixed committed-entry modulus helpers, fixed `GF2q.ofWord`/`GF2q.repr`
on the only public packed Conway entry, parametric `GFq.ofPoly`/`GFq.repr`,
and a parametric packed-vs-generic checksum over shared binary inputs.

Registered `hexgfq_bench` in `lakefile.lean`, added its `list`/`verify` smoke
gate to the existing single ubuntu CI bench step, bumped
`HexGfq.done_through` to 4, and added `status/hex-gfq.benchmarks-reviewed`.

Verification passed:

- `lake build HexGfq.Bench`
- `lake build HexGfq`
- `lake exe hexgfq_bench list`
- `lake exe hexgfq_bench verify`
- `lake exe hexgfq_bench run Hex.GfqBench.runGFqOfPolyReprChecksum`
- `lake exe hexgfq_bench run Hex.GfqBench.runPackedGenericSharedChecksum`
- `python3 scripts/check_phase4.py --base origin/main`
- `python3 scripts/status.py HexGfq`
- `python3 scripts/status.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|\bsorry\b' HexGfq/Bench.lean`

## Current frontier

HexGfq now reports Phase 5 ready. The supported `check_phase4.py --all` mode
still reports pre-existing missing cost-model comments in older benchmark
modules outside this issue; the changed-registration check for this branch
passes.

## Next step

Open the PR for #2583 with the scientific run verdicts in the body.

## Blockers

The worktree still contains the pre-existing pod-managed `.claude/CLAUDE.md`
modification, left untouched.
