# HexMatrix Phase 6 — basic multiplication automation polish

Session: `7f834b32` (work, issue #5590)

## Accomplished

Reviewed the `HexMatrix/Basic.lean` multiplication and identity theorem
cluster. Kept associativity and transpose-reversal unannotated because they
are not terminating normalization rewrites.

Added `@[grind =]` to stable entry-characterization lemmas for `ofFn`, row,
column, transpose, matrix-vector multiplication, matrix multiplication, and
identity entries.

Promoted terminating multiplication normal forms to `@[simp, grind =]`:
`one_mulVec`, `one_mul`, `mul_one`, and `mulVec_zero`.

Added one small wrapper lemma, `zero_mulVec`, for the missing zero-matrix
matrix-vector normalization.

## Current frontier

`HexMatrix.Basic` now exposes the basic multiplication surface more directly
to `simp` and `grind`, without changing executable definitions or touching
bridge-only determinant/Bareiss material.

## Next step

A future Phase 6 slice can review row-operation multiplication lemmas
(`rowSwap_mul`, `rowScale_mul`, `rowAdd_mul`) for similarly conservative
automation annotations.

## Blockers

None for this slice.

Verification completed:

- `lake build HexMatrix.Basic`
- `lake build HexMatrix`
- `lake build HexLLL` (passes; reports existing warnings in earlier-phase
  GramSchmidt/LLL modules)
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexMatrix/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches
