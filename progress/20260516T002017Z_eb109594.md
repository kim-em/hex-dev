# 2026-05-16 00:20 UTC - #4301 partial

## Accomplished

Started by claiming #4340, then verified it was stale: its prerequisite #4339 was
closed as planning-stale rather than implemented, and the expected Round 3a
relocations still remain in `HexGramSchmidt/Int.lean`. Marked #4340 for replan
with that blocker. #4341 was already claimed by another agent.

Claimed #4301 and added
`recombinationSearchModAux_first_success_witness_of_liftedSubset_candidate_eq_factor_of_matches`
in `HexBerlekampZassenhausMathlib/Basic.lean`. The helper is the exact-output
matched-rest companion to the existing isSome bridge: given a matched
remaining-index state and a proof that the selected split is the first
successful split, it returns the concrete `some` result and an emitted factor
associated to the represented factor.

Verification:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n 'native_decide|TODO|FIXME|^\s*axiom\b|^\s*sorry\b' HexBerlekampZassenhausMathlib/Basic.lean`

## Current frontier

#4301 still needs the full recursive
`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
theorem. The new helper covers the branch once the proof has selected a
first-success split and proved recursive rest success; it does not yet handle
earlier successful splits or perform the partition transport/induction.

## Next step

Use the helper inside the `LiftedFactorSubsetPartition` induction. The remaining
work is to reason over the `subsetSplitsWithFirst` prefix: if an earlier split
succeeds, recover an associated emitted factor from that branch; otherwise use
the new helper at the chosen subset and recurse with
`liftedFactorSubsetPartition_transport` plus `LiftedFactorListMatches.sdiff_of_subset`.

## Blockers

No blocker for the helper. The full recursive theorem remains a larger proof
over first-success traversal and partition transport.
