## Accomplished

Claimed and completed #3017, the exact executable `X`-power extraction
product layer for Berlekamp-Zassenhaus normalization.

Added `DensePoly.monomial_one_mul_poly_eq_shift` in `HexPoly/Euclid.lean`,
packaging the existing diagonal multiplication machinery into a reusable
unit-monomial shift theorem.

In `HexBerlekampZassenhaus/Basic.lean`, added private helpers showing:

- `DensePoly.shift 0` is identity on `ZPoly`;
- `DensePoly.ofCoeffs f.toArray = f`;
- repeated `X` factors multiply to coefficient shifts;
- `splitInitialZeros` reassembles its stripped coefficient list;
- `extractXPower_product`, the requested product preservation theorem.

Verified:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexPoly`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean HexPoly/Euclid.lean`

The grep reports only pre-existing `sorry`s in `HexBerlekampZassenhaus/Basic.lean`;
no new bad markers were added in the touched proof region or in
`HexPoly/Euclid.lean`.

## Current frontier

`extractXPower_product` is available in `HexBerlekampZassenhaus/Basic.lean`
for the normalization reassembly proof path.

## Next step

Use the new extraction product theorem with the existing content and
square-free decomposition product layers to close `normalizeForFactor_reassembles`.

## Blockers

None.
