# 20260517T120000Z — review session 9184c97e (#4581)

## Accomplished

Closed #4581: HO-1 review of PR #4574
(`factorsModP_coprime_of_factorsModPBerlekampForm` discharger plus
abstract Berlekamp pairwise-coprime theorem).

### Deliverable verification

All seven deliverables verified by inspection on commit `d2999083`:

1. **`_hsquareFree`-premise drop in `factorProduct_berlekampFactor`**:
   verified by tracing the new proof body's transitive call chain.
   `factorProduct_berlekampFactor` reduces to
   `factorProduct_berlekampFactorLoop_eq` ∘ `factorProduct_cons` ∘
   `factorProduct_nil` ∘ `DensePoly.mul_one_right_poly`; none of these
   carry a squarefree premise on the input.  The historical sibling
   `prod_berlekampFactor` already had its squarefree parameter
   underscored (`_hsquareFree`), confirming the parameter was already
   dead.  Both pre-existing call sites
   (`HexBerlekampMathlib/Basic.lean:347`,
   `HexBerlekampZassenhausMathlib/Basic.lean:4437`) continue to pass
   `hsquareFree` to `prod_berlekampFactor` unchanged.

2. **`monicModularImage` vs `modP p core` transport correctness**:
   verified.  The discharger transports `d * d ∣ monicModularImage X`
   to `d * d ∣ X` via `hmonicImage_dvd` (divisibility, not bidirectional
   unit-association — divisibility suffices because the no-squared
   invariant only needs `d * d ∣ X` to apply
   `isUnitPolynomial_of_squareFree_of_squared_dvd`).  The
   `monicModularImage ≠ 0` premise is genuinely established from
   `hzero : (modP p core).isZero = false` (from `hform`) plus
   `FpPoly.scale_size_eq_of_ne_zero`, not via `hcore_monic`.  The
   asymmetry with #4573 (which DOES take `hcore_monic`) is intentional:
   the polyProduct_congr lemma needs
   `monicModularImage (modP p core) = modP p core` so that
   `prod_berlekampFactor`'s `.product = monicImg` lands directly at
   `modP p core` on the bridge, while the coprime lemma only needs
   `monicModularImage ∣ modP p core` (which holds unconditionally
   under `modP p core ≠ 0`).

3. **Inductive helper soundness**
   (`quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared`):
   verified.  The recursion correctly:
   (a) transports `factorProduct (g :: rest) = g * factorProduct rest`
       to `rawGcd² ∣ X` for `rawGcd := gcd g (factorProduct rest)`;
   (b) bottoms out at `nil` and `[_g]` with `True.intro` (matching the
       predicate's `True` case on length ≤ 1);
   (c) computes `normalizedXGCD.gcd = scale (lc rawGcd)⁻¹ rawGcd` and
       reduces it to the literal `(1 : FpPoly p)` (= `DensePoly.C 1`)
       via `ext_coeff` after showing `rawGcd` is a nonzero constant
       (`rawGcd.size = 1`).

4. **Modular reduction bridge soundness**
   (`modP_polyProduct_liftToZ_eq_factorProduct`): verified.
   (a) The nil base case routes through `polyProduct_empty` (= `1`) and
       `modP_one` (= `1 : FpPoly p`), matching `factorProduct []` (= `1`).
   (b) The cons step uses `polyProduct_cons_toArray` (which preserves
       order, putting `liftToZ x` on the left), then `modP_lift_mul_left`
       (no coprimality precondition required — it works
       unconditionally because both sides reduce by `modP`'s
       multiplicative-homomorphism property), then `ih`, then
       `factorProduct_cons`.
   (c) Order is preserved throughout — the proof relies on syntactic
       left-multiplication, not commutativity, at each foldl step.

5. **Substrate-helper visibility**: verified.
   (a) `modP_polyProduct_liftToZ_eq_factorProduct` is shared between the
       coprime discharger and `polyProduct_map_liftToZ_congr_factorProduct`
       (hoisted by PR #4578 from the polyProduct_congr sibling).  Both
       call sites live in the same file, so `private` remains correct.
   (b) `quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared`
       has a single call site (`factorsModP_coprime_of_factorsModPBerlekampForm`),
       `private` is correct.
   (c) `berlekampFactor_factors_pairwise_coprime` is correctly placed in
       `HexBerlekamp/Factor.lean` (Mathlib-free) — its statement mentions
       only `List.Pairwise`, `Dvd.dvd`, and `DensePoly.degree?`, all
       Mathlib-free.

6. **Sibling-discharger composition**: verified.  The discharger's
   return type
   `letI := primeData.bounds; Hex.ZPoly.QuadraticMultifactorCoprimeSplits primeData.p primeData.factorsModP.toList`
   matches the `hcoprime` premise shape consumed by all three
   downstream umbrellas
   (`henselLiftData_liftedFactor_monic_of_choosePrimeData` line 3810,
   `_injective_of_choosePrimeData` line 4861, and
   `_natDegree_pos_of_choosePrimeData` line 5087) exactly.

7. **No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`**:
   verified by `git diff d2999083^..d2999083` — zero added lines match
   the forbidden-placeholder regex.  Local `lake build` shows the same
   sorry inventory as before this PR: 3 in
   `HexBerlekampZassenhausMathlib/Basic.lean` (lines 237, 8784, 8877),
   10 in `HexBerlekampMathlib/Basic.lean`, 1 in
   `HexBerlekampZassenhaus/Basic.lean`, 1 in `HexMatrix/Bareiss.lean` —
   all pre-existing.

### Refactor landed

One concrete simplification, paralleling PR #4578's refactor on the
sibling Mathlib-bridge helper `polyProduct_map_liftToZ_congr_factorProduct`:

`factorProduct_berlekampFactor` is hoisted from its post-`Nodup`-cluster
position (next to `berlekampFactor_factors_pairwise_coprime` at line
1170) up to immediately after `factorProduct_berlekampFactorLoop_eq`
(line 462).  The historical SPEC surface `prod_berlekampFactor` now
derives from it directly:

```lean
theorem prod_berlekampFactor ... :
    (berlekampFactor f hmonic).product = f := by
  simp only [Factorization.product_def]
  exact factorProduct_berlekampFactor f hmonic
```

This removes the duplicated proof body added by #4574 (both walked
through the same `berlekampFactorLoop` / `factorProduct_cons` /
`mul_one_right_poly` chain, differing only in the
`Factorization.product_def` unfold of the `.product` view).

No signature change.  `prod_berlekampFactor` keeps its `_hsquareFree`
parameter for caller compatibility; the two pre-existing call sites
(`HexBerlekampMathlib/Basic.lean:347`,
`HexBerlekampZassenhausMathlib/Basic.lean:4437`) compile unchanged.
The downstream `berlekampFactor_factors_pairwise_coprime` consumer
still applies `factorProduct_berlekampFactor` at the hoisted location.

### Build state

- `lake build HexBerlekamp.Factor` ✓ (clean)
- `lake build HexBerlekampZassenhausMathlib.Basic` ✓ (same 3 sorries)
- `lake build HexBerlekampMathlib.Basic` ✓ (same 10 sorries)
- `python3 scripts/check_dag.py` ✓
- `git diff --check` ✓
- Sorry inventory delta: 0

## Current frontier

PR #4574 review closed.  The HO-1 substrate's four
`QuadraticMultifactorLiftInvariant` boundary dischargers from #4563 now
sit at three landed (#4570, #4573, #4574) plus one in flight
(`factorsModP_monic`, #4567).  The slow-path exhaustive-arm umbrella
#4561 can compose them once #4567 lands.

## Next step

Two natural follow-ups, both out of scope for #4581:

1. **Caller cleanup for `factorProduct_berlekampFactor`**: two call
   sites still use the older
   `prod_berlekampFactor + Factorization.product_def` rewrite pattern
   that could now contract to a direct `factorProduct_berlekampFactor`
   call.  `HexBerlekampZassenhausMathlib/Basic.lean:4437-4441` is
   inside the `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`
   discharger and was declared out of scope by #4581.
   `HexBerlekampMathlib/Basic.lean:347` is inside a `sorry`-bearing
   proof and could be tidied as a separate small simplification PR.

2. **Whether `prod_berlekampFactor` still needs its `_hsquareFree`
   parameter**: the underscore already signals that the premise is
   dead.  Dropping it would touch both call sites and may unblock
   minor downstream simplification.  Not urgent.

## Blockers

None.
