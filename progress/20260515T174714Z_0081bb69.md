# Session 0081bb69 — feature, issue #4278

## Accomplished

- Claimed #4278.
- Added public `HexBerlekampZassenhaus.Basic` wrappers:
  - `factorWithBound_entry_multiplicity_pos`
  - `factorWithBound_pairwise_first`
  - `factor_entry_multiplicity_pos`
  - `factor_pairwise_first`
- Reused the existing `factorizationOfFactors` multiplicity and pairwise-key
  proofs; no executable factorization definitions changed.
- Verified:
  - `lake build HexBerlekampZassenhaus.Basic`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - no new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` in the
    `HexBerlekampZassenhaus/Basic.lean` diff.

## Current frontier

- Public bounded/default factorization callers can now use direct
  multiplicity-positive and no-duplicate-polynomial-key facts without
  unfolding the fast/slow implementation shape.

## Next step

- Downstream capstone issues can consume these wrappers for public
  `Factorization` shape obligations instead of duplicating collector-level
  entry proofs.

## Blockers

None.
