# Session 49b81d05 — feature #7505

Session type: feature (BHKS §5 auxiliary-domination decomposition, sub-piece 1).

## Accomplished

Closed sub-piece (1) of #7497. Added two theorems to
`HexBerlekampZassenhausMathlib/BadVectorAuxiliary.lean` (inside
`namespace BHKS`):

- `l2norm_sq_toPolynomial_auxiliaryPolynomialWithCorrections_le`: from a
  `ScaledShortnessCertificate input liftData vec corrections`, the genuine
  squared `l2norm` of the transported corrected auxiliary polynomial is
  bounded by `cldColumnNormBound input liftData.p`.
- `l2norm_sq_toPolynomial_auxiliaryPolynomial_le`: the zero-correction
  (`corrections = #[]`) specialisation consumed by sub-piece (4),
  discharged by `unfold auxiliaryPolynomial` then the corrected form.

Proof identifies `l2norm² = ∑_{i ∈ support} coeff i ²` (via `Real.sq_sqrt`),
restricts the support to `Finset.range n` (`n = input.degree?.getD 0`) using
`coeff_auxiliaryPolynomialWithCorrections` (coeffs at `i ≥ n` vanish),
bridges `toPolynomial`'s coeff to the ZPoly coeff via `coeff_toPolynomial`,
then applies the certificate's `coeffSqSum_le`.

## Current frontier

`lake build HexBerlekampZassenhausMathlib` passes (3788 jobs). No new
`sorry`/`axiom`/`native_decide`/`TODO` on added lines. Only pre-existing
deprecation warnings in `Resultant.lean`.

## Next step

#7506 (sub-pieces 3+4, joint auxiliary domination) unblocks once #7505
merges; it consumes the zero-correction form provided here.

## Blockers

None.
