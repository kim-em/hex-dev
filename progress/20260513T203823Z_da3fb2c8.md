# Session da3fb2c8 — BHKS recovery product wrapper (#3534)

## Accomplished

- Claimed #3534 after the human-oversight directives shown by
  `coordination orient` were blocked or not claimable.
- Added `ForwardRecoveryInputs.ExpectedTrueFactors` in
  `HexBerlekampZassenhausMathlib/Recovery.lean` to package the expected
  true-factor array, per-factor facts used by A2 reconstruction, and the
  final `Array.polyProduct expectedFactors = f` guard.
- Added `ForwardRecoveryInputs.productOfExpectedFactors`, extracting the
  final executable BHKS recovery product check from that package.
- Added `ForwardRecoveryInputs.ofMignottePrecisionExpectedFactors`, which
  builds the forward-recovery bundle from the expected true-factor package
  plus the existing per-indicator Mignotte candidate reconstruction inputs.
- Verified:
  `lake build HexBerlekampZassenhausMathlib.Recovery`,
  `lake build HexBerlekampZassenhausMathlib.Basic`,
  `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, and `git diff --check`.
- Checked
  `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/Recovery.lean HexBerlekampZassenhausMathlib/Basic.lean`;
  it reports only pre-existing `sorry`s in
  `HexBerlekampZassenhausMathlib/Basic.lean`.

## Current frontier

The forward-recovery layer no longer needs downstream callers to pass the
final BHKS product guard as an unstructured standalone equality when they
already have the expected true-factor package.

## Next step

Downstream BHKS Group D work can construct `ExpectedTrueFactors` from the
true integer-factor enumeration theorem, then use
`ofMignottePrecisionExpectedFactors` to supply both A2 candidate equality and
the final product check to `ForwardRecoveryInputs`.

## Blockers

None for this slice. The worktree had a pre-existing `.claude/CLAUDE.md`
modification that was left untouched and should not be included in this PR.
