# Session d2bc6c83 — work #4039

## Accomplished

- Claimed feature issue #4039.
- Added fold/readback helpers in `HexGramSchmidt/Int.lean` for column-targeted
  `setArrayEntry` folds.
- Added `getArrayEntry_writeScaledColumn_below`, proving that
  `writeScaledColumn` records below-diagonal entries from the pre-elimination
  matrix in the current column.
- Added `getArrayEntry_scaledCoeffArrayLoop_current_col_written`, proving the
  one-step current-column capture property for `scaledCoeffArrayLoop`, including
  the regular recursive branch via the existing processed-column preservation
  helper.
- Verified the partial milestone with `lake build HexGramSchmidt.Int`,
  `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

## Current frontier

The core current-column capture lemma needed by #4039 is available and
compiles. The full lower-triangle/off-diagonal sync theorem is still not
assembled, and the existing downstream stubs in `HexGramSchmidt/Int.lean`
remain unchanged.

## Next step

Use `getArrayEntry_scaledCoeffArrayLoop_current_col_written` as the write-side
base case, combine it with `getArrayEntry_scaledCoeffArrayLoop_preserve_col_before_step`,
and bridge the captured pre-elimination matrix entry to the corresponding
`Matrix.noPivotLoop` state entry.

## Blockers

No technical blocker for the helper layer. The full #4039 invariant is larger
than this session after orientation and helper extraction, so this work is being
published as a partial PR.
