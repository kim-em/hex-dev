# 2026-05-17T19:37 — splitter distinct-roots invariant for #4792

## Accomplished

* Triaged parent issue #4785 (pairwise non-association of
  `quadraticIntegerRootFactors?` output): the proof needs two
  substantial pieces of Mathlib-free substrate and is better split
  than rushed.
* Decomposed #4785 into sub-issues #4791 (SquareFreeRat ⇒ no linear
  factor squared divides core) and #4792 (this issue: splitter
  distinct-roots invariant). Left a `Decomposed into #4791, #4792`
  breadcrumb on #4785 and released the claim for replan.
* Claimed and executed #4792. Added in
  `HexBerlekampZassenhaus/Basic.lean`, in the
  `splitIntegerRootFactorsAux_*` helpers block:
  - `splitIntegerRootFactorsAux_factors_distinct_roots`
    (private, ~line 8204): every emitted factor list decomposes as
    `rs.map linearFactorForRoot` for some `rs : List Int` that is a
    `List.Sublist` of the input `roots`. Proof by `fuel`-induction
    then case-split on `roots`, threading through both the `none` and
    `some quotient` branches via the recursive call.
  - `splitIntegerRootFactorsAux_factors_form` (public wrapper):
    pulls the existence out of the universally-quantified form so the
    eventual #4785 assembly call site can pattern-match `rs` directly.
  - `positiveDivisors_nodup` (private): trivial — `List.range`'s
    `Nodup` is preserved by `List.filter`.
  - `nodup_flatMap_pos_divisor_pairs` (private helper): the per-divisor
    pair-list `flatMap` of `(d => [Int.ofNat d, -Int.ofNat d])` is
    `Nodup` over a `Nodup` list of *positive* `d`s. Positivity rules
    out `d = -d` and makes `[d, -d]` ∩ `[d', -d']` empty for `d ≠ d'`.
  - `integerRootCandidates_nodup` (private): combines the two above
    via the fact that `positiveDivisors` filters out `d = 0`.
* `lake build HexBerlekampZassenhaus.Basic` succeeds, no new
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`. `scripts/check_dag.py`
  passes.

## Current frontier

* PR for #4792 ready to push.
* #4791 (SquareFreeRat ⇒ no linear-squared) still unclaimed.
* #4785 (assembly) blocked on both #4791 and #4792 (planner will
  add the deps once this lands; the issue body currently doesn't
  list `depends-on` lines).

## Next step

* Push branch, open PR, exit. Next worker can claim #4791.

## Blockers

None for this issue.
