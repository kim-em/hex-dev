# 2026-05-07 21:27 UTC — BHKS indicator reconstruction (731e9f3b)

## Accomplished

Claimed issue #2612 and added the executable BHKS indicator reconstruction
surface in `HexBerlekampZassenhaus/Basic.lean`:

- `bhksIndicatorCandidate?` consumes a supplied `0/1` indicator row over
  `LiftData.liftedFactors`.
- It rejects row-size mismatches, non-binary rows, and all-zero rows before
  reconstruction.
- It reconstructs `lc(f) * product selected g_i` modulo `p^k`, centre-lifts
  the coefficients, removes content/sign, and accepts only after
  `exactQuotient?` verifies divisibility.
- Added guards for selected quadratic reconstruction, all-zero rejection,
  non-binary rejection, and successful quotient recovery.

Verification passed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The BHKS path now has an executable helper that later RREF/equivalence-class
recovery can call to turn candidate class indicators into exact integer
factor candidates.

## Next step

Implement the RREF equivalence-class extraction that supplies candidate
indicator rows to `bhksIndicatorCandidate?`, then wire the verified candidates
into the fast-path success criterion.

## Blockers

No blockers for this slice. The pre-existing pod-managed `.claude/CLAUDE.md`
worktree modification remains untouched.
