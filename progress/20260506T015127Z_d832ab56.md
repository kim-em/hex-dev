# 2026-05-06 01:51 UTC — work session (d832ab56)

## Accomplished

Processed the unclaimed queue according to the worker flow.

Claimed #2244 and found it stale: `HexConway/Basic.lean` already has theorem
implementations for the named Conway declarations and no `axiom` declarations
matching the issue. Released it with a stale/replan comment.

Claimed #2245 and found it stale: `HexGfqField/EmitFixtures.lean`,
`HexGfqField/Conformance.lean`, and `HexGfqField/Bench.lean` already use
certificate-backed irreducibility theorems and no longer contain the named
axioms. Released it with a stale/replan comment.

Claimed #2433 and found its premise stale: it assumes #2411 has landed, but
#2411 is still open and claimed. Added `depends-on: #2411`, left an explanatory
comment, and released #2433 as blocked.

Claimed #2315 and added the public Mathlib-facing bridge theorem
`HexMatrixMathlib.bareissDet_eq_det` in `HexMatrixMathlib/Determinant.lean`.
The theorem exposes row-pivoted Bareiss against `Matrix.det (matrixEquiv M)` by
composing the existing Hex-level `Hex.Matrix.bareiss_eq_det` theorem with
`det_eq`.

Verified:

- `lake build HexMatrixMathlib.Determinant`
- `lake build HexMatrixMathlib`
- `lake build HexGramSchmidt`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

#2315 is only partially advanced. The requested public Mathlib API now exists,
but the underlying Hex-level row-pivot theorem
`Hex.Matrix.bareiss_eq_det` in `HexMatrix/Bareiss.lean` is still an existing
`sorry`, so the full row-pivoting proof remains open.

## Next step

Continue #2315 by replacing `Hex.Matrix.bareiss_eq_det` with a real proof over
the row-pivoting executable path, or decompose that proof into smaller issues
for array/state correspondence, row-swap determinant tracking, and singular
pivot failure.

## Blockers

The row-pivoted executable currently uses the private array implementation
`bareissArrayState` / `bareissArrayDet`, while the existing proved invariant is
for the public `BareissState` no-pivot loop. Bridging those two execution paths
is the main remaining obstacle for #2315.
