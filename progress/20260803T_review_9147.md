# PR #9147 review — Kronecker multiplication

## Accomplished

- Audited the Kronecker slot-width proof, signed biasing, zero/constant cases,
  generated C for the `@[csimp]` call sites, extern-backed shifts/logarithm,
  crossover harness, and checked-in benchmark artifact.
- Confirmed generated code routes the full bignum quadratic Hensel step through
  one `factorErrorImpl` and eight `mulModSquareImpl` calls, while the monomial
  division path still calls its shift-and-scale implementation.
- Built `HexPolyZ.Kronecker`, `HexHensel.Quadratic`, and `HexConformance`.
- Re-ran the current crossover sweep and spot-checked unbalanced products from
  24×24 through 24×2048; no production-cutoff regression was observed.

## Current frontier

- No correctness hole was found in the arithmetic or generated production call
  graph.
- Review findings: the checked-in crossover JSON predates the final 20-bit
  cutoff and finer 14–19-bit grid; the benchmark's checksum branch is
  semantically constant; separate operand widths would tighten packing; and
  recursive array concatenation remains avoidable unpacking overhead.

## Next step

- Re-record the crossover JSON with the shipping executable and make the
  benchmark dependency genuinely value-dependent before relying on it as a
  durable cutoff artifact.
- Consider deriving the slot width from `maxAbs p` and `maxAbs q` separately and
  filling a preallocated output array during balanced unpacking.

## Blockers

None.
