# Progress — 2026-06-15 (session c1508fac, work/once #7455)

## Accomplished

Issue #7455: assembled the existential `toMonicLiftData` lift transport
producer in `HexBerlekampZassenhausMathlib/Basic.lean`, after deps #7452
and #7453 landed.

Added two declarations near `representsModP_correspondent`:

- `irreducible_toPolynomial_monicCorrespondent`: the monic correspondent
  `g` of an irreducible primitive integer factor is itself irreducible.
  Proof moves to `ℚ[X]` via Gauss
  (`IsPrimitive.Int.irreducible_iff_irreducible_map_cast`), uses the
  algebra automorphism `algEquivCMulXAddC lc 0` (composition with
  `C lc * X`) preserving irreducibility, and discharges the residual
  content scalar as a unit. This fills step 2 of the directive's proof
  outline (the "focused auxiliary lemma" it anticipated).
- `toMonicLiftData_represents_lifted_of_modP`: the final producer.
  Chains `exists_monicCorrespondent_of_dvd` →
  `irreducible_toPolynomial_monicCorrespondent` →
  `representsModP_correspondent` →
  `toMonicLiftData_represents_lifted_monicCorrespondent` (#7453) →
  `representsIntegerFactorAtLift_of_monicCorrespondent` (#7452).

## Verification

- `lake build HexBerlekampZassenhausMathlib`: green (3788 jobs).
- `lake build HexBerlekampZassenhaus`: green (497 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
  (Pre-existing `sorry` in `FactorSoundness.lean:18` is untouched.)

## Current frontier

The non-scope items remain open per the issue:
`HenselSubsetLiftHypotheses.represents_lifted_of_modP` is still
unpopulated and the slow-path consumers are unchanged — tracked outside
#7455.

## Next step

None for this issue.

## Blockers

None.
