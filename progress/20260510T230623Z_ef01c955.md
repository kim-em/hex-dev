# 2026-05-10 23:06 UTC — work session (ef01c955)

## Accomplished

- Claimed #3157.
- Added a proof-only no-pivot zero-column invariant for `rrefLoop` to
  `HexMatrix/RREF.lean`:
  - Helpers: `rowSwap_zero_column_preserve`,
    `rowScale_zero_column_preserve`, `eliminateColumn_foldl_other_column`,
    `eliminateColumn_other_column`.
  - Invariant: `RrefNoPivotZero`, plus `rrefNoPivotZero_initial`,
    `RrefNoPivotZero.widen_col_at_m`, `RrefNoPivotZero.widen_col_at_n`.
  - Preservation: `rrefLoop_no_pivot_zero` and the final-state lemma
    `rref_final_no_pivot_zero`.
  - Public corollary: `rref_zero_non_pivot` — for the `rref` output, every
    row at or beyond the discovered rank is zero in every non-pivot column.
- Verified with `lake build HexMatrix.RREF`, `lake build HexMatrix`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a banned-marker
  scan over `HexMatrix/RREF.lean` (only the pre-existing
  `rref_isRREF`/`all_goals sorry` remains).

## Current frontier

- The non-pivot-column slice of the `IsEchelonForm.zero_row` obligation is
  now provable directly from `rref_zero_non_pivot`. The pivot-column slice
  and the `pivot_one` / `above_pivot_zero` / `below_pivot_zero` obligations
  for `rref_isRREF` are still open as documented in #3111.

## Next step

- Plug `rref_zero_non_pivot` into a future PR that combines it with a
  pivot-column zero fact to discharge `zero_row` in the `rref_isRREF`
  packaging.

## Blockers

- None for this slice.
