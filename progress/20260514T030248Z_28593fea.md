# Session 28593fea — slow-path quadratic dispatch (#3913)

## Accomplished

- Claimed feature issue #3913.
- Added splitter-output invariants in `HexBerlekampZassenhaus/Basic.lean`:
  - factors emitted by `splitIntegerRootFactorsAux` are sign-normalized;
  - those factors are recordable polynomial factors;
  - their raw product has positive leading coefficient.
- Added quadratic-root branch invariants for `quadraticIntegerRootFactors?`,
  including the pushed residual factor case.
- Proved `factorSlowWithBound_product_of_quadratic_branch` using the existing
  all-recorded-normalized product wrapper.
- Confirmed the constant-branch lemma from this issue was already present in
  the current tree.
- Verified:
  - `lake build HexBerlekampZassenhaus.Basic`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|\bsorry\b' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The slow-path constant and quadratic branch dispatch lemmas are now available.
The remaining slow-path product assembly still depends on the exhaustive
branch work tracked by #3897.

## Next step

Use these dispatch lemmas from the exhaustive-branch assembly issue once its
sign-cancellation/product-preservation layer is proved.

## Blockers

None for this issue. The marker check still reports only the two pre-existing
BZ-core placeholders: `factor_product_of_bound` and `isIrreducible_iff`.
