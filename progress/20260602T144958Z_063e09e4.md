# 20260602T144958Z 063e09e4 — Issue #6269

## Accomplished

- Claimed feature issue #6269.
- Added `noPivotLoop_initial_gram_exists_rowVec` in
  `HexGramSchmidt/Int.lean`, a narrow row-vector consumer for the initial
  no-pivot Gram trajectory.
- Refactored
  `leadingPrefix_gram_zero_pivot_column_zero_of_singular_step` to consume the
  row-vector package instead of opening `BareissGramRowInvariant` directly.

## Current frontier

The singular leading-prefix helper now uses a clean local row-vector package:
one supported integer row combination represents an active no-pivot Gram row
against every original input row. This keeps the invariant internals out of
the downstream singular-column proof.

## Next step

Issue #5655 can propagate the quotient provenance into
`gramDetVecEntry_eq_leadingPrefix_bareiss` and its public determinant-vector
consumers.

## Blockers

- None for this API layer.

## Verification

- `lake build HexGramSchmidt.Int` — passed.
- `lake build HexGramSchmidt` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `HexGramSchmidt/Int.lean` — no matches.
- Existing warnings remain: `HexGramSchmidt/Basic.lean:1606` unnecessary
  `simpa`, `HexGramSchmidt/Int.lean` unused theorem binder, and the pre-existing
  `sorry` in `gramDetVecEntry_eq_leadingPrefix_bareiss`.
