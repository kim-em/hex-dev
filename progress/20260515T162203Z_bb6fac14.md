## Accomplished

Landed the induced subset-correspondence predicate (deliverable 1 of #4236) in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `HenselSubsetCorrespondenceRest core d J target`: a Prop packaging existence
  and uniqueness of representing lifted-factor subsets, constrained to
  `S ⊆ J`. Models the recursive state of `recombinationSearchModAux` after
  some prefix of subsets has been consumed.
- `henselSubsetCorrespondenceRest_initial`: initial-state bridge from
  `HenselSubsetCorrespondenceHypotheses` at `J = Finset.univ`,
  `target = core`.
- `existsUnique_liftedFactorSubset_of_henselSubsetCorrespondenceRest`:
  consumer-facing wrapper exposing `∃!` form.

## Current frontier

Two deliverables remain from #4236:

- Emission-step transport: from `HenselSubsetCorrespondenceRest core d J target`
  plus an emission of `factor` with subset `S ⊆ J`, derive
  `HenselSubsetCorrespondenceRest core d (J \ S) (target / factor)`. This
  requires a disjointness fact for representing subsets of distinct
  irreducible divisors of `target` — derivable from square-free reduction
  plus unique factorization in `F_p[x]`, but not currently captured by
  `HenselSubsetCorrespondenceHypotheses.unique_subset` alone.
- Recursive coverage lemma + assembly into the public
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  theorem. Induction on `J.card` (or fuel) using the emission-step
  transport.

## Next step

A follow-up issue should cover (a) the emission-step transport theorem, then
(b) the recursive coverage by induction, and (c) the public coverage theorem
assembly. The transport theorem is the technical bottleneck — it likely needs
an additional algebraic fact about the lifted-factor product semantics
(coprime products mod `p^k`).

## Blockers

None for this session's deliverable. Open question for the successor: which
additional hypothesis is the cleanest carrier for the cross-factor
disjointness (square-free reduction, modPSubsetPartition, or a fresh
algebraic helper).
