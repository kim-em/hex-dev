# Dynamic-scope review follow-ups

## Accomplished

- Made the SPEC's theorem-instantiation generation recurrence explicit: it
  includes the emitting application's creation generation, the action
  substitution, and every explicit existing node in drafts, equalities, and
  scopes.
- Documented and retained generation stamping for node-free equality and
  scoped-application admissions.
- Removed redundant draft, equality, and scope resolution from the policy
  generation lookup; retained proposals were already structurally validated,
  while program-watching freshness pins that validation snapshot.
- Updated the policy-frontier SPEC checksums to the merge-gated conformance
  values.
- Rebuilt the complete 22-job dynamic-scope target set successfully.

## Current frontier

PR #9082 now incorporates all nonblocking findings from its fresh independent
review. Its generation lookup has one authoritative recurrence shared with
final admission instead of a second partial structural-validation path.

## Next step

Push this review commit, inspect its exact CI run, and keep the PR stacked
until its arbitrary-scope prerequisite lands.

## Blockers

None.
