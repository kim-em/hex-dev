# 20260602T231133Z work #2567

## Accomplished

- Claimed directive #2567 and landed the corrected BHKS auxiliary-polynomial
  squared-l2-norm lift in
  `HexBerlekampZassenhausMathlib/BadVector.lean`.
- Added
  `BHKS.auxiliaryPolynomialWithCorrections_l2norm_sq_le`, summing the existing
  corrected per-coefficient bound over the polynomial support and exposing the
  diagonal-row correction contribution as an explicit finite sum over
  `j < input.degree?.getD 0`.

## Current frontier

The corrected auxiliary polynomial now has both per-coefficient and l2-norm
upper bounds.  The next BHKS D1 step can use this theorem to feed the
corrected-auxiliary side of the executable bad-vector contradiction and then
combine it with the actual-cap correction/cut bounds in `Recovery.lean`.

## Next step

Instantiate the corrected l2-norm theorem for the cap-lift witness in the
`ExecutableBadVectorWitness` / `BadVectorBridgeData` path, replacing any
remaining reliance on the zero-correction `auxiliaryPolynomial_l2norm_sq_le`
where the full lattice-row correction data is available.

## Blockers

No technical blocker for this slice.  Full `factorFast_terminates` still needs
the corrected auxiliary l2-norm bound to be composed with the diagonal
correction bound, the actual-cap separation hypotheses, and the final recovery
success assembly.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVector` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Lean file found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
