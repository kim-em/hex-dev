# Work session: HexPolyFp DivModLaws monic and mod-mod fields

## Accomplished

- Claimed #2235 and discharged the two targeted `sorry`s in
  `instDivModLawsZMod64Fp` (`HexPolyFp/Basic.lean`):
  - `divModMonic_eq_divMod_of_monic`
  - `mod_mod_of_not_pos_degree`
- Added three private helpers near the existing `zmod_div_mul_cancel_of_ne`
  helper:
  - `zmod_one_ne_zero` — `(1 : ZMod64 p) ≠ 0` from `PrimeModulus.prime`
    via `2 ≤ p` and `1 % p = 1`.
  - `zmod_div_one` — `a / (1 : ZMod64 p) = a`, used by the monic case
    where `Monic g` gives `g.leadingCoeff = 1`.
  - `cancel_lead_at_pos_size_core` — packages the existing leading-coeff
    nonzero argument plus `zmod_div_mul_cancel_of_ne` into the
    `hcancel` witness shape required by
    `DensePoly.divMod_remainder_eq_zero_of_degree_zero_core`.
- The monic field splits on the early degree shortcut: when
  `f.degree < g.degree`, both sides reduce to `(0, f)` via
  `DensePoly.divModArray_eq_zero_self_of_degree_lt`; otherwise we invoke
  `divModMonic_eq_divMod_of_monic_core` with `a / 1 = a`.
- The `mod_mod_of_not_pos_degree` field splits on `g.size`:
  size 0 → `divMod_remainder_eq_self_of_size_zero_core` directly;
  size 1 (forced by normalisation since `¬ 0 < g.degree?.getD 0` rules
  out `g.size > 1` on a normalised dense poly) →
  `divMod_remainder_eq_zero_of_degree_zero_core` reduces both
  remainders to `0`.
- Verified:
  - `lake build HexPolyFp.Basic`
  - `lake build HexPolyFp`
  - `python3 scripts/status.py HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`. The two
    targeted `sorry`s are gone; the remaining
    `mod_self_eq_zero`/`mod_eq_zero_of_dvd` and `GcdLaws` `sorry`s in
    this instance are out of scope per the issue.

## Current frontier

`instDivModLawsZMod64Fp` still has two `sorry`s:
- `mod_self_eq_zero`
- `mod_eq_zero_of_dvd`

Both need additional helper infrastructure (a `divMod` uniqueness or
`p % p = 0` argument) that the issue body explicitly carved out for
separate planner work.

## Next step

The natural follow-up is to plan the `mod_self_eq_zero` /
`mod_eq_zero_of_dvd` cluster, which needs a divisibility-aware uniqueness
lemma in `HexPoly.Euclid` to lift through to `ZMod64`-coefficient
remainders. The `cancel_lead_at_pos_size_core` helper added here may be
reusable if any of those proofs need a leading-coefficient hcancel
witness.

## Blockers

None for #2235.
