# Session 28b9d6c9 — feature, issues #4257 → #4258

## Accomplished

- Claimed #4257, found it still combined three separable proof layers:
  executable column entries, matrix action, and the fixed-space polynomial iff.
- Decomposed #4257 into:
  - #4258: executable Berlekamp column-entry bridge.
  - #4260: coefficient-vector matrix action identity, depending on #4258.
  - #4261: fixed-space polynomial kernel iff, depending on #4260.
- Released #4257 with the required `Decomposed into #4258, #4260, #4261`
  breadcrumb.
- Claimed #4258 and stacked on open PR #4255 locally.
- Added entry-level Berlekamp column lemmas in `HexBerlekamp/Basic.lean`:
  - `berlekampMatrix_entry_eq_columnPolys_coeff`
  - `berlekampColumn_entry_eq_powModMonic_coeff`
  - `berlekampColumn_powModMonic_mod_eq_linearPow_X`
- Verified:
  - `lake build HexBerlekamp.Basic`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` in the
    `HexBerlekamp/Basic.lean` diff.

## Current frontier

- #4258 now exposes the matrix-entry array coefficient and the public
  `powModMonic` column coefficient/residue statements.
- The stronger proof equating the optimized `berlekampColumnPolys` array
  column to the public `berlekampColumn` representative remains the natural
  next column-layer step before #4260.

## Next step

- Extend #4258 or a successor issue with an induction over
  `berlekampColumnPolys` showing its `j`-th pushed representative matches the
  public `powModMonic` representative modulo `f` (and, once reduced uniqueness
  is available, coefficient-wise).
- Then use #4260 to assemble the matrix-action identity.

## Blockers

- #4255 is still open, so this branch is stacked on `agent/642b1001` for the
  helper lemmas from that PR.
