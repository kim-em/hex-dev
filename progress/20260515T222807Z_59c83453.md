# 2026-05-15T22:28 — review: HexBerlekamp matrix kernel iff stack (#4328)

## Accomplished

- Reviewed the three-PR matrix→algebraic kernel bridge stack in
  `HexBerlekamp/Basic.lean` at `f8ee4e8`:
  - `ded3daa` (#4320 for #4313): default `Matrix.mulVec` refactor and
    canonical `Lean.Grind.Field (ZMod64 p)` instance.
  - `705562b` (#4321 for #4307): `basisSize` / `degree?` plumbing dedupe
    via two private helpers.
  - `f8ee4e8` (#4326 for #4314):
    `isFixedSpaceKernelPolynomial_iff_dvd_linearPow_sub_self` plus five
    private helpers.
- Verified the iff statement, helper generality, and edge-case handling
  against #4227's downstream consumer pattern. All names #4227 plans to
  invoke (`isFixedSpaceKernelPolynomial_iff_dvd_linearPow_sub_self`,
  `berlekampMatrix_mulVec_coeffVector_eq`,
  `fixedSpaceKernelPolynomial_coeffVector_complete`,
  `dvd_primeFieldProduct_witness_of_dvd_linearPow_sub_self`,
  `exists_kernelWitnessSplit?_some_of_witnessProduct_dvd_of_pos_degree`,
  `kernelWitnessSplit?_none_of_berlekampFactor_factors_length_le_one`)
  are present with compatible signatures.
- Confirmed the new public `zmod64FieldOfPrime` instance does not
  conflict with the private duplicate in `HexBerlekampZassenhaus/Basic.lean`
  (private + positional `Nat.Prime` argument, hence not a candidate for
  instance search).
- Built `HexBerlekamp.Basic`, `HexBerlekamp`,
  `HexBerlekamp.RabinSoundness`, `HexBerlekamp.Factor`,
  `HexBerlekamp.Irreducibility`, `HexBerlekamp.DistinctDegree`, and
  `HexBerlekampZassenhaus` (145/145 pass; pre-existing sorries
  unchanged). `python3 scripts/check_dag.py` and `git diff --check`
  both pass.
- Posted the review report as a comment on #4328.
- Opened follow-up issues:
  - #4329: relocate `zmod64FieldOfPrime` and 5 supporting helpers out
    of `HexBerlekamp/Basic.lean` (also dedupes the
    `HexBerlekampZassenhaus/Basic.lean` private copy).
  - #4330: dedupe `linearPow_zero_of_pos` between `HexPolyFp.Compose`
    (existing `private` copy) and `HexBerlekamp/Basic.lean` (new
    `_aux` copy).

## Current frontier

- #4227 is cleared to start: the iff, the matrix-action identity, the
  spanning lemma, and all downstream witness-product / split-surface
  lemmas line up with the issue body's proof sketch.
- The two cleanup follow-ups (#4329, #4330) are non-blocking; they can
  be picked up by any worker session.

## Next step

- A worker claims #4227 and composes
  `irreducible_of_no_kernelWitnessSplit_squareFree` plus
  `berlekampFactor_singleton_irreducible`.

## Blockers

- None.
