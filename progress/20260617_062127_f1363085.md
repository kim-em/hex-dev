# Progress - 2026-06-17 (session f1363085, review #7681)

## Accomplished

Claimed and executed #7681 (review: audit tight-CLD-norm cut-projection
consumption surface). **Verdict: PASS** — posted a per-deliverable verdict
comment on the issue. No code change (no docstring fix warranted as blocking;
one optional clarification noted).

### What was audited

The consumption surface in `HexBerlekampZassenhausMathlib/Lattice.lean` that
#7651 (produce `TightColumnBound`) and #7675 (compose through
`cutProjectionHypotheses_of_trueFactors`) build on:

- `TightColumnBound` (`Lattice.lean:803`) / `TrueFactorCLDTightNormBound`
  (`:698`) / `trueFactorCLDTightNormBound_of_cldTail_four_mul_sq_sum_le`
  (`:766`) / `tightNormBound_of_lift` (`:837`).
- `cutProjectionHypotheses_of_trueFactors` (`:1532`) and its survivor-span
  core `mem_prefixSubmodule_of_normSq_le` (`:1198`).
- Downstream consumption at
  `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_trueFactors`
  (`PartitionRefinement.lean:553`) → `…_of_cut` (`:574`).

### Key finding

The surface is sound and does **not** repeat the `CutRetention`/#7575
defect class. The disqualifying property of the refuted routes was
quantifying over an *arbitrary* `v : Fin r → ℤ` with only a norm bound (no
lattice membership). This surface instead:

1. constrains only the *concrete* `trueFactorCLDVector L S`
   (`rowCombination L.basis (trueFactorRowCoeffs L S)`, determined by `L,S`);
2. carries the structural lattice-membership witness
   (`trueFactorCLDVector_memLattice`) that `mem_prefixSubmodule_of_normSq_le`
   **requires** (`hv : memLattice reduced v`, `Lattice.lean:1204`);
3. demands the genuine tight per-column analytic bound from the caller
   without fabricating it.

Producer return type matches the downstream consumer exactly
(`CutProjectionHypotheses (bhksProjectedRows L hrows) trueSupports`,
`trueSupports` over `Fin (bhksProjectedRows L hrows).factorCount` on both
sides) — no type-level gap forcing a later `sorry`.

### Verification

- `lake build HexBerlekampZassenhausMathlib.Lattice` — clean (only
  pre-existing deprecation/unused-variable warnings).
- Surface and consumption chain confirmed sorry-free (Lattice.lean
  600-1710, PartitionRefinement.lean 430-580).

### One optional follow-up (non-blocking)

`TightColumnBound`'s `_D : TrueFactorLift L S` parameter does not appear in
the `bound` field — sound (it forces lift data to exist and
`tightNormBound_of_lift` consumes it via `D.blockForm`), but a docstring
note would prevent a reader misreading it as "the bound depends on `D`".

## Current frontier

Cut-projection consumption surface cleared for #7651/#7675 to build on.

## Next step

Not mine — #7651 produces the `TightColumnBound`/tight norm certificates
(BHKS Lemma 5.7 high-bit estimate); #7675 composes through the producer.

## Blockers

None. Review-only; issue closed via this progress-file PR.
