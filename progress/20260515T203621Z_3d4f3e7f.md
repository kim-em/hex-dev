# HexBZ factorWithBound wrapper consolidation

## Accomplished

- Claimed issue #4297 after the higher-priority directive #3899 could not be
  claimed because coordination reported it needs replan.
- Added `factorWithBound_eq_factorizationOfFactors` in
  `HexBerlekampZassenhaus/Basic.lean` to capture the shared fast-vs-slow raw
  factor case split.
- Rewrote the five bounded factorization wrapper theorems to consume the new
  shared lemma instead of duplicating the split:
  `factorWithBound_entry_mem_raw_source`,
  `factorWithBound_entry_multiplicity_pos`,
  `factorWithBound_entry_normalizeFactorSign_id`,
  `factorWithBound_entry_leadingCoeff_pos`, and
  `factorWithBound_pairwise_first`.
- Verified:
  - `lake build HexBerlekampZassenhaus.Basic`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`

## Current frontier

The requested wrapper consolidation is complete and the affected public theorem
statements were left unchanged. The only remaining `sorry` warning in
`HexBerlekampZassenhaus/Basic.lean` is pre-existing and outside this issue.

## Next step

Create the PR for issue #4297 and let CI validate the same build surface.

## Blockers

None for #4297. Directive #3899 still needs planner attention before it can be
claimed.
