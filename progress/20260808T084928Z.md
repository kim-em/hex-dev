# Arbitrary-function chronological replay restack

## Accomplished

- Reconciled chronological replay with current `main`, the repaired
  policy-session foundation, and the package-owned semantic replay layer.
- Preserved the newer repository conformance targets alongside semantic and
  chronological interval replay.
- Rebuilt chronological replay, semantic replay, policy-session,
  arbitrary-function, and payload conformance on the current Lean toolchain
  and dependency revisions.

## Current frontier

The exact program/version cursor follows the actual package-owned expression
instantiation and then the arbitrary propagator installed by that
instantiation, carrying checked facts across the program extension without a
central function language or rational backend.

## Next step

Resolve exact-head review findings, land the lower semantic stack, and then
implement complete cross-history ordering, cursor-owned equality transport,
and final target closure.

## Blockers

None. The earlier policy-session cumulative-payload atomicity defect is fixed
and covered by a matcher regression in the lower branch.
