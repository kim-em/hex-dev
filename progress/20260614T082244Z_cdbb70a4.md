# Progress — 2026-06-14T08:22Z (feature session, #6949)

## Accomplished

Claimed #6949 and implemented the first bridge-side replacement of the bare
BHKS resultant divisibility assumption in
`HexBerlekampZassenhausMathlib/BadVector.lean`.

`BadVectorBridgeData` now carries the selected-factor CLD producer inputs
instead of a `resultant_divisible_by_p_pow` field:

- transported selected lifted factor monicity;
- selected-factor `natDegree = W.localFactorDegree`;
- the selected-factor auxiliary syzygy congruence for `W.H`;
- the input and auxiliary degree-room bounds.

The public theorem
`ExecutableBadVectorWitness.resultant_divisible_by_p_pow_of_bridge_data`
now derives the divisibility by converting the congruence into a polynomial
syzygy and calling `cld_syzygy_pow_dvd_resultant`. The projection to
`ProjectedBadVectorSetupBridge` uses that theorem instead of projecting a
stored divisibility field.

Verified:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep for `sorry`, `axiom`, `native_decide`,
  `TODO`, and `FIXME`

## Current frontier

This is a partial #6949 implementation. The project-level bridge accessor no
longer hides the resultant divisibility as a field, but the older
`IsBhksBadVectorSetup` and compact `ProjectedBadVectorSetupBridge` surfaces
still expose resultant divisibility directly. The compact surface is still
needed by downstream cap-separation code, with `BadVectorBridgeData` now
populating it from the derived theorem.

`lake build HexBerlekampZassenhausMathlib.Recovery` is currently blocked before
reaching this change by existing errors in
`HexBerlekampZassenhausMathlib/Basic.lean` around the scale-to-dilate recovery
remodel, matching the active #7084 repair work.

## Next step

After #7084 lands, rebuild `HexBerlekampZassenhausMathlib.Recovery` and decide
whether to add explicit Recovery specialization wrappers for the new
`BadVectorBridgeData` fields or continue collapsing the remaining bare
resultant-divisibility setup surfaces.

## Blockers

`HexBerlekampZassenhausMathlib.Recovery` cannot currently be verified because
`HexBerlekampZassenhausMathlib.Basic` fails to build with pre-existing
scale-to-dilate recovery errors.
