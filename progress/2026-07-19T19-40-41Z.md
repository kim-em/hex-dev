# Accomplished

- Proved the pinned `invFloor` relative defect is strictly below `2⁻⁸`, yielding the uniform executable `z1` bound.
- Factored the executable approximate inverse as a scalar in `[0,1]` times the exact centre inverse and compared it with the exact-root correction on the local Newton neighbourhood.
- Bounded the executable radial `z2` sum by a continuous finite exact-centre majorant.
- Proved radius- and natural-precision-threshold theorems concluding the actual `Hex.nkWitness` for every doubled square with the required half-radius centre margin around a simple root.
- Registered and documented the recertification module.

# Current frontier

Plan item 29 is implementation-complete and focused-build green. PR #8828 (item 28) passed exact CI and merged while this work continued.

# Next step

Run umbrella/full and structural checks, obtain the Opus review, publish and merge item 29, then begin the `.pellet` separation-depth slice.

# Blockers

None.
