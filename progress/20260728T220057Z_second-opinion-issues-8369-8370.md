# Pre-merge review for issues #8369 and #8370

## Accomplished

- Obtained an independent, read-only pre-merge review of PR #9052. It found no
  merge-blocking correctness or soundness issue and confirmed that the D1/D2
  termination and no-fallback obligations are delivered without shifting the
  assumptions.
- Validated and fixed two medium review findings. Replaced the stale SPEC
  paragraph describing the deleted paper-shaped `bhksBound` with the shipped
  `R/V/E/C/M/A` resultant threshold and its proved strictness lemmas. Replaced
  the theorem-shaped #8521 guard with a concrete computational witness that
  normalization increases the square-free core's coefficient norm.
- Checked the proposed stronger historical guard before adopting it; it is
  false for the new resultant-based bound, so the corrected guard records the
  exact durable #8521 fact while the separate theorem continues to prove
  `bhksBound core ≤ latticePrecisionCap f`.
- Rebased cleanly onto `origin/main` at `d328c1a3`, incorporating the new
  resultant, number-field, RCF, Lake, and release-manifest work.
- Reverified the rebased tree: `lake build` completed all 9,572 jobs; BZ
  conformance built; benchmark listing succeeded; all 16 benchmark smoke cases
  passed; and fresh 208-line BZ fixtures are byte-identical to the committed
  JSONL.
- Re-ran the CI structural gates: copyright, line limits, dependency DAG,
  release manifest, published trust surface, Phase 4, 110 bench-lint tests,
  Mathlib-free bench imports, and the conformance matrix all pass. The changed
  trust surface introduces no `axiom`, `sorry`, or `native_decide`.

## Current frontier

- The reviewed implementation and its two follow-up corrections are locally
  green on current `main` and ready to publish as the updated PR #9052 head.

## Next step

- Amend and force-push the rebased commit, wait for the hosted build and FLINT
  oracle at that exact head, then merge PR #9052.

## Blockers

- None.
