# 20260510T081702Z - Issue #2962

## Accomplished

Started from the latest progress handoff and checked the coordination queue.
Human-oversight issues #2567, #2566, and #2565 were dependency-blocked.
#2942 was claimed by another worker before this session could take it.

Claimed #2953, reviewed the BHKS lattice independence goal, and marked it
blocked on #2855 because the determinant-based `Matrix.independent` proof needs
the pending Gram/Cauchy-Binet determinant infrastructure.

Claimed #2955, decomposed it into:

- #2962: nonzero gcd from nonzero right input.
- #2963: derivative product divisibility for a shared square factor.
- #2964: final square-free quotient common-divisor assembly.

Then claimed #2962 and added
`gcd_isZero_false_of_right_isZero_false` in `HexPolyFp/SquareFree.lean`.
The helper proves that `DensePoly.gcd a b` is nonzero whenever `b` is nonzero,
using `DensePoly.gcd_dvd_right`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep still reports pre-existing `sorry`s in `SquareFree.lean`; no new
`sorry`, `axiom`, or `native_decide` was introduced.

## Current frontier

#2962 is implemented. #2963 is the next prerequisite for the parent #2955
square-factor exclusion proof.

## Next step

Prove #2963's local product-rule divisibility lemma:
if `d ∣ c`, `d ∣ c'`, and `d ∣ g`, then
`d * g ∣ DensePoly.derivative (c * g)`.

## Blockers

No blocker for #2962. Parent #2955 remains decomposed until #2963 and #2964
land.
