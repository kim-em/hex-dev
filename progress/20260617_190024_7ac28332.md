# Progress — 2026-06-17 (session 7ac28332, /work)

## Accomplished

Claimed #7804 because it directly blocks #7805/#6672. Added the recovered-product
BHKS lift substrate:

- `BHKS.RecoveredLift` in `HexBerlekampZassenhausMathlib/Lattice.lean`, parallel
  to `TrueFactorLift` but with centered/dilated recovery equality instead of raw
  `supportProduct = factor`.
- `BHKS.recoveredLiftOfSubset` in `HexBerlekampZassenhausMathlib/LiftBridge.lean`,
  constructing the package from recovery-side subset evidence without requiring
  `liftedFactorProduct d T = factor`.
- `BHKS.recoveredLiftOfTrueFactorLift` as a thin adapter from existing raw lift
  packages when a caller also has recovered equality.

Posted a corrected issue comment on #7804 documenting the remaining boundary:
`tightColumnBound_of_lift` still derives per-factor integer divisibility from
raw `D.support_product_eq`; recovered centered/dilated equality is whole-product
congruence/recovery data and cannot supply that step.

## Current frontier

The recovered API/substrate mismatch is repaired, but it does not yet feed
`cutProjectionHypotheses_of_trueFactors`. The current CLD-column proof route
requires a raw product identity at `CLDColumnBound.lean:1141-1157` and uses it at
`CLDColumnBound.lean:1177-1185` for per-selected-factor divisibility before the
carry-cancellation argument.

## Next step

Replan the remaining work as a CLD-column theorem/API change: consume
recovered/congruence-level aggregate data for the selected product instead of
using per-factor raw divisibility through `TrueFactorLift.support_product_eq`.
After that theorem exists, thread `RecoveredLift` evidence through the
true-factor norm-bound family and into the forward cut certificate.

## Blockers

No build blocker. Mathematical blocker: the existing tight CLD theorem boundary
is raw-product based, so a recovered-product package alone cannot produce
`TrueFactorCLDTightNormBound` without reintroducing the stronger hypothesis this
issue forbids.
