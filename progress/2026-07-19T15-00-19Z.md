# Accomplished

- Proved that every successful `.nk` loop and `isolateAll?` output is an NK atom linked to an actual executable certification attempt.
- Proved Cauchy-started `.nk` coverage, target precision, pairwise-disjoint circumscribed discs, and the unique interior simple-root contract.
- Added an index-preserving all-atoms extraction bridge and proved successful `isolate` assigns every complex root to exactly one returned closed square.
- Tracked the zero-polynomial and nonzero-constant branches exactly, including nonvanishing of the complex cast for nonzero executable polynomials.
- Ran focused and full builds, structural policy checks, conformance and fixture targets, and all HexRoots benchmarks successfully.
- Obtained an independent Opus review with no blocking correctness or soundness concerns.

# Current frontier

The NK-only atom path is sound end to end: successful isolation emits pairwise-disjoint NK atoms, every root occurs in exactly one atom, and every atom carries a unique interior simple root.

# Next step

Land this slice, then begin the Mathlib-only circle partial-fraction and integral prerequisites in issue #8799.

# Blockers

None.
