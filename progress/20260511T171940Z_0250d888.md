# Work issue #3513 — HexBZ bench schedule retune

## Accomplished

- Claimed issue #3513 and retuned the HexBZ non-profile benchmark schedules.
- Widened the public `factor` and proof-facing `factorFast` split-family
  schedules from the old smoke ladder to the completing scientific ladder
  `n = 2..5`.
- Narrowed `runFactorSlowDegreeHeightChecksum` to the completing diagnostic
  subset `1002`, `2002`, and `3008`, removing the previously committed
  cap-hitting `4008` rung.
- Ran and committed a new timing artefact at
  `reports/bench-results/hex-berlekamp-zassenhaus-issue3513.json`.
- Updated `reports/hex-berlekamp-zassenhaus-performance.md` with the new
  command, SHA-256, verdicts, hashes, and revised concerns.
- Verified `lake build HexBerlekampZassenhaus.Bench`,
  `lake exe hexbz_bench list`, `lake exe hexbz_bench verify`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the banned-marker
  scan for `HexBerlekampZassenhaus/Bench.lean`.

## Current frontier

- The retuned public and fast split-family schedules have verdict-eligible
  rows beyond the old `n = 1..4` smoke surface, but the verdicts remain
  inconclusive.
- An exploratory wider run hit the eight-second cap at `n = 6`, so larger
  split inputs still need either algorithmic improvement or a longer scheduled
  run.
- The slow-path degree/height target is now an explicit completing diagnostic
  subset, not Phase 4 completion evidence for the full slow path.

## Next step

- Continue HO-3 work on the remaining benchmark concerns: full scientific
  verdicts, singleton adversarial scaling coverage, compare-domain widening,
  and the separately tracked symbolicated-profile gap.

## Blockers

- None for this slice.
