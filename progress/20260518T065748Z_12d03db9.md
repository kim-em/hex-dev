# 2026-05-18 06:57 UTC — exhaustiveCoreFactorsWithBound_degree_pos_of_primitive_pos_lc_core (12d03db9)

Claimed feature issue #4946 and landed the helper-5 weakened-conclusion
substrate plus its five supporting lemmas in
`HexBerlekampZassenhaus/Basic.lean`.

## What landed

Six lemmas in `HexBerlekampZassenhaus/Basic.lean`, all Mathlib-free,
`native_decide`-free, no new `sorry`/`axiom`/`TODO`/`FIXME`:

1. **`normalizeFactorSign_primitive`** (`f.Primitive → (normalizeFactorSign f).Primitive`).
   The `if_neg` branch is identity; the `if_pos` branch scales by `-1`,
   which preserves content via `DensePoly.content_scale_neg_one`.

2. **`scaledRecombinationSearchModAux_primitive`** (the substrate).
   Inductive proof mirroring
   `scaledRecombinationSearchModAux_normalizeFactorSign` shape-for-shape.
   The head emitted candidate is `normalizeFactorSign (primitivePart x)`
   where `x = centeredLiftPoly (scale coreLc (polyProduct subset)) modulus`.
   The `shouldRecord candidate = true` hypothesis forces
   `candidate ≠ 0`, which (via the contrapositive of
   `normalizeFactorSign_ne_zero_of_ne_zero`) gives `primitivePart x ≠ 0`,
   which (via the contrapositive of
   `DensePoly.primitivePart_eq_zero_of_content_eq_zero`) gives
   `content x ≠ 0`, then `Primitive (primitivePart x)` via
   `ZPoly.primitivePart_primitive`, and finally
   `Primitive candidate` via deliverable 1.

3. **`scaledRecombinationSearchMod_primitive`** — thin one-liner wrapper.

4. **`recombineScaledExhaustive_primitive`** — three-case match wrapper.

5. **`exhaustiveCoreFactorsWithBound_primitive`** — three-case match
   mirroring `_shouldRecord`: the two `#[core]` short-circuit branches
   return `hcore_primitive` directly; the genuine recombination branch
   dispatches to deliverable 4.

6. **`exhaustiveCoreFactorsWithBound_degree_pos_of_primitive_pos_lc_core`**
   — the headline weakened-conclusion sibling of
   `exhaustiveCoreFactorsWithBound_degree_pos`. Swaps
   `DensePoly.Monic` on `core` for the trio
   `(Primitive, leadingCoeff > 0, shouldRecord)`. The
   `normalizeFactorSign core = core` discharge uses
   `normalizeFactorSign_eq_self_of_leadingCoeff_nonneg` with the
   positivity weakened to non-negativity via `omega`.
   In the size-1 contradiction case, `Primitive q` combined with
   `q = C (q.coeff 0)` and `DensePoly.content_C` forces
   `(q.coeff 0).natAbs = 1`, while `normalizeFactorSign q = q` forces
   `q.coeff 0 ≥ 0` (via `normalizeFactorSign_leadingCoeff_nonneg` plus
   `leadingCoeff = coeff 0` for size-1 polys), so `q.coeff 0 = 1`,
   hence `q = 1`, contradicting `shouldRecord q = true`.

## Mathlib-free wrinkles

* `conv_lhs` / `le_of_lt` / `Int.ofNat_eq_cast` are unavailable in this
  Mathlib-free file. Replacements:
  - `conv_lhs => rw [hq_eq_C]` → `congrArg DensePoly.content hq_eq_C`
    followed by `.trans (DensePoly.content_C ...)` — sidesteps the
    self-rewriting issue where `rw [hq_eq_C]` on a goal containing
    `q.coeff 0` would expand the coefficient too.
  - `le_of_lt hcore_lc_pos` → `by omega`.
  - `Int.ofNat_eq_cast` → `Int.ofNat_eq_natCast` (the actual name).

* `content_C_int` (HexPolyZ/Basic.lean:2863) is `private` in a separate
  file. Used `Hex.DensePoly.content_C` (HexPoly/Euclid.lean:4625)
  directly via `DensePoly.content (DensePoly.C c) = Int.ofNat c.natAbs`,
  paired with the definitional equality `ZPoly.content = DensePoly.content`.

## Layout

All six lemmas sit alongside the existing `_normalizeFactorSign` /
`_shouldRecord` chain for their respective layers, matching the issue's
"mirroring the structure of the existing chains" prescription:

* D1 after `normalizeFactorSign_idem` (~line 3489).
* D2 between `scaledRecombinationSearchModAux_shouldRecord` and
  `scaledRecombinationSearchMod_normalizeFactorSign` (~line 8208).
* D3 after `scaledRecombinationSearchMod_normalizeFactorSign`.
* D4 after `recombineScaledExhaustive_normalizeFactorSign`.
* D5 after `exhaustiveCoreFactorsWithBound_shouldRecord`.
* D6 after `exhaustiveCoreFactorsWithBound_degree_pos`.

## Verification

* `lake build HexBerlekampZassenhaus.Basic` — clean, no errors. Two
  warnings, both pre-existing: the `simpa` linter warning at line 2407
  and the existing `sorry` at line 6570 (`Irreducible`).
* `lake build HexBerlekampZassenhausMathlib` — 16 errors, all in
  `HexBerlekampZassenhausMathlib/Basic.lean` (which this PR does not
  touch). The error set matches the documented baseline from
  `progress/20260518T055502Z_003d4956.md`: 8 whnf timeouts in lines
  4848-6293, the `cases` failure at 5851, kernel-unknown errors at
  6608/6672/15560/15606, and whnf timeouts at 15242/15353.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhaus/Basic.lean |
   grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` —
  no new entries.

## Carryover

* The helper-5 leaf substrate is now in place. The umbrella drop in
  #4880 (or a separate substrate sub-issue) can now thread
  `(Primitive, leadingCoeff > 0, shouldRecord)` through
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` via a
  Mathlib-bridge sibling of
  `reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`,
  per the umbrella drop plan in #4880 / #4942.
* The original `exhaustiveCoreFactorsWithBound_degree_pos` and
  `exhaustiveCoreFactorsWithBound_monic` remain unchanged, per the
  issue's explicit "stays as is" caveat (the `Monic` conclusion is
  false for non-monic primitive core).
