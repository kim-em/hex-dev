# Repair PR #3431

## Accomplished

- Claimed PR #3431 for repair.
- Diagnosed the current failed `conformance` check as a SAT solver timeout
  at `HexArith/Montgomery/InvNat.lean:20` while building
  `HexArith.Montgomery.InvNat`.
- Raised only that timed-out `bv_decide` proof's timeout from 30 seconds to
  120 seconds, matching the adjacent Montgomery inverse proof's existing
  CI-robust timeout.
- Verified locally with:
  - `lake build HexArith.Montgomery.InvNat`
  - `lake build`
  - `python3 scripts/conformance_targets.py --check`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide' HexArith/Montgomery/InvNat.lean`
  - `lake build $(python3 scripts/conformance_targets.py --space-separated) ...`

## Current frontier

- PR #3431 has a targeted robustness fix for the remote conformance failure.

## Next step

- Commit this progress note with the timeout fix, force-push the PR branch,
  and mark PR #3431 salvaged.

## Blockers

- None.
