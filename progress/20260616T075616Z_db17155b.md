# Progress — 2026-06-16T07:56:16Z (session db17155b, work)

## Accomplished

Claimed issue #7560 and re-keyed the slow modular exhaustive branch to use
`ZPoly.toMonicPrimeData?` for the normalized square-free core before building
`ZPoly.toMonicLiftData`.

Updated the executable slow raw-factor wrapper, slow modular branch
classification, branch-shape/product lemmas, and the non-monic guard to reflect
the `toMonicPrimeData?` source.  Also updated `factorWithBound?` so its explicit
success/failure surface treats either fast-path `choosePrimeData?` or slow-path
`toMonicPrimeData?` availability as a safe prime-data branch.

Adjusted the Mathlib-side `IntReductionMod` caller of
`factorWithBound_entry_mem_exhaustive_branch_xPower_or_core_of_reassemblyComplete`
to pass the matching `hselected : toMonicPrimeData? = some primeData` witness.

Verification:
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over the touched files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Second-opinion review agreed the slow branch re-key was internally
  consistent, flagged the stale `factorWithBound?` selector surface, and that
  surface was updated before final verification.

## Current frontier

The executable slow modular branch now selects prime data for the monic
transform it actually Hensel-lifts.  The fast branch remains keyed to
`choosePrimeData? (normalizeForFactor f).squareFreeCore`.

## Next step

Use the re-keyed slow modular executable/API surface in the downstream raw
irreducibility integration work.

## Blockers

None for #7560.  The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md`; it was not touched or staged.
