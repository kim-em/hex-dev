## Accomplished

Claimed #2998 after human-oversight issues #2567, #2566, and #2565 all failed
claim checks as blocked, and #2990 was claimed concurrently.

Added a compiling private nullspace entry helper layer in `HexMatrix/RREF.lean`:
`nullspaceMatrix_free`, `nullspaceMatrix_free_ne`, `nullspaceMatrix_pivot`,
`nullspace_get`, `nullspace_get_free`, `nullspace_get_free_ne`, and
`nullspace_get_pivot`. These expose the generated nullspace basis vector values
on free and pivot columns, reusing the existing pivot lookup lemmas.

Created successor issue #3011 for the remaining finite-sum row cancellation
needed to prove `nullspace_echelon_sound`, and left the required decomposition
breadcrumb on #2998.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`

## Current frontier

#2998 is ready for a partial PR containing the private nullspace entry rewrite
infrastructure. The main theorem `D.echelon * E.nullspace.get k = 0` remains
open and is now tracked by #3011.

## Next step

Claim #3011 and prove a finite-list/fold partition lemma over `List.finRange m`
using `E.toIsEchelonForm.colPartition`, then use it to close the rowwise
`nullspace_echelon_sound` helper.

## Blockers

None. The worktree still has an unrelated pre-existing `.claude/CLAUDE.md`
modification that this session did not touch.
