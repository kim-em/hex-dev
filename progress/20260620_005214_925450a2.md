# Phase 6 grind coverage — HexGF2/Field.lean GF2n enumeration cluster (#8124)

## Accomplished

Promoted the 7 public, `@[simp]`-only, literal `lhs = rhs` equation lemmas
in the GF2n coefficient-enumeration cluster (file head, lines ~67–950) of
`HexGF2/Field.lean` to `@[simp, grind =]`:

`boolCoeffValues_length` (L67), `coeffBoolLists_zero` (L135),
`coeffBoolLists_succ` (L141), `coeffBoolLists_length` (L197),
`inv_zero` (L583, the **GF2n** type — distinct from the GF2nPoly
`inv_zero` at L1290 which already carries `grind =`),
`reducePoly_val_eq_mod` (L680), `elements_length` (L950).

Each was re-read at the point of promotion and confirmed a literal
equation (no `↔`/`<`/`≤`/`Ne`/`Associated`/conjunction/`let`-wrapped
conclusion, no `getD`/`dite` LHS head). Every RHS strictly reduces term
structure (collapses a constructor, peels a `cons`, or projects through
`.val`), so all terminate as rewrite rules. No exclusions. The
`frobeniusIter` cluster and the `mem_*` (`↔`-shaped) lemmas were left
untouched, as the issue directed.

## Current frontier

`lake build HexGF2` green; `HexGF2.Field` elaborates in 1.6s — identical
to the #8121 baseline, so no grind-loop slowdown, and no grind
heartbeat/timeout warnings. Full downstream build (1139 jobs) clean
including `HexGFq.CrossCheck`, `HexGF2Mathlib`, `HexGFqField`. The only
linter notes in the downstream build are pre-existing ones in
`HexGF2Mathlib/Basic.lean` (untouched). `scripts/check_dag.py` exit 0.
Annotation-only; no proof bodies, signatures, imports touched; no
`sorry`/`axiom`/`native_decide` added.

## Next step

None for this issue. Sibling unclaimed Phase 6 sweeps remain: #8123
(HexGFqField `div_eq_mul_inv`) and #8133 (HexGF2 Basic.lean /
Irreducibility.lean missed equation lemmas), both disjoint from this file.

## Blockers

None.
