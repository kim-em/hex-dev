# Progress — 2026-06-18 (session 31f9670f, /once → #7869)

## Accomplished

Landed the period-aware aggregate carry lemma `two_mul_natAbs_sum_psiCut_period_le`
in `HexBerlekampZassenhausMathlib/CLDColumnBound.lean` (BHKS namespace, right after
the raw-sum `two_mul_natAbs_sum_psiCut_le`).

The lemma bounds the high-bit cut-sum *modulo the period* `q = p^(a−b)` from only an
**aggregate** residue hypothesis (`centeredResiduePow p a (Σ centeredResiduePow p a (z i)) = y`,
`|y| ≤ B`, `2B < p^b`), producing `∃ t, 2·|Σ psiCut − t·q| ≤ |T|+1`. This is the
shape the recombination/split-irreducible case needs, where the raw-sum bound is
false (no per-factor smallness is available).

Proof structure (mirrors the raw lemma's tail):
- per-element decomposition `p^b·psiCut(z_i) = w_i − lo_i` via
  `Hex.centeredResiduePow_add_pow_mul_psiCut`;
- aggregate `p^b·col = Σ w − Σ lo`;
- the aggregate-residue hypothesis unfolds to
  `Hex.centeredModNat (Σ w) (p^a) = y`, and `Hex.self_sub_centeredModNat_dvd`
  gives `p^a ∣ Σ w − y`, i.e. `Σ w = y + k·p^a`;
- `p^a = p^b · p^(a−b)` (using `b ≤ a`) lets the witness `t := k` absorb the
  large `k·p^a` term, yielding `p^b·(col − k·q) = y − Σ lo`;
- the centered lower-residue bound `2·|lo_i| ≤ p^b` plus `2B < p^b` cancels `p^b`
  to give `2·|col − k·q| < |T|+1`.

No new hypotheses on factor data; pure arithmetic over `ℤ`.

## Verification

- `lake build HexBerlekampZassenhausMathlib` → `Build completed successfully
  (3790 jobs)`; `CLDColumnBound` built with no warnings.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  in the diff.
- Pre-existing unrelated `sorry` in `FactorSoundness.lean:18` and the
  `.claude/skills/agent-worker-flow/SKILL.md` modification are untouched.

## Current frontier

`two_mul_natAbs_sum_psiCut_period_le` is the analytic core for the RecoveredLift
period-aware column bound.

## Next step

Consume this lemma in the RecoveredLift period-aware column bound (the next issue
under parent directive #2564).

## Blockers

None.
