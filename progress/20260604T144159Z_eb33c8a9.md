# 2026-06-04T14:41:59Z — Bareiss-side helpers for getArrayEntry_scaledCoeffRowsSchur_eq closure (sub-A of #6549)

Session UUID prefix: eb33c8a9
Issue: #6550

## Accomplished

Added three private helper lemmas to `HexGramSchmidt/Int.lean`, inserted after
`scaledCoeffRows_eq_zero_of_singularStep_lt`:

- `scaledCoeffRows_lower_eq_noPivotLoop_gramMatrix_of_no_singular`: the
  strict-lower non-singular Bareiss-side bridge at the `gramMatrix`
  matrix-level, extracted from the existing `h_state_level` intermediate of
  `scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix`. Composes
  `scaledCoeffArrayLoop_lower_matches_target_column` with the
  `j.val - 0 = j.val` step normalisation.
- `scaledCoeffRows_diag_eq_noPivotLoop_gramMatrix_of_no_singular`: the
  diagonal non-singular Bareiss-side bridge. Composes
  `scaledCoeffArrayLoop_diag_matches` at fuel `n` with
  `Matrix.noPivotLoop_diag_of_le_step` and
  `noPivotLoop_step_eq_add_of_singularStep_none` to bridge from the
  full-trajectory diagonal at `j` to the truncated-trajectory diagonal at
  the same row. The `s ≤ j` singular sub-case is closed by monotonicity
  (`noPivotLoop_step_monotone` forces `j ≤ s`, so `s = j`) plus
  `noPivotLoop_singular_inv` to extract `matrix[j][j] = 0` on both sides.
- `scaledCoeffRows_diag_eq_zero_of_singularStep_lt`: the diagonal singular
  Bareiss-side bridge. Lifts singularity from prefix `j` to full `n` via
  `noPivotLoop_singularStep_of_prefix_singular`, then reads off the
  `s' ≤ j` zero branch of `scaledCoeffArrayLoop_diag_matches`.

`lake build` clean across all 244 targets. Sorry count unchanged
(`HexGramSchmidt/Int.lean` 3 pre-existing at 6149, 6463 plus the prose
sorry at 6136; no new sorries). No `axiom` or `native_decide` introduced.

## Current frontier

This is sub-A of the residual sorry closure work (#6549). The three
helpers are private and currently unused; they will be consumed by the
follow-up sub-issue that closes the residual sorry at line 6463 (the
`j ≤ i, j ≠ 0` case of `getArrayEntry_scaledCoeffRowsSchur_eq`).

## Next step

A successor sub-issue (sub-B of #6549) can now thread these three
helpers into the residual sorry. The directive premise was sound: the
existing infrastructure (`scaledCoeffArrayLoop_diag_matches`,
`noPivotLoop_singular_inv`, `noPivotLoop_step_monotone`,
`Matrix.noPivotLoop_diag_of_le_step`,
`Matrix.noPivotLoop_add`,
`noPivotLoop_step_eq_add_of_singularStep_none`,
`noPivotLoop_singularStep_of_prefix_singular`) covers both the
non-singular bridge and the singular fall-through without any new
core lemmas.

## Blockers

None.
