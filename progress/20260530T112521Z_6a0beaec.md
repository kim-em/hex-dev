**Accomplished**

- Claimed #5770, a directive/feature issue for removing process jargon from Lean docstrings and comments.
- Avoided overlap with the open partial PR #5780 by cleaning a remaining file outside that PR's touched set: `HexBerlekampZassenhaus/Basic.lean`.
- Rewrote prose-only occurrences of `consumer`, `substrate`, `bridge`, and `umbrella` in `HexBerlekampZassenhaus/Basic.lean` to concrete terms such as call site, theorem, support lemma, Mathlib-side layer, and public wrapper.
- Verified `rg -n '\b(bridge|smoke|gate|substrate|umbrella|consumer)\b' HexBerlekampZassenhaus/Basic.lean` returns no hits.
- Verified `lake build HexBerlekampZassenhaus` succeeds.

**Current frontier**

- This is a partial continuation of #5770. The BZ `Basic.lean` prose sweep is done, but the repository-wide Lean prose sweep still has remaining hits in other libraries.
- The build still reports pre-existing warnings from replayed dependencies and an existing `sorry` warning in `HexBerlekampZassenhaus/Basic.lean`; this session did not add declarations or proof changes.

**Next step**

- Continue the repository-wide prose sweep with files not already covered by PR #5780, then rerun the issue grep and the relevant library builds.

**Blockers**

- None.
