# 20260601T132130Z — 5aa8e198: #5978 choosePrimeData compatibility wrapper

## Accomplished

Claimed #5964, verified it was too broad for one worker slice, and decomposed it
into #5978, #5979, and #5980. Marked #5966 blocked on #5933 because its cited
refreshed LLL evidence was still only in an open PR at the time.

Implemented #5978 in `HexBerlekampZassenhaus/Basic.lean`:

* Removed the false `choosePrimeData?_isSome` theorem and its `sorry`.
* Added `choosePrimeDataFallback`, an audited `p = 3` compatibility fallback
  with the correct modular image and empty modular factors.
* Redefined `choosePrimeData` as an explicit match on `choosePrimeData?`.
* Updated `choosePrimeData_eq_of_choosePrimeData?_some`,
  `choosePrimeData_prime`, slow-path docs, and the slow Option/total
  compatibility proof.
* Adjusted the fast product proof's no-prime-data cases so the deleted theorem
  is no longer required while preserving the existing executable fast-path
  behavior for the dependent #5979 slice.

## Current frontier

#5978 is ready for PR. The executable fast path still uses the compatibility
wrapper in non-singleton/core attempts; that behavior is intentionally left to
#5979, with downstream product/entry proof propagation split into #5980.

## Next step

Create the PR for #5978. After it lands, #5979 should refactor
`factorFastFactorsWithBound` to match on `choosePrimeData?` and return `none`
when no selected prime witness exists.

## Blockers

None for #5978. #5979 depends on #5978, and #5980 depends on #5979.

## Verification

* `lake build HexBerlekampZassenhaus` — green.
* `rg -n "choosePrimeData\\?_isSome" HexBerlekampZassenhaus/Basic.lean` —
  no matches.
* `git diff --check` — green.
* `git diff -- HexBerlekampZassenhaus/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true` —
  no matches.
* `python3 scripts/check_dag.py` — green.
