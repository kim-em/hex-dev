# Session 84c03d7e — terminal reachable Yun residual derivative-zero (#3864)

## Accomplished

- Claimed #3864 after checking the unclaimed and human-oversight queues.
- Added `yunFactorsPairwiseReachable_residual_dvd_derivative_product` in
  `HexPolyFp/SquareFree.lean`, packaging the reachable-state induction around
  the derivative-split base case and the existing Yun step transfer.
- Added `yunFactorsPairwiseReachable_terminal_residual_derivative_zero`, closing
  terminal reachable states by rewriting `c = 1` and applying
  `derivative_isZero_true_of_dvd_self_derivative`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned
  marker scan.

## Current frontier

The reachable terminal residual derivative-zero lemma is now available for the
next layer that wires the unscaled derivative-active Yun residual provider.

## Next step

Use `yunFactorsPairwiseReachable_terminal_residual_derivative_zero` in the
remaining #3846 provider wiring.

## Blockers

None. The worktree still has the pre-existing `.claude/CLAUDE.md` modification
from pod bootstrap; it was not staged.
