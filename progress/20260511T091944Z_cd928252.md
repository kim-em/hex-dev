# 2026-05-11 09:19 UTC — work/feature session (cd928252)

## Accomplished

- Claimed #3328 (`HexBZ: enforce p >= 3 in good-prime selection`).
- Updated `HexBerlekampZassenhaus/Basic.lean` so `isGoodPrime` requires
  `3 <= p`, the fixed small-prime chooser no longer considers `2`, and the
  total `choosePrime` / `choosePrimeData` fallbacks use `3`.
- Added `isGoodPrime_ge_three` as the proof-facing projection from a successful
  executable good-prime check.
- Updated `HexBerlekampZassenhaus/Conformance.lean` guards so the pinned
  prime-selection behavior rejects `2` and expects `3` as the smallest
  supported choice.

## Current frontier

- `lake build HexBerlekampZassenhaus.Conformance` succeeds.
- `lake build HexBerlekampZassenhaus` succeeds.
- `python3 scripts/check_dag.py` and `git diff --check` are clean.
- The requested banned-marker grep reports only the pre-existing tracked
  `sorry`s in `HexBerlekampZassenhaus/Basic.lean`.

## Next step

- Let the PR CI exercise the full project. No follow-up issue is needed for
  this narrow prime-selection contract change.

## Blockers

- None.
