# Progress: /plan cycle — diagnose red HexBZMathlib build, defer CI gate

Planner session. Queue deficit was 2 (POD_MIN_QUEUE=3, one unclaimed
item, #6866).

## Accomplished

- Merged ready PR #6957 (doc: record /work triage) — fully green,
  non-draft. Left #6974 (pending `build` check) and the draft spec PR
  #2656 alone.

- **Diagnosed the recurring "Mathlib bridge red on main, undetected by
  CI" failure.** On current main (`98e715b7`, Mathlib cache warm),
  `lake build HexBerlekampZassenhausMathlib.Basic` fails with 13
  `Application type mismatch` errors (9544, 9555, 9654, 9961, 10453,
  14401, 14523, 14746, 16140, 16522, …). Every one is a consumer passing
  `RepresentsIntegerFactorAtLift core d factor S` where a lemma now
  expects the unbundled congruence
  `(scaledLiftedFactorProduct core d S).reduceModPow d.p d.k =
   factor.reduceModPow d.p d.k`. This is fallout from the #6938
  definitional carrier swap (`RepresentsIntegerFactorAtLift :=
  Nonempty RecoveredAtLift`). The earlier #6890 progress note claiming
  "Basic builds clean" was stale/wrong; the #6956 review's red finding
  was correct.

- **Confirmed the red build is already covered by claimed work.** #6819
  (claimed, "migrate lifted partition consumers to recovered
  representation") has exactly these consumers as its deliverables and a
  green `lake build HexBerlekampZassenhausMathlib` as its verification
  step. The migration is actively progressing (#6980 "add recovered
  support partition field" merged today). So no new red-build fix issue
  was filed — that would duplicate #6819.

- **Deferred #6866 (CI gate) onto #6819.** Its premise — that #6855
  restored a green build — is false: #6855 closed but the library
  regressed via #6938. Ran `coordination add-dep 6866 6819` (now
  `blocked`) and posted a comment with the concrete error list. The gate
  must not be added while the target is red.

## Current frontier

The HO-1 directive (#2564) frontier is densely and correctly planned:
the blocked DAG (#6672 capstone down through the CLD chain #6977–#6979,
B-builder #6771/#6777/#6779/#6786, slow path #6773/#6774, dilation
migration #6804) plus four claimed issues (#6976, #6973, #6959, #6819).
The real bottleneck is #6819 restoring the green HexBZMathlib build;
until it lands, the headline theorem cannot be verified and the
dilation-dependent chain stays blocked.

## Next step

No new issues created this cycle. The foundational Phase-6 libraries
(HexArith etc., all `done_through: 5`) are already at Mathlib quality —
spot-checked `HexArith/ExtGcd.lean`: full docstrings, `@[simp]`,
characterising lemmas, clean structure. Filing Phase-6 issues there
would produce near-empty PRs, so it was not done (quality over
deficit-filling, per the planner doctrine). The queue will self-refill
from the HO-1 DAG as the four claimed issues land and unblock their
dependents; the next planner cycle should re-survey once #6819 (green
build) and #6976 (CLD Euclidean reconstruction) merge.

## Blockers

None for the planner. Watch #6819: it has been claimed several hours
with no open PR yet. If it goes stale, the green-build restoration — and
with it #6866 and the dilation chain — stalls. Stale-claim reaping is
the coordination system's responsibility, not the planner's.
