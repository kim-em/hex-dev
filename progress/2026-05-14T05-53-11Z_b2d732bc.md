# 2026-05-14T05:53Z — feature session b2d732bc

## Accomplished

- Claimed and executed #3978 ("HexGramSchmidt: prove List range and
  Array set fold lemmas for scaledCoeffArrayLoop"), the residual
  fold/set bookkeeping prerequisite for #3964.
- Added three reusable `List.foldl`-over-`Array.set!` helpers in
  `HexGramSchmidt/Int.lean`:
  - `getElem!_foldl_set!_of_notMem`: untouched index is unchanged.
  - `getElem!_foldl_set!_of_mem_nodup`: in-bounds member index reads
    `f r` for a `Nodup` index list.
  - `size_foldl_set!`: outer array size is preserved.
- Added six narrow per-entry lemmas for `stepScaledRows` (the inner
  nested-loop body of the scaled-coefficient sweep):
  - `getArrayEntry_stepScaledRows_of_row_le` / `_of_row_ge`: rows
    outside `[k+1, n)` are untouched.
  - `stepScaledRows_row_at_trailing`: intermediate fold-form
    characterisation for trailing rows.
  - `getArrayEntry_stepScaledRows_pivot_col`: pivot column cleared
    at every trailing row.
  - `getArrayEntry_stepScaledRows_trailing`: the fraction-free
    Bareiss update formula for entries in the trailing window.
  - `getArrayEntry_stepScaledRows_of_col_lt`: entries strictly left
    of the pivot column are preserved at trailing rows.
  - `stepScaledRows_size`: outer-array length unchanged.

## Current frontier

`HexGramSchmidt.Int` now exposes the fold/set scaffolding #3964 needs
to state and prove the `scaledCoeffArrayLoop` Cramer no-pivot
invariant against `Matrix.stepMatrix`.

## Next step

#3964 ("HexGramSchmidt: prove scaledCoeffArrayLoop Cramer no-pivot
invariant") is now unblocked and can be claimed by a feature worker.

## Blockers

None. `lake build HexGramSchmidt.Int` and `lake build HexGramSchmidt`
are green; `python3 scripts/check_dag.py` passes; no new
`axiom`/`native_decide`/`TODO`/`FIXME`/`sorry` markers introduced.
