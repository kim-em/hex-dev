# 20260510T001441Z - Issue #2904

## Accomplished

Read the latest progress note and followed the worker flow. Inspected the
human-oversight queue first; the available oversight items remain blocked by
dependencies.

Claimed #2834, verified that its requested consumer-side change already landed
on `origin/main` in PR #2898 / commit `2892758`, and released the stale claim
with a replan note. The targeted build `lake build HexPolyFp.SquareFree`
completed successfully with the pre-existing `sorry` warnings.

Attempted to claim #2876 after confirming its dependency #2875 is closed, but
the claim failed because another agent had already claimed it. Per the worker
flow, did not work on #2876 after the failed claim.

Claimed #2904 when it appeared in the unclaimed queue.

Added `ExecutableBadVectorWitness` to
`HexBerlekampZassenhausMathlib/BadVector.lean`, tying the executable input
`Hex.ZPoly`, `Hex.LiftData`, `Hex.BhksLatticeBasis`, projected BHKS rows,
selected local-factor index/degree, and auxiliary executable polynomial `H` to
the Mathlib-side resultant layer.

Added transport helpers:

- `ExecutableBadVectorWitness.inputPolynomial`
- `ExecutableBadVectorWitness.auxiliaryPolynomial`
- `ExecutableBadVectorWitness.selectedLiftedFactor`
- `ExecutableBadVectorWitness.toResultantData`
- `ExecutableBadVectorWitness.badVector_resultant_bounds`

The new theorem packages the later local coprimality and modular-divisibility
hypotheses into the existing `BadVectorResultantData.badVector_resultant_bounds`
API; it does not prove BHKS Lemma 3.2 or alter executable recombination.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhausMathlib/BadVector.lean`

## Current frontier

#2904 is implemented locally. `HexBerlekampZassenhaus/Basic.lean` still has its
pre-existing `sorry` markers; this session did not modify that file.

## Next step

Create the PR for #2904. Later workers can instantiate
`ExecutableBadVectorWitness` from concrete BHKS CLD bad-vector data and supply
the local coprimality/divisibility hypotheses.

## Blockers

The worktree had a pre-existing unrelated modification to `.claude/CLAUDE.md`,
which this session left untouched.
