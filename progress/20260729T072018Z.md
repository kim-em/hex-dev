# Interval sine arbitrary-prefix replay

## Accomplished

- Strengthened the sine package's structural model so old-node meanings can be
  transported across arbitrary append-only caller programs.
- Replaced the literal five-node instance theorem with a generic conservative
  extension theorem parameterized by the caller program, recovered input, and
  snapshot-local sine and negation operation identifiers.
- Made semantic operation resolution use opaque keys plus complete unary-real
  signature and exact lookup checks.
- Reworked the instance schema to recover `sin (-x)` structurally, verify the
  exact two-node recipe and event products, and replay the generic theorem.
- Added canaries for reordered operation tables, unrelated prior nodes, a
  wrong appended recipe, wrong product metadata, and a same-key wrong
  signature.
- Moved the noncomputable section below the compiled search guard and removed
  literal fixture-only semantic witnesses.
- `lake build HexInterval.SinOddConformance`, the line-count lint, and
  `git diff --check` pass without warnings.

## Current frontier

The semantic instance proof is now arbitrary-prefix and operation-order
independent. The executable callback still uses the older framework request
shape until this branch is restacked onto the final arbitrary-package head.

## Next step

Restack on the final proof-session integration branch, resolve sine and
negation identifiers with `ProgramView.findOp?` in the runtime callback,
remove obsolete request fields, rebuild the full focused graph, and obtain an
independent proof review.

## Blockers

None. The lower framework branches are being integrated in parallel.
