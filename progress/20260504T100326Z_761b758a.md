# Phase 1 audit: HexBerlekampZassenhaus → done_through 1 (#2071)

## Accomplished

- Audited every named SPEC declaration in
  `SPEC/Libraries/hex-berlekamp-zassenhaus.md` against the existing
  `HexBerlekampZassenhaus/Basic.lean`. All 13 SPEC-named declarations
  (`factorWithBound`, `factor`, `isGoodPrime`, `choosePrime`,
  `PrimeChoiceData`, `LiftData`, `choosePrimeData`, `henselLiftData`,
  `recombine`, `factor_product_of_bound`, `PrimeFactorData`,
  `ZPolyIrreducibilityCertificate`, `checkIrreducibleCert`) are
  present with signatures matching the SPEC modulo necessary
  `[ZMod64.Bounds p]` instance arguments and concrete instantiation of
  the certificate's "degree analysis data" comment as a
  `degreeObstructions : Array DegreeObstruction` field.
- Confirmed every `.lean` file under `HexBerlekampZassenhaus/` plus
  the library root carries a module docstring.
- Confirmed all 26 `sorry` placeholders are in `theorem` bodies (not
  `def` or data-carrying `structure` fields), i.e. allowed under
  Phase 1. No `axiom` / `native_decide` / `TODO` / `FIXME` / `...`
  placeholders.
- Bumped `libraries.yml HexBerlekampZassenhaus.done_through: 0 → 1`.
- `lake build HexBerlekampZassenhaus` green; `python3
  scripts/check_dag.py` clean; `git diff --check` clean.

## Current frontier

`HexBerlekampZassenhaus` ready to enter Phase 2 (oracle cross-check)
and Phase 3 (conformance). `HexBerlekampZassenhausMathlib` (deps:
`[HexBerlekampZassenhaus, HexPolyZMathlib]`) is now eligible for its
own Phase 1 once `HexPolyZMathlib` reaches `done_through ≥ 3`
(currently 2; Phase 3 conformance issue #2070 is the unblocking
work).

## Next step

Open follow-on work for the 26 capstone-library theorem `sorry`s, in
narrow slices (per Phase 1 norms — these are not part of this audit
PR's scope).

## Blockers

None.
