# Phase 6 grind coverage for HexBerlekampZassenhaus/Basic.lean (#8128)

## Accomplished

Promoted 17 equation-shaped public `@[simp]` lemmas in the Mathlib-free
executable file `HexBerlekampZassenhaus/Basic.lean` to `@[simp, grind =]`:

The 16 from the issue candidate list — `transformedCoeffs_size`,
`transformedCore_size`, `toMonic_core`, `toMonic_leadingCoeff`,
`toMonic_degree`, `factorPower_zero`, `factorPower_succ`,
`product_mk_empty`, `henselLiftData_p`, `henselLiftData_k`,
`factorSlowModular_eq_factorSlowModularWithBound_default`,
`factor_eq_factorWithBound_default`, `factor?_eq_factorWithBound?_default`,
`factorizationOfFactors_scalar_zero`, `factorWithBound_scalar_zero`,
`factor_scalar_zero` — plus `transformedCore_degree_getD` (one of the two
`getD`-headed lemmas flagged for testing).

All 17 confirmed literal `lhs = rhs` (Eq) on re-read; none `↔`/`<`/`≤`/
membership.

## Exclusion

`transformedCoeffs_getD_top` (`(transformedCoeffs core degree).getD degree
0 = 1`) is left `@[simp]`-only. Its LHS head is `Array.getD`, which unfolds
to a `dite` over `Array.getInternal`, and `grind =` rejects it with
`invalid pattern, (non-forbidden) application expected` (a hard build
failure) — exactly the known `Array.getD` offender from the
hex-lean-mathlib-boundary skill and the #8104 note. The sibling
`transformedCore_degree_getD` uses `Option.getD` as its head, which `grind
=` accepts, so it was promoted.

## Current frontier

- `lake build HexBerlekampZassenhaus.Basic` green (105s — no grind-loop
  slowdown; all RHSs strictly reduce term structure).
- `lake build HexBerlekampZassenhausMathlib` (CI-gated bridge) green, 3790
  jobs, zero errors.
- `lake build HexBerlekampZassenhaus` (full target, incl. guards/
  conformance) green, 497 jobs, zero errors.
- `python3 scripts/check_dag.py` exit 0.
- Annotation-only: no proof bodies, signatures, imports, or `*Mathlib`
  files touched. No `axiom`/`sorry`/`native_decide` on added lines.

## Next step

None for this issue. Other `Basic.lean` automation work is out of scope
for the Phase 6 equation sweep.

## Blockers

None.
