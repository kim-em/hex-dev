# review — HexGramSchmidt Phase 6 exit-criteria audit (#8173)

## Accomplished

Audited `HexGramSchmidt` against the four Phase 6 exit criteria gating
the `done_through 5 → 6` bump. **Three of four pass; the bump is NOT
made** — `done_through` stays at 5. Full report posted as a comment on
#8173.

1. **Mathlib linter clean — PASS.** `lake build HexGramSchmidt` green,
   396 jobs, zero warnings. `mathlib: false` library (no Mathlib import),
   no `set_option linter … false`/`nolint` suppressions, so the clean
   build is genuine.
2. **Docstring coverage — PASS.** 510 decls (`Basic.lean` 210,
   `Int.lean` 300); zero public decls undocumented except the rule-exempt
   `main` CLI entry points and a trivial `Hashable` instance.
3. **No dead code — FAIL.** 24 unreferenced private decls (~497 lines,
   verified single-occurrence repo-wide, no `@[simp]`/`@[grind]`) plus 4
   unreferenced public theorems. Includes a cascade in the
   `coeffBM`/bordered-minor multilinearity sub-cluster
   (`Int.lean:9084–9382`). Too large + cascading for review inline fixes;
   filed as bounded feature issue #8182.
4. **Native path — PASS.** No `native_decide`/oracle/`unsafe`/`@[extern]`
   /`IO`; zero `sorry`/`axiom`.

No source changes: the only failing criterion is the dead-code cluster,
which is feature-sized (iterative delete→rescan→rebuild fixpoint) and is
handed to #8182 per the issue's option (b). This PR carries the audit
report (this file + the #8173 comment) only.

## Current frontier

`HexGramSchmidt` remains Phase 6 ready (`done_through: 5`). The sole gate
on the bump is removing the dead-code cluster enumerated in #8182.

## Next step

Execute #8182 (a `feature` session): iteratively remove the 24 dead
private decls + confirm/remove the 4 public ones, rebuild to green, then
bump `done_through 5 → 6` and run `check_dag.py`.

## Blockers

None. The dead-code removal is mechanical but exceeds review-session
scope; #8182 captures it.
