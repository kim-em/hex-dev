# 20260509T204903Z - Issue #2871

## Accomplished

Claimed #2849 first, found its dependency was stale, and skipped it:
#2848 was closed as a decomposed parent while the required current-tail
implementation is still in open PR #2870 / successor #2851, not on
`origin/main`.

Claimed #2871 and added proven `rowToEuclidean` conversion lemmas in
`HexGramSchmidtMathlib/Basic.lean`:

- `rowToEuclidean_apply`
- `rowToEuclidean_zero`
- `rowToEuclidean_add`
- `rowToEuclidean_sub`
- `rowToEuclidean_smul`

Created successor issues:

- #2875 for the finite-sum inner/dot bridge.
- #2876 for the final rational `gramSchmidt` assembly after #2875.

Opened partial PR #2877 for the conversion lemmas and marked #2871 for
replan.

Verification completed:

- `lake build HexGramSchmidtMathlib.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidtMathlib/Basic.lean`

The grep reports the two pre-existing `sorry`s in
`HexGramSchmidtMathlib/Basic.lean`; this session did not add new proof holes
or banned markers.

## Current frontier

#2871 is broader than a single session: after the conversion lemmas, the next
missing layer is proving Mathlib's `PiLp.inner_apply` finite sum agrees with
the project `Hex.Vector.dotProduct` / `Matrix.dot` fold over `List.finRange`.

## Next step

#2875 should land first. Then #2876 can prove `rat_basis_row_eq_gramSchmidt`
using the inner/dot bridge to compare Mathlib's
`InnerProductSpace.gramSchmidt_def''` with
`GramSchmidt.Rat.basis_decomposition`.

## Blockers

None.
