# 4af57fb8 — work #5933

## Accomplished

- Claimed #5933 after skipping stale #5936, which depends on the still-open
  cofactor-row-pairing PR #5937.
- Rebased onto current `origin/main`, including the merged HexLLL parametric
  rerun evidence from PR #5934.
- Ran the full densified Lean/Isabelle fixed comparator sweep and committed
  `reports/bench-results/hex-lll-densified-6fcd1185cee0.json`.
- Updated `reports/hex-lll-performance.md` with the current comparator tables,
  SHA-256, gating verdicts, and retained Concern.
- Filed follow-up #5966 for the harsh-cubic Lean/Isabelle comparator crossover.
- Verified `lake exe hexlll_bench list`, `lake exe hexlll_bench verify`,
  `python3 scripts/status.py HexLLL`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

## Current frontier

HexLLL remains at `done_through: 3`. The current artifact confirms
`random-bounded` still meets the largest-rung gating comparator but trends
upward, while `harsh-cubic` crosses over at `n = 45` and is about `1.58×`
slower than Isabelle at `n = 55`.

## Next step

Work #5966: diagnose the harsh-cubic crossover in `lll.firstShortVector` and
either implement the narrow performance fix or split more specific follow-up
implementation issues.

## Blockers

None for this evidence refresh. Phase 4 promotion is blocked by the retained
HexLLL comparator Concern tracked by #5966.
