# 2026-05-16T17:05Z — feature session 788910ef (#4521)

## Accomplished

- Landed `LiftedFactorListMatches.nodup_of_injOn` in
  `HexBerlekampZassenhausMathlib/Basic.lean` next to
  `LiftedFactorListMatches.length_eq_card` (around L2310).
- The helper takes a matched `localFactors` and
  `Set.InjOn (liftedFactor d) (J : Set _)` and concludes
  `localFactors.Nodup`. Proof is three lines: rewrite via the matching
  predicate to expose the `(finRange).filter (· ∈ J) |>.map (liftedFactor d)`
  form, use `List.Nodup.map_on` with the existing
  `(List.nodup_finRange _).filter _` lemma for the index-side `Nodup`,
  and discharge the per-pair injectivity from `Set.InjOn` after
  `List.mem_filter` + `of_decide_eq_true`.
- Docstring names both consumer call sites
  (`liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches` and
  `liftedFactorSubsetPartition_prefix_none`) and cites Hensel coprimality
  as the source of the injectivity premise.
- Single commit `27d8d2a4`.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓ (built transitively)
- `python3 scripts/check_dag.py` ✓ (silent pass)
- `git diff --check` ✓
- Pre-existing `sorry` count at L160/L239 of
  `HexBerlekampZassenhausMathlib/Basic.lean` unchanged (2).

## Next step

- #4301's recursive coverage proof can now chain
  `LiftedFactorListMatches.nodup_of_injOn` into its calls to
  `liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches` /
  `liftedFactorSubsetPartition_prefix_none`. The remaining open question
  for that consumer is the source of the `Set.InjOn` witness —
  expected to come from `henselLiftData` invariants or directly from the
  partition's `pairwise_disjoint` / `unique_up_to_associated` fields
  combined with monicness, as the consumer's docstring already
  anticipates. That discharge is intentionally outside the scope of this
  issue.

## Blockers

- None.
