# Issue #8521 — review precision cap core-keying

## Accomplished

Reviewed the applied `factorFastPrecisionCap` change for issue #8521 against
the surrounding fast/lattice code and SPEC text. Checked that the new cap is
keyed as `max (bhksBound squareFreeCore) (defaultFactorCoeffBound f)`, that the
new `hprec` lemmas target the lattice call shape, and that no remaining local
references rely on the old `bhksBound f ≤ factorFastPrecisionCap f` theorem.

Attempted to use the local second-opinion wrapper, but it failed with a
connection error before returning substantive output.

## Current frontier

No code changes were made beyond this progress note. The main review note is
that the new BHKS cap lemma proves the cap clears `bhksBound squareFreeCore`,
but the executable fast loop also has a separate `cldCoeffFloor` acceptance
gate based on `(toMonic core).monic`; future #8417 completeness/acceptance
proofs should avoid silently treating the new lemma as a floor-dominance fact.

## Next step

If #8417 needs executable-loop acceptance rather than only cap-precision
irreducibility certification, add or reuse a lemma connecting
`cldCoeffFloor (normalizeForFactor f).squareFreeCore` to
`factorFastPrecisionCap f`, or thread the floor hypothesis explicitly.

## Blockers

None for this review. External Claude second-opinion was unavailable due to
`ConnectionRefused`.
