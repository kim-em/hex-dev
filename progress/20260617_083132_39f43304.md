# Progress - 2026-06-17 (session 39f43304, review #7704)

## Accomplished

Reviewed #7704: soundness audit of `quadraticHenselStep_unique_mod_pow_two_mul`
(`HexHenselMathlib/Correctness.lean:383`), the per-step quadratic uniqueness
lemma that the linear-vs-quadratic lift-agreement obligation (#7706 / #7700)
consumes. **Verdict: PASS.** Review-only, no code change (no docstring fix
needed). Recorded here and as a verdict comment on the issue.

Build baseline: `lake build HexHenselMathlib.Correctness` →
`Built HexHenselMathlib.Correctness (3.1s)`, no `error:`. The only `sorry`
warnings are at lines 81 (`hensel_degree`, #7702 chain) and 468
(`multifactorLift_eq_multifactorLiftQuadratic`, #7706/#7700 target) — both
out of scope. The target lemma (383) and its dependency chain
(`quadraticHenselStep_factor_correct`:126, `quadraticHenselStep_monic`:188,
`hensel_unique`:236) are `sorry`-free. `python3 scripts/check_dag.py` exit 0;
`git diff --check` clean.

## Findings (file:line evidence)

### 1. Modulus bridge is an identity, not a weakening — PASS

- `hmm : p ^ k * p ^ k = p ^ (2 * k)` (`:432`) proved `by rw [two_mul, pow_add]`
  — the genuine arithmetic identity `p^(k+k) = p^k * p^k`.
- Used only as a `ZMod`-modulus rewrite: `hprod_2k` (`:433`) takes
  `hfc := quadraticHenselStep_factor_correct …` whose conclusion is the product
  equality over `ZMod (p^k * p^k)` and closes the `ZMod (p^(2*k))` goal with
  `exact hmm ▸ hfc` (`:443`). `p^k * p^k` occurs in `hfc` **only** inside
  `ZMod (…)`, so the dependent `▸` rewrites the modulus and nothing else
  (product/degree structure untouched). Typechecking confirmed by the clean
  build.
- The doubling is real at the substrate: `quadraticHenselStep_factor_spec`
  (`HexHensel/Quadratic.lean:2248`) concludes `ZPoly.congr (r.g*r.h) f (m*m)`
  from input congruence at `m`. So the conclusion is genuine uniqueness mod
  `p^(2*k)` of the real quadratic-step output `toPolynomial r.g` / `r.h`, not a
  coarser modulus.

### 2. No vacuous or smuggled hypothesis — PASS

Every argument to `hensel_unique` (`:444`–`:448`) is discharged from real
evidence:
- `_hk : 0 < 2*k` via `by omega` from `hk : 0 < k` (genuine; `hensel_unique`
  clears it anyway).
- `hg_monic` via `quadraticHenselStep_monic (p^k) … hpk_gt1 hmonic` (`:428`),
  where `hpk_gt1 : 1 < p^k = Nat.one_lt_pow hk.ne' hprime.one_lt` (`:427`) —
  genuinely derived from `Nat.Prime p` + `0 < k`. **Not** assertable at `k = 0`:
  it needs `hk.ne'`. (`quadraticHenselStep_monic` requires `1 < m`; met.)
- `hm_pos : 0 < p^k = pow_pos hprime.pos k` (`:426`) feeds
  `quadraticHenselStep_factor_correct` (which needs only `0 < m`).
- `hprod_2k` from that factor-correctness lemma + `hprod`/`hbez`/`hmonic`.
- `hg'`, `hdeg`, `hprod'`, `hg1`, `hh1`, `hcop` are the lemma's own
  consumer-supplied hypotheses; no over-constrained binder makes any vacuous.

So `1 < p^k` is genuinely derived, not assumed, and not valid for `k = 0`.

### 3. Consumer-shape match — PASS (with note)

The lemma quantifies `g' h' : Polynomial ℤ` freely and concludes uniqueness of
the genuine `quadraticHenselStep (p^k)` output mod `p^(2*k)`. The consumer
`multifactorLift_eq_multifactorLiftQuadratic` (`:468`, still `sorry`) targets
mod `p^k`. The `2*k` exponent is **correct and intentional** (one quadratic
Newton step doubles `p^k → p^(2*k)`), not an off-by-one: the doubled-precision
conclusion is *stronger* than the consumer's mod-`p^k` need and reduces to it
via the standard `ZMod (p^(2k)) → ZMod (p^k)` cast (the same
`polynomial_map_zmod_pow_succ_to_pow` machinery `hensel_unique` uses
internally). It is a per-step building block; the consumer wraps it in a
multi-step generalized-target induction (#7706 territory, out of scope here).
The shape is therefore applicable, not inapplicable.

### Docstrings

No inaccuracies. The `quadraticHenselStep_monic` `1 < m` minimality note
(`:183`–`:187`), the `hensel_unique` `k = 0` / `hh1`-redundancy note
(`:230`–`:235`), and the doubled-precision docstring (`:379`–`:382`) are all
accurate.

## Current frontier

Per-step quadratic uniqueness is audited sound. #7706
(`multifactorLift_eq_multifactorLiftQuadratic` with the added per-head monicity
hypothesis) can build on it confidently.

## Next step

#7706: discharge the lift-agreement `sorry` at `Correctness.lean:468`,
consuming this lemma + `hensel_unique` under the generalized-target induction
over congruent-but-unequal targets.

## Blockers

None.
