# HO-1 Gap 1 exhaustive monicised-core lift routing

Session: `a1ba0eb2` (work, issue #5634)

## Accomplished

Claimed #5634 and routed the exhaustive slow Hensel-lift boundary in
`HexBerlekampZassenhaus/Basic.lean` through a new `monicisedCoreLiftData`
helper.  The helper uses `MonicisedCoreData.ofCore core` as the substrate for
`henselLiftData`, while `exhaustiveCoreFactorsWithBound` still recombines
against the original primitive core and preserves the existing public
`factorSlowWithBound` / `factorWithBound` / `factor` API shape.

Updated the local exhaustive-wrapper proof scripts to reason through the new
helper.  Added executable guards for the already-monic path and for a primitive
non-monic quadratic with no integer-root shortcut, including the monicised core
shape and final sign-normalisation behavior.

## Current frontier

The executable slow path now uses the monicised core before Hensel lifting.
The remaining HO-1 Gap 1 work is Mathlib-side bridge cleanup: relating
monicised-core lifts and scaled recombination facts to consumers that still
state assumptions directly on the original square-free core.

## Next step

Update the Mathlib bridge theorem layer that consumes
`exhaustiveCoreFactorsWithBound`, especially the slow exhaustive branch entry
lemmas that still carry original-core monicness assumptions.

## Blockers

None for this slice.

Verification completed:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `git diff -- HexBerlekampZassenhaus/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches

Unrelated worktree state:

- `.claude/CLAUDE.md` was already modified in the worktree and was left
  untouched.
