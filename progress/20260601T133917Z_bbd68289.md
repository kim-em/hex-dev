# bbd68289 - feature #5987

## Accomplished

- Claimed #5987 after blocked directive claims failed and #5929 was already claimed.
- Narrowed the `ZMod64.inv` automation surface by removing default `simp`/`grind`
  attributes from `ZMod64.toNat_inv_def` while keeping the named definition lemma.
- Added targeted `grind`-usable inverse laws in `HexModArith/Basic.lean` and
  `HexModArith/Prime.lean`, including an operator-level coprime inverse theorem.
- Verified with `lake build HexModArith`, full `lake build`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token diff scan.

## Current frontier

HexModArith's inverse API now favors algebraic inverse-use theorems for default
automation and keeps the extended-GCD representative equation explicit by name.

## Next step

Pick up the remaining HexModArith Phase 6 API issues, especially the neutral-law
simp coverage in #5988 if it remains unclaimed.

## Blockers

- The issue-referenced report `reports/hex-mod-arith-zmod64-api-phase6-review.md`
  is not present in this checkout; the issue body contained enough detail to execute.
- A pre-existing `.claude/CLAUDE.md` worktree modification was left untouched.
- `/reflect` is not available in this execution context.
