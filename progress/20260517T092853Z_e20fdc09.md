# 20260517T092853Z — feature session e20fdc09 (#4605)

## Accomplished

Closed #4605: HO-1 substrate — discharge small-mod singleton arm
`hchoose` premise for capstone wiring.

### Dispatcher restriction (Option A, surgical variant)

Modified `Hex.factorFastFactorsWithBound`
(`HexBerlekampZassenhaus/Basic.lean:5090`) to gate the small-mod
singleton branch on `(choosePrimeData? sf).isSome ∧ size ≤ 1` rather
than `size ≤ 1` alone. In the fallback case (`choosePrimeData? sf =
none ∧ size ≤ 1`) the singleton check fails, so the dispatcher routes
to the BHKS core path with the fallback prime; if that also returns
`none`, `factorWithBound` falls through to the slow exhaustive path.
The BHKS core path itself is unchanged (the BHKS arm's `hchoose`
analogue is gated on directive #2567 and out of scope here).

`#guard factorFastFactorsWithBound cldGuardF 1 = none` and
`#guard factorFastFactorsWithBound cldGuardF 4 = some bhksGuardFactors`
both still hold (verified during `lake build HexBerlekampZassenhaus.Basic`).

### Cascade updates

* `factorFastFactorsWithBound_eq_some_of_core_success` (line 5140):
  replaced the `hmulti : 1 < size` premise with the more general
  `hnotsingleton : ¬((isSome) ∧ size ≤ 1)`, since the dispatcher's
  BHKS path now fires under either `¬isSome` or `¬(size ≤ 1)`.

* `factorWithBound_entry_mem_small_mod_singleton_raw` (line 5612):
  added an `hchoose : choosePrimeData? sf = some (choosePrimeData sf)`
  premise. The proof builds the conjunctive `hpred` from
  `⟨hchoose-isSome-witness, hsmall⟩` and dispatches via
  `rw [if_pos hpred]`.

* `factorWithBound_entry_mem_fast_core_success_raw` (line 5731):
  derives `hnotsingleton` locally from `hmulti` to feed the updated
  `_eq_some_of_core_success`.

* `factor_ne_none_of_core_recovery_on_schedule` (line 5267):
  case-splits on `hpred` (the new dispatcher predicate) instead of
  `hsmall`.

* `factorFastFactorsWithBound_polyProduct_of_some` (line 8197) and
  `factorFastWithBound_product_of_small_mod_branch` (line 8788) and
  `factorFastWithBound_product_of_core_success_branch` (line 8869):
  same pattern — case-split / premise threading on the new dispatcher
  predicate.

* `factorFastWithBound_product_of_some` (line 8943): case-splits on
  the new `hpred` conjunction. The `singleton` case derives `hchoose`
  inline (via the `choosePrimeData? = some pd ⇒ choosePrimeData = pd`
  derivation that unfolds `choosePrimeData`) and dispatches to
  `_of_small_mod_branch`; the `¬hpred` case dispatches on `hcore` and
  hands `hpred` (the negation) to `_of_core_success_branch`.

### Classifier update (#4599 integration)

The in-flight fast-path dispatch classifier
`factorFastFactorsWithBound_branch` (line 5443, landed via #4599 = commit
`a4eb8a8e` on `main`) was updated to mirror the new dispatcher contract:

* Singleton cases (c) `B = 1` and (g) `B > 1`: premises strengthened
  from `size ≤ 1` to `(isSome) ∧ size ≤ 1`.

* BHKS cases (d), (e), (h), (i): premises generalised from
  `1 < size` to the dispatcher-faithful `¬((isSome) ∧ size ≤ 1)`. This
  covers both the good-prime `1 < size` case and the new
  fallback-prime `¬isSome` case that the dispatcher now also routes
  here. Downstream BHKS-arm consumers can recover `1 < size` from
  `¬((isSome) ∧ size ≤ 1) ∧ isSome` if their umbrella needs it.

Total cases unchanged at 9.

### Umbrella update + new no-`hchoose` discharger

* `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  (`IntReductionMod.lean:1053`, the #4564 umbrella): passes the
  existing `hchoose` through to the updated branch-shape lemma. No
  signature change.

* **New**: `factor_small_mod_singleton_branch_entry_irreducible`
  (`IntReductionMod.lean:1110`). Capstone-facing variant with no
  separate `hchoose` premise. Packs the `isSome` witness into a
  strengthened branch predicate `hsmall_chosen : (isSome) ∧ size ≤ 1`,
  which is the **exact predicate** the post-#4605 dispatcher tests
  (matching the classifier's cases (c) and (g)). Derives `hchoose`
  internally from `hsmall_chosen.1` (via the `cases hc : choosePrimeData?`
  + `unfold choosePrimeData; rw [hc]` pattern), then delegates to the
  existing `_of_choosePrimeData` umbrella.

This is the deliverable's "discharger of shape ... with no `hchoose`
premise" — the dispatcher restriction makes `hsmall_chosen` (the
exact dispatch predicate, provided by the classifier's (c)/(g) cases)
sufficient to discharge irreducibility without a separate `hchoose`
witness from the caller.

### Inventory deltas

* `HexBerlekampZassenhaus/Basic.lean`: 1 `sorry` (line 6290,
  `isIrreducible_iff` placeholder — pre-existing). No new
  `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`.
* `HexBerlekampZassenhausMathlib/IntReductionMod.lean`: 0 `sorry` /
  `axiom` / `native_decide` / `TODO` / `FIXME`. Unchanged from
  `origin/main`.
* `lake build HexBerlekampZassenhaus HexBerlekampZassenhausMathlib`
  succeeds (3469/3469).
* `python3 scripts/check_dag.py`: clean.
* `git diff --check`: clean.

## Current frontier

The small-mod singleton arm's HO-1 substrate is now complete:
* Branch-shape lemma + classifier reflect the dispatcher's exact
  singleton-firing condition (post-#4605).
* `hchoose`-free umbrella `factor_small_mod_singleton_branch_entry_irreducible`
  is composable with #4597's `hcomplete` discharger for the capstone.
* The umbrella's premise `hsmall_chosen` is exactly the predicate
  classifier cases (c) and (g) provide, so the capstone can extract
  it directly from a dispatch witness without proving `hchoose`
  separately.

Other open HO-1 substrate work is unchanged: #4602 (radical relation
between `squareFreeCore` and `repeatedPart`), #4334 (HexLLL Phase 4
benchmark verdicts on quiet hardware), and the BHKS arm gated on
directive #2567.

## Next step

The capstone wiring (#4170) for the small-mod singleton arm is
unblocked on the `hchoose` side. It now needs the `hcomplete`
discharger from #4597 (in flight, blocked on #4602/#4603 which are
also in flight) to fully discharge per-branch hypotheses.

For the BHKS path under the new fallback-included BHKS classifier
cases (`¬((isSome) ∧ size ≤ 1)`), the BHKS arm umbrella (#2567) will
need to handle the `¬isSome` sub-case where the fallback prime is
used. That's a separate concern gated on the BHKS directive.

## Blockers

None.
