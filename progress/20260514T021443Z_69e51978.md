# Progress - feature #3907

## Accomplished

Refactored the LLL independence surface onto Mathlib-free Gram-Schmidt data:

- Changed `GramSchmidt.Int.independent` to mean positivity of executable
  `gramDet` prefixes.
- Added `GramSchmidt.Int.independent_of_det_positive` and
  `GramSchmidt.Int.independent_one` as bridge helpers for existing special
  determinant-positive matrix families.
- Made `Matrix.independent` in `HexLLL/Basic.lean` alias
  `GramSchmidt.Int.independent`.
- Removed the private LLL bridge lemma
  `det_submatrix_gram_eq_gramDet_int`.
- Reproved `LLLState.sizeReduce_independent` directly from the stored
  `d_eq` certificates and `sizeReduce_d`.
- Updated the LLL conformance independence checker to compute via
  `GramSchmidt.Int.gramDet`.

Verification:

- `lake build HexLLL.Basic`
- `lake build HexLLL`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "det_submatrix_gram_eq_gramDet_int|leadingGramMatrixInt_det_eq_gramDet_int" HexLLL/Basic.lean`
- `rg -n "Matrix\.det" HexLLL/Basic.lean HexLLL/Conformance.lean`
- `rg -n "^axiom\b|native_decide|TODO|FIXME" HexGramSchmidt/Int.lean HexLLL/Basic.lean HexLLL/Conformance.lean`
- `git diff -- HexGramSchmidt/Int.lean HexLLL/Basic.lean HexLLL/Conformance.lean | rg -n "\+.*\bsorry\b|\+.*axiom\b|\+.*native_decide"`

## Current frontier

`HexLLL` no longer depends on the determinant-to-`gramDet` bridge lemma for
its public independence predicate or size-reduction preservation proof.

## Next step

Proceed with the parent HO-43 relocation work that moves determinant bridge
facts out of Mathlib-free modules once the remaining dependent issues are
ready.

## Blockers

None for this issue.
