# Phase 6 grind coverage: HexGF2/Clmul, HexModArith/Prime, HexBerlekamp/RabinSoundness (#8086)

## Accomplished

Promoted the six remaining clean `lhs = rhs` `@[simp]` lemmas in the
executable layer from `@[simp]` to `@[simp, grind =]`:

- `HexGF2/Clmul.lean`: `pureClmul_zero_left`, `pureClmul_zero_right`,
  `clmul_zero_left`, `clmul_zero_right` (each `... = (0, 0)`).
- `HexModArith/Prime.lean`: `pow_prime_of_prime_modulus` (`a ^ p = a`).
- `HexBerlekamp/RabinSoundness.lean`:
  `primeFieldLinearProduct_factor_count` (`(ZMod64.values p).length = p`).

Each re-read at the promotion site and confirmed a literal equation.
Annotation-only — no proof bodies, signatures, or imports changed; no
`*Mathlib` file touched.

## Current frontier

`lake build HexGF2` (52 jobs) green: `HexGF2.Clmul` 142s, no grind-loop
slowdown (its cost is the pre-existing oneHotWord bit proofs, unchanged).
`lake build HexModArith HexBerlekamp` (479 jobs) green: `HexModArith.Prime`
2.5s, `HexBerlekamp.RabinSoundness` 2.5s. `scripts/check_dag.py` exit 0.
`git diff --check` clean; no `sorry`/`axiom`/`native_decide` added.

## Next step

None for this issue. Per #8086, `HexBerlekampZassenhaus/Basic.lean`
(18 uncovered `@[simp]` lemmas) is left out of scope — under directive
#2564 rewrite.

## Blockers

None.
