# 20260614T062200Z — feature session efc1a08d (#7044)

## Accomplished

Claimed #7044 ("migrate the fast BHKS path lift to the monic transform")
and ran its premise check (the issue's own caveat asks to stop and report
a missing prerequisite rather than introduce a `sorry`). Found a premise
gap and reported it on the issue, then `coordination skip`ped #7044 to
`replan`.

## Finding

`lake build HexBerlekampZassenhausMathlib` fails first on the **slow
path**, not the fast-path chain #7044 targets:

- Error at `HexBerlekampZassenhausMathlib/Basic.lean:16623`
  (`have hsearch_step`), applying
  `Hex.scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`.
  The supplied `hprefix`/`hcandidate_def` carry the scale-inside shape
  `centeredLiftPoly (scale (leadingCoeff core) product) modulus`, but the
  executable lemma now requires the dilate-outside shape
  `dilate coreLc (centeredLiftPoly product modulus)`.
- Executable slow path went dilate-outside in #6802 (2026-06-13);
  Mathlib model `scaledRecombinationCandidate`
  (`Mathlib/Basic.lean:2387`, via `scaledLiftedFactorProduct`) is still
  scale-inside (from #4748, 2026-05-18) and was never migrated.
- #7060 only touched the fast path in `Mathlib/Basic.lean` (7 lines), so
  this slow-path break has been red on `main` since #6802. The layer is
  not CI-built (see `hex-lean-mathlib-boundary`), so it went unnoticed.
- The issue's "slow path already proves the analogous monic-coordinate
  soundness shape" claim is contradicted:
  `exhaustiveCoreFactorsWithBound` soundness lives in
  `IntReductionMod.lean`, which imports the broken `Basic.lean`.

Scope of the missing slow-path migration: 240 `scaledRecombinationCandidate`
+ 118 `scaledLiftedFactorProduct` references in `Mathlib/Basic.lean`, with
an `hscaled` hypothesis threaded through the whole recovery chain. Build
iteration on this 18k-line non-CI Mathlib file is tens of minutes/cycle.

The executable layer is green (`lake build HexBerlekampZassenhaus`,
including `.Conformance`/`.CrossCheck`); only the Mathlib soundness model
is stale.

## Current frontier

#7044 is back in the queue as `replan`. A planner should add a
slow-path-model-migration prerequisite (scale-inside → dilate-outside for
`scaledRecombinationCandidate`/`scaledLiftedFactorProduct`, re-ground the
`scaledRecombinationSearchModAux` soundness chain) and make #7044
`depends-on` it.

## Next step

Planner: split out the slow-path model migration as a prerequisite issue,
then re-verify #7044's fast-path residual on top of a green `Basic.lean`.

## Blockers

The fast-path residual (#7044's stated deliverable 3) is downstream of
`Basic.lean` and its errors are masked by the slow-path failure; it cannot
be verified green until the slow-path model is migrated first.
