# 2026-05-15 23:24 UTC - review #4342: HexBZ lifted-factor partition PR #4331

Claimed and executed review issue #4342, auditing PR #4331 (lifted-factor
representing-subset partition predicate) against issues #4325 and #4301.

## Accomplished

Reviewed PR #4331 along the two focus areas the issue body called out:

1. **API soundness vs. consumer needs.** Compared the delivered
   `LiftedFactorSubsetPartition` structure against the spec in #4325 and
   the consumer signature in #4301. The PR delivers strict generalisations
   beyond the issue spec (`cover` over arbitrary `i ∈ J` instead of just
   `J.min'`; new `target_squarefree` and `unique_up_to_associated` fields)
   that are required by the transport proof itself. `cover_at_min` is
   exposed as a one-line corollary.

2. **Transport theorem audit.** Walked the
   `liftedFactorSubsetPartition_transport` proof field-by-field
   (lines 1831–1976) for circularity and hidden assumptions:

   - The square-free propagation `Squarefree quotient ←
     quotient ∣ target` uses `Squarefree.squarefree_of_dvd`.
   - The "no irreducible divisor of `quotient` is associated to `emitted`"
     helper relies on `target_squarefree` to rule out `emitted^2 ∣ target`.
   - The cover transport's `by_cases` on `Associated f emitted` is exhaustive:
     associated case contradicts `i ∉ S` via `unique_up_to_associated`;
     non-associated case uses `Irreducible.prime` + `Prime.dvd_or_dvd` in
     the UFD `Polynomial ℤ` to lift `f ∣ target` to `f ∣ quotient`, then
     `pairwise_disjoint` to bound the rep subset.
   - The `hf_dvd_quot` casting back through the `HexPolyZMathlib.equiv`
     isomorphism is correct.
   - Argument-position bookkeeping in `hdisj` (struct's `S` vs. local `T`)
     is correct.

   No circular reasoning detected.

Verified locally:

- `lake build HexBerlekampZassenhausMathlib.Basic` — clean.
- `python3 scripts/check_dag.py` — clean.
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry`
  added by the PR diff.

Posted the review as a comment on PR #4331:
https://github.com/kim-em/hex/pull/4331#issuecomment-4464485724

PR was merged at `2026-05-15T23:20:23Z`, ~5 minutes after this session
claimed #4342, so the review is post-merge rather than pre-merge.

## Residual risks flagged for #4301

1. **Initial-state constructor deferred.** #4301's signature in its
   current issue body still names `HenselSubsetCorrespondenceRest`; the
   #4301 worker will need to switch to `LiftedFactorSubsetPartition` and
   either inline the initial-state construction or file the deferred
   sub-issue. A planner pass on #4301 to update its signature would tidy
   the handoff.
2. **No example construction.** The four new fields are exercised only
   inside `liftedFactorSubsetPartition_transport` itself. A subtle
   signature mismatch wouldn't surface until #4301 attempts the proof.
3. **`Squarefree` is on `toPolynomial target`.** The initial-state
   constructor will need either a square-free-in-`Polynomial ℤ`
   hypothesis on `core`, or a bridge from a Hex.ZPoly-side
   square-freeness predicate.

## Current frontier

Review delivered, no blocking issues found. Issue #4342 can be closed
manually (no PR; the deliverable was the review comment posted on #4331).

## Next step

Close #4342 with a link to the posted review. The next worker can claim
#4301; the planner should consider updating #4301's signature to use
`LiftedFactorSubsetPartition` and to scope the deferred initial-state
constructor.

## Blockers

None.
