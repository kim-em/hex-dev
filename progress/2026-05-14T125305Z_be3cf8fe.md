# Progress: agent/be3cf8fe — UFD partition cardinality bound (sub-issue from #4007)

## Accomplished

- Attempted #4007 (HexBZ Mathlib bridge: fast-core success branch
  irreducibility); after orienting on the algorithm + SPEC Group B
  obligations, judged it too large for one session and decomposed.
- Created sub-issues #4021 (abstract UFD partition cardinality bound)
  and #4022 (algorithm-side cardinality certification for the fast-core
  success branch). Left `Decomposed into #4021, #4022` breadcrumb on
  #4007 and skipped the parent for planner triage.
- Claimed #4021 and implemented it as
  `HexBerlekampZassenhausMathlib/UFDPartition.lean`:
  `irreducible_of_partition_card_eq_normalizedFactors_card`. Pure
  Mathlib UFD argument: any non-zero `f` admitting a list of non-unit
  divisors whose product is associated to `f` and whose length equals
  `(normalizedFactors f).card` must consist entirely of irreducibles.
- Wired the new module into the `HexBerlekampZassenhausMathlib`
  aggregator.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib` is green (no new
  `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`). The four pre-existing
  bridge-level `sorry`s in `HexBerlekampZassenhausMathlib/Basic.lean`
  are unchanged.
- DAG check (`scripts/check_dag.py`) passes.

## Next step

- A follow-up session on #4022 supplies the cardinality hypothesis from
  the BHKS algorithm success state (combining B6 with the algorithm's
  verified product / divisibility checks).
- Then a follow-up on the (re-narrowed) #4007 composes the UFD lemma
  with #4022's cardinality certification to discharge the fast-core
  branch irreducibility.

## Blockers

None.
