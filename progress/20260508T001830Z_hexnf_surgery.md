# 2026-05-08 00:18 UTC — interactive session (hex-nf surgery for Factorization API)

## Accomplished

Performed surgery on the `hex-nf` branch to disentangle it from
post-#2563 main and align with the upcoming `Factorization` record
API (a separate SPEC PR will introduce the record itself).

### Reverted (to match `origin/main`)

- `SPEC/Libraries/hex-berlekamp-zassenhaus.md` — drops the stale
  pre-#2563 prose hex-nf had introduced (the "factor returns Array
  ZPoly with content factors / X-power duplicates / repeated factors"
  output convention paragraph; the `nonUnitFactorCount` definition;
  the `Hex.ZPoly.Irreducible` class section). Keeping these would
  effectively revert PR #2563's two-tier architecture refactor.
- `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` — drops the
  `Hex.ZPoly.Irreducible_iff_polynomialIrreducible` bridge theorem
  hex-nf had introduced.
- `SPEC/Libraries/hex-poly-z.md` — drops the `Hex.ZPoly.IsUnit`
  definition hex-nf had introduced.

These all move to a separate "SPEC PR introducing the `Factorization`
record" landing on top of post-#2563 main. See
`/Users/kim/.claude/plans/okay-we-re-not-touching-robust-charm.md`
for the full plan.

### Updated for the upcoming `Factorization` API

- `SPEC/Libraries/hex-number-field.md` — `commonField`'s
  implementation sketch now references
  `(HexBerlekampZassenhaus.factor r₀).factors` (the `Array (ZPoly ×
  Nat)` projection of the `Factorization` record) instead of
  treating `factor`'s output as an `Array ZPoly` directly. Also
  notes that multiplicity > 1 indicates a degenerate shift `c`
  (signal to retry with a different `c`).
- `SPEC/Libraries/hex-number-field-mathlib.md` — replaced the
  speculative "Mathlib gap analysis" section with confirmed
  citations (`AdjoinRoot.lift`, `minpoly.equivAdjoin`,
  `Field.exists_primitive_element`, etc.) verified against the
  Mathlib commit currently checked out. The "must build" list is
  reduced from a speculative estimate to ~250 lines around three
  specific theorems.

## Current frontier

The hex-nf branch is now a focused HexResultant + HexNumberField +
HexRoots-extensions PR. The BZ-related work moves to a separate
SPEC PR on `bz-factorization-record-spec` (forthcoming) which
introduces the `Factorization` record on top of post-#2563 main.

`hex-number-field.md` references types that don't yet exist on
main: `HexResultant.subresultantChain`, `Hex.ZPoly.Irreducible`
(class form), `Factorization`. These are forward references — the
hex-nf PR description should note dependencies on:

- The forthcoming `Factorization` SPEC PR.
- The HexResultant SPEC additions (already in this hex-nf PR).
- The HexBerlekampZassenhaus SPEC PR for `Hex.ZPoly.Irreducible`
  class (forthcoming).

## Next step

1. Force-push amended `hex-nf` to `origin/hex-nf` with
   `--force-with-lease`.
2. Update the hex-nf PR description to reflect narrower scope.
3. Open SPEC PR for `Factorization` record on a new branch off main.
4. File the human-oversight issue describing the implementation
   work needed to migrate `HexBerlekampZassenhaus.factor` to return
   `Factorization` and discharge the associated sorry'd lemmas.

## Blockers

None for this surgery. The downstream BHKS implementation chain
(#2595, #2604, #2609, #2611, #2614, #2615, #2620, #2621, HO-2)
targets the fast-path CLD lattice and is orthogonal to the
slow-path normalisation chain that the `Factorization` migration
touches; conflicts unlikely.
