# Shared PNT+ positive-exponential leaves

## Accomplished

- Replayed the complete four-site FKS2 mu edge as a direct child of exact
  supported-driver candidate `e9c19ecaac7211965274aa1d71b608b5e869ad47`,
  preserving its retained-recipe files and hardening.
- Re-audited the four remaining numerical tactic sites and selected the
  largest coherent subset: `FKS2.Floor.exp10_lt` and the two integer-exp
  premises in `Goldbach.kadiri_lumley_odd_goldbach_finite`.
- Added one three-row package-owned provider using a fixed `x/128` reduction,
  the existing degree-11 Taylor/remainder theorem, seven squarings, and exact
  strict or weak final comparisons.
- Added exact source-shaped ordinary theorems, coordinate and incompatible-
  endpoint mutations with no retry, and guarded axiom reports.
- Extended the offline inventory validator to byte-pin every invocation and
  correlate its direction, numerals, normalized target, and literal provider
  row, with wrong/missing/duplicate tests.
- Reclassified only those three tactic records and documented the localized
  rewrite without claiming LeanCert API compatibility or surrounding FKS2-
  floor coverage.
- Verified the focused provider/conformance build and the complete
  `HexConformance` build (9,595 jobs), the 31-test inventory suite, pinned-
  source correlation, inventory digest, DAG/static checks, guarded trust
  scan, and exact factor-sweep freshness.

## Current frontier

The four-site mu edge and shared three-site positive-exp edge form a clean,
fully checked local stack. Among these file-local numerical leaves, only the
independent `FKS2.theorem_6_2` nested-log premise remains pending.

## Next step

Preserve this local stack until publication ordering is decided. Treat the
final nested-log premise as a separate follow-up edge.

## Blockers

None.
