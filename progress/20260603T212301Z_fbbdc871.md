# 20260603T212301Z work #6491

## Accomplished

Claimed directive #6491 and read the post-#6490 Mathlib-bridge SPEC clause.
Made the quotient-provider surface in `HexGramSchmidt/Int.lean` constructible
from the bridge layer by removing `private` from:

* `BareissGramRowInvariant`
* `BareissGramRegularStepQuotient`
* `BareissGramInitialRegularStepQuotient`
* `BareissGramInitialRegularStepQuotientProvider`

Added docstrings on the initial quotient and provider surfaces noting that
concrete witnesses/providers are constructed in `HexGramSchmidtMathlib`, where
the Bareiss-Desnanot infrastructure is permitted by SPEC.

## Current frontier

This is a partial PR. The public provider API gap is closed, but the residual
`getArrayEntry_scaledCoeffRowsSchur_eq` proof remains in `HexGramSchmidt/Int.lean`.
The full migration still needs a Mathlib-side construction of
`BareissGramInitialRegularStepQuotientProvider b` and a bridge-layer
Schur-vs-Bareiss characterization that can replace the current base-file
capstone theorem.

The reason this was not completed in one shot is structural: the current
base-file public diagonal APIs (`gramDetVec_eq_gramDet`,
`scaledCoeffs_diag_toNat`, and related signed diagonal facts) depend on the
private Schur/Bareiss equivalence theorem, while the bridge file imports the
base file rather than the other way around. Completing #6491 therefore requires
moving or re-layering those consumers, not just adding a theorem wrapper.

## Next step

In `HexGramSchmidtMathlib/Int.lean`, construct the provider using the existing
Bareiss-Desnanot/no-pivot machinery and then move the Schur/Bareiss
characterization plus dependent diagonal/lower public facts to the bridge layer
as needed. After that, remove the residual `sorry` in
`getArrayEntry_scaledCoeffRowsSchur_eq` or delete/move that theorem if the
dependency direction forces option 2 from the issue.

## Blockers

`/reflect` was requested but is not available in this environment:
`coordination reflect` reports an unknown command, and `/reflect` is not an
executable path.

Verification run:

* `lake build HexGramSchmidt` passed.
* `grep -c sorry HexGramSchmidt/Int.lean` remains `3`; this partial PR has not
  discharged the residual proof.
* Added no `axiom` or `native_decide` occurrences in the touched Lean file.
