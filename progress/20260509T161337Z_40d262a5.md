# 20260509T161337Z - Issue #2810

## Accomplished

Claimed human-oversight issue #2810 and ran the HexLLL fpLLL informational
comparator registrations on this host using a repo-local `.venv-oracles`
environment with `fpylll 0.6.4`, `cysignals`, and `python-flint 0.8.0`.

Added the successful fpLLL export artefact
`reports/bench-results/hex-lll-fpylll-ed9da7537e96.json` and created
`reports/hex-lll-performance.md` with the fixed comparator medians,
Lean/fpLLL pair ratios, expected-hash results, command lines, and artefact
checksum.

Verification completed:

- `PATH="$PWD/.venv-oracles/bin:$PATH" python3 scripts/oracle/lll_fpylll.py --bench-checksum`
- `PATH="$PWD/.venv-oracles/bin:$PATH" lake exe hexlll_bench run ... --export-file reports/bench-results/hex-lll-fpylll-ed9da7537e96.json`
- `PATH="$PWD/.venv-oracles/bin:$PATH" lake exe hexlll_bench compare ...` for the BZ, random-bounded, and harsh-cubic fixed pairs
- `lake build HexLLL`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `jq empty reports/bench-results/hex-lll-fpylll-ed9da7537e96.json`

## Current frontier

The fpLLL informational comparator snapshot is now recorded for HexLLL. The
run remains classified informational, not gating, per `SPEC/Libraries/hex-lll.md`.

The worktree still has the pre-existing unstaged `.claude/CLAUDE.md`
modification, which was not touched.

## Next step

Open a PR for #2810 with the report, JSON artefact, and this progress file.

## Blockers

No blockers.
