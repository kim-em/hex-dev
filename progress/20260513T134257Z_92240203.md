# #3512: canonical HexBZ scheduled recovery bridge

Session: `92240203-7c37-408d-99a9-5f84e1b9e1ea`
Branch: `agent/92240203`

## Accomplished

- Added `Hex.cap_mem_henselPrecisionSchedule` in
  `HexBerlekampZassenhaus/Basic.lean`: the fast-path cap `B` is itself a
  member of `henselPrecisionSchedule B (initialHenselPrecision B)
  (ZPoly.quadraticDoublingSteps B + 2)`.  Proof goes through a small
  private helper `henselPrecisionSchedule_mem_cap` that handles the
  general geometric fuel bound `B ≤ k * 2 ^ fuel`, then specializes to
  the canonical executable start/fuel using `Nat.lt_log2_self` from
  core Lean (Mathlib-free, no new tactics).
- Added `factorFast_ne_none_of_forwardInputs_at_cap` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`: a one-liner
  specialization of `factorFast_ne_none_of_forwardInputs_on_schedule`
  that drops the schedule-membership obligation by discharging it with
  the new `cap_mem_henselPrecisionSchedule`.  Later HO-4 work needs to
  provide only the `ForwardRecoveryInputs` bundle at the canonical
  terminal precision.
- Both deliverables are purely connective: no executable factorization
  behavior changed, no BHKS Lemma 3.3 / A2 reconstruction discharged,
  no `factorFast_terminates` attempted.

## Current frontier

`lake build HexBerlekampZassenhaus.Basic`,
`lake build HexBerlekampZassenhausMathlib.Recovery`, and
`lake build HexBerlekampZassenhausMathlib` all complete cleanly.
The two `sorry` warnings remaining in `HexBerlekampZassenhaus/Basic.lean`
(`isIrreducible_iff`, `factor_product_of_bound`) are pre-existing and
unrelated.

## Next step

Push branch, open PR closing `#3512`.  Follow-up HO-4 work in B7 / A2
discharge (`#3530`, `#3529`) and final composition (`#3526`, `#3534`)
remain blocked until prerequisite Mathlib bridges land.

## Blockers

None.
