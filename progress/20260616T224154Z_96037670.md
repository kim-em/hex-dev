# Progress - 2026-06-16 (session 96037670, one-shot #7640)

## Accomplished

Closed #7640: projected the BHKS prefix survivor span into
`CutProjectionHypotheses` for true-factor supports, and retired the
`CutRetention` route. All in `HexBerlekampZassenhausMathlib/`.

### Projection bridge (`Lattice.lean`)

- `projFirst r n : (Fin (r+n) → ℤ) →ₗ[ℤ] (Fin r → ℤ)` — restriction to the
  first `r` coordinates as a ℤ-linear map.
- `projFirst_vectorEquiv_row_mem`: a retained prefix row, projected to its
  first block, is a generator of `projectedRowSpanInt`. Proven via a
  conditional-push fold-membership lemma (`mem_foldl_push_if` +
  `mem_foldl_push_if_of_mem_init`) that locates each retained projected row in
  the `bhksCutProjectReducedRows` array, plus `bhksProjectIndicator_getD`
  reading off the first-block coordinate.
- `projFirst_mem_projectedRowSpanInt_of_mem_prefixSubmodule`: by
  `Submodule.span_induction`, projecting any vector of
  `prefixSubmodule reduced (bhksCutPrefixCount …)` lands in
  `projectedRowSpanInt`.

### True-factor producer (`Lattice.lean`)

- `cutProjectionHypotheses_of_trueFactors`: consumes the survivor-span lemma
  `mem_prefixSubmodule_of_normSq_le` (#7639), the tight norm certificate
  `TrueFactorCLDTightNormBound` (#7641/#7643), the lattice bridge
  `traceReducedMatrix_memLattice_iff`, `TrueFactorCLDVectorData.project_eq`,
  and `trueFactorCLDVector_memLattice` to produce `CutProjectionHypotheses`
  for true-factor supports — **without** `CutRetention`. Independence of the
  reduced basis is obtained from `L.basis.independent` via
  `lllSteered_independent`; the ℝ tight bound is converted to the ℚ survivor
  hypothesis through `normSq_eq_sum`.

### CutRetention retired

- Removed `CutRetention`, `CutProjectionHypotheses.ofRetention`, and
  `cutProjectionHypotheses_of_retention` from `Lattice.lean` (no remaining
  references).
- Replaced the dead `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_retention`
  wrapper in `PartitionRefinement.lean` with
  `…_of_trueFactors`, routing through the new producer.

## Current frontier

`CutProjectionHypotheses` for true-factor supports is now producible directly
from the CLD-vector + tight-norm certificates. The producer takes
`L.basis.independent`, the per-support `TrueFactorCLDVectorData`, and
`TrueFactorCLDTightNormBound` as hypotheses.

## Next step

Discharge the three producer hypotheses at a concrete call site: supply
`L.basis.independent` (true for a genuine BHKS block basis), and construct the
per-support `TrueFactorCLDVectorData` / `TrueFactorCLDTightNormBound` (the
`project_eq` / `coeff_eq` canonical-basis facts and the tight per-column
bound). Then wire `…_of_trueFactors` into the cap/separation path toward #7594.

## Blockers

None. Verification: `lake build HexBerlekampZassenhausMathlib.Lattice` and
`lake build HexBerlekampZassenhausMathlib` both clean; `check_dag.py` passes;
`git diff --check` clean; no new sorry/axiom/native_decide/TODO/FIXME.
Pre-existing unrelated `.claude/*` modifications in the worktree were not
touched.
