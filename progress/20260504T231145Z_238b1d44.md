# HexBerlekampZassenhausMathlib irreducibility decision surface (#2088)

## Accomplished

- Claimed issue #2088.
- Added an executable `irreducibleByFactorization` predicate for transported
  `Polynomial ℤ` values in `HexBerlekampZassenhausMathlib/Basic.lean`.
- Added `irreducibleByFactorization_iff` as the proof bridge from the
  executable predicate to Mathlib `Irreducible`.
- Added `irreducibleDecidablePred`, deriving `DecidablePred` from the
  executable predicate rather than a classical fallback.
- Verified `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested
  banned-token scan.

## Current frontier

The Mathlib bridge now exposes the SPEC's decidability surface for
`Polynomial ℤ` irreducibility. The correctness theorem remains a Phase 1
proof scaffold, consistent with the surrounding theorem-only scaffolds.

## Next step

Open a PR closing #2088 with the bridge update and this progress file.

## Blockers

None.
