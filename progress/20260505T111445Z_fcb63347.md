# 2026-05-05 11:14 UTC — work session (fcb63347)

## Accomplished

Claimed and discharged #2317 — the recovery-program audit of surviving
`borderedMinor` / `leadingPrefix` det theorems. After consumer-grep
across the codebase, every named candidate plus the local
`prefixRowDeletionMinor` helper had **no live consumers** (only
self-references inside `HexMatrix/Determinant.lean` and unrelated
mentions in earlier progress notes). All were artifacts of the
abandoned in-tree Desnanot-Jacobi reproof now superseded by the
Mathlib bridge in `HexMatrixMathlib`.

Classification: **all DELETE**, none Move, none Keep.

Deleted from `HexMatrix/Determinant.lean` (339 lines):
- `det_borderedMinor_zero` (was `@[simp]`)
- `det_borderedMinor_one_mul_prevPivot_zero`
- `det_leadingPrefix_borderedMinor_eq_det_leadingPrefix`
- `det_leadingPrefix_borderedMinor_succ_eq_det_borderedMinor`
- `prefixRowDeletionMinor` def + 6 entry simp lemmas
  (`prefixRowDeletionMinor_entry_{before_prefix,before_last,
  shifted_prefix,shifted_last,border_prefix,border_last}`)
- `detTerm_leadingPrefix_borderedMinor_succ_eq`
- `detTerm_borderedMinor_succ_insertAt_last`
- `detTerm_insertAt_boundary_eq_neg_rowSwap_insertAt_last` (private
  helper, only consumer was `_oldBoundary` below)
- `leadingPrefix_rowSwap_borderedMinor_succ_oldBoundary_eq` (private
  helper, only consumer was `_oldBoundary` below)
- `detTerm_borderedMinor_succ_insertAt_oldBoundary`
- `det_borderedMinor_succ_eq_zero_of_row_duplicate`
- `det_borderedMinor_succ_eq_zero_of_col_duplicate`

Verification:
- `lake build HexMatrix` clean (modulo pre-existing `RREF.lean` /
  `Bareiss.lean` sorries unchanged).
- `lake build HexMatrixMathlib` clean (modulo pre-existing
  `RankSpanNullspace.lean` / `Determinant.lean` sorries unchanged).
- `lake build` (full) green.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- No new `axiom`, `sorry`, `TODO`, `FIXME`, or `native_decide` in
  `HexMatrix/Determinant.lean`.

## Current frontier

`HexMatrix/Basic.lean` retains two reindexing theorems
(`leadingPrefix_borderedMinor_eq_leadingPrefix`,
`leadingPrefix_borderedMinor_succ_eq_borderedMinor`) at L247-276 that
became orphaned by these deletions. They sit in the L166-276 range the
issue scopes as out-of-scope ("data-definition simp lemmas in
`HexMatrix/Basic.lean`"), so I left them in place even though they're
no longer referenced. They're definitional structural facts that may be
useful for any future Mathlib-bridge work in this area.

## Next step

The `HexMatrix` recovery program (#2308) is now closer to its target
shape. The next batch of recovery sub-issues to consider for closure /
narrowing: #2250, #2293, #2296, #2298 (the prefix-row final-column
chain), called out by the issue body as overlap candidates needing
separate planner triage.

## Blockers

None.
