# Progress: #6774 diagnosis + #7059 linter cleanup

## Accomplished

- Ran `/work`. Picked #6774 (final slow-modular irreducibility integration)
  as highest-importance; all its declared dependencies (#6772/#6773/#6804)
  are closed.
- **Verified the premise (it does not hold).** The slow-modular Hensel
  substrate the issue says to "instantiate" has no producer in the tree:
  `HenselLiftDescentHypotheses`, the `toMonicLiftData` modP->lift transport
  (`hlifted_of_modP`), and `InitialLiftedFactorSubsetPartitionEvidence` are
  all consumed as hypotheses (~25 sites across `Basic.lean` /
  `IntReductionMod.lean`) and concluded by nothing. Root cause: #6773 was
  closed COMPLETED on a triage claiming `liftedFactorSubsetPartition_of_choosePrimeData`
  "does not assume" the evidence, but the source (`Basic.lean:17807-17809`)
  shows it takes `hinitial : InitialLiftedFactorSubsetPartitionEvidence` as
  a hypothesis and only projects fields out of it. Posted a detailed,
  line-referenced comment on #6774 and `coordination skip`ped it (replan).
  Tree left unmodified by the investigation; `HexBerlekampZassenhausMathlib`
  builds with 0 errors (so the gap is real, not a red baseline).
- Claimed and completed #7059: cleared the two `linter.unnecessarySimpa`
  warnings in `HexPolyZ/Basic.lean` (`content_mul_primitivePart` :732,
  `content_C_int` :2958). Both `simpa [...] using e` calls became direct
  term proofs (`content`/`primitivePart` are defeq wrappers over the
  `DensePoly` versions, so the `DensePoly` lemma proves each goal directly).

## Verification

- `lake build HexPolyZ` — clean, 0 errors / 0 warnings (both flagged sites
  silenced, no new warnings).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` clean; no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`
  on added lines. Diff confined to the two flagged proof bodies.

## Current frontier

#7059 ready to PR. #6774 routed to replan with a diagnosis the next planner
can act on.

## Next step

- Push branch, open PR closing #7059.
- A planner should re-scope #6773 to actually *produce*
  `InitialLiftedFactorSubsetPartitionEvidence` (and split out the
  descent-hypotheses + modP->lift transport producers) before #6774 is
  re-attempted.

## Blockers

#6774 is blocked on three missing substrate producers (documented on the
issue). Not actionable as a token-swap integration until they land.
