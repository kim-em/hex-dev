## Accomplished

- Claimed human-oversight issue #2702 for the HexPoly Phase-4 audit reset follow-up.
- Confirmed `HexPoly/Bench.lean` and the `hexpoly_bench` executable already register the expected dense polynomial benchmarks.
- Filed successor issue #2718 for the missing scientific verdicts, profile coverage, traceable final report, and re-promotion to Phase 4.
- Rolled `HexPoly.done_through` from 4 back to 3, added `HexPoly.phase4.input_families`, and added `reports/hex-poly-performance.md` with #2718 as the unresolved Concern.
- Updated `scripts/libgraph.py` so the documented nested `phase4` metadata in `libraries.yml` does not break the project DAG loader.

## Current frontier

- `HexPoly` is ready to re-enter Phase 4 via #2718.
- The benchmark wiring is intact; the missing evidence is the scientific `run` verdict set and one representative profile per input family.

## Next step

- Land the rollback PR for #2702, then execute #2718 to regenerate the complete Phase-4 evidence package and restore `HexPoly.done_through: 4` only if the report has an empty Concerns section.

## Blockers

- None for the rollback.
