# HexConway Conway-oracle review

## Accomplished

- Claimed review issue #2100 after #1997 could not be claimed because it is marked `replan`.
- Reproduced the PARI `ffinit` mismatch for `(2,1)`, `(2,3)`, and `(3,2)`.
- Checked local availability for `gp`, `sage`, `gap`, `cypari2`, and the standalone Python `conway-polynomials` package.
- Posted the review findings and recommendation on #2100.

## Current frontier

The viable narrow replacement for the stale PARI leg is a Python oracle using the `conway-polynomials` package, with the caveat that it is a table-source check rather than a true independent Conway oracle.

## Next step

Replan #1997 around a `python3` plus `conway-polynomials` oracle, or open a separate implementation issue for a genuinely independent Conway verifier/search checker if true third-source independence is required.

## Blockers

- PARI `ffinit` does not return Conway polynomials.
- Full Sage's Conway database path is broken in this local environment due missing `_lzma`.
- GAP is not installed locally.
