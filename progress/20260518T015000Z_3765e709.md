# Session 3765e709 — #4866 scaled exhaustive recombination wrapper + invariants

## Accomplished

Added the scaled sibling of `recombineExhaustive` plus its three structural
invariant lemmas to `HexBerlekampZassenhaus/Basic.lean`, sitting directly
on top of the scaled-search invariants merged in #4863.

* `recombineScaledExhaustive (coreLc : Int) (f : ZPoly) (d : LiftData) : Array ZPoly`
  — placed immediately after `scaledRecombinationSearchMod`. The issue
  body asked for placement next to the unscaled `recombineExhaustive`,
  but the scaled wrapper consumes `scaledRecombinationSearchMod` which
  is defined further down the file, so the definition has to follow its
  callee; placing it next to the unscaled sibling would forward-reference
  an undefined symbol.
* `recombineScaledExhaustive_normalizeFactorSign` (private) — placed
  immediately after `recombineExhaustive_shouldRecord`, delegating the
  `some factors` branch to `scaledRecombinationSearchMod_normalizeFactorSign`.
* `recombineScaledExhaustive_shouldRecord` (private) — same site,
  delegating to `scaledRecombinationSearchMod_shouldRecord`.
* `recombineScaledExhaustive_eq_of_scaledRecombinationSearchMod_some`
  (public) — placed immediately after
  `recombineExhaustive_eq_of_recombinationSearchMod_some`. One-line
  `unfold; rw [h]` proof, mirroring the unscaled version.

The change is purely additive — no existing definitions, theorems,
or call sites are touched.

## Verification

* `lake build HexBerlekampZassenhaus.Basic` — passes (pre-existing
  `sorry` at line 6436 and `simpa` lint at line 2291 only).
* `lake build HexBerlekampZassenhaus` — passes.
* `lake build HexBerlekampZassenhausMathlib` — passes (additive change
  does not perturb the Mathlib bridge).
* `python3 scripts/check_dag.py` — passes.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhaus/Basic.lean`
  filtered for `sorry|axiom|native_decide|TODO|FIXME` on additions:
  no new entries.

## Current frontier

The scaled exhaustive substrate is now in place. The next two
prerequisite issues from the /replan note on #4639 can proceed:

1. Swap `exhaustiveCoreFactorsWithBound`
   (`HexBerlekampZassenhaus/Basic.lean:5363`) onto
   `recombineScaledExhaustive` — out of scope here; depends on a
   decision about the `_product` post-condition shape (a scaled product
   divides but does not equal `core` for primitive non-monic cores).
2. Add the scaled membership wrapper
   `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
   in `HexBerlekampZassenhausMathlib/Basic.lean` — depends on (1).

## Next step

A planner picks up the call-site-swap issue, scoping the
`_product`-shape decision and the residual primitive-core wrapper
relaxation chain.

## Blockers

None.
