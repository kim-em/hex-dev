# 3e83e511 - work #6091

## Accomplished

- Claimed and completed feature issue #6091.
- Added a private generic q-row transport in
  `HexMatrixMathlib/Determinant/Core.lean`:
  - `matrixEquiv_nMatrix_p1_pt_eq_submatrix_setRow_q`
  - `det_setRow_q_eq_pow_mul_nDet`
- Exposed the ordered four-row bridge theorems:
  - `ordered_four_cofactorRowPairing_p2_q_eq_pow_mul_nDet`
  - `ordered_four_cofactorRowPairing_p3_q_eq_pow_mul_nDet`
- Verified with `lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token grep over the touched Lean diff.

## Current frontier

#6091 now provides rewrite-ready q-row cofactor pairing transports for the
ordered `p1.val < p2.val < p3.val < q.val` bridge setup. Together with the
existing p1-row transports, #6076 should have signed handles for all four
single-row pairings appearing in the two-row replacement identity.

## Next step

Use the p1-row and q-row bridge theorems to finish the double-row transport
consumer in #6076.

## Blockers

None.
