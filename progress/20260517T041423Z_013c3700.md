# 20260517T041423Z — feature session 013c3700 (#4561 → decomposed)

## Accomplished

Claimed #4561 (HO-1 substrate: exhaustive-branch arm umbrella for
`factor_irreducible_of_nonUnit`), oriented on the wrapper and substrate,
and decomposed into a sub-issue.  No code changes landed; the session
was spent diagnosing why the issue's "every numeric premise is now
dischargeable from `f ≠ 0` + `choosePrimeData? = some _`" premise does
not hold and writing a precise hand-off.

### Why the assembly does not compose as #4561 describes

The wrapper
`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
(`HexBerlekampZassenhausMathlib/Basic.lean:8043`, #4537) needs three
lifted-factor invariants whose dischargers all route through
`Hex.ZPoly.QuadraticMultifactorLiftInvariant_of_choosePrimeData`
(`HexBerlekampZassenhaus/Basic.lean:1760`):

* `hd_liftedFactor_monic` →
  `henselLiftData_liftedFactor_monic_of_choosePrimeData`
  (`HexBerlekampZassenhausMathlib/Basic.lean:3798`)
* `hd_liftedFactor_natDegree_pos` →
  `henselLiftData_liftedFactor_natDegree_pos_of_factorsModPBerlekampForm`
  (`HexBerlekampZassenhausMathlib/Basic.lean:4549`, #4556)
* `hd_liftedFactor_inj` →
  `henselLiftData_liftedFactor_injective_of_factorsModPBerlekampForm`
  (`HexBerlekampZassenhausMathlib/Basic.lean:4613`, #4560)

Each of the three umbrellas takes, as **explicit** input, four
`QuadraticMultifactorLiftInvariant` boundary hypotheses:

1. `hfactors_monic : ∀ g ∈ primeData.factorsModP, Hex.DensePoly.Monic g`
2. `hproduct_mod_p :
     Hex.ZPoly.congr
       (Array.polyProduct (primeData.factorsModP.map Hex.FpPoly.liftToZ))
       core primeData.p`
3. `hcoprime :
     Hex.ZPoly.QuadraticMultifactorCoprimeSplits primeData.p
       primeData.factorsModP.toList`
4. `hnonempty : primeData.factorsModP.toList ≠ []`

No `factorsModP_*_of_factorsModPBerlekampForm` convenience dischargers
exist for these four (only `factorsModP_nodup_of_factorsModPBerlekampForm`
at line 4010 and `factorsModP_natDegree_pos_of_factorsModPBerlekampForm`
at line 4104 are in place).  Without those four dischargers, #4561's
prescribed six-argument signature (`f`, `hf_ne`, `entry`, `hbranch`,
`hentry_mem`, `hchoose`) cannot be realised.

### Decomposition

* Created #4563 "HO-1 substrate: factorsModP convenience dischargers from
  factorsModPBerlekampForm" — the four `factorsModP_*_of_factorsModPBerlekampForm`
  theorems following the established `_nodup` / `_natDegree_pos` pattern.
  Each entry includes a concrete signature, proof sketch, and length
  estimate (15 / 50-100 / 100-150 / 150-250 lines).
* Posted `Decomposed into #4563` breadcrumb on #4561 with the discharger
  gap analysis.
* `coordination skip 4561 "Decomposed into #4563 — see comment"` —
  parent now `replan`.

### Build state

Untouched — no source files modified.  `git status` shows only the
pre-existing `.claude/CLAUDE.md` modification and the untracked
`.claude/commands/` / `.claude/skills/` directories that were present at
session start.

## Current frontier

The HO-1 exhaustive-branch arm assembly is blocked on the four
`factorsModP_*_of_factorsModPBerlekampForm` dischargers in #4563.  The
small-mod singleton arm (#4562) is independent of this gap — its
substrate (#4545) is already complete — and remains the most tractable
unclaimed HO-1 deliverable.

## Next step

1. Land #4563 (four dischargers).  Most tractable: `_ne_nil` first
   (~15 lines), then `_monic`, `_polyProduct_congr`, `_coprime` in
   increasing difficulty.  The `_coprime` one may need a new Berlekamp-
   level `berlekampFactor_factors_pairwise_coprime` prerequisite in
   `HexBerlekamp/Factor.lean` and could be split off if it overruns.
2. After #4563 lands, the planner re-issues the exhaustive-branch arm
   assembly (#4561's scope) as a small follow-up; the same six-argument
   signature becomes realisable with the new dischargers wired in.
3. In parallel, #4562 (small-mod singleton arm) can be claimed
   independently — its composition path does not touch the
   `QuadraticMultifactorLiftInvariant` chain.

## Blockers

None for the decomposition itself.  #4561 is now `replan`; #4563 is the
load-bearing sub-issue; #4562 is independently claimable.
