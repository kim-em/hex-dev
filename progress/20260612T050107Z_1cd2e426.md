# Progress: #6786 — lifted true-support partition bridge

## Accomplished

Claimed #6786 and added the concrete proof-side support family
`liftedTrueSupports core d` in `HexBerlekampZassenhausMathlib/Basic.lean`.
The family records lifted-index supports represented by irreducible integer
divisors via `RepresentsIntegerFactorAtLift`.

Proved the full-universe partition facts needed by the existing
`supportPartitionByMinColumn_length_eq_ncard_of_partition` spine:
coverage from `LiftedFactorSubsetPartition.cover`, same-support equality
from `unique_up_to_associated` plus `pairwise_disjoint`, and nonemptiness
from the existing empty-support exclusion lemma under the primitive-core
precision hypotheses.

Added
`supportPartitionByMinColumn_length_eq_liftedTrueSupports_ncard` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`, composing those
facts with the generic support-partition count theorem.

Ran a second-opinion review. It found no correctness blocker and requested
only a docstring clarification that `liftedTrueSupports` is specialized to
the full lifted-index universe; that clarification was added.

Verification passed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token scan over the touched Lean files

## Current frontier

This is a coherent partial bridge for #6786. The remaining #6786 obligation is
the arithmetic/UFD counting step:
`(liftedTrueSupports core d).ncard =
  (UniqueFactorizationMonoid.normalizedFactors
    (HexPolyZMathlib.toPolynomial core)).card`.

That step needs the bijection between support sets and associated classes of
irreducible divisors, using `unique_up_to_associated` and
`existsUnique_liftedFactorSubset_of_henselSubsetCorrespondenceRest`.

## Next step

Prove the `liftedTrueSupports.ncard = normalizedFactors.card` bijection, then
compose it with
`supportPartitionByMinColumn_length_eq_liftedTrueSupports_ncard` to expose the
final `hpartition` equality consumed by the fast-core irreducibility wrapper.

## Blockers

None for the partial bridge. The remaining count theorem is separate enough to
continue in a follow-up session.
