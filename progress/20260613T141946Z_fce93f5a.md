# Progress: #6838 — liftedRecoveryCandidate bridge surface

## Accomplished

Added recovered-candidate proof surface in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `centeredLiftPoly_dilatedCenteredLiftProduct_eq_factor_of_bound`: from a
  modular congruence for
  `dilate (leadingCoeff core) (centeredLiftPoly (liftedFactorProduct d S) ...)`
  plus abstract Mignotte precision, recovers `factor` after a final
  `centeredLiftPoly`.
- `liftedRecoveryCandidate_eq_factor_of_recovery_of_bound` and default wrapper:
  exact candidate equality from the raw recovered-coordinate equality, primitive
  factor content, and sign-normalisation fixed point.
- `size_dilate_eq_of_monic_of_ne_zero`: variable dilation by a nonzero integer
  preserves stored size on monic `ZPoly`s.
- `natDegree_toPolynomial_liftedRecoveryCandidate_eq_sum` and abstract-bound
  wrapper: the corrected recovered candidate has degree equal to the selected
  lifted-factor degree sum when `lc(core) > 0` and lifted factors are monic.
- `zpoly_primitive_liftedRecoveryCandidate` and abstract-bound wrapper: the
  corrected recovered candidate is primitive under the same positivity/monic
  surface.

## Current frontier

This is a partial completion of #6838. During proof, the stronger premise
"modular congruence + precision directly proves
`liftedRecoveryCandidate core d S = factor`" proved too strong for the current
candidate definition: `liftedRecoveryCandidate` is
`normalizeFactorSign (primitivePart (dilate lc (centeredLiftPoly product)))`
and does not perform a final centred lift after dilation. The precision theorem
therefore proves equality for the final centred lift of the dilated product;
exact candidate equality needs either the raw recovered-coordinate equality or
a separate coefficient-window theorem proving the dilated centred product is
already in the centred window.

## Next step

Prove the missing raw-window bridge for
`Hex.ZPoly.dilate (Hex.DensePoly.leadingCoeff core)
  (Hex.centeredLiftPoly (liftedFactorProduct d S) (d.p ^ d.k))`,
or route #6839 through the raw equality shape if that is what the executable
dilation congruence will provide.

## Blockers

`lake build HexBerlekampZassenhausMathlib.Basic` reaches these new declarations
and then fails at the known scale-to-dilate recursive-search seam:
`Basic.lean:16353` still passes an old `scale (leadingCoeff core)` prefix-none
hypothesis where `Hex.scaledRecombinationSearchModAux` now expects the
`dilate (leadingCoeff core) (centeredLiftPoly ...)` candidate shape.

Other checks:

- `lake build HexBerlekampZassenhaus` completed during the bounded build run.
- `python3 scripts/check_dag.py` exited 0.
- Added-line grep found no `sorry`, `axiom`, `native_decide`, `TODO`, or
  `FIXME`.
