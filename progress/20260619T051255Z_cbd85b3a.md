# HexBerlekampZassenhausMathlib toMonic recovery bridge (#8054)

**Accomplished**

Added bridge-side helpers in `HexBerlekampZassenhausMathlib/LiftBridge.lean`
for the sound non-monic `toMonic` recovery shape:

- `BHKS.recoveredAtLiftOfToMonic` transports a monic-coordinate
  `RepresentsIntegerFactorAtLift` through the existing inverse-recovery theorem
  and returns the data-bearing `RecoveredAtLift` carrier for the original core.
- `BHKS.representsOfToMonic` exposes the predicate form.
- `BHKS.recoveryCandidate_eq_of_toMonic` exposes the downstream
  `liftedRecoveryCandidate ... = factor` equality for the original non-monic
  core, using the primitive-part recovery package rather than a non-monic raw
  `RecoveredLift`.

Also checked directive #2564 first; `coordination claim 2564` failed because
the directive remains blocked by open dependency #4818, so this session claimed
and completed #8054.

**Current frontier**

`HexBerlekampZassenhausMathlib.LiftBridge` builds cleanly. Full
`lake build HexBerlekampZassenhausMathlib` succeeds with pre-existing warnings
and pre-existing sorries outside the touched file. `scripts/check_dag.py`,
`git diff --check`, and the added-line forbidden-token scan over the touched
Lean diff all pass.

**Next step**

Downstream cut-survival code can now consume the non-monic recovered-at-lift
package and candidate equality directly instead of trying to build a
non-monic-core `BHKS.RecoveredLift`.

**Blockers**

None for #8054. The parent directive #2564 remains blocked on open #4818.
