## Accomplished

- Claimed PR #2657 for repair after PR #2680 was already repair-claimed.
- Reproduced the stuck conformance symptom locally in `hexbz_emit_fixtures`.
- Fixed the BHKS fast path in `HexBerlekampZassenhaus/Basic.lean` so:
  - single modular-factor prime data returns the normalized square-free core immediately;
  - the public `factorFast` loop uses a practical precision cap before falling back to the slow path;
  - guards cover the irreducible cyclotomic case and the `x^4 + 1` split-mod-prime miss.
- Verified the previously stuck BZ fixture/oracle path completes.

## Current frontier

- PR #2657 is ready to push with the repair commit.
- The unrelated local `.claude/CLAUDE.md` modification remains uncommitted and untouched.

## Next step

- Commit the repair and this progress file, push the PR branch, and mark PR #2657 salvaged.

## Blockers

- Full local `scripts/ci/run_oracles.sh` is blocked by this machine's missing `conway-polynomials` system-Python package; the CI workflow installs it explicitly. The relevant BZ oracle path passes locally.
