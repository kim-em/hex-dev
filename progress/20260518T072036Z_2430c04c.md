# 2026-05-18 07:20 UTC — planner: singleton-arm `_of_pos_lc` substrate pair (2430c04c)

Planner session. `POD_QUEUE_DEFICIT=2` → filed two parallel atomic
feature issues for the small-mod singleton arm `_of_pos_lc` substrate
pair, mirroring the exhaustive-arm pattern that landed via #4946
(leaf) and #4949 (mid-layer).

## Accomplished

* **#4955** filed (leaf, Mathlib-free, `HexBerlekampZassenhaus/Basic.lean`):
  `_of_pos_lc` sibling of
  `expandRepeatedPartFactorArray_pow_singleton`. Weakened-conclusion
  form (`.2 = 1` only, not full pair) routing through the existing
  array-level public surface
  `expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
  (#4778) specialised to `coreFactors = #[q]`, `exponents = [k]`. The
  full-pair conclusion `((List.replicate k q).toArray, 1)` of the
  monic leaf has no non-monic counterpart — the `consumeExactPower`
  chain is genuinely monic-only — so the residual-only form is
  intentional.

* **#4956** filed (mid-layer, Mathlib bridge,
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`):
  `_of_pos_lc` sibling of `reassemblyExpansionComplete_singleton_of_irreducible`.
  Two viable proof paths offered: Option A routes through the new
  leaf #4955; Option B routes through the array-level public
  surface directly (mirroring the
  `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  precedent at lines 2400–2451 and #4949's exhaustive-arm
  mid-layer). The two issues are filed **in parallel** — #4956 does
  not block on #4955 because Option B is independent.

## Current frontier

The singleton arm has a docstring-level Gap 1 self-contained to the
`reassemblyExpansionComplete_singleton_of_irreducible` discharger
(no umbrella-internal rewiring needed; the singleton-arm umbrella
`factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
already takes `hcomplete` from caller and does not consume
`hcore_monic`). #4956 closes this gap at the discharger layer.

After #4955 + #4956 land, downstream dispatchers wanting to discharge
`hcomplete` for non-monic primitive `squareFreeCore` will route
through the new `_of_pos_lc` sibling instead of the monic-only
existing surface. The eventual capstone wiring for the singleton arm
(part of #4170) can then drop the explicit `hcore_monic` premise on
the singleton arm without requiring an umbrella-internal rewiring.

## Next step

Workers claim and execute. Both issues are independent and Mathlib-free
where applicable; both should fit within the standard atomic-issue
sizing (~2 files, ~80–100 lines each).

## Blockers

None. Queue depth now `3` (matches `POD_MIN_QUEUE`). Filed under
`agent-plan,feature` labels per the standard worker dispatch
expectations.

## Notes

* PR #2656 (spec: add hex-real-roots and hex-sturm) is mergeable
  but is a **draft** — skipped per merge-ready check at Step 2.
* PR #4917 has `repair-claimed` label — a repair agent is on it;
  no fix issue filed.
* Existing claimed work (#4935, #4946 just-landed, #4949 just-landed,
  and #4950 currently claimed) covers the exhaustive-arm parallel
  chain. The singleton-arm substrate pair filed here is independent
  of those.
* Did not call `coordination set-target` or `coordination
  set-min-queue` (active development, not winding down).
* Did not file substrate sub-issues for #4880 helpers 2/3/4 (per
  #4940's architectural-infeasibility recommendation).
