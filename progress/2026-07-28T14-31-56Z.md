# Resultant chain structure

## Accomplished

- Proved that `subresultantOrderedFuel` is stable under arbitrary fuel beyond
  the public budget.
- Proved that every public Brown chain term is nonzero, every adjacent pair
  after the two inputs strictly decreases in dense size, and every nonzero
  input pair satisfies the sharp `min(deg f, deg g) + 2` length bound.
- Added the structural size lemmas for runtime coefficientwise scaling and
  scalar division used by the worker proofs.
- Generalized all four public structural theorems to the operations-only class
  assumptions actually required; no commutative-ring or exact-division law is
  needed.
- Documented the certified chain and fuel API in the Resultant SPEC and manual.
- Extended conformance with theorem-level API checks and executable coverage of
  nonzero terms, strict descent, the sharp bound, defective degree drops, and
  extra-fuel stability.
- Verified the full build, focused conformance/manual targets, all four
  Resultant benchmarks, fixture regeneration, and explicit axiom reports for
  the four new theorems. The axiom reports contain no `sorryAx`.

## Current frontier

The Mathlib-free Resultant structural obligations are complete. The remaining
central obligation is `subresultantOrdered_brownLaw`, which must establish the
Brown--Traub exact divisibilities and exclude the worker's algebraic junk
branch. The downstream Resultant/Mathlib correspondence remains gated on that
value invariant.

## Next step

Land and merge this structural milestone as the only task PR. After it is
merged, develop the Brown exactness infrastructure as the next coherent
Resultant milestone before proceeding to the NumberField or tower layers.

## Blockers

The local Python environment lacks both `python-flint` and `cypari2`, so the
fresh Resultant fixture was compared byte-for-byte but the optional external
numeric oracle skipped locally. Merge-gating CI installs and requires those
dependencies. There is no blocker to the structural proofs themselves.
