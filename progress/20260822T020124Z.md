# PNT+ Ramanujan theta family

## Accomplished

- Audited the post-numeric pinned inventory and confirmed that all 340 actual
  interval-tactic sites are classified; the remaining native sites are
  structural or shared dependency-interface work.
- Replaced both bounded Ramanujan theta checker leaves by source-correlated,
  package-owned range and point certificates with ordinary-kernel semantic
  theorems and guarded axiom reports.
- Added offline inventory correlation for both native declaration identities
  and literal provider rows, including missing, duplicate, coordinate, and
  numeric-row mutation tests.

## Current frontier

The complete file-local Ramanujan theta family is accepted after a localized
predicate-eliminating rewrite. The shared Chebyshev dependency interface is
still pending for the top-level Chebyshev and FKS2-floor consumers.

## Next step

Treat FKS2Floor/Cor22Floor as a deliberate two-provider boundary: extend the
checked theta fold through 22027 and separately add a bounded transcendental
cover provider before attempting the source theorem adapters.

## Blockers

The current supported surface has no affine-cover provider. The FKS2-floor
edge therefore cannot be represented honestly by a dependency-only adapter;
it also requires the larger Chebyshev theta sweep.
