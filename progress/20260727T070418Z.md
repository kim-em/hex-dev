# Issue #8569 certificate replay diagnostic

## Accomplished

- Rebuilt the kernel-factor sweep around `Hex.ZPoly.factorize`, `factor_poly`,
  and `irreducibility`, with distinct provider-decline, timeout, resource-limit,
  and unexpected-error statuses.
- Added witness-class metadata and paired literal Rabin-certificate replay for
  the Linear and LinearIncremental checkers, including order-alternated repeats,
  medians, ranges, and truncation metadata.
- Added the reproducible `quartic_a4` multi-prime boundary case to the committed
  corpus and recorded a four-case validation sweep covering free witnesses,
  reducible factorization, multi-prime certification, and expected decline.
- Refreshed the schema-2 record, report, and plot with honest total/delta timing
  labels, visible import baselines and declines, and the current corpus hash.
- Addressed the requested Claude Opus review findings, including process-group
  timeout cleanup, censored-only frontier stopping, stricter metadata checks,
  commit/host artifact naming, repeated Rabin measurements, and historical
  corpus-hash notes.
- Rebased onto current `main`, rebuilt the affected factorization stack, and
  switched generated certificate modules to the supported
  `HexBerlekampZassenhausMathlib` umbrella after the release-preparation import
  split stopped the internal provider module from exporting `irreducibility`.
- Verified `lake build`, corpus regeneration, DAG checks, Python compilation and
  lint, status/schema invariants, a real timeout cleanup probe, deterministic
  plotting, and the absence of banned proof shortcuts or retired diagnostic
  APIs.

## Current frontier

The implementation and committed sample artifacts are ready for review.  The
sample confirms the current tactic/provider coverage boundary but deliberately
does not claim a full degree/family crossover.

## Next step

Open the issue PR, rebase if GitHub reports a conflict, monitor all Actions jobs,
and enable squash auto-merge once the checks are green.

## Blockers

None.
