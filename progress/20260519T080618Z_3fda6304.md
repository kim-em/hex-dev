# Session 3fda6304 - issue #5331

## Accomplished

- Claimed issue #5331 after blocked directives #2564 and #2567 could not be
  worked.
- Added direct zero-polynomial degree simp lemmas in `HexPoly/Dense.lean`:
  `DensePoly.degree?_zero` and `DensePoly.degree?_zero_getD`.
- Verified with:
  - `lake build HexPoly`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^@\[simp\] theorem degree\?_zero' HexPoly/Dense.lean`
  - `git diff -- HexPoly/Dense.lean | rg -n '^\+.*(sorry|axiom|native_decide|TODO|FIXME)'`

## Current frontier

- Issue #5331 is complete locally with a narrow DensePoly API change.

## Next step

- Create the PR for issue #5331 and let CI validate the branch.

## Blockers

None.
