# Session 5a5c95ae — #5004 IntReductionMod orphan hcore_size_pos cleanup

## Accomplished

- Claimed and executed #5004: deleted the orphan outer-scope
  `hcore_size_pos` `have` block at
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean:3414-3435`
  (post-#5006 line numbers; pre-rebase 3422-3443 as in the issue body),
  together with its preceding one-line stale comment.
- The block had zero consumers in the enclosing thin wrapper
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
  after #4991's rewire to
  `defaultFactorCoeffBound_leadingCoeff_natAbs_le`. PRs #4995 (closing
  #4996) and #4999 (closing #4997) cleared the analogous boilerplate
  at the 5 IntReductionMod + 12 Basic.lean rewire sites; #4996's worker
  miscounted this site as a "pre-existing internal positivity witness."
- 22-line pure deletion (zero additions) on a single file.

## Verification

- `grep -c "have hcore_size_pos\b" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  drops 1 → 0.
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`: 18 errors
  pre-edit, 18 errors post-edit, byte-identical error sets (all
  pre-existing whnf timeouts / kernel-unknown constants in Basic.lean;
  none in IntReductionMod.lean).
- `lake build HexBerlekampZassenhausMathlib`: same 18 errors.
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- `git diff origin/main`: 0 additions, 22 deletions, single file
  (`HexBerlekampZassenhausMathlib/IntReductionMod.lean`).
- No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in diff.

## Notes

- Branch was 1 commit behind `origin/main` at session-claim time;
  fast-forwarded over #5006 (Basic.lean-only rewire) before
  building. No conflicts (disjoint files).
- The plan's "currently 18 errors" baseline matched session-claim
  state both before and after the FF.

## Current frontier

PR pushed; closes #5004.

## Next step

None for this corner. The remaining boilerplate-cleanup work in the
queue (#5005, `zpoly_lc_pos_of_monic` helper extraction + 4 rewire
sites) is independent and already filed.

## Blockers

None.
