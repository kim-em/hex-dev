# Issue #4202

## Accomplished

- Claimed feature issue #4202.
- Added the fast-core BHKS branch theorem
  `factorFastCoreWithBound_some_factor_irreducible` in
  `HexBerlekampZassenhausMathlib/PartitionRefinement.lean`.
- Added the corresponding executable-predicate wrapper
  `factorFastCoreWithBound_some_factor_zpolyIrreducible`.
- Both theorems compose the existing partition-refinement count equality with
  the pre-existing count-scaffold irreducibility theorem; executable
  factorization behavior is unchanged.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhaus`
  - `lake build HexBerlekampZassenhausMathlib`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The successful BHKS fast-core branch now has a direct irreducibility theorem
  under the existing `ExpectedTrueFactors` and partition-cardinality
  hypotheses.
- Downstream assembly can consume the `Hex.ZPoly.Irreducible` wrapper without
  manually threading the intermediate normalized-factor count equality.
- Existing repository warnings remain unchanged, including pre-existing
  `sorry` warnings in unrelated dependencies and residual BZ Mathlib theorem
  stubs.

## Next step

- Use the new fast-core branch theorem when assembling
  `factor_irreducible_of_nonUnit` for the fast path.

## Blockers

- None for issue #4202.
