**Accomplished**

- Claimed #2567 and diagnosed the directive as stale against the current API:
  `factorFastFactorsWithBound` may return `none` when `choosePrimeData? f`
  fails, while current SPEC/Recovery targets carry an explicit
  `choosePrimeData?` witness. Commented with concrete references and skipped
  the issue.
- Claimed #6609 and found it blocked because `yunScalarStep` is not present in
  `HexPolyFp/SquareFree.lean`; the prerequisite PR #6607 has not landed.
  Commented and skipped the issue.
- Claimed #6456 and added the successful-candidate extraction bridge:
  `bhksIndicatorCandidates?_getD_candidate` in
  `HexBerlekampZassenhaus/Basic.lean`.
- Added Mathlib recovery bridges proving each successful BHKS indicator
  candidate represents the corresponding integer factor at the lift precision,
  and lifting successful candidate vectors to canonical per-class
  representations.
- Verified with `lake build HexBerlekampZassenhaus.Basic`,
  `lake build HexBerlekampZassenhausMathlib.Recovery`,
  `lake build HexBerlekampZassenhaus`, `lake build HexBerlekampZassenhausMathlib`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a grep over the diff
  for banned markers.

**Current frontier**

- #6456 is partially implemented. The remaining requested actual-cap wrapper
  appears misaligned with the current normalization API: `normalizeForFactor`
  exposes a primitive square-free core with positive leading coefficient, while
  the available BHKS representation wrapper requires an explicit monic
  hypothesis for the square-free core. I did not find an unconditional theorem
  making `(Hex.normalizeForFactor f).squareFreeCore` monic.
- `.claude/CLAUDE.md` was already dirty from pod-managed guidance and was not
  part of this session's work.

**Next step**

- Replan the residual #6456 actual-cap deliverable around the current API:
  either route the wrapper through an explicit monic normalization surface,
  keep the explicit `hcore_monic` hypothesis, or update the issue premise.

**Blockers**

- The actual-cap wrapper cannot currently discharge the required monic
  hypothesis from `normalizeForFactor` alone.
