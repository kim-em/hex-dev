# 2026-05-06 22:20 UTC — work session (e5c5b6ed)

## Accomplished

Claimed #2522 and implemented a coherent partial slice of the row-pivoted
Bareiss invariant work:

- added `Hex.Matrix.findPivotAux_ge_start` and `Hex.Matrix.findPivot?_ge_start`
  in `HexMatrix/Bareiss.lean`;
- added `bareissPivotInvariant_regular_no_swap`,
  `bareissPivotRegularSwap_det`, and `bareissData_sign_succ` in
  `HexMatrixMathlib/Determinant/Bareiss.lean`;
- created successor issue #2535 for the remaining full row-pivoted invariant.

Verification completed:

- `lake build HexMatrix.Bareiss`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Bareiss.lean HexMatrixMathlib/Determinant/Bareiss.lean`

The only `sorry` hit in the touched files is the pre-existing
`HexMatrix/Bareiss.lean` capstone.

## Current frontier

#2522 is ready for a partial PR. The remaining invariant proof is now tracked
by #2535 and blocked on #2522 landing.

## Next step

Create the partial PR for #2522.

## Blockers

`lake build HexMatrixMathlib.Determinant.Bareiss` was attempted but stopped
after compiling thousands of Mathlib dependencies without reaching the local
target. The worktree also has a pre-existing unrelated `.claude/CLAUDE.md`
modification that this session did not touch.
