# 20260609T100737Z (#6644)

## Accomplished

- Wired the Zenodo 2636367 `svp_certified` driver as the verified
  Isabelle certified-LLL comparator, using
  `scripts/oracle/setup_lll_isabelle.sh certified`.
- Added a persistent-stdin patch for `haskell_sources/Main_Certified.hs`
  mirroring the existing `svp_verified` patch.
- Added `runIsabelleCertified*NormSq` fixed benchmark registrations over
  the random-bounded and harsh-cubic ladders in `HexLLL/Bench.lean`.
- Extended the existing single CI bench job to install the `fplll` binary
  package and export `HEX_LLL_ISABELLE_CERTIFIED_SVP`.
- Added bottom-rung certified-vs-Isabelle-certified smoke evidence and
  per-request overhead notes to `reports/hex-lll-performance.md`.

## Current frontier

The comparator wiring is complete and all new Isabelle-certified fixed
registrations pass targeted `hexlll_bench verify`. The full
certified-vs-Isabelle-certified ladder is registered but still needs a
scheduled scientific timing run if the report should promote beyond the
two smoke rows.

## Next step

Run the full certified-vs-Isabelle-certified ladder on scheduled hardware
and refresh the comparator-ratio table with all rungs.

## Blockers

None.
