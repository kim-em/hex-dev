# Progress — 2026-06-19T23:48Z (feature, #8119)

## Accomplished

Phase 6 grind-automation sweep for the two unswept files in
`HexBerlekampZassenhausMathlib`. Promoted three public `@[simp]` lemmas to
`@[simp, grind =]`:

- `factor_product` (`Basic.lean:160`) —
  `Hex.Factorization.product (Hex.factor f) = f`. Literal `lhs = rhs`; RHS `f`
  strips the `product ∘ factor` wrapper, so it strictly reduces structure and
  terminates.
- `irreducibleByFactorization_iff` (`Basic.lean:246`) —
  `irreducibleByFactorization f = true ↔ Irreducible f`. `↔`, accepted by
  `grind =` as a left-to-right rewrite. LHS `irreducibleByFactorization f = true`
  rewrites to the semantic `Irreducible f`; RHS contains no
  `irreducibleByFactorization`, so it terminates. This is the standard
  Bool-test → predicate normal-form direction (mirrors the skill's
  `p.isZero = true ↔ p = 0` example).
- `mem_supportOfSubset` (`LiftBridge.lean:25`) —
  `i ∈ supportOfSubset f d T ↔ i ∈ T`. `↔`; strips the `supportOfSubset`
  wrapper to bare membership, terminating (mirrors the skill's `memLattice`
  example).

No exclusions: all three are sound, terminating rewrites and `grind =` accepted
all three shapes (green build, no "invalid pattern" / "Try this" rejection).

## Current frontier

`lake build HexBerlekampZassenhausMathlib` green (3790 jobs, zero `error:`).
`Basic` 25s / `LiftBridge` 3.0s — no grind-loop slowdown. `scripts/check_dag.py`
exit 0. Added lines `sorry`/`axiom`/`native_decide`-free; `Basic` warnings are
all pre-existing (sorry at 233, deprecations, unused vars), none at 160/246.

## Next step

None for this issue. The two files now carry `grind =` coverage; the BZ Mathlib
bridge layer's Phase 6 sweep targets are complete.

## Blockers

None.
