# 2026-05-19T23:56 UTC — HexMatrix row-echelon API polish

## Accomplished

- Claimed and executed #5567 after #5566 was claimed by another agent.
- Reviewed the `HexMatrix/RowEchelon.lean` and `HexMatrix/RREF.lean` row-echelon/span/nullspace public API against the Phase 6 criteria.
- Added docstrings for public free-column and column-partition lemmas in `IsEchelonForm`.
- Added `[simp]` characterising lemmas for `IsEchelonForm.spanContains` and the public `Matrix.spanContains` wrapper.
- Exposed the RREF nullspace pivot-entry formula as public `IsRREF.nullspace_get_pivot`, with `[simp]`, alongside documented free-column nullspace entry lemmas.

## Current frontier

- The row-echelon/span/nullspace API now has coefficient-level characterising lemmas for `spanContains` and the nullspace basis vector entries without unfolding executable definitions.
- Determinant and Bareiss bridge-sensitive surfaces were left untouched, per `SPEC/Libraries/hex-matrix.md`.

## Next step

- Continue Phase 6 polish on adjacent `HexMatrix` API surfaces if more issues are filed, especially any remaining public lemmas lacking docstrings or stable characterising statements.

## Blockers

- None. Verified with `lake build HexMatrix.RowEchelon`, `lake build HexMatrix.RREF`, `lake build HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a placeholder scan over the edited Lean files.
