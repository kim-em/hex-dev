# Bounded non-elementary integral canary

## Accomplished

- Implemented on exact `main` `4ecad503731d1a4662e350bc853d38649757cacf`
  a Mathlib-free one-cell integration package for
  `∫ x in 0..1, Real.exp (-(x ^ 2))`.
- Made the cell endpoints, eight-term Taylor order, exact polynomial integral
  `1009219 / 1351350`, integrated remainder `1 / 609280`, and requested
  four-decimal window load-bearing certificate atoms.
- Proved the analytic bridge without a known integral value: `Real.exp_bound`
  supplies the pointwise remainder, `x ^ 16` is integrated exactly, and every
  Taylor monomial is integrated with the interval-integral power theorem.
- Replayed the provider event through the generic package, chronology,
  semantic schema, and `ProofFrontend`, closing the ordinary theorem
  `7468 / 10000 < I ∧ I < 7469 / 10000`.
- Added planning, format, and replay rejection for an omitted cell, a falsely
  tightened error bound, and a changed right endpoint. The theorem axiom guard
  reports only `propext`, `Classical.choice`, and `Quot.sound`.
- Documented the fixed-canary boundary honestly: arbitrary integrands and
  intervals, multi-cell/adaptive integration, order selection, persistence,
  and performance remain future D10 work.
- Built the focused module and the full interval experiment/Mathlib/conformance
  targets (9,393 jobs), and passed copyright, file-line, DAG, trust-surface,
  Phase 4, exact post-commit factor-freshness, diff, and banned-mechanism
  checks.

## Current frontier

- The reusable certificate/replay boundary and kernel analytic proof are
  established for one fixed cell. The runtime decoder deliberately accepts
  only this bounded recipe.

## Next step

- Generalize the provider to multiple authenticated cells and a checked sum,
  then make partition/order choice precision-driven before attempting adaptive
  refinement or performance claims.

## Blockers

- None for the fixed canary. The missing work is provider generality and
  strategy, not a Mathlib interval-integral or proof-frontend bridge.
