# HexHensel Phase 6 review — issue #5582

## Accomplished

Narrow Phase 6 API-polish slice for `HexHensel/QuadraticMultifactor.lean`:

- Added three named projection lemmas on `QuadraticLiftLoopInvariant` —
  `prod_congr`, `bezout_congr`, `monic` — mirroring the existing
  `of_product_bezout_monic` constructor. Callers can now destructure the
  three conjuncts by name without `rcases` boilerplate or unfolding
  through the `And` representation.
- Marked `multifactorLiftQuadratic_size_eq_input` as `@[simp]`. Rewriting
  `(multifactorLiftQuadratic …).size` to `factors.size` is the natural
  forward direction and matches how downstream Berlekamp–Zassenhaus
  callers reason about output array length.
- Added two boundary `@[simp]` lemmas for `multifactorLiftQuadratic`:
  `multifactorLiftQuadratic_empty` and `multifactorLiftQuadratic_singleton`.
  These normalise the two trivial input cases so callers don't need to
  unfold the recursive worker.

`HexHensel/Quadratic.lean` was reviewed; no narrow polish item stood out
without touching the proof body or executable algorithm shape, so it is
left as-is for this slice.

## Current frontier

`lake build HexHensel.Quadratic HexHensel.QuadraticMultifactor` green;
`lake build HexHensel` green; `python3 scripts/check_dag.py` clean;
diff has no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` insertions.

## Next step

Push branch and open PR closing #5582. Further HexHensel Phase 6 slices
(e.g. additional simp annotations, public combined `_factor_congr_mod_base`
wrappers on `henselLiftQuadratic`, asymmetric `0 < m` vs `1 < m` bound
tightening on `quadraticHenselStep_factor_*`) remain available for a
follow-up planner pass but are out of scope here.

## Blockers

None.
