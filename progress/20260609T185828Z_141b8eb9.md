# 20260609T185828Z (#6704)

## Accomplished

Refactored the HO-1 public factorization soundness surface into a post-
`IntReductionMod` module:

- Added `HexBerlekampZassenhausMathlib/FactorSoundness.lean`, importing
  `HexBerlekampZassenhausMathlib.IntReductionMod`.
- Moved the public capstone-facing declarations out of
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  `factor_irreducible_of_nonUnit`,
  `factor_polynomialIrreducible_of_nonUnit`,
  `factor_headline_contract_core`,
  `factor_headline_contract_core_with_primitive`, and
  `factor_unique_of_product`.
- Imported `FactorSoundness` from the root
  `HexBerlekampZassenhausMathlib.lean` after `IntReductionMod`, so root
  importers still see the preserved public theorem names.

The generic raw-to-recorded-entry bridge remains in `Basic.lean`, while the
public capstone declaration now lives in a module that can see the branch
umbrellas in `IntReductionMod.lean`.

## Current frontier

The declaration-order/API visibility blocker from #6704 is removed. The
existing HO-1 capstone proof hole is still localized at
`factor_irreducible_of_nonUnit`, now in `FactorSoundness.lean`; this is the
same theorem-level `sorry` moved from `Basic.lean`, not an additional
capstone obligation.

## Next step

Once the remaining branch proof prerequisites land, the capstone worker can
replace the relocated `factor_irreducible_of_nonUnit` proof in
`FactorSoundness.lean` using both the raw bridge from `Basic.lean` and the
branch umbrellas from `IntReductionMod.lean`.

## Blockers

None for the #6704 module split.

Verification performed:

- `lake build HexBerlekampZassenhausMathlib.Basic HexBerlekampZassenhausMathlib.IntReductionMod HexBerlekampZassenhausMathlib.FactorSoundness`
- `lake build HexBerlekampZassenhausMathlib.FactorSoundness`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`

Notes:

- Builds still warn on the pre-existing project `sorry`s and on the relocated
  `factor_irreducible_of_nonUnit` `sorry`.
- The tracked-file forbidden-token diff shows deletion of the old `sorry`
  from `Basic.lean`; the new module contains that same localized capstone
  proof hole at the moved declaration.
