# Progress — 2026-06-18 (session ff4697df, /work → /feature #7802)

## Accomplished

Completed both deliverables of #7802 (Berlekamp factor irreducibility for
non-monic outputs), executable side only (`HexBerlekamp/`).

### No-split transport across a unit scaling (`HexBerlekamp/Factor.lean`)

`kernelWitnessSplit?_none_scale`: a failed Berlekamp split search is preserved
when the factor being split is replaced by a unit scaling `scale c g` (`c ≠ 0`).
Supporting machinery: mutually-dividing candidates share size/degree
(`size_eq_of_dvd_dvd`), so `isNontrivialSplitFactor` agrees on associates
(`isNontrivialSplitFactor_eq_of_associate`), and the split candidates of `g` and
`scale c g` are associates for every witness/constant
(`splitFactorAt_dvd_dvd_scale`). The aux-level transport is by induction on
fuel.

### Non-monic substrate (`HexBerlekamp/RabinSoundness.lean`)

`irreducible_of_no_kernelWitnessSplit_squareFree_of_dvd_nonmonic`: drops the
monicity hypothesis on the divisor of #7788 to bare `g ≠ 0`. Positive-degree
divisors normalize to their monic associate `scale (leadingCoeff g)⁻¹ g`, to
which #7788 applies after transporting the no-split fact; a nonzero constant is
irreducible by definition.

### Loop fixpoint + capstone

In `Factor.lean`: `berlekampFactorLoop_terminates_or_full` (the loop either
reaches a `splitFirstFactor? = none` fixpoint or grows by exactly `fuel`),
`factorProduct_length_le_degree` (positive-degree factors ⇒ length ≤ degree of
product), and `kernelWitnessSplit?_none_of_berlekampFactor_factors` (the
`f.size + 1` fuel always suffices, since each split adds a positive-degree
factor and the product is preserved as `f`, capping the count at `deg f`). Plus
`berlekampFactor_factors_dvd`. In `RabinSoundness.lean`:
`berlekampFactor_factors_irreducible` assembles nonzero + divides-`f` + no-split
through the non-monic substrate.

## Verification

- `lake build HexBerlekamp` → 478 jobs, `Build completed successfully`, no
  `error`/`warning`.
- `grep -c sorry HexBerlekamp/RabinSoundness.lean` → 0 (unchanged).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added-line `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.

## Current frontier

`berlekampFactor_factors_irreducible` is the executable per-factor headline.
This unblocks #7799 (the Mathlib Berlekamp factor irreducibility bridge).

## Next step

#7799: discharge the `HexBerlekampMathlib` bridge consuming the executable
result.

## Blockers

None for this issue.

Note: worktree carries pre-existing unrelated `.claude/*` modifications from
before this session; not touched or staged.
