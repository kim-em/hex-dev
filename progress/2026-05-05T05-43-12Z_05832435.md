# Session progress — 2026-05-05T05:43:12Z (05832435)

## Accomplished

- Claimed feature issue #2285 after the higher-priority human-oversight
  issues were either raced or marked for replan.
- Added `FpPoly.powModMonicLinear`, `frobeniusXModLinear`, and
  `frobeniusXPowModLinear`, a structurally recursive modular-power path for
  small closed `FpPoly` terms.
- Proved the linear modular-power path agrees with the existing
  square-and-multiply `powModMonic` / Frobenius API under
  `ZMod64.PrimeModulus`.
- Added `Berlekamp.checkPowChainLinear` and
  `Berlekamp.checkIrreducibilityCertificateLinear`, plus soundness bridges back
  to the existing `rabinTest`.
- Made `IrreducibilityCertificate.toAmbient?` public so downstream concrete
  certificate proofs can unfold the same-prime view.
- Extended `HexBerlekamp.Conformance` with a degree-2 example over `F_5`
  proving `Berlekamp.rabinTest irreducibleQuad irreducibleQuad_monic = true`
  via the new linear certificate checker, with no `native_decide`, new `axiom`,
  or new `sorry`.

## Current frontier

The immediate Rabin-test reduction blocker has a certificate-backed kernel
route for small concrete `FpPoly` terms. Downstream Conway/GfqField cleanup can
now use `checkIrreducibilityCertificateLinear_rabinTest` to obtain the
`rabinTest = true` input required by `rabinTest_imp_irreducible`.

## Next step

Use the new checker to generate or hand-write per-entry certificates for the
blocked Conway/GfqField axiom-discharge issues, starting with the dependent
Conway issue #2286.

## Blockers

- None for this issue.
- Pre-existing `.claude/CLAUDE.md` workspace modification was present before
  this session's code changes and was left untouched.
