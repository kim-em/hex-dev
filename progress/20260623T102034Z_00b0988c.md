# #8291 capstone reduced to the fast-core arm (one-shot `/once`)

Session type: feature (one-shot). UTC 2026-06-23T10:20.

## Accomplished

Landed an additive, green, `sorry`/`axiom`/`native_decide`-free **reduction**
of the HO-1 irreducibility capstone `factor_irreducible_of_nonUnit` to a single
clean hypothesis — the fast BHKS core-success arm. This is the issue's
explicitly-sanctioned fallback ("land all dischargeable arms green, isolate the
fast-core arm") when the fast-core bridge does not close in one session.

New declarations:

- `IntReductionMod.lean` (end of file, `HexBerlekampZassenhausMathlib`
  namespace): `fastCoreReassemblyComplete_of_coreIrreducible` — the cut-free
  part of `fastCoreComplete_of_cut`: reassembly expansion-completeness from loop
  success + emitted-factor irreducibility, with **no** forward-cut hypothesis.
  (Placed here, not `FactorSoundness`, because it needs that file's private
  `factorPower_size_lower_bound` / `mem_dvd_foldl_mul_zpoly` helpers.)
- `FactorSoundness.lean`:
  - `fastCoreRawGuarded_of_coreIrreducible` — guarded raw-factor irreducibility
    for branches (d)/(h) from emitted-core irreducibility, via
    `factorFastFactorsWithBound_eq_some_of_core_success` + the completeness
    helper + the Mathlib-free reassembly lift.
  - `factor_irreducible_of_nonUnit_of_fastCore` — the headline reduction: for
    `f ≠ 0`, `∀ entry ∈ (factor f).factors, Irreducible entry.1` **given** the
    single hypothesis "chosen-prime + fast-core loop success ⟹ emitted core
    factors irreducible". The constant (a), small-mod singleton (c)/(g),
    quadratic (f), slow-modular, and slow-trial arms are all discharged from
    landed producers; the `none` dispatch branches are closed by contradiction
    with the `= some rawFactors` source.

Verification: `lake build HexBerlekampZassenhausMathlib` green (3784 jobs, 0
errors). `#print axioms factor_irreducible_of_nonUnit_of_fastCore` =
`[propext, Classical.choice, Quot.sound]` (clean cone — no `sorryAx`, no
`Lean.ofReduceBool`). Additions are Mathlib-layer-only, so executable
conformance / bench are unaffected.

## Current frontier

`factor_irreducible_of_nonUnit` (FactorSoundness.lean:18) still carries its
pre-existing `sorry` — **not closed**. The capstone reduces (modulo the trivial
`f = 0` vacuity, `(factor 0).factors = #[]`) to discharging `h_fastcore`:

```
∀ primeData coreFactors,
  choosePrimeData? core = some primeData →
  factorFastCoreWithBound core (defaultFactorCoeffBound f) primeData … = some coreFactors →
  ∀ factor ∈ coreFactors.toList, Hex.ZPoly.Irreducible factor
```

which is exactly the conclusion of
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_cut`
(`PartitionRefinement.lean:683`).

## Next step / Blockers

`h_fastcore` is **not** dischargeable from existing infrastructure this session.
Every fast-core irreducibility producer (`…_of_cut`, `…_of_recoveredM1`,
`…_of_recoveredLift`, `…_of_forwardInputs(_on_schedule)`) takes the forward cut
`BHKS.CutProjectionHypotheses` / `RecoveredAtLiftM1` recovery witnesses +
`trueSupports` **as hypotheses**; no theorem produces them from the bare loop
success `factorFastCoreWithBound … = some coreFactors`. Grep confirms zero
producers concluding `RecoveredAtLiftM1` or a cut from loop success. The
`…_on_schedule` wrappers additionally require the determinism equality
`coreFactors = hinputs.expectedFactors` ("the loop's first success returns the
cap recovery"), explicitly "left to the caller" and not implied by a package at
the cap.

So the issue's premise ("thin once #8290 lands") does not hold: #8290 delivered a
cut **producer that consumes** recovery witnesses, and #8313/#8314 delivered
recovery-**success** (termination-side) producers — neither supplies the
irreducibility-side witnesses from loop success. The remaining bridge is the
"narrow M2-success → applicable-cut" the issue comment itself flags: the
executable loop is M2-hybrid (`toMonicLiftData`), the M1 cut producer
(`cutProjectionHypotheses_of_recoveredM1`) is over the `coreLiftData` lattice,
and bridging needs recovery-soundness substrate (the forbidden termination
tower), per the `hex-lean-mathlib-boundary` skill warnings (#7985, the period
trap #7866/#7867, the M2-vs-M1 mismatch). Diagnosis posted on the issue;
landing as a `--partial` PR.
