# Session 96a5d3b4 progress

## Accomplished

One-shot dispatch on #5817 (HO-5d-2 BZ proof-side fallback removal).
Triaged scope after the #5842/#5850 unblock; concluded the parent's
proof-facing surface is genuinely too large for one session and
decomposed into five focused sub-issues.

Sub-issue map (deps explicit in each body):

- #5853 — HBZ/Basic.lean: migrate ~11 proof-facing theorems off
  total `choosePrimeData` to explicit `primeData + hchoose` shape
  (`choosePrimeData_fModP_eq_modP`, both branch theorems
  `factorSlowFactorsWithBound_branch` and
  `factorFastFactorsWithBound_branch`, the `factorWithBound_entry_mem_*`
  family, `factorFastWithBound_product_of_*` private theorems,
  `factorFast_ne_none_of_core_recovery_on_schedule`). Foundation slice.
- #5854 — HBZMathlib/Basic.lean: migrate ~11 bridge wrappers
  (`existsUnique_modPFactorSubset_of_choosePrimeData`,
  `modPSubsetPartitionHypotheses_of_choosePrimeData`,
  the four `henselSubset*_of_choosePrimeData_*` variants, the five
  `factorWithBound/factor_exhaustive_branch_entry_core_zpolyIrreducible_*`
  bridges). Depends on #5853.
- #5855 — HBZMathlib/IntReductionMod.lean: migrate ~14 bridge
  wrappers (the `factor_*_branch_entry_irreducible_of_choosePrimeData_*`
  chain plus the `reassemblyExpansionComplete_*` and
  `exhaustiveCoreFactorsWithBound_expansion_preconditions_*` umbrellas).
  Depends on #5853, #5854.
- #5856 — HBZMathlib/Recovery.lean: migrate
  `factorFast_ne_none_of_forwardInputs_on_schedule` and four downstream
  callers off the `primeData = Hex.choosePrimeData …` equality shape.
  Depends on #5853 (for the upstream
  `factorFast_ne_none_of_core_recovery_on_schedule` signature).
- #5857 — comment cleanup: remove or rename stale "fallback `p = 3`"
  / "none branch" comments in the proof-facing surface once the four
  migrations land. Depends on all four.

Posted decomposition breadcrumb on #5817 ("Decomposed into #5853,
#5854, #5855, #5856, #5857") and skipped the parent via
`coordination skip` so the planner's next replan-triage cycle either
closes it (sub-issues fully cover it) or narrows it to residual scope.

## Current frontier

#5817 is now in `replan` state. Sub-issues #5853–#5857 are blocked on
their declared predecessors; #5853 (the foundation slice) is the only
sub-issue without a sub-issue dependency, but it still carries the
`blocked` label until #5817 closes (or the planner removes it during
triage), so the next worker should not claim it before triage.

## Next step

Planner's next cycle should triage #5817 — confirm sub-issues cover
the parent's deliverables and close, or narrow #5817 to a residual
scope (e.g. cross-cutting refactor that the per-file slices don't
absorb). Once #5817 closes, `check-blocked` unblocks #5853, and a
feature worker can claim it as the foundation for the chain.

## Blockers

None within this session — decomposition is the deliverable for a
parent whose scope exceeds one session.
