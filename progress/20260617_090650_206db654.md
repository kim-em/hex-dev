# Progress - 2026-06-17 (session 206db654, one-shot #7712)

## Accomplished

Claimed #7712 (feat: finish tight psiCut aggregation bound) via `pod once` and
**landed the full `tightColumnBound_of_lift` producer** in
`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`. The tight BHKS Lemma 5.7
column estimate `2·|col j| ≤ factorCount` is now proved end-to-end from a genuine
`TrueFactorLift` + `TrueFactorLiftSemantics` package, with no `sorry`/`axiom`.

New declarations (all in `namespace BHKS`):

- `C_dvd_of_monic_mul`: monic cancellation mod `m` over `Polynomial ℤ` (map to
  `ZMod m`, where `g.map` is monic hence a non-zero-divisor via
  `Monic.mul_right_eq_zero_iff`).
- `abs_phi_coeff_le_bhksCoeffBound`: BHKS Lemma 5.1 in executable-bound form —
  `|Phi(f,g).coeff j| ≤ Hex.bhksCoeffBound f j` for a monic divisor `g`. Bridges
  `abs_phi_coeff_le_of_monic_factor`'s real bound (choose · degree · l2norm) to
  the `Nat` bound via `natDegree_toPolynomial`,
  `l2norm_toPolynomial_le_coeffL2NormBound`, and a local `hex_choose_eq`
  (`Hex.Nat.choose = Nat.choose`).
- `residue_cldQuotientMod_eq_phi_coeff` (issue deliverable 1): the per-factor CLD
  residue bridge. From the selected congruence `g·cld ≡ f·g'` it cancels the
  monic `g` (via `C_dvd_of_monic_mul`) to get `cld ≡ Phi(f,g) (mod p^a)`, then
  recenters with `centeredResiduePow_eq_of_natAbs_le` using the column bound and
  the precision separation.
- `phi_sum_eq_phi_prod` (issue deliverable 2): the exact log-derivative support
  sum `Σ_{i∈T} Phi(F, P_i) = Phi(F, ∏ P_i)` over `Polynomial ℤ`, via the Leibniz
  product rule `Polynomial.derivative_prod_finset` and `Finset.mul_prod_erase` —
  no leave-one-out indexing in the statement.
- `tightColumnBound_of_lift` (capstone): rewrites the tail column through
  `coeff_eq_cldCoeffs` + `cldCoeffs_getD_of_lt` into a support-restricted sum of
  `psiCut`s, then feeds `two_mul_natAbs_sum_psiCut_le` (#7708) with `T = S`,
  `w_i = Phi(f, g_i).coeff j` (deliverable 1), `y = Phi(f, factor).coeff j`
  (deliverable 2), `B = bhksCoeffBound`, cut threshold `bhksCoeffCutThreshold`,
  and finishes with `|S| ≤ factorCount`.

The signature matches the issue (with `(hp : 2 ≤ D.p)` supplying the
`le_pow_ceilLogP` separation for the cut modulus), and the produced
`TightColumnBound.bound` is the exact tight form requested.

## Verification

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound` — clean (only
  pre-existing `HexBerlekampMathlib`/`HexHenselMathlib` sorries warn).
- `lake build HexBerlekampZassenhausMathlib.Lattice` — clean.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line forbidden-token scan: no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

`tightColumnBound_of_lift` is the analytic input `tightNormBound_of_lift`
consumes (`Lattice.lean`). With this in place the tight cut-radius norm bound for
true-factor CLD vectors is now constructible from genuine lift data.

## Next step

Wire `tightColumnBound_of_lift` into whatever still constructs `TightColumnBound`
abstractly (e.g. the prefix-cut survivor-span chain), now that a concrete
producer exists.

## Blockers

None. Pre-existing unrelated `.claude/*` modifications in the worktree were not
touched or staged.
