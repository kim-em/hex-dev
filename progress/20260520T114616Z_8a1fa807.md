# 2026-05-20T11:46Z — feature session 8a1fa807

## Accomplished

Claimed #5673 and migrated the first layer of
`HexBerlekampZassenhausMathlib/Basic.lean` from the old
`Hex.henselLiftData core B primeData` surface to the executable
`Hex.monicisedCoreLiftData core B primeData` surface:

- `HenselSubsetCorrespondenceHypotheses.lift_eq` now records the
  monicised lift data shape.
- `HenselSubsetLiftHypotheses.lift_eq` now records the same monicised
  shape.
- Added `monicisedCoreLiftData_liftedFactors_size_eq`.
- Updated the public exhaustive-wrapper membership bridges
  `exhaustiveCoreFactorsWithBound_mem_of_recombinationSearchMod_some`,
  `_of_scaledRecombinationSearchMod_some`, and
  `_of_recombinationSearchMod_first_success` to accept
  `d = Hex.monicisedCoreLiftData core B primeData`.
- Updated the local analytic-obligation constructors and exhaustive
  coverage / irreducibility wrapper signatures to keep `B` as the raw
  coefficient bound, since `Hex.monicisedCoreLiftData` applies
  `Hex.precisionForCoeffBound` internally.
- Updated the choose-prime lift-hypotheses constructor to use the
  monicised lift data and size bridge.

## Current frontier

The original membership goals at the exhaustive wrapper layer are gone.
`lake build HexBerlekampZassenhausMathlib.Basic` still fails before this
issue is complete. The remaining diagnostics are earlier declaration
timeouts around the existing lifted-factor invariant wrappers and their
knock-on unknown constants, plus downstream declarations that cannot
elaborate once those earlier declarations fail.

## Next step

Continue the migration by adding the monicised lifted-factor invariant
wrappers requested by #5673: monicness, positive natDegree, and
injectivity for `Hex.monicisedCoreLiftData core B primeData`, preferably
by reusing the existing `henselLiftData_*` wrappers on
`(Hex.MonicisedCoreData.ofCore core).monicCore`. Then update the
remaining downstream call sites that still rely on the old
`henselLiftData` surface.

## Blockers

No external blocker. The task is larger than the membership bridge alone:
the invariant-wrapper layer must migrate together with the correspondence
surface for the module to build.
