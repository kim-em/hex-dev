**Accomplished**

- Claimed #5774 and extended the BZ split-family scientific schedule in
  `HexBerlekampZassenhaus/Bench.lean` through the requested cascade-trigger
  range.
- Added `prepFallbackProbeInput`, `fallbackProbeSchedule`, and
  `runFactorFallbackProbeChecksum` for explicit `(X-1)...(X-n)` fallback-prime
  probe inputs at degrees `11, 12, 13, 15, 18, 22, 24`.
- Registered the `fallback-probe` input family in `libraries.yml`.
- Re-derived the adjacent cost-model comments for the extended split-family
  registrations and the new fallback-probe registration.
- Verified:
  - `lake build hexbz_bench`
  - `lake exe hexbz_bench list`
  - `.lake/build/bin/hexbz_bench run Hex.BerlekampZassenhausBench.runFactorFallbackProbeChecksum --param-floor 11 --param-ceiling 11 --param-schedule linear --max-seconds-per-call 8 --target-inner-nanos 1000000`
  - `python3 scripts/check_dag.py`
  - `git diff --check`

**Current frontier**

- The BZ bench suite now exposes the prime-search fallback cascade in a
  scheduled bench target without adding failing conformance fixtures.

**Next step**

- Open the PR for #5774 so scheduled HO-5b ratio reporting can consume the new
  fallback-probe target.

**Blockers**

- None.
