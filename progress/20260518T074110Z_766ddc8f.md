# 2026-05-18T07:41Z — feature session 766ddc8f (#4955)

## Accomplished

Added `expandRepeatedPartFactorArray_pow_singleton_of_pos_lc` in
`HexBerlekampZassenhaus/Basic.lean` immediately after the monic
sibling `expandRepeatedPartFactorArray_pow_singleton`. The new
non-monic singleton-arm leaf substrate replaces the `Monic q`
premise with `0 < leadingCoeff q` and carries the weakened
conclusion `(expandRepeatedPartFactorArray rp #[q]).2 = 1` (the
residual-only projection that the mid-layer
`reassemblyExpansionComplete` consumer unfolds to). The proof
routes through the existing array-level public surface
`expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
(#4778) specialised to `coreFactors = #[q]`, `exponents = [k]`,
discharging:

* per-factor `hpos_lc` / `hdegree` from the singleton hypotheses
* `hlen` by `rfl` (`#[q].size = 1 = [k].length`)
* `hnot_dvd_tail` by length-forcing `pre = suf = []` and then
  applying the private helper `irreducible_not_dvd_one`
* `hdecomp` by collapsing the singleton fold with
  `ZPoly.one_mul_zpoly`
* `hfuel` from the singleton hypothesis directly

## Current frontier

The leaf-level singleton-arm `_of_pos_lc` surface is now in
place. The mid-layer companion #4956 (the `_of_pos_lc` sibling
of `reassemblyExpansionComplete_singleton_of_irreducible`) can
now pick Option A (route through this leaf) or Option B (route
directly through the array-level public surface).

## Next step

A separate worker session claims #4956 to add the mid-layer
discharger.

## Blockers

None.

## Verification

* `lake build HexBerlekampZassenhaus.Basic` — clean (only
  pre-existing line-6570 `sorry` warning)
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing
  whnf-timeout pile in `HexBerlekampZassenhausMathlib/Basic.lean`
  (lines 4848, 5129, 5455, 5542, 5589, 5851, 5973, 6121, 6293,
  6274, 6608, 6672, 15242, 15353, 15560, 15606) as the
  `origin/main` baseline; my edit is in a different file and
  cannot induce new Mathlib errors
* `python3 scripts/check_dag.py` — exit 0
* `git diff --check` — exit 0
* `rg "sorry|axiom|native_decide|TODO|FIXME"` shows no new
  entries on the diff against `origin/main`
