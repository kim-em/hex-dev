**Accomplished**

- Claimed #5758 and added conservative `grind` coverage to the Barrett and
  Montgomery context APIs.
- Annotated Nat-level normalization facts with `[simp, grind =]`, equality
  bridge facts with `[grind =]`, and residue-bound facts with explicit
  `[grind =>]` implication patterns.
- Verified:
  - `lake build HexArith.Barrett.Context`
  - `lake build HexArith.Montgomery.Context`
  - `lake build HexArith`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -- HexArith/Barrett/Context.lean HexArith/Montgomery/Context.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returned no matches

**Current frontier**

- The requested Barrett/Montgomery context theorem surface now has direct
  `grind` coverage without changing executable definitions or theorem
  statement shapes.

**Next step**

- Open the PR for #5758.

**Blockers**

- None.
