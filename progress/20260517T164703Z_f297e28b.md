# Session f297e28b — #4761 coprimality substrate + #4733 decomposition

## Accomplished

Worked through three coordination items:

1. **Skipped #4334** (HexLLL Phase 4 inconclusive verdicts) with a
   host-load reason. Load averages were 20.69 / 12.03 / 10.37, which
   reproduces the measurement-infrastructure failure the issue body
   already documents. Re-dispatch belongs on dedicated hardware or a
   quiet host.

2. **Claimed and decomposed #4733** (`henselLiftData_represents_lifted_of_modP`
   assembly). Audited the substrate and identified that the issue text
   underestimates the work: `hensel_unique` needs an `IsCoprime` input
   over `(ZMod primeData.p)[X]` for the subset/complement pair, and the
   PR #4752 closing comment explicitly defers this coprimality piece.
   No subset/complement-shaped coprimality lemma existed in
   `HexBerlekampZassenhausMathlib/`. Filed #4761 as a precursor and
   skipped #4733 with a `Decomposed into #4761 — see comment` breadcrumb
   so the next planner narrows #4733 to the remaining capstone
   (monic-factor handling, integer-side complement `core / factor`,
   `hensel_unique` application, `reduceModPow` conversion).

3. **Claimed and discharged #4761** (the coprimality precursor I just
   filed; I had full context loaded). Landed in commit `3e786b03`,
   ~270 lines added to `HexBerlekampZassenhausMathlib/Basic.lean`:

   * `henselLiftData_liftedSubset_complement_isCoprime_mod_p` — the
     consumer-facing `IsCoprime` over `(ZMod primeData.p)[X]` between
     the lifted-subset product and its complement.
   * Supporting helpers: `modPIndexToLiftedEmbedding_surjective`,
     `map_univ_modPIndexToLiftedEmbedding`,
     `liftedSubsetOfModPSubset_compl_eq` (via `Finset.map_sdiff`),
     `toPolynomial_liftedSubset_map_intCast_zmod_eq_toMathlibPolynomial`,
     `modPFactor_ne_of_ne`, `isCoprime_toMathlibPolynomial_modPFactor_of_ne`.
   * Pulled in `HexHenselMathlib.Correctness`,
     `Mathlib.RingTheory.Coprime.Lemmas`,
     `Mathlib.RingTheory.PrincipalIdealDomain`; added the
     `HexHenselMathlib` entry to `libraries.yml`
     `HexBerlekampZassenhausMathlib.deps`.

   Proof strategy: bridge the lifted-subset products mod `p` to
   `modPFactor` subset products via
   `henselLiftData_liftedSubset_product_congr_mod_base` +
   `Hex.FpPoly.modP_liftToZ`. Expand the products via
   `toMathlibPolynomial_modPFactorProduct` and apply
   `IsCoprime.prod_left_iff` / `IsCoprime.prod_right_iff` to reduce to
   per-pair coprimality. For each distinct-index pair, two distinct
   monic polynomials are non-associated (via
   `Polynomial.eq_of_monic_of_associated`), so the irreducibility +
   non-association + PID coprime-iff-not-dvd chain closes
   (`Polynomial (ZMod p)` is PID once `Fact (Nat.Prime p)` makes
   `ZMod p` a field).

Verification:

* `lake build HexBerlekampZassenhausMathlib.Basic` (38s)
* `lake build HexBerlekampZassenhausMathlib` (full library, clean)
* `python3 scripts/check_dag.py` (clean after `libraries.yml` update)
* `git diff --check` (clean)
* `rg -n "^\s*sorry\s*$" HexBerlekampZassenhausMathlib/Basic.lean` —
  3 entries (242, 11761, 11891), same as `origin/main` apart from line
  shifts; no new theorem-level sorries.

## Current frontier

#4733 narrowed to the capstone now that #4761 supplies the coprimality
input. Remaining work on the capstone:

* Build the integer-side complement (`core / factor`) and its
  `g'.Monic` / `h'.Monic` / natDegree-matching properties.
* Apply `HexHenselMathlib.hensel_unique` with the assembled
  `(g, h, g', h')` inputs.
* Convert the resulting Mathlib `map φ_pk` equality back to the
  executable `Hex.ZPoly.reduceModPow` equality in
  `RepresentsIntegerFactorAtLift`.

## Next step

A planner re-triages #4733 (currently `replan`) — either narrows the
body to the capstone scope and re-opens for worker dispatch, or
decomposes further if the capstone itself is too large for a single
session. The breadcrumb on #4733 calls out the four remaining gaps
explicitly.

## Blockers

None.
