# Session 4eb5c320 — 2026-05-16 05:28 UTC

Worker session, `/feature` then `/work` meta-dispatched. Claimed
#4395 (HexBZ Mathlib: assemble cover-at-min containment from
recombination-candidate support) and decomposed it after determining
the issue's "thin proof" expectation does not match the available
infrastructure.

## Accomplished

- Claimed #4395, oriented in `HexBerlekampZassenhausMathlib/Basic.lean`
  around the partition API (`LiftedFactorSubsetPartition`,
  `cover_at_min`, `support_subset_of_dvd_recombinationCandidate`),
  the `recombinationCandidate` definition, and
  `exists_representingSubset_dvd_recombinationCandidate_of_exactQuotient`.
- Established that the issue's planned proof structure (cover_at_min
  → support lemma → S_min ⊆ T) reduces to showing
  `f_min ∣ recombinationCandidate d T`, which by UFD prime
  decomposition splits into `f_min ∣ quotient` (the case requiring
  contradiction) and `f_min ∣ candidate` (immediately discharges via
  support).
- Determined the `f_min ∣ quotient` case **cannot** be contradicted
  using only the existing partition fields plus
  `exists_representingSubset_dvd_recombinationCandidate_of_exactQuotient`:
  the partition's `support_subset_of_dvd_recombinationCandidate` field
  provides only the forward inclusion (irreducible divisors of the
  candidate have rep `⊆ T`); the reverse inclusion (`T ⊆ ⋃ S_h` for
  `h` ranging over irreducible factors of the candidate) is required
  to close the case and is genuinely missing.
- Decomposed #4395 into #4406 ("HexBZ Mathlib: irreducible factors
  of recombinationCandidate cover T"), which lands the missing
  reverse-inclusion structural fact. #4395 is now blocked on #4406
  and skipped to `replan` for the planner.
- Filed `Decomposed into #4406` comment on #4395, added
  `depends-on: #4406`, and used `coordination skip` per the
  agent-worker-flow decomposition protocol.

## Current frontier

The reverse-inclusion fact (#4406) is the bottleneck. Two viable
approaches surveyed in the sub-issue body:

- **Degree-based**: prove
  `natDegree(toPolynomial (recombinationCandidate d T))
    = ∑_{i ∈ T} natDegree(toPolynomial (d.liftedFactors[i.val]))`
  (centered-lift / primitivePart / normalizeFactorSign all
  preserve degree on nonzero inputs); prove the analogous formula
  for each irreducible factor of the candidate in terms of its
  rep (via Mignotte recovery for monic core); equate sums and use
  positivity of local-factor degrees to conclude `⋃ S_h = T`.
  Positivity likely needs an extra hypothesis (e.g.
  `target ∣ core` or a positivity field on the partition).
- **Recursive**: iterate
  `exists_representingSubset_dvd_recombinationCandidate_of_exactQuotient`
  plus `liftedFactorSubsetPartition_transport` to remove
  candidate irreducibles one at a time. Requires a non-monic
  companion to
  `exactQuotient?_eq_some_of_mul_eq_monic_of_pos_degree`.

## Next step

A planner picks up #4395 from `replan` after #4406 closes (the
parent becomes a one-paragraph successor: apply #4406 at
`J.min' ∈ T` and return the resulting `(f, S)`). A worker can
claim #4406 directly.

## Blockers

- #4406 itself requires non-trivial new infrastructure (degree
  formulas + local-factor positivity, **or** a non-monic
  exactQuotient converse). The sub-issue body documents both
  routes and leaves the choice to the worker who claims it.
