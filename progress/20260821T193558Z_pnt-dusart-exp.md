# PNT+ Dusart exponential family

## Accomplished

- Preserved the exact eight numerical leaves in `Dusart.proposition_5_4a`
  and `Dusart.proposition_5_4b` with ordinary-kernel Hex theorems.
- Checked seven leaves through the source-correlated bounded Taylor table and
  supplied `exp 22` by weakening the stronger checked
  `PntExpPoint.one_e9_le_exp_22` theorem.
- Parsed and correlated the seven source snippets that contain numerical goal
  text. The line-406 invocation contains no such text, so its exact context is
  byte-pinned and mapped to the separately audited `exp 10 < 4e18` row. Added
  wrong-number, wrong-direction, missing, and duplicate tests.
- Updated the inventory and SPEC to record the complete localized rewrite
  without claiming general `IntervalAuto` compatibility.
- Passed the focused conformance build, full `HexConformance` and default
  builds, pinned-source/inventory checks and tests, static/DAG, trust-surface,
  and factor-freshness gates.

## Current frontier

The complete eight-leaf Dusart family is green. The runtime Taylor table has
seven nonduplicated rows; the eighth theorem remains load-bearing through the
stronger package result.

## Next step

Publish the repaired exact branch head and monitor its automatic CI.

## Blockers

None.
