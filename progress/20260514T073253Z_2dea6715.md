# 2026-05-14 07:32 UTC — feature session (2dea6715) on #3953

## Accomplished

- Claimed #3953 (HexBZ: finish factor-entry irreducibility invariant after
  output refinement), assessed scope, and decomposed it into two
  self-contained sub-issues:
  - #3986 — generalise `reassemblePolynomialFactors` via per-coreFactor
    multiplicity expansion (algorithm change + cascading product proofs)
  - #3987 — prove the executable `factorWithBound_entries_irreducible`
    invariant on top of the new algorithm; depends on #3986
- Released #3953 with the `Decomposed into #3986, #3987` breadcrumb so the
  next planner cycle can narrow or close it.
- Claimed #3986, but released without implementation after analysing the
  scope: the algorithm rewrite requires new exact-division infrastructure
  (or an iterated `primitiveSquareFreeDecomposition` layering plus
  divisibility tests), and the proof obligations cascade through
  `polyProduct_reassemblePolynomialFactors` and every downstream slow/fast
  product theorem.
- Posted a concrete implementation plan on #3986 so the next worker has a
  starting point (iterate `primitiveSquareFreeDecomposition` on
  `d.repeatedPart` to produce square-free layers; count layer membership
  per `coreFactor` to recover multiplicities).

## Current frontier

- #3986 is the bottleneck for the irreducibility invariant chain
  (#3987 → #3969 → #3970). Open and unclaimed.
- The existing `reassemblePolynomialFactors` still emits reducible
  residuals for higher-multiplicity inputs such as `(X^2 - 1)^3`.

## Next step

A focused session on #3986. The implementation plan in its comments is a
reasonable starting point; the main external dependency is whether the
project already has Mathlib-free integer divisibility infrastructure on
`ZPoly` or whether that needs to be peeled off as its own sub-issue first.

## Blockers

- None — both sub-issues are unblocked. #3987 simply depends on #3986
  landing first.
