# Progress - 2026-06-17 (session 78a25790, work/review #7656)

## Accomplished

Claimed #7671 first because it was the oldest critical-path feature, then
released it as stale/blocked after its body showed open `depends-on: #7674` and
`depends-on: #7675`. Claimed #7656 and completed the requested review of the
#7640 `CutProjectionHypotheses` projection producer.

Posted a no-defect verdict on #7656 covering:
- projection from retained full prefix rows into `projectedRowSpanInt`,
- hypothesis discipline around true-factor CLD data and tight norm bounds,
- downstream fit for the `_of_cut` irreducibility wrappers.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Lattice` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.

## Current frontier

The #7640 projection packaging appears sound for its stated surface. It uses
`TrueFactorCLDVectorData`, `TrueFactorCLDTightNormBound`, and the prefix survivor
lemma directly, without reintroducing the refuted arbitrary-vector
`CutRetention` route.

## Next step

Continue the active BHKS first-success chain through the claimed feature issues
#7674 and #7675. The remaining work is proving or exposing the instance-level
shortness/cut facts needed at the actual recovery precision.

## Blockers

No blocker for #7656. #7671 should remain behind its declared open dependencies
until #7674 and #7675 land.
