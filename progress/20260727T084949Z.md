# Interval centered vertical review fixes

## Accomplished

- Closed the independent review's soundness-coverage gap by constructing a
  concrete valuation for the fixed SSA program and proving the unconditional
  real centered-product bound from the checked fixture.
- Added discriminating topology and whole-source-table adversarial checks,
  corrected the one-step-short compiled workload, and removed redundant
  certificate-level Core canaries already covered by conformance.
- Made axiom-report parsing fail closed, added `.olean.private` and
  `.olean.server` artifact accounting, and documented the build-only Mathlib
  proof-probe exception with a structural CI lint.
- Corrected lookup-work and one-generation claims in the interval SPEC and
  specified the Mathlib-free rational path as compiled Core `Rat` planning
  plus canonical raw-table validation and transparent kernel replay.
- Verified the focused Core, conformance, compiled-spike, and Mathlib targets;
  the full `lake build`, Python compilation, benchmark lint, and diff checks
  all pass.

## Current frontier

The source and specification fixes are ready to commit. The committed D2 JSON
and prose report still describe the pre-review source hashes, incomplete
artifact set, and overstrong axiom/storage/work-accounting conclusions.

## Next step

Commit this source state, rerun the clean five-sample measurement so provenance
names that commit, update the report from the new JSON, then push and recheck
the draft PR and CI before merging.

## Blockers

None.
