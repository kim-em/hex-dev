# Progress: #6845 — van Hoeij forward correspondence surface

## Accomplished

Claimed #6845 after the open directive #2564 was reported blocked by
`coordination claim`.

Added the bad-vector forward correspondence surface in
`HexBerlekampZassenhausMathlib/BadVector.lean`:

- `ExecutableBadVectorWitness.ForwardCorrespondence`, tying a corrected
  auxiliary-polynomial identity and a nonunit rational gcd to membership in
  `BHKS.trueFactorIndicatorLattice`.
- `ForwardCorrespondence.mem_indicator`, the direct forward form.
- `ForwardCorrespondence.coprime`, the contrapositive coprimality form used by
  bad-vector setup construction.
- `isBhksBadVectorSetup_of_forward`, deriving the old
  `coprime_input_aux_over_rat` setup field from `ForwardCorrespondence` plus
  `v ∉ W`, while leaving the resultant divisibility and degree positivity
  fields explicit.

## Current frontier

The forward mathematical obligation is now named and separated from the raw
bridge data. Follow-up consumers can use `ForwardCorrespondence.coprime` /
`isBhksBadVectorSetup_of_forward` instead of carrying rational coprimality as
an unrelated assumption.

## Next step

Use this surface in the #6846/#6835-style coprimality consumer: supply a
`ForwardCorrespondence` package from the actual true-factor/support
factorization data, then derive the coprimality clause by contradiction from
`v ∉ BHKS.trueFactorIndicatorLattice trueSupports`.

## Blockers

`lake build HexBerlekampZassenhausMathlib` still fails at the pre-existing
`HexBerlekampZassenhausMathlib/Basic.lean:16087` `scale` to `dilate`
mismatch in `scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`.
The affected target `lake build HexBerlekampZassenhausMathlib.BadVector`
builds successfully.
