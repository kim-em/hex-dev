# Precision-indexed logarithm landing

## Accomplished

- Replayed the bounded 20-/50-decimal `Real.log 2` provider and proof frontend
  canary onto exact main `d8aee56fdcbc5f7f02a6606edf2a490e239a0c02`.
- Preserved the current PNT+ Table 12 ordinary/logarithmic experiments and the
  huge-argument cosine experiment in all aggregate and conformance targets.
- Refreshed the proof-only Lake freshness exemption to the exact combined
  `lakefile.lean` blob.
- Rebuilt the focused logarithm, cosine, and Table 12 targets and the full
  `HexIntervalExperiment`, `HexIntervalMathlibExperiment`, and `HexConformance`
  target set successfully.
- Re-ran static, trust, Mathlib-free bench, factor freshness, and source-pinned
  PNT inventory checks successfully.
- Made each certificate's recorded term count the actual argument to the
  kernel-checked partial-sum and tail theorems, so recording too few terms
  breaks the endpoint proof rather than only failing table lookup.
- Described the exact two-sided `1/2 * log ((1+x)/(1-x))` Mathlib theorem shape
  used at `x = 1/3` instead of abbreviating it as a different series theorem.

## Current frontier

The precision request, selected proof-relevant term count, exact input, and
rational enclosure are all replay-visible and kernel-checked for the bounded
20- and 50-decimal `log 2` provider. The repaired rebased head is ready for
independent review and CI.

## Next step

Run a fresh exact-head review and exact CI, repairing only blocking correctness
or specification-honesty defects before merge.

## Blockers

None. The first build attempt encountered a full host filesystem while Lake was
cloning Mathlib; reusing the already checked-out compatible package tree and
removing only generated build data allowed all proof checks to complete.
