# Progress - 2026-06-17 (session a68be244, feature #7689)

## Accomplished

Discharged the `sorry` for `quadraticHenselStep_unique_mod_pow_two_mul`
(`HexHenselMathlib/Correctness.lean`), the Mathlib-facing quadratic-step
uniqueness obligation. Proved it as a specialisation of the already-proven
abstract `hensel_unique` at level `2 * k`:

- monicity hypothesis from `quadraticHenselStep_monic` (needs `1 < p^k`, derived
  from `Nat.Prime p` + `0 < k` via `Nat.one_lt_pow`);
- the product equality from `quadraticHenselStep_factor_correct` at modulus
  `m * m = p^k * p^k`, bridged to `p^(2*k)` with `hmm : p^k * p^k = p^(2*k)`
  (`two_mul` + `pow_add`) and a `hmm ▸` dependent rewrite;
- all remaining hypotheses (`hg'`, `hdeg`, `hprod'`, `hg1`, `hh1`, `hcop`)
  pass straight through.

The premise check (issue + project doctrine) held: `(p^k)*(p^k)` and `p^(2*k)`
are the same `ZMod` modulus, so the bridge is an identity, not a weakening.

One structural change beyond the directive: the target theorem was defined
*before* `hensel_unique` in the file, so it could not reference it. Relocated the
whole theorem block to immediately after `hensel_unique` (no logic change; pure
move). Dependencies `quadraticHenselStep_monic` / `quadraticHenselStep_factor_correct`
already sit above that point.

Verification:
- `lake build HexHenselMathlib.Correctness` — clean (target no longer in the
  `sorry` list).
- `lake build HexHenselMathlib` — clean (1538 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line forbidden-token scan — no sorry/axiom/native_decide/TODO/FIXME.

## Current frontier

`HexHenselMathlib/Correctness.lean` now carries three remaining `sorry`s, all
documented out-of-scope for #7689: `hensel_correct` (line 38), `hensel_degree`
(line 81), `multifactorLift_eq_multifactorLiftQuadratic` (line 468). These need
the linear-loop-invariant substrate (#7690) that is not yet exposed.

## Next step

#7690 exposes the linear Hensel loop-invariant discharge lemmas, which unblocks
`hensel_correct` / `hensel_degree`.

## Blockers

None. The worktree carries pre-existing unrelated `.claude/*` modifications from
before this session; not touched or staged.
