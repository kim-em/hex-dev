# 2026-05-05 22:32 UTC — work session (ee5bb95a)

## Accomplished

Claimed #2244 first because it was a human-oversight item, then marked it
stale after confirming `HexConway/Basic.lean` already has theorem bodies for
the named obligations and no remaining `axiom` declarations.

Claimed #2386 and added
`HexMatrixMathlib.bareissExactDiv_borderedMinor_of_mul_eq` in
`HexMatrixMathlib/Determinant.lean`. The lemma packages a Desnanot-style
bordered-minor product identity as the `exactDiv = next minor` premise needed
by `Hex.Matrix.stepMatrix_borderedMinor_update`.

Created follow-up #2399 for the remaining indexed Desnanot-Jacobi
specialization and left the required decomposition breadcrumb on #2386.

## Current frontier

The exact-division algebra side of the Bareiss bordered-minor recurrence is
available. The remaining work is to prove the Mathlib-side determinant identity
specializing `desnanot_jacobi` to Hex bordered-minor indexing, then compose it
with the new exact-division bridge.

## Next step

Work #2399: prove the Desnanot-Jacobi specialization for
`matrixEquiv_borderedMinor`/`det_borderedMinor_eq_submatrix_det` and use it to
produce the `hdesnanot` shape expected by
`bareissExactDiv_borderedMinor_of_mul_eq`.

## Blockers

None. Verification for this partial subset passed with only pre-existing
warnings:

- `lake build HexMatrixMathlib.Determinant`
- `lake build HexMatrixMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
