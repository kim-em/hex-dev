# Drop dead `_hsquareFree` parameter from `prod_berlekampFactor`

Issue: #4614 (feature, agent-plan).

## Accomplished

- `HexBerlekamp/Factor.lean`: removed the dead `_hsquareFree` premise from
  `prod_berlekampFactor`. Proof body is unchanged (it was already a
  `simp [Factorization.product_def] <;> exact factorProduct_berlekampFactor`).
  Updated the docstring: "for square-free monic inputs" → "for any monic
  input".
- `HexBerlekampMathlib/Basic.lean`
  (`irreducible_of_berlekampFactor_factors_length_le_one`, nil-cons case):
  contracted the `hg_eq` proof to call `factorProduct_berlekampFactor`
  directly, skipping the now-redundant `Factorization.product_def` rewrite.
  This is the only external call site of `prod_berlekampFactor` and now no
  longer references it. `hsquareFree` is still passed to the sibling
  `irreducible_of_mem_berlekampFactor` call (line 352), which genuinely
  needs it, so the enclosing theorem's signature is unchanged.
- `SPEC/Libraries/hex-berlekamp.md`: dropped the `(hf : squareFree f)`
  premise from the prose theorem statement so the SPEC matches the API.
- `HexBerlekampZassenhausMathlib/Basic.lean`: updated a stale doc comment
  on `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm` that
  described `prod_berlekampFactor` as needing square-freeness. The body
  of that theorem already calls `factorProduct_berlekampFactor` directly
  (line 4645 / now 4644), so the comment was already at odds with the
  proof; the parameter drop made that explicit.

After the change, `prod_berlekampFactor` is referenced only at its
definition site in `HexBerlekamp/Factor.lean:481`; no other `.lean` file
mentions it.

## Verification

- `lake build HexBerlekamp.Factor` — OK.
- `lake build HexBerlekampMathlib.Basic` — OK (no new sorries).
- `lake build HexBerlekampZassenhausMathlib.Basic` — OK (no new sorries).
- `lake build` (full, 244 jobs) — OK.
- `python3 scripts/check_dag.py` — OK.
- `git diff --check` — OK.
- `rg -n "prod_berlekampFactor" -- '*.lean'` — only the definition site.
- Sorry inventory delta on touched files: 0.

## Current frontier

The "underscore-prefixed dead-parameter" follow-up from PR #4574 / review
#4581 is now landed. Nothing else from that review carved out as a
separate issue is still on this branch.

## Next step

None on this branch — PR is on auto-merge.

## Blockers

None.
