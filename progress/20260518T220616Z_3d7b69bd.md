# 2026-05-18 22:06 UTC - forty-fifth Phase 4/5 checkpoint (3d7b69bd)

Claimed summarize issue #5121 and recorded this checkpoint covering
the merged-PR wave since #5078 closed at `2026-05-18T20:05:29Z`.
The recomputed window at summary time includes PRs merged through
`2026-05-18T22:04:46Z`.

```
gh pr list --state merged --limit 100 \
    --json number,mergedAt,title \
    --jq '.[] | select(.mergedAt > "2026-05-18T20:05:29Z")
          | "#\(.number) \(.mergedAt) \(.title)"'
```

= **18 new PRs merged** in the checkpoint window.

## `coordination orient` snapshot

```
=== DIRECTIVES (highest priority) ===
#2637 HexBerlekampZassenhaus: Factorization record API + sign/multiplicity bugs
#2567 HO-4: Formalise BHKS Theorem 5.2 (factorFast termination, leaf theorem)
#2564 HO-1: BZ recombination - rollback and rewrite to van Hoeij CLD

=== Unclaimed work items ===
(none)

=== Claimed work items ===
#5121 Summarize forty-fifth Phase 4/5 checkpoint (this session)

=== Issues with open PRs ===
#5119 BZ review: audit #2637 Factorization sign and multiplicity surface
#5112 HexPoly Phase 6: add DensePoly leadingCoeff nonzero helper
#5085 BZMathlib review: post-#5081 fpPoly isZero=false helper audit
#4919 BZMathlib: primitive-unscaled mem_T abstract-bound sibling

=== Open pull requests ===
#5122 [agent/69b35728] review: audit BZ Factorization sign and multiplicity surface
#5120 [agent/36c767d0] review: audit fpPoly isZero helper cluster
#5118 [agent/a540951f] feat: add primitive mem_T abstract-bound sibling
#5114 [agent/62909d54] HexPoly Phase 6: add DensePoly leadingCoeff nonzero helper
#2656 [spec/real-roots-and-sturm] spec: hex-real-roots and hex-sturm (planned, draft)

=== PRs needing attention ===
(none reported by coordination orient)
```

Blocked theorem chains remain concentrated in BZ/HO work:

* BZ abstract-bound chain:
  #4951 -> #4929 -> #4925 -> #4919, and
  #4934 -> #4931 -> #4927 -> #4924.
  #4924 now displays as `[Blocked on ]` in `coordination orient`;
  its predecessor #4918 has landed as PR #5116, so this looks like a
  queue-state cleanup target for the next `coordination check-blocked`
  pass rather than a new theorem blocker.
* BZ lifted-partition / executable-factor chain:
  #4832, #4831, #4830, #4825, #4821, #4819, #4818, #4680,
  plus #4880 blocked by directive #2564.
* HO residual chain:
  #4172 -> #4170 -> #2567.

`python3 scripts/status.py` still reports the broad Phase 6 proof-polishing
frontier for core libraries, Phase 5 for HexGramSchmidt / HexPolyZ /
HexGfqField / HexMatrixMathlib, Phase 4 for several Mathlib bridge
libraries, and HexBerlekampZassenhausMathlib blocked for Phase 3 until
HexBerlekampZassenhaus advances.

## Headline - BZMathlib helper extraction and audit cadence

Most landed work continued the BZMathlib proof-surface cleanup in
`HexBerlekampZassenhausMathlib/Basic.lean`, with the established pattern:
extract one small theorem, rewire all matching inline proof blocks, then
audit with explicit coverage / framing / cross-file invariants.

The `fpPoly_dvd_trans` cluster closed cleanly:

* **#5083** extracted `fpPoly_dvd_trans` and rewired 8 FpPoly-level
  divisor-transitivity sites, including 3 genuine sites beyond the
  planner's initial estimate.
* **#5087** audited the post-#5083 tree and recorded PASS across all
  three invariants: 8 routed call sites, contract-first helper framing,
  and no cross-file drift.

The follow-on FpPoly divisibility cleanup also landed:

* **#5095** extracted `fpPoly_mul_dvd_mul` and collapsed the
  `hrawGcd_sq_dvd_prod` product-of-divisors witness in the square-free
  contradiction proof.
* **#5103** audited that cluster and confirmed exactly one declaration
  and one intended call site, with remaining product-witness greps
  classified as helper internals or wrong-layer ZPoly sites.

The FpPoly nonzero/leading-coefficient thread advanced in parallel:

* **#5086** extracted
  `fpPoly_isZero_false_of_dvd_of_isZero_false` and rewired the two
  Shape-B divisibility-to-nonzero sites after its conflict repair landed.
* **#5092** extracted
  `fpPoly_leadingCoeff_ne_zero_of_size_pos` and rewired 9 inline
  leading-coefficient nonzero derivations, including 2 cross-file uses in
  `IntReductionMod.lean`.
* **#5098** audited the leading-coefficient cluster and recorded PASS:
  7 Basic call sites, 2 IntReductionMod call sites, public theorem
  visibility justified by cross-file use, and no Recovery drift.

The monic modular-image thread also tightened:

* **#5101** extracted
  `monicModularImage_eq_scale_inv_leadingCoeff_of_isZero_false` and
  rewired 4 scale-equation sites.
* **#5109** audited that scale helper and found the cluster clean.
* **#5110** extracted
  `monicModularImage_dvd_self_of_isZero_false` and rewired the 2
  monic-image divisibility sites in the Berlekamp-form proofs.

## BZ abstract-bound chain

The abstract-bound theorem chain made its first visible progress past
the repaired #4902 base:

* **#4917** landed after repair, adding abstract-bound siblings for
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
  in both monic and primitive-unscaled forms, while preserving default-bound
  wrappers.
* **#5116** added the monic
  `mem_T_iff_exists_irreducibleFactor_representingSubset_of_bound`
  sibling and restored the original unsuffixed theorem as the
  `defaultFactorCoeffBound_valid` wrapper.
* **#5118** is the open PR for the parallel primitive-unscaled `mem_T`
  abstract-bound sibling. Its merge should unblock the #4925 branch of
  the chain.

The monic successor #4924 appears ready for queue unblocking once the
coordination metadata drops the now-empty dependency, because its direct
predecessor #4918 closed via #5116.

## Monic-route cluster inversion

The `monic_primitive_sign_normalized_of_monic` cluster inversion closed
and was audited:

* **#5089** split first-class proofs for `zpoly_primitive_of_monic` and
  `zpoly_normalize_factor_sign_of_monic` out of the umbrella theorem,
  leaving the umbrella as a thin package.
* **#5093** audited the inversion and confirmed the expected PASS x3
  shape: unchanged downstream call sites, no lost umbrella theorem, and
  helper placement consistent with the earlier hoist work.

This completes the inversion path that the previous checkpoint described
as feasible but not yet executed.

## Non-BZ landed work

Four non-BZ items landed in the window:

* **#5105** reviewed the HexArith `UInt64` wide-word API. The API itself
  passed, but the review recorded a downstream executable-path deviation:
  Montgomery code still has product-splitting sites that should use
  `UInt64.mulFull`.
* **#5108** promoted the HexPolyFp
  `leadingCoeff_ne_zero_of_isZero_false` helper to public
  `fpPoly_leadingCoeff_ne_zero_of_isZero_false` and rewired its 4 local
  uses.
* **#5115** bumped HexMatrix through its Phase 5 exit after successful
  `lake build HexMatrix`, `lake build HexMatrix.Conformance`, DAG check,
  and no-placeholder grep. `scripts/status.py HexMatrix` now reports
  Phase 6 as next.
* **#5117** added HexArith `powMod` edge-case simp lemmas and landed the
  corresponding Phase 6 proof-polishing item.

The open #5114 PR is the sibling HexPoly Phase 6 helper work:
`DensePoly` leading coefficient nonzero polish.

## Verification and residual risk

This summarize session changed only this progress file. No Lean source,
SPEC file, top-level `PLAN.md`, `AGENTS.md`, or CI workflow was edited.

Residual queue risk is orchestration state rather than proof content:
#4924 still appears in the blocked list with an empty dependency rendering.
If `coordination check-blocked` does not release it automatically, the next
planner cycle should inspect that issue body before dispatching more monic
abstract-bound successors.

## Accomplished

Wrote the forty-fifth checkpoint summary for issue #5121, covering the
18 merged PRs after #5078 closed and the queue/frontier state at summary
time.

## Current frontier

The fresh queue has no unclaimed non-directive work. Active work is in
open PRs #5122, #5120, #5118, and #5114, while #2656 remains a draft
planned SPEC PR.

## Next step

Let the open PRs merge or route failures through repair. Then unblock or
inspect #4924 if it remains labelled blocked after `coordination check-blocked`.

## Blockers

None for this summarize task. The only observed residual blocker is the
coordination-state issue on #4924 described above.
