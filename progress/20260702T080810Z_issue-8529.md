# 2026-07-02 — #8529: classical recombination candidate prefilter

Worker session, branch `issue-8529` (wt worktree).

## Accomplished

- Added `Hex.scaledCandidatePrefilter` (plus `selectedDegreeSum`,
  `selectedProductResidue`) to `HexBerlekampZassenhaus/Basic.lean` and wired
  it into `scaledRecombinationSmartCandLoop`: each subset now passes a
  degree test and a trailing-coefficient test (running modular reduction,
  O(subset size)) before the loop forms the full
  `polyProduct`/`centeredLiftPoly`/`dilate`/`primitivePart`/`exactQuotient?`
  pipeline. Rejected subsets decrement the budget exactly as before, so
  `candidatesTried`, outputs, fixtures, and traces are all byte-identical.
- Soundness machine-checked in the Mathlib layer:
  `scaledCandidatePrefilter_eq_true_of_exactQuotient?_some`
  (`HexBerlekampZassenhausMathlib/Basic.lean`) proves the prefilter accepts
  every subset whose candidate passes `exactQuotient?` (the trailing test
  absorbs the dilation content through the product's top coefficient; the
  degree test is guarded by `coreLc ≠ 0` and a nonvanishing
  leading-coefficient residue). The #8413 completeness apparatus
  (`smartCandLoop_none_budget_zero`, `smartCandLoop_covers_of_bound`)
  consumes it to dismiss the new prefilter-rejected branch on the covering
  subset (`by_cases hpre` + `swap` keeps the original proof bodies intact);
  the five executable candidate-loop theorems gained mechanical recurse
  branches.
- Measured (compiled A/B probe on the issue's SD5(x)·SD5(x+1)
  budget-exhaustion case, deg 64, r = 32, 262144 candidates): **6.7 s with
  the prefilter** vs the issue's measured 143–193 s compiled baseline
  (~25×); identical result and stats (`tried=262144`, `exhausted=true`).
- Verification: full `HexBerlekampZassenhaus` target and full
  `HexBerlekampZassenhausMathlib` library green; fixtures byte-identical;
  trace gate 50/0; FLINT oracle 100/0; `hexbz_bench verify` 18/18.
- Filed #8535 for deliverable 3 (cap the size-level subset materialization
  by the running budget), with the completeness-proof shape it needs.

## Current frontier

Deliverables 1–2 of #8529 complete; PR open. Deliverable 3 is #8535.

## Next step

The lattice-tier conformance follow-up mentioned by #8529 ("directly
cheapens the merge-required lattice-tier conformance case") can now use the
cheap classical burn.

## Blockers

None. One instance-path note for successors: proofs about the prefilter's
`coreLc ^ degSum` must stay on executable-file-elaborated lemmas
(`Hex.ZPoly.coeff_dilate`) or defeq-tolerant steps; `show 1 * x = x`
against `coreLc ^ 0 * x` is *not* accepted (use `simp` there), and the
`Bool.or` chain in the prefilter is left-associated
(`Or.inl (Or.inl (Or.inl _))` for the first guard).
