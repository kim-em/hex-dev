# issue #9986: collapse the checked tier onto the fast surface

Session type: feature (pod worker, `claude -p`), 2026-09-05 UTC.
Branched from the head of `issue-9982` (#9985) rather than `main`,
since the only dependency is `Nauty.certifyCanon?_isSome`.

## Done

- `Nauty.certifyCanon`, `certifyCanon?_eq`, `certifyCanon_form`,
  `certifyCanon_relabel`, `canonicalize?_eq`, `canonicalize?_isSome`
  in `HexGraphIso/Nauty/SearchOutcomeCertify.lean`. They could not go in
  `Complete.lean`: the totality proof transitively imports it.
- `HexGraphIso/Ops.lean` is one tier: `canonicalize` is
  `(Nauty.canonicalize? G).get _`; `canonicalize_eq_certifyCanon` and
  `canon_eq_specCanon` carry every former `Checked` theorem to the short
  names; `checkCanon_sound`/`canon?_eq_some` conclude in `canon`.
  `Hex.Graph` mirrors this in `Uncolored.lean`. `Checked` is removed
  (not aliased): the released repos are mirrors of this tree, and the
  in-tree consumer (`HexGraphIsoMathlib/Encode.lean`) is updated.
- Deleted `permsOf`, `bruteCanon?`, `checkCanon_of_achieved`,
  `bruteCanon?_isSome`, `canonicalizeSpec` and its theorems.
- Drivers drop the checked legs (`runHexCanonChecked*`,
  `runIsIsoChecked12`, cactus `checked_ns`, profiler `canonchecked`,
  the emitters' fast/checked cross-checks); conformance guards compare
  `canonicalize` with `Nauty.certifyCanon?` instead.
- SPEC, README, library docstring, manual chapters, Mathlib SPEC/README.
- Cactus sweep refreshed (fingerprint `152d306db788`, chungus2); the
  sweep took about 15 minutes. Fixtures byte-identical.

## Verification

`lake build` of every graph-iso consumer green (11096 jobs, incl.
`HexConformance`, `HexReleaseTests`, `HexGraphIsoProofProbe`,
`HexManual`); `hexgraphiso_bench verify` passes;
`check_graphiso_sweep_freshness.py` passes; line-count check passes.

## Second opinion (Codex)

Confirmed the transport is complete and the new chain sound. Acted on:
SPEC layer bullets had been turned into present-tense claims about a
shared acceptance predicate and a policy-parameterised recursion the
landed proof does not use (restored as design constraints, with a
paragraph on the proof as landed); legacy plot scripts now label the
pre-collapse replay series and skip it when absent. The manual's
timing section is #9975, done as the stacked PR #9988 from the same
sweep.

## Remaining

PR #9987 (this issue) is stacked on #9985; #9988 (#9975) on #9987.
Both need a rebase as their bases merge.
