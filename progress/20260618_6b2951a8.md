# Progress — 2026-06-18 (session 6b2951a8, /work → /feature #7924)

## Accomplished

Delivered the reusable recovered-lift Hensel-semantics producers for the monic
`toMonicLiftData` coordinate (#7924), in `HexBerlekampZassenhausMathlib/LiftBridge.lean`.

Three additive theorems, all derived from the `toMonicPrimeData?` selection
witness rather than re-bundled as caller hypotheses:

- `toMonicLiftData_two_le_p`: `2 ≤ (toMonicLiftData core B primeData).p` — the
  `hp` side condition. From `toMonicPrimeData?_prime`.
- `toMonicLiftData_one_lt_modulus`: `1 < p ^ k` — the `hk` side condition. From
  the prime lower bound plus `1 ≤ precisionForCoeffBound`.
- `toMonicLiftData_liftedFactor_hensel_semantics` (deliverable 2): for every
  lifted-factor index `i`, `∃ g, Monic (liftedFactor d i) ∧ 0 < degree ∧
  congr (toMonic core).monic (liftedFactor d i * g) (p ^ k)`. This is exactly
  the per-index `hfac` body of
  `factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift`. The
  complement `g` is `liftedFactorProduct d (univ \ {i})`; the congruence is the
  per-index specialisation of
  `henselLiftData_liftedFactorProduct_subset_complement_congr_core`, factored
  out of the `selected_congr` field of `trueFactorLiftSemanticsOfToMonicSubset`.
  Monicity / positive degree reuse the existing
  `toMonicLiftData_liftedFactor_monic_of_monicPrimeData` /
  `_natDegree_pos_of_monicPrimeData`.

The remaining two endpoint side conditions are already one-liners in tree and
need no new lemma: `hf_lc` (`leadingCoeff (lift S).f = 1`) is the monicity of
`(toMonic core).monic` (`toMonic_monic_isMonic_of_pos_degree`, since `Monic` is
defeq `leadingCoeff = 1`); `hfactor_monic` (`(toPolynomial (lift S).factor).Monic`)
is `HexHenselMathlib.toPolynomial_monic_of_dense_monic` applied to the recovered
factor's monicity, which the family producer
`recoveredLift_family_of_indicatorCandidates` (#7913) already establishes
internally (`hcl_monic`). These are family-producer/`#7921`-wiring facts, not
`toMonicPrimeData?`-derived, so they live at the integration site.

## Verification

- `lake build HexBerlekampZassenhausMathlib.LiftBridge` → green.
- `lake build HexBerlekampZassenhausMathlib` → 3790 jobs, `Build completed
  successfully`, 0 errors (includes the `PartitionRefinement` endpoint module).
- `python3 scripts/check_dag.py` → DAG OK.
- `git diff -U0 … | grep` → no added `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.
- `git diff --check` clean.

## Current frontier

The lifted-factor and modulus side conditions of the fast `h_raw` disjunct
endpoint are now producible from `toMonicPrimeData?` + core facts, independent of
the candidate fold.

## Next step

#7921 (parent): wire `recoveredLift_family_of_indicatorCandidates` into
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift`, using
`toMonicLiftData_liftedFactor_hensel_semantics` for `hfac`, the two modulus
lemmas for `hp`/`hk`, and the in-tree monicity bridges for `hf_lc`/`hfactor_monic`.
The remaining substantive bridge is #7923 (emitted-indicator support family →
subtype-indexed `trueSupports`).

## Blockers

None for this issue.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
