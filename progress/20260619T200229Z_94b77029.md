## Accomplished

Claimed #8094 and completed the Phase 6 grind-automation sweep for
`HexGFqMathlib/Basic.lean`. Promoted six bare equality `@[simp]` lemmas to
`@[simp, grind =]`:

`FpPoly.coeff_ofIndexBelowDegree_of_lt`,
`FpPoly.coeff_ofIndexBelowDegree_of_ge`,
`FpPoly.coeffIndex_ofIndexBelowDegree`,
`FpPoly.ofIndexBelowDegree_coeffIndex`,
`FiniteField.fintype_card`,
`GFq.conwayPoly_degree`.

Left `GFq.fintype_card_eq_pow` as `@[simp]` only. Although it is an equality,
Lean rejected its `grind` registration because the generated pattern
`Fintype.card (@GFqField.FiniteField #4 #2 _ _ _ _)` cannot instantiate the
theorem parameters `n` and `h`.

## Current frontier

`lake build HexGFqMathlib` is green. `HexGFqMathlib.Basic` elaborated in 2.4s.
The build also covered direct downstream consumers `HexGFqMathlib.GF2q` and the
aggregate `HexGFqMathlib` module. Existing warnings replayed from
`HexGF2Mathlib/Basic.lean`; none were introduced in the touched file.

`python3 scripts/check_dag.py` and `git diff --check` both passed. Added-line
grep over `HexGFqMathlib/Basic.lean` found no new `sorry`, `axiom`,
`native_decide`, `TODO`, or `FIXME`.

## Next step

None for #8094. Remaining unclaimed fast-branch infrastructure issues include
#8096 and #8097.

## Blockers

The requested `hex-lean-mathlib-boundary` skill was not present in the local
skills tree, so this session used the issue instructions and existing
Mathlib-bridge file conventions directly.
