# Progress: BZMathlib `liftedFactorSubsetPartition_prefix_none_of_bound` (monic)

Issue: #4931. Branch: `agent/8e364447`.

## Accomplished

- Added `liftedFactorSubsetPartition_prefix_none_of_bound` in
  `HexBerlekampZassenhausMathlib/Basic.lean` as the abstract-bound
  monic sibling of `liftedFactorSubsetPartition_prefix_none`. Takes
  `(B' : Nat)`, `hvalid : ∀ g, g ∣ core → ∀ i, (g.coeff i).natAbs ≤ B'`,
  and `hprecision : 2 * B' < d.p ^ d.k` in place of the core-shape
  `defaultFactorCoeffBound core` precision constraint (Option A from
  the issue body).
- Proof body mirrors the original: wrapper-decomposition via
  `liftedSubsetSplit_prefix_exists_mem_sdiff_of_matches`, case-split on
  `shouldRecordPolynomialFactor` and `exactQuotient?`, contradiction via
  `pairwise_disjoint` / `unique_up_to_associated` on the shared `J.min'`.
  The only non-cosmetic change is the inner `some` branch, which builds
  `hvalid'_T : ∀ g, toPolynomial g ∈ normalizedFactors (toPolynomial
  (recombinationCandidate d T)) → ∀ i, (g.coeff i).natAbs ≤ B'` from
  `hvalid` by chaining `g ∣ recombinationCandidate d T ∣ target ∣ core`
  and threads it (with `B'`, `hprecision`) to
  `coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_bound`
  (#4927). The `hvalid'_T` construction matches the discharge block in
  the scaled sibling (`_scaled_of_bound` at line 13018 pre-change).
- Rewired the existing `liftedFactorSubsetPartition_prefix_none` as a
  thin wrapper that instantiates `B' := defaultFactorCoeffBound core`
  and discharges `hvalid` via `defaultFactorCoeffBound_valid core
  hcore_ne` directly (the universal `g ∣ core` shape matches the lemma
  signature exactly). The wrapper preserves the existing theorem
  signature; the downstream call site (now at line 14055, inside
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`)
  type-checks unchanged.
- Placement: file convention here is `_of_bound` immediately before its
  thin wrapper (matches the `_scaled_of_bound`/`_scaled` pair at
  lines 13018/13174 pre-change and the cover-extraction pair at
  10922/10970). The issue's "immediately following" wording would have
  produced a forward reference, which Lean 4 does not allow without
  `mutual`. The original docstring stays attached to the wrapper.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — no new errors. The
  pre-existing whnf timeouts (lines 4927, 5161, 5469, 5556, 5603, 5840,
  5939, 6084, 6228, 6247, 15795, 15906), the `cases failed` at 5840,
  and the unknown-kernel-constant errors at 6562, 6626, 16094, 16140
  are unchanged in count and identity from the documented baseline in
  `progress/20260518T034405Z_d1e63d44.md` (line numbers shift forward
  by the size of the inserted sibling, which is consistent with the
  diff).
- `lake build HexBerlekampZassenhausMathlib` — same 16 unique pre-existing
  errors, no new errors.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean |
   grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — empty.

## Current frontier

The monic prefix-none `_of_bound` sibling now lifts the A2 abstract-bound
propagation to the first theorem above the `coverAtMin_*` cover-at-min
layer on the unscaled monic side. The primitive unscaled flavour at
(former) line 12389 (blocked on #4929) and the scaled primitive flavour
at (former) line 12491 (blocked on #4930) are out-of-scope follow-ons
that share the same binder-scope wrinkle — the Option A design used
here can be reused verbatim.

## Next step

Out of scope here. The `recombinationSearchModAux_some_*_of_bound` layer
at (former) line 13128 is the next layer up; per the issue body, that
issue is blocked on this PR.

## Blockers

None for this issue.
