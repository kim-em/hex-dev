# Accomplished

- Replayed the exact branch-proof canary onto exact local #9232 boundary
  `e3bacd05331fd42131053054f710c297cf4790ed` while preserving all prior
  arithmetic, policy, branch, PNT, Table 12, and centered-function work.
- Audited the live backward contraction, retained post-run split snapshot,
  exact left and right child inputs, branch seeding, child trace replay, root
  contraction replay, split coverage, and empty refutation.
- Confirmed that the left proof consumes `[1/4,1/2]`, the right refutation
  consumes `(1/2,3/4]`, and sibling-substitution mutations are rejected in
  both directions.
- Preserved the SPEC's explicit distinction between this plumbing canary and
  the still-future theorem whose mathematics itself requires branching.
- Resolved the Lake/SPEC overlaps without dropping inherited content and
  refreshed the exact proof-only Lake exemption.
- Passed the focused 868-job exact-branch build and structural, trust, Phase 4,
  PNT inventory, factor-freshness, diff, and banned-proof checks.

# Current frontier

The local #9233 candidate demonstrates kernel proof emission from a live exact
post-contraction branch tree. Both child sides are proof-relevant, while the
final theorem is honestly retained as a conformance canary rather than the
future mathematically branch-essential example.

# Next step

After #9232 lands, rebase this candidate once onto its final merged lineage,
refresh the Lake exemption, then push and obtain exact-head review and CI.

# Blockers

None. The first build attempt hit local disk exhaustion only while writing the
final build hash; removing disposable generated IR and rerunning succeeded.
