# HexPolyMathlib Phase 6 — Euclid bridge automation review

Session: `cbee716c` (review, issue #5586)

## Accomplished

Reviewed the seven public theorems in `HexPolyMathlib/Euclid.lean` against
the Phase 6 automation-annotation criteria. Decisions:

- `toPolynomial_xgcd_bezout_raw` — promoted to `@[simp]`. Direction is a
  contraction (4-term Bezout combination collapses to the named raw gcd)
  and the LHS pattern is narrow (requires the literal
  `Hex.DensePoly.xgcd p q` with both coefficient projections).
- Other six theorems left un-annotated:
  - `equiv_xgcd_bezout_raw`: the `equiv → toPolynomial` simp rewrite
    (`equiv_apply`) already reduces its LHS to the `toPolynomial`-side
    Bezout shape, which the new `@[simp]` on `toPolynomial_xgcd_bezout_raw`
    then closes. A redundant `@[simp]` on the equiv-side variant would
    duplicate work and live off the simp-normal form.
  - Four `Associated` lemmas: not equalities, no canonical rewrite
    direction. Stay exposed as named lemmas.
  - `toPolynomial_xgcd_gcd_associated`: associatedness fact.

Incidental linter clean-up triggered by the new annotation:

- `equiv_xgcd_bezout_raw` proof simplified from
  `simpa using toPolynomial_xgcd_bezout_raw …` to `simp` (the lemma is
  now in the simp set).
- Private helper `dense_dvd_of_toPolynomial_dvd`: replaced
  `simpa [equiv_symm_apply] using equiv.symm.map_mul q r` with `simp`
  (a pre-existing `simpa` linter warning; the `ofPolynomial_mul` simp
  lemma already proves the step).

## Verification

- `lake build HexPolyMathlib.Euclid` — clean, no warnings.
- `lake build HexPolyMathlib` — clean.
- `lake build HexBerlekampMathlib` — clean (downstream of the umbrella).
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — fails, but
  the failure is in `HexBerlekampZassenhausMathlib.Basic` (whnf timeouts +
  cascading unknown-constant kernel errors at lines 16142/16253/16441/16487)
  and reproduces identically with this PR's changes stashed. Pre-existing,
  unrelated.
- `python3 scripts/check_dag.py` — silent (passes).
- `git diff --check` — clean.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in the diff.

## Current frontier

Phase 6 polish of the `HexPolyMathlib` library continues; the umbrella
`Euclid.lean` slice is now annotated. The two private `dvd` transport
helpers remain private (no concrete consumer needs them as public API).

## Next step

A future Phase 6 slice could revisit the `Associated` lemmas if a
concrete consumer wants a `grind`-shape pattern; today none of them
have a downstream call site that would benefit.

## Blockers

None for this slice. The pre-existing `HexBerlekampZassenhausMathlib.Basic`
build failure blocks fully verifying `IntReductionMod.lean` end-to-end
in isolation, but the affected call site (`toPolynomial_gcd_associated`)
has an unchanged statement, proof, and instance signature, so the bridge
surface this PR touches is independent of that failure.
