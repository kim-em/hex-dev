**Accomplished**

Claimed #2979 and added Mathlib-free matrix/vector transport lemmas in
`HexMatrix/Basic.lean`:

- `Hex.Matrix.mul_assoc_vec`
- `Hex.Matrix.one_mulVec`
- `Hex.Matrix.mulVec_zero`

The proofs use private `List.foldl` finite-sum helper lemmas over
`Lean.Grind.Ring`, with no new public surface beyond the three requested
transport lemmas.

Verified:

- `lake build HexMatrix.Basic`
- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Basic.lean`
  returns no matches

**Current frontier**

The matrix-vector transport algebra needed by the successor
`IsRREF.nullspace_sound` issue is now available from `HexMatrix.Basic`.

**Next step**

Use these lemmas in the successor nullspace proof issue to transport
`D.echelon * v = 0` back through `D.transform * M = D.echelon` and
`Tinv * D.transform = 1`.

**Blockers**

None.
