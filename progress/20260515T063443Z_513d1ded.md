# 2026-05-15 06:34 UTC — feature session, issue #4063

## Accomplished

- Added Step 3 of the Berlekamp completeness argument to
  `HexBerlekamp/RabinSoundness.lean` (in the
  `[ZMod64.PrimeModulus p]` section, after
  `isUnitPolynomial_gcd_quotient_of_squareFree`):
  - `witnessLinearFactor_sub_eq`: distinct `(w - C c)` and
    `(w - C d)` differ by the constant `C (d - c)`.
  - `witnessLinearFactor_distinct_common_dvd_one`: pairwise
    coprimality of distinct witness linear factors.
  - `dvd_of_witness_mul_of_gcd_isUnit`: one-step Bezout
    cancellation through a witness factor when its gcd with
    `f` is a unit polynomial.
  - `dvd_acc_of_foldl_witness_dvd_of_all_gcd_isUnit` and
    `dvd_one_of_witnessProduct_dvd_of_all_gcd_isUnit`: carry
    the cancellation through the foldl shape of the witness
    product.
  - `gcd_isZero_false_of_left_pos_degree`: `gcd(f, a)` is
    nonzero whenever `f` has positive degree.
  - `exists_gcd_not_isUnit_of_witnessProduct_dvd_of_pos_degree`:
    headline distribution result — a polynomial of positive
    degree dividing `Π_{c ∈ F_p} (w - C c)` has a non-unit
    gcd with some witness linear factor.
  - `exists_nontrivial_gcd_of_witnessProduct_dvd_of_pos_degree`:
    consumer-facing form. Under the additional non-constancy
    hypothesis that no single `(w - C c)` is divisible by
    `f`, some `gcd(f, w - C c)` is nonzero, nonconstant, and
    `≠ f`. Since `splitFactorAt f w c` is definitionally
    `DensePoly.gcd f (w - FpPoly.C c)`, this is directly
    usable by `kernelWitnessSplit?_some_of_nontrivial_splitFactorAt`
    in `HexBerlekamp/Factor.lean`.
- `lake build HexBerlekamp.RabinSoundness`,
  `lake build HexBerlekamp`,
  `python3 scripts/check_dag.py`, and `git diff --check` all
  pass after rebasing onto the latest `main`. No new `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME` introduced.

## Current frontier

- The witness-level divisibility hypothesis `f ∣ Π (w - C c)`
  is taken as a precondition. Deriving it from
  `f ∣ FpPoly.linearPow w p - w` lives in #4160 (decomposed
  from #4075, currently blocked on #4159).
- The square-freeness hypothesis is not needed for the
  distribution lemmas themselves; it is consumed at the call
  site to derive both the witness-level divisibility and the
  non-constancy hypothesis. The distribution surface in this
  PR is square-freeness-free.

## Next step

- Once #4160 lands, downstream callers (e.g. #4064 — compose
  nonconstant kernel witness into executable split) compose
  the witness-level divisibility theorem with
  `exists_nontrivial_gcd_of_witnessProduct_dvd_of_pos_degree`
  here and pass the resulting `c` to
  `kernelWitnessSplit?_some_of_nontrivial_splitFactorAt`
  to obtain a nontrivial `kernelWitnessSplit?` result.

## Blockers

- None for this PR. The deeper composition into a concrete
  Berlekamp split awaits #4160 and the non-constancy
  derivation in #4064.
