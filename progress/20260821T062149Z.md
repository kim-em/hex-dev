# Multivariate GCD SPEC repair

## Accomplished

- Rewrote `SPEC/Libraries/hex-mv-gcd.md` around a non-circular,
  certificate-checked design with explicit algebraic laws, total coefficient
  maps, deterministic fallback, rational lifting, zero conventions, and
  complete public contracts.
- Aligned the resultant, modular, modular-matrix, integer-polynomial-GCD, and
  future-work specifications with the repaired dependencies and ownership
  boundaries.
- Checked the design against the current implementations and incorporated an
  independent implementability review.
- Ran `lake build` successfully from a fresh clone (9,809 jobs) and verified
  the documentation diff with `git diff --check` and targeted stale-name
  searches.

## Current frontier

- The specification now separates proof-only GCD-domain infrastructure,
  certificate checking, complete production, and optional fast proposals.
- The core scope ends at characteristic-zero squarefree support; Zippel and
  full positive-characteristic decomposition remain explicit future work.

## Next step

- Implement the prerequisite extended subresultant chain, runtime prime
  bundle, and deterministic randomness interface before beginning the core
  certificate checker.

## Blockers

- None for the specification update. The implementation intentionally depends
  on the planned prerequisites recorded in the dependency section.
