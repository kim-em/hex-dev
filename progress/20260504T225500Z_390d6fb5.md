# PLAN+SPEC profile-size wording (#2021)

## Accomplished

- Claimed human-oversight issue #1997, verified its PARI assumption against local `gp`, and skipped it for replan because `ffinit(2,3)` does not match the Lübeck Conway table entry.
- Claimed issue #2021 and updated `SPEC/testing.md` to steer `core` conformance fixtures toward the upper end of the documented size ranges.
- Added the Phase 3 reviewer checklist item in `PLAN/Phase3.md` asking reviewers to check that input sizes are pushed toward those upper ranges, or that smaller choices are explained.
- Verified `git diff --check -- SPEC/testing.md PLAN/Phase3.md`, `python3 scripts/check_dag.py`, and `lake build`.

## Current frontier

The profile-size guidance and Phase 3 review checklist now reflect the requested upper-end default. The Conway PARI/Lübeck issue remains a replan item because the specified PARI function does not produce Conway polynomials in at least the `(2, 3)` case.

## Next step

Open a PR closing #2021 with the two documentation edits and this progress note.

## Blockers

None for #2021.
