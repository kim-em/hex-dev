# 2026-05-13T13:42:06Z — review #3560 cap-separation bridge surface

## Accomplished

- Reviewed `HexBerlekampZassenhausMathlib/BadVector.lean` and
  `HexBerlekampZassenhausMathlib/TerminationBound.lean` after the
  cap-separation bad-vector bridge from #3520/#3524 landed.
- Tightened `ExecutableBadVectorWitness.ProjectedBadVectorSetupBridge`:
  the `localFactorDegree_pos`, `coprime_input_aux_over_rat`, and
  `resultant_divisible_by_p_pow` fields were universally quantified over
  a projected vector `v ∈ L' \ W` that none of them referenced. Each is
  now a plain hypothesis on the fixed witness data (`W.H`, `W.input`,
  `W.liftData`); only `auxiliary_eq` retains its `∀ v` clause, since the
  BHKS auxiliary polynomial does depend on the chosen vector.
- Added a docstring sentence to the bridge structure flagging which
  field depends on `v` and which do not.
- Adjusted the lone consumer `bad_setup_of_projected_not_indicator` to
  match (drops three `v hin hnot` argument lists). The downstream
  `ExecutableCapSeparationHypotheses.ofProjectedBadVectorSetupBridge`
  and `projectedRowSpan_eq_trueFactorIndicatorLattice_of_cap_bridge`
  wrappers in `TerminationBound.lean` route through that consumer and
  needed no changes.

## Current frontier

- Cap-separation bridge surface is now in a simpler shape for the
  forward-recovery consumer (#3523) without restating each clause as a
  callback over `v`.

## Next step

- #3523 (`ForwardRecoveryInputs.ofCapSeparation`) can now build
  `ProjectedBadVectorSetupBridge` with three plain hypotheses plus one
  `auxiliary_eq` callback, instead of four callbacks.

## Blockers

None.
