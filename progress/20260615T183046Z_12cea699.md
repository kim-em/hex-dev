# Session 12cea699 — one-shot #7361 (descent producer)

## Accomplished

Premise-checked #7361 (produce `HenselLiftDescentHypotheses core B primeData
(toMonicLiftData core B primeData) True True` from `toMonicPrimeData? core =
some primeData`). The deliverable is **refutable as typed**, so no PR. Posted a
concrete refutation comment and skipped for replan.

## Key finding

`HenselLiftDescentHypotheses.represents_modP_of_lifted` (`Basic.lean:2747`)
concludes `RepresentsIntegerFactorModP primeData factor S` for the **original
non-monic** `factor ∣ core`. For `primeData = toMonicPrimeData? core =
choosePrimeData? (toMonic core).monic`, `primeData.factorsModP` are mod-`p`
factors of `M := (toMonic core).monic`, so this is exactly the predicate #7366
/#7364 already refuted as unsatisfiable: it forces
`monicModPImage (modP p factor) = monicModPImage (modP p g)` for the monic
correspondent `g ∣ M`, false because `factor` and `g` differ by the dilation
`X ↦ lc·X` (not a `scale`).

The field is non-vacuous: #7455 (`toMonicLiftData_represents_lifted_of_modP`,
`Basic.lean:20965`, landed) produces the lift representation `T`. Counterexample
satisfying every field hypothesis: `core = 2X²+X`, `factor = 2X+1`, `p = 3` —
`monicModPImage (modP 3 factor) = X+2`, not among the achievable
`modPFactorProduct ∈ {1, X, X+1, X²+X}`.

`RecoveredAtLift` (`Basic.lean:2360`) confirms the structural reason: a lift
representation carries a monic correspondent `monicFactor ∣ (toMonic core).monic`,
so the genuine mod-`p` representation lives at the correspondent, the sound shape
already realised by #7455/#7433.

Two further independent failures of the body's sketch: (1)
`henselLiftData_represents_modP_of_lifted` needs `ModPSubsetPartitionHypotheses
core primeData`, whose `fModP_eq` (`Basic.lean:1761`) requires `primeData.fModP =
modP p core`, false for toMonicPrimeData; (2) #7504's `toMonicLiftData_unique_subset`
(`Basic.lean:21661`) is conditioned on `2 * defaultFactorCoeffBound (toMonic
core).monic < p ^ precisionForCoeffBound B p`, not available for arbitrary `B ≠ 0`.

## Current frontier

#7361 marked `replan`. #7470 (its declared blocker) has landed but does not
unblock this issue.

## Next step

A planner/owner must restate the descent contract so the mod-`p` witness tracks
the monic correspondent's subset (#7433 shape), updating consumers at
`Basic.lean:19798/19935/19988`. This is an upstream contract change, not an
assembly. Not decomposed (would escalate, not fix).

## Blockers

The deliverable is refutable as written; requires a contract restatement
decision, not a worker patch.
