# Row-operation multiplication lemmas (#3060)

## Accomplished

- Added entry-read lemmas `rowScale_getElem` and `rowAdd_getElem` in
  `HexMatrix/RowEchelon.lean`, parallel to the existing `rowSwap_getElem`.
- Proved focused row-operation multiplication lemmas
  `rowSwap_mul`, `rowScale_mul`, `rowAdd_mul`: each says that left-multiplying
  by a matrix `B` commutes with the corresponding row operation on the left
  factor.
- Added the immediate `T * M = E` preservation wrappers
  `rowSwap_transform_mul_preserve`, `rowScale_transform_mul_preserve`, and
  `rowAdd_transform_mul_preserve` for the parent #3051 invariant shape.
- Build is green: `lake build HexMatrix.RowEchelon HexMatrix.RREF HexMatrix`.
  No new banned markers in the touched files.

## Current frontier

`HexMatrix/RowEchelon.lean` now exposes the row-operation transforms needed
by parent #3051 for proving the RREF transform invariant `T * M = E`.

## Next step

Issue #3051 (parent) and the inverse-preservation issue #3061 can now consume
these lemmas to drive the RREF loop invariant proof.

## Blockers

None.
