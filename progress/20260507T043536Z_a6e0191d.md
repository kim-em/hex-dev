# 2026-05-07 04:35 UTC — work session (a6e0191d)

## Accomplished

Claimed #2538 and added a compiled private reduction-zero helper layer in
`HexGramSchmidt/Basic.lean`:

- zero rows stay zero under `reduceAgainstBasis`;
- rows orthogonal to every listed basis row are unchanged by reduction;
- a basis row reduces to zero once it appears at the head of the reduction
  list;
- `(basisRows rows)[j]!` reduces to zero against the prefix through `j + 1`.

Created follow-up #2550 for the remaining source-row theorem requested by
#2538.

Verification completed:

- `lake build HexGramSchmidt.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean`

The only `sorry` hits in the touched file are the pre-existing public
`basis_span` theorem bodies.

## Current frontier

#2538 is partially reduced to #2550. The missing step is to combine
`basisRows_get!_eq_reduceAgainstBasis_forward` with the new basis-row
reduction-zero helpers to prove that the original earlier source row
`rows[j]!` reduces to zero against any prefix containing row `j`.

## Next step

Open the partial PR for #2538, then let a worker claim #2550 to finish the
source-row theorem.

## Blockers

The worktree still contains an unrelated pre-existing `.claude/CLAUDE.md`
modification that this session did not touch.
