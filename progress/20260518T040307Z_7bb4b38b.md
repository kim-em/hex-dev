# Session 7bb4b38b — audit + skip of #4880

## Accomplished

`/work` → claimed #4880 (BZ Mathlib-free: drop `hcore_monic` Gap 1 from
the `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
umbrella). #4334 (HexLLL Phase 4 inconclusive verdicts) was also in
the unclaimed queue but is blocked on quiet hardware — host load on
`carica` was ~15 on a 24-core machine at claim time.

Performed the deliverable-6 audit. Posted findings as
[issue #4880 comment 4474193431](https://github.com/kim-em/hex/issues/4880#issuecomment-4474193431)
and called `coordination skip 4880` with a routing summary for the
next planner.

## Audit summary

`#4879` (wrapper relaxation) is landed and correct, but is **not
sufficient** to drop `hcore_monic` from the umbrella signature. The
umbrella body still threads `hcore_monic` into five upstream callers
that have no primitive+pos-lc siblings:

1. `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`
   (`HexBerlekampZassenhausMathlib/Basic.lean:5467`).
2. `henselLiftData_liftedFactor_monic_of_choosePrimeData`
   (`HexBerlekampZassenhausMathlib/Basic.lean:4572`).
3. `henselLiftData_liftedFactor_natDegree_pos_of_factorsModPBerlekampForm`
   (`HexBerlekampZassenhausMathlib/Basic.lean:6560`).
4. `henselLiftData_liftedFactor_injective_of_factorsModPBerlekampForm`
   (`HexBerlekampZassenhausMathlib/Basic.lean:6624`).
5. `reassemblyExpansionComplete_exhaustive_of_ne_zero`
   (`HexBerlekampZassenhausMathlib/IntReductionMod.lean:3438`).

Confirmed via `rg
"henselLiftData_liftedFactor.*primitive|factorsModP.*primitive|reassemblyExpansionComplete.*primitive"`:
zero matches in either Mathlib or Mathlib-free trees. The 77 existing
`primitive_pos_lc` siblings in `HexBerlekampZassenhausMathlib/Basic.lean`
are all at the downstream recombination/representation layer, not the
Hensel-lift layer.

Each helper relaxation is architectural (the conclusion changes — for
non-monic core, mod-`p` products acquire a `lc⁻¹` factor and lifted
factors are no longer monic in the integer sense), not a thin sibling.
The docstring's "internal monicising-by-scaling refactor" parenthetical
is the actual path to Gap 1 closure, and the `recombineScaledExhaustive`
chain (`#4866`/`#4869`/`#4870`/`#4875`) is the substrate that path
needs.

## Current frontier

Issue #4880 is `replan`-labelled. Next planner cycle should choose
between:

* refile after the scaled-Hensel-lift substrate lands, or
* decompose into per-helper sibling sub-issues (each requires a
  signature design pass to align relaxed conclusions with their
  consumers — five sub-issues, not pure thin siblings).

## Next step

Worker exits. No code changes.

## Blockers

* `hcore_monic` cannot be derived from `(hcore_primitive, hcore_lc_pos)`
  + `hf_ne` — the squarefree core's leading coefficient is genuinely
  not 1 in general (this is the original Gap 1 substrate observation).
* Five upstream helpers consume monic and have no primitive+pos-lc
  siblings.
