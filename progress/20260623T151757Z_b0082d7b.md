# #8327 M1 subset Hensel uniqueness wrapper

## Accomplished

Added `coreLiftData_subset_congr_monicTarget` in
`HexBerlekampZassenhausMathlib/Basic.lean`.

The theorem specializes the generic additive subset uniqueness lemma
`henselLiftData_liftedSubset_congr_of_modP` to `coreLiftData core B primeData`
and concludes
`liftedFactorProduct ... ≡ monicTarget factor p k (mod p^k)` for a selected
mod-`p` subset.  The M1 product comparison
`monicTarget factor * cofactor ≡ monicTarget core` is kept as an explicit
premise, so the proof does not derive scale-coordinate correspondence from the
older dilation-coordinate recovery carrier.

Verified with `lake build HexBerlekampZassenhausMathlib` after the initial proof
and again after the theorem rename.  No `sorry`, `axiom`, or `native_decide` was
introduced.

## Current frontier

The additive M1 subset correspondence is now available in the `coreLiftData`
shape.  The next consumer still has to supply the per-true-support modular
product decomposition for the selected factor/cofactor in `monicTarget`
coordinates.

## Next step

Use `coreLiftData_subset_congr_monicTarget` in the true-support recovery path to
derive the scale-coordinate correspondence needed by
`honestCongr_of_correspondence`.

## Blockers

No build blockers.  The worktree still contains a pre-existing dirty
`.claude/CLAUDE.md`, which is off-limits and was not touched.

`/reflect` was requested by the session template but is not available in this
environment (`zsh: no such file or directory: /reflect`).
