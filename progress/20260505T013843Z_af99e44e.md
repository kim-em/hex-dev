# Work session: HexConway Tier 1 benchmark harness

## Accomplished

- Claimed #2180 and added `HexConway/Bench.lean` for the Tier 1 Conway
  committed-table lookup surface.
- Registered a parametric benchmark over the 36 committed Luebeck table keys
  for `luebeckConwayPolynomial?`, with an adjacent constant-cost derivation and
  fixed hot loop for harness signal stability.
- Registered a fixed canonical benchmark for the currently exported
  `SupportedEntry`/`conwayPoly` path, `C(2, 1)`.
- Added the `hexconway_bench` Lake executable and CI smoke commands.

## Current frontier

The HexConway Phase 4 harness slice is wired and locally verified. This issue
does not mark Phase 4 complete and does not add the benchmark-reviewed status
token.

## Next step

Future Phase 4 work can add Tier 1 irreducibility fixed benchmarks for selected
committed entries and later Tier 2/Tier 3 registrations as separate issues.

## Blockers

`python3 scripts/check_phase4.py --all` still reports pre-existing adjacent
cost-derivation failures in older benchmark modules outside HexConway.
