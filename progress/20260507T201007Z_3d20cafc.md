# 2026-05-07 20:10 UTC — BHKS CLD helper layer (3d20cafc)

## Accomplished

Claimed issue #2599 and added the preparatory BHKS CLD arithmetic helpers in
`HexBerlekampZassenhaus/Basic.lean`:

- `bhksCoeffBound`, `ceilLogP`, and `bhksCoeffCutThreshold` for per-coordinate
  `ell_j` thresholds.
- `centeredResiduePow` and `psiCut` for the two-sided BHKS cut.
- `cldCoeffs` backed by `DensePoly.divMod` for centred high-bit CLD
  coefficient data.
- `#guard` checks for centred-cut behavior and a small exact CLD quotient case.

Verification passed:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The executable support layer for BHKS all-coefficients CLD data is in place.
The existing additive recombination lattice and public factorization path were
left unchanged, as required by the issue scope.

## Next step

Use `cldCoeffs` and the threshold/cut helpers in the future BHKS lattice
replacement for `recombinationLattice?`.

## Blockers

No blockers for this slice. The pre-existing pod-managed `.claude/CLAUDE.md`
worktree modification remains untouched.
