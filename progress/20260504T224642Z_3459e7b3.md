# HexPolyZ Rat canonical remainder congruence (#1947)

## Accomplished

- Claimed issue #1947 after human-oversight issues #1997, #2005, and #2021 were rejected by `coordination claim` as needing replan.
- Added private Rat proof helpers in `HexPolyZ/Basic.lean` for:
  - nonzero-divisor `divMod` reconstruction via `DensePoly.divModArray_reconstruction`,
  - `DensePoly.Congr (p % m) p m`,
  - positive-degree canonical remainder uniqueness over `Rat`,
  - not-positive-degree zero/constant modulus edge cases using the generic `divMod_remainder_eq_self_of_size_zero_core` and `divMod_remainder_eq_zero_of_degree_zero_core` helpers.
- Filled `ratDivModLaws.mod_eq_mod_of_congr` with `rat_mod_eq_mod_of_congr_core`.
- Verified `lake build HexPolyZ.Basic`, `lake build HexPolyZ`, `git diff --check`, and no `axiom`/`native_decide`/`TODO`/`FIXME` in `HexPolyZ/Basic.lean`.

## Current frontier

`ratDivModLaws.mod_eq_mod_of_congr` is now proved. The remaining Rat division-law and gcd-law placeholders in `HexPolyZ/Basic.lean` are unchanged and outside this issue.

## Next step

Continue the Rat law chain with the remaining fields in `ratDivModLaws`, especially the modulo algebra fields that issue #1948 depends on.

## Blockers

None for this completed slice.
