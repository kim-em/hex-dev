**Accomplished**

- Claimed issue #4313.
- Refactored `HexBerlekamp/Basic.lean` so `fixedSpaceMatrix`,
  `IsFixedSpaceKernelVector`, `IsFixedSpaceKernelPolynomial`, and
  `isFixedSpaceKernelVector_iff_berlekampMatrix_mulVec_eq` use the default
  `ZMod64`/`Matrix.mulVec` instances rather than projected
  `Lean.Grind.Field` operations.
- Added a canonical `Lean.Grind.Field (ZMod64 p)` instance from
  `ZMod64.PrimeModulus p` locally in `HexBerlekamp.Basic`, allowing the
  fixed-space soundness/completeness theorems to use canonical operations while
  preserving the executable kernel API that accepts a field witness.
- Verified no remaining projected fixed-space `mulVec` forms in the touched
  Berlekamp surface.

**Current frontier**

The instance mismatch that blocked composing the fixed-space iff with
`berlekampMatrix_mulVec_coeffVector_eq` is removed. The executable
`fixedSpaceKernel` and factorization callers remain source-compatible with
existing field-witness users.

**Next step**

Create the PR for #4313. The follow-up issue #4314 can now attempt the
polynomial kernel iff using the default `Matrix.mulVec` statement.

**Blockers**

None for #4313. Builds still report pre-existing sorry/linter warnings outside
this change, notably in `HexMatrix.Bareiss`, `HexGramSchmidt`, `HexLLL`, and
`HexBerlekampZassenhaus.Basic`.
