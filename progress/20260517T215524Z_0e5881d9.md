# HO-18 directive completion PR

## Accomplished

Claimed directive issue #3662 and incorporated the existing partial HO-18
evidence commit from PR #4834 onto this session branch:

* `reports/bench-results/hex-lll-densified-fa57a699.json` adds the committed
  densified Lean + Isabelle fixed-benchmark sweep.
* `reports/hex-lll-performance.md` rewrites the `Comparator Ratios` section
  around that artifact, including per-call comparator overhead, rung tables,
  trend narratives, and gating verdicts.
* `HexLLL.done_through` intentionally remains at `3` because the report's
  `Concerns` section is non-empty and points to #4334 for quiet-hardware
  follow-up.

Verification run in this session:

* `lake exe hexlll_bench list`
* `lake exe hexlll_bench verify`
* `python3 scripts/check_phase4.py`
* `python3 scripts/check_dag.py`
* `git diff --check HEAD~1..HEAD`
* `shasum -a 256 reports/bench-results/hex-lll-densified-fa57a699.json`

The artifact hash matched the report:
`64b596ea0feb27835d2f844082b43a059c47f3fba8673741ea15baf468054490`.

## Current frontier

#3662 is ready for a closing PR: the densified ladder/report deliverables are
present, and the conditional re-promotion branch of the directive is blocked by
the documented #4334 Concern rather than by missing report work.

## Next step

After this PR lands, #4334 should rerun the same HexLLL scientific and
comparator measurements on quiet or dedicated hardware and either replace the
contention-flagged artifact or turn persistent adverse trends into
family-specific follow-up issues.

## Blockers

None for #3662. The only remaining HexLLL Phase 4 promotion blocker is #4334.
