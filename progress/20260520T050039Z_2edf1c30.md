# HexPolyZMathlib Schmeisser kernel adapter

Session: `2edf1c30` (work, issues #5614 and #5615)

## Accomplished

Claimed #5614, found that it bundled the reusable derivative-kernel algebra
with the full hard analytic de Bruijn-Springer/Schmeisser source theorem, and
found no current Mathlib/repo theorem to instantiate for that source result.

Decomposed #5614 into #5615 for the derivative-kernel adapter and #5616 for the
remaining analytic source theorem target, then marked #5614 for replanning.

Claimed #5615 and moved the Schmeisser derivative kernel API from
`HexPolyZMathlib/RobinsonForm.lean` into `HexPolyZMathlib/SchurSzego.lean`.
Added
`Polynomial.schmeisserComposition_derivativeKernel_eq_X_mul_derivative`,
identifying the binomial-normalized composition with the derivative kernel as
`X * p.derivative`.

## Current frontier

The local derivative specialization algebra now sits beside
`Polynomial.schmeisserComposition`; `RobinsonForm` consumes the shared API via
its existing `SchurSzego` import.

## Next step

Use #5616 to formalize the actual de Bruijn-Springer/Schmeisser source
inequality, then wire it through the existing radius/product wrappers and the
new derivative-kernel composition identity.

## Blockers

No blocker for #5615.

The analytic source theorem remains unproved in Mathlib and this repo; #5616
tracks that remaining hard theorem.

Verification completed:

- `lake build HexPolyZMathlib.SchurSzego`
- `lake build HexPolyZMathlib`
- `python3 scripts/oracle/mahler_schur_counterexample.py`
- `git diff --check`
- `git diff -- HexPolyZMathlib/SchurSzego.lean HexPolyZMathlib/RobinsonForm.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'`
  returned no matches
