**Accomplished**

- Claimed #5773 (HO-5b) — directive left claimable after #5822 partial.
- Extended `HexBerlekampZassenhaus/Bench.lean` with four per-rung
  `setup_fixed_benchmark` verified-Isabelle BZ comparator targets
  (`runIsabelleSplitN{2,3,4,5}Checksum`) on `smokeInput n` for the
  full `splitScientificSchedule`. Tagged `scheduled-hardware` so
  `verify` continues to skip the AFP comparator.
- Extended `isabelleFixtureInputs` to include `smokeInput 2..5` so the
  first-call cross-check guard sanity-checks Isabelle's output at every
  measured rung.
- Built the AFP-extracted `bz_isabelle` binary locally on `carica` via
  `scripts/oracle/setup_bz_isabelle.sh`.
- Ran the per-rung sweep
  (`runFactorChecksum`, `runIsabelleFactorBaselineChecksum`, four
  `runIsabelleSplitN*Checksum`) into
  `reports/bench-results/hex-berlekamp-zassenhaus-2f4ef93-split-ladder.json`.
- Confirmed factor-multiset agreement at every rung (Isabelle's
  `n=5` hash `0x2a6bd8144b402a41` matches Lean
  `runFactorChecksum`'s recorded `n=5` hash).
- Rewrote the Comparator Ratios section of
  `reports/hex-berlekamp-zassenhaus-performance.md` to:
  * promote the per-rung split-family ladder to the headline,
  * present the four-rung ratio table with adjusted + raw ratios and
    per-rung speedups,
  * record a monotonic trend (`~2.18×` per-rung Lean multiplier,
    constant Isabelle, ratio `+0.43` absolute over four rungs),
  * report the largest-eligible-rung gating verdict as **met**
    (adjusted ratio `0.4743`, Lean 2.11× faster),
  * call out the extrapolation concern (`n = 6` is predicted to flip
    the verdict and is the schedule's next rung up).
  * keep the canonical fixed comparator rung as background only.
- Updated §Concerns to reflect the new per-rung ladder, the
  extrapolation concern, and the remaining per-family ladder gaps
  for the other scientific schedules.
- Verified: `lake build`, `lake exe hexbz_bench verify` (17/17 passes).

**Current frontier**

- Headline section now reports per-rung `hex/isabelle` ratio verdicts
  on the split-family scientific schedule. The gating goal is met at
  the largest currently eligible rung.
- Other scientific schedules (`degreeHeightSchedule`,
  `slowDegreeHeightSchedule`, `precisionLocalSchedule`, HO-2
  adversarial singletons) still lack per-rung Isabelle ladders.

**Next step**

- Extend `setup_fixed_benchmark`-style Isabelle comparator
  registrations to the remaining scientific schedules so each one can
  contribute its own per-rung ratio ladder to the report. This is the
  natural follow-up scope for a fresh worker.

**Blockers**

- None for the split-family ladder. Adding the remaining per-family
  ladders is incremental, each one a small extension of this PR's
  pattern.
