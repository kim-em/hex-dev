# HexGfqRing repr_one

## Accomplished

- Claimed issue #5239.
- Added `Hex.GFqRing.repr_one` immediately after `repr_zero` in `HexGfqRing/Operations.lean`.
- Verified `lake build HexGfqRing.Operations`, `lake build`, `python3 scripts/check_dag.py`, `git diff --check`, placement grep, and a forbidden-token scan on the touched Lean file.

## Current frontier

- The new `repr_one` simp wrapper is ready for PR review.

## Next step

- Open the PR for issue #5239.

## Blockers

- None.
