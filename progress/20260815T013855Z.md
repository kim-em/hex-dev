# Accomplished

- Restacked the fixed integral-certificate experiment onto exact merged main
  `56f56fc4e420a3289ccdec85cd7b56277ae967e0`, preserving the merged PNT+
  Table 12, huge-cosine, and precision-log examples.
- Reconciled the experiment, Mathlib companion, conformance registration, and
  exact proof-only Lake freshness exemption without changing the three Lean
  feature blobs from their previously checked local implementation.
- Rebuilt the focused integral target and the complete interval experiment,
  Mathlib experiment, and conformance target set successfully (9,405 jobs).
- Re-ran copyright, line-count, DAG, trust-surface, Phase 4, conformance-target,
  Mathlib-free bench, factor-freshness, PNT inventory, diff, and banned-term
  checks successfully.

# Current frontier

The exact one-cell certificate closes the ordinary strict enclosure
`7468 / 10000 < ∫ x in 0..1, Real.exp (-(x ^ 2))` and
`∫ x in 0..1, Real.exp (-(x ^ 2)) < 7469 / 10000`. The polynomial integral
and integrated Taylor remainder are kernel-proved for the one admitted fixed
record; they are not yet interpreted as parameters by a generic proof. Runtime
mutations that omit the cell, record an unproved tighter error, or change the
right endpoint are rejected.

# Next step

Push the exact main-based edge, obtain a genuinely fresh exact-head review,
and require exact-head CI before merge. Generalize only afterward to multiple
cells and precision-driven partition/order selection.

# Blockers

None for this bounded canary. Arbitrary integrands, adaptive subdivision,
multi-cell accumulation, and performance selection remain explicit future
work rather than claims of this experiment.
