# Interval scale accounting audit

## Accomplished

- Independently reproduced every report number and JSON invariant from scale
  head `13b3690a`; no measurement, soundness, provenance, or prior-review
  discrepancy was found.
- Corrected the textual fallback for uncharged centered-recipe program-list
  work: `centerV1` performs nine lookups for the selected center and nine for
  each equality edge, so the literal constructor bound is
  `9 * (maxEdges + 1) * maxNodes`.
- Kept this work explicitly outside the recorded `maxLookupSteps` totals and
  retained the requirement to charge it or use validated random access in a
  production checker.

## Current frontier

The correction changes documentation only; all committed measurements and
numeric conclusions remain valid. PR #8937 CI is running asynchronously.

## Next step

Push the audit correction and let the restarted CI monitor run while rational
certificate and arithmetic-edge work continue.

## Blockers

None.
