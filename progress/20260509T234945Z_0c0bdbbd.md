# 20260509T234945Z - Issue #2893

## Accomplished

Claimed feature issue #2893 and added Lean-side pinned modular degree checks for
the remaining HexBerlekampZassenhaus adversarial shapes:

- `(X^2 - 2)(X^2 - 3)` at `p = 23` with degrees `[1, 1, 1, 1]`
- Swinnerton-Dyer SD3 at `p = 71` with eight linear modular factors
- `Phi15` at `p = 31` with eight linear modular factors

Extended the supported explicit small-prime candidate table with `23`, `31`,
and `71`, and taught `modularFactorDegreesAt?` to record complete linear split
evidence directly for pinned conformance checks while preserving the existing
Berlekamp branch for non-linear splits.

Verification completed:

- `lake build HexBerlekampZassenhaus.Conformance`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhaus/Conformance.lean`

The final grep reports only pre-existing `sorry` markers in
`HexBerlekampZassenhaus/Basic.lean`; no new proof holes or banned markers were
introduced.

## Current frontier

The Lean conformance surface now pins all four HO-2 modular stress shapes via
`modularFactorDegreesAt?`. The full HO-2 JSONL/oracle fixture corpus remains
out of scope for this slice.

## Next step

Create the PR for issue #2893 and let CI validate the pinned modular degree
checks.

## Blockers

None.
