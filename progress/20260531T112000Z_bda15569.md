# HO-5d-2e — drop stale fallback p=3 / none-branch comments

Issue: #5857.
Session UUID prefix: bda15569.

## Accomplished

Removed all proof-facing-surface comments that described the `none`
branch of `Hex.choosePrimeData?` as the fallback `p = 3` branch:

- `HexBerlekampZassenhausMathlib/Basic.lean` —
  `factors_irreducible_of_choosePrimeData_of_some` (line 6379 area),
  `existsUnique_modPFactorSubset_of_choosePrimeData` (line 16830 area),
  and `modPSubsetPartitionHypotheses_of_choosePrimeData` (line 16875
  area). The three docstrings now describe the explicit-witness
  premise (`hselected` / `hchoose : choosePrimeData? = some primeData`)
  and the `none` branch is identified without the stale `p = 3` label.
- `HexBerlekampZassenhaus/Basic.lean` —
  `factorWithBound_entry_mem_small_mod_singleton_raw` (line 7293 area):
  rephrased `In the fallback case (\`choosePrimeData? sf = none\`)` to
  `When \`choosePrimeData? sf = none\``, since the dispatcher contract
  being described is the no-good-prime case, not a fallback dispatch.

Dropped stale forward references to closed issues #5817 / #5819 from
four docstrings on
`exhaustiveSlowRawFactorsWithBound?`,
`factorSlowFactorsWithBound?`, `factorWithBound?`, and
`choosePrimeDataWithFallback`. The "retains the silent fallback
semantics" framing was kept (the total forms do still carry the silent
fallback for executable / benchmark consumers) but redirected at those
consumers rather than at the now-landed PRs.

Issue's first verification command,
`rg -n "fallback \`p = 3\`|fallback p = 3" HexBerlekampZassenhaus
HexBerlekampZassenhausMathlib --glob '*.lean'`, returns no matches.

## Current frontier

The second verification step,
`lake build HexBerlekampZassenhausMathlib succeeds`, is blocked by a
pre-existing build failure at `HexBerlekampZassenhausMathlib/Basic.lean`
lines 5099, 5994, 6280: `Hex.isGoodPrime_squareFreeModP` returns
`Hex.squareFreeModP` (defined as `gcdIsUnit gcd = true`, i.e.
`gcd.size == 1 = true`) but the three local `have hsf : … = 1` proof
sites at those lines still expect the stronger `gcd … = 1` form.
This failure is present at the parent commit 3bdee676 on `main`
(verified by stashing the comment edits and rebuilding) and is
documented in the prior session's progress note
`progress/20260531T101449Z_274301d2.md`. The same three errors are
reported in both baseline and post-change builds; no new errors are
introduced by this commit.

## Next step

A separate work item is needed to migrate the three call sites at
HBZMathlib/Basic.lean:5099,5994,6280 off the `gcd = 1` form. Either
weaken the local `have hsf` declarations to `squareFreeModP` (matching
the lemma's actual return type) and adapt downstream consumers, or
strengthen `isGoodPrime_squareFreeModP` to return `gcd = 1` (which
needs the Field-typeclass `PrimeModulus` instance already in scope at
those call sites). Once that lands, `lake build
HexBerlekampZassenhausMathlib` succeeds and this issue's second
verification criterion is met.

CI on `main` does not exercise this module — the bench targets are
Mathlib-free by spec, so HBZMathlib/Basic.lean is only built lazily
when something downstream of it is built, and nothing currently is.

## Blockers

Pre-existing build break described above. Blocks only the
`lake build` verification criterion, not the comment-cleanup
deliverables themselves.
