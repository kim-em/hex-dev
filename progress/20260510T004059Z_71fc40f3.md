# 20260510T004059Z - Issue #2907

## Accomplished

Claimed feature issue #2907 and extended `HexBerlekampZassenhaus/Bench.lean`
with fixed HO-2 adversarial benchmark registrations for:

- `adv/x4_plus_1`
- `adv/quad_sqrt2_sqrt3`
- `adv/swinnerton_dyer_sd3`
- `adv/phi15`

Added public `factor` and `factorFast` fixed targets for those inputs, using
the same coefficient lists as the conformance fixtures. The completing smoke
targets have pinned `expectedHash` values. The SD3 targets remain visible under
the fixed benchmark names and use smoke-mode checksums by default, with
`HEXBZ_BENCH_HEAVY=1` reserved for scheduled runs that should time the full
public pipeline on that input.

Verification completed:

- `lake build HexBerlekampZassenhaus.Bench`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_phase4.py`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

The final grep returned no matches.

## Current frontier

HO-3 now has fixed adversarial benchmark registrations visible through the
`hexbz_bench` CLI. Full Phase 4 performance reporting and broader scientific
matrix coverage remain outside this slice.

## Next step

Create the PR for issue #2907 once GitHub API access is available, then let CI
validate the benchmark smoke coverage.

## Blockers

`coordination create-pr 2907 "Add HexBZ adversarial bench targets"` could not
create the PR because `gh` API calls are currently rate-limited for the account.
The branch `agent/71fc40f3` has been pushed to `origin`.
