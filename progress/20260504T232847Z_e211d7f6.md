# HexMatrix residual bridge partial (#2101)

## Accomplished

- Claimed issue #2101.
- Added
  `detBorderedMinorOffDiagonalProductResidual_eq_zero_of_succ_offDiagonal_identity`
  in `HexMatrix/Determinant.lean`, reducing the named off-diagonal residual
  cancellation to one explicit successor off-diagonal product identity.
- Confirmed the direct fully general residual-zero theorem is not a simple
  `grind` consequence of the existing residual packaging; the missing step is
  the determinant expansion captured by the bridge hypothesis.
- Created successor issue #2106 for proving that successor off-diagonal product
  identity and then deriving the public residual-zero theorem.
- Verified `lake build HexMatrix.Determinant`, `lake build HexMatrix`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden
  placeholder scan on `HexMatrix/Determinant.lean`.

## Current frontier

The residual algebra is now isolated: proving the `hsucc` identity in the new
bridge theorem is enough to finish
`detBorderedMinorOffDiagonalProductResidual_eq_zero`.

## Next step

Work issue #2106 by proving the successor `detFinalColumnOffDiagonalSum`
product identity using the existing final-column/permutation-vector
infrastructure, then apply the bridge to finish #2101's original theorem.

## Blockers

None.
