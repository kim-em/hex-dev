# Progress — 2026-06-18 (session 17ba12cd, /work → /feature #7957)

## Accomplished

Completed both deliverables of #7957 (HexLLL Phase 6 `@[grind]` automation
coverage for `Basic.lean`), confined to `HexLLL/Basic.lean`.

### `@[grind =]` annotation pass

Added `@[grind =]` to the public characterising lemmas whose statements are
useful rewrites for automated reasoning:

- Lattice-preservation iff family (LLLState): `swapStep_memLattice_iff`,
  `sizeReduceColumn_memLattice_iff`, `sizeReduce_memLattice_iff`,
  `lllLoop_memLattice_iff`.
- Lattice-preservation iff family (SteeredState): `reduceColumn_memLattice_iff`,
  `sizeReduce_memLattice_iff`, `swap_memLattice_iff`, `prep_memLattice_iff`,
  `loop_memLattice_iff`, `finalSweep_memLattice_iff`, `steeredReduce_memLattice_iff`.
- Structural equations: `sizeReduce_d` (`(s.sizeReduce k).d = s.d`),
  `sizeReduce_basis`.
- Upgraded the two existing `@[simp]` projection lemmas `init_b` and
  `refreshRow_b` to `@[simp, grind =]`.

All of these are strictly term-reducing rewrites (`memLattice (op s).b v ↔
memLattice s.b v`, `(op s).d = s.d`), so the rule set is terminating — no
`grind`-loop risk. Skipped `swapStep_ν_eq` / `swapStep_d_eq`: both carry a
`(s.swapStep k).Valid` side hypothesis that `grind` would have to discharge,
making them conditional rewrites with no clear payoff and a loop/cost risk.

### Payoff demonstration

With the component iff lemmas tagged, two chained proofs collapse to `grind`:

- `prep_memLattice_iff`: `unfold prep; rw [...]; split <;> simp [...]` →
  `unfold prep; grind`.
- `steeredReduce_memLattice_iff`: `unfold steeredReduce; rw [finalSweep_…,
  loop_…, init_b]` → `unfold steeredReduce; grind`.

The remaining iff proofs (`loop_memLattice_iff`, `finalSweep_memLattice_iff`)
are inductions, so they keep their explicit structure; the annotations still pay
off as downstream `grind` availability for consuming layers and for these two
in-file simplifications.

## Verification

- `lake build HexLLL` → 399 jobs, `Build completed successfully`. `HexLLL.Basic`
  builds in 2.6s (was 2.7s pre-change) — no annotation slowdown.
- No `warning`/`error`/`sorry` in the build log.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/`TODO`/
  `FIXME`.
- `python3 scripts/check_dag.py` → exit 0.
- Confined to `HexLLL/Basic.lean`: no `libraries.yml` `done_through` bump, no
  `*Mathlib` bridge touched, no new imports.

## Current frontier

HexLLL Phase 6 `grind`-annotation surface for `Basic.lean` is covered. The
lattice-preservation rewrites are now visible to `grind` in consuming layers.

## Next step

Sibling Phase 6 `@[grind]` passes remain unclaimed: #7960 (HexGramSchmidt),
#7961 (HexGFq).

## Blockers

None.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
