# 20260509T202810Z - Issue #2862

## Accomplished

Claimed feature issue #2862 after the human-oversight queue proved blocked by
dependencies. Added reusable Yun current-tail coprimality helpers in
`HexPolyFp/SquareFree.lean`:

- `yunStep_quotient_factor_coprime_of_common_dvd_one`
- `yunFactors_current_tail_coprime_of_common_dvd_one`

The tail helper uses `yunFactors_factor_dvd_current` to turn each recursive
tail factor's divisibility by the current `y` into the per-factor common-divisor
condition needed by `FpPoly.gcd_eq_one_of_monic_of_common_dvd_one`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The grep still reports the four pre-existing `sorry`s in
`HexPolyFp/SquareFree.lean`; this session did not add proof holes or banned
markers.

## Current frontier

The remaining prerequisite for #2849/#2851 is proving the natural current
quotient hypothesis: every common divisor of `c / gcd c w` and `gcd c w`
divides `1`, plus the monicity payload for the executable gcd in the emitted
factor relation.

## Next step

Use the new tail helper to finish the derivative-split
`yunFactorsCurrentTailCoprime` payload, then wire it into
`yunFactorsPairwiseReady` in #2849.

## Blockers

None.
