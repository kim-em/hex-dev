# 20260517T144903Z — feature session 59abcd99 (#4693)

## Accomplished

Claimed #4693 (HO-1 substrate: existsUnique_modPFactorSubset_of_choosePrimeData
assembly) after its two dependencies (#4691 monicModPImage bridge, #4692 UFD
subset existence/uniqueness) landed on `main`.

Added the final assembly in `HexBerlekampZassenhausMathlib/Basic.lean`:

* `existsUnique_modPFactorSubset_of_choosePrimeData_of_some` — analyzable
  branch under `Hex.choosePrimeData? core = some primeData`. Composes:
  * the `monicModPImage` divisibility bridge from #4691,
  * `factors_irreducible_of_choosePrimeData_of_some` (#4686),
  * `factorsModP_monic_of_factorsModPBerlekampForm`,
  * `factorsModP_nodup_of_factorsModPBerlekampForm`,
  * the abstract UFD subset existence/uniqueness lemma
    `existsUnique_subset_product_eq_of_dvd_of_squarefree_prod` from #4692,
    instantiated at `Polynomial (ZMod p)` via
    `HexBerlekampMathlib.toMathlibPolynomial`,
  * the `Multiset.map`/`Finset.val` correspondence to translate the unique
    `Multiset` witness back to a `ModPFactorSubset = Finset (Fin n)`.

* `existsUnique_modPFactorSubset_of_choosePrimeData` — consumer-facing
  wrapper exposing the `let primeData := Hex.choosePrimeData core` shape
  required by the `ModPSubsetPartitionHypotheses.exists_subset` /
  `unique_subset` fields. Carries the `choosePrimeData? core = some
  (Hex.choosePrimeData core)` strengthening hypothesis (downstream consumers
  discharge it from the same `choosePrimeData?` chain that supplies the
  other partition fields) and derives `core ≠ 0` internally from
  `isGoodPrime`.

Supporting helpers (private):

* `toMathlibPolynomial_factorsModP_product_eq_monicModularImage` — identifies
  the Mathlib `Multiset.prod` of mapped `factorsModP.toList` with
  `toMathlibPolynomial (monicModularImage (modP p core))`, routing through
  `Hex.factorProduct_map_monicModularImage_eq_monicModularImage_factorProduct`
  and `toMathlibPolynomial_listFoldlMul_one`.
* `univ_val_map_modPFactor_eq_factorsModP_map` — identifies the
  `Finset.univ.val.map f` for the indexed Mathlib factor function with the
  mapped-to-Mathlib multiset of `factorsModP.toList`, via
  `Finset.val_univ_fin` + `List.ofFn_getElem` + `List.ofFn_eq_map`.
* `map_filter_eq_of_le_map_val` — recover a Finset whose injective image is
  a given submultiset of the universe image (the bijection used to translate
  the UFD `Multiset` witness back to a `ModPFactorSubset`).

Also removed a duplicate `private theorem toMathlibPolynomial_modPFactorProduct`
declaration at the old line 4159 — the public version landed in PR #4734 and
the private version in PR #4744; Lean rejects the duplicate name. After the
fast-forward to `origin/main`, the file did not compile without this removal.

Verified:

* `lake build HexBerlekampZassenhausMathlib.Basic`
* `lake build HexBerlekampZassenhausMathlib`
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg`/`git diff` for new `sorry|axiom|native_decide|TODO|FIXME` in
  `HexBerlekampZassenhausMathlib/Basic.lean` against `origin/main`: zero new
  entries.

## Current frontier

#4693 is complete. The remaining #4687 chain is now ready for the parent
constructor wrapper #4688 to compose this with the trivial fields
(`fModP_eq`, `admissible_prime`, `square_free_reduction`) and the per-index
irreducibility component `factors_irreducible_of_choosePrimeData_of_some`
into the full `ModPSubsetPartitionHypotheses_of_choosePrimeData`.

## Next step

Create the PR for #4693. The `_of_choosePrimeData` wrapper's `hsome`
hypothesis matches the shape the constructor wrapper (#4688) already needs
to discharge for the other fields, so no further sub-decomposition is
expected for the partition-hypothesis constructor.

## Blockers

The duplicate-declaration fix on `main` was necessary for the build to
proceed; it is a pre-existing breakage from the simultaneous merge of
#4734 and #4744 (neither saw the other's identical-name declaration).
The fix is bundled into this PR rather than a separate one because
without it nothing else can merge.
