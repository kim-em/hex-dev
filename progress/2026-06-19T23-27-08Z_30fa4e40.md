# Phase 6 grind coverage for HexGFqMathlib/GF2q.lean (#8112)

Session type: feature

## Accomplished

Promoted the one equation-shaped public `@[simp]` lemma in
`HexGFqMathlib/GF2q.lean` to `@[simp, grind =]`:

- `modulusFpPoly_eq_conway` — conclusion confirmed at the point of
  promotion to be a literal `lhs = rhs`
  (`HexGF2Mathlib.GF2n.modulusFpPoly … = GFq.modulus h.entry`), not
  `↔`/`<`/`≤`/`Ne`/`Associated`/conjunction. RHS does not mention
  `modulusFpPoly` and the match pattern is narrow, so the rule terminates.

All other declarations in the file are `private` and carry no `@[simp]`,
so they are outside the sweep scope. This completes the `HexGFqMathlib`
library sweep (`Basic.lean` was handled by #8100).

## Current frontier

`HexGFqMathlib` Phase 6 grind sweep is now complete across both files.

## Next step

Other unswept `*Mathlib` libraries (e.g. the HexBerlekampZassenhausMathlib
sweep in #8119, HexGF2 in #8121).

## Blockers

None.

## Verification

- `lake build HexGFqMathlib.GF2q` green (2.4s elaboration, no grind-loop
  slowdown).
- `lake build HexGFqMathlib` (CI-gated) green.
- `scripts/check_dag.py` exit 0.
- Annotation-only diff; no new `sorry`/`axiom`/`native_decide`.
