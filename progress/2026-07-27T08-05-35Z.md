# Issue #8901 conditional cached-GCD suffix

## Accomplished

- Re-claimed and rewrote #8901 around the exact executable suffix that exists
  when `2 ≤ f.size` and `f.size < witness.size`.
- Added the generic two-step `DensePoly.gcd` suffix theorem, the finite-field
  constant-shift identities, the production cached candidate, and a cached
  sweep proved exactly equal to the retained reference sweep.
- Preserved the exact factor representative, cofactor, multiset, and stored
  order; conformance now pins both the prior equal-size unit-drift fallback and
  a strictly larger witness that executes the fast branch.
- Updated the compiled profiler to use production's linear-leaf recursion and
  the production cached candidate directly. Two CPU-pinned 101-sample runs
  reproduce a 1.105x SD4 split speedup, a 1.047--1.052x SD3 speedup, and no
  grid regression above 0.7%; checksums and ordered degree pairs are identical.
- Tightened measurement provenance, production-shape matching,
  documentation, and regression coverage before publication.
- Passed `lake build` (9463 jobs), `HexBerlekamp.Conformance`,
  `check_benches_mathlib_free`, the focused
  `runBerlekampFullySplitChecksum`, all 16 BZ smoke checks, and the committed
  HexBerlekamp fixture comparison.

## Current frontier

The implementation, proof, regression coverage, and profile artifacts are
complete on `issue-8901` and ready to commit, rebase onto current `origin/main`,
publish, and send through CI.

## Next step

Commit the scoped files, rebase onto current `origin/main`, open the PR, inspect
job-level Actions state and logs, address any failures or conflicts, then merge
with `gh pr merge --squash --auto` without deleting the branch.

## Blockers

The local image lacks both `python-flint` and `pip`, so the external FLINT
oracle itself could not run locally. Fresh HexBerlekamp fixture emission is
byte-identical to the committed fixture, and CI installs `python-flint` before
running the oracle. No implementation blocker remains.
