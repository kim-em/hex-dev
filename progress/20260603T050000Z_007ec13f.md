# 20260603T050000Z work #6445

## Accomplished

- Claimed BHKS D1 residual issue #6445 (discharge the strict
  cap-arithmetic comparison for the canonical cap-separation inputs).
- Added `BHKS.bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`: once the canonical
  `coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree`
  is bounded above by `bhksPaperThresholdReal (Hex.normalizeForFactor f).squareFreeCore C`
  for any project constant `0 ≤ C ≤ 2`, the strict comparison
  ```
  ... < ((factorFastCapLiftData f primeData).p ^
         ((factorFastCapLiftData f primeData).k * localFactorDegree) : ℝ)
  ```
  required by `ofBridgeDataPointwiseAuxiliaryBounds` follows by chaining
  `bhksPaperThresholdReal_le_factorFastPrecisionCap` with
  `Hex.precisionForCoeffBound_spec` (applied through the stored
  `precision_eq`) and `pow_le_pow_right₀` against the `localFactorDegree`
  exponent. The prime lower bound `2 ≤ p` is discharged internally from
  `choose_eq` via `Hex.choosePrimeData?_prime`.
- Added the wrapper constructor
  `BHKS.FactorFastCapSeparationInputs.ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold`
  in the same file. It packages the paper-threshold bound on the
  input/auxiliary l2norm product into a `FactorFastCapSeparationInputs`
  record by composing
  `ofBridgeDataPointwiseAuxiliaryBounds` with
  `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree`, so the
  downstream `factorFast_terminates` consumer only needs the BHKS
  Theorem 5.2 inequality content (the `hpaper` hypothesis) plus the
  existing pointwise auxiliary bounds.

## Current frontier

Deliverables (2) and (3) of #6445 are closed by this slice:

- (2) The strict cap-arithmetic comparison
      `... < p ^ (k * localFactorDegree)` is dispatched by
      `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree` from
      the paper-threshold bound.
- (3) `ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold` instantiates
      the cap-separation input package directly from the canonical
      bridge data plus the paper-threshold bound.

Deliverable (1) — bounding the canonical
`coeffL2NormBound ^ aux.natDegree * auxiliaryBound ^ input.natDegree`
against `bhksPaperThresholdReal` for the actual cap-lift bad-vector bridge
— is BHKS Theorem 5.2 inequality content and is sized for a separate
session. It is routed through new issue #6452 (decomposed sub-issue,
blocked on the present partial PR).

## Next step

After the partial PR for #6445 lands, the next worker can pick up #6452
to discharge the canonical paper-threshold bound for the actual cap-lift
inputs. That work composes the canonical pointwise bounds at the BHKS
cut-radius shape with the BHKS paper-threshold factorisation
(`bhksPaperDegreeFactorReal * bhksPaperConstantFactorReal *
bhksPaperCoeffNormFactorReal * bhksPaperLogFactorReal`) and the
existing `factorFastCapLift_inputPolynomial_l2norm_le_coeffL2NormBound`
input-side fact.

## Blockers

No technical blocker for this slice. The remaining
deliverable (1) is decomposed into the sibling issue #6452.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `lake build` passed (full project, 244 jobs).
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Recovery.lean found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
