# 20260509T191603Z - Issue #2828

## Accomplished

Started by checking the unclaimed queue. Claimed #2851 first, then released it
for replan after verifying that its required Yun divisor helper from #2848 is
still only present in open PR #2852 and has not landed on `origin/main`.

Claimed #2828 and proved the private theorem
`inversionCount_inversePermutationValues_mod_two` in
`HexMatrix/Determinant.lean`. The proof peels the position of `Fin.last n`,
uses `insertAt_peelLastVector` to reconstruct the permutation, applies the
inserted-last inverse count theorem from #2830, applies the ordinary inserted
last inversion-count theorem to the original permutation, and closes the
modulo-2 equality by induction.

Committed the determinant change as:

- `c70d12e HexMatrix: prove inverse permutation parity`

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

## Current frontier

The inverse permutation inversion-count parity theorem is now available near
the `inversePermutationValues` proof cluster. This unblocks #2820, which can
use it to prove determinant sign preservation for inverse permutation vectors.

## Next step

Pick up #2820 and assemble `detSign_inversePermutationValues`, then continue
to the public `det_transpose` work in #2821.

## Blockers

The Yun current-tail coprimality issue #2851 is stale until #2848 / PR #2852
lands or is otherwise reconciled. The worktree still has the pre-existing
unrelated dirty `.claude/CLAUDE.md` change, which this session did not touch or
commit.
