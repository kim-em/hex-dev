# Accomplished

- Added function-agnostic proof-emitter helpers for caller assumptions, generated-node top facts, and ordered lists of already-established input evidence.
- Replaced hand-written input-soundness proofs in both the Mathlib-free contractor canary and the real-sine chain with the generic evidence list.
- Replaced the Mathlib-free generated-node previous fact, plus all three real-sine previous facts, with the generic fact-domain top theorem.
- Documented the versioned evidence table required by direct tactic emission.
- Built the core proof emitter and both conformance paths, then passed source, DAG, trust-surface, and diff checks.

# Current frontier

- The proof layer can now assemble every ordinary dependency premise without knowing which mathematical functions produced the facts.

# Next step

- Add a tactic-side schema-name registry keyed by exact replay address and emit the four replay applications from the reified trace in dependency order.

# Blockers

- None.
