# Release verification checkpoint

## Accomplished

- Measured the supported Hex integer-factorization function from clean revision
  `75291ae2fe6c58d95ba73dff9e5d2720df11b3d5` on Lean 4.32.2. The 392-row
  sweep answers 375 rows, records no factor-degree disagreement, and preserves
  the exact executable, corpus, host, CPU, cutoff, repetition, and protocol
  metadata.
- Regenerated the 25 cross-system cactus and runtime-by-degree figures using
  that Hex record and the unchanged 2026-07-28 FLINT, PARI/GP, NTL, and
  verified Isabelle records.
- Rewrote both current polynomial-factorization performance reports so they
  agree on the stable-toolchain artifact, coverage, paired comparisons, family
  results, and long tail without presenting retired diagnostics as public
  implementations.
- Obtained an independent review of the complete release diff. It found no
  correctness or proof regression and confirmed the namespace, compatibility,
  public-umbrella, toolchain, and performance conclusions.
- Addressed the review's release-publisher findings: Reservoir-style TOML
  requirements now receive exact external pins, unmanaged Lake formats and
  required test/development/executable roots are validated, and a failed later
  publication preserves every earlier pushed head in the release baseline.
  Nine publisher regression tests pass.
- Added manifest declarations for the complete Berlekamp-Zassenhaus proof
  closures and the LLL external-reduction executable. Prepared, but did not yet
  push, matching commits in the three released-repository skeletons so their
  current branches cannot refer to unpublished source.
- Replaced the last stale `hexlll_provider_probe` build target with
  `hexlll_external_reduction`, repaired prose damaged by earlier mechanical
  substitutions, corrected stale module references, and restored the detailed
  factor-tactic coverage contract in the manual.
- Removed two remaining file-wide Montgomery heartbeat settings. The only
  required replacement is a documented theorem-local budget for one
  bit-vector decision proof.
- Made the five headline `#print axioms` checks exact compiled regressions.
  Each reports only `propext`, `Classical.choice`, and `Quot.sound`.
- Rechecked all thirteen README Quickstart Lean blocks exactly as written,
  their required section order, the release prose vocabulary scan, the manual,
  release tests, affected source modules, release manifest, dependency DAG,
  corpus generator, and 408-file published trust surface.

## Current frontier

The refactor, stable-toolchain migration, documentation, trust checks,
performance refresh, independent review, and publication tooling are complete.
The remaining work is the final clean verification run, PR and CI, merge,
mirror publication, and fresh-clone standalone verification.

## Next step

Commit the reviewed cleanup and performance evidence, perform the final clean
build and runtime checks, then open and merge the PR before publishing all
released mirrors.

## Blockers

None.
