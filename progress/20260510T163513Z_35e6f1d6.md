## Accomplished

Claimed #2999 and proved `IsRREF.nullspace_sound` in `HexMatrix/RREF.lean`.
The proof uses the private `nullspace_echelon_sound` helper, transports
`D.echelon * b = 0` through `E.toIsEchelonForm.transform_mul`, and then applies
the left inverse from `E.toIsEchelonForm.transform_inv`.

Also renamed the local RREF nullspace fold helper to
`foldl_add_eq_acc_ring_echelon` to avoid colliding with the earlier private
helper of the same name in this file.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`
  reports only the pre-existing `sorry`s at lines 116 and 893.

## Current frontier

`nullspace_sound` is discharged. The remaining RREF nullspace theorem-level
frontier is `nullspace_complete`.

## Next step

Use `nullspace_sound` plus the pivot/free column decomposition to prove
`nullspace_complete`, keeping `rref_isRREF` and `spanCoeffs_complete` out of
scope unless a later issue explicitly includes them.

## Blockers

None.
