# PNT+ exponential point acceptance

## Accomplished

- Audited all twelve actual pinned `IEANTN/LogTables.lean` tactic sites left
  after the rational-log slice: nine exponential obligations, two nested-log
  bounds, and one π bound.
- Implemented the complete nine-site exponential family with one bounded
  Mathlib-free schema authenticating signed rational sources, rational Taylor
  steps, positive natural multipliers, direction, fourteen-term endpoints, and
  final rational cuts.
- Added one ordinary-kernel Taylor/power proof supporting lower and upper cuts,
  all nine original source statements, and generic `ProofFrontend` closure for
  the tight forty-factor `exp 20` row.
- Added all-row execution, no-draft unknown-source behavior, source/step/power/
  term/cross-row/cut mutations, a mathematically false endpoint, and guarded
  axiom reports.
- Updated SPEC and inventory evidence. The pinned inventory now has exactly
  three pending LogTables sites: two-sided `log (log 2)` and `π ≤ 3.15`.
  Chudnovsky's `proof_wanted` sum-to-π identity is explicitly not evidence.

## Current frontier

The exponential-point feature is complete locally as a direct child of
`7ad3a705ef83ba19d8f92709a987bdbb901f9666`. Focused conformance, both complete
experiment libraries, retained LogTables conformance, pinned-source inventory,
and structural checks are green.

## Next step

Preserve the local stack until the controller supplies a stable publication
base. The largest remaining coherent family is the two-sided nested `log
(log 2)` pair; the π site requires an independent proved identity or bound.

## Blockers

No blocker for the nine exponential sites. Mathlib's Chudnovsky development
does not currently prove that its series equals `π⁻¹`, so it cannot discharge
the remaining π statement.
