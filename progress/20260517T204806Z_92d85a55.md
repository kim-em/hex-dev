# Decomposed stale HO-1 umbrella (#2564)

## Accomplished

Claimed directive #2564 and verified that its issue body is stale against the
current tree: `libraries.yml` already has `HexBerlekampZassenhaus.done_through:
0` and `HexBerlekampZassenhausMathlib.done_through: 2`, while
`HexBerlekampZassenhaus/Basic.lean` already exposes the `Factorization`
surface, `factorSlow`, `factorFast`, `factorWithBound`, `factor`, and
`factorFastPrecisionCap`.

Decomposed the remaining visible zero-sorry frontier into narrower successor
issues:

* #4818: prove `Hex.ZPoly.isIrreducible_iff`.
* #4819: prove `factor_irreducible_of_nonUnit`.
* #4821: discharge `henselSubsetCorrespondence_analytic_obligation`.
* #4822: discharge `liftedFactorSubsetPartition_analytic_obligation`.

Left the required single-line decomposition breadcrumb on #2564 and released
the claim with `coordination skip`.

## Current frontier

#2564 is now an oversized umbrella whose original rollback and implementation
surface assumptions no longer match the repository. The remaining work is best
tracked by the four successor issues above, plus any already-open overlapping
HO-1 residual issues that a planner chooses to merge or close.

## Next step

Planner triage should close or rewrite #2564 around the successor issues. A
worker can then claim one of #4818, #4819, #4821, or #4822 directly.

## Blockers

None for the decomposition. No Lean build was run because no source files were
edited.
