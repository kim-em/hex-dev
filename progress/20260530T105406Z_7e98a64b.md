**Accomplished**

- Claimed #5776 and fixed `isGoodPrime`/`squareFreeModP` to test whether
  the executable modular gcd is a unit rather than structurally equal to `1`.
- Routed the small-mod singleton proof through the existing
  `isUnitPolynomial` common-divisor lemma, preserving the proof-facing
  square-free shape after the predicate change.
- Added regression coverage for `(x-1)...(x-11)`, `(x-1)...(x-24)`, and
  `(x-1)...(x-72)` in the BZ emitted fixture set and committed JSONL corpus.
- Added Lean cross-check guards for the `n=11` and `n=24` good-prime cases
  and a lightweight collision guard for the `n=72` small-prime search case.
- Verified:
  - `lake build HexBerlekampZassenhaus HexPoly`
  - `lake build HexBerlekampZassenhaus.CrossCheck`
  - `lake exe hexbz_emit_fixtures` matches
    `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl`
  - `python3 scripts/oracle/bz_flint.py --check`
  - `python3 scripts/check_dag.py`
  - `python3 scripts/status.py HexBerlekampZassenhaus`

**Current frontier**

- The tactical executable bug is patched locally in the BZ good-prime
  predicate. I did not normalize `DensePoly.gcd` globally because that breaks
  existing law/proof surfaces that intentionally expose raw `xgcd.gcd`.
- The `n=72` regression is pinned in the oracle fixture corpus rather than
  evaluated through `factor` in Lean cross-checks, because the direct Lean
  factor guard is too expensive for this target.

**Next step**

- Open the PR for #5776. A later architectural issue can decide whether to
  canonicalize `DensePoly.gcd` globally and update the corresponding law
  packages.

**Blockers**

- None for this PR.
