# 2026-05-10 18:43 UTC - twenty-eighth Phase 4/5 checkpoint (83f54206)

## Accomplished

Claimed summarize issue #3089 from a clean `agent/83f54206` worktree on
`origin/main` and recorded this checkpoint. The pre-existing pod-managed
`.claude/CLAUDE.md` modification and untracked `.claude/commands/` /
`.claude/skills/` were left untouched. No source code was changed.

Since the previous summarize issue #2947 closed at 2026-05-10T07:05:22Z,
58 PRs have merged in roughly 11 hours of intense work. The merged work
groups into eight reasonably distinct streams.

### HexBerlekampZassenhaus core (Mathlib-free)

- Modular field / image support: ZMod64 field instance and monic
  modular image (#3073), small-prime witnesses (#3064), prime chooser
  certificates (#3068).
- Normalization and reassembly: tightened normalization branch proof
  (#3077), primitive squarefree reassembly bridge (#3045), normalization
  product helpers (#3020), exact X-power extraction product (#3029).

### HexBerlekampZassenhaus Mathlib bridge

- BHKS lattice / recovery layer: projected lattice interface (#2972),
  bad-vector hypothesis package (#2994) and its executable
  instantiation (#2976), LLL delta rational bounds (#2957), L'=W
  separation interface (#3028) and the executable cap separation
  theorem (#3046), termination cap wrapper (#3031), and the
  forward-verification bridge `bhksRecover_eq_some_of_forwardInputs`
  proving BHKS recovery succeeds from L'=W (#3078).

### HexBerlekampZassenhaus bench / conformance / review

- Adversarial fixture and benchmark coverage: SD3 modular split smoke
  target (#2951), recombination pinned fixture (#2969), verify-safe
  fast adversarial singletons (#2970).
- Coverage audits and verdicts: HO-2 conformance audit and verdict
  (#2973, #3058), HO-4 theorem gap map (#2961), benchmark coverage
  map / verdict / audit (#2968, #3025, #3062).

### HexPolyFp Yun / square-free decomposition

- Square-free / quotient layer: squarefree gcd nonzero helper (#2965),
  squarefree power derivative helpers (#2993), constant divisor from
  arbitrary power divisibility (#3000), quotient derivative power
  divisibility helpers (#3009), squarefree quotient common divisor
  (#3030), quotient gcd successor (#3026).
- Yun loop layer: derivative quotient common divisor helper (#2956),
  derivative-split common-divisor base (#3041), reachable Yun common
  divisor provider (#3069), and a review of the square-free quotient
  proof path after the derivative-product counterexample (#2977).

### HexMatrix RREF / row-echelon

- RREF correctness chain: pivot lookup helpers (#3002), nullspace entry
  lemmas (#3012), forward row-span transport (#3013), row-span transport
  through echelon transform (#3015), echelon span solver reconstruction
  (#3021), nullspace echelon cancellation (#3038), spanContains
  completeness (#3040), nullspace soundness (#3044), row-operation
  multiplication lemmas (#3079).
- General matrix algebra used by RREF: matrix-vector transport lemmas
  (#2985), matrix multiplication laws (#3065).

### HexMatrix determinant column expansion

- Suffix-form expansion infrastructure: partial-prefix / partial-suffix
  expansion helpers (#2992), determinant partial column expansion helper
  (#2995), suffix determinant expansion endpoints (#3010), suffix
  coefficient fold helper (#3043), suffix assembly match-up helper
  (#3050).

### HexGramSchmidt prefix span

- Prefix-span helpers and invariance: two helper PRs (#2984, #3003) plus
  the invariance lemma (#3027).

### Cross-cutting / other

- HexArith Phase 4 evidence refresh (#3042), HO-4 precision-cap fix
  (#2911), irreducibility certificate projections (#3072), quarantine
  of the obsolete HexBZ recombination surface (#3070), and entry-level
  spec for `findPivot?` / `eliminateColumn` (#3081).

### Quality metrics at this checkpoint

- `python3 scripts/status.py`: same overall picture as the prior
  checkpoint - HexBerlekampZassenhaus remains formally at Phase 1 in
  `libraries.yml` (`done_through: 0`) while the SPEC-driven
  implementation, conformance, and bench streams continue to land
  inside Phase 1's loop. HexBerlekampZassenhausMathlib is at Phase 2
  (`done_through: 2`); HexLLL, HexPolyFp, HexGfqField,
  HexMatrixMathlib, HexModArithMathlib, HexGramSchmidtMathlib,
  HexPolyZMathlib, HexLLLMathlib, HexBerlekampMathlib,
  HexHenselMathlib, HexGF2Mathlib, and HexGfqMathlib are at Phase 4
  ("performance & benchmarking" loop). The remaining libraries sit at
  Phase 5 ("implementation work loop"). Planned-only libraries
  (HexRoots, HexRootsMathlib, HexResultant, HexResultantMathlib,
  HexNumberField, HexNumberFieldMathlib) remain at `done_through: 0`.
- `python3 scripts/check_dag.py`: passed (no output, exit 0).
- Lean `sorry` lines: 167 (down from 199 at the previous checkpoint;
  net -32 over 58 PRs). Heaviest residual concentrations are in
  `HexBerlekampMathlib/Basic.lean` (21), `HexGramSchmidt/Update.lean`
  (18), `HexLLL/Basic.lean` (12), `HexHenselMathlib/Correctness.lean`
  (12), `HexGF2Mathlib/Field.lean` (12), `HexGF2Mathlib/Basic.lean`
  (11), `HexBerlekampZassenhausMathlib/Basic.lean` (11),
  `HexGfq/CrossCheck.lean` (10), `HexGramSchmidt/Int.lean` (8),
  `HexGfqMathlib/GF2q.lean` (8), `HexGfq/EmitFixtures.lean` (8),
  and `HexGfqMathlib/Basic.lean` (7). The HexBerlekampZassenhaus
  capstone chain itself only has 3 sorries left in
  `HexBerlekampZassenhaus/Basic.lean` and 11 in the Mathlib bridge.
- Lean `axiom` declarations: 0.
- `native_decide` textual hits: one comment in
  `HexArith/CrossCheck.lean:141` (the same banned-tactic note as
  before).

### Open agent-plan frontier at the checkpoint

Unclaimed (work pickup queue):

- #2803 HexBZ Mathlib factor-entry irreducibility - depends on the
  executable invariant lane in `HexBerlekampZassenhaus/Basic.lean`.
- #3086 HexBZ core factor product preservation under bound hypothesis -
  one of three remaining gates for the capstone (#3087).
- #3088 HexPolyFp derivative-split Yun stepsSquareFree invariant -
  the only remaining `sorry` between the derivative-split Yun loop and
  the public `squareFree_factors_squareFree` theorem.
- #3092 HexPolyZ Gauss-style content multiplicativity for
  `ratPolyPrimitivePart` products - newly created precondition for
  Z-level reassembly.

Claimed and in flight:

- #3089 (this checkpoint).
- #3085 HexBZ core Z-level integer reassembly for `normalizeForFactor`
  (claimed pre-checkpoint, see PR #3082-class workflow on this lane).

With open PRs (`has-pr`):

- #3084 bridge `factorizationOfFactors` product to filtered array
  `polyProduct` (PR #3090) - capstone chain ingredient.
- #3074 suffix coefficient cast wrapper (PR #3091).
- #3048 bridge executable nullspace to Mathlib kernel (PR #3083).
- #2920 reachable Yun pairwise invariant provider (PR #3082).

Blocked:

- #3093 Z-level integer reassembly using HexPolyZ signed reassembly -
  blocked on #3092.
- #3087 capstone `factor_product_of_bound` - blocked on #3084, #3085,
  #3086.
- HexMatrix RREF residue: #3080 (eliminateColumn transform
  preservation), #3076 (suffix determinant after fold package), #3075
  (suffix RHS fold alignment), #3061 (row-operation inverse
  preservation), #3054 / #3053 (`rref_isRREF` from loop invariants and
  loop structural invariants).
- HexMatrixMathlib bridge: #3049 blocked on #3048.
- HexBZ core BHKS lattice basis independence: #2953 blocked on #2855.
- HexPolyFp pairwise coprime cluster: #2917 blocked on #2916, which is
  blocked on #2920.
- HexMatrix / GramSchmidt determinant nonnegativity chain: #2855 blocked
  on #3076; #2841 on #2840; #2840 on #2855; #2679 on #2841; #2674 on
  #2679. This loop still cannot start because of the
  #3076 -> #2855 -> #2840 cycle of dependencies recorded in the issues.
- HexBZ Mathlib `Factorization` capstone: #2833 blocked on #2803.

### Open PRs at the checkpoint

- PR #3091 `HexMatrix: package suffix coefficient cast wrapper` -
  merge BLOCKED, 1 success / 2 in-progress checks.
- PR #3090 `HexBZ core: bridge factorizationOfFactors product to
  filtered array polyProduct` - merge BLOCKED, 1 success / 2
  in-progress.
- PR #3083 `HexMatrix.RREF: prove nullspace completeness` - merge
  BLOCKED, 1 success / 2 in-progress.
- PR #3082 `HexPolyFp: assemble reachable Yun pairwise invariant
  provider` - merge BLOCKED, 2 success / 1 in-progress.
- PR #2656 `spec: add hex-real-roots and hex-sturm` - draft, all
  checks SUCCESS, planned spec-only PR.

### Human-oversight state

All five oversight directives remain open and `blocked`:

- #2564 HO-1 BZ recombination wrong lattice (van Hoeij CLD rewrite).
  No direct progress this period; surrounding BHKS support
  infrastructure (cap separation #3046, recovery bridge #3078)
  continues to land but the underlying lattice-rewrite has not
  begun.
- #2565 HO-2 BZ adversarial conformance. Coverage audits #2973 and
  #3058 record that current adversarial fixture coverage is
  meaningful but not yet exhaustive against the SPEC's adversarial
  taxonomy.
- #2566 HO-3 BZ `Bench.lean`. Audits #2968, #3025, #3062 map
  remaining benchmark gaps; #2951 / #2969 / #2970 added smoke and
  pinned-fixture coverage but the file-level coverage matrix is
  still incomplete.
- #2567 HO-4 BHKS Theorem 5.2 termination. Substantial bridge work
  landed (cap separation, termination cap wrapper, recovery bridge),
  but the leaf theorem is still gated on the unclaimed B7 / A2
  obligations summarised in `progress/20260510T180611Z_778bc6bc.md`
  and on the capstone chain #3084 - #3087.
- #2637 BZ Factorization record + sign/multiplicity bugs. Still
  blocked behind the same factorization chain.

## Current frontier

The single highest-leverage proof slice is the BZ core capstone chain
`#3084 -> #3085 -> #3086 -> #3087`, with #3084 already in PR (#3090)
and #3085 claimed. #3086 (factor product preservation) is unclaimed
and is the gating ingredient most directly blocking #3087. #3093
(Z-level reassembly using HexPolyZ signed reassembly) is the parallel
path now blocked on the freshly-cut #3092 (Gauss content
multiplicativity).

The Mathlib factorization capstone #2833 still cannot move until
#2803 (factor-entry irreducibility) lands, and #2803 is one of the
four currently-unclaimed work items. It is older (2026-05-09) than
the rest of the unclaimed queue and is the tightest practical
bottleneck on the BZ Mathlib bridge.

The HexPolyFp pairwise-coprime cluster (#2917 -> #2916 -> #2920) is
fully blocked behind PR #3082; once that lands, the cluster reopens
and #3088 (the only remaining sorry on the per-factor square-freeness
side) becomes the natural follow-up.

The HexMatrix / GramSchmidt determinant nonnegativity chain remains
the longest unresolved blocker. The dependency graph
(#3076 -> #2855 -> #2840 -> #2855 ...) suggests one of those issues
needs its dependencies revisited to break the apparent cycle; this
checkpoint flags it as a planning concern rather than dispatching it.

## Limitations and honest gaps

- Despite significant proof progress, `HexBerlekampZassenhaus` still
  reports `done_through: 0` in `libraries.yml`. The library has not
  cleared its Phase 1 conformance/bench/correctness gates - the SPEC
  for "factorization correctness" is still satisfied with sorries in
  the bridge. Counting merged PRs as "Phase 4/5 progress" is
  optimistic for this library.
- The HO-directed rewrites (#2564 in particular) remain logically
  unaddressed. The BHKS recovery bridge can only verify recovery
  succeeds *given* the current lattice; if the lattice is wrong per
  HO-1, the bridge does not save it.
- 167 sorries is the textual count, not a uniqueness count. Many
  sorries are in proof-only intermediate lemmas that may collapse
  together; the count overstates the number of independent obligations.
- The HexMatrix determinant chain's dependency cycle is real and
  cannot be papered over; either #3076's dependencies or #2855's need
  re-cutting before the chain can move.

## Next step

Fresh feature workers should prefer, in order:

1. #3086 (factor product preservation under bound hypothesis) -
   unblocks #3087 and unsticks the BZ core capstone.
2. #3088 (Yun stepsSquareFree invariant) - retires the only remaining
   sorry between the derivative-split Yun loop and
   `squareFree_factors_squareFree`.
3. #3092 (Gauss-content multiplicativity) - unblocks #3093.
4. #2803 (factor-entry irreducibility) - unblocks the Mathlib
   factorization capstone #2833.

Repair sessions should watch PRs #3090 / #3091 / #3083 / #3082 -
they are all merge-BLOCKED only on in-progress checks at checkpoint
time, and should land in the next CI cycle.

A planner cycle should look at the HexMatrix determinant chain's
dependency cycle (`#3076 - #2855 - #2840`) and re-issue one node
with a non-cyclic dependency set so that lane can move again.

## Blockers

None for this checkpoint. The pre-existing pod-managed
`.claude/CLAUDE.md` modification and the untracked `.claude/commands/`
and `.claude/skills/` directories were left untouched.
