## Accomplished

Reviewed `/Users/kim/.claude/plans/okay-let-s-make-a-eventual-treehouse.md` directly, focusing on transport robustness, IPC shape, race handling, and correctness gaps in the proposed `wait-for-ci` design.

Identified one major correctness omission: the plan models only `check_run` data, but required branch protection contexts can also be legacy commit statuses, so the current design can declare success or keep waiting incorrectly.

Identified two architectural concerns: the webhook path is being treated as more authoritative than it should be, and the subscribe/snapshot reconciliation logic is pushed into bash where it is harder to make race-free and operationally boring.

## Current frontier

The review is ready to send. The remaining work is only to present the findings in a concise issue-by-issue format with severity, plan locations, and concrete fixes.

## Next step

Send the opinionated review, recommending smee-over-Funnel for buffering but not as sole correctness machinery, keeping localhost HTTP while adding heartbeats/sequence semantics, moving covered-repo reconciliation into the daemon, and adding explicit support for `status` contexts.

## Blockers

None.
