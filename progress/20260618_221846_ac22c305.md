# Progress — 2026-06-18 (session ac22c305, /work → /feature #7989)

## Accomplished

Completed #7989: Phase 6 grind automation coverage for
`HexPolyFp/Frobenius.lean`.

Added `@[grind =]` alongside the existing `@[simp]` on all seven
equation-shaped public characterising lemmas of the Frobenius /
`powModMonic` reductions:

- `powModMonic_zero`
- `frobeniusXPowMod_zero`
- `modByMonic_powModMonic_pos_eq_self`
- `frobeniusXMod_mod_self`
- `modByMonic_frobeniusXMod_eq_self`
- `frobeniusXPowMod_mod_self`
- `modByMonic_frobeniusXPowMod_eq_self`

All seven are `LHS = RHS` self-reduction / idempotence rewrites
(`… % f = …`, `modByMonic f … = …`, zero-index closed forms), so they
are well-behaved `grind =` rewrites with no loop risk.

## Deliverable 2 (proof shortening)

No in-file proof shortens: the proofs in `Frobenius.lean` are bespoke
`calc`/`rw` chains over private helper lemmas (`powLinear_*`,
`powModMonicAux_*`), not closable by the characterising rewrites. The
payoff is **downstream `grind` availability** — these lemmas already
fired under `@[simp]`, and the `@[grind =]` addition makes them
available to `grind` in the consuming layers (`QuotientFrobenius`,
`Compose`/`ModCompose`, `SquareFree`, and `RabinSoundness`), per the
Phase 6 "maximize what simp and grind can close" criterion. This matches
the issue's premise note, which licenses recording downstream benefit
when no in-file proof shortens.

## Verification

- `lake build HexPolyFp` → 98 jobs, `Build completed successfully`. All
  downstream consumers (`SquareFree`, `Quotient`, `QuotientFrobenius`,
  `Compose`) rebuilt green; `Frobenius` itself built in 1.1s (no
  grind-loop/slowdown).
- No `warning`/`error`/`sorry` in build log.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `python3 scripts/check_dag.py` → exit 0.

## Current frontier

Frobenius.lean grind-annotation pass complete. Sibling Phase 6
grind-coverage issues remain unclaimed (#7995 HexHensel/Multifactor,
#7996 HexGF2/Irreducibility).

## Next step

Pick up #7995 or #7996 (same pass shape, different file/library).

## Blockers

None.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
