# Propagator framework review fixes

## Accomplished

- Made structural instance identity include sorted, deduplicated unordered
  equality endpoint pairs, including pure-equality duplicate detection.
- Restricted direct retained-proposal admission to a private helper; the public
  entry point accepts only an engine-owned `SuggestionId`.
- Prevented equality contraction while an external rule reply is pending and
  strengthened append-only application-prefix validation.
- Centralized trigger cardinality and reference checks in `proposalBounded`.
- Added deterministic effort and observation-value reply limits.
- Added conformance guards for canonical equality keys, repeated pure-equality
  selection, pending-reply exclusion, exact application prefixes, oversized
  observations, retry effort, and trigger lists.
- Built `HexInterval.PropagatorConformance`, the scheduler spike, and the full
  `HexIntervalExperiment` / `HexConformance` aggregate. The scheduler spike
  still reports 8 incremental calls versus 64 structural calls with checksum
  3615.

## Current frontier

The reviewed base-framework fixes are present and verified locally. They are
not committed or pushed, so the policy-frontier work can continue independently
and the base PR can receive these changes as one reviewed update.

## Next step

Review the focused diff, commit it on `agent/interval-propagator-framework`,
and update the base framework PR before rebasing the policy-frontier branch.

## Blockers

None.
