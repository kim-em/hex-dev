# HexPolyZ Rat divisibility helpers

## Accomplished

- Claimed feature issue #2126.
- Added private Rat polynomial divisibility closure helpers in `HexPolyZ/Basic.lean`:
  `rat_dvd_mul_left`, `rat_dvd_add`, and `rat_dvd_sub`.
- Verified the partial patch with `lake build HexPolyZ.Basic`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, and `git diff --check`.
- Created follow-up issue #2134 for the remaining Rat derivative product rule and left a `Decomposed into #2134` breadcrumb on #2126.

## Current frontier

#2126 is only partially complete: the downstream square-free proof now has local divisibility closure helpers, but the derivative product rule still needs a dedicated convolution reindexing proof over `DensePoly.mulCoeffSum`.

## Next step

Claim #2134 and prove the Rat derivative product rule, likely by reusing or exposing the existing diagonal-convolution machinery in `HexPoly/Euclid.lean`.

## Blockers

- `primitiveSquareFreeDecomposition_squareFreeCore` still has its pre-existing `sorry`.
- The worktree still has an unrelated pre-existing `.claude/CLAUDE.md` modification that this session did not touch.
