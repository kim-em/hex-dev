# Progress: #6866 — CI gate on HexBerlekampZassenhausMathlib build

Session type: feature (via `/work` → `/feature`).

## Accomplished

Claimed #6866 (add a merge-gating `lake build HexBerlekampZassenhausMathlib`
step to the existing single `build` job in `ci.yml`). All four declared
dependencies (#6855, #6973, #6819, plus the #6804 from #6773's chain) were
closed, so the issue was nominally workable.

Per the issue's explicit precondition ("Do not add the CI gate while the target
is still known red") and the project's directive-as-hypothesis doctrine, I
independently verified the build before landing anything. `lake build
HexBerlekampZassenhausMathlib` at `main` (commit a340aaec, exactly origin/main)
is **red**: 11 `Application type mismatch` errors in
`HexBerlekampZassenhausMathlib/Basic.lean` (lines 9544, 9555, 9654, 9961, 10453,
14401, 14523, 14746, 16140, 16522, 16622), plus a pre-existing `sorry` at
`Basic.lean:233`. #6855 (the prior compile-fix) is closed but the library
regressed again — the exact recurring gap this CI gate exists to surface.

The errors are the BZ-recovery candidate-shape mismatch: the recursive search
passes `hrep : RepresentsIntegerFactorAtLift core d factor S` to lemmas
expecting the old scaled modular equality
`(scaledLiftedFactorProduct core d S).reduceModPow d.p d.k = factor.reduceModPow d.p d.k`.

Actions taken (no code changes; ci.yml edit was drafted then reverted):

- Posted a comment on #6866 with the full error evidence and the first failing
  application (`recombinationCandidate_eq_factor_of_recovery_of_monic_core_of_bound`).
- Re-blocked #6866 via `coordination add-dep` on #7036, #7037, #7024 — the
  in-flight recovery-migration issues that eliminate the
  `scaledLiftedFactorProduct.reduceModPow` consumers and restore green.
- `coordination skip 6866` to release the claim with a clear, no-replan-needed
  reason; the issue is now `blocked` on those three issues.

## Current frontier

#6866 is blocked, not workable, until `HexBerlekampZassenhausMathlib` is green
again. The one-line CI gate itself is trivial and unchanged: append a sequential
`- run: lake build HexBerlekampZassenhausMathlib` step to the existing `build`
job after the bench build (its oleans are `Hex*`, already covered by the
`.lake/build` cache; Mathlib is already fetched and rebuild-gated). No new job,
matrix, or workflow — consistent with SPEC/CI.md.

## Next step

When #7036 / #7037 / #7024 land and `lake build HexBerlekampZassenhausMathlib`
is green, a worker re-verifies the build and lands the unchanged one-line gate.
`check-blocked` auto-clears the `blocked` label once the three deps close; the
`replan` label routes it through a planner who should re-confirm green before
re-queueing.

## Blockers

`HexBerlekampZassenhausMathlib/Basic.lean` red on main (11 type-mismatch errors
from the unfinished BZ-recovery candidate-shape migration). Cannot land a
merge-gating build of a red target without freezing the repo's merge pipeline.
