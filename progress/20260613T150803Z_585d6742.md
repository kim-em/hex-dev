# Progress: #6856 — common-factor Sylvester syzygy identities

## Accomplished

Decomposed the over-broad #6848 Sylvester common-factor column-reduction issue
into #6856, #6857, and #6858, with dependencies #6856 → #6857 → #6858, then
released #6848 for replan with the required decomposition breadcrumb.

Claimed #6856 and added the first reusable resultant-side kernel facts in
`HexBerlekampZassenhausMathlib/Resultant.lean`:

- `mul_X_pow_mem_degreeLT_of_natDegree_lt`;
- `neg_mul_X_pow_mem_degreeLT_of_natDegree_lt`;
- `sylvesterMap_commonFactor_syzygy`, showing the Sylvester map for
  `(q * a, q * b)` kills `(-a * X^t, b * X^t)` under explicit `degreeLT`
  bounds.

## Current frontier

#6856 now exposes the algebraic common-factor syzygy and basic shifted-degree
helpers. The remaining #6848 path is the monic triangular column-transform
step in #6857, followed by final explicit-witness divisibility packaging in
#6858 and the public `ZMod (p^k)` wrapper in #6849.

## Next step

Work #6857: use `q.Monic` and `q.natDegree = d` to package the shifted syzygy
directions as determinant-preserving integer column operations that expose
`d` Sylvester columns entrywise divisible by `m`.

## Blockers

No blocker for #6856. Full `HexBerlekampZassenhausMathlib` remains outside
this issue's verification scope; this session verified the targeted
`Resultant` module and DAG only.
