# 20260603T094849Z work #6478

## Accomplished

Added the Schur self-reading lemma
`getArrayEntry_scaledCoeffRowsSchur_eq_schurScaledCoeffEntry` in
`HexGramSchmidt/Int.lean`. The proof decomposes the outer row loop at row
`a`, decomposes the active column loop at column `c`, reads back the actual
write at `(a, c)`, and uses `schurSigma_congr_of_cell_eq` to replace the
partial write-state rows with the final `scaledCoeffRowsSchur` array on all
sigma-chain cells.

Moved `getArrayEntry_schurColumnLoop_col_not_mem` up near the other Schur
column-loop frame lemmas so the new theorem can use it at the requested
location. Added row-loop size preservation helpers needed to justify the
in-bounds `setArrayEntry` readback.

## Current frontier

The requested self-reading identity is now available immediately after the
`getArrayEntry_scaledCoeffRowsSchur_col_zero` block. It should be usable by
the downstream sigma-chain induction and composer issues for #6475/#6468.

## Next step

Use the new self-reading lemma in the sibling σ-chain induction/composer work
to eliminate the partial-state array from Schur per-cell recurrence calls.

## Blockers

None for this issue.

## Verification

- `lake build HexGramSchmidt` passed.
- Added-line forbidden-token grep for `sorry`, `axiom`, `native_decide`,
  `TODO`, and `FIXME` returned no matches.
