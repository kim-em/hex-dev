# HO-26: HexGfqRing FLINT `fq_default` informational comparator — skipped

Session: `9e5af24d-53b2-4ffe-972f-199fde585d45`
Branch: `agent/9e5af24d`
Issue: `#3674` (now `replan`)

## Accomplished

- Claimed `#3674` (HO-26). Title says HexGfqRing; body had a HexPoly
  typo (same shape as HO-23's HexPolyZ/HexPoly confusion noted on
  `#3671`). Read `SPEC/Libraries/hex-gfq-ring.md §"External
  comparators"` and confirmed HexGfqRing is the intended target.
- Smoke-tested the shared driver's `fq_default` family against a
  representative request shape and reproduced the python-flint
  failure mode: `ValueError: modulus must be irreducible` at
  `fq_default_ctx` construction. The existing `HexGfqRing/Bench.lean`
  `modulus degree` generator produces deterministic dense
  polynomials over `F_65537` that are not in general irreducible, so
  the named comparator does not run on the existing fixtures.
- Posted a technical-blocker comment on `#3674` laying out the three
  resolution paths (fixture rework via Rabin-tested irreducible
  search; SPEC primitive amendment to `nmod_poly` with explicit
  reduction; or drop the comparator). Skipped with `coordination
  skip` so the planner picks the route — neither fixture rework nor
  SPEC amendment is in HO-26's stated scope, and silently swapping
  `fq_default → nmod_poly+divrem` would mis-document the
  `libraries.yml` and SPEC entries.

## Current frontier

`#3674` is now `replan`. The planner has three options surfaced in
the issue comment; the choice affects the bench fixtures (option 1
— invasive but stays within `fq_default`) or `SPEC/Libraries/hex-gfq-ring.md`
(options 2/3 — non-invasive bench-side but each is a SPEC change).
At larger parameters on the existing `runAddChecksum`/`runNegSubChecksum`/`runNsmulNatCastChecksum`
ladder (`d` up to `16385`), Rabin testing for option 1 is the
practical bottleneck; the planner may pair it with a paramSchedule
reduction.

`#3675` (HO-27, HexGF2 NTL `GF2X`) is the only remaining unclaimed
HO. NTL is available on this host (`brew --prefix ntl` resolves; the
`extern_lib hexgf2ffi` pattern in `lakefile.lean` is a workable
template for an FFI shim). HO-27 needs a fresh wiring choice (FFI
vs persistent-subprocess driver), then new C/C++ code, then the
bench-side wiring and report regeneration — a full session in its
own right.

## Next step

- Planner: triage `#3674`'s `replan` and pick one of the three routes
  in the comment.
- Worker: HO-27 (`#3675`) — fresh tooling decision; pick FFI or
  process-call against the `lakefile.lean` `extern_lib hexgf2ffi`
  template versus a new `scripts/oracle/gf2_ntl_bench_driver.cc`.

## Blockers

- HO-26: python-flint `fq_default_ctx` precondition vs reducible
  bench fixtures. Documented on the issue.
- No blockers on HO-27 are known from this session.
