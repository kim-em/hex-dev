# b712abf1 - work #5994

## Accomplished

- Claimed feature issue #5994 after blocked directive claims were rejected by
  coordination.
- Optimized `Matrix.stepArray` so no-pivot Bareiss array updates preserve rows
  at or above the pivot instead of rebuilding the full row storage every step.
- Updated the `stepArray` pointwise proof and the downstream
  `HexGramSchmidt.Int` row-size invariant proof for the preserved-prefix shape.
- Ran `lake build HexMatrix HexGramSchmidt HexLLL` and
  `lake exe hexlll_bench verify`.
- Collected targeted harsh-cubic Lean/Isabelle evidence for `n = 45, 50, 55`
  in `reports/bench-results/hex-lll-b712abf1-harsh-cubic-rerun.json`.
- Updated `reports/hex-lll-performance.md` with the new evidence: the `n = 55`
  local ratio narrowed to `1.4691`, but the harsh-cubic gating verdict remains
  not met.
- Filed follow-up #6016 for deeper residual exact-integer operand reduction.
- Verified with `python3 scripts/status.py HexLLL`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token diff grep excluding the pre-existing
  `.claude/CLAUDE.md` worktree change.

## Current frontier

#5994 has a coherent prefix-preserving Bareiss array optimization plus benchmark
evidence. The remaining HexLLL Phase 4 blocker is still exact-integer
harsh-cubic operand growth in `GramSchmidt.Int.data` / `scaledCoeffRows`; #6016
tracks that residual work.

## Next step

Open the PR for #5994. Future workers should start from #6016 rather than
repeating the broad #5994 diagnosis.

## Blockers

No blocker for #5994. The pre-existing `.claude/CLAUDE.md` modification was left
untouched and excluded from this session's commit.
