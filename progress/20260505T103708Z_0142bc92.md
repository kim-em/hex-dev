# 2026-05-05 10:37 UTC — feature session (0142bc92)

## Accomplished

Claimed parent human-oversight issue #2245 and decomposed it into three
sub-issues (#2349, #2350, #2351), then claimed and executed #2349
(Conformance modulus_irreducible).

### Decomposition

The parent #2245 asked to discharge irreducibility-axiom sorries in
three `HexGfqField/` modules. Total residue is 31 `sorry`s (16 in
`EmitFixtures.lean`, 1 in `Conformance.lean`, 14 in `Bench.lean`); the
issue body's "single universally quantified Bench axiom" claim was
stale (the bench was already split into 14 per-degree sorries). The
three sub-tasks have very different shapes and tooling needs, so I
decomposed:

- **#2349** — Conformance.lean (1 sorry, x^4 + 2 over F_5).
- **#2350** — EmitFixtures.lean (16 sorries, small moduli p ≤ 7,
  n ≤ 6). Depends on #2349 as a worked template.
- **#2351** — Bench.lean (14 per-degree sorries, degrees up to 16385
  over F_8191). Distinctly harder: requires either an external
  irreducibility check at scale or a restructure of the deterministic
  modulus generator. Filed with an investigation phase before any
  proof work. Depends on #2349.

### Sub-issue #2349 (this PR)

- Added `HexBerlekamp` as a dep of `HexGfqField` in `libraries.yml`
  (no cycle: HexBerlekamp's deps are HexPolyFp/HexMatrix/HexGfqRing).
- Imported `HexBerlekamp.RabinSoundness` into
  `HexGfqField/Conformance.lean` to access
  `Berlekamp.rabinTest_imp_irreducible` and
  `Berlekamp.checkIrreducibilityCertificateLinear_rabinTest`.
- Added `modulus_monic : DensePoly.Monic modulus` (`by rfl`).
- Added `primeModulusFive : ZMod64.PrimeModulus 5` instance (needed
  by the Rabin bridge).
- Added `modulus_irreducibility_certificate :
  Berlekamp.IrreducibilityCertificate` with hand-computed pow chain
  `[X, 3X, 4X, 2X, X]` (verified against `X^4 ≡ -2 ≡ 3` and
  `X^5 ≡ 3X`) and Bezout pair `(3, 4 X^3)` for the maximal proper
  divisor `d = 2` of `n = 4`. Verified Bezout identity by hand:
  `3 · (X^4 + 2) + 4 X^3 · (3 X) = 3 X^4 + 1 + 2 X^4 = 5 X^4 + 1 = 1`
  in `F_5[X]`.
- Discharged `modulus_certificate_linear_check` via the same `simp`
  template used in `HexBerlekamp/Conformance.lean:252-270`, with
  five inner `rfl`s for `k = 0..4` (versus three for the existing
  degree-2 example).
- Composed `rabinTest_imp_irreducible` ∘
  `checkIrreducibilityCertificateLinear_rabinTest` to discharge
  `modulus_irreducible`.

## Verification

- `git diff --check` clean.
- `python3 scripts/check_dag.py` exit 0 (HexBerlekamp dep edge
  accepted).
- `rg "sorry" HexGfqField/Conformance.lean` returns 0 hits.
- `rg "^axiom |^private axiom " HexGfqField/Conformance.lean`
  returns 0 hits.
- Local `lake build HexBerlekamp.RabinSoundness` clean (493 jobs,
  only the two pre-existing foundational sorries on
  `degree_dvd_of_irreducible_dvd_xPowSubX` and
  `irreducible_dvd_xPowSubX_degree`, both out of scope here).

### Local build limitation (does not block correctness)

`lake build HexBerlekamp` (the umbrella .lean file) fails in this
worktree with `dyld: missing symbol called` after loading
`Hex_HexBerlekamp_RabinSoundness.dylib`. The error is environmental
(pre-existing on this worktree, unaffected by my changes; reproduces
on `main` after `lake clean Hex`; `HexBerlekamp.RabinSoundness`
submodule build alone is clean). It blocks
`lake build HexGfqField.Conformance` because that target pulls the
umbrella into the dep chain via the new `import
HexBerlekamp.RabinSoundness`. CI will rebuild from scratch in a clean
environment and is the authoritative check for the proof itself.

## Current frontier

`HexGfqField/Conformance.lean` now has zero sorries and uses the
project-side Rabin bridge that landed in #2291 / RabinSoundness. The
template (compute pow chain by hand from `X^n ≡ -tail-coeffs` and a
single Bezout pair per maximal proper divisor) is directly reusable
for the 16 EmitFixtures sub-issue (#2350) — likely with a small
helper lemma to reduce the per-modulus boilerplate.

## Next step

Pick up #2350 (EmitFixtures, 16 small moduli) using the same template
once #2349 lands. #2351 (Bench) needs the external investigation
phase first.

## Blockers

None for this issue. The local dyld error is pre-existing and CI is
the authoritative verifier.
