# Progress — 2026-06-18 (session a87b6cec, /work → /feature #7992)

## Accomplished

Premise-checked #7992 (floor-keyed BHKS bad-vector exclusion —
`no_projected_not_indicator` at the CLD-adequacy floor) and found the shape
**unsound**, super-exponentially. Posted a quantified diagnosis
(https://github.com/kim-em/hex/issues/7992#issuecomment-4746548516) and
`coordination skip`ped the issue (→ `replan`). No code changes.

### The obstruction

The single producer of `SeparationHypotheses.no_projected_not_indicator`
(`Lattice.lean:1858`) is
`no_projected_not_indicator_of_factorFastPrecisionCap_le`
(`TerminationBound.lean:508`). Its `False` is independent of the cap
hypothesis `ha` — it comes from `no_bhks_bad_setup_of_l2norm_upper_lt_divisor`
(`:361`) and reduces to the leaf field

    hlt : (l2norm input)^auxDeg · (l2norm aux)^inputDeg
            < p ^ (W.liftData.k · localFactorDegree)      -- :467

The only general handle on the LHS is the worst-case Hadamard chain
`bhksPaperThresholdReal_le_of_factorFastPrecisionCap_le` (`:222`), which lands
on **`bhksBound f`** (the load-bearing component of `factorFastPrecisionCap =
max bhksBound defaultFactorCoeffBound`, `Basic.lean:8028`). `cldCoeffFloor`
appears 0× in `TerminationBound.lean`, `Lattice.lean`, `BadVector.lean`.

`cldCoeffFloor = 2·maxⱼ bhksCoeffBound` (`Basic.lean:7020`) is `2n·‖·‖₂`-scale
(column adequacy — its own docstring says "only certifies column adequacy");
`bhksBound = 1+n·4^(n²)·(S+1)^n·(log₂(S+1))^n` (`Basic.lean:2830`) is
`4^(n²)·‖·‖₂^(2n)`-scale. Worked example `x²−5x+6`: floor = 32, bhksBound =
cap = 50 803 201 (both match the directive's numbers). At first success the
loop accepts at `k = floor` (`Basic.lean:7045`), lifting at
`precisionForCoeffBound floor p`, so `p^liftData.k ≈ 2·floor = 64`; the
divisor is a few hundred, the LHS up to 50 803 201 — `hlt` fails by ≈10⁵. The
`localFactorDegree` exponent does not rescue it:
`bhksBound/(2·floor)^n → ∞`.

This is the van Hoeij distinction: column adequacy (the #7980 floor gate,
hsep/hthr) vs. p-adic precision for bad-vector exclusion (the cap). Different
precisions by construction.

## Current frontier

#7992 is `replan`. The bad-vector exclusion genuinely needs cap precision.

## Next step

Owner-level re-scope of #7992: option 1 (force the loop / acceptance gate to
reach `k ≥ bhksBound` on the bad-vector-exclusion path, so `liftData.k`
reaches `precisionForCoeffBound bhksBound p` and the existing `:222` chain
discharges `hlt`). This changes the deliberate #7980 early-accept semantics,
so it is an owner decision, not a worker guess. Option 2 (a distinct
floor-level bad-vector bound) does not exist — the resultant Hadamard
magnitude is `bhksBound`-scale.

## Blockers

None for this session — diagnosis is the deliverable. #7917 / #7985 endpoint
remains blocked on a sound bad-vector-exclusion prerequisite.

Note: worktree carries a pre-existing unrelated `.claude/skills/
agent-worker-flow/SKILL.md` modification from before this session; not touched
or staged.
