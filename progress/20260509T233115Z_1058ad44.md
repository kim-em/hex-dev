# 20260509T233115Z - Issue #2834

## Accomplished

Claimed #2834 and narrowed the Yun-loop pairwise theorem in
`HexPolyFp/SquareFree.lean` so `yunFactors_pairwise_coprime_nil` now requires
the reachable/readiness `yunFactorsPairwiseInvariant` instead of asserting the
known-false arbitrary-state statement.

Added `squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant`, an
invariant-threaded derivative-active square-free core variant that passes the
derivative-split Yun invariant into both repeated-tail branches.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`

## Current frontier

The direct arbitrary wrapper `squareFreeAuxRev_pairwise_coprime_nil_core` still
has a proof hole. The new invariant-threaded core identifies the remaining
missing input precisely: a concrete provider of
`yunFactorsPairwiseInvariant` for every derivative-active branch.

## Next step

Prove the derivative-active invariant provider, likely by discharging the
`hmonic` and `hcommon` arguments to
`yunFactorsPairwiseInvariant_of_derivative_split_common_dvd_one`, then make
`squareFreeAuxRev_pairwise_coprime_nil_core` call the invariant-threaded core.

## Blockers

None at the build level. The remaining blocker is mathematical: proving the
reachable derivative-active quotient/gcd coprimality provider without using the
old arbitrary Yun theorem.
