# HexBZ direct-coordinate factorization

## Accomplished

- Replaced the transitional classical dispatcher with one direct-coordinate
  prime plan, lift, and recombination engine.
- Rebuilt CLD recovery around direct modular supports and removed the duplicate
  monic-coordinate CLD correspondence stack.
- Made the total dispatcher retain the classical prime plan and pass that exact
  plan to CLD after a typed decline.
- Split the former `PublicSurface` module into `FactorBound`,
  `Factorization`, `ModularPolynomial`, and `IrreducibilityCertificate`, with
  imports following their mathematical dependencies.
- Proved and implemented the direct-coordinate trailing-coefficient
  divisibility filter and carried degree/trailing residues through the subset
  tree instead of recomputing them at every leaf.
- Reduced the sampled SD5 runtime from roughly 3.0 seconds to 0.11 seconds,
  while retaining the Chebyshev and Legendre gains.
- Rewrote the current architecture documentation and verified the full build,
  conformance targets, FLINT oracle, deterministic trace gate, dependency DAG,
  benchmark import lint, lint tests, and benchmark smoke suite.

## Current frontier

The implementation, direct prefilter proof, cached-iterator proof, and full
build are green. The final Hex-only corpus sweep and performance report refresh
remain before opening the PR.

## Next step

Commit the verified source state, run the complete no-early-termination
`hex-factor` sweep against that commit, regenerate the comparison figures and
report, then open, review, and merge the PR.

## Blockers

None.
