# Progress — 2026-05-30T14:24Z — session 4939aaa0 (#5773 partial)

## Accomplished

Worked on directive #5773 (HO-5b): "rewrite hex-berlekamp-zassenhaus-performance.md
against hex/isabelle ratio verdicts". Two prior partial PRs (#5822, #5826) had
landed the per-rung Isabelle comparator registrations and a single-trial
public-combinator ladder. This session adds the fast-path and slow-backstop
ladders against the same Isabelle medians and corrects the public ladder
numbers.

- Ran a 3-trial Lean-only bench at commit `13eafd27` for `runFactorChecksum`,
  `runFactorFastChecksum`, and `runFactorSlowChecksum`. Per-rung spread ≤ 3.05%;
  the `n = 5` per-call median for the public combinator is `34.096 ms` —
  ~10× faster than the prior `2f4ef93` headline-ladder figure (`388.087 ms`,
  single trial). The earlier number was a one-shot cold/load-affected
  measurement at `outer-trials=1`; the present 3-trial sweep at the same
  shape is the stable steady-state.
- Replaced the `§Comparator Ratios / Split-family scaling ladder` table with
  the 3-trial data and rewrote the trend / gating-goal paragraphs. Headline
  gating ratio at `n = 5` drops from `0.4743` to `0.0417` (Lean ~24× faster
  vs `~2.1×`); the verdict is still **met**, by two orders of magnitude
  rather than the previously-reported single-order margin.
- Added two new sub-ladders: `§Split-family scaling ladder: fast path`
  (`n ∈ {2..5}`) and `§Split-family scaling ladder: slow backstop`
  (`n ∈ {1..4}`, with `n = 1` flagged as having no Isabelle pairing because
  no `runIsabelleSplitN1Checksum` exists). Both meet the gating goal with
  large margin.
- Added a `§Comparison to prior outer-trials=1 ladder` appendix subsection
  documenting the methodological reason for the headline number change.
- Added `runFactorFallbackProbeChecksum` (added by #5821) to the top-level
  `§Bench Targets` list.
- Updated `§Concerns` to drop the stale `0.4743 / extrapolation` bullets and
  replace them with the three-target coverage summary.

Export artefact:
`reports/bench-results/hex-berlekamp-zassenhaus-13eafd27-fast-slow-public-3trials.json`,
SHA-256 `7a693609e09587aa4651b12516fb9f04df28e4b3826843b03863c8d60c2ddf5f`.

## Current frontier

Three of the seventeen registered scientific bench targets now have ratio
verdicts (public / fast / slow combinator on the split family). The
remaining ones — `runFactorDegreeHeightChecksum`,
`runFactorFastDegreeHeightChecksum`, `runFactorSlowDegreeHeightChecksum`,
`runFastPathPrecisionLocalChecksum`, the five HO-2 adversarial singletons,
`runFactorFallbackProbeChecksum`, and the three compare-shape registrations
— all need new `setup_fixed_benchmark runIsabelle*Checksum` registrations on
their respective prepared inputs before they can produce ratio verdicts.
Adding those registrations requires `scripts/oracle/setup_bz_isabelle.sh`
to have produced a `bz_isabelle` binary on the bench host so that the
elaboration-time cross-check passes; that binary is not built in this
worktree (`.cache/oracles/bz-isabelle/` absent) and the AFP setup is
multi-hour first-run.

## Next step

A successor session on hardware where `bz_isabelle` is already built (or
with enough wallclock budget to build it once) should:

1. Add `runIsabelleDegreeHeightParam{...}Checksum` registrations for the
   `degreeHeightSchedule` rungs and pair against the existing
   `runFactorDegreeHeightChecksum` / `runFactorFastDegreeHeightChecksum` /
   `runFactorSlowDegreeHeightChecksum` Lean ladders.
2. Add `runIsabellePrecisionLocalParam{...}Checksum` registrations for the
   `precisionLocalSchedule` rungs.
3. Add the five HO-2 singleton Isabelle counterparts.
4. Decide whether `runFactorFallbackProbeChecksum` ratios are worth
   wiring — its inputs overlap with the split family at larger `n`.

Each of those additions extends `HexBerlekampZassenhaus/Bench.lean` along
the same `setup_fixed_benchmark` shape that #5826 used for the split
rungs.

## Blockers

`bz_isabelle` binary not present in this worktree's `.cache/oracles/` and
not present in the sibling worktrees either (only the main hex repo's
`.cache/oracles/lll-isabelle/` exists). Building it requires running
`scripts/oracle/setup_bz_isabelle.sh`, which downloads the
`afp-2026-05-29.tar.gz` archive, builds the AFP `Berlekamp_Zassenhaus`
session heap, and compiles the Haskell wrapper — multi-hour wallclock on
first run. No blocker for the present partial PR.
