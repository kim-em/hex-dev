# 2026-05-16T13:35Z — #4367 prefix-none assembler decomposition

## Accomplished

Claimed #4367 (prefix-none assembler for the partitioned recursive
recombination search) and discovered through proof-side analysis that
the issue body's suggested approach is missing one structural
prerequisite: an enumeration-order strengthening of
`liftedSubsetSplit_prefix_mem_of_matches` that produces an index
`i ∈ S \ T` for any prefix-recovered `T`.

The existing matched-prefix lemma at L2812 recovers `T ⊆ J` with
`J.min' hne ∈ T` for `split ∈ pre`, but says nothing about how `T`
relates to the chosen `S`. The assembler's contradiction step needs
this relationship — without it, the lemma is genuinely not provable
in general: a `T ⊋ S` (with `J.min' ∈ T`) has
`recombinationCandidate d T ∣ target` and the recursive search at
`T`'s rest factors can succeed, so the if-then-else at `T` returns
`some`, not `none`. The only way `T ⊋ S` is ruled out is via the
enumeration ordering of `Hex.subsetSplits`, which places `T ⊋ S`
*after* `S` (so it cannot be in `pre`).

Created sub-issue #4504 with the proposed mask-level helper statement
and subset-level wrapper, plus the proof outline (induction on `xs`,
four cases on `(b_S, b_T)`, with `xs.Nodup` discharging the
false-branch / true-branch disjointness step). Updated #4367 with
`depends-on: #4504` and a decomposition breadcrumb comment.

Then claimed #4504 myself and added a substantial proof-strategy
comment laying out the full induction with case-by-case argument and
the list-manipulation helpers from Mathlib that the proof needs
(`List.map_eq_append`, `List.map_eq_cons`, and a list-prefix-of-half
lemma for the Nodup-disjointness step). Skipped #4504 without
attempting the proof — the inductive list arithmetic is enough
substantive work to deserve its own session.

Also flagged a transitive prerequisite: `localFactors.Nodup` for the
final assembler. With `LiftedFactorListMatches d J localFactors`,
`localFactors = (J-filter of finRange).map (liftedFactor d)`, so
Nodup ↔ `liftedFactor d` injective on the J-filter. This isn't in
the current API; the consumer chain (#4367 → #4301) will need to
thread it through a dedicated injectivity lemma derived from the
partition's `pairwise_disjoint` field.

No code changes this session. Build state unchanged: warning sorries
at `HexBerlekampZassenhausMathlib/Basic.lean:160`, `:239`, and
`HexBerlekampZassenhaus/Basic.lean:5686` (all pre-existing).

## Current frontier

- #4367 is `replan` with `depends-on: #4504`. A future planner can
  either keep it as-is (residual scope = the assembler proper, once
  #4504 lands) or merge with #4504's scope if both are claimed
  together by a single substantial session.
- #4504 is `replan`. The roadmap comment is detailed enough for a
  next worker to claim it directly without re-orienting; the proof
  shape is well-understood, just needs the inductive list arithmetic
  written cleanly.
- The recursive coverage proof in #4301 (the ultimate target,
  blocked on #4367) will also need a separate `liftedFactor d`
  injectivity lemma derived from `LiftedFactorSubsetPartition`'s
  pairwise-disjoint field. That can be a future sub-issue if the
  consumer doesn't supply it inline.

## Next step

A `repair` or `feature` session claims #4504 with the proof shape
already in scope; once it lands, a fresh `feature` session takes #4367
(now unblocked) and writes the partition-uniqueness assembler against
the strengthened prefix lemma.

## Blockers

None for this session; the decomposition is itself the deliverable.
The remaining unclaimed feature work is #4334 (HexLLL bench), which
is still gated on host quietness — current host load average is 10+,
matching the conditions that previous workers skipped under.
