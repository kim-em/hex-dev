# 2026-06-02T02:30Z — issue #6101 (feature)

## Accomplished

Closed the long-standing `twoColDet_plucker_basisVec_of_lt_p1` deliverable
in `HexMatrix/Determinant.lean`:

- Added private helper `cofactorSign_succ_last_eq_neg`: when
  `x.val < n + 1`, `cofactorSign x (Fin.last (n+1)) =
  -cofactorSign ⟨x.val,_⟩ (Fin.last n)`. Sole consumer is the next
  theorem.
- Added private theorem `twoColDet_plucker_basisVec_of_lt_p1` exactly as
  specified in #6099 / #6101. The proof expands all six
  `twoColDet B (basisVec _) (basisVec _)` factors via
  `twoColDet_basisVec_basisVec_of_lt`, flips
  `cofactorSign p1 (Fin.last (n+1))` and
  `cofactorSign p2 (Fin.last (n+1))` to their `(Fin.last n)`
  counterparts (the `p3` `(Fin.last (n+1))` factor stays — it is common
  to all three Plucker terms), then closes with the canonical four-row
  `nDet` identity `nDet_plucker_four_row_canonical B q p1 p2 p3 hq1 h12
  h23` and `grind`.

## Current frontier

`HexMatrix.Determinant` builds clean; downstream `HexMatrix.Bareiss`,
`HexMatrix.Conformance`, and `HexMatrix` likewise. `python3
scripts/check_dag.py` exits 0. No `sorry`/`axiom`/`native_decide`/`TODO`/
`FIXME` introduced.

## Next step

PR ready; downstream consumers #6094 and #6044 can now drop the
`twoColDet_plucker_basisVec_of_lt_p1` shim.

## Blockers

None.
