# Issue #8417 — verify the lattice tier (van Hoeij / BHKS)

## Accomplished

Added `HexBerlekampZassenhausMathlib/LatticeTier.lean` (in the umbrella).
The lattice tier `Hex.latticeCoreFactorsWithBound` has three arms; the file:

- **`..._of_bhks`** (sorry-free): reduces the tier to the two BHKS obligations,
  proving **arm 1** (`factorsModP.size ≤ 1`, mod-p-irreducible) *unconditionally*
  via `squareFreeCore_irreducible_of_smallModSingletonBranch`.
- **unconditional top-level** `latticeCoreFactorsWithBound_squareFreeCore_factor_zpolyIrreducible`:
  arm 1 proved; arms 2/3 discharged by two explicitly-stated deep lemmas
  (`latticeArm2_fastCore_count`, `latticeArm3_bhksSingleAllOnes_irreducible`),
  each carrying ONE grep-able `sorry` (no axiom). Full library builds green.

Investigation (all per Kim's guidance): read the Klüners van Hoeij survey
(confirmed the approach is faithful, not doomed; irreducible case needs full
precision — no completeness+uniqueness shortcut); confirmed the PROVEN LLL path
(`HexLLLMathlib.lllNative_first_row_norm_sq_le_unconditional`,
`lllNative_mem_latticeSubmodule_iff`) applies to `lllNative`, which
`lll.shortVectorsUnchecked` (hence `bhksProjectedRows`) runs; comprehensive
inventory of existing vs greenfield theory (Codex second opinion agreed).

## Landed on the proven LLL path (sorry-free, builds green)

- `bhksLatticeBasis_basis_lowerZero` / `_diagPos` / `_basis_independent`: the van
  Hoeij knapsack basis `[I_r | Ã ; 0 | diag(p^(a-l_j))]` is upper-triangular with
  positive diagonal, hence LLL-independent, via the PUBLIC
  `Hex.GramSchmidt.Int.gramDet_pos_of_upperTriangular_pos_diag` (no de-privatizing
  needed) + the already-proven `bhksLatticeEntry_*` block lemmas + `getElem_ofFn`.
- `bhksLatticeBasis_lllNative_first_row_short`: the LLL short-vector bound applied
  to the bhks lattice — `‖(lllNative L.basis (3/4)).row 0‖² ≤ (1/(δ-1/4))^(n-1)·‖x‖²`
  for any nonzero lattice `x`. Direct use of the proven
  `HexLLLMathlib.lllNative_first_row_norm_sq_le_unconditional`.
- Added imports `HexGramSchmidtMathlib.Int.Swap`, `HexLLLMathlib.ShortVector`.

## Current frontier

Arm-3b (the deep van Hoeij adequacy `bhksSingleAllOnesPartition = true ⟹
Irreducible core`) is **greenfield** and needs, in order:

1. **Infra**: add `import HexLLLMathlib.ShortVector` to HexBerlekampZassenhausMathlib
   (currently NOT imported — only the Mathlib-free `HexLLL.Basic` is), and make
   `Hex.Matrix.independent_of_upperTriangular_pos_diag` public (currently
   `private` in HexLLLMathlib/Reducer.lean:71).
2. **`bhksLatticeBasis.basis` independence** (LLL-bound entry gate): the basis is
   `Matrix.ofFn (bhksLatticeEntry …)` = `[I_r | Ã ; 0 | diag(p^(a-l_j))]`,
   upper-triangular with positive diagonal (1's and `p^(a-l_j)`, needs `0<p`).
   Prove via `independent_of_upperTriangular_pos_diag` + `getElem_ofFn`
   (HexMatrix/Basic.lean:155, `@[grind =]`) + block lemmas
   `bhksLatticeEntry_topLeft/bottomLeft/bottomRight_offDiag/bottomRight_diag_pos`.
3. **CLD congruence** (greenfield, number-theoretic HEART): `cldQuotientMod f g p a`
   = coeffs of `f·g'/g mod p^a`; for a TRUE factor produce the integer witness
   `y` with `|y| ≤ bhksCoeffBound f j` and `y ≡ (cldQuotientMod…).coeff j
   (mod p^a)`, feeding the PROVEN `Hex.BHKS.abs_cldCoeffs_le_bhksCoeffBound`
   (Basic.lean:5199). This is the Landau-Mignotte/CLD column bound for true factors.
4. **LLL-bound → W membership**: apply `lllNative_first_row_norm_sq_le_unconditional`
   to `bhksLatticeBasis.basis`; relate `bhksWithinGramSchmidtCut`/`bhksCutRadiusSq4`
   to `‖·‖²`; at precision `bhksBound` (= Klüners Thm 3 bound, matches concrete
   `1+n·4^(n²)·(‖f‖²+1)^n·log₂^n`) short vectors ⟺ W (true-factor lattice).
5. **Equivalence classes = partition**: `bhksEquivalenceClassIndicators` (rowReduce
   over ℚ, kernel signature) = the true-factor subset partition; single all-ones
   class ⟹ W=⟨(1..1)⟩ ⟹ s=1.
6. **Arm-3a tail** (tractable, reuses #8413): no proper nonempty representing
   subset ⟹ Irreducible core, via `LiftedFactorSubsetPartition` cover/
   pairwise_disjoint/unique_up_to_associated + `liftedFactorSubsetPartition_of_
   toMonicPrimeData_complete`. NB prime-data subtlety: lattice uses
   `choosePrimeData? core`; partition lemma wants `toMonicPrimeData? core =
   choosePrimeData? (toMonic core).monic` — reconcile.

Arm-2 (`latticeArm2_fastCore_count`) is the same CLD completeness content
(count = #irreducible factors) via steps 3-5 applied to the fast-core recovery.

Already PROVEN & reusable: full executable pipeline + structural lemmas
(sorry-free); `abs_cldCoeffs_le_bhksCoeffBound` (given the witness);
`bhksProjectedRowsTrace_reducedMatrix_eq` (= lllNative L.basis); abstract LLL
bounds; the true-factor partition machinery.

## Next step

Do the infra (step 1) + basis independence (step 2) first — concrete, bounded,
proven-path entry gate — then the CLD congruence (step 3, the heart).

## Blockers

Full closure is a genuine multi-theory/multi-session formalization (confirmed by
the paper + a full inventory + Codex). NOT closable without an axiom this
session; the two `sorry`s are the honest, grep-able deep obligations. Structure,
reduction (arm 1 proven), and roadmap are landed.
