# Phase 6 grind coverage: HexGFqField/Operations.lean repr cluster

Session type: feature (`/work` → #8030)

## Accomplished

Promoted the `repr_*` representative cluster in
`HexGFqField/Operations.lean` from `@[simp]` to `@[simp, grind =]`,
mirroring the already-promoted `toQuotient_*` cluster. All 16 promoted
lemmas are literal `lhs = rhs` equation forms (15 `rfl`, plus `inv_zero`
which is `(0)⁻¹ = 0`):

`repr_natCast`, `inv_zero`, `repr_zero`, `repr_one`, `repr_add`,
`repr_mul`, `repr_neg`, `repr_sub`, `repr_pow`, `repr_div`,
`repr_zpow_ofNat`, `repr_zpow_negSucc`, `repr_intCast`, `repr_nsmul`,
`repr_zsmul`, `repr_frob`.

No lemma was excluded — none is iff / conjunction / `let`-bound.

## Grind-loop analysis

The `repr_*` and `toQuotient_*` clusters are inverse round-trips, but the
oriented `grind =` rewrites cannot cycle: `toQuotient_*` rules only fire
on composite `(op …).toQuotient`, whereas the `repr_*` RHSs contain
`x.toQuotient` only on atomic `x` (plus `GFqRing.repr`/`reduceMod`). No
oriented chain reproduces `repr (toQuotient x)`.

## Verification

- `lake build HexGFqField` → Build completed successfully (481 jobs);
  `HexGFqField.Operations` built in ~1.0s (no grind-loop slowdown).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`. Diff is exactly the 16 annotation changes.

## Current frontier / Next step

Issue #8030 fully complete. Remaining Phase 6 sweep issues (#8034, #8035)
are separate files.

## Blockers

None.
