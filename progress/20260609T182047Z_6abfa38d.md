# 20260609T182047Z (#4832)

## Accomplished

Added named initial-state support-containment projections in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `liftedFactorSubsetPartition_initial_support_subset_of_dvd_recombinationCandidate_of_choosePrimeData`
- `liftedFactorSubsetPartition_initial_support_subset_of_dvd_scaledRecombinationCandidate_of_choosePrimeData`

Both theorems expose the unscaled/scaled support fields as separate named
packages by projecting from the existing localized
`liftedFactorSubsetPartition_analytic_obligation`; no new analytic fallback was
introduced.  Updated `liftedFactorSubsetPartition_of_choosePrimeData`,
`liftedFactorSubsetPartition_of_toMonicPrimeData`, and
`liftedFactorSubsetPartition_of_choosePrimeData_success_descent` so their
support fields consume the named projections rather than destructuring anonymous
fields locally.

## Current frontier

Issue #4832 deliverables are complete. The support-containment fields now have
caller-facing names that can be used by downstream lifted-partition assembly.

## Next step

Downstream HO-1 partition and capstone work can refer to the named unscaled and
scaled support-containment theorems directly instead of depending on the full
`LiftedFactorSubsetPartition` constructor body.

## Blockers

None. Verification performed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token grep over `HexBerlekampZassenhausMathlib/Basic.lean`
  found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

The worktree also contained a pre-existing off-limits modification to
`.claude/CLAUDE.md`; it was not touched or staged.
