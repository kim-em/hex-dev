# HexPolyZ Phase 6 grind automation pass (#7952)

## Accomplished
- Added `@[grind]` automation coverage to `HexPolyZ/Basic.lean` (was 0
  grind annotations, 11 simp). 16 annotations added, no blanket pass:
  - Equation rewrites (`@[grind =]`): `dilate_one`, `coeff_toRatPoly`,
    `toRatPoly_zero`, `toRatPoly_C`, `toRatPoly_one`,
    `toRatPoly_scale_int`, `size_toRatPoly`, `toRatPoly_mul`,
    `content_mul`, `primitivePart_mul`, `content_mul_primitivePart`,
    `C_mul_eq_scale`. The cast/homomorphism lemmas (toRatPoly_*,
    content/primitivePart multiplicativity) are the characterising
    rewrites a downstream `grind` wants.
  - Unit closing-facts (`@[grind .]`): `isUnit_one`, `isUnit_C_one`,
    `isUnit_C_neg_one`, `isUnit_neg_one`. Used grind's own suggested
    apply-form kind (default `@[grind]` emitted a "Try this: [grind .]"
    info; `.` silences it and is the correct closing-fact behaviour).
  - Lemmas already carrying `@[simp]` kept simp and gained the grind
    kind (`@[simp, grind =]` / `@[simp, grind .]`); non-simp structural
    equalities got `@[grind =]` only, so simp normal forms are unchanged.
- Demonstrated payoff: `isUnit_of_eq_one` proof shortened from
  `rw [h]; exact isUnit_one` to `by grind`.

## Current frontier
- `HexPolyZ/Basic.lean` grind coverage done. `lake build HexPolyZ`
  green (Basic 1.8s, no slowdown vs baseline), no info/warnings,
  `check_dag.py` exit 0, diff --check clean, no new sorry/axiom.

## Next step
- Sibling issue #7955 (HexPolyFp/Basic.lean grind pass) is the parallel
  task. Other HexPolyZ Phase 6 surface (exit-criteria audit, done_through
  bump) remains out of scope for this issue.

## Blockers
- None. `isUnit_of_eq_neg_one` not shortened (its proof feeds
  `isUnit_neg_one`, so a grind there would be circular); left as-is.
