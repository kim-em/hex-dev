# 20260512T014438Z — feature 10f80423

Issue: #3653 — HexConway conformance: broaden SupportedEntry guards.

## Accomplished

- Extended `HexConway/Conformance.lean` with focused `#guard` coverage for
  two additional `SupportedEntry` values beyond `(2, 1)`:
  - **Binary higher-degree** `(2, 4)`, using the exported
    `supportedEntry_2_4` from `HexConway/Basic.lean`.
  - **Odd-prime** `(3, 1)`, constructed locally as
    `private def supportedEntry_3_1`. `HexConway/Basic.lean` exports
    `SupportedEntry` values only for `p = 2`, so the odd-prime case is
    built from the public `luebeckConwayPolynomial?_hit_3_1` lemma and a
    private inline `prime_three` witness mirroring the `private` Tier 1
    one in `Basic.lean`.
- For each new entry the guards check (a) `supportedEntry_*.poly` agrees
  with the corresponding `luebeckConwayPolynomial_*` declaration,
  (b) `luebeckConwayPolynomial?` returns `some` of the same poly,
  (c) `conwayPoly` returns the packaged polynomial, and
  (d) `0 < FpPoly.degree` of the packaged polynomial.
- Updated the file's docstring to record the broader covered properties
  and edge cases.
- `HexConway/Basic.lean`, the committed coefficient table, irreducibility
  proofs, benchmark registrations, and the `HexGfq` constructor layer
  remain untouched.

## Verification

- `lake build HexConway.Conformance` — clean.
- `lake build HexConway` — clean.
- `python3 scripts/check_dag.py` — clean.
- `python3 scripts/status.py HexConway` — `Phase 6 (proof polishing)`,
  `done_through: 5`, `ready: yes`.
- `git diff --check` — clean.
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexConway/Conformance.lean`
  — no matches.

## Current frontier

`HexConway/Conformance.lean` now exercises three distinct `SupportedEntry`
shapes (canonical `(2, 1)`, exported binary higher-degree `(2, 4)`,
locally-constructed odd-prime `(3, 1)`).

## Next step

Sibling Phase 6 conformance issue #3656 (`HexGfqRing` second quotient
example) follows a similar additive pattern. Issue #3652 (HexConway
benchmark dedup) and the broader `HexConway/Basic.lean` API review #3571
remain orthogonal.

## Blockers

None.
