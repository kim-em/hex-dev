# 2026-05-20T00:10 UTC — repair PR #5571 CI failure

## Accomplished

- Claimed and executed #5579 against PR #5571.
- Diagnosed the required-check failures as downstream `HexPolyFp.Basic` proof breakage caused by new global `[simp]` attributes on `ZMod64` ring identity lemmas.
- Kept the Phase 6 docstring/API additions but removed the new global simp attributes from the `add_zero`, `zero_add`, `mul_zero`, `zero_mul`, `mul_one`, `one_mul`, and `pow_zero` theorem cluster in `HexModArith/Ring.lean`.

## Current frontier

- `HexModArith` still exposes the documented ring identity lemmas, while avoiding the downstream simplification churn that broke PR #5571 CI.
- Verification passed locally: `lake build HexModArith.Basic`, `lake build HexModArith.Ring`, `lake build HexModArith`, `lake build HexPolyFp.Basic`, `lake build`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Next step

- Push the repair commit to PR #5571's head branch so CI can rerun.

## Blockers

- None.
