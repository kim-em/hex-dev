# 20260609T124024Z (#6659)

## Accomplished

Claimed and completed issue #6659 in `HexPolyFp/SquareFree.lean`.

Added a private constant-state substrate for `yunFactorsContributionWithLevel`:

- `pow_C_form`: powers of a nonzero `DensePoly.C a` remain nonzero constants.
- `constant_divisor_eq_C`: any divisor of a nonzero constant polynomial is a nonzero constant polynomial.
- `yunFactorsContributionWithLevel_constant_scalar_form`: if the active Yun `c` state is a nonzero constant, the recursive contribution is a nonzero constant and the residual is explicitly a nonzero scalar multiple of a witness polynomial.

The proof accounts for the non-terminal descent by showing
`DensePoly.gcd (DensePoly.C k) w` and the quotient
`DensePoly.C k / DensePoly.gcd (DensePoly.C k) w` are both nonzero constants,
then recursing through the gcd state.

## Current frontier

The constant-scalar excursion needed for the TF/FT cases of recursive scalar
synchronization is now available as a local private theorem. The full scalar
synchronization theorem from parent #6631 remains out of scope for this issue.

## Next step

Use `yunFactorsContributionWithLevel_constant_scalar_form` in the later
recursive scalar synchronization proof, alongside the existing asymmetric
one-step bridge.

## Blockers

None. Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexPolyFp/SquareFree.lean`

Note: `.claude/CLAUDE.md` was already modified at session start and was not
touched or included in this work.
