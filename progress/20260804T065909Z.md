# 2026-08-04 — issue #9157: root extraction for completely split modular images

## Accomplished

**Measurement gate.** Added `bench/HexBench/RootSplitProbe.lean`
(`hexbz_root_split_probe`), which prices the residue scan, the linear-factor
construction, and the `X^p mod f` certificate against `berlekampFactor` across
Wilkinson rows at their production-selected primes, synthetic completely split
inputs over a degree/field-size grid, wide-field controls, mixed-degree
controls, and controls sitting just inside the candidate budget. Record:
`reports/bench-results/hexbz-root-split-probe-f87a0c7d.json`; analysis:
`reports/berlekamp-root-extraction-measurement.md`.

Two findings decided the design:

* The `X^p mod f` certificate is *not* cheaper than the scan it would guard
  (295 µs against 237 µs at `d = 64`, `p = 67`) and is recomputed inside the
  fixed-space matrix afterwards, so it was rejected. The scan is its own
  certificate: the result is used only when the scan finds `deg f` roots, and
  that length test is what the reconstruction proof consumes.
* A budget of `25 · p ≤ (deg f)^2` — the point where the scan costs about a
  twenty-fifth of the matrix build it might replace — captures every Wilkinson
  row while holding an admitted-but-rejected scan to 0.8–2.4% of
  `berlekampFactor`.

**Implementation.** `HexBerlekamp/LinearFactors.lean` consolidates the monic
linear-factor lemmas that the Rabin `X^p - X` product identity and the new path
share (they were private in `RabinSoundness/RabinShape.lean`), adds root
enumeration `rootsIn`, and proves `eq_foldl_rootsIn_of_length`: a monic `f`
whose scan finds `deg f` roots is the product of the corresponding monic linear
factors. `Hex.Berlekamp.berlekampFactor` now has one selection point and two
branches; every public theorem about its output is proved for both.

**Evidence.** Paired phase profiles and corpus sweeps were measured against a
true baseline built from a `/tmp` worktree at `f87a0c7d` (the committed profile
predates three merged perf commits, so comparing against it would have
over-credited this change). `wilkinson_40/48/56` totals fall 29.3% / 26.6% /
30.3%, their `berlekampFactor` calls 94.5% / 94.3% / 95.3%. Every other
profiled row reports `rootExtraction: false` and moves inside the driver's
noise band. The sweep solves the same 377 of 392 instances for 2.0% less total
time. All 25 cactus and runtime-vs-degree figures regenerated.

**Budget revision.** The first budget was the economic test alone
(`25 · p ≤ (deg f)^2`), which admitted a wasted scan on about half the corpus
and showed 2.5–5% regressions on the sub-millisecond rows. Adding the
*necessary* test `deg f ≤ p` — `F_p` has only `p` elements, so a scan of a
higher-degree input can never find `deg f` distinct roots — removed the scan
from every non-Wilkinson corpus row at no cost, and the regressions with it.

**Second opinion.** Codex found no soundness defect and confirmed the
degenerate-input, Nat-arithmetic, non-squarefree, theorem-preservation,
lemma-move, and factor-ordering analyses. Its one substantive finding was that
`rootsIn` filtered `ZMod64.values p`, allocating one residue per field element,
while `rootScanBudget` bounds the scan only *relative* to the kernel
computation. Replaced with the tail-recursive `rootsBelow` loop, which conses
only the roots it keeps; `rootsIn_eq_filter` proves the loop is the filter. Two
smaller findings (an overstated `berlekampFactor` docstring, a repeated scan in
the diagnostic driver) were also addressed.

PR: https://github.com/kim-em/hex-dev/pull/9172

## Current frontier

PR open, awaiting CI. `python-flint`, PARI, NTL, and Isabelle are not installed
in this session, so the sweep is hex-only; the plot pipeline carries the
external comparator curves over from `hexbz-factor-sweep-aa68c920-chungus2` as
designed.

## Next step

Merge once CI is green.

## Note for a successor

The phase-profile driver's run-to-run spread on the small corpus rows is up to
3.1% for *identical* code. Any future claim of a sub-3% effect on those rows
needs repeats, not a single paired run.

## Blockers

None.
