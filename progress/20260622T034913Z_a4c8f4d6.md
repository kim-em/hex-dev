# Non-monic BHKS CLD coefficient bound (#8289)

Session type: feature (one-shot `/once`). UTC 2026-06-22T03:49.

## Accomplished

Delivered the non-monic BHKS Lemma 4.1 coefficient bound (#8289), the single
analytic obstacle to the core-coordinate forward cut, in
`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`.

**Premise correction.** The issue asks to drop `hg_monic` from
`abs_phi_coeff_le_bhksCoeffBound`, keeping the conclusion stated over
`phi (toPolynomial f) (toPolynomial g)`. But `phi f g = (f * g').divByMonic g`
and `Polynomial.divByMonic _ q = 0` whenever `q` is **not** monic. So the
literally-stated theorem is *vacuously* true for exactly the non-monic case it
claims to newly cover (LHS `= 0`); it says nothing about the real CLD column.
The genuine column is `Φ(g) = f·g'/g = h·g'` (for `f = g·h`), which `phi` only
computes when `g` is monic. I therefore stated the bound over the real column
`h * g'`.

**New lemmas (all monic-free, no `sorry`/`axiom`):**
- `BHKS.map_h_mul_deriv_eq_sum` (private) — partial-fractions decomposition of
  `(h * g').map ℂ` over the roots of `g.map ℂ`; the leading coefficient of `g`
  is absorbed into each `rootDeletionDerivativeSummand`, so no monic needed.
- `BHKS.natDegree_summand_le'` (private) — per-summand degree bound under
  `g ≠ 0`, `h ≠ 0` (replacing `Monic.natDegree_mul'` with the domain
  `Polynomial.natDegree_mul`).
- `BHKS.abs_factorDeriv_coeff_le` — `|coeff_j (h·g')| ≤ C(n-1,j)·n·‖f‖₂` for
  `f = g·h` over `ℤ`, no monic hypothesis. Generalises
  `abs_phi_coeff_le_of_monic_factor` (when `g` monic, `h·g' = phi f g`).
- `BHKS.abs_factorDeriv_coeff_le_bhksCoeffBound` — the ZPoly executable form:
  `|coeff_j (h·g')|.natAbs ≤ Hex.bhksCoeffBound f j`, the deliverable the
  core-coordinate cut consumes.

The analytic core (`derivative_eq_sum_rootDeletionDerivativeSummand`,
`mahlerMeasure_rootDeletionDerivativeSummand_le`,
`mahlerMeasure_h_mul_rootDeletionDerivativeSummand_le`) was already monic-free;
the only two uses of `hg_monic` in the old proof were `phi_eq_factor_mul_derivative`
(the `divByMonic` identity) and `Monic.natDegree_mul'` (degree of a product),
both replaced.

`lake build HexBerlekampZassenhausMathlib` green (3784 jobs). The only `sorry`
is the pre-existing `factor_irreducible_of_nonUnit` placeholder in
`FactorSoundness.lean:18` (untouched).

## Current frontier

The non-monic coefficient bound is landed over the real column `h·g'`. The old
monic `phi`-stated lemma and its consumers (lines ~1167/1306/1543) are
unchanged — no API breakage.

## Next step

- Corollary-4.2 L2 form (`‖Φ(g)‖₂ ≤ 2^{n-1}·n·‖f‖₂`) — not done this session;
  the coefficient bound is the analytic obstacle, the L2 form is a derived
  packaging.
- Re-point the core-coordinate-cut plumbing from `phi f g` to the real column
  `h·g'` for non-monic true factors, consuming
  `abs_factorDeriv_coeff_le_bhksCoeffBound`. The CLD residue bridge
  (`residue_eq_phi_coeff_of_congr`) is still monic-only and will need the
  analogous non-monic restatement.

## Blockers

None. Posted a comment on #8289 documenting the `phi = divByMonic = 0` premise
subtlety and the real-column restatement.
