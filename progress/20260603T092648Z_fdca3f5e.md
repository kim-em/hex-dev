# 20260603T092648Z work #6475

## Accomplished

Landed two private supporting helpers in `HexGramSchmidt/Int.lean` for
the Schur ≡ Bareiss σ-chain invariant proof (#6475):

* `getArrayEntry_schurColumnLoop_col_not_mem` — column-loop stability
  for a cell `(row, col_target)` when the loop never visits
  `col_target`. Mirrors `_col_zero_preserve` for arbitrary columns.

* `noPivotLoop_initial_gram_diag_ne_zero_of_lt` — for `q < p` with the
  no-pivot pass `noPivotLoop p` on `gramMatrix b` non-singular, the
  `q`-th diagonal pivot `M_q[q][q]` is nonzero. Composed from existing
  `noPivotLoop_prefix_none_of_final_none`,
  `noPivotLoop_initial_gram_step_eq_of_prefix_none`,
  `Matrix.noPivotLoop_singular_branch`, and `Matrix.noPivotLoop_add`.

Decomposed the remaining D2-prerequisite work into three sub-issues
that capture the natural fault lines of the proof:

* #6477 — σ-chain induction (foldl ≡ Bareiss correction term under
  outer-IH cell hypothesis); the "math heart" of the proof.
* #6478 — self-reading lemma
  (`R[a][c] = schurScaledCoeffEntry R gram a c` for `c ≤ a`); the
  loop-frame bridge that lets the outer strong induction substitute
  σ-chain reads with `R_final` values.
* #6479 — main composition theorem with strong induction on `p`,
  depending on #6477 and #6478.

#6477 and #6478 are independent and can run in parallel.

## Current frontier

The two landed helpers are immediately consumable by #6477's σ-chain
proof (`noPivotLoop_initial_gram_diag_ne_zero_of_lt` provides the
exact-div divisibility witness) and by #6478's self-reading proof
(`getArrayEntry_schurColumnLoop_col_not_mem` is the column-loop frame
the self-reading argument needs for the column suffix `[c+1..a+1)`).

## Next step

A subsequent worker can claim #6477 or #6478 independently. After both
merge, #6479's strong-induction composition is short — just
σ-chain + self-reading + outer IH + algebra.

## Blockers

None for the landed helpers. The decomposition routes the remaining
work without leaving anything dangling.

## Verification

- `lake build HexGramSchmidt` clean.
- No new `sorry`. No new `axiom`. No `native_decide`.
- Diff: 91 lines (two private lemmas + docstrings).
