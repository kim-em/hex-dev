# 20260603T082929Z work #2567

## Accomplished

Added the structured corrected `*AndPaperThreshold` rung of the HO-4
assembly chain in `HexBerlekampZassenhausMathlib/Recovery.lean`:

* `FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold`
  — composes `ofBridgeDataCorrectedAuxiliaryL2normSq` with the
  `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree` chain, so the
  strict cap-arithmetic comparison is dispatched internally once the caller
  supplies the structured corrected RHS sum bound plus the paper-threshold
  inequality.
* `factorFast_terminates_ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold`
  — final-step wrapper composing the above with `factorFast_terminates`.

These are pure compositions of existing infrastructure (no new analytical
content).

## Current frontier

The three sibling `*AndPaperThreshold` cap-separation builders now form a
complete orthogonal axis on the auxiliary-bound shape:

1. `ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold` —
   pointwise vector/correction estimates.
2. `ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold` (new) —
   structured corrected RHS sum bound.
3. `ofBridgeDataAuxiliaryL2normAndPaperThreshold` — direct `‖aux‖ ≤ B`
   bound.

Each has a matching `factorFast_terminates_*` final wrapper. The remaining
HO-4 analytical content is unchanged from the previous session:

* Derive a concrete `‖aux‖ ≤ B` (or structured RHS) estimate for the
  actual cap-lift bridge from L'-membership (BHKS Lemma 3.2).
* Prove the `hpaper` paper-threshold-shape inequality
  `coeffL2NormBound^aux.natDeg * B^input.natDeg ≤ bhksPaperThresholdReal core C`
  for the actual cap-lift bridge — the genuine BHKS §5 Theorem 5.2 content.

## Next step

The largest remaining analytic gap is still the paper-threshold inequality
(item 2 above).  An alternative incremental path is to specialise any of the
three `*AndPaperThreshold` builders to a fixed bridge-data accessor for its
respective bound shape, so the corresponding hypothesis becomes derived rather
than caller-supplied.

## Blockers

None.  The new wrappers are pure compositions of existing infrastructure.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing.
