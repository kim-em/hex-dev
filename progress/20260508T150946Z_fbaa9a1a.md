## Accomplished

- Claimed human-oversight issue #2709 for the HexPolyFp Phase-4 audit reset.
- Re-evaluated `SPEC/Libraries/hex-poly-fp.md` and `HexPolyFp/Bench.lean` against the post-#2687 Phase-4 evidence requirements in `PLAN/Phase4.md`, `SPEC/benchmarking.md`, and `SPEC/profiling.md`.
- Confirmed `hexpolyfp_bench` has 6 existing registrations and that `lake exe hexpolyfp_bench list` plus `lake exe hexpolyfp_bench verify` pass, with only pre-existing `sorry` warnings in `HexPolyFp/SquareFree.lean`.
- Rolled `HexPolyFp.done_through` from 4 to 3 in `libraries.yml` because the required headline report, `phase4.input_families` metadata, scientific verdict traceability, profile evidence, and empty Concerns section are absent.
- Filed re-promotion follow-up #2732 and marked it blocked on #2698.
- Linked the #2709 audit result from umbrella audit #2690.

## Current frontier

- `HexPolyFp` is no longer claiming Phase 4 without the new evidence package.
- Existing benchmark registrations are still wired and smoke-check successfully, so #2732 can focus on metadata, scientific runs, profiling, headline reporting, and re-promotion if Concerns are empty.

## Next step

- Complete #2698 so structured `phase4` metadata is parsed consistently, then work #2732 to generate the HexPolyFp Phase-4 evidence package and re-promote only if the report satisfies every exit criterion.

## Blockers

- #2732 is blocked on #2698 for structured `phase4.input_families` metadata support.
