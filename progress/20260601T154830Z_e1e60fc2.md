# e1e60fc2 - work #4170 (one-shot dispatch)

## Accomplished

- Claimed #4170 via the one-shot dispatch path.
- Read the four prior skip analyses (sessions d657773b, 083a8b57, 61b890c5, plus the issue-author comment that removed `blocked` after #5962 / #5957 landed).
- Audited `SlowPathHenselSubstrate` (`HexBerlekampZassenhausMathlib/Basic.lean:17375-17398`) and its constructor `slowPathHenselSubstrate_of_choosePrimeData` (line 17420).
- Audited the singleton substrate `smallModSingletonBranchPreconditions_of_choosePrimeData` and `squareFreeCore_irreducible_of_smallModSingletonBranch` (`HexBerlekampZassenhausMathlib/IntReductionMod.lean:2731-2776`) — these are unconditional under `hf_ne + hchoose` and are dispatchable today.
- Audited all four slow-exhaustive umbrellas (`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData(_of_bound)`, `_aux(_of_bound)`) and confirmed each routes lifted-factor monicness / positive-degree / injectivity through `Hex.ZPoly.toMonicLiftData_liftedFactor_*_of_monicPrimeData`, which requires `hselected : Hex.ZPoly.toMonicPrimeData? sf = some primeData`.
- Confirmed the new `slowPathHenselSubstrate_of_choosePrimeData` only derives `hselected` from a `hcore_monic` hypothesis (via `toMonic_monic_eq_core_of_leadingCoeff_eq_one`). For non-monic `(normalizeForFactor f).squareFreeCore` (the public-factor input), `hcore_monic` does not hold and the substrate does not apply.
- Posted detailed analysis comment + skipped via `coordination skip 4170 …` with the recommendation that a successor sub-issue add a non-monic-core `SlowPathHenselSubstrate` constructor consuming `hchoose + Primitive + 0 < lc + Squarefree` (matching the actual `squareFreeCore` invariants).

## Current frontier

- Single-issue scope of #4170 still hits the same gap the three previous worker sessions described: the substrate prerequisites partially landed but only for the monic-core sub-case.
- The singleton arm is dispatchable today; the constant / fast-quadratic / slow-quadratic arms have unconditional umbrellas; the slow exhaustive arm and the fast-BHKS arm remain blocked.

## Next step

A planner should open the non-monic substrate sub-issue (option (1) in the analysis comment). Once that lands, #4170 reduces to assembly + BHKS-conditional close as the issue body already describes.

## Blockers

- Missing `SlowPathHenselSubstrate_of_choosePrimeData_of_primitive_pos_lc` (or equivalent) — see analysis comment for the two options.
- Fast-BHKS arm gated on directive #2567 (out of scope per the issue body).
