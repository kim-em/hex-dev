# f037aa35 - work #6012

## Accomplished

- Claimed #6012 after confirming there were no unclaimed directive issues.
- Added `ordered_four_det_nMatrix_eq_nDet` in `HexMatrixMathlib/Determinant/Core.lean` to expose the base ordered minor as a named `nDet` rewrite.
- Added `ordered_four_det_mul_det_setRow_setRow_eq_det_setRow_mul_sub`, rewriting the ordered two-row replacement identity from cofactor-row pairings into row-replacement determinants.
- Verified the slice with `lake build HexMatrixMathlib`, `git diff --check`, and a forbidden-token grep over the touched Lean diff.
- Closed accidentally-created duplicate #6078 after finding the existing residual split #6074/#6075/#6076, and commented on #6012 with the decomposition breadcrumb.

## Current frontier

#6012 is partially advanced. The bridge layer now has the determinant-only identity needed immediately before the signed row-permutation transports.

## Next step

Use the existing split issues:

- #6074 for the two `B[p1]` one-row transports;
- #6075 for the two `B[q]` one-row transports;
- #6076 for the double-row transport and final sign normalization into the raw ordered four-row `nDet` Plucker kernel.

## Blockers

- No technical blocker for this partial slice.
- A pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and is not part of this work.
