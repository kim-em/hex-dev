# 20260601T135136Z — 8775133b: #5958 structured Schur two-pair obstruction

## Accomplished

Checked directive issues first; #2637, #2567, and #2564 were all blocked by
declared dependencies. Claimed #5930, verified its prerequisite chain was stale
because #5929 had been closed for replan and replaced by blocked #6006, then
marked #5930 for replan with that reason.

Claimed #5958 and implemented
`Polynomial.schur_structured_two_pair_ne_zero` in
`HexPolyZMathlib/SchurSzego.lean`. The theorem normalizes with
`A = a / z` and `B = b / z`, derives `‖A‖ < 1` and `‖B‖ < 1` from
`0 < r`, `r < ‖z‖`, `‖a‖ ≤ r`, and `‖b‖ ≤ r`, then applies the existing
closed-disk Schur fractional inequality to rule out a zero of
`(z + a*c) * (z + b*d) + (z + a*d) * (z + b*c)` under
`‖c‖ ≤ 1` and `‖d‖ ≤ 1`.

## Current frontier

#5958 is complete and ready for PR. The new theorem is derivative-adapter-free
and independent of `Polynomial.roots`, so it is ready for the downstream #5950
degree-two Schur kernel.

## Next step

After this lands, continue with #5950 to consume the structured obstruction in
the two-interior degree-two source proof.

## Blockers

None for #5958. #5930 remains blocked/stale until the replacement
HexMatrixMathlib chain reaches #6006.

## Verification

* `lake build HexPolyZMathlib` — green.
* `python3 scripts/oracle/mahler_schur_counterexample.py` — exits 0 and prints
  the expected endpoint-inequality counterexample.
* `git diff --check` — green.
* `git diff -- HexPolyZMathlib/SchurSzego.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true` —
  no matches.
