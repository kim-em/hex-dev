# Issue #4160

## Accomplished

- Attempted directive #3945 first; `coordination claim` rejected it because it
  needs replan.
- Claimed #4006, verified it is stale because its #4180 prerequisite was
  superseded by open coverage issue #4212, then released it with
  `coordination skip`.
- Claimed feature issue #4160.
- Added the explicit `HexPolyFp.Compose` import to
  `HexBerlekamp/RabinSoundness.lean`.
- Proved `primeFieldProduct_witness_eq` by transporting
  `primeFieldProduct_X_eq_xPowSubX` through
  `FpPoly.compose_primeFieldLinearProduct` and `compose_xPowSubX_one`.
- Added
  `dvd_primeFieldProduct_witness_of_dvd_linearPow_sub_self`, the downstream
  divisibility corollary from `f ∣ linearPow w p - w` to the witness product.
- Verified:
  - `lake build HexBerlekamp.RabinSoundness`
  - `lake build HexBerlekamp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - diff-only search for new `sorry`, `axiom`, `native_decide`, `TODO`, or
    `FIXME`

## Current frontier

- The witness-substituted prime-field product identity is now available in the
  Mathlib-free Berlekamp soundness layer.
- The square-free witness-product distribution lemmas can consume the new
  divisibility corollary instead of carrying the product divisibility as an
  external hypothesis.
- Existing repository warnings remain unchanged, including the pre-existing
  `sorry` warning in `HexMatrix/Bareiss.lean`.

## Next step

- Use
  `dvd_primeFieldProduct_witness_of_dvd_linearPow_sub_self` in the no-kernel
  split irreducibility composition chain.

## Blockers

- None for issue #4160.
