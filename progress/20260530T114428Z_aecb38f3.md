**Accomplished**

- Claimed #5772 and wired the verified-Isabelle BZ comparator infrastructure:
  AFP wrapper theory, persistent Haskell driver, setup script, and cached
  Lean-side subprocess plumbing in `HexBerlekampZassenhaus/Bench.lean`.
- Registered the Isabelle comparator, Lean same-domain target, and trivial
  baseline as fixed `scheduled-hardware` targets. `list` shows them, explicit
  `run` works, and the default `hexbz_bench verify` path filters them out so CI
  does not install Isabelle or build AFP.
- Verified:
  - `lake build hexbz_bench`
  - `lake exe hexbz_bench list`
  - `lake exe hexbz_bench verify`
  - `scripts/oracle/setup_bz_isabelle.sh`
  - `HEX_BZ_ISABELLE=.cache/oracles/bz-isabelle/wrapper/bz_isabelle lake exe hexbz_bench run Hex.BerlekampZassenhausBench.runIsabelleFactorChecksum --max-seconds-per-call 120`
  - `HEX_BZ_ISABELLE=.cache/oracles/bz-isabelle/wrapper/bz_isabelle lake exe hexbz_bench run Hex.BerlekampZassenhausBench.runIsabelleFactorBaselineChecksum --max-seconds-per-call 120`
  - `python3 scripts/check_dag.py`
  - `python3 scripts/status.py HexBerlekampZassenhaus`
  - `git diff --check`
  - `BENCH_VERIFY_HARD_CAP_SECONDS=360 scripts/ci/check_bench_verify_budget.sh hexbz_bench`

**Current frontier**

- The scheduled-hardware BZ Isabelle comparator is available to the bench
  harness without changing `.github/workflows/ci.yml` or slowing CI bench
  verify. The local AFP build produced `.cache/oracles/bz-isabelle/wrapper/bz_isabelle`.

**Next step**

- Open the PR for #5772.

**Blockers**

- `/reflect` was unavailable in this Codex environment (`coordination reflect`
  is not a command, and no `reflect` executable was on `PATH`).
