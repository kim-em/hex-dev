# 20260510T003351Z - Issue #2906

## Accomplished

Claimed review issue #2906 and audited the fast-path precision cap against
`SPEC/Libraries/hex-berlekamp-zassenhaus.md` Group D steps 4 and 5.

Found that `factorFastPrecisionCap` used `min (bhksBound f)
(ZPoly.defaultFactorCoeffBound f)`, which can fall below the Mignotte
reconstruction bound even though the Mathlib bridge already packages the
combined `max` cap. Updated `HexBerlekampZassenhaus/Basic.lean` so the public
fast path uses `max (bhksBound f) (ZPoly.defaultFactorCoeffBound f)`, exposed
the cap as a named proof-facing definition, and changed the negative smoke
guard to use an explicit small bound rather than forcing a large failing cap
search at build time.

Added `HexBerlekampZassenhausMathlib/BHKSBound.lean` lemmas showing the exposed
fast precision cap is exactly the combined max cap and dominates both
`bhksBound` and `ZPoly.defaultFactorCoeffBound`, including real-valued forms.

Verification completed:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhausMathlib.BHKSBound`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhausMathlib/BHKSBound.lean`

The final grep reports only pre-existing `sorry` markers in the touched files;
no new proof holes or banned markers were introduced.

## Current frontier

Later HO-4 termination work can consume
`factorFastPrecisionCap_eq_max_bhksBound_defaultFactorCoeffBound`,
`bhksBound_le_factorFastPrecisionCap`, and
`defaultFactorCoeffBound_le_factorFastPrecisionCap` instead of unfolding the
runtime cap.

## Next step

Create the PR for issue #2906 and let CI validate the combined cap change.

## Blockers

None.
