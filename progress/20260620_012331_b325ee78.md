# Phase 6 grind coverage — HexGFqField div_eq_mul_inv cross-layer parity (#8123)

## Accomplished

Promoted `HexGFqField.div_eq_mul_inv` (`HexGFqField/Operations.lean:759`)
from `@[simp]` to `@[simp, grind =]`, and added `@[grind =]` to the
`HexGFq.div_eq_mul_inv` wrapper (`HexGFq/Basic.lean:619`). Both are
literal `x / y = x * y⁻¹` equations, re-read at the point of promotion
(no `<`/`≤`/`Ne`/`↔`/`∧`/`Associated`/`let`-wrapped conclusion). This
matches the GF2n / GF2nPoly twins already carrying `grind =` and
completes the `HexGFqField/Operations.lean` Phase 6 sweep (was the last
`@[simp]`-only lemma there).

## Current frontier

`lake build HexGFqField HexGFq` green (525 jobs); `HexGFqMathlib` bridge
green (2382 jobs). No `grind =` rejection (a rejected shape is a hard
build error, so green proves eligibility), no grind heartbeat/timeout
warnings. The only linter note in the bridge build is a pre-existing
`unusedSimpArgs` in `HexGFqMathlib.Basic` (untouched). `check_dag.py`
exit 0. Annotation-only; no proof bodies, signatures, imports touched;
no `sorry`/`axiom`/`native_decide` added.

## Next step

None for this issue. Sibling unclaimed Phase 6 sweep #8141
(HexModArith/Basic.lean values-enumeration cluster) remains, disjoint
from this file.

## Blockers

None.
