# PNT+ Dusart exponential leaves

## Accomplished

- Audited the remaining pinned numerical clusters and selected the complete
  eight-leaf exponential workload in `Dusart.proposition_5_4a` and
  `Dusart.proposition_5_4b`.
- Added a source-correlated Mathlib-free rational certificate table and a
  shared ordinary-kernel 64-way Taylor enclosure proof for all eight leaves.
- Added exact leaf adapters, coordinate diagnostics, a false-bound mutation,
  guarded axiom reports, offline source/provider correlation, proportional
  inventory classifications, and current SPEC coverage.
- Passed the focused conformance build, both experiment-library builds, PNT
  inventory source verification/tests, structural/static/trust checks, and
  factor freshness. The experiment-library build completed 9,041 jobs.

## Current frontier

The eight Dusart tactic leaves and the file-local `IntervalAuto` import are
accepted after a localized rewrite. The surrounding number-theoretic proofs
and theorem statements remain PNT+-owned and unchanged.

## Next step

Review this local edge before sequencing it for publication. A later corpus
audit can treat the remaining theta consumers separately: the Ramanujan
bounded range and the much larger FKS2 floor range are not one provider slice.

## Blockers

The broad `HexConformance` build reached 9,495 of 9,498 jobs, then the shared
filesystem ran out of space while unrelated `HexGF2.Clmul` and
`HexNumberFieldTower` artifacts were being written. Only this isolated
worktree's generated `.lake/build` output was removed afterward; the shared
package cache was preserved. No feature or proof blocker remains.
