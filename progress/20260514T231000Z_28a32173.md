# Session 28a32173 — work #4044

## Accomplished

- Claimed #4044 after confirming all visible human-oversight directives were
  unavailable for direct claim because they were blocked or marked for replan.
- Added exhaustive-core UFD packaging in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  - `exhaustiveCoreFactorsWithBound_factor_count_le`
  - `exhaustiveCoreFactorsWithBound_factor_irreducible_of_count`
  - `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_count`
- Reused the executable exhaustive-core product and `shouldRecord` invariants
  with `UFDPartition` so downstream Group A work can turn a future
  completeness/count equality into polynomial and `Hex.ZPoly` irreducibility.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib`
  - `lake build HexBerlekampZassenhaus`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -U0 -- HexBerlekampZassenhausMathlib/Basic.lean | rg` for new
    `axiom` / `native_decide` / `TODO` / `FIXME` / `sorry`

## Current frontier

#4044 still needs the hard Group A completeness/count argument: prove that the
exhaustive subset enumeration returns at least as many non-unit factors as the
normalized irreducible factor multiset of a primitive square-free core under the
Hensel subset correspondence and Mignotte recoverability hypotheses.

## Next step

Prove the lower-bound/count-equality theorem for
`exhaustiveCoreFactorsWithBound`, routing through the existing A1/A2 packages:
each irreducible divisor has a recovering lifted-factor subset, enumeration
tries that subset, exact division accepts the centered lift, and uniqueness
prevents duplicate loss. Then feed that equality into the new
`*_factor_irreducible_of_count` theorem.

## Blockers

- No technical blocker found for the packaging added here.
- Full #4044 remains open because the enumeration completeness proof is a
  separate, larger proof obligation.
