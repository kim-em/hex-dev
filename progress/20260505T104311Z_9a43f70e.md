# 2026-05-05 10:43 UTC — work session (9a43f70e)

## Accomplished

Claimed and completed #2248 (`HexPolyZ: prove Rat quotient
common-divisor size bound from pow-divisibility`).

Added three private lemmas to `HexPolyZ/Basic.lean`, immediately after
`rat_pow_dvd_repeated_of_quotient_common_divisor`:

* `rat_pow_size_lower_bound (d : DensePoly Rat) (hd : 2 ≤ d.size) (n : Nat)`
  — `n + 1 ≤ (ratPolyPow d n).size`. Induction on `n`. Base case unfolds
  `(1 : DensePoly Rat) = DensePoly.C 1` and uses `coeffs_C_of_ne_zero`
  to compute `size = 1`. Step uses `rat_product_size_gt_top`.
* `rat_size_le_of_dvd (a b : DensePoly Rat) (hb : b.size ≠ 0)
  (hab : a ∣ b)` — `a.size ≤ b.size`. Trivial when `a = 0`; otherwise
  extract cofactor `k` from divisibility, observe `k ≠ 0` (else `b = 0`),
  apply `rat_product_size_gt_top`.
* `rat_quotient_common_divisor_size_le_one
  (ratPrimitive d : DensePoly Rat) (hrep : repeatedRat ≠ 0)` — the public
  goal. Case-split on `d.size ≤ 1`; in the `1 < d.size` branch take
  `N = repeatedRat.size + 1`, get `ratPolyPow d N ∣ repeatedRat` from
  the #2247 helper, lower-bound `(ratPolyPow d N).size ≥ N + 1` via
  `rat_pow_size_lower_bound`, upper-bound `≤ repeatedRat.size` via
  `rat_size_le_of_dvd`, contradict via `omega`.

The `hrep` hypothesis matches the issue's adjusted statement; it is
satisfied at the call site in `primitiveSquareFreeDecomposition_squareFreeCore`
where `derivative ≠ 0` plus `gcd_dvd_right` give `repeatedRat ≠ 0`.

Verification:
* `lake build HexPolyZ.Basic` (only pre-existing line-2093 sorry warning).
* `lake build HexPolyZ` (clean modulo pre-existing Mignotte sorry).
* `python3 scripts/check_dag.py` (silent).
* `python3 scripts/status.py HexPolyZ` (Phase 5, done_through 4, ready).
* `git diff --check` (clean).
* `rg "axiom|native_decide|TODO|FIXME|sorry" HexPolyZ/Basic.lean` lines
  added: none.

## Current frontier

The size-bound lemma is now available for the consumer in #2206
(`rat_quotient_derivative_squareFree`), which uses
`d := DensePoly.gcd quotientRat (DensePoly.derivative quotientRat)`
together with `gcd_dvd_left` / `gcd_dvd_right` to feed
`rat_quotient_common_divisor_size_le_one`.

## Next step

A subsequent worker should claim #2206 and use this lemma to discharge
`rat_quotient_derivative_squareFree`. The `repeatedRat ≠ 0` hypothesis
will need to be threaded; in the call site at
`primitiveSquareFreeDecomposition_squareFreeCore` it follows from the
derivative-active branch hypothesis.

## Blockers

None. The two pre-existing sorries in `HexPolyZ/Basic.lean` (line 2093)
and `HexPolyZ/Mignotte.lean` (line 137) are out of this issue's scope.

## Notes on the local build environment

- `norm_num`, `push_neg`, and `by_contra` are NOT available in
  `HexPolyZ/Basic.lean`'s import context (no Mathlib). Used `by decide`
  for `(1 : Rat) ≠ 0`, `Nat.lt_or_ge` for case splitting, and `omega`
  for arithmetic glue. No new imports required.
- `Nat.le_or_lt` is also not available; use `Nat.lt_or_ge` (already
  used elsewhere in the file).
