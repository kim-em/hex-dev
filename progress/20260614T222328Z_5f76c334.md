# Progress — 2026-06-15 (session 5f76c334, feature/work)

## Accomplished

Claimed #7364 (forward modP→lift transport for `toMonicLiftData` via
`toMonicPrimeData?`). Premise-checked it against the source and **decomposed**
it into two self-contained sub-issues, narrowing #7364 to a final assembly step.

- **#7365** — forward monic correspondent: existence of a monic `g ∣ (toMonic core).monic`
  with `primitivePart (dilate (leadingCoeff core) g) = factor`. This is the
  genuine missing crux (no producer exists; `dilate_recovery` only gives the
  backward ∣/degree/primitive half). Body carries the worked-out construction
  `g.coeff n = lc^(a-n)·factor.coeff n / leadingCoeff factor` (integer because
  `leadingCoeff factor ∣ lc` follows from `factor ∣ core`), the `g ∣ M` route
  via `dilate_monic_toMonic` + dilate-ring-hom injectivity, the
  `ratPolyPrimitivePart`/`normalizePrimitiveSign` tooling pointer, and a real
  **sign caveat** (primitivePart's sign convention vs `normalizeFactorSign`).
- **#7366** — mod-p preservation of the representing subset across that
  correspondent (`RepresentsIntegerFactorModP primeData factor S → … g S`),
  via `monicModPImage_scale`. Depends on #7365.

Wired `depends-on: #7365, #7366` on #7364, posted a `Decomposed into #7365, #7366`
breadcrumb with the full reduction analysis, and `coordination skip`'d #7364
(now `replan`) for the planner to narrow to assembly scope.

## Key findings (the reduction)

`d := toMonicLiftData core B primeData = henselLiftData M B' primeData`,
`M := (toMonic core).monic` (monic), `primeData = choosePrimeData? M`. Building
`RecoveredAtLift core d factor S_lifted`: the `congr` and `monic_dvd` fields come
for free from the EXISTING monic transport `henselLiftData_represents_lifted_of_modP`
applied to `M`; the only new obligation is `dilate_eq`
(`primitivePart (dilate lc g) = factor`), which needs #7365 + #7366.

No shortcut: `existsUnique_modPSubset_lifting_to_henselRepresentation` would close
the goal but consumes the `HenselSubsetLiftHypotheses` that `hlifted_of_modP`
itself builds (circular).

## Verification

No code changes this session (decomposition-only, a valid Step 4b outcome).
`coordination` operations succeeded; sub-issues created and linked.

## Current frontier

#7365 is the self-contained crux and the highest-value next pickup. It is a
~150-200 line Gauss/dilation/sign proof in `HexPolyZ`/`HexPolyZMathlib`; best
taken by a fresh full-budget session. #7366 follows it; #7364 (assembly) is
gated on both.

## Next step

Claim #7365 with full context budget; use `ratPolyPrimitivePart` (clear
denominators of `factor(X/lc)`) + `normalizePrimitiveSign` to construct `g`, and
resolve the sign convention first (it determines the exact side hypothesis).

## Blockers

None. (#7366 blocked on #7365; #7364 blocked on both — expected.)
