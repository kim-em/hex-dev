# 2026-05-05 09:16 UTC — feature session (0a3a140c)

## Accomplished

Claimed #2330 and added the permutation-vector bridge needed by
`HexMatrixMathlib/Determinant.lean`:

- exposed the existing executable `Hex.Matrix.permutationVectors_complete`,
  `permutationVectors_nodup`, and `permutationVectors_nodup_list` lemmas;
- added `HexMatrixMathlib.PermutationVector.toPerm` from nodup Hex permutation
  vectors to `Equiv.Perm (Fin n)`;
- proved the conversion applies as `fun i => perm[i]`, is injective on packaged
  nodup vectors, covers every Mathlib permutation, and produces a nodup list of
  converted permutations.

Verified with `lake build HexMatrixMathlib.Determinant`,
`python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

The determinant bridge now has the reusable row-to-column permutation-vector
enumeration facts needed for later Leibniz-sum reindexing.

## Next step

Use this bridge to prove the sign compatibility issue (#2331), matching
`Hex.Matrix.detSign` against `Equiv.Perm.sign` in the row-to-column convention.

## Blockers

None.
