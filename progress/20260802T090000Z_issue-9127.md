# BZ cactus elbow: phase-attributed baseline (issue #9127)

## Accomplished

Added a benchmark-only phase-attribution surface to the factorization
comparator service and recorded the durable baseline the elbow work items
depend on.

- `bench/HexBench/FactorService.lean` gains `--entry factorPhaseProfile` and
  `--entry primeCounterfactual`. Neither is reachable from
  `Hex.ZPoly.factorize`. The phase profiler walks one production execution
  through the production functions in production order and records times,
  small-allocation counts (Lean heartbeats), the retained good-prime
  candidates with their modular degree patterns, Hensel precision and product
  tree, and the recombination stage counters. `countedSearch` mirrors
  `searchDirect` leaf for leaf to carry `DirectCandidateStats` on the forced
  route, which the production search does not retain.
- `scripts/bench/factor_phase_profile.py` drives the representative set plus
  controls, collects the counterfactual prime plans, and validates the mirror
  against the production `factorTrace` on a wider sample.
- `scripts/profile/factor_sampling_profile.py` records symbolized samply
  profiles and writes the analytical summary `SPEC/profiling.md` requires.
- `scripts/bench/cactus_rank_table.py` prints exact cactus ranks in both the
  cumulative and paired readings; the record-selection rule moved to
  `scripts/plots/hexbz_sweep_data.py` so it cannot drift from the figures.
- `reports/hexbz-cactus-elbow.md` is the report, with go/no-go evidence for
  #9128, #9129, #9130, #9131, #9132, and #9133.
- Fresh full Hex sweep, all 25 SVGs regenerated, and the three headline
  factorization reports updated to the new record.

## Current frontier

The mirror agreed with the production trace on 367 of 367 applicable rows and
the instrumented total tracks the plain cascade within 2.6%, so the surface can
be trusted by the dependent issues without re-derivation.

## Next step

Remove the `blocked` label from #9128, #9129, and #9131 once this merges. #9132
should be remeasured only after #9128, as its issue already requires: two of
its three witnesses are single-prime rows whose row-reduction share survives
the walk change, and `cyclo_phi64_x_phi105` is not.

## Blockers

None.

## Addendum after second-opinion review

Codex review of PR #9134 found four fidelity gaps and four report claims that
overstated or contradicted the record. All were fixed and every measurement was
re-taken at revision `c34ffbbb`:

- assembly and self-certification now happen exactly once, and the trial
  fall-through follows the production policy per method;
- the counted recombination leaf materializes the selected lifted factors at
  the same operational point production does;
- the modular sub-phase repeat moved after the cascade's total mark;
- the lift product tree is count-balanced with a guarded dominant-degree split;
- the mirror cross-check now also requires completed cardinalities and the
  returned factor-degree multiset to match, and passes 367/367 on all four.

The corrected #9128 evidence separates the cost of the walk from the cost of
avoidable extra splitting: `cyclo_phi179`, `cyclo_phi128_x_phi165` and
`cyclo_phi385` retain one prime and have no #9128 prize despite large
prime-walk shares, while `cyclo_phi64_x_phi105` has 60.5% of its total
available. The selection itself is within 5% of the cheapest retained plan on
11 of 12 multi-prime representative rows; `xpow48_minus1` is a 1.308x miss.
