# 20260509T212348Z - Issue #2821

## Accomplished

Claimed feature issue #2821 after the human-oversight items were reported as
dependency-blocked by `coordination claim`. Added the public theorem
`Matrix.det_transpose` in `HexMatrix/Determinant.lean`, using the existing
inverse-permutation vector bijection over `permutationVectors`, sign
preservation, and transpose product reindexing.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

`lake build HexMatrix` still reports pre-existing `sorry` warnings in
`HexMatrix/RREF.lean` and `HexMatrix/Bareiss.lean`; this session did not touch
those files or add proof holes.

## Current frontier

The determinant transpose theorem is now available for downstream Gram
determinant nonnegativity work.

## Next step

Continue with the dependent Gram determinant chain, starting from the issues
blocked on determinant transpose and ordered column expansion infrastructure.

## Blockers

None for this session's completed scope.
