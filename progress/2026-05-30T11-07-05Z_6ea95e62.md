# Process-jargon purge in docstrings (#5770)

## Accomplished

Cleared prose hits for `bridge` / `smoke` / `gate` / `substrate` /
`umbrella` / `consumer` across the four issue-named libraries:

- `HexPolyFp` (Compose, Frobenius, SquareFree)
- `HexPolyZ` (Mignotte)
- `HexPolyMathlib` (Euclid)
- `HexMatrixMathlib` (Determinant, Determinant/Bareiss, Determinant/Core,
  RankSpanNullspace, and the root umbrella file)

Extended the sweep to the broader library set named in the issue's
catch-all bullet:

- `HexArith` (root umbrella, Barrett/Context, Barrett/Reduce,
  Montgomery/Redc, Montgomery/RedcNat, Nat/ModArith, Nat/Pow)
- `HexBerlekamp` (DistinctDegree, Factor, RabinSoundness)
- `HexBerlekampMathlib` (Basic)
- `HexBerlekampZassenhaus` (Conformance, EmitFixtures, SmallModSingleton)
- `HexBerlekampZassenhausMathlib` (root umbrella, BHKSBound, BHKSIndependent,
  BadVector, ColumnSignature, Lattice, PartitionRefinement, Recovery,
  Resultant, SignatureClasses, TerminationBound, UFDPartition) plus
  partial work on Basic.lean (5 of 107 hits cleared)
- `HexGF2` (Bench, Euclid, Irreducibility, Multiply, RabinSoundness, Smoke)
- `HexGF2Mathlib` (root umbrella, Basic)
- `HexGfq` (CrossCheck, EmitFixtures)
- `HexGfqField` (Bench, Operations)
- `HexGfqMathlib` (root umbrella, Basic, GF2q)
- `HexGfqRing` (Operations)
- `HexGramSchmidt` (Basic, Int, Update)
- `HexGramSchmidtMathlib` (root umbrella, Basic, Update)
- `HexHensel` (root umbrella, Basic, Bench, Conformance, Linear, Multifactor,
  QuadraticMultifactor)
- `HexLLL` (Basic, Bench)
- `HexLLLMathlib` (Basic, Independent)
- `HexManual` (root umbrella, Chapters/HexGfqRing)
- `HexMatrix` (Bareiss, Bench, Determinant) and root umbrella
- `HexModArith` (Smoke)
- `HexModArithMathlib` (Basic)
- `HexPolyZMathlib` (Basic, SchurSzego)

Net effect: 474 → 283 hits across all `*.lean` files. All edits are
prose-only — `git diff` touches only comments/docstrings, and
`lake build` completes cleanly (244 jobs).

## Current frontier

Five files still carry significant counts and are out of scope for this
PR's diff size:

| File | Hits | Notes |
|------|------|-------|
| `HexBerlekampZassenhausMathlib/Basic.lean` | 97 | mostly `Mathlib bridge` / `umbrella` references |
| `HexBerlekampZassenhausMathlib/IntReductionMod.lean` | 96 | `substrate` / `umbrella` references; `HO-1 substrate` is a recurring header tag |
| `HexBerlekampZassenhaus/Basic.lean` | 44 | parallel prose to the Mathlib-side Basic |
| `HexGramSchmidtMathlib/Int.lean` | 23 | `bridge` as a standing noun for det/Bareiss correspondence work |
| `HexBerlekampZassenhaus/Bench.lean` | 15 | `smoke` is bound to the identifiers `smokeInput` / `smokeSchedule`; needs an identifier-rename plan first |

`HexGF2/Multiply.lean` (8 hits) is almost entirely the local `Bool`
variable `gate` in clmul helper lemmas — identifier territory, so
out of scope for a prose-only sweep.

## Next step

A follow-up issue should cover the five large files. The
`HexBerlekampZassenhaus/Bench.lean` cleanup needs the
`smokeInput` / `smokeSchedule` identifier rename to land first so the
prose can switch to the new names instead of awkwardly straddling
banned-word identifiers.

## Blockers

None.
