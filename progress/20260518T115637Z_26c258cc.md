# Session 26c258cc — refresh stale `_of_bound`-future framing in two Basic.lean wrapper docstrings

Claimed and executed #4990.

## Accomplished

- Refreshed the
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  wrapper docstring at `HexBerlekampZassenhausMathlib/Basic.lean:14770-14784`:
  the "consumers wiring a larger `B` … discharge it via the bound
  monotonicity" clause now redirects larger-`B` consumers to the
  `_of_bound` sibling directly (which accepts an abstract `B'` and
  discharges validity via `defaultFactorCoeffBound_valid` paired with
  `leadingCoeff_eq_coeff_last`).
- Refreshed the
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
  wrapper docstring at `Basic.lean:15479-15528`: the three stale
  paragraphs (monotonicity-question narrative; "**recommended HO-1 path**"
  propagation plan; "separable upstream task" wrap-up) collapsed to a
  single tight paragraph pointing at the `_of_bound` sibling at
  `Basic.lean:15402` as the canonical HO-1 surface, with #4539 demoted
  to a one-sentence parenthetical. Added the canonical "Thin wrapper
  over … `_of_bound`" addendum in the template form used by the
  adjacent rewired wrapper (`:14786-14790`). Retained the
  `hbranch`/`hentry_mem` paragraph verbatim.
- No signature, proof-body, or `_of_bound` sibling docstring changes.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`: 16 errors at
  `:4848`, `:5129`, `:5455`, `:5542`, `:5589`, `:5851`, `:5973`,
  `:6121`, `:6274`, `:6293`, `:6608`, `:6672`, `:15862`, `:15973`,
  `:16180`, `:16226`. Identical to `origin/main` baseline modulo a
  uniform -15 line shift for the four errors past my deletion site
  (baseline `:15877` → `:15862`, `:15988` → `:15973`, `:16195` →
  `:16180`, `:16241` → `:16226`).
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`: 18 errors,
  identical to baseline (no file touched).
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- `git grep -n "natural construction route from the outer slow-path"
  HexBerlekampZassenhausMathlib/Basic.lean`: empty.
- `git grep -n "recommended HO-1 path"
  HexBerlekampZassenhausMathlib/Basic.lean`: empty.
- `git grep -n "separable upstream task"
  HexBerlekampZassenhausMathlib/Basic.lean`: empty.
- `git grep -n "discharge it via the bound monotonicity"
  HexBerlekampZassenhausMathlib/Basic.lean`: empty.
- PR diff: 23 insertions, 38 deletions, net -15 lines (target ≤ 60
  lines total).

## Current frontier

Closes the Basic.lean half of the stale-framing refresh wave
(precedents #4984, #4985); the IntReductionMod analogue is in flight
on #4989.

## Next step

None for this issue. Future planner cycles should pick up the
remaining stale-framing locations, if any, via /reflect-style audits.

## Blockers

None.
