# Manual chapter merged; session close-out

Follows `progress/2026-07-23T00-45-00Z.md` (the factor_poly/irreducibility
arc) in the same interactive session.

## Accomplished

- `HexManual/Chapters/FactorTactics.lean` merged (#8866, squash `89db1b56`):
  one cross-cutting usage-first chapter for the tactic family, every example
  a checked Verso block including the bang forms (manual chapters are
  non-`module`, so the `import all` closure is unnecessary there). A Codex
  review pass fixed seven accuracy issues pre-merge, including the corrected
  replay-budget statement `(degree + 1) · p ≤ 2²⁶` per distinct factor, the
  `q ≤ 2²⁶` ZMod-provider cap, the split input contract
  (evaluable/transparent for the executable types vs parser grammar for
  Mathlib polynomials), the native-LLL-tier limitation of the bang forms,
  and a `Hex.FactoredPoly.scalar` docstring fix.
- Housekeeping: all ten merged `factor-poly/*` / `manual/*` branches deleted
  (remote and local).
- Captured the session's key metaprogramming lesson in
  `.claude/skills/hex-lean-mathlib-boundary/SKILL.md`: a stuck plain
  `decide` is an elaborator-whnf symptom, not a kernel verdict — probe with
  `decide +kernel`; `import all` is per-file, not recursive; the genuine
  kernel boundary is `@[extern]`-backed opaques (`lllNative`), not
  `@[irreducible]`.

## Current frontier

The factor_poly/irreducibility feature is complete: code, tests, SPEC, and
manual all merged; main green. Pages workflow re-renders the site from main.

## Next step

None scheduled. Open follow-up ideas remain as listed in
`progress/2026-07-23T00-45-00Z.md`.

## Blockers

None.
