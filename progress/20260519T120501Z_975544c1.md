# Session 975544c1 - HexModArith Phase 6 ZMod64 power wrappers (#5457)

## Accomplished

- Claimed issue #5457.
- Added ring-facing `ZMod64` power identity wrappers in
  `HexModArith/Ring.lean`:
  - `@[simp] theorem pow_one (a : ZMod64 p) : a ^ 1 = a`
  - `theorem pow_succ (a : ZMod64 p) (n : Nat) : a ^ (n + 1) = a ^ n * a`
  - `@[simp] theorem zero_pow {n : Nat} (hn : n ≠ 0) : (0 : ZMod64 p) ^ n = 0`
  - `@[simp] theorem one_pow (n : Nat) : (1 : ZMod64 p) ^ n = 1`
- Kept the implementation, extern runtime path, ring instances, conformance
  fixtures, SPEC files, CI, and top-level plan files unchanged.
- Verified:
  - `lake build HexModArith.Basic`
  - `lake build HexModArith.Ring`
  - `lake build HexModArith`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `git diff -- HexModArith/Basic.lean HexModArith/Ring.lean | rg -n '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` printed no matches.
- Quality counters matched the issue baseline: `sorry` 85, Lean-file `axiom`
  occurrences 9, `native_decide` 1.

## Current frontier

`ZMod64` now has named operator-level wrappers for the standard exponentiation
identities needed by downstream finite-field and polynomial code, proved via
the existing `toNat_pow` characterisation.

## Next step

Push the branch and open the PR closing #5457.

## Blockers

None.
