# 2026-05-11 08:24 UTC — work/feature session (6b5cc34c)

## Accomplished

- Claimed #3244 (`HexBZ Mathlib: prove default factor coefficient
  bound bridge`).
- In `HexPolyZMathlib/Mignotte.lean` added two helper lemmas:
  - `foldl_binom_iter_eq_choose`: the iterative product/division fold
    used by the executable Mignotte binomial coefficient reproduces
    `Nat.choose`, by induction using `Nat.choose_succ_right_eq` and
    `Nat.mul_div_cancel`.
  - `binom_eq_choose`: `Hex.ZPoly.binom n k = Nat.choose n k`, handling
    `n < k`, the `k ≤ n - k` symmetric branch, and using
    `Nat.choose_symm` for the `n - k < k` branch.
- In `HexBerlekampZassenhausMathlib/Basic.lean` discharged every
  `sorry` inside `defaultFactorCoeffBound_valid`:
  - `toPolynomial f ≠ 0` from `equiv.injective` on `f ≠ 0`.
  - `toPolynomial g ∣ toPolynomial f` via
    `HexPolyMathlib.toPolynomial_dvd`.
  - Real-valued chain: Mignotte → `l2norm_toPolynomial_le_coeffL2NormBound`
    (#3215) → `binom_eq_choose` → `mignotteCoeffBound_le_defaultFactorCoeffBound`,
    cast back to `Nat`.
  - Out-of-range branch (`i > natDegree`) closes via
    `Polynomial.coeff_eq_zero_of_natDegree_lt` and `coeff_toPolynomial`.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` succeeds; the only
  remaining `sorry`s in the file are the out-of-scope capstones
  enumerated in the issue (`irreducibleByFactorization_iff`,
  `factor_product`, `factor_irreducible_of_nonUnit`, `factor_unique`,
  `checkIrreducibleCert_sound`).
- `lake build HexBerlekampZassenhausMathlib`, `lake build
  HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, and
  `git diff --check` are clean.

## Next step

- The Mathlib-side `factor_product` capstone is the next layer up;
  it now has access to `defaultFactorCoeffBound_valid` via
  `Hex.factor_product_of_bound`, but the executable
  `factor_product_of_bound` itself (covered by #3087) is the gating
  prerequisite.

## Blockers

- None.
