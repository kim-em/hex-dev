# HexArith Phase 6 — Nat exponent divisibility automation

Session: `d5e61cec` (work, issue #5585)

## Accomplished

- Claimed #5585 after the three directive issues shown by `coordination orient`
  were refused by `coordination claim` as blocked.
- Reviewed `HexArith/Nat/Pow.lean` against the Phase 6 automation criteria.
- Added `@[simp, grind .]` to the two public divisibility facts:
  - `Hex.Nat.sub_one_dvd_pow_sub_one`
  - `Hex.Nat.pow_sub_one_dvd_pow_sub_one_of_dvd`
- Tightened the first docstring from "named rewrite-free bridge" to
  "automation-facing bridge" to match the new annotations.
- Preserved the private geometric-series witness and both public theorem
  proofs unchanged.

Verification:

- `lake build HexArith.Nat.Pow` — clean.
- `lake build HexArith` — clean.
- `lake build HexBerlekamp.RabinSoundness` — clean.
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `git diff -- HexArith/Nat/Pow.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` — no matches.

## Current frontier

This Phase 6 slice covers the public Nat exponent divisibility surface in
`HexArith/Nat/Pow.lean`. The downstream call site in
`HexBerlekamp/RabinSoundness.lean` compiles unchanged.

## Next step

Continue HexArith Phase 6 on another module or public API cluster; do not bump
`libraries.yml` for this single slice.

## Blockers

None for #5585. The worktree still has a pre-existing local modification to
`.claude/CLAUDE.md` that this session did not touch.
