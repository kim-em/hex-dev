# Session 805aed23 — work (meta) → feature

## Accomplished

Surveyed unclaimed feature queue (#2315, #2336, #2370, #2395), all
nominally blocked on in-flight chain work. While orienting, PR #2421
landed `FpPoly.Quotient.pow_card_eq_self_of_irreducible` (closing
#2398), which unblocks the backward leg of #2336 even though the
forward leg still depends on #2370.

Claimed #2336 (after #2398 merged) and discharged the `sorry` for
`irreducible_dvd_xPowSubX_degree` in
`HexBerlekamp/RabinSoundness.lean` by composing:
- `Quotient.pow_card_eq_self_of_irreducible` applied to `Quotient.X`,
- `quotient_X_pow_eq_reduce_frobeniusXPowMod` to translate to the
  executable `frobeniusXPowMod` representative,
- `Quotient.congr_of_reduce_eq_reduce` to back out the polynomial
  congruence `g ∣ frobeniusXPowMod - X`,
- `frobeniusXPowMod_mod_eq_monomial_mod` for the absolute Frobenius
  identity `g ∣ monomial(p^d, 1) - frobeniusXPowMod`,
- a coefficient-level rewrite of `xPowSubX d` as the sum of those two
  divisible pieces, closed with `DensePoly.dvd_add_poly`.

`lake build HexBerlekamp.RabinSoundness` succeeds; the only remaining
`sorry` in the file is the forward direction
`degree_dvd_of_irreducible_dvd_xPowSubX` at line 495.

## Current frontier

`HexBerlekamp/RabinSoundness.lean` now has exactly one `sorry`
(forward direction). The remaining work for #2336 is gated by #2370
(open, `replan` after my earlier skip): the X^(p^n)=X → deg(g)|n
order theorem.

## Next step

Worker on #2370 can use the same quotient/bridge composition pattern
established in the backward proof. Once #2370 lands, the forward
direction is a small orchestration: `g ∣ xPowSubX n` →
`reduce frobeniusXPowMod = reduce X` (via the existing iff/bridge) →
apply #2370. Then #2336 closes.

## Blockers

- #2370 still open (reverted to `replan` by my earlier skip; planner
  needs to remove `replan` so a worker can claim).
- Pre-existing umbrella build failure: `lake build HexBerlekamp`
  aborts with `dyld: missing symbol called` while linking the
  umbrella module. Reproducible on `main`; unrelated to this change.
  Single-module build of `HexBerlekamp.RabinSoundness` succeeds.
