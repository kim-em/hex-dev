# c55f3284 - work #5953/#5982

## Accomplished

- Read the latest progress note and the `agent-worker-flow` skill.
- Checked directives first; #2637, #2567, and #2564 were all claim-blocked by open dependencies.
- Claimed #5953, confirmed its `det_mul` prerequisite has landed, and decomposed it into:
  - #5982 for the arbitrary cofactor Desnanot-Jacobi coefficient relation.
  - #5983 for the original one-row replacement cofactor bridge on top of #5982.
- Claimed #5982 and added `adjugate_mul_apply` in `HexMatrix/Determinant.lean`, the right-multiplication companion to `mul_adjugate_apply`.
- Verified the change with `lake build HexMatrix`, `git diff --check`, and the forbidden-token diff grep.

## Current frontier

#5982 is partially advanced. The right-adjugate identity is available, but the arbitrary two-row/two-column cofactor Jacobi coefficient relation is still not proved.

## Next step

Continue #5982 by deriving the arbitrary cofactor Jacobi coefficient theorem, likely by reindexing the existing corner Desnanot-Jacobi/adjugate machinery or by a direct adjugate-product coefficient proof. Once #5982 lands, #5983 should fold that coefficient theorem over the replacement row `u`.

## Blockers

- #5983 is intentionally blocked on #5982.
- A pre-existing `.claude/CLAUDE.md` worktree modification remains untouched.
