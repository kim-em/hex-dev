# Session ab4b5b8d — work #4036

## Accomplished

- Claimed feature issue #4036 after all unclaimed human-oversight items were
  rejected by coordination as blocked or needing replan.
- Added `Hex.Berlekamp.kernelWitnessSplit?_some_of_nontrivial_splitFactorAt`
  in `HexBerlekamp/Factor.lean`. The theorem reflects the executable bounded
  constant search: any nonzero, nonconstant `splitFactorAt f witness c` that is
  not the full input forces `kernelWitnessSplit? f witness` to return `some`.
- Added the private offset induction
  `kernelWitnessSplitAux_some_of_nontrivial_offset` to justify that the loop
  reaches any `ZMod64` constant via its canonical `toNat` representative.
- Decomposed the remaining algebra in #4036 into #4062, #4063, and #4064.

## Current frontier

#4036 still needs the finite-field algebraic half: the product identity for
`w^p - w`, square-free distribution across the gcd candidates, and final
composition from a nonconstant kernel polynomial to the executable split. The
new executable bridge gives #4064 a direct target once #4062 and #4063 land.

## Next step

Work #4062 first: prove the prime-field product identity for witness values in
the narrow form needed by `splitFactorAt`.

## Blockers

None for the partial executable-search bridge. The remaining #4036 theorem is
blocked on the new algebra sub-issues.
