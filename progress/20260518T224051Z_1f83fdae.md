# 1f83fdae - HexModArith hot-loop identity/commutativity wrappers (issue #5141)

## Accomplished

- Claimed feature issue #5141 after confirming there were no unclaimed
  directives.
- Added public docstringed Barrett wrapper facts in `HexModArith/HotLoop.lean`:
  `BarrettCtx.mulMod_one_left`, `BarrettCtx.mulMod_one_right`, and
  `BarrettCtx.mulMod_comm`.
- Added public docstringed Montgomery standard-residue round-trip facts:
  `MontCtx.fromMont_mulMont_toMont_one_left`,
  `MontCtx.fromMont_mulMont_toMont_one_right`, and
  `MontCtx.fromMont_mulMont_toMont_comm`.
- Kept proofs routed through the existing wrapper characterization lemmas
  (`mulMod_eq_mul` and `fromMont_mulMont_toMont`) plus `ZMod64.toNat_mul`,
  without unfolding Barrett or Montgomery executable implementations.
- Verified with `lake build HexModArith.HotLoop`, `lake build HexModArith`,
  `python3 scripts/check_dag.py`, `git diff --check`, the requested declaration
  grep, and a diff grep for new `sorry`, `axiom`, `native_decide`, `TODO`, or
  `FIXME`.

## Current frontier

Issue #5141 deliverables are complete. The wrapper API now exposes the common
identity and commutativity cases for opt-in Barrett and Montgomery hot loops.

## Next step

Publish the PR for #5141.

## Blockers

None for #5141. The worktree still contains a pre-existing `.claude/CLAUDE.md`
modification that this session did not touch or stage.
