# Option-native modular slow-path completion (#6603)

## Accomplished

Claimed #6603 and completed the residual #6597 migration.

- Removed the old total modular compatibility surface from
  `HexBerlekampZassenhaus/Basic.lean`: `factorSlowFactorsWithBound`,
  private `factorSlowWithBound`, public `factorSlow`, and the compatibility
  theorem routing modular success through the total view.
- Reworked raw-source branch statements so the modular branch records
  `factorSlowModularFactorsWithBound f B = some rawFactors` directly.
- Added direct product support for successful `factorSlowModularWithBound`
  outputs and rewired `factorWithBound_product` to use it.
- Switched total slow-path executable callers in Bench, Conformance, and
  CrossCheck to `factorSlowTrial`.
- Updated Mathlib bridge hypotheses and comments to the new modular raw
  witness shape.

Verification run:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `lake exe hexbz_bench list`
- `lake exe hexbz_bench verify --filter runFactor`
- `python3 scripts/check_dag.py`
- `git diff --check`
- removed-name scan for `factorSlowFactorsWithBound`, `factorSlowWithBound`,
  and public `factorSlow`
- added-line forbidden-token scan for `sorry`, `axiom`, `native_decide`,
  `TODO`, and `FIXME`

All passed. Existing warning output includes known dependency and bridge
warnings.

## Current frontier

The Option-native modular slow-path API is now the only modular slow surface.
The total slow fallback surface is `factorSlowTrial`.

## Next step

Proceed with #6598: remove `choosePrimeDataFallback` and tighten remaining
total `choosePrimeData` callers against the post-#6603 tree.

## Blockers

None.
