# HexLLL #4334 parametric benchmark evidence

## Accomplished

- Claimed #4334 and verified the HexLLL benchmark wiring with
  `lake exe hexlll_bench list` and `lake exe hexlll_bench verify`.
- Re-ran the five formerly inconclusive HexLLL parametric registrations at
  current HEAD `14537a67ebf1bd51b2275c8840562bb33ce813c1`.
- Added
  `reports/bench-results/hex-lll-14537a67ebf1-parametric-rerun.json`; all five
  registrations are `consistent_with_declared_complexity`.
- Updated `reports/hex-lll-performance.md` to cite the new artifact and clarify
  that the remaining #4334 blocker is the densified Lean/Isabelle comparator
  evidence, not the parametric verdicts.
- Created follow-up issue #5933 for the remaining densified Lean/Isabelle
  comparator sweep and left a decomposition breadcrumb on #4334.

## Current frontier

The current branch contains a coherent partial completion for #4334: the
parametric-verdict blocker is resolved with current evidence. HexLLL still
cannot advance to Phase 4 because the report's Concerns section is not empty;
the remaining Concern is the comparator-ratio sweep.

## Next step

Run #5933 on quiet or dedicated hardware, update the comparator-ratio tables
from the resulting artifact, and either remove the Concern plus bump
`HexLLL.done_through` to 4 or file the implementation-performance follow-up
required by the clean comparator verdict.

## Blockers

The full densified Lean/Isabelle comparator `run` was too long for this session
on `carica`: after about 28 minutes it had only reached
`runIsabelleHarshCubicNormSq25`, so no current full comparator artifact was
produced.
