# 20260603T005606Z work #6412

## Accomplished

- Attempted directive priority first: #2567 could not be claimed because it
  needs replan, and #2564 could not be claimed because it is blocked by
  dependencies. Claimed #6412.
- Added two diagonal-correction sum helper lemmas in
  `HexBerlekampZassenhausMathlib/BadVectorAuxiliary.lean`:
  - `BHKS.correctionWeightedSum_le_sum`
  - `BHKS.correctionWeightedSum_le_degree_mul`
- Added
  `BHKS.FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSq`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`. This constructor packages
  the corrected auxiliary-polynomial squared-l2 estimate into
  `FactorFastCapSeparationInputs` by instantiating
  `FactorFastCapLiftAnalyticComparison.ofBridgeDataCorrectedAuxiliaryL2normSq`
  internally.

## Current frontier

The actual-cap cap-separation input path now has a corrected-l2norm producer
surface. The remaining mathematical work is to supply closed bounds for the
constructor's two substantive arithmetic hypotheses:

- the corrected auxiliary RHS bound, especially the concrete diagonal-correction
  sum estimate for the canonical correction coordinates, and
- the strict divisor comparison against
  `(factorFastCapLiftData f primeData).p ^ ((factorFastCapLiftData f primeData).k * localFactorDegree)`.

The new correction-sum lemmas are intentionally small: they turn pointwise or
uniform weighted-correction estimates into the finite sum bound that appears in
the corrected l2norm RHS.

## Next step

Prove the canonical correction-coordinate magnitude/range estimate needed to
apply `BHKS.correctionWeightedSum_le_sum` or
`BHKS.correctionWeightedSum_le_degree_mul`, then use it with the existing
`BHKSBound.lean` cap dominance lemmas to discharge the
`hauxiliary_sq_bound` and `hstrict` arguments of
`FactorFastCapSeparationInputs.ofBridgeDataCorrectedAuxiliaryL2normSq`.

## Blockers

No technical blocker for this slice. A fully closed `FactorFastCapSeparationInputs`
producer still depends on the canonical correction-coordinate bounds and the
remaining strict cap arithmetic.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over touched Lean files found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.
