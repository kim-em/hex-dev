# 2026-05-18 — BZMathlib zpoly_dvd_trans rewire (26 sites)

Session UUID prefix: `798d2cf6`. Issue: #5064.

## Accomplished

- Rewired all 26 inline divisor-transitivity sites in
  `HexBerlekampZassenhausMathlib/Basic.lean` onto
  `zpoly_dvd_trans` (defined at the pre-edit line 6736).
- Three shape classes handled per the issue:
  - Shape A (17 sites): compressed `rcases/rcases/refine/rw`
    blocks collapsed to `have ... := zpoly_dvd_trans h1 h2`.
  - Shape B (3 sites): `obtain/obtain/refine/rw/exact` blocks
    collapsed to the same one-liner.
  - Shape C (5 sites): `conv_lhs`-guarded variants — the
    workaround drops along with the inline chain since the
    helper takes opaque divisors.
- Two `← hquot_eq` variants (sites 23 and 26) rewired via
  inline synthesis `⟨f_cov, hquot_eq.symm⟩`. At site 26 the
  later `have hquot_dvd_target := ⟨f_cov, hquot_eq.symm⟩` was
  moved above `hquot_dvd_core` and the duplicate dropped, so
  the rewire reuses the existing synthesis.

## Greppable invariants

After rebase onto current `main` (which includes the sibling
PR #5066 closing #5065 — that hoisted `zpoly_dvd_trans` to
the post-rebase line 2069 and rewired the 3 calc-form sites):

- `zpoly_dvd_trans` count: 39 (1 def + 9 prior + 3 from #5066 +
  26 from this PR).
- `Hex.DensePoly.mul_assoc_poly (S := Int)` count: 1
  (helper-internal at the post-rebase line 2075).

## Diff stat

`HexBerlekampZassenhausMathlib/Basic.lean`: 40 insertions, 153
deletions; net −113 lines.

## Build state

`lake build HexBerlekampZassenhausMathlib.Basic` produces the
same 16 errors / 4 warnings as `origin/main`. Pre-existing
errors above line 9444 stay at the same line numbers; the four
pre-existing errors below the rewired region shift by 113
lines (which matches the net deletion), and the failing lemmas
at the shifted lines are the same lemmas that failed pre-edit.
No new errors introduced.

`python3 scripts/check_dag.py` exits 0. `git diff --check`
clean. No new `sorry` / `axiom` / `native_decide` / `TODO` /
`FIXME` introduced.

## Current frontier / next step

This issue's deliverable is complete. The sibling issue #5065
(hoist + 3 calc-form rewires) was landed concurrently as
PR #5066 while this PR was in progress; this branch is rebased
onto that commit and the two changesets do not overlap.

## Blockers

None.
