# 20260519T004448Z — HexLLL benchmark check and BHKS resultant audit

## Accomplished

Claimed issue #4334 and rechecked the current HexLLL Phase 4 benchmark
state. Read the issue body, `PLAN/Phase4.md`, the relevant
`SPEC/benchmarking.md` sections, `SPEC/profiling.md`,
`SPEC/Libraries/hex-lll.md`, `libraries.yml`, and
`reports/hex-lll-performance.md`.

Verified the smoke wiring still passes on current `origin/main`:

- `lake exe hexlll_bench list` completed and listed 52 registrations.
- `lake exe hexlll_bench verify` completed with `all 52 benchmark(s) passed`.

The host was not suitable for the scientific timing deliverable. At claim/probe
time `uptime` reported load averages `13.42 15.64 17.15` on `carica`, with
multiple concurrent high-CPU VS Code/Python/background processes. The
enumeration-only `lake exe hexlll_bench list` probe took `91.39s` wall time
(`261.33s` user, `22.30s` sys), and `verify` took `65.69s` wall time. This is
the same contention regime already documented on #4334 as inflating the
lean-bench spawn floor enough to make Phase 4 scientific verdicts unusable.

No benchmark artifact, report update, or `libraries.yml` bump was produced.

Released #4334 with the host-contention blocker, then claimed #5186. Audited
Mathlib and the current `HexBerlekampZassenhausMathlib` bridge state for BHKS
resultant support. Added `reports/bhks-resultant-infrastructure.md`.

The audit found that #5186's original assumption is stale: `Polynomial.resultant`
is available from `Mathlib.RingTheory.Polynomial.Resultant.Basic`, and
`HexBerlekampZassenhausMathlib/Resultant.lean` plus
`HexBerlekampZassenhausMathlib/BadVector.lean` already expose the needed
integer-resultant, rational-coprimality, Hadamard/l2norm, and executable
bad-vector packaging surface. The new report records the exact theorem names
and identifies the remaining #2567 work as non-resultant BHKS Lemma 3.2 /
cap-separation instantiation.

Verification for #5186:

- `git diff --check` passed.
- `python3 scripts/check_dag.py` passed.
- `rg -n "axiom|native_decide|TODO|FIXME" reports HexBerlekampZassenhausMathlib`
  reported one pre-existing documentation line in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`; the new report adds no
  banned tokens.

## Current frontier

Issue #4334 remains blocked on measurement infrastructure rather than HexLLL
code state. Smoke wiring is healthy, but the affected scientific benchmarks
still need quiet/dedicated hardware before their verdicts can be promoted.

For #5186, no Lean bridge file was necessary. The resultant infrastructure is
already present; the missing work for directive #2567 is proving and wiring the
BHKS bad-vector algebraic clauses and cap-separation theorem above that layer.

## Next step

Reclaim #4334 only when `carica` is quiet enough for a stable spawn floor, or
run the release-candidate benchmarking workflow on dedicated hardware. Then
rerun the five affected HexLLL scientific registrations into a new
`reports/bench-results/hex-lll-*.json` artifact and update
`reports/hex-lll-performance.md` / `libraries.yml` only if the verdicts are
Phase-4-clean.

For #2567 follow-up, instantiate
`ExecutableBadVectorWitness.ProjectedBadVectorSetupBridge` from the executable
CLD/Hensel data, then use the existing resultant contradiction wrappers with
the `bhksBound` arithmetic work.

## Blockers

Current host contention: load averages in the `10-17` range during this
session and slow smoke command wall times (`list` `91.39s`, `verify` `65.69s`)
make scientific timing evidence invalid under `SPEC/benchmarking.md`
§"Scientific timing runs".

No blocker for #5186; it is report-only because the Lean resultant bridge
already exists.
