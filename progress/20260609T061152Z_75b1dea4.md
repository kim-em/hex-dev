# 20260609T061152Z (#6509 decomposed into #6636, #6637)

## Accomplished

Claimed #6509 (`choosePrimeData?_none_implies_huge`, SPEC D2 bridge)
via `/once`. Confirmed the directive prerequisite #6601 has landed
(PR #6625) and the executable now walks all 94 primes in `[3, 500]`
deterministically via `hotPathCandidates = smallPrimeCandidates ++
extendedSmallPrimeCandidates`.

Read the surrounding infrastructure: `Hex.isGoodPrime` /
`Hex.squareFreeModP` / `Hex.choosePrimeData?` in
`HexBerlekampZassenhaus/Basic.lean`; the success-case bridge
`toMathlibPolynomial_coprime_of_gcdIsUnit` in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`; the existing
resultant infrastructure in `HexBerlekampZassenhausMathlib/Resultant.lean`
(Hadamard bound, `int_resultant_eq_zero_iff_not_coprime_over_rat`,
`resultant_map_intCast_rat`); the Mathlib `Polynomial.discr` and
`Polynomial.resultant_deriv` API.

Sanity-checked the SPEC pathway: the conclusion
`(p : Int) ∣ lc(f) · disc(f)` does follow from `isGoodPrime f p = false`
for primitive square-free `f` at a prime `p ≥ 3`, via:

1. `p ∣ lc(f)` case (when leading coefficient vanishes mod `p`) is
   immediate.
2. Squarefree-modular failure case: `gcd(f̄, f̄')` non-unit over
   `F_p` ⟹ `¬ IsCoprime f̄ f̄'` (contrapositive of
   `toMathlibPolynomial_coprime_of_gcdIsUnit`) ⟹ `resultant_F_p =
   0` (`Polynomial.resultant_eq_zero_iff`) ⟹ `p ∣ resultant_Z`
   (`Polynomial.resultant_map_map`). Degree drop when `p ∣ f.natDegree`
   handled by `Polynomial.resultant_add_right_deg`. Bridge to
   `lc · disc` via `Polynomial.resultant_deriv`.

The pathway is sound but the in-tree infrastructure is thin: no
`Hex.ZPoly.discriminant` definition exists; the squarefree-failure
contrapositive bridge is not built; the hot-path
enumeration-completeness theorem (every prime `p ∈ [3, 500]` appears
as the `.p` field of some `c ∈ hotPathCandidates`) is not built.

Realistic single-session sizing put a one-shot proof at 400–500 lines
across two files with several Mathlib bookkeeping pieces, beyond
the safe envelope for one session. Per the worker-flow
decomposition guidance, split into:

- **#6636** (`feat: hot-path enumeration completeness for SPEC D2`):
  `mem_hotPathCandidates_prime` (every entry is a prime in
  `[3, 500]`) and `exists_mem_hotPathCandidates_of_prime` (every
  prime in `[3, 500]` is the `.p` of some entry). Independent,
  largely mechanical, lives in `HexBerlekampZassenhaus/Basic.lean`.
- **#6637** (`feat: isGoodPrime failure implies p ∣ lc · resultant`):
  the per-prime mathematical content — three-way case split on
  `isGoodPrime` failure mode, Mathlib-side resultant chain with
  degree-drop handling. Lives in
  `HexBerlekampZassenhausMathlib/Resultant.lean` or a sibling.
  Uses `Polynomial.resultant`, not `Polynomial.discr` (per the
  issue body's endorsement: no `ZPoly.discriminant` exists, and the
  resultant form is one `resultant_deriv` rewrite away from the
  `lc · disc` form).

Left a `Decomposed into #6636, #6637` breadcrumb on #6509 and
released the claim via `coordination skip 6509`. Added explicit
`depends-on:` links so #6509 stays `blocked` until both sub-issues
close.

## Current frontier

#6509 is `replan`-labelled and dependency-gated on the two
sub-issues. The next planner's replan-triage cycle picks it up;
either it narrows the body to the residual composition scope (a
small fold-`none` provenance helper plus the composition itself)
or closes #6509 with a forward link if the sub-issues end up
covering the composition too.

## Next step

Successors:

- **#6636 (worker)**: prove
  `exists_mem_hotPathCandidates_of_prime` (the harder of the two
  deliverables). `decide` over `Fin 501` with `maxRecDepth` raised
  is the first thing to try; fall back to explicit
  `interval_cases p` if the kernel chokes. `native_decide` is
  banned (`SPEC` / `.claude/CLAUDE.md`).

- **#6637 (worker)**: build the four-step resultant chain.
  Most-likely sticky bits: degree-drop case (`p ∣ f.natDegree`)
  via `resultant_add_right_deg`; `Polynomial.derivative_map`
  commuting derivative with `Polynomial.map`; the contrapositive
  of `toMathlibPolynomial_coprime_of_gcdIsUnit` (a non-unit
  executable gcd produces a non-unit Mathlib gcd, hence
  `¬ IsCoprime`).

- **#6509 (parent, after both sub-issues land)**: composition.
  Needs a small fold-`none` provenance helper
  (`choosePrimeData? f = none ⟹ ∀ c ∈ hotPathCandidates,
  isGoodPrime f c.p = false` — follows from observing
  `choosePrimeDataScoreStep` preserves `some`-ness once acquired
  and `primeChoiceDataScore f c = some _ ↔ isGoodPrime f c.p = true`).
  Then quantifier-discharge via `exists_mem_hotPathCandidates_of_prime`
  and per-prime closure via #6637's lemma.

## Blockers

None for this session; the decomposition is the deliverable. The
sub-issues are individually unblocked and claimable by the next
workers.
