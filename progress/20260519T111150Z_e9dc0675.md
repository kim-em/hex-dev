# Progress: HexConway Phase 6 — expose p=3 supported entries (#5432)

## Accomplished

- Added `supportedEntry_3_4`, `supportedEntry_3_5`, `supportedEntry_3_6`
  in `HexConway/Basic.lean`, packaging the already-certified
  `luebeckConwayPolynomial_3_4`/`_5`/`_6` table entries through the
  existing `SupportedEntry` surface. Each reuses
  `supportedEntry_3_1.prime` and the corresponding
  `luebeckConwayPolynomial?_hit_3_*` lemma, mirroring the established
  `_3_1`/`_3_2`/`_3_3` pattern.
- Added matching `#guard` conformance blocks for `(3, 4)`, `(3, 5)`,
  `(3, 6)` in `HexConway/Conformance.lean`, mirroring the existing
  `(3, 1)`/`(3, 2)`/`(3, 3)` blocks (coeff list, poly equality, lookup
  agreement, `conwayPoly` round-trip, positive-degree).
- No changes to the Lübeck table, certificates, irreducibility
  witnesses, or Tier 2 compatibility surface.

## Verification

- `lake build HexConway.Basic` — green.
- `lake build HexConway.Conformance` — green.
- `lake build HexConway` — green.
- `lake build` — green (244 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- `git diff -- HexConway/Basic.lean HexConway/Conformance.lean | rg`
  for `sorry|axiom|native_decide|TODO|FIXME` — no matches.
- Quality metrics unchanged: `sorry` 85, `axiom` 9, `native_decide` 1.

## Current frontier

The `SupportedEntry` surface now matches the committed Tier 1
table for `p ∈ {2, 3}` and `n ∈ {1..6}`. Other primes (`5, 7, 11,
13`) in the Lübeck table still expose `_1` only at the
`SupportedEntry` layer; analogous packaging work could land in
future Phase 6 issues.

## Next step

Follow-up issue could expose the remaining certified `p ∈ {5, 7, 11,
13}` entries through `SupportedEntry`, with matching conformance
guards.

## Blockers

None.
