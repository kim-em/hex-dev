# Remaining capstone risk review

## Accomplished

Reviewed the current checkout for the remaining `factor_irreducible_of_nonUnit`
plan, focusing on `HexBerlekampZassenhausMathlib/FactorSoundness.lean`,
`PartitionRefinement.lean`, `Recovery.lean`, `CLDColumnBound.lean`,
`Basic.lean`, and `HexBerlekampZassenhaus/Basic.lean`.

Confirmed that this worktree still has the executable fast-core loop routed
through `toMonicLiftData`, and the cited `coreLiftData` /
`ForwardRecoveryInputsCore` / `RecoveredAtLiftM1` names are not present in the
searched files. The current forward irreducibility wrappers require a same-row
`CutProjectionHypotheses`, same-lattice indicator size equality, and partition
count for the emitted factor array.

Attempted to invoke the `second-opinion` Claude wrapper as requested by the
skill instructions, but the API call failed with `ConnectionRefused`; no
substantive external response was obtained.

## Current frontier

The main risk remains the producer boundary: after an M1 executable swap, the
loop extractor and forward-input wrappers must expose the exact M1 recovery
candidate equality and same-projected-row cut for the array actually emitted by
`factorFastCoreWithBound`. A coordinate swap alone does not automatically prove
the emitted array is the one carried by the recovery/cut package.

## Next step

Before attempting the capstone, first port or create the M1 analogues of the
`factorFastCoreWithBound_some_indicatorCandidates` extractor and the
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_forwardInputs_on_schedule`
wrappers, then instantiate the capstone against those exact extracted
witnesses.

## Blockers

No code changes to Lean files were made. The requested Core tower is not visible
under the names described in this checkout, so the risk assessment is grounded
in the currently checked-out toMonic implementation and conditional on the
planned M1 swap landing with matching extractor/wrapper ports.
