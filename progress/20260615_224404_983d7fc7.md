# Progress - 2026-06-15 (session 983d7fc7, feature)

## Accomplished

Issue #7525: retired the stale scaled-candidate support surface from the lifted
partition proof API.

- Removed `support_subset_of_dvd_scaledRecombinationCandidate` from
  `LiftedFactorSubsetPartition`.
- Updated `liftedFactorSubsetPartition_transport` and initial partition
  constructors so they no longer request or fill scaled support.
- Deleted the old scaled support projection wrappers and the obsolete
  scale-coordinate cover/prefix chain.
- Removed downstream `hscaled` plumbing from `IntReductionMod.lean` wrappers
  that now rely only on recovered-coordinate partition evidence.

## Current frontier

The live recovered-coordinate chain remains intact:
`RecoveredScaledSearch.*`, `scaledRecombinationSearchModAux_*`, and exhaustive
coverage capstones still build against `liftedRecoveryCandidate`.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- stale scaled-surface grep returned no matches
- added-line forbidden-token grep returned no matches

## Next step

Open the PR for #7525. Adjacent cleanup can separately decide whether the
remaining independent `scaledRecombinationCandidate` historical lemmas should be
kept or retired.

## Blockers

None.
