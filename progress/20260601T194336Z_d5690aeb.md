# d5690aeb - work #6099

## Accomplished

- Followed the worker flow; the open directives were all blocked, so
  selected from the normal unclaimed feature queue. Picked #6099 as
  the deepest substrate blocker in the HexMatrix Plucker chain
  (#6094 / #6044 → #6030 → #6031 → #5805 → #5655 …).
- Read #6099 (twoColDet four-basis Plucker identity for the q-before
  case, replan of #6093), the related raw-`nDet` consumers #6094 and
  #6044, and the surrounding `HexMatrix/Determinant.lean`
  Plucker-helper block (lines ~10780–11014).
- Verified the existing infrastructure: `twoColDet_basisVec_basisVec_of_lt`,
  `twoColDet_eq_sum_basisVec_pairs`, `mDet_basisVec_eq_signed_nDet_of_lt/gt`,
  and the three trivial Plucker case-helpers
  (`det_plucker_three_term_basisVec_of_eq_p1/p2/p3`). Confirmed that
  `det_plucker_three_term_basisVec_of_gt_p3_of_nDet` already takes
  the raw `nDet` Plucker identity as an explicit hypothesis and has
  never been discharged unconditionally — the substrate has been
  deferred before. The Mathlib-bridge layer (HexMatrixMathlib
  Determinant Core) has been steadily building the
  "ordered four-row" `setRow`/`nMatrix` transports around this
  missing substrate.
- Worked through the sign-bookkeeping reduction: expanding each of
  the six `twoColDet B (basisVec a) (basisVec b)` factors (`a < b`
  for all pairs in the q-before chain) via
  `twoColDet_basisVec_basisVec_of_lt` produces a common
  `cofactorSign p1 · cofactorSign p2 · cofactorSign p3 · cofactorSign q`
  factor (paired with the `n`-row signs which differ from the
  `(n+1)`-row ones by a uniform `-1`). The three sum terms factor
  out this common sign uniformly, leaving the canonical raw `nDet`
  Plucker bracket `nDet(p2,p3)·nDet(q,p1) − nDet(p1,p3)·nDet(q,p2)
  + nDet(p1,p2)·nDet(q,p3)`. So the substrate is genuinely the raw
  four-row `nDet` Plucker identity, not a sign-manipulation issue.
- Concluded that the raw `nDet` Plucker identity is a full
  Grassmann–Plücker substrate (comparable in scope to the existing
  `det_desnanot_jacobi_mul` infrastructure at lines ~8398–9573)
  and cannot reasonably be done as a session insert. Per #6099's
  explicit decomposition sanction, decomposed into:
  - #6100: prove the canonical raw `nDet` four-row Plucker identity
    (the actual mathematical substrate, for any ordered indices
    `r0 < r1 < r2 < r3`; serves both q-before and q-between
    consumers).
  - #6101: sign-bookkeeping wrapper that produces the requested
    `twoColDet_plucker_basisVec_of_lt_p1` from #6100. Linked
    `depends-on: #6100`.
- Left the `Decomposed into #6100, #6101` breadcrumb on #6099 and
  released the claim via `coordination skip` so the next planner
  triages it.

## Current frontier

#6099 is decomposed (marked `replan`). The Mathlib-free
`HexMatrix/Determinant.lean` Plucker block now has a clearly
identified substrate (#6100) and a small consumer wrapper (#6101)
covering the original deliverable.

## Next step

A worker should pick up #6100 — the canonical four-row
Grassmann–Plücker identity for `nDet` minors of `(n+2) × n`
matrices. Most promising route: an auxiliary `(n+2) × (n+2)` matrix
construction in the style of `auxAdjugateM` / `auxP` /
`det_desnanot_jacobi_mul`, where a single determinant has two
evaluations whose equality gives the Plucker LHS. Once #6100 lands,
#6101 (sign-bookkeeping wrapper) is mechanical, after which #6094
and #6044 can instantiate the canonical identity with their
respective index labellings.

## Blockers

- No technical blocker for the decomposition itself.
- The pre-existing `.claude/CLAUDE.md` worktree modification remains
  untouched and is not part of this work.
