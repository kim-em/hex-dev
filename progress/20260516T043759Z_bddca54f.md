# Session bddca54f - Lifted recombination support containment

Session: `bddca54f-8e64-4731-bc74-08a5a423d32a`
Issue: `#4394`

## Accomplished

Claimed #4394 after directive issues were unavailable due to replan or blocked
state.

Added a proof-side `liftedFactorProductCandidate`, extended
`LiftedFactorSubsetPartition` with the support-containment law needed by the
recursive recombination coverage chain, preserved that law through
`liftedFactorSubsetPartition_transport`, and exposed
`representingSubset_subset_of_dvd_recombinationCandidate` at the executable
`recombinationCandidate` surface via an equality bridge.

Verified:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry` was
introduced in the changed Lean file.

## Current frontier

The support-containment API is now present on the abstract lifted subset
partition and available as a theorem for executable candidates. Successor
coverage work can use this projection instead of re-deriving support
containment from `exists_representingSubset_dvd_recombinationCandidate_of_exactQuotient`.

## Next step

Pick up #4395 and assemble the cover-at-min containment theorem using
`representingSubset_subset_of_dvd_recombinationCandidate`.

## Blockers

None for #4394.
