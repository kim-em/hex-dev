# /work session: triage #3651 HexGfqField Phase 4 completion

Session: `ee3e3b2a`
Branch: `agent/ee3e3b2a`

## Accomplished

Claimed #3651 ("HexGfqField: certify Phase 4 completion after
calibration"), ran verification, determined it cannot proceed, and
skipped it back to `replan` with a two-clause technical-blocker
rationale.

Verification commands run on the worktree at commit `e087be3`:

- `python3 scripts/check_phase4.py` → passes (13 headline reports,
  0 changed registrations).
- `python3 scripts/check_dag.py` → exit 0 (no output).
- `python3 scripts/status.py HexGfqField` →
  `HexGfqField -> Phase 4 (performance & benchmarking)`,
  `done_through: 3`,
  **`blocked by: HexBerlekamp.done_through >= 4`**.

## Why skipped (recorded in the skip comment on #3651)

Two independent blockers, either one sufficient:

1. **Unresolved report Concerns.** The prerequisite #3612 landed via
   PR #3623 (`ffdcda2`, "bench: widen HexGfqField calibration") and
   added a degree-5 calibration rung, but the merged report
   explicitly states `HexGfqField.done_through` remains 3 and keeps
   Concern #2801 open. Six target verdicts remain inconclusive
   (`runOfPolyReprChecksum`, `runAddChecksum`, `runMulChecksum`,
   `runPowChecksum`, `runInvDivChecksum`, `runZPowChecksum`). The
   issue body explicitly requires "no unresolved Concerns" before
   bumping `done_through`.

2. **Dependency-level blocker.** HexBerlekamp was rolled back
   `5 → 3` in commit `ad74d65` (PR #3713) per HO-21 (#3669) pending
   FLINT comparator wiring. Per `scripts/status.py`'s
   `DEPENDENCY_PHASES` rule, `HexGfqField` cannot advance to
   Phase 4 completion until `HexBerlekamp.done_through >= 4`.

## Current frontier

#3651 is now `replan`-labelled. Next planner needs to wait for
both:

- HO-21 (#3669) FLINT-Berlekamp gating-comparator wiring lands and
  HexBerlekamp returns to `done_through >= 4`, AND
- Either a new wider HexGfqField calibration path resolves the six
  inconclusive verdicts, or Concern #2801 is closed on its own
  merits (e.g. by SPEC adjustment of what "inconclusive on the
  warm-cache floor" means for Phase 4 exit).

## Next step

A planner re-files #3651 once both conditions hold. Other
unclaimed work I considered and rejected for this session:

- #3308 (HexPolyFp Yun derivative invariant): body explicitly
  declares scope "now research-level... A feature worker should
  not attempt this issue until a planner has either identified
  the missing helper lemma or decomposed the proof". Should be
  `replan` rather than unclaimed; flagging here for the next
  planner.
- #3626 / #3629 / #3634 / #3637 (Hex*Mathlib Phase 4 benchmark
  harnesses): all stale per HO-33 (PR #3689, `a9df25d`) which
  deleted the existing Mathlib-bridge bench files and added
  `SPEC/benchmarking.md §Mathlib-free benches` invariant 1:
  "`Hex*Mathlib` libraries do not have benchmarks. No
  `Hex*Mathlib/Bench.lean`, no `Hex*Mathlib/Bench/`, no
  `lean_exe *mathlib*_bench` in `lakefile.lean`." Flagging here
  for the next planner; they should be closed as "obsoleted by
  HO-33 SPEC update."

## Blockers

None for this session — the work item was triaged to its correct
state. Two upstream blockers documented above govern when #3651
becomes re-actionable.
