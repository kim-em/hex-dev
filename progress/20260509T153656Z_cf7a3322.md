# 20260509T153656Z - Issue #2778

## Accomplished

Claimed #2778 and added the first inverse-permutation infrastructure in
`HexMatrix/Determinant.lean`: `inversePermutationValues`, lookup facts in both
directions, nodup/permutation-vector membership, involution, the finRange
permutation induced by a permutation vector, and
`detProduct_transpose_inversePermutationValues`.

Committed the compiling checkpoint as
`d79bd86 feat: add inverse permutation product reindexing`.

Verification completed:

- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

## Current frontier

The product side of the transpose substitution is in place:
`detProduct M.transpose perm` reindexes to `detProduct M (inversePermutationValues perm hnodup)`.
The determinant theorem still needs the sign side:
`detSign (inversePermutationValues perm hnodup) = detSign perm`, followed by
fold-level reindexing of the Leibniz sum.

The worktree still has the pre-existing unstaged `.claude/CLAUDE.md`
modification, which was not touched.

## Next step

Prove inverse preserves inversion parity/sign for nodup full permutation
vectors, then use the inverse-permutation membership/involution facts to
permute the determinant fold and add the public `det_transpose` theorem.

## Blockers

No environmental blockers. The remaining proof is larger than the product
reindexing slice because it requires a genuine parity argument for inversion
under inverse permutations.
