# 2026-05-06 05:33 UTC — interactive session

## Accomplished

User asked whether the executable side could factor an integer-coefficient
polynomial fast (quadratic Hensel + LLL), and asked for an end-to-end
demonstration on a product of two degree-10 cyclotomic polynomials.

Established baseline: python-flint factors `Φ₁₁ · Φ₂₂` (deg 20) in
~0.2 ms.

Discovered two real bugs and one process gap while attempting the
demonstration locally on macOS:

1. Lake umbrella shared libraries (`libHex_Foo.dylib`) were not aggregating
   per-module `.c.o.export` files when the umbrella `.lean` did not import
   every module under `Foo/`. `HexPolyFp.lean` was missing imports of
   `Quotient`, `QuotientFrobenius`, and `Enumeration` even though
   downstream code (`HexBerlekamp.RabinSoundness`) imports `HexPolyFp.Quotient`
   directly. Linux flat-namespace lazy binding masked the gap; macOS dyld
   aborted on first downstream elaboration.

2. `HexHensel.QuadraticMultifactor.iterateQuadraticHensel` runs `k`
   doublings to reach modulus `p^(2^k)` instead of `⌈log₂ k⌉` doublings to
   reach `p^k`. With realistic Mignotte bounds (`B ≈ 40`) the intermediate
   integers are astronomical, so `HexBerlekampZassenhaus.factor` hangs at
   runtime on inputs as small as `(x-1)(x-2)(x-3)`.

3. The HexHensel Phase 4 bench parameterises only on the polynomial
   degree `n` with the precision `k` fixed at `3`, so the verdict surface
   accepted the wrong-asymptotic implementation and `HexHensel` was marked
   `done_through: 5`.

Landed PRs:

- #2441 — macOS CI job + umbrella-completeness rule in
  `scripts/check_dag.py` + `PLAN/Phase0.md` and `PLAN/Conventions.md`
  updates + the immediate `HexPolyFp.lean` imports fix.
- #2446 — rollback of `HexHensel.done_through` from 5 to 3.
- #2447 — SPEC tightening on quadratic-Hensel iteration count and Phase 4
  bench-domain coverage. *(Subsequently reverted on `main` by the user;
  the rollback PR #2446 stands.)*
- #2455 — follow-up SPEC/PLAN: `SPEC/testing.md` item 8 on end-to-end
  top-level entry-point coverage, plus a `PLAN/Phase4.md` Discipline
  bullet on covering downstream call patterns. *(Same: present in PR
  history, not in `main`.)*

Filed `human-oversight` issues:

- #2445 — `HexHensel.iterateQuadraticHensel` doubling-count fix.
  Deliverables include the rollback (already done), the SPEC tightening
  (proposed), and an implementation PR plus expanded bench domain.
- #2449 — `HexBerlekampZassenhaus.factor` hangs end-to-end on small
  inputs. `depends-on: #2445`.

## Current frontier

`main` carries the umbrella-completeness rule, the macOS CI job, and the
`HexHensel` rollback. The SPEC and PLAN do not currently encode the
quadratic-Hensel iteration-count contract or the bench-domain coverage
discipline that would make the issues #2445 and #2449 redundant from a
SPEC/PLAN-only rebuild — that is intentional in the working tree on
`main`; the issues themselves are the canonical record.

Local `.lake/build/` retains manually relinked umbrella shared libraries
from this session's diagnosis (used to bypass the macOS dyld failure
before PR #2441 landed). They are correct on the new lakefile inputs;
Lake will regenerate on demand.

## Next step

For the planner: dispatch implementation work for #2445 — corrected
iteration count in `iterateQuadraticHensel`, plus the expanded
`HexHensel/Bench.lean` registrations varying `k` over realistic Mignotte
values, plus a linear-vs-quadratic `compare` group on the joint
`(n, k)` domain. Re-entering `HexHensel` to Phase 4 unblocks #2449,
which in turn unblocks `HexBerlekampZassenhaus` Phase 4.

## Blockers

- `HexBerlekampZassenhaus` Phase 4 is blocked on `HexHensel` recovering
  `done_through ≥ 4` (per the Phase 4 dep-coupling rule).
- `HexBerlekampZassenhaus.factor` runtime correctness is blocked on
  #2445 landing.
