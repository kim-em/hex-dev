# Session 24cbff8f — HexPoly Phase 6 review of Operations.lean (#4416)

Session: `24cbff8f-70f2-4999-bb01-a37f5062767f`
Issues: `#4334` (skipped), `#4416`

## Accomplished

First claimed `#4334` (HexLLL Phase 4 inconclusive benchmark verdicts).
Smoke `lake exe hexlll_bench verify` passed but a probe of
`Hex.LLLBench.runSizeReduceChecksum` measured per-spawn floor `1.233 s`
and killed at cap with zero verdict-eligible rows under carica load
average 11.99/9.74/7.85. Skipped with that concrete blocker — same
host-contention pattern as ten prior skips on the same issue.

Then claimed `#4416` and added three additions to `HexPoly/Operations.lean`:

- **New public `DensePoly.coeff_derivative`**: characterising coefficient
  law for `derivative p`. The hypothesis
  `((n + 1 : Nat) : R) * (Zero.zero : R) = (Zero.zero : R)` mirrors the
  existing `coeff_scale` pattern; needed because the generic
  `NatCast`/`Mul`/`Zero` interface does not constrain `n * 0 = 0`.
  Eliminates the duplication where this exact lemma is currently
  re-proved as `private` four times across the tree:
  - `HexPolyZ/Basic.lean:1476` (`rat_coeff_derivative`, `Rat`)
  - `HexPolyFp/SquareFree.lean:3719` (`coeff_derivative`, `ZMod64 p`)
  - `HexPoly/Euclid.lean:1907` (`rat_coeff_derivative_generic`, `Rat`)
  - `HexPoly/Euclid.lean:2050` (`coeff_derivative_commring`, `CommRing S`)
- **`@[simp]` on `DensePoly.eval_zero`**: `eval (0 : DensePoly R) x = 0`,
  proof `rfl`, no hypotheses.
- **`@[simp]` on `DensePoly.derivative_zero`**: `derivative (0 : DensePoly R) = 0`,
  proof `rfl`, no hypotheses.

The simp promotions were left for second-opinion-free territory: they
have no hypotheses, RHS is `0`, and the proof is `rfl`. Per the
issue body's cautionary note these are not the "non-trivial simp
rewrite" class that needs additional review.

Did not touch private duplicates in `HexPoly/Euclid.lean` or downstream
modules per the issue's "Surface only" constraint. Those private
lemmas remain in place; future cleanup can replace them with the new
public `coeff_derivative` without further design work.

## Verification run

- `lake build HexPoly.Operations` — clean.
- `lake build HexPoly HexPolyZ HexPolyFp` — clean (all 102 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `python3 scripts/status.py HexPoly` — unchanged (`done_through: 5`).
- `git diff --check` — clean.
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b' HexPoly/Operations.lean` — no matches.

## Current frontier

`HexPoly/Operations.lean` now has a complete public characterising-
coefficient surface for every executable operation: `scale`, `shift`,
`shift_scale`, `add`, `sub`, `neg`, `mul`, and `derivative`. The
`eval`/`compose` Horner folds intentionally lack a single characterising
coefficient lemma — that surface is provided downstream where the ring
structure can be used (e.g. `derivative_mul` in `HexPoly/Euclid.lean`).

HexPoly remains at Phase 6 (`done_through: 5`); this was a focused
API review, not a Phase 6 exit audit.

## Next step

The natural follow-up review surfaces are:

- The Horner-fold `eval`/`compose` API: do downstream consumers need
  intermediate lemmas (e.g. `eval_add`, `compose_C`, `compose_zero`)?
- The `mulCoeffStep`/`mulCoeffSum` proof helpers — currently exposed
  publicly but only used internally; either keep with clearer docs
  explaining their role as proof-rewrite hooks, or hide them.
- Replace the four private `coeff_derivative` duplicates with calls to
  the new public `coeff_derivative`. This is a separate cleanup PR
  because it touches `HexPoly/Euclid.lean` (out of scope here) and
  three modules under `HexPolyZ`/`HexPolyFp`.

## Blockers

None for `#4416`. `#4334` remains environmentally blocked on carica
host contention; will keep being skipped until the host quiets or the
work moves to dedicated benchmark hardware per
`SPEC/benchmarking.md §"Scientific timing runs"`.
