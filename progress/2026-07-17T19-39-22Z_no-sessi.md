# Issue 8750 revised-directive validation

## Accomplished

- Read and implemented the revised directive from issue #8750: `n³` kernel
  models, deeper post-word-boundary schedules, the smooth half-integer driver
  family, wider compare schedules, and achieved-precision refinement rungs.
- Built `hexroots_bench` successfully and ran two scientific calibration passes.
- Established that achieved precisions 32773 through 524293 make `runRefineTo`
  consistent with `t²` (`β=-0.096`), closing that diagnosis.
- Established that Taylor remains sub-cubic through degree 1024 (already 1.36 s
  per call), the three witness/Newton kernels remain sub-cubic through degree
  256, and the current certify fixture grows only about `n^1.8`.
- Reverted experimental source changes, posted the complete measurements on
  issue #8750, and released the assignment.

## Current frontier

No source changes are retained and no PR was opened. Concern 5 has a validated
schedule, but Concerns 3–4 still cannot meet the all-consistent exit using the
revised `n³` registrations within feasible wall-clock limits.

## Next step

Choose how Phase 4 represents a mathematically derived asymptote that is not
observable before the wall-clock ceiling, and replace `midComponent` for
`runCertify` with a fixture whose intended certification branch is verified.

## Blockers

- Cubic limb dominance is not observable in the feasible Taylor/witness ranges.
- The certify fixture does not exhibit its adjacent comment's claimed dominant
  path.
- The pod coordination command and session UUID remain unavailable.
