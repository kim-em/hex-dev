## Accomplished

Reviewed the proposed `SPEC/Libraries/hex-gram-schmidt.md` addition
against the current `HexGramSchmidt/Int.lean` proof surface and the
`hex-matrix.md` Mathlib-free / Mathlib-bridge placement rule.

Confirmed that `BareissGramInitialRegularStepQuotientProvider` is currently
a private abbrev in `HexGramSchmidt/Int.lean`, that its current consumers are
private, and that the public Bareiss-side bridge
`scaledCoeffRows_lower_eq_noPivotLoop_scaledCoeffMatrix` does not expose or
instantiate that provider.

## Current frontier

The spec addition is directionally correct: Schur-side determinant
characterization with exact-division provenance should be bridge-layer work,
not a Mathlib-free local derivation. The main precision gap is API-level:
the provider name cited by the spec is currently private and cannot be the
actual bridge-layer hook unless it is made public or replaced by a public
wrapper theorem/type.

## Next step

If this spec addition is committed, tighten the wording to say explicitly
that bridge-layer work must first expose a public quotient-provider API (or
public characterization theorem) and construct it in `HexGramSchmidtMathlib`.
Also distinguish executable `Matrix.exactDiv` evaluation from proof-side
divisibility/integrality witnesses.

## Blockers

None.
