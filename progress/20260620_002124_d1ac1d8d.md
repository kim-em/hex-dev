# Phase 6 grind coverage — HexGF2/Field.lean Newton-interpolation cluster (#8121)

## Accomplished

Promoted the 12 public, `@[simp]`-only, literal `lhs = rhs` equation lemmas
in the GF2nPoly Newton-interpolation cluster of `HexGF2/Field.lean` to
`@[simp, grind =]`:

`evalCoeffList_nil`, `evalCoeffList_cons`, `dividedDifferenceCoeffs_nil`,
`dividedDifferenceCoeffs_singleton`, `dividedDifferenceCoeffs_cons_cons`,
`dividedDifferenceCoeffs_length`, `dividedDifference_nil`,
`dividedDifference_cons`, `linearPow_zero`, `linearPow_succ`,
`listProd_nil`, `listProd_cons`.

Each was re-read at the point of promotion and confirmed a literal equation
(no `↔`/`<`/`≤`/`Ne`/`Associated`/conjunction/`let`-wrapped conclusion).
Every RHS strictly reduces term structure (peels one `cons` or collapses a
base constructor), so all terminate as rewrite rules. No exclusions.

## Current frontier

`lake build HexGF2` green; `HexGF2.Field` elaborates in 1.7s — identical to
the #8040 baseline, so no grind-loop slowdown. Full downstream build (579
jobs) clean: `HexGF2.EmitFixtures`, `HexGFq`, `HexGFq.CrossCheck`,
`HexGF2Bench`. `scripts/check_dag.py` exit 0. Annotation-only; no proof
bodies, signatures, imports touched; no `sorry`/`axiom`/`native_decide`
added; no `*Mathlib`-boundary breakage.

## Next step

None for this issue. Sibling unclaimed Phase 6 sweep on the same file:
#8124 (GF2n coefficient-enumeration cluster, disjoint line range).

## Blockers

None.
