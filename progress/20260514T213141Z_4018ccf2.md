# Session 4018ccf2 — partial work #4086

## Accomplished

- Claimed #4086 as the most useful unclaimed issue because it unblocks the
  prime-field product-root chain.
- Added a scalar coefficient-power-sum bridge for `DensePoly.eval` over
  `FpPoly` in `HexPolyFp/Basic.lean`.
- Proved public scalar evaluation lemmas `FpPoly.eval_add` and
  `FpPoly.eval_sub`.
- Decomposed the remaining multiplication, `linearPow`, and linear-factor
  root-divisibility surface into #4096.
- Verified `lake build HexPolyFp.Basic`, `lake build
  HexBerlekamp.RabinSoundness`, `lake build HexBerlekamp`, `python3
  scripts/check_dag.py`, `git diff --check`, and the affected-file grep for
  banned markers.

## Current frontier

#4086 now has the additive/subtractive scalar evaluation layer needed before
the multiplicative root-product proof can be completed. The residual
convolution proof for `eval_mul` and the root-factor divisibility API remains
open in #4096.

## Next step

Use the new coefficient-power-sum bridge plus `FpPoly.coeff_mul` to prove
scalar `eval_mul`, then derive `eval_linearPow` and the linear-factor root
lemma requested by #4096.

## Blockers

None for this partial slice. The remaining proof is larger than the residual
#4086 framing because multiplication needs a finite convolution argument.
