# Audit executable CLD path

## Accomplished

- Claimed #3502, found PR #3488 already repair-claimed by another session, and requeued it with the coordination blocker after orphan-release refused without `.pod/agents/` liveness data.
- Claimed #3503, found it assumed unmerged #3447/#3504 API changes, added `depends-on: #3447`, and requeued it as blocked.
- Claimed #3505 and audited the executable BHKS CLD/recovery surface in `HexBerlekampZassenhaus/Basic.lean` against the HO-1 SPEC pitfalls.
- Added focused executable guards for negative centred-residue `Psi`, CLD coefficient width, recovery failure-to-`none` behavior, and bounded fast-path no-fallback behavior.
- Filed follow-up #3507 for the substantive audit gap: the fast precision loop still collapses classified recovery failures to `none` and does not distinguish the SPEC's reconstruction-only vs lattice-too-large remedies.

## Current frontier

- `HexBerlekampZassenhaus/Basic.lean` has additional guard coverage for the executable CLD audit surface.
- The broader failure-mode control-flow distinction is explicitly tracked in #3507 rather than folded into this review issue.

## Next step

- Create the PR for #3505 with only `HexBerlekampZassenhaus/Basic.lean` and this progress file included.

## Blockers

- Existing unrelated `.claude/CLAUDE.md` worktree change is present and intentionally left uncommitted.
