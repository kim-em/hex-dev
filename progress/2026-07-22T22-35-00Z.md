# Bang forms reopened and landed (plan step 6, follow-up)

Branch `factor-poly/int-provider` (PR #8863). Coordinator review corrected my
earlier descope diagnosis: the elaborator's whnf (which respects
`@[irreducible]` and cannot see non-exposed bodies at default transparency)
was the blocker in my probes, not the kernel. `decide +kernel` per the
boundary skill's "Kernel-facing recursive specs" section is the right tool.

## Accomplished

- **Probe results (all on v4.32.0-rc1, this tree).** With `import all` of the
  executable closure — critically including `HexHensel`, the matrix libs
  (`HexMatrix`/`HexRowReduce`), and core `Init.Data.Fin.Fold` (the
  non-exposed `Fin.foldl` was the last stuck constant, found by descending
  `Lean.Kernel.whnf` through the pipeline) — `decide +kernel` proves
  `Hex.ZPoly.Irreducible f` with clean axiom cones:
  x²+1 / x⁵+3 / x⁴−10x²+1 (Swinnerton-Dyer) / x⁴+1 all four in a 3.0s file
  build; degree 8 (SD3) a few seconds; degree 12 (Φ₁₃) tens of seconds
  (29s file with both). My earlier "structurally stuck" claim was wrong:
  every stuck I hit was a module-exposure gap (project libs at per-file
  granularity, plus `Init.Data.Fin.Fold`), not `Acc.rec` opacity —
  `WellFounded.Nat.fix` measures reduce fine in the kernel.
- **`BangElab.lean`**: `irreducibility!` (term + tactic `this`/`h :` + goal
  forms) and `factor_poly!` (term + tactic), running the plain pipeline
  first and falling back to raw-Expr emission of
  `Hex.ZPoly.irreducible_of_decide` / `forall_irreducible_of_decide` /
  `irreducible_ofZ_decide` / `Hex.FactoredPoly.ofZDecide` (new decide-slot
  helpers in `FactorTransport.lean`). The elaborator prechecks kernel
  reducibility with `Lean.Kernel.whnf` (clear error naming the `import all`
  closure requirement instead of a kernel type mismatch), pre-runs the
  compiled `isIrreducible` for honest reducible-input errors, and enforces
  a dense-size budget of 13 (degree 12) from the measurements.
- Extracted the `factor_poly` tactic's `Factored.mk` destructuring into
  `Hex.FactorTactic.introFactored` (FactorPolyElab.lean), shared with
  `factor_poly!`.
- Tests: bang forms exercised on the genuine decline cases (x⁴+1 free-layer
  term/goal, Swinnerton-Dyer free-layer + parsed `Polynomial ℤ`, transported
  goal, `factor_poly!` on `(x−1)(x⁴+1)` both ZPoly and `Polynomial ℤ`),
  pass-throughs, over-budget error guard, `#print axioms` guards (all clean
  cones). The test file's `import all` block now carries the full executable
  closure (~130 modules) — the documented calling-module cost of the bang
  forms.
- Provider decline diagnostics and SPEC updated to point at the bang forms;
  the SPEC descope paragraph replaced by an honest cost/closure description.

## Current frontier

Whole tree green; PR #8863 updated.

## Next step

If the `import all` closure burden ever matters for real users, an
`@[expose]` sweep over the factorize call graph would let the bang forms
work with plain imports; core's `Init.Data.Fin.Fold` exposure would need an
upstream change or an avoided dependency.

## Blockers

None.
