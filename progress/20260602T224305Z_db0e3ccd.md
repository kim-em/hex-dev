# 20260602T224305Z work #2567

## Accomplished

- Claimed directive #2567 and landed another HO-4 slice for the corrected
  BHKS auxiliary polynomial's per-coefficient norm bound.
- Added
  `BHKS.auxiliaryPolynomialWithCorrections_coeff_eq` (private) in
  `HexBerlekampZassenhausMathlib/BadVector.lean`: the `Finset.sum`-form
  coefficient identity for `auxiliaryPolynomialWithCorrections`, matching
  the existing `auxiliaryPolynomial_coeff_eq` shape.
- Added
  `BHKS.auxiliaryPolynomialWithCorrections_coeff_sq_le` in
  `HexBerlekampZassenhausMathlib/BadVector.lean`: under the same BHKS
  Lemma 5.1 column hypothesis consumed by
  `auxiliaryPolynomial_coeff_sq_le`, the squared `j`-th coefficient of
  `auxiliaryPolynomialWithCorrections input liftData vec corrections` is
  bounded by twice the uncorrected per-coefficient bound plus twice the
  squared diagonal-row correction at `j`.  The `2` factor is the slack
  from `(a − b)² ≤ 2·(a² + b²)`, which lets the correction term separate
  cleanly from the existing Cauchy–Schwarz bound on the uncorrected
  coefficient.

## Current frontier

The per-coefficient bound now covers both the zero-correction case
(`auxiliaryPolynomial_coeff_sq_le`) and the diagonal-corrected case
(`auxiliaryPolynomialWithCorrections_coeff_sq_le`).  The remaining
analytic step lifts the new per-coefficient bound to the squared
`l2norm` by summing over `j < input.degree?.getD 0`, analogous to how
`auxiliaryPolynomial_l2norm_sq_le` lifts the uncorrected per-coefficient
bound.

## Next step

Prove `BHKS.auxiliaryPolynomialWithCorrections_l2norm_sq_le` by summing
`auxiliaryPolynomialWithCorrections_coeff_sq_le` over the support range,
producing a squared-l2-norm bound shaped like
`2 · ‖v‖² · r · cldColumnNormBound input + 2 · Σⱼ correction_j² ·
p^(2·(k − cut(j)))`.  That theorem can then feed the `hauxiliary`
argument of
`ExecutableBadVectorWitness.l2norm_product_lt_divisor_of_auxiliary_bound`
once the cap-lift witness's auxiliary side is plumbed in.

## Blockers

No technical blocker for this slice.  The full `factorFast_terminates`
theorem still requires the squared-l2-norm lift, the diagonal-correction
norm bound, and the strict cap-arithmetic inequality against
`coeffL2NormBound`.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVector` passed.
- `lake build HexBerlekampZassenhausMathlib` passed (full umbrella).
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over the touched Lean file found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
