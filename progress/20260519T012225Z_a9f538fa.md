# 20260519T012225Z — BHKS bad-vector theorem-shape report

Session type: `/feature` worker (escalated from `/work`) on issue #5194.

## Accomplished

Issue #5194 asked for a report at
`reports/bhks-bad-vector-theorem-shape.md` mapping BHKS §3.2 / §5
notation to current Lean names, proposing the BHKS Lemma 3.2 theorem
shape for directive #2567, and splitting the next implementation
issues by what is unblocked.

Report covers:

1. **Notation mapping table** from BHKS notation (`f`, `r`, `g_i`, `a`,
   `ℓ_j`, `Φ`, `Ψ`, `L'`, `W`, `B'`, `H_v`, paper threshold) to existing
   project names with file/line citations. Identifies the deliberate
   gap: no `v ∈ L' \ W ⟹ ‖v‖₂ ≥ G(a)` form yet exists, even though all
   the surrounding pieces do.

2. **Three Lean theorem sketches** in current names:
   - `bhks_bad_vector_resultant_lower_bound` — composes existing
     `badVector_resultant_bounds_of_bhks_bad` halves into the chained
     resultant inequality.
   - `auxiliaryPolynomial_l2norm_sq_le` — Cauchy–Schwarz bound on
     `‖H_v‖₂²` in terms of `‖v‖₂²` and a `cldColumnNormBound` helper.
   - `bhks_bad_vector_norm_sq_gt_cutRadiusSq4_of_paperThreshold_le` —
     squared-norm strictly above `bhksCutRadiusSq4` at any precision
     ≥ `factorFastPrecisionCap`.

3. **Three-issue prerequisite split** in dependency order: (1) the
   resultant chain is unblocked review-sized work; (2) the
   `H_v` norm bound is unblocked feature-sized work independent of (1);
   (3) the cut-radius comparison depends on (1)+(2). Beyond (3),
   `factorFast_terminates` is a separate fourth issue.

PRs #5187 (`Polynomial.resultant` audit) and #5191 (`bhksBound`
arithmetic packaging) both merged during this session — the report
folds their conclusions in (no Mathlib port required; `bhksPaperThresholdReal` /
`bhksThresholdNatBound` are the relevant Nat/ℝ packaging).

`git diff --check` clean. No new `axiom` / `native_decide` / `TODO` /
`FIXME`. (`sorry` appears in two illustrative theorem snippets; this
matches the convention used by `reports/bareiss-dependency-review.md`.)

## Current frontier

The BHKS bad-vector pipeline now has a single missing algebraic surface
between the existing resultant-side comparison and the LLL cut-radius
comparison. That surface is two lemmas + one theorem (the split in §3
of the report).

## Next step

Spin out the three issues from the report's `Prerequisite split`
section. Step 1 is a review-sized composition lemma; steps 2–3 are
feature-sized.

## Blockers

None for the report itself. Implementation work has no blockers on
the resultant or BHKS-bound Mathlib audits (both merged today).
