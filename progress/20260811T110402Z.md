# Accomplished

- Added generic `ProofEmitter.assumedAt` so an elaborator can seed a caller
  fact from an exact base-list position with an ordinary equality proof.
- Reified the live planner's complete operation and node arrays into the
  emitted theorem rather than substituting the fixed `extendedProgram` name.
- Replaced every named initial proof in the tactic with algorithmic seeding:
  caller assumptions come from checked list positions and newly instantiated
  nodes come from exact final-program lookups plus fact-domain top soundness.
- Inspected the fully elaborated `tacticSine` theorem. It contains
  `seedAssumed` and `topFact`, and contains none of `sineBase`, `negatedBase`,
  `sinePrevious`, `negationPrevious`, or `transportPrevious`.
- Added a negative test showing that emission rejects a final program which
  omits the instantiation event's generated nodes.
- Updated the SPEC and passed the focused 1,956-target build, copyright,
  line-count, DAG, diff, and banned-proof checks.

# Current frontier

Initial fact evidence and the final expression program are now derived from
plain planner data. One leading instance event still uses fixed base-to-final
prefix witnesses, and the end-user closure still names this canary's semantic
adapter and target bridge.

# Next step

Integrate generic program-prefix reconstruction into the emitted event fold so
multiple instance events can occur at arbitrary chronology positions and seed
their new nodes only after the corresponding extension proof is established.

# Blockers

None in this experiment. The stacked branch chain still needs reconciliation
after the repaired #9201 head is accepted and the lower PRs begin merging.
