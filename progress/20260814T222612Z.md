# Accomplished

- Rebased the ordinary PNT+ Table 12 extension onto exact `main`
  `4ecad503731d1a4662e350bc853d38649757cacf`, preserving the centered
  conformance modules introduced by the intervening merges.
- Refreshed the exact `lakefile.lean` proof-only freshness transition and
  reran the source-pinned PNT inventory checks against revision
  `21998bb6196b56789f72a52656a781a75e134eb0`.
- Rebuilt the focused ordinary-row conformance target (2,221 jobs) and the
  full monorepo (9,757 jobs), and reran the release and benchmark Python unit
  suites (30 and 119 tests).
- Preserved the separate logarithmic-row work-in-progress branch without
  including it in this ordinary-only candidate.
- Validated a review concern directly against pinned source lines 1238--1239:
  row 31 intentionally shares its final three upper cuts with the following
  logarithmic row, so the exact source values were retained rather than
  tightened from a reconstructed calculation.
- Strengthened the replay evidence boundary with typed first, middle, and last
  kernel canaries while retaining the all-115 elaboration fold; equal semantic
  cuts with different diagnostic coordinates no longer count as improvements.

# Current frontier

- The ordinary-only candidate is ready for an exact-head review and CI run.
  It provides checked coverage for 120 of the 130 Table 12 cells while the
  inventory continues to mark the two logarithmic rows and full generated
  family as pending.

# Next step

- Push the immutable candidate, open a non-draft PR against upstream `main`,
  verify exact GraphQL base/head metadata, and run fresh exact-head review and
  CI before handing it back as merge-ready.
- Resume the preserved ten-cell logarithmic extension on top of this exact
  candidate only after the ordinary PR is stable.

# Blockers

- None for the ordinary extension.
