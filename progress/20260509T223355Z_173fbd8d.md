# 20260509T223355Z - Issue #2887

## Accomplished

Claimed feature issue #2887 and extended
`HexBerlekampZassenhausMathlib/BHKSBound.lean` with real-facing BHKS threshold
infrastructure:

- named real factors for the paper degree, `(2C)^(n^2)`, coefficient-norm,
  logarithmic, and product threshold terms;
- proved the direct degree cast bridge and the project `C <= 2` dominance
  lemma from `(2C)^(n^2)` to the packaged `4^(n^2)` Nat factor;
- exposed named analytic targets for the coefficient-norm and logarithmic
  factor bounds;
- added real-cast max dominance lemmas, including precision-parameter forms
  from `max (Hex.bhksBound f) (Hex.ZPoly.defaultFactorCoeffBound f) <= a`.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.BHKSBound`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekampZassenhausMathlib/BHKSBound.lean HexBerlekampZassenhausMathlib.lean`

The two new analytic target lemmas in `BHKSBound.lean` intentionally use
`sorry`, as allowed by #2887 for exact missing mathematical statements. No
`axiom`, `native_decide`, `TODO`, or `FIXME` was introduced.

## Current frontier

Later HO-4 work can refer to stable names for the real BHKS threshold factors
and the precision max bound instead of unfolding the executable Nat cap.

## Next step

Prove the two analytic target lemmas:
`bhksPaperCoeffNormFactorReal_le_coeffNormFactor` and
`bhksPaperLogFactorReal_le_log2Factor`.

## Blockers

None for this package. The remaining work is mathematical proof work around the
coefficient-vector norm and logarithmic comparisons.
