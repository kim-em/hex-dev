# 20260510T195608Z — feature session aa99cb01

## Accomplished

Claimed #3112 and added proof-only inverse-preservation infrastructure in
`HexMatrix/RREF.lean`:

- `eliminateColumn` now has left- and right-inverse preservation lemmas for
  its row-add fold over the transform side.
- `rrefLoop` threads both inverse-existence invariants through row swaps,
  nonzero pivot row scaling, and column elimination.
- `rref_transform_left_inverse` and `rref_transform_right_inverse` package the
  final corollaries needed by the later `rref_isRREF` assembly.

Verification run:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`

## Current frontier

`HexMatrix/RREF.lean` still has the pre-existing `rref_isRREF` proof hole. The
new inverse corollaries are private and ready to be consumed when that theorem
is assembled.

## Next step

Use #3110's rank/pivot-order facts and the remaining #3111 row-zero/pivot-column
facts to fill the `IsEchelonForm` and `IsRREF` fields in `rref_isRREF`.

## Blockers

None.
