## Accomplished

Claimed issue #5704 and added the Gram-Schmidt row-combination reconstruction
surface in `HexGramSchmidt/Int.lean`.

Added:

- `coeffs_mul_basis_eq_castIntMatrix`
- `rowCombination_basis_coeffs_reconstruction`

Verified `lake build HexGramSchmidt.Int` successfully. `git diff --check`
passed. The task-file-only forbidden-marker scan found no added `sorry`,
`axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The reconstruction theorem requested by #5704 is available on the
Gram-Schmidt integer surface. The final lattice norm proof can now use it
with `GramSchmidt.rowCombination_normSq_ge_of_orthogonal_coeff_sq_ge_one`.

## Next step

Use `rowCombination_basis_coeffs_reconstruction` in the parent #5649 proof of
`normSq_latticeVec_ge_min_basis_normSq`.

## Blockers

None.
