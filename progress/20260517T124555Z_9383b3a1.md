# 20260517T124555Z — feature session 9383b3a1 (#4675 helpers)

Session UUID prefix: 9383b3a1. Issue: #4675 — HO-1 substrate (#4618
sub): rational `repeatedPart ∣ squareFreeCore ^ N` over `Polynomial ℚ`.

## Accomplished

Landed three Polynomial-ℚ infrastructure helpers in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` covering the
abstract algebraic content of #4675:

- `divRadical_dvd_radical_pow_of_ne_zero` — in any Euclidean-domain UFD
  with a normalisation monoid, `divRadical a ∣ (radical a) ^ N` for
  some `N`. Proof routes through
  `exists_squarefree_dvd_pow_of_ne_zero` + `Squarefree.isRadical` +
  `IsRadical.dvd_radical` + `radical_dvd_radical` +
  `divRadical_dvd_self`. ~22 lines.

- `toPolynomial_scale` — `HexPolyMathlib.toPolynomial` intertwines
  `Hex.DensePoly.scale` with `Polynomial.C` multiplication for any
  `CommSemiring` base. Coefficient-wise, two `coeff_scale` /
  `coeff_toPolynomial` rewrites. ~10 lines.

- `rp_dvd_sf_pow_of_associated` — abstract divisibility step: in a
  characteristic-zero polynomial field, given `P ≠ 0`, `R` associated
  to `EuclideanDomain.gcd P P.derivative`, and `P ~ S * R`, conclude
  `∃ N, R ∣ S ^ N`. Composes
  `Polynomial.gcd_derivative_associated_divRadical_of_charZero` (the
  #4617 char-0 helper already in the file), the new
  `divRadical_dvd_radical_pow_of_ne_zero`, and
  `Associated.of_mul_right` for the
  `radical P ~ S` cancellation step. ~25 lines.

Decomposed the parent #4675 into #4704 covering the executable
specialisation. The infrastructure compiles cleanly; the specialisation
to `(normalizeForFactor f).{repeatedPart, squareFreeCore}` requires
careful handling of the `primitivePart core` vs `core` discrepancy that
emerges when `unfold`ing `primitiveSquareFreeDecomposition`, plus
likely a raised `maxHeartbeats` for the case-split block.

## Current frontier

The main #4675 deliverable theorem
`normalizeForFactor_repeatedPart_map_intCast_dvd_squareFreeCore_map_intCast_pow`
is *not* in this PR. Sketched the full proof and hit `whnf`/tactic
heartbeat timeouts when applying `unfold primitiveSquareFreeDecomposition`
followed by `rw [if_neg ...]` to extract the case-C value of
`repeatedPart`. The proof structure that needs to land in #4704:

1. Translate goal to `HexPolyMathlib.toPolynomial (toRatPoly _)` form
   via `toPolynomial_toRatPoly_eq_map_intCast`.
2. Set `core := (extractXPower (primitivePart f)).core`, with `core ≠ 0`
   and `Primitive core`; hence `primitivePart core = core`.
3. Reassembly: `P ~ S * R` from
   `primitiveSquareFreeDecomposition_reassembly_over_rat core` and
   `toPolynomial_scale`.
4. Gcd identification: case-split on `derivative.isZero`.
   - Case B (`true`): `rp = 1`, `R = 1`. `gcd P 0 = P`, `P` is a unit
     (char-0 + `eq_C_of_derivative_eq_zero` + nonzero), so
     `Associated 1 (gcd P 0)`.
   - Case C (`false`): `rp = ratPolyPrimitivePart (gcd ratPrimitive
     derivative)`. Use `ratPolyPrimitivePart_rational_associate` +
     `HexPolyMathlib.toPolynomial_gcd_associated` to get
     `R ~ gcd P P'`.
5. Apply `rp_dvd_sf_pow_of_associated`.

The case-split block hit heartbeats specifically when `ratPrimitive`
was defined as `toRatPoly core` while the executable inner `let`
produces `toRatPoly (primitivePart core)`. Matching them via `set
ratPrimitive := toRatPoly (primitivePart core)` and bridging through
`hpp_eq_core` separately should resolve the slow `whnf` reduction.

## Next step

Worker picks up #4704 and lands the specialisation. The new helpers
are immediately usable; the only remaining work is the executable
case-split + reassembly mechanics.

## Blockers

None — #4704 has the same `depends-on: #4674` as the parent, and
#4674 is already closed.
