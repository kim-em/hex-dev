# 776b5e52 - HexPoly semiring eval/derivative wrappers (issue #5127)

## Accomplished

- Checked unclaimed directives first; #2564, #2567, and #2637 were all blocked
  by dependencies, so claimed actionable feature issue #5127.
- Added public docstringed `@[simp]` wrappers
  `Hex.DensePoly.eval_C_semiring` and
  `Hex.DensePoly.coeff_derivative_semiring` in `HexPoly/Operations.lean`.
- Routed both wrappers through the existing generic lemmas instead of unfolding
  `eval` or `derivative`.
- Rewired the `HexPoly/Euclid.lean` commutative-ring derivative product-rule
  cluster to use `coeff_derivative_semiring` and removed its duplicate private
  derivative-coefficient helper.
- Verified with `lake build HexPoly.Operations`, `lake build HexPoly`,
  `python3 scripts/check_dag.py`, `python3 scripts/status.py HexPoly`,
  `git diff --check`, and the requested declaration grep.
- Checked the Lean diff for new `sorry`, `axiom`, `native_decide`, `TODO`, and
  `FIXME`; none were introduced.

## Current frontier

Issue #5127 deliverables are complete. `HexPoly` remains ready for Phase 6
proof polishing, and `libraries.yml` was intentionally not bumped because the
issue excluded the phase bump.

## Next step

Publish the PR for #5127 with the wrapper API and Euclid rewiring.

## Blockers

None for #5127. The worktree still contains a pre-existing `.claude/CLAUDE.md`
modification that this session did not touch or stage.
