# 20260609T082139Z (#6637)

## Accomplished

- New file `HexBerlekampZassenhausMathlib/HotPathDiscriminant.lean`
  exposes `isGoodPrime_false_implies_dvd_resultant_deriv`: when
  `Hex.isGoodPrime f p = false` at a prime `p ≥ 3`, the modulus divides
  the integer product `lc(f) · resultant(toPolynomial f,
  (toPolynomial f).derivative, n, n − 1)`. This is the per-prime
  ingredient for SPEC D2's `choosePrimeData?_none_implies_huge`
  (#6509).
- Two private helpers in the same file:
  - `size_eq_one_of_toMathlibPolynomial_isUnit` — the bridged
    executable polynomial is a `(ZMod p)[X]`-unit iff its `DensePoly`
    size equals one.
  - `toMathlibPolynomial_not_coprime_of_gcdIsUnit_false` — the
    contrapositive of `IntReductionMod.toMathlibPolynomial_coprime_of_gcdIsUnit`:
    a failing `gcdIsUnit` check produces a non-coprime witness in
    `(ZMod p)[X]`.
- `HexBerlekampZassenhausMathlib.lean` umbrella picks up the new module.

## Current frontier

Proof shape for the substantive branch (`leadingCoeffModP ≠ 0` and
`gcdIsUnit(...) = false`):

1. Push the integer divisibility through `ZMod.intCast_zmod_eq_zero_iff_dvd`
   and distribute the cast.
2. Identify `(toPolynomial f).map (Int.castRingHom (ZMod p))` with
   `toMathlibPolynomial (Hex.ZPoly.modP p f)` (via
   `IntReductionMod.toMathlibPolynomial_modP_eq_map_intCast_zmod`).
3. Apply `Polynomial.resultant_add_right_deg` to reduce the
   `(n, n − 1)` resultant to the default-argument form (using
   `Polynomial.natDegree_derivative_le` and the
   `natDegree_map_intCast_zmod_eq_of_leadingCoeffModP_ne_zero` bridge).
4. Close via `Polynomial.resultant_eq_zero_iff` over the field `ZMod p`,
   using the non-coprimality witness from the helper.

## Next step

#6509 (`choosePrimeData?_none_implies_huge`) now has both prereqs
landed: the enumeration coverage (#6641, just merged) and this
per-prime divisibility lemma. The next worker can compose them.

## Blockers

None.

Verification performed:

- `lake build HexBerlekampZassenhausMathlib.HotPathDiscriminant` green.
- `lake build` (full project) green.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- Forbidden-token grep over the new file (`sorry`, `axiom`,
  `native_decide`, `TODO`, `FIXME`) — none.
