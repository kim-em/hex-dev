# Progress — 2026-06-15 23:01:26 UTC (session 0e92e93f, work)

## Accomplished

Claimed issue #7517 and migrated the downstream to-monic slow-path wrappers in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` from the stale
original-core `HenselLiftDescentHypotheses` / fixed
`RepresentsIntegerFactorModP primeData factor S` premise shape to the
monic-correspondent API:

- `MonicDescentHypotheses` for the to-monic lift,
- an explicit monic-coordinate precision bound for `(Hex.ZPoly.toMonic core).monic`,
- existential lifted original-factor representatives,
- `slowPathHenselSubstrate_of_toMonicChoosePrimeData_success_descent` as the
  shared source for `corr` and `partition`.

The ordinary `choosePrimeData?` original-core constructor
`liftedFactorSubsetPartition_outerBound_of_choosePrimeData` was left unchanged.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token scan over the touched Lean file for
  `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`

The second-opinion wrapper was invoked for a final diff review, but the Claude
process did not return after several minutes and was terminated; no review
feedback was available.

## Current frontier

Issue #7517's requested slow-path wrapper migration is complete. The only
remaining original-core `HenselLiftDescentHypotheses` / fixed modP transport
shape in the touched region is the preserved ordinary `choosePrimeData?`
constructor path.

## Next step

Open the PR for #7517.

## Blockers

None.
