## Accomplished

- Claimed human-oversight issue #2712 for the HexBerlekamp Phase-4 audit reset.
- Re-evaluated `SPEC/Libraries/hex-berlekamp.md` and `HexBerlekamp/Bench.lean` against the post-#2687 Phase-4 requirements in `PLAN/Phase4.md`, `SPEC/benchmarking.md`, and `SPEC/profiling.md`.
- Confirmed `hexberlekamp_bench` has four existing registrations and that `lake exe hexberlekamp_bench list` plus `lake exe hexberlekamp_bench verify` pass.
- Rolled `HexBerlekamp.done_through` from 4 to 3 in `libraries.yml` because the required headline report, `phase4.input_families` metadata, profile evidence, and empty Concerns section are absent.
- Filed re-promotion follow-up #2739 and marked it blocked on #2698.
- Linked the #2712 audit result from umbrella audit #2690.

## Current frontier

- `HexBerlekamp` is no longer claiming Phase 4 without the new evidence package.
- Existing benchmark registrations are still wired and smoke-check successfully, so #2739 can focus on metadata, scientific runs, profiling, headline reporting, and re-promotion if Concerns are empty.

## Next step

- Complete #2698 so structured `phase4` metadata is parsed consistently, then work #2739 to generate the HexBerlekamp Phase-4 evidence package and re-promote only if the report satisfies every exit criterion.

## Blockers

- #2739 is blocked on #2698 for structured `phase4.input_families` metadata support.
