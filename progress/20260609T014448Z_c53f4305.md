# Scalar Yun step bridge (#6436 partial)

## Accomplished

Claimed directive #2567 first, verified that its literal unconditional
`factorFast_terminates : ∀ f, factorFast f ≠ none` target is stale against the
current SPEC and implementation, commented with concrete evidence, and released
it claimable with `coordination skip`.

Claimed #6436 and added a compiled per-step scalar synchronization bridge in
`HexPolyFp/SquareFree.lean`:

- `yunScalarStep` packages the derivative-active Yun split for a raw scaled
  input `DensePoly.C u * f`;
- it exposes the nonzero gcd scalar `v` selected by the executable gcd bridge;
- it proves both the repeated-part synchronization
  `gcd (C u * f) (D (C u * f)) = C v * gcd f (D f)` and the quotient
  synchronization
  `(C u * f) / rawG = C (u * v⁻¹) * (f / gcd f (D f))`.

Verification run:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep for `sorry`, `axiom`, `native_decide`,
  `TODO`, and `FIXME`

All passed.

## Current frontier

This is a partial #6436 result. The scalar algebra now has the missing
derivative-active step package, but the full recursive synchronization theorem
for `yunFactorsContributionWithLevel` has not yet been assembled.

## Next step

Use `yunScalarStep` as the induction step for a scalar trace through
`yunFactorsContributionWithLevel`, then combine that trace with the existing
`yunFactorsContributionWithLevel_normalized_tail_reconstruct` and
`yunFactorsContributionWithLevel_normalized_tail_product_bridge` lemmas.

## Blockers

None for the partial bridge. The remaining #6436 scope is larger recursive
proof assembly, not a local algebra blocker.
