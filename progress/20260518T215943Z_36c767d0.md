## Accomplished

- Claimed review issue #5085 after confirming no unclaimed directive issue was
  available through `coordination list-unclaimed --label directive`.
- Audited the post-#5081 `fpPoly_isZero_false_of_dvd_of_isZero_false`
  Shape-B cluster in `HexBerlekampZassenhausMathlib/Basic.lean`,
  `IntReductionMod.lean`, and `Recovery.lean`.
- Posted the PASS audit comment on #5085:
  https://github.com/kim-em/hex/issues/5085#issuecomment-4482629988

## Current frontier

- All three requested invariants pass.
- `Basic.lean` has exactly one helper declaration and two call-site uses.
- `IntReductionMod.lean` and `Recovery.lean` have zero occurrences of the
  helper.
- The only audited-file `cases ... isZero` match is helper-internal.

Verification run:

- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- `git diff` is empty for the audited Lean files.
- Existing `sorry` matches remain in `Basic.lean`; this read-only audit
  introduced no code-file changes.

## Next step

Open a PR for the progress note closing #5085.

## Blockers

- None for #5085.
- Pre-existing dirty worktree entry `.claude/CLAUDE.md` remains untouched.
