# Accomplished

- Merged the reviewed structural-cursor fixes and the corrected dynamic-scope
  generation contract into the engine-owned matcher branch.
- Resolved the cursor/conformance overlap without duplicating the reference
  implementation.
- Updated the SPEC for one registration-wide matcher, engine-owned cursor
  progress, FIFO/policy parity, visible matcher-resource exhaustion, and
  conservative structural-input generation.
- Rebuilt the full 26-job interval target set successfully.
- Ran `scripts/check_dag.py` successfully and rechecked the changed matcher
  path for banned proof shortcuts.

# Current frontier

The matcher/scheduler vertical is internally green and ready to be pushed as a
stacked draft PR.  It needs a fresh independent design/code review, especially
of retry semantics, global matcher compilation, policy completeness, and the
conservative causal-batch rule.

# Next step

Push and open the stacked PR, launch a fresh Opus review without waiting on it,
then continue integrating matcher structural inputs with frozen proof payloads
and semantic replay.

# Blockers

None.
