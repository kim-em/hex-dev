# Work issue #3512 — canonical scheduled recovery bridge

## Accomplished

- Claimed issue #3512.
- Added `Hex.cap_mem_henselPrecisionSchedule`, proving the terminal cap is
  inspected by the canonical finite Hensel precision schedule.
- Added `Hex.factorFastPrecisionCap_mem_henselPrecisionSchedule` for the
  proof-facing fast-path cap.
- Added
  `HexBerlekampZassenhausMathlib.BHKS.factorFast_ne_none_of_forwardInputs_at_cap`,
  a wrapper that consumes `ForwardRecoveryInputs` at the canonical cap without
  requiring callers to pass the schedule-membership proof.
- Verified the requested Lean targets and repository checks.

## Current frontier

- The final HO-4 theorem can now target `factorFastPrecisionCap f` directly
  through the Recovery wrapper, while the deeper BHKS semantic obligations
  remain explicit `ForwardRecoveryInputs` fields.
- The banned-marker scan reports only pre-existing `sorry`s in
  `HexBerlekampZassenhaus/Basic.lean`; this session added no `sorry`, `axiom`,
  or `native_decide`.

## Next step

- Use the new at-cap wrapper when assembling the eventual `factorFast f ≠ none`
  theorem from the remaining B7/A2 forward-recovery inputs.

## Blockers

- None for this support slice.
