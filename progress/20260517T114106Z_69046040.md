# 20260517T114106Z — feature session 69046040 (#4671 decomposition)

Session UUID prefix: 69046040. Issue: #4671 — HO-1 substrate: discharge
executable Hensel subset correspondence obligations.

## Accomplished

Scope assessment of #4671 found the two `sorry`s targeted by the issue
require a Mathlib-side wrapper (the classical squarefree Hensel subset
correspondence over the executable `Hex.choosePrimeData` /
`Hex.henselLiftData` surface) that does not yet exist anywhere in the
codebase or in Mathlib:

- Mathlib's `NumberTheory.Padics.Hensel` proves root lifting in ℤ_p, not
  the Berlekamp-Zassenhaus subset-correspondence form.
- Mathlib has no Berlekamp / Zassenhaus polynomial factorization
  infrastructure (only `SchurZassenhaus` from group theory).
- `HexHenselMathlib/Correctness.lean` already has its own open
  multifactor lift obligations (e.g. `multifactorLift_eq_multifactorLiftQuadratic`
  at line 269, still `sorry`).

The natural decomposition routes through the already-landed
`henselSubsetCorrespondence_of_modPSubsetPartition`
(`HexBerlekampZassenhausMathlib/Basic.lean` line 1513), which composes a
mod-`p` partition with Hensel lift transport. The parent #4671's
deliverable 3 explicitly endorses creating successor issues and leaving
existing declarations unchanged when the wrapper exceeds session scope,
which is the path taken.

### Sub-issues created

- **#4678**: build `modPSubsetPartitionHypotheses_of_choosePrimeData :
  ModPSubsetPartitionHypotheses core (choosePrimeData core) True True`.
  Discharges `factors_irreducible`, `exists_subset`, `unique_subset` for
  the executable mod-`p` factoring.

- **#4679** (depends on #4678): build
  `henselSubsetLiftHypotheses_of_choosePrimeData_henselLiftData :
  HenselSubsetLiftHypotheses core B primeData (henselLiftData core B
  primeData) True True True True`. Discharges `factor_count_eq`,
  `represents_lifted_of_modP`, `represents_modP_of_lifted` for the
  executable lift data. Listed as the natural consumer of the existing
  `HexHenselMathlib/Correctness.lean` obligations.

- **#4680** (depends on #4679): compose #4678 and #4679 via
  `henselSubsetCorrespondence_of_modPSubsetPartition` to discharge the
  two original `sorry`s in `henselSubsetCorrespondence_analytic_obligation`
  (line 9149) and `liftedFactorSubsetPartition_analytic_obligation`
  (line 9242). The four-field partition for `Finset.univ` and `target =
  core` needs additional analytic reasoning about how `core` decomposes
  into irreducible representing-subset pieces, called out explicitly in
  the body.

The parent #4671 was routed to replan via `coordination skip` with the
breadcrumb `Decomposed into #4678, #4679, #4680`.

## Current frontier

#4678 is the unblocked critical-path entry. The substantive content is
the mod-`p` subset partition: every irreducible integer factor `f` of
the squarefree `core` reduces mod `p` (with `p` chosen by
`Hex.choosePrimeData` to avoid the discriminant) to a unique product of
a subset of the executable `factorsModP` factors. The worker claiming
#4678 should investigate whether the contract guaranteed by
`Hex.choosePrimeData` (admissible prime, valid Berlekamp factoring) is
already accessible via existing Mathlib-bridge lemmas, or whether a
predecessor lemma about `choosePrimeData`'s certifications needs to be
written first.

#4679 leans on `Mathlib.NumberTheory.Padics.Hensel` and on the open
multifactor-lift obligations in `HexHenselMathlib/Correctness.lean`.
The worker should coordinate with the existing Hensel-mathlib state
before starting — completing #4679 may require completing one of those
predecessor obligations as well.

## Next step

A planner picks up the replan-marked #4671 next, verifying the three
sub-issues fully cover the parent and either closing it with a forward
link or narrowing it. The first worker on the HO-1 path then claims
#4678.

## Blockers

None for #4678. #4679 is blocked on #4678; #4680 is blocked on #4679.
The blocking chain is intentional.
