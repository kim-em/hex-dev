# HexBerlekamp certificate-to-Rabin unpacking

## Accomplished

- Claimed #2155 and completed `checkIrreducibilityCertificate_rabinTest` in `HexBerlekamp/Irreducibility.lean`.
- Added private list/divisor helpers to transfer checked Bezout witnesses over `maximalProperDivisors` into `(rabinWitnesses f hmonic).all Prod.snd = true`.
- Added the required `[ZMod64.PrimeModulus p]` assumption to the theorem, matching the existing checked Bezout-to-gcd bridge.
- Verified `lake build HexBerlekamp.Irreducibility`, `lake build HexBerlekamp`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexBerlekamp/Irreducibility.lean`.

## Current frontier

#2155 is complete. The remaining `sorry` in `HexBerlekamp/Irreducibility.lean` is the out-of-scope `checkIrreducibilityCertificate_irreducible_predicate` theorem.

## Next step

Prove the predicate-level irreducibility theorem by combining the now-proved certificate-to-`rabinTest` bridge with the mathlib Rabin correctness layer or an appropriate non-mathlib predicate bridge.

## Blockers

- None for the Rabin-test bridge.
- The certificate-to-Rabin theorem requires `[ZMod64.PrimeModulus p]`, because the Bezout witness only implies the executable gcd unit check over a prime modulus with the current APIs.
