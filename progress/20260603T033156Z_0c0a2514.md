# 20260603T033156Z work #6434

## Accomplished

- Claimed issue #6434 (HexGramSchmidt sub-task of #6423): pure determinant
  infrastructure for the coefficient bordered minor.
- Added `coeffBM G k hk i a` to `HexGramSchmidt/Int.lean`: a private
  `(k + 1) × (k + 1)` matrix whose first `k` columns match
  `Matrix.borderedMinor G k hk i j` (those columns are independent of `j`) and
  whose last column is the row-`a` indicator vector.
- Proved the multilinearity entry equation `det_borderedMinor_eq_sum_coeffBM`:
  ```
  Matrix.det (Matrix.borderedMinor G k hk i j) =
    (List.finRange n).foldl
      (fun acc a => acc + Matrix.det (coeffBM G k hk i a) * G[a][j]) 0
  ```
  The proof packages
  `Matrix.det_colReplace_sum_finRange` against the indicator-expansion of the
  last column of `Matrix.borderedMinor G k hk i j` and identifies each
  resulting `colReplace … (indicator a)` matrix with `coeffBM G k hk i a`.
- Proved the optional support lemma `det_coeffBM_eq_zero_of_ge_and_ne`:
  for `k ≤ a.val` with `a ≠ i`, the last column of `coeffBM G k hk i a` is
  all-zero, so the determinant vanishes (via `Matrix.det_colReplace_smul` at
  scalar `0`).
- Added supporting private helpers:
  - `foldl_acc_congr_int`: congruence under same-on-list bodies for `Int`
    accumulators (inlined locally because the analogous helper in
    `HexMatrix/Determinant.lean` is `private`).
  - `foldl_finRange_indicator_eval_int`: `g q = Σ_a g a * indicator(q.val = a.val)`
    in `Int`, the indicator expansion needed by the multilinearity proof.
  - `coeffBM_entry_lt`, `coeffBM_entry_last`: entry-level computation of
    `coeffBM` for `c.val < k` and `c = Fin.last k`.
  - `borderedMinor_last_col_eq`: `(borderedMinor G k hk i j)[r][Fin.last k] =
    G[liftBorderedIdx hk i r][j]`.
  - `colReplace_borderedMinor_indicator_eq_coeffBM`: replacing the last column
    of `borderedMinor G k hk i j` with the row-`a` indicator vector yields
    `coeffBM G k hk i a`.

All new declarations are `private` to `Hex.GramSchmidt.Int`. No executable
definitions changed; no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`
introduced.

## Current frontier

`coeffBM` and the multilinearity entry equation are now ready as inputs for
the residual #6423 work (Desnanot-Jacobi-style identification of
`hinv.coeff i [a]` with `det (coeffBM G k hk i a)` along reachable
`bareissGramRowInvariant_noPivotLoop_initial` steps).

## Next step

A successor session can claim the residual #6423 sub-issue, then proceed
to #6424 / #6425 with the coefficient identity available.

## Blockers

None. The build is green and the verification commands all pass on this
worktree.

## Verification

- `lake build HexGramSchmidt.Int` — success.
- `lake build HexGramSchmidt` — success.
- `lake build` — success (full project).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line forbidden-token grep over `HexGramSchmidt/Int.lean` finds no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
