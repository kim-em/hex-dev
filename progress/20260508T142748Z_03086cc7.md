## Accomplished

- Claimed human-oversight issue #2707 for the HexPolyZ Phase-4 audit reset.
- Re-evaluated `SPEC/Libraries/hex-poly-z.md` and `HexPolyZ/Bench.lean` against the post-#2687 Phase-4 requirements in `PLAN/Phase4.md`, `SPEC/benchmarking.md`, and `SPEC/profiling.md`.
- Confirmed `hexpolyz_bench` has 10 existing registrations and that `lake exe hexpolyz_bench list` plus `lake exe hexpolyz_bench verify` pass.
- Rolled `HexPolyZ.done_through` from 4 to 3 in `libraries.yml` because the required headline report, `phase4.input_families` metadata, scientific verdict traceability, profile evidence, and empty Concerns section are absent.
- Filed re-promotion follow-up #2728 and marked it blocked on #2698.
- Linked the #2707 audit result from umbrella audit #2690.

## Current frontier

- `HexPolyZ` is no longer claiming Phase 4 without the new evidence package.
- Existing benchmark registrations are still wired and smoke-check successfully, so #2728 can focus on metadata, scientific runs, profiling, headline reporting, and re-promotion if Concerns are empty.

## Next step

- Complete #2698 so structured `phase4` metadata is parsed consistently, then work #2728 to generate the HexPolyZ Phase-4 evidence package and re-promote only if the report satisfies every exit criterion.

## Blockers

- #2728 is blocked on #2698 for structured `phase4.input_families` metadata support.
