# RCF cell decomposition and bounded-domain intersection

## Accomplished

- Added the uniform `Cell n` representation, ordered enumeration, rank bounds,
  and enumeration coverage.
- Packaged checked isolation roots as `RootModel`, with roothood, interval
  membership, strict monotonicity, and completeness.
- Added exact dyadic open-cell samples and proved sample membership, semantic
  cell ordering, unique partition, cell nonemptiness, open-cell
  preconnectedness, and carrier-root exclusion.
- Added size-indexed lower/upper endpoint comparison vectors, replay checking,
  and transport of checked claims to the chosen root model.
- Implemented the exact cell/`Ioc` relevance table, its nonempty-domain
  equivalence, and a guarded all-endpoint-order wrapper.
- Relocated the generic dyadic midpoint lemmas from the heavyweight driver
  module to `ChainCorrespond`, removing the driver/isolation-engine import from
  the cell layer.
- Added zero-, singleton-, and two-root regressions covering every cell shape,
  exact samples, equal/reversed domains, endpoint-root asymmetry, and tampered
  lower and upper comparison arrays.
- Updated the umbrella documentation and `hex-rcf` SPEC to describe the actual
  indexed APIs and guarded bounded-domain behavior.
- Addressed the fresh independent review findings and completed a green full
  `lake build` (9467 jobs).

## Current frontier

The issue #8930 implementation is complete locally and ready for its commit,
rebase, PR, hosted CI, and merge gates.

## Next step

Publish and merge the cell milestone, then build common-root certificates and
the sign-matrix/formula-reflection layer on the cell API.

## Blockers

None.
