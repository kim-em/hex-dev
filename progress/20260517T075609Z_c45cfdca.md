# 2026-05-17T07:56:09Z — HO-1 substrate: constant-arm reassemblyExpansionComplete discharger (#4585)

## Accomplished

Discharged the explicit `hcomplete` hypothesis of the fast-path constant-arm
umbrella `factor_constant_branch_entry_irreducible_of_choosePrimeData`
(#4565 / `HexBerlekampZassenhausMathlib/IntReductionMod.lean:1153`) by adding
three new lemmas across the substrate.

In `HexPolyZ/Basic.lean`:
* `rat_derivative_size_le_pred (p : DensePoly Rat)` — basic helper bounding
  `(derivative p).size ≤ p.size - 1` (via `size_ofCoeffs_le` and
  `List.length_range`).
* `rat_derivative_zero_of_size_le_one` — converse of the existing
  `rat_size_le_one_of_derivative_zero`, a corollary of the new size bound.
* `primitiveSquareFreeDecomposition_repeatedPart_eq_one_of_squareFreeCore_degree_zero`
  — the structural companion to
  `primitiveSquareFreeDecomposition_squareFreeCore_eq_one_of_degree_zero`.
  Case 2 (`derivative.isZero`) discharges by literal definition (`repeatedPart := 1`).
  Case 3 (`derivative ≠ 0`) is ruled out via gcd-derivative degree arithmetic:
  the would-be `squareFreeCore = 1` forces `quotientRat = scale u 1` via
  `ratPolyPrimitivePart_rational_associate`, and (for `u ≠ 0`)
  `ratPrimitive = scale u repeatedRat` collapses sizes, putting
  `ratPrimitive.size ≤ derivative.size ≤ ratPrimitive.size - 1` against
  `ratPrimitive.size ≥ 2`.

In `HexBerlekampZassenhaus/Basic.lean`:
* `normalizeForFactor_repeatedPart_eq_one_of_constant` (private) — one-line
  forward of the new `HexPolyZ` lemma to `normalizeForFactor`.
* `reassemblyExpansionComplete_constant_of_ne_zero` (public) — the
  six-line discharger: rewrite `squareFreeCore = 1` via
  `squareFreeCore_eq_one_of_constant_of_ne_zero`, collapse
  `expandRepeatedPartFactorArray _ #[1]` via
  `expandRepeatedPartFactorArray_singleton_one`, close with the
  `repeatedPart = 1` helper.

## Verification

* `lake build HexBerlekampZassenhaus` — green; the only `sorry`s are pre-existing.
* `python3 scripts/check_dag.py` — exit 0.
* No new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` introduced.

## Next step

A later assembly issue composes
`reassemblyExpansionComplete_constant_of_ne_zero` with the three sibling
dischargers (small-mod singleton #4564, slow-quadratic #4575, fast-quadratic
#4571) into the HO-1 capstone `factor_irreducible_of_nonUnit` (#4170). Those
sibling dischargers are out of scope here; their existence is what blocks the
capstone, not the wiring into it.
