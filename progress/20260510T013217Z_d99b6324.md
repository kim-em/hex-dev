# 20260510T013217Z - Issue #2927

## Accomplished

Claimed issue #2927 and added a narrow derivative-active Yun split bridge in
`HexPolyFp/SquareFree.lean`:

- `dvd_derivative_mul_of_dvd_left_and_derivative`
- `yunInitialSplit_common_derivative_dvd_y`
- `yunInitialSplit_gcd_quotient_derivative_eq_one_of_common_dvd_one`

The bridge proves that, for the initial derivative-active split
`g = gcd f f'`, `c = f / g`, `y = gcd c g`, `z = c / y`, any divisor of both
`z` and `z'` also divides `y`. The assembly lemma packages this with the
existing common-divisor-to-one criterion for `z` and `y` to obtain
`gcd z z' = 1`.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

The final grep reports only the four pre-existing `sorry` markers in
`HexPolyFp/SquareFree.lean`; no new proof holes, axioms, or banned markers were
introduced.

## Current frontier

The local algebraic bridge from a common divisor of `z` and `z'` to a common
divisor of `z` and `y` is now available for the Yun derivative-active base
split. The remaining square-free step theorem still needs the corresponding
reachable-state common-divisor criterion for `z` and `y` plus the monicity
input for the executable gcd.

## Next step

Use `yunInitialSplit_gcd_quotient_derivative_eq_one_of_common_dvd_one` when
assembling `yunFactorsStepsSquareFree_of_derivative_split`, once the
`z`/`y` common-divisor criterion and gcd monicity bridge are available.

## Blockers

`coordination create-pr 2927` failed because the GitHub API rate limit was
exceeded while the coordinator was detecting the repository. The branch was
pushed to `origin/agent/d99b6324`; the GraphQL rate-limit reset reported by
`gh api rate_limit` is 2026-05-10 02:08:00Z.
