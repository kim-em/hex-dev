# Progress: HexPoly Phase 6 grind automation for Dense.lean

Session type: feature (#8006)

## Accomplished

Promoted 21 equation-shaped public characterising `@[simp]` lemmas in
`HexPoly/Dense.lean` to `@[simp, grind =]` (the `coeff_*`, `size_*`,
`degree?_*`, `support_*`, `toArray_size`, `ofCoeffs_*`, `ofList_*`,
`monomial_zero`, `coeffs_C_zero` forms), following the convention in
`HexArith/ExtGcd.lean` and `HexHensel/Basic.lean`.

Two lemmas deliberately excluded:
- `mem_support` (Dense.lean:483) — an `↔`, not a bare `lhs = rhs`.
- `toArray_getD` (Dense.lean:545) — its LHS `p.toArray.getD n 0` unfolds
  via `Array.getD` to a `dite` over `Array.getInternal`, which `grind`
  rejects (`invalid pattern, (non-forbidden) application expected`,
  followed by a whnf PANIC). Left as plain `@[simp]`.

## Current frontier

`lake build HexPoly` → success; `HexPoly.Dense` builds in 647ms (was
621ms pre-change) — no grind-loop slowdown. `scripts/check_dag.py`
exit 0; `git diff --check` clean; no added `sorry`/`axiom`/
`native_decide`/`TODO`/`FIXME`.

## Next step

PR for #8006. Deliverable 3 (proof shortening / optional Conformance
simplification) was not pursued — the promoted lemmas are themselves the
grind targets and their own proofs can't use them; no manual chain in
`Dense.lean` obviously collapsed. Sibling Phase 6 files (`HexGF2/Basic`,
`HexGFqRing/Basic`) remain unclaimed.

## Blockers

None.
