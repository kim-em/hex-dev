# 2026-06-05 00:32Z — fuel-bounded LLL loop (#6564)

## Accomplished

Replaced the well-founded recursive `lllAux` body in `HexLLL/Basic.lean`
with a structurally recursive `lllLoop` over explicit natural fuel.

Added `lllFuel (s) := (s.potential + 1) * (n + 1)` and rewrote `lllAux` as
the compatibility wrapper `lllLoop s k δ hδ hδ' hk hkn (lllFuel s)`, preserving
the original `lllAux` signature and leaving `lll`, `lllUnchecked`,
`firstShortVector*`, and `shortVectors*` untouched.

Documented the `fuel = 0` fallback in `lllLoop` as pipeline-unreachable under
the forthcoming fuel-sufficiency theorem tracked by issue #6567.

Verification:

- `lake build HexLLL` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff -- HexLLL/Basic.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)'` returned no matches.
- `git diff --check -- HexLLL/Basic.lean` passed.

## Current frontier

Issue #6564 is complete. The termination `sorry` in `HexLLL/Basic.lean` has
been removed without adding a new `termination_by`, `decreasing_by`, `sorry`,
`axiom`, or `native_decide`.

## Next step

Open the PR for #6564. Follow-up issue #6567 remains responsible for proving
that `lllFuel` is sufficient and closing the SPEC §8 fallback obligation.

## Blockers

None. Builds still report pre-existing linter warnings outside this change in
`HexGramSchmidt/Basic.lean`, `HexGramSchmidt/Int.lean`, and `HexPolyZ/Basic.lean`.
The `/reflect` slash command was not available as a callable tool in this
session.
