# Session 6ae68e25 — /work → #7062 (BZMathlib Basic.lean scale→dilate)

## Accomplished
- Claimed #7062 (the HO-1 gate: restore HexBerlekampZassenhausMathlib.Basic
  to green, 7 "Application type mismatch" errors).
- Established the root cause: `RepresentsIntegerFactorAtLift` was redefined from
  (defeq) the scaled `reduceModPow` congruence to `Nonempty (RecoveredAtLift …)`
  (dilate model), so every consumer feeding `hrep` into a scale-model recovery
  lemma stopped typechecking. The scaled equality is false in the new model.
- Landed the one cascade-free fix: in
  `representedFactor_dvd_recombinationCandidate_of_subset` the `2 ≤ d.p^d.k`
  fact was being derived by abusing the scale recovery lemma; replaced with a
  direct derivation from the Mignotte precision + `defaultFactorCoeffBound_pos`.
  Build confirmed: errors 7 → 6, no new errors, no sorry/axiom.
- Scoped the residual accurately into #7084 (the remaining 6 errors are one
  connected precision-model remodel + executable-bridge contract change; not
  splittable into green pieces).

## Current frontier
- 6 errors remain in `Basic.lean`, all the scale→dilate recovery/coverage
  remodel: `representsIntegerFactorAtLift_primitive_of_bound`,
  `natDegree_…_of_bound`, the two `not_represents_empty_…_of_bound`, and the
  search coverage proof
  `scaledRecombinationSearchModAux_some_and_covers_…_of_bound` (both the
  `hrec_eq` recovery step and the `hsearch_step` executable bridge, the latter
  now expecting a `dilate`-shaped candidate per #7050).

## Next step (see #7084 for full recipe)
- Bridge each consumer `hrep → liftedRecoveryCandidate core d S = factor` via
  `RecoveredAtLift.candidate_eq_of_monic_dvd`, route downstream facts through
  the existing dilate analogs (`natDegree_…_liftedRecoveryCandidate_…`,
  `zpoly_primitive_liftedRecoveryCandidate`), and thread the
  `(toMonic core).monic` precision + `hmonic_ne` + `hfactor_norm` hypotheses
  through every caller up to the top driver, where `(toMonic core).monic = core`
  collapses them to the existing core precision.

## Blockers
- The residual is a multi-session remodel: changing leaf signatures cascades to
  dozens of callers with no intermediate green state. Possibly overlaps with
  #7037 (claimed: "migrate primitive scaled search to liftedRecoveryCandidate") —
  the next worker on #7084 should check #7037's outcome first.
