# Hex truncated series implementation

## Accomplished

- Implemented the complete Mathlib-free `HexTruncatedSeries` library: fixed-precision representation, ring and precision operations, bounded Newton infrastructure, inversion, square root, exponential/logarithm, Brent--Kung composition, and Newton/Lagrange reversion.
- Proved the public algebraic, precision, uniqueness, functional-equation, composition, and two-sided reversion contracts without `sorry`, `axiom`, or `native_decide`; added cross-module kernel-reduction tests.
- Implemented `HexTruncatedSeriesMathlib`, including the `PowerSeries /(X^n)` equivalence and correspondences for derivatives, inversion, substitution, reversion, exponential, logarithm, and square-root existence/uniqueness.
- Added deterministic Int/Rat conformance fixtures, a SymPy `ring_series` oracle, CI oracle registration, and Lean differential checks. The committed 264-line fixture is reproducible and the oracle checks all 164 result records.
- Added eleven Mathlib-free scientific benchmark registrations covering every family and ladder in the SPEC. Horner/Brent--Kung agree with the required improving ratio, and Newton inversion stays within 4x of the full `-u * sum` recurrence through precision 4096.
- Integrated both libraries, conformance targets, fixture emitter, and benchmark executable into `lakefile.lean` and `libraries.yml`.
- Verified the complete 9,835-job `lake build`, the dedicated core/Mathlib/conformance/fixture/bench build, benchmark smoke tests, dependency DAG, Mathlib-free bench boundary, deterministic fixtures, SymPy oracle, Python syntax, proof-escape audit, and `git diff --check`.

## Current frontier

The specification is fully implemented and validated. The worktree is ready for review as one coherent library addition.

## Next step

Review and merge the implementation; scheduled performance runs can populate the informational FLINT comparison report without gating the library.

## Blockers

None.
