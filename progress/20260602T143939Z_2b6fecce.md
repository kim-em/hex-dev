# 20260602T143939Z 2b6fecce — Issue #6340

## Accomplished

- Claimed feature issue #6340.
- Split the `squareFree_weightedProduct` proof dependency in
  `HexPolyFp/SquareFree.lean` into two narrower provider boundaries:
  `YunDerivativeActiveLevelStateProvider` for level completion and
  `YunDerivativeActiveRawTailProvider` for the raw repeated-tail recursion.
- Threaded those providers through the weighted-product proof chain:
  `squareFreeAuxRevContribution_derivative_active_pow_obligation`,
  `squareFreeAuxRevContribution_correct_pow_of_nonzero`,
  `squareFreeAuxRevContribution_correct`, and
  `squareFreeAux_weightedProduct_nonzero`.
- Updated public `squareFree_weightedProduct` so it no longer exposes the
  old broad raw derivative-active state provider shape.

## Current frontier

The weighted-product theorem now reflects the actual split between
level-completion state data and raw tail validity. The raw tail recursion is
still a real proof input, but callers no longer have to provide a universal
raw reachability payload just to feed the level-completion chain.

## Next step

Successor SquareFree work can use the new provider split to route #6230's
normalized pairwise/residual provider work through the exact boundary it needs,
instead of depending on the former broad raw state provider.

## Blockers

- None for this API-surface refactor.

## Verification

- `lake build HexPolyFp` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check` — passed.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` /
  `FIXME` in `HexPolyFp/SquareFree.lean` — no matches.
