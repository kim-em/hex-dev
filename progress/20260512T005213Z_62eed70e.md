# PR repair #3516 — stuck CI restart

## Accomplished

- Claimed PR #3516 for repair after higher-priority stuck PRs were already
  repair-claimed.
- Checked out branch `agent/0250d888` and confirmed the PR is mergeable
  against `main`.
- Diagnosed the remote state as stale CI: Linux `build` remained queued and
  `conformance` remained in progress, while the macOS build had passed.
- Verified the HexBZ bench slice locally with `lake build
  HexBerlekampZassenhaus.Bench`, `lake exe hexbz_bench list`, and `lake exe
  hexbz_bench verify`.
- Ran `python3 scripts/check_dag.py`, `python3 scripts/conformance_targets.py
  --check`, `python3 scripts/status.py HexBerlekampZassenhaus`, `git diff
  --check`, and a targeted banned-marker scan on
  `HexBerlekampZassenhaus/Bench.lean`.

## Current frontier

- PR #3516 remains locally healthy and mergeable.
- This progress-only commit is the bounded repair action to trigger a fresh CI
  run for the stuck remote jobs.

## Next step

- Push `agent/0250d888` with `--force-with-lease` and mark PR #3516 salvaged.

## Blockers

- None.
