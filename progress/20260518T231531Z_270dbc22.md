## Accomplished

- Claimed feature issue #5159 after the open directive issues were rejected by
  `coordination claim` as dependency-blocked.
- Added public docstringed `@[simp]` lemmas
  `Hex.ZPoly.polyProduct_pair` and
  `Hex.ZPoly.polyProduct_replicate_zero_toArray` in
  `HexHensel/Multifactor.lean`.
- Kept the change scoped to the upstream `Array.polyProduct` wrapper surface;
  no downstream call sites were rewired.

## Current frontier

- Issue #5159 deliverables are complete and ready for PR publication.

Verification:

- `lake build HexHensel.Multifactor` passed.
- `lake build HexHensel` passed.
- `lake build` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- `git grep -n "polyProduct_pair\\|polyProduct_replicate_zero_toArray" HexHensel/Multifactor.lean`
  found both declarations.
- `git diff -- HexHensel/Multifactor.lean | rg -n "sorry|axiom|native_decide|TODO|FIXME"`
  produced no matches.

## Next step

- Publish the PR for #5159.

## Blockers

- None for #5159.
