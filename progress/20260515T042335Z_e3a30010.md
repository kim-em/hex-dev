# Session e3a30010 — work #4085

## Accomplished

- Claimed #4085 (variable prime-field product identity) and rebased onto
  the post-#4138/#4137/#4136 main.
- In `HexBerlekamp/RabinSoundness.lean`:
  - `xPowSubX_one_eval_eq_zero`: every prime-field residue is a root of
    `xPowSubX 1`, routed through `ZMod64.pow_prime_of_prime_modulus` and
    `FpPoly.eval_sub`/`eval_monomial`/`eval_X`.
  - `primeFieldLinearFactor_dvd_xPowSubX_one`: per-residue linear-factor
    divisibility, composed via `FpPoly.X_sub_C_dvd_of_eval_eq_zero`.
  - `primeFieldLinearFactor_distinct_common_dvd_one`: distinct linear
    factors are coprime (collapse-to-constant-difference plus
    `C_ne_zero_dvd_one`).
  - `foldl_primeFieldLinearProduct_dvd` and
    `primeFieldLinearProduct_dvd_xPowSubX_one`: foldl-shape divisibility
    using `mul_dvd_of_dvd_dvd_common` and a new
    `coprime_mul_of_coprime_both` helper.
  - `primeFieldLinearFactor_size` / `primeFieldLinearFactor_monic`,
    `leadingCoeff_mul_fpoly` / `monic_mul_monic`, and the foldl
    `foldl_size_and_monic` invariant that gives
    `primeFieldLinearProduct_size = p + 1`,
    `primeFieldLinearProduct_ne_zero`, and
    `primeFieldLinearProduct_monic`.
  - `xPowSubX_one_coeff_p`, `xPowSubX_one_coeff_high`,
    `xPowSubX_one_size = p + 1`, and `xPowSubX_one_monic`.
  - `eq_of_dvd_of_size_eq_of_monic`: monic divisors of equal size are
    equal (via leadingCoeff comparison of the constant quotient).
  - `primeFieldProduct_X_eq_xPowSubX`: headline identity for #4085.
- In `HexPolyFp/Basic.lean`: dropped `private` from
  `ZMod64.coeff_mul_at_top` so the leading-coefficient-of-product fact is
  reusable in `HexBerlekamp/RabinSoundness.lean`.
- Verified `lake build HexBerlekamp.RabinSoundness`, `lake build
  HexBerlekamp`, `lake build HexBerlekampZassenhaus`, `python3
  scripts/check_dag.py`, `git diff --check`, and the affected-file
  forbidden-placeholder grep (no `axiom`, `native_decide`, `TODO`,
  `FIXME`, or theorem-level `sorry` introduced).

## Current frontier

The variable prime-field product identity downstream of `xPowSubX 1` is
now Mathlib-free. `primeFieldProduct_X_eq_xPowSubX` lives in
`HexBerlekamp/RabinSoundness.lean`, ready for #4075 (witness
substitution) to consume.

## Next step

Proceed with #4075 (substitute Berlekamp witness into the
prime-field product identity) using the new headline theorem plus the
existing kernel-witness substitution API.

## Blockers

- None.
