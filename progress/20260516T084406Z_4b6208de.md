# Repair PR #4455 (HexPoly Phase 6 simp surface polish)

## Accomplished

- Reproduced the CI failure on `HexPolyFp/Basic.lean:2178` and `:2223`:
  marking `DensePoly.coeff_shift` as `@[simp]` (in commit 35fe82a) caused
  the preceding `simp [hnj]` to unfold the shift coefficient into its
  `if`-form, after which the explicit `rw [DensePoly.coeff_shift_scale …]`
  could no longer locate its pattern.
- Surgical fix: pass `-DensePoly.coeff_shift` to both `simp [hnj]` calls
  in `coeff_mul_shift_scale_one`, restoring the pre-simp shape that the
  downstream `coeff_shift_scale` rewrite expects. No new lemmas, no proof
  rework; behaviour of the new `@[simp]` lemmas is preserved everywhere
  else.
- `lake build HexPolyFp.Basic` and the full `lake build` now succeed
  locally.

## Current frontier

PR #4455 ready to push for re-run. Conformance failed for the same root
cause, so a single fix unblocks both checks.

## Next step

Force-push and `coordination mark-pr-salvaged 4455`.

## Blockers

None.
