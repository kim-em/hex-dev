# Session 4d21cf4f — #4737 scaled cover-at-min bridges (partial)

## Accomplished

Claimed #4737 (scaled candidate cover-at-min chain). Audited scope and found
the three target theorems mirror the existing primitive cover-at-min surface
but break at the candidate-monicness step — the scaled candidate is not monic
for non-monic primitive `core`. Landed the supporting bridges as a partial PR
and filed #4755 for the residual three-theorem deliverable.

Added to `HexBerlekampZassenhausMathlib/Basic.lean`:

* `size_normalizeFactorSign_eq` (private): stored-size preservation through
  the sign normaliser.
* `size_primitivePart_eq_of_ne_zero` (private): stored-size preservation
  through `Hex.ZPoly.primitivePart` on nonzero inputs, via
  `content_mul_primitivePart` + `Hex.ZPoly.scale_size_of_nonzero`.
* `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum`: the
  scaled-candidate counterpart of
  `natDegree_toPolynomial_recombinationCandidate_eq_sum`. Threads the
  scale + centred-lift + primitive-part + sign-normalisation chain through
  the size helpers and concludes via `Polynomial.natDegree_prod_of_monic` on
  the monic lifted-factor product.
* `toPolynomial_scaledRecombinationCandidate_squarefree`: scaled counterpart
  of `toPolynomial_recombinationCandidate_squarefree`, inheriting
  squarefreeness from `target` via the exact-quotient witness.

Verification:

* `lake build HexBerlekampZassenhausMathlib`
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/Basic.lean`
  — no new entries (10 / 10 against `origin/main`).

## Current frontier

Residual scope of #4737 lives on #4755 (scaled cover-at-min chain theorems),
which depends on these bridges plus a per-factor packaging analog that
derives `content g = 1` and `normalizeFactorSign g = g` without using
candidate monicness.

## Next step

A worker picks up #4755 and writes the three target theorems plus a
`exists_representingSubset_of_mem_normalizedFactors_scaledRecombinationCandidate_of_primitive_pos_lc_core`
packaging helper, mirroring the existing primitive chain but routing through
`Polynomial.natDegree_multiset_prod` (non-monic) and a primitivity-based
derivation of `content g = 1` and `normalizeFactorSign g = g`.

## Blockers

None on the partial PR. #4755 is the natural successor for the remaining
three-theorem deliverable.
