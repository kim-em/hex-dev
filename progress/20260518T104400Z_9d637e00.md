# 2026-05-18 10:44 UTC — forty-second Phase 4/5 checkpoint (9d637e00)

Claimed summarize issue #4980 and recorded this checkpoint covering
the merged-PR wave since #4958 closed at `2026-05-18T07:58:42Z`.
Window upper bound is the issue's creation timestamp
`2026-05-18T10:39:00Z` (no PRs merged between issue creation and
this session picking it up; the next post-window PR will be the
in-flight #4976 capstone). PR count:

```
gh pr list --state merged --limit 30 \
    --json mergedAt,number,title \
    --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
  | awk '$1 > "2026-05-18T07:58:42Z"' \
  | awk '$1 <= "2026-05-18T10:39:00Z"' \
  | wc -l
```
= **10 new PRs merged**, matching the issue body's headline estimate
exactly.

## `coordination orient` snapshot

```
=== DIRECTIVES (highest priority) ===
#2637 HexBerlekampZassenhaus: Factorization record API + sign/multiplicity bugs
#2567 HO-4: Formalise BHKS Theorem 5.2 (factorFast termination, leaf theorem)
#2564 HO-1: BZ recombination — rollback and rewrite to van Hoeij CLD

=== Unclaimed work items ===
#4880 BZ Mathlib-free: drop hcore_monic Gap 1 from umbrella
      (directive-level-blocked per #4940; not actionable as a feature)

=== Claimed work items ===
#4980 Summarize forty-second Phase 4/5 checkpoint                (this session)
#4976 BZMathlib: abstract-bound sibling of
      factor_exhaustive_branch_entry_irreducible_of_choosePrimeData
      (public umbrella _of_bound)                                (52 min)

=== Issues with open PRs ===
#4902 BZMathlib: abstract-bound siblings of
      exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate
      (monic + primitive unscaled)                       (PR #4917, 8 h)

=== Blocked issues (truncated; full chain in orient) ===
#4951 [#4929] liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core
              (unscaled primitive)
#4934 [#4931] recombinationSearchModAux_some_and_covers_* (monic)
#4931 [#4927] liftedFactorSubsetPartition_prefix_none (monic)
#4929 [#4925] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_*
#4927 [#4924] coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd
#4925 [#4919] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd_*
#4924 [#4918] exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd
#4919 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset_*
#4918 [#4902] mem_T_iff_exists_irreducibleFactor_representingSubset (monic)
#4832 [#4821, #4830, #4831] BZMathlib: lifted partition recombination support
#4831 [#4821, #4830] BZMathlib: lifted partition disjointness + assoc uniq
#4830 [#4821, #4821] BZMathlib: initial lifted partition cover field
#4825 [#4680, #2567] BZ Mathlib-free: ZPoly.Irreducible per factor.factors entry
#4821 [#4680] BZMathlib: discharge executable Hensel subset analytic obligation
#4819 [#4821] BZMathlib: prove factor_irreducible_of_nonUnit
#4818 [#4825] BZ: prove executable ZPoly isIrreducible_iff
#4680 [#2567] HO-1 substrate: compose ModP and Hensel lift constructors
#4172 [#4170] HO-1 residual: executable irreducibility checker theorem
#4170 [#2567] HO-1 residual: executable factor irreducibility bridge

=== Open pull requests ===
#4917 [agent/8a4738a1] exists_representingSubset_of_mem_normalizedFactors_*
                       (repair-claimed) [CONFLICTS]
#2656 [spec/real-roots-and-sturm] spec: hex-real-roots and hex-sturm (planned)
```

The HexLLL Phase-4 inconclusive-bench issue #4334, parked across
multiple recent windows on host-contention measurement skips, has
since been closed or otherwise removed from the queue — it is no
longer listed in the `Unclaimed work items` section.

## Per-theme PR groupings (10 PRs)

### BZMathlib A2 `_of_bound` propagation — public-bridge layer into `IntReductionMod` (8 PRs)

The A2 abstract-bound (`_of_bound`) cascade completed its run through
the upper layers of `HexBerlekampZassenhausMathlib/Basic.lean` and
crossed into `HexBerlekampZassenhausMathlib/IntReductionMod.lean`,
reaching the slow-path exhaustive umbrella aux and the
`reassemblyExpansionComplete_exhaustive_*` discharger. Each sibling
lifts a lemma whose original constrained precision via the core-shape
`defaultFactorCoeffBound core` to an abstract bound `B' : Nat` with
`hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'` and
`2 * B' < d.p ^ d.k`. The originals are rewired as thin instantiations
on the abstract interface, so consumers continue to type-check
unchanged.

* **#4962** — abstract-bound siblings of scaled `_some_factor_associated_`
  public wrappers (primitive + primitive_pos_lc_core).
* **#4965** — `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence_of_bound`
  (closes #4938). One layer above #4962.
* **#4969** — `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
  (closes #4944). One layer above #4965.
* **#4972** — `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
  (closes #4963). Inner-B pairing wrapper at the `factorWithBound`
  inner-coefficient bound surface.
* **#4973** — `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
  (closes #4947). Public-bridge layer of `Basic.lean`.
* **#4975** — `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`
  (closes #4971). First `_of_bound` in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.
* **#4978** — `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`
  (closes #4970). Umbrella aux `_of_bound` in `IntReductionMod.lean`.
* **#4979** — paired `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
  (monic + primitive_pos_lc_core) (closes #4974). The exhaustive-arm
  reassembly discharger now has its `_of_bound` companion.

After this wave, the public umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`
(`IntReductionMod.lean`) has both consumers of `hprecision` —
the inner `reassemblyExpansionComplete_exhaustive_of_ne_zero` call
(via #4979) and the forwarded `hprecision` argument to the aux
(via #4978) — already `_of_bound`-lifted. The umbrella's own
`_of_bound` capstone is filed as #4976 and is currently claimed (PR
expected this window or next).

### `hcomplete` premise drops on slow-path arm umbrellas (2 PRs)

Two more pre-existing slow-path arm umbrellas dropped their explicit
`hcomplete` premise, discharging it internally via the matching arm
discharger. With these, the constant / quadratic / singleton arm
umbrellas in `IntReductionMod.lean` are now all `hcomplete`-free; only
the slow-path exhaustive umbrella still threads `hcomplete` implicitly
via its `reassemblyExpansionComplete_exhaustive_of_ne_zero` call.

* **#4966** — `factor_quadratic_branch_entry_irreducible_of_quadraticRoots`
  and `factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`
  (both quadratic-arm umbrellas; closes #4964). Discharger
  `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  (the non-monic #4747 product).
* **#4968** — `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  and `factor_small_mod_singleton_branch_entry_irreducible` (both
  singleton-arm umbrellas). Discharger
  `reassemblyExpansionComplete_singleton_of_irreducible_of_pos_lc`
  (the #4956 `_of_pos_lc` product).

## Status snapshot

* **`libraries.yml`**: no commits in window
  (`git log --since="2026-05-18T07:58:42Z" --until="2026-05-18T10:39:00Z" -- libraries.yml`
  empty). The next `done_through` bump is still gated on the HO-1
  critical-path chain.
* **In-flight PRs at orient time** (`2026-05-18T10:44:05Z`):
  * **PR #4917** (abstract-bound siblings of
    `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
    for #4902, monic + primitive unscaled) — `repair-claimed` with
    `CONFLICTS`, branch `agent/8a4738a1`. Has been in repair across
    the previous two summarize windows. This PR blocks a long chain
    of monic + primitive_pos_lc_core `_of_bound` substrate siblings
    in `Basic.lean`: #4918, #4919, #4924, #4925, #4927, #4929,
    #4931, #4934, #4951.
  * **PR #2656** (`spec/real-roots-and-sturm`) — long-running SPEC
    PR, status `planned`. Not part of any active phase.
* **Claimed but no PR yet**:
  * **#4976** (public umbrella `_of_bound` capstone for
    `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`,
    `IntReductionMod.lean`) — claimed by session
    `67fe92e6` ~52 min before this summarize ran. Unblocked when
    #4979 landed.
* **Directive closures in window**: none.
  `gh issue list --label directive --state closed --jq '.[] | "\(.closedAt)"'`
  filtered to `> 2026-05-18T07:58:42Z` is empty. #2637, #2567,
  #2564 all still open.
* **Substrate investigation closures in window**: none new (the
  helper 2/3/4 #4940 audit and helper-5 #4942 investigation closed
  in earlier windows).

## HO-1 critical-path commentary (capstone #4170 / sub-capstone #4825)

The BZ Mathlib-free chain remains gated on #4680 (Hensel subset
analytic obligation composition) and ultimately on directive #2567
(BHKS Theorem 5.2).

Within this window the substrate `_of_bound` cascade advanced
significantly: the public-bridge layer of
`HexBerlekampZassenhausMathlib/Basic.lean` is now fully
`_of_bound`-lifted (`#4962` → `#4965` → `#4969` → `#4972` → `#4973`),
and the cascade has crossed into
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` (`#4975` →
`#4978` and `#4979`) — covering the umbrella aux and the
`reassemblyExpansionComplete_exhaustive_*` discharger consumed by
the public umbrella.

The role this plays on the critical path: lowering the precision
threshold required by the
`recombinationSearchModAux_some_factor_associated_*` /
`exhaustiveCoreFactorsWithBound_coverage_*` chain, which is the
primary downstream consumer once the A2 / Mignotte recovery wiring is
complete. With this window's landings, the public umbrella
`_of_bound` capstone (#4976) is the **final step** of the A2 cascade
for the slow-path exhaustive arm — both of its body's `hprecision`
consumers (inner discharger + aux) now have `_of_bound` siblings in
place, and #4976 is claimed and in flight.

Downstream of the public-umbrella capstone, the unscaled-monic side
of the recursive-coverage layer (#4918 / #4919 / #4924 / #4925 /
#4927 / #4929 / #4931 / #4934 / #4951) is still filed and queued
behind the in-repair PR #4917 (covering #4902). If the repair agent
abandons PR #4917, the underlying issue #4902 returns to the queue
and the monic-side cascade restarts behind it.

## #4880 umbrella drop posture

The `_of_bound` cascade addresses precision (Gap 3, `hprecision`) at
the public umbrella, but is **independent** of Gap 1 (`hcore_monic`)
at the umbrella surface — so this window's landings do not alter the
#4880 posture. The current state remains:

* Helper 1 substrate (`factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`):
  **closed** via #4945.
* Helper 5 substrate (`reassemblyExpansionComplete_exhaustive_of_ne_zero`):
  weakened-conclusion sibling **closed** via #4953 plus mid-layer
  `_of_pos_lc` substrate via #4952 and umbrella-internal rewiring via
  #4957.
* Helpers 2, 3, 4: **architecturally infeasible** at the present API
  surface per #4940's audit. The underlying
  `multifactorLiftQuadratic_each_monic`
  (`HexHensel/QuadraticMultifactor.lean:1196`) has a per-output
  monicness conclusion that is false for non-monic primitive core:
  for `n` mod-`p` factors and a non-monic primitive core, the
  multifactor recursion's final residual carries `lc(core)` as its
  leading coefficient. Helpers 3 and 4 route through helper 2's
  per-output monicness internally and inherit the same blocker.

The umbrella drop remains **directive-level blocked**. Closing the
residual gap requires either:

* **Option A** — consumer-side recombination invariant co-redesign:
  relax `hd_liftedFactor_monic` along the consumer chain to
  "all-but-one monic; the residual carries `lc(core)`", spanning
  ~10–15 substrate sub-issues across the `_of_primitive_pos_lc_core`
  chain.
* **Option B** — executable-side monicising-by-scaling refactor:
  modify `exhaustiveCoreFactorsWithBound`
  (`HexBerlekampZassenhaus/Basic.lean:5495`) or introduce a
  `henselLiftData_scaled` variant that monicises by scaling before
  invoking `multifactorLiftQuadratic`, with cascading proof updates.

Per #4940's recommendation, the next planner cycles should **not**
file substrate sub-issues beneath #4880; the umbrella's residual
Gap 1 is documented in the docstring at
`HexBerlekampZassenhausMathlib/IntReductionMod.lean:3589` as
architecturally infeasible at the present API surface.

## Carryover for the next summarize

* **#4976** (public umbrella `_of_bound` capstone in
  `IntReductionMod.lean`, for `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`)
  — claimed and in flight; its PR will land in the next summary
  window. It is the A2 cascade's final step for the slow-path
  exhaustive arm.
* **PR #4917** (abstract-bound siblings of
  `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
  for #4902) — repair-claimed, `CONFLICTS`. Has now sat in this
  state across three summarize windows; the monic-side recursive-
  coverage `_of_bound` chain (#4918 / #4919 / #4924 / #4925 / #4927 /
  #4929 / #4931 / #4934 / #4951) remains queued behind it.
* **Blocked chain** (transitive on #2567 / #4680):
  #4818 / #4819 / #4821 / #4825 / #4830 / #4831 / #4832 / #4170 /
  #4172.
* **#4880 umbrella drop**: directive-level blocked on Option A /
  Option B. Workers should not claim this issue (per its own body).
* **Slow-path `hcomplete` drop residual**: only the slow-path
  exhaustive umbrella still threads `hcomplete` implicitly via its
  `reassemblyExpansionComplete_exhaustive_of_ne_zero` internal call.
  This is the natural follow-on once the #4976 `_of_bound` capstone
  lands and stabilises the umbrella's signature.
