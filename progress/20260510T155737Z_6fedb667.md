## Accomplished

Claimed #3008 and wired the RREF-specific row-span completeness path in
`HexMatrix/RREF.lean`.

Removed the false general `IsEchelonForm.spanCoeffs_complete` theorem and
narrowed the general span membership theorem to one-way soundness. Added
`IsRREF.spanCoeffs_complete`, `IsRREF.spanContains_iff`, and the public
`Matrix.spanContains_iff` wrapper using `rref_isRREF`.

Also removed a duplicate private `foldl_add_eq_acc_ring` helper declaration
that was blocking `HexMatrix.RREF`; the earlier identical helper now serves the
later nullspace proof code.

Verified:

- `lake build HexMatrix.RREF`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/RREF.lean`
  reports only the pre-existing `sorry`s at lines 116, 849, and 854.

## Current frontier

The public `spanContains` API now has the corrected RREF-backed iff theorem.
The unrelated RREF proof holes remain: `rref_isRREF`, `nullspace_sound`, and
`nullspace_complete`.

## Next step

Continue the HexMatrix proof frontier by discharging the remaining nullspace
transport theorems or the `rref_isRREF` contract, depending on the next claimed
issue.

## Blockers

None.
