# HexPolyFp Phase 6 closeout (#8214)

Session type: feature. Re-confirmed the Phase 6 exit criteria for
`HexPolyFp` on current `main`, removed three leftover dead private
helpers the re-check surfaced, and bumped
`libraries.yml[HexPolyFp].done_through` 5 → 6.

## Exit-criteria evidence

1. **Mathlib linter clean** — PASS. `lake exe runLinter` on the root and
   on each core module (`Basic`, `Compose`, `Quotient`, `SquareFree`,
   `ModCompose`, `Enumeration`) reports "Linting passed". Verified
   against a *fresh* recompile (deleted HexPolyFp oleans/ileans/c first,
   since `lake build` cache hits replay zero warnings) — no
   warning/error/linter/unused/deprecated output.
2. **Docstring coverage** — PASS. Detector over all public top-level
   `theorem`/`lemma` in the core surface (`Basic`, `Quotient`,
   `Compose`, `ModCompose`, `Enumeration`, `Frobenius`,
   `QuotientFrobenius`, `PrimeField`) plus SquareFree's public theorems:
   0 undocumented (#8200 closed the original ~27-theorem gap). The
   default `runLinter` `docBlame` suite also passes, covering public
   defs/structure fields.
3. **No dead/unreferenced declarations** — PASS after fixes. The #8178
   flagged dead names (`monicGcd_mul_div_reconstruct`,
   `yunFactorsContribution_reconstruct`,
   `yunFactorsContributionWithLevel_..._sync`) and the `_smoke` probes
   are gone (#8199). A fresh reachability scan over untagged private
   decls (557 of them) surfaced 3 genuinely-dead leftovers, now removed:
   - `HexPolyFp/Basic.lean` `fold_add_single_range_zero`
   - `HexPolyFp/Enumeration.lean` `coeffLists_nodup_cons_branch`
   - `HexPolyFp/SquareFree.lean` `derivative_C_mul`
   Two other scan hits were false positives (qualified/explicit uses):
   `PrimeField.intPow` (used as `ZMod64.intPow`), `SquareFree.pow_pow_mul'`
   (used at `rw [pow_pow_mul']`).
4. **Native computational path** — PASS. `grep -rn native_decide
   HexPolyFp` empty; no runtime oracle. No `axiom`/`sorry` (the two
   `Quotient.lean` matches are the words "axiom" in prose docstrings).
5. **Irreducibility / field-construction evidence** — PASS (N/A as an
   oracle concern). `FpPoly.Irreducible` is a `Prop` (absence of
   nontrivial factorizations). The `Field` instance and
   inverse-cancellation theorems take irreducibility as a *hypothesis*
   (`hg_irr : FpPoly.Irreducible g`); the library parametrizes over
   irreducibility rather than deciding a concrete polynomial via an
   oracle. Fully Lean-checked, no external oracle.
6. **Performance regression** — PASS by construction. The only changes
   remove three unused *proof* lemmas; zero runtime-path impact, so no
   benchmark regression is possible.

## Verification

- `lake build HexPolyFp` green (98 jobs) after removals.
- `lake exe runLinter` passes on root + all core modules.
- `python3 scripts/check_dag.py` exit 0 after the `libraries.yml` edit.
- `python3 scripts/status.py` lists `HexPolyFp` at Phase 7 (ready),
  `done_through: 6`.
- No `axiom`/`sorry`/`native_decide` introduced.

## Current frontier

`HexPolyFp` is at Phase 6 complete / Phase 7 ready.

## Next step

Phase 7 reference chapter `HexManual/Chapters/HexPolyFp.lean` (separate
future issue, explicitly out of scope here).

## Blockers

None.
