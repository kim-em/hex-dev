# 2026-05-11 14:20 UTC — issue #3452

## Accomplished

Claimed #3452 and added the Gauss content multiplicativity invariant to
`HexPoly/Euclid.lean` (Mathlib-free, `HexPoly` deps unchanged):

- Local `NatPrime` predicate plus Euclid's lemma in `Nat` and `Int` forms,
  threaded through `Nat.Coprime` (Lean core).
- `exists_natPrime_dvd_of_one_lt` by strong induction without `push_neg`.
- Polynomial Euclid: `natPrime_dvd_all_or_all_of_dvd_mul_coeff` —
  if a prime divides every coefficient of `p * q`, it divides every
  coefficient of `p` or every coefficient of `q`. Built on top of the
  existing `dvd_coeff_mul_last_of_dvd_mul_coeff_of_dvd_larger_left_products`
  largest-index split.
- Top-coefficient identity `coeff_mul_top` and integral-domain helper
  `mul_ne_zero_of_primitive` to rule out the `p * q = 0` edge case in the
  primitive Gauss step.
- `content_mul_of_primitive`: the product of two primitive integer
  polynomials is primitive.
- `coeff_scale_mul_scale`: `((scale a r) * (scale b s)).coeff n = a * b *
  (r * s).coeff n` via a `diagonalMulCoeffTerm` scaling helper.
- Deliverable `dvd_contentNat_mul_of_dvd_mul_coeff`: if a natural number
  `d` divides every coefficient of `p * q`, then `d ∣ contentNat p *
  contentNat q`. Proven by reducing both factors to their primitive parts
  and applying `nat_dvd_of_scalar_mul_primitive_coeff_dvd` to the primitive
  product.

Verified:

- `lake build HexPoly.Euclid`
- `lake build HexPoly`
- `lake build HexPolyZ HexPolyFp HexBerlekamp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg '^axiom\b|native_decide|TODO|FIXME|sorry' HexPoly/Euclid.lean` is empty.

## Current frontier

The chain `#3440 → #3435 → #3389 → #3346 → #3252` now has the algebraic
invariant it needed. `#3440`'s McCoy row instantiation can consume
`dvd_contentNat_mul_of_dvd_mul_coeff` (or any of the new helpers, all
within `HexPoly/Euclid.lean`).

## Next step

Open the PR for #3452 and let the next planner sweep pick up the now-
unblockable McCoy row work on #3440 / #3435.

## Blockers

None.
