# Repair session: PR #3582

## Accomplished

- Claimed PR #3582 (HexGramSchmidt: derive GS norm ratio from determinant
  product bridge) via `coordination claim-pr-repair`.
- Merged `origin/main` into `agent/419be9f9`. Single conflict in
  `HexGramSchmidt/Int.lean` inside `basis_normSq`: HEAD provided a direct
  proof using `gramDet_eq_prod_normSq`, `gramDet_pos`,
  `gramSchmidtNormProduct_succ`, and `Rat.mul_div_cancel`; `origin/main`
  delegated to a stub `basis_normSq_core` (with `sorry`).
- Resolved by moving `basis_normSq_core` to after its dependencies
  (`gramDet_eq_prod_normSq`, `gramDet_pos`, `gramSchmidtNormProduct_succ`)
  and filling its body with HEAD's proof. `basis_normSq` continues to
  delegate to `_core`, matching the `_core` pattern adopted by the
  Phase-6 API polish PRs on main.
- `lake build` clean; the `sorry` for `basis_normSq_core` is gone (the
  remaining `sorry`s in `HexGramSchmidt/Int.lean` are pre-existing on
  main: `gramDet_eq_prod_normSq_core`, the `scaledCoeffRows_*` helpers,
  etc.).
- Pushed with `--force-with-lease`; `coordination mark-pr-salvaged 3582`.

## Current frontier

PR #3582 now mergeable; auto-merge will land it once CI is green.

## Next step

Pick the next entry from `coordination list-pr-repair` (next was #3588).

## Blockers

None.
