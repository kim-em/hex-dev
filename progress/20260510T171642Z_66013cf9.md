## Accomplished

Claimed #3066 and discharged the prime-selection certificate holes in
`HexBerlekampZassenhaus/Basic.lean`.

Added a private `choosePrimeScoreStep` helper matching the existing
`choosePrimeScore?` fold body, then proved that successful candidate scoring,
score replacement, and the full fold preserve `isGoodPrime` evidence. Used
those helpers to prove `choosePrimeScore?_isGoodPrime` and
`choosePrime_isGoodPrime_of_selected`.

Also proved the two projection theorems from a successful `isGoodPrime` check:
`isGoodPrime_leadingCoeffAdmissible` and `isGoodPrime_squareFreeModP`.

Verified:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean`
  reports only unrelated pre-existing holes.

## Current frontier

The prime chooser now exports proof-carrying admissibility certificates. The
remaining nearby `HexBerlekampZassenhaus/Basic.lean` holes are outside #3066,
including the finite-field instance and monic modular image work covered by
#3067.

## Next step

Pick up #3067 or another unblocked HexBZ proof issue after this PR lands.

## Blockers

None.
