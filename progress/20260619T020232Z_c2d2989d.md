# HexGF2 Phase 6 grind automation coverage for Euclid.lean (#8029)

## Accomplished

Promoted all 14 equation-shaped public `@[simp]` lemmas in
`HexGF2/Euclid.lean` to `@[simp, grind =]`, matching the ongoing Phase 6
grind-coverage sweep:

- `divMod_fst`, `divMod_snd`, `divMod_zero_right`, `divMod_zero_left`,
  `div_zero_right`, `mod_zero_right`, `zero_div`, `zero_mod` (the
  divMod/div/mod cluster)
- `gcd_zero_right`, `gcd_zero_left`
- `degree?_ofUInt64Monic_of_lt_64`, `degree_ofUInt64Monic_of_lt_64`
- `mod_add_mul_right_eq_mod`, `mod_eq_self_of_reduced`

All 14 are literal `lhs = rhs` forms. None of the issue's listed
candidates turned out to be an iff/conjunction/let-bound, so all were
promoted.

The `degree?`/`degree` pair is conditional on `n < 64` (deliverable 2):
the side hypothesis becomes a `grind` subgoal and the rewrite is oriented
`lhs → some n` / `lhs → n` with no recursive `degree`/`degree?` term on
the RHS, so it cannot loop. The two `mod` lemmas at lines 983/992 are
likewise oriented with the LHS strictly larger than the RHS (`(a+c*f)%f →
a%f`, `p%f → p`), so no cycle against the terminal rewrites.

## Current frontier

`lake build HexGF2` green; `HexGF2.Euclid` built in 1.1s — no grind-loop
slowdown. `scripts/check_dag.py` exit 0. `git diff --check` clean; no
added `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`. Annotation-only; no
proof bodies changed, no new imports, no `*Mathlib` file touched.

## Next step

None for this issue. Remaining unclaimed Phase 6 sweeps: #8035
(HexArith/Nat/Prime.lean), #8044 (HexGramSchmidt/Int.lean).

## Blockers

None.
