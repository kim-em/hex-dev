# Planner decomposition of #6584 (factorSlow → factorSlowModular cleanup)

## Accomplished

Decomposed #6584 ("rename factorSlow → factorSlowModular and drop
choosePrimeDataFallback") into two atomic sub-issues:

- #6597 (sub-issue A, unblocked): pure rename + Option signature
  migration. Renames `factorSlow / factorSlowWithBound /
  factorSlowFactorsWithBound` (and the companion theorems) to
  `factorSlowModular*`, folds the `?` shim from #6583 into the
  renamed totals so they're `Option`-returning natively, drops the
  shim and the Array-returning historical total, rewires the
  three-tier combinator, propagates the rename through the Mathlib
  bridge, and pivots Conformance / CrossCheck / Bench callers to
  `factorSlowTrial` (total) or `factor` (combinator).

- #6598 (sub-issue B, blocked on #6597): silent-fallback removal.
  Drops `choosePrimeDataFallback` (Basic.lean ~2117–2121),
  surveys the remaining `choosePrimeData` (total) call sites on
  the post-A tree, and either removes `choosePrimeData` entirely
  (preferred) or rewires its body to a non-silent form. Bridge
  updates in Mathlib include the
  `_hselected : primeData = Hex.choosePrimeData core` hypothesis
  shape in `IntReductionMod.lean` (~line 282).

Parent #6584 marked blocked on both subs via `coordination add-dep`;
claim released; replan label removed (decomposition is the terminal
state, not a replan trigger). Parent will auto-unblock once both
subs close and a future planner can close it then.

Why decompose (justifying override of the issue's "Single PR" note):

- Scope survey shows ~40 `factorSlow*` identifier sites + structural
  `Option` signature change + 121 `choosePrimeData[^?]` token
  matches (with ~13 actual term-level call sites in
  `HexBerlekampZassenhaus/Basic.lean` and one in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`) — well over
  the planner sizing rule (max 3 deliverables, ~2 files, ~200 lines).
- The rename and the fallback removal cut along a clean logical
  seam: A is mechanical, B requires per-call-site strategy choice
  (remove `choosePrimeData` entirely vs. rewire body). B can do its
  call-site survey more accurately on the post-A tree where
  `factorSlowModular`'s body has already migrated off
  `choosePrimeData` (total) onto `choosePrimeData?`.

Side cleanup: closed accidentally-created issue #6596 (artefact of
running `coordination plan --help` — the tool interprets `--help`
as an issue title).

## Current frontier

#6597 sits unblocked at the top of the unclaimed queue; the next
worker dispatch picks it up. #6598 unblocks when #6597 closes.
Parent #6584 stays open and blocked until both close.

## Next step

Worker dispatch to claim #6597 and execute the rename.

## Blockers

None.
