# Session c9cba686 — HexBZ Phase 4 report closure (#3527)

## Accomplished

- Claimed #3527 after checking the unclaimed queue and confirming the
  human-oversight queue was empty.
- Verified #3513 and #3514 are closed, then audited
  `reports/hex-berlekamp-zassenhaus-performance.md` against the current
  `HexBerlekampZassenhaus/Bench.lean` registrations and Phase 4 report
  requirements.
- Ran the missing `runFastPathPrecisionLocalChecksum` scientific target and
  committed its JSON artefact under
  `reports/bench-results/hex-berlekamp-zassenhaus-issue3527-precision-local.json`.
- Updated the HexBZ performance report with the precision-local command,
  artefact path, SHA-256, encoded parameters, inconclusive verdict, and final
  hash.
- Updated the report Concerns to reflect the live state: all scientific
  verdicts remain inconclusive, dependency phase gates still block
  `HexBerlekampZassenhaus`, comparator metadata is still absent, and HO-3
  (#2566) remains the linked complexity-evidence concern.

## Current frontier

HexBerlekampZassenhaus remains at `done_through: 3`. The report is more
complete, but Phase 4 cannot be claimed because
`scripts/status.py HexBerlekampZassenhaus` still reports blockers on
`HexBerlekamp.done_through >= 4` and `HexLLL.done_through >= 4`, and the
headline report still contains live Concerns.

## Next step

Land this report-cleanup PR. Follow-up work should resolve the upstream
HexBerlekamp and HexLLL Phase 4 blockers, then either produce consistent
HexBZ scientific verdicts and comparator metadata or keep HO-3 open with the
remaining precise gaps.

## Blockers

- `HexBerlekamp.done_through >= 4`
- `HexLLL.done_through >= 4`
- HexBZ scientific verdicts are still inconclusive.
- HexBZ comparator metadata remains absent.
