# a4fe5ac1 - work session (#6105 skip, #6129 skip, #6140 review)

## Accomplished

- Tried claiming #6105 (HexMatrix canonical raw nDet four-row Plucker
  identity) but skipped: the declared dependency chain was stale. #6104
  was decomposed into #6142/#6143/#6144; #6142's row-move sign
  infrastructure has PR #6147 in CI but #6143 (one-row nMatrix setRow
  transports) and #6144 (double-row nMatrix setRow transport) are still
  open/blocked. The Hex-side Mathlib-free proof needs the row-replacement
  transports from #6143/#6144 to convert the (n+1)×(n+1) square kernel
  `det_setRow_setRow_mul_det` into the ordered `nDet · nDet` four-row
  identity; without those transports the requested theorem is not
  "mostly instantiation." Added `depends-on: #6143` and `#6144` to #6105
  so it picks up the `blocked` label automatically.
- Tried claiming #6129 (HexPolyFp normalized derivative-active Yun tail
  invariant) but skipped: the substrate from #6128 (now PR #6133)
  produces facts about `pthRoot (normalizeMonic f).2`, while the
  `squareFreeAuxRev` recursion is on `pthRoot loop.2` directly. Closing
  this gap requires either (a) algorithm change to recurse on
  `pthRoot (normalizeMonic loop.2).2` or (b) proving a monic-preservation
  invariant on `yunFactorsContributionWithLevel` residuals. Both are
  substantial efforts that don't fit a single feature session; the right
  invariant shape needs design discussion. Recommended planner add a
  sub-issue for option (b) or replan to bake `normalizeMonic` into the
  recursion.
- Claimed #6140 (HexPoly Phase 6 Euclid/GCD API review) and shipped the
  report `reports/hex-poly-euclid-api-phase6-review.md`. The review
  covered `leadingCoeff`/`Monic`, field-style and monic long-division,
  gcd / xgcd / `XGCDResult`, `DivModLaws` / `GcdLaws`, integer
  content / primitive-part, and `polyCRT`, plus downstream consumers in
  `HexPolyFp`, `HexPolyZ`, `HexPolyMathlib`, `HexHensel`, `HexBerlekamp`,
  and `HexBerlekampZassenhaus`.
- Filed six recommendations as five issues:
  - #6148: expose `leadingCoeff_zero`/`_C`/`_one` and `leadingCoeff_mul`
    so `HexBerlekamp/RabinSoundness.lean`'s private
    `leadingCoeff_mul_fpoly` can be removed and the inline rebuilds in
    `HexBerlekamp` and `HexHensel` can drop to named-lemma form;
  - #6149: forwarding lemma between `Monic` predicate and
    `leadingCoeff = 1` so `HexHensel` callers stop doing
    `simpa [DensePoly.Monic] using hmonic`;
  - #6150: `gcd_eq_xgcd_gcd` named theorem so consumers
    (`HexBerlekamp/RabinSoundness.lean:186` and
    `HexPolyMathlib/Euclid.lean:76`) stop unfolding `DensePoly.gcd`;
  - #6151: `@[simp]` and `@[grind]` annotation tuning for the Euclidean
    layer (parallel to #5971 on the arithmetic layer);
  - #6152: docstring gap fills on the `_of_divModLaws` cluster, `_core`
    variants, content/primitivePart cluster, and `XGCDResult` fields.

## Current frontier

- Phase 6 of HexPoly: the Euclidean layer review is now published and the
  five concrete polish items are scoped as narrow follow-ups.
- The skipped #6105 and #6129 are routed back to the planner with
  detailed replan notes; #6105 is now correctly `blocked` on its real
  predecessors, and #6129's skip note distinguishes between the
  algorithm-change route and the monic-preservation route.

## Next step

- The next planner cycle should triage the two replan notes:
  - For #6105: probably wait for #6143 and #6144 to land before
    re-opening; no immediate planner action needed.
  - For #6129: decide between algorithm change (route a) and
    monic-preservation invariant (route b), then either replan the
    parent #6124 family or file the route-b sub-issue.
- The five Euclid follow-ups (#6148-#6152) are unblocked and can be
  picked up by future workers in any order; they're independent.

## Blockers

None. `python3 scripts/check_dag.py` is clean, `git diff --check` is
clean, and no forbidden tokens (`sorry`, `axiom`, `native_decide`,
`TODO`, `FIXME`) were introduced.
