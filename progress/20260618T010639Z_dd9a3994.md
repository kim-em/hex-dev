# Progress — 2026-06-18 (session dd9a3994, /work → /feature #7855)

## Accomplished

Claimed and completed #7855. Added fast-core reassembly-completeness producers in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`:

- `fastCoreComplete_of_forwardInputs`
- `fastCoreComplete_of_cut`

These prove `Hex.reassemblyExpansionComplete (Hex.normalizeForFactor f) ...`
from successful fast-core output plus the same forward/cut certificates already
used to prove core-factor irreducibility. The proof reuses the existing
`IntReductionMod.reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`
assembler and derives the needed product, sign-normalization, positive-degree,
positive-leading-coefficient, and repeated-part fuel facts from fast-core
success.

Threaded those producers through no-`hcomplete` public raw irreducibility
siblings:

- `rawIrreducible_of_forwardInputs`
- `rawIrreducible_of_cut`

Kept the existing lower-level wrappers with explicit `hcomplete` unchanged.
Exposed two executable fast-core invariants from
`HexBerlekampZassenhaus/Basic.lean`: sign-normalization and positive degree for
successful core factors.

## Current frontier

The fast raw-factor irreducibility wrappers no longer require callers to supply
`reassemblyExpansionComplete` separately when the raw array comes from the
successful fast-core branch. The remaining capstone work is to assemble the
unconditional fast raw irreducibility producer using these wrappers plus the
existing true-support/cut substrate.

## Next step

Proceed to #7856 once its dependencies are unblocked: assemble the unconditional
`factorFastRaw_irreducible_of_some`-style capstone from the fast-core success,
cut/true-support, and new reassembly-completeness producers.

## Blockers

None for #7855.

Verification:

- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over touched Lean files: no matches for
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

Note: the worktree still has a pre-existing unrelated `.claude/CLAUDE.md`
modification; it was not touched or staged.
