# Session a0d74c4b: triage of #4597 reassemblyExpansionComplete singleton arm

## Accomplished

* Claimed #4597 (HO-1 substrate: discharge
  `reassemblyExpansionComplete` for the small-mod singleton arm
  Mathlib-bridge variant, supersedes #4593).
* Oriented on the relevant Mathlib-free machinery
  (`expandRepeatedPartFactorArray`,
  `consumeExactPower`, `consumeExactPower_invariant`,
  `consumeExactPower_one`,
  `expandRepeatedPartFactorArray_singleton_one`,
  `divMod_eq_of_monic_mul_eq`,
  `exactQuotient?_eq_some_of_mul_eq_monic_of_pos_degree`,
  `exactQuotient?_eq_some_of_divMod_eq_of_shouldRecord`) and the
  Mathlib-bridge consumer
  `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  at `HexBerlekampZassenhausMathlib/IntReductionMod.lean:1053`.
* Identified that this issue's three deliverables are covered by
  already-existing sibling sub-issues under #4594 (quadratic arms
  parent):
  * Deliverable 1 (Mathlib structural lemma
    `normalizeForFactor_repeatedPart_isPow_squareFreeCore_of_irreducible`)
    specialises from #4602's more general
    `normalizeForFactor_repeatedPart_isPow_polyProduct_of_irreducible_factors_cover`
    at `coreFactors := #[squareFreeCore]`.
  * Deliverable 2 (Mathlib-free helper
    `expandRepeatedPartFactorArray_pow_singleton`) specialises from
    #4603's more general
    `expandRepeatedPartFactorArray_residual_eq_one_of_pow_decomposition`
    at `coreFactors := #[q]`, `exponents := [k]`.
  * Deliverable 3 (the public discharger
    `reassemblyExpansionComplete_singleton_of_irreducible`) is a
    ≤10-line composition of the two specialisations.
* Verified that the planner of #4597's "concrete computation, ~80–150
  lines" estimate for deliverable 2 is optimistic: existing
  `exactQuotient?` infrastructure on `Hex.ZPoly` only covers monic
  divisors directly (`divMod_eq_of_monic_mul_eq`,
  `HexPolyZ/Basic.lean:1191`), so a singleton-power helper for a
  non-monic irreducible (which `squareFreeCore` can be) requires
  either a new divMod lemma for non-monic divisors of pure powers, or
  a degree-counting reformulation that bounds the multiplicity
  returned by `consumeExactPower` independently. Both routes are
  150–300 lines.
* Briefly created sub-issue #4606 (singleton-specific expansion
  helper) before noticing #4603 supersedes it; closed #4606 with a
  superseded-by note.
* Added `depends-on: #4602` and `depends-on: #4603` to #4597's body
  (auto-applied the `blocked` label).
* Posted a breadcrumb comment on #4597 explaining the dependence and
  residual scope, then `coordination skip`ped to route it through
  planner re-triage.

## Current frontier

#4597 is blocked on #4602 + #4603 landing. Once both are merged, the
next planner should either:

* close #4597 entirely if the singleton corollaries can be folded into
  #4604's assembly or absorbed directly into the HO-1 capstone wiring
  (#4170);
* otherwise narrow #4597's body to the residual scope: two ≤10-line
  specialisation corollaries (one Mathlib-free, one Mathlib-bridge)
  plus the public discharger
  `reassemblyExpansionComplete_singleton_of_irreducible`.

## Next step

Wait for #4602 and #4603 to merge. No further work on #4597 should be
claimed until then; the `blocked` label and skip-to-replan should
keep it out of the worker queue.

## Blockers

* #4602 (claimed; Mathlib structural radical-relation between
  `squareFreeCore` and `repeatedPart`).
* #4603 (claimed; Mathlib-free expansion helper for repeated-part pow
  decomposition).

No code landed in this session — the value is the queue-state cleanup
(removed redundant sub-issue #4606, added dependency links, posted
breadcrumb).
