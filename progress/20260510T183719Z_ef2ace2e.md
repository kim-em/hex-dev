## Accomplished

Claimed and resolved #3074. Added two new private theorems in
`HexMatrix/Determinant.lean` immediately after
`partialColumnTupleCoeff_insertAt_last_fold`:

1. `partialColumnTupleCoeff_vectorLengthCast` — cast lemma that exposes
   `partialColumnTupleCoeff coeff chosen (vectorLengthCast hk v)` as the
   raw fold over `Fin k` with the un-cast vector. Proof uses `subst hk`
   followed by `unfold`, `rcases v`, and `rfl`, exploiting structure-level
   proof irrelevance for `Vector.mk` to drop the cast cleanly.
2. `partialColumnTupleCoeff_insertAt_last` — the wrapper requested by
   #3074. Composes the cast lemma, then
   `partialColumnTupleCoeff_insertAt_last_fold`, then
   `Lean.Grind.CommSemiring.mul_comm`. Final `congr 1` closes because
   `(c :: chosen).length` reduces to `chosen.length + 1` definitionally,
   so the two `Fin n` indices match by defeq + proof irrelevance.

Verified:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `lake build` (full project)
- `python3 scripts/check_dag.py`
- `git diff --check`
- No new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry` in
  `HexMatrix/Determinant.lean`.

## Current frontier

`partialColumnTupleCoeff_insertAt_last` is now available for the suffix
determinant induction in #3033. The cast engineering is fully isolated;
the next slice (#3075) can fold the result into the determinant proof
without re-deriving the vectorLengthCast bridge.

## Next step

Pick up #3075 (suffix RHS fold alignment) or another HexMatrix slice
once #3074 lands.

## Blockers

None.
