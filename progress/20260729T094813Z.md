# Dynamic-scope generation order independence

## Accomplished

- Removed storage-order dependence from instantiation generation: a proposed
  node remains an output of its theorem instance when CSE reuses an older
  node, including when that output is an equality endpoint or scope port.
- Retained the emitting application's creation generation as an authoritative
  input, so scope-only instantiation chains cannot reset their logical depth.
- Replaced the former CSE-causality canaries with scope and equality canaries
  requiring generation-one results after reuse of generation-one storage.
- Clarified that application `policyItems` is a conservative charge over both
  ordered reads and writes.
- Rebuilt the propagator, scope, and propagator conformance targets.

## Current frontier

Dynamic arbitrary-scope admission is again independent of expression storage
order while preserving causal depth through the application which emitted the
proposal.

## Next step

Run the complete affected target set, push the review fix, refresh PR #9082,
and obtain confirmation that the independent review blocker is closed.

## Blockers

None.
