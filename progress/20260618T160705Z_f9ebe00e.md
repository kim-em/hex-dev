# Progress — 2026-06-18 (session f9ebe00e, /work → /feature #7947)

## Accomplished

Claimed #7947 and added a narrow wrapper in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`:
`factorFastCore_irreducible_of_monicRecoveredLift`.

The wrapper composes the existing recovered-lift endpoint
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift` with
the #7923 monic-lattice recovered-lift producer
`BHKS.ForwardRecoveryInputs.recoveredLift_subtypeFamily_of_indicatorCandidates`.
It keeps the deep BHKS residuals explicit (`hindicators`, monic-lattice
`hsize`, `hpartition`, `hsep`, `hthr`, and `hfac`) while deriving the prime and
modulus side conditions from `Hex.ZPoly.toMonicPrimeData?`.

Static checks completed:

- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line grep for `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`

## Current frontier

The wrapper is intentionally conditional and does not try to derive monic
`hsize` / `hindicators` from non-monic executable success. Those remain the
cap-separation residuals called out by #7947.

## Next step

Run the Lean target once the `HexBerlekampZassenhaus.CrossCheck` rebuild is not
pathologically slow in this worktree:

- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- then `lake build HexBerlekampZassenhausMathlib`

If the wrapper type needs adjustment, the likely area is definitional equality
between the named `lift` family and the producer after `subst lift`.

## Blockers

Lean verification could not reach the touched file. Repeated attempts to build
`HexBerlekampZassenhausMathlib.PartitionRefinement` and then the direct
prerequisite `HexBerlekampZassenhaus.CrossCheck` stayed CPU-bound compiling
`HexBerlekampZassenhaus/CrossCheck.lean` for more than ten minutes with no
diagnostics. The build runs were stopped to avoid leaving long-running
processes behind.

Pre-existing unrelated `.claude/CLAUDE.md` modification remains in the worktree;
it was not touched.
