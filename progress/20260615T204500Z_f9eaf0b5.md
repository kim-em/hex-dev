# Progress — 2026-06-15 (session f9eaf0b5, feature)

## Accomplished

Claimed #7360 (forward modP->lift transport for `toMonicLiftData`).
Traced the intended composition end to end and found the **literal
deliverable premise is unsound for non-monic `core`**. Posted a
detailed evidence-backed premise report and `coordination skip`ped the
issue for replan (per the "Directives are hypotheses, not specs"
doctrine). No code landed — this is a diagnosis turn.

## Key finding

`toMonicLiftData core B primeData = henselLiftData (toMonic core).monic
B' primeData` lifts `primeData.factorsModP` toward `M := (toMonic
core).monic`. The only forward transport
(`henselLiftData_represents_lifted_of_modP`, Mathlib `Basic.lean:9066`)
and the lift invariant (`QuadraticMultifactorLiftInvariant_of_choosePrimeData`,
`Basic.lean:2763`) both require `∏ factorsModP ≡ M (mod p)`.

With `primeData = choosePrimeData? core` (the issue's `hchoose`), the
factors are `core`'s mod-p factors, so `∏ ≡ core`. For non-monic core
`core ≢ M (mod p)` (they differ by the `dilate`-by-`leadingCoeff`
relation, `dilate_monic_toMonic`, `Basic.lean:6384`), so `hproduct_mod_p`
is false. The `choosePrimeData?` substrate constructor only works because
it additionally assumes `hcore_monic` and derives `toMonicPrimeData?`
from `(toMonic core).monic = core` (`Basic.lean:19808-19813`).

Corrected target: state against `toMonicPrimeData? core = some primeData`
(consumed by the `_of_toMonicPrimeData_` producers at `Basic.lean:19350`
/ `19540`).

## Current frontier

Even the corrected `toMonicPrimeData?` target needs two correspondence
lemmas that do not exist yet:
1. Integer monic-correspondent existence: from `factor ∣ core`
   irreducible, produce monic `factor' ∣ (toMonic core).monic` with
   `primitivePart (dilate (leadingCoeff core) factor') = factor`
   (irreducible). `dilate_recovery` (`HexPolyZMathlib/Basic.lean:295`)
   covers the divisibility/degree half but not existence of `factor'`.
2. Mod-p subset preservation across that bijection (M's mod-p factors are
   the `lc`-dilations of core's; `monicModPImage_scale`, `Basic.lean:1933`,
   is the likely lever).

Once both exist, the transport is a clean composition: instantiate the
monic lemma at `M`, reuse the `congr` field, transfer `monic_dvd` via
`(toMonic M).monic = M`, and convert `dilate_eq` via `dilate (C c · p) =
C c · dilate _ p` + `primitivePart` constant-absorption.

## Next step

Planner re-issues #7360 with the `toMonicPrimeData?` signature, plus two
prerequisite issues for the correspondence lemmas above.

## Blockers

None for the diagnosis. The corrected feature is blocked on the two
missing correspondence lemmas (named in the issue comment).
