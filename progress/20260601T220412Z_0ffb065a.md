# 0ffb065a - work #6015

## Accomplished

- Claimed and completed feature issue #6015.
- Added a private bridge-layer `cofactorSign_consecutive_last_neg` helper in
  `HexMatrixMathlib/Determinant/Core.lean`.
- Added private order-case transports from the ordered four-row `nDet` Plucker
  kernel to the basis-vector coefficient identity for:
  - `q < p1`
  - `p1 < q < p2`
  - `p2 < q < p3`
  - `p3 < q`
- Exposed `det_plucker_three_term_basisVec_of_ne`, the arbitrary-row
  basis-vector Plucker coefficient theorem for `q` distinct from `p1`, `p2`,
  and `p3`.
- Verified with `lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token grep over the Lean diff.

## Current frontier

#6015 is complete and ready for PR. The bridge layer now has the ordered
four-row raw `nDet` kernel and the arbitrary basis-row coefficient theorem
needed by downstream universal Plucker assembly.

## Next step

Create the PR for #6015. Downstream work can use
`det_plucker_three_term_basisVec_of_ne` together with the special basis-vector
cases when assembling the final fold/universal Plucker theorem.

## Blockers

None.
