# 20260516T061047Z — session 5202aaf7 (feature, partial)

## Accomplished

Claimed issue #4413 and landed prerequisite infrastructure for the
`QuadraticMultifactorLiftInvariant` boundary theorem. Commit `c8983e5`
adds `Hex.FpPoly.monic_liftToZ_of_monic` in `HexHensel/Basic.lean`:
for `1 < p`, the canonical integer lift of a monic `FpPoly p` is monic
as a `ZPoly`.

The proof also adds private local helpers for the nontrivial-modulus
`ZMod64.toNat` facts and the positive-size argument needed to compare
the lifted leading coefficient. No new `sorry`, `axiom`, `native_decide`,
`TODO`, or `FIXME` was introduced.

Verification run:

- `lake build HexHensel`
- `lake build HexBerlekampZassenhaus.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`

`HexBerlekampZassenhaus.Basic` still reports the pre-existing `sorry` at
`HexBerlekampZassenhaus/Basic.lean:5647`; this session did not touch it.

## Current frontier

Issue #4413 remains open beyond this prerequisite helper. The main
missing piece is still the companion theorem in
`HexHensel/QuadraticMultifactor.lean`, especially pinning and proving the
side condition that turns pairwise modular coprimality into the concrete
`normalizedXGCD` Bezout identity required by `QuadraticLiftLoopInvariant`
at each recursive split.

The `choosePrimeData` wrapper in `HexBerlekampZassenhaus/Basic.lean`
also remains unimplemented and should wait for the companion theorem
surface to settle.

## Next step

Continue #4413 by adding the exact coprimality/Bezout bridge for
`normalizedXGCD` over `FpPoly p`, then use it in the recursive
`quadraticMultifactorLiftInvariant_of_factorsModP` proof. Once that
surface is stable, specialise it to `PrimeChoiceData`.

## Blockers

No environmental blockers. The mathematical/API blocker is the missing
formal connection between the issue's intended pairwise coprimality
hypothesis and the executable `normalizedXGCD` witness package.
