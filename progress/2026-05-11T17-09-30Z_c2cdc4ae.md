## Accomplished

- Claimed #3284 first, then skipped it as stale: #3447 was decomposed into #3503, and #3503 is blocked on open PR #3504, so the helper API #3284 needs is not on current `main`.
- Claimed #3507 and updated `HexBerlekampZassenhaus/Basic.lean` so `factorFastCoreWithBound` branches on `bhksRecoverClassified` directly.
- Added classifier predicates and guards distinguishing reconstruction failures from degenerate lattice failures.
- Updated the fast-loop schedule and product-preservation proofs to reason over classified BHKS recovery results.

## Current frontier

- #3507 is implemented and verified locally.
- The unrelated `.claude/CLAUDE.md` worktree modification was present before this task and remains untouched.

## Next step

- Commit `HexBerlekampZassenhaus/Basic.lean` and this progress file, then create the PR for #3507.

## Blockers

- None for #3507.
