# Scaled primitive recursive coverage assembly (#4816)

## Accomplished

In `HexBerlekampZassenhausMathlib/Basic.lean`, added the scaled recursive
coverage assembly requested by #4816:

* private
  `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`,
  mirroring the existing monic
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  but routing through `Hex.scaledRecombinationSearchModAux`.
* public
  `scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`,
  exposing the fixed-factor primitive + positive-leading wrapper for downstream
  #4648 callers.

The scaled induction uses the landed scaled substrate:

* `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core_scaled`
  for prefix-none.
* `scaledRecombinationCandidate_eq_factor_of_recovery` for candidate recovery.
* `shouldRecord_scaledRecombinationCandidate_of_eq_factor` for record checks.
* `exactQuotient?_scaledRecombinationCandidate_eq_some_of_eq_factor_of_primitive_pos_lc`
  for non-monic exact quotient.
* #4813 primitive / positive-leading target helpers to preserve the recursive
  target invariant across quotient descent.

The old monic recursive theorem was left intact as the compatibility wrapper.

## Current frontier

#4816 is implemented. The downstream #4648 wrapper exposure can now call the
scaled recursive theorem directly instead of reviving the false unscaled
candidate route for primitive non-monic cores.

## Next step

Work #4648: expose the primitive recombination wrapper by wiring the scaled
recursive coverage theorem through the public executable boundary expected by
the remaining HO-1 capstone.

## Blockers

None.
