# Planner session d54062a9

## Accomplished

Filed two new planning issues for the
`HexBerlekampZassenhausMathlib/Basic.lean` FpPoly-divisibility
cluster:

- **#5081 (feature, immediately actionable):** Extract
  `private theorem fpPoly_isZero_false_of_dvd_of_isZero_false`
  in `HexBerlekampZassenhausMathlib/Basic.lean` and rewire two
  inline `cases ... isZero` + `apply ext_coeff` + `Hex.FpPoly.zero_mul`
  contradiction blocks at `Basic.lean:1318` (inside
  `monicModPImage_dvd_monicModularImage_of_dvd_of_choosePrimeData?_some`,
  line 1287) and `Basic.lean:15442` onto the helper. Discovered the
  parallel site at 15442 alongside the residual rewire flagged in
  #5072's progress note. Each rewire collapses ≈ 20 lines to 2 or
  4 lines, sharing the same `(a ∣ b) ∧ (b.isZero = false) →
  (a.isZero = false)` lemma. Independent of #5077 (which extracts
  `fpPoly_dvd_trans` for the Shape-A transitivity sites at 4985 /
  5722 / 5790); shares only the FpPoly-divisibility framing
  convention.

- **#5082 (review, blocked on #5077):** Post-#5077 audit of the
  `fpPoly_dvd_trans` cluster mirroring the #5069 audit of the
  `zpoly_dvd_trans` cluster. Audits three invariants — call-site
  coverage (rcases-form 4985/5722/5790 + calc-form 1347/1383),
  framing/docstring/hoist correctness (private theorem +
  `instDvdOfAddOfMul` semantic note + minimal hoist position),
  and cross-file consistency (1 def in `Basic.lean`, 0 in
  `IntReductionMod.lean` / `Recovery.lean`). Accounts for both
  possible #5077 worker outcomes: 1 def + 5 uses if calc-form
  1347/1383 candidates were rewired, or 1 def + 3 uses + deviation
  report listing the calc-form sites if they were documented and
  skipped.

Closed errant issue #5080 (created accidentally when invoking
`coordination plan --help` instead of `coordination help`; the
`--help` flag is not recognised by the plan subcommand and was
parsed as a title).

## Current frontier

After this cycle, the FpPoly-divisibility cluster has two issues
in flight at the planning layer:

- #5077 (claimed): `fpPoly_dvd_trans` helper + Shape-A
  transitivity rewires.
- #5081 (unclaimed): `fpPoly_isZero_false_of_dvd_of_isZero_false`
  helper + Shape-B isZero=false rewires.

The two helpers cover distinct shapes (transitivity vs
dvd-preserves-`isZero=false`) and place at different hoist
positions in `Basic.lean` (#5077 anchors around line 4985,
#5081 anchors around line 1287). Workers can pick them up in
either order without conflict.

#5082 will unblock when #5077's PR merges; the audit comment then
fires against the merged state of `fpPoly_dvd_trans`.

Queue state after planning: `unclaimed feature = 1` (#5081),
`unclaimed review = 0` (#5082 is blocked on #5077). Deficit
moves from 2 → 1; the next planner cycle picks up the residual.

## Next step

A next planner cycle should:

- Watch for #5077's merge and #5082 unblocking. After the audit
  fires, lift any deviation report onto a follow-up feature
  issue (especially if calc-form 1347/1383 were documented but
  not rewired).
- If queue-deficit re-emerges after #5081 and #5077 land,
  candidate next rewires include the remaining
  `apply Hex.DensePoly.ext_coeff` boilerplate sites in
  `Basic.lean` (currently 17 occurrences) and
  `IntReductionMod.lean` (currently 8 occurrences) — but most
  of these have idiosyncratic surrounding context and don't
  share a unifying helper signature, so per-site evaluation is
  needed.
- The `Hex.FpPoly.zero_mul` contrapositive at `Basic.lean:5712-5717`
  (`hrawGcd_ne : rawGcd ≠ 0` derivation inside
  `quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared`)
  is a different shape (`rawGcd * rawGcd ∣ X` + `X ≠ 0` →
  `rawGcd ≠ 0` via `mul_ne_zero` contrapositive) — not covered
  by the helper #5081 will extract; document if it stays inline
  after the current cluster work lands.

## Blockers

None. The repair workflow is already on #4917 (`repair-claimed`
label); planner does not touch it.
