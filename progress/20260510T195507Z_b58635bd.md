## Accomplished

Claimed #3099 and added the reachable-state Yun helper
`yunFactors_factors_coprime_repeated_of_reachable` in
`HexPolyFp/SquareFree.lean`.

The helper proves that every factor emitted by a reachable `yunFactors` loop is
coprime, in normalized-gcd form, with the loop's final repeated component.  It
packages the PR #3096 current-vs-repeated helper through the full Yun loop using
`yunFactorsPairwiseReachable_step`, `yunFactors_reverse_append`, and
`yunFactors_repeated_eq_nil`.

Verification run:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`

## Current frontier

This is partial progress on #3099.  The remaining proof obligation is the
pth-root tail divisibility bridge: factors emitted by
`squareFreeAuxRev (pthRoot loop.2) ... []` must be related back to `loop.2`
under the reachable/fuel assumptions, then combined with the new
current-loop-vs-repeated coprimality helper to prove the narrowed tail-cross
theorem.

The file still has the pre-existing proof holes in the product obligation and
tail-cross theorem cluster.

## Next step

Prove the focused divisibility bridge for recursive pth-root tail factors and
use it to replace the remaining `yunFactors_squareFreeAuxRev_tail_cross_coprime`
stub with a reachable-state theorem consumed by #2917.

## Blockers

None.
