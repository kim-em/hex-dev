# 20260602T195841Z work #2567

## Accomplished

- Claimed directive #2567 and treated it as an incremental D1/HO-4 session.
- Added
  `BHKS.factorFast_ne_none_of_canonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`
  in `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The new wrapper composes the packaged `CanonicalRecoveryInputs` recovery
  theorem while deriving cap positivity from `one_le_factorFastPrecisionCap`
  and the prime lower bound from `Hex.choosePrimeData?_prime`.

## Current frontier

The canonical recovery surface now needs only the prime-choice equation, the
executable precision equation, and the mathematical `CanonicalRecoveryInputs`
package.  The public `factorFast_terminates` theorem is still open; the
remaining work is to construct the analytic/canonical recovery inputs
unconditionally for the fast-path cap.

## Next step

Continue with the focused D1 prerequisites already queued: prove the remaining
bad-vector bridge data field lemmas and the strict cap-lift Hadamard/l2norm
inequality, then use the canonical recovery wrapper in the final assembly.

## Blockers

No blocker for this wrapper.  Full D1 remains a broad theorem requiring the
remaining analytic and recovery-input producers.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
