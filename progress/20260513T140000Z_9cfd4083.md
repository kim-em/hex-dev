# #3598: HexBZ core — review irreducibility certificate API

Session: `9cfd4083`
Branch: `agent/9cfd4083`

## Accomplished

- Reviewed the executable irreducibility-certificate surface in
  `HexBerlekampZassenhaus/Basic.lean`: `PrimeFactorData`,
  `ZPolyIrreducibilityCertificate`, `checkIrreducibleCert`, and the
  certificate-success theorem helpers.
- Added module-doc-style docstrings to the five top-level theorems that
  were undocumented (`checkIrreducibleCert_prime_data`,
  `checkIrreducibleCert_degree_obstructions`,
  `checkIrreducibleCert_obstructs_candidate_degrees`,
  `degreeObstruction_no_subset_degree`, plus a forward-reference note
  on the umbrella `checkIrreducibleCert_prime_data` pointing consumers
  to the new conjunct extractors).
- Added four small wrapper lemmas so consumers can extract individual
  conjuncts of `PrimeFactorData.checkForPolynomial` without unfolding
  the bool checker:
  - `checkIrreducibleCert_isGoodPrime` — admissible prime per block.
  - `checkIrreducibleCert_factorDegrees_positive` — every recorded
    factor degree is positive (forall-over-index form, since `Array.all`
    normalizes to that shape under `simp`).
  - `checkIrreducibleCert_degreeSum_eq` — recorded `degreeSum` matches
    the modular image's degree.
  - `checkIrreducibleCert_factorProduct_eq` — recorded `factorProduct`
    matches the modular image polynomial.
- Verified `lake build HexBerlekampZassenhaus.Basic`, `.Conformance`,
  and the umbrella, plus `python3 scripts/check_dag.py` and
  `python3 scripts/status.py HexBerlekampZassenhaus`.
- `git diff --check` passes; no new banned markers (the two pre-existing
  `sorry` warnings on `isIrreducible_iff` and `factor_product_of_bound`
  are out of scope per the issue body).

## Current frontier

The certificate-success surface now exposes each `checkForPolynomial`
conjunct individually, plus the existing degree-obstruction wrappers.
Downstream consumers building the `Hex.ZPoly.Irreducible` proof (the
deeper iff theorem from #3304) can quote these without re-deriving the
bool decomposition.

## Next step

Ship the PR closing #3598. Sibling work `#3590` (`Factorization`
entrypoint review) has already landed; the remaining adjacent reviews
are #3592 (Bench API) and #3560 (Mathlib cap-separation surface).

## Blockers

None for this issue. The previously-claimed #3308 was skipped with a
detailed analysis of why direct structural induction on
`yunFactorsPairwiseReachable` cannot close the step case (recorded on
the issue) — that one needs replan for a strengthened invariant.
