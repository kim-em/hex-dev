# Progress - 2026-06-17 (session f08b55e7, feature #7674)

## Accomplished

Claimed #7674 (feat: add semantic lift facts for TrueFactorLift CLD
aggregation) — head of the fast-BHKS critical path
`#7674 → #7651 → #7675 → #6672 → #2564`. **Landed deliverables 2 and 3 (the
product-side CLD aggregation, column-wise and #7651-consumable). Deliverable 1
(derive `TrueFactorLiftSemantics` from concrete lift evidence) remains** and is
routed to replan.

### Product log-derivative congruence over a factor list (landed)

`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`: added
`congr_polyProduct_mul_listSum_derivative` — for any list `gs` whose every
factor `g` satisfies the per-factor CLD congruence
`g * q g ≡ f * g' (mod m)`, the product times the sum of `q`-images is
congruent to `f` times the derivative of the product:
`polyProduct gs * Σ (q g) ≡ f * (polyProduct gs)' (mod m)`. Proved by
structural induction on the list using the Leibniz product rule
(`DensePoly.derivative_mul`) and the executable congruence arithmetic
(`congr_add`/`congr_mul`/`congr_refl`). No leave-one-out indexing — the
cons step factors the head out and recurses, which keeps the proof short.

### Support-set instantiation (landed)

- `HexBerlekampZassenhausMathlib/Lattice.lean`: added `supportCldSum`, the sum
  of per-selected-factor executable CLD quotients in `supportProduct` order
  (defined with the same `classical`/`decide (i ∈ S)` pattern as
  `supportProduct`, so the two unfold compatibly).
- `CLDColumnBound.lean`:
  `TrueFactorLiftSemantics.supportProduct_cldSum_congr` proves the product
  logarithmic-derivative sum identity over the selected support:
  `supportProduct L S * supportCldSum L S f p a ≡ f * (supportProduct L S)'
  (mod p^a)`. It threads the landed per-factor
  `selected_cldQuotientMod_congr_mul_derivative` through the generic list
  lemma; the membership bridge converts `g ∈ filtered-mapped list` to
  `∃ i ∈ S, g = liftedFactors.getD i 1` (via `List.mem_map`/`List.mem_filter`),
  and `TrueFactorLift.liftedFactors_eq` aligns `L.liftedFactors` with
  `D.liftedFactors`. The final `exact` closes by defeq (the `classical`
  instances match between the proof's filtered list and
  `supportProduct`/`supportCldSum`).

Deliverable 3 (column-wise, consumable by #7651) is satisfied by construction:
`(supportCldSum L S f p a).coeff j` is the pre-`psiCut`, pre-indicator column-`j`
entry of the true-factor CLD vector. The statement deliberately stops before
`psiCut`/indicator weighting, which is #7651's layer.

Verification:
- `lake build HexBerlekampZassenhausMathlib.Lattice` — clean.
- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound` — clean.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line forbidden-token scan over touched Lean files — none.

## Current frontier

The product side of the CLD aggregation is closed. #7651 can now bound the
true-factor CLD column by relating `supportCldSum`'s coefficients to a Landau /
Mignotte bound on the genuine product factor `supportProduct L S`.

## Next step (remaining deliverable 1, for the replan)

Derive `TrueFactorLiftSemantics` from concrete lift/recovery evidence
(`RepresentsIntegerFactorAtLift` / Hensel `LiftData`) instead of taking the
carrier as an arbitrary hypothesis bundle. This is a separable bridge between
the `TrueFactorLift`/`bhksLatticeBasis` world (Lattice.lean) and the
`LiftData`/`LiftedFactorSubset` recovery world (Basic.lean/Recovery.lean/
IntReductionMod.lean): construct a `TrueFactorLift` + `TrueFactorLiftSemantics`
from Hensel multifactor-lift output, proving per-selected-factor monicity,
positive degree, and the modular cofactor witness from the lift invariant.
Larger than one corner of this session; landed as a partial PR so it gets its
own issue.

## Blockers

None for the landed change.
Note: the worktree carries pre-existing unrelated `.claude/*` modifications
from before this session; not touched or staged.
