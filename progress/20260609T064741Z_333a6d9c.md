# 20260609T064741Z (#6636)

## Accomplished

- Added `mem_hotPathCandidates_prime` and `exists_mem_hotPathCandidates_of_prime`
  to `HexBerlekampZassenhaus/Basic.lean`, the two enumeration-completeness
  facts SPEC D2's `choosePrimeData?_none_implies_huge` (#6509) needs.
- Added a private helper `isPrimeTrial_of_prime` (converse to
  `Hex.Nat.isPrimeTrial_isPrime`) to bridge between propositional
  `Hex.Nat.Prime` and the kernel-decidable `Hex.Nat.isPrimeTrial = true`
  surface; this lets the coverage proof discharge a bounded universal
  `∀ q : Fin 501, ...` by `decide`.
- Coverage `decide` needs `set_option maxRecDepth 4096`; soundness
  `decide` (94-element list bounds check) runs at default depth.

## Current frontier

The two lemmas are public theorems in `Hex` namespace referencing the
file-private `SmallPrimeCandidate` and `hotPathCandidates`. Downstream
consumer #6509 (`choosePrimeData?_none_implies_huge`) must therefore
live in `HexBerlekampZassenhaus/Basic.lean` or a sibling that opens the
private surface; that placement decision is #6509's call, not this
issue's.

## Next step

Companion sub-issue #6637 (`isGoodPrime failure ⟹ p divides
leadingCoeff · resultant`) is unclaimed and pairs with this one to
unblock #6509.

## Blockers

None.

Verification performed:

- `lake build HexBerlekampZassenhaus.Basic`
- `lake build` (full project, green)
- `python3 scripts/check_dag.py` (clean)
- `git diff --check` (clean)
- Touched-file grep for `sorry`, `axiom`, `native_decide`, `TODO`,
  `FIXME` (none).
