# HexBerlekamp Rabin Bezout bridge

## Accomplished

- Claimed #2154 after #2156 was lost to a claim race.
- Added private Rabin gcd/unit bridge lemmas in `HexBerlekamp/Irreducibility.lean`, including an explicit `[ZMod64.PrimeModulus p]` surface for the field-dependent step.
- Added `checkRabinBezoutWitness_rabinCoprimeTest`, connecting one checked Bezout witness plus its pow-chain equality to the executable `rabinCoprimeTest`.
- Verified `lake build HexBerlekamp.Irreducibility`, `lake build HexBerlekamp`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/check_dag.py`, `git diff --check`, and a diff-only forbidden-token scan.

## Current frontier

#2154 is complete for the local witness-to-Rabin-coprime bridge. The full `checkIrreducibilityCertificate_rabinTest` theorem remains out of scope and still has its pre-existing placeholder.

## Next step

Use `checkPowChain_spec` together with `checkRabinBezoutWitness_rabinCoprimeTest` to discharge the certificate/list unpacking in the dependent full Rabin checker theorem.

## Blockers

- None for this bridge.
- The build still reports pre-existing `sorry` warnings in upstream modules and the out-of-scope HexBerlekamp certificate predicates.
