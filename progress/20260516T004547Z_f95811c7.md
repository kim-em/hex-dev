# 2026-05-16 00:45 UTC - #4350 PR #4349 repair

## Accomplished

Claimed issue #4350 and repair-claimed PR #4349. Diagnosed the Ubuntu
`Bench verify` failure as budget time being spent through `lake exe` after the
workflow had already built the bench executables. The failed run's largest
contributor was `hexgf2_bench` at 163s; locally, after rebuilding the current
`hexgf2_bench`, the patched CI-direct path verified all 147 registrations in
73s.

Updated `scripts/ci/check_bench_verify_budget.sh` so GitHub Actions invokes
the already-built `.lake/build/bin/<bench>` executable directly for `list` and
`verify`. Local runs keep the previous `lake exe` behavior to avoid stale
binary hazards.

Verification:

- `python3 scripts/check_dag.py`
- `bash -n scripts/ci/check_bench_verify_budget.sh`
- `git diff --check`
- `GITHUB_ACTIONS=true BENCH_VERIFY_HARD_CAP_SECONDS=300 bash scripts/ci/check_bench_verify_budget.sh hexarith_bench`
- `GITHUB_ACTIONS=true BENCH_VERIFY_HARD_CAP_SECONDS=300 bash scripts/ci/check_bench_verify_budget.sh hexgf2_bench`

## Current frontier

PR #4349 has a focused CI-budget repair on top of its existing feature commit.
The repair does not change Lean code or benchmark scientific settings.

## Next step

Push the repaired PR branch and mark PR #4349 salvaged so CI can rerun on the
new head.

## Blockers

None. The worktree still has a pre-existing `.claude/CLAUDE.md` modification
outside this repair; it was intentionally left untouched and uncommitted.
