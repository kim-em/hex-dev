# Progress - 2026-06-17 (session a79406c1)

## Accomplished

Claimed #7736 after the directive #2564 was blocked by dependencies.

Added `factorWithBound_fastCore_entry_irreducible_of_forwardInputs` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`.  The theorem wires the
existing scheduled forward-input fast-core irreducibility consumer through the
public fast-core branch-shape lemma and reassembly irreducibility lift, proving
recorded `factorWithBound` entries irreducible for the BHKS fast-core success
arm when the caller supplies:

- the executable fast-core branch dispatch hypotheses,
- `ForwardRecoveryInputs` at `precisionForCoeffBound B primeData.p`,
- the matching core-success equality,
- the forward cut and partition-count certificates,
- the reassembly-completeness certificate for the successful core array.

Verification:

- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token scan over
  `HexBerlekampZassenhausMathlib/PartitionRefinement.lean`

## Current frontier

This is a partial #7736 bridge.  It proves the public recorded-entry fast-core
irreducibility shape, but it does not yet expose the full raw `h_raw` fast
disjunct consumed by `factor_entries_irreducible`.

The key remaining gap is still packaging the actual first-success executable
state into the expected raw array shape without relying on the refuted
no-early-success/cap-determinism shortcut.  The executable reassembly array is
private, so successor work should continue through existing public branch-shape
lemmas or add an explicit public raw-shape API in the executable layer.

## Next step

Add a public fast raw-disjunct theorem that composes this entry-level fast-core
result with the existing slow raw dischargers, or first expose the missing
public raw-array API needed to state the fast branch directly without naming the
private reassembly definition.

## Blockers

No local build blockers.  Full completion of #7736 still needs the public
first-success/raw-disjunct packaging described above.
