# 2026-05-07 22:15 UTC - BHKS equivalence-class extraction (0e037678)

## Accomplished

Claimed issue #2614 and added the BHKS Lemma 3.3 equivalence-class slice in
`HexBerlekampZassenhaus/Basic.lean`:

- `bhksProjectedRowsAsRatMatrix` lifts `BhksProjectedRows.projectedRows` to a
  `Matrix Rat n r` consumable by `Matrix.rref`.
- `bhksEquivalenceClassIndicators` runs `Matrix.rref` over `Q`, groups column
  indices by their echelon-column signature, and returns one compact `0/1`
  indicator vector per equivalence class.
- Private supporting helpers `bhksColumnSignature`,
  `bhksInsertSignatureClass`, and `bhksClassIndicator`.
- `#guard` fixtures for the three partition cases: two-class
  (`#[#[1,1,0,0]]` projection), singleton (empty `projectedRows` →
  all-`1` indicator), and no-progress (identity rows → unit-vector
  indicators).

Verification:

- `lake build HexBerlekampZassenhaus`
- `lake build HexMatrix`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexBerlekampZassenhaus/Basic.lean`

## Current frontier

The BHKS recovery pipeline now goes LLL → cut → project → RREF +
equivalence-class identification → indicator vectors. Indicator candidates
still need to be wired into `factorFast` together with the per-precision
loop and exact-division verification.

## Next step

Wire `bhksEquivalenceClassIndicators` together with
`bhksIndicatorCandidate?` into a recombination helper, and add the
precision-loop / failure-mode discrimination that decides between
re-lifting and re-running LLL (BHKS recovery modes (a) and (b)).

## Blockers

None.
