# Full FKS2 local profile

## Accomplished

- Moved the thirteen generated complete-family Mathlib proof wrappers and
  aggregate from `HexIntervalMathlibExperiment` into the dedicated non-default
  `HexIntervalPntFks2Local` library.
- Added executable fail-closed runner
  `scripts/conformance/run_pnt_fks2_family.sh`; it resolves the repository root
  and invokes the non-default executable containing all runtime, replay,
  mutation, and guarded axiom checks.
- Kept the source-pinned Mathlib-free data/checker in the merge-gating
  experiment and preserved the merged shard-11 fixture, all mathematics, and
  every inventory classification unchanged.
- Registered the build-only library with the DAG/umbrella checks and updated
  both SPECs and the exact Lake freshness exemption to describe the boundary.
- Passed the runner repeatedly from the repository and an external working
  directory, the retained PNT build, explicit merge-gating experiment and
  conformance builds, the full default build, pinned-source and inventory
  checks, and static/trust gates using the retained cache.

## Current frontier

The default Mathlib experiment and conformance graphs exclude the expensive
complete-family Mathlib proof modules. The explicit local runner remains the
ordinary-kernel release gate for all 13,590 source cells.

## Next step

Publish the repaired exact PR head and bind the automatic CI run to it for the
final independent review.

## Blockers

None.
