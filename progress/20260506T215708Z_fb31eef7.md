# 2026-05-06 21:57 UTC — work session (fb31eef7)

## Accomplished

Claimed #2516, confirmed it was too broad for one session, and decomposed it into:

- #2524 for private list-level `basisRows` row-add invariants.
- #2525 for public rational row-add basis and coefficient formulas.
- #2526 for integer row-add wrappers usable from `HexGramSchmidt/Update.lean`.

Released #2516 via `coordination skip` with the required decomposition breadcrumb, then claimed #2524.

Made partial implementation progress in `HexGramSchmidt/Basic.lean` by proving two private projection-coefficient algebra lemmas:

- `projectionCoeff_sub_smul_self`
- `projectionCoeff_sub_smul`

These discharge the scalar projection algebra needed by the later pivot and lower coefficient update proofs once the `basisRows` row-add invariant is available.

Verification run:

- `lake build HexGramSchmidt.Basic`
- `lake build HexGramSchmidt.Update`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom |native_decide|TODO|FIXME|sorry' HexGramSchmidt/Basic.lean`

`HexGramSchmidt/Basic.lean` still has its two pre-existing `basis_span` sorries, now at lines 806 and 858 after this insertion. No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` was introduced.

## Current frontier

#2524 still needs the main list-recursion proof: show that adding a scalar multiple of an earlier input row to row `k` leaves the generated `basisRows` output unchanged. The new projection lemmas cover the coefficient algebra but not the span/reduction invariant.

## Next step

Continue #2524 by proving that an earlier input row reduces to zero against the prefix basis already generated before `k`, using `basisRows_get!_eq_reduceAgainstBasis_forward`, `basisMatrix_reconstruction_invariant`, and `projectionCombination`.

## Blockers

No technical blocker. The remaining proof is larger than the parent issue suggested and should proceed through the private list-level invariant before exposing public `Rat`/`Int` theorem surfaces.
