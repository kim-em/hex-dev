# Progress - 2026-06-16T13:37:36Z (session cdc115b0, work #7590)

## Accomplished

Claimed #7590 after the higher-priority directive #2564 was rejected by
coordination as blocked. Audited the executable BHKS cut path in
`HexBerlekampZassenhaus/Basic.lean`, the proof-facing
`CutProjectionHypotheses` / `CutRetention` surfaces in
`HexBerlekampZassenhausMathlib/Lattice.lean`, and the forward-count consumers in
`PartitionRefinement.lean` / `Recovery.lean`.

Chose the sound route for the BHKS cut semantics: keep the current per-row
executable Gram-Schmidt cut in `Hex.bhksProjectedRows`, and make the needed
prefix behavior explicit as a proof-facing `CutSeparation` / van-Hoeij gap
certificate. A first-fail prefix cut would still be false on the known
LLL-reduced witness, and sorting rows by norm would require a new semantic proof
for Gram-Schmidt coordinates.

Created critical-path successor feature issues:

- #7591: expose BHKS projected-row trace and LLL span bridge.
- #7592: add BHKS cut-separation prefix certificate.
- #7593: place true-factor CLD vectors in the BHKS lattice.
- #7594: produce `CutProjectionHypotheses` from separation and true-factor
  vectors.

Recorded the decision and successor decomposition in
`reports/hbz-bhks-d1-frontier.md`.

Verification:

- `git diff --check`
- `python3 scripts/check_dag.py`
- Added-line forbidden-token scan on `reports/hbz-bhks-d1-frontier.md`

## Current frontier

The false #7585 proof shape is no longer queued as an unconditional proof task.
The next executable worker target is #7591, which opens the trace/span surface
needed before the cut-separation theorem can be stated against real runtime
data.

## Next step

Claim #7591 and expose the public trace for `bhksProjectedRows` together with
the `lll.shortVectorsUnchecked` / `.toArray` lattice-span bridge. Once that
lands, #7592 can state the gap-conditioned prefix theorem over actual reduced
rows and retained indices.

## Blockers

No blocker for #7591. #7592, #7593, and #7594 are intentionally blocked by the
dependency chain created during this session.
