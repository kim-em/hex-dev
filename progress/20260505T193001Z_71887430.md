# 2026-05-05 19:30 UTC — work session (71887430)

## Accomplished

Claimed #2206 and proved the private
`rat_quotient_derivative_squareFree` theorem in `HexPolyZ/Basic.lean`.
The proof uses the existing quotient common-divisor power-divisibility
infrastructure, a local size-growth argument for nonconstant rational
polynomial powers, and a divisor-size bound for nonzero targets.

Added two small public zero-case facts in `HexPoly/Euclid.lean` so
`HexPolyZ` can avoid unfolding private division/gcd internals:
`divMod_eq_zero_self_of_size_zero_core` and `gcd_zero_zero`.

Verification run:

* `lake build HexPolyZ.Basic` (only the pre-existing downstream
  `primitiveSquareFreeDecomposition_squareFreeCore` sorry warning).
* `lake build HexPolyZ` (same pre-existing Basic warning, plus existing
  `HexPolyZ/Mignotte.lean` sorry warning).
* `python3 scripts/status.py HexPolyZ`.
* `python3 scripts/check_dag.py`.
* `git diff --check`.
* `rg -n "axiom|native_decide|TODO|FIXME" HexPoly/Euclid.lean HexPolyZ/Basic.lean`
  returns no hits.

## Current frontier

The requested private square-free theorem is now available. The derivative
branch of `primitiveSquareFreeDecomposition_squareFreeCore` still carries
its pre-existing `sorry`; this issue explicitly scoped that branch out.

## Next step

Use `rat_quotient_derivative_squareFree` in the follow-up that finishes the
derivative-active branch of `primitiveSquareFreeDecomposition_squareFreeCore`.

## Blockers

None for this session.
