# HexPolyZ Rat derivative product rule

## Accomplished

- Claimed feature issue #2137.
- Added private `rat_derivative_mul` in `HexPolyZ/Basic.lean`.
- Proved it by coefficient extensionality using `rat_coeff_derivative`, `DensePoly.coeff_mul`, `DensePoly.coeff_add`, and `DensePoly.rat_mulCoeffSum_derivative_product_rule`.
- Verified `lake build HexPolyZ.Basic`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

`HexPolyZ/Basic.lean` now has the local Rat derivative product rule needed by quotient-by-gcd square-free work.

## Next step

Use `rat_derivative_mul` in the blocked square-free core/quotient-by-gcd proof chain.

## Blockers

- `HexPolyZ/Basic.lean` still has the pre-existing downstream square-free-core `sorry`, outside #2137 scope.
