**Accomplished**

- Claimed #1978 (CI: derive conformance matrix from root-imports).
- Added `scripts/conformance_targets.py`. Walks the repo for
  `Hex*/Conformance.lean`, cross-checks each against
  `import <lib>.Conformance` in the matching root file, and prints the
  intersection on stdout (or as JSON with `--json`). `--check` exits
  non-zero on drift. Hard error: orphan `Conformance.lean`. Warning
  (suppressible with `--no-warn-orphan-imports`): root import without
  a backing file (lake build catches that anyway).
- Refactored `.github/workflows/conformance.yml` into a `setup` job
  that runs `--check` and emits the matrix as `targets`, consumed by
  the existing `conformance` job via
  `matrix.target: ${{ fromJSON(needs.setup.outputs.targets) }}`. No
  more hand-listed targets.

**Current frontier**

- 21 libraries currently feed the matrix (all that have both a
  `Conformance.lean` and the matching root import). Up from 8.

**Next step**

- Publish the PR closing #1978.

**Blockers**

- None.
