# Accomplished

- Strengthened `StableStep.holdsOld` from agreement at the fact's node to
  `Semantics.AgreeOn` across the complete old program.
- Updated entailment lifting and the arbitrary-function semantics adapter for
  the stronger premise.
- Added a regression semantics whose fact at one node reads another old node,
  exercising the whole-prefix agreement requirement.
- Corrected the interval SPEC to state that equality transport is not yet a
  cursor transition and to list exact initial-fact binding, equality lookup
  and chronology, complete event interleaving, and final target closure as
  open work.
- Rebuilt `HexInterval.ChronologicalReplayConformance` and ran the structural,
  source, DAG, diff, banned-token, and factor-data freshness checks.

# Current frontier

The exact review blockers on PR #9097 are repaired without extending the
chronological experiment's claimed completion boundary.

# Next step

Push the repair, obtain a fresh exact-head review, and merge only after exact
CI and review approval.

# Blockers

None.
