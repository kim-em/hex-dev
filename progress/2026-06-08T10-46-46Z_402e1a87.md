# lllLoop fuel sufficiency (issue #6567)

## Accomplished

* `sizeReduceColumn_valid` + `sizeReduce_valid` in
  `HexLLLMathlib/Independent.lean` — fills in the missing prerequisite
  for propagating the `LLLState.Valid` certificate through the inner
  size-reduction loop.  Mirrors the existing `swapStep_valid`.
* `lllLoop_fuel_sufficient` in `HexLLLMathlib/Independent.lean` —
  closes the SPEC §8 fuel obligation: for `Valid` + independent state
  `s`, the bound `lllFuel s = (s.potential + 1) * (n + 1)` suffices,
  so the `fuel = 0` fallback is unreachable on the public pipeline
  path.  Inductive lemma: each iteration strictly decreases the lex
  measure `s.potential * (n + 1) + (n - k)` (advance: drops `n - k`
  by 1; swap: drops `potential` strictly via
  `swapStep_potential_lt`).
* Unprivated two `HexLLL/Basic.lean` helpers
  (`nearestQuotient`, `foldl_finRange_set_outerSubMul_get_eq`) so the
  Mathlib-side proof can reference them.

## Current frontier

PR open against #6567.  `lake build HexLLLMathlib` and full `lake
build` succeed.  No new sorry / axiom / native_decide.

## Next step

`lll_short_vector` capstone (#6576 or follow-on) can now compose the
`δ`-reducedness certificate of `lll`'s output with the existing
`lll_short_vector` short-vector inequality, using
`lllLoop_fuel_sufficient` to discharge the fallback unreachability.

## Blockers

None.
