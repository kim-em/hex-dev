# Progress - 2026-06-15T04:53:10Z (session 76497a67, feature)

## Accomplished

Issue #7458: retired proof-facing uses of the total `choosePrimeData`
wrapper from the exhaustive slow branch membership lemmas in
`HexBerlekampZassenhaus/Basic.lean`.

- `factorWithBound_entry_mem_exhaustive_branch_raw` now takes explicit
  `primeData` plus `hchoose : choosePrimeData? ... = some primeData`, and
  concludes membership in the explicit reassembled exhaustive raw array.
- `exhaustiveSlowRawFactorsWithBound_mem_normalization_or_core` now routes
  through `exhaustiveSlowRawFactorsWithBound? = some rawFactors` plus the
  explicit `hchoose` witness.
- The reassembly-complete exhaustive branch lemma forwards the explicit
  witness instead of rewriting through `choosePrimeData`.
- Executable definitions were left unchanged; the total
  `exhaustiveSlowRawFactorsWithBound` compatibility wrapper remains.

Verification:

- `lake build HexBerlekampZassenhaus`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `rg -n "choosePrimeData \\(" HexBerlekampZassenhaus/Basic.lean` now reports
  only the `choosePrimeData` definition and the compatibility wrapper body.
- Added-line forbidden-token grep over `HexBerlekampZassenhaus/Basic.lean`
  found no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Second-opinion review found a potential stale-reference risk from renaming
  the membership theorem; the final patch preserves the original theorem name
  while changing its proof-facing signature.

## Current frontier

The local exhaustive slow proof-facing membership cluster is updated. Broader
fallback-discipline cleanup for directive #2564 remains in the queue; the
public total raw-array wrapper still exists intentionally for compatibility.

## Next step

Continue #2564 support by retiring remaining proof-facing fallback wrappers in
subsequent BZ branch lemmas, or by working the next monic/Hensel dependencies
already claimed in the queue.

## Blockers

None.
