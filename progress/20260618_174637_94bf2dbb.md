# Progress — 2026-06-18 (session 94bf2dbb, /work → /feature #7965)

## Accomplished

`HexMatrix` Phase 6 grind-automation pass on `HexMatrix/Basic.lean`
(#7965). Added `@[grind =]` (alongside the existing `@[simp]`) to the
equation-shaped public characterising lemmas:

- `transpose_transpose`, `transpose_one`
- `gramMatrix_getElem`, `gramMatrix_one`
- `leadingPrefix_entry`, `leadingRows_entry`, `submatrix_entry`,
  `submatrix_one`
- `borderedMinor_entry_lt_lt` / `_lt_last` / `_last_lt` / `_last_last`

12 lemmas total, all `@[simp] → @[simp, grind =]`.

### Excluded: `dotProduct_basis_basis`

`@[grind =]` rejected this one — grind reports "invalid pattern(s)":
the LHS pattern `dotProduct (ofFn fun b => if i = b ..) (ofFn fun b => if
j = b ..)` does not constrain `i`/`j` (they appear only inside the
indicator lambdas, which grind's E-matching cannot use to recover them).
Left as `@[simp]`-only. This is the project doctrine "don't force
contrived changes" path applied to a single lemma.

## Justification

Primarily downstream `grind` availability, matching the in-file
justification class. The annotated entry-normalization lemmas
(`leadingPrefix_entry`, `submatrix_entry`, the `borderedMinor_entry_*`
family) are documented as "the normalization that rewrites a prefix /
submatrix lookup back to a source lookup in determinant expansions."
Their consumers are grind-heavy: `HexMatrix/Determinant.lean` (128
`grind` sites) and `HexMatrix/RREF.lean` (50). Marking these `@[grind =]`
makes the entry rewrites visible to those existing `grind` calls.

No in-file proof in `Basic.lean` shortened: the remaining proofs of these
lemmas and their reindexing siblings are `Vector.ext`-structured, which
grind does not drive, so no contrived rewrites were introduced.

## Verification

- `lake build HexMatrix` → 389 jobs, `Build completed successfully`, no
  `error`/`warning`. No grind-loop; Determinant.lean build time
  unchanged (~22s vs ~23s baseline).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- Scope respected: only `HexMatrix/Basic.lean` touched; no
  `libraries.yml` bump, no `*Mathlib` bridge, no `Determinant.lean` /
  `Bareiss.lean` edits.

## Current frontier

`HexMatrix/Basic.lean` Phase 6 automation coverage complete for the
equation-shaped characterising surface.

## Next step

Sibling Phase 6 grind passes remain unclaimed: `HexGFq` (#7961) and
`HexGF2` (#7963).

## Blockers

None.

Note: worktree carries a pre-existing unrelated modification to
`.claude/skills/agent-worker-flow/SKILL.md` from before this session;
not touched or staged.
