# 64966a90 — HexBZ HO-4 cap separation canonical indicators (#4348)

## Accomplished

- Added `BHKS.ForwardRecoveryInputs.ofCapSeparationCanonicalIndicators` to
  `HexBerlekampZassenhausMathlib/Recovery.lean`. This wraps `ofCapSeparation`,
  selects `expectedIndicators := expectedIndicatorArrayOfSupports trueSupports`
  internally, and discharges `indicators_match` via
  `equivalenceClassIndicatorsOfLiftData_eq_expectedIndicatorArrayOfSupports`,
  removing duplicated B7 indicator-array plumbing from callers. The remaining
  obligations (`mignotte_precision`, `nondegenerate`, `expectedFactors`,
  per-indicator candidate facts, `product_eq`) pass through unchanged, with
  `nondegenerate` and `hcandidate` stated against the canonical indicator
  array.

- Added the lift-data-shape convenience constructor
  `BHKS.ForwardRecoveryInputs.ofCapSeparationCanonicalIndicatorsAtPrecisionForCoeffBound`
  for `d.k = precisionForCoeffBound (factorFastPrecisionCap f) d.p`. Drops
  `mignotte_precision` by routing through
  `mignotte_precision_of_liftData_precisionForCoeffBound_factorFastPrecisionCap`
  (landed in #4347) under `hp : 2 ≤ d.p`.

Both constructors are `noncomputable` because the canonical indicator array
`expectedIndicatorArrayOfSupports` is `noncomputable`.

## Current frontier

The HO-4 cap-recovery packaging now exposes a canonical-indicator constructor
chain, so later HO-4 leaf consumers (e.g. the bad-vector route into
`factorFast_ne_none_of_forwardInputs_at_cap`) only need to supply the BHKS
separation hypothesis, the support set, and the residual A2/exact-division
candidate facts — the B7 indicator array and Mignotte side condition are
discharged inside the constructors.

## Next step

Pick up the BHKS bad-vector / resultant audit (#4352) or the A2 candidate
chain (no open issue yet) to feed concrete `expectedFactors`/`hcandidate`
witnesses into these constructors.

## Blockers

None for this issue.
