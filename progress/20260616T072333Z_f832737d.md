# Progress — 2026-06-16T07:23:33Z (session f832737d, work)

## Accomplished

Claimed issue #7539 and added `BHKS.jointAux_le_paperThreshold` in
`HexBerlekampZassenhausMathlib/BadVectorAuxiliary.lean`.

The theorem is a generic joint BHKS auxiliary-domination bridge: from
`auxNorm ^ 2 ≤ cldColumnNormBound core p`, `auxDegree ≤ bhksDegree core - 1`,
`2 ≤ bhksDegree core`, `1 < ‖toPolynomial core‖₂`, and `0 ≤ auxNorm`, it proves
the joint product
`‖toPolynomial core‖₂ ^ auxDegree * auxNorm ^ bhksDegree core ≤
bhksPaperThresholdReal core 2`.  It composes `BHKS.cldColumnNormBound_le` with
the strict-norm coefficient/log absorption lemma from #7538, preserving the
joint product shape requested by #7539.

Verification:
- `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`
- `lake build HexBerlekampZassenhausMathlib.BHKSBound`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Added-line forbidden-token scan over `BadVectorAuxiliary.lean` found no
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The reusable joint paper-threshold theorem is available in the BHKS namespace.
It has not yet been wired into the downstream Recovery cap-separation chain.

## Next step

Use `BHKS.jointAux_le_paperThreshold` in the #7540 Recovery wiring task to
replace the remaining hand-split/factored paper-threshold assumptions.

## Blockers

None for #7539.  The worktree still contains an unrelated pre-existing
modification to `.claude/CLAUDE.md`; this session did not touch or stage it.
