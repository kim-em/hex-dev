# Progress — 2026-06-15 (session ba9240aa, feature)

## Accomplished

Issue #7480: proved the lift-stage pairwise-disjointness theorem
`representsIntegerFactorAtLift_disjoint_of_not_associated` in
`HexBerlekampZassenhausMathlib/Basic.lean` (placed right after the
`HenselLiftDescentHypotheses` structure, ~line 2756).

Shape:

```
HenselLiftDescentHypotheses core B primeData d successfulLift coprimeLift →
RepresentsIntegerFactorAtLift core d f S →
RepresentsIntegerFactorAtLift core d g T →
¬ Associated (toPolynomial f) (toPolynomial g) →
Disjoint S T
```

(plus the mod-p disjointness ingredients `hprime`, `hmod`,
`hcore_modP_nz`, `hsqfree`, and per-factor irreducibility/divisibility).

Proof follows the issue's prescribed route exactly:
1. descend `S`, `T` to mod-p subsets `S₀`, `T₀` via
   `hdescent.represents_modP_of_lifted` (the descent field of
   `HenselLiftDescentHypotheses`);
2. apply `modPFactorSubset_disjoint_of_not_associated` to get
   `Disjoint S₀ T₀`;
3. reflect through `liftedSubsetOfModPSubset_disjoint_iff` (a #7478
   structural bridge) to conclude `Disjoint S T`.

Non-circular: assumes neither `LiftedFactorSubsetPartition` nor
`InitialLiftedFactorSubsetPartitionEvidence`. This is the standalone
sibling of the `pairwise_disjoint` evidence field, callable by a
future non-circular producer of that evidence.

## Verification

- `lake build HexBerlekampZassenhausMathlib`: green, 0 `error:` lines;
  downstream modules importing `Basic.lean` (Resultant, ColumnSignature)
  rebuilt; full target reached Conformance.
- `python3 scripts/check_dag.py`: exit 0.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` on added lines.

## Current frontier

The descent package `HenselLiftDescentHypotheses` is still consumed as a
hypothesis here; its producer from `toMonicPrimeData?` / `choosePrimeData?`
success is the open #7361. A non-circular producer of
`InitialLiftedFactorSubsetPartitionEvidence` can now fill its
`pairwise_disjoint` field by calling this theorem.

## Next step

#7361 (descent-hypotheses producer) and the remaining
`InitialLiftedFactorSubsetPartitionEvidence` field producers for the
parent #7474 decomposition.

## Blockers

None.
