# 20260609T225938Z (#6726)

## Accomplished

Claimed #6726 and added a compiled square-free/no-repeated-divisor substrate in
`HexBerlekampZassenhaus/Basic.lean`:

- `square_not_dvd_of_squareFreeRat`
- `dvd_polyProduct_toArray_of_mem`
- `trialDivisionPeelAux_no_emitted_residual_divisor_of_squareFreeRat`
- `exactQuotient?_eq_none_of_not_dvd_core`
- `trialDivisionPeelAux_no_residual_candidate_of_squareFreeRat`

These lemmas combine the previously landed
`trialDivisionPeelAux_no_missed_unemitted` invariant with squarefreeness: an
unemitted candidate cannot divide the final residual by the no-missed invariant,
and an emitted positive-degree candidate cannot also divide the final residual
because that would make `candidate * candidate` divide the square-free peel
target.

## Current frontier

The full slow-trial residual irreducibility theorem is not yet closed. The next
proof step is to instantiate the combined residual-candidate theorem at the
`exhaustiveIntegerTrialCoreFactorsWithBound` split/peel residual, then compose it
with the #6717 bounded-candidate completeness theorem and the default-bound
divisor coefficient hypothesis.

## Next step

Prove the public raw theorem for `exhaustiveIntegerTrialCoreFactorsWithBound`
under the normalized square-free core hypotheses, covering:

- integer-root split factors;
- peeled bounded candidates;
- the final appended residual.

For the residual case, use `trialDivisionPeelAux_no_residual_candidate_of_squareFreeRat`
to reject every bounded positive-leading candidate divisor after applying the
candidate-completeness lemma.

## Blockers

No technical blocker in the compiled substrate. The remaining theorem is larger
than this session.

Verification performed:

- `lake build HexBerlekampZassenhaus.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over `HexBerlekampZassenhaus/Basic.lean`

Attempted `lake build HexBerlekampZassenhaus`; it built through
`HexBerlekampZassenhaus.Conformance` and then produced no output for several
minutes, so the process group was terminated. No Lean error was reported before
termination.
