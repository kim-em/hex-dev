# 2026-05-19T00:37Z — review session 3417a05d

## Accomplished

- Claimed #5181 (review: migrate `HexPolyFp/Quotient.lean` and
  `QuotientFrobenius.lean` to `coeff_*_ring` simp wrappers).
- Performed the full migration locally as the issue body described
  (replaced 6 private-helper `coeff_add _ _ _ zmod64_*_local` /
  `coeff_sub _ _ _ zmod64_*_local` rewrites and the trailing
  `exact zmod64_*_local(.symm)?` leaves in `Quotient.lean`, replaced 2
  one-off `have hzero_sub`-then-`coeff_sub _ _ _ hzero_sub` patterns at
  the `monomial_pPow_sub_X_*` lemmas, replaced the single `hzero_add`
  pattern in `QuotientFrobenius.lean`, deleted the now-unused
  `zmod64_add_zero_zero_local` / `zmod64_sub_zero_zero_local` private
  helpers and their `omit` lines, left the 2297–2300 `hzero_sub` on
  `Quotient` elements alone per the issue's carve-out).
- Built `HexPolyFp.Quotient` — every single one of the migrated
  rewrites failed with `Tactic 'rewrite' failed: Did not find an
  occurrence of the pattern (?p + ?q).coeff ?n` (and the `sub` variant),
  even though the target literally has that form. Re-tried with
  `simp only [DensePoly.coeff_add_semiring]` on a subset and got
  `'simp' made no progress` for the same goals.
- Confirmed this is the same wrapper-level diamond reproduced on the
  sibling issue #5180 for `Compose.lean` (session 1d42bb1c-…),
  diagnosed as `Lean.Grind.Semiring.toAdd` (the only `Add S` witness
  available inside `coeff_add_semiring`'s `[Lean.Grind.Semiring S]`
  binder) failing to unify with the direct
  `instance : Add (ZMod64 p)` from `HexModArith/Basic.lean:431`. The
  two instances are propositionally equal but `rw`'s syntactic matcher
  and `simp`'s discrimination tree both refuse them. Tracked in
  #5184.
- Reverted the working-tree changes to `HexPolyFp/Quotient.lean` and
  `HexPolyFp/QuotientFrobenius.lean` so the branch stays clean.
- `coordination skip 5181 …` with a full diagnosis referencing #5184
  (the wrapper-level fix). Issue is now `replan`-labelled.
- `coordination add-dep 5182 5184` and
  `coordination add-dep 5183 5184` — the remaining
  `HexPolyFp/Basic.lean` and `HexPolyFp/SquareFree.lean` migrations
  share the same wrapper dependency and were sitting in the unclaimed
  queue. Both now carry `depends-on: #5184` and the `blocked` label
  so future workers don't claim them and rediscover the same wall.

## Current frontier

- #5184 (`feature`) is the unblocker — it has to land before any of
  the four `HexPolyFp` migration issues (#5180 / #5181 / #5182 /
  #5183) can succeed. The fix surface is in
  `HexPoly/Operations.lean`: re-state the three wrappers
  (`coeff_add_semiring`, `coeff_sub_ring`, `coeff_neg_ring`) so their
  LHS uses an `Add S` / `Sub S` instance that matches what call sites
  see, not `Lean.Grind.Semiring.toAdd` / `Lean.Grind.Ring.toSub`.
- The unclaimed review queue after this turn is empty for
  `HexPolyFp` migration work until #5184 closes.

## Next step

- A `feature` worker claims #5184, restates the three wrappers (most
  natural fix: take `[Add S]` / `[Sub S]` as explicit binders
  alongside the `Lean.Grind.*` binders, so the wrapper's LHS picks
  the call-site instance instead of the `Semiring`/`Ring` projection;
  alternative: add a `Sub`-side analogue of whatever pattern is
  already proven for `coeff_scale_semiring`/`coeff_derivative_semiring`
  which do fire correctly when `S` is left abstract — they avoid the
  diamond only because no concrete `S` appears in their existing call
  sites). Add a smoke `example` at the end of `HexPoly/Operations.lean`
  that calls `rw [coeff_add_semiring]` on a goal of the form
  `(_ + _ : DensePoly (ZMod64 p)).coeff _` (or equivalent
  diamond-prone shape) so future regressions trip in the same file
  that owns the wrappers.
- Once #5184 merges, the four migration issues unblock automatically
  via `coordination check-blocked`.

## Blockers

- None on this session — work is cleanly handed off via #5184. No
  uncommitted code remains on `agent/3417a05d`.
