# Session 790ee0bc — work, issues #4006 → #4272

## Accomplished

- Claimed #4006, found it stale, and skipped it for replan:
  the issue still depended on the public
  `exhaustiveCoreFactorsWithBound` coverage theorem that did not land with
  #4236; successor issues #4272/#4273/#4274 now track that missing
  non-circular coverage chain.
- Claimed #4272 and added
  `henselSubsetCorrespondenceRest_transport_of_disjoint` in
  `HexBerlekampZassenhausMathlib/Basic.lean`.
- The new lemma transports `HenselSubsetCorrespondenceRest` across one emitted
  recombination factor from `target` to `quotient`, shrinks the remaining
  lifted-factor index set from `J` to `J \ S`, and reuses the parent
  uniqueness field. It explicitly isolates the remaining square-free
  disjointness obligation needed by recursive coverage.
- Verified:
  - `lake build HexBerlekampZassenhausMathlib.Basic`
  - `lake build HexBerlekampZassenhausMathlib`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level
    `sorry` in the diff.

## Current frontier

- #4272 now has the structural rest-state transport lemma needed for the next
  recursive-search coverage step.
- The main remaining mathematical obligation is to prove the disjointness fact:
  irreducible divisors of the quotient are represented by lifted-factor subsets
  disjoint from the emitted subset. That should come from the square-free
  factorisation/associated-polynomial side, not from the rest predicate alone.

## Next step

- Continue #4272/#4273 by proving the square-free disjointness/associatedness
  lemma and then use it with
  `henselSubsetCorrespondenceRest_transport_of_disjoint` to drive recursive
  `recombinationSearchModAux` coverage.

## Blockers

- None for the transport lemma landed here.
