# Phase 6: expose Monic forwarding lemma (issue #6149)

## Accomplished

- Added `DensePoly.leadingCoeff_eq_one_of_monic` in `HexPoly/Euclid.lean`
  immediately after the `Monic` definition. The lemma is the identity
  function on the underlying proof (`hp : p.Monic` is definitionally
  `p.leadingCoeff = 1`), giving callers a named forwarding API.
- Rewired the four `simpa [DensePoly.Monic] using hmonic` call sites in
  `HexHensel/Linear.lean:1303`, `:1368`,
  `HexHensel/Quadratic.lean:668`, and `:2094` to use
  `DensePoly.leadingCoeff_eq_one_of_monic hmonic` directly. The two
  cases inside `have ... := by simpa ...` collapsed to non-tactic
  `:=` term proofs since the lemma is already a forward direction.

## Current frontier

- `simpa [DensePoly.Monic]` no longer appears in `HexHensel/` (verified
  by `git grep`).
- The other Phase 6 sibling issues (`leadingCoeff` characterizations
  #6148, `gcd_eq_xgcd_gcd` bridge #6150, simp/grind annotations #6151,
  docstring gaps #6152) remain open and may be picked up in subsequent
  worker sessions.

## Next step

Land the PR for #6149; next worker can claim #6148, #6150, #6151, or
#6152 (all Phase 6 cleanups), or move to #6143 / #6155.

## Blockers

None. `lake build` succeeds (244 jobs), `python3 scripts/check_dag.py`
is clean, `git diff --check` is clean, and no forbidden tokens
(`sorry`, `axiom`, `native_decide`, `TODO`, `FIXME`) were introduced.
