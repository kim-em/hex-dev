# Rat DivModLaws modulo fields

## Accomplished

- Claimed issue #1948.
- Closed the non-gcd `ratDivModLaws` modulo obligations in `HexPolyZ/Basic.lean`:
  `mod_self_eq_zero`, `mod_eq_zero_of_dvd`, `mod_mod_of_not_pos_degree`,
  `mod_add_mod`, and `mod_mul_mod`.
- Added private Rat helper lemmas for zero remainders, divisibility reduction,
  additive remainder congruence, and multiplicative remainder congruence.
- Verified `lake build HexPolyZ.Basic`, `lake build HexPolyZ`,
  `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

## Current frontier

The Rat `DivModLaws` instance no longer has `sorry`s. The remaining
`HexPolyZ/Basic.lean` `sorry`s are in the Rat gcd law package and the
square-free Rat theorem branch, outside this issue's scope.

## Next step

Proceed with the dependent Rat gcd issue once this PR lands.

## Blockers

None.
