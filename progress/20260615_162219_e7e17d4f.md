# Progress — 2026-06-15 (session e7e17d4f, review)

## Accomplished

Issue #7495: read-only audit of the dilation / unit-substitution
divisibility-reflection cluster in `HexPolyZMathlib/Basic.lean`
(declarations at lines 216–390). Posted a CLEAN verdict comment
(https://github.com/kim-em/hex/issues/7495#issuecomment-4710007141)
covering all three deliverables:

1. **Premises load-bearing, not vacuous.** `IsUnit u` on
   `dvd_of_comp_unit_mul_X` is required (gave a concrete `ℤ`, `u=2`,
   `a=C 2`, `b=X` counter-instance where the reflection is false without
   it); `c ≠ 0` on `natDegree_toPolynomial_dilate` is required
   (`dilate 0 X = 0`); `lc ≠ 0` / `core ≠ 0` on `dilate_recovery` both
   feed the non-vanishing chain (`hK0`/`hlhs0`/`hG0`/`hdg0`) and the
   degree branch.
2. **Generic `{R} [CommRing R]` is necessary, not just acceptable.** The
   unit-reflection is staged for instantiation over `ZMod (p^k)` (where
   `lc` is a unit because `p ∤ lc`), not over `ℤ` (units only `±1`). The
   iff's `map_dvd` direction correctly ignores `hu`. No external call
   site forces `ℤ` (the lemmas are referenced nowhere outside the
   defining file yet — staged infrastructure).
3. **`dilate_recovery` sound.** The `pg ∣ core` conclusion is pure Gauss
   machinery (`dvd_primPart_iff_dvd` + `primPart_mul` + `isUnit_primPart_C`
   absorbs the `C (lc^(d-1))` scalar as a primitive-part unit); no monic
   or association side-condition smuggled. `primitivePart`
   non-sign-normalisation (#7365) is consumer-side, correctly out of scope.

No defect found; no follow-up `feature` issue filed.

## Verification

- `lake build HexPolyZMathlib.Basic`: green (1604 jobs), 0 `error:`
  lines, no `sorry` in target.
- No declaration statements or proofs touched (read-only audit).

## Current frontier

Cluster verified sound. The `comp_unit_mul_X_*` lemmas have no call
site yet — they await the lift-stage support step that consumes them.

## Next step

None for this issue. The sibling BZMathlib-consumer-side review and the
scaled-candidate support field (#7500) remain.

## Blockers

None.
