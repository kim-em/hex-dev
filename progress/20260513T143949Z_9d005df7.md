# #3522: HexMatrix — last-column Laplace expansion core

Session: `9d005df7`
Branch: `agent/9d005df7`

## Accomplished

- Added `det_eq_foldl_laplace_last` in `HexMatrix/Determinant.lean`:
  `det M = (List.finRange (n + 1)).foldl
            (fun acc row => acc + M[row][Fin.last n] *
              cofactor M row (Fin.last n)) 0`
  over `Lean.Grind.CommRing R` for `M : Matrix R (n + 1) (n + 1)`.
- Added the supporting private helper layer needed to factor a Leibniz
  term whose permutation sends row `i` to the last column:
  - `detSign_insertAt_general`: sign formula for insertAt at any position.
  - `cofactorSign_last_eq_pow`: bridge `cofactorSign i last = (-1)^(n - i.val)`.
  - `insertAt_get_skipIndex`: `(insertAt x v i)[skipIndex i r'] = v[r']`.
  - `list_finRange_succ_eq_insertIdx_skipIndex` and `_perm_skipIndex`:
    `List.finRange (n+1)` is `((List.finRange n).map (skipIndex i)).insertIdx i.val i`,
    so it's a `List.Perm` of `i :: (List.finRange n).map (skipIndex i)`.
  - `foldl_finRange_succ_factor_skipIndex`: factor a multiplicative
    foldl over `List.finRange (n+1)` at index `i`.
  - `detProduct_insertAt_general`: `detProduct M (insertAt last v.castSucc i) =
      M[i][Fin.last n] * detProduct (deleteRowCol M i (Fin.last n)) v`.
  - `detTerm_insertAt_general`: combined sign + product bridge.
  - `foldl_detTerm_insertions_eq_cofactor`: the per-i collapse
    `Σ_v detTerm M (insertAt last v.castSucc i) = M[i][last] * cofactor M i last`.
- Final assembly uses the existing `permutationVectors (n+1) = flatMap …`
  unfolding, `foldl_det_sum_nested_zero`, and `foldl_det_sum_swap` to
  rearrange the Leibniz sum into the row-indexed Laplace foldl.

## Current frontier

`HexMatrix.Determinant` builds; full `HexMatrix` builds (only
pre-existing `sorry` at `HexMatrix/Bareiss.lean:1118` remains, untouched).

## Next step

`#3518` (transport Laplace expansion to arbitrary rows and columns) is
the immediate follow-on; it can quote `det_eq_foldl_laplace_last`
together with row/column transposition lemmas already in the file.

## Blockers

None for this issue.
