# Accomplished

Rebased the polynomial-dispatch experiment onto the repaired integral-canary
edge `d6e4a3940`.  The feature still reconstructs integer polynomial programs
without function cases, dispatches real work to Sturm isolation and complex
work to the certified NK/Pellet engine, and kernel-replays complete root
coverage for the two fixed canaries.

The combined focused build completed successfully (8,960 jobs).  DAG, Phase 4,
published-trust, factor-freshness, PNT-inventory, conformance-registration,
diff, and prohibited-proof checks remain green after the rebase.

# Current frontier

The candidate is local only at `8d68f8016`.  Its parent is the exact repaired
integral PR head, whose docs now distinguish a fixed authenticated record from
a future certificate whose decoded numeric fields are proof parameters.

# Next step

After the integral canary merges, rebase this single feature commit onto the
resulting exact `main`, refresh the narrow lakefile exemption if necessary,
open the polynomial-dispatch PR, and require a fresh exact-head review and CI.

# Blockers

None for the real or complex root-dispatch experiment.  General polynomial
normalization, rational/division and multivariate recognition, arbitrary root
schemas, and performance-based routing remain later work.
