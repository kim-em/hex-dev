**Accomplished**

Reviewed PR #8734's `Submatrix` semantics, quadrant/padding abstraction lemmas,
`mulStrassenView_eq_mul` reduction, flat column engines, generated C ownership
behavior, and public naming/attributes. Per the request, did not rebuild.

**Current frontier**

No soundness or correctness-proof blocker found. The main review finding is a
documentation overclaim: generated C eliminates quadrant buffer copies but still
allocates small shared-view records for sibling quadrants. Empty/inverted real
windows are semantically safe but should be documented as intentional.

**Next step**

Before merge, soften “allocates nothing” / exhaustive allocation claims to
“does not materialize or copy quadrant buffers,” document inverted endpoints as
the empty-window encoding, and optionally clarify that column operations allocate
one or two contiguous column snapshots before conditionally mutating the backing.

**Blockers**

None.
