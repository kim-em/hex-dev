# 20260602T163840Z 783403b3 — Issue #6361

## Accomplished

- Claimed feature issue #6361.
- Added `BHKS.FactorFastCapLiftAnalyticComparison.ofBridgeData` in
  `HexBerlekampZassenhausMathlib/Recovery.lean`.
- The new theorem supplies the actual `factorFast` cap-lift analytic comparison
  from the existing bridge-data cap-separation inputs by composing through
  `capSeparationOfBridgeData`, so callers with cut hypotheses, bridge data,
  prime positivity, and the BHKS Hadamard/l2norm strict inequality do not need
  to manually assemble `ExecutableCapSeparationHypotheses` just to extract the
  comparison package.

## Current frontier

- The cap-lift comparison package now has a direct bridge-data producer surface.
- The remaining analytic arithmetic leaf is still the proof of the strict
  Hadamard/l2norm inequality itself from the auxiliary-polynomial norm and
  cap-arithmetic estimates.

## Next step

- Use the new comparison supplier at future HO-4 capstone call sites, and
  continue narrowing the remaining raw strict-inequality obligation toward the
  existing `BadVector.lean`, `TerminationBound.lean`, and `BHKSBound.lean`
  arithmetic surfaces.

## Blockers

- Final `factorFast_terminates` assembly remains blocked on the active D1
  prerequisite chain (#6344, #6345, #6346).

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` — green.
- `lake build HexBerlekampZassenhausMathlib` — green, with pre-existing warnings
  in unrelated files.
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Added-line scan for `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`
  in `HexBerlekampZassenhausMathlib/Recovery.lean` — no matches.
