# Planner session b64aa8e9

## Accomplished

Refiled the next two prerequisite sub-issues of the closed #4639 →
#4866 chain that `/replan` asked the planner to carry forward as
sub-issues of (closed) #4626. Queue deficit at session start was 2;
two new feature issues posted, both blocked.

* #4869 — `BZ Mathlib-free: add scaledRecombinationSearchMod product
  lemma and recombineScaledExhaustive_product wrapper`. Depends on
  #4866. Adds three private lemmas in
  `HexBerlekampZassenhaus/Basic.lean` mirroring the unscaled
  `_product` triple at lines 7636 / 7684 / 7791. The proof analysis
  argues the post-condition shape (`polyProduct factors = target`)
  carries over from the unscaled chain unchanged, because the only
  fact the unscaled product proof needs about the candidate is that
  `exactQuotient? target candidate = some quotient` implies
  `candidate * quotient = target`. The "scaled candidate's product
  divides core but does not equal it" remark in #4866's out-of-scope
  notes is about the raw `scale coreLc (∏ T)` before primitivisation,
  not the recorded candidate. The body includes a fallback path:
  if the worker discovers a reshape is genuinely needed they should
  file a follow-up and `coordination skip` this issue.
* #4870 — `BZ Mathlib-free: switch exhaustiveCoreFactorsWithBound to
  recombineScaledExhaustive call site`. Depends on #4866 and #4869.
  Updates `HexBerlekampZassenhaus/Basic.lean:5363` to call
  `recombineScaledExhaustive (DensePoly.leadingCoeff core)`, then
  rechains the downstream `exhaustiveCoreFactorsWithBound_*`
  invariants through the scaled siblings. The three Mathlib-side
  `_product` call sites at
  `HexBerlekampZassenhausMathlib/Basic.lean:11538, 11602, 11685` only
  need a re-check (no rewrite if #4869 lands the identical shape).

## Current frontier

Remaining sub-issues in the chain (not filed this cycle, awaiting
predecessor completion):

* Successor to #4870: scaled membership wrapper
  `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
  in `HexBerlekampZassenhausMathlib/Basic.lean`.
* Final #4639 successor: relax the three top-level wrappers in
  `HexBerlekampZassenhausMathlib/Basic.lean` from `Monic core` to
  `Primitive core + 0 < leadingCoeff core`.

## Next step

The next planner cycle should either (a) wait for #4866/#4869/#4870 to
unblock before filing the scaled membership wrapper sub-issue, or (b)
identify orthogonal work in the HO-1 ModP/Hensel substrate chain
(#4680 family) or in the HexGramSchmidt adjacent-swap chain
(#4853 → #4854 → #4845) if the queue deficit grows again.

## Blockers

None on the planning side. The chain remains blocked on #4863 (in
flight, PR #4867) → #4866 (next).
