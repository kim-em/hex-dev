# 03ae0469 — work #5925

## Accomplished

- Checked directive priority and found only blocked directive issues.
- Attempted #5919, but claim lost to another agent.
- Claimed and skipped stale Schmeisser issues #5559 and #5903:
  - #5559 still depended on the closed all-degree packaging route #5570, which was replaced by narrower degree-two work.
  - #5903 asked for a closed `natDegree ≤ 2` wrapper even though #5891 records a counterexample to the corresponding open predicate and #5909 only added exact-degree API.
- Claimed #5925 and repaired the small-mod singleton square-free bridge in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.
- Added a local `gcdIsUnit`-to-Mathlib-coprime bridge so the monic modular image proof no longer requires the raw executable gcd representative to be definitionally `1`.
- Updated `gcd_monicModularImage_derivative_eq_one` and
  `squareFreeCore_irreducible_of_small_mod_singleton_of_choosePrimeData_squareFreeModP`
  to consume the `Hex.squareFreeModP` fact through its actual gcd-unit shape.

## Current frontier

The #5925 local bridge errors are gone. `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
still fails on stale call sites owned by #5926:

- `liftedFactorSubsetPartition_of_choosePrimeData` calls missing the explicit
  `primeData` and `hchoose` arguments.
- Missing/renamed
  `monicReductionCorrespondence_liftedFactor_facts_of_normalizeForFactor_squareFreeCore`
  consumers.
- `henselSubsetCorrespondenceHypotheses_of_choosePrimeData` calls still using
  the old arity.

I attempted to claim #5926 to repair those too, but it was already claimed by
another agent, so I did not edit that scope.

## Next step

Let the #5926 worker finish the stale witness-threading call sites, then rerun:

- `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
- `lake build HexBerlekampZassenhausMathlib.Basic`

## Blockers

- Full target verification for #5925 is blocked by #5926, which is claimed by
  another agent.
- The worktree had a pre-existing unrelated modification to `.claude/CLAUDE.md`;
  I left it untouched.
