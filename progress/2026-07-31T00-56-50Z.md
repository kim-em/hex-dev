# HexBZ direct classical engine

## Accomplished

- Added the typed direct-coordinate problem, modular prime plan, lift plan,
  lifted basis, classical outcome, decline reason, tier, and trace records.
- Implemented one cached direct prime plan, one direct Hensel lift, and one
  streaming head-forced size-ordered search with complete-level accounting.
- Proved direct recovery, support partitioning, iterator completeness, minimal
  accepted support, recursive factor correctness, and the public dispatch
  irreducibility bridge.
- Cut the executable classical public API over to the direct engine and made
  traced and untraced calls derive from one execution.
- Updated service, conformance, fixture emission, sweep configuration, and the
  trace gate to remove M1 fallback/no-decline routes and emit typed statistics.
- Regenerated the BZ conformance fixture and trace baseline; the trace gate
  checks all 54 traces with no failures.
- Built the full `HexBerlekampZassenhausMathlib` target and the BZ service and
  conformance targets successfully.

## Current frontier

- The new direct engine and proofs are green, but the production cutover still
  contains transitional compatibility wrappers and the now-dead classical
  M2/recursive-relift implementation in `FactorEntryPoints.lean`.

## Next step

- Remove the dead executable and proof modules, isolate the lattice-only
  `toMonic` machinery, shrink the public surface, then run the full correctness
  and current-public-performance gates.

## Blockers

- None.
