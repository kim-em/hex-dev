# 2026-05-11 16:17 UTC — work session (162b627c)

## Accomplished

- Claimed #3483 for HexBerlekampZassenhaus benchmark compare coverage.
- Added shared-domain compare targets in `HexBerlekampZassenhaus/Bench.lean`:
  - `runFactorCompareChecksum`
  - `runFactorSlowCompareChecksum`
  - `runFactorFastCompareChecksum`
- Registered the compare targets over the deterministic split smoke domain
  `smokeInput n` for `n = 1..4`.
- Kept `factorFast` compare behavior explicit: successful fast-path results use
  the same factorization checksum, while `none` returns an input-dependent
  sentinel so `compare` reports divergence.
- Updated `reports/hex-berlekamp-zassenhaus-performance.md` with the new compare
  domain, commands, observed agreement, and remaining smoke-sized limitation.
- Verified:
  - `lake build HexBerlekampZassenhaus.Bench`
  - `lake exe hexbz_bench list`
  - `lake exe hexbz_bench verify`
  - `lake exe hexbz_bench compare Hex.BerlekampZassenhausBench.runFactorCompareChecksum Hex.BerlekampZassenhausBench.runFactorSlowCompareChecksum`
  - `lake exe hexbz_bench compare Hex.BerlekampZassenhausBench.runFactorCompareChecksum Hex.BerlekampZassenhausBench.runFactorSlowCompareChecksum Hex.BerlekampZassenhausBench.runFactorFastCompareChecksum`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Bench.lean`

## Current frontier

- The benchmark suite now has a valid small shared-domain compare surface for
  public `factor`, exhaustive `factorSlow`, and successful `factorFast` results.
- The full scientific-domain LLL-assisted versus exhaustive recombination
  comparison remains unresolved and is still called out as a Phase 4 concern.
- The worktree still has a pre-existing `.claude/CLAUDE.md` modification; this
  session did not touch it.

## Next step

- Land the #3483 PR, then continue HO-3 work by expanding the scientific-domain
  compare coverage beyond the smoke split family.

## Blockers

- None for the #3483 compare-coverage slice.
