# HexConway conway-polynomials adapter

UTC: 2026-05-04T23:29:51Z. Session type: work/feature.

## Accomplished

- Claimed feature issue #2105 after human-oversight issue #1997 could not be claimed because it is marked replan.
- Added `scripts/oracle/conway_polynomials_table.py`, a small Python adapter around the `conway-polynomials` package.
- Implemented lookup by `(p, n)` with coefficients normalized as ascending `[a0, a1, ..., 1]`.
- Added a `--smoke` mode checking the Luebeck review cases `(2,1)`, `(2,3)`, and `(3,2)`.
- Documented in the module docstring that this is a table-source check, not an independent Conway recomputation.

## Current frontier

The adapter is ready for a later #1997 worker to plug into the full Lean fixture/cache/CI workflow. The default environment does not currently have `conway-polynomials` installed, but an isolated temp venv with the package installed passed the smoke cases.

## Next step

Use this adapter from the #1997 replacement fixture flow: compare Lean-emitted coefficients, the committed Luebeck-derived cache, and `conway_polynomials.database()[p][n]` for the selected Conway entries.

## Blockers

- The default `python3` environment lacks the `conway-polynomials` package; callers must install it where the oracle is run.
