# 20260509T203617Z - Issue #2859

## Accomplished

Claimed feature issue #2859 after `coordination claim 2851` correctly refused
the replan-marked prerequisite issue. Proved
`Hex.GramSchmidtMathlib.int_basis_row_eq_gramSchmidt` in
`HexGramSchmidtMathlib/Basic.lean` by reducing the integer cast path to the
rational bridge theorem.

Created successor issue #2871 for the remaining rational rowwise bridge proof
and left a `Decomposed into #2871` breadcrumb on #2859. This session is being
published as a partial PR because the remaining rational proof needs a focused
projection/recurrence bridge between the project dense-row implementation and
Mathlib's `InnerProductSpace.gramSchmidt_def`.

Verification completed:

- `lake build HexGramSchmidtMathlib.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexGramSchmidtMathlib/Basic.lean`

The grep still reports the pre-existing `sorry` at
`rat_basis_row_eq_gramSchmidt`; this session removed the integer bridge proof
hole and did not add any new banned markers.

## Current frontier

Issue #2871 now owns the remaining rational bridge proof. The likely next
step is to prove `rowToEuclidean` projection helpers and connect
`GramSchmidt.Rat.basis_decomposition` / `prefixCombination` / `coeffs` to
Mathlib singleton `starProjection` sums over `Iio`.

## Next step

Continue with #2871: prove `rat_basis_row_eq_gramSchmidt`, keeping the integer
bridge as the final direct reduction.

## Blockers

None.
