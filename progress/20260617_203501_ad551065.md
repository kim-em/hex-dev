# Progress — 2026-06-17 (session ad551065, /work → /feature)

## Accomplished

No code changes. Triaged the two unclaimed feature items; both were
non-actionable, and I left concrete diagnoses so the queue reflects reality.

### #7799 (Mathlib Berlekamp factor irreducibility bridge) — skipped (replan)

Premise wrong: the issue asks to remove the `irreducible_of_mem_berlekampFactor`
sorry "scoped to `HexBerlekampMathlib/Basic.lean`" without touching the Zassenhaus
capstones. That is impossible for a sound fix. The unconditional statement is
false at `f = 1` (the issue itself notes this), so any narrowing changes the
signature, and that sorry is load-bearing for a tower of downstream theorems in
the merge-gating `HexBerlekampZassenhausMathlib` layer that are *themselves*
unsound on constant inputs and only compile through it:

- `irreducible_of_mem_berlekampFactor` consumed unconditionally at
  `HexBerlekampMathlib/Basic.lean:1043` and
  `HexBerlekampZassenhausMathlib/Basic.lean:7908`.
- `factors_irreducible_of_factorsModPBerlekampForm`
  (`HexBerlekampZassenhausMathlib/Basic.lean:7833`) is false for constant `core`
  (`monicModularImage 1 = 1` ⇒ claims `Irreducible 1`); its only hypothesis
  `isGoodPrime` (`HexBerlekampZassenhaus/Basic.lean:80`) admits `f = 1`, so the
  nonconstant guard cannot be discharged locally. Feeds 7 CLD-partition sites.
- IntReductionMod singleton chain → `squareFreeCore_irreducible_of_smallModSingletonBranch`
  (`IntReductionMod.lean:3331`), whose docstring says it feeds the
  `factor_irreducible_of_nonUnit` capstone path.

The needed guard already exists on the Mathlib-*free* analog
`squareFreeCore_irreducible_of_small_mod_singleton`
(`HexBerlekampZassenhaus/SmallModSingleton.lean:298`, `hdeg`), and
`berlekampFactor_factors_pos_degree` (`HexBerlekamp/Factor.lean:1466`) supplies
positive-degree factors from a positive-degree input. So the sound fix is to
thread the nonconstant guard from the capstone dispatch down the Mathlib chain —
spanning Basic.lean + IntReductionMod + Zassenhaus Basic + the capstone path,
not a Basic.lean-only change. Recommended re-scope in the issue comment.

### #6672 (HO-1 capstone) — re-blocked on #2564

Both declared deps (#7804, #7805) closed, so `check-blocked` surfaced it as
unclaimed. But #7805 (the unconditional fast-BHKS raw substrate this capstone
routes through) closed **NOT_PLANNED**, forwarded to directive #2564: the
tight-cut route is unsound and the whole fast-BHKS path is being rewritten.
`factorFastRaw_irreducible_of_some` therefore does not exist and cannot be
stated soundly until #2564 lands. Added `depends-on: #2564` (re-blocked) with an
explanatory comment.

## Current frontier

Both Berlekamp-irreducibility threads (#7799 executable→Mathlib bridge, #6672
capstone) are gated on the same architectural truth: emitted factors are only
irreducible when nonconstant, and the Mathlib-side consumer chain never threads
that guard. #7799 needs a re-scope; #6672 needs directive #2564.

## Next step

A planner should re-scope #7799 to cover the full Mathlib consumer chain
(IntReductionMod squareFreeCore family + Zassenhaus partition consumers + the
`factor_irreducible_of_nonUnit` capstone path), mirroring the Mathlib-free
`hdeg` guard. #6672 stays parked behind #2564.

## Blockers

No actionable unclaimed work this session: #7799 (re-scope needed), #6672
(blocked on #2564), #2564 (blocked directive, 16 open deps).
