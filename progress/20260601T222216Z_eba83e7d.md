# eba83e7d - work #6136

## Accomplished

- Claimed and completed feature issue #6136.
- Made the three `IsRREF.nullspaceMatrix` entry characterisation lemmas public
  in `HexMatrix/RREF.lean`: `nullspaceMatrix_free`,
  `nullspaceMatrix_free_ne`, and `nullspaceMatrix_pivot`.
- Added concise docstrings for the own-free-column, other-free-column, and
  pivot-row entry cases.
- Added `@[simp]` to the now-public matrix-entry lemmas to match the existing
  public `nullspace_get_*` vector-entry API.
- Checked `HexMatrixMathlib/RankSpanNullspace.lean`; it has no local helper
  that unfolds `nullspaceMatrix` merely to recover these entry facts, so no
  consumer edit was needed.
- Verified with `lake build HexMatrix`, `lake build`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token
  grep over the touched Lean diff.
- Ran the `second-opinion` helper over the diff; it found no blocker and noted
  the `@[simp]` addition is low-risk but should be called out in the PR.

## Current frontier

The nullspace-matrix basis-column entry facts are now public and documented.
Downstream bridge callers can use these lemmas directly instead of unfolding
`IsRREF.nullspaceMatrix`.

## Next step

Create the PR for #6136.

## Blockers

None.
