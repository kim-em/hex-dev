# issue-8545: both verified Isabelle systems built and swept on carica

Session type: feature (issue #8545), branch `issue-8545`, PR #8546.

Follow-up to the six-deliverable landing: the user asked for both Isabelle
systems on the plots. Built both on carica and re-swept all eight systems.

## Accomplished

- **isabelle-bz** built from AFP `Berlekamp_Zassenhaus` (`setup_bz_isabelle.sh`);
  driver round-trips and agrees with hex.
- **isabelle-lll build spike PASSED.** Found the real AFP API: the verified
  direct-LLL reconstruction is bundled as
  `one_lattice_LLL_factorization :: int_poly_factorization_algorithm` (a typedef
  pairing the algorithm with its soundness proof), and `factorize_int_poly_generic`
  takes that bundle. Corrected the export theory to
  `factorize_int_poly_generic one_lattice_LLL_factorization` (the initial guess
  passed a raw function). Compiles, code-exports, driver agrees with all systems.
  Added `-c` to the setup export step to avoid the cached-session emit skip.
- **Full 8-system sweep** (`hexbz-factor-sweep-cc203779-carica.json`,
  sha256 33a1d9b4…): cross-check clean across all eight. Retired the earlier
  6-system record; regenerated all 21 figures; rewrote the report with the
  verified-vs-verified headline.

## The headline (per-family max degree solved)

swinnerton-dyer: hex-lattice **64** vs isabelle-bz 32 vs isabelle-lll 16 (flint 128).
sd-products: hex-lattice **56** vs isabelle-bz 42 vs isabelle-lll 16.
hex's van Hoeij CLD knapsack beats both verified systems on the lattice-stress
families; isabelle-lll (full-degree direct-LLL) is the slowest verified system
overall (142/205), as the literature predicts. On irreducible cyclotomics
exponential isabelle-bz wins among verified — honest and family-dependent.

## Next step

Commit + push; CI is green on the code (drivers/report are not CI). Done.
