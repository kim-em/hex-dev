# Session c7d6679d — #4371 matched lifted-factor mask-to-subset bridge

Session: `c7d6679d`
Issue: #4371

## Accomplished

Claimed and executed issue #4371. Added the mask-to-subset bridge converse to
`liftedSubsetSplit_mem_subsetSplitsWithFirst_of_matches` in
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `liftedSubsetSelectedList_eq_mask_partition_of_matches`: given a matched
  `localFactors = head :: tail` (via `LiftedFactorListMatches d J localFactors`,
  `J.Nonempty`) and a Boolean mask of length `tail.length`, returns a
  `LiftedFactorSubset d` containing `J.min'`, contained in `J`, whose
  selected/rest list partition equals the matched-list mask partition:
  - `liftedSubsetSelectedList d T = head :: (tail.zip mask).filterMap selected`
  - `liftedSubsetSelectedList d (J \ T) = (tail.zip mask).filterMap rest`

The subset is constructed as `insert (J.min' hne) tailSelected.toFinset` where
`tailSelected = (Js.tail.zip mask).filterMap (fun p => if p.2 then some p.1 else none)`
and `Js` is the J-filter index list.

Added two `private` helpers used by the main proof:

- `List.nodup_filter_mem_toFinset_zip_filterMap_selected`: for `Nodup xs` with
  matching mask, filtering xs by membership in `(xs.zip bs).filterMap selected`
  recovers the filterMap itself.
- `List.nodup_filter_not_mem_toFinset_zip_filterMap_rest`: the dual,
  recovering the rest filterMap when filtering by non-membership.

These are list/index bookkeeping only; no quotient, irreducibility, or
partition algebra. The proof reuses the existing local helpers
`finRange_filter_head?_eq_min'`,
`finRange_filter_mem_and_mem_eq_of_subset`,
`finRange_filter_mem_and_not_mem_eq_sdiff_of_subset`, and
`liftedSubsetSelectedList_eq_filter_map`, as requested by the issue.

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry` was
introduced.

## Current frontier

The matched lifted-factor mask-to-subset bridge is available for the successor
issue #4372 (subsetSplitsWithFirst prefix characterization), which combines it
with the mask converse from PR #4375 (#4370) to express arbitrary prefix splits
as lifted-factor subsets. With #4372 landed, the prefix-none lemma required by
the recursive coverage proof in #4301 becomes scopable.

Verification passed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- diff grep for new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry`

## Next step

A successor worker can claim #4372 and consume
`liftedSubsetSelectedList_eq_mask_partition_of_matches` together with
`subsetSplitsWithFirst_mem_exists_tail_mask` from PR #4375 to characterise
arbitrary executable prefix splits as lifted-factor-subset partitions.

## Blockers

None for this issue.
