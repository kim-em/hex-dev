# progress 20260517T193226Z_bdfc72e2

## Accomplished

Phase 6 polish slice for `HexGF2/Basic.lean` from `toWords` (line ~487) to
end-of-file (~1768), per issue #4788.

- Added caller-facing docstrings to public theorems missing them:
  - `isZero_eq_*` / `isZero_iff_eq_zero` / `degree_eq_of_degree?_eq_some`
    cluster (~525–584);
  - `eq_zero_of_isZero` / `isZero_of_eq_zero` / `degree?_eq_none_of_isZero`
    / `isZero_false_of_degree?_eq_some` / `ne_zero_of_degree?_eq_some`
    (~542–582);
  - `(0 : GF2Poly)` cluster: `words_zero`, `wordCount_zero`, `isZero_zero`,
    `coeff_zero`, `degree?_zero`, `degree_zero` (~838–860);
  - add-algebra cluster: `normalizeWords_replicate_zero`, `xorWords_self`,
    `add_self`, `zero_add`, `add_zero`, `add_comm`, `add_assoc`,
    `add_add_cancel_left`, `add_add_cancel_right` (~975–1044);
  - shift-left implementation lemmas used by `HexGF2/Multiply.lean`:
    `coeffWords_shiftLeftBitsList_same_word`,
    `coeffWords_shiftLeftBitsList_carry_word`,
    `coeffWords_shiftLeftBitsList_add`,
    `coeffWords_replicate_append_shiftLeftBitsList_add`,
    `coeffWords_replicate_append_add_of_mod_eq_zero`,
    `coeffWords_replicate_append_shiftLeftBitsList_add_of_not_word`,
    `coeffWords_replicate_append_shiftLeftBitsList_lt`.

- Tagged two unconditional canonical-form rewrites with `@[simp]`:
  - `coeff_monomial_self : (monomial n).coeff n = true`
  - `degree?_monomial : (monomial n).degree? = some n`

  Both rewrites have no side conditions and characterise the canonical
  behaviour of `monomial`. The new attribute made one downstream `simpa
  using coeff_monomial_self` in `HexGF2/Field.lean:284` redundant; that
  call was tightened to `by simp`.

- Did **not** tag `coeff_monomial`, `coeff_monomial_ne`, `words_monomial`,
  `coeff_mulXk`, `coeff_shiftLeft`, or the `wordCount_*_le` inequality
  cluster: their right-hand sides are not canonical normal forms, the
  side conditions involve free variables, or the LHS-vs-RHS direction is
  ambiguous for simp normalisation.

- No statement-level changes, no renames, no argument-order changes, no
  new `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME`.

## Current frontier

`HexGF2/Basic.lean` bitwise/shift/mul polish slice is complete and
PR-ready. Verification (Basic-only build, full HexGF2 module build, full
project `lake build`, `check_dag`, `git diff --check`) is clean.

The remaining Phase 6 work for `HexGF2` is `HexGF2/Multiply.lean`
(~6811 lines) and the existing `HexGF2/Euclid.lean` plus the downstream
`HexGF2/Field.lean` / `HexGF2/Irreducibility.lean` / etc.; this slice
does not address those.

## Next step

Land this PR; a planner can open a follow-up Phase 6 slice for
`HexGF2/Multiply.lean`.

## Blockers

None.
