# Session 8bf00d01 — /work

## Accomplished

- **Directive #2564 (HO-1 van Hoeij CLD)**: investigated as highest
  priority. Confirmed it is genuinely blocked, not workable this
  session. The headline five-clause theorem (`factor_headline`,
  `FactorSoundness.lean:205`) is already proved *modulo* one `sorry`:
  `factor_irreducible_of_nonUnit` (`FactorSoundness.lean:20`), plus the
  dependent `Hex.ZPoly.isIrreducible_iff` (`Basic.lean:235`). Those are
  the only two `sorry`s left in the whole BZ bridge. The remaining gap
  is the slow-modular exhaustive branch's *unconditional* irreducibility,
  which needs `HenselLiftDescentHypotheses` /
  `InitialLiftedFactorSubsetPartitionEvidence` discharged — these are
  everywhere *assumed*, nowhere proved (deep open analytic work, related
  to the blocked fast-path #6779). The directive also sits in a known
  coordination deadlock (backwards `depends-on #4818`), already
  documented exhaustively in prior #2564 comments (latest 2026-06-18).
  No new comment added — it would duplicate existing diagnosis.

- **#8144 (HexGFq inv_zero grind sweep)**: claimed, premise-checked,
  found unsound, skipped for replan with concrete build evidence.
  `inv_zero` is NOT a valid `grind =` candidate: `GFq p n h` reduces to
  `GFqField.FiniteField (modulus h) …`, so grind's LHS pattern collapses
  to the bare literal `@OfNat.ofNat (GFqField.FiniteField …) 0`, which
  cannot instantiate `n`/`h` → hard build error `invalid pattern(s)`.
  The issue's claimed precedent (GF2n `inv_zero`, #8124) does not
  transfer: GF2n's LHS keeps the `GF2n n …` type. No annotation-only
  fix; sole deliverable impossible. Suggested disposition: close as "no
  eligible candidate" (HexGFq/Basic.lean grind sweep already complete).

- **#8151 (HexPolyZ ratPolyPow grind sweep)**: completed. Promoted
  `ratPolyPow_zero` and `ratPolyPow_succ` (`HexPolyZ/Basic.lean:2893,
  2896`) to `@[simp, grind =]`. Both LHS heads are the opaque
  `ratPolyPow` carrying `d`/`n`, so valid E-matching patterns form (the
  failure mode that sank #8144 does not arise). Verified: `lake build
  HexPolyZ` green, `lake build HexPolyZMathlib` (CI-gated) green,
  `check_dag.py` exit 0, no axiom/sorry/native_decide added.

## Current frontier

HexPolyZ Phase 6 grind sweep is complete with this PR. The HO-1
directive remains the project's frozen critical path.

## Next step

PR for #8151. Planner should triage the #8144 replan (recommend close
as no-eligible-candidate). The #2564 critical path needs owner action
on the slow-modular Hensel correspondence and the dependency-cycle
edge; no worker session can advance it as-is.

## Blockers

#2564 / #4818: deep missing infra (unconditional slow-modular branch
irreducibility) + coordination deadlock. Not session-workable.
