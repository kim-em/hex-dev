# HexGF2 Phase 6 exit-criteria audit (#8215)

## Accomplished

Audited `HexGF2` against the four Phase 6 exit criteria, fixed the only
gating gaps inline, and bumped `libraries.yml[HexGF2].done_through` 5 → 6.

### Criterion-by-criterion verdict

1. **Mathlib linter clean — PASS (after fixes).** `runLinter` on the
   eight importable core modules reported 13 `docBlame` (missing-docstring)
   hits and a handful of `simpNF` findings. The 13 `docBlame` hits were
   genuine and are now fixed (see below); `runLinter` re-run is clean of
   `docBlame`. The `simpNF` findings ("simp can prove this" / LHS-not-
   normal-form on intentional `@[simp]` lemmas) are non-gating by project
   precedent: the passed sibling `HexConway` (`done_through 6`) exhibits 4
   of the same category, and the actual CI gate is the build-time linter,
   which is green with zero warnings.

2. **Docstring coverage — PASS (after fixes).** Authoritative source is
   `docBlame`. Fixed all 13 hits:
   - `Basic.lean:20` — `GF2Poly.words` field.
   - `Euclid.lean:264-266` — `XGCDResult.{gcd,left,right}` fields.
   - `Field.lean:367` — `GF2nPoly.val` field.
   - `Field.lean:376` — `GF2n.val` field.
   - `Field.lean` — `GF2n.pow.go` / `GF2nPoly.pow.go` square-and-multiply
     recursion helpers (documented via `add_decl_doc`; a `/-- -/` cannot
     precede a `let rec` in term position).
   - `Irreducibility.lean:125-126` — `RabinBezoutWitness.{left,right}`.
   - `Irreducibility.lean:134-136` — `IrreducibilityCertificate.{n,powChain,bezout}`.
   Note: a first-pass exploration over-reported ~30 "missing docstrings"
   in `Field.lean`; those are all typeclass `instance` declarations
   (`Zero`/`One`/`Add`/…), which are not in the `SPEC/design-principles.md`
   rule (`def`/`structure`/`class`/`inductive`) and are exempt by
   `docBlame`. They are not gaps.

3. **No dead/unreferenced declarations — PASS.** Reachability check over
   all `Hex*/` dirs (treating instances and `@[simp]`/`@[grind]` lemmas
   as live roots; private decls checked file-locally with non-qualified
   word-boundary matching) found no unreachable declarations. Every
   public decl has at least one downstream use or an automation tag.

4. **Native computational path + Lean-checked irreducibility — PASS.**
   Zero `native_decide`, `sorry`, or `axiom` in the library. The single
   extern boundary, `Clmul.lean`'s `@[extern "lean_hex_clmul_u64"] clmul`,
   carries a pure-Lean reference (`pureClmul`) and a Lean-checked equality
   theorem `clmul_eq_pureClmul`; the C shim is an optimization only, so
   the logical path is fully native. The Rabin irreducibility surface is
   Lean-checked: `Irreducibility.lean` proves the executable
   certificate-checker soundness up to `rabinTest`, and
   `RabinSoundness.lean` proves the Rabin-theorem layer from
   `rabinTest = true` to the packed irreducibility predicate — all genuine
   Lean proofs, no oracle.

## Verification

- `lake build HexGF2` green (52 jobs), zero warnings.
- `runLinter` on all eight core modules: `docBlame` clean.
- `python3 scripts/check_dag.py` exit 0.
- `python3 scripts/status.py` now lists `HexGF2 -> Phase 7`.

## Current frontier

`HexGF2` Phase 6 complete; eligible for Phase 7 (reference chapter).
`HexGFq` (#8220) is the remaining un-audited active computational library.

## Next step

A planner can file the `HexGF2` Phase 7 reference-chapter feature issue.

## Blockers

None.
