# 20260516T053704Z — session 92d2cd97 (feature, partial)

## Accomplished

Landed prerequisite infrastructure for issue #4393 (HexBZ Mathlib:
derive `QuadraticMultifactorLiftInvariant` from `choosePrimeData`
boundary inputs). The committed commit `8011fc3` adds three new public
theorems:

- `Hex.ZPoly.quadraticHenselStep_factor_congr_mod_base` in
  `HexHensel/Quadratic.lean`: after one quadratic step at modulus
  `m`, both `r.g ≡ g mod m` and `r.h ≡ h mod m`. Only hypothesis is
  `congr (g * h) f m`.
- `Hex.ZPoly.iterateQuadraticHensel_h_congr_mod_base` (private) in
  `HexHensel/QuadraticMultifactor.lean`: through the full doubling
  loop, `looped.h ≡ initial.h mod p`. Induction on fuel using the
  per-step lemma.
- `Hex.ZPoly.henselLiftQuadratic_h_congr_mod_base` (public) in
  `HexHensel/QuadraticMultifactor.lean`: full lift cofactor
  `(henselLiftQuadratic p k f g h s t).h ≡ h mod p`. This is the
  surface the upcoming companion theorem needs to propagate the
  mod-`p` product hypothesis across the recursive split.

`lake build HexHensel HexBerlekampZassenhaus.Basic
HexBerlekampZassenhausMathlib.Basic` is green; the pre-existing
`sorry`s in `HexBerlekampZassenhausMathlib/Basic.lean` (lines 160,
239) and `HexBerlekampZassenhaus/Basic.lean` (line 5647) are untouched
by this PR.

## Current frontier

The deliverables in the parent issue beyond this infrastructure are
still open:

1. The main companion theorem
   `Hex.ZPoly.quadraticMultifactorLiftInvariant_of_factorsModP` —
   takes boundary inputs (`Monic f`, factors monic over `Fp`,
   `polyProduct factors.liftToZ ≡ f mod p`, plus a coprimality /
   Bezout side condition) and concludes
   `QuadraticMultifactorLiftInvariant p k f (factors.map liftToZ)`.
   Requires:
   - `Hex.FpPoly.monic_liftToZ_of_monic` (small new lemma).
   - The recursive proof structure using
     `henselLiftQuadratic_h_congr_mod_base` (this PR) and
     `henselLiftQuadratic_h_monic` (already exists) to thread the
     `(Monic f, polyProduct factors.liftToZ ≡ f mod p)` package
     across each split.
   - The empty-list case argument: `Monic f + congr 1 f p + 1 < p`
     forces `f = 1`, hence `congr 1 f (p^k)`.
   - Choosing the exact form of the coprimality hypothesis (the
     issue body leaves this open — natural choice is
     `factors.Pairwise (fun a b => gcd a b = 1)` over `Fp`,
     combined with a lemma connecting that to `normalizedXGCD`).
2. `Hex.ZPoly.QuadraticMultifactorLiftInvariant_of_choosePrimeData`
   convenience wrapper in `HexBerlekampZassenhaus/Basic.lean` —
   specialises the companion to the
   `(primeData : Hex.PrimeChoiceData)` boundary using existing
   `choosePrimeData?_isGoodPrime` /
   `choosePrimeData?_factorsModP_berlekamp_form` provenance.
3. End-to-end consumer combinator in
   `HexBerlekampZassenhausMathlib/Basic.lean` that chains the
   convenience wrapper from (2) with
   `henselLiftData_liftedFactor_monic` (already in tree, PR #4390) to
   produce `∀ i, Monic (liftedFactor d i)` directly from
   `Monic core` plus a `choosePrimeData`-boundary hypothesis package.

## Next step

Submit a partial PR for this infrastructure against #4393 with
`coordination create-pr 4393 --partial`. The planner will narrow the
remaining scope on the parent (or open the obvious follow-up
sub-issue) once this is merged.

If the planner prefers explicit sub-issue creation up front, a clean
3-way split is:

- Sub-issue B: prove the companion theorem (depends on this PR's
  infrastructure).
- Sub-issue C: `_of_choosePrimeData` convenience wrapper (depends on
  B).
- Sub-issue D: end-to-end consumer in `HexBerlekampZassenhausMathlib`
  (depends on C).

## Blockers

None for the committed infrastructure. The remaining companion
theorem is genuinely non-trivial — the coprimality hypothesis form,
the empty-list `Monic f` case, and the recursive plumbing all need
careful design. Treat as its own focused issue rather than rolling it
into a single mega-PR.
