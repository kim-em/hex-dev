# 20260602T144205Z 542bc722 — Issue #6326

## Accomplished

- Claimed feature issue #6326.
- Added conservative automation attributes to the packed quotient-field wrapper
  surface in `HexGF2/Field.lean`:
  - `@[simp]` for representative-value normal forms and zero/one reductions.
  - `@[grind =]` for division and nonzero inverse-cancellation wrappers.
  - `@[grind =>]` for Frobenius preservation and fixed-element closure.
- Added proof-mode conformance examples in `HexGF2/Conformance.lean` for
  small-word `GF2n` division/cancellation and polynomial-backed `GF2nPoly`
  value normalization, characteristic-two zero/cancellation facts, division,
  inverse cancellation, and Frobenius closure.
- Updated the `GF2nPoly.mul_inv_cancel` proof to use the new
  `reducePoly_val_eq_mod` simp normal form.

## Current frontier

`GF2n` and `GF2nPoly` wrapper facts now have automation tags that downstream
proofs can consume directly, with conformance examples covering the intended
`simp` and `grind` surfaces.

## Next step

Downstream Phase 6 work can use the tagged quotient-field wrapper facts without
manually naming the basic division, inverse-cancellation, and Frobenius lemmas.

## Blockers

- None.

## Verification

- `lake build HexGF2.Field` — passed.
- `lake build HexGF2.Conformance` — passed.
- `lake build HexGF2` — passed.
- `python3 scripts/check_dag.py` — passed.
- `git diff --check -- HexGF2/Field.lean HexGF2/Conformance.lean` — passed.
- Added-line scan over touched Lean files for `sorry` / `axiom` /
  `native_decide` / `TODO` / `FIXME` — no matches.
- Repository Lean grep counts remain `sorry=84`, `axiom=0`,
  `native_decide=2`.
