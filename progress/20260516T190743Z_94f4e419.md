# 2026-05-16T19:07Z — feature session 94f4e419 (#4274)

## Accomplished

- Proved the final public coverage theorem `#4274` in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`.
- The theorem composes the recursive recombination-search coverage capstone
  (`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`,
  from #4524) with the public-wrapper membership bridge
  (`exhaustiveCoreFactorsWithBound_mem_of_recombinationSearchMod_some`) at
  `J = Finset.univ`, `target = core`,
  `localFactors = d.liftedFactors.toList`, and
  `fuel = d.liftedFactors.toList.length + 1` to recover the
  `Hex.recombinationSearchMod` shape.
- Hypothesis adjustments versus the issue body's draft signature:
  - **Added** `hpartition : LiftedFactorSubsetPartition core d Finset.univ core`
    as a hypothesis. The initial-state constructor for
    `LiftedFactorSubsetPartition` is out of scope (cf. the structure's
    doc-comment), and the recursive capstone is phrased in terms of this
    predicate. Threading it as a hypothesis is the minimal change required.
  - **Added** `hB_ne_zero : Hex.ZPoly.defaultFactorCoeffBound core ≠ 0`,
    required by the public-wrapper bridge so that
    `exhaustiveCoreFactorsWithBound` runs the recombination path rather than
    returning `#[core]`.
  - **Added** the lifted-factor side hypotheses required by the recursive
    capstone: `hd_modulus`, `hd_liftedFactor_monic`,
    `hd_liftedFactor_natDegree_pos`, `hd_liftedFactor_inj`.
  - **Dropped** `hcore_record`, `hfactor_prim`, `hfactor_norm`,
    `hfactor_monic`, `hfactor_degree` from the issue's draft signature: none
    are needed for this assembly because the recursive capstone consumes
    `LiftedFactorSubsetPartition` directly. The issue says "without
    strengthening the public API beyond what is necessary"; the recursive
    capstone discharges those normalisation facts internally via
    `monic_primitive_sign_normalized_of_monic` at each recursive step.
  - **Corrected** the conclusion's outer `B` parameter of
    `Hex.exhaustiveCoreFactorsWithBound`: the issue body draft used
    `precisionForCoeffBound (defaultFactorCoeffBound core) primeData.p`, but
    that is the precision exponent (used inside
    `HenselSubsetCorrespondenceHypotheses`), not the raw coefficient bound
    the wrapper accepts. The corrected form
    `defaultFactorCoeffBound core` matches the inner Hensel lift call
    `henselLiftData core (precisionForCoeffBound B primeData.p) primeData`
    and aligns with how every other public theorem in the file references
    the wrapper.

## Current frontier

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `lake build HexBerlekampZassenhaus` ✓ (no changes downstream)
- `python3 scripts/check_dag.py` ✓ (silent pass)
- `git diff --check` ✓
- Sorry inventory in `HexBerlekampZassenhausMathlib/Basic.lean` unchanged at
  2 (pre-existing on lines 160 and 239); no new `axiom`, `native_decide`,
  `TODO`, `FIXME`, or `sorry` introduced by this PR.

## Next step

- The natural follow-ups are the two pieces this issue's deliverable
  delegates upstream:
  1. The initial-state constructor `LiftedFactorSubsetPartition core d
     Finset.univ core` from `HenselSubsetCorrespondenceHypotheses` plus a
     square-free reduction hypothesis (mentioned in
     `LiftedFactorSubsetPartition`'s doc-comment as a separable future
     issue).
  2. Helpers producing the lifted-factor side facts
     (`hd_liftedFactor_natDegree_pos`, `hd_liftedFactor_inj`) from
     Hensel-lift invariants; per-output monicness already has the
     `henselLiftData_liftedFactor_monic_of_choosePrimeData` wrapper.
- Once those land, the final count equality assembly (#4128) can compose
  this theorem with the existing count-le / count-ge lemmas to close the
  HexBZ Mathlib bridge for monic core polynomials.

## Blockers

- None.
