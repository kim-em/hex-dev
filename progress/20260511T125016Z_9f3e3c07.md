# 2026-05-11 12:50 UTC — issue #3422

## Accomplished

Skipped stale #3418 after confirming its required local Yun API is only on
open PR #3419, not on `origin/main`.

Claimed #3422 and hardened `scripts/oracle/setup_lll_isabelle.sh` against
transient Zenodo failures. The script now verifies cached archives through a
shared checksum helper, discards a corrupt cached archive before re-downloading,
verifies temporary downloads before promotion, and uses a bounded larger curl
retry window for transient download failures.

Verified:

- `bash -n scripts/oracle/setup_lll_isabelle.sh`
- `HEX_ORACLE_CACHE="$(mktemp -d)" scripts/oracle/setup_lll_isabelle.sh`
- `HEX_LLL_ISABELLE_SVP="$(HEX_ORACLE_CACHE="$tmp_cache" scripts/oracle/setup_lll_isabelle.sh)" lake exe hexlll_bench verify`
- `python3 scripts/check_dag.py`
- `git diff --check`

The `hexlll_bench verify` rerun reproduced several `curl: (22)` Zenodo 504s,
then recovered under the new retry settings and passed all 32 benchmarks.

## Current frontier

The LLL Isabelle oracle setup path is more resilient without changing the
oracle contract or CI workflow shape.

## Next step

Create the PR for #3422 and let CI confirm the same recovery path in the
ubuntu smoke gate.

## Blockers

None for #3422. Issue #3418 remains blocked/stale until PR #3419 lands or its
dependency is represented as a PR dependency rather than a closed issue.
