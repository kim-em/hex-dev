# 20260517T115615Z — work session c22b9d0c (#4670)

## Accomplished

Claimed #4646 first after all owner directives were either blocked or marked
for replan. Released #4646 as stale because its requested primitive helper
variants still target the unscaled `recombinationCandidate`, while its
predecessor #4652 established that the primitive non-monic recovery path must
use `scaledRecombinationCandidate`.

Claimed #4670 and added concrete executable BHKS bad-vector packaging in
`HexBerlekampZassenhausMathlib/BadVector.lean`:

- `ExecutableBadVectorWitness.ofProjectedVector` builds a witness for a fixed
  projected vector using the executable BHKS lattice/projected rows, computes
  `H` with `BHKS.auxiliaryPolynomial`, and reads the selected local-factor
  degree from the chosen lifted factor.
- `ExecutableBadVectorWitness.isBhksBadVectorSetup_of_projectedVector`
  packages a vector in `L' \ W` into `IsBhksBadVectorSetup`, discharging the
  canonical auxiliary-polynomial equality and threading the executable selected
  degree. The remaining positive-degree, coprimality, and resultant
  divisibility clauses stay explicit for later BHKS Lemma 3.2 work.

Verified:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib.TerminationBound` (succeeded as
  part of the sequential Recovery build after an earlier parallel Lake artifact
  race)
- `lake build HexBerlekampZassenhausMathlib.Recovery`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/BadVector.lean`
  returned no hits

## Current frontier

#4670 now has a concrete fixed-vector bridge from executable projected vectors
to the existing bad-vector setup record. The universal
`ProjectedBadVectorSetupBridge` callback is still intentionally supplied by
callers; constructing it from failed-recovery data requires choosing or
deriving the bad vector uniformly for every `v ∈ L' \ W`.

## Next step

Continue HO-4 by proving the remaining BHKS Lemma 3.2 algebraic clauses for
the concrete witness: positivity of the selected lifted-factor degree from the
executable local-factor data, rational coprimality of the input and auxiliary
polynomial, and the `p^(k*d)` resultant divisibility.

## Blockers

None for the fixed-vector bridge landed here. The full cap-separation callback
still depends on the substantive BHKS resultant argument.
