# 20260603T215653Z work #2567

## Accomplished

Added next-step (1) "Integer-vs-real coefficient bridge" from
`progress/20260603T211754Z_7bade7d4.md`: a sibling factored cap-arithmetic
chain whose LHS coefficient factor is the real `‖core‖₂^aux.natDegree`
instead of the integer `coeffL2NormBound^aux.natDegree`. This matches the
direction of the BHKS paper threshold factor
`bhksPaperCoeffNormFactorReal core = ‖core‖₂^(2n-1)`, so the recently
proven composite
`l2norm_pow_auxiliaryPolynomialWithCorrections_natDegree_le_bhksPaperCoeffNormFactorReal`
(`HexBerlekampZassenhausMathlib/TerminationBound.lean`) discharges the
`h_coeff` hypothesis outright for any nonzero source polynomial.

In `HexBerlekampZassenhausMathlib/Recovery.lean`:

* `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_l2norm` —
  unfactored l2-norm-LHS sibling. Same chain steps (paper threshold ≤
  factorFastPrecisionCap, cap < `p^k`, `p^k ≤ p^(k·d)`) with the LHS
  coefficient factor replaced by `‖core‖₂^aux.natDegree`. Output strict
  inequality matches the shape consumed by
  `FactorFastCapLiftAnalyticComparison.ofL2normBounds` with the input
  bound chosen as `inputBound := ‖core‖₂` and `hinput := le_refl _`.

* `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_l2norm_factored`
  — factored l2-norm-LHS sibling. Identical hypotheses to the integer
  factored chain (auxiliary nonneg, `h_coeff`, `h_aux`) but with the
  l2-norm-shaped `h_coeff`. Composes the new unfactored chain with
  `bhksPaperThresholdReal_ge_of_factored_bounds`, just like the integer
  factored chain.

## Current frontier

Both LHS shapes of the BHKS Theorem 5.2 paper-threshold strict comparison
are now exposed by the cap-arithmetic chain layer:

* the integer-LHS form
  (`bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree[_factored]`,
  using `coeffL2NormBound^aux.natDegree`), and
* the real-l2-norm-LHS form
  (`bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_l2norm[_factored]`,
  using `‖core‖₂^aux.natDegree`).

The factored l2-norm chain accepts `h_coeff` in exactly the shape that
`l2norm_pow_auxiliaryPolynomialWithCorrections_natDegree_le_bhksPaperCoeffNormFactorReal`
(from `HexBerlekampZassenhausMathlib/TerminationBound.lean`) discharges
outright for any nonzero source, closing the coefficient-power sub-bound
on the real-l2-norm axis.

The remaining analytic open piece is the `h_aux` sub-bound itself: the
genuine BHKS Lemma 3.2 / Theorem 5.2 content
`auxiliaryBound^input.natDegree ≤
n · (2C)^(n²) · (log ‖core‖₂)^n`.

## Next step

Independent options:

1. **Auxiliary-power sub-bound.** Prove the BHKS Lemma 3.2 / Theorem 5.2
   inequality `auxiliaryBound^input.natDegree ≤
   bhksPaperDegreeFactorReal core * bhksPaperConstantFactorReal core C *
   bhksPaperLogFactorReal core` for the cap-lift auxiliary polynomial.
   This is the substantive analytic content that remains.

2. **Downstream l2-norm cap-separation constructors.** Build sibling
   `FactorFastCapSeparationInputs.ofBridgeDataXxxAndL2normFactoredPaperThreshold`
   constructors (and matching `factorFast_terminates_*` wrappers) that
   thread through the new l2-norm chain via
   `FactorFastCapLiftAnalyticComparison.ofL2normBounds` rather than
   `l2norm_product_lt_divisor_of_auxiliary_bound`. This exposes the
   real-l2-norm axis at the final `factorFast f ≠ none` consumer.

## Blockers

None. The new chain lemmas are pure compositions over existing
infrastructure
(`bhksPaperThresholdReal_le_factorFastPrecisionCap`,
`bhksPaperThresholdReal_ge_of_factored_bounds`,
`Hex.choosePrimeData?_prime`, `Hex.precisionForCoeffBound_spec`).

## Verification

- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus`
  passed (3780 jobs, no new warnings introduced by this change).
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing on the new lemmas.
- Diff: 197 added lines in `HexBerlekampZassenhausMathlib/Recovery.lean`,
  zero deletions (`git diff --stat` confirms 197 + 0).
- No changes to `factor`, `factorFast`, `factorSlow`, or any existing
  theorem statement; the two new chain lemmas are strictly additive.
