# Session 5100afc8 — review #5090 (post-#5084 cluster inversion audit)

## Accomplished

- Claimed and audited #5090: post-#5084 audit of the
  `monic_primitive_sign_normalized_of_monic` cluster inversion.
- Audit target tree: commit
  `f0463005f9b540648a8e07efadcb4b30840973e7` (#5084 → main as #5089).
- Posted three-invariant audit comment at
  https://github.com/kim-em/hex/issues/5090#issuecomment-4482176579.
- Verdict: PASS on all three invariants.
- Evidence enumerated:
  - 14 external helper call sites compile unchanged
    (`zpoly_primitive_of_monic`: 9 sites — 7 in `Basic.lean`, 2 in
    `IntReductionMod.lean`; `zpoly_normalize_factor_sign_of_monic`:
    5 sites, all in `Basic.lean`).
  - Umbrella `monic_primitive_sign_normalized_of_monic` collapsed
    to a single term-mode anonymous-constructor expression at
    `Basic.lean:4501-4508`, zero direct callers, 3 docstring
    references intact.
  - Hoist band correct: `zpoly_size_pos_of_monic` (4442) →
    `zpoly_primitive_of_monic` (4456) →
    `zpoly_normalize_factor_sign_of_monic` (4477) →
    `monic_primitive_sign_normalized_of_monic` (4501).
  - Helper bodies are faithful branch-by-branch extractions from
    the pre-#5084 umbrella body; umbrella docstring identical
    (`diff` of pre/post text is empty).
  - Cross-file: exactly one declaration of each of the three
    names in `Basic.lean`; zero `(private theorem|theorem)`-form
    matches in `IntReductionMod.lean` or `Recovery.lean`.
- Flagged two planner-spec inaccuracies (audit notes, not
  worker deviations):
  - Issue body cited "13 call sites" with 12 enumerated Basic.lean
    line numbers; the actual count is 14 external sites (12 Basic +
    2 IntReductionMod for `zpoly_primitive_of_monic`).
  - Issue body said helpers should be `private theorem` "matching the
    pre-#5084 corollary form"; pre-#5084 form was `theorem` (not
    `private theorem`), and post-#5084 is `theorem`, so the
    *intent* (preserve pre-#5084 visibility) is satisfied. `private`
    would have broken the build due to the IntReductionMod cross-file
    callers.

## Current frontier

The Monic-route ZPoly-layer cluster is closed. The cluster-cleanup
wave's next direction (per the out-of-scope observations in the
audit comment) is FpPoly-layer helpers — `fpPoly_dvd_trans`
extraction landed (#5083 / #5082 audit), `fpPoly_mul_dvd_mul` is
unclaimed (#5091), `fpPoly_leadingCoeff_ne_zero_of_size_pos` is
claimed (#5088), and `fpPoly_isZero_false_of_dvd_of_isZero_false`
has an open PR with conflicts (#5081 / PR #5086).

## Next step

No follow-up for this audit cluster. Subsequent reviewers on the
BZMathlib wave should pick up #5091 (`fpPoly_mul_dvd_mul`
extraction) or any post-#5081 audit issue once #5081's PR lands.

## Blockers

None. Audit comment posted, issue ready for closure by the
reviewer-comment-driven workflow.
