# Session 1b366742 — #4373 monic cofactor helper

Session: `1b366742`
Issue: #4373

## Accomplished

Claimed and executed issue #4373 after the directive queue entries were either
blocked or marked as needing replan.

Added `monic_of_congr_mul_monic_monic` in
`HexBerlekampZassenhausMathlib/Basic.lean`. The lemma proves that a nonzero
cofactor `h` is monic when `g * h` is coefficientwise congruent to a monic
target `f` modulo `m`, `g` is monic, `1 < m`, and every coefficient of `h` is
bounded in `[0, m)`.

Added `monic_reduceModPow_of_congr_mul_monic_monic`, the companion wrapper for
`Hex.ZPoly.reduceModPow`, deriving the coefficient bounds from
`coeff_reduceModPow_eq_emod_of_pos`.

No new `axiom`, `native_decide`, `TODO`, `FIXME`, or theorem-level `sorry` was
introduced.

## Current frontier

The foundational cofactor-monic argument requested by #4373 is available for
the multifactor-lift monicness chain. The next issue in that chain can wire the
`reduceModPow` wrapper through the relevant `multifactorLiftQuadratic` output
contracts.

Verification passed:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhaus`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- diff grep for new `axiom`, `native_decide`, `TODO`, `FIXME`, or `sorry`

## Next step

Use `monic_reduceModPow_of_congr_mul_monic_monic` in the blocked successor that
proves every `multifactorLiftQuadratic` output is monic, then continue through
the `henselLiftData_liftedFactor_monic` umbrella wrapper.

## Blockers

None for this issue.
