# Berlekamp successful split API

## Accomplished

- Claimed #2097 first, verified it was stale because the residual-zero theorem it needs is not present on `origin/main`, and skipped it for replan with the open Matrix dependency chain noted.
- Claimed feature issue #2123.
- Proved `kernelWitnessSplit_product_spec` in `HexBerlekamp/Factor.lean` by induction over `kernelWitnessSplitAux`, using the gcd divisibility law plus finite-field division reconstruction for the recorded cofactor.
- Proved `kernelWitnessSplit_nontrivial` from the successful `isNontrivialSplitFactor` branch of the same search.

## Current frontier

The immediate successful-split API gaps in `HexBerlekamp/Factor.lean` are closed. The broader factorization product theorem remains as the only `sorry` in that file and is explicitly downstream/out of scope for #2123.

## Next step

Use the split product/nontrivial API to prove the loop invariant for `prod_berlekampFactor`.

## Blockers

- `kernelWitnessSplit_product_spec` now requires `[ZMod64.PrimeModulus p]`, matching the finite-field division law needed to prove the executable quotient cofactor reconstructs `f`.
- `lake build HexBerlekamp` succeeds but still reports pre-existing/out-of-scope `sorry` warnings in dependencies and downstream Berlekamp files, including `prod_berlekampFactor`.
