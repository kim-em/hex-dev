# Work session: issue #3671

## Accomplished

- Claimed human-oversight issue #3671.
- Extended `scripts/oracle/flint_bench_driver.py` with the `fmpz_poly`
  operations needed by HexPoly integer-input targets: subtraction,
  derivative, composition, content, and primitive part.
- Wired `HexPoly/Bench.lean` to the shared FLINT driver and added fixed
  representative Lean/FLINT comparator pairs for addition, subtraction,
  multiplication, composition, derivative, content, and primitive part.
- Densified the HexPoly integer-input parameter ladders for the affected
  scientific registrations.
- Regenerated `reports/hex-poly-performance.md` comparator-ratio evidence and
  committed the fixed comparator export artefact.

## Current frontier

The FLINT comparator surface is wired and locally verified. The full fixed
ladder matrix was not kept because listing/verifying a large fixed IO matrix
overflowed the current bench registry; the report records this limitation and
uses representative fixed rows.

## Next step

Create the PR for #3671 and let CI validate the new bench wiring.

## Blockers

None.
