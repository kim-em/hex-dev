# Progress - 2026-06-16 (session e195f125, work #7628)

## Accomplished

Claimed #7628 and added the generic prefix row-span helper package in
`HexLLLMathlib/Basic.lean`:
- `prefixSubmodule`, the submodule generated by rows with index `< t`.
- `row_mem_prefixSubmodule` and `prefixSubmodule_le_latticeSubmodule`.
- `rowCombination_mem_prefixSubmodule` for Mathlib finite linear combinations
  whose coefficients vanish outside the prefix.
- `rowCombination_mem_prefixSubmodule_of_vector` and
  `prefixRowCombination_memLattice` for executable `Hex.Matrix.rowCombination`
  consumers.

Verification:
- `lake build HexLLLMathlib` succeeded.
- `lake build HexBerlekampZassenhausMathlib.Lattice` succeeded during the first
  pass before the redundant import experiment was reverted.
- `python3 scripts/check_dag.py` succeeded.
- `git diff --check` succeeded.
- Added-line forbidden-token scan over touched Lean code found no `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

Second-opinion review of the initial diff found the lemmas sound and suggested
tightening the executable wrapper. The final API uses the direct executable
`Hex.Matrix.rowCombination` form. A follow-up second-opinion run on the revised
diff wedged and was terminated without output.

## Current frontier

The reusable algebraic support step for #7620 is available in `HexLLLMathlib`.
It does not prove the Gram-Schmidt survivor theorem or assemble BHKS
`CutProjectionHypotheses`.

## Next step

Claim #7620 and use these prefix row-span lemmas in the full-lattice survivor
span proof: after the largest nonzero reduced-basis coefficient is shown below
the executable prefix cut, convert the row combination into membership in
`prefixSubmodule`, then project the retained rows to the BHKS first block.

## Blockers

None for #7628. The worktree still contains a pre-existing unrelated
`.claude/CLAUDE.md` modification, not touched or staged.
