# Progress: #6758 — extend certified harsh-cubic ladder to n = 60, 65

## Accomplished

Registered `runCertifiedFirstShortVectorHarshCubic{60,65}Checksum` and
`runCertifiedCheckerHarshCubic{60,65}Checksum` in `HexLLL/Bench.lean`,
following the 50/55 fixed-target pattern, with paired
`certifiedHarshCubic{60,65}Ref` caches. Extended the
`runCertifiedCheckerIntervalTally` expectation from 7 to 9
interval-dispatched rungs (both new rungs route to the interval pass);
exact-primary stays 10, indecision fallback stays 0. Hash pin updated
and verified.

Ran the full certified harsh-cubic ladder (15–65) on `carica` with the
`fplll-ffi` provider in one sweep, committed as
`reports/bench-results/hex-lll-certified-harsh-extended-1e6679ff.json`
(SHA-256 `ccaec1be0a5209f63387a8b2af0f093274f06d6518323f631093ad82288ee6cb`,
commit 1e6679ff, not dirty). All 15–55 result hashes match the committed
`hex-lll-certified-c3d2fecb.json` export exactly; rejection 0/22.

Repointed the comparator harsh-cubic family's Lean-certified source to the
new export (random-bounded stays c3d2fecb), regenerated both comparator
figures (only harsh-cubic changed), and regenerated the scaling report
harsh-cubic block via `hex-lll-scaling.py` (window kept at 40–55).

Updated `reports/hex-lll-performance.md`: certified-vs-native and
certified-vs-Isabelle harsh-cubic ratio tables extended to n=65, trend
prose, export references + SHA-256 + rejection counts, registration lists,
gating verdict, and the `~n^2.79` exponent. Updated
`reports/hex-lll-scaling.md` harsh-cubic block.

## New-rung medians (carica, 1e6679ff)

- n=60: certified full 45.38 ms, checker 41.39 ms, vs native 381.56 ms →
  ratio 0.119 (8.4× faster); vs Isabelle certified 628.55 ms → 13.85×.
- n=65: certified full 56.10 ms, checker 55.04 ms, vs native 621.32 ms →
  ratio 0.090 (11.1× faster); vs Isabelle certified 1.01 s → 18.08×.
- Reducedness tally: interval 9, exactPrimary 10, indecision fallback 0.
- Rejection 0/22; all rows repeat-stable.

All Go/no-go sanity gates pass: fallback 0 at both new rungs, certified
median well below native, rejection 0, 15–55 hashes unchanged.

## Current frontier

`lake build HexLLL HexLLLMathlib`, `lake exe hexlll_bench verify` (153/153),
`python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token
grep all pass.

## Next step

Second opinion, then open the PR.

## Blockers

None.
