# PNT+ natural-number logarithm table

## Accomplished

- Audited the post-Table10 source-pinned inventory on exact main
  `c34048d8f603b043b809adcdb93676dfe9f44b7d`. The largest pending
  non-small-prime numerical cluster is `IEANTN/LogTables.lean`; the twelve
  remaining natural-number rows form one coherent independent family.
- Added one bounded Mathlib-free package for the 24 exact declarations
  `log_n_gt`/`log_n_lt` at
  `n = 3, 5, 7, 10, 11, 13, 17, 19, 23, 29, 30, 32`. Its payload
  authenticates the exact source, dyadic shift, reduced atanh parameter,
  eight-term count, decimal scale, and both source endpoints.
- Added one ordinary-kernel companion using checked `log 2` bounds, a finite
  atanh lower sum, an explicit geometric tail, and exact dyadic range
  reduction. It exports all 24 source-shaped theorems through one generic
  certificate theorem.
- Exercised every row through the generic package runtime and row 29 through
  chronology, schema replay, and `ProofFrontend`. Added fail-closed guards for
  an unlisted source, insufficient terms, a wrong reduction, cross-row replay,
  a wrong window, and a mathematically false endpoint with an ordinary
  refutation.
- Added guarded axiom reports for the first and last source wrappers and the
  frontend theorem; each reports only `propext`, `Classical.choice`, and
  `Quot.sound`.
- Reclassified exactly the 24 pinned inventory records as accepted after a
  localized PNT+ rewrite and documented the fixed-source limitations in both
  interval SPECs. Exact source regeneration against PNT+ commit
  `21998bb6196b56789f72a52656a781a75e134eb0` passes.
- Built the focused runtime, Mathlib, and conformance targets, the complete
  Mathlib-free experiment library, and the retained PNT proof targets. Passed
  inventory tests/check/source verification, copyright, line-count, DAG,
  Phase-4, trust-surface, release-manifest, Mathlib-free bench, freshness,
  diff-hygiene, and banned-mechanism checks.

## Current frontier

The 24 natural-number `LogTables` tactic sites now have package-owned
ordinary-kernel replacements. Exactly 108 actual tactic sites in that source
file remain pending. This slice does not claim arbitrary-input logarithms,
precision-selected endpoint generation, persistent tables, or LeanCert API
compatibility.

## Next step

Preserve this local commit for publication sequencing. A later follow-up should
audit a distinct remaining `LogTables` family with a shared analytic identity;
the Li(2) interface should remain pending until Hex owns the required
integrand, positivity, boundedness, value, and integral bounds rather than
importing admitted LeanCert public theorems.

## Blockers

The full combined `HexIntervalMathlibExperiment` build was stopped when the
host filesystem fell to roughly 300 MiB free while Lake attempted to populate
unrelated polynomial-dispatch/whole-Mathlib artifacts in this fresh worktree.
The feature and retained PNT targets are green, and all static/trust/source
gates pass. There is no theorem or API blocker for this completed slice.
