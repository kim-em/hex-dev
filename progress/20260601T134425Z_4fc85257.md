# 4fc85257 - feature #5997

## Accomplished

- Claimed #5997 after blocked directive claims were rejected by coordination.
- Added `matrixEquiv_deleteRowCol` in `HexMatrixMathlib/Determinant/Core.lean`.
- Added endpoint Hex-minor Desnanot-Jacobi theorem `desnanot_jacobi_deleteRowCol_endpoints`.
- Added arbitrary row/column reindexed Mathlib bridge theorem `desnanot_jacobi_matrixEquiv_reindex`, keeping the proof in `HexMatrixMathlib`.
- Verified with `lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan over the touched Lean diff.

## Current frontier

#5997 now exposes a reusable bridge-layer Desnanot-Jacobi substrate independent of the Plucker-specific `nMatrix`/`nDet` definitions. Downstream one-row and two-row replacement issues can choose row and column equivalences to put their arbitrary rows/columns at `0` and `Fin.last`, then consume the endpoint Hex-minor form.

## Next step

Use this substrate in #5998 to derive the one-row replacement cofactor bridge.

## Blockers

- The top-level directive issues listed by `coordination orient` could not be claimed because coordination reported unresolved dependencies.
- A pre-existing `.claude/CLAUDE.md` worktree modification was left untouched.
