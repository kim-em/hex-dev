# Progress — 2026-06-15 21:09 UTC (session 7e7a14ad, work/planning)

## Accomplished

Claimed issue #7503 and validated its proposed decomposition against the
current `HexBerlekampZassenhausMathlib/Basic.lean`.

The requested recovered/dilate executable correctness chain is already present:
`RecoveredScaledSearch.prefixNone_of_bound`,
`RecoveredScaledSearch.covers_of_bound`,
`scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_bound`,
and `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence_of_bound`
all route the executable `Hex.scaledRecombinationSearchModAux` through
`liftedRecoveryCandidate`.

Created successor issue #7525 for the validated residual work: retire the stale
`support_subset_of_dvd_scaledRecombinationCandidate` field from
`LiftedFactorSubsetPartition`, update transports/constructors, and delete the
now-dead scale-coordinate support and prefix wrappers.

Commented on #7503 with the validation result and the #7525 breadcrumb.

## Current frontier

The migration no longer needs a chain-building issue. The live frontier is
#7525: remove the remaining scaled-candidate support field and constructor
argument surface while preserving the existing recovered-coordinate chain.

## Next step

Pick up #7525 as a feature issue and run the full Lean verification after
field removal:

- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`

## Blockers

None.
