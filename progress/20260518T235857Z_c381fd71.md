# 2026-05-18T23:58Z — feature session, issue #5172

## Accomplished

- Added three conditional `monomial` API wrappers to
  `HexPoly/Dense.lean`:
  - `degree?_monomial_of_ne_zero` — `(monomial n c).degree? = some n`
    when `c ≠ 0`.
  - `degree?_monomial_getD_of_ne_zero` — `(monomial n c).degree?.getD 0 = n`
    when `c ≠ 0`.
  - `monomial_ne_zero_of_ne_zero` — `monomial n c ≠ 0` when `c ≠ 0`.
- All three are docstringed, non-`@[simp]` (carry the `c ≠ 0` side
  condition, matching the sibling `size_monomial_of_ne_zero` and
  `isZero_monomial_eq_false_of_ne_zero` style).
- Proofs are one-/two-liners reusing `size_monomial_of_ne_zero`,
  `degree?_eq_some_of_pos_size`, and `size_zero` — no new imports.
- Verified `lake build HexPoly.Dense`, `lake build HexPoly`, full
  `lake build`, `python3 scripts/check_dag.py`, `git diff --check`,
  and the banned-token grep all clean.

## Placement deviation

The issue specified placement at line 408 (after
`isZero_monomial_eq_false_of_ne_zero`). The natural proof of the
first lemma calls `degree?_eq_some_of_pos_size` (line 429), so
placing the cluster at line 408 produces an `Unknown identifier`
error. Per `Dependency Planning and Code Organization` in the project
conventions ("reorder definitions, don't inline"), the new lemmas
were placed immediately after `degree?_eq_some_of_pos_size` instead.
This keeps the helper-then-consumer ordering without moving existing
declarations.

## Current frontier

`HexPoly/Dense.lean` now has the conditional `degree?` / `≠ 0`
corner of the `monomial` cluster filled in. The unconditional
`GF2Poly` sibling triple is the separate issue #5170.

## Next step

None for this issue. Follow-up call-site migration (e.g. inline
monomial-degree reconstructions in `HexPoly/Operations.lean` or
`HexPolyFp/Basic.lean`) is explicitly out of scope per the issue.

## Blockers

None.
