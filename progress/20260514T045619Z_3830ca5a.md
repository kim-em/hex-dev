## Accomplished

- Claimed #3956 after checking that no human-oversight directive was claimable.
- Added `Berlekamp.quotientStepCoeffCheck` and theorem helpers that reconstruct
  quotient-step coefficient-array equality from a bounded reflected coefficient
  check plus size bounds.
- Added a compact `gf2WordQuotientStepCoeffCheck` wrapper for the existing
  `gf2WordPoly` bit representation.
- Added an N32 step-0 pilot `#guard` in `HexGfq/CrossCheck.lean` using the new
  quotient-step checker.
- Verified `lake build HexBerlekamp.Irreducibility`, `lake build
  HexGfq.CrossCheck`, `git diff --check`, and the banned-marker grep.

## Current frontier

- The helper API now exposes the coefficient reconstruction shape needed by
  N32 quotient-witness steps.
- The step-0 theorem-level proof still needs structural size proofs before it
  can be fed directly into
  `checkPowChainLinearIncrementalQuotientWitnessStep_of_entry_size_bounds`;
  broad `decide` over dense-polynomial multiplication still does not reduce.

## Next step

- Use the new checker theorem with explicit size bounds for the N32 step
  entries, then assemble the first quotient-witness theorem in the dependent
  HexGfq issue.

## Blockers

- None for the helper surface. The remaining work is proof plumbing, not an
  implementation blocker.
