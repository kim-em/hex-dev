# 2026-06-03T04:15Z — #6435 FpPoly scalar-multiple algebra

## Accomplished

Landed `HexPolyFp.SquareFree` foundational scalar-multiple algebra
(#6435 deliverables 1-3) as a single self-contained block of private
lemmas after `normalizeMonic_nonzero_isZero_false`:

- `C_ne_zero_of_ne_zero` and `one_size_eq` — `DensePoly.C` nonzero
  characterisation for prime modulus.
- `derivative_C_mul` — derivative linearity over constant scaling,
  proved via `coeff_derivative` and `coeff_scale` (the `_semiring` simp
  variants would not rewrite the dot-notation `.derivative.coeff` form,
  see Blockers).
- `C_mul_left_cancel`, `C_mul_dvd_C_mul_of_dvd`, `dvd_of_C_mul_dvd_C_mul`
  — dvd cancellation by a nonzero constant scalar.
- `eq_C_of_size_one`, `coeff_zero_ne_zero_of_size_one`,
  `size_eq_one_of_mul_eq_one`, `eq_C_of_mul_eq_one` — `FpPoly p` unit
  characterisation: every unit is a constant polynomial.
- `gcd_C_mul_C_mul_eq_C_mul_gcd` — existential gcd scale bridge over a
  prime modulus, proved through `xgcd_bezout` + `dvd_gcd` to extract
  associates of `gcd f g`, then `eq_C_of_mul_eq_one` to pin the unit
  scalar.
- `div_C_mul_C_mul_of_dvd` — exact-division quotient compatibility:
  `(C u * f) / (C v * g) = C (u * v⁻¹) * (f / g)` when `g ∣ f` and `g`
  is nonzero. Uses uniqueness of the executable quotient through
  `mul_right_cancel_of_ne_zero`.

No executable definitions changed; no `sorry`/`axiom`/`native_decide`/
`TODO`/`FIXME` introduced. `lake build HexPolyFp.SquareFree`,
`lake build HexPolyFp`, and `scripts/check_dag.py` all pass.

## Current frontier

Sub-task #6435 is complete. The downstream consumer is #6436
(`yunFactorsContributionWithLevel` synchronisation), which is blocked
on #6435 and will be unblocked once this PR merges.

## Next step

After this PR lands, a worker can claim #6436 and assemble the Yun
split synchronisation theorem using the new `derivative_C_mul`,
`gcd_C_mul_C_mul_eq_C_mul_gcd`, and `div_C_mul_C_mul_of_dvd` lemmas
together with the #6426 reconstruction substrate.

I also skipped #6376 (`package closed SquareFree providers`) earlier
in this session: its parent issues #6378 and #6379 are CLOSED only
by decomposition, and the real bridge work lives in #6394
(blocked on #6431) and #6414 (blocked on #6436 → #6435). The closed
`YunDerivativeActiveLevelStateProvider`, `YunDerivativeActiveRawTailProvider`,
and `residualInvariant` values it needs do not yet exist. Re-blocked
on #6394 and #6414.

## Blockers

`DensePoly.coeff_derivative_semiring` and `DensePoly.derivative_C_semiring`
both refused to fire on the dot-notation form `(p * q).derivative` or
`(C c).derivative` in `HexPolyFp.SquareFree` (rewrites failed with
"Did not find an occurrence of the pattern"). The same lemmas work
fine inside `Hex.DensePoly`. Root cause is presumably differing
elaboration of the `NatCast`/`Mul` instances at the site versus the
lemma. Worked around by switching to the non-`_semiring`
`DensePoly.coeff_derivative` with an explicit `c * 0 = 0` proof.
Worth investigating if other downstream proofs hit the same wall.
