# Interval scale review hardening

## Accomplished

- Opened draft PR #8937 and obtained a fresh Opus review while the next rational
  vertical continued in a separate stacked worktree.
- Moved all list-derived `StructureLimit` construction out of compiled timed
  loops, so structural-cap measurements isolate checker traversal rather than
  repeated harness `List.length` calls.
- Made both parity variants fail closed before timing, while retaining the odd
  spare lookup/fact capacity used to keep scalar limit fields live.
- Documented that centered equality-recipe program lookups are bounded by the
  node/edge cap product but are not yet charged to `maxLookupSteps`; production
  replay must charge them or use validated random access, and edge-table growth
  remains a required experiment.
- Added the scale conformance module to evidence provenance and removed an
  unreachable calibration branch.
- Passed the focused scale executable/conformance build, Python compilation,
  whitespace checks, and representative early/accept driver probes.

## Current frontier

The checker and harness changes are ready for a clean replacement evidence run.
The existing report numbers remain the pre-hardening record and must be updated
after that source-matched sweep; the PR stays draft meanwhile.

## Next step

Commit the review hardening, run the five-sample sweep from a clean tree, update
the report and SPEC with the new numbers and explicit accounting/closed-term
sharing caveats, then push and re-run CI without pausing the rational vertical.

## Blockers

None.
