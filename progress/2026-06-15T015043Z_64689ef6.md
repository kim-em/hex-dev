# Session 64689ef6 — feature (#7425)

## Accomplished

Added Phase 6 one-sentence docstrings to the six `getD`-indexing /
list-extensionality helper lemmas in `HexPoly/Operations.lean`:

- `list_getD_map_mul_zero`
- `list_getD_replicate_append_zero`
- `list_getD_map_range`
- `array_getD_set!_schoolbook` (issue called it `array_getD_set`)
- `list_foldl_ignore`
- `list_eq_of_length_eq_of_getD_eq`

Doc-only: signatures, statements, proofs, simp sets, declaration order
and `private` modifiers untouched. Diff is additions-only (18 lines).

## Key pattern

These lemmas all carry an `omit [...] in` modifier. A docstring placed
*before* `omit ... in` fails to parse (`unexpected token 'omit'`); it
must sit between `omit ... in` and `private theorem` to attach to the
declaration.

## Current frontier

Verification all green: `lake build HexPoly.Operations`,
`python3 scripts/check_dag.py` (exit 0), `git diff --check` clean,
no `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` in added lines.

## Next step

Open PR closing #7425.

## Blockers

None.
