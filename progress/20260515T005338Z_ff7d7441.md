# Session ff7d7441 — work #4119

## Accomplished

- Claimed #4116, determined the scalar `eval_mul` proof splits into a
  shifted/scaled row-evaluation helper plus the final convolution assembly.
- Decomposed #4116 into #4119 and #4120, added the dependency from #4120 to
  #4119, and marked #4116 for replan with the required breadcrumb.
- Claimed #4119 and added `FpPoly.eval_shift_scale_row` in
  `HexPolyFp/Basic.lean`.
- Added private coefficient-power-sum rebase and zero-prefix shift helpers
  used by the row theorem.
- Verified `lake build HexPolyFp.Basic`, `lake build
  HexBerlekamp.RabinSoundness`, `lake build HexBerlekamp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested grep
  for `axiom` / `native_decide` / `TODO` / `FIXME` / `sorry` in affected
  files.

## Current frontier

The shifted/scaled row contribution now evaluates as
`(c * x ^ i) * DensePoly.eval f x`. The remaining scalar multiplication
homomorphism work is #4120: assemble the finite convolution/prefix helper
using this row theorem and `FpPoly.coeff_mul` / `mulCoeffSum`.

## Next step

Work #4120 by proving the prefix fold over shifted/scaled multiplication rows
evaluates to the prefix coefficient power sum times `DensePoly.eval h x`, then
specialize the prefix to `f.size` to prove `FpPoly.eval_mul`.

## Blockers

- No external blocker found for #4119.
- `lake build HexBerlekamp` replays an existing `HexMatrix/Bareiss.lean`
  `sorry` warning outside the files touched by this session.
