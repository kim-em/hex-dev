# SchurSzego dead-code decision — excise (#8254)

Session type: review. UTC 2026-06-20T21:15.

## Accomplished

Resolved the SchurSzego-only dead-code criterion for `HexPolyZMathlib`
(#8254, the corrected-scope successor to #8245) with **path (A) excise**,
backed by source evidence.

- Confirmed module-level orphan status against the current tree:
  extracted all 92 declaration names from
  `HexPolyZMathlib/SchurSzego.lean` and grepped each (word-boundary, so
  namespace-qualified uses count) across every `*.lean` excluding the
  defining file — **zero external references**. `RobinsonForm.lean`
  references none of them, so its `import HexPolyZMathlib.SchurSzego` was
  itself dead.
- Re-checked directive #2564 (van Hoeij CLD headline, still open): its
  planned argument names its analytic inputs as Landau
  (`mahlerMeasure_le_sqrt_sum_sq_coeff`), Hadamard, UFD, Hensel, and
  Mignotte — all Mathlib imports or `Mignotte.lean` — and states
  "Missing infrastructure: none for HO-1's obligations." The
  Schmeisser / Grace-Walsh-Szegő / `rootsRadiusProduct` surface is
  absent, so the open directive does not resurrect the Schmeisser route.
- Confirmed `RobinsonForm.lean` is **live** (issue premise carried this
  in): `CLDColumnBound.lean` consumes its `rootDeletionDerivativeSummand`
  family (`derivative_eq_sum_rootDeletionDerivativeSummand`,
  `mahlerMeasure_rootDeletionDerivativeSummand_le`) to discharge the
  `hphi_mahler` hypothesis of `abs_phi_coeff_le`. Left RobinsonForm and
  its CLDColumnBound import untouched.

Changes:
- Deleted `HexPolyZMathlib/SchurSzego.lean` (1366 lines, 92 decls).
- Dropped `import HexPolyZMathlib.SchurSzego` from `HexPolyZMathlib.lean`
  (root) and `HexPolyZMathlib/RobinsonForm.lean` (dead import).
- Amended `SPEC/Libraries/hex-poly-z-mathlib.md`: replaced the
  "Schmeisser / de Bruijn-Springer source theorem surface" ownership
  section with a "Robinson-form derivative surface (CLD Φ Mahler bound)"
  section describing the live route actually consumed by CLDColumnBound.

## Key decisions / patterns

- **SPEC describes current design-of-record, not history.** Per the
  project style rule, the amended SPEC section documents the live
  RobinsonForm route rather than journaling "the Schmeisser route was
  superseded." The supersession rationale lives in the PR/issue comment.
- **Word-boundary name grep is the conservative orphan check.** A false
  "live" (from a same-named decl in another namespace) is safe — it
  would keep code, not wrongly delete it. The full `lake build` of both
  the library and the CI-gated downstream is the ground truth for the 13
  `@[simp, grind =]`-tagged decls that name-grep cannot catch.
- **Stale worktree hygiene.** The reused worktree had uncommitted
  deletions stripping the dead-code-scan guidance from
  `.claude/commands/review.md` and the `agent-worker-flow` skill (no PR,
  no commit). Restored both to `main` before starting.

## Verification

- `lake build HexPolyZMathlib` green (3401 jobs).
- `lake build HexBerlekampZassenhausMathlib` (CI-gated) green (3783
  jobs); `CLDColumnBound` rebuilt clean — no simp/grind-tag fallout.
- `python3 scripts/check_dag.py` exit 0.
- `git grep` for removed-symbol families clean (the one `schmeisser`
  hit is a RobinsonForm-owned theorem name, not a SchurSzego reference).
- No `axiom`/`sorry`/`native_decide` introduced.
- `libraries.yml[HexPolyZMathlib].done_through` stays at 5 (Phase 6 still
  needs the docstring criterion — a later issue).

## Current frontier

`HexPolyZMathlib` dead-code criterion resolved. The bulk of the Phase 6
docstring gap (the SchurSzego docstrings) vanished with the module.

## Next step

A follow-up issue can scope the remaining `HexPolyZMathlib` Phase 6
docstring-coverage criterion (now confined to `RobinsonForm.lean`) and
then bump `done_through` 5 → 6.

## Blockers

None.
