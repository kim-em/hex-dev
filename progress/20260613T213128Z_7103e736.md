# Progress: #6947 partial — CLD cut decomposition lemmas

Work session `7103e736`.

## Accomplished

- Checked unclaimed directives first; none were available.
- Claimed #6942, found PR #6938 was already merged with required checks green,
  and released it as stale.
- Claimed parent #6945, sanity-checked the requested BHKS CLD resultant
  valuation, and decomposed it into #6947, #6948, #6949 because the current
  repo lacked both the executable CLD cut decomposition layer and the abstract
  non-common-factor Sylvester valuation theorem.
- Claimed #6947 and corrected its issue body after finding the originally
  proposed "`psiCut` is congruent to the uncut coefficient modulo `p^a`" premise
  was false: `psiCut` is the high-bit quotient, not a residue representative.
- Added executable-side lemmas in `HexBerlekampZassenhaus/Basic.lean`:
  - `self_sub_centeredModNat_dvd`
  - `centeredResiduePow_add_pow_mul_psiCut`
  - `cldQuotientMod_coeff_decomp_of_lt`

Commit:

- `f5fc6398 feat: add CLD cut decomposition lemmas`

Verification:

- `lake build HexBerlekampZassenhaus.Basic` succeeded.
- `python3 scripts/check_dag.py` succeeded.
- Added lines contain no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

#6947 is only partially complete. The verified executable decomposition lemmas
are in place, but the Mathlib-facing helper over
`BHKS.auxiliaryPolynomialWithCorrections` was not landed.

I attempted `lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`, but
it repeatedly spent several minutes recompiling
`HexBerlekampZassenhaus/CrossCheck.lean` after the executable changes. I stopped
that run rather than commit an unverified Mathlib wrapper.

## Next step

Finish #6947 by adding the Mathlib-facing `BadVectorAuxiliary` helper that
specializes `Hex.cldQuotientMod_coeff_decomp_of_lt` to `LiftData` and the
corrected auxiliary-polynomial coefficient formula, then verify
`lake build HexBerlekampZassenhausMathlib.BadVectorAuxiliary`.

## Blockers

No conceptual blocker for #6947 remains. The practical blocker this session was
the slow `CrossCheck.lean` dependency rebuild when trying to verify the Mathlib
target.
