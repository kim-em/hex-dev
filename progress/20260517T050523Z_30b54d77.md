# 20260517T050523Z — feature session 30b54d77 (#4563 partial)

## Accomplished

Claimed #4563 (HO-1 substrate: factorsModP convenience dischargers from
factorsModPBerlekampForm).  Delivered the first of four dischargers and
decomposed the remaining three into separate sub-issues.

### Landed

Added `factorsModP_ne_nil_of_factorsModPBerlekampForm` in
`HexBerlekampZassenhausMathlib/Basic.lean` (immediately after the existing
sibling `factorsModP_natDegree_pos_of_factorsModPBerlekampForm` at
line 4239).  Discharges `primeData.factorsModP.toList ≠ []` from the
Berlekamp-form invariant via `berlekampFactor_factors_ne_nil`; no
`isGoodPrime` premise required.  Sorry-free, ~15 lines.

### Decomposed (deliverables 2-4)

Created three sub-issues for the remaining dischargers, each carrying
the relevant analysis a future feature session needs:

- #4567 — `factorsModP_monic_of_factorsModPBerlekampForm`.  The parent
  planner assumed `splitFactorAt = gcd f (witness - C c)` returns the
  unique monic representative under square-freeness, but the codebase's
  EEA-based `DensePoly.gcd` (`HexPoly/Euclid.lean:826`) does not
  normalise its output, and the conformance fixtures
  (`HexBerlekamp/Conformance.lean:278-283`) and a hand-traced
  `gcd(X² - 1, 2X - 2)` over `F_5` exhibit non-monic positive-degree
  gcd outputs.  The sub-issue lays out three resolution options
  (normalise `splitFactorAt`; reformulate the umbrellas to take an
  "associate of monic" hypothesis; or post-process at the
  `berlekampFactorsModP` extraction layer) and asks the implementer to
  pick one with documented justification.

- #4568 — `factorsModP_polyProduct_congr_of_factorsModPBerlekampForm`.
  The parent signature is not provable from `hform` + `hgood`:
  `prod_berlekampFactor` yields `factorProduct factors =
  monicModularImage (modP p core)`, which lifts back to
  `lc⁻¹ · core mod p`, not `core mod p`.  `isGoodPrime` only ensures
  `lc ≢ 0 mod p` via `leadingCoeffAdmissible`, not `lc ≡ 1 mod p`.
  The sub-issue proposes adding `hcore_monic` to the signature (which
  the umbrellas already require) and details a ~150-200 line proof
  strategy.

- #4569 — `factorsModP_coprime_of_factorsModPBerlekampForm`.  The
  parent's already-flagged escape-hatch case ("if deliverable 4 needs
  deeper Berlekamp-level structural lemmas, deliver as a separate
  sub-issue").  Needs a new Berlekamp-level theorem
  `berlekampFactor_factors_pairwise_coprime` mirroring
  `_nodup_of_no_squared` but strengthening to coprimality.  Likely
  ~200-300 lines.

Left the standard `Decomposed into #4567, #4568, #4569` breadcrumb on
#4563 with the one-line scope-assessment reason, per the
worker-flow skill.

## Build state

- `lake build HexBerlekampZassenhausMathlib.Basic` ✓
- `lake build HexBerlekampZassenhausMathlib` ✓
- `rg "sorry|axiom|native_decide|TODO|FIXME"
  HexBerlekampZassenhausMathlib/Basic.lean`: four pre-existing
  `sorry`s at lines 160, 239, 8228, 8321 unchanged in count (line
  numbers shifted by the insertion).  Zero new `sorry`/`axiom`/
  `native_decide`/`TODO`/`FIXME`.

## Current frontier

#4563 should be `replan`ed by the next planner.  Three sub-issues
(#4567, #4568, #4569) carry the remaining work, each independently
claimable since the four dischargers feed independent
`QuadraticMultifactorLiftInvariant` boundary hypotheses.

#4561 (the exhaustive-branch umbrella assembly) remains blocked on
all four dischargers; closing it requires #4567, #4568, #4569 plus
deliverable 1 (already landing).

## Next step

A planner triages #4563 (either close it with a forward link to the
three sub-issues, or narrow its body to whichever residual scope
remains — likely just close-out).  Subsequent feature workers can
claim any of #4567, #4568, #4569 independently.

#4567 is the load-bearing one: its design decision (whether to
normalise the Berlekamp output, reformulate the umbrellas, or
post-process at extraction) affects whether #4568 and #4569 need
adjustment.  Recommend tackling #4567 first.

## Blockers

None for the partial delivery itself.
