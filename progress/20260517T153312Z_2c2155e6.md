# 20260517T153312Z — feature session 2c2155e6 (#4688)

Session UUID prefix: 2c2155e6. Issue: #4688 — HO-1 substrate (#4678 sub):
compose `modPSubsetPartitionHypotheses_of_choosePrimeData` wrapper.

## Accomplished

Landed the wrapper constructor that bundles the landed analytic
predecessors with the trivial fields of `ModPSubsetPartitionHypotheses`:

* `fModP_eq` via `Hex.choosePrimeData_fModP_eq_modP`;
* `admissible_prime` / `square_free_reduction` via `trivial` (`True`
  hooks);
* `factors_irreducible` via `factors_irreducible_of_choosePrimeData_of_some`
  (#4686);
* `exists_subset` via `(existsUnique_modPFactorSubset_of_choosePrimeData
  ... hsome).exists` (#4693);
* `unique_subset` via the uniqueness component of
  `existsUnique_modPFactorSubset_of_choosePrimeData` (#4693).

The wrapper added an explicit `hsome :
Hex.choosePrimeData? core = some (Hex.choosePrimeData core)` hypothesis
not shown in the issue's literal signature: both predecessor lemmas
require it (the consumer-facing #4693 wrapper threads `hsome` through,
and #4686 only landed as a `_of_some` variant). The fallback `p = 3`
branch is genuinely unprovable from the structure's analytic content,
so the hypothesis cannot be eliminated without re-introducing a `sorry`.
Downstream consumers (#4680) will supply `hsome` from the same
`choosePrimeData?` chain they thread elsewhere.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — clean (only
  pre-existing sorries at lines 237, 11149, 11244 and pre-existing
  warning at 11475 unused-variable; no new sorries introduced by this
  change).
* `python3 scripts/check_dag.py` — clean.
* `git diff --check` — clean.
* `git diff` `+`-side rg of `sorry|axiom|native_decide|TODO|FIXME` —
  empty.

## Current frontier

#4688 is closed by the PR. The HO-1 critical path for #4678 / #4671:

* #4679 (HenselSubsetLiftHypotheses constructor) sub-issue #4697
  remains open, blocked on #4733 (forward Hensel transport).
* #4680 (compose #4688 + #4679 to discharge the two analytic-obligation
  sorries) cannot land until #4697 + #4733 do.

## Next step

A worker on the next-tier critical path can claim #4733 (forward
Hensel transport) to unblock #4697. The remaining unclaimed feature
issues (#4604, #4627, #4647) all stay blocked on still-open
predecessors (#4747, #4746, #4738).

## Blockers

None for this PR; CI build via `coordination create-pr` will exercise
the full conformance/oracle pipeline.
