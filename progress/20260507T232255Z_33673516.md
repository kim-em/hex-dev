# Progress — 2026-05-07T23:22 UTC — agent/33673516

## Accomplished

Claimed issue #2526 and added public `GramSchmidt.Int` row-add wrappers in
`HexGramSchmidt/Basic.lean`:

- `basis_rowAdd`
- `coeffs_rowAdd_lower`
- `coeffs_rowAdd_pivot`
- `coeffs_rowAdd_above_pivot`
- `coeffs_rowAdd_other_row`

Kept `castIntMatrix` private by adding only a private bridge lemma showing that
integer `Matrix.rowAdd` commutes with the rational cast used internally by
Gram-Schmidt.

Used the wrappers in `HexGramSchmidt/Update.lean` to discharge the first five
size-reduction proofs: basis invariance plus the four rational coefficient
update formulas for pivot, lower entries, other rows, and above-pivot entries.

## Current frontier

The requested integer row-add coefficient surface is available from
`HexGramSchmidt.Basic` and directly consumes the rational row-add lemmas. The
remaining `Update.lean` sorries begin at the scaled-coefficient size-reduction
formulas and adjacent-swap formulas.

## Next step

Use the newly proved coefficient wrappers together with `gramDet_sizeReduce` and
`scaledCoeffs_eq` to attack the scaled-coefficient size-reduction formulas in
`HexGramSchmidt/Update.lean`, likely as a separate focused issue.

## Blockers

None.
