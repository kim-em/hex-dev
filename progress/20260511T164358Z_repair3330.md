# Repair PR #3330 — BZ conformance fixture salvage

## Accomplished

- Claimed PR #3330 for repair.
- Diagnosed the stuck conformance job as hanging during
  `hexbz_emit_fixtures` before the oracle ran.
- Trimmed BZ JSONL fixture emission to remove cases that are no longer
  tractable or oracle-valid after the PR's `p >= 3` good-prime rule:
  `irr/cyclo11`, `irr/cyclo17`, and `red/quad2_deg4`.
- Regenerated `conformance-fixtures/HexBerlekampZassenhaus/bz.jsonl`.
- Verified `lake build`, `lake build HexBerlekampZassenhaus
  hexbz_emit_fixtures`, `python3 scripts/conformance_targets.py --check`,
  `python3 scripts/check_dag.py`, `python3 scripts/status.py
  HexBerlekampZassenhaus`, `git diff --check`, and
  `bash scripts/ci/run_oracles.sh`.

## Current frontier

- PR #3330 is locally green for the repaired conformance path.
- The BZ oracle now checks 22 cases and completes without failures.

## Next step

- Push the repair commit and mark PR #3330 salvaged.

## Blockers

- None.
