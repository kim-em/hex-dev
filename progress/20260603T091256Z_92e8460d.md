# 20260603T091256Z work #2567

## Accomplished

Added the bridge-derived `Real.sqrt` rung of the HO-4 assembly chain in
`HexBerlekampZassenhausMathlib/BadVector.lean` and
`HexBerlekampZassenhausMathlib/Recovery.lean`:

* `BadVectorBridgeData.auxiliaryPolynomial_l2norm_le_sqrt_of_bridge_data` —
  unsquared form of the existing squared-l2 bridge lemma. Bounds
  `‖aux‖ ≤ Real.sqrt [the structured corrected RHS sum]` by taking square
  roots of both sides of `auxiliaryPolynomial_l2norm_sq_le_of_bridge_data`,
  using `Real.sqrt_le_sqrt` and the non-negativity of `l2norm`.
* `FactorFastCapSeparationInputs.ofBridgeDataAndPaperThreshold` — builds the
  cap-separation record from the bridge data alone, with the auxiliary
  l2-bound discharged internally via the new sqrt lemma. Caller supplies the
  bridge, the projected vector `v` with `hin`/`hnot`, the CLD-coefficient
  bound `hcld`, the paper-threshold inequality on
  `coeffL2NormBound ^ aux.natDegree * (Real.sqrt RHS) ^ input.natDegree`,
  and the executable precision/prime-choice equations — no caller-chosen
  `auxiliaryBound : ℝ` is needed.
* `factorFast_terminates_ofBridgeDataAndPaperThreshold` — final-step
  wrapper composing the above with `factorFast_terminates`.

These are pure compositions of existing infrastructure plus the square-root
step of the squared-l2 bridge bound.

## Current frontier

The four sibling `*AndPaperThreshold` cap-separation builders now form a
complete orthogonal axis on the auxiliary-bound shape:

1. `ofBridgeDataPointwiseAuxiliaryBoundsAndPaperThreshold` —
   pointwise vector/correction estimates.
2. `ofBridgeDataCorrectedAuxiliaryL2normSqAndPaperThreshold` —
   structured corrected RHS sum bound.
3. `ofBridgeDataAuxiliaryL2normAndPaperThreshold` — direct `‖aux‖ ≤ B`
   bound.
4. `ofBridgeDataAndPaperThreshold` (new) — bridge-derived `Real.sqrt`
   bound, no caller-supplied `auxiliaryBound`.

Each has a matching `factorFast_terminates_*` final wrapper. The remaining
HO-4 analytical content is the paper-threshold inequality itself — the
genuine BHKS §5 Theorem 5.2 content — which is unchanged from the previous
session.

## Next step

The largest remaining analytic gap is still the paper-threshold inequality
`coeffL2NormBound^aux.natDeg * auxiliaryBound^input.natDeg ≤
bhksPaperThresholdReal core C`. With the new `ofBridgeDataAndPaperThreshold`
wrapper, the caller now has the simplest possible API: just the bridge data
plus this single analytic inequality (with `auxiliaryBound` instantiated to
the bridge-derived sqrt expression). A natural follow-up is to break the
paper-threshold inequality into its component arithmetic steps (`(2C)^(n²)`,
`‖f‖₂^(2n-1)`, `(log ‖f‖₂)^n`) and prove each.

## Blockers

None. The new lemma and wrappers are pure compositions of existing
infrastructure.

## Verification

- `lake build HexBerlekampZassenhausMathlib.BadVector` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing.
