# 2026-06-01T08:18Z — HexGfq → HexGFq rename (#5874)

## Accomplished

- Renamed the four non-conformant library identifiers per
  PLAN/Conventions.md §"Acronym exception": `HexGfq`, `HexGfqField`,
  `HexGfqMathlib`, `HexGfqRing` → `HexGFq*`. Mechanical sed pass over
  the 55 in-scope files followed by two-step `git mv` (case-insensitive
  APFS) for the umbrella `.lean` files, the lib directories,
  `conformance-fixtures/HexGfq{,Field,Ring}/`, and the
  `HexManual/Chapters/HexGfqRing.lean` chapter file.
- Updated `libraries.yml` keys and `deps:` entries; updated
  `lakefile.lean` `lean_lib HexGFq*` declarations and `root := \`HexGFq*\``
  arguments. Left the snake_case `lean_exe` names (`hexgfq_bench` etc.)
  alone per the directive.
- Updated the in-fixture `"lib":"HexGfq..."` JSONL fields to
  `"lib":"HexGFq..."` in the three fixture files.
- Updated `SPEC/Libraries/hex-gfq*.md`, `reports/hex-gfq*-performance.md`,
  the HexManual checkpoint markdown files, `scripts/libgraph.py`,
  `scripts/ci/run_oracles.sh`, the three `scripts/oracle/gfq*_flint.py`
  driver scripts, and `status/hex-gfq-mathlib.scaffolding-reviewed`.
  Kebab-case filenames under `SPEC/Libraries/`, `reports/`, and `status/`
  unchanged per the directive.
- Inner Lean namespaces `Hex.GfqBench`, `Hex.GfqConformance`,
  `Hex.GfqCrossCheck` are out of scope (the directive lists exactly
  four library identifiers; these are not `HexGfq*`-prefixed and are
  not library identifiers). Left untouched.

## Verification

- `lake build` → 244/244 jobs OK.
- `lake exe hexgfq_bench list` → 6 registered benchmarks, wiring intact.
- `python3 scripts/check_dag.py` → exit 0.
- `python3 scripts/libgraph.py` → exit 0.
- `bash -n scripts/ci/run_oracles.sh` → OK.
- `git grep "HexGfq"` returns matches only under `progress/`.

## Current frontier / Next step / Blockers

None — work complete; opening PR.
