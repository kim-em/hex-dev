# BHKS recovered-lift cut package (#8051)

## Accomplished

Claimed #8051 after the blocked directive #2564 and raced #6246 were not
claimable. Added the public
`BHKS.cutProjectionHypotheses_of_recoveredLift` producer in
`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`, packaging a recovered-lift
family into `CutProjectionHypotheses` through
`supportShortVectorData_of_recoveredLift` and
`cutProjectionHypotheses_of_shortVectors`.

Updated
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift` in
`HexBerlekampZassenhausMathlib/PartitionRefinement.lean` to consume the named
producer instead of rebuilding the cut package inline.

Verification completed:

- `lake build HexBerlekampZassenhausMathlib.CLDColumnBound`
- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over touched Lean files

## Current frontier

#8051 now has a named forward-cut package producer from recovered-lift data. The
remaining fast-branch assembly still needs the separate size and partition
producers from the neighboring issues before the capstone can remove all local
caller-side assumptions.

## Next step

Open the PR for #8051. Follow-up workers can use
`BHKS.cutProjectionHypotheses_of_recoveredLift` directly when assembling
`factorFastCoreWithBound_some_factor_count_ge_of_cut` /
`factorFastFactorsWithBound_raw_zpolyIrreducible_of_cut`.

## Blockers

None for this issue. The worktree still contains a pre-existing, unrelated
off-limits `.claude/CLAUDE.md` modification that was not touched or staged.
