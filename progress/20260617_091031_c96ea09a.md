# Progress - 2026-06-17 (session c96ea09a, /work → #7702)

## Accomplished

Claimed and executed **#7702** (feat: expose linear Hensel discharge lemmas for
henselLift bridge, nonconstant g). All deliverables landed in
`HexHensel/Linear.lean`; `lake build HexHensel.Linear`, `lake build HexHensel`,
`python3 scripts/check_dag.py`, and `git diff --check` all clean. No
sorry/axiom/native_decide/TODO/FIXME on any added line.

### Deliverable 1 — invariant strengthening + degree-bound core

- Strengthened `LinearLiftLoopInvariant` with a fourth component
  `0 < acc.g.degree?.getD 0` (nonconstant leading factor); added the
  `pos_degree` accessor and repointed `monic_g` (`.2.2 → .2.2.1`).
- `henselLiftLoop_invariant` now propagates positivity through each step via
  `linearHenselStep_g_degree?_eq` (degree preserved), so the invariant is
  maintained without weakening any conclusion.
- New private substrate: `size_le_of_coeff_zero_above`,
  `degree?_getD_eq_size_sub_one` (generic), `modP_size_eq_of_monic` /
  `modP_degree?_eq_of_monic` (reduction mod p preserves a monic factor's
  degree), `liftScaledIncrement_degree_lt`, and the headline
  `stepDegree_of_monic_pos`: `LinearLiftStepDegreeInvariant` from monicity +
  positive degree, via the FpPoly `divMod` remainder degree bound
  (`DensePoly.divMod_remainder_degree_lt_of_pos_degree`, available through the
  `DivModLaws (ZMod64 p)` instance).

### Deliverable 2 — base-discharge corollaries

- `henselLift_congr_of_base` / `henselLift_monic_of_base` (public): take the
  base mod-p data (`hprod`, `hbez`, `hmonic`) plus the nonconstant hypothesis
  `0 < g.degree?.getD 0`, and discharge `hstart` / `hstepDegree` / `hstepBezout`
  internally before calling `henselLift_spec` / `henselLift_monic`.
- Supporting private helpers: `loopInvariant_one_of_base` (assembles the level-1
  invariant from base data) and `stepBezout_of_invariant` (modP unchanged across
  a step → Bezout transfers).

The nonconstant hypothesis is genuine and is the corrected premise from #7690:
the constant case `g = 1` satisfies every other component yet admits no strict
per-step degree drop, so the bound is unprovable without it.

## Current frontier

The discharge lemmas exist; `Correctness.lean` is untouched per the issue. The
strengthened invariant changed `MultifactorLiftInvariant`'s shape (its level-1
conjunct is now the strengthened invariant), but every consumer in CI-built
`HexHensel` only *threads* the invariant as a hypothesis, so the full
`HexHensel` library still builds.

## Next step

Fast follow-up (separate issue, as the directive notes): dispatch the two
Mathlib-facing sorrys in `HexHenselMathlib/Correctness.lean` (`hensel_correct`
line 38, `hensel_degree` line 81) against the new corollaries, threading the
same nonconstant hypothesis. `HexHenselMathlib` is not CI-built and was not
built this session; a consumer that *constructs* `MultifactorLiftInvariant`
there will now need to supply the positive-degree component.

## Blockers

None.
