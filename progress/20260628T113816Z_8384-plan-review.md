# #8384 — proof plan review

## Accomplished
Reviewed the proposed multi-session plan for closing
`factor_irreducible_of_nonUnit`, focusing on the fast-core reduction,
`hpartition`/`hsize`/`hcut` endpoints, accepted-precision extraction, and the
cap/floor arithmetic around `factorFastPrecisionCap`.

Key files inspected:
- `HexBerlekampZassenhausMathlib/FactorSoundness.lean`
- `HexBerlekampZassenhausMathlib/PartitionRefinement.lean`
- `HexBerlekampZassenhausMathlib/BHKSBound.lean`
- `HexBerlekampZassenhausMathlib/CLDColumnBound.lean`
- `HexBerlekampZassenhausMathlib/Recovery.lean`
- `HexBerlekampZassenhaus/Basic.lean`

Also attempted to run the configured second-opinion skill, but the external API
call failed with `ConnectionRefused`, so no substantive external review was
available.

## Current frontier
The plan is directionally correct that the public capstone reduces to fast-core
success soundness, but the review found important corrections:
- loop-success accepted-precision extraction already exists via
  `factorFastCoreWithBound_some_indicatorCandidates`;
- `cldCoeffFloor_le_factorFastPrecisionCap` already exists for normalized
  inputs;
- `hpartition` is already discharged at the extracted success precision inside
  `factorFastCore_irreducible_of_liftedTrueSupport`;
- the major unresolved shape is still turning bare executable success into the
  right support/cut/count data, with a serious route choice between the existing
  `toMonicLiftData` loop and the newer `coreLiftData`/M1 recovery stack.

## Next step
If pursuing #8384, first choose the route explicitly:
either prove the missing core-vs-monic equivalence-class indicator-size bridge
for the existing `factorFastCore_irreducible_of_liftedTrueSupport` route, or
switch the executable fast-core path to M1/core recovery and verify the conformance
impact before building the cut-data proof on top.

## Blockers
No local build or proof changes were attempted. The second-opinion API was
unavailable.
