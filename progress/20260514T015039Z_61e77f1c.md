# Session 61e77f1c — HO-43 decomposition

## Accomplished

- Claimed HO-43 (#3899) — relocate `HexGramSchmidt.Int` theorems crossing the
  Mathlib-free / Mathlib-bridge boundary.
- Mapped the full transitive `Matrix.bareiss_eq_det` consumer chain inside
  `HexGramSchmidt/Int.lean`: ~17 theorems including public consumers
  (`gramDet_pos`, `gramDet_eq_prod_normSq`, `basis_normSq`, `scaledCoeffs_eq`,
  `leadingGramMatrixInt_det_eq_gramDet_int*`) plus the cast helper stack
  (`det_intCast` and its `foldl_*` underpinnings). Found the additional direct
  user `gramDet_adjacentSwap_of_ne` in `HexGramSchmidt/Update.lean` not
  mentioned in the issue body, plus the `det_submatrix_gram_eq_gramDet_int`
  helper in `HexLLL/Basic.lean`.
- First-pass decomposition into #3902 (Int.lean physical relocation),
  #3903 (Update.lean relocation), #3904 (HexLLL/Basic.lean relocation).
- Skipped HO-43 with breadcrumb pointing to the three sub-issues.
- After deeper inspection, found the decomposition was wrong: keeping the
  `Hex.GramSchmidt.Int` namespace on relocated theorems does *not* let
  downstream Mathlib-free files (Update.lean, HexLLL/Basic.lean) continue to
  resolve references — they would need `import HexGramSchmidtMathlib.Int`,
  which is precisely the smuggle HO-43 forbids. None of the three sub-issues
  can land alone with a green build.
- Skipped #3902 with a corrective recommendation, left linking comments on
  #3903 and #3904.

## Current frontier

- HO-43 (#3899) is open with `replan` label.
- #3902 is open with `replan` label.
- #3903 and #3904 are open with `blocked` label, both linked to the
  recommendation in #3902's skip comment.
- The repo state is unchanged from the start of the session — no code edits,
  no commits.

## Next step

The next planner should:

1. Collapse #3902 / #3903 / #3904 back into a single sub-issue (or two — one
   for the HexGramSchmidt move, one for HexLLL — provided the HexGramSchmidt
   one is self-contained for a green build).
2. Re-decompose HO-43 (#3899) accordingly, or have a single sub-issue do the
   full move atomically.
3. The single-sub-issue option will be a large PR (~20 theorems relocated
   across 3 files, plus 3 new bridge files: `HexGramSchmidtMathlib/Int.lean`,
   `HexGramSchmidtMathlib/Update.lean`, `HexLLLMathlib/Independent.lean` or
   similar). The full set of theorems to relocate is enumerated in #3902's
   body and in this session's exploration trail.

## Blockers

None. The work is well-scoped; it just needs a non-broken decomposition or
a session with enough context for the atomic PR.
