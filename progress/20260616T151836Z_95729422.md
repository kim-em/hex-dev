# Review: toMonic Hensel substrate producer soundness (#7597)

Session type: review. Verdict: **SOUND** — the three carrier-free
`toMonicPrimeData?` substrate producers are ready for capstone consumption.

## Accomplished

Reviewed the producer cluster named in #7597 against the directive #2564
soundness discipline (canonical-witness conclusions; no vacuous quantifiers
or silently-unused hypotheses; genuine `rfl`/`trivial` discharges; no new
`sorry`/`axiom`/`native_decide`):

- `henselSubsetCorrespondenceHypotheses_of_toMonicPrimeData`
  (`HexBerlekampZassenhausMathlib/Basic.lean:20784`)
- `liftedFactorSubsetPartition_of_toMonicPrimeData_complete`
  (`HexBerlekampZassenhausMathlib/IntReductionMod.lean:3096`)
- `slowPathHenselSubstrate_of_toMonicPrimeData`
  (`HexBerlekampZassenhausMathlib/IntReductionMod.lean:3141`)

### Findings

1. **Narrowed existence quantifier is in place.**
   `HenselSubsetCorrespondenceHypotheses.exists_subset` (`Basic.lean:2588`)
   carries the `Hex.normalizeFactorSign factor = factor` guard (the
   #7569/#7573 narrowing). So the field is *not* the unsatisfiable bare
   `∀`-factor existence flagged in the boundary skill; it ranges only over
   sign-normalized irreducible divisors, which is exactly the set the landed
   recovery producer covers.

2. **All producer hypotheses consumed.** Each of the three producers threads
   every hypothesis into a real sublemma; verified by reading the bodies.
   `exists_subset` is discharged by the genuine, sign-guard-consuming
   `toMonicLiftData_represents_lifted_of_modP` (`Basic.lean:19924`), and
   `unique_subset` by `toMonicLiftData_unique_subset` (`Basic.lean:20620`).
   (`toMonicLiftData_unique_subset` underscore-marks `_hirr`/`_hdvd` — it
   derives sign-normalization from the representation witness itself, so it
   holds *more* generally; that makes it stronger, not vacuous.)

3. **Canonical conclusion shapes.** `LiftedFactorSubsetPartition core d
   Finset.univ core` (index set `Finset.univ`, target `core`, via
   `henselSubsetCorrespondenceRest_initial`) and `SlowPathHenselSubstrate`
   (`Basic.lean:18919`) only admit witnesses over the canonical lift
   `d = Hex.ZPoly.toMonicLiftData core B primeData`. The partition's
   `support_subset_of_dvd_recombinationCandidate` field is guarded by
   `leadingCoeff core = 1` (monic-only), routing the non-monic path through
   the recovered `liftedRecoveryCandidate` coordinate — sound, not vacuous.

4. **`rfl`/`trivial` discharges are genuine.** In the carrier-free producer
   (`Basic.lean:20802`), `lift_eq := rfl` typechecks because `d` is the
   `let`-bound `toMonicLiftData core B primeData`; `successful_lift :=
   trivial` is just `True` at the `True True` instantiation. Neither hides a
   dropped obligation. The carrier-free version needs no `MonicDescentHypotheses`
   carrier at all (its `descends` field is never consumed by the correspondence).

5. **No new sorry/axiom/native_decide.** No such token appears in the producer
   bodies or the leaf existence/uniqueness lemmas. `#print axioms` reports
   `sorryAx` for all three producers — but this is the inherited infrastructure
   state, not a regression:
   - The control lemma `normalizeFactorSign_eq_self_of_leadingCoeff_nonneg`
     in the same file is `sorryAx`-free, so the file is not blanket-poisoned.
   - The pre-existing, already-accepted sibling
     `slowPathHenselSubstrate_of_choosePrimeData` has the *identical* axiom
     profile (`propext, sorryAx, Classical.choice, Quot.sound`).
   - Every `sorryAx` source in the import closure is a documented, open-issue-
     tracked bridge sorry: `HexBerlekampMathlib/Basic.lean` (~16, the
     `toMathlibPolynomial`/Berlekamp bridge), `HexHenselMathlib/Correctness.lean`
     (6, Hensel-lift correctness), and the single `Hex.ZPoly.isIrreducible_iff`
     (`Basic.lean:233`, #4818/#4172). The producers inherit these through the
     lift/Berlekamp machinery, exactly as the whole Mathlib layer does.

### Verification

`lake build HexBerlekampZassenhausMathlib.Basic
HexBerlekampZassenhausMathlib.IntReductionMod` — green (3734/3734, 0 errors;
only the pre-existing bridge-sorry warnings). Baseline confirmed against the
unmodified tree (review made no source changes).

## Current frontier

Verdict posted on #7597. The three producers are ready for capstone
consumption (#6771 / #7561). Per the boundary skill, the slow-path capstone
that consumes them is still blocked on the unscaled-support producer (#7479
deliverable 2), which is a separate issue, not a soundness defect in these
producers.

## Next step

None for this review. Capstone wiring (#6771/#7561) can consume
`slowPathHenselSubstrate_of_toMonicPrimeData` directly.

## Blockers

None.
