# 20260609T161157Z (#6688)

## Accomplished

Claimed #4830 first, audited it against current `HexBerlekampZassenhausMathlib`
state, and skipped it as stale because newer replacement #6678 records that
the arbitrary lifted-partition fallback is unsound.

Claimed #6688 and added the private recursive scalar synchronization theorem
`yunFactorsContributionWithLevel_scalar_sync_of_derivative_active_reachable`
in `HexPolyFp/SquareFree.lean`. The theorem proves that, at any
derivative-active reachable Yun state, nonzero scalar multiples of the input
`c` and `w` produce contribution and residual components that remain nonzero
scalar multiples of the unscaled run. The proof handles top-level `isOne`
mismatch cases through `yunFactorsContributionWithLevel_constant_scalar_sync`
and the ordinary recursive step through
`yunFactorsContributionWithLevel_scalar_step_bridge` plus
`yunFactorsContributionWithLevel_scalar_branch_sync`.

## Current frontier

#6688's recursive scalar-sync substrate is now available for wrapper work in
#6661. The theorem is intentionally private and scoped to derivative-active
reachable states, which supplies the nonzero gcd condition needed by the
step bridge.

## Next step

Use the new scalar-sync theorem with
`yunFactorsContributionWithLevel_normalized_tail_reconstruct` and
`yunFactorsContributionWithLevel_normalized_tail_product_bridge` to expose the
normalized Yun contribution sync wrappers requested by #6661.

## Blockers

None. Verification performed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexPolyFp/SquareFree.lean`
