# HexBerlekamp pow-chain certificate spec

## Accomplished

- Claimed feature issue #2141.
- Proved `checkPowChain_spec` in `HexBerlekamp/Irreducibility.lean`.
- Added a private `List.all` membership extraction helper for the local certificate proof.
- Verified `lake build HexBerlekamp.Irreducibility`, `lake build HexBerlekamp`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/check_dag.py`, and `git diff --check`.

## Current frontier

`checkPowChain_spec` now exposes every checked pow-chain entry as the corresponding `FpPoly.frobeniusXPowMod` value for indices `k <= cert.n`.

## Next step

Use `checkPowChain_spec` while proving `checkIrreducibilityCertificate_rabinTest`, alongside the divisibility and Bezout certificate checks.

## Blockers

- `HexBerlekamp/Irreducibility.lean` still has the two downstream certificate correctness placeholders, which were out of scope for #2141.
- Broader builds still report pre-existing `sorry` warnings in dependencies and adjacent Berlekamp modules.
