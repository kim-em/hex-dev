# 20260603T003020Z work #6397

## Accomplished

- Claimed issue #6397 (BHKS D1 prerequisite: close canonical recovery inputs at
  factorFast cap) and landed three additions in
  `HexBerlekampZassenhausMathlib/Recovery.lean`:
  1. `BHKS.FactorFastCapSeparationInputs.latticeIdentification` — the closed
     actual-cap `L' = W` accessor derived from the packaged
     `BadVectorBridgeData`, cut hypotheses, analytic comparison, and
     cap-precision dominance.  The required `0 < (factorFastCapLiftData f
     primeData).p` is discharged internally from `choose_eq` via
     `Hex.choosePrimeData?_prime`.
  2. `BHKS.CanonicalRecoveryInputs.ofFactorFastCapSeparationInputsAndCanonicalRecoveryTailInputs`
     — the closed `CanonicalRecoveryInputs f primeData` constructor.  It
     bundles a `FactorFastCapSeparationInputs` (cap-side bridge / cut /
     comparison / prime-choice / precision data) together with a
     `CanonicalRecoveryTailInputs` (recovery-side shape facts, expected
     true-factor package, Mignotte product congruences) and feeds the
     lattice identification through `latticeIdentification` so the caller
     never has to materialise it manually.
  3. `BHKS.factorFast_ne_none_of_factorFastCapSeparationInputsAndCanonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`
     — the parallel HO-4 wrapper that consumes both packaged input records
     and routes through the new `CanonicalRecoveryInputs` constructor into
     `factorFast_ne_none_of_canonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`.
     This sits alongside the existing
     `factorFast_ne_none_of_factorFastCapSeparationInputsCanonicalRecoveryTailInputs`
     which takes the same two packaged inputs but goes through the
     disparate-arguments cap-separation wrapper.

## Current frontier

The canonical recovery inputs at the actual `factorFast` cap branch now have a
closed `CanonicalRecoveryInputs` constructor, so the public-theorem assembly
no longer needs to thread the `lattice_eq_indicators` field manually.  The
remaining gap to `factorFast_terminates` is the construction of the
`FactorFastCapSeparationInputs` and `CanonicalRecoveryTailInputs` *fields*
themselves for the canonical true supports — i.e. the cap-separation bridge
data (the deep BHKS D1 content), the cut / analytic comparison witnesses, and
the canonical-support representations behind the Mignotte product congruences.

## Next step

Either:
- (cap side) construct a closed `FactorFastCapSeparationInputs f primeData
  rows_pos trueSupports` from the analytic side: produce the
  `BadVectorBridgeData` package and the cut / analytic comparison witnesses
  for the canonical true supports at the actual cap, OR
- (recovery side) construct a closed `CanonicalRecoveryTailInputs f primeData
  rows_pos trueSupports`: package the expected true-factor data and per-class
  Mignotte product congruences via the landed canonical-support representation
  surfaces.

Both producers feed directly into the new
`factorFast_ne_none_of_factorFastCapSeparationInputsAndCanonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`.

## Blockers

No technical blocker for this slice.  The new constructors are entirely
additive: existing callers (`factorFast_ne_none_of_canonicalRecoveryInputs_internalCapPositiveAndPrimeLowerBound`,
`factorFast_ne_none_of_factorFastCapSeparationInputsCanonicalRecoveryTailInputs`)
continue to work unchanged.

## Verification

- `lake build HexBerlekampZassenhausMathlib.TerminationBound` passed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `lake build HexBerlekampZassenhaus` passed.
- `python3 scripts/check_dag.py` passed (exit 0).
- `git diff --check` passed.
- `git diff HexBerlekampZassenhausMathlib/Recovery.lean` added 126 lines, no
  deletions; no `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` in the
  added lines.
