# 2026-06-08 11:08Z — #6576 LLL correctness capstone: decomposed

## Accomplished

Worker-led decomposition of #6576 ("prove lll output is LLL-reduced +
unconditional short-vector"). After orienting on `HexLLL/Basic.lean`,
`HexLLLMathlib/Basic.lean`, `HexLLLMathlib/Independent.lean` (1495 lines
of existing LLL infrastructure, including the #6567 fuel-sufficiency
chain), and the `HexGramSchmidt/Update.lean` + `HexGramSchmidtMathlib/
Update.lean` adjacent-swap / size-reduce update lemmas, concluded that
deliverable 1 alone is the standard LLL loop-invariant proof and runs
to roughly 800–1300 lines of new Lean. This exceeds a single-session
budget; the issue itself flagged the loop invariant as "likely the
largest piece — split out a dedicated invariant lemma if it exceeds
the size budget; worker-led decomposition is fine."

Created four sub-issues against #6576:

- #6587 (A): `sizeReduce` makes row `k` size-reduced — rounding
  correctness of `nearestQuotient` plus foldl induction over
  `(List.finRange k).reverse` using `coeffs_sizeReduce_above_pivot`.
- #6588 (B): `swapStep` preserves prefix LLL invariants for `i < k-1`
  via `basis_adjacentSwap_of_lt` + a coefficient-preservation
  corollary (may need a small Mathlib-side helper).
- #6589 (C): integer Lovász check (`δ.den * (dNext * dPrev + B²) ≥
  δ.num * dk²`) ↔ rational Lovász condition, via `gramDet_succ_rat`
  + `lllState_ν_eq_coeffs`.
- #6590 (D): loop invariant definition, fuel induction over
  `lllLoop`, capstone `isLLLReduced (lll b δ ... hind) δ`,
  unconditional short-vector corollary. Depends on A, B, C.

#6587–#6589 are independent and can run in parallel. #6590 is blocked
on all three and packages the loop induction + the two public
capstones (#6576 deliverables 2 and 3).

Skipped #6576 with the `Decomposed into #X, #Y, #Z, #W` breadcrumb
for the next planner cycle.

## Current frontier

#6576 is now decomposed; the LLL correctness capstone is unblocked at
the leaf level. The Gram-Schmidt update infrastructure already in
`HexGramSchmidt/Update.lean` and `HexGramSchmidtMathlib/Update.lean`
covers all of the basis/coefficient transition lemmas the sub-issues
need, so no new Gram-Schmidt-side prerequisites have to be filed.

## Next step

Workers pick up #6587, #6588, #6589 in parallel; once all three merge,
a worker claims #6590 for the loop induction + capstone bundle.

## Blockers

None for the decomposition. The sub-issues themselves are sized to
single sessions against the existing infrastructure.
