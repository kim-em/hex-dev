# Session 74343b06 — switch square-free Yun execution to scaled multiplicities (#3161)

## Accomplished

- Switched `squareFreeAuxRev` / `squareFreeAuxRevContribution` derivative-active
  branches to the scaled `yunFactorsWithLevel` / `yunFactorsContributionWithLevel`
  helpers introduced by #3160, with entry point `base := multiplicity, level := 1`.
- Refactored the multiplicity-insensitive invariant families to recurse over a
  fixed `base` plus advancing `level`:
  - `yunFactorsCurrentTailCoprime`, `yunFactorsPairwiseReady`,
    `yunFactorsPairwiseInvariant`;
  - `yunFactorsWithLevel_factor_mem_acc_or_dvd_current` /
    `_factor_dvd_current`;
  - `yunFactorsWithLevel_current_tail_coprime_of_common_dvd_one`,
    `_current_repeated_coprime_of_common_dvd_one`,
    `_factors_coprime_repeated_of_reachable`;
  - `yunFactorsPairwiseReady_{step,succ_of_current_tail,succ_of_common_dvd_one,
    of_reachable_common_dvd_one,of_derivative_split_common_dvd_one}`;
  - `yunFactorsPairwiseInvariant_{step,of_derivative_split,
    of_derivative_split_common_dvd_one,of_derivative_split_reachable}`;
  - `yunFactorsWithLevel_pairwise_coprime_nil_of_ready`, `_of_invariant`,
    `_pairwise_coprime_nil`, and the `_factors_squareFree_of_*` chain.
- Updated structural lemmas to `WithLevel`: `_reverse_append`,
  `_repeated_eq_nil`, `_repeated_dvd_repeated_of_acc`, `_repeated_dvd_repeated`.
- Updated `squareFreeAuxRev_reverse_append`,
  `squareFreeAuxRev_reconstruction_invariant`,
  `squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant`,
  `squareFreeAuxRev_factors_squareFree` to use the scaled-loop API.
- Reshaped the pre-existing tail-cross sorry to
  `yunFactorsWithLevel_squareFreeAuxRev_tail_cross_coprime` with multiplicity
  `base * level * p`, matching the new execution. This is the target of #3122.
- Flipped the PR #3136 characteristic-2 guard from `!=` to `==`: the corrected
  scaled execution now reconstructs `(X + 1)^6` over `F_2` exactly, where the
  level-3 factor in the derivative-zero descent receives `base * level = 2 * 3 = 6`.

## Verification

- `lake build HexPolyFp.SquareFree` — clean (2 pre-existing sorries only: 2767
  `squareFreeAuxRevContribution_derivative_active_pow_obligation` for #3153,
  4209 `yunFactorsWithLevel_squareFreeAuxRev_tail_cross_coprime` for #3122).
- `lake build HexPolyFp` — clean.
- `lake build HexBerlekamp` — clean (downstream squarefree consumer unaffected).
- `python3 scripts/check_dag.py` — clean.
- `git diff --check` — clean.
- `grep -nE '^axiom|native_decide|TODO|FIXME' HexPolyFp/SquareFree.lean` — clean.

## Current frontier

The square-free decomposition over `F_p` now produces the correct
`{factor, multiplicity}` records for all derivative-zero cascading inputs
(level → `base * level` rather than `multiplicity + (k - 1)`). Pairwise
coprimality and per-factor square-freeness theorems track the new shape.

## Next step

- #3153 (HexPolyFp: prove scaled square-free weighted product reconstruction)
  is now unblocked at the executable level. Its theorem chain feeds into
  `squareFreeAuxRevContribution_derivative_active_pow_obligation` at line 2777.
- #3122 still needs #3155 to land before the tail-cross sorry at line 4209
  becomes tractable.

## Blockers

None for this issue.
