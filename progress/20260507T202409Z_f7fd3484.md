# 2026-05-07 20:24 UTC — Rat row-add basis public API (f7fd3484)

## Accomplished

Claimed issue #2525 after the human-oversight queue proved dependency-blocked.
Added public `GramSchmidt.Rat.basis_rowAdd` in
`HexGramSchmidt/Basic.lean`, exposing the existing private
`basisMatrix_rowAdd` invariant for `Matrix.rowAdd b src dst c` when
`src.val < dst.val`.

Filed follow-up issue #2606 for the remaining rational coefficient update
formulas and left a decomposition breadcrumb on #2525. The follow-up records
the pivot-column caveat: the current degenerate convention hardcodes diagonal
`coeffs` entries as `1`, while `projectionCoeff` is `0` for zero-norm basis
rows, so the pivot formula needs a public nondegeneracy surface or an explicit
nonzero-norm hypothesis.

Verification passed:

- `lake build HexGramSchmidt.Basic`
- `lake build HexGramSchmidt.Update`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME' HexGramSchmidt/Basic.lean`

## Current frontier

The rational row-add basis invariance surface is public and available for
downstream wrappers once this partial slice lands. The coefficient formulas are
split into #2606 and blocked on this issue.

## Next step

Land the partial PR for #2525, then complete #2606 by proving the lower,
pivot, above-pivot, and non-destination coefficient update theorems without
exposing private implementation details in public hypotheses.

## Blockers

No blocker for the partial basis-invariance slice. The pre-existing
pod-managed `.claude/CLAUDE.md` worktree modification remains untouched and
uncommitted.
