# 20260517T050153Z — feature session 8282a24c (#3970)

## Accomplished

Claimed #3970 and completed the remaining tractable Mathlib bridge item in
`HexBerlekampZassenhausMathlib/Basic.lean`.

- Reworked `irreducibleByFactorization` to delegate to the existing
  Mathlib-free executable checker
  `Hex.ZPoly.isIrreducible (HexPolyZMathlib.ofPolynomial f)`.
- Proved `irreducibleByFactorization_iff` by composing
  `Hex.ZPoly.isIrreducible_iff`,
  `Hex.ZPoly.Irreducible_iff_polynomialIrreducible`, and
  `toPolynomial_ofPolynomial`.
- Moved the `irreducibleDecidablePred` instance below the bridge theorem so
  declaration order is explicit.
- Removed the now-dead local `isUnitFactor` / `nonUnitFactorCount` helpers.

This removes the `irreducibleByFactorization_iff` theorem-level `sorry`. The
remaining `HexBerlekampZassenhausMathlib/Basic.lean` theorem-level `sorry` near
the top is `factor_irreducible_of_nonUnit`, which is the HO-1 capstone tracked
by #4170 and blocked on #2567 / the current HO-1 substrate chain.

Verification run:

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/Basic.lean`
- `git show HEAD:HexBerlekampZassenhausMathlib/Basic.lean | rg -n "sorry|axiom|native_decide|TODO|FIXME"`

The focused grep changed from two top-of-file theorem-level `sorry`s to one;
the remaining analytic `sorry`s in the lower Hensel-substrate helpers are
pre-existing.

## Current frontier

#3970's named deliverables are now covered on this branch:

- `factor_product` was already proved on `HEAD`.
- `irreducibleByFactorization_iff` is proved here.
- `checkIrreducibleCert_sound` was already proved on `HEAD`.
- `factor_unique` was already proved on `HEAD`.

The adjacent `factor_irreducible_of_nonUnit` placeholder remains intentionally
out of this issue's deliverable list and is still tied to the HO-1 capstone
work.

## Next step

Continue the HO-1 chain rather than expanding #3970: once #4563 lands, #4561
can assemble the exhaustive-branch umbrella that feeds #4170. That is the next
useful path toward removing `factor_irreducible_of_nonUnit`.

## Blockers

None for #3970's scoped deliverables. The broader default factor-entry
irreducibility capstone remains blocked on the HO-1/BHKS work tracked by #4170
and #2567.
