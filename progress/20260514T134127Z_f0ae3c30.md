# Session 2026-05-14 13:41Z (f0ae3c30)

## Accomplished

Closed issue #4002: `HexGramSchmidt: wrap no-pivot leading-prefix sync at
bareissNoPivotData level`.

Added three private helpers and the wrapper theorem in
`HexGramSchmidt/Int.lean`:

- `noPivotLoop_step_eq_add_of_singularStep_none`: with sufficient room and
  no singular step at start or end, the step counter advances by exactly
  `fuel`.
- `noPivotLoop_step_monotone`: the step field is non-decreasing under
  further iterations.
- `gramDetVecEntry_bareissNoPivot_eq_at_r`: the `gramDetVecEntry` slot at
  `r + 1` is determined by the `noPivotLoop r` intermediate state. Proof
  case-splits on whether the `r`-iteration state already recorded a
  singular step (fixed point) or not (diagonal preserved by
  `noPivotLoop_diag_of_le_step`, with the late-singular-at-`r` subcase
  matching the zero pivot).
- `gramDetVecEntry_bareissNoPivot_full_eq_leadingPrefix`: the wrapper
  itself. Composes the existing
  `noPivotLoop_full_eq_leadingPrefix_at_gramDetVecEntry` with two
  applications of the new helper, then reduces both sides to a match on
  the shared singular-step value.

## Current frontier

The downstream sorry at `gramDetVecEntry_eq_leadingPrefix_bareiss` still
needs to bridge `bareissNoPivotData` to the public row-pivoted
`Matrix.bareiss`. That is the final assembly issue #3974.

## Next step

Plumb the new wrapper into the public Bareiss bridge as #3974 lands.

## Blockers

None for this issue. `lake build HexGramSchmidt.Int` and
`lake build HexGramSchmidt` succeed; `python3 scripts/check_dag.py`
returns clean; sorry count in `HexGramSchmidt/Int.lean` is unchanged (4)
relative to baseline.
