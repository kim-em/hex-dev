# 5d836d81 - work #6006

## Accomplished

- Claimed #6006 after checking directives and the unclaimed queue.
- Added ordered-row bridge lemmas in `HexMatrixMathlib/Determinant/Core.lean` for the `p1 < p2 < p3 < q` `nMatrix B p1 q` setup.
- Proved the `skipIndex2` identities for the original `p2` and `p3` rows, exposed the corresponding `nMatrix` row equalities, and added row-distinct/setRow preservation helpers.
- Verified the partial slice with `lake build HexMatrixMathlib`, `git diff --check`, and a forbidden-token grep over the touched Lean diff.
- Split the remaining determinant/sign transport into #6064 and left the required `Decomposed into #6064` breadcrumb on #6006.

## Current frontier

#6006 is partially complete. The row-indexing substrate for instantiating #5999 with `M = Hex.Matrix.nMatrix B p1 q` is in place; the remaining work is the signed determinant transport from those row replacements to the five `Hex.Matrix.nDet` minors.

## Next step

Work #6064: prove the cofactor-row-pairing and two-row setRow determinant transports to signed `nDet` minors, then use them in #6012's raw ordered four-row Plucker kernel.

## Blockers

- No technical blocker for the completed slice.
- A pre-existing `.claude/CLAUDE.md` worktree modification remains untouched and is not part of this work.
