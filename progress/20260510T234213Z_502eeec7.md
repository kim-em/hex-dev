# 2026-05-10 23:42 UTC — feature session (502eeec7)

## Accomplished

- Claimed #2840 (HexMatrix: group injective column tuples into squared minors),
  recognized that the grouping theorem requires three substantial layers
  (strictly-increasing tuple enumeration, sort/permutation factorization, final
  grouping proof), and decomposed it into #3178, #3179, #3180 with ordering
  dependencies. Skipped the parent for replan with a `Decomposed into …`
  breadcrumb.
- Claimed #3178 (enumerate strictly increasing column tuples) and added the
  enumeration scaffolding to `HexMatrix/Determinant.lean`:
  - `selectedColumnTuplesUpTo m n bound` — push-based recursive helper that
    enumerates strictly increasing length-`n` tuples in `Fin m` whose entries
    are all strictly less than `bound`.
  - `selectedColumnTuples n m` — public entry point (bound = `m`).
  - `IsStrictlyIncreasingColumnTuple` predicate.
  - Forward and backward membership characterizations
    (`mem_selectedColumnTuplesUpTo_imp`,
    `mem_selectedColumnTuplesUpTo_of_strictly_increasing`) plus the public
    `mem_selectedColumnTuples_iff`.
  - `isStrictlyIncreasingColumnTuple_injective` and the convenience corollary
    `mem_selectedColumnTuples_injective` (the induced
    `columnTupleVectorFn` is `Function.Injective`).
  - `selectedColumnTuples_nodup`, via an outer-list flatMap-Nodup helper that
    threads inner Nodup through `Vector.push` injectivity and uses last-element
    distinctness for cross-list disjointness.
- Verified with `lake build HexMatrix.Determinant`, `lake build HexMatrix`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the banned-marker
  scan over `HexMatrix/Determinant.lean`.

## Current frontier

- #3178 is implemented locally and ready to publish as a PR.
- #3179 (sort/orbit factorization) is blocked on #3178 and should consume the
  enumeration above to factor any injective `columnTupleVectorFn` as
  `selected ∘ permutation`.
- #3180 (final `det_gramMatrix_eq_sum_minors_sq`) is blocked on #3179.

## Next step

- Open the PR for #3178 and exit; the next worker can pick up #3179 once the
  sub-issue lands.

## Blockers

- None for #3178.
