# HexRCF SPEC relocation

## Accomplished

- Moved SPEC/Libraries/hex-rcf.md to HexRCF/SPEC/hex-rcf.md, the
  per-library location the release sync resolves and status.py already
  printed. Updated the six live references: the proof-sweep runner and
  its unit test (hard-coded paths), the central SPEC index, the
  poly-smith cross-reference, and the hex-real-roots-mathlib sibling
  link that had been broken since the SPECs moved per-library.
  Historical progress files keep the old path as journal fact.
- Proof-sweep unit tests, check_phase4, check_phase7, and check_dag
  all pass after the move.

## Current frontier

- HexRCF's publish blockers remaining: the released.yml entry with the
  13-module test_modules move (atomic, wave task F22) and the bench
  sidecar targets for ProofProbe in the skeleton (F21).

## Next step

- F21 payload matrix and skeleton seeding for the eight repos.

## Blockers

- None.
