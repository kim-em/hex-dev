# Progress — 2026-06-18 (session 86baa868, /work → /feature #7995)

## Accomplished

Completed #7995: HexHensel Phase 6 grind-automation coverage for
`HexHensel/Multifactor.lean`.

Added `@[grind =]` alongside the existing `@[simp]` on the six
equation-shaped public characterising lemmas (now `@[simp, grind =]`,
matching the `HexModArith/Basic.lean` / `HexMatrix/Basic.lean`
convention):

- `one_mul_zpoly` — `(1 : ZPoly) * g = g`
- `polyProduct_singleton` — `polyProduct #[g] = g`
- `polyProduct_empty` — `polyProduct #[] = 1`
- `polyProduct_append` — `polyProduct (xs ++ ys) = polyProduct xs * polyProduct ys`
- `polyProduct_pair` — `polyProduct #[g, h] = g * h`
- `polyProduct_replicate_zero_toArray` — `polyProduct (replicate 0 g).toArray = 1`

These are exactly the six candidates the directive listed. The two
deliberately-untagged structural splitters (`polyProduct_singleton_append`,
`polyProduct_cons_toArray`) were left untouched — their docstrings explain
they are applied explicitly downstream and should not be `simp`/`grind`
normal forms; `list_foldl_mul_eq_mul_foldl_one` is likewise not a normal
form.

## Decision: no in-file proof shortened

Deliverable 2 allows either shortening an in-file proof or confirming a
downstream consumer benefits. The in-file proofs (`multifactorLiftList_spec`)
are explicit congruence-threading `simpa [...] using` chains; `grind` does
not shorten them and forcing it would be contrived (the directive warns
against this). The payoff is **downstream `grind =` availability**: confirmed
`polyProduct` consumers exist in `HexHensel/Conformance.lean`,
`HexHensel/CrossCheck.lean`, and the `HexBerlekampZassenhaus` layer
(`Basic.lean`, `Conformance.lean`, `CrossCheck.lean`, …). No proof bodies
changed.

## Verification

- `lake build HexHensel` → 112 jobs, `Build completed successfully`, no
  `error`/`warning`. `Multifactor` builds in 527ms (no grind-loop/slowdown).
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `libraries.yml` `done_through` unchanged; no `*Mathlib` bridge touched;
  no new imports.

## Current frontier

#7995 complete; PR to follow. Sibling Phase 6 grind passes remain
(`#7996` HexGF2/Irreducibility.lean still unclaimed).

## Next step

Open PR for #7995. Successor can claim #7996.

## Blockers

None.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
