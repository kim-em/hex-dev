# Accomplished

- Reconciled the semantic-replay branch with current `main`, preserving both the interval target registration and the newer factorization measurement exemptions.
- Updated the exact Lake source exemption for the merged target set.
- Narrowed two fact-dispatch docstrings so they accurately describe where full action-origin equality is enforced.
- Received a fresh independent Opus review of exact pre-merge head `47c69c272`; it found no soundness blocker and independently confirmed exact green CI and the absence of banned proof mechanisms.

# Current frontier

- The semantic registry layer is ready for a new exact CI run after the merge reconciliation.

# Next step

- Build the focused semantic targets, push the reconciliation, and merge the PR once exact CI is green.

# Blockers

- None in the semantic layer; only the new exact CI run remains.
