# Work session a0dc9965 — issue #3612

## Accomplished

- Claimed #3612 and widened the six unresolved `HexGfqField` benchmark schedules with a CI-safe degree-5 `F_7` irreducible modulus.
- Added the degree-5 certificate and `bundleForN 5` in `HexGfqField/Bench.lean` without introducing a Conway dependency.
- Exported `reports/bench-results/hex-gfq-field-n5-n8-schedule.json` and updated `reports/hex-gfq-field-performance.md` with the new verdicts, artefact hash, and remaining concern.
- Verified `lake build HexGfqField.Bench`, `lake exe hexgfqfield_bench list`, `lake exe hexgfqfield_bench verify`, the scientific benchmark export, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexGfqField`, `git diff --check`, and the focused banned-marker scan.

## Current frontier

- `HexGfqField` remains in Phase 4 with `done_through: 3`.
- The degree-5 rung widens evidence for the six unresolved targets, but all six remain inconclusive.
- `runNegSubChecksum` and `runFrobChecksum` remain consistent on their existing degree-8 schedule.

## Next step

- Plan a larger calibration path for the six unresolved targets that avoids normal-CI degree-12/16 certificate elaboration.

## Blockers

- None.
