# 20260509T211719Z - Issue #2867

## Accomplished

Claimed #2867 after checking the unclaimed queue. The older HexPolyFp issues
still pointed at open dependency work, while #2867's prerequisite #2866 was
closed, so I selected the Matrix determinant expansion chain.

Added a compiling helper layer in `HexMatrix/Determinant.lean` for the ordered
column-tuple expansion:

- `columnSumMatrix`, representing the square matrix whose columns are finite
  linear combinations of source columns.
- `columnSumMatrix_entry` and `colReplace_columnSumMatrix_self` for entry-level
  normalization.
- `det_columnSumMatrix_expand_column`, the one-column determinant expansion
  specialized to `columnSumMatrix` using `det_colReplace_sum_list`.

Created follow-up issue #2878 for the remaining all-column induction and left a
`Decomposed into #2878` breadcrumb on #2867.

Verification completed:

- `lake build HexMatrix.Determinant`
- `lake build HexMatrix`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexMatrix/Determinant.lean`

## Current frontier

The full all-column ordered tuple theorem is not proved yet. The next proof
needs an induction over determinant columns carrying a partial ordered-column
assignment, with the base case identifying the fully chosen matrix with
`columnTupleMatrix source (columnTupleVectorFn cols)` and the accumulated
coefficient product with `columnTupleCoeff coeff cols`.

## Next step

Land #2878 after this partial PR merges: prove
`det_columnSumMatrix_eq_sum_columnTuples` (or an equivalent directly usable
statement) from `det_columnSumMatrix_expand_column`, `columnTupleVectors`, and
`columnTupleCoeff`.

## Blockers

No technical blocker for the helper layer. The remaining theorem was scoped out
because it is a separate proof-sized induction rather than a direct application
of the single-column expansion lemma.
