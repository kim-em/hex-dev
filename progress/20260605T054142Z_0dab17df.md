# 2026-06-05 05:41Z — hex-lll fuel termination spec (#6577)

## Accomplished

- Updated `SPEC/Libraries/hex-lll.md` so the main-loop snippet matches the
  as-built Route A fuel design:
  - `lllLoop` recurses structurally on `fuel`.
  - `lllFuel s = (s.potential + 1) * (n + 1)`.
  - `lllAux` is a thin wrapper with no `s.b.independent` argument.
- Rewrote the termination formalization strategy to keep the potential
  swap-decrease mathematics while moving it into the Mathlib-side
  fuel-sufficiency theorem.
- Documented why in-place well-founded recursion was rejected: the
  Mathlib-free `decreasing_by` import wall and the proof-free BZ consumer path
  that cannot supply independence at every recursive call.
- Classified the `fuel = 0` branch as `unreachable-by-pipeline-invariant`.

## Current frontier

The LLL spec now describes the executable fuel recursion and the separate
proof-layer sufficiency argument consistently with `HexLLL/Basic.lean`.

## Next step

Use the spec as the contract for the Mathlib-side fuel-sufficiency proof chain.

## Blockers

None.
