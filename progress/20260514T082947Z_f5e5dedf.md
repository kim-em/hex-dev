# 2026-05-14 08:29 UTC — work session f5e5dedf on #3959

## Accomplished

- Claimed #3959 and threaded the fuel-bounded
  `SquareFreeAuxRevPairwiseResidualProvider` shape (introduced by #3958
  in PR #3962) through the downstream pairwise-coprime wrapper cascade
  in `HexPolyFp/SquareFree.lean`:
  - `squareFreeAuxRev_pairwise_coprime_nil_core` now delegates to
    `squareFreeAuxRev_pairwise_coprime_nil_core_of_yun_invariant` and
    takes `residualProvider`, `stateProvider`, `hfuel`, `hreachable`.
  - `squareFreeAuxRev_pairwise_coprime_core`,
    `squareFreeAuxRev_pairwise_coprime_of_acc`, and
    `squareFreeAuxRev_pairwise_coprime_nil` pass the four new
    parameters through unchanged.
  - Public conditional `squareFree_pairwise_coprime` now takes
    `residualProvider` and `stateProvider`. It instantiates `hfuel`
    with `Nat.lt_succ_self _` and `hreachable` via the existing
    `normalizeMonic_squareFreeContributionReachable hp f`.
- No new helper step wrapper was needed: each wrapper is a one-line
  pass-through, so deliverable 2 is vacuous.

## Current frontier

- Pairwise wrappers now consume the richer provider shape directly.
- The old `_of_residual_invariant` core (lines ~6576–6668) is dead
  code after this change — it was kept by #3958 specifically as a
  transitional scaffold and is no longer referenced anywhere. Removing
  it is out of scope for #3959 ("thread through wrappers") but is the
  obvious follow-up cleanup.
- `squareFree_weightedProduct` still uses the old
  `squareFreeAuxRevResidualSatisfied_of_invariant` glue; that path is
  independent of the pairwise cascade and not part of this issue.

## Next step

- Downstream assembly (#3927) supplies concrete
  `residualProvider`/`stateProvider` witnesses to discharge the
  hypotheses on the public `squareFree_pairwise_coprime`.
- Follow-up cleanup issue to remove the now-unused
  `squareFreeAuxRev_pairwise_coprime_nil_core_of_residual_invariant`.

## Blockers

- None for #3959.
