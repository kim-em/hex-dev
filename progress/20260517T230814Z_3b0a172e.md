# Session 3b0a172e — #4842 quadratic factorPower fuel bound + discharger

## Accomplished

In `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, landed the #4842
deliverables: a reusable fuel-bound lemma for `factorPower` exponent lists
and the public quadratic-arm `hcomplete` discharger.

New top-level additions inside `namespace IntReductionMod`:

* `factorPower_decomposition_exponent_size_bound` (public): given a
  `factorPower`-fold decomposition `rp = foldl_mul (zip.map factorPower)` with
  `rp ≠ 0` and each core factor of positive degree, every exponent `e` in the
  zipped list satisfies `e + 1 ≤ rp.size + 1`. Proves via size lower bound on
  `factorPower q e` from positive degree (private helper
  `factorPower_size_ge_of_degree_pos`), divisibility of `factorPower q e` by
  the full fold (private helper `zpoly_mem_dvd_foldl_mul_one`), and transfer
  through `Hex.ZPoly.size_le_of_dvd_nonzero`.

* `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  (public): the #4747 residual discharger. Composes the per-factor invariants
  from `Hex.quadraticIntegerRootFactors?_*` (irreducibility, polyProduct
  shape, sign-normalisation, positive leading coefficient, positive degree),
  the #4759 `factorPower` decomposition theorem
  (`normalizeForFactor_repeatedPart_isFactorPower_polyProduct_of_irreducible_factors_cover`),
  the no-tail-divisibility theorem
  (`factorPower_cover_not_dvd_tail_of_irreducible_squarefree`), the fuel
  bound above, and the non-monic public expansion-complete surface
  `Hex.expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
  (#4778). The repeated-part non-zero hypothesis is discharged inline
  mirroring the argument from `normalizeForFactor_squareFreeCore_toPolynomial_squarefree`.

Supporting private helpers inside the same namespace:

* `size_ge_two_of_degree_pos`: `0 < q.degree?.getD 0 → 2 ≤ q.size` (extracted
  from the existing inline derivation in
  `reassemblyExpansionComplete_singleton_of_irreducible`).
* `factorPower_size_ge_of_degree_pos`: `e + 1 ≤ (factorPower q e).size`
  under positive degree of `q`.
* `zpoly_mem_dvd_foldl_mul_one`: `x ∈ l → x ∣ l.foldl (· * ·) 1` for
  `Hex.ZPoly`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — passes
  (175s warm).
* `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus` —
  passes.
* `python3 scripts/check_dag.py` — passes (no output).
* `git diff --check` — clean.
* `git diff origin/main` for `sorry|axiom|native_decide|TODO|FIXME` on
  additions: 0 new entries.

## Current frontier

The quadratic-arm `hcomplete` discharger is now available without any
explicit `coreFactors`-shape side condition: callers in the fast-path and
slow-path quadratic umbrellas
(`factor_quadratic_branch_entry_irreducible_of_quadraticRoots` and
`factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`) can
drop the `hcomplete` premise once the eventual capstone wiring (#4170)
arrives.

## Next step

A successor session can:

* Rewire the fast-path and slow-path quadratic arm umbrellas to consume
  the new discharger internally, removing their explicit `hcomplete`
  premises — analogous to the constant arm's #4585 / #4598 wiring.
* Investigate whether the exhaustive arm `hcomplete` Gap 2 (the explicit
  `reassemblyExpansionComplete` premise in
  `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`)
  can be discharged by a parallel exhaustive-arm `hcomplete` substrate
  (the `coreFactors` shape there depends on the recombination output,
  which is a separate substrate sub-issue).

## Blockers

None.
