# Accomplished

- Replaced the opaque imperative square gluer with a structurally recursive
  union-by-insertion implementation and proved exact multiset coverage,
  connectedness, cross-component separation, duplicate behavior, and that the
  `glueCovered` fallback is unreachable.
- Proved a quantitative converse to the executable `T₀` check. At
  `separationDepth`, every retained square is associated with a unique nearby
  root, and edge connectivity propagates that association through a component.
- Proved the sharp survivor bound gives at most a two-level precision loss in
  `encSquare`, including the executable dyadic `ceilLog2` rounding theorem.
- Combined the enclosure bound with fixed-depth NK certification: a
  root-bearing glued component at leaf precision `separationDepth + 3` passes
  both `.nk` and `.nkThenPellet`, for its actual `candidateK`, and the returned
  certificate still covers the designated root.
- Added four-corner NK/mixed certification and separated two-component gluing
  regressions, and updated the three benchmark hashes whose deterministic
  output ordering changed with the gluer. The full
  build, full conformance build, all 14 HexRoots benchmark smoke checks, and
  repository structural checks pass.
- Corrected #8830 and the SPEC to distinguish root-wise loop coverage from the
  stronger component-wise containment assertion. Opus independently reviewed
  the full diff, found no soundness defects, and recommended the two additional
  executable guards above, which are now green.
- Fixed the first CI run's fixture-order failure by canonically sorting
  serialized certificate objects. Fresh HexRoots emission now exactly matches
  the regenerated committed JSONL; certificate order remains semantically
  unobservable to the root oracle.

# Current frontier

The component-width and root-bearing certification slice is complete and has
passed its pre-publication review and checks.

# Next step

Address any actionable Opus findings, publish and merge the #8830 PR, then
prove driver completeness by closing the rootless-survivor-halo alternative
and wiring the resulting all-ready depth into the fuel-based loop.

# Blockers

Final driver completeness cannot infer that every survivor component contains
its associated nearby root. The remaining proof must show a rootless halo
joins its root component or disappears within the fixed fuel budget.
