# Work session: HexGramSchmidt sizeReduce unchanged-entry API

## Accomplished

- Claimed #2226 (label: `feature`).
- In `HexGramSchmidt/Update.lean`, added four new theorem signatures
  (proofs left as `sorry`, per the issue's deliverables):
  - `coeffs_sizeReduce_other_row` — for any `i ≠ k`, the row at `i` of
    `coeffs (sizeReduce b j k r)` matches the row at `i` of `coeffs b`.
  - `coeffs_sizeReduce_above_pivot` — for row `k` and column `l` with
    `j.val < l.val < k.val`, the entry of `coeffs` is unchanged.
  - `scaledCoeffs_sizeReduce_other_row` and
    `scaledCoeffs_sizeReduce_above_pivot` — the corresponding
    `scaledCoeffs` analogues.
- Grouped each new pair next to the existing `coeffs_sizeReduce_*` and
  `scaledCoeffs_sizeReduce_*` API for the changed-entry cases.
- Verified:
  - `lake build HexGramSchmidt.Update`
  - `lake build HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - Touched file scanned for forbidden placeholders (no `axiom`, no
    `native_decide`, no `TODO`/`FIXME`); only the four newly introduced
    `sorry`s are present.

## Current frontier

`HexGramSchmidt/Update.lean` now exposes a complete unchanged-entry surface
for `sizeReduce`, alongside the existing changed-entry pivot/lower lemmas.

## Next step

Downstream issue can discharge `LLLState.sizeReduceColumn.ν_eq` in
`HexLLL/Basic.lean` using these new theorems without inlining
Gram-Schmidt internals. Separate issues should later prove the four new
`sorry`s, ideally by reducing each to `basis_sizeReduce` plus the
`coeffMatrix`/`castIntMatrix` definition.

## Blockers

None.
