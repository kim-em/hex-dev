# Progress - session 99b094e3

## Accomplished

Claimed and completed #3870 in `HexPolyFp/SquareFree.lean`.

- Added `yunFactorsPairwiseReachable_residual_dvd_derivative_product`.
- Proved the derivative-split base case by reconstructing
  `(f / gcd f f') * gcd f f' = f` and applying `DensePoly.gcd_dvd_right`.
- Proved the step case by invoking the landed
  `yunStep_residual_dvd_derivative_product_of_previous` transport lemma.

Verification:
- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^axiom\b|native_decide|TODO|FIXME|sorry' HexPolyFp/SquareFree.lean`
  returned no matches.

## Current frontier

The reachable residual product-divisibility invariant is available for the
terminal derivative-zero proof.

## Next step

Use `yunFactorsPairwiseReachable_residual_dvd_derivative_product` to prove the
terminal reachable Yun residual derivative-zero lemma tracked by the downstream
issue.

## Blockers

None.
