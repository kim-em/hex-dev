# Work session — #4334 (HexLLL Phase 4 inconclusive verdicts)

## Accomplished

Claimed [#4334](https://github.com/kim-em/hex/issues/4334) under
session `9132f906`, then skipped (marked `replan`) for the same
host-contention reason documented in the existing skip chain. The
quantitative probe:

- `uptime` at claim: loadavg 11.96 / 10.54 / 11.38 on 24 cores.
- `time lake exe hexlll_bench list`: 86.71s wallclock (252.45s
  user / 19.25s sys / 313% cpu), vs ~33ms clean spawn-floor
  baseline in the `e211854d` artifact — ~2627× inflation. Squarely
  in the 1.2-1.3s+ spawn-floor regime the issue body warns drives
  every parametric registration below the signal floor or to cap.

Per `SPEC/benchmarking.md` §"Scientific timing runs", Phase 4
scientific verdict promotion requires a stable sub-second spawn
floor on quiet or dedicated hardware. Carica is still too loaded.

Posted a follow-up comment correcting the literal `--reason` text
in the auto-generated skip comment (caused by invoking
`coordination skip` with a flag instead of a positional argument)
with the actual quantitative reason.

## Current frontier

#4334 remains the sole unclaimed issue and remains environmentally
blocked. No code or report artifact changed this session.

## Next step

Re-dispatch when carica is idle enough for a stable sub-second
spawn floor, or run the release-candidate benchmarking workflow
on dedicated hardware (per `SPEC/benchmarking.md`). Future skip
sessions should pass the reason as a positional argument:
`coordination skip 4334 "..."`, not `--reason "..."`.

## Blockers

Host carica load. Not actionable from an autonomous worker
session; needs human/operator action or genuinely quiet carica.
