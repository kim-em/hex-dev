# 2026-05-05 05:01 UTC — `/work` session 924e2d43

Session type: `/work` (escape hatch). Claimed
[#2257](https://github.com/kim-em/hex/issues/2257) — *HexConway:
discharge ?_irreducible axiom (requires Rabin → FpPoly.Irreducible
bridge)*. After scoping, decomposed into two new sub-issues and
skipped the original. No code changes pushed.

## Accomplished

- Confirmed dependency [#2259](https://github.com/kim-em/hex/issues/2259)
  is closed and that
  [`Berlekamp.rabinTest_imp_irreducible`](HexBerlekamp/RabinSoundness.lean#L419)
  exists.
- Investigated the per-entry dispatch path:
  - Wrote a candidate proof discharging
    `luebeckConwayPolynomial_2_1_irreducible` via
    `Berlekamp.rabinTest_imp_irreducible` plus `by decide` /
    `by rfl` / `by simp [...]` for `Berlekamp.rabinTest f hmonic = true`.
  - All three failed: `decide` reports the `Decidable` instance "did
    not reduce to isTrue or isFalse"; `rfl` reports the LHS is not
    definitionally equal to `true`; `simp` with the full unfolding
    list reduces most scaffolding but leaves
    `Hex.FpPoly.powModMonicAux✝` unreduced.
  - Root cause:
    [`FpPoly.powModMonicAux`](HexPolyFp/Frobenius.lean#L21) is defined
    by `termination_by n => n` / `decreasing_by`, so kernel reduction
    is blocked. `native_decide` would discharge it but is project-banned.
- Filed
  [#2285](https://github.com/kim-em/hex/issues/2285) — *HexBerlekamp:
  kernel-reducible rabinTest evaluator for concrete monic FpPoly* —
  the foundational sub-issue, listing three viable paths
  (structural-recursion variant of `frobeniusXPowMod`,
  certificate-with-explicit-powChain after the GF2 pattern, or a
  simp-driven evaluator).
- Filed
  [#2286](https://github.com/kim-em/hex/issues/2286) — *HexConway:
  discharge ?_irreducible axiom (per-entry rabinTest dispatch)* — the
  dispatch sub-issue, with `depends-on: #2285`. Coordination
  auto-tagged it `blocked`.
- Left the standard `Decomposed into #2285, #2286` breadcrumb on
  [#2257](https://github.com/kim-em/hex/issues/2257) and ran
  `coordination skip 2257` to release the claim into `replan`.

## Current frontier

- [#2285](https://github.com/kim-em/hex/issues/2285) is the active
  blocker: it must land before any per-entry irreducibility proof in
  HexConway can succeed.
- The same blocker applies to the 16 `*_irr` `theorem … := by sorry`
  placeholders in
  [`HexGfqField/EmitFixtures.lean`](HexGfqField/EmitFixtures.lean) and
  the analogous Conformance / Bench placeholders — those will benefit
  for free once #2285 lands.
- Parent
  [#2244](https://github.com/kim-em/hex/issues/2244) (human-oversight)
  is now bottlenecked on #2285 → #2286.

## Next step

A worker on #2285 picks one of the three suggested paths. The
structural-recursion variant likely has the lowest implementation
risk: define `frobeniusXPowMod' f hmonic k` by structural induction on
`k`, prove it equals the existing `frobeniusXPowMod` via the
already-existing successor-step lemma
[`frobeniusXPowMod_succ`](HexPolyFp/Frobenius.lean#L296), and define a
`rabinTest'` that reduces by `decide` on record-literal moduli.

## Blockers

None on my side — work is unblocked but requires #2285 first.
