# 20260517T133052Z — feature session 58837371 (#4647 → #4717)

Session UUID prefix: 58837371. Claimed #4647 (HO-1 substrate: primitive
recursive recombination coverage), decomposed it into precursor #4717, and
executed #4717 in the same session.

## Accomplished

### #4647 decomposition

Scope assessment of #4647 against the corrected issue body (post-replan-triage)
confirmed the structural blocker called out in the previous skip: the executable
`Hex.recombinationSearchModAux` (lines ~4892 of `HexBerlekampZassenhaus/Basic.lean`)
builds its per-step candidate from the *unscaled* `liftedFactorProduct`, while
`RepresentsIntegerFactorAtLift` (line ~1319 of `HexBerlekampZassenhausMathlib/Basic.lean`)
is stated against the *scaled* product. The #4652 counterexample (modulus
`101`, `lc core = 2`, `factor = 2X + 1`) shows the unscaled candidate
normalises to `X - 50`, which is not even associated to the integer factor.
No sound primitive recursive coverage theorem can therefore be stated against
the existing executable.

The minimal valid substrate is a parallel scaled executable family, the
matching step lemma, and the scaled-candidate `exactQuotient?` /
`shouldRecord` discharges. Decomposed into a single self-contained precursor
sub-issue #4717 and skipped the parent with the `Decomposed into #4717`
breadcrumb; `coordination add-dep 4647 4717` re-marked the parent `blocked`
on the new substrate.

### #4717 implementation

Claimed #4717 in the same session and landed it on `agent/58837371`:

- `HexBerlekampZassenhaus/Basic.lean` (+121 lines)
  - `Hex.scaledRecombinationSearchModAux (coreLc : Int) (target modulus localFactors fuel)`:
    parallel executable using `DensePoly.scale coreLc` inside the
    centred-lift candidate.
  - `Hex.scaledRecombinationSearchMod` surface wrapper (length-based fuel,
    mirroring the unscaled `recombinationSearchMod`).
  - `Hex.scaledRecombinationSearchModAux_eq_some_of_step_of_prefix_none`:
    structurally identical to the existing unscaled step lemma, substituting
    `DensePoly.scale coreLc` into the prefix-none hypothesis and the goal.
- `HexBerlekampZassenhausMathlib/Basic.lean` (+41 lines)
  - `shouldRecord_scaledRecombinationCandidate_of_eq_factor`: mirror of
    `shouldRecord_recombinationCandidate_of_eq_factor`.
  - `exactQuotient?_scaledRecombinationCandidate_eq_some_of_eq_factor`:
    mirror of `exactQuotient?_recombinationCandidate_eq_some_of_eq_factor`,
    parameterised over a `target` polynomial (rather than `core`) since the
    recursive coverage proof will use it on intermediate quotient targets.

No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` (banned-token
counts in both files match `origin/main`). No production caller wired up;
that exposure is #4648's responsibility. `lake build HexBerlekampZassenhaus`
and `lake build HexBerlekampZassenhausMathlib` both pass clean.

## Current frontier

The substrate (#4717) is shipped on `agent/58837371` and a PR is being
opened. The parent #4647 remains `blocked` on #4717 and represents the
remaining proof composition: a primitive variant of the ~280-line
recursive coverage proof against `Hex.scaledRecombinationSearchModAux`.
That follow-up will likely need scaled-candidate analogues of the existing
primitive-pos-lc helper chain — at minimum
`coverAtMin_representingSubset_subset_of_recombinationCandidate_dvd_of_primitive_pos_lc_core`
and `liftedFactorSubsetPartition_prefix_none_of_primitive_pos_lc_core`
need scaled counterparts, since both currently reference the unscaled
`recombinationCandidate` in their statements. The next worker on #4647
should perform a fresh scope assessment with those helpers in hand and
may need to decompose further.

## Next step

After #4717 merges and `check-blocked` unblocks #4647, a worker claims
#4647 and either (a) lands the primitive recursive coverage theorem
directly if the existing primitive helpers suffice, or (b) decomposes
further into scaled-variant helper sub-issues (cover-at-min, prefix-none,
exists-representing-subset chain) before assembling the coverage proof.

## Blockers

None. #4647 remains correctly blocked on the in-flight #4717 PR; the
dependency graph routes through `check-blocked` once #4717 closes.
