# 2026-05-11T06:42Z — session f45717f8 (/work)

## Accomplished

- Claimed #3255 and confirmed it is the critical unclaimed HexPolyFp residual
  invariant prerequisite for #3239 and #3209.
- Added the compiling private helper
  `right_factor_derivative_isZero_of_mul_derivative_isZero_of_common_dvd_one`
  in `HexPolyFp/SquareFree.lean`. It proves that if `DensePoly.derivative
  (z * y)` is zero and `y` is coprime to `z`, then `DensePoly.derivative z`
  is zero, using the existing Bezout cancellation helper and the
  `f ∣ f'` degree contradiction.
- Created successor #3279 for the remaining quotient-transfer invariant needed
  to assemble #3255's original reachable-state theorem.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the banned-marker
  grep for `HexPolyFp/SquareFree.lean`. The only `sorry` remains the
  pre-existing one at line 2960.

## Current frontier

- The new helper supplies the final coprime factor-cancellation step once a
  Yun transition has produced a zero-derivative product `z * y`.
- The remaining proof obligation is to derive that zero-derivative product for
  `w / DensePoly.gcd c w` from a reachable source state, then assemble
  `yunFactorsPairwiseReachable_coprime_residual_derivative_zero`.

## Next step

- Work #3279: prove the quotient-transfer invariant in `HexPolyFp/SquareFree.lean`
  and then close the original #3255 target theorem.

## Blockers

None for this session.
