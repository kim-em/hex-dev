# 20260511T010906Z — repair session acfcb0cb

## Accomplished

Claimed repair for PR #3204 (`agent/7d39591a`,
`feat: bridge scaled Yun residual to unscaled`). The Linux `build` check
failed in the bench smoke gate on
`Hex.LLLBench.runIsabelleHarshCubicNormSq15` with `child error: child
exited with code 1` from the verified-Isabelle SVP binary; every other
Isabelle bench in the same run passed (random-bounded 30/60/120/240,
BZ recombination, harsh-cubic 30/45). The PR's only code change is a
37-line additive extension to `HexPolyFp/SquareFree.lean`
(`yunFactorsWithLevel` and `yunFactorsContributionWithLevel`), which is
unrelated to LLL or any Isabelle oracle.

Confirmed `lake build HexPolyFp.SquareFree` succeeds locally with only
the pre-existing `sorry` warnings on lines 2767 and 4330.

This is a recurring cold-cache flake: the previous repair sessions for
this exact benchmark (`progress/20260509T232311Z_7855e394.md`,
`progress/20260510T190657Z_d2574bc3.md`) widened the per-call budget
from 40 s → 90 s and salvaged PR #3090 by re-running CI on a fresh
commit.

Triggered `gh run rerun --failed 25644276429` and pushing this progress
note will land another fresh CI run on the branch.

## Current frontier

PR #3204 is `MERGEABLE` against main with auto-merge already armed.
The only blocker was the transient bench gate.

## Next step

`coordination mark-pr-salvaged 3204` and exit; auto-merge will land the
PR once the re-run reports green.

## Blockers

None.
