# 20260602T183833Z work #6345

## Accomplished

- Claimed #6345 after confirming there were no unclaimed directive issues.
- Added `ForwardRecoveryInputs.liftedFactorSubsetOfMembers`, its membership
  characterisation, and the canonical `liftedFactorSubsetsOfSupports` array
  induced by `supportPartitionByMinColumn`.
- Added `liftedFactorSubsetsOfSupports_getD` and
  `productCongruencesOfCanonicalSupportMemberRepresentations`, so callers can
  thread the canonical member-induced subset array directly into the existing
  per-class Mignotte product-congruence bridge.

## Current frontier

The proof-side subset array is now canonical and indexed by the support
partition, but #6345 is not fully discharged.  The remaining bridge is the
selector-product equality:

`Array.polyProduct (selectedFactorsOfMembers d.liftedFactors members) =
 liftedFactorProduct d (liftedFactorSubsetOfMembers d members)`.

I tried the direct stronger array equality between `selectedFactorsOfMembers`
and `liftedSubsetSelectedList`; it reduces to a nontrivial list-index
alignment between `List.range`/`Array.getD` and `List.finRange`/`zip`.

## Next step

Prove the selector-product equality, likely by reducing both sides through
`polyProduct_toPolynomial` and reusing the existing
`finRange_filter_mem_perm_toList`/`polyProduct_liftedSubsetSelectedList_eq_liftedFactorProduct`
pattern from `HexBerlekampZassenhausMathlib.Basic`.

## Blockers

No technical blocker for the partial bridge.  The remaining equality is larger
than the wrapper packaging landed here and should be its own focused proof
step.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib.IntReductionMod` passed.
- `lake build HexBerlekampZassenhausMathlib` passed.
- `python3 scripts/check_dag.py` passed.
- `git diff --check` passed.
- Added-line forbidden-token grep over
  `HexBerlekampZassenhausMathlib/Recovery.lean` found no `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
