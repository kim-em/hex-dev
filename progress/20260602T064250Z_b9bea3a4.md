# Progress: BHKS D1 coprime bridge accessor (#6261)

## Accomplished

Added `coprime_input_aux_over_rat_of_bridge_data` in
`HexBerlekampZassenhausMathlib/BadVector.lean`.

The theorem exposes the rational coprimality clause carried by
`BadVectorBridgeData` under the issue-specific name expected by downstream
bridge assembly.

Verification:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over
  `HexBerlekampZassenhausMathlib/BadVector.lean` was clean.

## Current frontier

`BadVectorBridgeData` now has a named rational-coprimality accessor alongside
the existing projection into `ProjectedBadVectorSetupBridge`.  The theorem is
an accessor because the merged #6260 foundation record already carries the
coprimality clause as part of the bridge data.

## Next step

Prove or expose the sibling modular-resultant divisibility bridge theorem for
#6262, then assemble the `ProjectedBadVectorSetupBridge` and cap wrapper in
the blocked follow-up #6263.

## Blockers

None for this session.  The worktree still contains an unrelated pre-existing
local modification to `.claude/CLAUDE.md`, which was not touched or staged.
