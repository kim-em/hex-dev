# Progress: BHKS D1 resultant bridge accessor (#6262)

## Accomplished

Claimed #6262 after #6261 was claimed by another agent.

Added `ExecutableBadVectorWitness.resultant_divisible_by_p_pow_of_bridge_data`
in `HexBerlekampZassenhausMathlib/BadVector.lean`.  The theorem exposes the
BHKS Lemma 3.2 modular-resultant divisibility clause from the merged
`BadVectorBridgeData` project-level package:

- input: `D : BadVectorBridgeData W trueSupports`;
- output:
  `((W.liftData.p ^ (W.liftData.k * W.localFactorDegree) : Nat) : Int) ∣
    Polynomial.resultant W.inputPolynomial W.auxiliaryPolynomial`.

Verification:

- `lake build HexBerlekampZassenhausMathlib.BadVector`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Forbidden-token diff grep over
  `HexBerlekampZassenhausMathlib/BadVector.lean` found no added `sorry`,
  `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Current frontier

The current `BadVectorBridgeData` surface already carries the modular
divisibility clause as a field, so the #6262 deliverable is an API exposure
of that field rather than a lower-level derivation from Hensel congruence and
Sylvester determinant data.

## Next step

If the project wants the full BHKS Lemma 3.2 divisibility proof from
executable CLD/Hensel data, introduce a narrower lower-level hypothesis
package for the selected lifted local factor and prove that package implies
`BadVectorBridgeData.resultant_divisible_by_p_pow`.

## Blockers

None for this session.  The worktree still contains an unrelated pre-existing
local modification to `.claude/CLAUDE.md`, which was not touched or staged.
