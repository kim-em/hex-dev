# Session 53443c0b — HexGramSchmidt Rat adjacent-swap current row (#3733)

## Accomplished

- Checked the unclaimed queue, confirmed no unclaimed `human-oversight`
  issue was available, and selected #3640 first as a downstream Phase 4
  support item.
- Verified #3640 was stale because prerequisite #3637 closed as `replan`
  without landing `HexModArithMathlib/Bench.lean`, `hexmodarithmathlib_bench`,
  or phase4 metadata; released #3640 with that reason.
- Claimed #3733 and proved
  `GramSchmidt.Rat.basis_rowSwap_adjacent_curr` in
  `HexGramSchmidt/Basic.lean`, translating the private adjacent-swap current
  row identity into the public Rat-layer LLL formula.
- Ran the required checks for the touched proof surface.

## Current frontier

The rational swapped-current row formula is now available. The adjacent-swap
suffix-preservation theorem remains separate work in #3734, and the integer
wrappers in `HexGramSchmidt/Update.lean` remain blocked on the full rational
basis helper layer.

## Next step

Land this PR, then continue with #3734 to prove
`GramSchmidt.Rat.basis_rowSwap_of_after`; after that, #3554 can wire the
integer adjacent-swap basis wrappers.

## Blockers

- #3734 is still needed for suffix preservation.
- Existing unrelated `sorry` declarations remain in
  `HexGramSchmidt/Update.lean`, `HexGramSchmidt/Int.lean`, and
  `HexMatrix/Bareiss.lean`.
