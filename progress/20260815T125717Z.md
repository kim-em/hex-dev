# Accomplished

- Restacked the exact contraction-to-split proof canary onto the experimental
  subtraction replay candidate while preserving current public interval and
  PNT registrations.
- Kept the retained root bound to its completed backward-contraction snapshot,
  both children bound to exact branch inputs, and sibling-fact mutations
  rejected during branch replay.
- Confirmed the contradictory right runtime leaf supplies no evidence:
  package replay first derives exact empty, then `refuteCurrent` applies the
  checked empty refuter; the root joins only through `replaySplit`.
- Added an explicit satisfying witness for all ordinary theorem premises, so
  the branch theorem is demonstrably nonvacuous.
- Rebuilt exact-branch, arithmetic, policy, public interval, and PNT targets and
  ran the repository formatting, DAG, trust, registration, PNT, and freshness
  checks.

# Current frontier

The local retained tree now reconstructs the parent contraction, the left
target proof, the right contradiction proof, and split coverage as ordinary
kernel evidence. Runtime stop classifications remain search data only.

# Next step

Complete the normal remote review and CI cycle after the preceding stack edges
land.

# Blockers

None.
