# Progress — 2026-06-18 (session 6c3304f5, /work → /feature #7894)

## Accomplished

Premise-checked #7894 ("derive BHKS cut package from successful indicator
candidates", the narrowed replacement for #7880) and found it **not
satisfiable** with current infrastructure. Posted a detailed diagnosis with
exact missing-theorem shapes and `coordination skip`ped to `replan`
(deliverable 4 + "directives are hypotheses" doctrine). No code changes.

### The three gaps (all blocking the cut package)

The two target consumer endpoints —
`factorFastCoreWithBound_some_factor_zpolyIrreducible_of_recoveredLift`
(`PartitionRefinement.lean:656`) and `rawIrreducible_of_cut`
(`PartitionRefinement.lean:1633`) — have **zero in-tree callers**; they are
unbridged because the producer this issue asks for does not exist.

(A) **RecoveredLift family, non-monic core.** Sole witness producer
`recoveredLiftOfRepresents` (`LiftBridge.lean:137`) needs `Monic core`;
`recovered_eq` is the exact `dilate (lc f) (centeredLiftPoly …) = factor` with
no `primitivePart`, which is false in the non-monic regime (`content ≠ 1`). Fast
path runs on the non-monic `(normalizeForFactor f).squareFreeCore`. The
non-monic producer is the fast-BHKS-monic-lift migration, not done.

(B) **trueSupports + CutProjectionHypotheses.** Every `CutProjectionHypotheses`
occurrence is a field or `hcut :` binder; constructors
`cutProjectionHypotheses_of_shortVectors`/`_of_trueFactors` (`Lattice.lean:2079`)
take `trueSupports` + per-support `SupportShortVectorData` as inputs. Sole
`SupportShortVectorData` producer (`CLDColumnBound.lean:1863`) routes through (A).
Integer row-span membership needs an LLL short-vector witness; success only
gives rational RREF dependencies.

(C) **hpartition.** `≤` is free (`UFDPartition.lean:93`); equality needs a
coverage map (`normalizedFactors_card_le_length_of_coverage`, `UFDPartition.lean:202`).
The only coverage producers (`Basic.lean:17281`) are for the slow exhaustive
path, not the fast indicator path; fast-vs-slow equivalence is itself open.

## Current frontier

#7894 is `replan`. The cut package derivation is equivalent to BHKS van Hoeij
soundness+completeness for the non-monic fast path — multi-issue research.

## Next step

A planner should either split into producers (A)/(B)/(C) as prerequisites, or
narrow #7894 to the monic-core regime where (A) exists.

## Blockers

The three missing producers above. No way to discharge without them; per
deliverable 4, no free-assumption wrapper was written.

Note: worktree carries a pre-existing unrelated `.claude/skills/agent-worker-flow/SKILL.md`
modification from before this session; not touched or staged.
