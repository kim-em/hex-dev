# 2026-05-14 13:48Z — feature session for #4022

## Accomplished

- Claimed #4022 (HexBZ Mathlib bridge: prove fast-core success branch
  factor count equals irreducible count).
- Oriented on the BHKS fast-core recovery chain in
  `HexBerlekampZassenhaus/Basic.lean` (`bhksIndicatorCandidate?`,
  `bhksIndicatorCandidates?`, `bhksRecoverClassified`,
  `factorFastCoreWithBound`) and the bridge infrastructure in
  `HexBerlekampZassenhausMathlib/` (UFDPartition, Lattice, Recovery).
- Landed the algorithm-side divisibility chain in
  `HexBerlekampZassenhaus/Basic.lean`:
  - `bhksIndicatorCandidate?_dvd`,
    `bhksIndicatorCandidates?_dvd`,
    `bhksRecoverClassified_success_dvd`,
  - `factorFastCoreWithBound_some_dvd`.
- Exposed `shouldRecordPolynomialFactor`, `factorFastCoreWithBound`,
  `factorFastCoreWithBound_product`,
  `factorFastCoreWithBound_some_shouldRecord`, and
  `factorFastCoreWithBound_some_dvd` publicly so the Mathlib bridge
  can consume them.
- Added Mathlib-bridge ingredients in
  `HexBerlekampZassenhausMathlib/Basic.lean`:
  - `toPolynomial_foldl_mul` and `polyProduct_toPolynomial` (push the
    `toPolynomial` ring hom through `Array.polyProduct`).
  - `toPolynomial_ne_zero_and_not_isUnit_of_shouldRecord` (lift
    `shouldRecord = true` to `≠ 0 ∧ ¬ IsUnit` over `Polynomial ℤ`).
- Landed the scaffold theorem
  `factorFastCoreWithBound_some_factor_irreducible_of_count`, which
  composes the algorithm-side facts with #4021's UFD lemma to conclude
  per-candidate `Irreducible (toPolynomial factor)` from a single
  count-equality hypothesis.
- Created #4030 for the residual count equality and posted a
  `Decomposed into #4030` breadcrumb on #4022.

## Current frontier

The scaffold reduces #4022 to a single count-equality obligation. The
ingredients on both halves of the inequality are in scope:

- Upper bound (`coreFactors.length ≤ |normalizedFactors core|`):
  internal to `UFDPartition.lean`; just needs to be factored out.
- Lower bound (`coreFactors.length ≥ |normalizedFactors core|`):
  load-bearing B8 partition-refinement argument that uses B6
  (already proved) plus algorithm-side verification.

The lower-bound half is the deep work and likely needs further
decomposition by whoever picks up #4030.

## Next step

Plan/feature cycle picks up #4030 once it unblocks. After the parent
`replan` triage on #4022 resolves (it can be closed in favour of
#4030), the chain proceeds:
`#4030 → #4007` (fast-core branch irreducibility) `→ #4008` (per-branch
output theorem) `→ #4003`/#3987 cascade.

## Blockers

None for this session. #4030 itself is blocked on the parent #4022
closure (replan triage) but is otherwise self-contained.
