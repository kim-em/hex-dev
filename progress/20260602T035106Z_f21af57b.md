# Session 20260602T035106Z (agent/f21af57b)

Issue: #6236 — HexPolyFp Phase 6: document FpPoly schoolbook coefficient
helpers as proof-facing API.

## Accomplished

- Took Option A from the issue (docs-only, no namespace move).
- Added a `/-! ### Schoolbook coefficient helpers (proof-facing Hensel
  scaffolding) -/` block above `FpPoly.mulCoeffTerm` in
  `HexPolyFp/Basic.lean` that:
  - names the cluster as proof-facing Hensel scaffolding,
  - directs ordinary `FpPoly` multiplication callers to `coeff_mul`
    instead, and
  - flags the surrounding private `coeff_mul_fold` / `mulCoeffTerm_*`
    / `fold_mulCoeff_*` plumbing as intentionally not exported.
- Expanded the existing docstrings on `mulCoeffTerm` and `mulCoeffSum`
  with one short sentence each repeating the proof-facing contract, so
  IDE hover surfaces the warning at each declaration.
- Verified `lake build HexPolyFp`, `lake build HexHensel`, full
  `lake build`, and `python3 scripts/check_dag.py` clean.
- Forbidden-token diff grep returns no matches.

## Current frontier

Option A landed; no source change to consumers and no namespace move.
The six `FpPoly.mulCoeffTerm` references in `HexHensel/Linear.lean`
(lines 277, 280, 416, 421, 482, 494) are unchanged, and `coeff_mul`
remains the public characterising lemma at its existing line.

## Next step

Push branch and open PR closing #6236.

## Blockers

None.
