# HexArith Montgomery context grind coverage (#8048)

## Accomplished

Promoted 14 equation-shaped `@[simp]` theorems in
`HexArith/Montgomery/Context.lean` to `@[simp, grind =]`:

`mk_p'`, `mk_p'_eq`, `mk_r2`, `mk_r2_eq`,
`fromMont_mulMont_toMont_zero_left`,
`fromMont_mulMont_toMont_zero_right`, `powMod_modulus_zero`,
`powMod_zero_exp`, `powMod_one_exp`, `powMod_zero_base`,
`powMod_zero_base_of_pos_exp`, `powMod_one_base`,
`powMod_modulus_one`, and `powMod_mod_base`.

Left the issue-listed parity facts `p_odd_nat` and `mk_p_odd_nat` as
`@[simp]` only after `lake build HexArith` rejected `p_odd_nat` as an
invalid `grind =` pattern: the equality body does not mention the
explicit context/proof parameter needed to instantiate the theorem.
`mk_p_odd_nat` has the same uninstantiable-proof-parameter shape.

Also left the requested non-promotions unchanged: `mk_p_odd`, `p_pos`,
`p_lt_R`, `mk_p_pos`, and `mk_p_lt_R`; the existing recursive
`powMod` `@[grind =>]` lemmas were not touched.

## Current frontier

`lake build HexArith` is green, with `HexArith.Montgomery.Context`
building in 773ms after the final annotation set. `python3
scripts/check_dag.py` exits 0. `git diff --check` is clean. No added
`sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` appears in the
target diff. PR #8055 is open for issue #8048.

## Next step

None for this issue.

## Blockers

None.
