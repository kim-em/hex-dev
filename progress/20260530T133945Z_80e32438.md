# 2026-05-30 13:39 UTC — #5804 cofactor analysis comment, skipped

## Accomplished

- Surveyed the unclaimed work queue. The four feature-labeled candidates
  were all blocked or recently skipped for non-actionable reasons (#4334
  needs quiet hardware; #5668 blocked on open #5748; #5765 needs the
  unbounded analytic source theorem; #5735 was claimed by another
  session). Directives #5773/#5775/#2564 were all recently skipped
  (comparator infrastructure, decomposed-and-blocked children).
- Claimed #5804 (HexGramSchmidt Phase 5: coefficient-level Bareiss
  divisibility for the Gram noPivot trajectory). Previous session
  (b8f57f34) had skipped after ~45min orientation, leaving the pointer
  "read BHKS §3 minors + Mathlib `Matrix.det_succ_column_zero`".
- Read parent #5803 (closed; the deterministic `coeff` field is in
  place) and the relevant code at `HexGramSchmidt/Int.lean` lines
  2243–2628, plus the bordered-minor framework in
  `HexMatrix/Bareiss.lean` (`stepMatrix_borderedMinor_update`,
  `bareissExactDiv_borderedMinor_of_mul_eq`) and `borderedMinor` in
  `HexMatrix/Basic.lean`.
- Identified the structural insight the previous session's skip note
  was reaching for: on the Gram trajectory the coefficient
  `coeff[i][α]` after `k` Bareiss noPivot steps from
  `noPivotInitialState (gramMatrix b)` equals the signed cofactor
  determinant
  `(-1)^{α+k} · D_{{0..k-1, i} \ {α}, {0..k-1}}` (for `α < k`),
  `prevPivot` for `α = i`, and `0` for other α. Verified by
  hand-computation at `k = 0` and `k = 1`.
- Established that with that identification:
  * The `α = i` and `α = k` divisibility cases are trivial
    (numerator factors as `pivot · prevPivot` and
    `−state.matrix[i][k] · prevPivot` respectively).
  * The `α < k` case reduces to a *new* Sylvester identity on
    cofactor determinants that is **not** an instance of the existing
    `bareissExactDiv_borderedMinor_of_mul_eq` — that lemma uses column
    `α` as the bordering column, but the cofactor formula's column
    set is `{0..k-1}` with no `α` (because cofactor expansion deletes
    the bordering column).
  * The required identity has the Desnanot-Jacobi shape
    `pivot · cofA − cof[i][k] · cofB = prevPivot · cofNext`.
- Posted the full analysis (closed-form cofactor formula, per-α case
  breakdown, refined two-sub-issue decomposition recommendation) as a
  comment on #5804.
- Skipped #5804 with a one-paragraph summary of the analysis and a
  pointer to the comment.

## Current frontier

The cofactor identification is the right entry point for a third worker
session. It refines the previous session's "Sylvester core" split
(which was correctly rejected as predicate-without-core) into a split
that **does** factor through:

1. *Cofactor identification* (sub-issue 1): strengthen
   `BareissGramRowInvariant` (or add a derived theorem) to carry the
   cofactor formula on the Gram trajectory; prove the formula is
   preserved by `bareissGramRowInvariantStepCoeff`.
2. *Divisibility from cofactor* (sub-issue 2): given the cofactor
   formula, the divisibility is trivial for `α = i` and `α = k`, and
   reduces the hard `α < k` case to a focused Desnanot-Jacobi instance
   on a specific `(k+2)×(k+2)` matrix.

The skip-with-analysis is intentionally a comment-only handoff rather
than a partial PR: the cofactor identification proof itself is a
substantial Sylvester preservation, and the no-`sorry` constraint
ruled out landing a half-proved divisibility wrapper as a checkpoint.

## Next step

Next worker on #5804 should:

1. Read the analysis comment on #5804 before coding.
2. Decide whether to file sub-issues for the two-step decomposition or
   work the whole thing in one session. The cofactor-identification
   piece on its own is probably one session of focused work; the
   Desnanot-Jacobi piece a second.
3. Consider whether the cofactor formula belongs in
   `BareissGramRowInvariant` as a new field or in a separate
   `gramTrajectoryCoeff` aux definition with a `_eq_invariant_coeff`
   bridge theorem. The former keeps the invariant single-source; the
   latter avoids forcing every consumer to thread the cofactor
   formula.

## Blockers

None on #5804 directly — the prior session's "no bounded local proof
slice" assessment was a search-depth limitation, not a hard blocker.
The cofactor formula gives a concrete, well-bounded entry point.
