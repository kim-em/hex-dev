# Session bed10d50 — feature: BZMathlib `_of_bound` for scaled `_some_factor_associated_*` wrappers

## Accomplished

Claimed #4936 and implemented the abstract-bound (`_of_bound`)
propagation through the public scaled `_some_factor_associated_*`
wrapper layer in `HexBerlekampZassenhausMathlib/Basic.lean`. The PR
adds two new `_of_bound` theorems and rewires the two originals as
thin wrappers:

* `scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_bound`
  — forwards to `scaledRecombinationSearchModAux_some_and_covers_..._of_bound`
  (the #4935 substrate) and specialises the per-factor coverage at the
  supplied `factor`.
* `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core_of_bound`
  — single `exact` to the scaled `_of_bound`.
* Both originals rewired to instantiate
  `B' := Hex.ZPoly.defaultFactorCoeffBound core`, discharging
  `hcore_lc_le` via `defaultFactorCoeffBound_valid` + `leadingCoeff_eq_coeff_last`,
  and `hvalid` via the universal `defaultFactorCoeffBound_valid core hcore_ne`.

The originals keep identical signatures; the sole in-tree consumer
(`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`)
continues to type-check via the rewired thin wrapper.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — 16 errors, all
  pre-existing (whnf timeouts at 4848/5129/5455/5542/5589/5973/6121/6293/6274,
  cases-failed at 5851, kernel-unknowns at 6608/6672, plus the second
  region at 15505/15616/15823/15869 shifted +163 lines from the
  `origin/main` baseline at 15342/15453/15660/15706). Confirmed by a
  stashed rebuild on `origin/main` producing the identical error set.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no
  matches.

## Current frontier

The next `_of_bound` consumer one layer up is
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
(file as `#4938` per the issue's context). It is unblocked once this
PR lands.

## Next step

Land the PR; the `exhaustiveCoreFactorsWithBound_coverage_*_of_bound`
sibling is the natural next target for the A2 abstract-bound cascade.

## Blockers

None for this work item.
