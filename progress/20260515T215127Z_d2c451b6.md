# 2026-05-15T21:51 — fixed-space polynomial kernel iff (#4314)

## Accomplished

- Proved
  `Hex.Berlekamp.isFixedSpaceKernelPolynomial_iff_dvd_linearPow_sub_self`
  in `HexBerlekamp/Basic.lean`. The theorem packages the matrix-side iff
  `isFixedSpaceKernelVector_iff_berlekampMatrix_mulVec_eq` (from #4313)
  with the matrix-action identity `berlekampMatrix_mulVec_coeffVector_eq`
  into a polynomial-level Berlekamp kernel characterisation:
  `IsFixedSpaceKernelPolynomial f hmonic w ↔ f ∣ FpPoly.linearPow w p - w`,
  for `w.size ≤ basisSize f`.
- Added supporting private helpers in `HexBerlekamp/Basic.lean`:
  - `linearPow_zero_of_pos_aux`: `0 < n → linearPow (0 : FpPoly p) n = 0`.
  - `eq_zero_of_size_eq_zero`: a polynomial of zero `size` is the zero
    polynomial (used in the `basisSize f = 0` edge case).
  - `coeffVector_eq_iff_eq_of_size_le`: `coeffVector` is injective on
    polynomials of size at most `basisSize f`.
  - `mod_eq_self_of_size_le_basis`: a polynomial of `size ≤ basisSize f`
    is its own remainder modulo `f` when `0 < basisSize f`.
  - `linearPow_mod_size_le_of_basis_pos`: `(linearPow w p % f).size ≤ basisSize f`
    when `0 < basisSize f`.

## Current frontier

- Downstream composition #4227
  (`irreducible_of_no_kernelWitnessSplit_squareFree` and
  `berlekampFactor_singleton_irreducible`) is now unblocked. It consumes
  this iff to translate algebraic kernel membership into the matrix-level
  spanning predicate and back.

## Next step

- Land this PR, then a future worker picks up #4227.

## Blockers

- None.
