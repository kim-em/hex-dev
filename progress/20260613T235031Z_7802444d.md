# Progress: #6940 — recovered Hensel forward transport

UTC 2026-06-13, work session for feature issue #6940.

## Accomplished

- Claimed #6940 after #6840 raced and was already claimed.
- Fast-forwarded the branch to `origin/main` after #6969 landed.
- Updated `henselLiftData_represents_lifted_of_modP` in
  `HexBerlekampZassenhausMathlib/Basic.lean` so the forward Hensel transport
  caller constructs a `RecoveredAtLift` witness and wraps it with
  `RepresentsIntegerFactorAtLift.ofRecovered`.
- The proof now uses Hensel uniqueness for the unscaled lifted-product
  congruence and uses monicity of `core` / `factor` for the dilation and
  primitive-part field.

## Current frontier

`lake build HexBerlekampZassenhausMathlib.Basic` advances past the old explicit
Hensel forward-transport failure near the 9090s.  The first remaining failure is
now `HexBerlekampZassenhausMathlib/Basic.lean:9466`, where
`natDegree_toPolynomial_eq_sum_of_represents_of_bound` still passes
`RepresentsIntegerFactorAtLift` to old scaled-product recovery lemmas expecting
`reduceModPow (scaledLiftedFactorProduct ...) = reduceModPow factor ...`.
Later failures remain in the same recovered-carrier migration family, plus the
late recursive scaled-search prefix-candidate mismatch around 16544.

Verified:
- `lake build HexBerlekampZassenhaus` — success.
- `python3 scripts/check_dag.py` — success.
- `git diff --check` — success.
- Added-line forbidden-token grep over the touched Lean file found no new
  `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Before/after `lake build HexBerlekampZassenhausMathlib.Basic` comparison:
  the baseline on `HEAD~1` had 12 errors, starting with the old
  `Basic.lean:9124` `show` failure in `henselLiftData_represents_lifted_of_modP`;
  this branch has 11 errors, with that Hensel transport error removed and the
  remaining errors matching the downstream recovered-carrier caller family
  (line numbers shifted by the local edit).

Second-opinion review agreed the local `RecoveredAtLift` witness is sound under
the theorem's monic `core` / monic `factor` hypotheses.  It also flagged that
the PR body should explicitly say the Mathlib layer remains red and is not the
CI-gated layer.  The recovered carrier has a precision-based path to executable
candidate recovery via `RecoveredAtLift.candidate_eq_of_bound`; the remaining
consumers still need migration away from the old scaled-product recovery shape.

## Next step

Pick up the remaining recovered-carrier exact-recovery callers under #6964 /
#6965 rather than expanding #6940.  The first concrete target is
`natDegree_toPolynomial_eq_sum_of_represents_of_bound` at the 9460s.

## Blockers

`HexBerlekampZassenhausMathlib.Basic` is still red on sibling migration surfaces
outside #6940's scoped Hensel forward-transport caller.
