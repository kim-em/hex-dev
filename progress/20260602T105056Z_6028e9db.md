# 20260602T105056Z 6028e9db — Issue #6310

## Accomplished

- Claimed and completed issue #6310.
- Tagged five public quadratic Hensel doubling-step theorems in
  `HexHensel/Quadratic.lean` with `@[grind =>]`:
  `quadraticHenselStep_factor_spec`, `quadraticHenselStep_bezout_spec`,
  `quadraticHenselStep_spec`, `quadraticHenselStep_monic`, and
  `quadraticHenselStep_factor_congr_mod_base`. Matched the existing
  `@[grind =>]` convention used by sibling theorems in
  `HexHensel/QuadraticMultifactor.lean`.
- Added a proof-mode conformance cluster of five `example` blocks in
  `HexHensel/Conformance.lean` exercising each tagged theorem via
  `have := <theorem>; grind only`.

## Current frontier

The quadratic Hensel doubling-step API now exposes the same kind of
automation surface as the linear-lift API. The conformance examples
double as regression coverage for the `@[grind =>]` tags.

## Next step

Sibling Phase 6 polish issues #6311 (HexConway) and #6314 (HexGFq)
remain unclaimed and can be picked up next.

## Blockers

None.

## Verification

- `lake build HexHensel.Quadratic` — green, no `Try these` info
  messages remaining after switching from bare `@[grind]` to
  `@[grind =>]`.
- `lake build HexHensel.Conformance` — green.
- `lake build HexHensel` — green.
- `lake build` (full project) — green (244 jobs).
- `python3 scripts/check_dag.py` — exit 0.
- `git diff --check` — clean.
- Forbidden-token diff grep over `HexHensel/Quadratic.lean` and
  `HexHensel/Conformance.lean` found no added `sorry`, `axiom`,
  `native_decide`, `TODO`, or `FIXME`.
