# 20260601T141726Z — a3a3ba24: #5950 two-interior Schur kernel

## Accomplished

Claimed #5950 and added
`Polynomial.schur_two_interior_roots_norm_le` in
`HexPolyZMathlib/SchurSzego.lean`. The theorem consumes the explicit
degree-two Schmeisser Vieta data
`ζ₁ + ζ₂ = -((a + b) * (c + d) / 2)` and
`ζ₁ * ζ₂ = a * b * c * d`, derives the structured two-pair zero equation
for each candidate root, and applies
`schur_structured_two_pair_ne_zero` to rule out either root lying strictly
outside radius `r`.

## Current frontier

#5950 is complete. The result is independent of `Polynomial.roots` and
derivative adapters, so the downstream one-exterior exclusion and exact
degree-two packaging issues can use it as the all-interior source-root case.

## Next step

Continue with #5947 to use this all-interior kernel in the one-exterior
degree-two Schur exclusion.

## Blockers

None for #5950.

## Verification

* `lake build HexPolyZMathlib` — green.
* `python3 scripts/oracle/mahler_schur_counterexample.py` — exits 0 and prints
  the expected endpoint-inequality counterexample.
* `git diff --check` — green.
* `git diff -- HexPolyZMathlib/SchurSzego.lean | rg -n -e '^\\+.*(sorry|axiom|native_decide|TODO|FIXME)' || true` —
  no matches.
