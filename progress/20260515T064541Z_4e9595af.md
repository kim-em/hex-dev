# HexPolyFp.Compose infrastructure (partial of #4159)

Session UUID: `4e9595af`. Issue #4159 (HexPolyFp: prove non-modular
compose hom laws for FpPoly substitution).

## Accomplished

Created `HexPolyFp/Compose.lean` and wired it through
`HexPolyFp.lean`. Landed the foundational compose infrastructure
covering deliverables 1 and 2 of the original issue:

- `compose_zero`, `compose_C`, `compose_X`, `compose_one` (basic
  Horner-form simp lemmas; `compose_X` is gated on `PrimeModulus p`
  because the indeterminate's coefficient list relies on
  `1 ≠ 0` in `ZMod64 p`).
- Compose-as-power-sum characterisation: private
  `composeScalarCoeffList` / `composeCoeffPowerSumFrom` polynomial
  analogs of the scalar `evalScalarCoeffList` /
  `evalCoeffPowerSumFrom` infrastructure, with the linking lemmas
  `mul_composeCoeffPowerSumFrom_eq_succ`,
  `composeScalarCoeffList_eq_powerSumFrom_zero`,
  `foldl_compose_reverse_eq_composeScalarCoeffList`. Exposed as
  `compose_eq_powerSum` (private) and
  `compose_eq_composeScalarCoeffList` (public, the Horner-form bridge
  needed by downstream sub-issues).
- Scalar-to-polynomial `C` homomorphism laws: `C_add_eq`, `C_sub_eq`,
  `C_mul_C_eq` (needed for the coefficient computations of products
  like `f * (X - C c)`).

`lake build HexPolyFp HexBerlekamp` green; `python3
scripts/check_dag.py` green; `git diff --check` clean. No `axiom`,
`native_decide`, `TODO`, `FIXME`, or new theorem-level `sorry` in
affected files.

## Current frontier

The two remaining deliverables from #4159 are decomposed into
focused sub-issues:

- #4175 — `compose_mul_X_sub_C` per-step lemma plus the
  prime-field-product foldl transport. The intended route is a
  list-level `mulXSubCList` plus a polynomial-equality bridge via
  `ext_coeff` and `coeff_monomial_mul`, together with a small
  "compose-respects-trailing-zero-trim" lemma so that
  `compose (ofCoeffs cs.toArray) q` agrees with
  `composeScalarCoeffList cs q`.

- #4176 — `compose (linearPow X p - X) w = linearPow w p - w`
  transport. Induction on `k` for `compose (linearPow X k) w =
  linearPow w k` (needs a narrowly scoped `compose (a * X) w =
  compose a w * w` that overlaps with #4175's primitives), plus a
  direct coefficient analysis for `compose (a - X) w =
  compose a w - w`, then a small unfolding step linking
  `xPowSubX 1` to `linearPow X p - X`.

A breadcrumb comment is on #4159 noting the decomposition.

## Next step

After this partial lands, the natural next worker should claim #4175
first (it provides the headline foldl transport that's directly used
by the square-free divisor distribution work in #4063). #4176 can
then layer on top, after which #4159 itself can be closed because
its sub-issues collectively cover the original deliverables.

## Blockers

None for this partial. The substantive multiplication/subtraction
step is genuinely a larger chunk of polynomial-coefficient bookkeeping
than fits cleanly in one session alongside the foundational
infrastructure — the decomposition is the right shape to land
incrementally.
