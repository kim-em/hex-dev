# Progress — 2026-06-18 (session 56eb0520, /once #7934)

## Accomplished

Fixed the double-`precisionForCoeffBound` bug on the fast path (#7934,
diagnosed on #7928). The fast path was applying `precisionForCoeffBound`
twice: the public caller passed `a := precisionForCoeffBound B primeData.p`
as the schedule cap, and `toMonicLiftData` log-scaled it again, so the CLD
lattice ran at `precisionForCoeffBound (precisionForCoeffBound B)` — far below
CLD-adequate precision, recovering only tiny-coefficient inputs.

### Executable fix (`HexBerlekampZassenhaus/Basic.lean`)

Both fast-path call sites in `factorFastFactorsWithBound` now pass the
coefficient bound `B` directly to `factorFastCoreWithBound` (instead of
`a := precisionForCoeffBound B primeData.p`). The schedule variable now
iterates coefficient bounds up to `B`, and the single `toMonicLiftData`
conversion gives `liftData.k = precisionForCoeffBound (scheduleVar)` — the
correct single log. The loop still early-exits at the first adequate bound.

Propagated the call-site shape through every Basic.lean proof that mirrors
it: `factorFastFactorsWithBound_eq_some_of_core_success`,
`factorFastFactorsWithBound_branch`, `factorWithBound_entry_mem_fast_core_success_raw`,
`factorFast_ne_none_of_core_recovery_on_schedule` (its `hmem` now ranges over
the cap schedule directly), `factorFastWithBound_product_of_*`, and
`factorFastFactorsWithBound_polyProduct_of_some`. Two structural wrinkles:
removing the `let a :=` indirection made `cases hcore :` substitute the
scrutinee directly into the disjunct's core-equation field (now `rfl`, with
`hcore` still needed in the `simp` to reduce the un-substituted `match`); and
the literal-`B=1` branches reduce `quadraticDoublingSteps 1 + 2` to `2`, so
those `cases` terms use the literal `2`.

### Mathlib bridge alignment (`HexBerlekampZassenhausMathlib/Recovery.lean`)

`factorFast_ne_none_of_forwardInputs_on_schedule` (`hmem`),
`factorFast_ne_none_of_forwardInputs_at_cap` (`hinputs`), and the private
`factorFastCapLiftData` abbrev now use the single-log cap precision. This
*aligns* the bridge: the `ofCapSeparation…AtPrecisionForCoeffBound` builders
already required `hk : d.k = precisionForCoeffBound (cap) d.p`, which the new
`factorFastCapLiftData = toMonicLiftData core (factorFastPrecisionCap f)`
satisfies exactly (its `.k` is `precisionForCoeffBound cap p`). The old
double-log lift made that `hk` a buggy idempotence demand.

## Verification

- `lake build HexBerlekampZassenhaus` → 497 jobs, Build completed
  successfully, 0 errors (includes CrossCheck external-oracle cross-check and
  Conformance `#guard` fixtures — outputs unchanged: `factorFastFactorsWithBound
  cldGuardF 4 = some bhksGuardFactors` still holds, now recovered at lift
  precision 2 instead of 1).
- `lake build HexBerlekampZassenhausMathlib` (the CI-gated library) → 0 errors,
  completed. No conformance fixture changes were needed.
- `lake build HexBerlekampZassenhausMathlib.Recovery` → 0 errors.
- No `sorry`/`axiom`/`native_decide` added (only the pre-existing
  `factor_irreducible_of_nonUnit` sorry remains, untouched).

## Current frontier

Deliverable 2's *new* soundness proofs remain: the precision-soundness lemma
(`factorFastCoreWithBound … = some coreFactors → hsep ∧ hthr` per-coordinate)
and the #7917 assembly (`factorFastCore_irreducible_of_success_given_partition`).
These feed the still-`sorry` headline `factor_irreducible_of_nonUnit` and live
in the recovery-direction files (`PartitionRefinement.lean`, which is **not**
CI-gated and is pre-existing-red from the #7122 toMonicLiftData-migration
heartbeat/unknown-constant cluster — `PartitionRefinement.lean` is byte-identical
to `origin/main`, and its `_on_schedule` wrappers are generic in `B`/`target`,
so my change neither breaks nor needs to touch them). Per the
hex-lean-mathlib-boundary skill (period trap, RecoveredLift insufficiency, the
#7876 SupportShortVectorData machinery), this is multi-session structural work,
not a token-swap. Landed as a partial PR; the bug fix itself is complete and
verified.

## Blockers

None for the executable fix. The precision-soundness / #7917-assembly proofs
are a separate frontier on the path to the irreducibility headline.
