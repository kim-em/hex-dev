## Accomplished

Claimed feature issue #3063 and quarantined the obsolete additive
recombination surface in `HexBerlekampZassenhaus/Basic.lean`.

Removed the old additive lattice API and wrappers:
`RecombinationLattice`, `recombinationLattice?`, `decodeShortVector`,
`shortVectorCandidates`, `recombineLLL?`, `recombine`,
`LiftedFactorsRecombineTo`, `adaptiveCoreFactors`, and the
`recombine_product_of_lifted_factors` theorem that depended on that surface.

Retargeted `HexBerlekampZassenhaus/CrossCheck.lean` and
`HexBerlekampZassenhaus/Conformance.lean` toward the supported BHKS/slow-path
interfaces: conditional `bhksRecover?` checks, `factorSlow`, `factorFast`, and
the existing `factor` / `factorWithBound` combinators.

Merged current `origin/main` after the edits and re-applied the quarantine
change cleanly.

Verified:

- `lake build HexBerlekampZassenhaus.Basic HexBerlekampZassenhaus.CrossCheck HexBerlekampZassenhaus.Conformance HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n 'recombineLLL\\?|RecombinationLattice|decodeShortVector|shortVectorCandidates|LiftedFactorsRecombineTo|adaptiveCoreFactors' HexBerlekampZassenhaus` reports no matches.
- `rg -n '^axiom\\b|native_decide|TODO|FIXME|sorry' HexBerlekampZassenhaus/Basic.lean HexBerlekampZassenhaus/CrossCheck.lean HexBerlekampZassenhaus/Conformance.lean` reports only pre-existing `sorry`s in `Basic.lean`; the diff removes one `sorry` and adds none.

## Current frontier

The production-facing stale additive recombination names are gone from
`HexBerlekampZassenhaus`, and conformance no longer exercises
`recombineLLL?` / `recombine`.

## Next step

Open the PR for #3063 and let CI validate the merged branch.

## Blockers

None.
