# 20260603T100234Z work #2567

## Accomplished

Added a factored sibling to the `*AndPaperThreshold` cap-separation builder
family in `HexBerlekampZassenhausMathlib/Recovery.lean`, threading the two
independent BHKS Theorem 5.2 LHS sub-bounds straight into the cap-arithmetic
chain in place of the combined paper-threshold product inequality:

* `FactorFastCapSeparationInputs.ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold` —
  consumes the direct `‖aux‖ ≤ auxiliaryBound` bound together with
  `h_coeff : coeffL2NormBound^aux.natDeg ≤ bhksPaperCoeffNormFactorReal core`
  and `h_aux : auxiliaryBound^input.natDeg ≤ bhksPaperDegreeFactorReal *
  bhksPaperConstantFactorReal C * bhksPaperLogFactorReal`. Internal
  `hauxiliaryBound_nonneg` derived from `hauxiliary` via `Real.sqrt_nonneg`
  on the l2norm. Dispatches through
  `bhksPaperThresholdReal_chain_lt_p_pow_kLocalFactorDegree_factored`.
* `factorFast_terminates_ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold` —
  final wrapper composing the new constructor with `factorFast_terminates`
  for the canonical-support recovery tail.

Both are pure compositions of existing infrastructure.

## Current frontier

Callers targeting `factorFast f ≠ none` via the direct `‖aux‖ ≤ B` shape now
have two equivalent routes:

1. **Combined route** (existing) — supply `hpaper : coeffPow * auxPow ≤
   bhksPaperThresholdReal` and feed through
   `factorFast_terminates_ofBridgeDataAuxiliaryL2normAndPaperThreshold`.
2. **Factored route** (new) — supply the two independent sub-bounds and feed
   through
   `factorFast_terminates_ofBridgeDataAuxiliaryL2normAndFactoredPaperThreshold`.

The factored sub-bounds match the natural BHKS shape and decouple the
coefficient-power piece (`‖core‖₂^(2n-1)`) from the auxiliary-power piece
(`n · (2C)^(n²) · (log ‖core‖₂)^n`), each a tractable analytic step.

## Next step

Natural follow-ups (any can be picked up independently):

1. **Factored variants of the other three `*AndPaperThreshold` builders** —
   `ofBridgeDataPointwiseAuxiliaryBoundsAndFactoredPaperThreshold`,
   `ofBridgeDataCorrectedAuxiliaryL2normSqAndFactoredPaperThreshold`, and
   `ofBridgeDataAndFactoredPaperThreshold`. Each is a near-identical swap of
   the unfactored chain for the factored chain.
2. **Coefficient-power sub-bound** — prove `coeffL2NormBound^aux.natDeg ≤
   bhksPaperCoeffNormFactorReal core` from the degree relation
   `aux.natDeg ≤ 2n - 1` combined with the integer ⌈√⌉ vs. real l2-norm
   relation already encoded by `l2norm_toPolynomial_sq_le_coeffNormSq`.
3. **Auxiliary-power sub-bound** — the genuine BHKS Lemma 3.2 content,
   bounding `auxiliaryBound^input.natDeg` against
   `n · (2C)^(n²) · (log ‖core‖₂)^n`.

Items 2 and 3 are the actual analytic gaps; item 1 is the remaining plumbing
to expose the factored axis on every sibling builder.

## Blockers

None. The new constructor and wrapper are pure compositions over existing
infrastructure.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Recovery` passed.
- `lake build HexBerlekampZassenhausMathlib HexBerlekampZassenhaus` passed.
- Added-line forbidden-token grep (`sorry|axiom|native_decide|TODO|FIXME`)
  returns nothing.
- Diff: 166 added lines, zero deletions.
