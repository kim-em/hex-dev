# Progress — 2026-06-16 (session 3de9e0f7, feature)

## Accomplished

Issue #7492: added one-sentence Phase 6 docstrings to the twelve
undocumented private declarations in the column/row add-and-duplicate
elementary-operation cluster of `HexMatrix/Determinant.lean`
(lines ~4340–4640):

- Column: `detProduct_colDuplicate_swapValues`,
  `detTerm_colDuplicate_swapValues`, `colAddDuplicate` (def),
  `colAddDuplicate_get`, `colAdd_get`, `detProduct_colAdd`,
  `detTerm_colAdd`.
- Row: `rowAddDuplicate` (def), `rowAdd_get`, `rowAddDuplicate_get`,
  `detProduct_rowAdd`, `detTerm_rowAdd`.

Doc-only: no signatures, statements, proofs, simp sets, attributes, or
declaration order touched. The two transpose/swap lemmas at ~4488/4499
(`detTerm_rowSwap_transposeValues`,
`permutationVectors_transposeValues_neg_sum`) were deliberately left
undocumented — they belong to the separate rowSwap/transpose batch.

Two earlier claims this session were skipped on premise-check grounds
before reaching code:
- #7441 (BHKS §5 aux domination): infeasible as written (deliverable
  scales wrong in ‖core‖₂; dep #7440 closed infeasible; corrected
  certificate #7487 still open/claimed). Skipped → replan.
- #7361 (descent-hypotheses producer): genuinely blocked on the open
  `depends-on: #7470` (lifted-subset uniqueness, itself blocked+replan);
  leaked into the queue with a missing `blocked` label. Skipped → replan.

## Verification

- `lake build HexMatrix.Determinant`: green (376 jobs).
- `python3 scripts/check_dag.py`: exit 0.
- `git diff --numstat -- HexMatrix/Determinant.lean`: `23 0`
  (additions only).
- `git diff --check`: clean.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` on added
  lines.

## Current frontier

This cluster's docstrings complete. `Determinant.lean` still has many
undocumented private helpers (~100 at issue-authoring time) across other
Phase 6 doc clusters, including the rowSwap/transpose cluster
(~4488/4499) explicitly deferred here.

## Next step

None for this issue. Other Phase 6 doc batches remain in the queue.

## Blockers

None.
