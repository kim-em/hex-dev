# Progress: #6811 — remove raw-tail IH from derivative-active SquareFree contribution

## Accomplished

Refactored `HexPolyFp/SquareFree.lean` so
`derivative_active_raw_tail_weighted_product_bridge_via_normalized` no longer
takes a pre-built raw-tail correctness equality. It now takes the raw tail's
reachability/nonzero/fuel/residual facts plus the recursive correctness
provider and derives the raw equality internally before composing with the
normalized-tail bridge.

Updated `squareFreeAuxRevContribution_derivative_active_pow_obligation` to stop
constructing the local
`ih (pthRoot contribution.2) ...` raw-tail correctness proof. The
derivative-active call site now keeps the normalized-tail recursive correctness
call and passes raw validity/residual payloads through the strengthened bridge.

Verification completed:

- `lake build HexPolyFp.SquareFree`
- `lake build HexPolyFp`
- `python3 scripts/check_dag.py`
- `git diff --check`
- added-line forbidden-token grep over `HexPolyFp/SquareFree.lean`

## Current frontier

#6811's local refactor is complete. The weighted-product chain still has the
broader raw-provider cleanup tracked by the parent SquareFree issues; this pass
only removed the direct raw-tail IH equality at the derivative-active
contribution obligation.

## Next step

Continue with the downstream SquareFree normalized-provider cleanup issues once
their remaining prerequisites are unblocked.

## Blockers

None for this slice.
