/-
Copyright (c) 2026 Lean FRO, LLC. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kim Morrison
-/

import HexGraphIso.Cases

/-!
Deterministic JSONL fixtures for the `HexGraphIso` external nauty oracle.

The stream contains the SPEC's merge fixture: all 1,100 labelled
uncoloured graphs for `0 ≤ n ≤ 5`, all 4,912 graph and
ordered-surjective-partition pairs for `0 ≤ n ≤ 4`, deterministic
relabellings and colourings of named larger examples, positive and
negative isomorphism pairs, and graphs whose automorphism groups are
trivial, small, and large. A second pass over the small graphs and the
named and pseudo-random examples carries the automorphism-group records.
The cases are generated by
`HexGraphIso.Cases`; graphs are enumerated by an adjacency bitmask over
the lexicographic `i < j` pair order, colour vectors are enumerated as
base-`k` numerals with vertex `0` most significant, and pseudo-random
cases use the SPEC's SplitMix64 corpus seeds.

Each record carries the *public* answer: the canonical label and
canonical upper-triangle bits are read off `canonicalize` (the
production pipeline behind `canon` and `label`), with the coloured
graph built through the public checked constructors.
The search-node count comes from the transcribed search
(`Nauty.runColored`). The oracle recomputes all of them with the pinned
external nauty, so the fixture pins the public operations, not only the
transcribed search.

`--engine` emits the same cases with the label, canonical bits and node
count read off `Cases.engine` instead, so the oracle pins that search
against real nauty on its own. The committed fixture is the default
mode; nothing in CI passes `--engine`.
-/

def main (args : List String) : IO Unit := do
  let runner ← Hex.GraphIsoCases.runnerOfArgs args
  Hex.GraphIsoCases.eachFixture (Hex.GraphIsoCases.emit runner)
  Hex.GraphIsoCases.eachAutos (Hex.GraphIsoCases.emitAutos runner)
