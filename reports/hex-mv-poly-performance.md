# HexMvPoly Performance Report

## Bench Targets

The Mathlib-free compiled suite is registered in
`bench/HexMvPoly/Bench.lean`. Inputs come from the shared deterministic
corpus in `bench/HexMvPolyBench/Corpus.lean`; no random seed is involved.

| target | input family | declared model |
|---|---|---|
| `runSparseAdditionLex` | `sparse-addition` | `n * log₂ (n + 1)` |
| `runSparseAdditionGrlex` | `sparse-addition` | `n * log₂ (n + 1)` |
| `runSparseAdditionGrevlex` | `sparse-addition` | `n * log₂ (n + 1)` |
| `runSparseMultiplicationLow` | `sparse-multiplication` | `n² * log₂ (n² + 1)` |
| `runSparseMultiplicationHigh` | `sparse-multiplication` | `n² * log₂ (n + 1)` |
| `runCancellationInt` | `cancellation-arithmetic` | `n² * log₂ (n² + 1)` |
| `runCancellationRat` | `cancellation-arithmetic` | `n² * log₂ (n² + 1)` |
| `runRenameCollisions` | `structural-collisions` | `n` |
| `runPartialEvalCollisions` | `structural-collisions` | `n` |
| `runSubstCollisions` | `structural-collisions` | `n * log₂ (n + 1)` |
| `runSumOfSquaresArithmetic` | `sum-of-squares-arithmetic` | `n² * log₂ (n² + 1)` |

The native comparator adapters are reproducible with
`scripts/bench/setup_hex_mv_poly_comparators.py`. The first adapter exercises
CompPoly's actual `CMvPolynomial`; the second exercises the canonical
sorted-list adapter used by the proof probes. Both import the shared corpus.
The five checked-in plots are regenerated by:

```sh
for family in sparse-addition sparse-multiplication cancellation-arithmetic structural-collisions sum-of-squares-arithmetic
do
  python3 scripts/plots/hex-mv-poly-comparator.py --family "$family"
done
```

Reusable container machinery is isolated in `HexBasic/ExtTreeMap.lean`.
`Std.ExtTreeMap.foldl₂` is a generic ordered joint traversal, while
`mergeValue?` and `mergeWith?` are generic deletion-capable merges.
`HexMvPoly` adds only monomial ordering, coefficient arithmetic, and the
zero-deletion policy. A direct tree-cursor implementation of the existing
joint fold, together with a linear ordered-tree builder, would support a
linear deletion-capable merge without materializing lists. Those primitives
belong in that reusable file, not in `MvPoly`.

### Consumer acceptance

The public surface was compiled against both named consumers. The exact
migrations and builds are reproducible with
`scripts/bench/setup_hex_mv_poly_consumers.py`.

- `leanprover/sos` at `4e52845513a5a7f70927c96e094592db1bf124d1`
  was migrated through a compatibility module aliasing its old canonical
  multivariate types to `Hex.MvPoly`. `SOS.Certificate`, `SOS.EqElim`,
  `SOS.Symmetry`, `SOS.Verifier`, and `SOS.Tactic` all built as explicit
  `.olean` targets in a fresh clone. `SOSTest.Examples` also elaborated as an
  explicit `.olean` target, replaying its `by sos`, `by sos?`, and pinned
  `sos_witness` examples through the kernel verifier; the complete acceptance
  build passed all 1,545 jobs. The compatibility
  module supplies a low-priority `DecidableEq` from `LawfulBEq`, matching the
  implication that the former CompPoly representation supplied; it adds no
  Grind instances. Its exact legacy-arity `aeval` remains direct evaluation,
  which avoids adding equality bounds to the consumer signature, while its
  zero/one/add/mul/pow/C/X/neg/sub compatibility lemmas compile against the
  corresponding `HexMvPolyMathlib.aeval_*` theorems.
- `Verified-zkEVM/CompPoly` at
  `f4c59f9e6a00b4e73f3e43ca362480468a508011` was migrated to the public
  `isEmptyRingEquiv`, `finSuccEquiv`, and dense-polynomial bridges.
  `CompPoly.Univariate.CMvEquiv` and `CompPoly.Bivariate.CMvEquiv` passed all
  1,902 jobs under Lean `4.32.0-rc1` in a fresh clone. The reproducible overlay
  rewrites the consumer's `ofPoly_zero` proof through the public ring
  equivalence because the pinned proof no longer elaborates under this
  toolchain; it does not change the statement or runtime adapter.

The setup script applies these compile-acceptance overlays only in temporary
pinned clones. The machine-readable acceptance record is
`reports/bench-results/hex-mv-poly-consumers-d05d0635.json` (SHA-256
`6bcd144c785dc64f305d9b82851b7811302e755e4cafc245ef5a62cfcea78e17`);
it records clean Hex source commit `d05d0635`, the pinned revisions, manifest
and adapter hashes, exact build targets, job counts, and hashed Lake output.

## Verdicts

The three-trial scientific run used clean commit
`163e100c9eaefe35389e2cb2f2b04a17103d3490` on `chungus2` (AMD EPYC 9455,
x86-64 Linux), Lean `4.32.0-rc1`, and lean-bench `0.1.0`. Command:

```sh
lake exe hexmvpoly_bench run \
  Hex.MvPolyBench.runSparseAdditionLex \
  Hex.MvPolyBench.runSparseAdditionGrlex \
  Hex.MvPolyBench.runSparseAdditionGrevlex \
  Hex.MvPolyBench.runSparseMultiplicationLow \
  Hex.MvPolyBench.runSparseMultiplicationHigh \
  Hex.MvPolyBench.runCancellationInt \
  Hex.MvPolyBench.runCancellationRat \
  Hex.MvPolyBench.runRenameCollisions \
  Hex.MvPolyBench.runPartialEvalCollisions \
  Hex.MvPolyBench.runSubstCollisions \
  Hex.MvPolyBench.runSumOfSquaresArithmetic \
  --outer-trials 3 \
  --export-file /tmp/hex-mv-poly-native-163e100c-chungus2.json
```

The committed export is
`reports/bench-results/hex-mv-poly-native-163e100c-chungus2.json`
(SHA-256
`9e2a2f086134a99361a75322baef08a7aaa66ed6eb3b9b62ad07bdfa3c8e89d4`).

| target | rungs | median at first → last rung | fitted residual slope β | verdict |
|---|---:|---:|---:|---|
| `runSparseAdditionLex` | 128…4096 | 0.100 → 5.408 ms | +0.005 | consistent |
| `runSparseAdditionGrlex` | 128…4096 | 0.110 → 5.831 ms | −0.010 | consistent |
| `runSparseAdditionGrevlex` | 128…4096 | 0.119 → 6.329 ms | −0.004 | consistent |
| `runSparseMultiplicationLow` | 8…128 | 0.084 → 59.302 ms | +0.069 | consistent |
| `runSparseMultiplicationHigh` | 8…512 | 0.033 → 254.524 ms | −0.084 | consistent |
| `runCancellationInt` | 8…256 | 0.178 → 399.234 ms | −0.028 | consistent |
| `runCancellationRat` | 8…256 | 0.409 → 873.109 ms | −0.036 | consistent |
| `runRenameCollisions` | 64…1024 | 0.023 → 0.360 ms | −0.009 | consistent |
| `runPartialEvalCollisions` | 64…1024 | 0.035 → 0.727 ms | +0.112 | consistent |
| `runSubstCollisions` | 64…1024 | 0.130 → 2.555 ms | −0.060 | consistent |
| `runSumOfSquaresArithmetic` | 8…512 | 0.151 → 2,157.113 ms | +0.038 | consistent |

All eleven targets returned `consistent_with_declared_complexity` in all three
outer trials; none was budget-truncated. `lake exe hexmvpoly_bench verify`
also passed all registrations.

## Comparator Ratios

The two informational comparators are **CompPoly CMvPolynomial** and the
**canonical sorted-list MvSparsePoly proxy** used in place of the unavailable
Mathlib `MvSparsePoly`.
Times below are per-call medians in milliseconds. The ratio is
`Hex time / comparator time`; values above 1 mean the comparator is faster.
These are in-process Lean executables, so no process-call overhead is present.

CompPoly was pinned at
`f4c59f9e6a00b4e73f3e43ca362480468a508011`. Its export records a dirty
checkout because the reproducible setup changes the toolchain, Lake
dependency, and adapter modules. The measured type remains CompPoly's
`CMvPolynomial`. Its storage comparator is fixed vector lexicographic order,
including when the shared family generator requests grlex or grevlex.
Consequently these are matched workload-shape comparisons, but not
comparator-identical measurements for every family. Export:
`reports/bench-results/hex-mv-poly-comppoly-a7430482-chungus2.json`
(SHA-256
`485ab0442431381ad652755a3f0672ad5f17305db6d174b229b29d081e2f80e3`).

The pinned Mathlib revision does not contain `MvSparsePoly`, so the second
adapter is a canonical sorted-list proxy with linear addition and balanced
translated-row multiplication. It matches the intended comparator for each
family and implements the specified representation shape, but it is not an
upstream Mathlib measurement. Export:
`reports/bench-results/hex-mv-poly-sorted-proxy-a7430482-chungus2.json`
(SHA-256
`90a657282e183f603acf336a097b0b760592ab2eac6ef73e260941c1e9ec0be9`).

Both exports used clean Hex commit
`a7430482`, three outer trials per rung, and consistent verdicts throughout.

### `sparse-addition`

| n | Hex | CompPoly | Hex / CompPoly | sorted proxy | Hex / sorted |
|---:|---:|---:|---:|---:|---:|
| 128 | 0.100 | 0.037 | 2.669× | 0.015 | 6.800× |
| 256 | 0.222 | 0.082 | 2.713× | 0.029 | 7.522× |
| 512 | 0.493 | 0.181 | 2.718× | 0.059 | 8.345× |
| 1024 | 1.112 | 0.388 | 2.869× | 0.118 | 9.385× |
| 2048 | 2.426 | 0.834 | 2.908× | 0.247 | 9.809× |
| 4096 | 5.408 | 1.792 | 3.019× | 0.484 | 11.173× |

Both comparators have linear addition, while the current reusable
`mergeWith?` implementation folds the smaller tree through `alter` on the
larger tree. This is the clearest native case for an upstreamable joint merge
plus linear tree builder.

### `sparse-multiplication`

| n | Hex | CompPoly | Hex / CompPoly | sorted proxy | Hex / sorted |
|---:|---:|---:|---:|---:|---:|
| 8 | 0.084 | 0.102 | 0.824× | 0.042 | 2.007× |
| 12 | 0.224 | 0.427 | 0.524× | 0.120 | 1.858× |
| 16 | 0.464 | 1.162 | 0.399× | 0.235 | 1.973× |
| 24 | 1.218 | 4.631 | 0.263× | 0.626 | 1.946× |
| 32 | 2.463 | 12.242 | 0.201× | 1.211 | 2.033× |
| 48 | 6.378 | 47.110 | 0.135× | 3.127 | 2.040× |
| 64 | 12.539 | 121.918 | 0.103× | 5.830 | 2.151× |
| 96 | 30.864 | 476.219 | 0.065× | 15.114 | 2.042× |
| 128 | 59.302 | 1,210.563 | 0.049× | 27.793 | 2.134× |

Hex remains within its `n² log n` declaration. CompPoly's nested singleton
merge follows its derived cubic-logarithmic model and is 20.4× slower at the
largest shared rung. The sorted proxy is 2.13× faster than Hex at `n=128`.

### `cancellation-arithmetic`

| n | Hex `Int` | CompPoly | Hex / CompPoly | sorted proxy | Hex / sorted |
|---:|---:|---:|---:|---:|---:|
| 8 | 0.178 | 0.349 | 0.511× | 0.067 | 2.657× |
| 16 | 0.851 | 3.268 | 0.260× | 0.316 | 2.695× |
| 32 | 4.059 | 32.702 | 0.124× | 1.412 | 2.874× |
| 64 | 19.152 | 314.984 | 0.061× | 6.383 | 3.000× |
| 96 | 47.523 | 1,165.727 | 0.041× | 14.782 | 3.215× |
| 128 | 88.671 | 3,130.162 | 0.028× | 27.302 | 3.248× |
| 192 | 211.122 | — | — | 63.686 | 3.315× |
| 256 | 399.234 | — | — | 129.512 | 3.083× |

CompPoly reaches its four-second call cap after `n=128`; Hex is 35.3× faster
at that shared rung. The sorted proxy stays within its declared model and is
3.08× faster than Hex at `n=256`.

### `structural-collisions`

| n | Hex rename | CompPoly | Hex / CompPoly | sorted proxy | Hex / sorted |
|---:|---:|---:|---:|---:|---:|
| 64 | 0.023 | 0.040 | 0.576× | 0.026 | 0.890× |
| 128 | 0.046 | 0.082 | 0.561× | 0.052 | 0.874× |
| 256 | 0.091 | 0.162 | 0.559× | 0.106 | 0.855× |
| 512 | 0.180 | 0.323 | 0.555× | 0.214 | 0.841× |
| 768 | 0.270 | 0.490 | 0.551× | 0.323 | 0.836× |
| 1024 | 0.360 | 0.664 | 0.541× | 0.434 | 0.829× |

All three curves are linear. Hex is 1.85× faster than CompPoly and about
17–20% faster than the sorted proxy across the ladder.

### `sum-of-squares-arithmetic`

| n | Hex | CompPoly | Hex / CompPoly | sorted proxy | Hex / sorted |
|---:|---:|---:|---:|---:|---:|
| 8 | 0.151 | 0.103 | 1.458× | 0.069 | 2.175× |
| 16 | 0.803 | 1.099 | 0.731× | 0.348 | 2.310× |
| 32 | 4.152 | 11.415 | 0.364× | 1.743 | 2.382× |
| 64 | 20.331 | 114.114 | 0.178× | 8.442 | 2.408× |
| 128 | 95.737 | 1,103.494 | 0.087× | 38.991 | 2.455× |
| 192 | 233.468 | — | — | 95.188 | 2.453× |
| 256 | 441.906 | — | — | 182.773 | 2.418× |
| 384 | 1,119.376 | — | — | 489.180 | 2.288× |
| 512 | 2,157.113 | — | — | 1,047.070 | 2.060× |

CompPoly reaches its call cap after `n=128`, where Hex is 11.5× faster.
Hex and the sorted proxy both retain `n² log n` scaling; the proxy is 2.06×
faster at the largest rung.

The five visualizations are:

- [sparse addition](figures/hex-mv-poly-comparator-sparse-addition.svg)
- [sparse multiplication](figures/hex-mv-poly-comparator-sparse-multiplication.svg)
- [cancellation arithmetic](figures/hex-mv-poly-comparator-cancellation-arithmetic.svg)
- [structural collisions](figures/hex-mv-poly-comparator-structural-collisions.svg)
- [sum of squares](figures/hex-mv-poly-comparator-sum-of-squares-arithmetic.svg)

Native ratios are informational rather than the representation gate. The
release-quality kernel sweep measured construction-subtracted point ratios of
5.164× for addition, 2.201× for cancellation, 3.249× for SOS, and 2.068× for
structural collisions. Their conservative intervals start at 1.903×, 1.377×,
1.772×, and 1.459× respectively; addition is also noise-limited.
Collision-heavy multiplication is a resolved 0.971× failure. No family has a
lower bound above 2×, so the
predeclared two-family gate is not met. The decision and its robust
null-envelope checks are recorded in
[`hex-mv-poly-mathlib-performance.md`](hex-mv-poly-mathlib-performance.md):
keep `ExtTreeMap` as the single production representation.

## Profile

Profiles were captured from clean commit
`23181fc72b705e4defc38c74e647de8b4655c531` on `chungus2` (AMD EPYC 9455
48-Core Processor, x86-64 Linux 6.12.95), with Lean `4.32.0-rc1`,
lean-bench `0.1.0` at `fa30c2763cf523f3ac8e46dc3a1dad0845a40098`,
lean-bench-samply
`9356baa2f5757ee40320a897bd284914d5bb9f5e`, and samply `0.13.1` at
999 Hz. Inputs are deterministic and use no seed. Raw filtered profiles and
symbol sidecars remain under `/tmp` and are not committed.

The common command form was:

```sh
LEAN_BENCH_SAMPLY_HOME=/tmp/lean-bench-samply.XAz9Og/repo \
  scripts/profile/run_profile.sh ./.lake/build/bin/hexmvpoly_bench \
  BENCH_NAME PARAM 5000000000
```

### `sparse-addition`

`BENCH_NAME=Hex.MvPolyBench.runSparseAdditionLex`, `PARAM=4096`.
Leaf cost was allocation/free 47.36%, Lean runtime/Std 47.44%, own
HexMvPoly code 5.16%, other system 0.04%, and GMP 0%. Inclusive cost was led
by the registered target (99.96%), the tree fold (92.63%), `alter` (89.16%),
`Mono.lex` (64.42%), and tree balancing (11.18%). This is the current generic
`mergeWith?` path: it folds one tree through deletion-capable updates in the
other. No preparation phase is present, and the shape directly motivates the
future direct tree-cursor fold and linear builder in the reusable map layer.

### `sparse-multiplication`

`BENCH_NAME=Hex.MvPolyBench.runSparseMultiplicationLow`, `PARAM=128`.
Leaf cost was Lean runtime/Std 44.92%, allocation/free 40.35%, and own code
14.73%; GMP and other system were 0%. Inclusive cost was led by the registered
target (100%), the `MvPoly.mul` tree fold (97.40%),
`addMonomial`/`alter` (95.29%), `Mono.grlex` (84.19%), and monomial degree
folding (7.45%). This is the registered Gustavson-style accumulation path.

### `cancellation-arithmetic`

`BENCH_NAME=Hex.MvPolyBench.runCancellationRat`, `PARAM=256`.
Leaf cost was Lean runtime/Std 41.46%, allocation/free 37.07%, own code
14.61%, GMP 6.84%, and other system 0.02%. Inclusive cost was led by the
registered target (98.16%), the `MvPoly.mul` tree fold (97.15%),
`addMonomial`/`alter` (88.49%), `Mono.grevlex` (67.09%), and `Mono.revlex`
(15.27%). GMP appears as expected for rational coefficient arithmetic; the
dominant work remains the registered multiplication and cancellation path.

### `structural-collisions`

`BENCH_NAME=Hex.MvPolyBench.runSubstCollisions`, `PARAM=1024`.
Leaf cost was Lean runtime/Std 38.07%, own code 36.89%, and allocation/free
25.04%; GMP and other system were 0%. Inclusive cost was led by the registered
target and `MvPoly.bind`/`subst` tree fold (99.98%), `Mono.prod` (82.19%),
the inner `MvPoly.mul` fold (63.63%), and `Mono.powBySq` (54.72%). These are
the expected substitution stages and all belong to the registered structural
target.

### `sum-of-squares-arithmetic`

`BENCH_NAME=Hex.MvPolyBench.runSumOfSquaresArithmetic`, `PARAM=256`.
Leaf cost was Lean runtime/Std 41.26%, allocation/free 38.33%, own code
20.35%, other system 0.05%, and GMP 0%. Inclusive cost was led by the
registered target (100%), `Mono.grevlex` (72.17%), the `MvPoly.mul` fold
(69.75%), and `addMonomial`/`alter` (67.04%). The three sparse squares and
their accumulation account for the profile.

All five filters passed confidence and ±5 ms sensitivity checks:

| family | residual | timed duration | retained / rejected | other-thread noise | sensitivity |
|---|---:|---:|---:|---:|---|
| sparse addition | 0.710 ms | 5,224.9 ms | 5,213 / 33 | 0 | passed |
| sparse multiplication | 0.517 ms | 3,713.8 ms | 3,693 / 7 | 0 | passed |
| cancellation arithmetic | 0.637 ms | 4,369.5 ms | 4,354 / 7 | 0 | passed |
| structural collisions | 1.188 ms | 5,377.3 ms | 5,367 / 8 | 0 | passed |
| sum of squares | 0.576 ms | 4,150.8 ms | 4,132 / 7 | 0 | passed |

No dominant inclusive cost is outside its registered benchmark target.

The native and comparator captures predate the final query, bridge,
proof-probe, and measurement-harness hardening. Those later changes do not
alter a registered native rung or a timed implementation path; the
size-eight collision correction is confined to the smaller kernel probe,
while the native structural ladder starts at size 64. The captures therefore
remain applicable to the final implementation.

## Concerns

The implementation milestone is complete. The representation experiment is
not a positive performance result: four terminal comparisons remain
statistically unresolved and multiplication is a resolved failure. Under the
predeclared gate, zero families pass.

The Mathlib-native curve is explicitly a sorted-list proxy because
`MvSparsePoly` is unavailable at the pinned revision, and CompPoly's fixed
lexicographic comparator prevents exact comparator matching on every family.
These are disclosed measurement limitations, not Hex implementation defects.

The release-quality kernel gate does not justify a second representation.
Within the single `ExtTreeMap` implementation, the addition profile identifies
a promising optimization: a direct tree-cursor version of the existing
ordered joint fold plus a linear tree builder. Such primitives belong in the
reusable, Std-only `HexBasic/ExtTreeMap.lean` upstream-candidate layer.
Coefficient combination and removal of zero coefficients remain `HexMvPoly`
policy.
