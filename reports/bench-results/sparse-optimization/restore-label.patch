--- a/HexGraphIso/Nauty/Sparse/Refine.lean
+++ b/HexGraphIso/Nauty/Sparse/Refine.lean
@@ -44,13 +44,9 @@
 /-- Vertex-to-cell indices, using `n` for vertices in singleton cells. -/
 def cellstarts (n : Nat) (lab ptn : Array Nat) (level : Nat) : Array Nat := Id.run do
   let mut starts := Array.replicate n n
-  let mut first := 0
-  for _ in [0:n] do
-    if first >= n then break
-    let last := cellEnd ptn level first
+  for (first, last) in cells ptn level n do
     if first < last then
       for i in [first:last + 1] do starts := starts.set! lab[i]! first
-    first := last + 1
   return starts
 
 /-- Divide one cell by counts or distances. The first two minimum
@@ -233,32 +229,21 @@
 singleton splitters among the first ten active entries. -/
 def refine (g : Graph n) (level : Nat) (lab ptn : Array Nat)
     (active : VSet n) (numcells : Nat) : RefineSt n := Id.run do
-  -- Walk the packed set in the same ascending order as nauty's initial
-  -- active scan, without constructing and filtering a list of all vertices.
-  let mut initialQueue := #[]
-  let mut next := active.nextElem none
-  for _ in [0:n] do
-    let some i := next | break
-    initialQueue := initialQueue.push i
-    next := active.nextElem (some i)
+  let queue := ((List.range n).filter active.mem).toArray
   let mut s : RefineSt n := {
-    lab, ptn, active, queue := initialQueue
+    lab, ptn, active, queue
     cellstart := #[], hits := Array.replicate n 0, marks := Array.replicate n 0,
     vmarks := Array.replicate n 0,
     numcells, longcode := numcells }
-  if initialQueue.isEmpty then return { s with longcode := cleanup s.longcode }
+  if queue.isEmpty then return { s with longcode := cleanup s.longcode }
   s := { s with cellstart := cellstarts n lab ptn level }
-  if level <= 2 && initialQueue.size == 1 && ptn[initialQueue[0]!]! <= level && numcells <= n / 8 then
-    let split := initialQueue[0]!
+  if level <= 2 && queue.size == 1 && ptn[queue[0]!]! <= level && numcells <= n / 8 then
+    let split := queue[0]!
     s := { s with
       queue := #[], active := s.active.erase split
       hits := distvals g lab[split]! }
-    let mut first := 0
-    for _ in [0:n] do
-      if first >= n then break
-      let last := cellEnd ptn level first
+    for (first, last) in cells ptn level n do
       if first < last then s := splitCounts level first true s
-      first := last + 1
   -- A split adds no more queue entries than new cells. With at most n
   -- initial active entries bounded by the initial cell count, n iterations
   -- suffice before the queue empties.
--- a/HexGraphIso/Nauty/Sparse/Target.lean
+++ b/HexGraphIso/Nauty/Sparse/Target.lean
@@ -20,16 +20,12 @@
   let mut starts := #[]
   let mut sizes := #[]
   let mut nntcell := Array.replicate n n
-  let mut first := 0
-  for _ in [0:n] do
-    if first >= n then break
-    let last := cellEnd ptn level first
+  for (first, last) in cells ptn level n do
     if first < last then
       let index := starts.size
       starts := starts.push first
       sizes := sizes.push (last - first + 1)
       for j in [first:last + 1] do nntcell := nntcell.set! lab[j]! index
-    first := last + 1
   if starts.isEmpty then return n
   let mut hits := Array.replicate starts.size 0
   let mut best := 0
--- a/bench/HexGraphIso/SparseBench.lean
+++ b/bench/HexGraphIso/SparseBench.lean
@@ -8,9 +8,7 @@
 
 /-! Native sparse timing driver for the six-way comparison. The shared
 adjacency-text corpus is decoded before timing. Native JSON edge input is
-also supported for larger graphs. Every timed repetition is emitted. An
-optional iteration count runs one stage repeatedly for CPU profiling;
-parsing and preparation remain outside that loop. -/
+also supported for larger graphs. Every timed repetition is emitted. -/
 
 open Lean Hex.GraphIso
 
@@ -97,8 +95,7 @@
     let (warmup, samples) ← time action
     let key := if mode == "canon" then "hex_sparse_ns"
       else if mode == "run" then "hex_sparse_search_ns"
-      else if mode == "build" then "hex_sparse_build_ns"
-      else if mode == "label" then "hex_sparse_label_ns" else "hex_sparse_output_ns"
+      else if mode == "build" then "hex_sparse_build_ns" else "hex_sparse_output_ns"
     let fields := [("name", toJson name), ("family", toJson family), ("n", toJson n),
       (key, toJson (samples.foldl min samples[0]!)),
       ("samples_ns", toJson samples), ("warmup_ns", toJson warmup),
