# Phase-4 Performance Reports

This directory holds the current headline performance report for each library
that is being audited or has reached Phase 4.

Reports are observed state, not SPEC text. Re-running an audit replaces the
library's report in place; `git log -- reports/<lib>-performance.md` is the
history channel.
