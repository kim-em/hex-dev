## Current state

`HexGramSchmidtMathlib` is a proof-only Mathlib bridge (`mathlib: true`,
deps include `HexGramSchmidt`; the executable Gram–Schmidt kernel and its
`hexgramschmidt_bench` live in `HexGramSchmidt`). It sits at `libraries.yml`
`HexGramSchmidtMathlib.done_through: 4` — Phase 4 was recorded as a
bookkeeping completion in #7808, since `SPEC/benchmarking.md §"Mathlib-free
benches"` forbids any `Hex*Mathlib` bench.

`python3 scripts/status.py HexGramSchmidtMathlib` reports the library is now
at `Phase 5 (implementation work loop)` and `ready: yes` (Phase 5 has local
coupling only, requiring `done_through ≥ 4`, which is met).

The three source files under `HexGramSchmidtMathlib/` currently contain
**zero `sorry` occurrences** (tokenized `grep -rnw sorry` → 0).

## Deliverables

1. Confirm `HexGramSchmidtMathlib` meets the Phase 5 exit criteria
   (`PLAN/Phase5.md`): zero `sorry` occurrences across all declarations,
   `lake build HexGramSchmidtMathlib` green on the pinned toolchain, and no
   library-specific conformance regression (proof-only bridges carry no own
   conformance fixtures; the executable `HexGramSchmidt` suite is
   unaffected).
2. Bump `libraries.yml` `HexGramSchmidtMathlib.done_through` `4 → 5`. Single
   one-line change; touch no other library entry.

This mirrors the established Phase-5 audit-and-bump precedent (e.g. #6925
for the executable `HexGramSchmidt`) and the proof-only Phase-4 precedent
(#7808, #7809).

## Context

- `libraries.yml` semantics: `done_through: K` means phases 1..K complete
  (linear, no skipping).
- Phase 5 (`PLAN/Phase5.md`): "Fill in `sorry`'d proofs." Exit criteria:
  zero `sorry`, green `lake build`, conformance still passes. For a
  proof-only bridge already at zero sorries this is an audit-and-bump, not
  new proof work.
- Premise check (per `.claude/CLAUDE.md` "Directives are hypotheses"): if
  the build is **not** green, or any `sorry` is in fact present, do **not**
  bump. Stop and comment with the concrete blocker (file:line of the sorry,
  or the build error), and leave the issue claimable — do not weaken the
  exit criteria or paper over a real gap.

## Verification

- `grep -rnw sorry HexGramSchmidtMathlib --include='*.lean'` → no matches.
- `lake build HexGramSchmidtMathlib` → builds clean, 0 `error:`.
- `python3 scripts/status.py HexGramSchmidtMathlib` → `done_through: 5`,
  advanced to `Phase 6`, `ready: yes`.
- `python3 scripts/status.py` → exit 0, no new blocked regressions.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff` touches only `libraries.yml` (plus the turn's progress file);
  `git diff --check` clean.
- Added-line forbidden-token scan: no `sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`.
