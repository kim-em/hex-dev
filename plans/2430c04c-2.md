## Current state

`HexBerlekampZassenhausMathlib/IntReductionMod.lean` exposes the
small-mod singleton arm `hcomplete` discharger
`reassemblyExpansionComplete_singleton_of_irreducible` (line 2246),
which takes `Hex.ZPoly.Irreducible (normalizeForFactor f).squareFreeCore`
together with `hmonic : DensePoly.Monic (normalizeForFactor f).squareFreeCore`
and `hdeg : 0 < (normalizeForFactor f).squareFreeCore.degree?.getD 0`
and produces
`reassemblyExpansionComplete (normalizeForFactor f) #[(normalizeForFactor f).squareFreeCore]`.
The docstring (lines 2231–2241) explicitly flags the `hmonic`
premise as **Gap 1** — analogous to (and contemporaneous with) the
exhaustive arm umbrella's Gap 1 tracked under #4880, but
self-contained to this discharger.

The proof body (lines 2253–2298) routes through two substrates:

1. `normalizeForFactor_repeatedPart_isFactorPower_squareFreeCore_of_irreducible`
   (line 2181) — produces `repeatedPart = Factorization.factorPower core k`.
   This step is monicness-agnostic: it consumes
   `Hex.squareFreeCore_normalizeFactorSign_of_ne_zero` (the
   `normalizeFactorSign core = core` invariant) which holds for any
   non-zero `f`, not just monic core.
2. `Hex.expandRepeatedPartFactorArray_pow_singleton`
   (`HexBerlekampZassenhaus/Basic.lean:11872`) — the leaf
   monic-only singleton specialisation, which currently consumes
   `hmonic` to discharge `consumeExactPower_pow_mul_of_not_dvd`'s
   monicness requirement.

For a non-monic primitive core with positive leading coefficient
(e.g. the `2X + 3` residual from `(X-1)(2X+3) = 2X^2 + X - 3`,
which is exactly the case `quadraticIntegerRootFactors?` already
emits), the existing array-level public surface
`expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
(`HexBerlekampZassenhaus/Basic.lean:11777`, #4778) suffices to
discharge `(expand ...).2 = 1` (the residual projection that
`reassemblyExpansionComplete` unfolds to). The quadratic arm's
discharger
`reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
(line 2346) already routes through this `_of_pos_lc` array-level
surface; this issue does the singleton specialisation.

## Deliverables

In `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, immediately
following `reassemblyExpansionComplete_singleton_of_irreducible`
(line 2246–2298), add the `_of_pos_lc` sibling:

```lean
theorem reassemblyExpansionComplete_singleton_of_irreducible_of_pos_lc
    (f : Hex.ZPoly) (hf : f ≠ 0)
    (hirr : Hex.ZPoly.Irreducible (Hex.normalizeForFactor f).squareFreeCore)
    (hpos_lc : 0 < Hex.DensePoly.leadingCoeff
      (Hex.normalizeForFactor f).squareFreeCore)
    (hdeg :
      0 < (Hex.normalizeForFactor f).squareFreeCore.degree?.getD 0) :
    Hex.reassemblyExpansionComplete (Hex.normalizeForFactor f)
      #[(Hex.normalizeForFactor f).squareFreeCore]
```

The proof body mirrors the monic version (lines 2253–2298) with two
substitutions:

* The size lower-bound block (`hcore_size_ge_two` at line 2259,
  `hfactorPower_size_ge` at line 2271) is monicness-agnostic and
  carries over verbatim. The induction at line 2271–2287 uses only
  `0 < core.size` (forced by `hcore_size_ge_two`) and the size
  multiplication lemma `ZPoly.mul_size_eq_top_succ_of_nonzero`, both
  monic-free.

* The final apply (lines 2293–2298) is the **only** substitution.
  Replace:
  ```lean
  unfold Hex.reassemblyExpansionComplete
  have hexpand :=
    Hex.expandRepeatedPartFactorArray_pow_singleton
      core k hmonic hdeg hirr (Hex.normalizeForFactor f).repeatedPart hk hfuel
  rw [hexpand]
  ```
  with either:
  * **Option A (preferred):** apply the non-monic singleton leaf
    `expandRepeatedPartFactorArray_pow_singleton_of_pos_lc` (the
    deliverable of the companion leaf issue), which produces
    `.2 = 1` directly. If the companion leaf has not yet landed,
    Option B is used.
  * **Option B:** apply the array-level public surface
    `Hex.expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`
    (`HexBerlekampZassenhaus/Basic.lean:11777`) directly,
    specialised to `coreFactors = #[core]`, `exponents = [k]`. The
    proof obligations are:
    - `hpos_lc'` per-factor: `∀ q ∈ #[core].toList, 0 < leadingCoeff q`
      — `simp` reduces to `0 < leadingCoeff core`, supplied by
      `hpos_lc`.
    - `hdegree'` per-factor: similar, supplied by `hdeg`.
    - `hlen`: `[k].length = #[core].size` — both equal `1`, by
      `rfl` / `decide`.
    - `hnot_dvd_tail` for split `pre = []`, `(core, k)`, `suf = []`:
      tail product is `1`; `¬ core ∣ 1` follows from
      `Hex.irreducible_not_dvd_one hirr` — wait, that helper is
      `private` in `HexBerlekampZassenhaus/Basic.lean` (line 11809).
      Re-derive locally via `Hex.ZPoly.size_le_of_dvd_nonzero` +
      irreducibility size lower bound (mirroring the existing
      derivation pattern in
      `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
      lines 2400–2451). For any non-trivial split the zip has
      length 1, so the precondition is vacuously satisfied.
    - `hdecomp`: `repeatedPart = ([(core, k)].map factorPower).foldl (· * ·) 1`
      reduces via `simp [List.zip_cons_cons, List.zip_nil_right,
      List.map_cons, List.map_nil, List.foldl_cons, List.foldl_nil,
      Hex.ZPoly.one_mul_zpoly]` to `repeatedPart = factorPower core k`,
      which is exactly `hk`.
    - `hfuel'` for `(core, k) ∈ [(core, k)]`: `k + 1 ≤ repeatedPart.size + 1`,
      which is exactly the existing `hfuel`.

  The worker picks Option A if the companion leaf has landed,
  otherwise Option B. Either path produces the same conclusion.

The new sibling's docstring should:
* Reference the monic-flavoured sibling and explain the
  conclusion-equivalent residual-projection form.
* Note that the singleton-arm umbrella
  `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  (line 2570) does not itself require `hcore_monic` — it consumes
  `hcomplete` from the caller — so the value of this sibling is in
  allowing downstream dispatchers to discharge `hcomplete` under
  non-monic primitive `squareFreeCore`.

No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

This is a **substrate addition only**; do not edit the existing
monic mid-layer or its consumers in this PR.

## Context

* Companion leaf substrate (filed in parallel): `_of_pos_lc`
  sibling of `expandRepeatedPartFactorArray_pow_singleton`. Either
  proof path (Option A: route through the leaf; Option B: route
  through the array-level public surface) is valid.
* Existing `_of_pos_lc` consumer pattern:
  `reassemblyExpansionComplete_quadraticIntegerRootFactors_of_ne_zero`
  (line 2346) — routes through the array-level public surface
  `expandRepeatedPartFactorArray_residual_eq_one_of_factorPower_decomposition_of_pos_lc`.
* Exhaustive arm mid-layer `_of_pos_lc` parallel: #4949 (landed) —
  added `reassemblyExpansionComplete_of_irreducible_squarefree_cover_of_pos_lc`
  using the same array-level public surface.
* Available substrates to discharge `hpos_lc` + `hdeg` for a
  downstream dispatcher caller:
  - `Hex.squareFreeCore_leadingCoeff_pos_of_ne_zero` for `hpos_lc`,
  - the existing `hdeg` premise (already supplied by callers; the
    constant-arm guard ensures `hdeg ≠ 0` is propagated to
    `hdeg > 0` by `squareFreeCore_size_pos_of_ne_zero` / similar).
* Parent tracking: the singleton-arm Gap 1 is documented in the
  existing
  `reassemblyExpansionComplete_singleton_of_irreducible` docstring
  (lines 2231–2241). After this issue, the docstring of the new
  sibling should reflect that the singleton-arm Gap 1 is now
  closed at the discharger layer.

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus.md` —
  Mathlib-bridge reassembly discharger.
* **File:** `HexBerlekampZassenhausMathlib/IntReductionMod.lean`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.IntReductionMod`
* `lake build HexBerlekampZassenhausMathlib` (regression check —
  no new errors relative to `origin/main` baseline in the
  `HexBerlekampZassenhausMathlib/Basic.lean` whnf-timeout pile)
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  shows no new entries on the diff against `origin/main`.

## Out of scope

* Rewiring the existing monic-flavoured
  `reassemblyExpansionComplete_singleton_of_irreducible` as a
  wrapper over the new sibling. The existing surface stays as-is;
  the new sibling adds an additional API surface.
* The companion leaf substrate
  `expandRepeatedPartFactorArray_pow_singleton_of_pos_lc` — filed
  as a separate parallel issue. This issue does not block on it;
  the worker can pick Option B if the leaf has not yet landed.
* The singleton-arm umbrella
  `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
  is not modified — it takes `hcomplete` from caller and already
  has no `hcore_monic` premise.
* The umbrella drop in #4880 (architecturally blocked, exhaustive
  arm).
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
