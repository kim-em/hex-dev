## Current state

The A2 abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/Basic.lean` is climbing past the
public-facing coverage layer.  The public coverage theorem
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
(line 14245) gets its `_of_bound` sibling in #4938 (blocked on #4936).

This issue is the **slow-path capstone** one layer up:

* `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
  at `HexBerlekampZassenhausMathlib/Basic.lean:14326`.

It takes
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k`
and consumes it at exactly **one** call site (line 14388) — the
forward call to
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
(the line 14245 wrapper that gets an `_of_bound` sibling in #4938).

The rest of the proof body — the `hcoverage` construction that
synthesises a `q ∈ normalizedFactors f` cover by lifting each
irreducible integer divisor `q` of `f := toPolynomial core` to
`factor : Hex.ZPoly` and invoking the coverage call, the `hcount_ge`
lower bound via
`UFDPartition.normalizedFactors_card_le_length_of_coverage`, the
`hcount_le` upper bound via
`exhaustiveCoreFactorsWithBound_factor_count_le`, and the final
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_count`
discharge — is precision-agnostic and carries over verbatim.

## Hypothesis shape

Mirror the established `_of_bound` shape from
#4907 / #4920 / #4922 / #4928 / #4930 / #4932 / #4933 / #4935 /
#4936 / #4938.  Replace the existing
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k`
binder with the four-parameter block

```lean
(B' : Nat)
(hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hvalid : ∀ g : Hex.ZPoly, g ∣ core → ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

placed at the same syntactic position as the original `hprecision` to
keep parameter ordering consistent with established `_of_bound`
siblings at this layer.  `hcore_lc_le` and `hvalid` are required
because the substrate `_of_bound` from #4938 takes them; the
forwarder threads both straight through to the coverage call.

depends-on: #4938

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, slow-path capstone
  layer.
* **Natural strategy uses Mathlib?** Yes — the wrapper forwards to a
  Mathlib-bridge substrate already in the file.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4938.

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add one abstract-bound
sibling theorem and rewire the existing original as a thin wrapper.

1. **`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`**
   — same signature as the original at line 14326 except `hprecision`
   is replaced by the four-parameter block above.  Per the convention
   established by the prior `_of_bound` siblings at this layer, insert
   the `_of_bound` **before** the wrapper — necessary because Lean 4
   top-level definitions are sequential and the wrapper
   forward-references the `_of_bound`.

   Proof body: same as the original (lines 14351–14410-ish) with the
   single call at line 14385–14388 replaced by
   `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence_of_bound`
   (from #4938), threading `B'`, `hcore_lc_le`, `hvalid`,
   `hprecision`.  All other plumbing (the `hcoverage` quantifier-body
   construction, the `hcount_ge` / `hcount_le` count-bound
   derivations, the `hcount_eq` join, and the final
   `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_count`
   application) is precision-agnostic and carries over verbatim.

2. **Rewire** the existing
   `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
   (line 14326) as a thin wrapper.  The wrapper:
   - derives `hcore_lc_le` via
     `defaultFactorCoeffBound_valid core hcore_ne core
     (Hex.DensePoly.dvd_refl_poly core) (core.size - 1)`
     paired with `leadingCoeff_eq_coeff_last` (same discharge shape as
     #4907 / #4920 / #4922 / #4928 / #4930 / #4932 / #4933 / #4935 /
     #4936 / #4938's wrapper rewiring),
   - discharges the universal `hvalid` for an arbitrary `g ∣ core` via
     `defaultFactorCoeffBound_valid core hcore_ne g hg_dvd_core`
     (identical to #4935 / #4936 / #4938's wrapper discharge),
   - delegates into the new `_of_bound` sibling with
     `B' := Hex.ZPoly.defaultFactorCoeffBound core` and `hprecision`
     threaded verbatim.

   The wrapper preserves the existing signature exactly; the single
   in-tree consumer
   `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
   at line 14729 continues to type-check unchanged.

No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — pre-existing
  error set only; no new errors in the edit range
  (~14326–~14410-ish).  The 15 baseline errors on `origin/main`
  (whnf timeouts at 4848–6245, `cases`-nested failure at 5803,
  kernel-unknown at 6560 / 6624, and four
  timeouts / kernel-unknown errors at the upper end of the file) will
  shift by the net line addition (~80 lines per prior `_of_bound`
  sibling at this layer); confirm via diff against `origin/main` that
  the line shift matches the net `+` minus `−` count.
* `lake build HexBerlekampZassenhausMathlib` — same.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean |
   grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` —
  no new entries.

## Context

This is the slow-path **capstone** of the recombination-coverage
chain: it converts the per-irreducible-factor coverage produced by
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
into a full irreducibility statement for every emitted factor.  Its
sole in-tree consumer is the outer-bound wrapper
`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
at line 14729 (which itself carries the same `hprecision` and forwards
it verbatim) — and so the outer-bound wrapper becomes the next
natural `_of_bound` target after this issue lands, as a separate
follow-on.

Files touched: `HexBerlekampZassenhausMathlib/Basic.lean` only.  No
new imports.
