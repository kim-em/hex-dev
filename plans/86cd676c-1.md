## Current state

The abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/Basic.lean` has cleared both flavours
of the unscaled finite-degree cover (#4912 / #4915 — landed). The
scaled-candidate counterpart at line 11112,

* `exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`,

still consumes
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k`
in its core-shape form. It threads `hprecision` at exactly two call
sites:

* Line 11154: `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum`
  — the scaled-candidate natDegree decomposition.
* Line 11166: `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core`
  — the per-factor natDegree identity.

Both callees now have `_of_bound` siblings: the per-factor identity
landed via #4904, and the scaled-candidate natDegree decomposition is
landing as PR #4920 / issue #4916
(`natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum_of_bound`,
signature `(B' : Nat) (hcore_ne) (hcore_lc_pos) (hd_liftedFactor_monic)
(hcore_lc_bound : (lc core).natAbs ≤ B') (hprecision : 2 * B' < d.p ^ d.k)`).

The rest of the proof body (the `f` definition, `h_g_eq`, `h_pwdisj`,
`h_sub`, `h_sum_eq`, `h_zero`, `h_empty`, and the final
`Finset.mem_biUnion` extraction at lines 11147–11206) is
precision-agnostic and carries over verbatim.

The natural `_of_bound` shape mirrors the unscaled primitive sibling
at line 9766
(`exists_mem_representedSubset_of_degree_cover_of_primitive_pos_lc_core_of_bound`),
parameterising the abstract bound over `gs`:

```lean
(B' : Nat)
(hcore_lc_bound : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hvalid : ∀ g ∈ gs, ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

The wrapper instantiates `B' := defaultFactorCoeffBound core`,
discharges `hcore_lc_bound` via `defaultFactorCoeffBound_valid core
hcore_ne core hcore_dvd_self (core.size - 1)` + the
`leadingCoeff_eq_coeff_last` identity (same derivation block used in
the unscaled primitive wrapper at lines 9914–9938), and discharges
`hvalid` via the divisor chain `g ∣ target ∣ core` plus
`defaultFactorCoeffBound_valid` (also identical to the unscaled
wrapper at lines 9939–9949). Note the scaled cover does **not** carry
`hd_modulus` (the unscaled cover passes it to
`natDegree_toPolynomial_recombinationCandidate_eq_sum`; the scaled
sibling routes precision through the scaled natDegree decomposition,
which has a different signature).

The downstream consumer is
`mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
at line 11228; its `_of_bound` sibling is the scaled counterpart of
#4918 / #4919 and remains separately blocked on #4907 (scaled
per-factor bridge `_of_bound`, currently claimed). This issue
unblocks the scaled cover input but is independent of #4907.

depends-on: #4916

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, finite degree-cover
  layer, scaled primitive non-monic flavour.
* **Natural strategy uses Mathlib?** Yes — relies on
  `Finset.sum_biUnion`, `Finset.sum_sdiff`, etc.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** No — both precision
  consumers have `_of_bound` siblings (#4904 landed, #4916 in flight
  as PR #4920).

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add an abstract-bound
sibling immediately before the existing theorem at line 11112, and
rewire the existing theorem as a thin wrapper.

1. **`exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`**:

   ```lean
   theorem exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound
       {core target : Hex.ZPoly} {d : Hex.LiftData}
       {J T : LiftedFactorSubset d}
       (B' : Nat)
       (hcore_ne : core ≠ 0)
       (hcore_primitive : Hex.ZPoly.Primitive core)
       (hcore_lc_pos : 0 < Hex.DensePoly.leadingCoeff core)
       (hcore_lc_bound : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
       (hd_liftedFactor_monic :
         ∀ i, Hex.DensePoly.Monic (liftedFactor d i))
       (hd_liftedFactor_natDegree_pos :
         ∀ i, 0 < (HexPolyZMathlib.toPolynomial (liftedFactor d i)).natDegree)
       (hprecision : 2 * B' < d.p ^ d.k)
       (hpartition : LiftedFactorSubsetPartition core d J target)
       (htarget_dvd_core : target ∣ core)
       (_hTJ : T ⊆ J)
       (gs : Finset Hex.ZPoly)
       (S_of : Hex.ZPoly → LiftedFactorSubset d)
       (h_each : ∀ g ∈ gs,
         Irreducible (HexPolyZMathlib.toPolynomial g) ∧
         g ∣ target ∧
         g ∣ scaledRecombinationCandidate core d T ∧
         RepresentsIntegerFactorAtLift core d g (S_of g) ∧
         S_of g ⊆ J ∧
         S_of g ⊆ T ∧
         Hex.ZPoly.content g = 1 ∧
         Hex.normalizeFactorSign g = g)
       (hvalid : ∀ g ∈ gs, ∀ i, (g.coeff i).natAbs ≤ B')
       (h_pairwise_not_associated :
         ∀ ⦃g h : Hex.ZPoly⦄, g ∈ gs → h ∈ gs → g ≠ h →
           ¬ Associated (HexPolyZMathlib.toPolynomial g)
             (HexPolyZMathlib.toPolynomial h))
       (h_degree_total :
         (HexPolyZMathlib.toPolynomial
             (scaledRecombinationCandidate core d T)).natDegree =
           ∑ g ∈ gs, (HexPolyZMathlib.toPolynomial g).natDegree) :
       ∀ {i : LiftedFactorIndex d}, i ∈ T → ∃ g ∈ gs, i ∈ S_of g
   ```

   Proof body mirrors the existing theorem at lines 11146–11206 with
   exactly two call-site replacements:

   * Line 11153–11154 — route the `h_cand_eq` step through
     `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum_of_bound`
     (PR #4920), threading `B'`, `hcore_ne`, `hcore_lc_pos`,
     `hd_liftedFactor_monic`, `hcore_lc_bound`, `hprecision`, `T`.
     Use the actual parameter order from the landed PR.
   * Line 11165–11167 — route the per-`g` `h_g_eq` step through
     `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`
     (already landed via #4904), threading `B'`, `hvalid g hg`,
     `hcore_lc_bound`, plus the remaining hypotheses. Use the actual
     parameter order from `Basic.lean` (the in-tree call site appears
     in the unscaled primitive `_of_bound` cover at line 9820–9822).

   The remaining `h_pwdisj` / `h_sub` / `h_sum_eq` / `h_zero` /
   `h_empty` / `Finset.mem_biUnion` extraction blocks copy across
   from the unsuffixed scaled cover unchanged.

2. **Wrapper rewiring**: rewrite the existing
   `exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
   at line 11112 as a thin wrapper that instantiates
   `B' := Hex.ZPoly.defaultFactorCoeffBound core`, discharges
   `hcore_lc_bound` and `hvalid` from `defaultFactorCoeffBound_valid`,
   and delegates. The discharge derivations are textually identical
   to those in the unscaled primitive wrapper at lines 9914–9949
   (`hcore_dvd_self`, `hcore_size_pos`, `hcore_lc_bound`, `hvalid`),
   then a single call to the new `_of_bound` sibling.

   The wrapper signature preserves the existing parameter order from
   line 11112 exactly so the two in-tree call sites
   (`Basic.lean:11402` — `mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`,
   and any earlier scaled-cover consumer) type-check unchanged.

## Context

* Pattern precedent: the parallel unscaled-side issue #4912 / PR
  #4915 (already landed) used exactly the same shape and the same
  wrapper-discharge derivations; the present scaled issue is the
  textual mirror with `recombinationCandidate` →
  `scaledRecombinationCandidate` everywhere and the precision
  consumer swapped to its scaled sibling. Cross-reference: the
  unscaled `_of_bound` sibling at lines 9766–9861 and its wrapper at
  9879–9956 are the model.
* Other `_of_bound` precedents in the chain: #4894, #4899, #4900,
  #4904, #4906, #4908.
* Sibling consumer issue (out of scope here): the scaled
  `mem_T_iff_*_of_primitive_pos_lc_core_of_bound` sibling at line
  11228, which becomes plannable once this issue plus #4907 (scaled
  per-factor bridge `_of_bound`) both land.
* Files touched: `HexBerlekampZassenhausMathlib/Basic.lean` only.
* No new imports, no `axiom`, no `native_decide`, no `sorry`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — the only new
  diagnostics in the edit region should be standard unused-variable
  warnings on vestigial wrapper parameters where applicable
  (consistent with the convention in #4894 / #4900 / #4904 / #4906
  / #4908 / #4912). Pre-existing heartbeat-timeout /
  kernel-unknown-constant errors on this file are out of scope.
* `python3 scripts/check_dag.py` — passes.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no
  new entries.
* Confirm in-tree call sites of the unsuffixed scaled cover still
  type-check unchanged.
