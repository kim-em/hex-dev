## Current state

`HexMatrix/Bareiss.lean` is in Phase 6 (proof polishing). Phase 6 requires
docstring coverage per `SPEC/design-principles.md` (public declarations
and non-obvious private helpers). The array-backed Bareiss
fraction-free determinant driver cluster (lines ~824–1060) is
undocumented. These six private declarations assemble the executable
`Array (Array Int)` determinant pipeline:

- `getEntry_rangeMap₂` (~824): entry-access lemma for the
  doubly-ranged map₂ used to materialise the working matrix.
- `pivotArrayLoop` (~903): the fuelled main elimination loop over the
  array state.
- `bareissArrayState` (~1035): builds the initial `BareissArrayState`
  from a `Matrix Int n n`.
- `arraySign` (~1048): the sign contributed by the recorded row swaps.
- `arrayLastDiag?` (~1051): reads the last diagonal entry of the
  reduced rows.
- `bareissArrayDet` (~1056): assembles the determinant value from the
  final array state.

The neighbouring pivot-array search cluster (`findPivotArray*`, lines
~724–797) is already being documented in PR #7331 / issue #7328 — do
**not** touch that region; this issue covers only lines ~824 onward.

## Deliverables

1. Add a one-sentence docstring to each of the six private declarations
   above, leading with the backticked name and stating what it computes
   or proves and its role in the array-backed Bareiss determinant driver.

## Context

Doc-only change. Do **not** touch signatures, statements, proofs, names,
attributes, `simp`/`grind` annotations, declaration order, or imports.
Match the existing Phase 6 docstring style in this file (backtick-led,
one sentence, present tense). No progress notes or meta-commentary (see
`.claude/CLAUDE.md`). No banned vocabulary ("smoke", "gate", "bridge")
and no em-dashes on added lines. Rebase on `main` after #7331 lands so
the two doc batches compose cleanly.

## Verification

- `lake build HexMatrix.Bareiss`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Bareiss.lean` shows additions only.
- Confirm no banned vocabulary or em-dashes on added lines.
