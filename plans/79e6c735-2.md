## Current state

`HexGF2/Euclid.lean` carries packed `GF(2)[x]` long division
(`divMod`/`div`/`mod`), divisibility, and the extended Euclidean
algorithm (`xgcd`/`gcd`). The **public** API already has Phase 6
docstrings (`divMod`, `divMod_spec`, `xgcd`, `gcd`,
`quotient_step_reconstruct`, …).

The **private helper cluster** behind long-division reconstruction and
the xgcd Bezout/divisibility chain (lines ~140–449) is still
undocumented.

## Deliverables

Add one-sentence, backtick-led `/-- … -/` docstrings to the
undocumented private declarations in the long-division +
xgcd-Bezout cluster (lines ~140–449). As of writing these are:

- `add_cancel_middle`, `add_pair_swap`
- `divModAux_reconstruct`, `isZero_eq_true_of_degree?_eq_none`,
  `divModAux_remainder_degree_lt`
- `dvd_refl`, `dvd_zero`, `dvd_add`, `dvd_mul_left`,
  `dvd_of_division_step`
- `xgcd_step_bezout`, `xgcdAux_bezout`,
  `xgcdAux_dvd_current_of_fuel`

Each docstring states *what* the lemma asserts (the reconstruction
invariant of one division step, the remainder degree bound, the
Bezout identity carried by `xgcdAux`, the divisibility closure facts),
not *how* it is proved.

Do **not** document the single-word reduction / canonicalisation
cluster below line ~556 (`nonzero_degree_zero_eq_one`,
`lowerMask_toNat_of_lt_64`, `canonicalWordLT_*`, `coeff_ofUInt64_*`,
`ofUInt64_reduced_*`, …) — that is a separate concern left for a
later batch.

## Context

This follows the Phase 6 docstring convention used in recently merged
work that extends documentation to coherent private-helper clusters
(e.g. #7155 HexPolyFp SquareFree, #7128 HexHensel Quadratic, #7144
HexBerlekamp DistinctDegree). Docstrings only — no signature,
statement, or proof changes.

Skip-if-documented: if any listed declaration already carries a
docstring at claim time, skip it and note it in the PR body rather
than rewriting it. If the whole cluster is already documented, treat
the premise as stale, post evidence, and `coordination skip`.

## Verification

- `lake build HexGF2.Euclid` is green.
- `git diff --check` is clean; the diff is pure docstring insertions
  (no changes to any `theorem`/`def` signature, statement, or proof
  body).
- No banned process jargon in the new docstrings (no "smoke", "gate",
  "bridge", em-dashes).
