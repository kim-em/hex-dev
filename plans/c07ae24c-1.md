## Current state

The public-facing coverage theorem
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
at `HexBerlekampZassenhausMathlib/Basic.lean:13958` is the next
natural `_of_bound` target on the active A2 abstract-bound
propagation cascade.  It carries
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k`
and consumes it at exactly **one** call site (line 13998) — the
forward call to
`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core`
(the line 13894 wrapper that gets an `_of_bound` sibling in #4936).
Its sole in-tree consumer is
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
at line 14098, which itself carries the same `hprecision` and
forwards it verbatim — and so will become the next `_of_bound`
target after this issue lands.

#4936's "Out of scope" section explicitly lists this theorem as the
intended next follow-on:
> An `_of_bound` sibling of
> `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
> at line 13958 — separate follow-on, will be unblocked when both
> wrappers in this issue land.

## Hypothesis shape

Mirror the established `_of_bound` shape from #4922 / #4928 / #4933 /
#4936.  Replace the existing
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k`
binder with the four-parameter block

```lean
(B' : Nat)
(hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hvalid : ∀ g : Hex.ZPoly, g ∣ core → ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

placed at the same syntactic position as the original `hprecision` to
keep parameter ordering consistent with the established `_of_bound`
siblings at this layer.  `hcore_lc_le` and `hvalid` are required
because the substrate `_of_bound` from #4936 takes them; the
forwarder threads both straight through.

depends-on: #4936

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, public coverage layer.
* **Natural strategy uses Mathlib?** Yes — the wrapper forwards to a
  Mathlib-bridge substrate already in the file.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4936.

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add one abstract-bound
sibling theorem and rewire the existing original as a thin wrapper.

1. **`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence_of_bound`**
   — same signature as the original at line 13958 except `hprecision`
   is replaced by the four-parameter block above.  Per the convention
   established by the prior `_of_bound` siblings at this layer
   (#4907 / #4920 / #4922 / #4928 / #4930 / #4932 / #4933 / #4935 /
   #4936), insert the `_of_bound` **before** the wrapper — necessary
   because Lean 4 top-level definitions are sequential and the
   wrapper forward-references the `_of_bound`.

   Proof body: same as the original (lines 13986–14010) with the
   single call at line 13994 replaced by
   `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core_of_bound`
   (from #4936), threading `B'`, `hcore_lc_le`, `hvalid`,
   `hprecision`.  All other plumbing (`hmatches` / `hfuel` derivations
   from `LiftedFactorListMatches.univ` and
   `LiftedFactorListMatches.length_eq_card`, the `hsearchMod` unfold
   step, the final
   `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
   application at line 14009) is precision-agnostic and carries over
   verbatim.

2. **Rewire** the existing
   `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
   (line 13958) as a thin wrapper.  The wrapper:
   - derives `hcore_size_pos` from `hcore_lc_pos` (no zero-size core),
   - derives `hcore_lc_le` via
     `defaultFactorCoeffBound_valid core hcore_ne core
     (Hex.DensePoly.dvd_refl_poly core) (core.size - 1)`
     paired with `leadingCoeff_eq_coeff_last` (same discharge shape as
     #4907 / #4920 / #4922 / #4928 / #4930 / #4932 / #4933 / #4935 /
     #4936's wrapper rewiring),
   - discharges the universal `hvalid` for an arbitrary `g ∣ core` via
     `defaultFactorCoeffBound_valid core hcore_ne g hg_dvd_core`
     (identical to #4935 / #4936's wrapper discharge),
   - delegates into the new `_of_bound` sibling with
     `B' := Hex.ZPoly.defaultFactorCoeffBound core` and `hprecision`
     threaded verbatim.

   The wrapper preserves the existing signature exactly; the single
   in-tree consumer
   `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
   at line 14098 continues to type-check unchanged.

No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Context

This is the public-facing capstone of the recombination-coverage
chain.  It is the gateway between the private
`_some_factor_associated_*` substrate (which gets its `_of_bound`
sibling in #4936) and the consumer-facing exhaustive-arm proof
`exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
(whose `_of_bound` sibling is the natural follow-on after this issue).
Both downstream wrappers (factor-irreducibility at line 14039 and the
exhaustive branch entry at line 14233) consume `hprecision` solely
through their forward call into this coverage theorem.

Files touched: `HexBerlekampZassenhausMathlib/Basic.lean` only.  No
new imports.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — must complete
  without new errors.  Pre-existing `whnf` heartbeat timeouts on this
  file (15 errors, documented in `progress/20260518T034405Z_d1e63d44.md`
  and `progress/20260518T035027Z_184d5454.md`) are environment-specific
  and must not increase in count.
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing
  error set, no new errors.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — exit 0.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
* Confirm
  `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
  at line 14039 still type-checks unchanged through the rewired thin
  wrapper at the original line 13958 position.

## Out of scope

* `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`
  `_of_bound` (line 14039) — separate follow-on issue, will be
  unblocked when this issue lands.
* The monic public wrapper
  `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
  at line 13808 — separate follow-on issue, no in-tree consumers
  (per #4936 out-of-scope note).
* BHKS Theorem 5.2 / directive #2567.
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
