## Current state

`HexPolyFp/SquareFree.lean` (library `HexPolyFp`, `done_through: 5`, so
eligible for Phase 6) develops Yun's square-free factorization over
`FpPoly`. The `yunFactorsContribution` recursion termination /
initial-state cluster around lines ~3919–4083 is undocumented. These
lemmas characterise when the contribution recursion stops, how each step
branches on `isOne` tests, and how the initial state splits.

The 9 declarations lacking docstrings:

- `yunFactorsContribution_stop_of_isOne` (~3919)
- `yunFactorsContribution_terminal_of_isOne` (~3925)
- `yunFactorsContribution_step_of_not_isOne_of_isOne_z` (~3936)
- `yunFactorsContribution_step_of_not_isOne_of_not_isOne_z` (~3945)
- `yunFactorsContribution_tail_repeated_descent` (~3957)
- `yunFactorsContribution_step_preserves_target` (~3970)
- `yunFactorsContribution_initial_state_split` (~4056)
- `yunFactorsContribution_initial_state_done` (~4066)
- `yunFactorsContribution_initial_state_tail` (~4083)

(Line numbers approximate; locate by name.)

## Deliverables

1. Add a one-to-three-sentence `/-- ... -/` docstring to each of the 9
   declarations above, stating what the lemma proves and its role in the
   termination / step-branching analysis of Yun's `yunFactorsContribution`
   recursion.

## Context

This is a Phase 6 docstring-coverage batch (see `PLAN/Phase6.md` and the
docstring rule in `SPEC/design-principles.md`). Doc-only: do not touch
signatures, statements, proofs, `simp`/`grind` attributes, declaration
order, or `private` modifiers. `HexPolyFp` is a CI-built core library
(not a `*Mathlib` bridge), so a normal `lake build HexPolyFp.SquareFree`
verifies it.

This file is large with ~214 undocumented declarations; this issue covers
only the contribution-recursion termination/initial-state subgroup above.
The reconstruction lemmas (`yunFactorsContribution_reconstruct_*`,
~4193–4231), the `derivativeSplit_*` / `quotient_common_dvd_*` divisibility
lemmas (~3542–3779), and other clusters are separate future batches —
leave them alone.

## Verification

- `lake build HexPolyFp.SquareFree` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` is clean.
- `git diff --numstat -- HexPolyFp/SquareFree.lean` shows additions only
  (`N 0`).
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` in added
  lines.
