## Current state

PR #4878 closed #4873 by landing the bottom three `_of_bound` siblings
of the A2 mignotte-precision recovery chain in
`HexBerlekampZassenhausMathlib/Basic.lean`, each accepting an abstract
bound `B' : Nat` plus a validity hypothesis
`hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'` and a precision hypothesis
`2 * B' < d.p ^ d.k`, in place of the core-shape `2 *
defaultFactorCoeffBound core < d.p ^ d.k`.

#4890 is propagating that abstraction one layer up through the monic
capstone `representsIntegerFactorAtLift_monic`
(`HexBerlekampZassenhausMathlib/Basic.lean:7618`), which calls
`centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`
directly (line 7647) without going through any recovery-candidate
intermediary — so #4890's abstraction is independent of the parallel
recovery-candidate `_of_bound` chain (#4882 / #4883).

The natural primitive/positive-leading sibling of
`representsIntegerFactorAtLift_monic` is
`representsIntegerFactorAtLift_primitive`
(`HexBerlekampZassenhausMathlib/Basic.lean:7854`). Its proof body at
line 7933 also calls
`centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`
directly with no recovery-candidate intermediary, so its abstract-bound
sibling is independent of #4882 / #4883 in exactly the same way #4890
is. This issue propagates the abstraction through
`representsIntegerFactorAtLift_primitive` symmetrically to #4890.

depends-on: #4873

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add an abstract-bound
sibling alongside the existing `representsIntegerFactorAtLift_primitive`
at line 7854, and rewire the existing theorem as a thin wrapper that
delegates into the new variant.

1. **`representsIntegerFactorAtLift_primitive_of_bound`** (sibling of
   `representsIntegerFactorAtLift_primitive`):

   ```lean
   theorem representsIntegerFactorAtLift_primitive_of_bound
       {core target factor : Hex.ZPoly} {d : Hex.LiftData}
       {S : LiftedFactorSubset d}
       (B' : Nat)
       (hvalid : ∀ i, (factor.coeff i).natAbs ≤ B')
       (hcore_ne : core ≠ 0)
       (hcore_primitive : Hex.ZPoly.Primitive core)
       (hcore_lc_pos : 0 < Hex.DensePoly.leadingCoeff core)
       (hd_liftedFactor_monic :
         ∀ i, Hex.DensePoly.Monic (liftedFactor d i))
       (hfactor_dvd_target : factor ∣ target)
       (htarget_dvd_core : target ∣ core)
       (hrep : RepresentsIntegerFactorAtLift core d factor S)
       (hprecision : 2 * B' < d.p ^ d.k) :
       Hex.ZPoly.Primitive factor ∧ 0 < Hex.DensePoly.leadingCoeff factor
   ```

   Proof body mirrors the existing theorem, replacing the call to
   `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`
   (line 7933) with
   `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`
   (`HexBerlekampZassenhausMathlib/Basic.lean:3992`, landed by #4878)
   instantiated with `B'`, `hvalid`, `hrep`, `hprecision`. Every other
   step (`hfactor_poly_primitive`, `hcore_size_pos`,
   `hcore_lc_bound`, the centred-lift size/leading-coeff transport)
   is unchanged.

   Note: `hcore_lc_bound` in the existing body discharges itself via
   `defaultFactorCoeffBound_valid core hcore_ne core hcore_dvd_self
    (core.size - 1)`. This step takes the core-shape bound on the
   *core itself* (not on `factor`); preserve it as is — only the
   precision-related call to the bottom-level recovery lemma changes
   to the `_of_bound` flavour. The `hcore_lc_bound` derivation does
   *not* enter the abstract bound `B'`; it is still derived from the
   core's own `defaultFactorCoeffBound`.

2. Rewire the existing `representsIntegerFactorAtLift_primitive` as a
   thin wrapper that instantiates
   `B' := Hex.ZPoly.defaultFactorCoeffBound core` and discharges
   `hvalid` via `defaultFactorCoeffBound_valid core hcore_ne factor
   (Dvd.dvd.trans hfactor_dvd_target htarget_dvd_core)`. The existing
   signature stays identical; only the body changes.

Do **not** propagate the abstraction further at this issue — leave the
downstream consumers
(`representsIntegerFactorAtLift_primitive` call sites at lines 10831
and 12524 on `origin/main`) using the existing core-shape wrapper.
Their abstract-bound siblings are separate follow-up issues, mirroring
the layer-by-layer pattern #4873 established.

Do not introduce `axiom`, `sorry`, `native_decide`, `TODO`, or
`FIXME`. Do not modify top-level `PLAN.md` or `AGENTS.md`.

## Context

Read:

- `HexBerlekampZassenhausMathlib/Basic.lean`:
  - the three `_of_bound` siblings landed by PR #4878 (around lines
    1834, 1889, 3992 on `origin/main`) — the pattern to follow;
  - `representsIntegerFactorAtLift_primitive` (line 7854 on
    `origin/main`) — the rewrite target;
  - `defaultFactorCoeffBound_valid` (line 68) — the validity
    discharger used by the rewired wrapper;
  - `representsIntegerFactorAtLift_monic` (line 7618) and the
    in-flight `representsIntegerFactorAtLift_monic_of_bound` from
    #4890 — the structural twin of this issue on the monic side, for
    reference once #4890 lands.
- #4873 (#4878 PR) — establishes the abstract-bound pattern.
- #4890 — the parallel monic-side issue; this issue mirrors the same
  pattern on the primitive/positive-leading side.

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus.md` — Mathlib
  bridge A2 recovery layer (primitive non-monic path).
* **Mathlib use:** yes. All work in
  `HexBerlekampZassenhausMathlib/Basic.lean`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic`
* `lake build HexBerlekampZassenhausMathlib`
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhausMathlib/Basic.lean`
  shows no new entries on the diff against `origin/main`.

## Out of scope

* Propagating the abstraction into downstream consumers of
  `representsIntegerFactorAtLift_primitive` (lines 10831 / 12524 on
  `origin/main`) — separate follow-up issues.
* The recovery-candidate `_of_bound` siblings (#4882 / #4883).
* The monic-side propagation (#4890).
* The recursive workhorse
  `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  abstract-bound sibling — separate follow-up after #4890.
* Gap 1 (`hcore_monic`) — tracked by #4880.
* BHKS Theorem 5.2 / directive #2567.
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
