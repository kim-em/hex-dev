## Current state

The A2 abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` reached the
public substrate
`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
via #4971 (claimed; substrate `_of_bound` sibling immediately above
the wrapper at line 3389). That sibling takes the abstract bound block

```lean
(B' : Nat)
(hcore_lc_le : (Hex.DensePoly.leadingCoeff
    (Hex.normalizeForFactor f).squareFreeCore).natAbs ≤ B')
(hvalid : ∀ g : Hex.ZPoly, g ∣ (Hex.normalizeForFactor f).squareFreeCore →
    ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision :
  2 * B' <
    (Hex.choosePrimeData (Hex.normalizeForFactor f).squareFreeCore).p ^
      Hex.precisionForCoeffBound
        (Hex.ZPoly.defaultFactorCoeffBound f)
        (Hex.choosePrimeData (Hex.normalizeForFactor f).squareFreeCore).p)
```

in place of the existing
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound
  (Hex.normalizeForFactor f).squareFreeCore < ...`.

This issue extends the cascade one layer up into both immediate
in-tree consumers of that substrate in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean`:

* `reassemblyExpansionComplete_exhaustive_of_ne_zero` at line 3644
  (calls the substrate at line 3693).
* `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`
  at line 3804 (calls the substrate at line 3855).

Both currently take the existing inner-form `hprecision` and consume
it at exactly the single forward call to
`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
in their bodies. The rest of each body — `hdeg`/`hcore_ne`/`hcore_record`
derivations, the per-factor monicness or `0 < leadingCoeff`
substrate, the `exhaustiveCoreFactorsWithBound_product` /
`exhaustiveCoreFactorsWithBound_degree_pos` calls, the fuel bound,
and the final assembler call to
`reassemblyExpansionComplete_of_irreducible_squarefree_cover` (monic)
or `..._of_pos_lc` (primitive variant) — is precision-agnostic and
carries over verbatim.

Both consumers feed into the public umbrella
`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` at
line 3972, which in its current shape (line 3999) calls the monic
variant. The monic variant's `_of_bound` lifting prepares the umbrella's
own `_of_bound` sibling. The `_of_primitive_pos_lc_core` variant
currently has no in-tree consumer but is the natural pairing-partner
to lift in lockstep (matching the precedent at #4936 for the scaled
`_some_factor_associated_*` wrappers).

## Hypothesis shape

Mirror the established `_of_bound` shape from #4907 / #4920 / #4922 /
#4928 / #4930 / #4932 / #4933 / #4935 / #4936 / #4938 / #4944 /
#4947 / #4963 / #4970 / #4971. Replace the existing
`hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound
  (Hex.normalizeForFactor f).squareFreeCore < ...`
binder with the four-parameter block above, placed at the same
syntactic position as the original `hprecision` binder.

depends-on: #4971

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, extending the cascade
  one layer up into the two `reassemblyExpansionComplete_exhaustive_*`
  consumers in `IntReductionMod.lean`.
* **Natural strategy uses Mathlib?** No — pure forward edit; both
  consumers are already in `IntReductionMod.lean` and their bodies
  consume the upstream substrate verbatim.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4971.

## Deliverables

In `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, add two
`_of_bound` siblings and rewire the two existing originals as thin
wrappers.

1. **`reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`**
   — theorem with the same signature as the original at line 3644
   except `hprecision` is replaced by the four-parameter block above.
   Per the convention established by the prior `_of_bound` siblings
   at adjacent layers (#4971 in particular), insert `_of_bound`
   **before** the thin wrapper.

   Proof body: same as the original (lines 3665–3771) with the single
   call to
   `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
   at line 3693 replaced by a call to its `_of_bound` sibling
   (the #4971 product), threading `B'`, `hcore_lc_le`, `hvalid`,
   `hprecision` through verbatim.

2. **Rewire the original
   `reassemblyExpansionComplete_exhaustive_of_ne_zero`** at line 3644
   as a thin wrapper instantiating
   `B' := Hex.ZPoly.defaultFactorCoeffBound
     (Hex.normalizeForFactor f).squareFreeCore`. Discharge:

   * `hcore_lc_le` via `defaultFactorCoeffBound_valid` +
     `leadingCoeff_eq_coeff_last` (matching #4971 / #4944 / #4938
     boilerplate). `hcore_ne` is in scope identically to the
     original's body.
   * `hvalid` via the universal
     `defaultFactorCoeffBound_valid (Hex.normalizeForFactor f).squareFreeCore hcore_ne`.
   * `hprecision` passes through unchanged at the new tighter form.

3. **`reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core_of_bound`**
   — theorem with the same signature as the original at line 3804
   except `hprecision` is replaced by the four-parameter block above.
   Inserted **before** its thin wrapper.

   Proof body: same as the original (lines 3829–3948) with the single
   call to
   `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`
   at line 3855 replaced by a call to its `_of_bound` sibling,
   threading the four-tuple verbatim.

4. **Rewire the original
   `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_primitive_pos_lc_core`**
   at line 3804 as a thin wrapper, with the same `B'` instantiation
   and `hcore_lc_le` / `hvalid` discharge as deliverable 2.
   `hcore_ne` is derivable from `hcore_lc_pos` exactly as in the
   original body (lines 3833–3837).

## Context

The pairing-partner / inner-B / outer-B distinction does **not**
apply at this layer: both consumers inherit the hybrid `hprecision`
shape (squareFreeCore-bound on LHS, f-bound in the precision
exponent) directly from the #4971 substrate. The abstract bound `B'`
replaces only the LHS factor; the RHS exponent stays as
`precisionForCoeffBound (defaultFactorCoeffBound f)` because the
surrounding executable surface fixes the modulus to that f-bound
choice.

Once both `_of_bound` siblings land:

* Combined with #4970 (the umbrella aux `_of_bound`, blocked on
  #4947), the umbrella itself
  (`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData` at
  line 3972) becomes ready for its own `_of_bound` sibling: the body
  at lines 3997–4001 has exactly two consumers of `hprecision` —
  the inner `reassemblyExpansionComplete_exhaustive_of_ne_zero` call
  at line 3999 (lifted here) and the forwarded `hprecision` argument
  to the aux at line 4001 (lifted by #4970).
* The `_of_primitive_pos_lc_core` variant becomes available as a
  drop-in once the #4880 directive-blocked refactor of helpers 2/3/4
  lands and the umbrella's monic-to-primitive variant is filed.

## Verification

1. `lake build HexBerlekampZassenhausMathlib.IntReductionMod` — same
   error/warning set as `origin/main` (the pre-existing whnf-timeout
   pile in `Basic.lean` is unaffected; the new theorems are in
   `IntReductionMod.lean` only).
2. `lake build HexBerlekampZassenhausMathlib` — same as origin/main.
3. `python3 scripts/check_dag.py` — exit 0.
4. `git diff --check` — clean.
5. `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
    | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` — empty.
6. The two originals' signatures are unchanged; the single in-tree
   consumer of the monic original (the umbrella at line 3999)
   continues to type-check via the rewired thin wrapper without
   further edits.
