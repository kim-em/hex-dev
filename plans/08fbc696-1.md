## Current state

`HexGramSchmidt/Basic.lean` Phase 6 docstring sweep has documented batches 1
and 2 (the size-reduction / `reduceAgainstBasis` reconstruction and
orthogonal `basisRows` assembly clusters, per recent merged PRs). The next
undocumented region is the left-linearity cluster (lines ~881-1003), which
records how `dot`, `projectionCoeff`, `subtractProjection`, and
`reduceAgainstBasis` behave under vector addition and scalar scaling of their
left argument. These ten private theorems carry no docstrings:

- `dot_add_left` (L881)
- `foldl_dot_smul_left` (L889)
- `dot_smul_left` (L907)
- `projectionCoeff_add_left` (L914)
- `projectionCoeff_smul_left` (L924)
- `subtractProjection_add_left` (L933)
- `subtractProjection_smul_left` (L955)
- `reduceAgainstBasis_add_left` (L974)
- `reduceAgainstBasis_smul_left` (L990)
- `reduceAgainstBasis_append` (L1003)

## Deliverables

1. Add a one-sentence, backtick-led docstring to each of the ten declarations
   above in `HexGramSchmidt/Basic.lean`, naming the noun each lemma
   characterises (left-additivity / left-homogeneity of the dot product, the
   projection coefficient, the projection subtraction, and the basis
   reduction; the append-decomposition of `reduceAgainstBasis`).

This is a **doc-only** change: do not touch any theorem statement, signature,
proof body, or declaration order. Additive docstring lines only.

## Library placement

`HexGramSchmidt/Basic.lean` (library `hex-gram-schmidt`).

## Context

- Phase 6 exit criteria and docstring-coverage rule:
  [PLAN/Phase6.md](../PLAN/Phase6.md) and
  [SPEC/design-principles.md](../SPEC/design-principles.md) (public
  declarations and non-obvious private helpers carry docstrings).
- Style: short verb-noun docstrings, no "research completed" notes, no
  meta-commentary, no em-dashes, no banned vocabulary (see `.claude/CLAUDE.md`).
- Stop before `basisRows_get!_dot_eq_zero_of_list` (L1010); the orthogonality
  consequences are a separate later batch.

## Verification

- `lake build HexGramSchmidt.Basic` passes.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- `git diff --numstat -- HexGramSchmidt/Basic.lean` shows additive-only edits
  (insertions, zero deletions).
- Added-line scan finds no banned tokens or em-dashes.
