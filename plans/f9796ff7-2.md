## Current state

Review #5031 audited the post-#4991 Monic-route helper-extraction
cascade against three invariants
(https://github.com/kim-em/hex/issues/5031#issuecomment-4479617158).
It found exactly one deviation against invariant 1: the inline 9-line
`by_contra`/`cases` proof at `HexBerlekampZassenhausMathlib/Basic.lean:5338`
in `monicModularImage_modP_eq_of_monic`, where
`zpoly_size_pos_of_monic` is shape-applicable but currently
forward-referenced (helper at `Basic.lean:6720`, call site at 5338).
A separate feature issue tracks the hoist + rewire.

Review #5031's grep patterns covered the obvious call-site shapes:

```
have .*: 0 < .*\.size
have .*_ne(_zero)?\s*:.*≠ 0
show .*leadingCoeff.*= 1
0 < Hex.DensePoly.leadingCoeff
monic_primitive_sign_normalized_of_monic .*\.2\.[12]
:= zpoly_ne_zero_of_pos_lc \(f :=
leadingCoeff.*natAbs.*≤.*defaultFactorCoeffBound
Hex.ZPoly.Primitive .Hex.normalizeForFactor
```

These miss inline proofs of the same five cluster propositions
(`0 < ?.size`, `? ≠ 0`, `0 < leadingCoeff ?` / `leadingCoeff ? = 1`,
`content ? = 1`, `normalizeFactorSign ? = ?`) that derive from a
`Monic` (or `0 < leadingCoeff`) hypothesis in scope but are phrased
in alternative idioms:

- `Nat.pos_of_ne_zero hne` chains for `0 < ?.size` from `? ≠ 0`,
  where the `? ≠ 0` itself comes from `Monic` (via `.1` of
  `monic_primitive_sign_normalized_of_monic` or unfolded leadingCoeff).
- `exact ?.symm.trans` / `omega` chains that introduce a `Monic`
  hypothesis purely to discharge a single cluster-shape goal.
- Anonymous-constructor literals (e.g., `⟨hcore_monic, …, …⟩`)
  passed to lemmas whose argument slot is one of the cluster shapes
  — these are not necessarily wrong, but per-site review may find
  rewires through the named helpers.
- Forward-reference-blocked sites (mirror of the line-5338 case
  surfaced by #5031): call sites *above* one of the cluster helpers
  that would route through it after a hoist.

## Deliverables

Post a single audit comment on this issue covering:

1. **Forward-reference scan.** For each of the seven cluster helpers
   (`zpoly_size_pos_of_monic`, `zpoly_ne_zero_of_monic`,
   `zpoly_lc_pos_of_monic`, `zpoly_primitive_of_monic`,
   `zpoly_normalize_factor_sign_of_monic`, `zpoly_ne_zero_of_pos_lc`,
   `defaultFactorCoeffBound_leadingCoeff_natAbs_le`), list every
   call site in `HexBerlekampZassenhausMathlib/Basic.lean` and
   `HexBerlekampZassenhausMathlib/IntReductionMod.lean` that sits
   *above* the helper's definition line and has a `Monic` (or
   `0 < leadingCoeff`) hypothesis in scope. Report which inline
   discharge the site currently uses and whether a hoist would
   enable a one-line rewire. The line-5338 case (already in flight)
   should appear in this list as a control row — confirming it is
   the only such site, or surfacing peers, is the audit's primary
   deliverable.

2. **Alternative-idiom scan.** Using the four greps below, list
   every site that derives one of the cluster propositions inline
   despite having a `Monic` or `0 < leadingCoeff` hypothesis in
   scope, and is not already covered by review #5031's eight
   patterns. Reuse the standard "site / helper / one-line rewire"
   table from #5031. For each site, mark whether a rewire is in
   scope today (helper already above the site) or requires the
   feature-issue's hoist (or a further hoist).

   ```
   grep -nE "Nat\.pos_of_ne_zero" HexBerlekampZassenhausMathlib/*.lean
   grep -nE "Nat\.eq_zero_or_pos.*\.size" HexBerlekampZassenhausMathlib/*.lean
   grep -nE "\(monic_primitive_sign_normalized_of_monic.*\)\.1" HexBerlekampZassenhausMathlib/*.lean
   grep -nE "leadingCoeff .*= \(1 : Int\)" HexBerlekampZassenhausMathlib/*.lean
   ```

3. **Draft follow-up issue bodies** for every confirmed concrete
   deviation, in the same minimal "hoist (if needed) + per-site
   rewire" shape as the line-5338 feature issue. If the audit
   finds no deviations beyond the known line-5338 case, say so
   explicitly and close the issue — that itself is a useful
   structural signal that the cluster's coverage is now exhaustive
   under these wider patterns.

## Context

- Read-only audit. No code changes. Verify clean
  `git diff --check` and empty `git diff origin/main` before posting
  the comment.
- Out of scope: any rewrite of review #5031's eight patterns (those
  passed); any audit of helpers outside the seven listed above
  (e.g., `monic_primitive_sign_normalized_of_monic` itself, or
  `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`); any
  cross-cluster framing or naming work.
- Cluster track lineage (for context, not for audit): PRs #4991,
  #5001, #5009, #5013, #5022, #5026, #5028, #5029, #5033. Review:
  #5031. Forward-reference feature: see the most recent
  `agent-plan / feature` issue mentioning `Basic.lean:5338` /
  hoist.

## Verification

- `python3 scripts/check_dag.py`: exit 0.
- `git diff --check`: clean.
- `git diff origin/main`: empty (read-only audit).
- Audit comment posted on this issue with verbatim grep commands
  and observed-vs-expected counts per scan; deviations enumerated
  per cluster helper; per-deviation draft follow-up issue bodies
  included inline.
