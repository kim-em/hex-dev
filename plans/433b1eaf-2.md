## Current state

`HexGF2/Field.lean` is in Phase 6 (proof polishing). The four
`private theorem`s relating `GF2Poly` reduction-mod to the ring
operations and to divisibility are undocumented. They encode a
non-trivial specification (mod is an additive/multiplicative
homomorphism, and `% f = 0 ↔ f ∣ ·`), so the Phase 6 docstring rule
applies.

## Deliverables

Add a one-sentence docstring to each of the following `private theorem`s
in `HexGF2/Field.lean` (lines ~716–840), stating what it proves and why
the field construction relies on it:

- `mod_add_mod_eq_mod_add`
- `mod_mul_mod_eq_mod_mul`
- `mod_eq_zero_of_dvd`
- `dvd_of_mod_eq_zero`

Doc-only: do not touch signatures, statements, proofs, declaration
order, or `private` modifiers.

## Context

- `SPEC/design-principles.md` §Docstrings sets the coverage rule.
- `PLAN/Phase6.md`: docstring coverage is a Phase 6 exit criterion.
- Follows the established Phase 6 doc-batch pattern (e.g. the merged
  `HexGF2/Multiply.lean` `xorBoolList` batch).

## Verification

- `lake build HexGF2.Field` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean; `git diff --numstat` shows additions only.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
