## Current state

Six PRs landed today form a tightly-coupled monic-API cluster that
feeds the recursive coverage proof in #4301:

- PR #4358 (issue #4355) added `monic_primitive_sign_normalized_of_monic`
  in `HexBerlekampZassenhausMathlib/Basic.lean` L3084 (+116 lines).
- PR #4379 (issue #4373) added `monic_of_congr_mul_monic_monic` in
  `HexHensel/QuadraticMultifactor.lean` L363 (+197 lines), the Monic
  cofactor lemma deriving monicness of `h` from `ZPoly.congr (g*h) f m`
  under coefficient bounds.
- PR #4386 (issue #4374) added `monic_reduceModPow_of_congr_mul_monic_monic`,
  `henselLiftQuadratic_g_monic`, `henselLiftQuadratic_h_monic`, and the
  umbrella `Hex.ZPoly.multifactorLiftQuadratic_each_monic` in the same
  file (+385/-150 lines).
- PR #4390 (issue #4376) added `henselLiftData_liftedFactor_monic` in
  `HexBerlekampZassenhausMathlib/Basic.lean` L3142 (+71 lines), the
  thin Mathlib-facing umbrella over the executable surface.

The Mathlib-bridge consumer side has two open follow-ups that will lean
on this cluster:

- #4377 (claimed): `representsIntegerFactorAtLift_monic` consumes
  `∀ i, Monic (liftedFactor d i)`.
- #4393 (claimed): `QuadraticMultifactorLiftInvariant_of_choosePrimeData`
  is the boundary-facts bridge into the umbrella.
- #4400 (blocked on #4393): the convenience composition at the Mathlib
  bridge.

This cluster spans three files and ~700 added lines of monic API. A
surface audit *before* the consumer PRs land is cheap: it can still
shape the hypothesis blocks of the new theorems without a wave of
downstream rewrites.

## Deliverables

A review comment posted on this issue answering the four questions
below; no code changes unless a surface defect is found whose fix is
strictly local. Open follow-up issues for any larger findings.

1. **Hypothesis-block consistency across the cluster.** Compare the
   hypothesis lists of:
   - `monic_of_congr_mul_monic_monic`
     (`HexHensel/QuadraticMultifactor.lean` L363)
   - `monic_reduceModPow_of_congr_mul_monic_monic`
     (`HexHensel/QuadraticMultifactor.lean` L466)
   - `henselLiftQuadratic_g_monic`, `henselLiftQuadratic_h_monic`
     (`HexHensel/QuadraticMultifactor.lean` L490, L516)
   - `Hex.ZPoly.multifactorLiftQuadratic_each_monic` (umbrella)
   - `HexBerlekampZassenhausMathlib.henselLiftData_liftedFactor_monic`
     (`HexBerlekampZassenhausMathlib/Basic.lean` L3142)
   - `HexBerlekampZassenhausMathlib.monic_primitive_sign_normalized_of_monic`
     (`HexBerlekampZassenhausMathlib/Basic.lean` L3084)

   For each pair where two theorems share a conceptual argument
   (e.g., `Monic core`, `1 < p`, `1 ≤ B`, coefficient bounds), check
   that the argument is named identically and appears in the same
   relative position. Flag avoidable drift; trivial-rename fixes can
   land in this PR.

2. **Umbrella delegation is thin.** Verify that
   `henselLiftData_liftedFactor_monic` is genuinely a one-line
   delegation to `multifactorLiftQuadratic_each_monic` (currently
   L3142 of Basic.lean appears to be a single `exact …` step) and
   that no proof content has accidentally migrated into the Mathlib
   bridge layer that belongs in the executable layer.

3. **Doc-comment cross-references.** Each theorem's doc-comment
   should name its immediate consumer and (where applicable) its
   immediate prerequisite. Verify both directions:
   - `monic_of_congr_mul_monic_monic` → consumed by
     `monic_reduceModPow_of_congr_mul_monic_monic` /
     `henselLiftQuadratic_h_monic`.
   - `henselLiftQuadratic_{g,h}_monic` → consumed by
     `multifactorLiftQuadratic_each_monic` (probably via the
     `QuadraticLiftLoopInvariant`).
   - `multifactorLiftQuadratic_each_monic` → consumed by
     `henselLiftData_liftedFactor_monic`.
   - `henselLiftData_liftedFactor_monic` → consumed by the upcoming
     `representsIntegerFactorAtLift_monic` (#4377) and the upcoming
     convenience wrapper (#4400).
   - `monic_primitive_sign_normalized_of_monic` → consumed by
     `recombinationCandidate_eq_factor_of_recovery`.

   Where a consumer is still open (issue, not landed PR), the
   doc-comment may forward-reference the issue number.

4. **No Mathlib imports in `HexHensel/QuadraticMultifactor.lean`.**
   The cluster added ~580 lines to that file. Spot-check the diff
   does not introduce a Mathlib import (direct or transitive via a
   new neighbour import).

## Context

Read:

- The six PRs above (#4358, #4373→#4379, #4374→#4386, #4376→#4390).
- `HexHensel/QuadraticMultifactor.lean` L363–540 (the new
  `monic_…` cluster and the lift-step monic theorems).
- `HexBerlekampZassenhausMathlib/Basic.lean` L3084–3160 (the two
  Mathlib-facing wrappers).
- The open consumer issues #4377, #4393, and #4400.

## Verification

- `lake build HexHensel.QuadraticMultifactor` (any local fix lands
  here cleanly)
- `lake build HexBerlekampZassenhausMathlib.Basic`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b' HexHensel/QuadraticMultifactor.lean HexBerlekampZassenhausMathlib/Basic.lean`

## Out of scope

- Strengthening the umbrella to absorb the `choosePrimeData` boundary
  facts (covered by #4393 plus #4400).
- Discharging `RepresentsIntegerFactorAtLift_monic` (#4377).
- The recursive coverage proof (#4301).
- Editing `SPEC/`, top-level `PLAN.md`, top-level `AGENTS.md`, or
  `.claude/CLAUDE.md`.
