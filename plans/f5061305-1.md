## Current state

The last closed summarize issue is #4958 (forty-first Phase 4/5
checkpoint), which closed at 2026-05-18T07:58:42Z. Since then 10
PRs have merged, matching the planner summarize trigger (10+ PRs
merged since the last summarize closed).

The merge window is a single thematic block with two sub-threads:

1. **A2 abstract-bound (`_of_bound`) propagation** continuing
   through the upper layers of `HexBerlekampZassenhausMathlib/Basic.lean`
   and crossing into `HexBerlekampZassenhausMathlib/IntReductionMod.lean`,
   completing the slow-path exhaustive-arm cascade up to the umbrella
   aux and the `reassemblyExpansionComplete_exhaustive_*` discharger.
   With this wave landed, the only remaining unclaimed `_of_bound`
   sibling is the public umbrella itself (#4976, now unblocked).

2. **`hcomplete` premise drops** in two more pre-existing slow-path
   arm umbrellas (quadratic, singleton), bringing the
   `IntReductionMod` branch-entry umbrellas almost fully to the
   `hcomplete`-free shape (only the slow-path exhaustive umbrella
   still threads `hcomplete` implicitly via its
   `reassemblyExpansionComplete_exhaustive_of_ne_zero` call).

No new conformance, oracle, or benchmarking work landed.

Recent landings (chronological order):

* **2026-05-18T07:59:01Z** #4962 — BZMathlib: abstract-bound
  siblings of scaled `_some_factor_associated_` public wrappers
  (primitive + primitive_pos_lc_core).
* **2026-05-18T08:33:12Z** #4965 — BZMathlib: abstract-bound
  sibling of `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`.
* **2026-05-18T08:35:26Z** #4966 — BZMathlib: drop `hcomplete`
  premise from two quadratic-arm umbrellas (discharge internally
  via #4747).
* **2026-05-18T08:59:53Z** #4968 — BZMathlib: drop `hcomplete`
  premise from two small-mod singleton-arm umbrellas (discharge
  internally via #4956 `_of_pos_lc`).
* **2026-05-18T09:09:22Z** #4969 — BZMathlib: abstract-bound
  sibling of `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`.
* **2026-05-18T09:39:22Z** #4972 — BZMathlib: abstract-bound
  sibling of `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
  (inner-B pairing wrapper).
* **2026-05-18T09:39:41Z** #4973 — BZMathlib: abstract-bound
  sibling of `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
  (public bridge).
* **2026-05-18T09:53:22Z** #4975 — BZMathlib: abstract-bound
  sibling of `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`.
* **2026-05-18T10:21:40Z** #4978 — BZMathlib: abstract-bound
  sibling of `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
  (umbrella aux `_of_bound`).
* **2026-05-18T10:35:09Z** #4979 — BZMathlib: abstract-bound
  siblings of `reassemblyExpansionComplete_exhaustive_of_ne_zero`
  (monic + primitive_pos_lc_core).

## Deliverables

Produce the **forty-second Phase 4/5 checkpoint summary**. The
summary issue (this one) should include:

1. **Header** with the PR count (10 new PRs as of issue creation;
   recompute at execution time excluding any PRs that merge
   between issue creation and the summarize agent picking it up),
   the closing-of-#4958 timestamp (2026-05-18T07:58:42Z), and the
   `coordination orient` snapshot at the time the summarize is
   opened (open directives, unclaimed/claimed/blocked buckets,
   open PRs).

2. **Per-theme PR groupings** clustering the merged PRs into
   coherent themes. Expected themes based on the commit log:

   * **BZMathlib A2 `_of_bound` propagation — public-bridge layer
     and into `IntReductionMod`**: #4962 (scaled
     `_some_factor_associated_` public wrappers), #4965
     (`_coverage_of_henselSubsetCorrespondence`), #4969
     (`_factor_zpolyIrreducible_of_henselSubsetCorrespondence`),
     #4972 (`factorWithBound_*` inner-B pairing wrapper), #4973
     (public-bridge `factor_exhaustive_branch_entry_core_zpolyIrreducible_*`),
     #4975 (`exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData`,
     first `IntReductionMod` `_of_bound`), #4978 (umbrella aux
     `_of_bound`), #4979 (paired
     `reassemblyExpansionComplete_exhaustive_of_ne_zero_of_bound`
     monic + primitive_pos_lc_core). This sub-thread completed the
     A2 ladder from the public-bridge layer of `Basic.lean` through
     to the discharger and umbrella aux in `IntReductionMod.lean`;
     the public umbrella `_of_bound` (#4976) is the only remaining
     A2 capstone in this cascade and is now unblocked.

   * **`hcomplete` premise drops on slow-path arm umbrellas**:
     #4966 (quadratic-arm umbrellas `factor_quadratic_branch_entry_irreducible_of_quadraticRoots`
     and `factor_slow_quadratic_branch_entry_irreducible_of_choosePrimeData`,
     discharger #4747), #4968 (singleton-arm umbrellas
     `factor_small_mod_singleton_branch_entry_irreducible_of_choosePrimeData`
     and `factor_small_mod_singleton_branch_entry_irreducible`,
     discharger #4956 `_of_pos_lc`). With these, the constant /
     quadratic / singleton arm umbrellas are all `hcomplete`-free;
     only the slow-path exhaustive umbrella still carries
     `hcomplete` through its `reassemblyExpansionComplete_exhaustive_of_ne_zero`
     internal call, which is the natural follow-on to the #4976
     `_of_bound` capstone.

3. **In-flight at issue creation time**:

   * PR #4917 (closing #4902, abstract-bound siblings of
     `exists_representingSubset_of_mem_normalizedFactors_recombinationCandidate`
     monic + primitive unscaled) is `repair-claimed` in
     `CONFLICTS` state. This PR blocks a long chain of monic +
     primitive_pos_lc_core `_of_bound` substrate siblings in
     `Basic.lean` (#4918, #4919, #4924, #4925, #4927, #4929,
     #4931, #4934, #4951).
   * #4976 (public umbrella `_of_bound` for
     `factor_exhaustive_branch_entry_irreducible_of_choosePrimeData`,
     `IntReductionMod.lean`) became unblocked when #4979 landed
     and is in the queue as the next-up `_of_bound` capstone.
   * Any `directive` issues that closed during the window (none
     expected — #2637, #2567, #2564 should all still be open).

4. **HO-1 critical-path commentary**: the BZ Mathlib-free chain
   (capstone #4170, sub-capstone #4825) is still gated on #4680
   (analytic obligation composition) and #2567 (directive).
   The `_of_bound` cascade's role: it lowers the precision
   threshold required by the `recombinationSearchModAux_some_factor_associated_*`
   / `exhaustiveCoreFactorsWithBound_coverage_*` chain, which is
   the primary downstream consumer once the A2 / Mignotte recovery
   wiring is complete. With the umbrella aux `_of_bound` (#4978)
   and the discharger `_of_bound` (#4979) landed, the public
   umbrella `_of_bound` capstone (#4976) is the final step of the
   A2 cascade for the slow-path exhaustive arm.

5. **`#4880` umbrella drop posture**: helpers 1 (#4945) and 5
   (#4953) substrate remain closed; helpers 2/3/4 remain
   architecturally infeasible per the #4940 audit. The new
   `_of_bound` cascade does **not** alter this posture — Gap 1
   (`hcore_monic`) at the umbrella surface is independent of Gap 3
   (`hprecision`, which the `_of_bound` cascade addresses). No
   further substrate sub-issues should be filed beneath #4880;
   planner cycles should treat it as directive-level blocked.

## Context

Per the project's CLAUDE.md, this summary feeds Phase 4/5
checkpoint tracking. It is a journal-style review of the merge
window, not a roadmap update — `SPEC/` and `PLAN.md` remain the
single source of truth on the current design state. Don't
duplicate planning content into the summary; instead, link out to
the relevant open issues and SPECs.

Files touched: this should be a single comment posted on the
created summarize issue (or the issue body itself, depending on
the writer's preference); no source-tree files are edited.

## Verification

* The summary comment / issue body must list exactly the PRs in
  the merge window (recompute at execution time):

  ```
  gh pr list --state merged --limit 30 \
      --json mergedAt,number,title \
      --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
    | awk '$1 > "2026-05-18T07:58:42Z"' \
    | awk '$1 <= "<creation-timestamp>"'
  ```

  Headline estimate at issue creation: 10 PRs. If any PRs merged
  between this issue's creation and the summarize agent picking it
  up, those land in the *next* summary, not this one — pin the
  upper bound to this issue's creation timestamp.

* The summary's "directive issues that closed during the window"
  bullet must match the actual delta from
  `gh issue list --label directive --state closed`.

* The summary's "in-flight PRs" bullet must reflect the
  `coordination orient` snapshot at the time the summarize agent
  runs, not at issue creation time.

* No edits to `SPEC/`, `PLAN.md`, `AGENTS.md`, or `.claude/CLAUDE.md`.

## Out of scope

* Re-summarizing PRs covered by #4958 or earlier checkpoints.
* Updating `libraries.yml` (no `done_through` bumps expected in
  the merge window per `git log`; reconfirm at execution time).
* Filing new substrate sub-issues beneath #4880 (per #4940 audit,
  the umbrella drop is blocked at directive level).
* Editing roadmap / SPEC / CLAUDE.md.
