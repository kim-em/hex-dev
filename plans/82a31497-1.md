## Current state

PR #5036 (closes #5035) extracted

```
private theorem zpoly_dvd_trans
    {a b c : Hex.ZPoly} (hab : a ∣ b) (hbc : b ∣ c) : a ∣ c := by
  obtain ⟨q, hq⟩ := hab
  obtain ⟨v, hv⟩ := hbc
  refine ⟨q * v, ?_⟩
  rw [hv, hq]
  exact Hex.DensePoly.mul_assoc_poly (S := Int) _ _ _
```

into `HexBerlekampZassenhausMathlib/Basic.lean:6779` (private helper
block, immediately after `zpoly_monic_mul`). The PR rewired three sites
at `Basic.lean:9805`, `9978`, and `11382` onto it, each collapsing a
6-line inline `obtain`/`refine`/`rw`/`exact` block to a one-line
`have := zpoly_dvd_trans …` invocation.

A grep for the inline pattern shows **six remaining sites** in
`Basic.lean` that derive `a ∣ c` from `a ∣ b` and `b ∣ c` inline,
all below line 6779 (so the helper is in scope at every site — no
hoist required):

| # | Lines | Enclosing theorem | Inputs | Output |
|---|---|---|---|---|
| 1 | 8006–8011 | `representsIntegerFactorAtLift_monic_of_bound` | `hfactor_dvd_target`, `htarget_dvd_core` | `hfactor_dvd_core : factor ∣ core` |
| 2 | 8083–8088 | `representsIntegerFactorAtLift_monic` | `hfactor_dvd_target`, `htarget_dvd_core` | `hfactor_dvd_core : factor ∣ core` |
| 3 | 8299–8304 | `representsIntegerFactorAtLift_primitive_of_bound` | `hfactor_dvd_target`, `htarget_dvd_core` | `hfactor_dvd_core : factor ∣ core` |
| 4 | 8382–8387 | `representsIntegerFactorAtLift_primitive` | `hfactor_dvd_target`, `htarget_dvd_core` | `hfactor_dvd_core : factor ∣ core` |
| 5 | 12374–12378 | (Case B of `recombinationCandidate_eq_factor_…`-cluster theorem near 12340) | `hfactor_dvd_target`, `htarget_dvd_core` | `hfactor_dvd_core : factor ∣ core` |
| 6 | 12423–12427 | (Subcase B1 of same theorem) | `hg_dvd_cand`, `hcand_dvd_target` | `hg_dvd_target : g ∣ target` |

Sites 1–4 are the canonical 6-line shape

```
have hfactor_dvd_core : factor ∣ core := by
  obtain ⟨u, hu⟩ := hfactor_dvd_target
  obtain ⟨v, hv⟩ := htarget_dvd_core
  refine ⟨u * v, ?_⟩
  rw [hv, hu]
  exact Hex.DensePoly.mul_assoc_poly (S := Int) _ _ _
```

Sites 5 and 6 are 5-line compressed variants that fold the final
`mul_assoc_poly` step into the `rw`:

```
have hfactor_dvd_core : factor ∣ core := by
  obtain ⟨u, hu⟩ := hfactor_dvd_target
  obtain ⟨v, hv⟩ := htarget_dvd_core
  refine ⟨u * v, ?_⟩
  rw [hv, hu, Hex.DensePoly.mul_assoc_poly (S := Int)]
```

Both shapes are pointwise replaceable by

```
have hfactor_dvd_core : factor ∣ core :=
  zpoly_dvd_trans hfactor_dvd_target htarget_dvd_core
```

The IntReductionMod.lean side has been checked and contains no peer
sites — the only `Hex.DensePoly.mul_assoc_poly` use there
(`IntReductionMod.lean:2567-2569`) is a commute-then-associate chain,
not a divisor-transitivity derivation, and is out of scope.

## Deliverables

1. Rewire all six sites in
   `HexBerlekampZassenhausMathlib/Basic.lean` to the one-line
   `zpoly_dvd_trans hX_dvd_Y hY_dvd_Z` form. Preserve the `have`
   name and its expected type (`factor ∣ core` at sites 1–5;
   `g ∣ target` at site 6).
2. Verify the helper invocation is the literal `zpoly_dvd_trans`
   form — do not inline its body or expand to `Dvd.Dvd.trans`
   (Mathlib's `dvd_trans` does not see through `Hex.ZPoly`'s bespoke
   `Dvd` instance derived from the executable `Mul`; this is the
   semantic note documented in the `zpoly_dvd_trans` docstring at
   `Basic.lean:6775-6778`).
3. Do not introduce any new `sorry`, `axiom`, `native_decide`, `TODO`,
   or `FIXME`. Do not touch any line outside the six call-site
   bodies above (no docstring edits on the helper, no signature
   tweaks, no opportunistic refactors).

## Context

- Helper definition + docstring:
  `HexBerlekampZassenhausMathlib/Basic.lean:6775-6785`.
- Three already-rewired control sites (do not touch):
  `Basic.lean:9805`, `9978`, `11382`.
- Original extraction PR:
  https://github.com/kim-em/hex/pull/5036
- Originating issue: https://github.com/kim-em/hex/issues/5035.
- This issue is invariant 1 (call-site coverage) follow-up for the
  `zpoly_dvd_trans` helper, in the same shape as the post-#4991
  Monic-route cluster cleanup tracked by #5031 / #5039.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic` — reproduces the
  documented 16-error baseline (11 whnf timeouts + 1 `cases` failure
  + 4 kernel unknown constants) and 2 pre-existing `sorry` warnings,
  unchanged in type-set. Line offsets on errors after line 12427
  shift by exactly `-4 * N₆ + -3 * N₅` where `N₆` = number of
  6-line sites rewired and `N₅` = number of 5-line sites rewired
  (so a full sweep shifts by `4 * 4 + 3 * 2 = 22` lines).
- `lake build HexBerlekampZassenhausMathlib` — same 16-error
  baseline, no new errors elsewhere.
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` clean.
- Grep counts:
  - `obtain ⟨u, hu⟩ := hfactor_dvd_target` in `Basic.lean`:
    5 → 0 (sites 1, 2, 3, 4, 5).
  - `obtain ⟨r₁, hr₁⟩ := hg_dvd_cand` in `Basic.lean`:
    1 → 0 (site 6).
  - `zpoly_dvd_trans` refs in `Basic.lean`:
    4 → 10 (= 1 def + 1 docstring + 3 existing sites + 6 new sites).
- Diffstat expectation: `Basic.lean` only, roughly `+12 / -34`
  (six new two-line `have :=` invocations replacing four six-line
  blocks and two five-line blocks).
