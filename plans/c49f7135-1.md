## Current state

The A2 abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/Basic.lean` is reaching the
`coverAtMin_*` cover-at-min layer immediately above the per-T
cover-extraction theorems. The monic and primitive-unscaled
cover-at-min siblings are planned as #4927 (blocked on #4924) and
#4929 (blocked on #4925). The scaled primitive flavour is the
third and final cover-at-min `_of_bound` and is not yet planned.

The scaled-flavour consumer up from the cover-extraction layer is

* `coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
  at line 11648 of `HexBerlekampZassenhausMathlib/Basic.lean`.

It takes `hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core <
d.p ^ d.k` and consumes it at exactly **three** call sites:

* Lines 11676–11680: a call to
  `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
  threading `hprecision`.
* Lines 11685–11688: a call to `representsIntegerFactorAtLift_primitive`
  (to extract `Hex.ZPoly.Primitive f` for `f` returned from the
  previous step) threading `hprecision`.
* Lines 11694–11698: a call to
  `representingSubset_subset_of_dvd_scaledRecombinationCandidate_of_primitive_pos_lc_core`
  threading `hprecision`.

All three callees have `_of_bound` siblings:

* The first is `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
  — planned as #4926 (blocked on #4923).
* The second is `representsIntegerFactorAtLift_primitive_of_bound`
  at line 8188 (landed via #4899). Signature:
  `(B' : Nat) (hvalid : ∀ i, (factor.coeff i).natAbs ≤ B') (hcore_lc_le : (lc core).natAbs ≤ B') ... (hprecision : 2 * B' < d.p ^ d.k)`.
* The third is `representingSubset_subset_of_dvd_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
  at line 4042 (landed via #4908). Signature:
  `... (B' : Nat) (hvalid : ∀ i, (f.coeff i).natAbs ≤ B') (hprecision : 2 * B' < d.p ^ d.k) ...`.

The final repack at line 11699 is precision-agnostic and carries over
verbatim.

The `_of_bound` callees variously expect a single-factor bound on the
extracted `f` and a leading-coefficient bound on `core`. So the
`_of_bound` cover-at-min sibling needs:

* the per-normalised-factor bound (matching #4926's signature), so the
  inner call can pass it through, and
* `hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B'`, used
  by `representsIntegerFactorAtLift_primitive_of_bound`.

Plus a derivation of the single-factor bound for the extracted `f`
(`hvalid_f`) by identifying `f` as a normalised factor of
`toPolynomial (scaledRecombinationCandidate d T)` (via `f ∣
scaledRecombinationCandidate d T` and `hf_irr`). The derivation
mirrors #4929's monic / unscaled analogue.

The natural `_of_bound` shape:

```lean
(B' : Nat)
(hvalid : ∀ g : Hex.ZPoly,
  HexPolyZMathlib.toPolynomial g ∈
    UniqueFactorizationMonoid.normalizedFactors
      (HexPolyZMathlib.toPolynomial (scaledRecombinationCandidate core d T)) →
  ∀ i, (g.coeff i).natAbs ≤ B')
(hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

depends-on: #4926

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, cover-at-min layer,
  primitive non-monic scaled flavour.
* **Natural strategy uses Mathlib?** Yes — relies on
  `UniqueFactorizationMonoid.normalizedFactors` plumbing already in
  the file.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4926.

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add an abstract-bound
sibling immediately following
`coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
at line 11648, and rewire the existing original as a thin wrapper.

1. **`coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`**
   — same signature as the original at line 11648 except `hprecision :
   2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k` is replaced
   by the four-parameter block
   `(B' : Nat) (hvalid : ∀ g, toPolynomial g ∈ normalizedFactors (toPolynomial (scaledRecombinationCandidate core d T)) → ∀ i, (g.coeff i).natAbs ≤ B') (hcore_lc_le : (lc core).natAbs ≤ B') (hprecision : 2 * B' < d.p ^ d.k)`
   (full shape above). The proof body:
   - Threads `B'`, `hvalid`, `hcore_lc_le`, and `hprecision` to
     `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
     (from #4926) — this returns the cover witness `f` together with
     `hf_irr`, `hf_dvd_target`, `hf_dvd_cand`, `hSJ`, `hmin_in_S`,
     `hrep`. (Note: #4926's signature includes `hcore_lc_le` because
     its own internal call to the scaled per-normalised-factor bridge
     #4923 / #4907's `_of_bound` substrate needs it. Confirm against
     #4926's final signature when it lands and forward whichever bound
     parameters it surfaces.)
   - Derives the single-factor bound
     `hvalid_f : ∀ i, (f.coeff i).natAbs ≤ B'` from `hvalid` applied
     to `f` (using `f ∣ scaledRecombinationCandidate d T` from
     `hf_dvd_cand` plus `hf_irr` to identify `f` with an entry of
     `normalizedFactors (toPolynomial (scaledRecombinationCandidate
     d T))`). The exact bridge mirrors the derivation block used in
     #4927's monic analogue and #4929's primitive-unscaled analogue
     — see their "Deliverables 1" for the recipe; the scaled flavour
     differs only in the candidate (`scaledRecombinationCandidate`
     vs `recombinationCandidate`).
   - Threads `B'`, `hvalid_f`, `hcore_lc_le`, and `hprecision` to
     `representsIntegerFactorAtLift_primitive_of_bound` (line 8188)
     to extract `hf_primitive` and `_hf_lc_pos` from `hf_dvd_target`,
     `htarget_dvd_core`, and `hrep`.
   - Reconstructs `hf_content : Hex.ZPoly.content f = 1` and
     `hf_norm_sign : Hex.normalizeFactorSign f = f` from `hf_primitive`
     and `_hf_lc_pos` exactly as in the original (lines 11689–11693).
   - Threads `B'`, `hvalid_f`, and `hprecision` to
     `representingSubset_subset_of_dvd_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
     (the already-landed scaled-flavour `_of_bound` at line 4042) to
     obtain `hST : S ⊆ T`.
   - Repacks `⟨f, S, hf_irr, hf_dvd_target, hSJ, hmin_in_S, hrep,
     hST⟩` as before.

2. **Rewire** the original
   `coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
   at line 11648 as a thin wrapper that instantiates `B' :=
   defaultFactorCoeffBound core` and discharges:
   - `hcore_lc_le` via `defaultFactorCoeffBound_valid core hcore_ne
     core (dvd_refl core)` paired with `leadingCoeff_eq_coeff_last`
     (same shape as the wrapper rewiring landed in #4907 / #4920);
   - `hvalid` for an arbitrary normalised factor `g` by the divisor
     chain `g ∣ scaledRecombinationCandidate d T ∣ core` plus
     `defaultFactorCoeffBound_valid core hcore_ne g hg_dvd_core`. The
     chain step `scaledRecombinationCandidate d T ∣ core` uses
     `Hex.exactQuotient?_dvd_left hquot` (target divides core via the
     existing `htarget_dvd_core` hypothesis) — but be careful: the
     scaled-cascade gotcha from
     `progress/20260518T035027Z_184d5454.md` applies because
     `scaledRecombinationCandidate core d T` depends on `core`. Use
     `conv_lhs => rw [...]` rather than a plain `rw [hr₂]` for any
     rewrite of `core` inside an equality whose RHS still mentions
     `scaledRecombinationCandidate core d T`. (See the discharge block
     in `_of_bound`-wrapper #4907.)

The wrapper preserves the existing signature exactly. The two
downstream consumers (the `liftedFactorSubsetPartition_*` scaled-
flavour tier) continue to type-check unchanged.

## Context

This issue lands the third and final `coverAtMin_*` flavour `_of_bound`
sibling, completing the cover-at-min layer of the abstract-bound
propagation. Together with #4927 (monic) and #4929 (primitive
unscaled), the `coverAtMin_*` siblings unblock the next layer up:
`liftedFactorSubsetPartition_prefix_none_*_of_bound` (three flavours).

Once #4926 lands, this issue becomes unblocked. The wrapper rewiring
ensures all existing scaled-flavour consumers (lines 11843 and 11975 in
the current source tree — the `liftedFactorSubsetPartition_prefix_none`
scaled flavour) carry on type-checking against the unchanged signature.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — no new errors
  relative to `origin/main` (the pre-existing whnf timeouts and
  unknown-kernel-constant errors are environment-specific; see
  progress notes from this chain).
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing
  error set, no new errors.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
