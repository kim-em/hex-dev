## Current state

`HexLLL/Basic.lean` is in Phase 6 (proof polishing). The Phase 6 exit
criterion in `PLAN/Phase6.md` requires docstring coverage per
`SPEC/design-principles.md` (public declarations and non-obvious private
helpers). Inside `namespace LLLCore` there is a cluster of six private
rational-arithmetic helper theorems (lines ~423–595) that back the LLL
Lovász-condition step bounds and the norm-nonnegativity lemmas. All six
are currently undocumented:

- `ratMulSelfNonneg` (~423): a rational square is nonnegative.
- `foldlNonneg` (~426): a left-fold accumulating nonnegative summands
  onto a nonnegative accumulator stays nonnegative.
- `stepBound_arith` (~444): the Lovász-condition arithmetic rearrangement
  `(δ − η²)·N ≤ Np` from the size-reduced bound `μ² ≤ η²` and
  `δ·N ≤ Np + μ²·N`.
- `divStep_arith` (~460): from `d·N ≤ Np` with `0 < d`, derive
  `N ≤ (1/d)·Np`.
- `ratPow_nonneg` (~476): a nonnegative rational raised to a natural
  power is nonnegative.
- `ratPow_le_pow_of_one_le` (~584): for `1 ≤ α`, the power `α^i` is
  monotone in the exponent.

(The interleaved public lemmas `basisNormSq_nonneg` and the
`normSq_map_intCast` site are already documented — leave them untouched.)

## Deliverables

1. Add a one-sentence docstring to each of the six private helpers above,
   leading with the backticked declaration name and stating what the
   lemma asserts plus its role in the LLL rational-arithmetic / step-bound
   machinery.

## Context

This is a doc-only change. Do **not** touch signatures, statements,
proofs, names, attributes, `simp`/`grind` annotations, declaration order,
or imports. Match the existing Phase 6 docstring style already present in
this file (backtick-led, one sentence, present tense). No "research
completed" notes or meta-commentary (see `.claude/CLAUDE.md`). No banned
vocabulary ("smoke", "gate", "bridge") and no em-dashes on added lines.

## Verification

- `lake build HexLLL.Basic`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexLLL/Basic.lean` shows additions only (`N 0`).
- Confirm no banned vocabulary or em-dashes on added lines.
