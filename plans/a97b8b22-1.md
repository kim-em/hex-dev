## Current state

Decomposed from #4170 ("HO-1 residual: discharge executable factor
irreducibility bridge"), substrate item 1 ("Hensel substrate bundle"),
final remaining piece for the slow-path exhaustive arm.

The slow-path consumer wrapper
`factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
in `HexBerlekampZassenhausMathlib/Basic.lean` (around line 7464) and its
outer-bound sibling
`factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`
(around line 7590, landed by PR #4537) both consume

```lean
hpartition :
  LiftedFactorSubsetPartition core d Finset.univ core
```

as a hypothesis. Sibling substrate items have landed or are in flight:

- `HenselSubsetCorrespondenceHypotheses` constructor over the executable
  `Hex.choosePrimeData` / `Hex.henselLiftData` surface — landed by
  PR #4546 (#4543), including the outer-bound specialisation
  `henselSubsetCorrespondenceHypotheses_outerBound_of_choosePrimeData`
  (line 7733). The `exists_subset` / `unique_subset` fields are routed
  through a single localised `private` helper
  `henselSubsetCorrespondence_analytic_obligation` carrying the only
  new sorry.
- `hprim` / `hlc_map_ne` for the small-mod singleton branch — landed by
  PR #4545 (#4544).
- `hd_liftedFactor_natDegree_pos` discharge — in PR #4548 (#4547).
- `hd_liftedFactor_monic_of_choosePrimeData` (around line 3798) and
  `hd_liftedFactor_inj_of_choosePrimeData` (around line 4101) — already
  in place from earlier work.

The `LiftedFactorSubsetPartition` constructor is the last remaining
substrate piece for the slow-path arm of #4170. Once it lands, the
slow-path arm of `factor_irreducible_of_nonUnit` (#4170) has all its
substrate hypotheses dischargeable from `choosePrimeData` /
`henselLiftData` invariants modulo a single localised analytic
obligation; the residual gap for #4170 reduces to the BHKS fast
branch (directive #2567).

## Deliverables

1. In `HexBerlekampZassenhausMathlib/Basic.lean`, add a parametric
   constructor

   ```lean
   theorem liftedFactorSubsetPartition_of_choosePrimeData
       (core : Hex.ZPoly) (B : Nat) :
       let primeData := Hex.choosePrimeData core
       let d := Hex.henselLiftData core B primeData
       LiftedFactorSubsetPartition core d Finset.univ core
   ```

   placed adjacent to `henselSubsetCorrespondenceHypotheses_of_choosePrimeData`
   (around line 7704), at the end of the file before the closing
   `end HexBerlekampZassenhausMathlib` (around line 7745).

   Discharge each field of `LiftedFactorSubsetPartition` (declared
   around line 1797):

   - **`toHenselSubsetCorrespondenceRest`**: apply
     `henselSubsetCorrespondenceRest_initial` (line 1686) to
     `henselSubsetCorrespondenceHypotheses_of_choosePrimeData core B`.
   - **`pairwise_disjoint`**: derive from the inherited `unique_subset`
     field — two non-associated irreducible divisors with associated
     representing subsets would contradict uniqueness; otherwise the
     subsets are equal, but the divisors are non-associated, so the
     subsets are disjoint trivially. Concretely the contrapositive
     argument: a shared element `i ∈ S ∩ T` plus the existence of an
     irreducible divisor in each subset would, by `unique_subset`,
     force the divisors to share a representing subset, hence (by
     square-freeness of `target = core`) be associated — contradicting
     the non-associated assumption.
   - **`unique_up_to_associated`**: derive from `unique_subset` directly.
     Associated factors share their representing subset by the uniqueness
     part of the analytic Hensel correspondence; the proof composes the
     `RepresentsIntegerFactorAtLift` invariance under `Associated` (look
     for or add a small helper if it does not already exist) with
     `unique_subset`.
   - **`target_squarefree`**: square-freeness of
     `HexPolyZMathlib.toPolynomial core` for `core =
     (Hex.normalizeForFactor f).squareFreeCore`. The parametric
     `core` form requires `core` to be square-free as a `Hex.ZPoly`
     plus a transport lemma to `Polynomial ℤ`. If a `Hex.ZPoly.SquareFree`
     hypothesis is needed, take it as an explicit hypothesis on the
     constructor — analogous to how the `hcore_record`/`hcore_monic`
     hypotheses are taken externally by the consumer wrappers. The
     specialisation in deliverable 2 can then discharge it from the
     `squareFreeCore` invariants.
   - **`cover`** and **`support_subset_of_dvd_recombinationCandidate`**:
     these are the genuinely analytic fields that cannot be derived
     from `HenselSubsetCorrespondenceHypotheses` alone. Follow the
     #4543 fallback pattern: introduce a single localised `private`
     helper `liftedFactorSubsetPartition_analytic_obligation` carrying
     the combined analytic content as one `sorry`, and route both fields
     through it. Mirror the docstring on
     `henselSubsetCorrespondence_analytic_obligation` (around line
     7686), explaining that a real proof would either come from BHKS
     Theorem 5.2 (tracked by #2567) or from the classical square-free
     Hensel correspondence + partition completeness lemma transported
     to the executable surface. The constructor itself must be
     `sorry`-free, with all sorries localised inside the private helper.

2. Add an outer-bound specialisation

   ```lean
   theorem liftedFactorSubsetPartition_outerBound_of_choosePrimeData
       (f : Hex.ZPoly) :
       let core := (Hex.normalizeForFactor f).squareFreeCore
       let primeData := Hex.choosePrimeData core
       let B := Hex.precisionForCoeffBound
         (Hex.ZPoly.defaultFactorCoeffBound f) primeData.p
       let d := Hex.henselLiftData core B primeData
       LiftedFactorSubsetPartition core d Finset.univ core
   ```

   placed immediately after the parametric constructor, mirroring
   `henselSubsetCorrespondenceHypotheses_outerBound_of_choosePrimeData`
   (line 7733). If deliverable 1's parametric constructor requires a
   square-freeness hypothesis on `core`, discharge it here from the
   existing `(Hex.normalizeForFactor f).squareFreeCore` invariants.

3. Do not change executable behavior. No new `axiom`, `native_decide`,
   `TODO`, `FIXME`, or theorem-level `sorry` outside the single
   localised `liftedFactorSubsetPartition_analytic_obligation` helper.
   No edits to `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.

## Context

Read:

- `HexBerlekampZassenhausMathlib/Basic.lean`:
  - lines 1662–1776 (`HenselSubsetCorrespondenceRest`, the
    `henselSubsetCorrespondenceRest_initial` constructor, and the
    transport theorem).
  - lines 1797–1843 (`LiftedFactorSubsetPartition` structure
    declaration with the six fields to discharge).
  - lines 7645–7741 (the #4543 pattern: localised analytic obligation
    helper + parametric constructor + outer-bound specialisation).
  - lines 7444–7642 (the slow-path consumer wrappers `factorWithBound_…`
    and `factor_…_of_henselSubsetCorrespondence`) to confirm the shape
    of the `hpartition` hypothesis that the new constructor must match.
- `RepresentsIntegerFactorAtLift` definition and its invariance lemmas
  (search for `RepresentsIntegerFactorAtLift` in
  `HexBerlekampZassenhausMathlib/Basic.lean`).
- Issue #4543, #4544, #4547 for the substrate-construction style; in
  particular the explicit "fallback allowance for a single localised
  analytic sorry" pattern in #4543.
- Issue #4170's "Status (post-replan, 2026-05-17)" section, substrate
  item 1.

The proof should follow the #4543 pattern closely: the constructor
itself is `sorry`-free, all structurally-derivable fields are
discharged from `HenselSubsetCorrespondenceHypotheses` /
`HenselSubsetCorrespondenceRest` infrastructure and `unique_subset`,
and the genuinely analytic content (cover + recombination-support) is
isolated in a single private helper carrying one `sorry` with a
docstring explaining what would discharge it.

If deliverable 1 uncovers that the `target_squarefree` field needs
a non-trivial ZPoly→Polynomial ℤ square-freeness bridge that does not
already exist, it is acceptable to either (a) take `Squarefree
(HexPolyZMathlib.toPolynomial core)` as an explicit hypothesis (the
specialisation in deliverable 2 discharges it from the squareFreeCore
invariants), or (b) add the small ZPoly→Polynomial ℤ square-freeness
bridge in `HexPolyZMathlib/` next to the existing primitive bridges
(cf. `toPolynomial_isPrimitive_of_zpoly_primitive` from #4544). Do
not duplicate any existing helper.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `lake build HexBerlekampZassenhaus`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom" HexBerlekampZassenhausMathlib/Basic.lean`
  should show exactly one new theorem-level `sorry` inside the new
  private `liftedFactorSubsetPartition_analytic_obligation` helper
  (existing sorries at lines 162, 241, and 7695 unchanged). No new
  `axiom`.
- No new `native_decide`, `TODO`, `FIXME`.

## Out of scope

- The HO-1 capstone assembly itself (#4170): wiring the new
  constructor + #4543 + #4544 + #4547 into the slow-path arm of
  `factor_irreducible_of_nonUnit` is the natural follow-up but
  belongs in a separate issue (the #4170 capstone or a partial
  slow-path assembly sub-issue).
- BHKS Theorem 5.2 (directive #2567): the genuine discharge of
  `liftedFactorSubsetPartition_analytic_obligation` lives there.
- Substrate work for the small-mod singleton branch (#4544 — already
  landed) or BHKS fast branch (out of scope per directive #2567).
- The `henselSubsetCorrespondence_analytic_obligation` from #4543 is
  intentionally left alone — discharging it is the same BHKS work.
- Editing `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
