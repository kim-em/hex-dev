## Current state

The A2 abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/Basic.lean` is reaching the
cover-extraction layer immediately above the per-T
membership-equivalence theorems. The scaled-side per-T
membership-equivalence layer `_of_bound` sibling is in flight as
issue #4923 (the scaled
`mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`,
blocked on the in-flight scaled cover sibling #4921).

The scaled-side primitive + positive-leading-core consumer up from
the scaled mem_T_iff layer is

* `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
  at line 11589 of `HexBerlekampZassenhausMathlib/Basic.lean`.

It takes `hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound core <
d.p ^ d.k` and consumes it at exactly **one** call site:

* Line 11617–11621: a call to
  `mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core`
  threading `hprecision`.

The rest of the proof body (the `hcand_dvd_target`
`exactQuotient?_product` unfolding, the `hf_dvd_target` algebra step,
and the final repack at lines 11622–11634) is precision-agnostic and
carries over verbatim.

The natural `_of_bound` shape mirrors the unscaled primitive sibling
#4925 and the scaled mem_T_iff sibling #4923. Because #4923's
signature must carry `hcore_lc_bound` (the scaled per-factor bridge
`_of_bound` from #4907 needs `(leadingCoeff core).natAbs ≤ B'`), this
issue's `_of_bound` sibling also threads `hcore_lc_bound`:

```lean
(B' : Nat)
(hcore_lc_bound : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hvalid : ∀ g : Hex.ZPoly,
  HexPolyZMathlib.toPolynomial g ∈
    UniqueFactorizationMonoid.normalizedFactors
      (HexPolyZMathlib.toPolynomial
        (scaledRecombinationCandidate core d T)) →
  ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

The wrapper instantiates `B' := defaultFactorCoeffBound core`,
discharges `hcore_lc_bound` via `defaultFactorCoeffBound_valid core
hcore_ne core hcore_dvd_self (core.size - 1)` paired with
`leadingCoeff_eq_coeff_last` (same derivation block used in #4907's
scaled per-factor wrapper), and discharges `hvalid` for an arbitrary
`g ∈ normalizedFactors (toPolynomial (scaledRecombinationCandidate
core d T))` via the divisor chain `g ∣ scaledRecombinationCandidate
core d T ∣ target ∣ core` plus `defaultFactorCoeffBound_valid`.

depends-on: #4923

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, cover-extraction
  layer, scaled primitive non-monic flavour.
* **Natural strategy uses Mathlib?** Yes — relies on
  `UniqueFactorizationMonoid.normalizedFactors` plumbing already in
  the file.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4923.

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add an abstract-bound
sibling immediately following
`exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
at line 11589, and rewire the existing original as a thin wrapper.

1. **`exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`**
   — same signature as the original at line 11589 except `hprecision :
   2 * Hex.ZPoly.defaultFactorCoeffBound core < d.p ^ d.k` is replaced
   by the four-parameter block `(B' : Nat) (hcore_lc_bound) (hvalid)
   (hprecision : 2 * B' < d.p ^ d.k)` (shape above). The proof body
   threads `B'`, `hcore_lc_bound`, `hvalid`, and `hprecision` to the
   single call site
   `mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
   (from #4923) and leaves the rest of the proof unchanged.

2. **Rewire** the original
   `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
   at line 11589 as a thin wrapper that instantiates `B' :=
   defaultFactorCoeffBound core`, discharges `hcore_lc_bound` via the
   `defaultFactorCoeffBound_valid` + `leadingCoeff_eq_coeff_last`
   identity (same block as #4907's wrapper), and discharges `hvalid`
   for an arbitrary normalised factor `g` via the divisor chain
   `g ∣ scaledRecombinationCandidate core d T ∣ target ∣ core` plus
   `defaultFactorCoeffBound_valid core hcore_ne g hg_dvd_core`. The
   chain construction must use `conv_lhs => rw [...]` (not bare `rw`)
   when rewriting `core` on the LHS to avoid the scaled-cascade
   gotcha documented in `progress/20260518T035027Z_184d5454.md` and
   in the body of #4907.

The wrapper preserves the existing signature exactly. The single
downstream consumer
`coverAtMin_representingSubset_subset_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core`
at line 11648 continues to type-check unchanged.

## Context

This issue lifts the third (scaled) flavour of the
`exists_representingSubset_of_mem_T_*_of_*` family to its
`_of_bound` sibling. The monic flavour is #4924 (blocked on #4918),
and the primitive unscaled flavour is #4925 (blocked on #4919). With
this issue and those two, the cover-extraction substrate layer is
complete for all three flavours, unblocking the `coverAtMin_*_of_bound`
tier above it.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — no new errors
  relative to `origin/main` (the pre-existing whnf timeouts and
  unknown-kernel-constant errors are environment-specific; see
  progress notes from this chain).
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing
  error set, no new errors.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
