## Current state

`scripts/status.py` reports `HexHensel -> Phase 6 (proof polishing)` as
ready. Several Phase 6 slices have landed or are in flight:

- PR #4775 / issue #4772: `HexHensel/Basic.lean` reduction / lift API
  docstrings and simp surface (landed).
- PR #4790 / issue #4789: `HexHensel/Multifactor.lean` `polyProduct_*`
  rewrite cluster simp surface (landed).
- issue #4793: `HexHensel/Multifactor.lean` `normalizedXGCD` +
  `multifactorLift` API surface (in flight).
- issue #4796: `HexHensel/Linear.lean` definition cluster lines 1-167
  surface 1 polish (in flight).

`HexHensel/Quadratic.lean` (2296 lines) has not yet had a Phase 6
polish pass. The file owns the executable quadratic Hensel doubling
step — lifting a factorisation `f ≡ g * h (mod m)` and Bezout
witnesses `s * g + t * h ≡ 1 (mod m)` to congruences modulo `m^2`.

The file has a clean public surface concentrated at two ends:

**Top of file (lines 1-33) — `QuadraticLiftResult` namespace:**
- `QuadraticLiftResult` struct (line 13) with four `ZPoly` fields
  `g h s t` — already carries docstring "Result of one quadratic
  Hensel lift step."
- `QuadraticLiftResult.reduceModSquare` (line 22): canonical
  coefficient reduction modulo `m^2`, defined via
  `ZPoly.reduceModPow f m 2`.
- `QuadraticLiftResult.factorError` (line 26): `f - g * h`.
- `QuadraticLiftResult.bezoutError` (line 30): `s * g + t * h - 1`.

All three definitions carry one-line docstrings; the bodies are
algebraic.

**End of file (lines 1779-2295) — quadratic step + spec theorems:**
- `Hex.ZPoly.quadraticHenselStep` (line 1780): the executable
  doubling step. Already documented.
- `quadraticHenselStep_factor_spec` (line 2151): updated factors
  multiply to `f` modulo `m^2`.
- `quadraticHenselStep_bezout_spec` (line 2164): updated Bezout
  witnesses certify coprimality modulo `m^2`.
- `quadraticHenselStep_spec` (line 2177): conjunction of the two
  specs.
- `quadraticHenselStep_monic` (line 2193): monic factor stays monic.
- `quadraticHenselStep_factor_congr_mod_base` (line 2208): both
  output factors are congruent to inputs modulo `m`. Already carries
  a multi-line docstring.

Lines 35-1778 are entirely private helpers (modular arithmetic
helpers, `divModMonicModSquare` driver, fold-shape lemmas, Bezout
correction proofs). The polish audit for this issue treats the
private body as out of scope — it is a long internal proof chain
that would require its own dedicated decomposition to polish
meaningfully, and the SPEC docstring rule applies primarily to public
declarations and *non-obvious* private helpers (the private helpers
in this file are named after the algebraic operations they
implement: `addModSquare`, `mulModSquare`, `divModMonicModSquare`,
etc.).

The file has **zero** `@[simp]` annotations. The polish audit must
identify which (if any) of the public declarations qualify as
unconditional canonical-form rewrites suitable for `@[simp]`.

## Deliverables

1. Tighten docstrings on the four public declarations in lines 1-33:

   - `QuadraticLiftResult` struct: explain that the four fields
     package the updated leading factor (`g`), complementary factor
     (`h`), and updated Bezout witnesses (`s`, `t`) after one
     doubling step.
   - `QuadraticLiftResult.reduceModSquare`: existing "Canonical
     coefficient reduction modulo `m^2`." is already adequate.
     Confirm whether the docstring should mention the
     `ZPoly.reduceModPow f m 2` realisation or whether keeping the
     abstract specification is preferable. Default: keep abstract.
   - `QuadraticLiftResult.factorError`: the rewrite `f - g * h` is
     the residue the doubling step corrects; the docstring should
     name this role rather than repeat the formula.
   - `QuadraticLiftResult.bezoutError`: same — name the role
     ("residue corrected by the Bezout update of the doubling step").

2. Tighten docstrings on the public declarations in lines 1779-2295:

   - `quadraticHenselStep`: existing "One quadratic Hensel
     correction step from modulus `m` to modulus `m^2`." is
     adequate; confirm or extend to name the four inputs (`f`, `g`,
     `h`, `s`, `t`) and the precondition `g` monic.
   - `quadraticHenselStep_factor_spec`,
     `quadraticHenselStep_bezout_spec`, `quadraticHenselStep_spec`,
     `quadraticHenselStep_monic`: existing one-liners adequately
     characterise the result.
   - `quadraticHenselStep_factor_congr_mod_base`: existing multi-line
     docstring explicitly mentions "the data modulo `m^2 \ m`" — fix
     LaTeX-style backslash to standard Lean-doc-friendly notation
     (e.g. "the data modulo `m^2` outside of modulo `m`") if the
     renderer does not handle it cleanly.

3. Audit `@[simp]` coverage for the public surface. Tag only
   unconditional canonical-form rewrites that match the project's
   simp discipline. Candidates:

   - The four struct projection accessors (`QuadraticLiftResult.g`,
     `.h`, `.s`, `.t`) are auto-generated; their `@[simp]` story is
     controlled by Lean's structure mechanism, not by this file.
     Verify whether `quadraticHenselStep` is consumed downstream via
     pattern-matching on the struct or via explicit projections —
     if the latter, consider tagging `quadraticHenselStep`'s
     `let r := ...` pattern (no — that is not a rewrite; it is a
     binding). The audit should likely conclude that no `@[simp]` tag
     applies to `quadraticHenselStep` itself.
   - `quadraticHenselStep_factor_spec`, `_bezout_spec`, `_spec`,
     `_monic`, `_factor_congr_mod_base` are not simp candidates —
     they all carry the precondition cluster `hm`, `hprod`, `hbez`,
     `hmonic`.

   Document the audit outcome — "no public declaration in this file
   qualifies for `@[simp]`" — in the PR description. This is
   acceptable; the polish audit is not required to produce simp tags
   if none qualify.

4. Verify the private helpers in lines 35-1778 are *not* missing
   docstrings under the SPEC rule. Pass over the private names and
   confirm each is named after the algebraic operation it implements
   (and therefore is "obvious" under the SPEC's definition). If a
   private helper's name does not telegraph the fact it proves,
   record it in the PR description as a follow-up candidate but do
   **not** add a docstring in this slice — a dedicated polish issue
   for the private body is a better unit of work.

5. Preserve behaviour and public API shape: no changes to definitions,
   theorem statements, argument order, namespace placement, or proof
   structure beyond docstrings and (if the audit produces any) safe
   local attributes. No structural refactors. No moves between public
   and private visibility.

6. Do not introduce `axiom`, `native_decide`, `TODO`, `FIXME`, or new
   theorem-level `sorry`.

## Library placement

Primary file:

- `HexHensel/Quadratic.lean`.

Governing SPEC:

- `SPEC/Libraries/hex-hensel.md`: quadratic Hensel doubling step.
- `PLAN/Phase6.md`: proof polishing requires clean API design,
  docstring coverage, and appropriate automation annotations.
- `SPEC/design-principles.md`: "non-obvious private helpers"
  docstring requirement.

Placement answers:

- SPEC section: `hex-hensel` quadratic Hensel doubling step.
- Natural strategy uses Mathlib? No; Mathlib-free core API polish.
- Is this already in Mathlib? No; the executable quadratic Hensel step
  is project-specific.
- Missing infrastructure? None expected.

## Context

This is the **first** Phase 6 polish slice for
`HexHensel/Quadratic.lean`. The file is large (2296 lines) but most
of the body is private; this slice covers the public surface only.
A dedicated follow-up for the private helper body would warrant a
separate polish issue if the audit step surfaces gaps.

Read before editing:

- `PLAN/Phase6.md`
- `SPEC/Libraries/hex-hensel.md`
- `SPEC/design-principles.md` (Naming and documentation section,
  in particular the "non-obvious private helpers" rule)
- `HexHensel/Quadratic.lean` (target file)
- `HexHensel/Multifactor.lean` (uses the quadratic step indirectly
  via `HexHensel/QuadraticMultifactor.lean`; the polish patterns
  established by #4789 / #4790 apply)
- Predecessor Phase 6 polish slices: PR #4775 / issue #4772 (covered
  `HexHensel/Basic.lean` reduction / lift API); PR #4790 / issue
  #4789 (`polyProduct_*` simp cluster).

## Verification

- `lake build HexHensel.Quadratic`
- `lake build HexHensel`
- `lake build` (full project — confirms no downstream callers break
  from any docstring tightening or new attribute)
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexHensel/Quadratic.lean`
  should show no new entries compared to `origin/main`.

## Out of scope

- Statement-level refactors, renames, or argument-order changes.
- Phase 6 polish for the private helper body of
  `HexHensel/Quadratic.lean` (lines 35-1778) — if the audit step
  identifies gaps, file a follow-up issue but do not address them
  here.
- Phase 6 polish for `HexHensel/QuadraticMultifactor.lean` — separate
  slice.
- Phase 6 polish for other surfaces of `HexHensel/Linear.lean` —
  covered by #4796 (surface 1) and any subsequent surface-2/3/4
  slices.
- Algorithmic changes to `quadraticHenselStep` or any of its private
  building blocks.
