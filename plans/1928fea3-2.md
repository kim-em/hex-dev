## Current state

Five `_of_bound` theorems in `HexBerlekampZassenhausMathlib/Basic.lean`
declare `hcore_ne : core ≠ 0` as a hypothesis but never reference it in
their proof bodies, producing five `unused variable hcore_ne` linter
warnings. The progress note for PR #5003 (commit f8ea0dd3, written
2026-05-18) explicitly enumerated them and flagged them as by-design
parameters kept "for symmetry with the `_of_bound` siblings" pending a
harmonisation cleanup:

> Investigate the 5 remaining `unused variable hcore_ne` warnings —
> by-design now that the helper consumes it directly, but the
> thin-wrapper signatures still accept it for symmetry with the
> `_of_bound` siblings; harmonisation is a separate cleanup.

The established suppression convention in the same file is the
underscore-prefix idiom `(_hcore_ne : core ≠ 0)`, already in use at 8
sites (`Basic.lean:1290, 3904, 3937, 3965, 3997, 4029, 4062, 4161`) where
the same "kept for symmetry, body never references it" rationale applies.

## Per-site breakdown (current line numbers at `origin/main` 3ecc822a)

For each site, the `(hcore_ne : core ≠ 0)` parameter declaration must be
renamed to `(_hcore_ne : core ≠ 0)`. The proof bodies of these five
theorems do **not** reference `hcore_ne` anywhere; the rename is the
single edit per site.

1. `Basic.lean:4344` — body of theorem
   `scaledRecombinationCandidate_eq_factor_of_recovery_of_bound`
   (theorem header at line 4340). Body lines 4350–4357 do not mention
   `hcore_ne`.
2. `Basic.lean:8263` — body of theorem
   `representsIntegerFactorAtLift_primitive_of_bound`
   (theorem header at line 8257). The internal `defaultFactorCoeffBound_*`
   chain runs through `hcore_lc_le`, not `hcore_ne`.
3. `Basic.lean:8513` — body of theorem
   `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum_of_bound`
   (theorem header at line 8510). All bound-related steps thread
   `hcore_lc_bound` (to be renamed to `hcore_lc_le` by a sibling
   cleanup issue) and `hprecision`; `hcore_ne` is unused.
4. `Basic.lean:8672` — body of theorem
   `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`
   (theorem header at line 8665). The signature already underscore-
   prefixes other unused parameters (`_hcore_primitive`, `_hdvd`,
   `_hfactor_irr`, `_hfactor_prim`, `_hfactor_norm`), so this rename
   completes the pattern.
5. `Basic.lean:10866` — body of `private theorem`
   `zpoly_primitive_scaledRecombinationCandidate_of_bound`
   (theorem header at line 10864). Body threads `hcore_lc_le`,
   `hcore_lc_pos`, `hd_liftedFactor_monic`, `hprecision`; `hcore_ne`
   is unused.

Mechanical verification of "unused" status at each site: search the
proof body between the `:= by` opening and the next top-level
`theorem` / `private theorem` for any token `hcore_ne` — all five
sites return 0 occurrences. (The token may still appear in the
*signature* of nearby theorems; only body uses count.)

## Deliverables

1. At each of the 5 sites above, rename the parameter
   `(hcore_ne : core ≠ 0)` to `(_hcore_ne : core ≠ 0)`. No other
   edits to those signatures (other parameter names and positions
   unchanged).
2. No edits to the proof bodies, since the parameter is unused. No
   docstring changes (the surrounding `/-- ... -/` blocks discuss
   the hypothesis by its role, not by its identifier; the literal
   token `hcore_ne` does not appear in those docstrings — verify
   per site before posting).
3. No callers change. The 5 theorems are invoked by their non-
   `_of_bound` wrapper siblings, which pass the `hcore_ne` value
   positionally; renaming the receiver parameter to `_hcore_ne`
   does not affect positional binding.
4. No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Naming

The `_hcore_ne` underscore-prefix convention is already established at
8 sites in `Basic.lean` (1290, 3904, 3937, 3965, 3997, 4029, 4062,
4161). These five new sites bring the total to 13 and complete the
suppression sweep for the post-#4991 helper rollout.

## Verification

* `git grep -c "(_hcore_ne : core ≠ 0)" HexBerlekampZassenhausMathlib/Basic.lean`
  rises from 8 to 13.
* `git grep -c "(hcore_ne : core ≠ 0)" HexBerlekampZassenhausMathlib/Basic.lean`
  drops by exactly 5 (from current value, which the worker should
  measure at session-claim time — pre-state count includes both
  used and unused `hcore_ne` parameters across the file).
* `lake build HexBerlekampZassenhausMathlib.Basic`: same 16-error
  baseline as `origin/main` 3ecc822a (11 whnf timeouts + 1 cases-
  failure + 4 kernel-unknown constants); **the 5 `unused variable
  hcore_ne` warnings at the listed sites disappear** (post-state
  warning count for `hcore_ne` drops to 0 in this file).
* `lake build HexBerlekampZassenhausMathlib`: same baseline.
* `python3 scripts/check_dag.py`: exit 0.
* `git diff --check`: clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
   | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"`:
  empty.
* `git diff --stat origin/main`: 1 file changed, +5 / −5 (per-site
  one-character `_` insertions appear as paired add/remove in unified
  diff format).

## Context

* PR #4991 (commit 89ac1f67) extracted
  `defaultFactorCoeffBound_leadingCoeff_natAbs_le`, which consumes
  `hcore_ne` directly at caller sites; the `_of_bound` thin wrappers
  hand the value through positionally but the `_of_bound` callees
  no longer need it internally — hence the unused-warning crop.
* PR #5003 (commit 3ecc822a) closing #5003 explicitly flagged the
  5 remaining `unused variable hcore_ne` warnings as harmonisation
  follow-up; its progress note listed the line numbers 4344, 8257,
  8507, 8665, 10859 (theorem-header line for sites 2–5; sites 4344
  is the parameter line itself — the slight offset is because the
  warning points to the theorem header in some Lean versions). The
  per-site breakdown above gives the precise parameter-line
  locations at `origin/main` 3ecc822a.
* Underscore-prefix precedent: 8 sites already use `_hcore_ne`. The
  signature at site 4, `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`,
  already underscore-prefixes 5 other unused parameters
  (`_hcore_primitive`, `_hdvd`, `_hfactor_irr`, `_hfactor_prim`,
  `_hfactor_norm`), so this rename completes the per-site sweep.

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus.md` —
  Mathlib-bridge style-cleanup / warning-suppression. No new
  upstream API.
* **Mathlib use:** yes. Changes confined to
  `HexBerlekampZassenhausMathlib/Basic.lean`.

## Out of scope

* The `hcore_lc_bound` → `hcore_lc_le` parameter rename (separate
  sibling issue, dispatched independently).
* The "kept-for-symmetry" parameters at the wrapper (non-
  `_of_bound`) siblings. Those signatures still genuinely use
  `hcore_ne` internally to discharge `defaultFactorCoeffBound_*`
  preconditions, so they are not unused.
* Any change to the 8 already-underscore-prefixed sites — those
  remain as-is.
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
