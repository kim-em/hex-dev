## Current state

`HexMatrix/RREF.lean` is in Phase 6 (proof polishing). The cluster of
`private theorem`s describing how a row combination interacts with the
pivot columns and echelon coefficients of an RREF is undocumented. These
encode the structural invariants behind row-span membership, so the
Phase 6 docstring rule applies.

## Deliverables

Add a one-sentence docstring to each of the following `private theorem`s
in `HexMatrix/RREF.lean` (lines ~2005–2110), stating what it proves and
its role in the RREF row-span / nullspace argument:

- `pivot_column_entry`
- `rowCombination_pivotCoeff`
- `rowCombination_eq_of_coeffs_eq_on_rank`
- `rowCombination_echelonCoeffs_of_rowCombination`

Doc-only: do not touch signatures, statements, proofs, declaration
order, or `private` modifiers.

## Context

- `SPEC/design-principles.md` §Docstrings sets the coverage rule.
- `PLAN/Phase6.md`: docstring coverage is a Phase 6 exit criterion.
- Follows the established Phase 6 doc-batch pattern (e.g. the merged
  `HexMatrix/Determinant.lean` and `HexMatrix/Basic.lean` batches).

## Verification

- `lake build HexMatrix.RREF` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean; `git diff --numstat` shows additions only.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
