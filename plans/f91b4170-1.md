## Current state

HexGfqRing reached `done_through: 6` in PR #4520 (`libraries.yml`
line 213), and `python3 scripts/status.py HexGfqRing` now reports
`HexGfqRing -> Phase 7 (user-facing documentation)`. This is the
first library in the repo to clear Phase 6, so HexGfqRing's Phase 7
chapter is the first Verso chapter the project needs to land.

Scaffolding already in place:

- `lakefile.lean:7-8` requires Verso `v4.30.0-rc2`.
- `lakefile.lean:214-215` declares `lean_lib HexManual` as the
  `@[default_target]`.
- `HexManual.lean` exists as a single-line placeholder (no chapter
  imports yet).
- `HexManual/` exists but holds only legacy Phase-1 / Phase-2-3-4-5
  checkpoint markdown — no `.lean` chapter files.

`PLAN/Phase7.md:34-37` pins the convention for chapter location:
chapters live at `HexManual/Chapters/<LibraryName>.lean` and the
top-level `HexManual.lean` imports them. This issue lands that
convention together with the first chapter's content.

## Deliverables

1. **Write `HexManual/Chapters/HexGfqRing.lean`.** A Verso chapter
   for HexGfqRing covering the five chapter elements named in
   `PLAN/Phase7.md:48-58`:

   - short introduction (what `PolyQuotient p f hf` is for and what
     downstream users — `HexGfqField`, `HexConway` — get from it);
   - the core data types, with signatures pulled programmatically
     from source (`reduceMod`, `PolyQuotient`, `ofPoly`, `repr`);
   - the principal operations, with at least one worked example
     that compiles (add, mul, neg, sub, pow);
   - key correctness theorems, with statements pulled from source
     and prose explaining them (the `repr (ofPoly a) = reduceMod
     f a`, degree-bound, and reduction-respects-ring-ops shape
     listed in `SPEC/Libraries/hex-gfq-ring.md:36-42` and proved
     under the `Hex.GFqRing` namespace in
     `HexGfqRing/Operations.lean`);
   - a cross-reference to `HexGfqField` (the field extension when
     `f` is irreducible, mentioned at
     `SPEC/Libraries/hex-gfq-ring.md:32-34`) and to the dependency
     `HexPolyFp` (the `FpPoly` representation `reduceMod` operates
     on). HexGfqRing has no `*Mathlib` bridge library, so the
     usual "computational vs Mathlib bridge" cross-reference does
     not apply here — say so explicitly in the chapter so future
     chapters know how to handle that case.

   Pull signatures and theorem statements **programmatically** from
   source (Verso's idiom — the Lean Language Reference pattern named
   in `PLAN/Phase7.md:39-46`), do not duplicate text. The conventions
   chosen here (which Verso macros to use for signature pulls,
   docstring extraction, code-example embedding) will be reused by
   every subsequent chapter, so prefer the closest Verso-idiomatic
   pattern over ad-hoc custom commands.

2. **Wire the chapter into `HexManual.lean`.** Replace the empty
   placeholder with an `import HexManual.Chapters.HexGfqRing` (plus
   whatever Verso top-level setup the manual aggregator needs to
   render). This is the first import of a chapter; future chapters
   will be added to this same aggregator.

3. **Bump `libraries.yml HexGfqRing.done_through: 6 -> 7`** once the
   chapter builds inside the `HexManual` `lean_lib` and the embedded
   Lean code blocks typecheck (per `PLAN/Phase7.md:78-89`'s exit
   criteria for library `L`).

## Context

- `PLAN/Phase7.md` — full Phase 7 spec: chapter location, authoring
  style (programmatic signature pulls), exit criteria, and the
  tutorial table (HexGfqRing has **no** anchored tutorial; that is
  intentional, and out of scope here).
- `SPEC/Libraries/hex-gfq-ring.md` — Contents / Key properties /
  External-comparator structural-layer justification; the chapter
  prose should follow this same narrative ordering.
- `HexGfqRing/Basic.lean` (223 lines) — `PolyQuotient` type and
  smart constructors.
- `HexGfqRing/Operations.lean` (1096 lines) — ring operations,
  reduction laws, ring/semiring/grind instances.
- `HexGfqRing.lean` — root file aggregating the above.
- `HexGfqField/` — downstream consumer; reference it as the field
  extension target when `f` is irreducible.
- Verso source: `require verso from git
  "https://github.com/leanprover/verso.git" @ "v4.30.0-rc2"` in
  `lakefile.lean:7-8`. The Lean Language Reference (the project
  Verso was carved out of) is the closest in-the-wild example of
  this chapter style; consult Verso's own documentation /
  examples to pick idiomatic macros for signature pulls.

## Out of scope

- Chapters for any other library. They are blocked on those
  libraries reaching `done_through ≥ 6` — none have, as of this
  cycle.
- Tutorials. HexGfqRing has no anchored tutorial in the
  `PLAN/Phase7.md:62-76` table; the `hex-gfq` prime-splitting
  tutorial is anchored to `hex-gfq` (still at `done_through: 5`).
- The full-manual release-level render with all cross-references
  resolved — that is a `PLAN/Releases.md` concern, not a per-library
  Phase 7 exit criterion (`PLAN/Phase7.md:91-97`).
- Splitting `HexGfqRing/Operations.lean` (1096 lines) — a separate
  refactor flagged in `progress/20260516T153130Z_33ea7f9d.md` and
  `progress/20260516T163154Z_c0b0be5a.md`, not blocking Phase 7.
- Any change to `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
- Verso version bumps. Stay on `v4.30.0-rc2`.

## Verification

- `lake build HexManual` clean.
- `lake build HexManual.Chapters.HexGfqRing` clean (chapter
  typechecks independently — all embedded Lean blocks compile
  against the real library, per `PLAN/Phase7.md:80-82`).
- `lake build HexGfqRing HexGfqField HexConway` clean (downstream
  computational consumers are unaffected — Phase 7 must not pull
  Mathlib into the computational DAG, per `PLAN/Phase7.md:27-29`).
- `python3 scripts/status.py HexGfqRing` reports Phase 7-complete
  (i.e. no longer in the "Ready" section; library either no longer
  listed or listed as fully done).
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` exit 0.
- No new `axiom`, `sorry`, `native_decide`, `TODO`, or `FIXME`. The
  pre-existing `sorry` inventory is unchanged.
