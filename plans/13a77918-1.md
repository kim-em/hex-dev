library: HexPoly

## Current state

`HexPoly` sits at `libraries.yml` `done_through: 6` (Phase 6, proof
polishing). `PLAN/Phase6.md` calls out "Appropriate automation
annotations" explicitly: *"`@[grind]` for automated reasoning. Maximize
what `simp` and `grind` can close automatically."*

The Phase 6 grind-automation sweep — one `.lean` file per issue,
promoting equation-shaped public `@[simp]` characterising lemmas to
`@[simp, grind =]` so downstream `grind` goals close without an explicit
lemma list — has run for several sibling modules (`HexArith/ExtGcd.lean`
#7974, `HexHensel/Basic.lean` #7967, `HexPolyFp/Basic.lean` #7955) but
has **not** reached `HexPoly/Dense.lean` (582 lines), the dense-polynomial
core constructor/coefficient surface. A scan shows the gap: **23**
`@[simp]` annotations, **0** `@[grind]`.

Most of those `@[simp]` lemmas are clean `lhs = rhs` characterising facts
for the dense representation (`coeff`, `size`, `degree?`, `support`,
`toArray`, `ofCoeffs`, `ofList`, `monomial`, `C`) and are good
`@[grind =]` candidates. Representative examples:

- `coeff_ofCoeffs` (`Dense.lean:173`): `(ofCoeffs coeffs).coeff n = coeffs.getD n 0`
- `coeff_C` (`Dense.lean:180`): `(C c).coeff n = if n = 0 then c else 0`
- `coeff_monomial` (`Dense.lean:191`): `(monomial n c).coeff i = if i = n then c else 0`
- `size_zero` (`Dense.lean:327`), `degree?_zero` (`Dense.lean:331`),
  `degree?_zero_getD` (`Dense.lean:336`), `degree?_C_getD` (`Dense.lean:437`)
- `coeffs_C_zero` (`Dense.lean:352`), `size_C_zero` (`Dense.lean:373`),
  `monomial_zero` (`Dense.lean:399`)
- `support_zero` (`Dense.lean:488`), `support_C` (`Dense.lean:492`),
  `support_monomial` (`Dense.lean:519`)
- `toArray_size` (`Dense.lean:539`), `toArray_getD` (`Dense.lean:545`),
  `ofCoeffs_toArray` (`Dense.lean:550`), `ofList_toArray_toList`
  (`Dense.lean:558`), `ofCoeffs_empty` (`Dense.lean:563`), `ofList_nil`
  (`Dense.lean:568`), `ofCoeffs_replicate_zero` (`Dense.lean:573`),
  `coeff_ofList` (`Dense.lean:227`), `coeff_ofCoeffs_list` (`Dense.lean:234`)

**Premise note (premise-check before executing):** the `if`-RHS lemmas
(`coeff_C`, `coeff_monomial`, `support_C`, `support_monomial`) are still
literal `lhs = rhs` equations (the RHS is an `if`-expression) and are
fine for `grind =`. One listed `@[simp]` lemma is **not** an equation:
`mem_support` (`Dense.lean:483`) is an `↔` (`i ∈ p.support ↔ …`), so it
is **not** a `grind =` target — exclude it (or, if it pays off, use the
appropriate `grind` form for an iff, but do not force it). Before
annotating, re-read each lemma: only promote forms that are a literal
`lhs = rhs`. Watch for `grind`-loop risk on the round-trip lemmas
(`ofCoeffs_toArray`, `ofList_toArray_toList`) — confirm `grind` treats
them as terminating equations. If, after reading the file, the
annotations do not pay off (no in-file proof shortens, risk of a
`grind`-loop), **do not force contrived changes** — `coordination skip`
with a note explaining the premise does not hold is the correct outcome.

Independent of the other in-flight Phase 6 passes (different file, runs
in parallel).

## Deliverables

1. **`@[grind]` audit of `HexPoly/Dense.lean`.** For each clean
   equation-shaped public characterising lemma (the `coeff_*`, `size_*`,
   `degree?_*`, `support_*`, `toArray_*`, `ofCoeffs_*`, `ofList_*`,
   `monomial_zero`, `coeffs_C_zero` forms listed above) change `@[simp]`
   to `@[simp, grind =]`, following the convention already used in
   `HexArith/ExtGcd.lean` and `HexArith/UInt64/Wide.lean`. Do **not**
   blanket-annotate; only genuinely useful `grind` rewrites with no
   `grind`-loop or build-time regression.
2. **Deliberately exclude** `mem_support` (it is an `↔`, not a bare
   `lhs = rhs`) and any other form that turns out on closer reading to be
   non-equation-shaped; say so in the PR body. If you find an additional
   clean equation `@[simp]` lemma in the file that fits the pattern,
   include it.
3. **Shorten any proof the annotations now close** in `Dense.lean` (e.g.
   a manual `rw`/`simp` chain that bare `grind` now closes), and if a
   `HexPoly/Conformance.lean` example simplifies as a result you may
   enable that bare-`grind` simplification — but neither is required.
   Confirm no `grind` loop or build-time regression.

## Context

This is one file in the ongoing Phase 6 "grind automation coverage"
sweep. Scope is confined to `HexPoly/Dense.lean`; do not touch other
files (except an optional `HexPoly/Conformance.lean` simplification per
deliverable 3). `HexPoly` is Mathlib-free — only the executable layer's
own lemmas are in scope; no Mathlib tactics.

## Verification

- `lake build HexPoly` → `Build completed successfully`, and
  `HexPoly.Dense` builds without a grind-loop slowdown.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff --check` clean; no added-line `sorry`/`axiom`/`native_decide`/
  `TODO`/`FIXME`.
- `done_through` and any FFI/executable code untouched.
