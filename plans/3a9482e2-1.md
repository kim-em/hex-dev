## Current state

`python3 scripts/status.py HexGfqRing` reports
`HexGfqRing -> Phase 6 (proof polishing)` ready, with `libraries.yml`
still at `HexGfqRing.done_through: 5`.

Recent dedicated Phase 6 cluster reviews of the library have all
landed:

- #4472 (core quotient-representation API in `Basic.lean`)
- #4474 (`ofPoly`/`reduceMod` simp normal form cluster)
- #4478 (`natCast` cluster in `Operations.lean`)
- #4479 (`repr_*` ring-axiom cluster in `Operations.lean`)
- #4482 (`nsmul`/`intCast`/`zsmul` cluster in `Operations.lean`)
- #4489 (`pow`/`linearPow` exponentiation cluster)
- #4490 (final `Lean.Grind` instance fields)
- #4496 (private `linearNSmul` / `nsmul.go` binary-decomposition
  bridge)

The progress note for #4496 (`progress/20260516T153130Z_33ea7f9d.md`)
explicitly flagged the Phase 6 exit-criteria pass as the next natural
follow-up for a future planner.

Quality metrics recorded by the planner on the current worktree:

- `rg -n '^\s*sorry\b|^axiom\b|native_decide|^\s*TODO|^\s*FIXME'
  HexGfqRing HexGfqRing.lean` returns no matches.
- `HexGfqRing/Basic.lean`: 223 lines.
- `HexGfqRing/Operations.lean`: 1164 lines.
- `reports/hex-gfq-ring-performance.md` documents a recent scientific
  run on dedicated hardware exercising the seven Phase-4 bench
  targets (`runReduceModChecksum`, `runOfPolyReprChecksum`,
  `runAddChecksum`, `runMulChecksum`, `runNegSubChecksum`,
  `runPowChecksum`, `runNsmulNatCastChecksum`).

No open `agent-plan` issue currently covers a HexGfqRing Phase 6
completion audit or a `done_through: 5 -> 6` bump.

## Deliverables

1. Re-audit the HexGfqRing Phase 6 exit criteria against
   [PLAN/Phase6.md](PLAN/Phase6.md):
   - the project's style / naming / docstring standards (the library
     is Mathlib-free, so the "Mathlib linter clean" criterion reduces
     to the local quality bar described in
     [SPEC/design-principles.md](SPEC/design-principles.md));
   - docstring coverage on public declarations and non-obvious
     private helpers per
     [SPEC/design-principles.md](SPEC/design-principles.md);
   - no dead or unreferenced declarations in the library (check via
     a grep-driven sweep across `HexGfqRing`, `HexGfqField`,
     `HexGfq`, `HexGfqMathlib`, and the executable bench / fixture
     targets — anything declared in `HexGfqRing/` must be referenced
     either inside the library or by a downstream library/exe);
   - performance regression check against the committed baseline in
     `reports/hex-gfq-ring-performance.md` (no fresh benchmark run
     required — the audit confirms the baseline is the current
     comparator and that no recent merged PR has invalidated it);
   - native-decide-free at runtime;
   - Lean-checked irreducibility / field-construction evidence: not
     applicable — the library exposes a quotient-ring API only and
     makes no irreducibility or field-construction claims.
2. If the audit passes, update `libraries.yml` so
   `HexGfqRing.done_through: 5 -> 6`.
3. If the audit finds a narrow local gap (a missing docstring on a
   public declaration, a dead private helper, an obviously redundant
   theorem, a `simp` / `grind` annotation that should be added or
   removed), fix only that HexGfqRing-local gap in the same PR
   before bumping, keeping the targeted Lean diff under ~50 source
   lines. If the gap is broader or cross-library, skip the issue
   with `coordination skip` and a concrete blocker (e.g. "needs a
   separate cluster review of Conformance.lean") rather than
   broadening this audit.
4. Do not weaken or sorry any HexGfqRing declaration. Do not edit
   `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.

## Library placement

Primary file: `libraries.yml` (the `done_through` bump). Secondary
(only under deliverable 3): narrow targeted fixes in
`HexGfqRing/Basic.lean`, `HexGfqRing/Operations.lean`, or
`HexGfqRing.lean`.

Governing SPEC quote from
[SPEC/Libraries/hex-gfq-ring.md](SPEC/Libraries/hex-gfq-ring.md):
"Canonical quotient-ring implementation for `F_p[x] / (f)`. We only
form quotient rings by a nonconstant modulus."

Governing PLAN quote from [PLAN/Phase6.md](PLAN/Phase6.md): "For
library `hex-foo`, Phase 6 is done when: the Mathlib linter is clean
on the library; docstring coverage meets the rule in
`SPEC/design-principles.md` (public declarations and non-obvious
private helpers); the library contains no dead or unreferenced
declarations; the library passes a performance regression check
against the previous tagged benchmark baseline if one exists,
otherwise against the most recent committed benchmark baseline;
the library's computational path runs natively in Lean (no
`native_decide`, no reliance on external oracles at runtime); any
irreducibility or field-construction claim exposed by the library
is backed by Lean-checked evidence, not by an external oracle (for
libraries that expose such claims). Record completion by bumping
`libraries.yml[L].done_through` to `6`."

- Q1 SPEC §: `libraries.yml` owns the per-library phase counter;
  `HexGfqRing.lean` and `HexGfqRing/` own the quotient-ring API
  named by the SPEC.
- Q2 Mathlib strategy: HexGfqRing is Mathlib-free; no bridge layer
  is involved in this audit.
- Q3 Already in Mathlib?: not applicable — this is project
  metadata plus optional narrow local fixes.
- Q4 Infrastructure?: none missing — `scripts/status.py
  HexGfqRing` already reports ready for Phase 6.

## Context

Read:

- [SPEC/Libraries/hex-gfq-ring.md](SPEC/Libraries/hex-gfq-ring.md)
- [PLAN/Phase6.md](PLAN/Phase6.md)
- [PLAN/Conventions.md](PLAN/Conventions.md)
- [SPEC/design-principles.md](SPEC/design-principles.md) (docstring
  rule, proof placeholders)
- [HexGfqRing.lean](HexGfqRing.lean) (root)
- [HexGfqRing/Basic.lean](HexGfqRing/Basic.lean)
- [HexGfqRing/Operations.lean](HexGfqRing/Operations.lean)
- [reports/hex-gfq-ring-performance.md](reports/hex-gfq-ring-performance.md)
- The closed Phase 5 audit reference at #4299 for the canonical
  audit-and-bump shape (note: Phase 5 used `scripts/check_phase4.py`;
  Phase 6 has no equivalent script, so the audit is grep-driven and
  manual).
- The cluster review PRs cited under "Current state" for what has
  already been polished in this library.

## Verification

- `rg -n '^\s*sorry\b|^axiom\b|native_decide|^\s*TODO|^\s*FIXME'
  HexGfqRing HexGfqRing.lean` returns no matches.
- `lake build HexGfqRing` exit 0.
- `python3 scripts/status.py HexGfqRing` after the bump reports
  HexGfqRing advanced to Phase 7.
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` exit 0.
- If deliverable 3 introduces narrow fixes, the targeted files
  build clean and the Lean source diff is bounded (≤ ~50 lines).

## Out of scope

- Re-running the Phase 4 benchmark suite (the committed baseline is
  the comparator; a fresh benchmark run belongs in a separate
  Phase 4 issue).
- New conformance fixtures, oracle wiring, or bench targets.
- Phase 6 work on libraries other than HexGfqRing.
- File splits of `HexGfqRing/Operations.lean` (1164 lines) —
  separate refactor issue if motivated.
- Editing `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
