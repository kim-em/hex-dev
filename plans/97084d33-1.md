## Current state

`HexGF2/Multiply.lean` (library `HexGF2`, `done_through: 5`, so eligible
for Phase 6) carries the carryless-multiplication proof development for
`GF2Poly`. Within it, the boolean-list XOR-fold algebra cluster around
lines ~2812–2985 is undocumented. These are the `xorBoolList` helper
lemmas underpinning the word-pair / flatMap reasoning for clmul.

The 10 declarations lacking docstrings:

- `xorBoolList` (def, ~2812)
- `xorBoolList_cons` (~2832)
- `xorBoolList_append` (~2839)
- `xorBoolList_flatMap_append` (~2878)
- `xorBoolList_wordPairs_swap` (~2895)
- `xorBoolList_flatMap_congr_xor` (~2929)
- `xorBoolList_map_xorBoolList` (~2943)
- `xorBoolList_map_bne_congr` (~2954)
- `xorBoolList_flatMap_bne_congr` (~2969)
- `xorBoolList_flatMap_ranges_swap_list` (~2985)

(Line numbers approximate; locate by name.)

## Deliverables

1. Add a one-to-three-sentence `/-- ... -/` docstring to each of the 10
   declarations above, stating what the lemma/def proves and its role in
   the boolean-list XOR-fold algebra behind carryless multiplication.

## Context

This is a Phase 6 docstring-coverage batch (see `PLAN/Phase6.md` and the
docstring rule in `SPEC/design-principles.md`). Doc-only: do not touch
signatures, statements, proofs, `simp`/`grind` attributes, declaration
order, or `private` modifiers. `HexGF2` is a CI-built core library (not a
`*Mathlib` bridge), so a normal `lake build HexGF2.Multiply` verifies it.

Other undocumented clusters in this file (e.g. the `coeffWords_mulWords_*`
monomial lemmas ~2216–2804, and the `xorClmulAt`/`xorWordList` getElem
lemmas ~3100+) are separate future batches — leave them alone.

## Verification

- `lake build HexGF2.Multiply` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` is clean.
- `git diff --numstat -- HexGF2/Multiply.lean` shows additions only
  (`N 0`).
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` in added
  lines.
