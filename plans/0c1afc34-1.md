## Current state

`HexHenselMathlib` is a proof-only Mathlib bridge (`mathlib: true`, deps
`HexHensel` + `HexPolyMathlib`; the executable Hensel-lift kernel and its
bench live in `HexHensel`). It sits at `libraries.yml`
`HexHenselMathlib.done_through: 4` — Phase 4 was recorded as a bookkeeping
completion in #7809, since `SPEC/benchmarking.md §"Mathlib-free benches"`
forbids any `Hex*Mathlib` bench.

`python3 scripts/status.py HexHenselMathlib` reports the library is now at
`Phase 5 (implementation work loop)` and `ready: yes` (no blocking deps —
Phase 5 has local coupling only, requiring `done_through ≥ 4`, which is
met).

The two source files (`HexHenselMathlib/Basic.lean`,
`HexHenselMathlib/Correctness.lean`) currently contain **zero `sorry`
occurrences** (tokenized `grep -rnw sorry` → 0).

## Deliverables

1. Confirm `HexHenselMathlib` meets the Phase 5 exit criteria
   (`PLAN/Phase5.md`): zero `sorry` occurrences across all declarations,
   `lake build HexHenselMathlib` green on the pinned toolchain, and no
   library-specific conformance regression (proof-only bridges carry no
   own conformance fixtures; the executable `HexHensel` suite is
   unaffected).
2. Bump `libraries.yml` `HexHenselMathlib.done_through` `4 → 5`. Single
   one-line change; touch no other library entry.

This mirrors the established Phase-5 audit-and-bump precedent (e.g. #6925
for the executable `HexGramSchmidt`) and the proof-only Phase-4 precedent
(#7808, #7809).

## Context

- `libraries.yml` semantics: `done_through: K` means phases 1..K complete
  (linear, no skipping).
- Phase 5 (`PLAN/Phase5.md`): "Fill in `sorry`'d proofs." Exit criteria:
  zero `sorry`, green `lake build`, conformance still passes. For a
  proof-only bridge already at zero sorries this is an audit-and-bump, not
  new proof work.
- Premise check (per `.claude/CLAUDE.md` "Directives are hypotheses"): if
  the build is **not** green, or any `sorry` is in fact present, do **not**
  bump. Stop and comment with the concrete blocker (file:line of the
  sorry, or the build error), and leave the issue claimable — do not
  weaken the exit criteria or paper over a real gap.

## Verification

- `grep -rnw sorry HexHenselMathlib --include='*.lean'` → no matches.
- `lake build HexHenselMathlib` → builds clean, 0 `error:`.
- `python3 scripts/status.py HexHenselMathlib` → `done_through: 5`,
  advanced to `Phase 6`, `ready: yes`.
- `python3 scripts/status.py` → exit 0, no new blocked regressions.
- `python3 scripts/check_dag.py` → exit 0.
- `git diff` touches only `libraries.yml` (plus the turn's progress file);
  `git diff --check` clean.
- Added-line forbidden-token scan: no `sorry`, `axiom`, `native_decide`,
  `TODO`, `FIXME`.
