## Current state

`scripts/status.py` reports `HexHensel -> Phase 6 (proof polishing)` as
ready. Two HexHensel Phase 6 slices have already landed or are in flight:

- PR #4775 / issue #4772: `HexHensel/Basic.lean` reduction / lift API
  docs and simp surface (landed).
- PR #4790 / issue #4789: `HexHensel/Multifactor.lean` `polyProduct_*`
  rewrite cluster simp surface (in flight).

`HexHensel/Multifactor.lean` (275 lines) splits cleanly into two
disjoint API surfaces: the `Array.polyProduct` algebra cluster covered
by #4789, and the **normalised xgcd + sequential multifactor lift**
cluster covered by this issue. The two surfaces touch disjoint
declarations and can be polished independently; ordering after #4789
only avoids merge conflicts on the same file.

The non-`polyProduct` surface in `HexHensel/Multifactor.lean` is:

- `Hex.ZPoly.normalizedXGCD` (line 27) — Bezout combination scaled to
  be monic when the raw Euclidean gcd is a constant unit. Docstring
  present.
- `Hex.ZPoly.normalizedXGCD_bezout` (line 38) — Bezout identity for
  the normalised gcd component. Docstring present.
- `Hex.ZPoly.normalizedXGCD_liftToZ_bezout_congr_of_gcd_eq_one`
  (line 57) — integer-polynomial Bezout congruence obtained by
  lifting when the normalised gcd equals `1`. Docstring present.
- `Hex.ZPoly.multifactorLiftList` (line 87, private) — recursive
  worker for the sequential multifactor lift. **No docstring.**
- `Hex.ZPoly.multifactorLift` (line 103) — public driver. Docstring
  present.
- `Hex.ZPoly.MultifactorLiftInvariant` (line 115) — recursive
  precondition predicate consumed by the spec. Docstring present.
- `Hex.ZPoly.multifactorLiftList_spec` (line 207, private) —
  recursive correctness lemma feeding `multifactorLift_spec`.
  **No docstring.**
- `Hex.ZPoly.multifactorLift_spec` (line 263) — public correctness
  theorem. Docstring present.

The file currently has zero `@[simp]` annotations in this cluster.
None of these declarations are simp-normal-form candidates by shape
(the `normalizedXGCD_*` statements have side conditions; the
`multifactorLift_*` statements are correctness theorems, not rewrite
targets), so this slice is dominated by docstring coverage on the two
private helpers and a tightening pass on the existing public
docstrings — not by attribute changes.

## Deliverables

1. Add concise caller-facing docstrings to the two undocumented
   private helpers in `HexHensel/Multifactor.lean`:
   - `multifactorLiftList` (line 87): explain that it is the
     recursive list-shape worker behind `multifactorLift`, lifts the
     head factor against the running complementary product via
     `henselLift`, and recurses on the lifted complement.
   - `multifactorLiftList_spec` (line 207): explain that it is the
     induction-on-`factors` correctness statement consumed by
     `multifactorLift_spec`, threading `MultifactorLiftInvariant`
     through each recursive split.

   Both helpers count as "non-obvious private helpers" under
   `SPEC/design-principles.md` (the recursive structure is non-trivial
   and the spec helper has a long statement), so docstrings are
   required by the Phase 6 exit criteria for docstring coverage.

2. Audit the existing public docstrings on `normalizedXGCD`,
   `normalizedXGCD_bezout`,
   `normalizedXGCD_liftToZ_bezout_congr_of_gcd_eq_one`,
   `multifactorLift`, `MultifactorLiftInvariant`, and
   `multifactorLift_spec`. Tighten any docstring that does not
   characterise the result — in particular, the existing
   `MultifactorLiftInvariant` docstring should make explicit that
   the four conjuncts in the `g :: h :: tail` arm correspond to
   (initial state, step-degree, step-Bezout, recursive tail) inputs
   of `henselLift_spec` plus the recursive precondition for the
   lifted complement.

3. Audit `@[simp]` coverage in the same cluster. Expectation: do not
   add `@[simp]` to any declaration in this surface. The
   `normalizedXGCD_*` lemmas carry side conditions
   (`xgcd.gcd = (1 : FpPoly p)` or a primality typeclass on `p`) that
   make them unsuitable as unconditional simp lemmas; the
   `multifactorLift_*` theorems are statements about a congruence
   relation and have no canonical rewrite direction. If the audit
   surfaces an unexpected unconditional canonical-form rewrite
   candidate, document the rationale in the issue PR description
   rather than silently tagging.

4. Preserve behaviour and public API shape: no changes to
   definitions, theorem statements, argument order, namespace
   placement, or proof structure beyond docstrings.

5. Do not introduce `axiom`, `native_decide`, `TODO`, `FIXME`, or new
   theorem-level `sorry`. Do not modify any declaration covered by
   the in-flight #4789 (`polyProduct_*`, `one_mul_zpoly`,
   `list_foldl_mul_eq_mul_foldl_one`); merge with #4789 should be a
   no-op.

## Library placement

Primary file:

- `HexHensel/Multifactor.lean`.

Governing SPEC:

- `SPEC/Libraries/hex-hensel.md`: multifactor linear-Hensel lift
  surface.
- `PLAN/Phase6.md`: proof polishing requires clean API design,
  docstring coverage, and appropriate automation annotations.
- `SPEC/design-principles.md`: "non-obvious private helpers"
  docstring requirement.

Placement answers:

- SPEC section: `hex-hensel` multifactor lift driver.
- Natural strategy uses Mathlib? No; Mathlib-free core API polish.
- Is this already in Mathlib? No; the sequential multifactor lift
  driver is project-specific.
- Missing infrastructure? None expected.

## Context

This is the second of two natural Phase 6 polish slices for
`HexHensel/Multifactor.lean`:

- #4789: `polyProduct_*` simp surface (in flight, PR #4790).
- this issue: `normalizedXGCD` + `multifactorLift` API surface.

After this issue lands, the only HexHensel files outstanding for
Phase 6 polish are `Linear.lean` (1661 lines), `Quadratic.lean` (2296
lines), and `QuadraticMultifactor.lean` (1146 lines). Those are large
enough that they will need further per-section decomposition; this
issue does not attempt that scoping.

The depends-on link to #4789 is only to avoid merge conflicts on the
same file. The two issues touch disjoint declarations and could in
principle land in either order; serialising is the cheaper choice.

## Verification

- `lake build HexHensel.Multifactor`
- `lake build HexHensel`
- `python3 scripts/check_dag.py`
- `git diff --check`
- Compare `rg -n "sorry|axiom|native_decide|TODO|FIXME"
  HexHensel/Multifactor.lean` against `origin/main` and ensure no
  new entries.

## Dependency notes

depends-on: #4789
