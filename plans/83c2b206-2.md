## Current state

The last closed summarize issue is #5018 (forty-third Phase 4/5
checkpoint), which closed at 2026-05-18T15:01:29Z. Since then 27
PRs have merged, well above the planner summarize trigger (10+
PRs merged since the last summarize closed).

The merge window is again entirely thematic in
`HexBerlekampZassenhausMathlib/` (Mathlib-bridge layer). Three
sub-threads dominate:

1. **`zpoly_dvd_trans` cluster closure.** Extracted in #5036,
   extended in #5045, hoisted by #5066, and finished in #5070,
   the helper now routes **38** `Hex.ZPoly`-level divisor-
   transitivity sites in `Basic.lean` (1 def at line 2069 + 38
   uses across the file). The post-#5064/#5066 audit in #5069
   (already commented PASS during this window, issue still open
   to be closed) confirms cluster closure with all three
   invariants passing.

2. **Reverse-direction `isZero = false` plumbing cleared.**
   #5071 (`IntReductionMod.lean:1445`), #5074 (4 inline
   `cases hz : <poly>.isZero with` blocks in `Basic.lean`),
   and #5075 (`IntReductionMod.lean:1500` `by_cases hderiv`
   block) route the dual direction of the `isZero_eq_false_iff`
   plumbing through either `(Hex.DensePoly.isZero_eq_false_iff
   _).mpr ∘ size_pos_of_ne_zero _` or `Bool.eq_false_iff.mpr`.
   The forward direction's residual sites were already cleared
   by #5059 + #5060 (each rewiring 6 inline isZero/size plumbing
   sites onto `(isZero_eq_false_iff _).mp`). Only one remaining
   `isZero = false := by` site survives in the reverse direction
   — `IntReductionMod.lean:1492-1497` (the `hderiv_false`
   derivative-case site) — closed by #5075.

3. **Monic-route cluster consolidation and extension.**
   - #5026 extracts `zpoly_normalize_factor_sign_of_monic` and
     rewires 2 inline destructure sites.
   - #5025 / #5028 harmonise 7 + 2 `zpoly_ne_zero_of_monic` /
     `zpoly_ne_zero_of_pos_lc` `IntReductionMod` call sites to
     the implicit-`f` form (post-#5001 + post-#5028 idiom).
   - #5029 rewires 3 inline triple-destructure sites at
     `Basic.lean:10083 / 10193 / 13595` onto Monic-route
     cluster helpers.
   - #5042 / #5049 perform two `zpoly_size_pos_of_monic` hoists
     that expose the helper to the
     `monicModularImage_modP_eq_of_monic` body (collapsing a
     9-line `by_contra` proof at `Basic.lean:5338`) and to the
     `monic_primitive_sign_normalized_of_monic` body (collapsing
     a 13-line `hcs_pos` inline proof at `Basic.lean:4479`).
   - #5058 extracts `zpoly_size_pos_of_pos_lc` (sibling of
     `_of_monic`) and rewires 2 inline pos_lc-source size_pos
     sites at `Basic.lean:8214 / 8425`.

Five audit issues / feasibility studies landed during the
window: #5032 (post-#4991 cascade audit), #5043 (hoist
feasibility for `monic_primitive_sign_normalized_of_monic`
neighbourhood, deferred from #5038), #5054 (cluster inversion
feasibility for issue #5050), #5062 (post-#5045 audit), #5063
(post-#5052 audit). All five PASS. The in-flight audit on #5069
(post-#5064/#5066 zpoly_dvd_trans closure) also posted a PASS
verdict in the audit comment during this window.

Two documentation touches: #5033 (3 missing Monic-route helper
docstrings added) and #5051 (reframe
`normalizeForFactor_squareFreeCore_primitive_of_ne_zero` +
drop a stale `(#4611)` PR-metadata leak in helper-3's
`IntReductionMod` docstring).

Warning suppression: #5020 (5 unused-warning sites in `_of_bound`
Basic.lean signatures).

Progress entry housekeeping: #5061 (post-#5053 read-only audit
session log).

The cluster-extension pattern continues to be **productive**:
five new helpers entered the cascade idiom (`zpoly_dvd_trans`,
`zpoly_normalize_factor_sign_of_monic`,
`zpoly_size_pos_of_pos_lc`,
`normalizeForFactor_squareFreeCore_mul_repeatedPart_primitive_of_ne_zero`,
the pre-window cluster's two non-shipped helpers) routed 38 + 2
+ 2 + 3 = 45 inline boilerplate sites across the two BZMathlib
files this window.

Recent landings (chronological order, 27 PRs):

(Worker will list these inline by re-running the query at
issue execution time.)

## Deliverables

Produce the **forty-fourth Phase 4/5 checkpoint summary**. The
summary issue should include:

1. **Headline narrative.** The `zpoly_dvd_trans` cluster reached
   closure during this window: 38 sites routed through a single
   helper, 5 audit issues PASS, one in-flight cluster-closure
   audit (#5069) also posted PASS. Reverse-direction
   `isZero = false` plumbing cleanup wrapped up across both
   `Basic.lean` and `IntReductionMod.lean`. The Monic-route
   cluster's two hoists (#5042, #5049) expose
   `zpoly_size_pos_of_monic` to the
   `monic_primitive_sign_normalized_of_monic` neighbourhood for
   the first time, opening that area for further consolidation
   downstream.

2. **Per-sub-thread bullets** (one per sub-thread above):

   * **zpoly_dvd_trans closure**: #5036 (extraction + 3 initial
     rewires), #5045 (+6 more), #5066 (hoist + 3 calc-form),
     #5070 (+26 rcases-form). Audit pair #5062 / #5069 confirms
     PASS at 38 sites with no remaining inline
     `Hex.ZPoly`-level transitivity sites.
   * **Reverse-direction isZero=false plumbing**: #5071, #5074,
     #5075 close out `Basic.lean` and `IntReductionMod.lean`
     reverse-direction sites. The forward direction landed in
     this window via #5059 / #5060.
   * **Monic-route consolidation**: hoists #5042 / #5049, new
     siblings #5026 / #5058, harmonisations #5025 / #5028, and
     downstream rewires #5022 / #5029. Cluster footprint is
     stable: 4 `_of_monic` siblings (size_pos / ne_zero /
     lc_pos / primitive) + 2 `_of_pos_lc` siblings + 1
     `normalize_factor_sign_of_monic` + the helper bundle's
     `monic_primitive_sign_normalized_of_monic` rewire surface.

3. **Audit pattern observation.** The audit issues all use the
   same three-invariant template (call-site coverage / framing /
   cross-file consistency) introduced by #5031. Of the 6 audit
   issues in this window (#5032 / #5043 / #5054 / #5062 /
   #5063 / #5069), 5 PASS verbatim, 1 (the #5048 audit) reports
   DEVIATION on 26 + 3 missed transitivity sites — both of
   which were subsequently closed by #5070 / #5066. The
   `zpoly_dvd_trans` cluster's closure path is the
   pattern's first end-to-end success: file → audit DEVIATION →
   file follow-ups → re-audit PASS.

4. **In-flight at issue creation time:**

   * PR #4917 (closing #4902) still `repair-claimed` in
     `CONFLICTS`. Blocks the monic + primitive_pos_lc_core
     `_of_bound` substrate siblings (#4918, #4919, #4924, #4925,
     #4927, #4929, #4931, #4934, #4951). Recompute the
     dependent chain at execution time via `coordination orient`
     / `gh issue list --label blocked`.
   * #5069 audit PASS comment posted; issue still open to be
     closed. The audit's "Out-of-scope observation" drafts an
     `fpPoly_dvd_trans` analogue cluster, picked up by a fresh
     issue in this planner cycle.

5. **HO-1 critical-path commentary.** The BZ Mathlib-free chain
   (capstone #4170, sub-capstone #4825) remains blocked on
   #4680 (analytic obligation composition) and #2567 (directive).
   This window's helper-extraction / plumbing-cleanup
   contributes infrastructure consolidation in
   `HexBerlekampZassenhausMathlib/Basic.lean` (Mathlib-bridge
   layer) — it does not change the directive-blocked status of
   #2564 / #2567 / #2637 nor unblock anything on the executable
   side. The cascade continues to provide useful planner-queue
   work without competing with directive-level threads.

6. **#4880 umbrella drop posture.** Helpers 1 (#4945) and 5
   (#4953) substrate remain closed; helpers 2/3/4 remain
   architecturally infeasible per the #4940 audit. No window
   activity altered this posture. The Monic-route cluster's
   continued extension sits at the Mathlib-bridge layer, not
   at the umbrella's Gap-1 / Gap-3 surface.

## Context

Per the project's CLAUDE.md, this summary feeds Phase 4/5
checkpoint tracking. It is a journal-style review of the merge
window, not a roadmap update — `SPEC/` and `PLAN.md` remain the
single source of truth on the current design state. Don't
duplicate planning content into the summary; instead, link out
to the relevant open issues and SPECs.

Files touched: this should be a single comment posted on the
created summarize issue (or the issue body itself, depending on
the writer's preference); no source-tree files are edited.

## Verification

* The summary comment / issue body must list exactly the PRs in
  the merge window (recompute at execution time):

  ```
  gh pr list --state merged --limit 30 \
      --json mergedAt,number,title \
      --jq '.[] | "\(.mergedAt) #\(.number) \(.title)"' \
    | awk '$1 > "2026-05-18T15:01:29Z"' \
    | awk '$1 <= "<creation-timestamp>"'
  ```

  Headline estimate at issue creation: 27 PRs. If any additional
  PRs merge between this issue's creation and the worker
  picking it up, list those too.

* The summary identifies the `zpoly_dvd_trans` cluster closure
  arc (extraction → rewires → hoist → audit DEVIATION →
  follow-ups → re-audit PASS) as the headline.

* The summary references all five audit issues that PASS plus
  the one DEVIATION/follow-up cycle for #5048 → #5070 /
  #5066 → #5069.

* The summary does NOT advance roadmap state, does NOT change
  `libraries.yml`, does NOT alter SPEC files.

* The summary does NOT speculate about future work beyond the
  in-flight / queued items already filed (e.g., the
  `fpPoly_dvd_trans` follow-up filed alongside this issue —
  reference it by its issue number once available).

## Out of scope

- Any planning / roadmap changes.
- Any edits to source-tree `.lean` files.
- Any changes to `SPEC/` or `PLAN.md`.
- The audit pattern itself (don't rewrite the three-invariant
  template; this is journal-style commentary on the pattern's
  outcomes, not pattern revision).
- Re-auditing any cluster (audit issues are filed separately,
  not part of summarize work).
