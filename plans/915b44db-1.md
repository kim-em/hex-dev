## Current state

PR #4497 closed #4457, the #4430 capstone, by adding two declarations to
`HexBerlekampZassenhausMathlib/Basic.lean`:

- `not_represents_empty_of_irreducible_dvd_core` (private helper) at
  `HexBerlekampZassenhausMathlib/Basic.lean:4891`, ~110 lines including
  docstring. Dispatches the `S = ∅` subcase of the squarefreeness
  contradiction in the capstone — collapses
  `centeredLiftPoly (scaledLiftedFactorProduct core d ∅) (d.p^d.k)` to
  `centeredLiftPoly 1 (d.p^d.k)` under monic core, derives
  `2 ≤ d.p^d.k` from `factor ≠ 0`, then forces `factor = 1` via
  `centeredModNat_one_of_two_le` and contradicts `Irreducible 1`.
- `representedFactor_dvd_recombinationCandidate_of_subset` (the capstone)
  at `HexBerlekampZassenhausMathlib/Basic.lean:5002`, ~140 lines. Splits
  on `toPolynomial factor`'s prime divisibility of
  `toPolynomial quotient * toPolynomial candidate`: Case A transports
  back to `Hex.ZPoly` via `HexPolyZMathlib.equiv.injective`; Case B
  splits on `S = ∅` (delegates to the new helper) vs. `S ≠ ∅` (picks
  `i ∈ S ⊆ T`, applies #4469's
  `mem_T_iff_exists_irreducibleFactor_representingSubset`, then uses
  `pairwise_disjoint` contrapositively to force `factor² ∣ target` and
  contradict `target_squarefree`).

Now that the capstone has landed, the cluster warrants a focused review
pass following the same pattern as the recent HexGfqRing Phase 6
reviews (#4484, #4492, #4493, #4495). PR #4497 introduced 318 lines in
a single feature push; the review is the cleanup half of the
implementation/review pair.

## Deliverables

A single PR against `main` that does *only* the following, all confined
to `HexBerlekampZassenhausMathlib/Basic.lean` between L4878 and L5138:

1. Polish or tighten proof steps in
   `not_represents_empty_of_irreducible_dvd_core` and
   `representedFactor_dvd_recombinationCandidate_of_subset` where
   simplification is clearly possible — combining sequential `rw [..]`
   blocks, dropping `simpa` in favour of `exact` when the goal is
   literally the lemma statement, removing helper `change` lines that
   are redundant with the next tactic, and similar narrow polish. Both
   declarations construct `hd_modulus : 2 ≤ d.p ^ d.k` from the
   centered-lift recovery in the same way (cf. L5056–L5080 of the
   capstone and L4923–L4945 of the helper); if the duplication is
   identical up to renaming, leave it as-is — extracting a shared
   helper is out of scope unless the duplication is literally the same
   block.
2. Audit and improve the two existing docstrings. Each currently
   describes the proof structure at a high level; verify the
   description matches the implementation, cites the right neighbouring
   API (#4469's `mem_T_iff_exists_irreducibleFactor_representingSubset`,
   the `pairwise_disjoint` / `target_squarefree` partition fields, the
   recovery equation from
   `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`),
   and does not exceed what a future reader needs in order to navigate
   the proof.
3. Audit naming consistency with surrounding declarations
   (`representedFactor_*`, `representingSubset_*`,
   `RepresentsIntegerFactorAtLift`,
   `exists_representingSubset_of_mem_T_of_recombinationCandidate_dvd`,
   etc.). If the empty-support helper has an obviously better name in
   context, rename it (and update the single call site at L5083);
   otherwise leave it.

Stop after these three. Do **not** rewrite proof structure, do **not**
generalise either declaration's signature, do **not** touch other
declarations in the file, and do **not** introduce new lemmas to factor
out shared subproofs unless an identical block appears within the
cluster itself.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`
- `lake build HexBerlekampZassenhausMathlib`
- `python3 scripts/check_dag.py`
- `git diff --check`
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|^\s*sorry\b'
  HexBerlekampZassenhausMathlib/Basic.lean` returns only the two
  pre-existing file-level `sorry`s at L162 and L241.

## Out of scope

- The pre-existing `sorry`s at L162 and L241.
- Downstream successors that consume the capstone (#4274, #4301, #4367,
  #4006, #4008, #4170, #4172, #3969, #3970, #3987). Their work-items
  remain blocked on their own chains, not on this review.
- Editing `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
- Any change outside `HexBerlekampZassenhausMathlib/Basic.lean`.

## Context

- PR #4497: the capstone landing the cluster under review.
- Issue #4457: the capstone work item.
- Parents: #4430 (the capstone work-stream), #4469 (the `mem_T_iff_*`
  API used in Case B of the capstone proof).
- Recent precedent for review scope and proof-polish style: #4484
  (natCast), #4492 (nsmul/intCast/zsmul), #4493 (final Lean.Grind),
  #4495 (pow/linearPow).
