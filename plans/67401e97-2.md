## Current state

PR #4878 closed #4873 by landing the bottom three `_of_bound` siblings
of the A2 mignotte-precision recovery chain in
`HexBerlekampZassenhausMathlib/Basic.lean`, each accepting an abstract
bound `B' : Nat` plus a validity hypothesis
`hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'` and a precision hypothesis
`2 * B' < d.p ^ d.k`, in place of the core-shape
`2 * defaultFactorCoeffBound core < d.p ^ d.k`.

Two private utility theorems at this layer still carry the core-shape
precision hypothesis and each call
`centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`
directly:

* `not_represents_empty_of_irreducible_dvd_core` at
  `HexBerlekampZassenhausMathlib/Basic.lean:11012` — monic flavour.
  Its proof calls the recovery lemma at line 11028.
* `not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core`
  at `HexBerlekampZassenhausMathlib/Basic.lean:11102` — primitive +
  positive-leading flavour. Its proof calls the recovery lemma at
  line 11119.

Neither theorem touches the recovery-candidate level (no call to
`recombinationCandidate_eq_factor_of_recovery_of_monic_core` or
`scaledRecombinationCandidate_eq_factor_of_recovery`), so the
abstract-bound siblings depend only on the already-landed bottom-level
`_of_bound` sibling (`centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`)
from PR #4878 — independent of #4882 / #4883.

Both theorems are consumed downstream by:

* `representedFactor_dvd_recombinationCandidate_of_subset` (line 11250+)
  — uses the monic flavour at line 11330.
* `recombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition`
  (line 12117+) — uses the monic flavour at line 12378.
* `scaledRecombinationSearchModAux_*` (line ~12556+) — uses the
  primitive flavour at line 12665.

The next layer up (`representedFactor_dvd_*` and `recombinationSearchModAux_*`)
will need abstract-bound siblings of these utilities to thread the
abstraction down to the recovery layer without re-deriving the empty
subset contradiction at every call site. This issue lands the abstract-
bound siblings of both empty-subset utility theorems together so the
next-layer propagation is unblocked from a single PR.

depends-on: #4873

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add abstract-bound
siblings alongside the two existing private theorems, and rewire the
existing theorems as thin wrappers that delegate into the new variants.

1. **`not_represents_empty_of_irreducible_dvd_core_of_bound`** (sibling
   of `not_represents_empty_of_irreducible_dvd_core`):

   ```lean
   private theorem not_represents_empty_of_irreducible_dvd_core_of_bound
       {core factor : Hex.ZPoly} {d : Hex.LiftData}
       (B' : Nat)
       (hvalid : ∀ i, (factor.coeff i).natAbs ≤ B')
       (hcore_ne : core ≠ 0)
       (hcore_monic : Hex.DensePoly.Monic core)
       (hfactor_dvd : factor ∣ core)
       (hfactor_irr : Irreducible (HexPolyZMathlib.toPolynomial factor))
       (hprecision : 2 * B' < d.p ^ d.k) :
       ¬ RepresentsIntegerFactorAtLift core d factor
         (∅ : LiftedFactorSubset d)
   ```

   Proof body mirrors the existing theorem (lines 11022–11088) with one
   call-site replacement: replace
   `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery
      hcore_ne hfactor_dvd hrep hprecision`
   (line 11028) with the `_of_bound` sibling
   `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound
      B' hvalid hcore_ne hfactor_dvd hrep hprecision`.
   All other lines unchanged.

2. **`not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core_of_bound`**
   (sibling of
   `not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core`):

   ```lean
   private theorem not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core_of_bound
       {core factor : Hex.ZPoly} {d : Hex.LiftData}
       (B' : Nat)
       (hvalid : ∀ i, (factor.coeff i).natAbs ≤ B')
       (hcore_ne : core ≠ 0)
       (hcore_primitive : Hex.ZPoly.Primitive core)
       (hcore_lc_pos : 0 < Hex.DensePoly.leadingCoeff core)
       (hfactor_dvd : factor ∣ core)
       (hfactor_irr : Irreducible (HexPolyZMathlib.toPolynomial factor))
       (hprecision : 2 * B' < d.p ^ d.k) :
       ¬ RepresentsIntegerFactorAtLift core d factor
         (∅ : LiftedFactorSubset d)
   ```

   Proof body mirrors the existing theorem (lines 11113–11217) with the
   analogous single call-site replacement at line 11119, threading the
   abstract bound through the recovery lemma.

   **Note**: the existing proof at line 11163–11172 derives
   `hcore_lc_bound : (lc core).natAbs ≤ defaultFactorCoeffBound core`
   from `defaultFactorCoeffBound_valid` and then feeds it into
   `centeredModNat_eq_of_pos_natAbs_le hcore_lc_pos hcore_lc_bound hprecision`
   at line 11188. In the `_of_bound` variant, this derivation must
   produce `(lc core).natAbs ≤ B'` instead; the cleanest route is to
   *not* assume `hvalid` directly bounds `lc core` (since `core` and
   `factor` are different polynomials here) but to require an
   additional explicit hypothesis `hcore_lc_bound : (lc core).natAbs ≤ B'`
   on the abstract sibling. Mirror this in the wrapper by
   discharging it from
   `defaultFactorCoeffBound_valid core hcore_ne core ⟨1, ...⟩ (core.size - 1)`
   as the original does at lines 11166–11172. If the worker finds a
   cleaner formulation that lets `hvalid` discharge `hcore_lc_bound`
   without thresholding through `defaultFactorCoeffBound`, they may use
   it; otherwise the explicit extra hypothesis is acceptable.

3. Rewire the existing two theorems as thin wrappers that instantiate
   `B' := Hex.ZPoly.defaultFactorCoeffBound core`, discharge `hvalid`
   via `defaultFactorCoeffBound_valid core hcore_ne factor hfactor_dvd`,
   and (for the primitive variant) discharge the leading-coefficient
   bound the same way the original does. The existing signatures stay
   identical; only the bodies change.

Do **not** propagate the abstraction further at this issue. The
next-layer consumers `representedFactor_dvd_recombinationCandidate_of_subset`
and `recombinationSearchModAux_*` are separate follow-up issues — this
PR just lands the empty-subset utility siblings.

No `axiom`, `sorry`, or `native_decide`. No new public API (both new
theorems are `private`).

## Context

Parent abstract-bound chain: #4873 (closed by PR #4878). Sibling
follow-ups already in flight: #4890 (PR #4894 open), #4891, #4892.

Relevant code:

- `HexBerlekampZassenhausMathlib/Basic.lean:11012` —
  `not_represents_empty_of_irreducible_dvd_core` (monic).
- `HexBerlekampZassenhausMathlib/Basic.lean:11102` —
  `not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core`
  (primitive + positive-leading).
- `HexBerlekampZassenhausMathlib/Basic.lean:3992` (approx) —
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`,
  the already-landed bottom-level `_of_bound` sibling.
- `HexBerlekampZassenhausMathlib/Basic.lean:68` —
  `defaultFactorCoeffBound_valid`, the validity discharger used by the
  existing-wrapper rewiring.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`.
- `lake build HexBerlekampZassenhausMathlib`.
- `python3 scripts/check_dag.py`.
- `git diff --check`.
- `git diff origin/main HexBerlekampZassenhausMathlib/Basic.lean |
  grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
