## Current state

Several recently-merged PRs assembled the BHKS *fast raw irreducibility*
soundness core on the critical path of directive #2564 (the van Hoeij CLD
rewrite). It now stands as a thin assembly with no review pass:

- `factorFastFactorsWithBound_raw_zpolyIrreducible_of_recoveredLift`
  (`HexBerlekampZassenhausMathlib/PartitionRefinement.lean`, near line 2012)
  — the fast-core success disjunct: a successful
  `factorFastFactorsWithBound` dispatch plus a fast-core run plus the
  per-support `BHKS.RecoveredLift` family yields `Hex.ZPoly.Irreducible`
  for every recorded raw factor.
- It routes through `rawIrreducible_of_cut`
  (`PartitionRefinement.lean`, near line 1926) for the count-equality /
  UFD-partition / `reassemblyExpansionComplete` plumbing.
- The cut hypotheses are produced by
  `BHKS.cutProjectionHypotheses_of_recoveredLift`
  (`HexBerlekampZassenhausMathlib/CLDColumnBound.lean`, near line 2132)
  and the lattice basis is pinned to
  `BHKS.latticeBasisOfLiftData core (toMonicLiftData core B primeData)`,
  claimed defeq to the `projectedRowsOfLiftData` data that
  `rawIrreducible_of_cut` consumes (so `trueSupports` indices are said to
  align by defeq with no bridge lemma).

This is exactly the kind of assembly where a soundness defect is most
dangerous: the headline `factor_irreducible_of_nonUnit` (#8068) will
consume it, and the whole directive #2564 exists because the *previous*
recombination "implemented the wrong lattice." A vacuous side-condition,
a misaligned `trueSupports` index, or a defeq that silently holds for the
wrong reason would let a false irreducibility claim pass CI.

## Deliverables

A review report (posted as the issue's closing comment, and, for any
actionable defect, a short follow-up `feature` issue or a code comment
patch) covering:

1. **Soundness of the cut bridge.** Verify the claimed defeq alignment
   between `latticeBasisOfLiftData` / `projectedRowsOfLiftData` and the
   `trueSupports` indexing actually holds and is not an accidental
   coincidence of `getD`/default values. Confirm no hypothesis of
   `rawIrreducible_of_cut` or `cutProjectionHypotheses_of_recoveredLift`
   is vacuously satisfiable (e.g. an empty-quantifier or
   always-`none`/always-`0` discharge) in a way that would admit a
   non-canonical witness.
2. **Hypothesis tightness.** Check that the `RecoveredLift` side
   conditions (monicity, primality, Hensel precision, threshold,
   per-index congruence) are each genuinely used and that none silently
   weakens the conclusion. Flag any hypothesis that is stronger than the
   downstream consumer (#8068) can actually supply at default precision.
3. **No hidden `sorry`/`axiom`/`native_decide`** anywhere in the
   transitive proof term of the two theorems, and confirm the assembly
   does not re-open or duplicate BHKS cut/partition obligations that
   were closed elsewhere.

If the assembly is sound, say so explicitly with the evidence that
convinced you (the defeq chain checked, the discharged hypotheses traced
to real witnesses). If it is not, follow the directive doctrine: report
the concrete defect (counterexample or missing-bridge shape) rather than
papering over it.

## Context

Parent directive: #2564 (HO-1: rewrite to van Hoeij CLD). Assembled by
PRs #8073, #8071, #8069, #8060, #8059 and the #8058 follow-up. Downstream
consumer: #8068 (`factor_irreducible_of_nonUnit`). Read the
`hex-lean-mathlib-boundary` skill before reasoning about the
Mathlib-free / Mathlib boundary in these files.

Review-only: do not change executable factorization behaviour. Any fix
that is more than a comment or a one-line hypothesis tightening should
be spun out as its own `feature` issue rather than landed here.

## Verification

- `lake build HexBerlekampZassenhausMathlib.PartitionRefinement` and
  `…CLDColumnBound` — confirm the reviewed theorems still elaborate
  (baseline; the review changes no proof bodies).
- `python3 scripts/check_dag.py` — exit 0.
- The closing comment names each theorem reviewed and gives a per-
  deliverable verdict (sound / defect-with-evidence).
