## Current state

PR #4878 closed #4873 by landing the bottom three `_of_bound` siblings
of the A2 mignotte-precision recovery chain in
`HexBerlekampZassenhausMathlib/Basic.lean`, each accepting an abstract
bound `B' : Nat` plus a validity hypothesis
`hvalid : ∀ i, (factor.coeff i).natAbs ≤ B'` and a precision hypothesis
`2 * B' < d.p ^ d.k`, in place of the core-shape
`2 * defaultFactorCoeffBound core < d.p ^ d.k`.

PR #4888 then closed #4883 by landing the monic-side recovery-candidate
`_of_bound` sibling:

* `recombinationCandidate_eq_factor_of_recovery_of_monic_core_of_bound`

PR #4889 landed the scaled side (#4882).

The next-layer monic capstone that uses the recovery-candidate identity
is `natDegree_toPolynomial_eq_sum_of_represents` at
`HexBerlekampZassenhausMathlib/Basic.lean:7699`. Its proof has two
recovery-style steps:

* `hrec_eq` (line 7716) — calls
  `recombinationCandidate_eq_factor_of_recovery_of_monic_core`. Used at
  line 7751 (`rw [← hrec_eq]`).
* `hcenter` (line 7726) — calls
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery`
  (the bottom-level lemma). Used at line 7744 in the `hpk_ge_two`
  promotion.

Both callees now have `_of_bound` siblings landed, so the abstract-bound
sibling of `natDegree_toPolynomial_eq_sum_of_represents` is unblocked.
The parallel primitive sibling
`natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core`
is already in flight as #4892 (claimed). This issue files the monic
sibling — symmetric to #4892 but threading through the monic recovery
chain (#4883/#4888) rather than the dead-code drop pattern that the
primitive variant could use.

depends-on: #4873, #4883

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add an abstract-bound
sibling alongside the existing `natDegree_toPolynomial_eq_sum_of_represents`
at line 7699, and rewire the existing theorem as a thin wrapper that
delegates into the new variant.

1. **`natDegree_toPolynomial_eq_sum_of_represents_of_bound`** (sibling
   of `natDegree_toPolynomial_eq_sum_of_represents`):

   ```lean
   theorem natDegree_toPolynomial_eq_sum_of_represents_of_bound
       {core factor : Hex.ZPoly} {d : Hex.LiftData}
       {S : LiftedFactorSubset d}
       (B' : Nat)
       (hvalid : ∀ i, (factor.coeff i).natAbs ≤ B')
       (hcore_ne : core ≠ 0)
       (hcore_monic : Hex.DensePoly.Monic core)
       (hd_liftedFactor_monic :
         ∀ i, Hex.DensePoly.Monic (liftedFactor d i))
       (hdvd : factor ∣ core)
       (hfactor_irr : Irreducible (HexPolyZMathlib.toPolynomial factor))
       (hfactor_prim : Hex.ZPoly.content factor = 1)
       (hfactor_norm : Hex.normalizeFactorSign factor = factor)
       (hrep : RepresentsIntegerFactorAtLift core d factor S)
       (hprecision : 2 * B' < d.p ^ d.k) :
       (HexPolyZMathlib.toPolynomial factor).natDegree =
         ∑ i ∈ S,
           (HexPolyZMathlib.toPolynomial (liftedFactor d i)).natDegree
   ```

   Proof body mirrors the existing theorem (lines 7715–7753) with two
   call-site replacements:

   * Replace
     `recombinationCandidate_eq_factor_of_recovery_of_monic_core
        hcore_ne hcore_monic hdvd hfactor_prim hfactor_norm hfactor_irr
        hrep hprecision`
     (line 7717) with
     `recombinationCandidate_eq_factor_of_recovery_of_monic_core_of_bound
        B' hvalid hcore_ne hcore_monic hdvd hfactor_prim hfactor_norm
        hfactor_irr hrep hprecision`.
     (Pass arguments in whichever order the new sibling expects; mirror
     #4888's signature.)

   * Replace
     `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery
        hcore_ne hdvd hrep hprecision`
     (line 7728) with
     `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound
        B' hvalid hcore_ne hdvd hrep hprecision`.

   All other lines are unchanged.

2. Rewire the existing `natDegree_toPolynomial_eq_sum_of_represents` as
   a thin wrapper that instantiates
   `B' := Hex.ZPoly.defaultFactorCoeffBound core` and discharges
   `hvalid` via `defaultFactorCoeffBound_valid core hcore_ne factor hdvd`.
   The existing signature stays identical; only the body changes.

Do **not** propagate the abstraction further at this issue. The next
propagation layer (`recombinationSearchModAux_*` / etc.) is a separate
follow-up; this PR strictly mirrors the #4890/#4891/#4892 pattern of
abstracting one layer at a time.

No `axiom`, `sorry`, or `native_decide`. No new public API beyond the
one sibling theorem.

## Context

Parent abstract-bound chain: #4873 (closed by PR #4878), #4883 (closed
by PR #4888). Sibling follow-ups already in flight: #4890 (PR #4894 open),
#4891, #4892.

Relevant code:

- `HexBerlekampZassenhausMathlib/Basic.lean:7699` —
  `natDegree_toPolynomial_eq_sum_of_represents`, the existing
  core-shape theorem.
- `HexBerlekampZassenhausMathlib/Basic.lean` —
  `recombinationCandidate_eq_factor_of_recovery_of_monic_core_of_bound`,
  the already-landed monic recovery-candidate `_of_bound` sibling
  (from #4888); search for the exact line in the current tree.
- `HexBerlekampZassenhausMathlib/Basic.lean:3992` (approx) —
  `centeredLiftPoly_scaledLiftedFactorProduct_eq_factor_of_recovery_of_bound`,
  the already-landed bottom-level `_of_bound` sibling (from #4878).
- `HexBerlekampZassenhausMathlib/Basic.lean:68` —
  `defaultFactorCoeffBound_valid`, the validity discharger used by
  the existing-wrapper rewiring.

## Verification

- `lake build HexBerlekampZassenhausMathlib.Basic`.
- `lake build HexBerlekampZassenhausMathlib`.
- `python3 scripts/check_dag.py`.
- `git diff --check`.
- `git diff origin/main HexBerlekampZassenhausMathlib/Basic.lean |
  grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
