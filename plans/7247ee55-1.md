## Current state

The audit of `zpoly_dvd_trans` rewires landed by #5036 + #5045
(issue #5048, comment
[#5048#issuecomment-4480680980](https://github.com/kim-em/hex/issues/5048#issuecomment-4480680980)
plus the post-#5058 rebase addendum) found two clusters of inline
divisor-transitivity sites in
`HexBerlekampZassenhausMathlib/Basic.lean` that the #5036/#5045
rewire wave did not yet route through the helper:

- **26 inline `rcases`/`refine`/`rw` sites** in `Basic.lean`
  (the rcases-form cluster), targeted by issue #5064 (claimed at
  audit-issue-write time, expected to land as a PR closing #5064
  before this audit fires).
- **3 calc-form transitivity sites** in the
  `henselSubsetCorrespondenceRest` family at the pre-hoist
  `Basic.lean:2090-2284` neighbourhood. Routing these onto the
  helper required hoisting `zpoly_dvd_trans` from its prior
  location (post-#5058: `Basic.lean:6759`) to above the
  `henselSubsetCorrespondenceRest_transport_of_disjoint`
  declaration. PR #5066 (closes #5065, merged as `dc2ba756`)
  performed the hoist to `Basic.lean:2076` and rewired the 3
  calc-form sites at the post-hoist neighbourhoods
  `Basic.lean:2103 / 2113 / 2275`.

After both #5064 and #5066 have landed,
`HexBerlekampZassenhausMathlib/Basic.lean` should contain:

- **One** `private theorem zpoly_dvd_trans` definition at
  `Basic.lean:2076` (modulo line-shift after #5064 lands).
- **38** call-site invocations (3 from #5036 + 6 from #5045 + 3
  from #5066 + 26 from #5064 = 38).

This issue audits the post-#5064 / post-#5066 state of the
`zpoly_dvd_trans` cluster against the same three invariants
applied to the Monic-route cluster in #5031 / #5039, to the
`normalizeForFactor` primitivity micro-cluster in #5041, to the
post-#5045 baseline in #5048, and to the `_size_pos` cluster pair
in #5053 / #5057:

1. **Call-site coverage.** Every site in `Basic.lean` and
   `IntReductionMod.lean` that derives `a ∣ c` from `a ∣ b` and
   `b ∣ c` (executable `Hex.ZPoly`-level divisibility, *not*
   Mathlib `Polynomial`-level) routes through `zpoly_dvd_trans`
   rather than inlining the `obtain`/`refine`/`mul_assoc_poly`
   chain or the calc-form `_ := …trans…` chain. Both shapes
   flagged in the #5048 audit are in scope:
   - `rcases`/`refine`/`rw` form (26 sites #5064 targets);
   - calc-form `(_ : a ∣ c) := …` chains in the
     `henselSubsetCorrespondenceRest` family (3 sites #5066
     targets).
2. **Framing, docstring, and hoist correctness.** The hoist
   preserved the post-#5048 `zpoly_dvd_trans` docstring verbatim
   (contract-first sentence + executable-vs-Mathlib semantic
   note about the bespoke `Dvd` instance), kept `private`
   visibility, and parked the helper at the *minimal* position
   needed to expose it to the calc-form callers at the
   `henselSubsetCorrespondenceRest` family — i.e., the
   immediately-preceding declaration is one whose body does
   not need to mention `zpoly_dvd_trans` and the
   immediately-following declaration is the first calc-form
   caller's enclosing theorem (or a declaration immediately
   above it).
3. **Cross-file consistency.** `zpoly_dvd_trans` is defined
   exactly once across `Basic.lean`, `IntReductionMod.lean`,
   and `Recovery.lean` (no peer/duplicate/shadow).

The audit is read-only: a single comment on this issue
containing the three invariant verdicts with verbatim grep
commands and expected-vs-observed counts. It mirrors the
structure of the #5048 / #5053 / #5057 audit comments.

## Deliverables

Post a single audit comment on this issue with three sections.

1. **Invariant 1 (call-site coverage).** Run at least these
   greps from the repository root and report each output
   verbatim:

   ```
   grep -nE "obtain .*hu.*:= .*_dvd_" HexBerlekampZassenhausMathlib/*.lean
   grep -nE "refine .*⟨.*\* .*, \?_⟩" HexBerlekampZassenhausMathlib/*.lean
   grep -nE "Hex\.DensePoly\.mul_assoc_poly \(S := Int\)" \
        HexBerlekampZassenhausMathlib/*.lean
   grep -nE "rw \[.*\]; rw \[.*\]; exact .*mul_assoc_poly" \
        HexBerlekampZassenhausMathlib/*.lean
   git grep -n "zpoly_dvd_trans" HexBerlekampZassenhausMathlib/Basic.lean
   git grep -nE "_ \∣ _ := .*\.trans " HexBerlekampZassenhausMathlib/Basic.lean
   ```

   For every `mul_assoc_poly` / `_dvd_*` / calc-form match,
   classify as one of:
   - **routed** — already `zpoly_dvd_trans` (post-#5036 /
     post-#5045 / post-#5066 / post-#5064 rewire);
   - **helper-internal** — the single `exact mul_assoc_poly`
     inside the body of `zpoly_dvd_trans` itself at the
     post-hoist `Basic.lean:2076` neighbourhood;
   - **non-transitivity** — `mul_assoc_poly` or calc-form
     `.trans` used in a context that is *not* a divisor-
     transitivity derivation (e.g., as part of a different
     algebraic rewrite, or `Eq.trans` on a non-divisibility
     statement). Show enclosing-theorem name and a one-line
     description of the surrounding intent so the reader can
     confirm the classification;
   - **missed transitivity site** — inline `a ∣ b ∧ b ∣ c →
     a ∣ c` proof (either shape) that should have been rewired
     but was not. For each such site, list line number,
     enclosing theorem, exact inline shape, and the projected
     one-line `zpoly_dvd_trans …` rewire. Use the same draft-
     issue-body format as #5048's deviation report so the next
     planner cycle can lift it verbatim.

   Report the post-#5064 + post-#5066 expected counts:
   - 1 definition + 38 rewired call sites = **39 occurrences**
     of `zpoly_dvd_trans` in `Basic.lean`.
   - 0 occurrences in `IntReductionMod.lean` and `Recovery.lean`.
   - 0 remaining inline divisor-transitivity sites of either
     shape (rcases-form or calc-form).

2. **Invariant 2 (framing, docstring, and hoist correctness).**
   Open `HexBerlekampZassenhausMathlib/Basic.lean` at the
   `zpoly_dvd_trans` declaration site (post-hoist
   `Basic.lean:2076` neighbourhood) and verify:
   - The docstring matches the post-#5048 audited form
     verbatim: contract-first sentence stating the executable
     `Hex.ZPoly`-level divisor-transitivity claim, with the
     bespoke-`Dvd`-instance semantic note preserved.
   - No `(#NNNN)` PR-number references, no session metadata,
     no "hoisted from `Basic.lean:6759` …" / "extracted from …"
     provenance hints in the docstring or surrounding comments.
   - The declaration is `private theorem` (not `theorem`).
   - The hoist target is the minimal position needed to expose
     the helper to the three calc-form callers at the post-hoist
     `Basic.lean:2103 / 2113 / 2275` neighbourhoods. The audit
     comment should name the immediately-preceding and
     immediately-following declarations to document the
     cluster's new neighbours, and confirm that the
     immediately-preceding declaration's body does not need to
     mention `zpoly_dvd_trans` (i.e., the hoist isn't gratuitous).

3. **Invariant 3 (cross-file consistency).** Run:

   ```
   git grep -c "zpoly_dvd_trans" \
       HexBerlekampZassenhausMathlib/Basic.lean \
       HexBerlekampZassenhausMathlib/IntReductionMod.lean \
       HexBerlekampZassenhausMathlib/Recovery.lean
   ```

   Expected: 39 in `Basic.lean` (1 def + 38 uses), 0 in
   `IntReductionMod.lean`, 0 in `Recovery.lean`. If
   `IntReductionMod.lean` or `Recovery.lean` shows a count
   > 0, the helper has either been duplicated or prematurely
   exported; report which.

   Additionally confirm no second
   `private theorem zpoly_dvd_trans` anywhere in the BZMathlib
   tree (a `git grep -n "private theorem zpoly_dvd_trans"`
   should return exactly one match, at the post-hoist
   `Basic.lean:2076` neighbourhood).

If all three invariants pass, the comment concludes with a
single line "All three invariants pass; ready to close."
Otherwise it lists each violation with file:line citations.

This is a **read-only audit**. No code changes. No new helper
extractions. Any follow-up extraction work spotted during the
audit (e.g., additional inline divisor-transitivity sites that
neither #5064 nor #5066 addressed) is drafted as a fresh issue
body within the audit comment for the next planner cycle, not
committed.

## Context

This continues the audit pattern of #5031 (post-#4991 cascade),
#5039 (post-#4991 Monic-route cluster extension), #5041
(`normalizeForFactor` primitivity micro-cluster), #5048
(post-#5045 baseline), #5053 (post-#5049 `_size_pos` of_monic),
and #5057 (post-#5052 `_size_pos` of_pos_lc).

The #5048 audit (post-#5045 baseline) flagged 26 + 3 missed
transitivity sites in its single PASS comment plus the post-#5058
rebase addendum. PR #5066 (closes #5065, merged as `dc2ba756`)
closes the 3 calc-form cluster via a `zpoly_dvd_trans` hoist plus
rewires. Issue #5064 (claimed at audit-write time) closes the 26
rcases-form cluster. After both land, this audit verifies that
the cleanup is complete and that the hoist did not introduce
regressions in framing, visibility, or cross-file consistency.

The cluster is "closed" once this audit passes: no further
`zpoly_dvd_trans` extraction or rewire work is anticipated
beyond what #5036 + #5045 + #5064 + #5066 have collectively
landed.

## Verification

- A single audit comment is posted on this issue.
- The comment contains verbatim grep output for at least the
  six commands listed under Invariant 1, plus the cross-file
  count under Invariant 3.
- The audit comment classifies each invariant as PASS or FAIL
  with file:line citations for any FAILs.
- No commits are made on the issue branch beyond the
  end-of-turn `progress/<UTC-timestamp>.md` entry.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean.

This issue is blocked on the PR that closes #5064; do not
claim it until that PR is merged. (PR #5066 has already merged
as `dc2ba756`, so it does not gate this audit.)

depends-on: #5064
