## Current state

`HexMatrix/Determinant.lean` lines ~6150–6251 contain a nine-lemma
private cluster of helpers for the `moveColumnToLastValues` column
permutation used in the Laplace (cofactor) expansion of the
determinant. The defining `private def moveColumnToLastValues`
(line 6146) carries a docstring, but every helper theorem built on top
of it is undocumented (no leading `/-- ... -/`). These helpers are
load-bearing for the column-cofactor expansion proof immediately below
them (the `cofactorSign_col_*` / Laplace-by-column theorems around
lines 6253–6320), and `neg_one_pow_mul_self` is reused widely
elsewhere in the file (e.g. lines 9303+, 9533+, 10786).

## Deliverables

Add a one-sentence, backtick-led Phase 6 docstring to each of the
following nine undocumented private theorems in
`HexMatrix/Determinant.lean` (lines ~6150–6251):

- `moveColumnToLastValues_last`
- `moveColumnToLastValues_castSucc`
- `moveColumnToLastValues_nodup`
- `moveColumnToLastValues_mem_permutationVectors`
- `skipIndex_eq_raiseFinAbove`
- `moveColumnToLastValues_toList`
- `detSign_moveColumnToLastValues`
- `neg_one_pow_mul_self`
- `cofactorSign_col_eq_move_mul_last`

Each docstring should lead with the backticked declaration name and
state what the lemma asserts and its role in the column-permutation /
cofactor-sign machinery behind the Laplace-by-column expansion. Match
the house style of the already-documented Phase 6 clusters
(e.g. the fold-sum helpers in `HexMatrix/Basic.lean`).

## Context

This is a doc-only batch. Do NOT touch any signature, statement, proof,
`private`/`@[simp]` annotation, declaration order, simp set, or import.
Additions only. No banned vocabulary ("smoke", "gate", "bridge") and no
em-dashes on added lines.

## Verification

- `lake build HexMatrix.Determinant`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexMatrix/Determinant.lean`: additions only
  (second column `0`).
- Confirm each of the nine names now has a preceding `/-- ... -/`.
