## Current state

`HexLLL/Basic.lean` has eight undocumented `private theorem` helpers in
the base-`2^K` packing / folded-dot-bound region (lines ~92–310). These
sit underneath the public lemmas `natAbs_le_maxAbs`, `dotProduct_packRow`,
and `natAbs_dotProduct_le`, which support the `mulEqCert` matrix-product
equality check. The interleaved public lemmas in the same region already
carry docstrings; only the private helpers are bare.

This is a Phase 6 docstring batch. It is distinct from the in-flight
`HexLLL/Basic.lean` LLLCore rational-arithmetic step-bound cluster
(#7335, `stepBound_arith` / `divStep_arith` / `ratPow_*`, lines ~423–584);
do not touch that region.

## Deliverables

Add one-sentence, backtick-led `/-- ... -/` docstrings to exactly these
eight private declarations in `HexLLL/Basic.lean`:

1. `foldl_max_le_init` (~L92) — the fold accumulator lower-bounds the
   running `Nat.max` fold.
2. `le_foldl_max` (~L100) — every list member's value is `≤` the
   running `Nat.max` fold.
3. `packDigits_replicate_zero` (~L158) — packing an all-zero digit list
   is `0`.
4. `packRow_replicate_zero` (~L164) — row-level form of the previous.
5. `foldl_dot_pack` (~L170) — folding the dot-with-packed-rows commutes
   with packing the folded coefficient-times-row update.
6. `foldl_zipWith_getElem` (~L183) — entry `j` of the folded `zipWith`
   row update equals the folded scalar update at `j`.
7. `foldl_dot_natAbs_le` (~L281) — `natAbs` bound on a folded integer
   dot product in terms of per-entry bounds and list length.
8. `two_mul_lt_width` (~L310) — `2 * x < 2 ^ (B.log2 + 2)` when `x ≤ B`.

Each docstring leads with the backticked declaration name and states
what the lemma proves plus its role in the packing / dot-bound
machinery.

## Context

Doc-only change. Do NOT touch any signature, statement, proof, name,
attribute, declaration order, or simp set. Additions only.

Follow the established Phase 6 style (see merged batches, e.g. the
`HexMatrix/Basic.lean` fold-sum cluster and the `HexPoly/Euclid.lean`
Bezout-descent cluster): one sentence, backtick-led, no banned
vocabulary (no "smoke", "gate", "bridge"), no em-dashes, no progress
notes or history.

## Verification

- `lake build HexLLL.Basic`: green.
- `python3 scripts/check_dag.py`: exits 0.
- `git diff --check`: clean.
- `git diff --numstat -- HexLLL/Basic.lean`: additions only (`N 0`).
- Added-line grep finds no `sorry` / `axiom` / `native_decide` /
  `TODO` / `FIXME` and no banned vocabulary or em-dashes.
