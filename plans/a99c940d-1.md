## Current state

depends-on: #5088

`HexBerlekampZassenhausMathlib/Basic.lean` and
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` are in the
middle of an `Hex.FpPoly p`-level cleanup wave mirroring the
`Hex.ZPoly`-level work that closed under #5069's audit. The
currently-claimed #5088 will:

- Extract `theorem fpPoly_leadingCoeff_ne_zero_of_size_pos`
  (signature
  `{p : Nat} [Hex.ZMod64.Bounds p] (f : Hex.FpPoly p)
  (hf_size_pos : 0 < f.size) :
  Hex.DensePoly.leadingCoeff f ≠ (0 : Hex.ZMod64 p)`)
  with a docstring matching the `fpPoly_dvd_trans` framing
  convention (post-#5077 audit-compliant shape, contract-first
  sentence plus a one-clause semantic note that the helper
  composes `Hex.FpPoly.leadingCoeff_eq_coeff_pred` with
  `Hex.DensePoly.coeff_last_ne_zero_of_pos_size`).

- Hoist the helper to a minimal position above its lowest call
  site. Per #5088 the natural target is between
  `monicModPImage_eq_monicModularImage` (line 1195) and
  `monicModPImage_dvd_self_of_ne_zero` (line 1222) — i.e. above
  Site 1 at `Basic.lean:1233`. The existing `fpPoly_dvd_trans`
  at line 1291 stays where it is.

- Rewire the **9** inline `have ... ≠ 0 := by rw
  [Hex.FpPoly.leadingCoeff_eq_coeff_pred …]; exact
  Hex.DensePoly.coeff_last_ne_zero_of_pos_size …` blocks per
  #5088's table:

  | # | File                     | Pre-rewire block lines | Enclosing context |
  | - | ------------------------ | ----------------------:| ------------------ |
  | 1 | `Basic.lean`             | 1233–1236              | `monicModPImage_dvd_self_of_ne_zero` (line 1222) |
  | 2 | `Basic.lean`             | 4892–4896              | `monicModularImage_dvd_modP` neighbourhood |
  | 3 | `Basic.lean`             | 5168–5172              | above `monicModularImage_modP_eq_of_monic` (line 5280) |
  | 4 | `Basic.lean`             | 5714–5718              | `quadraticMultifactorCoprimeSplits_of_factorProduct_no_squared` `hlc_ne` for `rawGcd` |
  | 5 | `Basic.lean`             | 5813–5816              | `hmodP_lead_ne` for `modP primeData.p core` |
  | 6 | `Basic.lean`             | 5994–5997              | inline `hlead_ne` for `f` (sites 6 and 8 inline `hpos` via `(isZero_eq_false_iff _).mp hzero`) |
  | 7 | `Basic.lean`             | 6152–6155              | `hg'_lead_ne` for `g'` |
  | 8 | `IntReductionMod.lean`   |  336–339               | inline `hlead_ne` for `f` (inline `hpos`) |
  | 9 | `IntReductionMod.lean`   |  485–489               | `hlead_ne` for `Hex.ZPoly.modP primeData.p core` |

After #5088 lands, the BZMathlib tree should contain:

- **One** `theorem fpPoly_leadingCoeff_ne_zero_of_size_pos`
  definition at a minimal-hoist position exposing it to both
  `Basic.lean` and `IntReductionMod.lean` call sites
  (`IntReductionMod.lean` imports `Basic.lean`).
- **9** call-site invocations across the two files (7 in
  `Basic.lean`, 2 in `IntReductionMod.lean`), modulo any
  audit-missed sites the rewire surfaces beyond the table above.

This issue audits the post-#5088 state of the
`fpPoly_leadingCoeff_ne_zero_of_size_pos` cluster against the
same three invariants applied to the `zpoly_dvd_trans` cluster
in the #5048 / #5057 / #5069 audits, to the Monic-route cluster
in #5031 / #5039 / #5054 / #5063, to the `_size_pos` pair in
#5053 / #5057, to the `fpPoly_dvd_trans` cluster in #5082
(post-#5077), and to the Shape-B `fpPoly_isZero_false_of_*`
cluster in the pending #5085 audit. The pattern is now the
project's standard audit shape.

## Deliverables

The audit comment posted on this issue must address all three
invariants verbatim:

1. **Call-site coverage.** Every site in `Basic.lean`,
   `IntReductionMod.lean`, and `Recovery.lean` that derives
   `Hex.DensePoly.leadingCoeff f ≠ 0` from a positive-size
   hypothesis on `f : Hex.FpPoly p` routes through
   `fpPoly_leadingCoeff_ne_zero_of_size_pos` rather than
   inlining the 2-line `rw
   [Hex.FpPoly.leadingCoeff_eq_coeff_pred f …]; exact
   Hex.DensePoly.coeff_last_ne_zero_of_pos_size f …` block. All
   nine pre-rewire sites listed in #5088's table must be
   confirmed rewired. If any audit-missed sites surface
   (mirroring the +3 audit-missed sites surfaced in the #5083
   `fpPoly_dvd_trans` rewire vs. #5077's table), they must be
   listed explicitly with line numbers, enclosing theorem, and
   rcases/calc/cases shape, and a DEVIATION verdict issued on
   Invariant 1 with a recommended follow-up issue draft inline.

   Out-of-scope sites that look superficially similar but have
   a different conclusion shape must be classified, not
   rewired:

   - `Basic.lean:5337` (`rw [leadingCoeff_eq_coeff_pred _
     hmodP_size_pos, hmodP_size_eq]; exact hmodP_coeff_lead`)
     proves `leadingCoeff X = 1`, not `≠ 0` — out of scope.
   - `Basic.lean:5722` (`rw [leadingCoeff_eq_coeff_pred rawGcd
     hrawGcd_size_pos]; congr 1; omega`) proves `coeff 0 =
     leadingCoeff` — opposite-direction rewrite, out of scope.
   - Any `coeff_last_ne_zero_of_pos_size` use *not* composed
     with `leadingCoeff_eq_coeff_pred` (e.g. for purposes other
     than the `leadingCoeff ≠ 0` conclusion).

2. **Framing, docstring, and hoist correctness.** The
   `fpPoly_leadingCoeff_ne_zero_of_size_pos` declaration:
   - Has a docstring matching the
     [`zpoly_dvd_trans` post-#5048 audited form](https://github.com/kim-em/hex/issues/5069#issuecomment-4481345752)
     and the post-#5077 `fpPoly_dvd_trans` form — contract-first
     sentence stating the executable `Hex.FpPoly p`-level
     leading-coefficient nonvanishing claim, followed by a
     one-clause semantic note that the helper composes
     `Hex.FpPoly.leadingCoeff_eq_coeff_pred` with
     `Hex.DensePoly.coeff_last_ne_zero_of_pos_size`. No
     `(#NNNN)` PR references, no session metadata, no
     "extracted from `Basic.lean:NNNN`" / "hoisted from …"
     provenance hints.
   - Is `theorem`, not `private theorem` — the cluster spans
     `Basic.lean` and `IntReductionMod.lean`, and the
     `IntReductionMod`-side rewires need cross-file visibility.
     This is the only audited departure from the
     `private theorem` convention used by `fpPoly_dvd_trans`,
     the Shape-B helper, and the ZPoly/Monic-route helper
     family; the audit comment confirms the cross-file
     necessity by citing the two
     `IntReductionMod.lean`-side call sites.
   - The hoist target is the minimal position needed to expose
     the helper to its lowest call site (Site 1 at the
     pre-rewire `Basic.lean:1233` inside
     `monicModPImage_dvd_self_of_ne_zero` at line 1222). The
     audit comment names the immediately-preceding and
     immediately-following declarations at the hoist line, and
     confirms the immediately-preceding declaration's body
     does not mention
     `fpPoly_leadingCoeff_ne_zero_of_size_pos` (i.e., the
     hoist isn't gratuitous).

3. **Cross-file consistency.**
   `fpPoly_leadingCoeff_ne_zero_of_size_pos` is defined
   exactly once across `Basic.lean`, `IntReductionMod.lean`,
   and `Recovery.lean` (no peer / duplicate / shadow). The
   audit comment reports the exact occurrence count in each of
   the three files:
   - `Basic.lean`: 1 def + 7 uses = **8**.
   - `IntReductionMod.lean`: **2** uses.
   - `Recovery.lean`: **0**.
   Any deviation from these counts (beyond the audit-missed
   sites surfaced under Invariant 1) is itself a DEVIATION.

The audit comment closes with a one-line PASS/DEVIATION summary
per invariant (e.g. `Invariant 1: PASS`, `Invariant 2: PASS`,
`Invariant 3: PASS`) plus an out-of-scope observations section
listing any sibling-cluster cleanup opportunities surfaced
during the audit. Concretely:

- Are the `Basic.lean:5337` / `5722` sites (different-shape
  `leadingCoeff_eq_coeff_pred` users) candidates for a
  follow-up sibling-cluster extraction (e.g. a
  `fpPoly_leadingCoeff_eq_of_coeff_eq` helper)? Surface the
  count of `leadingCoeff_eq_coeff_pred` uses with non-`≠ 0`
  conclusions in `Basic.lean` and `IntReductionMod.lean`.
- Does the upstream `private theorem
  leadingCoeff_ne_zero_of_isZero_false` at
  `HexPolyFp/SquareFree.lean:1243` become a public-promotion
  candidate now that the BZMathlib helper exists? Note this is
  a separate planner-level decision, not a follow-up rewire.

## Library placement

- **File path the deliverables land in.** Only this issue (an
  audit comment on the GitHub issue) and a per-turn progress
  file under `progress/<UTC>_<short>.md`. No
  `HexBerlekampZassenhausMathlib/` edits.
- **SPEC §**: governed by
  `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md`. No
  SPEC edit (audit is read-only against the substrate).
- **Single-library?** Yes — `Basic.lean` and
  `IntReductionMod.lean` are both inside
  `HexBerlekampZassenhausMathlib`.
- **Forward-reference / hoist risk?** None — audit is
  read-only. Any follow-up issue this audit drafts may have
  forward-reference consequences (e.g., audit-missed sites at
  higher line numbers); those must be flagged inside the audit
  so the next planner cycle can sequence a fan-out follow-up.

## Context

- depends-on: #5088 (helper extraction + 9-site rewire across
  `Basic.lean` and `IntReductionMod.lean`).
- Peer audits running in parallel:
  - #5082 (post-#5077 audit of the Shape-A `fpPoly_dvd_trans`
    cluster) — CLOSED PASS via #5087.
  - #5085 (post-#5081 audit of the Shape-B
    `fpPoly_isZero_false_of_dvd_of_isZero_false` cluster) —
    blocked on #5081 (in CONFLICTS, repair-claimed).
  - #5090 (post-#5084 audit of the Monic-route
    `monic_primitive_sign_normalized_of_monic` cluster
    inversion) — claimed.

  This issue's audit applies the same three-invariant template
  used in those peers; the worker should cross-reference
  #5082's audit comment and #5085's issue body for the
  docstring framing conventions, while noting that the
  `theorem` (non-`private`) departure for this cluster is a
  cross-file-visibility requirement, not a framing deviation.
- Cluster predecessor audits in the same cascade and shape:
  - #5048 / #5057 / #5069 (`zpoly_dvd_trans` ZPoly-level
    transitivity).
  - #5031 / #5039 / #5054 / #5063 (Monic-route cluster).
  - #5041 (`normalizeForFactor` primitivity micro-cluster).
  - #5053 / #5057 (`_size_pos` pair audit).
- Related `Hex.FpPoly p`-level idiomatic helpers already in
  scope:
  - `Hex.FpPoly.leadingCoeff_ne_zero_of_pos_degree` at
    `HexPolyFp/Basic.lean:2665` takes
    `0 < f.degree?.getD 0` — excludes the constant case
    (`f.size = 1`), so it is not a substitute for the new
    helper at any of the 9 sites.
  - `Hex.FpPoly.leadingCoeff_eq_coeff_pred` and
    `Hex.DensePoly.coeff_last_ne_zero_of_pos_size` are the two
    Mathlib-free executable lemmas composed inside the helper.

## Verification

- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean; `git diff origin/main` empty for
  any file outside `progress/`.
- Audit comment posted on this issue covering all three
  invariants with a per-invariant PASS/DEVIATION verdict and
  an out-of-scope observations section.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME`
  introduced in any code file (vacuous: audit edits no code).
- If a DEVIATION verdict is issued, the audit comment includes
  an inline-drafted follow-up issue body in the canonical
  Current state / Deliverables / Library placement / Context /
  Verification shape so the next planner cycle can post it via
  `coordination plan` with minimal editing.
