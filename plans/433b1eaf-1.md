## Current state

`HexPolyFp/Quotient.lean` is in Phase 6 (proof polishing). The cluster
of `private theorem`s that prove evaluation into the quotient ring
`Quotient g` is a ring homomorphism is undocumented. These are
non-obvious private helpers (each encodes the coeff-power-sum expansion
behind the public `eval_add`/`eval_sub`/`eval_C_mul`/`eval_monomial`
characterising lemmas), so the Phase 6 docstring rule applies.

## Deliverables

Add a one-sentence docstring to each of the following `private theorem`s
in `HexPolyFp/Quotient.lean` (lines ~2062–2189), stating what it proves
and its role in establishing that quotient evaluation respects the ring
operations:

- `eval_add_core_by_coeff_power_sum`
- `eval_sub_core_by_coeff_power_sum`
- `eval_C_mul_core_by_coeff_power_sum`
- `eval_add_core`
- `eval_sub_core`
- `eval_C_mul_core`
- `eval_monomial_core`

Doc-only: do not touch signatures, statements, proofs, declaration
order, or `private` modifiers. If any helper is genuinely trivial
plumbing under the `SPEC/design-principles.md` docstring rule (an
`_aux`/unfolding lemma), leave it undocumented and say so in the PR
body rather than writing filler.

## Context

- `SPEC/design-principles.md` §Docstrings: non-obvious private helpers
  encoding a non-trivial specification carry a docstring; routine
  plumbing is exempt.
- `PLAN/Phase6.md`: docstring coverage is a Phase 6 exit criterion.
- Follows the established Phase 6 doc-batch pattern (e.g. the merged
  `HexPolyFp/SquareFree.lean` and `HexPolyFp/Basic.lean` batches).

## Verification

- `lake build HexPolyFp.Quotient` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` clean; `git diff --numstat` shows additions only.
- No `sorry`/`axiom`/`native_decide`/`TODO`/`FIXME` on added lines.
