## Current state

`HexPolyFp/Basic.lean` is in the Phase 6 docstring sweep. The
multiplication-coefficient fold cluster (lines ~1601-1813) is undocumented.
It expresses the `n`-th coefficient of a polynomial product as the executable
schoolbook convolution sum (`mulCoeffTerm` / `mulCoeffSum`, which already
carry docstrings) and proves the fold-accumulator and degree/size truncation
facts the `coeff_mul` characterisation rests on. These twelve private
theorems carry no docstrings:

- `coeff_mul_one_fold` (L1601)
- `coeff_mul_fold` (L1662)
- `foldl_mulCoeffStep_select_fp` (L1679)
- `foldl_mulCoeffStep_outer_fp` (L1705)
- `foldl_mulCoeffStep_outer_eq_mulCoeffTerm` (L1723)
- `mulCoeffTerm_eq_zero_of_size_le` (L1756)
- `fold_mulCoeff_extend` (L1765)
- `mulCoeffSum_eq_bound` (L1779)
- `coeff_mul_of_size_le` (L1787)
- `mulCoeffTerm_eq_zero_of_degree_lt` (L1793)
- `fold_mulCoeff_truncate_degree` (L1798)
- `mulCoeffSum_eq_degree_bound` (L1813)

## Deliverables

1. Add a one-sentence, backtick-led docstring to each of the twelve
   declarations above in `HexPolyFp/Basic.lean`, naming the noun each lemma
   computes or characterises (the single-step fold contribution, the
   inner/outer fold rewrites to `mulCoeffTerm`, the size/degree vanishing of
   product terms, and the bounded/degree-truncated forms of `mulCoeffSum`).

This is a **doc-only** change: do not touch any theorem statement, signature,
proof body, or declaration order. Additive docstring lines only.

## Library placement

`HexPolyFp/Basic.lean` (library `hex-poly-fp`).

## Context

- Phase 6 exit criteria and docstring-coverage rule:
  [PLAN/Phase6.md](../PLAN/Phase6.md) and
  [SPEC/design-principles.md](../SPEC/design-principles.md) (public
  declarations and non-obvious private helpers carry docstrings).
- Style: short verb-noun docstrings, no "research completed" notes, no
  meta-commentary, no em-dashes, no banned vocabulary (see `.claude/CLAUDE.md`).
  Do not rename the existing `_smoke` lemmas; they are out of scope.
- Stop before `fold_add_right` (L1825); the generic `fold_add` reverse/range
  helpers are a separate later batch.

## Verification

- `lake build HexPolyFp.Basic` passes.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- `git diff --numstat -- HexPolyFp/Basic.lean` shows additive-only edits
  (insertions, zero deletions).
- Added-line scan finds no banned tokens or em-dashes.
