## Current state

Three sites carry a byte-equivalent 3-line block deriving
`Hex.ZPoly.Primitive core` from `Hex.DensePoly.Monic core` via the
existing `monic_primitive_sign_normalized_of_monic` triple-witness
theorem and its `.2.1` projection:

```lean
have hcore_primitive :
    Hex.ZPoly.Primitive (Hex.normalizeForFactor f).squareFreeCore :=
  (monic_primitive_sign_normalized_of_monic hcore_monic).2.1
```

This is the "Monic-implies-Primitive" sibling of the helper cluster
that already covers "Monic-implies-nonzero"
(`zpoly_ne_zero_of_monic`, PR #5001) and "Monic-implies-leading-
coefficient-positive" (`zpoly_lc_pos_of_monic`, PR #5009). All three
fall out trivially from the same `monic_primitive_sign_normalized_of_monic`
triple (Monic ∧ content = 1 ∧ normalizeFactorSign = id) but each
projection is named separately because the triple-projection
`(...).2.1` is opaque at the call site.

`Hex.ZPoly.Primitive f := Hex.ZPoly.content f = 1`
(`HexPolyZ/Basic.lean:36`), so the helper body is one line by
definitional unfolding.

## Per-site breakdown (current line numbers at `origin/main` 1c331b3d)

For each site below, the rewire deletes the 3-line `have hcore_primitive : Hex.ZPoly.Primitive (Hex.normalizeForFactor f).squareFreeCore := (monic_primitive_sign_normalized_of_monic hcore_monic).2.1` block and inserts a single line
`have hcore_primitive := zpoly_primitive_of_monic hcore_monic`.

1. `Basic.lean:14996-14998` — body of
   `factorWithBound_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence_of_bound`
   (theorem signature ending before line 14994). 2-space indent
   ("`  have hcore_primitive :`" form, not inside a `by`'s
   tactic-block scope).
2. `IntReductionMod.lean:3254-3256` — body of `private theorem
   factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux_of_bound`.
   4-space indent (inside a nested `by` block).
3. `IntReductionMod.lean:3571-3573` — body of
   `exhaustiveCoreFactorsWithBound_expansion_preconditions_of_choosePrimeData_of_bound`.
   4-space indent (inside a nested `by` block).

Net deletion: 3 × 3 = 9 lines deleted across 3 sites; 3 × 1 = 3
lines inserted; ~5 lines added for the helper definition + docstring.
Net change ≈ −1 line; the readability gain is the point, not the
line count.

## Deliverables

1. Add a new (non-`private`) helper `zpoly_primitive_of_monic` in
   `HexBerlekampZassenhausMathlib/Basic.lean` immediately after
   `zpoly_lc_pos_of_monic` (which currently ends at line 6744 on
   `origin/main` 1c331b3d) and before `private theorem zpoly_monic_one`.
   Signature:
   ```lean
   theorem zpoly_primitive_of_monic {f : Hex.ZPoly}
       (h : Hex.DensePoly.Monic f) : Hex.ZPoly.Primitive f :=
     (monic_primitive_sign_normalized_of_monic h).2.1
   ```
   `monic_primitive_sign_normalized_of_monic` is defined at line 4473
   of the same file, so it is in scope here. The cluster grows in
   one block (`zpoly_ne_zero_of_monic` → `zpoly_lc_pos_of_monic` →
   `zpoly_primitive_of_monic`).

2. Rewire the 3 sites listed above to the single-line invocation
   `have hcore_primitive := zpoly_primitive_of_monic hcore_monic`,
   preserving the surrounding indent at each site. Drop the explicit
   type annotation; the helper's return type unifies under the
   `hcore_monic` argument.

No other edits. No proof-body or signature changes elsewhere. No
docstring changes at the rewired sites. No new `sorry`, `axiom`,
`native_decide`, `TODO`, or `FIXME`.

## Naming

`zpoly_primitive_of_monic` matches the established
`zpoly_<property>_of_monic` cluster (`zpoly_size_pos_of_monic` at
line 6707, `zpoly_ne_zero_of_monic` at 6733, `zpoly_lc_pos_of_monic`
at 6740 — all on `origin/main` 1c331b3d). The output type
`Hex.ZPoly.Primitive` parallels `Hex.ZPoly.Primitive` already used
at the rewire sites' result type annotation, so no naming friction
downstream.

## Verification

Mechanical:

* `git grep -c "(monic_primitive_sign_normalized_of_monic hcore_monic).2.1" HexBerlekampZassenhausMathlib/Basic.lean HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  drops by 3 (current count on `origin/main`: Basic.lean = 1,
  IntReductionMod.lean = 2; total = 3 → expected post-rewire 0).
* `git grep -c "zpoly_primitive_of_monic" HexBerlekampZassenhausMathlib/Basic.lean HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  rises from 0 to 4 (1 definition in Basic.lean + 3 rewire-site
  invocations).
* `git grep -n "have hcore_primitive :$" HexBerlekampZassenhausMathlib/Basic.lean HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  drops by 3 from the rewire sites (sites 14996, 3254, 3571);
  other `have hcore_primitive :` blocks at IntReductionMod.lean
  2481, 2971, 3074 remain (Shape B — covered by sibling issue #5007).

Build:

* `lake build HexBerlekampZassenhausMathlib.Basic`: error set unchanged
  (baseline at `origin/main` 1c331b3d is the post-#5009 set; the only
  shifts are line numbers of pre-existing errors past line 14996,
  shifted by −2 from the −2-net at that site).
* `lake build HexBerlekampZassenhausMathlib.IntReductionMod`: error set
  unchanged (the rewire is purely a body refactor; the two sites
  shift line numbers of errors past lines 3254 and 3571 by a uniform
  −2 each).
* `lake build HexBerlekampZassenhausMathlib`: same.
* `python3 scripts/check_dag.py`: exit 0.
* `git diff --check`: clean.
* `git diff origin/main | grep -E '^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b'`:
  empty.

## Context

* Helper-cluster precedents (all in `HexBerlekampZassenhausMathlib/Basic.lean`
  at the same anchor):
  - `zpoly_size_pos_of_monic` (line 6707) — pre-existing.
  - PR #5001 — non-`private` `zpoly_ne_zero_of_monic` /
    `zpoly_ne_zero_of_pos_lc`, 9 rewires (line 6733).
  - PR #5009 (issue #5005) — `zpoly_lc_pos_of_monic`, 4 rewires
    (line 6740).
* Sibling in-flight helper extraction at a different `Primitive`
  source: issue #5007 — `normalizeForFactor_squareFreeCore_primitive_of_ne_zero`,
  3 rewires (Shape B route from `f ≠ 0` via
  `zpoly_primitive_of_toPolynomial_isPrimitive`). Distinct shape;
  no conflict with this issue.
* The `.2.1` projection is also used once inline at
  `Basic.lean:5597` (passing the primitivity witness as an argument
  to a theorem call rather than naming it `hcore_primitive`). That
  site could be harmonised to `zpoly_primitive_of_monic hcore_monic`
  later (same length, readability gain only); deliberately out of
  scope here to keep the issue atomic.

## Out of scope

* The inline `.2.1` projection at `Basic.lean:5597` (passing the
  primitivity witness as an argument to a theorem call). Atomic
  follow-up; this issue only rewires the named-`hcore_primitive` `have`
  blocks.
* Renaming `monic_primitive_sign_normalized_of_monic` or refactoring
  its triple return type — the helper composes on top, no signature
  change to that theorem.
* No edits to `HexBerlekampZassenhausMathlib/Recovery.lean` (the
  pattern does not appear there).
