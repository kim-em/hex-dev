## Current state

`HexGramSchmidt/Int.lean` carries eight `@[simp]` theorems but only one
`@[grind =]` annotation (`scaledCoeffs_upper`). The Phase 6 grind sweep
(see e.g. #8028 HexMatrix/Determinant, #8030 HexGFqField/Operations) has
promoted clean equation-shaped `@[simp]` lemmas to `@[simp, grind =]`
across the rest of the Phase-6 libraries, but `HexGramSchmidt` has not
yet been swept. `HexGramSchmidt` is `mathlib: false`, `done_through: 5`,
so it is Phase 6 ready.

The eight `@[simp]` theorems are:

- `bareissGramCanonicalCoeff_zero` — base case, clean `lhs = rhs`
- `bareissGramCanonicalCoeff_succ_regular` — recursive successor case,
  carries side hypothesis (`hnext`)
- `bareissGramCanonicalCoeff_succ_processed` — recursive successor case,
  side hypothesis
- `bareissGramCanonicalCoeff_succ_singular` — recursive successor case,
  side hypothesis
- `bareissGramCanonicalCoeff_succ_done` — recursive terminal case, side
  hypothesis (`hDone`)
- `gramDet_zero` — base case, clean `lhs = rhs`
- `coeffs_mul_basis_eq_castIntMatrix` — clean `lhs = rhs`
- `normSq_map_intCast` — clean `lhs = rhs`

## Deliverables

1. Promote the clean, unconditional equation-shaped `@[simp]` theorems
   (`bareissGramCanonicalCoeff_zero`, `gramDet_zero`,
   `coeffs_mul_basis_eq_castIntMatrix`, `normSq_map_intCast`) to
   `@[simp, grind =]`. Re-read each before promoting to confirm it is a
   literal `lhs = rhs` form.

2. Evaluate the four recursive `bareissGramCanonicalCoeff_succ_*`
   theorems individually. They are conditional equations (side
   hypotheses `hnext` / `hDone`); per the #8028 precedent a side
   hypothesis is fine — `grind =` discharges it as a side goal — but the
   recursive successor cases of a fuel-driven definition carry grind-loop
   risk. Promote the ones that strictly reduce inner structure and
   terminate; leave any that risk a loop `@[simp]`-only and record the
   exclusion and its reason in the progress file.

3. Do not promote any `↔`, `<`, or `≤` shaped lemma (none are in the
   `@[simp]` set above, but verify on re-read).

## Context

This is an annotation-only sweep: no proof bodies change, no new
imports, no `*Mathlib` file is touched. `HexGramSchmidt` does not import
Mathlib, so it is not directly CI-merge-gated, but keep it green
regardless. Match the convention used by the merged Phase 6 grind PRs
(#8028, #8030, #8025, #8013): `@[simp]` becomes `@[simp, grind =]` in
place.

Scope is the single file `HexGramSchmidt/Int.lean`. Do not touch the
`bareissGramCanonicalCoeff` definition or any other declaration.

## Verification

- `lake build HexGramSchmidt` green, with no grind-loop slowdown on
  `HexGramSchmidt.Int` (compare build time against the sibling modules;
  a runaway promotion shows up as a multi-second regression).
- `python3 scripts/check_dag.py` exit 0.
- `git diff --check` clean; no added `sorry` / `axiom` /
  `native_decide` / `TODO` / `FIXME`.
- Diff is annotation-only (equal insertions and deletions on the
  promoted lines), no proof bodies changed.
