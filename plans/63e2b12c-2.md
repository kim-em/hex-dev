## Current state

`scripts/status.py` reports `HexArith -> Phase 6 (proof polishing)`.
Phase 6 reviews under HexArith so far have covered the user-facing
modular layers — Barrett (`#3557`), Montgomery REDC (`#3556`), extended
GCD (`#3425`), and most recently the UInt64 wide-word extern boundary
(`#4398`, closed via `#4403`).

The remaining HexArith surface that has not received a dedicated Phase 6
polish pass is the `HexArith/Nat/` helper layer (~394 lines):

- `HexArith/Nat/Prime.lean` (~299 lines) — `Hex.Nat.Prime`, `choose`,
  `choose_*`, `dvd_mul`, `choose_prime_dvd`, `add_pow_prime_mod`,
  `pow_prime_mod`.
- `HexArith/Nat/ModArith.lean` (~23 lines) — `coprime_pow_two_of_odd`.
- `HexArith/Nat/Pow.lean` (~72 lines) — `sub_one_dvd_pow_sub_one`,
  `pow_sub_one_dvd_pow_sub_one_of_dvd`.

These lemmas underpin downstream modular-arithmetic facts (Fermat's
little theorem callers, Frobenius `add_pow_prime_mod` rewriters,
order/cyclic-group support in HexModArith and HexBerlekamp), but the
layer was written before the Phase 6 polish pass that gave Barrett /
Montgomery / Wide their consistent docstring + characterising-lemma
shape.

## Deliverables

1. Review `HexArith/Nat/Prime.lean` against the HexArith SPEC and the
   Phase 6 API bar already applied to Barrett / Montgomery / Wide. For
   each public definition (`choose`, `Hex.Nat.Prime`) and each public
   theorem (the `choose_*` family, `dvd_mul`, `choose_prime_dvd`,
   `add_pow_prime_mod`, `pow_prime_mod`), confirm the docstring states
   the user-facing fact and that the lemma name follows the
   library-wide convention.
2. Review `HexArith/Nat/ModArith.lean` and `HexArith/Nat/Pow.lean`
   for the same Phase 6 polish bar. These are short files; the goal is
   to confirm that `coprime_pow_two_of_odd`, `sub_one_dvd_pow_sub_one`,
   and `pow_sub_one_dvd_pow_sub_one_of_dvd` are usable as named rewrites
   by downstream callers without unfolding their proofs, and to add
   characterising lemmas only where a documented gap exists.
3. Add or adjust narrow characterising lemmas, docstrings, and
   `@[simp]` annotations only where the gap is real and the simp normal
   form is safe across the immediate consumers (HexModArith,
   HexBerlekamp). Run a second-opinion check before promoting any
   non-trivial `[simp]` rewrite. Do not bump `libraries.yml`.

## Library placement

- `HexArith/Nat/Prime.lean`
- `HexArith/Nat/ModArith.lean`
- `HexArith/Nat/Pow.lean`

Allowed downstream spot-builds: `HexArith`, `HexModArith`,
`HexBerlekamp.RabinSoundness` (immediate consumers of these helper
lemmas). Do not modify these consumer modules unless the spot-build
fails on a newly promoted simp lemma; in that case prefer reverting the
simp promotion over editing the consumer.

## Verification

- `lake build HexArith.Nat.Prime` — clean.
- `lake build HexArith.Nat.ModArith` — clean.
- `lake build HexArith.Nat.Pow` — clean.
- `lake build HexArith` — clean.
- `lake build HexModArith` — clean (immediate consumer).
- `lake build HexBerlekamp.RabinSoundness` — clean (downstream
  Fermat-style consumer).
- `python3 scripts/check_dag.py` — exit 0.
- `python3 scripts/status.py HexArith` — unchanged (`done_through: 5`).
- `git diff --check` — clean.
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b' HexArith/Nat/Prime.lean HexArith/Nat/ModArith.lean HexArith/Nat/Pow.lean`
  — no matches.
- No `axiom` introduction (per repo `.claude/CLAUDE.md`).
