## Current state

The /replan agent closed #4639 ("relax three exhaustive wrappers to
primitive + pos-leading core") with an explicit request that the
planner refile its prerequisite chain as sub-issues of #4626 (closed).
The first sub-issue, #4866 (currently blocked on #4863), adds
`recombineScaledExhaustive` and its three structural-invariant lemmas.

This issue is the next link in that chain: the structural recovery
lemma that the call-site swap in `exhaustiveCoreFactorsWithBound` will
eventually consume. #4866's "Out of scope" section names it explicitly:

> A `recombineScaledExhaustive_product` analog of
> `recombineExhaustive_product`. … That belongs in a separate follow-on
> issue alongside any callers that consume the product equality.

The unscaled siblings live in `HexBerlekampZassenhaus/Basic.lean`:

* `recombinationSearchModAux_product` at line 7636.
* `recombinationSearchMod_product` at line 7684.
* `recombineExhaustive_product` at line 7791.

The proof of the aux variant works by induction on `fuel`, using
`exactQuotient?_product` at each step to derive `candidate * quotient
= target`. The scaled aux `scaledRecombinationSearchModAux`
(`HexBerlekampZassenhaus/Basic.lean:5025`) recurses through the same
`exactQuotient?` peel; the per-step candidate is

```lean
normalizeFactorSign <|
  ZPoly.primitivePart <|
    centeredLiftPoly (DensePoly.scale coreLc (Array.polyProduct split.1.toArray)) modulus
```

but the only fact the product proof needs is that `exactQuotient?
target candidate = some quotient` implies `candidate * quotient =
target`, which is independent of how the candidate is built. So the
post-condition shape is the same as the unscaled chain:
`Array.polyProduct factors.toArray = target`.

(The "scaled candidate's product divides core but does not equal it"
remark in #4866 refers to the *raw scaled product* before
primitivisation — `scale coreLc (∏ T)` — not the candidate that
`exactQuotient?` consumes. After `primitivePart ∘ centeredLift ∘ scale`,
the recorded candidate divides the running target exactly.)

depends-on: #4866

## Deliverables

In `HexBerlekampZassenhaus/Basic.lean`, add the following three private
lemmas, mirroring the unscaled siblings at lines 7636–7799. Same
visibility (`private`) as the unscaled siblings.

1. **`scaledRecombinationSearchModAux_product`** — mirror of
   `recombinationSearchModAux_product` (line 7636):

   ```lean
   private theorem scaledRecombinationSearchModAux_product
       (coreLc : Int) (target : ZPoly) (modulus : Nat)
       (localFactors factors : List ZPoly) (fuel : Nat)
       (hsearch :
         scaledRecombinationSearchModAux coreLc target modulus localFactors fuel
           = some factors) :
       Array.polyProduct factors.toArray = target
   ```

   Proof by induction on `fuel` following the unscaled proof line by
   line; the only structural difference is the candidate shape, which
   the proof never inspects beyond the `exactQuotient?` step.

2. **`scaledRecombinationSearchMod_product`** — mirror of
   `recombinationSearchMod_product` (line 7684):

   ```lean
   private theorem scaledRecombinationSearchMod_product
       (coreLc : Int) (f : ZPoly) (modulus : Nat)
       (localFactors factors : List ZPoly)
       (hsearch :
         scaledRecombinationSearchMod coreLc f modulus localFactors = some factors) :
       Array.polyProduct factors.toArray = f
   ```

   Trivial wrapper invoking (1) with `fuel = localFactors.length + 1`.

3. **`recombineScaledExhaustive_product`** — mirror of
   `recombineExhaustive_product` (line 7791):

   ```lean
   private theorem recombineScaledExhaustive_product
       (coreLc : Int) (f : ZPoly) (d : LiftData) (factors : List ZPoly)
       (hsearch :
         scaledRecombinationSearchMod coreLc f (liftModulus d) d.liftedFactors.toList =
           some factors) :
       Array.polyProduct (recombineScaledExhaustive coreLc f d) = f
   ```

   Unfold `recombineScaledExhaustive` (from #4866) and apply (2).

If during implementation the worker discovers the product equation
does *not* hold in the stated `polyProduct factors = target` form (for
example, because of a primitivisation interaction we have not seen),
they should:

* file a follow-up issue stating the actual structural relationship
  the proof exposes, and
* `coordination skip <this issue> 'product shape needs reshape: <explanation>'`.

No existing call site changes in this issue. The call-site swap of
`exhaustiveCoreFactorsWithBound` to consume `recombineScaledExhaustive`
is the next prerequisite sub-issue, not in scope here.

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus.md` — internal
  recombination infrastructure for the Mathlib bridge.
* **Mathlib use:** no. Purely additive in `HexBerlekampZassenhaus/Basic.lean`.

## Verification

* `lake build HexBerlekampZassenhaus`
* `lake build HexBerlekampZassenhausMathlib` (additive change; should
  not perturb the Mathlib bridge, but confirm cleanly)
* `python3 scripts/check_dag.py`
* `git diff --check`
* `rg -n "sorry|axiom|native_decide|TODO|FIXME" HexBerlekampZassenhaus/Basic.lean`
  shows no new entries on the diff against `origin/main`.

## Out of scope

* Switching `exhaustiveCoreFactorsWithBound`
  (`HexBerlekampZassenhaus/Basic.lean:5363`) to call
  `recombineScaledExhaustive`. Successor sub-issue.
* The scaled membership wrapper
  `exhaustiveCoreFactorsWithBound_mem_of_scaledRecombinationSearchMod_some`
  in `HexBerlekampZassenhausMathlib/Basic.lean`. Later sub-issue,
  depends on the call-site swap.
* Relaxing the three top-level wrappers in
  `HexBerlekampZassenhausMathlib/Basic.lean`
  (`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`,
  `exhaustiveCoreFactorsWithBound_factor_zpolyIrreducible_of_henselSubsetCorrespondence`,
  `factor_exhaustive_branch_entry_core_zpolyIrreducible_of_henselSubsetCorrespondence`)
  from `Monic core` to `Primitive core + 0 < leadingCoeff core` —
  final sub-issue in this chain (#4639 successor).
* Dropping `hcore_monic` Gap 1 from the umbrella consumer in
  `HexBerlekampZassenhausMathlib/IntReductionMod.lean` (covered by
  #4640, already closed).
* BHKS Theorem 5.2 / directive #2567.
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
