## Current state

The A2 abstract-bound (`_of_bound`) propagation in
`HexBerlekampZassenhausMathlib/Basic.lean` has nearly reached the
public `_some_factor_associated_*` wrapper layer that sits
immediately above the private `_some_and_covers_*` recursive-coverage
substrate.  The scaled primitive (non-monic) substrate
`scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition_of_bound`
is planned as #4935 (blocked on #4932).  Once #4935 lands, the public
forwarders one layer up — currently the only consumers of the scaled
substrate in-tree — become the natural next `_of_bound` targets.

The two target theorems are the scaled-primitive public forwarders:

* `scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`
  (`HexBerlekampZassenhausMathlib/Basic.lean:13850`).  Body is a
  single `obtain`+`exact` against the private substrate at line
  13531, threading `hprecision` verbatim.
* `recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core`
  (`HexBerlekampZassenhausMathlib/Basic.lean:13894`).  Body is a
  single `exact` call to the previous wrapper at line 13850, threading
  `hprecision` verbatim.  This is the wrapper consumed by
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  at line 13994.

Both wrappers carry `(hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound
core < d.p ^ d.k)` and consume it at exactly **one** call site each:
the forward call into the next layer down.  Their proof bodies are
otherwise precision-agnostic and carry over verbatim.

## Hypothesis shape

Mirror the substrate `_of_bound` shape from #4935 (Option A,
universal-on-divisors):

```lean
(B' : Nat)
(hcore_lc_le : (Hex.DensePoly.leadingCoeff core).natAbs ≤ B')
(hvalid : ∀ g : Hex.ZPoly, g ∣ core → ∀ i, (g.coeff i).natAbs ≤ B')
(hprecision : 2 * B' < d.p ^ d.k)
```

replacing the existing `hprecision : 2 * Hex.ZPoly.defaultFactorCoeffBound
core < d.p ^ d.k` binder.  The four-parameter block goes immediately
before the position of the original `hprecision` to keep parameter
ordering consistent with the established `_of_bound` siblings at this
layer (#4907, #4922, #4928, #4933, #4935).

`hcore_lc_le` is required because the substrate `_of_bound` from
#4935 takes it; both forwarders thread it straight through.

depends-on: #4935

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus-mathlib.md` —
  Mathlib-bridge A2 abstract-bound propagation, public
  `_some_factor_associated_*` wrapper layer, scaled primitive
  (non-monic) flavours.
* **Natural strategy uses Mathlib?** Yes — wrappers forward to
  Mathlib-bridge substrates already in the file.
* **Already in Mathlib?** N/A — bespoke Mathlib-bridge plumbing.
* **Presupposes missing infrastructure?** Only #4935.

## Deliverables

In `HexBerlekampZassenhausMathlib/Basic.lean`, add two abstract-bound
sibling theorems immediately following the originals at lines 13850
and 13894, and rewire the existing originals as thin wrappers.

1. **`scaledRecombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_bound`**
   — same signature as the original at line 13850 except `hprecision`
   is replaced by the four-parameter block above.  Proof body: single
   `obtain`+`exact` against
   `scaledRecombinationSearchModAux_some_and_covers_of_liftedFactorSubsetPartition_of_bound`
   (from #4935), threading `B'`, `hcore_lc_le`, `hvalid`,
   `hprecision`, and all other hypotheses through unchanged.

2. **`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition_of_primitive_pos_lc_core_of_bound`**
   — same signature as the original at line 13894 except `hprecision`
   is replaced by the four-parameter block above.  Proof body: single
   `exact` call to the previous `_of_bound` (deliverable 1), threading
   `B'`, `hcore_lc_le`, `hvalid`, `hprecision`, and all other
   hypotheses through unchanged.

3. **Rewire** the two originals at lines 13850 and 13894 as thin
   wrappers that instantiate `B' := Hex.ZPoly.defaultFactorCoeffBound
   core` and discharge:
   - `hcore_lc_le` via `defaultFactorCoeffBound_valid core hcore_ne
     core (dvd_refl core)` paired with `leadingCoeff_eq_coeff_last`
     (same shape as #4907's / #4920's / #4930's / #4932's / #4935's
     wrapper rewiring).
   - The universal `hvalid` for an arbitrary `g ∣ core` via
     `defaultFactorCoeffBound_valid core hcore_ne g hg_dvd_core` —
     identical discharge shape to #4935's wrapper.

   The originals preserve their existing signatures exactly; the only
   in-tree consumer
   (`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
   at line 13994, which calls the `_of_primitive_pos_lc_core` wrapper)
   continues to type-check unchanged.

No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Context

This issue is the public-wrapper companion to the private substrate
`_of_bound` siblings (#4934 monic, #4935 scaled primitive).  Issue
#4935 explicitly lists this layer as out-of-scope follow-on work
("The `_factor_associated_*` public wrappers at lines 13694 / 13780
— separate follow-on issues").

The monic public wrapper at line 13808
(`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`,
forwarding to the monic substrate from #4934) is intentionally
**not** covered here.  It has no in-tree consumers (verified via
`grep -rn ... --include="*.lean"`), so an `_of_bound` sibling is not
on the critical path to
`exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`.
File it as a separate follow-on issue when (and if) a downstream
consumer needs it.

Files touched: `HexBerlekampZassenhausMathlib/Basic.lean` only.
No new imports, no `axiom`, no `native_decide`, no `sorry`.

## Verification

* `lake build HexBerlekampZassenhausMathlib.Basic` — must complete
  without new errors.  Pre-existing `whnf` heartbeat timeouts on this
  file (documented in `progress/20260518T034405Z_d1e63d44.md` and
  `progress/20260518T035027Z_184d5454.md`) are environment-specific
  and must not increase in count.
* `lake build HexBerlekampZassenhausMathlib` — same pre-existing
  error set, no new errors.
* `python3 scripts/check_dag.py` — exit 0.
* `git diff --check` — exit 0.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
  | grep -E "^\+.*(sorry|axiom|native_decide|TODO|FIXME)"` — no new
  entries.
* Confirm
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  at line 13958 still type-checks unchanged through the rewired thin
  original at line 13894.

## Out of scope

* The monic public wrapper at line 13808
  (`recombinationSearchModAux_some_factor_associated_of_liftedFactorSubsetPartition`)
  — separate follow-on issue, would block on #4934; no in-tree
  consumers yet.
* An `_of_bound` sibling of
  `exhaustiveCoreFactorsWithBound_coverage_of_henselSubsetCorrespondence`
  at line 13958 — separate follow-on, will be unblocked when both
  wrappers in this issue land.
* BHKS Theorem 5.2 / directive #2567.
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
