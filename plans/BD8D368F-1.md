## Current state

The last closed summarize issue is #4622 (thirty-seventh Phase 4/5
checkpoint), which closed at 2026-05-17T09:52:01Z. Since then ~60+ PRs
have merged (commit count: 65 from `git log --since="2026-05-17T09:52:01Z"`),
well above the planner summarize trigger (10+ PRs merged since the last
summarize closed).

Recent landings continue the HO-1 substrate buildout, with three
parallel substrate sub-chains now visible:

* **scaled primitive recombination** (#4647 precursor chain): scaled
  executable + step lemma + exactQuotient bridge (#4720), scaled
  candidate support direction (#4748), scaled candidate cover-at-min
  bridges (#4756), scaled cover-at-min chain theorems (#4762), scaled
  prefix-none in flight (open PR #4765).
* **Hensel-lift integer-side transport** (#4733 chain): Hensel
  uniqueness input package (#4752), lifted subset descent wrapper
  (#4739), subset/complement coprimality mod `p` (#4763),
  `henselLiftData_liftedFactors_size_eq` (#4698),
  `monicModPImage` algebra (#4718), `hensel_unique` via induction
  (#4722), coprime polynomial cancellation with degree bound (#4708).
* **repeatedPart factorPower decomposition** (#4611/#4746 chain):
  repeated-part power decomposition bridge (#4753), repeated-part
  normalized factors cover (#4749), factorPower repeated-part
  expansion wrapper (#4760), factorPower decomposition bridge helpers
  (#4764), rational repeatedPart-divides-squareFreeCore-power
  specialisation (#4719).

In parallel: HexGramSchmidt Nat diagonal sync (#4751, #4754) and
Gram determinant Bareiss bridge (#4732, #4683, #4677, #4653) advance
the HO-43/HO-44 directive surface; HexPoly/HexArith API docs were
polished (#4740, #4742, #4743).

## Deliverables

Produce the thirty-eighth Phase 4/5 checkpoint summary. The summary
issue (this one) should include:

1. **Header** with the PR count, the closing-of-#4622 timestamp,
   and the `coordination orient` snapshot at the time the summarize
   issue is opened (open directives, unclaimed/claimed/blocked
   buckets, open PRs).

2. **Per-theme PR groupings** clustering the merged PRs into
   coherent themes. Expected themes based on the commit log:

   * **HO-1 substrate — scaled primitive recombination chain**:
     #4720 (scaled executable + step + exactQuotient), #4748 (scaled
     candidate support direction), #4756 (scaled candidate cover-at-min
     bridges), #4762 (scaled cover-at-min chain theorems), #4703
     (primitive empty-prefix and degree helpers), #4657 (scaled
     primitive recombination recovery substrate).
   * **HO-1 substrate — Hensel-lift integer-side transport**:
     #4698 (`liftedFactors_size_eq`), #4718 (`monicModPImage` algebra +
     integer-to-modular divisibility bridge), #4722 (`hensel_unique`
     by induction), #4708 (coprime cancellation with degree bound),
     #4734 (mod-`p` subset product bridge), #4744 (lifted subset
     products mod `p`), #4739 (Hensel lifted subset descent wrapper),
     #4752 (package Hensel uniqueness inputs), #4763 (subset/complement
     coprimality of Hensel-lifted factors mod `p`).
   * **HO-1 substrate — repeatedPart factorPower decomposition**:
     #4682 (repeatedPart primitivity bridge), #4705 (rational
     repeatedPart-divides-squareFreeCore-power infrastructure),
     #4719 (specialise to `normalizeForFactor`), #4735 (descend
     repeatedPart divisibility to integers), #4749 (cover repeated-part
     normalized factors), #4753 (bridge repeated-part power
     decomposition), #4760 (expose factorPower repeated-part
     expansion wrapper), #4764 (factorPower decomposition bridge
     helpers).
   * **HO-1 substrate — ModP subset partition assembly**: #4699
     (`factors_irreducible_of_choosePrimeData`), #4702 (UFD subset
     existence + uniqueness for squarefree product divisors), #4750
     (`existsUnique_modPFactorSubset_of_choosePrimeData_of_some`),
     #4757 (compose `modPSubsetPartitionHypotheses_of_choosePrimeData`
     wrapper).
   * **HO-1 substrate — supporting bridges**: #4663 (char-0 gcd ~
     divRadical), #4645 (drop `hB_ne_zero`/`hd_modulus` from
     exhaustive-arm umbrella), #4672 (primitive represented-factor
     package), #4690 (project BHKS bad-vector witness package).
   * **HO-43/HO-44 — GramSchmidt determinant surface**: #4653
     (row-add determinant relocation), #4677 (relocate GramSchmidt
     determinant surface), #4683 (remove GramSchmidt bridge
     `bareiss_eq_det` compatibility calls), #4732 (Gram determinant
     Bareiss bridge), #4751 (HexGramSchmidt Nat diagonal sync),
     #4754 (`GramSchmidt` Nat diagonal sync feature).
   * **Reviews**: #4655 (mark HexBZ scaffolding review complete),
     #4658 (review PR #4653 row-add), #4666 (planned real-roots
     review), #4667 (scaled primitive recombination review),
     #4724/#4725/#4727 (Factorization conformance / API directive /
     HO-43 closure reviews).
   * **Docs / progress**: #4642, #4650, #4664, #4673, #4681, #4685,
     #4689, #4694, #4701, #4713, #4714, #4740, #4741, #4742, #4743.

3. **In-flight at survey time**: list any open PRs / claimed issues
   mid-execution. Likely candidates: #4765 (scaled prefix-none for
   primitive recursive coverage, blocking the next ring of #4647 work).

4. **Current queue frontier**: cite `coordination orient` output —
   open directives, unclaimed/claimed/blocked buckets, open PRs and
   PRs needing attention.

5. **Frontier analysis**: identify which substrate sub-chains are
   close to closing and which dischargers are the immediate
   bottleneck for the HO-1 capstone (#4170). Likely candidates:

   * Scaled primitive recombination chain: #4738 (open PR #4765)
     unblocks #4647, which in turn unblocks #4648, #4639, #4640.
   * Hensel-lift transport chain: #4763 unblocks #4733 capstone,
     which unblocks #4697 (Hensel lift constructor), then #4680
     (analytic obligations composition).
   * repeatedPart factorPower chain: #4759 (assembly) unblocks
     #4747 (quadratic discharger) and #4627 (exhaustive
     discharger), #4604 (quadratic assembly), and #4597 (small-mod
     singleton).

6. **What's still blocked at the directive layer**: #2564 (HO-1
   rewrite), #2567 (BHKS Theorem 5.2), #2637 (Factorization record
   API), #3662 (HO-18 perf report), #3900 (HO-44).

## Context

Routine periodic checkpoint. The 38th summarize issue keeps the
HO-1 substrate landscape navigable and gives the next planner a
single PR-grouped narrative to plan against.

Out of scope: any code or substrate work. This is a documentation
summary issue only.

## Verification

* The new summarize issue body is written and posted.
* The PR count, timestamps, and theme groupings match the actual
  merge log between #4622's close timestamp and the new issue's
  opening time.
* `coordination orient` output is quoted verbatim at survey time.
* No `axiom`, `native_decide`, `TODO`, `FIXME`, or new `sorry`
  is introduced — this is a non-code issue.
