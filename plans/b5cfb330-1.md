## Current state

PR #4608 (commit `a4eb8a8e`) landed `factorFastFactorsWithBound_branch`
in `HexBerlekampZassenhaus/Basic.lean` with a `set_option maxHeartbeats
4000000 in` annotation on the wrapping theorem (line 5410). The closing
review (#4616, session `2160de69`) reverted the bump locally and
confirmed the proof elaborates fine at the file default 800000
heartbeats in ~106s (versus ~105s with the bump). The progress note
that triggered the bump was stale — the proof had been simplified
during a later iteration without removing the override.

The same docstring (lines 5411-5431) refers to the slow-side sibling
`factorSlowFactorsWithBound_branch` as "(line 5043)", but its actual
location after subsequent edits is line 5275.

Both items were explicitly flagged in the review report (#4616) as
non-blocking follow-ups deferred to a separate planner cycle.

## Deliverables

1. Remove the `set_option maxHeartbeats 4000000 in` annotation on
   `HexBerlekampZassenhaus/Basic.lean:5410` (the line directly above the
   `factorFastFactorsWithBound_branch` theorem). Confirm the file
   default of 800000 heartbeats suffices by building
   `HexBerlekampZassenhaus.Basic` cleanly after the removal.

2. In the same theorem's docstring (lines ~5411-5431), update the line
   reference `Mirrors `factorSlowFactorsWithBound_branch` (line 5043)`
   to point to the current location of `factorSlowFactorsWithBound_branch`
   in `HexBerlekampZassenhaus/Basic.lean` (currently line 5275; re-grep
   at PR time in case the file has shifted again).

3. Do not touch any other `set_option maxHeartbeats` overrides in the
   file — three other bumps (lines 5269, 5790, 8553) are unrelated and
   out of scope here.

## Context

- Review report: `progress/20260517T091349Z_2160de69.md` (closes
  #4616). The "deliverable 5" subsection explicitly tested the bump
  removal and recorded the timing measurement.
- Originating PR: #4608 (commit `a4eb8a8e`, "feat:
  factorFastFactorsWithBound dispatch classifier").
- File: `HexBerlekampZassenhaus/Basic.lean`.

## Verification

- `lake build HexBerlekampZassenhaus.Basic` is green after both edits.
- `lake build HexBerlekampZassenhaus` is green.
- `python3 scripts/check_dag.py` is green.
- `git diff --check` is clean.
- `rg -n "maxHeartbeats" HexBerlekampZassenhaus/Basic.lean` shows the
  remaining three bumps but not the one previously at line 5410.
- Sorry inventory delta on `HexBerlekampZassenhaus/Basic.lean`: 0 (the
  existing single sorry remains; no new ones).

## Out of scope

- Auditing or removing the three other `maxHeartbeats` overrides in
  `HexBerlekampZassenhaus/Basic.lean` (lines 5269, 5790, 8553) or
  elsewhere in the project. Those have their own histories; treat as
  load-bearing unless an independent measurement says otherwise.
- Any structural change to `factorFastFactorsWithBound_branch` or
  the docstring beyond the line-reference update.
- The "speculative improvements" path mentioned in the #4616 report
  outside of the two concrete items listed above.
