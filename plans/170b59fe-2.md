## Current state

`python3 scripts/status.py HexPolyFp` reports
`HexPolyFp -> Phase 5 (implementation work loop)` ready, with
`libraries.yml` still at `HexPolyFp.done_through: 4`.

PR #4269 (`HexPolyFp Phase4: refresh profile evidence and promote`)
merged earlier today and bumped `HexPolyFp.done_through: 3 -> 4`. A
banned-marker scan over `HexPolyFp/` and `HexPolyFp.lean` finds no
`sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME` markers. The
root import already includes `HexPolyFp.Conformance`, so the Phase 3
conformance surface is on the library build path.

There are no open `agent-plan` issues currently covering a HexPolyFp
Phase 5 completion audit and status bump.

## Deliverables

1. Re-audit the HexPolyFp Phase 5 exit criteria against
   [PLAN/Phase5.md](PLAN/Phase5.md): zero `sorry` occurrences across
   the library (counted across all declarations, not just top-level
   theorems), green library build on the pinned toolchain, and the
   Phase 3 conformance module still imported from the root.
2. If the audit passes, update `libraries.yml` so
   `HexPolyFp.done_through: 4 -> 5`.
3. If the audit finds a narrow local gap (e.g. a stray `axiom`,
   `native_decide`, `TODO`, or `FIXME` left behind by an upstream PR),
   fix only that HexPolyFp-local gap in the same PR before bumping. If
   it finds a larger or cross-library gap, skip with the concrete
   blocker instead of broadening this issue.
4. Do not weaken or sorry any HexPolyFp declaration. Do not edit
   `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.

## Library placement

Primary file: `libraries.yml`. No Lean source files are expected to
change unless step 3 finds a narrow local gap.

Governing SPEC quote from
[SPEC/Libraries/hex-poly-fp.md](SPEC/Libraries/hex-poly-fp.md):
"`HexPolyFp` specializes the executable dense-polynomial API to
`Hex.ZMod64 p`, exposing `FpPoly p` together with Frobenius-power
computations, square-free decomposition, and modular composition modulo
a monic polynomial."

Governing PLAN quote from [PLAN/Phase5.md](PLAN/Phase5.md): "For library
`hex-foo`, Phase 5 is done when it has zero `sorry` occurrences ...
`lake build` is green ... and the Phase 3 conformance suite for the
library still passes. Record completion by bumping
`libraries.yml[L].done_through` to `5`."

- Q1 SPEC §: `libraries.yml` owns the per-library phase counter;
  `HexPolyFp.lean` and `HexPolyFp/` own the Frobenius-power /
  square-free / modular-composition API named by the SPEC.
- Q2 Mathlib strategy: HexPolyFp is Mathlib-free; no Mathlib bridge
  layer is involved in this audit.
- Q3 Already in Mathlib?: not applicable — this is project metadata.
- Q4 Infrastructure?: none missing — `scripts/status.py HexPolyFp`
  already reports `ready: yes`.

## Context

Read:

- [SPEC/Libraries/hex-poly-fp.md](SPEC/Libraries/hex-poly-fp.md)
- [PLAN/Phase5.md](PLAN/Phase5.md)
- [PLAN/Conventions.md](PLAN/Conventions.md), especially proof
  placeholders and Phase 5 cleanup.
- [SPEC/design-principles.md](SPEC/design-principles.md), the
  proof-placeholder rule.
- [HexPolyFp.lean](HexPolyFp.lean) (imports `Basic`, `Compose`,
  `Enumeration`, `Frobenius`, `SquareFree`, `ModCompose`, `Quotient`,
  `QuotientFrobenius`, `Conformance`).
- [HexPolyFp/Conformance.lean](HexPolyFp/Conformance.lean) for the
  Phase 3 conformance surface.
- The closed reference templates at issues #3449 (HexHensel Phase 5
  audit), #3451 (HexGfqRing Phase 5 audit), and #3432 (HexBerlekamp
  Phase 5 audit) for the canonical shape.
- `libraries.yml` entry for `HexPolyFp`.
- PR #4269 (merged today) for the immediate Phase 4 promotion context.

## Verification

- `rg -n '^\s*sorry\b|^axiom\b|native_decide|TODO|FIXME' HexPolyFp HexPolyFp.lean`
  returns no matches.
- `lake build HexPolyFp`
- `lake build HexPolyFp.Conformance`
- `python3 scripts/status.py HexPolyFp` after the bump reports
  HexPolyFp advanced to Phase 6.
- `python3 scripts/check_dag.py`
- `git diff --check`

## Out of scope

- Adding new conformance fixtures, oracle wiring, or bench targets.
- Phase 6 proof polishing work for HexPolyFp (separate phase, separate
  issue).
- Downstream phase advances for libraries other than HexPolyFp.
- Re-running the Phase 4 promotion (#4269 just landed).
- Editing `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
