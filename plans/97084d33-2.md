## Current state

`HexPolyZ/Basic.lean` (library `HexPolyZ`, `done_through: 5`, so eligible
for Phase 6) develops the integer/rational polynomial layer for `ZPoly`.
The rational-modulus remainder-congruence cluster around lines
~2402–2606 is undocumented. These `rat_mod_*` / `rat_*` lemmas establish
that polynomial remainders are well-defined up to congruence and vanish
under divisibility, over the rational-coefficient view.

The 8 declarations lacking docstrings:

- `rat_canonical_remainder_unique_of_pos_degree` (~2402)
- `rat_mod_remainders_congr_of_congr` (~2493)
- `rat_mod_eq_mod_of_congr_pos_degree` (~2532)
- `rat_eq_of_sub_eq_zero` (~2541)
- `rat_mod_eq_mod_of_congr_not_pos_degree` (~2552)
- `rat_mod_eq_mod_of_congr_core` (~2590)
- `rat_mod_eq_zero_of_dvd_core` (~2597)
- `rat_divMod_remainder_eq_zero_of_not_pos_degree` (~2606)

(Line numbers approximate; locate by name.)

## Deliverables

1. Add a one-to-three-sentence `/-- ... -/` docstring to each of the 8
   declarations above, stating what the lemma proves and its role in the
   rational-polynomial remainder/congruence theory.

## Context

This is a Phase 6 docstring-coverage batch (see `PLAN/Phase6.md` and the
docstring rule in `SPEC/design-principles.md`). Doc-only: do not touch
signatures, statements, proofs, `simp`/`grind` attributes, declaration
order, or `private` modifiers. `HexPolyZ` is a CI-built core library (not
a `*Mathlib` bridge), so a normal `lake build HexPolyZ.Basic` verifies
it.

Other undocumented clusters in this file (e.g. the `normalizePrimitiveSign_*`
lemmas ~1428–1535, the `rat_quotient_derivative_*` square-free lemmas
~2750+, and the `ratPolyPow_*` lemmas ~2823+) are separate future
batches — leave them alone.

## Verification

- `lake build HexPolyZ.Basic` is green.
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` is clean.
- `git diff --numstat -- HexPolyZ/Basic.lean` shows additions only
  (`N 0`).
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` in added
  lines.
