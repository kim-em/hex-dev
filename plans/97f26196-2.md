## Current state

`HexPolyZ/Mignotte.lean` computes the Mignotte / coefficient-norm bounds
used to size integer-polynomial factor searches (coefficient L2-norm,
`mignotteCoeffBound`, `defaultFactorCoeffBound`). Eight **public**
(non-`private`) theorems in this file have no preceding `/-- … -/`
docstring. `HexPolyZ` has not yet had a Phase 6 docstring pass; this
issue covers the Mignotte-bound file. (Its sibling `HexPolyZ/Basic.lean`
is covered by a separate issue; this one touches only `Mignotte.lean`,
so the two can run in parallel without conflict.)

## Deliverables

Add a concise docstring to each of the following 8 public theorems in
`HexPolyZ/Mignotte.lean`. **Docstring-only**: do not change any
statement, signature, attribute, proof body, or import.

- `binom_eq_zero_of_lt`
- `floorSqrt_sq_le`
- `coeffNormSq_eq_sum`
- `coeffL2NormBound_eq_ceilSqrt_coeffNormSq`
- `mignotteCoeffBound_eq`
- `defaultFactorCoeffBound_eq`
- `mignotteCoeffBound_eq_zero_of_lt`
- `coeffL2NormBound_le_defaultFactorCoeffBound`

## Context

- Imitate the tone of neighbouring documented declarations in the same
  file. State **what the theorem says** (the bound or identity it
  establishes), not proof history or use sites.
- Several names describe arithmetic identities (`_eq`) versus
  inequalities (`_le`, `floorSqrt_sq_le`); the docstring must match the
  exact relation and any hypothesis (`_of_lt` guards a degree / size
  precondition). Read each statement before documenting it.
- No banned vocabulary ("smoke", "gate", "bridge"), no em-dashes, no
  process / progress commentary (per `.claude/CLAUDE.md`).
- `private` declarations are intentionally excluded; this issue is only
  the public API.
- Line numbers in the source are approximate; locate by name.

## Verification

- `lake build HexPolyZ` is clean (no new warnings on touched lines).
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` passes; the diff adds only `/-- … -/` lines.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` introduced.
- All 8 named declarations now carry a docstring.
