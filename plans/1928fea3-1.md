## Current state

Seven `_of_bound` theorems in `HexBerlekampZassenhausMathlib/Basic.lean` use the
older parameter name `hcore_lc_bound` for the leading-coefficient bound
hypothesis, while ten siblings — and every local `have`-binding that introduces
the same hypothesis via the post-#4991 helper
`defaultFactorCoeffBound_leadingCoeff_natAbs_le` — use `hcore_lc_le`. The
inconsistency was flagged as an explicit follow-up in the progress note for
PR #5006 (issue #5002):

> Harmonise `hcore_lc_bound` vs `hcore_lc_le` parameter naming on the
> `_of_bound` siblings, where the inconsistency now sits at the signature layer.

The semantic content is identical at all sites: `(Hex.DensePoly.leadingCoeff
core).natAbs ≤ B'` where `B' : Nat` is the abstract bound.

## Per-site breakdown (current line numbers at `origin/main` 3ecc822a)

Total occurrences of the literal token `hcore_lc_bound` in
`HexBerlekampZassenhausMathlib/Basic.lean` (44 occurrences across one file):

* **7 parameter declarations** in `_of_bound` theorem signatures, at lines
  8517, 8670, 9810, 11221, 11393, 11682, 12157.
  - 8517: `natDegree_toPolynomial_scaledRecombinationCandidate_eq_sum_of_bound`
  - 8670: `natDegree_toPolynomial_eq_sum_of_represents_of_primitive_pos_lc_core_of_bound`
  - 9810: `exists_mem_representedSubset_of_degree_cover_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
  - 11221: `mem_T_iff_exists_irreducibleFactor_representingSubset_of_scaledRecombinationCandidate_of_primitive_pos_lc_core_of_bound`
  - 11393: (second sig of the same pair — see surrounding docstring at 11380–11388)
  - 11682: `exists_representingSubset_of_mem_T_of_scaledRecombinationCandidate_dvd_of_primitive_pos_lc_core_of_bound`
  - 12157: `not_represents_empty_of_irreducible_dvd_core_of_primitive_pos_lc_core_of_bound`

* **7 local `have` bindings** at caller sites: lines 8635, 8785, 9951, 11356,
  11621, 11770, 12274, all of the form
  ```lean
  have hcore_lc_bound := defaultFactorCoeffBound_leadingCoeff_natAbs_le hcore_ne
  ```

* **~7 docstring mentions** of the parameter name in the `/-- ... -/` blocks
  preceding the affected theorems (lines 8496, 8504, 8506, 8616, 8643, 8655,
  9797, 9912, 11206, 11318, 11380, 11388, 12148 — total 13 hits in surrounding
  docstrings).

* **Remaining ~17 occurrences** are body uses inside the 7 callee theorem
  bodies plus the 7 caller bodies, each of which references the local
  binding or the parameter by name when threading it to other theorems.

`hcore_lc_bound` does **not** appear in `HexBerlekampZassenhausMathlib/IntReductionMod.lean`
(0 occurrences), nor anywhere outside `Basic.lean`. None of the parameter
declarations are referenced from callers via named-argument syntax
(`hcore_lc_bound := …`); all uses are positional. Renaming is therefore
contained within `Basic.lean`.

## Deliverables

1. Rename every occurrence of the identifier `hcore_lc_bound` to `hcore_lc_le`
   in `HexBerlekampZassenhausMathlib/Basic.lean`. A single `replace_all` Edit
   should suffice: pre-state grep count is 44; post-state grep count for
   `hcore_lc_bound` must be 0, and `hcore_lc_le` count rises by exactly 44
   (from 64 to 108). The rename covers:
   - 7 parameter declarations in the `_of_bound` signatures listed above
   - 7 local `have hcore_lc_bound := …` bindings at caller sites
   - All internal body uses of the renamed parameter / local binding
   - All docstring mentions in surrounding `/-- … -/` blocks
2. No other edits. No structural changes to any theorem body, no signature
   reordering, no docstring rewriting beyond the literal token substitution.
   No new `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.

## Naming

The post-#4991 cluster established `hcore_lc_le` as the convention via the
helper `defaultFactorCoeffBound_leadingCoeff_natAbs_le`, whose output type is
`(lc core).natAbs ≤ defaultFactorCoeffBound core` and whose 17-site local
binding (`have hcore_lc_le := defaultFactorCoeffBound_leadingCoeff_natAbs_le
hcore_ne`) names the value `hcore_lc_le`. The 10 `_of_bound` siblings using
`hcore_lc_le` as a parameter follow the same convention. The 7 sites still
using `hcore_lc_bound` predate #4991 and are the harmonisation targets.

## Verification

* `git grep -c "hcore_lc_bound" HexBerlekampZassenhausMathlib/Basic.lean`
  drops from 44 to 0.
* `git grep -c "hcore_lc_bound" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  remains 0 (was 0).
* `git grep -c "hcore_lc_le" HexBerlekampZassenhausMathlib/Basic.lean`
  rises from 64 to 108 (Δ +44, matching the rename count).
* `lake build HexBerlekampZassenhausMathlib.Basic`: same 16-error baseline
  as `origin/main` 3ecc822a (11 whnf timeouts + 1 cases-failure + 4
  kernel-unknown constants); no new errors and no warnings introduced.
* `lake build HexBerlekampZassenhausMathlib`: same baseline; no new errors.
* `python3 scripts/check_dag.py`: exit 0.
* `git diff --check`: clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/Basic.lean
   | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"`: empty.
* `git diff --stat origin/main`: 1 file changed, +44 / −44 (pure
  textual rename).

## Context

* PR #4991 (`defaultFactorCoeffBound_leadingCoeff_natAbs_le` helper) — established
  `hcore_lc_le` as the canonical name of the bound at 17 local `have`
  bindings.
* Follow-up rewires: #4998 (5 IntReductionMod sites), #4999 (12 Basic.lean
  `_of_bound` thin-wrapper sites), #5006 (7 `hcore_lc_bound` body sites,
  via PR #5002), #5003 (drops unused haves at the 7 #5006 rewire sites).
* PR #5006's worker progress note (commit ab303d05) explicitly flagged
  the surviving `hcore_lc_bound` ↔ `hcore_lc_le` inconsistency at the
  signature layer as a separate-cleanup follow-up — this issue is
  that follow-up.
* The rename is purely textual; no Lean type or proof obligation
  changes. Single-file diff.

## Library placement

* **SPEC §:** `SPEC/Libraries/hex-berlekamp-zassenhaus.md` — Mathlib-bridge
  boilerplate-cleanup / naming-convention harmonisation. No new upstream API.
* **Mathlib use:** yes. Changes confined to
  `HexBerlekampZassenhausMathlib/Basic.lean`.

## Out of scope

* The 13 sites in `IntReductionMod.lean` that use `hcore_lc_le` already
  follow the convention — no changes needed there.
* Renaming the helper itself (`defaultFactorCoeffBound_leadingCoeff_natAbs_le`)
  or its return-type structure.
* Any change to the 10 already-conforming `_of_bound` siblings (they
  already use `hcore_lc_le`).
* Editing `SPEC/`, top-level `PLAN.md`, or `AGENTS.md`.
* Suppressing the 5 unused-`hcore_ne` warnings (a separate sibling
  cleanup, dispatched independently).
