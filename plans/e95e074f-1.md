## Current state

PR #4991 (commit `89ac1f67`) extracted
`Hex.ZPoly.defaultFactorCoeffBound_leadingCoeff_natAbs_le` in
`HexBerlekampZassenhausMathlib/Basic.lean:139` and rewired the 17
`hcore_lc_le` discharge sites across `Basic.lean` (12 sites) and
`IntReductionMod.lean` (5 sites) to a single-line call.

The issue body of #4987 explicitly deferred a follow-up cleanup:

> Per the issue's explicit guidance, did not remove now-unused
> `hcore_dvd_self` / `hcore_size_pos` `have`s preceding the rewired
> blocks ("a follow-up cleanup can address that separately"); they
> remain in place at sites where they are no longer consumed.

At the 5 rewired sites in
`HexBerlekampZassenhausMathlib/IntReductionMod.lean` (lines 3446, 3728,
4019, 4263, 4388 — the lines of `have hcore_lc_le :=
defaultFactorCoeffBound_leadingCoeff_natAbs_le hcore_ne`), the
immediately preceding `have hcore_dvd_self : … ∣ …` and (at 4 of 5
sites) `have hcore_size_pos : 0 < … .size := …` bindings are no longer
referenced by the `exact` that follows: the rewired call passes
`hcore_lc_le` plus `defaultFactorCoeffBound_valid …` directly, with no
mention of `hcore_dvd_self` or `hcore_size_pos`.

Per-site breakdown:

* **Site 3446** (`factor_exhaustive_branch_entry_irreducible_of_choosePrimeData_aux`
  thin wrapper):
  preceding block has only `hcore_dvd_self` (no local `hcore_size_pos`
  — the long `hcore_ne` derivation at this site is structured
  differently). 1 `have` to delete (~4 lines).
* **Sites 3728, 4019, 4263, 4388**: each has both
  `have hcore_size_pos : 0 < (Hex.normalizeForFactor f).squareFreeCore.size :=
    Hex.ZPoly.size_pos_of_ne_zero _ hcore_ne`
  (2 lines) and
  `have hcore_dvd_self : (Hex.normalizeForFactor f).squareFreeCore ∣
    (Hex.normalizeForFactor f).squareFreeCore := ⟨…⟩`
  (4 lines). 2 `have`s to delete per site (~6 lines each).

Total mechanical deletion: ~28 lines across one file.

Out of scope for this issue: the 12 analogous Basic.lean sites (file
`HexBerlekampZassenhausMathlib/Basic.lean` at the call sites grepped
via `defaultFactorCoeffBound_leadingCoeff_natAbs_le hcore_ne`). The
Basic.lean cleanup is intentionally split into a separate issue
because the sites have more variable shape (some use `core ∣ core`
with a different elaboration), and keeping the two files independent
makes review and rollback cleaner.

## Deliverables

In `HexBerlekampZassenhausMathlib/IntReductionMod.lean`, at each of
the 5 thin-wrapper rewire sites, delete the now-unused
`have hcore_size_pos` and `have hcore_dvd_self` bindings that
immediately precede `have hcore_lc_le := defaultFactorCoeffBound_leadingCoeff_natAbs_le hcore_ne`:

1. **Site at line 3446** (current numbering): delete the 4-line
   `have hcore_dvd_self : (Hex.normalizeForFactor f).squareFreeCore ∣
     (Hex.normalizeForFactor f).squareFreeCore :=
   ⟨(1 : Hex.ZPoly), (Hex.DensePoly.mul_one_right_poly _).symm⟩` block.
   (No local `hcore_size_pos` at this site.)

2. **Site at line 3728**: delete the 2-line `have hcore_size_pos` and
   the 4-line `have hcore_dvd_self` immediately preceding the
   `have hcore_lc_le` line.

3. **Site at line 4019**: same pattern — delete the 2-line
   `have hcore_size_pos` and the 4-line `have hcore_dvd_self`.

4. **Site at line 4263**: same pattern.

5. **Site at line 4388**: same pattern.

No other edits. No proof-body changes. No signature changes. No
docstring changes. No new helpers.

## Context

`defaultFactorCoeffBound_leadingCoeff_natAbs_le` (Basic.lean:139)
internally constructs the `g ∣ core` divisibility witness and the
`0 < core.size` positivity witness it needs from its single
`(hf : core ≠ 0)` premise. Consequently the outer-scope copies of
`hcore_dvd_self` and `hcore_size_pos` that the pre-#4991 proof body
threaded into the chain are not consumed by anything that follows
the rewired one-line call.

Inspection of each `exact` body (grep for `hcore_dvd_self` and
`hcore_size_pos` after the rewire line, within the proof's
indentation) confirms no downstream reference at any of the 5 sites.

This cleanup matches the precedent of similar follow-ups elsewhere
in `HexBerlekampZassenhausMathlib` where boilerplate-packaging
helpers were extracted (e.g. `defaultFactorCoeffBound_pos_of_ne_zero`
from #4637).

## Verification

* `git grep -n "have hcore_size_pos\b" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  returns at most one entry per pre-existing internal positivity
  witness (none of the rewired thin-wrappers in #4991 should remain).
* `git grep -n "have hcore_dvd_self\b" HexBerlekampZassenhausMathlib/IntReductionMod.lean`
  returns 0 entries after this PR (all 5 instances were inserted by
  the wrapper rewiring sites).
* `lake build HexBerlekampZassenhausMathlib.IntReductionMod` matches
  the `origin/main` baseline at session-claim time (currently 18
  errors / 39 warnings, all pre-existing in `Basic.lean`). The
  per-site `unused variable` linter warnings that the current state
  emits for `hcore_size_pos` and `hcore_dvd_self` at the 5 rewired
  sites should be eliminated.
* `lake build HexBerlekampZassenhausMathlib` matches the `origin/main`
  baseline.
* `python3 scripts/check_dag.py` exits 0.
* `git diff --check` clean.
* `git diff origin/main -- HexBerlekampZassenhausMathlib/IntReductionMod.lean
  | grep -E "^\+.*\b(sorry|axiom|native_decide|TODO|FIXME)\b"` empty.
* `git diff --stat origin/main` shows a net negative line change of
  ~25–30 lines on the single touched file.

## Out of scope

* The 12 analogous sites in
  `HexBerlekampZassenhausMathlib/Basic.lean`. A separate planner cycle
  can issue that cleanup once this lands and the deletion template is
  validated on the smaller surface area.
* Docstring or comment edits.
* Any change to `defaultFactorCoeffBound_leadingCoeff_natAbs_le` itself
  or to `defaultFactorCoeffBound_valid`.
* Removal of `hcore_size_pos` / `hcore_dvd_self` `have`s anywhere they
  are still consumed downstream (the verification step's grep counts
  catch this).
