## Current state

`HexPolyZ/Basic.lean` is the integer-polynomial (`ZPoly`) core: rational
conversion, congruence, content / primitive-part, leading-coefficient
sign normalisation, and the primitive square-free decomposition. Forty
**public** (non-`private`) declarations in this file have no preceding
`/-- … -/` docstring. The Phase 6 docstring sweep has covered HexArith,
HexLLL, HexGramSchmidt, HexMatrix, HexPoly, and HexGF2; `HexPolyZ` has
not yet had a Phase 6 docstring pass, so this is the gap for the
`ZPoly` core.

## Deliverables

Add a concise docstring to each of the following 40 public theorems in
`HexPolyZ/Basic.lean`. This is **docstring-only**: do not change any
statement, signature, attribute, proof body, or import. Group the work
into the three natural clusters below.

1. **Conversion / congruence / content (L68–L901):**
   `coeff_dilate`, `isUnit_iff`, `coeff_toRatPoly`, `toRatPoly_scale_int`,
   `size_toRatPoly`, `toRatPoly_ne_zero_of_ne_zero`, `toRatPoly_mul`,
   `congr_refl`, `congr_symm`, `congr_trans`, `congr_add`, `congr_mul`,
   `content_mul_primitivePart`, `content_dvd_coeff`,
   `dvd_content_of_nat_dvd_coeff`, `natCast_dvd_content_of_dvd_coeff`,
   `primitivePart_primitive`, `primitivePart_eq_self_of_primitive`,
   `primitive_mul`, `leadingCoeff_mul_of_nonzero`,
   `leadingCoeff_mul_pos_of_pos`.

2. **Leading-coefficient sign / rational primitive part (L1317–L2189):**
   `primitiveSquareFreeDecomposition_primitive`,
   `leadingCoeff_normalizePrimitiveSign_nonneg`,
   `leadingCoeff_ne_zero_of_ne_zero`,
   `leadingCoeff_normalizePrimitiveSign_pos_of_ne_zero`,
   `leadingCoeff_ratPolyPrimitivePart_nonneg`,
   `leadingCoeff_ratPolyPrimitivePart_pos_of_ne_zero`,
   `ratPolyPrimitivePart_primitive`,
   `ratPolyPrimitivePart_rational_associate`,
   `rat_size_le_of_dvd_nonzero`.

3. **Square-free decomposition (L2933–L3565):**
   `ne_zero_of_primitive`,
   `primitiveSquareFreeDecomposition_reassembly_over_rat`,
   `primitiveSquareFreeDecomposition_squareFreeCore`,
   `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_primitive`,
   `primitiveSquareFreeDecomposition_squareFreeCore_eq_one_of_degree_zero`,
   `leadingCoeff_squareFreeCore_nonneg`,
   `leadingCoeff_repeatedPart_nonneg`,
   `primitiveSquareFreeDecomposition_squareFreeCore_repeatedPart_leadingCoeff_pos`,
   `primitiveSquareFreeDecomposition_reassembly_signed`,
   `coprimeModP_of_bezout`.

## Context

- Imitate the tone of neighbouring documented declarations in the same
  file. Describe **what the theorem says** (the mathematical content of
  the statement), not its proof history or where it is used.
- No banned vocabulary ("smoke", "gate", "bridge"), no em-dashes, no
  process / progress commentary (per `.claude/CLAUDE.md`).
- Read each statement before documenting it; several names are similar
  (`squareFreeCore` vs `repeatedPart`, `nonneg` vs `pos_of_ne_zero`) so
  the docstring must match the exact hypotheses and conclusion.
- `private` declarations in this file are intentionally excluded; this
  issue is only the public API.
- Line numbers above are approximate (current `main`); locate by name.

## Verification

- `lake build HexPolyZ` is clean (no new warnings on touched lines).
- `python3 scripts/check_dag.py` exits 0.
- `git diff --check` passes; the diff adds only `/-- … -/` lines.
- No `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` introduced.
- All 40 named declarations now carry a docstring.
