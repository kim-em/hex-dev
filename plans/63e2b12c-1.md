## Current state

`scripts/status.py` reports `HexPoly -> Phase 6 (proof polishing)`. Two
HexPoly review issues have already landed under Phase 6:

- `#4397` — DensePoly core API (closed via `#4402`).
- `#4408` — executable division and GCD surface in `HexPoly/Euclid.lean`
  (in flight as PR `#4414`).

The next remaining executable HexPoly surface that has not received a
Phase 6 polish pass is `HexPoly/Operations.lean` (~383 lines), which
exposes the `DensePoly` arithmetic API: `scale`, `shift`, `add`, `sub`,
`neg`, `mul` (with helpers `mulCoeffStep`, `mulCoeffSum`), `eval`,
`compose`, `derivative`, plus the existing characterising lemmas:

```lean
DensePoly.coeff_scale
DensePoly.coeff_shift
DensePoly.coeff_shift_scale
DensePoly.coeff_mul
DensePoly.coeff_add
DensePoly.coeff_sub
DensePoly.coeff_neg
DensePoly.eval_zero
DensePoly.eval_C
DensePoly.derivative_zero
```

Only one `@[simp]` attribute is currently attached in the file; many of
the `coeff_*` lemmas above are plausibly safe simp normal forms in the
same style as the characterising lemmas added under `#4397` /  `#4402`.

## Deliverables

1. Review `HexPoly/Operations.lean` against the HexPoly SPEC and the
   Phase 6 API bar set by `#4397` / `#4402` on `HexPoly/Dense.lean`.
   For each public definition (`scale`, `shift`, `add`, `sub`, `neg`,
   `mul`, `eval`, `compose`, `derivative`), confirm there is a stable
   characterising `coeff_*` / evaluation lemma a downstream caller can
   use without unfolding the definition.
2. Add narrow characterising lemmas, docstrings, and `@[simp]`
   annotations only where the gap is real and the simp normal form is
   consistent with the existing Dense and Euclid surface. Run a
   second-opinion check before promoting any non-trivial `[simp]`
   rewrite — see the cautionary note on `coeff_monomial` in the
   progress file `progress/20260516T045815Z_25d0c04d.md`.
3. Surface only — do not migrate proofs from other modules, do not
   touch `HexPoly/Euclid.lean` (covered by `#4408`), and do not bump
   `libraries.yml`. This is a focused API review, not a Phase 6 exit
   audit.

## Library placement

- `HexPoly/Operations.lean` — surface under review.
- Allowed downstream spot-builds: `HexPoly`, `HexPolyZ`, `HexPolyFp`
  (immediate consumers). Do not modify these modules unless the
  spot-build fails on a newly promoted simp lemma; in that case prefer
  reverting the simp promotion over editing the consumer.

## Verification

- `lake build HexPoly.Operations` — clean.
- `lake build HexPoly` — clean.
- `lake build HexPolyZ` — clean (immediate consumer).
- `python3 scripts/check_dag.py` — exit 0.
- `python3 scripts/status.py HexPoly` — unchanged (`done_through: 5`).
- `git diff --check` — clean.
- `rg -n '^\s*axiom\b|\bnative_decide\b|TODO|FIXME|\bsorry\b' HexPoly/Operations.lean`
  — no matches.
- No `axiom` introduction (per repo `.claude/CLAUDE.md`).
